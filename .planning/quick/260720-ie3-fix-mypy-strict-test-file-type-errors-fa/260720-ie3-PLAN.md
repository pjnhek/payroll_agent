---
quick_id: 260720-ie3
status: planned
description: Fix the 19 `uv run mypy` (strict) errors in tests/ that fail the ci workflow's "Type check (mypy --strict)" job on master — type-annotation / narrowing changes only, no test-behavior changes
---

# Quick Task 260720-ie3 Plan

## Context

CI runs bare `uv run mypy` (config: `files = ["app", "eval", "scripts", "tests"]`, `strict = true`; the `tests.*` override relaxes only the untyped-def rules and keeps `check_untyped_defs = true`). Production code is clean; only `tests/` has 19 errors, all mechanical strict-mode type hygiene. The exact current errors (verbatim from `uv run mypy`):

- `tests/test_queue_durability.py` — 7 × `"object" has no attribute "id"/"lease_token"/"attempts"` at lines 2492, 2493, 2507, 2508, 2627, 2628, 2633.
- `tests/test_pump_workflow.py` — 3 × `Missing type arguments for generic type "dict"` at 141, 150, 160.
- `tests/test_queue_config.py` — 2 × `Missing type arguments for generic type "dict"` at 179, 184, + 1 × `Returning Any from function declared to return "list[dict[Any, Any]]"` at 181.
- `tests/test_demo_landing.py` — 3 × `Value of type "object" is not indexable` at 1297, 1385, 1386.
- `tests/test_ops_route.py` — 2 × `Call to untyped function "_flatten_routes" in typed context` at 87, 127.
- `tests/test_proof_mutation_targets.py` — 1 × `Incompatible types in assignment (expression has type "expr | None", variable has type "expr")` at 220.

**HARD CONSTRAINTS (all tasks):**
- Type-annotation / narrowing changes ONLY. Do NOT change any assertion, parametrization, fixture value, control flow, or runtime logic. Every code path that ran before must run identically after — the diff is annotations, `cast(...)` (a runtime no-op), and generic type-args, nothing else.
- No new `assert` statements are required or wanted here: each `object`-typed read already has an adjacent `is not None` check or `if ... is not None:` branch, so a `cast` to the real optional type lets the EXISTING check do the narrowing. Prefer a plain annotation / `cast` over adding an `isinstance`.
- uv-only project: verification is `uv run mypy` and `env -u DATABASE_URL uv run pytest -q`. Never pip/venv/`.venv/bin/python`.
- Match each file's existing import style. Add `Any` / `cast` to an existing `from typing import ...` line where one exists; otherwise add a new `from typing import ...` line alongside the file's other stdlib imports (all six files already have `from __future__ import annotations`).
- Executor runs WITHOUT worktree isolation (it needs the project `.venv`). Do NOT commit — the orchestrator handles the commit. Do NOT touch any `.md`/doc file.

## Task 1: Annotate / narrow the 19 strict-mode type errors across the 6 test files

**Files:** `tests/test_queue_durability.py`, `tests/test_pump_workflow.py`, `tests/test_queue_config.py`, `tests/test_demo_landing.py`, `tests/test_ops_route.py`, `tests/test_proof_mutation_targets.py`

**Action:** Apply exactly the following edits. Line numbers are current-master anchors; if a line has drifted, locate the named construct rather than trusting the number.

1. `tests/test_queue_durability.py` (7 errors). The file has `from typing import Any` and `from app.models.job import Job`; `repo.claim_job(...)` returns `Job | None`, but it is stored into `results: dict[str, object]` / `outcomes: dict[str, object]` (both correctly stay `object`-valued because they also hold bools/None), so the read must be re-narrowed. (a) Add `cast` to the typing import so it reads `from typing import Any, cast`. (b) At line 2490 change `reclaimed = results["reclaimed"]` to `reclaimed = cast("Job | None", results["reclaimed"])` — the existing `assert reclaimed is not None` on the next line then narrows it to `Job` for the `.id`/`.lease_token`/`.attempts` reads at 2492/2493/2507/2508. (c) At line 2620 change `reclaimed = outcomes["reclaim"]` to `reclaimed = cast("Job | None", outcomes["reclaim"])` — the existing `if reclaimed is not None:` branch then narrows it to `Job` for the reads at 2627/2628/2633. Leave `zombie_completed = outcomes["zombie"]` untouched (it is only compared with `is False`/`is True`, no attribute access, no error).

2. `tests/test_pump_workflow.py` (3 errors). The file has no `typing` import; the three `list[dict]` annotations are over `yaml.safe_load(...)` step mappings. (a) Add a `from typing import Any` line alongside the existing imports (`import pathlib`, `import yaml`). (b) Line 141: `def _steps() -> list[dict]:` → `-> list[dict[str, Any]]:`. (c) Line 150: `def _drain_index(cls, steps: list[dict]) -> int:` → `steps: list[dict[str, Any]]`. (d) Line 160: `def _alarm_index(cls, steps: list[dict]) -> int:` → `steps: list[dict[str, Any]]`.

3. `tests/test_queue_config.py` (3 errors). The file has no `typing` import. (a) Add a `from typing import Any, cast` line alongside the existing imports (`import pathlib`, `import pytest`, `import yaml`). (b) Line 179: `def _workflow_steps() -> list[dict]:` → `-> list[dict[str, Any]]:`. (c) Line 181: the return value comes off a `yaml.safe_load` (`Any`) subscript chain, so annotating the signature alone leaves the no-any-return; wrap it — change `return list(parsed["jobs"].values())[0]["steps"]` to `return cast("list[dict[str, Any]]", list(parsed["jobs"].values())[0]["steps"])`. This clears both the 181 no-any-return and gives the function a concrete return type. (d) Line 184: `def _proof_running_steps(steps: list[dict]) -> list[dict]:` → both the param and the return become `list[dict[str, Any]]` (its comprehension over the now-typed `steps` yields `dict[str, Any]`, so no cast is needed there).

4. `tests/test_demo_landing.py` (3 errors). `Any` is already imported. Two separate test functions build a heterogeneous `run = { ... }` dict literal that mypy infers as `dict[str, object]`, making the nested `run["decision"]["resolutions"][0][...] = ...` subscript-assignments fail. Annotate each literal as `dict[str, Any]`: (a) at line 1226 change `run = {` to `run: dict[str, Any] = {` (this is the function whose failing line is 1297); (b) at line 1312 change `run = {` to `run: dict[str, Any] = {` (the function whose failing lines are 1385/1386). Do NOT annotate the other `run = {` literals at 557 / 1165 — they do not error and must stay untouched.

5. `tests/test_ops_route.py` (2 errors). The recursive generator `_flatten_routes` at line 80 is fully unannotated, so both its recursive self-call (87) and the typed-context call (127) trip no-untyped-call. The file has no `typing`/`collections.abc` import. (a) Add `from collections.abc import Iterable, Iterator` and `from typing import Any` alongside the existing imports. (b) Change the signature `def _flatten_routes(routes):` to `def _flatten_routes(routes: Iterable[Any]) -> Iterator[Any]:`. Use `Any` as the element type deliberately — the downstream comprehensions at 128/133 do bare-attribute access (`r.path`) on the yielded items, so a narrower element type (e.g. Starlette `BaseRoute`, which has no `.path`) would introduce NEW attr-defined errors and would require editing those assertions, which the constraints forbid. `Iterable[Any]`/`Iterator[Any]` makes the function typed (clearing no-untyped-call) while leaving `all_routes` as `list[Any]` so nothing downstream changes.

6. `tests/test_proof_mutation_targets.py` (1 error). `import ast` is present. Inside `_resolve_assignment`, `value` is first bound in the `ast.Assign` branch from `node.value` (type `ast.expr`), then re-bound in the `ast.AnnAssign` branch from `node.value` (type `ast.expr | None`), which is where the assignment-incompatibility fires at line 220; the subsequent `if value is None: continue` at 223 already handles the None. Widen the declared type at the first binding: change line 217 `value = node.value` (the line inside `if isinstance(node, ast.Assign):`) to `value: ast.expr | None = node.value`. This makes line 220 compatible and lets the existing `if value is None` narrow `value` back to `ast.expr` for the downstream `isinstance(value, ...)` use — no logic change.

**Verify:**
- BEFORE editing, capture the hermetic baseline so a regression is detectable: `env -u DATABASE_URL uv run pytest -q 2>&1 | tail -3` (expect roughly `~1303 passed, ~105 skipped`; record the exact numbers).
- `uv run mypy` prints `Success: no issues found in 170 source files` (0 errors). This is the primary automated gate.
- `env -u DATABASE_URL uv run pytest -q 2>&1 | tail -3` shows the SAME passed/skipped counts as the captured baseline — zero new failures, zero newly-skipped tests, no collection errors.
- `git diff --stat` shows only the 6 test files changed; `git diff` contains only added/edited type annotations, `cast(...)` calls, and generic type-args — no changed assertions, values, or control flow.

**Done:** `uv run mypy` reports 0 errors ("Success: no issues found in 170 source files"); the hermetic suite (`env -u DATABASE_URL uv run pytest -q`) is green with no new failures or skips vs the captured baseline; and the diff is annotations/narrowing only (no assertion or behavior change) across exactly the 6 named test files.

## Success Criteria

- The ci workflow's "Type check (mypy --strict)" job would pass on master (locally proven via `uv run mypy` → 0 errors).
- No test behavior changed: baseline hermetic pass/skip counts are unchanged.
- Changes confined to the 6 test files; no production code, no docs, no committed artifacts (orchestrator commits).
