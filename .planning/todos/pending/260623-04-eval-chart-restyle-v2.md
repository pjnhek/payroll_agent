---
id: 260623-04
created: 2026-06-23
source: Phase 05 UAT — eval page look
resolves_phase:
priority: low
---

# Eval chart looks "matplotlib / data-science" — restyle to v2

The eval page CARDS + metric strip were restyled to the UI-SPEC (UAT #5) and look
consistent with the dashboard. But the CHART itself (`/eval/chart.svg`, committed,
generated by `eval/run_eval.py` via matplotlib) still reads as a raw DS plot — default
matplotlib styling, not aligned with the dashboard palette.

v2 options (pick one):
- Restyle the matplotlib output in run_eval.py: dashboard palette (#1E3A5F / #6B7280),
  remove chart junk (top/right spines, heavy gridlines), clean sans-serif, tighter
  labels. Regenerate + recommit eval/chart.svg.
- OR replace the SVG with an inline HTML/CSS bar chart in eval.html (no image, no
  matplotlib dependency at serve time) — fully on-brand, scales with the page. Heavier
  template work but removes the "embedded plot" feel entirely.

Not a blocker — the metrics + numbers are correct and legible; this is pure aesthetics.
The eval CHART is the one place the dashboard still looks like a notebook.
