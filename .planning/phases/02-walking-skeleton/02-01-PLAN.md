---
phase: 02-walking-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .env.example
  - app/config.py
  - app/db/schema.sql
  - app/db/repo.py
  - app/llm/__init__.py
  - app/llm/client.py
  - app/llm/prompts/__init__.py
  - app/email/__init__.py
  - app/email/gateway.py
  - app/pipeline/__init__.py
  - tests/conftest.py
  - tests/test_llm_client.py
  - tests/test_status_drift.py
autonomous: true
requirements: [LLM-01, LLM-02, EMAIL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "The pinned openai/fastapi/uvicorn/python-multipart deps are installed in .venv and importable (LLM-01 foundation)."
    - "One OpenAI-compatible client wrapper routes per tier (extraction/decision/draft) by swapping base_url/model/api_key from Settings, sets temperature=0 + json_object, selects DeepSeek non-thinking, and does ONE reflective retry on ValidationError/empty content (LLM-01, LLM-02, D-A2-02, D-A2-03)."
    - "The stub email gateway records every outbound message with a synthetic RFC-shaped Message-ID in email_messages and exposes parse_inbound/send_outbound behind one interface (EMAIL-01, D-A4-02 forward-compat)."
    - "A live-LLM opt-in flag exists in Settings + .env.example mirroring the live-DB two-factor pattern, and a live_llm pytest marker is registered (D-A2-01)."
    - "payroll_runs carries a reconciliation JSONB column AND an error_reason TEXT column applied via bootstrap, and the status-drift guard stays green (D-A3-05 option a; D-A1-03 error persistence home)."
    - "app/db/repo.py commits the FULL repo surface every later wave imports — sender lookup, run/source-email loading, outbound Message-ID retrieval via email_messages, both header-chain lookups (awaiting_reply-only resume + any-status late-reply), and error-reason persistence — so Plans 02/03/04 never discover a missing helper mid-wave (review FIX 9)."
    - "set_status is the SOLE writer of payroll_runs.status; the one documented exception is record_run_error, a deliberate thin wrapper that writes error_reason AND routes its ERROR transition THROUGH set_status (so there is exactly one status-write path); the data-persistence helpers (persist_decision/persist_extracted/persist_reconciliation/replace_line_items) write data columns only and never touch status — persist_decision takes NO final_status argument (review FIX B)."
    - "load_source_email returns the ORIGINAL CLEANED inbound body for a run, because insert_inbound_email persists the already-cleaned body (post clean_body()) into email_messages.body_text at ingest — the cleaned body is the single source of truth and is NOT re-cleaned on read (review FIX C; the Plan 04 resume re-extraction context)."
  artifacts:
    - path: "app/llm/client.py"
      provides: "call_structured(tier, messages, response_model) + a draft/free-text call path; per-tier routing; JSON mode; reflective retry; DeepSeek non-thinking toggle"
      min_lines: 60
    - path: "app/email/gateway.py"
      provides: "EmailGateway interface: parse_inbound -> InboundEmail, send_outbound -> synthetic Message-ID + outbound email_messages row"
      min_lines: 30
    - path: "app/db/repo.py"
      provides: "FULL repo surface: run/email/line-item accessors + sole status mutator (set_status) + record_run_error wrapper (writes error_reason, routes ERROR through set_status) + data-only JSONB persistence (persist_decision takes NO final_status) + sender lookup + run/source-email load (cleaned body) + outbound Message-ID lookup + awaiting_reply-only AND any-status header-chain lookups + error-reason persistence"
      min_lines: 90
    - path: "app/db/schema.sql"
      provides: "reconciliation JSONB column + error_reason TEXT column on payroll_runs"
      contains: "error_reason"
    - path: "tests/conftest.py"
      provides: "mocked-LLM client factory, committed InboundEmail fixtures loader, Roster-from-seed builder"
      min_lines: 30
  key_links:
    - from: "app/llm/client.py"
      to: "app/config.py Settings"
      via: "get_settings() per-tier resolution"
      pattern: "get_settings"
    - from: "app/email/gateway.py"
      to: "app/db/repo.py"
      via: "insert outbound email_messages row + return synthetic Message-ID"
      pattern: "email_messages"
    - from: "app/db/repo.py"
      to: "email_messages(direction='outbound', run_id)"
      via: "outbound clarification Message-ID is anchored on the linked email_messages row, NOT a payroll_runs column"
      pattern: "direction.*outbound"
    - from: "app/db/repo.py record_run_error"
      to: "app/db/repo.py set_status"
      via: "record_run_error routes its ERROR transition through set_status (single status-write path, FIX B)"
      pattern: "set_status"
---

<objective>
Lay the judgment-spine substrate every Phase 2 slice consumes: install the pinned runtime deps, build the ONE OpenAI-compatible LLM client wrapper (per-tier routing, JSON mode, reflective retry, DeepSeek non-thinking toggle), the stub email gateway behind one interface, and the DB repo layer (the FULL accessor surface later waves need — run/email/line-item accessors, the sole status mutator `set_status`, the `record_run_error` wrapper, data-only JSONB persistence, sender lookup, run/source-email load returning the cleaned body, outbound Message-ID lookup, both header-chain lookups, and error-reason persistence). Add the `reconciliation` JSONB column AND the `error_reason` TEXT column, the live-LLM opt-in flag, and the `live_llm` marker.

This is interface-first: it defines the contracts (the client call surface, the gateway functions, the FULL repo accessor set) that Plans 02/03/04 import without re-exploring the codebase and without discovering a missing helper mid-wave. It calls no real model in the test suite — everything is mocked.

Purpose: Without this substrate, every later slice would re-derive the LLM call shape and DB write discipline. Building it once, correctly — with the COMPLETE repo surface named here so no later wave has to invent a helper, with ONE status-write path (`set_status`, plus the documented `record_run_error` wrapper), and with the cleaned body persisted at ingest so the resume re-extraction reads clean text — keeps the gate-block thesis (Plan 02/03) and resume loop (Plan 04) pure glue over a stable seam.
Output: `app/llm/client.py`, `app/email/gateway.py`, `app/db/repo.py` (full surface), schema (reconciliation + error_reason) + config + marker changes, mocked client-wrapper tests, and the shared `conftest.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-walking-skeleton/02-CONTEXT.md
@.planning/phases/02-walking-skeleton/02-RESEARCH.md
@.planning/phases/02-walking-skeleton/02-AI-SPEC.md
@.planning/phases/02-walking-skeleton/02-PATTERNS.md
@.planning/phases/02-walking-skeleton/02-VALIDATION.md
@app/config.py
@app/db/supabase.py
@app/db/schema.sql
@app/models/contracts.py
@app/models/roster.py
@app/models/status.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Activate deps, register live_llm marker, add live-LLM flag + reconciliation + error_reason columns, apply schema</name>
  <read_first>
    - requirements.txt (pinned deps: openai==2.43.0, fastapi==0.138.0, uvicorn[standard]==0.49.0, python-multipart==0.0.20 — already declared, ALREADY PINNED; Wave 0 only `pip install`s it, does NOT edit it, so requirements.txt is intentionally NOT in files_modified per review FIX 11)
    - pyproject.toml lines 12-16 (existing `integration` marker — mirror its exact form for `live_llm`)
    - app/config.py lines 16-48 (Settings; SettingsConfigDict(env_file=".env", extra="ignore") at line 48 — a new bool field with a default just works)
    - .env.example line 11 (TAX_YEAR=2026 — append the new flag below it, flat KEY=value style)
    - app/db/schema.sql lines 79-80 (extracted_data/decision JSONB inline-comment style to mirror for the new reconciliation column) + lines 61-85 (payroll_runs has NO error_reason column today — this task adds it)
    - app/db/bootstrap.py (the idempotent apply path; CREATE TABLE IF NOT EXISTS does NOT add a column to an existing table — needs ALTER TABLE ... ADD COLUMN IF NOT EXISTS or a --reset)
    - tests/test_status_drift.py lines 34-86 (drift guard parses CHECK (col IN (...)) value sets ONLY; a new JSONB/TEXT column is not in any CHECK value set, so NO drift-test change is needed — confirm it stays green)
    - PATTERNS.md §`app/db/schema.sql` (the reconciliation-column inline-comment form + ALTER TABLE idempotency note)
  </read_first>
  <action>
    Run `.venv/bin/python -m pip install -r requirements.txt` to activate the pinned openai/fastapi/uvicorn/python-multipart (per VALIDATION.md Wave 0). Do NOT edit requirements.txt — the deps are already pinned from Phase 1; Wave 0 only installs them (review FIX 11). Add a `live_llm` marker to pyproject.toml `[tool.pytest.ini_options].markers` mirroring the existing `integration` marker line verbatim (per D-A2-01). Add a `allow_live_llm: bool = False` field to `Settings` in app/config.py (name is Claude's discretion mirroring the live-DB `ALLOW_DB_RESET` two-factor pattern per D-A2-01) and append the corresponding `ALLOW_LIVE_LLM=0` line to .env.example below TAX_YEAR. Add TWO columns to `payroll_runs` in app/db/schema.sql following the exact inline-comment + style of the existing extracted_data/decision lines: (1) `reconciliation JSONB` (per D-A3-05 option a — the persistence home for the per-run `list[NameMatchResult]`), and (2) `error_reason TEXT` (per D-A1-03 / review FIX 7 — the persistence home for the orchestrator's ERROR reason; `payroll_runs` has no such column today). Guard BOTH with idempotent `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS reconciliation JSONB` and `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS error_reason TEXT` blocks so re-applying never errors. Re-apply via `.venv/bin/python -m app.db.bootstrap` if DATABASE_URL is set, otherwise note the ALTER blocks apply on next bootstrap. Confirm tests/test_status_drift.py stays green (neither a JSONB nor a TEXT column is in any CHECK value set, so no drift-test edit is needed — the guard only parses the status CHECK set).
  </action>
  <verify>
    <automated>.venv/bin/python -c "import openai, fastapi, uvicorn, multipart; print('deps ok')" && .venv/bin/python -m pytest tests/test_status_drift.py -x -q && grep -q "live_llm" pyproject.toml && grep -q "reconciliation" app/db/schema.sql && grep -q "error_reason" app/db/schema.sql && grep -q "ALLOW_LIVE_LLM" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `import openai, fastapi, uvicorn, multipart` succeeds in .venv.
    - `pyproject.toml` contains a `live_llm` marker line and `.env.example` contains `ALLOW_LIVE_LLM`.
    - `app/db/schema.sql` contains `reconciliation` JSONB AND `error_reason` TEXT on payroll_runs, each guarded by `ADD COLUMN IF NOT EXISTS`.
    - `.venv/bin/python -m pytest tests/test_status_drift.py -x -q` is green (the drift guard parses only CHECK value sets, unaffected by the two new non-CHECK columns).
    - `app/config.py` Settings carries the live-LLM opt-in bool with a `False` default.
    - `requirements.txt` is UNCHANGED (only `pip install`ed, not edited — review FIX 11).
  </acceptance_criteria>
  <done>Deps installed (requirements.txt untouched); live_llm marker + ALLOW_LIVE_LLM flag + reconciliation JSONB + error_reason TEXT columns present; status-drift guard green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The one OpenAI-compatible LLM client wrapper (per-tier routing, JSON mode, reflective retry, non-thinking toggle)</name>
  <read_first>
    - RESEARCH.md §Pattern 2 lines 288-323 (the call shape: OpenAI(base_url=...), temperature=0, response_format json_object, extra_body non-thinking, model_validate_json, ONE reflective retry, then raise)
    - AI-SPEC.md §1 (per-stage tier/env mapping) + §7 (prompt design constraints: literal "json" + example shape; max_tokens high)
    - app/config.py lines 29-54 (Settings extraction_*/decision_*/draft_* triples; get_settings() @lru_cache)
    - app/models/contracts.py (Extracted/Decision are the response_model targets; model_validate_json raises ValidationError on bad payload; ALL three are extra="forbid" — a structured response_model must therefore be a single BaseModel, see the FIX 6 wrapper note for the reconcile list case in Plan 03; and see FIX A — the extraction stage validates an `ExtractionPayload` (no run_id), NOT `Extracted` directly, because `Extracted` requires a code-owned run_id)
    - PATTERNS.md §`app/llm/client.py` (no Phase 1 analog — the config it reads IS the analog; do NOT use .parse(), DeepSeek lacks strict json_schema)
    - tests/test_models_contracts.py (the Decision/Extracted construction-assert style to mirror for the mocked client tests)
  </read_first>
  <behavior>
    - Test: call_structured(tier="extraction", messages, response_model=ExtractionPayload) with a mocked client returning valid JSON → returns a validated ExtractionPayload (the LLM payload model WITHOUT run_id — FIX A); the OpenAI client was constructed with extraction_base_url/extraction_api_key/extraction_model from Settings. (The stage code then stamps the code-owned run_id to build the full `Extracted` — see Plan 02 Task 2.)
    - Test: a tier whose model id contains "deepseek" sends extra_body selecting non-thinking mode; a non-deepseek tier does NOT.
    - Test: every structured call sets temperature=0 and response_format={"type":"json_object"}.
    - Test: first response is invalid JSON / empty content → wrapper retries EXACTLY once with the validation error appended to messages; second response valid → returns the object; the create() call count is exactly 2.
    - Test: both attempts fail validation → the wrapper raises (propagates to the orchestrator → ERROR; the orchestrator persists the reason via repo.record_run_error per FIX 7).
    - Test: the draft/free-text path (compose tier) returns raw string content, uses no json_object/response_format, and falls back to None/empty so the caller can template a body (does not raise on empty).
  </behavior>
  <action>
    Create `app/llm/__init__.py`, `app/llm/prompts/__init__.py`, and `app/llm/client.py`. Implement `call_structured(tier, messages, response_model)` per RESEARCH §Pattern 2: resolve `(base_url, model, api_key)` for the tier from `get_settings()` (tiers map: extraction→`extraction_*`, decision→`decision_*`, draft→`draft_*` per D-A2-02), construct `OpenAI(base_url=..., api_key=...)`, call `chat.completions.create(model=..., messages=..., temperature=0, response_format={"type":"json_object"}, max_tokens=2048, **extra)` where `extra["extra_body"]={"thinking":{"type":"disabled"}}` ONLY when the model id contains "deepseek" (the non-thinking per-request toggle — exact param is an open console blocker, code it as the documented best guess with a TODO comment per D-A2-02). `response_model` is a single Pydantic `BaseModel` (e.g. `ExtractionPayload` for extraction per FIX A, `Decision`, or the `NameReconciliationResponse` wrapper Plan 03 introduces per FIX 6 — never a bare `list[...]`, because the wrapper validates via `model_validate_json`; and the extraction tier validates the no-run_id `ExtractionPayload`, never `Extracted`, since `Extracted.run_id` is required and code-owned, not an LLM output). Validate with `response_model.model_validate_json(content)`; on `ValidationError` or empty content, retry ONCE with the error text appended to messages, then raise (D-A2-03 — exactly one retry, CLAUDE.md locks ONE). Add a separate `call_text(tier, messages, temperature=...)` path for the drafting call that returns raw string content with NO json_object and on empty content returns None (the caller falls back to a template per AI-SPEC §1 drafting). Do NOT use `.parse()`. Tier resolution must be config-driven so re-pointing a tier is a one-line env change. Create `tests/test_llm_client.py` and `tests/conftest.py` with a mocked-OpenAI client factory (monkeypatch the `OpenAI` constructor or inject a fake) so no network is touched.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_llm_client.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_llm_client.py -x` passes (per VALIDATION.md LLM-01/LLM-02 row).
    - `app/llm/client.py` resolves tier config via `get_settings()` and never hardcodes a base_url/model/key.
    - The reflective-retry path calls `create()` exactly twice on a first-failure-then-success sequence and raises on double-failure (assertable via the mock's call count).
    - The DeepSeek non-thinking `extra_body` is sent only for deepseek-model tiers.
    - `app/llm/client.py` contains no `.parse(` call (DeepSeek lacks strict json_schema), and `response_model` is always a single BaseModel (never a bare list); the extraction tier's structured response_model is `ExtractionPayload` (no run_id), never `Extracted` (FIX A).
  </acceptance_criteria>
  <done>The single client wrapper routes per tier, enters JSON mode, retries once reflectively, toggles non-thinking for DeepSeek, requires a BaseModel response_model (ExtractionPayload for extraction, not Extracted — FIX A), and the mocked tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stub email gateway + FULL DB repo layer (accessors, sole status mutator, JSONB persistence, sender/run/header-chain lookups, error persistence)</name>
  <read_first>
    - RESEARCH.md §Pattern 5 lines 391-402 (EmailGateway interface: parse_inbound -> InboundEmail, send_outbound -> synthetic Message-ID + outbound row) + §Pattern 6 lines 404-421 (header-chain lookup shape the resume helpers implement)
    - PATTERNS.md §`app/db/repo.py` (pooled get_connection() + conn.transaction() + %s placeholders; ON CONFLICT (message_id) DO NOTHING dedupe; JSONB persistence via json.dumps(obj.model_dump(mode="json")); SELECT with explicit column list + dict_row, never SELECT *)
    - app/db/supabase.py lines 51-63 (get_connection() pooled context manager)
    - app/db/seed.py lines 264-358 (the canonical transaction + parameterized %s insert discipline to mirror; NEVER f-string SQL)
    - app/db/schema.sql lines 61-125 (payroll_runs columns incl. extracted_data/decision/reconciliation/error_reason; email_messages incl. run_id/direction/message_id/in_reply_to/references_header/from_addr/to_addr/body_text + UNIQUE(message_id); businesses.contact_email for sender lookup) — NOTE email_messages.body_text holds the CLEANED inbound body (FIX C)
    - app/models/status.py (RunStatus enum — use `.value` in SQL, never string literals)
    - app/models/contracts.py (Extracted/Decision/PaystubLineItem shapes persisted; all extra="forbid"; persist_decision writes the Decision JSONB only — no status — per FIX B) + app/models/roster.py (Employee/Roster read-back shape with extra="forbid")
  </read_first>
  <behavior>
    - Test: send_outbound(...) generates an RFC-shaped synthetic Message-ID matching `<uuid@payroll-agent.local>` and (with a DB) inserts an email_messages row with direction='outbound' + run_id and returns the Message-ID — the canonical anchor for the clarification Message-ID is this outbound email_messages row (FIX 3), NOT a payroll_runs column.
    - Test: repo.set_status(run_id, RunStatus.AWAITING_APPROVAL) writes the enum `.value`, not a bare string; and set_status is the ONLY function that writes payroll_runs.status (FIX B).
    - Test: repo.persist_decision(run_id, Decision) serializes via model_dump(mode="json") so Decimal confidence becomes a JSON string and round-trips losslessly — and it writes the decision JSONB ONLY, never status (FIX B: persist_decision takes NO final_status; a test asserts the run's status is unchanged by a persist_decision call).
    - Test: record_run_error(run_id, reason) writes payroll_runs.error_reason AND advances status to ERROR by INTERNALLY calling set_status (FIX 7 + FIX B — the single status-write path; assert it both wrote error_reason and the status is ERROR.value).
    - Test: insert_inbound_email persists the body it is GIVEN (the already-cleaned body) into email_messages.body_text; load_source_email(run_id) returns that SAME cleaned body unchanged (no re-cleaning on read — FIX C).
    - Test: a read-back helper that rebuilds a Roster uses an explicit column list + dict_row (no SELECT *) so extra="forbid" does not crash on created_at/updated_at.
    - Test (mock/in-memory where no DB): the inbound dedupe uses ON CONFLICT (message_id) DO NOTHING and reports whether a row was inserted (so the webhook can decide whether to create a second run).
    - Test: find_business_by_sender(from_addr) returns the business whose businesses.contact_email matches (single-row lookup); an unknown sender returns None (the webhook stops, no guess — INGEST-03).
    - Test: find_awaiting_reply_for_header(in_reply_to, references_header) matches the linked outbound Message-ID restricted to status='awaiting_reply' (the parameterized LIKE on references); find_any_run_for_header(...) does the SAME header match across ANY status (for late-reply observability — FIX 10); both use named placeholders, never interpolation.
  </behavior>
  <action>
    Create `app/email/__init__.py` + `app/email/gateway.py` and `app/db/repo.py` and `app/pipeline/__init__.py`. In gateway.py implement `parse_inbound(raw) -> InboundEmail` (Phase 2 near-passthrough: validate the canonical InboundEmail JSON) and `send_outbound(*, run_id, to_addr, subject, body, in_reply_to=None) -> str` generating `f"<{uuid.uuid4()}@payroll-agent.local>"` and inserting an `email_messages(run_id=..., direction='outbound', message_id=..., in_reply_to=..., subject=..., body_text=..., to_addr=...)` row via repo.py, returning the Message-ID (EMAIL-01, D-A4-02 forward-compat). The outbound Message-ID lives ONLY on this linked email_messages row (direction='outbound', run_id) — that row is the single canonical anchor Plans 03/04 read; there is NO `payroll_runs.clarification_message_id` column (FIX 3). In repo.py implement the FULL surface every later wave needs (so no wave discovers a missing helper — review FIX 9), all using pooled `get_connection()` + `conn.transaction()` + `%s`/named placeholders ONLY (mirror seed.py; PATTERNS.md):
    — Ingest/run lifecycle: `insert_inbound_email(...)` with `ON CONFLICT (message_id) DO NOTHING` returning whether inserted — and it persists the ALREADY-CLEANED body it is given (post `clean_body()`, applied by the Plan 02 webhook BEFORE calling this helper) straight into `email_messages.body_text`, so the inbound row IS the cleaned-body source of truth (review FIX C); `find_business_by_sender(from_addr) -> business_id | None` (single-row match on businesses.contact_email; unknown → None, INGEST-03); `create_run(business_id, source_email_id, pay_period_*) -> run_id`; `load_run(run_id) -> run row` (explicit columns + dict_row); `load_source_email(run_id) -> InboundEmail | row` returning the original CLEANED inbound body straight from `email_messages.body_text` with NO re-cleaning on read (the body was cleaned at ingest — FIX C; the resume re-extraction context in Plan 04 FIX 4).
    — Status/persistence: `set_status(run_id, status: RunStatus)` is the ONE AND ONLY function that writes `payroll_runs.status` (uses `status.value`, never a string literal). The SINGLE documented exception is `record_run_error(run_id, reason)`, a deliberate thin WRAPPER that writes `payroll_runs.error_reason = %s` AND advances the run by INTERNALLY CALLING `set_status(run_id, RunStatus.ERROR)` — it is NOT a second raw status writer; it routes its status transition through `set_status`, so there is still EXACTLY ONE status-write path (FIX 7 + review FIX B; the D-A1-03 error home). The data-persistence helpers write data columns ONLY and never touch `status`: `persist_extracted(run_id, Extracted)`; `persist_decision(run_id, Decision)` — this writes the Decision JSONB ONLY and takes NO `final_status` argument (review FIX B: the orchestrator calls `set_status` SEPARATELY to advance state after persisting; persistence helpers do not own status transitions); `persist_reconciliation(run_id, list[NameMatchResult])`; `replace_line_items(run_id, list[PaystubLineItem])` (DELETE-by-run then insert per the idempotency invariant).
    — Email/threading: `insert_email_message(...)`; `get_outbound_message_id(run_id) -> str | None` (read the clarification Message-ID back from the linked outbound email_messages row — the FIX 3 anchor); `find_awaiting_reply_for_header(in_reply_to, references_header) -> run_id | None` JOINing payroll_runs to outbound email_messages with `em.message_id = %(in_reply_to)s OR %(references)s LIKE '%%' || em.message_id || '%%'` restricted to `pr.status = 'awaiting_reply'`; `find_any_run_for_header(in_reply_to, references_header) -> run_id | None` doing the SAME header match across ANY status (so a header match to an already-sent/reconciled run is observable as a late reply, not silently dropped — FIX 10).
    — Roster: `load_roster_for_business(business_id) -> Roster` using the explicit EMPLOYEE_COLS column list + `dict_row` (no SELECT *).
    All JSONB writes use `json.dumps(obj.model_dump(mode="json"))`. The header-chain `references` LIKE MUST be a named placeholder, NEVER interpolated (SQL-injection defense). Add focused tests in a new tests/test_gateway.py for the synthetic-Message-ID shape, the model_dump serialization, the persist_decision-writes-no-status assertion (FIX B), the record_run_error-routes-through-set_status assertion (FIX B), the insert/load cleaned-body round-trip (FIX C), and the sender/header-chain lookup shapes (DB-touching round-trips go behind `@pytest.mark.integration` + the existing two-factor guard; the parameterized-SQL shape is asserted statically where possible).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -m "not integration and not live_llm" tests/test_gateway.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/email/gateway.py` exposes exactly `parse_inbound` and `send_outbound` (the one provider seam); `send_outbound` returns an `<uuid@payroll-agent.local>`-shaped Message-ID and inserts the outbound email_messages row with `run_id` + `direction='outbound'` (the FIX 3 anchor; NO payroll_runs Message-ID column).
    - `app/db/repo.py` commits the FULL named surface (FIX 9): `find_business_by_sender`, `load_run`, `load_source_email`, `record_run_error`, `get_outbound_message_id`, `find_awaiting_reply_for_header`, `find_any_run_for_header`, plus the insert/persist/replace helpers — `grep -q "def find_business_by_sender\|def load_source_email\|def record_run_error\|def find_awaiting_reply_for_header\|def find_any_run_for_header\|def get_outbound_message_id" app/db/repo.py` finds all six.
    - `set_status` is the only function that writes `payroll_runs.status` and uses `RunStatus(...).value`; `record_run_error` writes `error_reason` and advances to ERROR by CALLING `set_status` (the single status-write path) — `grep -q "set_status" app/db/repo.py` shows record_run_error routes through it, and `persist_decision`'s signature is `persist_decision(run_id, decision)` with NO `final_status` parameter (FIX B; assertable by the persist_decision-writes-no-status test).
    - `load_source_email` returns the cleaned `email_messages.body_text` that `insert_inbound_email` persisted, with no re-cleaning on read (FIX C; assertable by the cleaned-body round-trip test).
    - Every SQL statement in repo.py uses `%s`/named placeholders — `grep -nE "execute\(f|execute\(.*%[^s(]" app/db/repo.py` finds no f-string SQL; the references LIKE is a named placeholder (FIX 10 lookups, T-injection).
    - JSONB persistence helpers call `model_dump(mode="json")` (assertable by the serialization unit test).
    - The mocked/in-memory tests pass with `-m "not integration and not live_llm"`.
  </acceptance_criteria>
  <done>The gateway interface + the FULL repo accessor surface (sender lookup, run/source-email load returning the cleaned body per FIX C, outbound Message-ID lookup, awaiting_reply-only AND any-status header-chain lookups, error-reason persistence) exist with parameterized SQL, set_status as the sole status mutator with record_run_error as the one documented wrapper (persist_decision writes no status — FIX B), synthetic Message-IDs anchored on email_messages, and Decimal-safe JSONB persistence; mocked tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider → app | Untrusted model output (JSON content) crosses into the pipeline; never trusted as schema-valid until Pydantic-validated. |
| DB ← repo writes | All SQL is constructed in repo.py; parameter values originate from validated contracts. |
| reply header → run lookup | In-Reply-To/References from an inbound reply feed a parameterized header-chain lookup; never interpolated into SQL. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | repo.py SQL (all writes + header-chain LIKE) | mitigate | Parameterized psycopg `%s`/named placeholders only; the `references` LIKE uses a named placeholder, never f-string SQL (PATTERNS Shared Patterns; RESEARCH Security Domain V5). |
| T-02-02 | Tampering | LLM JSON output → contracts | mitigate | `model_validate_json` on the real Pydantic contract is the field-correctness gate; `json_object` guarantees syntax only (RESEARCH Pitfall 1). |
| T-02-03 | Information disclosure | LLM client / gateway logging | mitigate | Do not log full email bodies at info level; log Message-IDs + status, not PII (RESEARCH Security Domain PII-safe logging). |
| T-02-04 | Spoofing | inbound email_messages | mitigate | `UNIQUE(message_id)` + `ON CONFLICT DO NOTHING` replay defense surfaced in repo.insert_inbound_email (consumed by Plan 02 webhook). |
| T-02-12 | Spoofing | sender lookup (INGEST-03 foundation) | mitigate | `find_business_by_sender` matches from_addr to a known businesses.contact_email; an unknown sender returns None so the webhook stops without guessing (the access-control seam Plan 02 enforces and Plan 04 reuses for reply revalidation). |
| T-02-14 | Tampering | status writes (FIX B) | mitigate | `set_status` is the SOLE status writer; `record_run_error` is the one documented wrapper that routes its ERROR transition through `set_status` (single status-write path); data-persistence helpers (persist_decision/persist_extracted/persist_reconciliation) never write status, so no second path can silently corrupt the state machine. |
| T-02-SC | Tampering | npm/pip installs | mitigate | No NEW package names this plan (openai/fastapi/uvicorn/python-multipart pre-vetted + pinned in Phase 1; requirements.txt is only installed, not edited — FIX 11). A reply-parser, IF chosen later, requires the full legitimacy gate + a blocking human checkpoint. |
| T-02-05 | Repudiation | webhook signing-secret / SPF-DKIM | accept | Real-gateway signing-secret + SPF/DKIM verification are P6 (no real provider in Phase 2); documented, not implemented (per planning context — no high-severity item blocks Phase 2). |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m "not integration and not live_llm" -x -q` is green (mocked client + gateway + drift guard).
- `import openai, fastapi, uvicorn, multipart` succeeds.
- `live_llm` marker registered; `ALLOW_LIVE_LLM` in .env.example; `reconciliation` JSONB AND `error_reason` TEXT columns present.
- No f-string SQL in repo.py; `set_status` is the sole status writer with `record_run_error` the one documented wrapper that routes through it (persist_decision writes no status — FIX B); the FULL repo surface (FIX 9) is committed — sender lookup, run/source-email load (cleaned body — FIX C), outbound Message-ID lookup, both header-chain lookups, error-reason persistence.
- `load_source_email` returns the cleaned `email_messages.body_text` persisted at ingest (FIX C).
- `requirements.txt` unchanged (only installed — FIX 11).
</verification>

<success_criteria>
- The LLM client wrapper routes per tier, enters JSON mode, retries once reflectively, and toggles DeepSeek non-thinking — all proven by mocked tests (LLM-01, LLM-02); the extraction tier validates `ExtractionPayload` (no run_id), never `Extracted` (FIX A).
- The stub gateway records outbound messages with synthetic Message-IDs anchored on email_messages(direction='outbound', run_id) behind one interface (EMAIL-01 foundation; the FIX 3 single anchor).
- The repo layer (FULL surface) + reconciliation + error_reason columns + live-LLM flag + marker give Plans 02/03/04 a stable seam to build slices on with no missing helper; the status-write path is single (set_status, with record_run_error the documented wrapper — FIX B) and load_source_email returns the cleaned body (FIX C).
</success_criteria>

<output>
Create `.planning/phases/02-walking-skeleton/02-01-SUMMARY.md` when done.
</output>
