---
phase: 02-walking-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/pipeline/reconcile_names.py
  - app/pipeline/decide.py
  - app/pipeline/compose_email.py
  - app/pipeline/orchestrator.py
  - app/llm/prompts/reconcile.py
  - app/models/reconcile.py
  - app/db/repo.py
  - fixtures/gate_block_hero.json
  - tests/test_reconcile.py
  - tests/test_gate.py
  - tests/test_clarify.py
  - tests/test_demo_fixtures.py
autonomous: true
requirements: [LLM-05, LLM-09, CLAR-01, DEMO-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Only residual (failed-deterministic) names reach the layer-2 LLM, which classifies each as llm_typo/llm_nickname/unknown with confidence + reason against the full roster via a NameReconciliationResponse wrapper model (matches: list[NameMatchResult], extra='forbid'), and never re-decides a clean layer-1 hit (LLM-05, D-A3-01, review FIX 6)."
    - "The layer-2 reconcile and the extended decide stages stay PURE for eval-reuse: no DB connection, no DB I/O (the eval can call them with typed fixture inputs). decide() returns a Decision directly — the Decision contract has NO run_id field (verified contracts.py:94-113), so no run_id stamping is needed for decide; run_id stamping is the extract/Extracted concern only (review FIX A — purity means no-DB, not no-run_id)."
    - "Reconciliation enforces a one-to-one roster mapping in pure code by EXTENDING the empty-but-real check_one_to_one(matches, extracted) function shipped in Plan 02 Task 2: a duplicate submitted name, two names resolving to one employee, or a name resolving to no employee gates the run to clarification — even with a confident model (LLM-09, D-A3-02)."
    - "When final_action is request_clarification, the LLM drafts a clarification email (cheap tier), the stub gateway sends it and records the synthetic Message-ID on the linked outbound email_messages row (direction='outbound', run_id) — the SINGLE canonical anchor, NOT a payroll_runs column — and status moves to awaiting_reply via repo.set_status (the sole status writer — FIX B) (CLAR-01, review FIX 3)."
    - "The gate-block hero fixture (David Reyez against seeded David Reyes) is committed and replays end-to-end: TWO distinct mocked LLM responses (reconcile layer-2 returns match_type='llm_typo', matched_employee_id=David Reyes's id, confidence=0.6; decision-advisory returns model_action=process) so the code gate blocks → clarify → awaiting_reply — the 'model was willing, code said no' story (DEMO-01 gate-block half, review FIX 8)."
    - "The gated (request_clarification) branch persists the full per-name list[NameMatchResult] to payroll_runs.reconciliation by REUSING the same repo.persist_reconciliation call already wired in Plan 02's clean branch — so reconciliation is persisted on EVERY run (clean + gated) (D-A3-05)."
  artifacts:
    - path: "app/models/reconcile.py"
      provides: "NameReconciliationResponse(BaseModel){ matches: list[NameMatchResult] } with model_config extra='forbid' — the layer-2 structured-output wrapper so call_structured validates via model_validate_json (review FIX 6)"
      contains: "NameReconciliationResponse"
      min_lines: 10
    - path: "app/pipeline/reconcile_names.py"
      provides: "layer-2 LLM reconcile over residual names + merge with layer-1; full roster in-context; unwraps NameReconciliationResponse.matches; DB-free pure stage (FIX A purity = no-DB)"
      contains: "residual"
      min_lines: 30
    - path: "app/pipeline/decide.py"
      provides: "EXTENDS the Plan 02 empty-but-real check_one_to_one into full one-to-one mapping enforcement (dup name / two->one emp / name->no emp) returning gate_reasons; decide() returns Decision (no run_id field) — DB-free pure stage (FIX A)"
      contains: "check_one_to_one"
      min_lines: 50
    - path: "app/pipeline/compose_email.py"
      provides: "clarification drafting (cheap tier, free text) with templated fallback on empty content"
      min_lines: 20
    - path: "fixtures/gate_block_hero.json"
      provides: "David Reyez gate-block hero fixture (model says process, code blocks on sub-0.8)"
      contains: "Reyez"
  key_links:
    - from: "app/pipeline/reconcile_names.py"
      to: "app/models/reconcile.py NameReconciliationResponse"
      via: "call_structured('decision', ..., NameReconciliationResponse) then unwrap .matches"
      pattern: "NameReconciliationResponse"
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/compose_email.py + gateway.send_outbound"
      via: "final_action == request_clarification branch; Message-ID anchored on email_messages(direction='outbound', run_id)"
      pattern: "request_clarification"
    - from: "app/db/repo.py"
      to: "payroll_runs.reconciliation"
      via: "persist_reconciliation(list[NameMatchResult]) — same call reused from Plan 02"
      pattern: "reconciliation"
---

<objective>
Deliver slice (b) per the LOCKED build order D-A5-01: the gate-block case — "the model was willing; the code said no." Wire the layer-2 LLM reconciliation (only residual names reach the model, via a `NameReconciliationResponse` wrapper so the structured call validates), EXTEND the empty-but-real `check_one_to_one` (shipped in Plan 02) into the full pure-code one-to-one mapping enforcement (LLM-09), the clarification draft+send loop into `awaiting_reply` with the outbound Message-ID anchored on the linked `email_messages` row (CLAR-01), and commit the David Reyez hero fixture so the whole gate-block path replays end-to-end on mocked responses.

This proves the core value of the project beyond the clean path: a model that proposes `process` on a sub-0.8 name (a confident-but-wrong typo match) is hard-blocked by code into clarification, and a name can never silently collapse onto another employee.

The reconcile and decide stages stay PURE for eval-reuse: no DB connection, no DB I/O. `decide()` returns a `Decision` directly — the verified `Decision` contract (contracts.py:94-113) has NO `run_id` field, so no run_id stamping is needed here; run_id stamping is the extract/`Extracted` concern only (review FIX A — purity means no-DB, not no-run_id).

Purpose: After this plan, a real user can POST the gate-block hero fixture and watch the code gate override a willing model and route to clarification (the on-camera money shot), then the run pauses at `awaiting_reply`. This is the second new capability stacked on slice (a).
Output: the layer-2 reconcile + its wrapper model, the extended one-to-one mapping gate, `compose_email.py`, the orchestrator clarify branch, the hero fixture, and the gate-block test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-walking-skeleton/02-CONTEXT.md
@.planning/phases/02-walking-skeleton/02-RESEARCH.md
@.planning/phases/02-walking-skeleton/02-AI-SPEC.md
@.planning/phases/02-walking-skeleton/02-PATTERNS.md
@.planning/phases/02-walking-skeleton/02-VALIDATION.md
@.planning/phases/02-walking-skeleton/02-01-SUMMARY.md
@.planning/phases/02-walking-skeleton/02-02-SUMMARY.md
@app/models/contracts.py
@app/models/roster.py
@app/models/status.py
@app/db/seed.py
</context>

## Phase Goal

**As a** payroll operator, **I want to** see a low-confidence name mismatch force a clarification even when the model proposed processing, **so that** a shaky name match can never reach a real payroll calculation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Layer-2 LLM reconciliation (NameReconciliationResponse wrapper) + EXTEND check_one_to_one into full one-to-one mapping enforcement (LLM-09) in decide.py</name>
  <read_first>
    - AI-SPEC.md §1 Stage 2 (layer-2 LLM: residual names → llm_typo/llm_nickname/unknown + confidence + reason; full roster in-context; temp 0 mandatory; decision tier DECISION_*) + §2 gate rule 4 (one-to-one mapping: dup name / two→one emp / name→no emp)
    - RESEARCH.md §Pattern 3 lines 342-346 (layer-2 over residual + FULL roster so genuine ambiguity drives low confidence) + §Pattern 4 lines 374-387 (check_one_to_one returns gate_reasons) + §7 (reconcile prompt is the tuning surface; "json"+example required)
    - PATTERNS.md §`app/pipeline/reconcile_names.py` (layer-1 already done in Plan 02; layer-2 is the new LLM call; merge into one list[NameMatchResult], one per submitted name; the stage stays DB-free — purity is no-DB, not no-run_id, FIX A) + §`app/pipeline/decide.py` (one-to-one pure code over matched_employee_id collisions; compare Decimal("0.8"))
    - app/models/contracts.py (ALL contracts are extra="forbid" — CRITICAL for FIX 6: `call_structured` validates via `response_model.model_validate_json`, which requires a single BaseModel; a bare `list[NameMatchResult]` is NOT a BaseModel and has no model_validate_json, so layer-2 needs a wrapper model) + lines 94-113 (Decision has NO run_id field — decide() returns Decision directly; no run_id stamping for decide, FIX A)
    - app/models/roster.py (NameMatchResult match_type Literal incl. llm_typo/llm_nickname/unknown; confidence Decimal; matched_employee_id field; the roster.py docstring is the binding purity constraint — the stages take typed values, no DB inside) — the wrapper NameReconciliationResponse holds a list of THESE
    - app/pipeline/decide.py + app/pipeline/reconcile_names.py (the Plan 02 layer-1 + gate skeleton: `def check_one_to_one(matches, extracted) -> list[str]` ALREADY EXISTS returning [] and is ALREADY called inside decide() — EXTEND that concrete function with the three collision rules; do NOT create a new one or rewrite decide(); decide() already returns Decision with no run_id field — FIX A)
    - tests/test_gate.py (Plan 02's test_check_one_to_one_stub_shape pins the signature — the three-collision tests added here must keep that signature)
    - app/db/seed.py lines 117-138 (David Reyes emp 3, business 2 — the hero target; capture his employee id for the FIX 8 hero mock)
  </read_first>
  <behavior>
    - Test (test_reconcile.py::test_reconcile_wrapper_model): the layer-2 structured call uses `NameReconciliationResponse` (a BaseModel with `matches: list[NameMatchResult]`, extra="forbid") as response_model so `call_structured` can `model_validate_json` it; the stage unwraps `.matches` into the merged list (review FIX 6 — a bare list[...] would have no model_validate_json).
    - Test (test_reconcile.py::test_residual_only_to_llm): clean names resolve via layer-1 with NO model call; only residual names are sent to the layer-2 LLM; a layer-1 hit is never re-decided by the model.
    - Test (test_reconcile.py): the layer-2 result (unwrapped from NameReconciliationResponse.matches) merges with layer-1 into one list[NameMatchResult], exactly one per submitted name.
    - Test (test_reconcile.py::test_reconcile_is_db_free — FIX A): reconcile_names imports no DB module and takes no connection (purity = no-DB) — assertable statically.
    - Test (test_gate.py::test_one_to_one_collisions): (a) two distinct submitted names → same matched_employee_id gates to clarify; (b) a duplicated submitted name gates; (c) a name → no roster employee gates — each as a distinct gate_reason (LLM-09).
    - Test (test_gate.py): a high-confidence (≥0.8) name that collides under rule 4 still gates (mapping gate catches collision-type mismatches even at high confidence, G6).
  </behavior>
  <action>
    First create `app/models/reconcile.py` with `class NameReconciliationResponse(BaseModel): model_config = ConfigDict(extra="forbid"); matches: list[NameMatchResult]` (review FIX 6 — the layer-2 structured-output schema; `call_structured` validates via `response_model.model_validate_json`, which a bare `list[NameMatchResult]` cannot satisfy because it is not a BaseModel). Extend `app/pipeline/reconcile_names.py` with layer-2: gather residual names (those layer-1 returned no clean hit for), call `client.call_structured("decision", ..., NameReconciliationResponse)` passing the FULL roster in-context (RESEARCH §Pattern 3 — so genuine ambiguity drives low confidence by construction), then unwrap `response.matches` (a `list[NameMatchResult]`), each classified `llm_typo`/`llm_nickname`/`unknown` with `confidence: Decimal` + reason; temperature 0 is mandatory (the 0.8 gate keys off this confidence, D-A2-03). Merge layer-2 results with the layer-1 results into one `list[NameMatchResult]` (one per submitted name); the model MUST NOT see or re-decide a layer-1 hit (LLM-05, D-A3-01). The stage stays PURE for eval-reuse — no DB connection, no DB I/O (FIX A purity = no-DB, not no-run_id). Add the reconcile prompt template in `app/llm/prompts/reconcile.py` with the literal word "json" + an example shape of the `{"matches": [...]}` wrapper (RESEARCH Pitfall 1) — this is the tuning surface for the live hero run (Plan 04). In `app/pipeline/decide.py`, EXTEND the EXISTING empty-but-real `check_one_to_one(matches, extracted) -> list[str]` function (shipped by Plan 02 Task 2 returning `[]` and already invoked inside `decide()`) with the three collision rules (LLM-09, D-A3-02): (a) two distinct submitted_names → same matched_employee_id, (b) a duplicated submitted_name, (c) a name → no roster employee; each collision → a gate_reason string in the returned list. Do NOT create a second function or change its signature or its call site — it is already wired into the gate, so adding the rules immediately strengthens the gate so a confident model can never let a name collapse onto another. `decide()` still returns a `Decision` directly (the contract has no run_id field — FIX A; no run_id stamping here). Extend tests/test_reconcile.py (incl. test_reconcile_wrapper_model + test_reconcile_is_db_free) + tests/test_gate.py (mocked, deterministic).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_reconcile.py::test_reconcile_wrapper_model tests/test_reconcile.py::test_residual_only_to_llm tests/test_gate.py::test_one_to_one_collisions -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/models/reconcile.py` defines `NameReconciliationResponse(BaseModel)` with `matches: list[NameMatchResult]` + extra="forbid", and reconcile_names.py calls `call_structured("decision", ..., NameReconciliationResponse)` then unwraps `.matches` — `grep -q "NameReconciliationResponse" app/pipeline/reconcile_names.py` and the response_model is the wrapper, never a bare list (review FIX 6).
    - `pytest tests/test_reconcile.py::test_reconcile_wrapper_model -x` passes — the wrapper validates via model_validate_json and the stage unwraps .matches.
    - `pytest tests/test_reconcile.py::test_residual_only_to_llm -x` passes — only residual names reach the LLM; layer-1 hits never re-decided (VALIDATION.md LLM-05 row).
    - `reconcile_names.py` and `decide.py` are DB-free (purity = no-DB, not no-run_id — FIX A): `grep -L "supabase\|get_connection\|repo\." app/pipeline/reconcile_names.py app/pipeline/decide.py` lists both; `pytest tests/test_reconcile.py::test_reconcile_is_db_free -x` passes.
    - `pytest tests/test_gate.py::test_one_to_one_collisions -x` passes — all three collision shapes gate to clarify (LLM-09 row).
    - `decide.py`'s `check_one_to_one` is EXTENDED (not replaced): the signature `def check_one_to_one(matches, extracted) -> list[str]` is unchanged from Plan 02 and Plan 02's `test_check_one_to_one_stub_shape` still passes, while it now returns gate_reasons on collisions; `decide()` still returns Decision with no run_id stamping (FIX A).
    - A high-confidence colliding name still gates (mapping gate independent of confidence, G6).
    - The reconcile prompt carries the literal word "json" + an example shape of the `{"matches": [...]}` wrapper.
  </acceptance_criteria>
  <done>Layer-2 reconcile uses the NameReconciliationResponse wrapper (FIX 6), sends only residual names to the model, merges with layer-1, and stays DB-free (purity = no-DB — FIX A); the EXISTING check_one_to_one is extended (signature unchanged) so the one-to-one mapping gate blocks dup/two-to-one/no-employee collisions even with a confident model; decide() returns Decision (no run_id field); mocked tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clarification draft+send into awaiting_reply (CLAR-01) with Message-ID anchored on email_messages + reuse persist_reconciliation on the gated branch</name>
  <read_first>
    - AI-SPEC.md §1 Drafting call (compose_email: DRAFT_* cheap tier, free text NOT json mode, may run warmer, NO schema retry; empty content → templated fallback so a draft failure never strands the run) + §4 item 4 (persist per-name list[NameMatchResult])
    - RESEARCH.md §Pattern 5 lines 391-402 (gateway.send_outbound returns synthetic Message-ID + outbound row) + diagram lines 210-216 (final_action==request_clarification → compose → send → record Message-ID on the outbound email_messages row → AWAITING_REPLY)
    - PATTERNS.md §`app/pipeline/compose_email.py` (analog client.py warm/no-JSON path) + §`app/db/repo.py` (persist_reconciliation via model_dump(mode="json") — ALREADY authored + wired into the clean branch in Plan 02; the outbound Message-ID is stored on the email_messages row by send_outbound, NOT on a payroll_runs column — review FIX 3; set_status is the sole status writer — FIX B)
    - app/db/schema.sql lines 109-125 (email_messages has run_id + direction + message_id; payroll_runs has NO clarification_message_id column — the anchor is the linked outbound email_messages row, FIX 3)
    - app/models/status.py (AWAITING_REPLY pause) + app/pipeline/orchestrator.py (the Plan 02 final_action branch — extend the request_clarification arm; the clean process arm already calls persist_reconciliation then advances via set_status — FIX B)
    - 02-01-SUMMARY.md + 02-02-SUMMARY.md (gateway.send_outbound returns + records the Message-ID on the outbound email_messages row; repo.get_outbound_message_id reads it back; repo.persist_reconciliation, repo.set_status [sole status writer — FIX B] signatures; persist_reconciliation is already on the clean path)
  </read_first>
  <behavior>
    - Test (test_clarify.py::test_clarify_sends_and_pauses): a gated run (final_action==request_clarification) → compose_email drafts a body (mocked cheap LLM), gateway.send_outbound records an outbound email_messages row (direction='outbound', run_id) with a synthetic Message-ID, that Message-ID is retrievable via repo.get_outbound_message_id(run_id) FROM the email_messages row (NOT a payroll_runs column — FIX 3), and status moves to awaiting_reply via repo.set_status (the sole status writer — FIX B) (CLAR-01).
    - Test (test_clarify.py): when the draft LLM returns empty content, compose_email falls back to a templated clarification body (no raise, the run still pauses cleanly).
    - Test: the per-name list[NameMatchResult] is persisted to payroll_runs.reconciliation on the gated branch too (D-A3-05) — the SAME persist_reconciliation call as the clean branch, so reconciliation is non-NULL on BOTH clean and gated runs.
  </behavior>
  <action>
    Create `app/pipeline/compose_email.py`: `compose_clarification(decision, *, llm) -> str` using `client.call_text("draft", ...)` (cheap DRAFT_* tier, free text, NOT json mode, may run a low non-zero temperature per AI-SPEC — Claude's discretion; NO schema retry); turn `decision.gate_reasons` + `unresolved_names` + `missing_fields` into a human-readable ask; on empty content fall back to a templated clarification body so a draft failure never strands the run (AI-SPEC §1). Extend the `request_clarification` branch in `app/pipeline/orchestrator.py`: call compose_clarification, `gateway.send_outbound(run_id=..., to_addr=..., subject=..., body=...)` (the stub generates the synthetic Message-ID and records it on the outbound email_messages row, direction='outbound', run_id — that linked row is the SINGLE canonical anchor for the clarification Message-ID; there is NO payroll_runs Message-ID column, review FIX 3), set status `AWAITING_REPLY` via `repo.set_status` (the sole status writer — FIX B; CLAR-01 pause). Plan 04 reads the Message-ID back from this same email_messages row (via repo.get_outbound_message_id / the header-chain lookup) for the In-Reply-To match — confirm this plan does NOT write the Message-ID to a payroll_runs column anywhere. The orchestrator already calls `repo.persist_reconciliation(run_id, matches)` on EVERY run before branching (wired in Plan 02 Task 3 on the clean path) — confirm the gated branch reaches that SAME call (i.e. persist_reconciliation runs before the final_action branch, not inside only one arm) so the gated run's reconciliation is non-NULL exactly like the clean run (D-A3-05). Do NOT add a second persist_reconciliation call; reuse the existing one. Add tests/test_clarify.py (mocked draft LLM; the DB round-trip behind @pytest.mark.integration + two-factor guard, the in-memory state assertions always-run).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_clarify.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_clarify.py::test_clarify_sends_and_pauses -x` passes — drafts, stub-sends, records the Message-ID on the outbound email_messages row (retrievable via repo.get_outbound_message_id), status→awaiting_reply via repo.set_status (VALIDATION.md CLAR-01 row; FIX 3 + FIX B).
    - The clarification Message-ID is anchored on email_messages(direction='outbound', run_id), NOT a payroll_runs column — `grep -L "clarification_message_id" app/pipeline/orchestrator.py app/db/repo.py` (no such column written; FIX 3).
    - `compose_email.py` falls back to a templated body on empty content (assertable by the empty-content test) — a draft failure never strands the run.
    - The gated branch's reconciliation is persisted via the SAME `repo.persist_reconciliation` call as the clean branch (no second call added) — `grep -c "persist_reconciliation" app/pipeline/orchestrator.py` is 1 (one call, reached by both branches) and the gated run's reconciliation is non-NULL (D-A3-05).
    - The compose call uses the DRAFT_* tier and NOT json_object (free-text prose); the status advance goes through repo.set_status (FIX B).
  </acceptance_criteria>
  <done>A gated run drafts + stub-sends a clarification, records the outbound Message-ID on the linked email_messages row (the single anchor, FIX 3), pauses at awaiting_reply via the sole set_status writer (FIX B), and the SAME persist_reconciliation call (from Plan 02) covers the gated branch so reconciliation is non-NULL on every run; mocked tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit the David Reyez gate-block hero fixture + end-to-end gate-block replay (DEMO-01 gate-block half)</name>
  <read_first>
    - CONTEXT.md D-A4-01 (hero fixture: ambiguous submitted name failing deterministic match that the model reconciles WITH a matched employee at confidence <0.8 — reuse seeded David Reyes, submitted as David Reyez; model proposes process but code gate blocks → clarify) + D-A4-01a (the LIVE validation is Plan 04 — THIS plan proves the gate fires on MOCKED sub-0.8 input)
    - VALIDATION.md "mock-proves-the-gate" distinction (the mock feeds a hand-authored 0.6 + model_action=process and asserts final_action=request_clarification; it does NOT prove the real model returns that — that's Plan 04's live exit gate)
    - PATTERNS.md §fixtures (canonical InboundEmail JSON; fixed-UUID discipline; David Reyez against seeded David Reyes emp 3 e0000003-...; from_addr = Business 2 contact_email hr@metrodeli.example)
    - app/db/seed.py lines 117-138 (David Reyes, business 2, hourly — the hero target row; his employee id is what the FIX-8 hero mock sets as matched_employee_id) + lines 57-62 (Business 2 contact_email)
    - app/models/roster.py (NameMatchResult: match_type Literal incl. llm_typo; matched_employee_id) — the FIX-8 hero mock uses match_type="llm_typo" + matched_employee_id=David Reyes's id + confidence=0.6
    - tests/test_demo_fixtures.py (the Plan 02 clean-fixture replay test — extend with the gate-block fixture)
    - tests/conftest.py (the mocked-LLM client factory — it must support distinct per-call responses so the reconcile call and the decision-advisory call return DIFFERENT mocks; extend it if it only supports one canned response)
  </read_first>
  <behavior>
    - Test (test_demo_fixtures.py): POSTing fixtures/gate_block_hero.json with TWO distinct mocked LLM responses — (1) the layer-2 reconcile call returns a `NameReconciliationResponse` whose `matches` contains a `NameMatchResult` for "David Reyez" with `match_type="llm_typo"`, `matched_employee_id=<David Reyes's seeded id>`, and `confidence=Decimal("0.6")` (the model WAS willing — it matched the typo to a real employee, just not confidently enough — review FIX 8, NOT match_type="unknown"), and (2) the decision-advisory call returns model_action="process" — → the code gate fires, final_action=="request_clarification" while model_action=="process" (the override is asserted explicitly), and the run reaches awaiting_reply end-to-end (DEMO-01 gate-block half; the mock proves the gate).
    - Test: both committed fixtures (clean + gate-block) replay end-to-end via POST (DEMO-01 fully exercised on mocks).
  </behavior>
  <action>
    Author `fixtures/gate_block_hero.json` as a canonical `InboundEmail` (D-A4-01): `from_addr` = Business 2's seeded contact_email, body submits **David Reyez** (the one-letter y→z transposition of the seeded David Reyes) with explicit hours so the ONLY gate trigger is the sub-0.8 name (not a missing field). Extend `tests/test_demo_fixtures.py` with the gate-block replay driven by TWO structurally-distinct mocked responses (the reconcile layer-2 surface and the decision-advisory surface are SEPARATE `call_structured` invocations — do NOT conflate them into one mock): (1) the layer-2 reconcile mock returns a `NameReconciliationResponse` whose `matches` holds a `NameMatchResult` for David Reyez with `match_type="llm_typo"`, `matched_employee_id=<David Reyes's seeded employee id from seed.py>`, and `confidence=Decimal("0.6")` — the model is WILLING (it found the right employee for the typo) but at sub-threshold confidence, which is the cleanest "model willing, code said no" shape (review FIX 8 — do NOT use match_type="unknown", which is the model being CAUTIOUS, not willing, and weakens the narrative); (2) the decision-advisory mock returns a Decision-advisory payload with `model_action=="process"`. Assert the orchestrator's `decide()` produces `final_action=="request_clarification"` WHILE `model_action=="process"` (the override is the money shot — assert BOTH fields, not just final_action) and the run reaches `awaiting_reply` (the mock proves the gate — the LIVE proof is Plan 04, D-A4-01a). If the Plan 01/02 conftest mocked-LLM factory only returns a single canned response, extend it to dispatch distinct responses per tier/call so the two surfaces can differ. Confirm both fixtures replay end-to-end via POST (DEMO-01). Run the full mocked suite to confirm no regressions.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_demo_fixtures.py -x -q && .venv/bin/python -m pytest -m "not integration and not live_llm" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_demo_fixtures.py -x` passes — both clean and gate-block fixtures replay end-to-end via POST (VALIDATION.md DEMO-01 row).
    - `fixtures/gate_block_hero.json` submits "David Reyez" against the seeded "David Reyes" and validates as InboundEmail (`grep -q "Reyez" fixtures/gate_block_hero.json`).
    - The gate-block replay uses TWO distinct mocked responses — reconcile returns match_type="llm_typo" + matched_employee_id=David Reyes's id + confidence=0.6 (the willing-but-sub-threshold shape, review FIX 8, NOT "unknown") + decision model_action="process" — and asserts the override explicitly: `final_action=="request_clarification"` WHILE `model_action=="process"` → awaiting_reply (the mock proves the gate; the live proof is Plan 04).
    - The full mocked suite (`-m "not integration and not live_llm"`) is green — no regressions.
  </acceptance_criteria>
  <done>The David Reyez hero fixture is committed and replays end-to-end: TWO distinct mocks (reconcile llm_typo→David Reyes @0.6 + decision process) → code gate blocks → final_action=request_clarification while model_action=process → awaiting_reply; both fixtures replay via POST; mocked suite green — slice (b) complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| residual name → layer-2 LLM | Attacker-influenced submitted names cross into the model; the model's confidence is advisory and code-gated. |
| model confidence → 0.8 gate | The model's self-reported confidence is uncalibrated; the per-name code gate is the trust enforcement. |
| draft LLM output → outbound email | Free-text body sent to the client; templated fallback bounds a failure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Elevation of privilege | model says process on a sub-0.8 / colliding name | mitigate | The code gate (per-name <0.8 + one-to-one mapping in decide.py) hard-blocks regardless of model_action — the thesis; final_action is the SOLE branch source (LLM-07/09, G4/G6). |
| T-03-02 | Spoofing | a name silently collapsing onto another employee | mitigate | check_one_to_one pure-code mapping enforcement (extended from Plan 02's empty-but-real signature): dup name / two→one emp / name→no emp each gate to clarify even with a confident model (LLM-09, D-A3-02). |
| T-03-03 | Tampering | LLM reconcile output → NameMatchResult | mitigate | model_validate_json on the NameReconciliationResponse wrapper (FIX 6); reflective retry; confidence bounded ge=0 le=1 by the contract. |
| T-03-04 | Information disclosure | clarification email body / reconcile logging | mitigate | No full inbound bodies at info level; the outbound draft is the operator-authored ask, not client PII echoed back verbatim. |
| T-03-05 | Repudiation | outbound clarification send | mitigate | Every outbound recorded in email_messages with a synthetic Message-ID (the audit trail AND the single Message-ID anchor, FIX 3); the no-duplicate-send hard guarantee (CLAR-04) is Phase 5, recording-only here (forward-compat, D-A1-04). |
| T-03-SC | Tampering | package installs | mitigate | No new package names this plan (reuses Plan 01/02 deps); no install task. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m "not integration and not live_llm" -q` is green.
- Layer-2 reconcile validates through the NameReconciliationResponse wrapper (FIX 6); only residual names reach the model; layer-1 hits never re-decided; reconcile + decide stay DB-free (purity = no-DB, not no-run_id — FIX A); decide() returns Decision (no run_id field).
- check_one_to_one is EXTENDED (signature unchanged from Plan 02); the one-to-one mapping gate blocks all three collision shapes even at high confidence.
- The clarification Message-ID is anchored on email_messages(direction='outbound', run_id), NOT a payroll_runs column (FIX 3); the awaiting_reply advance goes through repo.set_status (the sole status writer — FIX B).
- The gate-block hero fixture replays end-to-end on TWO distinct mocks (reconcile llm_typo→David Reyes @0.6 + decision process — FIX 8) → final_action=request_clarification while model_action=process → awaiting_reply.
- Per-name reconciliation persisted to payroll_runs.reconciliation on the gated branch via the SAME Plan-02 persist_reconciliation call (non-NULL on every run).
</verification>

<success_criteria>
- The model proposes process on the David Reyez sub-0.8 name (matched to the real David Reyes via llm_typo) but the code gate blocks → clarification (success criterion 1, the on-camera money shot on mocks).
- Reconciliation enforces one-to-one mapping (success criterion 3, LLM-09).
- All 4 of this plan's requirement IDs are exercised by green automated tests (LLM-05, LLM-09, CLAR-01, DEMO-01 gate-block half).
</success_criteria>

<output>
Create `.planning/phases/02-walking-skeleton/02-03-SUMMARY.md` when done.
</output>
