---
phase: 02-walking-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/pipeline/extract.py
  - app/pipeline/reconcile_names.py
  - app/pipeline/validate.py
  - app/pipeline/decide.py
  - app/pipeline/calculate.py
  - app/pipeline/orchestrator.py
  - app/llm/prompts/extract.py
  - app/llm/prompts/decide.py
  - app/main.py
  - app/email/clean.py
  - app/db/repo.py
  - fixtures/clean_happy_path.json
  - README.md
  - tests/conftest.py
  - tests/test_webhook.py
  - tests/test_ingest.py
  - tests/test_extract.py
  - tests/test_reconcile.py
  - tests/test_validate.py
  - tests/test_gate.py
  - tests/test_orchestrator_states.py
  - tests/test_persistence.py
  - tests/test_hitl.py
  - tests/test_demo_fixtures.py
autonomous: true
requirements: [INGEST-01, INGEST-02, INGEST-03, INGEST-04, LLM-03, LLM-04, LLM-06, LLM-07, LLM-08, HITL-01, DEMO-01]
nyquist_compliant: true

must_haves:
  truths:
    - "POSTing the clean fixture to /webhook/inbound returns 200 fast, schedules the pipeline as a BackgroundTask (FastAPI BackgroundTasks, in-process, no external queue — D-A1-01), and an explicit orchestrator.py owns the legal status transitions and the two pause points (plain Python state machine, Postgres checkpoint, not LangGraph — D-A1-02); the run reaches awaiting_approval end-to-end (INGEST-01, INGEST-04, HITL-01, DEMO-01 clean half)."
    - "A duplicate POST (same Message-ID) creates no second run, and an unknown sender is logged + stopped with no run created (INGEST-01/FOUND-02, INGEST-03)."
    - "decide.py computes a code-owned final_action that hard-blocks on any missing required field or any per-name confidence < Decimal('0.8') even when model_action=='process'; final_action is the SOLE branch source and the orchestrator never reads model_action (LLM-07, D-A3-01)."
    - "The four judgment stages (extract, reconcile layer-1, validate, decide) are pure importable functions — data in, data out, no run_id, no DB — so the eval can reuse them; extraction may return an optional current-run-only 401k override that never mutates the employee's stored default (LLM-03/04/06/07, D-A3-04, D-14)."
    - "validate.py emits 'missing' for absent required hours (None) plus cross-field sanity issues; a NON-NUMERIC extraction value is NOT a validate.py issue — it manifests as an extraction-stage Pydantic ValidationError → the ONE reflective retry → ERROR if still failing (LLM-06 covered HONESTLY, review FIX 1)."
    - "Computed paystubs show gross + FICA with net labeled pre-federal as a DISPLAY/SERIALIZATION concern (a module constant + README + run rendering, NOT a field on the extra='forbid' PaystubLineItem contract); federal_withholding is Decimal('0') and no fabricated federal figure appears anywhere (success criterion 5, D-A6-01, review FIX 2)."
    - "On EVERY run reaching awaiting_approval (clean path), the Decision contract is persisted to payroll_runs.decision, Extracted to payroll_runs.extracted_data, AND the per-name list[NameMatchResult] to payroll_runs.reconciliation — never NULL on a clean run; an unhandled stage exception routes through repo.record_run_error (writing payroll_runs.error_reason + status ERROR) so the failure reason is persisted, not lost (LLM-08, D-A3-03, D-A3-05, D-A1-03, review FIX 7)."
  artifacts:
    - path: "app/pipeline/decide.py"
      provides: "the code gate: LLM advisory model_action + code-owned final_action; per-name confidence test; missing-field gate; min() confidence collapse; check_one_to_one(matches, extracted) -> list[str] (empty-but-real signature, collisions wired in Plan 03)"
      contains: "Decimal(\"0.8\")"
      min_lines: 40
    - path: "app/pipeline/orchestrator.py"
      provides: "run_pipeline(run_id) state machine; error-wrap via repo.record_run_error (FIX 7); branches SOLELY on final_action; persists decision + extracted + reconciliation on the clean process branch"
      contains: "final_action"
      min_lines: 40
    - path: "app/main.py"
      provides: "FastAPI POST /webhook/inbound (BackgroundTasks) + crude approve/reject re-entry"
      contains: "BackgroundTasks"
      min_lines: 30
    - path: "app/pipeline/calculate.py"
      provides: "thin gross + FICA calc; net labeled pre-federal via a module constant (NOT a PaystubLineItem field — FIX 2)"
      contains: "PRE_FEDERAL"
      min_lines: 20
    - path: "fixtures/clean_happy_path.json"
      provides: "canonical clean InboundEmail fixture, all names deterministic-match, runs to approval"
    - path: "tests/test_persistence.py"
      provides: "test_decision_roundtrip — asserts BOTH decision AND reconciliation round-trip from payroll_runs for a clean run (LLM-08, D-A3-05)"
    - path: "README.md"
      provides: "minimal disclaimer stub: educational model + net is pre-federal in this slice"
      contains: "pre-federal"
  key_links:
    - from: "app/main.py"
      to: "app/pipeline/orchestrator.py run_pipeline"
      via: "background_tasks.add_task"
      pattern: "add_task"
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/decide.py decide"
      via: "branch on Decision.final_action"
      pattern: "final_action"
    - from: "app/pipeline/decide.py"
      to: "NameMatchResult.confidence"
      via: "per-name < 0.8 test"
      pattern: "0.8"
    - from: "app/pipeline/orchestrator.py"
      to: "payroll_runs.reconciliation"
      via: "repo.persist_reconciliation(list[NameMatchResult]) on the clean process branch"
      pattern: "persist_reconciliation"
    - from: "app/pipeline/orchestrator.py"
      to: "payroll_runs.error_reason"
      via: "repo.record_run_error(run_id, reason) on any unhandled stage exception (FIX 7)"
      pattern: "record_run_error"
---

<objective>
Deliver slice (a) per the LOCKED build order D-A5-01: the clean happy path end-to-end. A clean fixture POSTed to the webhook returns 200 fast, schedules the pipeline via BackgroundTasks, flows through the four PURE judgment stages (extract → reconcile layer-1 deterministic → validate → decide+code-gate), runs the thin gross+FICA calc (net "pre-federal"), pauses at `awaiting_approval`, and a crude approve/reject re-entry proves the gate pauses and resumes.

This lands the one-third end-to-end proof and builds the spine: webhook+BackgroundTasks (INGEST-01/02/03), the four stages + the code gate inside decide.py (LLM-03/04/06/07), Decision + Extracted + per-name reconciliation persistence (LLM-08, D-A3-05), the orchestrator state machine + error-on-raise persisted via repo.record_run_error (INGEST-04, D-A1-03), and the `awaiting_approval` operator pause (HITL-01). The gate (decide.py) is the thesis and is exhaustively, deterministically tested with mocked LLM responses.

Purpose: After this plan, a real user can POST a clean payroll email and watch it reach an operator-approval pause with a computed (pre-federal) paystub — something impossible before. Per MVP mode, Task 1 is the failing end-to-end test that defines that capability.
Output: `app/pipeline/{extract,reconcile_names,validate,decide,calculate,orchestrator}.py`, `app/main.py`, the clean fixture, the README stub, and the deterministic test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-walking-skeleton/02-CONTEXT.md
@.planning/phases/02-walking-skeleton/02-RESEARCH.md
@.planning/phases/02-walking-skeleton/02-AI-SPEC.md
@.planning/phases/02-walking-skeleton/02-PATTERNS.md
@.planning/phases/02-walking-skeleton/02-VALIDATION.md
@.planning/phases/02-walking-skeleton/02-01-SUMMARY.md
@app/models/contracts.py
@app/models/roster.py
@app/models/status.py
@app/db/seed.py
</context>

## Phase Goal

**As a** payroll operator, **I want to** POST a clean payroll email and watch it run end-to-end to an approval pause with a computed (pre-federal) paystub, **so that** I can prove the gated judgment spine works before any edge cases.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing end-to-end webhook test + ingest (webhook, BackgroundTasks, dedupe, sender match, body clean)</name>
  <read_first>
    - RESEARCH.md §Pattern 1 lines 267-286 (BackgroundTasks + fast 200; TestClient runs the task SYNCHRONOUSLY so the test asserts the pause with no sleeps); Pitfall 1/2 (JSON-mode trigger; hours None load-bearing)
    - RESEARCH.md §Don't Hand-Roll note on reply parsing — but READ the qualifier: a purpose-built reply-parser (talon / email-reply-parser) is the P6 path for real-client variety; for the SMALL set of authored Phase 2 fixtures a minimal in-house code-strip is sufficient and adds NO new dependency (see action + threat model T-02-10)
    - PATTERNS.md §No Analog (app/main.py — use RESEARCH §Pattern 1) + §`app/db/repo.py` (find_business_by_sender single-row contact_email lookup + insert_inbound_email ON CONFLICT (message_id) DO NOTHING — both committed in Plan 01)
    - app/models/contracts.py lines 30-47 (InboundEmail shape the webhook validates)
    - app/db/seed.py lines 50-69 (businesses + contact_email values; the clean fixture's from_addr must match a seeded contact_email, e.g. payroll@coastalcleaning.example for Business 1)
    - tests/test_seed_roundtrip.py §1 (always-runs in-memory) vs §2 (live-DB two-factor guard) — split pattern to mirror
    - 02-01-SUMMARY.md (repo.insert_inbound_email / find_business_by_sender / create_run signatures; gateway.parse_inbound)
  </read_first>
  <behavior>
    - Test (test_webhook.py::test_post_fixture_reaches_pause): POST fixtures/clean_happy_path.json via TestClient → 200 returned fast; because TestClient runs BackgroundTasks synchronously, immediately after the POST the run is at awaiting_approval (the full happy path). (This test is RED until Tasks 2-4 land — it is the slice's end-to-end definition.)
    - Test (test_webhook.py::test_duplicate_delivery_idempotent): POST the same Message-ID twice → exactly one payroll_runs row (ON CONFLICT (message_id) DO NOTHING).
    - Test (test_webhook.py::test_unknown_sender_no_run): POST a fixture whose from_addr matches no businesses.contact_email → find_business_by_sender returns None → logged + stopped, no run created, 200 returned.
    - Test (test_ingest.py::test_body_cleaned): an inbound body carrying quoted reply history + a signature is stripped before extraction by the in-house code-strip (the buried-reply regression) — no third-party parser involved.
  </behavior>
  <action>
    Create `app/main.py` with `FastAPI()` and `POST /webhook/inbound` per RESEARCH §Pattern 1: validate body into `InboundEmail` via `gateway.parse_inbound`, dedupe via `repo.insert_inbound_email(... ON CONFLICT (message_id) DO NOTHING)` and on conflict return 200 with NO second run (INGEST-01/FOUND-02), match `from_addr` to a business via `repo.find_business_by_sender(from_addr)` (the Plan 01 single-row lookup) and on `None` (unknown sender) log + return 200 with NO run (INGEST-03, never guess), else `repo.create_run(status='received')`, then `background_tasks.add_task(run_pipeline, run_id)` and return 200 fast. Store the inbound row with `message_id`/`in_reply_to`/`references_header` (INGEST-02). For body cleaning (INGEST-02 strip quoted history + signatures), implement a MINIMAL in-house code-strip — NOT a third-party reply-parser and NOT a sprawling hand-rolled engine: a small `clean_body(text) -> str` helper in a NEW `app/email/clean.py` (NOT `app/email/gateway.py`, which Plan 01 owns — keep file ownership disjoint for parallel safety) that strips common quoted-history markers (lines beginning with `>`, an `On <date> ... wrote:` attribution block and everything below it) and a trailing signature delimiter (`-- ` / `Sent from my`), sufficient for the committed Phase 2 fixtures. This adds NO new dependency and removes the package-legitimacy gate; adopting `talon`/`email-reply-parser` for real-client variety is explicitly deferred to P6 (RESEARCH Don't-Hand-Roll qualifier; D-A4-03 fixture-first). Author `fixtures/clean_happy_path.json` as a canonical `InboundEmail` whose `from_addr` matches a seeded business contact_email and whose submitted names are all clean exact/alias matches (e.g. Maria Chen + James Okafor in Business 1), with explicit regular hours. Write tests/test_webhook.py + tests/test_ingest.py; the end-to-end pause assertion relies on TestClient's synchronous BackgroundTasks (no server, no sleeps). Add the conftest mocked-LLM client + fixture loader (extend the Plan 01 conftest).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_webhook.py::test_duplicate_delivery_idempotent tests/test_webhook.py::test_unknown_sender_no_run tests/test_ingest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_webhook.py::test_duplicate_delivery_idempotent -x` passes (per VALIDATION.md INGEST-01/FOUND-02 row).
    - `pytest tests/test_webhook.py::test_unknown_sender_no_run -x` passes (INGEST-03 row) — driven by `repo.find_business_by_sender` returning None.
    - `pytest tests/test_ingest.py::test_body_cleaned -x` passes (INGEST-02 row) — body cleaned by the in-house code-strip.
    - `app/main.py` schedules work via `background_tasks.add_task(run_pipeline, ...)` and returns before the pipeline runs in production (RESEARCH §Pattern 1).
    - `fixtures/clean_happy_path.json` validates as `InboundEmail` and its from_addr equals a seeded `businesses.contact_email`.
    - NO new package is installed — `requirements.txt` is unchanged (the body-clean is in-house code, not talon/email-reply-parser).
    - The end-to-end `test_post_fixture_reaches_pause` exists (may be RED until Task 4) and asserts `awaiting_approval`.
  </acceptance_criteria>
  <done>Webhook ingests + dedupes + sender-matches (via repo.find_business_by_sender) + body-cleans via in-house code-strip (no new dependency); the failing end-to-end pause test exists; idempotency/sender/clean tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The four pure judgment stages — extract, reconcile (layer-1), validate, and the code gate in decide.py</name>
  <read_first>
    - AI-SPEC.md §1 Stage 1/2/3 (extract/reconcile/decide call contracts; tiers; temp 0; reflective retry) + §2 (the code gate rules + confidence collapse min())
    - RESEARCH.md §Pattern 3 lines 325-346 (layer-1 deterministic match: exact/casefold/whitespace/known_aliases → confidence Decimal("1.0"), NO model) + §Pattern 4 lines 348-389 (decide.py: advisory model_action + code-owned final_action; per-name < 0.8 test; min() collapse; check_one_to_one returns gate_reasons)
    - PATTERNS.md §`app/pipeline/decide.py` (Decision contract already exists at contracts.py:104-113 — do NOT redefine; compare with Decimal("0.8") not float; gate on EACH name not the scalar — test_per_name_not_average guards) + §pure stages (purity contract from roster.py:1-11; validate.py mirrors the @model_validator issue-collection style at roster.py:75-107)
    - app/models/contracts.py lines 55-87 (ExtractedEmployee hours are `Decimal | None` with `Field(ge=0)` AND model_config extra="forbid" — CRITICAL for FIX 1: a non-numeric value like "forty" raises Pydantic ValidationError at EXTRACTION parse time, and a negative value fails ge=0 at parse time too; NEITHER reaches validate(extracted). So validate.py's real job is presence/missing on None + cross-field sanity, NOT non_numeric classification) + lines 94-113 (Decision shape)
    - app/models/roster.py (NameMatchResult match_type Literal line 145; ValidationIssue issue_type Literal line 165 — note `non_numeric` exists in the Literal but is produced by the extraction-stage ValidationError path, not by validate())
    - tests/test_models_contracts.py lines 184-198 (test_decision_gate_shape — the gate must produce exactly that shape)
    - 02-01-SUMMARY.md (client.call_structured signature; the reflective-retry-then-raise behavior that the extraction-stage ValidationError feeds — FIX 1)
  </read_first>
  <behavior>
    - Test (test_extract.py): extract(InboundEmail, Roster, *, llm) → Extracted with per-employee entries incl. optional run-only contribution_401k_override; absent hours stay None (never coerced to 0). (Mocked LLM.)
    - Test (test_extract.py::test_non_numeric_hours_routes_to_retry_then_error — FIX 1): a mocked extraction response carrying a NON-NUMERIC hours value (e.g. "forty") makes `Extracted.model_validate_json` raise a Pydantic ValidationError; the client wrapper does its ONE reflective retry; if the retry also returns non-numeric, call_structured raises and the run is destined for ERROR — this is the documented `non_numeric` path (an EXTRACTION-stage parse failure, NOT a validate.py issue). Asserts the create() count is 2 (one retry) and the second failure propagates.
    - Test (test_reconcile.py::test_layer1_deterministic): exact / casefold / whitespace / known-alias names resolve with match_type in {exact,alias}, confidence=Decimal("1.0"), and NO model call (assert the mock client was not invoked for clean names).
    - Test (test_validate.py): validate(Extracted) emits ValidationIssue(issue_type="missing") for a required hours field that is None, plus cross-field sanity issues where applicable — pure, no model. It does NOT (and structurally CANNOT) emit `non_numeric` or `out_of_bounds` for ge=0, because those failed at extraction parse time (FIX 1) — a test asserts validate() over a typed Extracted never produces a non_numeric issue (the typed path can't reach it).
    - Test (test_gate.py::test_sub_threshold_blocks_process): decide() fed a NameMatchResult(confidence=Decimal("0.6")) + model_action="process" → final_action == "request_clarification" (THE THESIS).
    - Test (test_gate.py::test_per_name_not_average): one 0.6 name among three 1.0 names → gate fires (proves per-name test, not the collapsed scalar).
    - Test (test_gate.py::test_check_one_to_one_stub_shape): check_one_to_one(matches, extracted) exists with the FINAL signature, is invoked by decide(), and on a clean (no-collision) clean-fixture input returns an empty list[str] (the real-but-empty function Plan 03 extends with the three collision rules).
    - Test (test_gate.py): a clean run with no LLM-layer names → Decision.confidence collapses to Decimal("1.0") via min(); final_action == model_action == "process".
  </behavior>
  <action>
    Create the four pure stage modules in `app/pipeline/`. Signatures take typed values (`Roster`/`InboundEmail`/`Extracted`), NEVER a `run_id` or a DB connection (the DRY seam, RESEARCH Anti-Pattern 2; roster.py docstring is the binding constraint). `extract.py`: pure `extract(email, roster, *, llm) -> Extracted` via `client.call_structured("extraction", ..., Extracted)`; keep hours `Decimal|None` (RESEARCH Pitfall 2). FIX 1 — RECONCILE THE non_numeric PATH HONESTLY: because `ExtractedEmployee` hours are `Decimal | None` + `ge=0` + extra="forbid" (contracts.py:67-75), a non-numeric extraction value (e.g. "forty") raises Pydantic `ValidationError` INSIDE `call_structured`'s `model_validate_json` — it triggers the ONE reflective retry (Plan 01 client) and, if still failing, propagates as a raise that the orchestrator converts to ERROR (D-A1-03). It NEVER reaches `validate.py`. So `validate.py` is pure `validate(extracted) -> list[ValidationIssue]` mapping an absent required hours field (None) to `issue_type="missing"` (LLM-06) PLUS cross-field sanity (e.g. no hours at all for an employee) — it does NOT attempt to classify `non_numeric` (unreachable through the typed path) or `out_of_bounds` for the ge=0 case (also enforced by Field at parse time). Add a test in test_extract.py asserting the non-numeric input routes to retry-then-error, and a test in test_validate.py asserting validate() over a typed Extracted never yields a non_numeric issue — so LLM-06's `non_numeric` is covered by a real path (the extraction-stage parse failure), not a silent gap. `reconcile_names.py`: implement ONLY layer-1 deterministic match in this plan (exact/casefold/whitespace/`known_aliases` → `NameMatchResult(match_type="exact"|"alias", confidence=Decimal("1.0"))`, NO model call per LLM-04, D-A3-01 layer 1); the layer-2 LLM path is Plan 03 — for now residual names produce an `unknown` result so the clean fixture (all-deterministic) needs no model. `decide.py`: `decide(extracted, matches, issues, *, llm) -> Decision` doing the TWO structurally-separate things — (a) ask the model for advisory `model_action` + reasons via `call_structured("decision", ...)`, and (b) compute the code-owned `final_action` per RESEARCH §Pattern 4: per-name `m.confidence < Decimal("0.8")` test (NOT the collapsed scalar, D-A3-03a), unresolved (`match_type=="unknown"` or `matched_employee_id is None`), missing required field, and the one-to-one mapping check. For the one-to-one check, create a REAL function `def check_one_to_one(matches, extracted) -> list[str]:` with the FINAL signature that returns `[]` (an empty list — NOT a comment, NOT a `pass`/no-op): it is genuinely called inside `decide()` and its returned gate_reasons are unioned into the gate now, even though it detects no collisions yet. The three collision rules (dup name / two→one emp / name→no emp) and the full LLM-09 ownership land in Plan 03 by EXTENDING this existing function — Plan 02 only ships the empty-but-real signature so Plan 03 has a concrete artifact to extend and a stub-shape test pinning it. Collapse `Decision.confidence = min(m.confidence ...) default Decimal("1.0")`. `gate_triggered = (final_action != model_action) or any(gate_reasons)`. Add the extract + decide prompt templates in `app/llm/prompts/` carrying the literal word "json" + an example object shape (RESEARCH Pitfall 1). Write tests/test_extract.py (incl. the FIX-1 non-numeric retry/error test), test_reconcile.py, test_validate.py (incl. the FIX-1 "no non_numeric from validate()" test), test_gate.py — all mocked, deterministic, DB-free; test_gate.py includes test_check_one_to_one_stub_shape asserting the function is present, called, and returns a list.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_gate.py tests/test_extract.py tests/test_reconcile.py tests/test_validate.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_gate.py::test_sub_threshold_blocks_process -x` passes — the gate forces clarify on a sub-0.8 name even when model_action=="process" (VALIDATION.md LLM-07 row, THE THESIS).
    - `pytest tests/test_gate.py::test_per_name_not_average -x` passes (D-A3-03a — gate on each name, not the average).
    - `pytest tests/test_extract.py::test_non_numeric_hours_routes_to_retry_then_error -x` passes — a non-numeric extraction value raises at parse time, triggers the one reflective retry, then propagates to ERROR; `non_numeric` is covered by this extraction-stage path, NOT by validate.py (FIX 1).
    - `pytest tests/test_validate.py -x` passes AND asserts validate() over a typed Extracted never produces a `non_numeric` issue (the typed path structurally can't reach it — FIX 1).
    - `decide.py` contains `def check_one_to_one(` with the final signature returning a list, called inside `decide()` (source assertion: `grep -q "def check_one_to_one(" app/pipeline/decide.py`); `test_gate.py::test_check_one_to_one_stub_shape` passes.
    - `decide.py` compares against `Decimal("0.8")` (source assertion: `grep -q 'Decimal("0.8")' app/pipeline/decide.py`).
    - `pytest tests/test_reconcile.py::test_layer1_deterministic -x` passes and asserts NO model call for clean names (LLM-04 row).
    - The stage signatures take no `run_id`/connection (purity) — `grep -L "run_id" app/pipeline/extract.py app/pipeline/validate.py app/pipeline/reconcile_names.py` confirms they are DB-free pure functions.
    - Absent hours are not coerced to 0 (test_extract asserts None preserved).
  </acceptance_criteria>
  <done>The four pure stages exist; the non_numeric path is reconciled to the extraction-stage retry/error route (validate.py never classifies it — FIX 1); check_one_to_one is a real empty-but-called function with the final signature; the code gate forces clarify on sub-0.8/missing-field even when the model says process; confidence collapses via min(); mocked gate suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Orchestrator state machine + thin gross+FICA calc (net pre-federal as display label) + Decision/Extracted/reconciliation persistence + error_reason on raise</name>
  <read_first>
    - RESEARCH.md §Pattern 4 lines 348-389 (orchestrator branches SOLELY on final_action; NEVER reads model_action) + Architecture diagram lines 194-232 (the run_pipeline flow + the two pauses + error-wrap) + §5 FICA constants (SS 6.2% to $184,500 cap honoring ytd_ss_wages; Medicare 1.45% no cap) — but Phase 2 calc is THIN: gross + FICA only, NO federal
    - AI-SPEC.md §3 G11 (error-wrap: unhandled stage exception → RunStatus.ERROR + reason, nothing hangs) + §0 (persist via model_dump(mode="json")) + §4 item 4 (persist per-name list[NameMatchResult] on EVERY run)
    - PATTERNS.md §`app/pipeline/orchestrator.py` (analog seed.py transaction loop; import RunStatus, use .value; error-wrap new per D-A1-03 — call repo.record_run_error) + §`app/pipeline/calculate.py` (FICA math analog test_seed_roundtrip test_seed_high_earner_ss_cap_straddle)
    - app/models/status.py (RunStatus 11 values — both pauses already exist; no new status needed) + app/models/contracts.py lines 121-153 (PaystubLineItem is extra="forbid" with fields net_pay + federal_withholding — CRITICAL for FIX 2: there is NO net_pay_label field and one CANNOT be added; "pre-federal" must be a DISPLAY/SERIALIZATION concern outside the contract. PaystubLineItem already carries net_pay and federal_withholding=Decimal("0") — that pair IS the pre-federal semantic; the human-readable label lives in a calculate.py module constant + the README + run rendering, not in the contract)
    - app/db/seed.py lines 117-198 (employee comp fields + ytd_ss_wages for the SS-cap honoring; Decimal discipline)
    - 02-01-SUMMARY.md (repo.set_status / persist_extracted / persist_decision / persist_reconciliation / replace_line_items / record_run_error signatures — record_run_error writes payroll_runs.error_reason + status ERROR, committed in Plan 01 per FIX 7)
  </read_first>
  <behavior>
    - Test (test_orchestrator_states.py): with a mocked LLM that yields a clean all-deterministic run, run_pipeline drives received → extracting → ... → computed → awaiting_approval; persists Extracted to extracted_data, Decision to decision, AND the per-name list[NameMatchResult] to reconciliation (NULL reconciliation on a clean run is a FAILURE — D-A3-05).
    - Test (test_orchestrator_states.py::test_stage_raise_sets_error): a stage that raises is caught and routed through `repo.record_run_error(run_id, reason)` which sets the run to RunStatus.ERROR AND persists the reason into payroll_runs.error_reason (no silent hang; the reason is retrievable — D-A1-03, FIX 7).
    - Test (test_orchestrator_states.py): the orchestrator branches on final_action only — it never reads model_action (assert by feeding a Decision where model_action != final_action and checking the branch follows final_action).
    - Test (test_persistence.py::test_decision_roundtrip): a clean run persisted to payroll_runs round-trips BOTH decision AND reconciliation (LLM-08, D-A3-05).
    - Test (calc): thin calc computes gross (hourly×rate, salary÷periods) + FICA (SS 6.2% up to remaining wage-base cap honoring ytd_ss_wages; Medicare 1.45% no cap); net = gross − pretax − FICA with federal_withholding == Decimal("0"); a `PRE_FEDERAL_NET_LABEL` module constant carries the human-readable "pre-federal" label (NOT a PaystubLineItem field — FIX 2); no fabricated federal number anywhere.
  </behavior>
  <action>
    Create `app/pipeline/calculate.py` and `app/pipeline/orchestrator.py`. calculate.py: pure `calculate(resolved_hours, employee) -> PaystubLineItem` — thin gross (hourly×rate or annual÷pay_periods_per_year) + FICA only (SS 6.2% applied to the lesser of period gross and remaining wage base `Decimal("184500") - ytd_ss_wages`, honoring the cap per RESEARCH §5; Medicare 1.45% no cap); set `federal_withholding=Decimal("0")` and compute `net_pay = gross − pretax_401k − fica_ss − fica_medicare`. FIX 2 — the "pre-federal" semantic is the (net_pay, federal_withholding=0) PAIR, and the human-readable label is a DISPLAY/SERIALIZATION concern OUTSIDE the contract: do NOT add a field to `PaystubLineItem` (it is extra="forbid" at contracts.py:130 and adding `net_pay_label` would raise ValidationError). Instead define a module-level `PRE_FEDERAL_NET_LABEL = "Net pay (pre-federal — real federal withholding lands in Phase 3)"` constant in calculate.py that the README and any run rendering reuse, so "net is pre-federal" is asserted everywhere without inventing a contract field. All money is `Decimal`, never float. orchestrator.py: `run_pipeline(run_id)` loads the run + roster via repo, calls the pure stages in order, persists `Extracted` (repo.persist_extracted), `Decision` (repo.persist_decision), AND the full per-name `list[NameMatchResult]` (repo.persist_reconciliation) on EVERY run — including this clean process path — so a clean run reaching awaiting_approval NEVER leaves reconciliation NULL (D-A3-05; Plan 03's clarify branch reuses the SAME already-wired persist_reconciliation call). Then branch SOLELY on `Decision.final_action` (RESEARCH Anti-Pattern; NEVER read model_action): on `process` → calculate + `repo.replace_line_items` + set status `computed` then `awaiting_approval` (HITL-01 pause); on `request_clarification` → set `needs_clarification` (the clarify draft+send lands in Plan 03; for the clean slice this branch is not exercised). Wrap the whole run in try/except → on any unhandled exception call `repo.record_run_error(run_id, reason)` (the Plan 01 helper that writes payroll_runs.error_reason AND sets RunStatus.ERROR — the D-A1-03 persistence home per FIX 7; never swallow, never a bare set_status that loses the reason). Use `RunStatus.<X>.value` via repo, never string literals. Write tests/test_orchestrator_states.py (in-memory mocked LLM for the always-runs assertions; DB round-trips behind @pytest.mark.integration + the two-factor guard) AND tests/test_persistence.py::test_decision_roundtrip asserting BOTH decision AND reconciliation round-trip from payroll_runs for a clean run (behind @pytest.mark.integration + the two-factor guard, mirroring tests/test_seed_roundtrip.py §2).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_orchestrator_states.py tests/test_persistence.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_orchestrator_states.py -x` passes including `test_stage_raise_sets_error` (VALIDATION.md INGEST-04 rows) — a raise routes through `repo.record_run_error` which persists `payroll_runs.error_reason` + sets ERROR (FIX 7; `grep -q "record_run_error" app/pipeline/orchestrator.py`).
    - The orchestrator never reads `model_action` — `grep -c "model_action" app/pipeline/orchestrator.py` returns 0 (it branches only on `final_action`).
    - The orchestrator persists reconciliation on the clean process path — `grep -q "persist_reconciliation" app/pipeline/orchestrator.py` and a clean run's reconciliation is NOT NULL.
    - `pytest tests/test_persistence.py::test_decision_roundtrip` asserts BOTH `decision` AND `reconciliation` round-trip from `payroll_runs` for a clean run (VALIDATION.md LLM-08 row; D-A3-05).
    - calculate.py sets `federal_withholding=Decimal("0")` and the pre-federal label is a module constant, NOT a PaystubLineItem field — `grep -q "PRE_FEDERAL" app/pipeline/calculate.py` and `grep -L "net_pay_label" app/models/contracts.py` (the contract gains NO new field — FIX 2); no fabricated federal value is computed.
    - SS honors the wage-base cap using `ytd_ss_wages` (assertable by a cap-straddle calc test mirroring the seed straddle case).
    - Extracted, Decision, and reconciliation are persisted via `model_dump(mode="json")` (LLM-08, D-A3-05).
  </acceptance_criteria>
  <done>The orchestrator drives the clean run to awaiting_approval branching only on final_action, error-wraps via repo.record_run_error (persisting payroll_runs.error_reason — FIX 7), persists Extracted + Decision + reconciliation (never NULL on a clean run), the thin gross+FICA calc emits net with a pre-federal module-constant label (no contract field added — FIX 2), and the decision+reconciliation round-trip test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Crude approve/reject re-entry (HITL-01), end-to-end demo replay green, README disclaimer stub</name>
  <read_first>
    - RESEARCH.md Architecture diagram lines 220-232 (the awaiting_approval pause + crude approve/reject code/DB re-entry → APPROVED/REJECTED terminal for Phase 2; send is Phase 5) + §Pattern 1 line 286 (TestClient synchronous BackgroundTasks makes the full replay assertable)
    - PATTERNS.md §No Analog (app/main.py approve/reject endpoints — RESEARCH §Pattern 1) + §fixtures (canonical InboundEmail JSON; fixed-UUID discipline)
    - CONTEXT.md D-A6-01 (README stub: ONLY the educational-model statement + the "net is pre-federal in this slice (real federal withholding lands in Phase 3)" note — full README deferred to hosting phase; reuse the calculate.py PRE_FEDERAL_NET_LABEL wording for consistency, FIX 2) + D-A4-03 (fixture replay via POST; the DASH-05 button is Phase 5)
    - app/models/status.py (AWAITING_APPROVAL → APPROVED / REJECTED transitions)
    - 02-01-SUMMARY.md (repo.set_status; the FOR-UPDATE double-approval guard is Phase 5 / FOUND-04 — Phase 2 crude approve only)
  </read_first>
  <behavior>
    - Test (test_hitl.py): a run at awaiting_approval → crude approve sets status APPROVED; reject sets REJECTED (the operator pause proves it pauses and resumes; HITL-01).
    - Test (test_webhook.py::test_post_fixture_reaches_pause): now GREEN — POST fixtures/clean_happy_path.json → run at awaiting_approval (the slice's end-to-end definition from Task 1 passes).
    - Test (test_demo_fixtures.py): the committed clean fixture replays end-to-end via POST and reaches awaiting_approval (DEMO-01 clean half; the gate-block fixture replay lands in Plan 03).
  </behavior>
  <action>
    Add crude approve/reject endpoints to `app/main.py` (e.g. `POST /runs/{run_id}/approve` and `/reject`): load the run, require `awaiting_approval`, set `RunStatus.APPROVED` / `RunStatus.REJECTED` via repo.set_status (crude code/DB re-entry; NO confirmation email / PDF / FOR-UPDATE guard — those are HITL-02/03/FOUND-04 = Phase 5 per CONTEXT). This proves the gate pauses and resumes (HITL-01). Create the minimal `README.md` stub carrying ONLY the two honesty-critical disclaimers (D-A6-01): the "explicitly educational model" statement and the "net is pre-federal in this slice (real federal withholding lands in Phase 3)" note — reuse the calculate.py `PRE_FEDERAL_NET_LABEL` wording so the disclaimer and the computed label agree (FIX 2). The full README (setup/architecture/demo/eval/citations/OBBBA) is deferred. Add tests/test_hitl.py and tests/test_demo_fixtures.py; confirm the Task-1 `test_post_fixture_reaches_pause` end-to-end test is now green (the slice is complete). Run the full mocked suite to confirm no regressions.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_hitl.py tests/test_demo_fixtures.py tests/test_webhook.py::test_post_fixture_reaches_pause -x -q && .venv/bin/python -m pytest -m "not integration and not live_llm" -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_hitl.py -x` passes — approve→APPROVED, reject→REJECTED (VALIDATION.md HITL-01 row).
    - `pytest tests/test_webhook.py::test_post_fixture_reaches_pause -x` is GREEN — the clean fixture reaches awaiting_approval end-to-end via TestClient (INGEST-01/DEMO-01).
    - `pytest tests/test_demo_fixtures.py -x` passes (DEMO-01 clean half).
    - README.md contains the "pre-federal" disclaimer and the educational-model statement (D-A6-01), matching the calculate.py PRE_FEDERAL_NET_LABEL wording (FIX 2).
    - The full mocked suite (`-m "not integration and not live_llm"`) is green — no regressions.
  </acceptance_criteria>
  <done>The clean fixture runs end-to-end to an operator-approval pause, crude approve/reject works, the README disclaimer stub exists (matching the pre-federal label constant), and the full mocked suite is green — slice (a) complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /webhook/inbound | Untrusted inbound email payload crosses into the app; validated by Pydantic InboundEmail (extra="forbid"). |
| email body → extraction LLM | Attacker-controlled prose ("pay everyone $999/hr"); the code gate + deterministic validation is the structural defense. |
| sender → business | from_addr must match a known businesses.contact_email (repo.find_business_by_sender); unknown senders are stopped, never guessed (access control). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Spoofing | /webhook/inbound sender | mitigate | INGEST-03 sender access control: from_addr matched to businesses.contact_email via repo.find_business_by_sender; unknown sender (None) logged + stopped, no run created (test_unknown_sender_no_run). |
| T-02-07 | Tampering | webhook payload | mitigate | Pydantic `extra="forbid"` on InboundEmail rejects unexpected fields at the boundary. |
| T-02-08 | Elevation of privilege | email body → process decision (prompt injection) | mitigate | The code gate in decide.py is the structural defense — a model talked into "process" is still code-blocked on sub-0.8/missing-field; final_action is the SOLE branch source (LLM-07, G7). |
| T-02-09 | Denial of service | duplicate webhook delivery | mitigate | UNIQUE(message_id) + ON CONFLICT DO NOTHING → no second run (INGEST-01/FOUND-02; test_duplicate_delivery_idempotent). |
| T-02-10 | Tampering | inbound body cleaning (INGEST-02) | mitigate | Quoted-history/signature stripping via a MINIMAL in-house code-strip (`clean_body`) sufficient for the committed Phase 2 fixtures, so a signature name is never pulled in as an employee. NO third-party parser and NO new dependency in Phase 2 → no package-legitimacy gate. A purpose-built reply-parser is deferred to P6 and would re-introduce the legitimacy gate at that point. |
| T-02-11 | Information disclosure | pipeline logging / error_reason | mitigate | No full email bodies at info level; log run_id/status/Message-IDs only; payroll_runs.error_reason stores a stage/exception summary, not raw PII bodies (PII-safe logging; FIX 7). |
| T-02-13 | Denial of service | malformed/non-numeric extraction | mitigate | A non-numeric/negative hours value raises a Pydantic ValidationError at extraction parse time → ONE reflective retry → ERROR with reason persisted via repo.record_run_error; the run never hangs and never reaches the calc with bad data (FIX 1 + FIX 7). |

> **Package legitimacy:** This plan installs NO new third-party package (the INGEST-02 body-clean is in-house code, not talon/email-reply-parser). `requirements.txt` is unchanged from Plan 01's pre-vetted, pinned set. There is therefore NO package-legitimacy checkpoint in this autonomous plan. If a purpose-built reply-parser is adopted in P6 for real-client variety, that future install MUST go through the full legitimacy gate + a blocking-human checkpoint at that time (it is out of scope here).
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m "not integration and not live_llm" -q` is green (full mocked suite, no regressions).
- The clean fixture reaches `awaiting_approval` end-to-end via TestClient.
- decide.py gates on `Decimal("0.8")` per-name; `check_one_to_one` is a real empty-but-called function; orchestrator never reads `model_action`.
- The non_numeric path is the extraction-stage retry/error route (validate.py never classifies it — FIX 1).
- net is labeled pre-federal via a calculate.py module constant (no PaystubLineItem field added — FIX 2); no fabricated federal figure anywhere.
- A stage raise is routed through repo.record_run_error, persisting payroll_runs.error_reason (FIX 7) — the reason is never lost.
- Decision + Extracted + per-name reconciliation persisted for audit/eval on EVERY run (clean run reconciliation never NULL — D-A3-05).
- NO new package installed; INGEST-02 body-clean is in-house code.
</verification>

<success_criteria>
- POST a clean fixture → 200 fast → BackgroundTask → four pure stages → code-gated decision → thin gross+FICA calc (net pre-federal label) → awaiting_approval → crude approve/reject (success criteria 1, 2, 5 for the clean path).
- The code gate is the SOLE branch source and is exhaustively tested deterministically (LLM-07).
- All 11 of this plan's requirement IDs are exercised by a green automated test (INGEST-01/02/03/04, LLM-03/04/06/07/08, HITL-01, DEMO-01 clean half).
</success_criteria>

<output>
Create `.planning/phases/02-walking-skeleton/02-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
