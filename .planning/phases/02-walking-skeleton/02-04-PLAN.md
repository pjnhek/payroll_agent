---
phase: 02-walking-skeleton
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - app/email/gateway.py
  - app/db/repo.py
  - app/main.py
  - app/pipeline/orchestrator.py
  - fixtures/clarify_reply.json
  - tests/test_threading.py
  - tests/test_live_llm.py
autonomous: false
requirements: [CLAR-02, CLAR-03, EMAIL-01]
user_setup:
  - service: deepseek
    why: "Live hero-fixture run (D-A4-01a) hits the real DeepSeek extraction model"
    env_vars:
      - name: EXTRACTION_API_KEY
        source: "DeepSeek console -> API keys (confirm deepseek-v4-flash ID + non-thinking param)"
  - service: moonshot
    why: "Live hero-fixture run hits the real Kimi/Moonshot reconcile + decision model"
    env_vars:
      - name: DECISION_API_KEY
        source: "Kimi/Moonshot console -> API keys (confirm moonshot-v1-* non-reasoning ID)"
nyquist_compliant: true

must_haves:
  truths:
    - "A fixture reply POSTed to the same inbound webhook is routed to its run via the RFC In-Reply-To/References header chain (subject/provider-thread are fallbacks only), matching only a run in awaiting_reply (CLAR-02, D-A4-02)."
    - "A matched reply re-enters the pipeline at extraction idempotently: extracted_data is overwritten (not appended), line items are replaced by run, and a header match to an already-sent/reconciled run is logged as a late reply and NOT resumed (CLAR-03, D-A4-02)."
    - "The stub gateway supports injecting a fixture reply with zero real email, completing the clarify -> reply -> resume loop (EMAIL-01)."
    - "The live hero-fixture run is validated as a DISTINCT exit gate: the real configured DeepSeek/Kimi models return process + sub-0.8 confidence for David Reyez so the code gate genuinely fires on real output (D-A4-01a) — a manual, human-judged step, not an autonomous pass/fail."
  artifacts:
    - path: "app/db/repo.py"
      provides: "header-chain run lookup (parameterized LIKE on references; awaiting_reply only)"
      contains: "in_reply_to"
      min_lines: 80
    - path: "fixtures/clarify_reply.json"
      provides: "reply fixture whose in_reply_to points at the clarification's synthetic Message-ID"
      contains: "in_reply_to"
    - path: "tests/test_threading.py"
      provides: "header-chain match + idempotent resume + late-reply-not-resumed tests"
      min_lines: 40
    - path: "tests/test_live_llm.py"
      provides: "env-gated two-factor live hero test asserting model says process + gate blocks on sub-0.8"
      min_lines: 30
  key_links:
    - from: "app/main.py /webhook/inbound"
      to: "app/db/repo.py header-chain lookup"
      via: "reply routing on In-Reply-To/References"
      pattern: "in_reply_to"
    - from: "app/db/repo.py"
      to: "payroll_runs.status = 'awaiting_reply'"
      via: "match only awaiting_reply runs; late reply logged not resumed"
      pattern: "awaiting_reply"
---

<objective>
Deliver slice (c) per the LOCKED build order D-A5-01 (LAST, because CLAR-03 re-entrancy is the trickiest sub-piece): the clarify → reply → resume loop. A fixture reply POSTed to the same inbound webhook routes to its run via the RFC In-Reply-To/References header chain, re-enters the pipeline at extraction idempotently, and a late reply to an already-resolved run is logged but not resumed. Then validate the live hero-fixture run as a distinct Phase 2 exit gate (D-A4-01a) — the mocked suite passing is necessary but NOT sufficient.

This closes the walking skeleton: both pause states (`awaiting_reply`, `awaiting_approval`) are now fully wired and exercisable end-to-end with zero real email, and the on-camera demo narrative is proven against the REAL models, not just a recording.

Purpose: After this plan, a real user can run the entire clarify→reply→resume loop on fixtures, and the hero gate-block has been proven to fire on live model output. This is the final new capability that makes Phase 2 callable "done" per the exit criteria.
Output: header-chain routing in repo + main, idempotent re-entry in the orchestrator, the reply fixture, the threading test suite, and the env-gated live hero test + a blocking manual exit-gate checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-walking-skeleton/02-CONTEXT.md
@.planning/phases/02-walking-skeleton/02-RESEARCH.md
@.planning/phases/02-walking-skeleton/02-AI-SPEC.md
@.planning/phases/02-walking-skeleton/02-PATTERNS.md
@.planning/phases/02-walking-skeleton/02-VALIDATION.md
@.planning/phases/02-walking-skeleton/02-01-SUMMARY.md
@.planning/phases/02-walking-skeleton/02-02-SUMMARY.md
@.planning/phases/02-walking-skeleton/02-03-SUMMARY.md
@app/models/contracts.py
@app/models/status.py
@app/db/seed.py
</context>

## Phase Goal

**As a** payroll operator, **I want to** have a client's reply automatically routed back to its paused run and resumed from extraction, **so that** a clarification round-trip completes without manual re-keying — and I want the hero gate-block proven on the real models before the demo.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header-chain reply routing (CLAR-02) + idempotent re-entry at extraction (CLAR-03)</name>
  <read_first>
    - RESEARCH.md §Pattern 6 lines 404-421 (header-chain lookup: scan stored outbound Message-ID against reply In-Reply-To AND the full References chain; subject is fallback only; parameterized LIKE; match ONLY status='awaiting_reply') + Idempotency invariants 1-5 (overwrite extracted_data, replace-by-run line items, late reply to sent/reconciled = log not resume, email_messages append-only) + Pitfall 4 (subject collisions → wrong-run resumption)
    - AI-SPEC.md §1 Stage 1 (re-extraction overwrites; idempotent re-entry) + D-A4-02 (reply POSTed to the SAME inbound webhook; header chain primary)
    - PATTERNS.md §`app/db/repo.py` (threading header-chain lookup — references LIKE %(references)s MUST be parameterized, NEVER interpolated; match only awaiting_reply; late reply logged not resumed) + Shared Patterns "Parameterized SQL only"
    - app/models/contracts.py lines 30-47 (InboundEmail in_reply_to / references_header fields) + app/models/status.py (AWAITING_REPLY vs sent/reconciled/rejected)
    - app/main.py (the Plan 02 /webhook/inbound — extend to detect a reply and route via repo) + app/pipeline/orchestrator.py (the resume re-entry point at extraction)
    - 02-01-SUMMARY.md + 02-03-SUMMARY.md (repo.insert_inbound_email, the outbound Message-ID stored on the run, persist_extracted/replace_line_items)
  </read_first>
  <behavior>
    - Test (test_threading.py::test_header_chain_match): a reply whose in_reply_to equals a stored outbound Message-ID routes to that run; a reply matching via the References chain also routes; subject-only is a fallback (lower priority); the SQL LIKE is parameterized.
    - Test (test_threading.py::test_idempotent_resume): a matched reply to an awaiting_reply run re-enters extraction, OVERWRITES extracted_data (not appended), and replaces line items by run (DELETE-by-run then insert).
    - Test (test_threading.py): a header match to a run in sent/reconciled/rejected/computed is logged as a late reply and NOT resumed (only awaiting_reply runs resume).
    - Test: re-running resume twice yields the same final state (idempotent re-entrancy).
  </behavior>
  <action>
    Add a header-chain lookup to `app/db/repo.py` per RESEARCH §Pattern 6: `find_run_for_reply(in_reply_to, references_header) -> run_id | None` that JOINs payroll_runs to outbound email_messages and matches `em.message_id = %(in_reply_to)s OR %(references)s LIKE '%%' || em.message_id || '%%'`, restricted to `pr.status = 'awaiting_reply'` (the LIKE MUST be parameterized with named placeholders, NEVER interpolated — SQL-injection defense, PATTERNS Shared Patterns). Subject matching is a fallback only (lower priority). In `app/main.py /webhook/inbound`: after dedupe, if the inbound carries an in_reply_to/references, call `find_run_for_reply`; on a match to an awaiting_reply run, set status `EXTRACTING` and `background_tasks.add_task(resume_pipeline, run_id, inbound)`; on a header match to any other status, log a late-reply (do NOT resume) per invariant 4. Add `resume_pipeline(run_id, inbound)` (or a resume flag on run_pipeline) to `app/pipeline/orchestrator.py` that re-enters at extraction idempotently: `repo.persist_extracted` OVERWRITES extracted_data (single JSONB cell, never append), `repo.replace_line_items` does DELETE-by-run then insert (invariants 1-2), and the rest of the pipeline proceeds as normal. email_messages stays append-only (audit). Write tests/test_threading.py (mocked LLM; DB round-trips behind @pytest.mark.integration + two-factor guard; the header-chain match logic asserted with in-memory/parameterized-SQL checks where possible).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_threading.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_threading.py::test_header_chain_match -x` passes — routes via In-Reply-To and References; subject is fallback only (VALIDATION.md CLAR-02 row).
    - `pytest tests/test_threading.py::test_idempotent_resume -x` passes — re-entry overwrites extracted_data + replaces line items; late reply to a resolved run logged not resumed (CLAR-03 row).
    - The references LIKE query is parameterized — `grep -nE "references.*LIKE" app/db/repo.py` shows a named placeholder, and no Message-ID is f-string-interpolated into SQL (T-injection).
    - The lookup is restricted to `status='awaiting_reply'` (source assertion).
    - Resume is idempotent (running it twice yields the same final state).
  </acceptance_criteria>
  <done>A reply routes to its run via the header chain (awaiting_reply only), re-enters extraction idempotently, and a late reply is logged not resumed; parameterized SQL; mocked tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reply-fixture injection completing the clarify->reply->resume loop (EMAIL-01)</name>
  <read_first>
    - CONTEXT.md D-A4-02 (the fixture reply is POSTed to the same inbound webhook with In-Reply-To/References pointing at the clarification's stored outbound Message-ID) + D-A4-03 (fixture replay via POST; zero real email)
    - RESEARCH.md §Pattern 5 (EMAIL-01 stub supports injecting a fixture reply) + §Pattern 6 invariants
    - PATTERNS.md §fixtures (canonical InboundEmail JSON; the reply fixture's in_reply_to points at the clarification's synthetic Message-ID — but synthetic IDs are uuid-random, so the test must wire the reply's in_reply_to to the actual sent Message-ID at runtime, OR the fixture carries a placeholder the test substitutes)
    - app/models/contracts.py (InboundEmail in_reply_to/references_header/message_id) + 02-03-SUMMARY.md (gateway.send_outbound returns the clarification Message-ID; the gate-block hero fixture from Plan 03)
    - tests/test_demo_fixtures.py + tests/test_threading.py (the gate-block run that reaches awaiting_reply — chain the reply onto it)
  </read_first>
  <behavior>
    - Test (test_threading.py, end-to-end): run the gate-block hero fixture (mocked) to awaiting_reply, capture the clarification's synthetic Message-ID, substitute it into the reply payload, ASSERT the substitution succeeded (the reply's in_reply_to == the captured clarification Message-ID) BEFORE POSTing, then POST fixtures/clarify_reply.json → the run resumes at extraction and (with a now-resolved mocked reconcile) proceeds to a computed/awaiting_approval state — the full clarify→reply→resume loop with zero real email (EMAIL-01).
    - Test: a reply whose in_reply_to matches no outbound Message-ID creates/handles a no-match gracefully (logged, no wrong-run resume).
  </behavior>
  <action>
    Author `fixtures/clarify_reply.json` as a canonical reply `InboundEmail`: a new message_id, an `in_reply_to`/`references_header` slot that points at the clarification's outbound Message-ID (because synthetic IDs are uuid-random, carry a documented placeholder token the test substitutes with the actual sent Message-ID at runtime, per PATTERNS §fixtures), and a corrected body that resolves the David Reyez name (e.g. the client confirms the correct spelling) so the resumed run can proceed. Extend `tests/test_threading.py` (or test_demo_fixtures.py) with the full loop: drive the gate-block fixture to awaiting_reply (mocked), read back the stored clarification Message-ID, substitute it into the reply payload, and — BEFORE the reply POST — assert the substitution actually took (the reply's `in_reply_to` equals the captured clarification Message-ID and the placeholder token is gone), so a broken substitution fails LOUDLY here rather than silently routing to the graceful no-match branch and letting the resume test pass without resuming. Then inject the reply fixture via the webhook and assert the run resumes at extraction and advances (EMAIL-01 — the loop is exercisable with zero real email). Run the full mocked suite to confirm no regressions.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_threading.py -x -q && .venv/bin/python -m pytest -m "not integration and not live_llm" -q</automated>
  </verify>
  <acceptance_criteria>
    - The full clarify→reply→resume loop is exercised with zero real email: gate-block fixture → awaiting_reply → injected reply (in_reply_to = the clarification Message-ID) → resume at extraction → advance (VALIDATION.md EMAIL-01/CLAR-03 rows).
    - BEFORE the reply POST, the test asserts the reply's `in_reply_to` equals the captured clarification Message-ID (and the placeholder token is no longer present), so a broken Message-ID substitution fails loudly at that assertion instead of silently passing via the no-match branch (WARNING 8 — substitution must be verified, not assumed).
    - `fixtures/clarify_reply.json` validates as InboundEmail and carries an in_reply_to slot (`grep -q "in_reply_to" fixtures/clarify_reply.json`).
    - A reply with no matching outbound Message-ID is handled gracefully (no wrong-run resume) — and this branch is NOT how the resume test passes (the pre-POST in_reply_to assertion guarantees the resume path is the one exercised).
    - The full mocked suite (`-m "not integration and not live_llm"`) is green — no regressions.
  </acceptance_criteria>
  <done>The reply fixture completes the clarify→reply→resume loop with zero real email; the Message-ID substitution is asserted before the POST so a broken substitution fails loudly (not via the no-match path); the run resumes at extraction and advances; mocked suite green — slice (c) complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live hero-fixture exit gate (D-A4-01a) — real models say process AND code gate blocks on sub-0.8</name>
  <read_first>
    - CONTEXT.md D-A4-01a (CRITICAL: the hero fixture must be validated LIVE, not just against a mock; a one-time live run against real DeepSeek/Kimi is an explicit Phase 2 exit criterion; budget a tuning loop on the submitted-name variant + reconcile prompt) + D-A5-01 (the live run is a distinct exit gate; the mocked suite passing is necessary but not sufficient)
    - VALIDATION.md §Manual-Only Verifications (live hero-fixture run: validates a model-behavior + demo-narrative property no test can assert; tune the prompt/fixture until it genuinely produces "model willing, code said no") + §The mock/live distinction
    - PATTERNS.md §`tests/test_live_llm.py` (EXACT analog tests/test_seed_roundtrip.py §2 two-factor guard: _HAS_LLM_KEYS + ALLOW_LIVE_LLM; @pytest.mark.live_llm; assert model_action=="process" AND final_action=="request_clarification" AND a per-name confidence < 0.8)
    - app/models/contracts.py (Decision is `extra="forbid"` at line 104 — the live-vs-mock marker CANNOT be a key inside the Decision JSONB; it must be a log field or a separate column)
    - STATE.md Blockers (confirm exact DeepSeek/Kimi model IDs + the non-thinking request param from the consoles BEFORE the live run — open blocker) + memory payroll-agent-provider-ids-unconfirmed
    - AI-SPEC.md §7 (the reconcile prompt is the tuning surface for the hero fixture)
  </read_first>
  <what-built>
    The env-gated live test `tests/test_live_llm.py::test_hero_fixture_live` (mirroring the Phase 1 two-factor live-DB guard verbatim: `_HAS_LLM_KEYS = bool(EXTRACTION_API_KEY and DECISION_API_KEY)`, `_LIVE_LLM = os.environ.get("ALLOW_LIVE_LLM") == "1"`, `@pytest.mark.live_llm`) runs the David Reyez hero fixture through the REAL configured DeepSeek/Kimi models and asserts the model genuinely returns `model_action=="process"` AND `final_action=="request_clarification"` AND a per-name confidence `< Decimal("0.8")`. A live-vs-mock run marker records that this decision came from a live model call, not a recording (NICE-TO-HAVE per D-A2-01). Because the `Decision` contract is `extra="forbid"` (contracts.py:104), the marker MUST NOT be a key inside the Decision JSONB object — it is constrained to either a structured log field or a separate column/run-detail field outside the Decision object (Claude's discretion between those two, but NOT inside Decision). The mocked suite is already green (Tasks 1-2 + Plans 01-03); this gate proves the DEMO, which the mock cannot.
  </what-built>
  <how-to-verify>
    1. Confirm the exact DeepSeek/Kimi non-reasoning model IDs + the DeepSeek non-thinking request parameter from the provider consoles and pin them in `.env` (STATE.md open blocker; legacy deepseek-chat/deepseek-reasoner retire 2026/07/24 — never alias to them).
    2. Optionally run the live provider smoke-test first (one tiny temperature=0, max_tokens=5 call per tier) to confirm creds + IDs resolve + non-thinking mode is accepted.
    3. Run: `ALLOW_LIVE_LLM=1 EXTRACTION_API_KEY=... DECISION_API_KEY=... .venv/bin/python -m pytest tests/test_live_llm.py::test_hero_fixture_live -m live_llm -x`.
    4. EXPECTED: the real model returns `process` for David Reyez at a sub-0.8 confidence so the code gate fires → `final_action=="request_clarification"`. This is "the model was willing; the code said no" on REAL output.
    5. Confirm the live-vs-mock marker is recorded OUTSIDE the Decision object (a log line and/or a separate column/run field) — verify it does NOT attempt to add a key into the `extra="forbid"` Decision JSONB (which would raise ValidationError).
    6. IF the model self-clarifies (proposes request_clarification on its own → gate never fires) OR returns confidence ≥0.8 (mismatch would process): TUNE the submitted-name variant (`David Reyez`) and/or the reconcile prompt (the tuning surface, AI-SPEC §7) and repeat until the live run genuinely produces model-says-process AND gate-blocks-on-sub-0.8. This is a human-judgment loop, not an automated pass/fail.
    7. For the actual recording, capture the exact good run (hosted APIs are not bit-deterministic even at temp 0).
  </how-to-verify>
  <resume-signal>Type "approved" once the live run genuinely produces model-says-process AND gate-blocks-on-sub-0.8 (tune + repeat first if it self-clarifies or returns ≥0.8) and the live-vs-mock marker is recorded outside the Decision object, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reply POST → webhook | Untrusted reply payload carries In-Reply-To/References used in a DB lookup; must be parameterized. |
| header chain → run resume | A header match must not resume a wrong or already-resolved run; only awaiting_reply runs resume. |
| real LLM provider → live decision | The live hero run depends on real model behavior; the code gate remains the trust enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | header-chain references LIKE query | mitigate | Parameterized named placeholders only — `references LIKE %(references)s`; never interpolate a Message-ID into SQL (PATTERNS Shared Patterns; RESEARCH Security Domain V5). |
| T-04-02 | Spoofing | reply routed to wrong run | mitigate | Match only `status='awaiting_reply'`; a header match to any other status is logged as a late reply, not resumed (CLAR-03 invariant 4); subject is fallback only to avoid collisions (Pitfall 4). |
| T-04-03 | Tampering | re-extraction across resumes | mitigate | extracted_data overwritten wholesale (never appended); line items replaced by run (DELETE-by-run then insert) — idempotent re-entrancy (CLAR-03 invariants 1-2). |
| T-04-04 | Repudiation | reply / resume audit | mitigate | email_messages is append-only; every inbound reply recorded with its Message-ID + headers (audit trail). |
| T-04-05 | Information disclosure | live-LLM creds / logging | mitigate | API keys read from env, never logged; no full email bodies at info level; the live-vs-mock marker (a log field / separate column, NEVER inside the extra="forbid" Decision object) records provenance, not secrets. |
| T-04-SC | Tampering | package installs | mitigate | No new package names this plan (reuses Plan 01-03 deps); no install task. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m "not integration and not live_llm" -q` is green (full mocked suite).
- Reply routes via header chain (awaiting_reply only); references LIKE is parameterized.
- Re-entry is idempotent (overwrite extracted_data, replace line items); late reply logged not resumed.
- The clarify→reply→resume loop runs end-to-end with zero real email; the reply's in_reply_to substitution is asserted before the POST.
- The live-vs-mock marker is a log field / separate column — NOT a key inside the extra="forbid" Decision object.
- EXIT GATE (manual): the live hero run produces model-says-process AND gate-blocks-on-sub-0.8 (D-A4-01a).
</verification>

<success_criteria>
- A clarify run reaches awaiting_reply; an injected fixture reply routes back via In-Reply-To/References and resumes at extraction idempotently (success criterion 4, CLAR-02/03, EMAIL-01).
- The live hero-fixture exit gate is satisfied: the real models genuinely produce the "model willing, code said no" narrative (D-A4-01a) — the distinct exit gate beyond the mocked suite.
- All 3 of this plan's requirement IDs are exercised by green automated tests, plus the manual live exit gate (CLAR-02, CLAR-03, EMAIL-01).
</success_criteria>

<output>
Create `.planning/phases/02-walking-skeleton/02-04-SUMMARY.md` when done.
</output>
