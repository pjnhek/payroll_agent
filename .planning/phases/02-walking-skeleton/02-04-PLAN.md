---
phase: 02-walking-skeleton
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - app/email/gateway.py
  - app/db/repo.py
  - app/main.py
  - app/pipeline/orchestrator.py
  - fixtures/clarify_reply.json
  - tests/test_threading.py
  - tests/test_live_llm.py
autonomous: false
requirements: [CLAR-02, CLAR-03, EMAIL-01]
user_setup:
  - service: deepseek
    why: "Live hero-fixture run (D-A4-01a) hits the real DeepSeek extraction model"
    env_vars:
      - name: EXTRACTION_API_KEY
        source: "DeepSeek console -> API keys (confirm deepseek-v4-flash ID + non-thinking param)"
  - service: moonshot
    why: "Live hero-fixture run hits the real Kimi/Moonshot reconcile + decision model"
    env_vars:
      - name: DECISION_API_KEY
        source: "Kimi/Moonshot console -> API keys (confirm moonshot-v1-* non-reasoning ID)"
nyquist_compliant: true

must_haves:
  truths:
    - "A fixture reply POSTed to the same inbound webhook is routed to its run via the RFC In-Reply-To/References header chain (subject/provider-thread are fallbacks only), matching only a run in awaiting_reply via repo.find_awaiting_reply_for_header; a header match to a run in any OTHER status is detected via repo.find_any_run_for_header and logged as a late reply, not resumed (CLAR-02, CLAR-03, D-A4-02, review FIX 10)."
    - "After the header match, the reply's sender is RE-ASSERTED against the matched run's business (the original inbound sender / businesses.contact_email): a sender mismatch is logged and NOT resumed, so a spoofed reply on a guessed/leaked Message-ID cannot bypass INGEST-03 (review FIX 5 — safety)."
    - "A matched reply re-enters the pipeline at extraction idempotently using (original cleaned inbound body from repo.load_source_email + the clarification reply body) as the extraction context, then OVERWRITES extracted_data with the full re-extracted result — so a partial reply (answer only) does NOT lose the original employees'/hours; line items are replaced by run (CLAR-03 idempotent overwrite preserved — it is WHAT we extract from that changes, review FIX 4). The resume re-extraction passes the run's code-owned run_id into extract() so the re-built Extracted carries the correct run_id (review FIX A — the LLM returns an ExtractionPayload, code stamps run_id)."
    - "The stub gateway supports injecting a fixture reply with zero real email, completing the clarify -> reply -> resume loop, with the reply's in_reply_to wired to the clarification's Message-ID read back from the linked outbound email_messages row (EMAIL-01, FIX 3 anchor)."
    - "The live hero-fixture run is validated as a DISTINCT exit gate: the real configured DeepSeek/Kimi models return process + sub-0.8 confidence for David Reyez so the code gate genuinely fires (D-A4-01a) — a manual, human-judged step. A live-vs-mock run marker (a structured log field, explicitly NOT a key in the extra='forbid' Decision object) records provenance (NICE-TO-HAVE per D-A2-01, review FIX 12)."
  artifacts:
    - path: "app/db/repo.py"
      provides: "uses the Plan-01 header-chain lookups (find_awaiting_reply_for_header for resume + find_any_run_for_header for late-reply observability — FIX 10); load_source_email returns the cleaned inbound body (FIX C); any new query is parameterized LIKE on references"
      contains: "find_awaiting_reply_for_header"
      min_lines: 80
    - path: "app/pipeline/orchestrator.py"
      provides: "resume_pipeline re-entering at extraction over (original inbound body + reply body) context, passing the code-owned run_id into extract() (FIX A), overwriting extracted_data (FIX 4)"
      contains: "resume"
      min_lines: 40
    - path: "fixtures/clarify_reply.json"
      provides: "reply fixture whose in_reply_to points at the clarification's synthetic Message-ID (read back from the outbound email_messages row)"
      contains: "in_reply_to"
    - path: "tests/test_threading.py"
      provides: "header-chain match + sender-revalidation + partial-reply-preserves-hours + idempotent resume + late-reply-not-resumed tests"
      min_lines: 50
    - path: "tests/test_live_llm.py"
      provides: "env-gated two-factor live hero test asserting model says process + gate blocks on sub-0.8"
      min_lines: 30
  key_links:
    - from: "app/main.py /webhook/inbound"
      to: "app/db/repo.py find_awaiting_reply_for_header + find_any_run_for_header"
      via: "reply routing on In-Reply-To/References (resume vs late-reply log)"
      pattern: "find_awaiting_reply_for_header"
    - from: "app/main.py reply routing"
      to: "matched run's business contact_email"
      via: "reply.from_addr revalidation before resume (INGEST-03 on replies)"
      pattern: "from_addr"
    - from: "app/pipeline/orchestrator.py resume_pipeline"
      to: "repo.load_source_email (original cleaned inbound body) + extract(run_id=run_id)"
      via: "re-extract over (original body + reply body), passing the code-owned run_id, overwrite extracted_data"
      pattern: "load_source_email"
---

<objective>
Deliver slice (c) per the LOCKED build order D-A5-01 (LAST, because CLAR-03 re-entrancy is the trickiest sub-piece): the clarify → reply → resume loop. A fixture reply POSTed to the same inbound webhook routes to its run via the RFC In-Reply-To/References header chain, the reply sender is re-asserted against the matched run's business (so a spoofed reply cannot bypass INGEST-03), the run re-enters the pipeline at extraction idempotently over (original cleaned inbound body + reply body) so a partial reply never loses original hours, and a late reply to an already-resolved run is logged but not resumed. Then validate the live hero-fixture run as a distinct Phase 2 exit gate (D-A4-01a) — the mocked suite passing is necessary but NOT sufficient.

This closes the walking skeleton: both pause states (`awaiting_reply`, `awaiting_approval`) are now fully wired and exercisable end-to-end with zero real email, the resume loop is safe (sender-revalidated) and lossless (re-extracts over original+reply), and the on-camera demo narrative is proven against the REAL models, not just a recording.

Purpose: After this plan, a real user can run the entire clarify→reply→resume loop on fixtures safely and losslessly, and the hero gate-block has been proven to fire on live model output. This is the final new capability that makes Phase 2 callable "done" per the exit criteria.
Output: header-chain routing (resume + late-reply lookups) + reply sender revalidation in repo/main, idempotent re-entry over original+reply context in the orchestrator (passing the code-owned run_id into extract per FIX A), the reply fixture, the threading test suite, and the env-gated live hero test + a blocking manual exit-gate checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-walking-skeleton/02-CONTEXT.md
@.planning/phases/02-walking-skeleton/02-RESEARCH.md
@.planning/phases/02-walking-skeleton/02-AI-SPEC.md
@.planning/phases/02-walking-skeleton/02-PATTERNS.md
@.planning/phases/02-walking-skeleton/02-VALIDATION.md
@.planning/phases/02-walking-skeleton/02-01-SUMMARY.md
@.planning/phases/02-walking-skeleton/02-02-SUMMARY.md
@.planning/phases/02-walking-skeleton/02-03-SUMMARY.md
@app/models/contracts.py
@app/models/status.py
@app/db/seed.py
</context>

## Phase Goal

**As a** payroll operator, **I want to** have a client's reply automatically routed back to its paused run and resumed from extraction without losing the original hours and without a spoofed reply slipping through, **so that** a clarification round-trip completes safely without manual re-keying — and I want the hero gate-block proven on the real models before the demo.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header-chain reply routing (CLAR-02) + reply sender revalidation (FIX 5) + idempotent re-entry over original+reply context (CLAR-03, FIX 4)</name>
  <read_first>
    - RESEARCH.md §Pattern 6 lines 404-421 (header-chain lookup: scan stored outbound Message-ID against reply In-Reply-To AND the full References chain; subject is fallback only; parameterized LIKE; match ONLY status='awaiting_reply') + Idempotency invariants 1-5 (overwrite extracted_data, replace-by-run line items, late reply to sent/reconciled = log not resume, email_messages append-only) + Pitfall 4 (subject collisions → wrong-run resumption)
    - AI-SPEC.md §1 Stage 1 (re-extraction overwrites; idempotent re-entry; the extraction stage takes a code-owned run_id and returns Extracted via an ExtractionPayload — FIX A) + D-A4-02 (reply POSTed to the SAME inbound webhook; header chain primary)
    - PATTERNS.md §`app/db/repo.py` (the Plan-01 header-chain lookups: find_awaiting_reply_for_header for resume + find_any_run_for_header for late-reply — both already committed in Plan 01 per FIX 9/10; references LIKE %(references)s MUST be parameterized; load_source_email returns the CLEANED inbound body persisted at ingest — FIX C) + Shared Patterns "Parameterized SQL only"
    - app/db/schema.sql lines 109-125 (email_messages direction/message_id/in_reply_to/references_header/from_addr — the outbound row is the Message-ID anchor, FIX 3; the inbound row carries the original CLEANED body for the run, read via repo.load_source_email for FIX 4 — the body was cleaned at ingest, FIX C)
    - app/models/contracts.py lines 30-47 (InboundEmail in_reply_to / references_header / from_addr fields) + lines 78-86 (Extracted REQUIRES run_id — the resume re-extraction must stamp the code-owned run_id, FIX A) + app/models/status.py (AWAITING_REPLY vs sent/reconciled/rejected/computed)
    - app/main.py (the Plan 02 /webhook/inbound + the find_business_by_sender sender-match used at first ingest — REUSE the same business/contact_email comparison for reply revalidation, FIX 5) + app/pipeline/orchestrator.py (the resume re-entry point at extraction — extract(email, roster, *, run_id, llm), FIX A)
    - 02-01-SUMMARY.md (repo.find_awaiting_reply_for_header, find_any_run_for_header, load_source_email [returns cleaned body — FIX C], get_outbound_message_id, find_business_by_sender, persist_extracted/replace_line_items — all committed in Plan 01) + 02-02-SUMMARY.md (extract takes a code-owned run_id and validates ExtractionPayload — FIX A) + 02-03-SUMMARY.md (the outbound clarification Message-ID stored on the email_messages row)
  </read_first>
  <behavior>
    - Test (test_threading.py::test_header_chain_match): a reply whose in_reply_to equals a stored outbound Message-ID routes to that run via repo.find_awaiting_reply_for_header; a reply matching via the References chain also routes; the SQL LIKE is parameterized. NOTE (Codex r3 LOW): subject/provider-thread fallback is NOT implemented as a code path in Phase 2 — per CLAR-02 the RFC In-Reply-To/References header chain is the primary and ONLY routing path proven here; subject fallback is a deliberately-deferred P6 concern (real-provider thread variety), not built now. No test asserts subject-fallback routing.
    - Test (test_threading.py::test_reply_sender_revalidated — FIX 5): a reply that header-matches an awaiting_reply run BUT whose from_addr does NOT match the matched run's business contact_email / original inbound sender is logged and NOT resumed (a spoofed reply on a guessed Message-ID cannot bypass INGEST-03). A reply whose from_addr DOES match resumes normally.
    - Test (test_threading.py::test_partial_reply_preserves_hours — FIX 4): a reply containing ONLY the answer to the clarification (not the full payroll) resumes; re-extraction runs over (original cleaned inbound body via load_source_email + reply body); the resumed run RETAINS the original employees'/hours that were NOT mentioned in the reply (they are not lost), and the corrected name is now resolved.
    - Test (test_threading.py::test_resume_stamps_run_id — FIX A): the resume re-extraction passes the run's run_id into extract(..., run_id=run_id, ...) so the overwritten extracted_data's Extracted.run_id equals the resumed run's id (the code-owned run_id is supplied by the orchestrator, not the model).
    - Test (test_threading.py::test_idempotent_resume): a matched reply to an awaiting_reply run re-enters extraction, OVERWRITES extracted_data (not appended), and replaces line items by run (DELETE-by-run then insert).
    - Test (test_threading.py::test_late_reply_logged_not_resumed — FIX 10): a header match to a run in sent/reconciled/rejected/computed is detected via repo.find_any_run_for_header and logged as a late reply, NOT resumed (only awaiting_reply runs resume via find_awaiting_reply_for_header).
    - Test: re-running resume twice yields the same final state (idempotent re-entrancy).
  </behavior>
  <action>
    In `app/main.py /webhook/inbound`: after dedupe, if the inbound carries an in_reply_to/references, FIRST call `repo.find_awaiting_reply_for_header(in_reply_to, references_header)` (the Plan-01 resume lookup, restricted to status='awaiting_reply', parameterized LIKE on references — NEVER interpolated). On a match: BEFORE resuming, RE-ASSERT the reply sender (review FIX 5) — load the matched run's business and compare `reply.from_addr` against that business's `businesses.contact_email` (the original inbound sender), reusing the same comparison `find_business_by_sender` performs at first ingest; on a MISMATCH, log it and do NOT resume (a spoofed reply on a guessed/leaked Message-ID must not bypass INGEST-03). On a sender match, set status `EXTRACTING` via repo.set_status (the sole status writer — FIX B) and `background_tasks.add_task(resume_pipeline, run_id, inbound)`. If `find_awaiting_reply_for_header` returns None, call `repo.find_any_run_for_header(in_reply_to, references_header)` (the Plan-01 any-status lookup, FIX 10): a header match to a run NOT in awaiting_reply (sent/reconciled/rejected/computed) is logged as a LATE REPLY and NOT resumed (invariant 4); no match → handled as an ordinary inbound. Add `resume_pipeline(run_id, inbound)` to `app/pipeline/orchestrator.py` that re-enters at extraction idempotently and LOSSLESSLY (review FIX 4): build the extraction CONTEXT from (the ORIGINAL cleaned inbound body via `repo.load_source_email(run_id)` — already cleaned at ingest, FIX C + the clarification reply body from `inbound.body_text`), then call `extract(...)` passing the run's CODE-OWNED `run_id` (`extract(combined_email, roster, *, run_id=run_id, llm=...)` — the model returns an ExtractionPayload and extract stamps run_id onto the rebuilt Extracted, FIX A), then `repo.persist_extracted` OVERWRITES extracted_data wholesale with the FULL re-extracted result (single JSONB cell, never append — so the idempotent overwrite of CLAR-03 is preserved; what changes is WHAT we extract from), and `repo.replace_line_items` does DELETE-by-run then insert (invariants 1-2). Because the re-extraction sees the original body, employees/hours not mentioned in the reply are retained, not lost. The rest of the pipeline proceeds as normal (reconcile → decide → calc → set_status awaiting_approval). email_messages stays append-only (audit). Write tests/test_threading.py (mocked LLM; DB round-trips behind @pytest.mark.integration + two-factor guard; the header-chain match, sender-revalidation, partial-reply-preserves-hours, and run_id-stamping logic asserted with in-memory/parameterized-SQL checks where possible).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_threading.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_threading.py::test_header_chain_match -x` passes — routes via In-Reply-To and References through repo.find_awaiting_reply_for_header; subject is fallback only (VALIDATION.md CLAR-02 row).
    - `pytest tests/test_threading.py::test_reply_sender_revalidated -x` passes — a header-matched reply whose from_addr does not match the run's business contact_email is logged + NOT resumed; a matching sender resumes (FIX 5; INGEST-03 on replies).
    - `pytest tests/test_threading.py::test_partial_reply_preserves_hours -x` passes — an answer-only reply resumes over (original cleaned body + reply body) and the original unmentioned employees'/hours are retained, not overwritten away (FIX 4 + FIX C).
    - `pytest tests/test_threading.py::test_resume_stamps_run_id -x` passes — resume passes the code-owned run_id into extract so the rebuilt Extracted.run_id equals the run (FIX A).
    - `pytest tests/test_threading.py::test_idempotent_resume -x` passes — re-entry overwrites extracted_data + replaces line items.
    - `pytest tests/test_threading.py::test_late_reply_logged_not_resumed -x` passes — a header match to a non-awaiting_reply run is found via find_any_run_for_header and logged not resumed (FIX 10; CLAR-03 row).
    - The references LIKE query is parameterized — `grep -nE "references.*LIKE" app/db/repo.py` shows a named placeholder, and no Message-ID is f-string-interpolated into SQL (T-injection).
    - The resume lookup is restricted to `status='awaiting_reply'` and the late-reply lookup is a SEPARATE any-status call (`grep -q "find_awaiting_reply_for_header" app/main.py && grep -q "find_any_run_for_header" app/main.py`).
    - The resume re-extraction passes the code-owned run_id into extract (`grep -q "run_id=run_id" app/pipeline/orchestrator.py` covers both the initial run_pipeline and resume_pipeline call sites — FIX A).
    - Resume is idempotent (running it twice yields the same final state).
  </acceptance_criteria>
  <done>A reply routes to its run via the header chain (awaiting_reply only), the reply sender is revalidated against the run's business before resume (FIX 5), re-entry re-extracts over (original cleaned body via load_source_email + reply body) so partial replies don't lose hours (FIX 4 + FIX C), passes the code-owned run_id into extract so the rebuilt Extracted carries the right run_id (FIX A), and overwrites extracted_data idempotently, and a late reply is found via find_any_run_for_header and logged not resumed (FIX 10); parameterized SQL; status writes through set_status; mocked tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reply-fixture injection completing the clarify->reply->resume loop (EMAIL-01)</name>
  <read_first>
    - CONTEXT.md D-A4-02 (the fixture reply is POSTed to the same inbound webhook with In-Reply-To/References pointing at the clarification's stored outbound Message-ID) + D-A4-03 (fixture replay via POST; zero real email)
    - RESEARCH.md §Pattern 5 (EMAIL-01 stub supports injecting a fixture reply) + §Pattern 6 invariants
    - PATTERNS.md §fixtures (canonical InboundEmail JSON; the reply fixture's in_reply_to points at the clarification's synthetic Message-ID — but synthetic IDs are uuid-random, so the test must wire the reply's in_reply_to to the actual sent Message-ID at runtime, OR the fixture carries a placeholder the test substitutes)
    - app/models/contracts.py (InboundEmail in_reply_to/references_header/message_id/from_addr — the reply's from_addr MUST equal the gate-block run's business contact_email so it passes the FIX-5 sender revalidation) + 02-03-SUMMARY.md (gateway.send_outbound records the clarification Message-ID on the outbound email_messages row; repo.get_outbound_message_id reads it back — the FIX-3 anchor; the gate-block hero fixture from Plan 03)
    - tests/test_demo_fixtures.py + tests/test_threading.py (the gate-block run that reaches awaiting_reply — chain the reply onto it)
  </read_first>
  <behavior>
    - Test (test_threading.py, end-to-end): run the gate-block hero fixture (mocked) to awaiting_reply, read back the clarification's synthetic Message-ID via repo.get_outbound_message_id (the FIX-3 email_messages anchor), substitute it into the reply payload, ASSERT the substitution succeeded (the reply's in_reply_to == the captured clarification Message-ID) BEFORE POSTing, then POST fixtures/clarify_reply.json (whose from_addr matches the run's business so it passes the FIX-5 revalidation) → the run resumes at extraction over (original cleaned body + reply body) and (with a now-resolved mocked reconcile) proceeds to a computed/awaiting_approval state — the full clarify→reply→resume loop with zero real email (EMAIL-01).
    - Test: a reply whose in_reply_to matches no outbound Message-ID creates/handles a no-match gracefully (logged, no wrong-run resume).
  </behavior>
  <action>
    Author `fixtures/clarify_reply.json` as a canonical reply `InboundEmail`: a new message_id, an `in_reply_to`/`references_header` slot that points at the clarification's outbound Message-ID (because synthetic IDs are uuid-random, carry a documented placeholder token the test substitutes with the actual sent Message-ID at runtime, per PATTERNS §fixtures), a `from_addr` equal to the gate-block run's business contact_email (so the reply passes the FIX-5 sender revalidation), and a corrected body that resolves the David Reyez name (e.g. the client confirms the correct spelling) WITHOUT restating every employee — so the test exercises the FIX-4 partial-reply path (the original hours must survive). Extend `tests/test_threading.py` (or test_demo_fixtures.py) with the full loop: drive the gate-block fixture to awaiting_reply (mocked), read back the stored clarification Message-ID via repo.get_outbound_message_id (the FIX-3 anchor), substitute it into the reply payload, and — BEFORE the reply POST — assert the substitution actually took (the reply's `in_reply_to` equals the captured clarification Message-ID and the placeholder token is gone), so a broken substitution fails LOUDLY here rather than silently routing to the graceful no-match branch and letting the resume test pass without resuming. Then inject the reply fixture via the webhook and assert the run resumes at extraction and advances, retaining the original hours (EMAIL-01 + FIX 4 — the loop is exercisable with zero real email). Run the full mocked suite to confirm no regressions.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_threading.py -x -q && .venv/bin/python -m pytest -m "not integration and not live_llm" -q</automated>
  </verify>
  <acceptance_criteria>
    - The full clarify→reply→resume loop is exercised with zero real email: gate-block fixture → awaiting_reply → injected reply (in_reply_to = the clarification Message-ID read from the email_messages anchor; from_addr = the run's business so it passes FIX-5 revalidation) → resume at extraction over (original cleaned body + reply body) → advance retaining original hours (VALIDATION.md EMAIL-01/CLAR-03 rows; FIX 4).
    - BEFORE the reply POST, the test asserts the reply's `in_reply_to` equals the captured clarification Message-ID (and the placeholder token is no longer present), so a broken Message-ID substitution fails loudly at that assertion instead of silently passing via the no-match branch (WARNING 8 — substitution must be verified, not assumed).
    - `fixtures/clarify_reply.json` validates as InboundEmail, carries an in_reply_to slot (`grep -q "in_reply_to" fixtures/clarify_reply.json`), and its from_addr equals the gate-block run's business contact_email (passes FIX-5 revalidation).
    - A reply with no matching outbound Message-ID is handled gracefully (no wrong-run resume) — and this branch is NOT how the resume test passes (the pre-POST in_reply_to assertion guarantees the resume path is the one exercised).
    - The full mocked suite (`-m "not integration and not live_llm"`) is green — no regressions.
  </acceptance_criteria>
  <done>The reply fixture completes the clarify→reply→resume loop with zero real email; the Message-ID substitution is asserted before the POST so a broken substitution fails loudly (not via the no-match path); the reply passes sender revalidation and the run resumes at extraction over (original cleaned body + reply body), retaining original hours and advancing; mocked suite green — slice (c) complete.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Live hero-fixture exit gate (D-A4-01a) — real models say process AND code gate blocks on sub-0.8</name>
  <read_first>
    - CONTEXT.md D-A4-01a (CRITICAL: the hero fixture must be validated LIVE, not just against a mock; a one-time live run against real DeepSeek/Kimi is an explicit Phase 2 exit criterion; budget a tuning loop on the submitted-name variant + reconcile prompt) + D-A5-01 (the live run is a distinct exit gate; the mocked suite passing is necessary but not sufficient)
    - VALIDATION.md §Manual-Only Verifications (live hero-fixture run: validates a model-behavior + demo-narrative property no test can assert; tune the prompt/fixture until it genuinely produces "model willing, code said no") + §The mock/live distinction
    - PATTERNS.md §`tests/test_live_llm.py` (EXACT analog tests/test_seed_roundtrip.py §2 two-factor guard: _HAS_LLM_KEYS + ALLOW_LIVE_LLM; @pytest.mark.live_llm; assert model_action=="process" AND final_action=="request_clarification" AND a per-name confidence < 0.8)
    - app/models/contracts.py (Decision is `extra="forbid"` at line 104 — the live-vs-mock marker CANNOT be a key inside the Decision JSONB; it must be a structured log field — review FIX 12: the marker is a structured log field, the concrete choice, NOT a schema column and NOT inside Decision)
    - STATE.md Blockers (confirm exact DeepSeek/Kimi model IDs + the non-thinking request param from the consoles BEFORE the live run — open blocker) + memory payroll-agent-provider-ids-unconfirmed
    - AI-SPEC.md §7 (the reconcile prompt is the tuning surface for the hero fixture)
  </read_first>
  <what-built>
    The env-gated live test `tests/test_live_llm.py::test_hero_fixture_live` (mirroring the Phase 1 two-factor live-DB guard verbatim: `_HAS_LLM_KEYS = bool(EXTRACTION_API_KEY and DECISION_API_KEY)`, `_LIVE_LLM = os.environ.get("ALLOW_LIVE_LLM") == "1"`, `@pytest.mark.live_llm`) runs the David Reyez hero fixture through the REAL configured DeepSeek/Kimi models and asserts the model genuinely returns `model_action=="process"` AND `final_action=="request_clarification"` AND a per-name confidence `< Decimal("0.8")`. A live-vs-mock run marker records that this decision came from a live model call, not a recording (NICE-TO-HAVE per D-A2-01). Per review FIX 12, the marker is CONCRETELY a structured log field (e.g. `log.info("decision", run_id=..., source="live")` / `source="mock"`) emitted at the decide step, where `source` is derived from a value the orchestrator passes through (or `Settings.allow_live_llm`) — NOT a schema column and NOT a key inside the Decision JSONB (which is `extra="forbid"` at contracts.py:104 and would raise ValidationError). The mocked suite is already green (Tasks 1-2 + Plans 01-03); this gate proves the DEMO, which the mock cannot.
  </what-built>
  <how-to-verify>
    1. Confirm the exact DeepSeek/Kimi non-reasoning model IDs + the DeepSeek non-thinking request parameter from the provider consoles and pin them in `.env` (STATE.md open blocker; legacy deepseek-chat/deepseek-reasoner retire 2026/07/24 — never alias to them).
    2. Optionally run the live provider smoke-test first (one tiny temperature=0, max_tokens=5 call per tier) to confirm creds + IDs resolve + non-thinking mode is accepted.
    3. Run: `ALLOW_LIVE_LLM=1 EXTRACTION_API_KEY=... DECISION_API_KEY=... .venv/bin/python -m pytest tests/test_live_llm.py::test_hero_fixture_live -m live_llm -x`.
    4. EXPECTED: the real model matches David Reyez to David Reyes and returns `process` at a sub-0.8 confidence so the code gate fires → `final_action=="request_clarification"`. This is "the model was willing; the code said no" on REAL output.
    5. Confirm the live-vs-mock marker is recorded as a structured LOG FIELD (source="live"), derived from an orchestrator-passed value / Settings, NOT a key inside the Decision object and NOT a schema column (FIX 12) — verify it does NOT attempt to add a key into the `extra="forbid"` Decision JSONB (which would raise ValidationError).
    6. IF the model self-clarifies (proposes request_clarification on its own → gate never fires) OR returns confidence ≥0.8 (mismatch would process): TUNE the submitted-name variant (`David Reyez`) and/or the reconcile prompt (the tuning surface, AI-SPEC §7) and repeat until the live run genuinely produces model-says-process AND gate-blocks-on-sub-0.8. This is a human-judgment loop, not an automated pass/fail.
    7. For the actual recording, capture the exact good run (hosted APIs are not bit-deterministic even at temp 0).
  </how-to-verify>
  <resume-signal>Type "approved" once the live run genuinely produces model-says-process AND gate-blocks-on-sub-0.8 (tune + repeat first if it self-clarifies or returns ≥0.8) and the live-vs-mock marker is recorded as a structured log field outside the Decision object, or describe the issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reply POST → webhook | Untrusted reply payload carries In-Reply-To/References used in a DB lookup; must be parameterized. |
| reply sender → matched run | A header-matched reply's from_addr must be re-asserted against the matched run's business before resume; a spoofed reply on a guessed Message-ID must not bypass INGEST-03 (FIX 5). |
| header chain → run resume | A header match must not resume a wrong or already-resolved run; only awaiting_reply runs resume; late replies are observable via find_any_run_for_header (FIX 10). |
| reply LLM payload → run_id | The resume re-extraction stamps the run's code-owned run_id onto the rebuilt Extracted; the model returns only an ExtractionPayload, so a reply cannot drive a wrong/forged run_id into the resumed run (FIX A). |
| real LLM provider → live decision | The live hero run depends on real model behavior; the code gate remains the trust enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | header-chain references LIKE query | mitigate | Parameterized named placeholders only — `references LIKE %(references)s`; never interpolate a Message-ID into SQL (PATTERNS Shared Patterns; RESEARCH Security Domain V5). |
| T-04-02 | Spoofing | reply routed to wrong / already-resolved run | mitigate | find_awaiting_reply_for_header matches only `status='awaiting_reply'`; a header match to any other status is found via find_any_run_for_header and logged as a late reply, not resumed (CLAR-03 invariant 4, FIX 10); subject is fallback only to avoid collisions (Pitfall 4). |
| T-04-06 | Spoofing | spoofed reply bypassing INGEST-03 on the reply path | mitigate | After the header match, the reply's from_addr is re-asserted against the matched run's business contact_email; a mismatch is logged and NOT resumed, so a guessed/leaked Message-ID cannot drive a resume from an unauthorized sender (FIX 5). |
| T-04-07 | Tampering | reply-supplied run_id on resume (FIX A) | mitigate | The resume re-extraction validates an ExtractionPayload (no run_id) and stamps the run's code-owned run_id; a reply cannot inject/forge a run identity into the resumed Extracted. |
| T-04-03 | Tampering | re-extraction across resumes / partial-reply data loss | mitigate | Re-extraction runs over (original cleaned body via load_source_email + reply body) so a partial reply never drops original hours; extracted_data overwritten wholesale (never appended); line items replaced by run (DELETE-by-run then insert) — idempotent AND lossless re-entrancy (CLAR-03 invariants 1-2, FIX 4 + FIX C). |
| T-04-04 | Repudiation | reply / resume audit | mitigate | email_messages is append-only; every inbound reply recorded with its Message-ID + headers (audit trail). |
| T-04-05 | Information disclosure | live-LLM creds / logging / marker | mitigate | API keys read from env, never logged; no full email bodies at info level; the live-vs-mock marker is a structured log field (source="live"/"mock"), NEVER inside the extra="forbid" Decision object and NOT a schema column (FIX 12) — it records provenance, not secrets. |
| T-04-SC | Tampering | package installs | mitigate | No new package names this plan (reuses Plan 01-03 deps); no install task. |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -m "not integration and not live_llm" -q` is green (full mocked suite).
- Reply routes via header chain through find_awaiting_reply_for_header (awaiting_reply only); references LIKE is parameterized; late replies are observable via find_any_run_for_header (FIX 10).
- The reply sender is re-asserted against the matched run's business before resume; a mismatch is logged not resumed (FIX 5).
- Re-entry re-extracts over (original cleaned body via load_source_email + reply body) so a partial reply retains original hours; the re-extraction passes the code-owned run_id into extract (FIX A); extracted_data overwritten, line items replaced (FIX 4 + FIX C).
- The clarify→reply→resume loop runs end-to-end with zero real email; the reply's in_reply_to substitution (read from the email_messages anchor) is asserted before the POST.
- The live-vs-mock marker is a structured log field — NOT a key inside the extra="forbid" Decision object and NOT a schema column (FIX 12).
- EXIT GATE (manual): the live hero run produces model-says-process AND gate-blocks-on-sub-0.8 (D-A4-01a).
</verification>

<success_criteria>
- A clarify run reaches awaiting_reply; an injected fixture reply routes back via In-Reply-To/References, passes sender revalidation, and resumes at extraction idempotently and losslessly over (original cleaned body + reply body), stamping the code-owned run_id (success criterion 4, CLAR-02/03, EMAIL-01, FIX 4/5/A).
- The live hero-fixture exit gate is satisfied: the real models genuinely produce the "model willing, code said no" narrative (D-A4-01a) — the distinct exit gate beyond the mocked suite.
- All 3 of this plan's requirement IDs are exercised by green automated tests, plus the manual live exit gate (CLAR-02, CLAR-03, EMAIL-01).
</success_criteria>

<output>
Create `.planning/phases/02-walking-skeleton/02-04-SUMMARY.md` when done.
</output>
