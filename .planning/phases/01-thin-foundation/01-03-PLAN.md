---
phase: 01-thin-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - app/db/seed.py
  - tests/test_seed_roundtrip.py
autonomous: true
requirements:
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "Running `python -m app.db.seed` against a live Postgres with the schema applied loads 3 businesses and 6+ employees with no validation error"
    - "Every seeded employee passes Employee contract validation — a missing FOUND-06 field (ytd_ss_wages, pay_periods_per_year, filing_status, step_2_checkbox, etc.) fails at seed time, not mid-demo"
    - "The seed includes at least 1 hourly employee, 1 salary employee, all 3 filing statuses spread across the roster, at least 1 employee with a known alias (deterministic fast-path case), and 1 employee whose name will be submitted with a single-character typo in Phase 2 (the hero gate CANDIDATE per D-12 — not final proof)"
    - "The seed includes a high earner positioned so ytd_ss_wages + this run's per-period gross straddles the $184,500 SS wage base cap — the partial-cap branch fires because per_period_gross > remaining_cap (D-13 correction: the straddle condition is period SS WAGES exceeding the remaining wage base, not tax amount exceeding wage base)"
    - "Re-running seed is idempotent: second run upserts in place; row counts for businesses and employees are unchanged"
    - "The loader NEVER inserts rows into payroll_runs or email_messages (D-11 containment)"
    - "Seed business contact_email values are used as the upsert natural key; employee (business_id, full_name) is the natural key per D-11 — relies on UNIQUE(business_id, full_name) from Plan 02's schema.sql"
    - "All UUIDs in seed data are fixed/stable — same UUIDs across runs so FK references in later fixture files remain stable per D-11"
    - "seed() wraps all writes in a single explicit transaction so partial seed failure leaves no orphaned rows (Finding #10)"
    - "seed(dry_run=True) returns a structured result with both businesses and employees so the behavior check can inspect distinct business contact_emails (Finding #10)"
    - "Integration test fixture requires ALLOW_DB_RESET=1 in addition to DATABASE_URL before calling bootstrap(reset=True) — a second guard against accidental destructive reset (Finding #10)"
    - "Round-trip test selects only Employee model fields (not SELECT *) and uses psycopg.rows.dict_row so Employee(**row) does not collide with extra DB columns or tuple rows (Finding #4)"
  artifacts:
    - path: "app/db/seed.py"
      provides: "Pydantic-validated seed loader — every Employee validated through the contract before insert; transactional; dry_run returns SeedResult with businesses+employees"
      exports: ["seed", "SeedResult"]
    - path: "tests/test_seed_roundtrip.py"
      provides: "Live-DB round-trip test: seed then read back and validate through Employee contract; requires DATABASE_URL + ALLOW_DB_RESET=1; uses dict_row and explicit column select"
  key_links:
    - from: "app/db/seed.py"
      to: "app/models/roster.py"
      via: "Every seeded employee is constructed as an Employee model and validated before INSERT; validation failure aborts the seed"
      pattern: "Employee("
    - from: "app/db/seed.py"
      to: "app/db/supabase.py"
      via: "Uses get_connection() from the pool; prepare_threshold=None already set at pool level"
      pattern: "get_connection"
    - from: "seed.py (high-earner ytd_ss_wages)"
      to: "CALC-04 golden tests (Phase 3)"
      via: "ytd_ss_wages + per-period gross straddles $184,500 so the partial-cap branch fires in Phase 3"
      pattern: "ytd_ss_wages"
---

<objective>
Author the Pydantic-contract-driven seed loader that populates 3 businesses and 6+ employees covering every calc path and name-match case, with a live-DB round-trip test that reads seeded rows back through the Employee contract.

Purpose: Seed data is the fixture against which Phases 2-4 exercise the pipeline. If a required calc field is missing now, it surfaces mid-demo in Phase 3 or Phase 4. By validating every seeded record through the Employee contract before writing, missing fields fail at seed time. The high-earner employee's ytd_ss_wages + per-period gross straddling $184,500 ensures Phase 3's golden tests exercise the SS wage-base partial-cap branch rather than leaving it dead code.

Output: app/db/seed.py with stable fixed UUIDs, Pydantic validation on insert, transactional writes, upsert idempotency, never touching payroll_runs/email_messages, and dry_run returning a structured SeedResult. tests/test_seed_roundtrip.py to prove the round-trip holds against a live DB, guarded by both DATABASE_URL and ALLOW_DB_RESET=1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-thin-foundation/01-CONTEXT.md
@CLAUDE.md
@.planning/phases/01-thin-foundation/01-01-SUMMARY.md
@.planning/phases/01-thin-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: app/db/seed.py — coverage-driven seed with Pydantic validation, transactional writes, and structured dry_run result</name>
  <files>app/db/seed.py</files>

  <read_first>
    - .planning/phases/01-thin-foundation/01-CONTEXT.md — D-10 (Pydantic-contract-driven loader; validation fails at seed time), D-11 (upsert on natural key; fixed UUIDs; never touch payroll_runs/email_messages), D-12 (hero gate case: single clean typo — e.g. "Reyez" for a real employee named "Reyes" — CANDIDATE for Phase 2 confirmation, not locked proof), D-13 (coverage-driven minimum ~3 businesses ~5-8 employees; all 3 filing statuses; ≥1 hourly ≥1 salary; ≥1 alias; ≥1 high earner whose ytd_ss_wages + per-period gross straddles $184,500 — NOT just under)
    - .planning/REQUIREMENTS.md §FOUND-05 (3+ businesses, mixed hourly/salary, known aliases, filing statuses, happy-path + name-mismatch cases), §FOUND-06 (full calc-input set per employee: pay frequency, wage type + rate or salary, filing status, Step-2, Step-3/4 values, static YTD SS wages)
    - CLAUDE.md §"FICA constants" ($184,500 SS wage base 2026; $11,439 employee SS max; 6.2% rate)
    - app/models/roster.py (Employee fields that must be populated; pay_periods_per_year drives the annual ÷ periods salary calc)
    - app/db/supabase.py (get_connection() context manager to use)
    - app/db/schema.sql (UNIQUE(business_id, full_name) and updated_at columns confirmed present from Plan 02)
  </read_first>

  <behavior>
    - Test: constructing the seed's high-earner Employee model with ytd_ss_wages=Decimal('183900.00'), annual_salary=Decimal('240000.00'), pay_type='salary', pay_periods_per_year=26 validates without error
    - Test: Thomas Bergmann (biweekly salary) SS cap straddle — per-period gross = 240000 / 26 = $9,230.77; remaining cap = $184,500 - $183,900 = $600; straddle condition is `remaining_cap > 0 AND per_period_gross > remaining_cap` ($9,230.77 > $600 is True) — the partial-cap branch fires, SS is capped at $600 × 0.062 = $37.20 (not zero and not full 6.2% on $9,230.77). The straddle condition compares per-period GROSS WAGES to the remaining WAGE BASE, not a tax amount to the wage base — see note on Finding #5 below.
    - Test: `seed(dry_run=True)` returns a SeedResult with .businesses (list of 3 business dicts) and .employees (list of 6 Employee objects); no DB calls in dry_run mode
    - Test: SeedResult.businesses has exactly 3 entries with distinct contact_email values
    - Test: the seed data contains at least 6 Employee records spanning: pay_type='hourly' (≥1), pay_type='salary' (≥1), filing_status='single' (≥1), filing_status='married_jointly' (≥1), filing_status='married_separately' (≥1), len(known_aliases) > 0 (≥1), ytd_ss_wages near cap (the partial-cap employee)
  </behavior>

  <action>
app/db/seed.py: importable module with a `SeedResult` dataclass (or simple namedtuple/dataclass) and a `seed(dry_run: bool = False) -> SeedResult` function and a `if __name__ == "__main__":` entry point.

SeedResult (Finding #10 — dry_run must return both businesses and employees for behavior inspection):
Define as a dataclass: `@dataclasses.dataclass` with fields `businesses: list[dict]` and `employees: list[Employee]`. The behavior check in dry_run mode inspects `result.businesses` to confirm 3 distinct contact_email values, so the return type must carry both.

The seed data is declared as Python data structures inside the module (not read from a YAML/JSON file). Fixed UUIDs are assigned to every record with `uuid.UUID("...")` literals so they never churn across runs per D-11. Use UUIDs that look like real UUIDs (e.g., UUID("b0000001-0000-0000-0000-000000000001") for businesses, sequential).

BUSINESS ROSTER (3 businesses — declared as plain dicts for the INSERT, not as Pydantic models since there is no Business contract in Phase 1):

Business 1 — "Coastal Cleaning Co." (happy-path business)
  id: UUID("b0000001-0000-0000-0000-000000000001")
  contact_email: "payroll@coastalcleaning.example"
  pay_period: "weekly"

Business 2 — "Metro Deli Group" (name-mismatch hero case)
  id: UUID("b0000002-0000-0000-0000-000000000002")
  contact_email: "hr@metrodeli.example"
  pay_period: "weekly"

Business 3 — "Summit Tech Solutions" (salary + biweekly + high-earner)
  id: UUID("b0000003-0000-0000-0000-000000000003")
  contact_email: "finance@summittech.example"
  pay_period: "biweekly"

EMPLOYEE ROSTER (6 employees — coverage across all required paths, validated through Employee Pydantic model before insert):

Employee 1 — Maria Chen (Business 1, hourly, single, filing status coverage)
  id: UUID("e0000001-0000-0000-0000-000000000001")
  business_id: b0000001
  full_name: "Maria Chen"
  known_aliases: ["Maria", "M. Chen"]  — alias fast-path coverage per D-13
  pay_type: "hourly"
  hourly_rate: Decimal("18.50")
  annual_salary: None
  retirement_contribution_pct: Decimal("0.00")
  filing_status: "single"
  step_2_checkbox: False
  step_3_dependents: Decimal("0")
  step_4a_other_income: Decimal("0")
  step_4b_deductions: Decimal("0")
  ytd_ss_wages: Decimal("12000.00")
  pay_periods_per_year: 52

Employee 2 — James Okafor (Business 1, salary, married_jointly, 401k)
  id: UUID("e0000002-0000-0000-0000-000000000002")
  business_id: b0000001
  full_name: "James Okafor"
  known_aliases: []
  pay_type: "salary"
  hourly_rate: None
  annual_salary: Decimal("62400.00")
  retirement_contribution_pct: Decimal("0.04")
  filing_status: "married_jointly"
  step_2_checkbox: False
  step_3_dependents: Decimal("4000.00")
  step_4a_other_income: Decimal("0")
  step_4b_deductions: Decimal("0")
  ytd_ss_wages: Decimal("24000.00")
  pay_periods_per_year: 52

Employee 3 — David Reyes (Business 2, hourly, single — the HERO GATE CANDIDATE per D-12)
  Phase 2 will submit "David Reyez" (one-letter transposition: y→z in surname).
  This is a Phase 1 seed CANDIDATE, not final proof. Whether the model returns
  model_action='process' with confidence in the 0.6-0.79 band depends on Phase 2's
  prompt and model behavior. Phase 2 owns an exit check (DEMO-01) that confirms the
  gate visibly fires on camera. Do not treat the Phase 1 seed as demonstrating the gate.
  id: UUID("e0000003-0000-0000-0000-000000000003")
  business_id: b0000002
  full_name: "David Reyes"
  known_aliases: []
  pay_type: "hourly"
  hourly_rate: Decimal("22.00")
  annual_salary: None
  retirement_contribution_pct: Decimal("0.00")
  filing_status: "single"
  step_2_checkbox: False
  step_3_dependents: Decimal("0")
  step_4a_other_income: Decimal("0")
  step_4b_deductions: Decimal("0")
  ytd_ss_wages: Decimal("8000.00")
  pay_periods_per_year: 52

Employee 4 — Priya Nair (Business 2, salary, married_separately, Step-2 checkbox)
  id: UUID("e0000004-0000-0000-0000-000000000004")
  business_id: b0000002
  full_name: "Priya Nair"
  known_aliases: ["P. Nair"]
  pay_type: "salary"
  hourly_rate: None
  annual_salary: Decimal("72800.00")
  retirement_contribution_pct: Decimal("0.06")
  filing_status: "married_separately"
  step_2_checkbox: True  — Step-2-checkbox branch coverage for Phase 3 Pub 15-T
  step_3_dependents: Decimal("0")
  step_4a_other_income: Decimal("2000.00")
  step_4b_deductions: Decimal("0")
  ytd_ss_wages: Decimal("35000.00")
  pay_periods_per_year: 52

Employee 5 — Thomas Bergmann (Business 3, salary, married_jointly, biweekly, HIGH-EARNER SS CAP STRADDLE)
  SS cap straddle math (D-13 corrected per Finding #5 — the straddle condition is:
  `remaining_cap > 0 and per_period_gross > remaining_cap`, where remaining_cap is the
  remaining SS WAGE BASE (not a tax amount), and per_period_gross is period WAGES):
    ytd_ss_wages = $183,900
    pay_periods_per_year = 26 (biweekly)
    annual_salary = $240,000
    per_period_gross = $240,000 / 26 = $9,230.77
    remaining_cap = $184,500 - $183,900 = $600
    Straddle check: remaining_cap ($600) > 0 AND per_period_gross ($9,230.77) > remaining_cap ($600) → TRUE
    Therefore: SS tax = $600 × 0.062 = $37.20 (only the remaining $600 of wages is SS-taxable; wages above $184,500 are exempt)
    This is NOT $250.33 (which would be the SS tax on the FULL gross of $4,037.50 from an hourly example).
    The partial-cap branch fires correctly.
  id: UUID("e0000005-0000-0000-0000-000000000005")
  business_id: b0000003
  full_name: "Thomas Bergmann"
  known_aliases: ["Tom Bergmann", "Tom"]
  pay_type: "salary"
  hourly_rate: None
  annual_salary: Decimal("240000.00")
  retirement_contribution_pct: Decimal("0.08")
  filing_status: "married_jointly"
  step_2_checkbox: False
  step_3_dependents: Decimal("8000.00")
  step_4a_other_income: Decimal("0")
  step_4b_deductions: Decimal("0")
  ytd_ss_wages: Decimal("183900.00")  — $600 below $184,500 cap; per-period gross ($9,230.77) exceeds remaining cap ($600) → partial cap fires
  pay_periods_per_year: 26

Employee 6 — Sandra Kim (Business 3, hourly, single, 401k, biweekly — FIX B: pay_periods_per_year corrected to 26 to match Business 3's biweekly cadence)
  id: UUID("e0000006-0000-0000-0000-000000000006")
  business_id: b0000003
  full_name: "Sandra Kim"
  known_aliases: []
  pay_type: "hourly"
  hourly_rate: Decimal("45.00")
  annual_salary: None
  retirement_contribution_pct: Decimal("0.05")
  filing_status: "single"
  step_2_checkbox: False
  step_3_dependents: Decimal("0")
  step_4a_other_income: Decimal("0")
  step_4b_deductions: Decimal("0")
  ytd_ss_wages: Decimal("52000.00")
  pay_periods_per_year: 26

CADENCE VERIFICATION: every employee's pay_periods_per_year matches their business's pay_period.
  Business 1 weekly (52): Maria Chen 52 ✓, James Okafor 52 ✓
  Business 2 weekly (52): David Reyes 52 ✓, Priya Nair 52 ✓
  Business 3 biweekly (26): Thomas Bergmann 26 ✓, Sandra Kim 26 ✓ (corrected from erroneous 52)

IMPLEMENTATION LOGIC — the seed() function:
1. Constructs Employee model objects and validates them via Pydantic (construction raises ValidationError if any FOUND-06 field is wrong — fails at seed time per D-10).
2. If dry_run=True, returns SeedResult(businesses=[list of business dicts], employees=[list of Employee objects]) and exits without DB access (Finding #10 — return structured result with both, not just employees).
3. Opens a connection via get_connection() from app.db.supabase.
4. Wraps ALL writes in a SINGLE explicit transaction using `with conn.transaction():` (Finding #10 — atomic: either all 3 businesses + all 6 employees commit, or nothing does; no orphaned rows on partial failure).
5. Inside the transaction, upserts businesses via INSERT ... ON CONFLICT (contact_email) DO UPDATE SET name=EXCLUDED.name, pay_period=EXCLUDED.pay_period, updated_at=now() (per D-11 natural key; updated_at column is present per Plan 02 Finding #1 fix).
6. Inside the same transaction, upserts employees via INSERT ... ON CONFLICT (business_id, full_name) DO UPDATE SET known_aliases=EXCLUDED.known_aliases, pay_type=EXCLUDED.pay_type, hourly_rate=EXCLUDED.hourly_rate, annual_salary=EXCLUDED.annual_salary, retirement_contribution_pct=EXCLUDED.retirement_contribution_pct, filing_status=EXCLUDED.filing_status, step_2_checkbox=EXCLUDED.step_2_checkbox, step_3_dependents=EXCLUDED.step_3_dependents, step_4a_other_income=EXCLUDED.step_4a_other_income, step_4b_deductions=EXCLUDED.step_4b_deductions, ytd_ss_wages=EXCLUDED.ytd_ss_wages, pay_periods_per_year=EXCLUDED.pay_periods_per_year, updated_at=now() (per D-11; UNIQUE(business_id, full_name) constraint is present per Plan 02 schema; updated_at column is present per Plan 02 Finding #1 fix).
7. Persists jsonb fields via model_dump(mode="json") per D-06 (none in the current employee shape, but follow the pattern for consistency).
8. NEVER inserts into payroll_runs or email_messages (D-11 containment) — add an assertion or comment at the top of the function body: `# D-11: seed() is explicitly forbidden from touching payroll_runs or email_messages`.
9. Prints summary: "Seeded 3 businesses, 6 employees." on success.
10. Returns SeedResult(businesses=[business dicts], employees=[employee objects]) from the live path as well.
  </action>

  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && python -c "
from app.db.seed import seed, SeedResult
from decimal import Decimal

# Dry-run: validates all Employee models without DB (Finding #10 — returns SeedResult)
result = seed(dry_run=True)
assert isinstance(result, SeedResult), f'Expected SeedResult, got {type(result)}'
print(f'Dry run: {len(result.employees)} employees, {len(result.businesses)} businesses validated')
assert len(result.employees) == 6, f'Expected 6 employees, got {len(result.employees)}'
assert len(result.businesses) == 3, f'Expected 3 businesses, got {len(result.businesses)}'

# Business contact_email distinct check (Finding #10 — inspect via SeedResult.businesses)
contact_emails = {b['contact_email'] for b in result.businesses}
assert len(contact_emails) == 3, f'Expected 3 distinct contact_emails, got {contact_emails}'

# Coverage checks
pay_types = {e.pay_type for e in result.employees}
assert 'hourly' in pay_types and 'salary' in pay_types

filing_statuses = {e.filing_status for e in result.employees}
assert 'single' in filing_statuses
assert 'married_jointly' in filing_statuses
assert 'married_separately' in filing_statuses

has_alias = any(len(e.known_aliases) > 0 for e in result.employees)
assert has_alias, 'No employee has known_aliases'

# High-earner cap straddle check (Finding #5 corrected math)
high_earner = next(e for e in result.employees if e.ytd_ss_wages >= Decimal('183000'))
per_period_gross = high_earner.annual_salary / high_earner.pay_periods_per_year
remaining_cap = Decimal('184500.00') - high_earner.ytd_ss_wages
assert remaining_cap > 0, 'ytd_ss_wages already at or over cap — no cap event possible'
# Straddle condition: per-period WAGES exceed remaining WAGE BASE (not tax amount)
assert per_period_gross > remaining_cap, f'Per-period gross wages ({per_period_gross:.2f}) do not exceed remaining wage base ({remaining_cap:.2f}) — straddle requires period wages > remaining cap'
print(f'Cap straddle confirmed: per-period gross wages={per_period_gross:.2f}, remaining wage base={remaining_cap:.2f}, partial SS tax=\${(remaining_cap * Decimal(\"0.062\")):.2f}')

# FIX B: cadence consistency check — all Business 3 employees must have pay_periods_per_year=26
b3_employees = [e for e in result.employees if str(e.business_id) == 'b0000003-0000-0000-0000-000000000003']
for e in b3_employees:
    assert e.pay_periods_per_year == 26, f'{e.full_name} (Business 3/biweekly) has pay_periods_per_year={e.pay_periods_per_year}, expected 26'
print(f'Cadence check: all {len(b3_employees)} Business 3 employees have pay_periods_per_year=26')

print('All seed coverage checks passed')
"</automated>
  </verify>

  <done>seed() validates all 6 Employee models without error and returns SeedResult with both businesses and employees. Dry-run coverage checks pass: 3 distinct business contact_emails, 2 pay types, all 3 filing statuses, ≥1 alias, high-earner partial-cap straddle confirmed with correct straddle condition (per-period wages vs remaining wage base). Sandra Kim's pay_periods_per_year is 26 (biweekly), consistent with Business 3's pay cadence. Against live DB: 3 businesses and 6 employees written in one transaction; second run produces same row counts (upsert idempotency); payroll_runs and email_messages tables untouched.</done>

  <acceptance_criteria>
    - Source: app/db/seed.py defines SeedResult dataclass with .businesses (list[dict]) and .employees (list[Employee]) fields (Finding #10)
    - Source: seed() wraps all INSERT/UPSERT statements in `with conn.transaction():` (Finding #10 — atomic writes)
    - Source: seed(dry_run=True) returns SeedResult without any DB calls (Finding #10)
    - Source: app/db/seed.py contains fixed UUID literals for all records (not uuid4() calls) per D-11
    - Source: seed() constructs Employee(...) Pydantic models before any INSERT — validation happens before DB write per D-10
    - Source: seed() has no INSERT INTO payroll_runs or INSERT INTO email_messages statements (D-11 containment)
    - Source: the ON CONFLICT clause uses (contact_email) for businesses and (business_id, full_name) for employees per D-11
    - Source: business upsert includes `updated_at=now()` and employee upsert includes `updated_at=now()` in the DO UPDATE SET clause (Finding #1 — schema now has these columns per Plan 02)
    - Source: the cap-straddle prose for Thomas Bergmann describes the straddle condition as `remaining_cap > 0 AND per_period_gross > remaining_cap` where remaining_cap is the remaining SS WAGE BASE and per_period_gross is the period WAGES — NOT a comparison of a tax dollar amount to the wage base (Finding #5 corrected)
    - Source: Sandra Kim (Business 3, biweekly) has pay_periods_per_year=26, not 52 — consistent with her business's biweekly pay cadence (FIX B)
    - Behavior: `python -c "from app.db.seed import seed; r = seed(dry_run=True); assert len(r.employees)==6 and len(r.businesses)==3"` exits 0
    - Behavior (dry_run): SeedResult covers hourly/salary, all 3 filing statuses, ≥1 alias, high-earner with correct straddle condition
    - Behavior (live DB): `python -m app.db.seed` exits 0; `SELECT COUNT(*) FROM businesses` = 3, `SELECT COUNT(*) FROM employees` = 6
    - Behavior (live DB): `python -m app.db.seed` again exits 0 with same row counts (idempotency)
    - Behavior (live DB): `SELECT ytd_ss_wages FROM employees WHERE full_name='Thomas Bergmann'` returns 183900.00
    - Behavior (live DB): `SELECT pay_periods_per_year FROM employees WHERE full_name='Sandra Kim'` returns 26 (FIX B)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_seed_roundtrip.py — live-DB read-back validation through Employee contract with dict_row and explicit column select</name>
  <files>tests/test_seed_roundtrip.py</files>

  <read_first>
    - app/db/seed.py (fixed UUIDs and field values to assert against; SeedResult type)
    - app/models/roster.py (Employee model for round-trip validation; field names must match DB column names)
    - app/db/supabase.py (get_connection())
    - app/config.py (DATABASE_URL env var — test skips if DATABASE_URL is not set)
    - .planning/phases/01-thin-foundation/01-CONTEXT.md §D-06 (numeric fields from DB arrive as Decimal via psycopg's native type mapping — no float)
    - .planning/REQUIREMENTS.md §FOUND-05, §FOUND-06
  </read_first>

  <behavior>
    - Test: skip entire module with pytest.skip if DATABASE_URL is not set in environment (CI without a real DB skips gracefully)
    - Test: skip entire module with pytest.skip if ALLOW_DB_RESET=1 is not set in environment (second destructive-reset guard per Finding #10 — prevents accidentally wiping a non-test DB)
    - Test: after calling seed(), SELECT COUNT(*) FROM businesses returns 3
    - Test: after calling seed(), SELECT COUNT(*) FROM employees returns 6
    - Test: Thomas Bergmann's row has ytd_ss_wages = Decimal('183900.00') and pay_periods_per_year = 26
    - Test: every row in employees can be loaded as Employee(**row_dict) without a ValidationError — using explicit column select and dict_row factory (Finding #4: no SELECT *, no tuple rows)
    - Test: second call to seed() leaves row counts unchanged (idempotency)
    - Test: after seed(), zero rows exist in payroll_runs and email_messages (D-11 containment proof)
    - Test: David Reyes exists in employees with full_name='David Reyes' (hero gate CANDIDATE — exactly 1 row confirms seed; Phase 2 proves gate behavior)
    - Test: Maria Chen's known_aliases contains 'Maria'
  </behavior>

  <action>
tests/test_seed_roundtrip.py: a pytest module with `pytest.mark.integration` marker and TWO module-level skip guards (Finding #10):

At module top:
  import pytest, os
  if not os.environ.get("DATABASE_URL"):
      pytest.skip("DATABASE_URL not set — skipping live DB tests", allow_module_level=True)
  if not os.environ.get("ALLOW_DB_RESET") == "1":
      pytest.skip("ALLOW_DB_RESET=1 not set — refusing to run destructive reset fixture (Finding #10)", allow_module_level=True)

Both guards are mandatory. The ALLOW_DB_RESET guard prevents `bootstrap(reset=True)` in the fixture from running against a non-test DB even when DATABASE_URL is accidentally set. Together they form the two-factor guard for any destructive operation in tests.

Fixture: `@pytest.fixture(scope="module")` that calls `bootstrap(reset=True)` then `seed()` once for the module (fresh DB for the test run, then seeded). The fixture can call bootstrap(reset=True) safely because both env guards have already been checked at module skip level.

ROUND-TRIP EMPLOYEE QUERY (Finding #4 — critical fix):

Do NOT use `SELECT * FROM employees`. Instead, explicitly select only the columns that map to Employee model fields. The DB row includes `created_at` and `updated_at` which are NOT Employee fields, and Employee has `extra="forbid"` — passing them to Employee(**row) would raise ValidationError with extra fields. Use `psycopg.rows.dict_row` as the row factory so psycopg returns dicts keyed by column name instead of tuples.

The correct query pattern:
  `SELECT id, business_id, full_name, known_aliases, pay_type, hourly_rate, annual_salary, retirement_contribution_pct, filing_status, step_2_checkbox, step_3_dependents, step_4a_other_income, step_4b_deductions, ytd_ss_wages, pay_periods_per_year FROM employees`

With `row_factory=psycopg.rows.dict_row` set on the cursor (or on the connection), each row is a dict with only the listed columns. Pass the dict directly to `Employee(**row)`.

Note: D-06 means psycopg with the binary extension returns NUMERIC columns as Python Decimal objects — no float conversion needed. If a column arrives as float (e.g., without binary extension), the Employee model will reject it because pay_type Literal and other typed fields guard against wrong types, and Decimal fields are typed — add an assertion in the test that ytd_ss_wages comes back as Decimal, not float.

Tests:
- `test_business_count`: SELECT COUNT(*) FROM businesses → 3
- `test_employee_count`: SELECT COUNT(*) FROM employees → 6
- `test_high_earner_fields`: SELECT ytd_ss_wages, pay_periods_per_year FROM employees WHERE full_name='Thomas Bergmann'; assert ytd_ss_wages == Decimal('183900.00'), pay_periods_per_year == 26; assert isinstance(ytd_ss_wages, Decimal), "ytd_ss_wages must be Decimal not float"
- `test_employee_roundtrip`: use explicit column list + dict_row (see above); for each row dict, construct Employee(**row) and assert no ValidationError — proves FOUND-06 (all required calc fields present and correctly typed in the DB)
- `test_idempotent_reseed`: call seed() again; SELECT COUNT(*) FROM businesses → still 3; SELECT COUNT(*) FROM employees → still 6
- `test_seed_containment`: SELECT COUNT(*) FROM payroll_runs → 0; SELECT COUNT(*) FROM email_messages → 0 (D-11)
- `test_hero_case_exists`: SELECT full_name FROM employees WHERE full_name='David Reyes'; assert exactly 1 row returned (the hero gate CANDIDATE per D-12; Phase 2 proves model behavior, not this test)
- `test_alias_exists`: SELECT known_aliases FROM employees WHERE full_name='Maria Chen'; assert 'Maria' in known_aliases
  </action>

  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && python -m pytest tests/test_seed_roundtrip.py -v 2>&1 | head -20</automated>
  </verify>

  <done>test_seed_roundtrip.py passes all integration tests against a live DB (requires DATABASE_URL + ALLOW_DB_RESET=1): 3 businesses, 6 employees, Thomas Bergmann's ytd_ss_wages=183900.00, all Employee rows round-trip through Pydantic validation using explicit column select + dict_row (no SELECT *, no extra-column collision), second seed is idempotent, payroll_runs and email_messages stay empty. Tests skip gracefully when DATABASE_URL or ALLOW_DB_RESET=1 is unset.</done>

  <acceptance_criteria>
    - Source: tests/test_seed_roundtrip.py has TWO module-level skip guards: one for DATABASE_URL and one for ALLOW_DB_RESET==1 (Finding #10 — two-factor guard for destructive reset fixture)
    - Source: test_employee_roundtrip uses an explicit column list (not SELECT *) naming only the Employee model fields, and sets row_factory=psycopg.rows.dict_row on the cursor or connection (Finding #4 — prevents extra-column collision with extra="forbid")
    - Source: tests/test_seed_roundtrip.py imports `bootstrap` and calls it with `reset=True` in the setup fixture (safe because both env guards have already fired)
    - Source: test_seed_containment asserts 0 rows in payroll_runs and email_messages (D-11 proof)
    - Source: test_hero_case_exists comment notes David Reyes is a Phase 2 CANDIDATE, not Phase 1 proof (D-12 / Finding #10 LOW)
    - Behavior (no DATABASE_URL): `python -m pytest tests/test_seed_roundtrip.py -v` exits 0 with all tests skipped (DATABASE_URL guard fires)
    - Behavior (DATABASE_URL set but no ALLOW_DB_RESET=1): `DATABASE_URL=... python -m pytest tests/test_seed_roundtrip.py -v` exits 0 with all tests skipped (ALLOW_DB_RESET guard fires)
    - Behavior (live DB): `DATABASE_URL=... ALLOW_DB_RESET=1 python -m pytest tests/test_seed_roundtrip.py -v -m integration` exits 0 with all 8 tests passing
    - Behavior (live DB): test_employee_roundtrip constructs Employee(**row) for every employee row without ValidationError (FOUND-06 proof — all calc fields present); ytd_ss_wages arrives as Decimal not float
    - Behavior (live DB): test_high_earner_fields confirms Thomas Bergmann's ytd_ss_wages=183900.00 and the partial-cap straddle math is live in the DB
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed data → live DB | Seed data is developer-authored static Python; no user input enters this path |
| test fixture → live DB | Integration tests call bootstrap(reset=True) — destructive; only run with both DATABASE_URL and ALLOW_DB_RESET=1 explicitly set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | seed.py writing to payroll_runs/email_messages | mitigate | Enforced by D-11: seed() has no INSERT INTO payroll_runs or email_messages; test_seed_containment proves 0 rows in those tables after seed |
| T-03-02 | Tampering | test fixture calling bootstrap(reset=True) against a production DB | mitigate | Two-factor guard (Finding #10): DATABASE_URL skip + ALLOW_DB_RESET=1 skip at module level; bootstrap(reset=True) never executes in the fixture unless BOTH guards pass. A transactional seed() also limits blast radius of partial failures |
| T-03-03 | Information Disclosure | Fixed UUIDs in seed data | accept | Seed UUIDs are not credentials; they're stable identifiers for a demo dataset; committing them is correct and intentional per D-11 |
| T-03-SC | Tampering | No new pip packages introduced | accept | This plan adds no new dependencies; psycopg and pydantic are already in requirements.txt from Plan 01 |
</threat_model>

<verification>
After both tasks complete:

1. `python -c "from app.db.seed import seed, SeedResult; r = seed(dry_run=True); assert isinstance(r, SeedResult) and len(r.employees)==6 and len(r.businesses)==3"` — exits 0 (Finding #10 — SeedResult carries both)
2. `python -m pytest tests/test_status_drift.py tests/test_seed_roundtrip.py -v` (without DATABASE_URL) — drift test passes, round-trip tests skip on both guards
3. With DATABASE_URL set but without ALLOW_DB_RESET=1: `DATABASE_URL=... python -m pytest tests/test_seed_roundtrip.py -v` — skips on ALLOW_DB_RESET guard (Finding #10)
4. With DATABASE_URL and ALLOW_DB_RESET=1: `python -m app.db.bootstrap && python -m app.db.seed` — exits 0; SELECT counts confirm 3 businesses, 6 employees, 0 payroll_runs, 0 email_messages
5. Re-run `python -m app.db.seed` — counts unchanged (idempotency)
6. `DATABASE_URL=... ALLOW_DB_RESET=1 python -m pytest tests/test_seed_roundtrip.py -v -m integration` — all 8 tests pass
7. Confirm Thomas Bergmann ytd_ss_wages=183900.00 and per-period gross ($9,230.77) exceeds remaining wage base cap ($600) — partial SS tax = $37.20 (Finding #5: straddle is wages vs wage-base, not tax vs wage-base)
8. Confirm Sandra Kim pay_periods_per_year=26 in live DB: `SELECT pay_periods_per_year FROM employees WHERE full_name='Sandra Kim'` returns 26 (FIX B — biweekly cadence match with Business 3)
</verification>

<success_criteria>
Seed loader validates every Employee through Pydantic before writing; wraps all writes in one transaction; returns SeedResult with both businesses and employees from dry_run. All 6 seeded employees pass FOUND-06 field coverage; second seed run leaves row counts unchanged; payroll_runs and email_messages stay empty (D-11). The high-earner employee's per-period gross exceeds the remaining SS wage base ($600), confirmed with the correct straddle condition. Sandra Kim's pay_periods_per_year is 26, consistent with Business 3's biweekly cadence (FIX B). Integration tests pass with DATABASE_URL + ALLOW_DB_RESET=1; skip gracefully without either. Round-trip test uses explicit column select + dict_row — no SELECT * collision with extra="forbid".
</success_criteria>

<output>
Create `.planning/phases/01-thin-foundation/01-03-SUMMARY.md` when done.
</output>
