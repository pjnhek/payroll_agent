---
phase: 01-thin-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/db/__init__.py
  - app/db/schema.sql
  - app/db/supabase.py
  - app/config.py
  - tests/test_status_drift.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "Running `python -m app.db.bootstrap` against a live Postgres instance applies all 6 tables with no error"
    - "A query to information_schema.tables confirms the six required tables exist in schema public: businesses, employees, payroll_runs, paystub_line_items, email_messages, eval_results (Finding #9 — assert presence not exact count)"
    - "A pg_get_constraintdef(oid) query confirms payroll_runs has a CHECK constraint listing all 11 status values (Finding #9 — use pg_get_constraintdef not consrc)"
    - "Inserting two email_messages rows with the same message_id raises a unique-violation error (live DB test)"
    - "Re-running bootstrap is idempotent — a second run returns success with no errors"
    - "pytest tests/test_status_drift.py exits 0 — the SQL CHECK values and RunStatus enum members are set-equal"
    - "The bootstrap script reads DATABASE_URL via pydantic-settings, never from a hardcoded string or CLI argument"
    - "The --reset flag drops all tables in reverse-dependency order using DROP TABLE IF EXISTS ... CASCADE; the default (no flag) is non-destructive (CREATE TABLE IF NOT EXISTS) (Finding #9)"
    - "schema.sql contains UNIQUE (business_id, full_name) on employees so Plan 03's ON CONFLICT upsert works (Finding #1)"
    - "businesses and employees tables have updated_at TIMESTAMPTZ NOT NULL DEFAULT now() columns consistent with Plan 03's upserts (Finding #1)"
    - "schema.sql includes CREATE EXTENSION IF NOT EXISTS pgcrypto so gen_random_uuid() works on local Postgres (Finding #9)"
    - "The circular FK between email_messages and payroll_runs is resolved without inline forward reference — run_id on email_messages is added via ALTER TABLE after payroll_runs exists (Finding #2)"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "DDL source of truth — 6 tables with IF NOT EXISTS, TEXT CHECK status, unique index on message_id, UNIQUE(business_id,full_name) on employees, updated_at on businesses+employees, pgcrypto extension, deferred FK for circular reference"
      contains: "CREATE TABLE IF NOT EXISTS businesses"
    - path: "app/db/supabase.py"
      provides: "Connection pool using psycopg[pool], prepare_threshold=None, DATABASE_URL from config"
      exports: ["get_connection", "get_pool"]
    - path: "app/db/bootstrap.py"
      provides: "Idempotent schema apply script with opt-in --reset flag using DROP ... CASCADE in reverse order"
      exports: ["bootstrap"]
    - path: "app/config.py"
      provides: "pydantic-settings Settings class loading DATABASE_URL and model config from env"
      exports: ["Settings", "get_settings"]
    - path: "tests/test_status_drift.py"
      provides: "CI gate: SQL CHECK list == RunStatus members; fails if drift occurs"
  key_links:
    - from: "app/models/status.py"
      to: "app/db/schema.sql"
      via: "The 11 RunStatus values must appear verbatim in the CHECK constraint; test_status_drift.py asserts set-equality"
      pattern: "CHECK.*status.*IN"
    - from: "app/db/supabase.py"
      to: "DATABASE_URL"
      via: "app/config.py Settings; prepare_threshold=None on every connection per D-04 gotcha"
      pattern: "prepare_threshold"
    - from: "app/db/schema.sql (employees)"
      to: "app/db/seed.py"
      via: "UNIQUE(business_id, full_name) constraint is required by Plan 03's ON CONFLICT upsert (Finding #1)"
      pattern: "UNIQUE.*business_id.*full_name"
---

<objective>
Author `app/db/schema.sql` (the DDL source of truth for all 6 tables) and the idempotent bootstrap script that applies it to a real Postgres instance, then add the CI drift-guard test that ensures the Python RunStatus enum and the SQL CHECK constraint never diverge.

Purpose: This is the live-DB proof that the connection path works (Supavisor pooler, IPv4, transaction mode, port 6543, prepare_threshold=None), the schema applies correctly, and the idempotency guarantee holds. Without running against a real Postgres, Success Criteria #1 and #2 from the ROADMAP are unverified — file content alone does not prove a schema applied.

Output: schema.sql, bootstrap.py (with --reset), supabase.py (connection pool), config.py (pydantic-settings), test_status_drift.py. The live DB must show all 6 tables + status CHECK + unique message_id + UNIQUE(business_id,full_name) on employees + updated_at on businesses and employees after bootstrap runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-thin-foundation/01-CONTEXT.md
@payroll-agent-build-plan.md
@CLAUDE.md
@.planning/phases/01-thin-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema.sql + config.py + supabase.py connection pool</name>
  <files>app/db/__init__.py, app/db/schema.sql, app/db/supabase.py, app/config.py</files>

  <read_first>
    - payroll-agent-build-plan.md §"Data model (Supabase Postgres)" — authoritative column list for all 6 tables
    - .planning/phases/01-thin-foundation/01-CONTEXT.md — D-02 (TEXT + CHECK, not native Postgres ENUM; the exact 11 values), D-03 (Python RunStatus is source of truth; SQL mirrors it), D-04 (pooler host + port 6543 + prepare_threshold=None gotcha), D-06 (jsonb columns hold Pydantic model_dump(mode="json") output — not raw Decimal), D-07 (jsonb columns: extracted_data + decision)
    - CLAUDE.md §"What NOT to Use" (direct Supabase host is IPv6-only; use pooler 6543; no supabase-py), §"Prescriptive Usage Patterns #3" (psycopg connection + transaction mode), §"Version Compatibility" (psycopg[binary,pool] 3.3.4)
    - .planning/REQUIREMENTS.md §FOUND-01 (6 tables + 11-value status), §FOUND-02 (message_id unique)
    - app/models/status.py (the 11 RunStatus values to mirror in the CHECK constraint)
  </read_first>

  <action>
app/db/__init__.py: empty file.

app/db/schema.sql: DDL for all 6 tables using CREATE TABLE IF NOT EXISTS (idempotent; no DROP). Follow the build plan column list exactly. The file must be structured as follows — TABLE ORDERING is non-negotiable (see circular FK note below).

OPENING BLOCK (before any CREATE TABLE): `CREATE EXTENSION IF NOT EXISTS pgcrypto;` (Finding #9 — needed for gen_random_uuid() on local Postgres; Supabase already has it but the IF NOT EXISTS guard makes the statement idempotent).

TABLE ORDER AND DEFINITIONS:

1. businesses: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), name TEXT NOT NULL, contact_email TEXT NOT NULL UNIQUE, pay_period TEXT NOT NULL CHECK (pay_period IN ('weekly','biweekly','semi_monthly','monthly')), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(). (Finding #1 — updated_at added so Plan 03 business upserts with `updated_at=now()` do not fail with a missing-column error. Both businesses and employees must have this column.)

2. employees: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), business_id UUID NOT NULL REFERENCES businesses(id) ON DELETE CASCADE, full_name TEXT NOT NULL, known_aliases TEXT[] NOT NULL DEFAULT '{}', pay_type TEXT NOT NULL CHECK (pay_type IN ('hourly','salary')), hourly_rate NUMERIC(12,4), annual_salary NUMERIC(14,2), retirement_contribution_pct NUMERIC(5,4) NOT NULL DEFAULT 0, filing_status TEXT NOT NULL CHECK (filing_status IN ('single','married_jointly','married_separately')), step_2_checkbox BOOLEAN NOT NULL DEFAULT FALSE, step_3_dependents NUMERIC(12,2) NOT NULL DEFAULT 0, step_4a_other_income NUMERIC(12,2) NOT NULL DEFAULT 0, step_4b_deductions NUMERIC(12,2) NOT NULL DEFAULT 0, ytd_ss_wages NUMERIC(14,2) NOT NULL DEFAULT 0, pay_periods_per_year INTEGER NOT NULL CHECK (pay_periods_per_year IN (12,24,26,52)), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(). (Finding #1 — updated_at added.) Then add as a table constraint on the same CREATE TABLE statement: CONSTRAINT uq_employee_business_name UNIQUE (business_id, full_name). (Finding #1 — this constraint is required by Plan 03's `ON CONFLICT (business_id, full_name) DO UPDATE` upsert; placing it here means it exists before the seed runs.)

3. payroll_runs: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), business_id UUID NOT NULL REFERENCES businesses(id), source_email_id UUID, status TEXT NOT NULL DEFAULT 'received' CHECK (status IN ('received','extracting','needs_clarification','awaiting_reply','computed','awaiting_approval','approved','sent','reconciled','rejected','error')), extracted_data JSONB, decision JSONB, pay_period_start DATE, pay_period_end DATE, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now(). IMPORTANT: source_email_id is declared as `UUID` with NO inline FOREIGN KEY reference here — the FK to email_messages is added AFTER email_messages exists (see deferred FK block below). This avoids the circular dependency DDL error (Finding #2).

4. paystub_line_items: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), run_id UUID NOT NULL REFERENCES payroll_runs(id) ON DELETE CASCADE, employee_id UUID REFERENCES employees(id), submitted_name TEXT NOT NULL, match_confidence NUMERIC(4,3) NOT NULL DEFAULT 0, hours_regular NUMERIC(6,2) NOT NULL DEFAULT 0, hours_overtime NUMERIC(6,2) NOT NULL DEFAULT 0, hours_vacation NUMERIC(6,2) NOT NULL DEFAULT 0, hours_sick NUMERIC(6,2) NOT NULL DEFAULT 0, hours_holiday NUMERIC(6,2) NOT NULL DEFAULT 0, gross_pay NUMERIC(12,2) NOT NULL DEFAULT 0, pretax_401k NUMERIC(12,2) NOT NULL DEFAULT 0, fica_ss NUMERIC(12,2) NOT NULL DEFAULT 0, fica_medicare NUMERIC(12,2) NOT NULL DEFAULT 0, federal_withholding NUMERIC(12,2) NOT NULL DEFAULT 0, state_withholding NUMERIC(12,2), net_pay NUMERIC(12,2) NOT NULL DEFAULT 0, created_at TIMESTAMPTZ NOT NULL DEFAULT now()

5. email_messages: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), run_id UUID REFERENCES payroll_runs(id), direction TEXT NOT NULL CHECK (direction IN ('inbound','outbound')), message_id TEXT NOT NULL, in_reply_to TEXT, references_header TEXT, subject TEXT, from_addr TEXT, to_addr TEXT, body_text TEXT, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), CONSTRAINT uq_message_id UNIQUE (message_id)

6. eval_results: id UUID PRIMARY KEY DEFAULT gen_random_uuid(), suite_run_id UUID NOT NULL, fixture_id TEXT NOT NULL, metric_name TEXT NOT NULL, value NUMERIC(8,4) NOT NULL, details JSONB, created_at TIMESTAMPTZ NOT NULL DEFAULT now()

DEFERRED FK BLOCK (Finding #2 — resolves the circular reference between payroll_runs and email_messages): After all six CREATE TABLE statements, add the deferred foreign key that could not be declared inline because payroll_runs is defined before email_messages:

  DO $$
  BEGIN
    IF NOT EXISTS (
      SELECT 1 FROM pg_constraint
      WHERE conname = 'fk_payroll_runs_source_email'
        AND conrelid = 'payroll_runs'::regclass
    ) THEN
      ALTER TABLE payroll_runs
        ADD CONSTRAINT fk_payroll_runs_source_email
        FOREIGN KEY (source_email_id) REFERENCES email_messages(id);
    END IF;
  END;
  $$;

This DO block is idempotent (the IF NOT EXISTS guard uses pg_constraint, not the non-existent ADD CONSTRAINT IF NOT EXISTS syntax), satisfying D-01's re-runnable requirement. email_messages already exists by this point so the FK is valid.

FINAL NOTE ON TABLE ORDER: The correct creation order is businesses → employees → payroll_runs → paystub_line_items → email_messages → eval_results → [deferred FK block]. payroll_runs references businesses (exists) and its source_email_id FK is deferred. email_messages references payroll_runs (exists by its position). The deferred FK block adds the reverse direction safely after both tables exist.

The status CHECK values must exactly match the RunStatus enum values from app/models/status.py (D-03): 'received','extracting','needs_clarification','awaiting_reply','computed','awaiting_approval','approved','sent','reconciled','rejected','error' — all 11, no extras.

app/config.py: use pydantic-settings BaseSettings. Class Settings(BaseSettings) with: database_url (str — no default; missing key fails at startup), extraction_model (str, default "deepseek-v4-flash"), extraction_base_url (str, default "https://api.deepseek.com"), extraction_api_key (str, default ""), decision_model (str, default "moonshot-v1-8k"), decision_base_url (str, default "https://api.moonshot.ai/v1"), decision_api_key (str, default ""), draft_model (str, default "moonshot-v1-8k"), draft_base_url (str, default "https://api.moonshot.ai/v1"), draft_api_key (str, default ""), tax_year (int, default 2026). model_config = SettingsConfigDict(env_file=".env", extra="ignore"). Add a module-level `@lru_cache` getter: `def get_settings() -> Settings: return Settings()`.

app/db/supabase.py: create a psycopg ConnectionPool. Key requirements per D-04 gotcha: every connection must have prepare_threshold=None to prevent psycopg3's auto-prepare from failing under Supavisor transaction mode. Expose two functions: `get_pool() -> ConnectionPool` (returns a module-level singleton pool, min_size=1, max_size=5) and `get_connection()` as a context manager yielding a connection from the pool. The pool is initialized with `conninfo=get_settings().database_url` and `kwargs={"prepare_threshold": None}` (the psycopg3 pool keyword for per-connection options). Import psycopg.pool.ConnectionPool. Do NOT use asyncpg or supabase-py — psycopg only per CLAUDE.md.
  </action>

  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && python -c "
import re, pathlib
sql = pathlib.Path('app/db/schema.sql').read_text()

# Check pgcrypto extension
assert 'CREATE EXTENSION IF NOT EXISTS pgcrypto' in sql, 'Missing pgcrypto extension'

# Check all 6 tables present
tables = ['businesses', 'employees', 'payroll_runs', 'paystub_line_items', 'email_messages', 'eval_results']
for t in tables:
    assert f'CREATE TABLE IF NOT EXISTS {t}' in sql or f'CREATE TABLE IF NOT EXISTS \"{t}\"' in sql, f'Missing table: {t}'

# Check updated_at on businesses and employees (Finding #1)
biz_section = sql[sql.index('CREATE TABLE IF NOT EXISTS businesses'):sql.index('CREATE TABLE IF NOT EXISTS employees')]
assert 'updated_at' in biz_section, 'businesses missing updated_at'
emp_section = sql[sql.index('CREATE TABLE IF NOT EXISTS employees'):sql.index('CREATE TABLE IF NOT EXISTS payroll_runs')]
assert 'updated_at' in emp_section, 'employees missing updated_at'
assert 'UNIQUE' in emp_section and 'business_id' in emp_section and 'full_name' in emp_section, 'employees missing UNIQUE(business_id, full_name)'

# Check the 11-value CHECK constraint on status
sql_no_comments = re.sub(r'--[^\n]*', '', sql)
m = re.search(r'CHECK\s*\(\s*status\s+IN\s*\((.*?)\)\s*\)', sql_no_comments, re.DOTALL | re.IGNORECASE)
assert m, 'No status CHECK constraint found'
check_vals = set(v.strip().strip(\"'\") for v in m.group(1).split(','))
from app.models import RunStatus
enum_vals = {m.value for m in RunStatus}
assert check_vals == enum_vals, f'Drift: SQL CHECK={check_vals}, RunStatus={enum_vals}'

# Check unique constraint on message_id
assert 'UNIQUE' in sql.upper() and 'message_id' in sql, 'Missing unique constraint on message_id'

# Check deferred FK block exists (Finding #2)
assert 'fk_payroll_runs_source_email' in sql, 'Missing deferred FK for circular email_messages <-> payroll_runs reference'

# Check source_email_id has no inline FK in payroll_runs DDL (it must be plain UUID)
pr_section = sql[sql.index('CREATE TABLE IF NOT EXISTS payroll_runs'):sql.index('CREATE TABLE IF NOT EXISTS paystub_line_items')]
assert 'REFERENCES email_messages' not in pr_section, 'payroll_runs.source_email_id must not have inline FK (circular ref — use deferred ALTER TABLE)'

print('schema.sql static checks passed')
"</automated>
  </verify>

  <done>schema.sql has 6 tables with IF NOT EXISTS, pgcrypto extension, the 11-value status CHECK matching RunStatus members, UNIQUE on email_messages.message_id, UNIQUE(business_id, full_name) on employees, updated_at on both businesses and employees, and a deferred FK block resolving the email_messages/payroll_runs circular reference. config.py loads DATABASE_URL from env. supabase.py has prepare_threshold=None on the connection pool. Static schema checks pass.</done>

  <acceptance_criteria>
    - Source: app/db/schema.sql opens with `CREATE EXTENSION IF NOT EXISTS pgcrypto;` (Finding #9 — local Postgres compatibility)
    - Source: app/db/schema.sql contains CREATE TABLE IF NOT EXISTS for all 6 tables: businesses, employees, payroll_runs, paystub_line_items, email_messages, eval_results
    - Source: businesses and employees tables both have `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()` columns (Finding #1 — required by Plan 03 upserts)
    - Source: employees table has `CONSTRAINT uq_employee_business_name UNIQUE (business_id, full_name)` (Finding #1 — required by Plan 03 ON CONFLICT upsert)
    - Source: payroll_runs.source_email_id is declared as plain `UUID` with NO inline REFERENCES email_messages — the FK is added only in the deferred DO block (Finding #2)
    - Source: The deferred DO $$ ... $$ block after all CREATE TABLE statements adds `fk_payroll_runs_source_email` to payroll_runs, guarded by a pg_constraint existence check (Finding #2 — idempotent per D-01)
    - Source: The status CHECK constraint in schema.sql lists exactly 11 values matching the RunStatus enum members
    - Source: schema.sql contains UNIQUE on email_messages.message_id (per FOUND-02)
    - Source: app/db/supabase.py uses prepare_threshold=None in the pool kwargs (the D-04 Supavisor gotcha)
    - Source: app/config.py uses pydantic-settings BaseSettings with database_url field (no default — fails fast if unset)
    - Behavior: The static verification script exits 0 confirming SQL CHECK values == RunStatus members, updated_at present, UNIQUE(business_id,full_name) present, deferred FK block present, no inline circular FK
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: bootstrap.py (idempotent apply + --reset with CASCADE) and test_status_drift.py (CI drift guard)</name>
  <files>app/db/bootstrap.py, tests/test_status_drift.py</files>

  <read_first>
    - .planning/phases/01-thin-foundation/01-CONTEXT.md — D-01 (idempotent; --reset is opt-in DROP-then-recreate; default is non-destructive), D-04 (prepare_threshold=None, DATABASE_URL from env), security requirement (--reset is destructive and must be clearly separated from the default path)
    - app/db/schema.sql (the file bootstrap.py reads and executes)
    - app/db/supabase.py (the connection helper bootstrap.py uses)
    - app/models/status.py (the RunStatus members the drift test reads)
    - .planning/phases/01-thin-foundation/01-CONTEXT.md §security_threat_model_requirement (--reset must be opt-in; destructive path clearly separated)
  </read_first>

  <action>
app/db/bootstrap.py: a runnable script (`if __name__ == "__main__"`) and importable function `bootstrap(reset: bool = False)`.

The bootstrap function:
1. Reads DATABASE_URL from config (get_settings().database_url). Never from a CLI positional arg.
2. Opens a single psycopg connection (not the pool — bootstrap is a one-shot admin operation) with `prepare_threshold=None`.
3. If `reset=True` (opt-in only): DROP each table in reverse-dependency order using `DROP TABLE IF EXISTS ... CASCADE` (Finding #9 — CASCADE handles any lingering FK dependencies; reverse order avoids errors when CASCADE is not used, but with CASCADE order is less critical; still use reverse order as explicit documentation of the dependency direction). The correct DROP order is: paystub_line_items, eval_results, email_messages, payroll_runs, employees, businesses. Each as a separate `DROP TABLE IF EXISTS {name} CASCADE;` statement. Print a clear warning "RESET: dropping all tables in reverse dependency order — this is destructive" before executing. The DROP path is inside an explicit `if reset:` block, completely separate from the default path. Also print the target DB host (strip password from DATABASE_URL before printing).
4. Execute the contents of `app/db/schema.sql` using `conn.execute(sql_text)` in a transaction (all 6 tables + deferred FK block applied atomically — either all succeed or none do).
5. Print "Bootstrap complete. Tables applied." on success.

CLI entry: `python -m app.db.bootstrap` (no args = non-destructive apply). `python -m app.db.bootstrap --reset` = opt-in destructive reset. Parse `--reset` from sys.argv. No argparse dependency needed; just `"--reset" in sys.argv`.

Security note: the --reset flag must never be the default. The function signature `def bootstrap(reset: bool = False)` encodes this. The CLI check is `reset = "--reset" in sys.argv`. The destructive path prints a warning including the target host extracted from DATABASE_URL (strip the password first before printing — use `urllib.parse.urlparse` to reconstruct without the password segment).

tests/test_status_drift.py: a pytest test that is the CI gate for D-03 (Python RunStatus is source of truth; SQL mirrors it — drift fails CI). The test:
1. Reads app/db/schema.sql as a string.
2. Strips SQL line comments before running any regex: `sql_clean = re.sub(r'--[^\n]*', '', sql)` (prevents a commented-out old status value from poisoning the check).
3. Uses a regex to extract the values from the status CHECK constraint: `re.search(r"CHECK\s*\(\s*status\s+IN\s*\((.*?)\)\s*\)", sql_clean, re.DOTALL | re.IGNORECASE)`. Extract the CSV inside the IN(...) parens.
4. Parse each value by splitting on commas, stripping whitespace, and stripping single quotes.
5. Assert the extracted set equals `{m.value for m in RunStatus}`.

This is a pure static file test — no DB connection needed. It runs in CI on every push with no env var requirements.

NOTE on must_haves removal of `consrc` (Finding #9): The acceptance criteria and live-DB verification use `pg_get_constraintdef(oid)` to inspect constraints, NOT the deprecated `consrc` column from `pg_constraint`. `consrc` was removed in Postgres 9.0; `pg_get_constraintdef(oid)` is the correct modern form. This is a live-verification note for the executor, not a code change.
  </action>

  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && python -m pytest tests/test_status_drift.py -v</automated>
  </verify>

  <done>bootstrap.py runs idempotently (default path) and supports --reset (opt-in) with DROP ... CASCADE in reverse order. test_status_drift.py passes: SQL CHECK values set-equal RunStatus members. Against a live DB: all 6 tables exist in schema public, message_id uniqueness rejects a duplicate insert, second bootstrap run returns success.</done>

  <acceptance_criteria>
    - Source: app/db/bootstrap.py has `def bootstrap(reset: bool = False)` with the destructive DROP path inside `if reset:` explicitly separated from the default path
    - Source: The --reset path uses `DROP TABLE IF EXISTS {name} CASCADE` for each table in reverse-dependency order: paystub_line_items, eval_results, email_messages, payroll_runs, employees, businesses (Finding #9)
    - Source: bootstrap.py reads DATABASE_URL from config (get_settings()), not from a hardcoded string or positional CLI arg
    - Source: bootstrap.py opens the connection with prepare_threshold=None (not relying on the pool default)
    - Source: bootstrap.py strips the password from DATABASE_URL before any diagnostic print (use urllib.parse)
    - Source: tests/test_status_drift.py strips SQL line comments with `re.sub(r'--[^\n]*', '', sql)` before parsing the CHECK constraint
    - Source: tests/test_status_drift.py reads schema.sql and asserts set-equality between SQL CHECK values and RunStatus members; does NOT make a DB connection
    - Behavior: `python -m pytest tests/test_status_drift.py -v` exits 0
    - Behavior (live DB): `python -m app.db.bootstrap` exits 0; `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('businesses','employees','payroll_runs','paystub_line_items','email_messages','eval_results')` returns 6 rows (Finding #9 — assert the 6 required tables exist, not "exactly 6 total")
    - Behavior (live DB): inserting a second email_messages row with the same message_id raises psycopg.errors.UniqueViolation (FOUND-02 proof)
    - Behavior (live DB): `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='payroll_runs'::regclass AND contype='c'` output contains all 11 status values (Finding #9 — pg_get_constraintdef not consrc)
    - Behavior (live DB): `python -m app.db.bootstrap` again exits 0 (idempotency — CREATE TABLE IF NOT EXISTS + idempotent DO $$ FK block)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env → DB connection | DATABASE_URL contains the Supabase credentials; must never be logged or printed (strip password before any diagnostic print) |
| bootstrap --reset → live DB | The DROP-then-recreate path is irreversible; opt-in only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | bootstrap.py diagnostic output | mitigate | When printing the target DB host for the --reset confirmation, strip the password from DATABASE_URL before printing (use `urllib.parse.urlparse` and reconstruct without the password segment) |
| T-02-02 | Tampering | --reset destructive path | mitigate | reset=False is the default both in the function signature and the CLI. The --reset path is inside an explicit `if reset:` block with a printed warning; it cannot be triggered accidentally by `python -m app.db.bootstrap` without the flag. DROP uses CASCADE in reverse order (Finding #9) |
| T-02-03 | Information Disclosure | schema.sql | accept | schema.sql contains no credentials or sensitive data — only table structures; safe to commit |
| T-02-04 | Denial of Service | Supavisor connection pool under prepare_threshold | mitigate | prepare_threshold=None on all connections (D-04 gotcha) prevents server-side prepared statement accumulation from starving the pooler backend connections |
| T-02-SC | Tampering | psycopg[binary,pool] install | mitigate | Version pinned to 3.3.4 per CLAUDE.md; package is on the PyPI verified list (Jun 2026) |
</threat_model>

<verification>
After both tasks complete:

1. `python -m pytest tests/test_status_drift.py -v` — exits 0 (CI-runnable, no DB needed)
2. Static schema check script (inline in Task 1 verify) exits 0 — confirms pgcrypto, 6 tables, updated_at on businesses+employees, UNIQUE(business_id,full_name), no inline circular FK, deferred FK block present
3. Set DATABASE_URL to a local Postgres or Supabase test instance; run `python -m app.db.bootstrap`; confirm exit 0 and "Bootstrap complete" output
4. Query `SELECT table_name FROM information_schema.tables WHERE table_schema='public' AND table_name IN ('businesses','employees','payroll_runs','paystub_line_items','email_messages','eval_results')` — returns 6 rows (Finding #9 — presence check, not exact count)
5. Query `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid='payroll_runs'::regclass AND contype='c'` — output contains all 11 status values (Finding #9 — NOT consrc)
6. Insert two email_messages rows with the same message_id; confirm `psycopg.errors.UniqueViolation` is raised (FOUND-02)
7. Run `python -m app.db.bootstrap` again — exits 0 (idempotency check)
</verification>

<success_criteria>
schema.sql contains all 6 tables (IF NOT EXISTS), pgcrypto extension, updated_at on businesses and employees, UNIQUE(business_id,full_name) on employees, the 11-value status CHECK, UNIQUE on message_id, and a deferred idempotent FK block resolving the email_messages/payroll_runs circular reference. bootstrap.py applies the schema idempotently with DROP ... CASCADE in reverse order for --reset. The drift test passes in CI without a DB connection. Against a live Postgres: all required tables exist, duplicate message_id rejected, second bootstrap run succeeds. prepare_threshold=None is set on the connection pool.
</success_criteria>

<output>
Create `.planning/phases/01-thin-foundation/01-02-SUMMARY.md` when done.
</output>
