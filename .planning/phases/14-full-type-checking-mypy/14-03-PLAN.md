---
phase: 14-full-type-checking-mypy
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/main.py
  - app/pipeline/__init__.py
  - app/pipeline/alias_learning.py
  - app/pipeline/calculate.py
  - app/pipeline/clarification.py
  - app/pipeline/compose_email.py
  - app/pipeline/decide.py
  - app/pipeline/delivery.py
  - app/pipeline/extract.py
  - app/pipeline/orchestrator.py
  - app/pipeline/pdf.py
  - app/pipeline/reconcile_names.py
  - app/pipeline/suggest.py
  - app/pipeline/tax_tables_2026.py
  - app/pipeline/validate.py
autonomous: true
requirements: [TYPE-01]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to app/pipeline/ and app/main.py"
    - "The D-09-sanctioned # type: ignore[attr-defined] for delivery.py's exc.payroll_roster assignment is the ONLY new ignore comment in this plan's diff, with a reason comment"
    - "reportlab imports in pdf.py/delivery.py resolve cleanly via Plan 01's scoped override, with no per-line ignores needed"
    - "No annotation introduces a behavior change — the full hermetic test suite passes unmodified"
  artifacts:
    - path: "app/pipeline/delivery.py"
      provides: "exc.payroll_roster assignment carries a D-09-sanctioned type: ignore[attr-defined] with reason comment"
      contains: "type: ignore[attr-defined]"
    - path: "app/main.py"
      provides: "thin app-assembly module, fully annotated"
  key_links:
    - from: "app/pipeline/*.py"
      to: "app/main.py router registration"
      via: "module-object imports (D-11 Phase 13 pattern)"
      pattern: "from app.pipeline import"
---

<objective>
Bring `app/pipeline/` (all 14 files: alias_learning, calculate, clarification, compose_email, decide, delivery, extract, orchestrator, pdf, reconcile_names, suggest, tax_tables_2026, validate — federal_withholding.py already fixed in Plan 01) and `app/main.py` to zero mypy errors under the strict config from Plan 01.

Purpose: This is the money-path core of the application — the pipeline package computes payroll, decides deterministic actions, and handles delivery. It carries the highest behavior-change risk in the whole phase, so every fix here must be provably annotation-only except the one pre-identified D-09-sanctioned ignore (delivery.py's dynamic exception attribute).

Output: Zero mypy errors across all of `app/pipeline/` and `app/main.py`; the one sanctioned `# type: ignore[attr-defined]` landed per D-09; full hermetic suite green with zero assertion changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate money-path core (calculate, decide, reconcile_names, validate, tax_tables_2026, alias_learning) to mypy-clean</name>
  <files>app/pipeline/calculate.py, app/pipeline/decide.py, app/pipeline/reconcile_names.py, app/pipeline/validate.py, app/pipeline/tax_tables_2026.py, app/pipeline/alias_learning.py</files>
  <read_first>
    app/pipeline/calculate.py (the _money() helper and gross/FICA computation — Decimal-heavy, highest money-path sensitivity)
    app/pipeline/decide.py (the pure deterministic decision function — final_action, no LLM, no confidence)
    app/pipeline/reconcile_names.py (deterministic_match, normalize_name)
    app/pipeline/validate.py (HOURS_FIELDS, is_paid — already-promoted BOUND-01 public names)
    app/pipeline/tax_tables_2026.py (BracketRow NamedTuple — the type Plan 01 already imported into federal_withholding.py)
    app/pipeline/alias_learning.py (the alias-learning rule set carved out in Phase 13)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns: dict[str, Any] only at genuinely dynamic edges; concrete Decimal/UUID types once data crosses into pipeline logic per D-10)
  </read_first>
  <action>
    Run `uv run mypy app/pipeline/calculate.py app/pipeline/decide.py app/pipeline/reconcile_names.py app/pipeline/validate.py app/pipeline/tax_tables_2026.py app/pipeline/alias_learning.py` first to get the real current error list for exactly this file set.
    Fix every reported error with concrete types — Decimal/UUID/Literal for money-path values (never widen to Any or object; D-10 requires concrete types once data crosses into pipeline logic), `dict[str, Any]` ONLY at genuinely dynamic JSONB/raw-input boundaries if any exist in this set. Annotate every function currently missing param/return types (no-untyped-def) and add type args to bare dict/list (type-arg).
    These six files compute or gate money-moving values — after fixing each file, immediately re-run its corresponding test file (see acceptance criteria) before moving to the next file, so a mistaken type-narrowing that would change behavior is caught file-by-file rather than at the end of the task.
  </action>
  <acceptance_criteria>
    - `uv run mypy app/pipeline/calculate.py app/pipeline/decide.py app/pipeline/reconcile_names.py app/pipeline/validate.py app/pipeline/tax_tables_2026.py app/pipeline/alias_learning.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_calculate.py tests/test_decide_field_regression.py tests/test_reconcile.py tests/test_validate.py tests/test_tax_tables_2026.py tests/test_alias_write.py tests/test_alias_full_loop.py -q` exits 0
    - Zero new `# type: ignore` comments anywhere in this task's diff (D-09 zero-tolerance — no sanctioned exception exists for these 6 files)
    - `git diff` for these 6 files shows only annotation additions/narrowing, no assertion, branching, or Decimal-rounding logic changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/pipeline/calculate.py app/pipeline/decide.py app/pipeline/reconcile_names.py app/pipeline/validate.py app/pipeline/tax_tables_2026.py app/pipeline/alias_learning.py</automated>
  </verify>
  <done>`uv run mypy app/pipeline/calculate.py app/pipeline/decide.py app/pipeline/reconcile_names.py app/pipeline/validate.py app/pipeline/tax_tables_2026.py app/pipeline/alias_learning.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Annotate remaining pipeline modules (extract, suggest, compose_email, clarification, pdf, delivery, orchestrator) with the D-09-sanctioned ignore in delivery.py</name>
  <files>app/pipeline/extract.py, app/pipeline/suggest.py, app/pipeline/compose_email.py, app/pipeline/clarification.py, app/pipeline/pdf.py, app/pipeline/delivery.py, app/pipeline/orchestrator.py, app/pipeline/__init__.py</files>
  <read_first>
    app/pipeline/delivery.py (lines 190-232, the `exc.payroll_roster = roster` best-effort exception attribute inside `contextlib.suppress(Exception)`, and the surrounding WR-04 comment)
    app/pipeline/pdf.py (reportlab imports — SimpleDocTemplate, Table, TableStyle, Paragraph, Spacer — confirm Plan 01's reportlab.* override in pyproject.toml resolves these cleanly with no per-line ignores needed)
    app/pipeline/orchestrator.py (the core state machine — run_pipeline, _run, resume_pipeline, _run_stages, _compute_line_items, backfill_extracted — the largest remaining file at ~800-900 lines post-Phase-13-split)
    app/pipeline/extract.py, app/pipeline/suggest.py, app/pipeline/compose_email.py, app/pipeline/clarification.py (LLM-calling pipeline stages)
    .planning/phases/14-full-type-checking-mypy/14-RESEARCH.md (Pitfall 4 — the exact D-09-sanctioned ignore pattern and comment text for delivery.py)
  </read_first>
  <action>
    Run `uv run mypy app/pipeline/extract.py app/pipeline/suggest.py app/pipeline/compose_email.py app/pipeline/clarification.py app/pipeline/pdf.py app/pipeline/delivery.py app/pipeline/orchestrator.py app/pipeline/__init__.py` first to get the real current error list.
    Fix every error EXCEPT the one pre-identified `exc.payroll_roster = roster` line in `delivery.py` (~line 231) using concrete types (Decimal/UUID for money-path values, dict[str, Any] only for genuinely dynamic LLM-JSON/JSONB payloads per D-10). Confirm `pdf.py`'s reportlab-derived return types (e.g. `-> Table`) type-check cleanly against Plan 01's `ignore_missing_imports = true` override — no per-line ignores needed inside pdf.py itself.
    For the `exc.payroll_roster = roster` line in delivery.py: add `# type: ignore[attr-defined]  # WR-04: best-effort debug attribute on an arbitrary exception, suppressed if assignment fails; never restructure — see WR-04 comment above` immediately after the assignment. Do NOT subclass Exception, change the `except Exception as exc:` clause, or otherwise restructure this pattern — per RESEARCH.md Pitfall 4 and the phase's own D-09/D-08 guidance, this is the one sanctioned case, and any structural change here risks altering what the outer caller (`app/routes/runs.py`'s `getattr(exc, "payroll_roster", None)`) reads.
    For `orchestrator.py` (the largest file), annotate the core state-machine functions' signatures matching their actual call sites in `app/routes/pipeline_glue.py` (read that file's calls if a signature's shape is ambiguous — do not guess at what `_run_stages`/`resume_pipeline` actually receive/return).
  </action>
  <acceptance_criteria>
    - `uv run mypy app/pipeline/extract.py app/pipeline/suggest.py app/pipeline/compose_email.py app/pipeline/clarification.py app/pipeline/pdf.py app/pipeline/delivery.py app/pipeline/orchestrator.py app/pipeline/__init__.py` exits 0 with `Success: no issues found`
    - `app/pipeline/delivery.py` contains exactly one `# type: ignore[attr-defined]` with the WR-04 reason comment, at the `exc.payroll_roster = roster` line
    - No other `# type: ignore` comments appear anywhere else in this task's diff
    - `uv run pytest tests/test_extract.py tests/test_suggest.py tests/test_clarify.py tests/test_clarify_rounds.py tests/test_pdf.py tests/test_delivery.py tests/test_orchestrator_states.py tests/test_resume_pipeline.py tests/test_multi_employee_delivery.py -q` exits 0
    - `git diff app/pipeline/delivery.py` shows the exception-handling shape (`except Exception as exc:`, `contextlib.suppress(Exception):`) is byte-for-byte unchanged except for the added ignore comment
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/pipeline/extract.py app/pipeline/suggest.py app/pipeline/compose_email.py app/pipeline/clarification.py app/pipeline/pdf.py app/pipeline/delivery.py app/pipeline/orchestrator.py app/pipeline/__init__.py</automated>
  </verify>
  <done>`uv run mypy app/pipeline/extract.py app/pipeline/suggest.py app/pipeline/compose_email.py app/pipeline/clarification.py app/pipeline/pdf.py app/pipeline/delivery.py app/pipeline/orchestrator.py app/pipeline/__init__.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 3: Annotate app/main.py to mypy-clean</name>
  <files>app/main.py</files>
  <read_first>
    app/main.py (post-Phase-13-split, ~100 lines of thin app assembly: app creation, router registration, startup/shutdown wiring)
    app/routes/__init__.py (the router modules main.py registers, to confirm import/annotation compatibility)
  </read_first>
  <action>
    Run `uv run mypy app/main.py` to get the real current error list (expected to be small — this file is thin app assembly post-Phase-13-split, ~16-100 lines).
    Fix every reported error — FastAPI app creation, router `include_router` calls, and any startup/shutdown event handlers should already type-check cleanly against FastAPI's own shipped types; add any missing return annotations (e.g. `-> None` on startup/shutdown handlers) and confirm no bare `dict`/`list` types remain.
  </action>
  <acceptance_criteria>
    - `uv run mypy app/main.py` exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 (full hermetic suite — main.py wires every router, so this is the correct regression check)
    - `git diff app/main.py` shows only annotation additions, no changes to router registration order or app-creation logic
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/main.py</automated>
  </verify>
  <done>`uv run mypy app/main.py` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Money-path computation (calculate.py, decide.py, federal withholding call sites) | Annotation edits touch the highest-consequence code in the repo — any behavior slip here directly causes mispay |
| Exception-attribute dynamic assignment (delivery.py) | A monkey-patched attribute on a generic Exception, read downstream by routes/runs.py's scrub boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Tampering | calculate.py/decide.py annotation edits | mitigate | File-by-file re-test discipline in Task 1 (re-run the specific test file immediately after annotating each money-path file, not batched at the end) catches a behavior slip at its source commit, not several files later |
| T-14-07 | Tampering | delivery.py's exc.payroll_roster D-09 ignore | mitigate | The ignore is scoped to exactly one line with a reason comment; the surrounding except/suppress structure is verified byte-for-byte unchanged via git diff in acceptance criteria |
| T-14-08 | Information Disclosure | orchestrator.py annotation of LLM-facing signatures (extract/suggest/compose_email call sites) | accept | No new data exposure — annotations do not change what data flows where, only the type-checker's understanding of existing flows |
</threat_model>

<verification>
Run `uv run mypy app/pipeline/ app/main.py` — exits 0.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, no assertion changes.
Confirm exactly one `# type: ignore` comment exists in the entire `app/pipeline/` diff (delivery.py's D-09-sanctioned case).
</verification>

<success_criteria>
- Zero mypy errors across all of `app/pipeline/` (14 files) and `app/main.py`.
- Exactly one `# type: ignore[attr-defined]` landed, with reason comment, at delivery.py's exc.payroll_roster line.
- Full hermetic test suite green, zero assertion changes, money-path logic byte-for-byte behavior-identical.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-03-SUMMARY.md` when done
</output>
