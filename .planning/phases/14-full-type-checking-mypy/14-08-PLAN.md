---
phase: 14-full-type-checking-mypy
plan: 08
type: execute
wave: 3
depends_on: ["14-02", "14-03", "14-04", "14-05"]
files_modified:
  - tests/test_alias_write.py
  - tests/test_atomic_persist.py
  - tests/test_bootstrap_safe_url.py
  - tests/test_clarify_rounds.py
  - tests/test_compose_confirmation.py
  - tests/test_cr_regressions.py
  - tests/test_demo_fixtures.py
  - tests/test_detect_field_regression.py
  - tests/test_eval_wiring.py
  - tests/test_federal_withholding.py
  - tests/test_hitl.py
  - tests/test_llm_client.py
  - tests/test_multiround_context_edge.py
  - tests/test_orchestrator_states.py
  - tests/test_persistence.py
  - tests/test_schema_introspect.py
  - tests/test_stuck_run_recovery.py
  - tests/test_validate.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to this plan's 18 test files, under the tests.* relaxed override from Plan 01"
    - "No test assertion changes — only type-level fixes"
    - "This is the final tests/ group — after this plan, uv run mypy over the full tests/ directory (all 56 files) exits 0"
  artifacts: []
  key_links: []
---

<objective>
Bring the third and final third (18 files, ~8935 lines, balanced group 3) of `tests/` to zero mypy errors under Plan 01's `tests.*` relaxed override — this is the last of the three test-group plans, completing full-repo `tests/` type-cleanliness.

Purpose: Same as Plans 06/07 — TYPE-02 requires no blind spot anywhere, including tests. This group includes `test_federal_withholding.py` (1278 lines, the highest-bug-risk unit's test file per CLAUDE.md) and `test_eval_wiring.py` (already touched by Plan 01's D-08 regression test — this plan completes any remaining annotation work in that file beyond the one test Plan 01 added).

Output: Zero mypy errors across this plan's 18 test files; full hermetic suite green with zero assertion changes; combined with Plans 06/07, `uv run mypy` over all of `tests/` now exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve mypy errors in the federal_withholding/atomic_persist/alias_write test cluster</name>
  <files>tests/test_federal_withholding.py, tests/test_atomic_persist.py, tests/test_alias_write.py, tests/test_persistence.py, tests/test_validate.py, tests/test_stuck_run_recovery.py</files>
  <read_first>
    tests/test_federal_withholding.py (1278 lines — the IRS Pub 15-T table-driven test suite, highest-bug-risk-unit's tests per CLAUDE.md; must confirm alignment with Plan 01's federal_withholding.py BracketRow fix)
    tests/test_atomic_persist.py, tests/test_alias_write.py (966 and 1592 lines — the two largest files in this task)
  </read_first>
  <action>
    Run `uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py` first to get the real current error list.
    Fix reported errors following the same discipline as prior test-group plans — only annotate what's needed to resolve a real error, never touch assertions or table-driven test data values. For `test_federal_withholding.py` specifically, confirm any `_find_bracket`-adjacent test helpers correctly reference `BracketRow` (from Plan 01's fix) rather than a bare `object`/`Any` local variable if mypy flags a mismatch.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py -q -m "not integration and not live_llm"` exits 0 with unchanged pass/skip counts
    - `git diff tests/test_federal_withholding.py` shows zero changes to any IRS-worked-example bracket/rate/base table value — annotation-only
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py</automated>
  </verify>
  <done>`uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Resolve mypy errors in the remaining test files in this group, confirm full tests/ is clean</name>
  <files>tests/test_bootstrap_safe_url.py, tests/test_clarify_rounds.py, tests/test_compose_confirmation.py, tests/test_cr_regressions.py, tests/test_demo_fixtures.py, tests/test_detect_field_regression.py, tests/test_eval_wiring.py, tests/test_hitl.py, tests/test_llm_client.py, tests/test_multiround_context_edge.py, tests/test_orchestrator_states.py, tests/test_schema_introspect.py</files>
  <read_first>
    tests/test_eval_wiring.py (already has Plan 01's new D-08 regression test — confirm no conflicting re-annotation needed)
    tests/test_orchestrator_states.py, tests/test_multiround_context_edge.py (state-machine edge-case tests, moderate size)
  </read_first>
  <action>
    Run `uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py` first to get the real current error list.
    Fix reported errors following the same discipline as prior tasks.
    After this task's files are clean, run `uv run mypy .` (the full repo, using the committed `files=` scope from Plan 01 — no directory arguments needed) as a final confirmation that combining all of Wave 2 + all three Wave 3 test-group plans (06, 07, 08) yields a fully clean repo-wide mypy run. If any residual error appears that doesn't belong to this task's file list (e.g., a cross-file interaction only visible in the combined run, per RESEARCH.md's Open Question 1), fix it here as the final integration point for TYPE-01/TYPE-02, and record it explicitly in the plan SUMMARY.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py` exits 0 with `Success: no issues found`
    - `uv run mypy .` (full repo-wide run, no directory args, config-driven scope) exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 with the same pass/skip counts as the phase's pre-Plan-01 baseline (614 passed, 20 skipped) or a documented, justified delta in the plan SUMMARY
    - `git diff` shows zero assertion changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py</automated>
  </verify>
  <done>`uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only code (no production trust boundary) | This plan touches only `tests/`, never exercised in the deployed app |
| Full-repo integration point | This plan's Task 2 is the first point where the ENTIRE repo is checked together — any cross-module error only visible in combination surfaces here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-15 | Tampering | test_federal_withholding.py annotation touching money-path test data | mitigate | Explicit git-diff acceptance criterion confirming zero changes to any IRS-worked-example table value |
| T-14-16 | Repudiation | Full-repo `uv run mypy .` surfacing a residual cross-module error not caught by any single directory-scoped plan | mitigate | Task 2 explicitly runs the full-repo check as its final step and requires any residual error to be fixed and documented, closing RESEARCH.md's Open Question 1 |
</threat_model>

<verification>
Run `uv run mypy .` (full repo, config-driven scope) — exits 0, `Success: no issues found`.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green.
Run `uv run pytest -q` (including integration/live_llm-marked tests, if a live DB is available in this environment) to confirm no regression beyond the hermetic subset — otherwise document that this was hermetic-only per the sandboxed execution environment.
</verification>

<success_criteria>
- Zero mypy errors across this plan's 18 test files under the tests.* relaxed override.
- `uv run mypy .` (the full repo, all four directories combined) exits 0 for the first time in the phase.
- Full hermetic test suite green, identical pass/skip counts, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-08-SUMMARY.md` when done
</output>
