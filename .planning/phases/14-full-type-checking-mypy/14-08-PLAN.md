---
phase: 14-full-type-checking-mypy
plan: 08
type: execute
wave: 5
depends_on: ["14-04", "14-05"]
files_modified:
  - tests/test_alias_write.py
  - tests/test_atomic_persist.py
  - tests/test_bootstrap_safe_url.py
  - tests/test_clarify_rounds.py
  - tests/test_compose_confirmation.py
  - tests/test_cr_regressions.py
  - tests/test_demo_fixtures.py
  - tests/test_detect_field_regression.py
  - tests/test_eval_wiring.py
  - tests/test_federal_withholding.py
  - tests/test_hitl.py
  - tests/test_llm_client.py
  - tests/test_multiround_context_edge.py
  - tests/test_orchestrator_states.py
  - tests/test_persistence.py
  - tests/test_schema_introspect.py
  - tests/test_stuck_run_recovery.py
  - tests/test_validate.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to this plan's 18 test files, under the tests.* relaxed override from Plan 14-01"
    - "No test assertion is weakened — proven by a dynamic before/after pass-skip baseline PLUS a diff review showing zero removed/modified assertion lines"
    - "This plan touches ONLY its 18 declared files — the full-repo combined mypy run and any residual cross-module fixes belong to Plan 14-09, the dedicated integration gate"
  artifacts: []
  key_links: []
---

<objective>
Bring the third and final third (18 files, ~8935 lines, balanced group 3) of `tests/` to zero mypy errors under Plan 14-01's `tests.*` relaxed override — the last of the three parallel Wave 5 test-group plans.

Purpose: Same as Plans 14-06/14-07 — TYPE-02 requires no blind spot anywhere, including tests. This group includes `test_federal_withholding.py` (1278 lines, the highest-bug-risk unit's test file per CLAUDE.md) and `test_eval_wiring.py` (already touched by Plan 14-01's D-08 regression test — this plan completes any remaining annotation work in that file beyond the one test Plan 14-01 added).

Scope boundary (addresses Codex review HIGH — the old plan claimed to be the full-repo integration point while depending only on Plans 02-05 and authorized fixing residual errors outside its file list): this plan is now strictly a test-group plan. The full-repo bare `uv run mypy` combined check — and ownership of any residual cross-module error it surfaces — moved to Plan 14-09, a dedicated integration plan that runs alone in Wave 6 after all three test groups complete. This plan may not modify any file outside its 18 declared test files.

Output: Zero mypy errors across this plan's 18 test files; full hermetic suite green with zero assertion changes.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **HIGH — missing dependencies on Plans 06/07 while claiming to integrate their output:** the integration claim is removed from this plan entirely; the combined full-repo check moved to the new Plan 14-09 (wave 6, depends on 14-06/14-07/14-08).
- **HIGH — residual errors outside the declared file list were authorized to be fixed here:** removed. This plan's file ownership is strictly its 18 test files; residual cross-module errors are routed to Plan 14-09, which runs solo (no concurrency, no collision risk).
- **MEDIUM — `uv run mypy .` violated the locked bare-command parity rule (D-06) and was mis-described as having no directory arguments:** the full-repo command (now in Plan 14-09) is bare `uv run mypy` — no `.`, no directory args; scope comes only from pyproject.toml's `files=` key.
- **MEDIUM — stale pinned `614/20` baseline:** replaced with the dynamic in-session baseline + assertion-diff review (same two-layer rule as Plans 14-06/14-07).

## Artifacts this phase produces (Plan 08)

- No new public symbols — type-level fixes in existing test files only.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve mypy errors in the federal_withholding/atomic_persist/alias_write test cluster</name>
  <files>tests/test_federal_withholding.py, tests/test_atomic_persist.py, tests/test_alias_write.py, tests/test_persistence.py, tests/test_validate.py, tests/test_stuck_run_recovery.py</files>
  <read_first>
    tests/test_federal_withholding.py (1278 lines — the IRS Pub 15-T table-driven test suite, highest-bug-risk-unit's tests per CLAUDE.md; must confirm alignment with Plan 14-01's federal_withholding.py BracketRow fix)
    tests/test_atomic_persist.py, tests/test_alias_write.py (966 and 1592 lines — the two largest files in this task)
  </read_first>
  <action>
    FIRST, before any edit: run `uv run pytest -q -m "not integration and not live_llm"` and record the exact passed/skipped counts — the dynamic baseline for this plan (never pin counts from planning docs; Codex review MEDIUM).
    Run `uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py` to get the real current error list.
    Fix reported errors following the same discipline as prior test-group plans — only annotate what's needed to resolve a real error, never touch assertions or table-driven test data values. For `test_federal_withholding.py` specifically, confirm any `_find_bracket`-adjacent test helpers correctly reference `BracketRow` (from Plan 14-01's fix) rather than a widened local type if mypy flags a mismatch.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py -q -m "not integration and not live_llm"` exits 0
    - `git diff tests/test_federal_withholding.py` shows zero changes to any IRS-worked-example bracket/rate/base table value — annotation-only
    - `git diff -- tests/` shows zero removed/modified assertion lines; any added narrowing assertion is listed in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_federal_withholding.py tests/test_atomic_persist.py tests/test_alias_write.py tests/test_persistence.py tests/test_validate.py tests/test_stuck_run_recovery.py</automated>
  </verify>
  <done>`uv run mypy` over this task's 6 files exits 0 with `Success: no issues found`, IRS table values untouched</done>
</task>

<task type="auto">
  <name>Task 2: Resolve mypy errors in the remaining test files in this group</name>
  <files>tests/test_bootstrap_safe_url.py, tests/test_clarify_rounds.py, tests/test_compose_confirmation.py, tests/test_cr_regressions.py, tests/test_demo_fixtures.py, tests/test_detect_field_regression.py, tests/test_eval_wiring.py, tests/test_hitl.py, tests/test_llm_client.py, tests/test_multiround_context_edge.py, tests/test_orchestrator_states.py, tests/test_schema_introspect.py</files>
  <read_first>
    tests/test_eval_wiring.py (already has Plan 14-01's new D-08 regression test — confirm no conflicting re-annotation needed)
    tests/test_orchestrator_states.py, tests/test_multiround_context_edge.py (state-machine edge-case tests, moderate size)
  </read_first>
  <action>
    Run `uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py` first to get the real current error list.
    Fix reported errors following the same discipline as prior tasks. Do NOT run the full-repo combined mypy check here and do NOT fix any error reported in a file outside this task's list — the combined bare `uv run mypy` gate and residual-error ownership belong exclusively to Plan 14-09 (addresses Codex review HIGH: file ownership stays intact while sibling Wave 5 plans may still be running).
    After all edits, re-run `uv run pytest -q -m "not integration and not live_llm"` and compare against the baseline captured at Task 1 start.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py` exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 with passed/skipped counts IDENTICAL to the baseline captured before any edit in Task 1 (both recorded in the SUMMARY; any legitimate delta documented and justified)
    - `git status --porcelain` shows modified files ONLY within this plan's 18 declared test files (file-ownership proof)
    - `git diff -- tests/` shows zero removed/modified assertion lines
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_bootstrap_safe_url.py tests/test_clarify_rounds.py tests/test_compose_confirmation.py tests/test_cr_regressions.py tests/test_demo_fixtures.py tests/test_detect_field_regression.py tests/test_eval_wiring.py tests/test_hitl.py tests/test_llm_client.py tests/test_multiround_context_edge.py tests/test_orchestrator_states.py tests/test_schema_introspect.py</automated>
  </verify>
  <done>`uv run mypy` over this task's 12 files exits 0 with `Success: no issues found`, no out-of-scope file touched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only code (no production trust boundary) | This plan touches only `tests/`, never exercised in the deployed app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-15 | Tampering | test_federal_withholding.py annotation touching money-path test data | high | mitigate | Explicit git-diff acceptance criterion confirming zero changes to any IRS-worked-example table value, plus the dynamic-baseline + assertion-diff two-layer proof |
</threat_model>

<verification>
Run `uv run mypy` over this plan's 18 files — exits 0 (the combined full-repo gate is Plan 14-09's job).
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, counts identical to the in-session baseline.
Confirm `git status` shows no modification outside the 18 declared files.
</verification>

<success_criteria>
- Zero mypy errors across this plan's 18 test files under the tests.* relaxed override.
- File ownership respected: nothing outside the declared list modified.
- Full hermetic test suite green, counts identical to the dynamically captured baseline, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-08-SUMMARY.md` when done
</output>
