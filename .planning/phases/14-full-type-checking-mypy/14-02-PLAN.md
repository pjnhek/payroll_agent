---
phase: 14-full-type-checking-mypy
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/__init__.py
  - app/config.py
  - app/db/__init__.py
  - app/db/bootstrap.py
  - app/db/check_schema.py
  - app/db/schema_introspect.py
  - app/db/seed.py
  - app/db/supabase.py
  - app/db/repo/__init__.py
  - app/db/repo/_shared.py
  - app/db/repo/demo.py
  - app/db/repo/emails.py
  - app/db/repo/pipeline_state.py
  - app/db/repo/roster.py
  - app/db/repo/runs.py
  - app/models/__init__.py
  - app/models/contracts.py
  - app/models/roster.py
  - app/models/status.py
  - app/llm/__init__.py
  - app/llm/client.py
  - app/llm/prompts/__init__.py
  - app/llm/prompts/clarify.py
  - app/llm/prompts/extract.py
  - app/llm/prompts/suggest.py
autonomous: true
requirements: [TYPE-01]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to app/db/, app/models/, app/llm/, app/config.py, app/__init__.py"
    - "The no_implicit_reexport question for app/db/repo/__init__.py's facade is resolved by direct measurement (run mypy first), not by pre-emptively rewriting exports"
    - "No annotation introduces a behavior change — the full hermetic test suite passes unmodified (assertions untouched, only type hints added)"
  artifacts:
    - path: "app/db/repo/__init__.py"
      provides: "facade confirmed clean under strict no_implicit_reexport, or fixed with the minimal required export style if the real run shows errors"
    - path: "app/models/contracts.py"
      provides: "fully strict-annotated Pydantic contract models, pydantic plugin verified working"
      contains: "ConfigDict(extra=\"forbid\")"
  key_links:
    - from: "app/db/repo/*.py"
      to: "app/db/repo/_shared.py"
      via: "internal plumbing imports (_conn_ctx, _nulltx)"
      pattern: "from app.db.repo._shared import"
---

<objective>
Bring `app/db/` (including the `app/db/repo/` package), `app/models/`, `app/llm/`, `app/config.py`, and `app/__init__.py` to zero mypy errors under the strict config landed in Plan 01 — this is the DB/data-layer and contracts/LLM-client corner of the codebase's mechanical annotation sweep.

Purpose: These modules form the data-persistence and contract-shape backbone the rest of `app/` depends on (`app/db/repo` is imported by nearly every pipeline/route module as `from app.db import repo`). Annotating them first, in isolation, means Wave 2's other plans (pipeline, routes) type-check against already-clean, already-typed DB/contract signatures rather than guessing at in-flight types.

Output: Zero mypy errors across the listed files; `app/db/repo/__init__.py`'s `no_implicit_reexport` question settled by direct measurement per RESEARCH.md Pitfall 1; full hermetic suite green with no assertion changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate app/db/ (bootstrap, check_schema, schema_introspect, seed, supabase, config) to mypy-clean</name>
  <files>app/__init__.py, app/config.py, app/db/__init__.py, app/db/bootstrap.py, app/db/check_schema.py, app/db/schema_introspect.py, app/db/seed.py, app/db/supabase.py</files>
  <read_first>
    app/config.py (current state — Settings via pydantic-settings, already mostly typed)
    app/db/supabase.py (get_connection's Generator return type — the psycopg.Connection pattern every conn: param elsewhere should match)
    app/db/bootstrap.py, app/db/check_schema.py, app/db/schema_introspect.py, app/db/seed.py (current annotation state)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns: "conn=None params" -> psycopg.Connection | None, bare dict/list -> dict[str, Any]/list[X], from __future__ import annotations already the house style)
  </read_first>
  <action>
    Run `uv run mypy app/config.py app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py app/__init__.py` first to get the real, current error list for exactly this file set (do not trust RESEARCH.md's directory-level aggregate counts as exact per its own Open Question 1).
    Fix every reported error using the house patterns: add missing param/return annotations (`no-untyped-def`), add type args to bare `dict`/`list` (`type-arg`) using `dict[str, Any]` for genuinely dynamic DB-row/JSONB shapes (D-10) and concrete element types elsewhere, annotate `conn=None` parameters as `conn: psycopg.Connection | None = None` matching `app/db/supabase.py`'s own `psycopg.Connection` usage, and resolve any `Optional`-narrowing (`union-attr`) errors with explicit `if x is not None:` checks rather than assertions or ignores.
    Do not touch `app/db/repo/` in this task — it is Task 2's separate scope (its facade/no_implicit_reexport question needs isolated measurement).
    Preserve `from __future__ import annotations` at the top of every file that already has it; add it to any file in this set that is missing it, matching the codebase-wide house style confirmed in RESEARCH.md.
  </action>
  <acceptance_criteria>
    - `uv run mypy app/config.py app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py app/__init__.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_bootstrap_safe_url.py tests/test_bootstrap_timeouts.py tests/test_check_schema_cli.py tests/test_schema_introspect.py tests/test_seed_roundtrip.py -q` exits 0
    - `git diff` for this task shows only type-annotation additions/narrowing — no assertion or logic changes in any touched file
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/config.py app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py app/__init__.py</automated>
  </verify>
  <done>`uv run mypy app/config.py app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py app/__init__.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Annotate app/db/repo/ package (5 aggregate modules + facade) to mypy-clean, resolve no_implicit_reexport by measurement</name>
  <files>app/db/repo/__init__.py, app/db/repo/_shared.py, app/db/repo/demo.py, app/db/repo/emails.py, app/db/repo/pipeline_state.py, app/db/repo/roster.py, app/db/repo/runs.py</files>
  <read_first>
    app/db/repo/__init__.py (the existing __all__ list, already present — the candidate remediation style if no_implicit_reexport actually fires)
    app/db/repo/_shared.py (shared _conn_ctx/_nulltx plumbing)
    app/db/repo/runs.py, app/db/repo/emails.py, app/db/repo/roster.py, app/db/repo/pipeline_state.py, app/db/repo/demo.py (current annotation state across the 5 aggregate modules)
    .planning/phases/14-full-type-checking-mypy/14-RESEARCH.md (Common Pitfall 1 — no_implicit_reexport very likely does not fire; verify with a real run first, do not pre-emptively restructure exports)
  </read_first>
  <action>
    First, run `uv run mypy app/db/repo/` in isolation and grep the output specifically for `attr-defined`/reexport-shaped errors referencing `app.db.repo` or `app.db`. Per RESEARCH.md Pitfall 1, expect zero such errors (100% of real callers use `from app.db import repo`, the module object, not `from app.db.repo import some_function`). If zero reexport errors are confirmed, do NOT modify `app/db/repo/__init__.py`'s import/export style — note this as verified-unneeded rather than spending effort on `__all__`/`from x import y as y` restructuring. If the real run DOES show reexport errors (contrary to research expectation), fix them using the already-present `__all__` list style consistently (add any missing re-exported names to `__all__`, do not switch to `from x import y as y` for a subset only).
    Fix every remaining reported error (no-untyped-def, type-arg, union-attr, etc.) across the 5 aggregate modules and `_shared.py` using the same house patterns as Task 1: `conn: psycopg.Connection | None = None`, `dict[str, Any]` for DB-row/JSONB dynamic shapes (D-10), concrete element types for lists of UUIDs/employees/etc., explicit None-narrowing instead of ignores.
    Pay particular attention to `runs.py`'s `_ACCENT_CLASS_MAP: dict[str, str]`-style module-level constants (already correctly typed per PATTERNS.md — use as the template for any other bare module-level dict/list constants in the other 4 modules).
  </action>
  <acceptance_criteria>
    - `uv run mypy app/db/repo/` exits 0 with `Success: no issues found`
    - Either (a) `app/db/repo/__init__.py`'s import/export style is UNCHANGED from its pre-task state (if the real run confirmed zero reexport errors), or (b) it is updated using the existing `__all__` list style with a comment noting why (if the real run showed reexport errors) — one of these two must be true, not a mix
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 (full hermetic suite — repo.py's facade is imported nearly everywhere, so this is the correct regression check, not just repo-specific test files)
    - `git diff app/db/repo/` shows only annotation additions/narrowing, no assertion or logic changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/db/repo/</automated>
  </verify>
  <done>`uv run mypy app/db/repo/` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 3: Annotate app/models/ and app/llm/ (incl. prompts/) to mypy-clean</name>
  <files>app/models/__init__.py, app/models/contracts.py, app/models/roster.py, app/models/status.py, app/llm/__init__.py, app/llm/client.py, app/llm/prompts/__init__.py, app/llm/prompts/clarify.py, app/llm/prompts/extract.py, app/llm/prompts/suggest.py</files>
  <read_first>
    app/models/contracts.py (lines 1-80 — already-idiomatic Pydantic v2 style: from __future__ import annotations, Decimal | None unions, Literal, ConfigDict(extra="forbid"))
    app/models/roster.py, app/models/status.py (current annotation state)
    app/llm/client.py (the OpenAI-compatible client wrapper — JSON-mode call signatures)
    app/llm/prompts/clarify.py, app/llm/prompts/extract.py, app/llm/prompts/suggest.py (prompt-builder functions)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Pydantic model typing shared pattern — do not introduce Optional[X]/typing.Dict in new annotations, match the modern X | None / dict[K,V] style)
  </read_first>
  <action>
    Run `uv run mypy app/models/ app/llm/` in isolation to get the real current error list for this file set.
    Fix every reported error following the codebase's own established Pydantic v2 idiom already visible in `contracts.py` (X | None unions, Literal, dict[str, Any] only at genuinely dynamic JSONB/LLM-JSON boundaries per D-10, concrete Decimal/UUID/str types once data crosses into typed model fields). Confirm the pydantic mypy plugin (`plugins = ["pydantic.mypy"]` from Plan 01) is correctly recognizing `ConfigDict(extra="forbid")` semantics — no spurious plugin-related errors should remain after annotation.
    For `app/llm/client.py`'s JSON-mode call wrapper and the three `app/llm/prompts/*.py` builder functions, annotate parameter/return types matching their actual call sites in `app/pipeline/extract.py`/`suggest.py`/`compose_email.py` (read those call sites if a signature's expected shape is ambiguous from the function body alone — do not guess).
  </action>
  <acceptance_criteria>
    - `uv run mypy app/models/ app/llm/` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_models_contracts.py tests/test_llm_client.py tests/test_extract.py tests/test_suggest.py tests/test_clarify.py -q` exits 0
    - `git diff app/models/ app/llm/` shows only annotation additions/narrowing, no assertion or logic changes
    - No new `# type: ignore` comments appear anywhere in this task's diff (D-09 zero-tolerance — these modules have no known D-09-sanctioned exceptions)
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/models/ app/llm/</automated>
  </verify>
  <done>`uv run mypy app/models/ app/llm/` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| psycopg boundary (app/db/repo/*.py) | Raw DB row dicts cross from psycopg into typed repo function returns |
| LLM JSON boundary (app/llm/client.py, app/models/contracts.py) | Raw LLM JSON output crosses into Pydantic-validated contract models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Tampering | Annotation-only edits to app/db/repo/ (DB write/read paths) | mitigate | Every task requires a full hermetic-suite pass (not just file-scoped tests) after touching app/db/repo/, since the facade is imported nearly everywhere; any assertion diff in the test suite is a signal the "annotation-only" boundary was violated |
| T-14-05 | Information Disclosure | dict[str, Any] typing at the psycopg/JSONB boundary (D-10) | accept | Any is the documented, sanctioned choice at genuinely dynamic DB-row/JSONB boundaries per D-10 — narrowing further would require a full schema-typed row mapper, out of this phase's behavior-neutral scope |
</threat_model>

<verification>
Run `uv run mypy app/config.py app/db/ app/models/ app/llm/ app/__init__.py` — exits 0.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, no assertion changes.
</verification>

<success_criteria>
- Zero mypy errors across `app/db/` (including the `repo/` package), `app/models/`, `app/llm/`, `app/config.py`, `app/__init__.py`.
- `no_implicit_reexport` question for the repo facade resolved by direct measurement, documented in the plan SUMMARY.
- Full hermetic test suite green, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-02-SUMMARY.md` when done
</output>
