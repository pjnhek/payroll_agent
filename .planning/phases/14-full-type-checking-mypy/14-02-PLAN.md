---
phase: 14-full-type-checking-mypy
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - app/__init__.py
  - app/config.py
  - app/db/__init__.py
  - app/db/bootstrap.py
  - app/db/check_schema.py
  - app/db/schema_introspect.py
  - app/db/seed.py
  - app/db/supabase.py
  - app/db/repo/__init__.py
  - app/db/repo/_shared.py
  - app/db/repo/demo.py
  - app/db/repo/emails.py
  - app/db/repo/pipeline_state.py
  - app/db/repo/roster.py
  - app/db/repo/runs.py
  - app/models/__init__.py
  - app/models/contracts.py
  - app/models/roster.py
  - app/models/status.py
  - app/llm/__init__.py
  - app/llm/client.py
  - app/llm/prompts/__init__.py
  - app/llm/prompts/clarify.py
  - app/llm/prompts/extract.py
  - app/llm/prompts/suggest.py
autonomous: true
requirements: [TYPE-01]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to app/config.py, app/__init__.py, app/models/, app/llm/, and app/db/ (including the repo/ package)"
    - "Task order follows the import graph: config/models first, then llm, then db — so each task's file-scoped mypy gate only traverses modules already cleaned by an earlier task in this same plan"
    - "The no_implicit_reexport question for app/db/repo/__init__.py's facade is resolved by direct measurement (run mypy first), not by pre-emptively rewriting exports, and the actual result is recorded in the SUMMARY"
    - "No annotation introduces a behavior change — the full hermetic test suite passes unmodified (assertions untouched, only type hints added)"
  artifacts:
    - path: "app/db/repo/__init__.py"
      provides: "facade confirmed clean under strict no_implicit_reexport, or fixed with the minimal required export style if the real run shows errors"
    - path: "app/models/contracts.py"
      provides: "fully strict-annotated Pydantic contract models, pydantic plugin verified working"
      contains: "ConfigDict(extra=\"forbid\")"
  key_links:
    - from: "app/db/repo/*.py"
      to: "app/db/repo/_shared.py"
      via: "internal plumbing imports (_conn_ctx, _nulltx)"
      pattern: "from app.db.repo._shared import"
    - from: "app/db/repo/roster.py, runs.py, pipeline_state.py"
      to: "app/models/"
      via: "domain-model imports — the reason models must be cleaned BEFORE db/repo in this plan's task order"
      pattern: "from app.models"
---

<objective>
Bring the typed substrate of the codebase — `app/config.py`, `app/__init__.py`, `app/models/`, `app/llm/` (including prompts/), and `app/db/` (including the `app/db/repo/` package) — to zero mypy errors under the strict config landed in Plan 14-01. This is the foundation layer every other runtime module imports.

Purpose: mypy follows imports, so a plan's zero-error gate is only schedulable when everything its files import is already clean. These modules import nothing app-internal beyond each other (models: nothing internal; config: nothing internal; llm: config+models; db: config+models), which makes this plan the unique valid Wave 2 — Plans 14-03/04/05 all traverse this substrate and are sequenced after it (addresses Codex review HIGH — the old Wave 2 dependency graph was incompatible with mypy's import traversal).

Output: Zero mypy errors across the listed files; `app/db/repo/__init__.py`'s `no_implicit_reexport` question settled by direct measurement per RESEARCH.md Pitfall 1 with the result recorded in the SUMMARY; full hermetic suite green with no assertion changes.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **HIGH — old Task 2 (db/repo) preceded the models task it imports:** task order is now config/models → llm → db/repo, matching the import graph (`app/db/repo/roster.py:9`, `runs.py:13-14`, `pipeline_state.py:10-11` all import `app.models`).
- **MEDIUM — plan described the DB layer as a prerequisite for siblings but left siblings in the same wave:** Plans 14-03/04/05 now depend on this plan (waves 3-4); nothing that imports this substrate runs concurrently with it.
- **LOW — context and research disagree about re-export behavior:** the direct-measurement approach is retained, and the SUMMARY must record the actual measured result (fired / did not fire) so the disagreement is settled with evidence.

## Artifacts this phase produces (Plan 02)

- No new public symbols — this plan adds annotations, type narrowing, and (only if measurement demands it) `__all__` additions in `app/db/repo/__init__.py`. Any typed module-level constants gaining explicit annotations (e.g. dict/list constants in `app/db/repo/*.py`) keep their existing names.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate app/__init__.py, app/config.py, app/models/, and app/llm/ to mypy-clean (foundation first)</name>
  <files>app/__init__.py, app/config.py, app/models/__init__.py, app/models/contracts.py, app/models/roster.py, app/models/status.py, app/llm/__init__.py, app/llm/client.py, app/llm/prompts/__init__.py, app/llm/prompts/clarify.py, app/llm/prompts/extract.py, app/llm/prompts/suggest.py</files>
  <read_first>
    app/config.py (current state — Settings via pydantic-settings, already mostly typed)
    app/models/contracts.py (lines 1-80 — already-idiomatic Pydantic v2 style: from __future__ import annotations, Decimal | None unions, Literal, ConfigDict(extra="forbid"))
    app/models/roster.py, app/models/status.py (current annotation state)
    app/llm/client.py (the OpenAI-compatible client wrapper — JSON-mode call signatures)
    app/llm/prompts/clarify.py, app/llm/prompts/extract.py, app/llm/prompts/suggest.py (prompt-builder functions)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Pydantic model typing shared pattern — do not introduce Optional[X]/typing.Dict in new annotations, match the modern X | None / dict[K,V] style)
  </read_first>
  <action>
    Order within this task mirrors the import graph (config and models import nothing app-internal; llm imports config/models): fix `app/config.py`, `app/__init__.py`, and `app/models/` first, then `app/llm/`.
    Run `uv run mypy app/__init__.py app/config.py app/models/` first to get the real, current error list for the foundation set (do not trust RESEARCH.md's directory-level aggregate counts as exact per its own Open Question 1). Fix every reported error following the codebase's own established Pydantic v2 idiom already visible in `contracts.py` (X | None unions, Literal, dict[str, Any] only at genuinely dynamic JSONB/LLM-JSON boundaries per D-10, concrete Decimal/UUID/str types once data crosses into typed model fields). Confirm the pydantic mypy plugin (`plugins = ["pydantic.mypy"]` from Plan 14-01) correctly recognizes `ConfigDict(extra="forbid")` semantics — no spurious plugin-related errors should remain after annotation.
    Then run `uv run mypy app/llm/` and fix every reported error there. For `app/llm/client.py`'s JSON-mode call wrapper and the three `app/llm/prompts/*.py` builder functions, annotate parameter/return types matching their actual call sites in `app/pipeline/extract.py`/`suggest.py`/`compose_email.py` (read those call sites if a signature's expected shape is ambiguous from the function body alone — do not guess).
    Preserve `from __future__ import annotations` at the top of every file that already has it; add it to any file in this set missing it, matching the codebase-wide house style confirmed in RESEARCH.md.
  </action>
  <acceptance_criteria>
    - `uv run mypy app/__init__.py app/config.py app/models/ app/llm/` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_models_contracts.py tests/test_llm_client.py -q` exits 0
    - `git diff app/models/ app/llm/ app/config.py app/__init__.py` shows only annotation additions/narrowing, no assertion or logic changes
    - No new `# type: ignore` comments appear anywhere in this task's diff (D-09 zero-tolerance — these modules have no known D-09-sanctioned exceptions)
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/__init__.py app/config.py app/models/ app/llm/</automated>
  </verify>
  <done>`uv run mypy app/__init__.py app/config.py app/models/ app/llm/` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Annotate app/db/ (bootstrap, check_schema, schema_introspect, seed, supabase) to mypy-clean</name>
  <files>app/db/__init__.py, app/db/bootstrap.py, app/db/check_schema.py, app/db/schema_introspect.py, app/db/seed.py, app/db/supabase.py</files>
  <read_first>
    app/db/supabase.py (get_connection's Generator return type — the psycopg.Connection pattern every conn: param elsewhere should match)
    app/db/bootstrap.py, app/db/check_schema.py, app/db/schema_introspect.py, app/db/seed.py (current annotation state)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns: "conn=None params" -> psycopg.Connection | None, bare dict/list -> dict[str, Any]/list[X], from __future__ import annotations already the house style)
  </read_first>
  <action>
    Run `uv run mypy app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py` first to get the real current error list for exactly this file set (these files import app/config and app/models — already clean from Task 1, so the traversal reports only this set's own errors).
    Fix every reported error using the house patterns: add missing param/return annotations (`no-untyped-def`), add type args to bare `dict`/`list` (`type-arg`) using `dict[str, Any]` for genuinely dynamic DB-row/JSONB shapes (D-10) and concrete element types elsewhere, annotate `conn=None` parameters as `conn: psycopg.Connection | None = None` matching `app/db/supabase.py`'s own `psycopg.Connection` usage, and resolve any `Optional`-narrowing (`union-attr`) errors with explicit `if x is not None:` checks rather than assertions or ignores.
    Do not touch `app/db/repo/` in this task — it is Task 3's separate scope (its facade/no_implicit_reexport question needs isolated measurement).
  </action>
  <acceptance_criteria>
    - `uv run mypy app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_bootstrap_safe_url.py tests/test_bootstrap_timeouts.py tests/test_check_schema_cli.py tests/test_schema_introspect.py tests/test_seed_roundtrip.py -q` exits 0
    - `git diff` for this task shows only type-annotation additions/narrowing — no assertion or logic changes in any touched file
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/db/__init__.py app/db/bootstrap.py app/db/check_schema.py app/db/schema_introspect.py app/db/seed.py app/db/supabase.py</automated>
  </verify>
  <done>`uv run mypy` over the six non-repo app/db/ files exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 3: Annotate app/db/repo/ package (5 aggregate modules + facade) to mypy-clean, resolve no_implicit_reexport by measurement</name>
  <files>app/db/repo/__init__.py, app/db/repo/_shared.py, app/db/repo/demo.py, app/db/repo/emails.py, app/db/repo/pipeline_state.py, app/db/repo/roster.py, app/db/repo/runs.py</files>
  <read_first>
    app/db/repo/__init__.py (the existing __all__ list, already present — the candidate remediation style if no_implicit_reexport actually fires)
    app/db/repo/_shared.py (shared _conn_ctx/_nulltx plumbing)
    app/db/repo/runs.py, app/db/repo/emails.py, app/db/repo/roster.py, app/db/repo/pipeline_state.py, app/db/repo/demo.py (current annotation state across the 5 aggregate modules)
    .planning/phases/14-full-type-checking-mypy/14-RESEARCH.md (Common Pitfall 1 — no_implicit_reexport very likely does not fire; verify with a real run first, do not pre-emptively restructure exports)
  </read_first>
  <action>
    First, run `uv run mypy app/db/repo/` in isolation and grep the output specifically for `attr-defined`/reexport-shaped errors referencing `app.db.repo` or `app.db`. Per RESEARCH.md Pitfall 1, expect zero such errors (100% of real callers use `from app.db import repo`, the module object, not `from app.db.repo import some_function`). If zero reexport errors are confirmed, do NOT modify `app/db/repo/__init__.py`'s import/export style — record "verified-unneeded" WITH the actual measured output in the plan SUMMARY (addresses Codex review LOW — the context/research disagreement about re-export behavior is settled by recording the real result, not left implicit). If the real run DOES show reexport errors (contrary to research expectation), fix them using the already-present `__all__` list style consistently (add any missing re-exported names to `__all__`, do not switch to a from-import-as style for a subset only), and record that outcome in the SUMMARY instead.
    Fix every remaining reported error (no-untyped-def, type-arg, union-attr, etc.) across the 5 aggregate modules and `_shared.py` using the same house patterns as Task 2: `conn: psycopg.Connection | None = None`, `dict[str, Any]` for DB-row/JSONB dynamic shapes (D-10), concrete element types for lists of UUIDs/employees/etc., explicit None-narrowing instead of ignores. These files import `app.models` — clean since Task 1 — so the file-scoped gate reports only this package's own errors (addresses Codex review HIGH — old ordering had this task before the models it imports).
    Pay particular attention to `runs.py`'s module-level typed dict constants (already correctly typed per PATTERNS.md — use as the template for any other bare module-level dict/list constants in the other 4 modules).
  </action>
  <acceptance_criteria>
    - `uv run mypy app/db/repo/` exits 0 with `Success: no issues found`
    - Either (a) `app/db/repo/__init__.py`'s import/export style is UNCHANGED from its pre-task state (if the real run confirmed zero reexport errors), or (b) it is updated using the existing `__all__` list style with a comment noting why (if the real run showed reexport errors) — one of these two must be true, not a mix; the measured result is recorded in the SUMMARY either way
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 (full hermetic suite — the repo facade is imported nearly everywhere, so this is the correct regression check, not just repo-specific test files)
    - `git diff app/db/repo/` shows only annotation additions/narrowing, no assertion or logic changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/db/repo/</automated>
  </verify>
  <done>`uv run mypy app/db/repo/` exits 0 with `Success: no issues found`, reexport question settled by measurement and recorded</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| psycopg boundary (app/db/repo/*.py) | Raw DB row dicts cross from psycopg into typed repo function returns |
| LLM JSON boundary (app/llm/client.py, app/models/contracts.py) | Raw LLM JSON output crosses into Pydantic-validated contract models |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | Annotation-only edits to app/db/repo/ (DB write/read paths) | high | mitigate | Task 3 requires a full hermetic-suite pass (not just file-scoped tests) after touching app/db/repo/, since the facade is imported nearly everywhere; any assertion diff in the test suite is a signal the "annotation-only" boundary was violated |
| T-14-05 | Information Disclosure | dict[str, Any] typing at the psycopg/JSONB boundary (D-10) | low | accept | Any is the documented, sanctioned choice at genuinely dynamic DB-row/JSONB boundaries per D-10 — narrowing further would require a full schema-typed row mapper, out of this phase's behavior-neutral scope |
</threat_model>

<verification>
Run `uv run mypy app/__init__.py app/config.py app/models/ app/llm/ app/db/` — exits 0.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, no assertion changes.
Confirm the SUMMARY records the measured no_implicit_reexport outcome.
</verification>

<success_criteria>
- Zero mypy errors across `app/config.py`, `app/__init__.py`, `app/models/`, `app/llm/`, and `app/db/` (including the `repo/` package).
- Task execution order followed the import graph (config/models → llm → db), so every file-scoped gate was schedulable.
- `no_implicit_reexport` question for the repo facade resolved by direct measurement, actual result documented in the plan SUMMARY.
- Full hermetic test suite green, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-02-SUMMARY.md` when done
</output>
