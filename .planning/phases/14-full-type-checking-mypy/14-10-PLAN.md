---
phase: 14-full-type-checking-mypy
plan: 10
type: execute
wave: 7
depends_on: ["14-09"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md
autonomous: false
requirements: [TYPE-03]

must_haves:
  truths:
    - "ci.yml has a third parallel job named typecheck that runs bare uv run mypy, mirroring the lint job's exact recipe"
    - "A push with a deliberately injected type error demonstrably fails the typecheck job (red), captured as evidence"
    - "The real master branch's typecheck job run is green, captured as evidence"
    - "The throwaway red-proof branch is deleted after evidence capture (locally AND on origin, with master checked out first), per Phase 12's D-14 precedent"
    - "14-VERIFICATION.md is committed and pushed to master AFTER the checkpoint — the evidence file's lifecycle completes inside this plan, not as an untracked loose end"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "third typecheck job alongside lint and test"
      contains: "Run mypy"
    - path: ".planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md"
      provides: "red-proof evidence: red run URL + green run URL for the typecheck job, committed to master"
  key_links:
    - from: ".github/workflows/ci.yml typecheck job"
      to: "pyproject.toml [tool.mypy]"
      via: "bare uv run mypy command, config-driven scope"
      pattern: "run: uv run mypy"
---

<objective>
Wire mypy into CI as a third, blocking, named job (`typecheck`) alongside the existing `lint` and `test` jobs, and produce the same red/green red-proof evidence pattern Phase 12 used for its own gates — completing TYPE-03 and closing out the phase.

Purpose: TYPE-01 and TYPE-02 (landed across Plans 14-01..14-09) prove the codebase IS type-clean; TYPE-03 proves that guarantee is ENFORCED and can't silently regress. This is the last plan in the phase — it only runs after Plan 14-09's integration gate has already run the exact same bare `uv run mypy` command green locally (D-06 byte-for-byte parity means the CI job's first master run should be green by construction).

Output: `.github/workflows/ci.yml` with a new `typecheck` job; a red-proof branch pushed with an exactly specified single-cause type error, its failing run URL captured, then deleted (master checked out first); the real master branch's green `typecheck` run URL captured; both recorded in `14-VERIFICATION.md`, which is then committed and pushed in a final post-checkpoint task.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **MEDIUM — no post-checkpoint task to commit 14-VERIFICATION.md:** Task 4 (new, automated, post-approval) commits and pushes the evidence file and confirms the final master run stays green — the evidence lifecycle now completes inside the plan.
- **MEDIUM — branch cleanup omitted checking out master before deleting the currently checked-out throwaway branch:** the checkpoint steps now explicitly `git checkout master` BEFORE `git branch -D`.
- **LOW — open-ended "loose-end fixes" authorization in Task 2:** removed. Task 2's scope is strictly `.github/workflows/ci.yml`; if the pre-push state is not clean, the executor stops and reports rather than folding unrelated fixes into the CI commit (Plan 14-09 already guaranteed a clean, green master).
- **LOW — no exact isolated type-error snippet for repeatable red-proofing:** the checkpoint now specifies the exact single-line injection (a function whose declared int return type is contradicted by a returned string in `app/config.py`) — mypy-only, invisible to ruff's E/F/I/B/UP/SIM ruleset, unreferenced by any test.
- **LOW — "failing Actions job blocks merge" terminology:** phrased precisely — the phase criterion requires a type-error push to FAIL CI (a red named check); repository branch-protection settings are out of scope.

## Artifacts this phase produces (Plan 10)

- `.github/workflows/ci.yml` — new `typecheck:` job (name: "Type check (mypy --strict)") with steps Checkout / Set up uv + Python 3.12 / Install deps / Run mypy
- `.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` — phase evidence file (green master run URL, red red-proof run URL, injected-error description)
- Temporary (deleted before plan completion): branch `red-proof/mypy-14` carrying a one-line function `_red_proof_type_error` in `app/config.py`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@.planning/phases/12-ci-quality-gates/12-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the typecheck job to ci.yml, mirroring the lint job's exact recipe</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (the existing lint job, lines 17-33 — the exact copy-paste template per D-05)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (the typecheck job YAML already drafted in the pattern map)
    .planning/phases/12-ci-quality-gates/12-CONTEXT.md (the house CI recipe: pinned action SHAs, --locked sync, named jobs)
  </read_first>
  <action>
    Add a new `typecheck` job to `.github/workflows/ci.yml`, placed after the existing `test` job, with the exact same shape as `lint`: `name: "Type check (mypy --strict)"`, `runs-on: ubuntu-latest`, checkout step using the SAME pinned SHA already in the file (`actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5`), `astral-sh/setup-uv` step using the SAME pinned SHA already in the file (`astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86`) with `python-version: "3.12"`, `uv sync --locked` install step, then a final step `name: Run mypy` with `run: uv run mypy` — the bare command, zero arguments (D-06 requires scope to live only in `pyproject.toml`'s `files=` key so local and CI invocations agree byte-for-byte; this is the exact command Plan 14-09 already ran green locally).
    Do not modify the file-level `permissions: contents: read` or `concurrency:` blocks — these already apply repo-wide to every job.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `typecheck:` job with `name: "Type check (mypy --strict)"`
    - The `typecheck` job's checkout and setup-uv steps use the identical pinned SHAs as the `lint`/`test` jobs (byte-for-byte match on the `uses:` lines)
    - The `typecheck` job's final step is `run: uv run mypy` with zero arguments
    - `git diff .github/workflows/ci.yml` shows only the new job added, zero changes to `lint`/`test`/file-level blocks
  </acceptance_criteria>
  <verify>
    <automated>grep -q "typecheck:" .github/workflows/ci.yml && grep -q "run: uv run mypy" .github/workflows/ci.yml</automated>
  </verify>
  <done>`.github/workflows/ci.yml` contains a `typecheck:` job whose final step is bare `run: uv run mypy`</done>
</task>

<task type="auto">
  <name>Task 2: Push master, confirm the real typecheck job runs green</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (post-Task-1 state, to confirm the job is correctly wired before pushing)
  </read_first>
  <action>
    Commit Task 1's ci.yml change — and ONLY that change; this task's file scope is strictly `.github/workflows/ci.yml` (the open-ended loose-end-fix authorization was removed per Codex review — Plan 14-09 already guaranteed master is mypy-clean and suite-green; if `git status` shows any unexpected dirty file, stop and report instead of committing it here). Push to origin master. Poll the GitHub Actions run for this push (via `gh run list`/`gh run view`) until it completes. Confirm all three jobs (`lint`, `test`, `typecheck`) report conclusion success.
    Record the run's URL for `14-VERIFICATION.md` (written at the Task 3 checkpoint, committed in Task 4).
  </action>
  <acceptance_criteria>
    - `git log -1 --stat` shows a commit touching only `.github/workflows/ci.yml`
    - `git push` to master succeeds
    - `gh run list --branch master --limit 1` shows the triggered run
    - `gh run view <run-id>` shows all three jobs (`lint`, `test`, `typecheck`) with conclusion `success`
  </acceptance_criteria>
  <verify>
    <automated>gh run list --branch master --limit 1</automated>
  </verify>
  <done>Master push triggers a run where lint, test, and typecheck all conclude success</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Red-proof the typecheck CI gate</name>
  <files>.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md</files>
  <what-built>The typecheck CI job is wired and green on master. This checkpoint red-proofs it per Phase 12's D-07/D-14 pattern: push a throwaway branch with a deliberately injected, exactly specified type error, confirm the typecheck job goes red (and ONLY that job — lint/test stay unaffected by a pure type error), capture both run URLs, then delete the branch with master checked out first.</what-built>
  <action>Human-gated red-proof of the typecheck CI gate (D-07): execute the numbered steps in how-to-verify — create branch red-proof/mypy-14, inject the exact single-cause snippet specified in step 2 into app/config.py, push, confirm typecheck fails while lint/test conclude success, capture both run URLs, delete the branch (master checked out first), and write 14-VERIFICATION.md. Do not commit the evidence file — Task 4 commits it post-approval.</action>
  <how-to-verify>
    1. Create a throwaway branch `red-proof/mypy-14` off master.
    2. Inject the exact single-cause error (repeatable snippet per Codex review): append to the END of `app/config.py` a two-line function — `def _red_proof_type_error() -> int:` with body `return "deliberately-not-an-int"`. This is mypy-only by construction: mypy flags it as an incompatible return value (str vs int); ruff's E/F/I/B/UP/SIM ruleset has no rule for return-type mismatches and the function is never imported, so lint stays green; no test references it, so pytest stays green (single-cause isolation, matching Phase 12 D-14's precedent).
    3. Commit and push the branch. Confirm via `gh run list --branch red-proof/mypy-14` that the run's `typecheck` job fails while `lint` and `test` conclude success.
    4. Capture the red run's URL.
    5. Delete the throwaway branch — IN THIS ORDER (per Codex review — never delete the branch you have checked out): first `git checkout master`, then `git push origin --delete red-proof/mypy-14`, then `git branch -D red-proof/mypy-14`. Branch deletion does not delete the recorded Actions run history (Phase 12 D-14 precedent).
    6. Write `.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` recording: the green master run URL (from Task 2), the red red-proof-branch run URL (this checkpoint), the exact injected snippet from step 2, and confirmation that lint/test stayed green on the red run. Note precisely: a red `typecheck` job is a failing named CI check on that push (the TYPE-03 criterion); whether it also blocks a merge depends on branch-protection settings, which are out of this phase's scope.
    7. Do NOT commit the file yet — Task 4 (automated) commits and pushes it after your approval.
  </how-to-verify>
  <resume-signal>Reply "approved" once both run URLs are captured in 14-VERIFICATION.md and the throwaway branch is deleted locally and on origin, or describe any issue (e.g. the injected error also broke ruff/pytest, meaning it wasn't single-cause and needs a different injection).</resume-signal>
  <verify>
    <automated>MISSING — this is a CI-level red-proof exercise with no pytest equivalent; verification is the human confirming both run URLs in 14-VERIFICATION.md per how-to-verify above (Task 4 then verifies the file's committed state automatically)</automated>
  </verify>
  <done>14-VERIFICATION.md exists on disk with the green master run URL, the red red-proof-branch run URL, the exact injected snippet, and confirmation lint/test stayed green on the red run; the throwaway branch is deleted locally and on origin with master checked out.</done>
</task>

<task type="auto">
  <name>Task 4: Commit and push the red-proof evidence, confirm final master state (post-checkpoint)</name>
  <files>.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md</files>
  <read_first>
    .planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md (the evidence file written at the Task 3 checkpoint — confirm both run URLs and the injected-snippet description are present before committing)
  </read_first>
  <action>
    Post-approval evidence lifecycle (addresses Codex review MEDIUM — the old plan pushed master before the checkpoint and then never committed the evidence file): confirm `git branch --show-current` prints `master`; confirm the red-proof branch is gone locally (`git branch --list red-proof/mypy-14` prints nothing) and on origin (`git ls-remote --heads origin red-proof/mypy-14` prints nothing). Commit `.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` (and the plan SUMMARY if written by now) as a docs commit, push to origin master, and poll the triggered run with `gh run view` to confirm all three jobs (`lint`, `test`, `typecheck`) conclude success on this final push — the phase ends with a green master carrying the committed evidence.
  </action>
  <acceptance_criteria>
    - `git branch --show-current` prints `master`
    - `git ls-remote --heads origin red-proof/mypy-14` produces no output (remote branch deleted)
    - `git status --porcelain .planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` produces no output (file committed, working tree clean for it)
    - `git push` succeeds and `gh run view` on the resulting run shows `lint`, `test`, `typecheck` all `success`
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md && git ls-remote --heads origin red-proof/mypy-14 | wc -l | grep -q "^ *0$"</automated>
  </verify>
  <done>14-VERIFICATION.md is committed and pushed to master, the red-proof branch is gone from origin, and the final master run is green across all three jobs</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI workflow file (.github/workflows/ci.yml) | Modifying CI configuration is itself a supply-chain-sensitive change — a malicious or careless edit here could weaken the security posture of every future push |
| Throwaway red-proof branch | A temporary branch pushed to the real origin remote, deleted after evidence capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-17 | Tampering | ci.yml typecheck job addition | medium | mitigate | The new job reuses the exact same pinned action SHAs already vetted for `lint`/`test` (Phase 12's own supply-chain review) — no new third-party Action is introduced, only a new step running an already-vetted-this-phase tool (mypy); Task 2's diff criterion proves nothing else in the workflow changed |
| T-14-18 | Elevation of Privilege | Red-proof branch push to origin | low | accept | Push-then-delete is the same pattern Phase 12 D-14 already used and the user already accepted; the `permissions: contents: read` block at file level means the workflow itself has no write access regardless |
| T-14-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new package-manager installs in this plan (mypy was already added in Plan 14-01, verified via the research session's Package Legitimacy Audit); this plan only adds a CI step invoking the already-installed tool |
</threat_model>

<verification>
`gh run view` on the real master push shows `lint`, `test`, `typecheck` all green.
`gh run view` on the red-proof branch push shows `typecheck` red while `lint`/`test` stay green (single-cause isolation).
`.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` records both run URLs and the exact injected snippet, and is COMMITTED to master (Task 4).
`git ls-remote --heads origin red-proof/mypy-14` is empty.
</verification>

<success_criteria>
- `.github/workflows/ci.yml` has a third, blocking, named `typecheck` job identical in shape to `lint`/`test`, running bare `uv run mypy`.
- Real master push is green across all three jobs.
- A red-proof push with the exactly specified single-cause snippet demonstrates the `typecheck` job fails on a genuine type error, isolated from `lint`/`test`.
- Evidence (both run URLs, the injected snippet) is committed AND pushed in `14-VERIFICATION.md` after the checkpoint.
- Throwaway branch deleted locally and on origin, with master checked out, and the final master run is green.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-10-SUMMARY.md` when done
</output>
