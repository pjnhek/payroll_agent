---
phase: 14-full-type-checking-mypy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - app/pipeline/federal_withholding.py
  - app/email/gateway.py
  - eval/run_eval.py
  - tests/test_gateway.py
  - tests/test_eval_wiring.py
autonomous: true
requirements: [TYPE-01, TYPE-02]

must_haves:
  truths:
    - "A committed mypy config exists so every subsequent task checks against the real, final scope/strictness rules (not an ad hoc per-task config)"
    - "The one genuine reachable bug mypy's strict mode surfaces (eval/run_eval.py's undefined llm_client import on the --record path) is fixed in its own test-first commit, separate from any annotation commit"
    - "gateway.py's ReceivedEmail type-level mismatch (TypedDict declared, ResponseDict with __getattr__ shim returned at runtime) is resolved with a typing-only fix (cast), since the runtime attribute access is already correct and must not change"
    - "The one pre-existing mechanical widen-to-object return type (federal_withholding.py's _find_bracket) is corrected to its real concrete type"
    - "D-04: the reportlab.* override's inline justification comment doubles as the documented policy — any future untyped dependency gets the same scoped [[tool.mypy.overrides]] + justification-comment pattern, never a global ignore_missing_imports"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.mypy] block with strict=true, pydantic plugin, files=[app,eval,scripts,tests], plus tests.* and reportlab.* overrides"
      contains: "[tool.mypy]"
    - path: "app/pipeline/federal_withholding.py"
      provides: "_find_bracket annotated to return BracketRow, not object"
      contains: "-> BracketRow"
    - path: "eval/run_eval.py"
      provides: "corrected llm_client import matching the rest of the codebase"
      contains: "from app.llm import client as llm_client"
    - path: "app/email/gateway.py"
      provides: "typed access to resend.EmailsReceiving.get()'s ResponseDict result via cast, no behavior change"
      contains: "cast("
  key_links:
    - from: "pyproject.toml [tool.mypy]"
      to: "uv run mypy"
      via: "bare command reads files= from config, no CLI args"
      pattern: "files = \\[.app.*eval.*scripts.*tests.\\]"
    - from: "eval/run_eval.py _record_extraction"
      to: "app/llm/client.py module object"
      via: "module-object import matching extract.py/suggest.py/compose_email.py"
      pattern: "from app.llm import client as llm_client"
---

<objective>
Author the mypy configuration this entire phase's remaining work depends on, and land the three pre-identified fixes that are NOT bulk mechanical annotation work: two genuine-or-suspected runtime issues surfaced by mypy strict mode (D-08 test-first protocol) and one pre-existing "widened to object" return-type bug that is purely mechanical but has a specific, already-known fix (research Pattern 2).

Purpose: Every later plan in this phase runs `uv run mypy` against the real, final, committed config — not a placeholder — so error counts and fixes are measured against ground truth from the first bulk-annotation task onward. The two D-08 items are isolated here (in files no other Wave 2 plan touches) so they land as clean, separately-reviewable commits before the mechanical noise of annotation work begins.

Output: `pyproject.toml` with `[tool.mypy]` + both `[[tool.mypy.overrides]]` blocks; `mypy` added as a dev dependency; `eval/run_eval.py`'s import bug fixed with a regression test; `app/email/gateway.py`'s TypedDict/ResponseDict mismatch resolved via `cast()`; `app/pipeline/federal_withholding.py`'s `_find_bracket` returning a concrete `BracketRow`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author [tool.mypy] config and add mypy as a dev dependency</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    pyproject.toml (current [tool.ruff]/[tool.ruff.lint]/[dependency-groups] blocks — the comment-citing-decision-ID convention to match)
    .planning/phases/14-full-type-checking-mypy/14-RESEARCH.md (Pattern 1's exact TOML, Code Examples' census commands)
    .planning/phases/14-full-type-checking-mypy/14-CONTEXT.md (D-01 through D-06, D-10)
  </read_first>
  <action>
    Run `uv add --dev mypy` (installs mypy 2.2.0 per RESEARCH.md's verified version; updates pyproject.toml [dependency-groups].dev and uv.lock together — never hand-edit either file for this).
    Add a `[tool.mypy]` block to pyproject.toml, placed after the existing `[tool.ruff.lint]` block, with: `python_version = "3.12"`, `strict = true`, `plugins = ["pydantic.mypy"]`, `files = ["app", "eval", "scripts", "tests"]`. Add inline comments citing D-01 (strict scope) and D-06 (files= scope, byte-for-byte local/CI parity) matching the existing `[tool.ruff]` block's D-XX comment convention.
    Add two `[[tool.mypy.overrides]]` blocks immediately after: one for `module = "tests.*"` setting `check_untyped_defs = true`, `disallow_untyped_defs = false`, `disallow_incomplete_defs = false`, `disallow_untyped_decorators = false` (cite D-02 inline — every test body still checked, only annotation requirement relaxed); one for `module = "reportlab.*"` setting `ignore_missing_imports = true` (cite D-03 inline — reportlab ships no py.typed, verified this phase's research, no global ignore).
    Do not run a full annotation pass yet — this task only lands the config. Confirm the config parses by running `uv run mypy --config-file pyproject.toml app/config.py` (a single small file) and observing it does not error on config-parsing (real annotation errors in that one file are expected and out of scope for this task).
  </action>
  <acceptance_criteria>
    - `pyproject.toml` contains a `[tool.mypy]` table with `strict = true` and `plugins = ["pydantic.mypy"]`
    - `pyproject.toml` contains exactly two `[[tool.mypy.overrides]]` tables, for `tests.*` and `reportlab.*` respectively
    - `pyproject.toml [dependency-groups].dev` lists `mypy` (added via `uv add --dev mypy`, not hand-typed)
    - `uv run mypy --version` exits 0 and prints `mypy 2.2.0` (or the version `uv add --dev mypy` actually resolved)
    - `git diff uv.lock` shows mypy + its transitive deps added, no unrelated changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy --version</automated>
  </verify>
  <done>`pyproject.toml` contains a `[tool.mypy]` table with `strict = true` and `plugins = ["pydantic.mypy"]`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix eval/run_eval.py's undefined llm_client import (D-08 bug, test-first)</name>
  <files>eval/run_eval.py, tests/test_eval_wiring.py</files>
  <read_first>
    eval/run_eval.py (lines ~706-749, `_record_extraction`'s current `from app.llm.client import llm_client` import)
    app/pipeline/extract.py (lines 26-37, the correct module-object import pattern: `from app.llm import client as llm_client`)
    tests/test_eval_wiring.py (existing test file and fixtures for this module, to match style)
    app/llm/client.py (confirm no module-level `llm_client` attribute exists — only functions/classes)
  </read_first>
  <behavior>
    - Test: importing `eval.run_eval` and calling its internal `_record_extraction` (or a narrower unit that exercises just the import line, e.g. asserting `eval.run_eval._record_extraction.__code__` references a valid name, or directly invoking the lazy import statement in isolation) must NOT raise `ImportError`/`AttributeError` for `llm_client`.
    - Before the fix: `from app.llm.client import llm_client` raises `ImportError: cannot import name 'llm_client' from 'app.llm.client'` — write a test that triggers this lazy import path and currently fails with that error.
    - After the fix: the same test passes because `from app.llm import client as llm_client` binds the module object, matching `extract.py`/`suggest.py`/`compose_email.py`.
  </behavior>
  <action>
    In `tests/test_eval_wiring.py`, add a regression test that exercises `eval/run_eval.py`'s `_record_extraction` lazy-import statement directly (e.g. via `importlib` re-exec of the function body's import, or by monkeypatching `_require_live_llm`/`FIXTURE_DIR` minimally and calling `_record_extraction` with zero fixtures so the import executes but the loop body is a no-op) and asserts no `ImportError` occurs. Run it first to confirm it fails with `ImportError: cannot import name 'llm_client'`.
    Fix `eval/run_eval.py` line 724: replace `from app.llm.client import llm_client  # noqa: PLC0415` with `from app.llm import client as llm_client  # noqa: PLC0415`, matching the exact pattern already used in `app/pipeline/extract.py:26`, `app/pipeline/suggest.py:31`, `app/pipeline/compose_email.py:21`.
    Commit the failing test and the fix as two separate commits per D-08 (test-first, then minimal fix) — do not bundle with Task 1's config changes or any annotation work.
  </action>
  <acceptance_criteria>
    - `tests/test_eval_wiring.py` contains a new test function asserting the `_record_extraction` import path resolves without `ImportError`
    - `uv run pytest tests/test_eval_wiring.py -q` exits 0 (all tests in the file pass, including the new one)
    - `eval/run_eval.py` contains `from app.llm import client as llm_client` and no longer contains `from app.llm.client import llm_client`
    - `git log --oneline -3` shows the test-first commit followed by the fix commit as distinct commits
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_wiring.py -q</automated>
  </verify>
  <done>`tests/test_eval_wiring.py` contains a new test function asserting the `_record_extraction` import path resolves without `ImportError`</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Resolve gateway.py's ReceivedEmail TypedDict/ResponseDict type mismatch + fix federal_withholding.py's object return type</name>
  <files>app/email/gateway.py, tests/test_gateway.py, app/pipeline/federal_withholding.py</files>
  <read_first>
    app/email/gateway.py (lines 105-172, `parse_inbound`/`_parse_resend_envelope` — the attribute-access reads on `email_obj`)
    tests/test_gateway.py (existing `FakeReceivedEmail`/fixture around line 1144 mirroring `resend.EmailsReceiving.get`'s shape; existing signature-verification tests to re-run after the change)
    app/pipeline/federal_withholding.py (lines 33-60, `_find_bracket`'s current `-> object` signature and its 3 call sites at ~line 139-141)
    app/pipeline/tax_tables_2026.py (lines 24-38, the `BracketRow(NamedTuple)` definition already used by the same subsystem)
  </read_first>
  <behavior>
    - Test (gateway): a fixture shaped like `resend`'s real runtime return (a `dict`-like object supporting both `["headers"]` and `.headers` access, i.e. `resend.response.ResponseDict`) is passed through `_parse_resend_envelope`, and the resulting `InboundEmail.message_id`/`in_reply_to`/`body_text` match the fixture's values exactly — this must already pass today (runtime behavior is correct); the fix only changes the type annotation/cast, not behavior.
    - Test (federal_withholding): `_find_bracket` returns an object with `.lower`/`.rate`/`.base` accessible without a type-checker complaint, and remains functionally identical for all existing bracket-lookup test cases.
  </behavior>
  <action>
    Confirmed by this phase's research: `resend.EmailsReceiving.get()` returns `resend.response.ResponseDict` (a `dict` subclass with `__getattr__` forwarding to key lookup) cast to the `ReceivedEmail` TypedDict at the type level — so `email_obj.headers`/`.message_id`/`.text` in `_parse_resend_envelope` are runtime-correct today and must NOT change to bracket access (that would be an unnecessary, unproven behavior-risking edit). The mypy error here is a type-level gap only: mypy sees the declared `ReceivedEmail` TypedDict return type and correctly flags attribute access on a TypedDict as invalid, even though the real runtime object supports it via `ResponseDict.__getattr__`.
    Resolve with `typing.cast`: import `from typing import cast` in `app/email/gateway.py`, and at the point `email_obj = resend.EmailsReceiving.get(email_id)` in `_parse_resend_envelope`, add a local variable typed via `cast` to `Any` (or a small local `Protocol`/`TypedDict`-with-total=False capturing just `headers`/`message_id`/`text` if a `Protocol` reads cleaner) so subsequent attribute access type-checks without changing runtime behavior. Do not add a bare `# type: ignore` here — D-09 requires fixing the type properly since the correct target type (a dict-with-attribute-access proxy) is knowable, not genuinely dynamic in the D-10 sense.
    Add one comment at the `email_obj = ...` line stating why: resend's `EmailsReceiving.get()` is typed to return the `ReceivedEmail` TypedDict but actually returns `resend.response.ResponseDict` (verified in Phase 14 research — a dict subclass with `__getattr__` attribute-access shimming), so the cast documents the real runtime shape without changing it.
    Re-run `tests/test_gateway.py`'s full suite (not just new tests) to confirm zero regression to `verify()`'s signature-checking tests, per the Security Domain note in RESEARCH.md (V5 Input Validation).
    Separately in `app/pipeline/federal_withholding.py`: change `_find_bracket(annual_wage: Decimal, brackets: list) -> object` to `_find_bracket(annual_wage: Decimal, brackets: list[BracketRow]) -> BracketRow`, adding `BracketRow` to the existing `from app.pipeline.tax_tables_2026 import (...)` block. This is a pure annotation change — no logic edits. Bundle this in the same commit as the gateway.py cast only if convenient; otherwise as its own annotation-only commit (not a D-08 bug — this is Pattern 2's mechanical fix, no test-first cycle needed since behavior is provably unchanged).
  </action>
  <acceptance_criteria>
    - `app/email/gateway.py` contains `from typing import cast` (or a `Protocol` import) and a comment near `resend.EmailsReceiving.get` explaining the ResponseDict/TypedDict mismatch
    - `app/email/gateway.py`'s attribute-access lines (`.headers`, `.message_id`, `.text`) are UNCHANGED (no switch to bracket access) — runtime behavior is provably identical
    - `uv run mypy app/email/gateway.py` no longer reports `attr-defined` errors on `email_obj.headers`/`.message_id`/`.text` (some other errors in the same file may remain — those belong to the bulk annotation plans, not this task)
    - `uv run pytest tests/test_gateway.py -q` exits 0 (all existing tests, including signature-verification tests, still pass)
    - `app/pipeline/federal_withholding.py` contains `def _find_bracket(annual_wage: Decimal, brackets: list[BracketRow]) -> BracketRow:` and no longer contains `-> object`
    - `uv run pytest tests/test_federal_withholding.py tests/test_tax_tables_2026.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/email/gateway.py</automated>
  </verify>
  <done>`app/email/gateway.py` contains `from typing import cast` (or a `Protocol` import) and a comment near `resend.EmailsReceiving.get` explaining the ResponseDict/TypedDict mismatch</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI/dev-machine -> PyPI | `uv add --dev mypy` pulls a new package + transitive deps from PyPI into the dev dependency group |
| Inbound webhook -> gateway.py parse_inbound | Untrusted inbound email payload crosses into `_parse_resend_envelope`'s attribute/cast-typed access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | `uv add --dev mypy` supply chain | mitigate | mypy is a 13-year-old, extremely high-download PyPI package with an official `python/mypy` GitHub repo; RESEARCH.md's Package Legitimacy Audit already verified it directly via `uv add --dev mypy` resolution this research session — Approved disposition, no blocking checkpoint needed (dev-only tooling dep, not shipped in the runtime Docker image) |
| T-14-02 | Tampering | `app/email/gateway.py` cast at `email_obj = resend.EmailsReceiving.get(...)` | mitigate | The cast must not silently widen to `Any` in a way that masks a genuine future field-access bug; scope the cast/Protocol narrowly to only the fields actually read (`headers`, `message_id`, `text`), and re-run `tests/test_gateway.py`'s full suite (including signature-verification tests) to confirm no incidental behavior change |
| T-14-03 | Repudiation | eval/run_eval.py import fix | accept | Low risk — the `--record` path is manual/local-only, never exercised by hermetic CI or the live production pipeline; the regression test closes the gap going forward |
</threat_model>

<verification>
Run `uv run mypy --version` to confirm mypy is installed and config parses.
Run `uv run pytest tests/test_eval_wiring.py tests/test_gateway.py tests/test_federal_withholding.py tests/test_tax_tables_2026.py -q` — all pass.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite still green (no regression from any of the three fixes).
Inspect `git log` for at least 3 distinct commits: config, eval/run_eval.py test-first fix, gateway.py+federal_withholding.py fix.
</verification>

<success_criteria>
- `pyproject.toml` has a real, final `[tool.mypy]` config with both overrides that every later plan checks against.
- `mypy` is a committed dev dependency via `uv add --dev mypy` (not hand-edited).
- The one genuine reachable bug (`eval/run_eval.py`'s `llm_client` import) is fixed with a preceding failing regression test, in its own commit.
- The gateway.py type-level mismatch is resolved via `cast`/`Protocol` with zero behavior change, verified by the full `test_gateway.py` suite staying green.
- `federal_withholding.py`'s `_find_bracket` returns the concrete `BracketRow` type.
- Full hermetic test suite (614+ tests) remains green throughout.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-01-SUMMARY.md` when done
</output>
