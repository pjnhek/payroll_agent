---
phase: 14-full-type-checking-mypy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - app/pipeline/federal_withholding.py
  - app/email/gateway.py
  - eval/run_eval.py
  - tests/test_eval_wiring.py
autonomous: true
requirements: [TYPE-01, TYPE-02]

must_haves:
  truths:
    - "A committed mypy config exists so every subsequent task checks against the real, final scope/strictness rules (not an ad hoc per-task config)"
    - "The one genuine reachable bug mypy's strict mode surfaces (eval/run_eval.py's undefined llm_client import on the --record path) is fixed in its own test-first commit, separate from any annotation commit"
    - "gateway.py's ReceivedEmail type-level mismatch (TypedDict declared, ResponseDict with __getattr__ shim returned at runtime) is resolved with a typing-only fix — a narrow Protocol capturing exactly the three attributes read (headers, message_id, text) plus a cast — since the runtime attribute access is already correct and must not change"
    - "The one pre-existing mechanical widen-to-object return type (federal_withholding.py's _find_bracket) is corrected to its real concrete type"
    - "D-04: the reportlab.* override's inline justification comment doubles as the documented policy — any future untyped dependency gets the same scoped [[tool.mypy.overrides]] + justification-comment pattern, never a global ignore_missing_imports"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.mypy] block with strict=true, pydantic plugin, files=[app,eval,scripts,tests], plus tests.* and reportlab.* overrides"
      contains: "[tool.mypy]"
    - path: "app/pipeline/federal_withholding.py"
      provides: "_find_bracket annotated to return BracketRow, not a widened base type"
      contains: "-> BracketRow"
    - path: "eval/run_eval.py"
      provides: "corrected llm_client import matching the rest of the codebase"
      contains: "from app.llm import client as llm_client"
    - path: "app/email/gateway.py"
      provides: "typed access to resend.EmailsReceiving.get()'s ResponseDict result via a narrow Protocol + cast, no behavior change"
      contains: "cast("
  key_links:
    - from: "pyproject.toml [tool.mypy]"
      to: "uv run mypy"
      via: "bare command reads files= from config, no CLI args"
      pattern: "files = \\[.app.*eval.*scripts.*tests.\\]"
    - from: "eval/run_eval.py _record_extraction"
      to: "app/llm/client.py module object"
      via: "module-object import matching extract.py/suggest.py/compose_email.py"
      pattern: "from app.llm import client as llm_client"
---

<objective>
Author the mypy configuration this entire phase's remaining work depends on, and land the three pre-identified fixes that are NOT bulk mechanical annotation work: the one genuine runtime bug surfaced by mypy strict mode (D-08 test-first protocol), the gateway typing-only Protocol fix, and one pre-existing "widened to object" return-type bug that is purely mechanical but has a specific, already-known fix (research Pattern 2).

Purpose: Every later plan in this phase runs `uv run mypy` against the real, final, committed config — not a placeholder — so error counts and fixes are measured against ground truth from the first bulk-annotation task onward. The D-08 item is isolated here so it lands as a clean, separately-reviewable commit before the mechanical noise of annotation work begins.

Gateway ownership boundary (addresses Codex review HIGH — gateway ownership gap): this plan fixes ONLY the ResponseDict/TypedDict attribute-access mismatch in `app/email/gateway.py`. All remaining strict-mode errors in `gateway.py` (missing annotations, type-args, etc.) are explicitly owned by Plan 14-03 Task 2, which brings all of `app/email/` to zero errors once its import dependencies (app/config, app/db, app/models — Plan 14-02) are clean. No file is left ownerless.

Output: `pyproject.toml` with `[tool.mypy]` + both `[[tool.mypy.overrides]]` blocks; `mypy` added as a dev dependency; `eval/run_eval.py`'s import bug fixed with a regression test; `app/email/gateway.py`'s TypedDict/ResponseDict mismatch resolved via a narrow `Protocol` + `cast()`; `app/pipeline/federal_withholding.py`'s `_find_bracket` returning a concrete `BracketRow`.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **HIGH — gateway ownership gap:** resolved by explicit ownership split — this plan fixes only the attr-defined trio; Plan 14-03 Task 2 owns full `gateway.py` cleanliness (see objective).
- **HIGH — Task 3's automated gate could legitimately stay nonzero:** replaced the `uv run mypy app/email/gateway.py` exit-0 gate with a targeted output assertion — no `[attr-defined]` error lines attributed to `app/email/gateway.py` itself (other error categories in that file may remain until 14-03).
- **MEDIUM — `cast(Any, ...)` contradicted the narrow-edge policy:** the fix now REQUIRES a small Protocol capturing exactly `headers`/`message_id`/`text`; `cast` to `Any` is prohibited (D-10 — the shape is stable and known, not genuinely dynamic).
- **LOW — Task 3 marked TDD though the behavior test already passes:** Task 3 is no longer a TDD task. Existing coverage at `tests/test_gateway.py:657-709` already exercises the attribute access; it serves as the regression harness for this typing-only change, so no new failing-first test is required (D-08 applies to behavior bugs; this is not one — research Open Question 2 resolved it as a type-level-only gap).

## Artifacts this phase produces (Plan 01)

- `pyproject.toml` — new `[tool.mypy]` table; new `[[tool.mypy.overrides]]` tables for `tests.*` and `reportlab.*`; `mypy` entry in `[dependency-groups].dev`
- `app/email/gateway.py` — new module-private Protocol class `_ReceivedEmailLike` (attributes: `headers`, `message_id`, `text`) and a `cast` call site in `_parse_resend_envelope`
- `eval/run_eval.py` — corrected import binding `llm_client` (module object)
- `tests/test_eval_wiring.py` — new regression test function `test_record_extraction_llm_client_import_resolves` (name may vary; a test exercising the `_record_extraction` lazy-import path)
- `app/pipeline/federal_withholding.py` — `_find_bracket` re-signed as `(annual_wage: Decimal, brackets: list[BracketRow]) -> BracketRow`

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author [tool.mypy] config and add mypy as a dev dependency</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>
    pyproject.toml (current [tool.ruff]/[tool.ruff.lint]/[dependency-groups] blocks — the comment-citing-decision-ID convention to match)
    .planning/phases/14-full-type-checking-mypy/14-RESEARCH.md (Pattern 1's exact TOML, Code Examples' census commands)
    .planning/phases/14-full-type-checking-mypy/14-CONTEXT.md (D-01 through D-06, D-10)
  </read_first>
  <action>
    Run `uv add --dev mypy` (installs mypy 2.2.0 per RESEARCH.md's verified version; updates pyproject.toml [dependency-groups].dev and uv.lock together — never hand-edit either file for this).
    Add a `[tool.mypy]` block to pyproject.toml, placed after the existing `[tool.ruff.lint]` block, with: `python_version = "3.12"`, `strict = true`, `plugins = ["pydantic.mypy"]`, `files = ["app", "eval", "scripts", "tests"]`. Add inline comments citing D-01 (strict scope) and D-06 (files= scope, byte-for-byte local/CI parity) matching the existing `[tool.ruff]` block's D-XX comment convention.
    Add two `[[tool.mypy.overrides]]` blocks immediately after: one for `module = "tests.*"` setting `check_untyped_defs = true`, `disallow_untyped_defs = false`, `disallow_incomplete_defs = false`, `disallow_untyped_decorators = false` (cite D-02 inline — every test body still checked, only annotation requirement relaxed); one for `module = "reportlab.*"` setting `ignore_missing_imports = true` (cite D-03 inline — reportlab ships no py.typed, verified this phase's research, no global ignore; cite D-04 — this scoped-override-plus-comment pattern is the documented policy for any future untyped dependency).
    Do not run a full annotation pass yet — this task only lands the config. Confirm the config parses by running `uv run mypy app/config.py` (a single small file) and observing it does not error on config-parsing (real annotation errors reported for that file and its imports are expected and out of scope for this task).
  </action>
  <acceptance_criteria>
    - `pyproject.toml` contains a `[tool.mypy]` table with `strict = true` and `plugins = ["pydantic.mypy"]`
    - `pyproject.toml` contains exactly two `[[tool.mypy.overrides]]` tables, for `tests.*` and `reportlab.*` respectively
    - `pyproject.toml [dependency-groups].dev` lists `mypy` (added via `uv add --dev mypy`, not hand-typed)
    - `uv run mypy --version` exits 0 and prints `mypy 2.2.0` (or the version `uv add --dev mypy` actually resolved)
    - `git diff uv.lock` shows mypy + its transitive deps added, no unrelated changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy --version</automated>
  </verify>
  <done>`pyproject.toml` contains a `[tool.mypy]` table with `strict = true` and `plugins = ["pydantic.mypy"]`, both overrides present, mypy installed via uv</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix eval/run_eval.py's undefined llm_client import (D-08 bug, test-first)</name>
  <files>eval/run_eval.py, tests/test_eval_wiring.py</files>
  <read_first>
    eval/run_eval.py (lines ~706-749, `_record_extraction`'s current lazy from-import of an `llm_client` attribute that does not exist in app/llm/client.py)
    app/pipeline/extract.py (lines 26-37, the correct module-object import pattern: `from app.llm import client as llm_client`)
    tests/test_eval_wiring.py (existing test file and fixtures for this module, to match style)
    app/llm/client.py (confirm no module-level `llm_client` attribute exists — only functions/classes)
  </read_first>
  <behavior>
    - Test: importing `eval.run_eval` and exercising `_record_extraction`'s lazy-import statement must NOT raise `ImportError`/`AttributeError` for `llm_client`.
    - Before the fix: the current from-import at line ~724 raises `ImportError: cannot import name 'llm_client' from 'app.llm.client'` — write a test that triggers this lazy import path and currently fails with that error.
    - After the fix: the same test passes because `from app.llm import client as llm_client` binds the module object, matching `extract.py`/`suggest.py`/`compose_email.py`.
  </behavior>
  <action>
    In `tests/test_eval_wiring.py`, add a regression test that exercises `eval/run_eval.py`'s `_record_extraction` lazy-import statement directly (e.g. by monkeypatching `_require_live_llm`/`FIXTURE_DIR` minimally and calling `_record_extraction` with zero fixtures so the import executes but the loop body is a no-op) and asserts no `ImportError` occurs. Run it first to confirm it fails with `ImportError: cannot import name 'llm_client'`.
    Fix `eval/run_eval.py` line ~724: replace the broken from-import (which names a nonexistent module attribute) with `from app.llm import client as llm_client  # noqa: PLC0415`, matching the exact pattern already used in `app/pipeline/extract.py:26`, `app/pipeline/suggest.py:31`, `app/pipeline/compose_email.py:21`.
    Commit the failing test and the fix as two separate commits per D-08 (test-first, then minimal fix) — do not bundle with Task 1's config changes or any annotation work.
  </action>
  <acceptance_criteria>
    - `tests/test_eval_wiring.py` contains a new test function asserting the `_record_extraction` import path resolves without `ImportError`
    - `uv run pytest tests/test_eval_wiring.py -q` exits 0 (all tests in the file pass, including the new one)
    - `eval/run_eval.py` contains `from app.llm import client as llm_client` at the `_record_extraction` import site
    - `git log --oneline -3` shows the test-first commit followed by the fix commit as distinct commits
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_eval_wiring.py -q</automated>
  </verify>
  <done>`tests/test_eval_wiring.py` contains a new passing regression test for the `_record_extraction` import path, and `eval/run_eval.py` uses the module-object import pattern</done>
</task>

<task type="auto">
  <name>Task 3: Resolve gateway.py's ReceivedEmail TypedDict/ResponseDict mismatch with a narrow Protocol + cast, and fix federal_withholding.py's widened return type</name>
  <files>app/email/gateway.py, app/pipeline/federal_withholding.py</files>
  <read_first>
    app/email/gateway.py (lines 105-172, `parse_inbound`/`_parse_resend_envelope` — the attribute-access reads on `email_obj`: `.headers`, `.message_id`, `.text`)
    tests/test_gateway.py (lines 657-709 — the existing tests that already exercise the attribute-access behavior; plus the FakeReceivedEmail fixture near line 1144 mirroring resend.EmailsReceiving.get's shape; and the signature-verification tests to re-run after the change)
    app/pipeline/federal_withholding.py (lines 33-60, `_find_bracket`'s current widened return signature and its 3 call sites at ~line 139-141)
    app/pipeline/tax_tables_2026.py (lines 24-38, the `BracketRow(NamedTuple)` definition already used by the same subsystem)
  </read_first>
  <action>
    Confirmed by this phase's research: `resend.EmailsReceiving.get()` is typed to return the `ReceivedEmail` TypedDict but actually returns `resend.response.ResponseDict` (a `dict` subclass with `__getattr__` forwarding to key lookup) — so `email_obj.headers`/`.message_id`/`.text` in `_parse_resend_envelope` are runtime-correct today and must NOT change to bracket access (that would be an unnecessary, unproven behavior-risking edit). The mypy error here is a type-level gap only.
    Resolve with a narrow Protocol + cast (addresses Codex review MEDIUM — a cast to `Any` is PROHIBITED here; the shape is stable and known, so widening to `Any` would contradict D-10's narrow-edge policy): define a module-private `class _ReceivedEmailLike(Protocol):` in `app/email/gateway.py` declaring exactly the three members `_parse_resend_envelope` reads — `headers` (a str-keyed mapping of header values supporting `.items()`), `message_id` (`str | None`), `text` (`str | None`) — importing `Protocol` and `cast` from `typing`. At the `email_obj = resend.EmailsReceiving.get(email_id)` line, wrap the call in `cast` to `_ReceivedEmailLike` so subsequent attribute access type-checks without changing runtime behavior. Do not add a bare `# type: ignore` here — D-09 requires fixing the type properly since the correct target shape is knowable, not genuinely dynamic in the D-10 sense.
    Add one comment at the `email_obj = ...` line stating why: resend's `EmailsReceiving.get()` is typed to return the `ReceivedEmail` TypedDict but actually returns `resend.response.ResponseDict` (verified in Phase 14 research — a dict subclass with `__getattr__` attribute-access shimming), so the Protocol+cast documents the real runtime shape without changing it. Note in the SUMMARY that `tests/conftest.py`/`tests/test_gateway.py`'s `FakeReceivedEmail` fixture structurally satisfies this Protocol via duck typing — no fixture change is required.
    No new test is written for the gateway change (addresses Codex review LOW): existing coverage at `tests/test_gateway.py:657-709` already exercises the attribute access and passes today; it is the regression harness proving zero behavior change. Re-run the FULL `tests/test_gateway.py` suite (including the `verify()` signature-verification tests, per RESEARCH.md's Security Domain V5 note) to confirm no regression.
    Scope boundary: fix ONLY the attr-defined errors on `email_obj` attribute access. Other strict-mode errors in `gateway.py` (missing annotations, type-args) are owned by Plan 14-03 Task 2 and must NOT be fixed here — keeping this commit reviewably narrow.
    Separately in `app/pipeline/federal_withholding.py`: re-sign `_find_bracket` as `def _find_bracket(annual_wage: Decimal, brackets: list[BracketRow]) -> BracketRow:`, adding `BracketRow` to the existing `from app.pipeline.tax_tables_2026 import (...)` block. This is a pure annotation change — no logic edits, no test-first cycle needed (research Pattern 2's mechanical fix; behavior is provably unchanged). Land it as its own annotation-only commit.
  </action>
  <acceptance_criteria>
    - `app/email/gateway.py` contains `Protocol` in a `typing` import, a `_ReceivedEmailLike` Protocol class declaring exactly `headers`, `message_id`, `text`, and a `cast(` call at the `resend.EmailsReceiving.get` result, with an explanatory comment about the ResponseDict/TypedDict mismatch
    - `app/email/gateway.py`'s attribute-access lines (`.headers`, `.message_id`, `.text`) are UNCHANGED (no switch to bracket access) — runtime behavior is provably identical
    - `uv run mypy app/email/gateway.py 2>&1` output contains zero lines matching `app/email/gateway.py:` + `[attr-defined]` (targeted assertion — other error categories in this file may remain; they are owned by Plan 14-03)
    - `uv run pytest tests/test_gateway.py -q` exits 0 (all existing tests, including signature-verification tests, still pass — zero test-file edits needed)
    - `app/pipeline/federal_withholding.py` contains `def _find_bracket(annual_wage: Decimal, brackets: list[BracketRow]) -> BracketRow:`
    - `uv run pytest tests/test_federal_withholding.py tests/test_tax_tables_2026.py -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_gateway.py tests/test_federal_withholding.py tests/test_tax_tables_2026.py -q && ! uv run mypy app/email/gateway.py 2>&1 | grep -E "app/email/gateway\.py:[0-9]+.*\[attr-defined\]"</automated>
  </verify>
  <done>Gateway Protocol+cast landed with zero attr-defined errors attributed to gateway.py, all gateway/withholding tests green, `_find_bracket` returns concrete `BracketRow`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI/dev-machine -> PyPI | `uv add --dev mypy` pulls a new package + transitive deps from PyPI into the dev dependency group |
| Inbound webhook -> gateway.py parse_inbound | Untrusted inbound email payload crosses into `_parse_resend_envelope`'s Protocol-typed access |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-01 | Tampering | `uv add --dev mypy` supply chain | low | mitigate | mypy is a 13-year-old, extremely high-download PyPI package with an official `python/mypy` GitHub repo; RESEARCH.md's Package Legitimacy Audit already verified it directly via `uv add --dev mypy` resolution this research session — Approved disposition, no blocking checkpoint needed (dev-only tooling dep, not shipped in the runtime Docker image) |
| T-14-02 | Tampering | `app/email/gateway.py` Protocol+cast at `email_obj = resend.EmailsReceiving.get(...)` | medium | mitigate | The cast must not silently widen to `Any` in a way that masks a genuine future field-access bug; the Protocol is scoped to only the fields actually read (`headers`, `message_id`, `text`) per the review-mandated narrow-edge policy, and `tests/test_gateway.py`'s full suite (including signature-verification tests) is re-run to confirm no incidental behavior change |
| T-14-03 | Repudiation | eval/run_eval.py import fix | low | accept | Low risk — the `--record` path is manual/local-only, never exercised by hermetic CI or the live production pipeline; the regression test closes the gap going forward |
</threat_model>

<verification>
Run `uv run mypy --version` to confirm mypy is installed and config parses.
Run `uv run pytest tests/test_eval_wiring.py tests/test_gateway.py tests/test_federal_withholding.py tests/test_tax_tables_2026.py -q` — all pass.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite still green (no regression from any of the three fixes).
Run `uv run mypy app/email/gateway.py 2>&1` and confirm zero `[attr-defined]` lines attributed to `app/email/gateway.py` itself.
Inspect `git log` for at least 3 distinct commits: config, eval/run_eval.py test-first fix (2 commits), gateway.py/federal_withholding.py fixes.
</verification>

<success_criteria>
- `pyproject.toml` has a real, final `[tool.mypy]` config with both overrides that every later plan checks against.
- `mypy` is a committed dev dependency via `uv add --dev mypy` (not hand-edited).
- The one genuine reachable bug (`eval/run_eval.py`'s `llm_client` import) is fixed with a preceding failing regression test, in its own commit.
- The gateway.py type-level mismatch is resolved via a narrow Protocol + cast (never `Any`) with zero behavior change, verified by the full `test_gateway.py` suite staying green.
- `federal_withholding.py`'s `_find_bracket` returns the concrete `BracketRow` type.
- Full hermetic test suite remains green throughout.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-01-SUMMARY.md` when done
</output>
