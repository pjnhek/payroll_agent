---
phase: 14-full-type-checking-mypy
plan: 09
type: execute
wave: 6
depends_on: ["14-06", "14-07", "14-08"]
files_modified: []
autonomous: true
requirements: [TYPE-01, TYPE-02]

must_haves:
  truths:
    - "Bare `uv run mypy` (no CLI arguments — scope comes exclusively from pyproject.toml's files= key, per D-06) exits 0 over the entire repo for the first time in the phase"
    - "Any residual cross-module error only visible in the combined run (RESEARCH.md Open Question 1) is fixed here, annotation-only, and individually documented in the SUMMARY — this plan runs alone in its wave, so cross-file fixes cannot collide with concurrent work"
    - "The full hermetic test suite passes with counts identical to a dynamically captured in-session baseline — the integration gate proves type-cleanliness AND behavior-neutrality together"
  artifacts: []
  key_links:
    - from: "pyproject.toml [tool.mypy] files= key"
      to: "bare uv run mypy"
      via: "config-driven scope — the exact command Plan 14-10 will wire into CI, run locally first for byte-for-byte parity (D-06)"
      pattern: "uv run mypy"
---

<objective>
Run the full-repo combined type-check — bare `uv run mypy`, no directory arguments, scope taken exclusively from the committed `files = ["app", "eval", "scripts", "tests"]` config per D-06 — and bring it to exit 0, fixing any residual cross-module error that only surfaces when all four directories are checked together in one pass.

Purpose: This is the dedicated integration gate the Codex review required (HIGH — the old Plan 08 claimed to be the integration point without depending on the other test groups, and authorized out-of-scope fixes while siblings might still be running). It runs ALONE in Wave 6, after Plans 14-06/14-07/14-08 complete, so: (a) every file in the repo has already been cleaned by its owning plan, (b) any residual fix made here cannot collide with concurrent work, and (c) the command verified here is the byte-for-byte identical command Plan 14-10 wires into CI — running it green locally first is the D-06 parity proof. RESEARCH.md Open Question 1 predicted the directory-by-directory censuses (170/163/521) are an upper bound and a combined run may surface a handful of extra cross-module errors; this plan is where that question gets its final empirical answer.

Expected outcome: zero or very few residual errors (frontmatter `files_modified` is intentionally empty — the expected diff is nothing; if residual fixes are needed, each touched file and the exact error it resolves must be recorded in the SUMMARY).

Output: Bare `uv run mypy` exits 0 with `Success: no issues found`; full hermetic suite green; TYPE-01 and TYPE-02 fully satisfied and ready for Plan 14-10's CI enforcement (TYPE-03).
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **HIGH — integration check lacked dependencies on the test-group plans:** this plan depends on 14-06, 14-07, AND 14-08, and runs solo in Wave 6 — nothing it verifies can still be in flight.
- **HIGH — out-of-scope residual fixes broke file ownership:** residual-fix authority now lives ONLY here, where it is collision-free by construction (solo wave). Every residual fix must be annotation-only, minimal, and individually documented (file, error code, one-line rationale) in the SUMMARY.
- **MEDIUM — `uv run mypy .` violated the locked bare-command parity rule:** the gate command here is bare `uv run mypy` — no `.`, no directory arguments (D-06, 14-CONTEXT.md). Passing `.` is a CLI target and does not mean "use only the files= scope."
- **MEDIUM — stale pinned test baseline:** dynamic in-session baseline, same rule as the test-group plans.

## Artifacts this phase produces (Plan 09)

- No new symbols — this plan is a gate. Any residual annotation fixes reuse existing names; each is listed in the SUMMARY.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run bare `uv run mypy` over the full repo, fix any residual cross-module errors, confirm suite green</name>
  <files>(expected: none — residual annotation-only fixes in any file reported by the combined run are authorized here and only here, each documented in the SUMMARY)</files>
  <read_first>
    pyproject.toml ([tool.mypy] files= key — confirm the scope is ["app", "eval", "scripts", "tests"] before running, so the bare command checks exactly what CI will check)
    .planning/phases/14-full-type-checking-mypy/14-RESEARCH.md (Open Question 1 — why the combined run may surface errors the per-directory runs did not)
  </read_first>
  <action>
    FIRST: run `uv run pytest -q -m "not integration and not live_llm"` and record the exact passed/skipped counts as the in-session baseline.
    Run bare `uv run mypy` — exactly that command, zero arguments (D-06: scope lives only in pyproject.toml's `files=` key; this is the byte-for-byte command Plan 14-10 puts in CI; addresses Codex review MEDIUM — never `uv run mypy .` or any directory-arg variant).
    If it exits 0: record `Success: no issues found` in the SUMMARY, note that RESEARCH.md Open Question 1 resolved with zero residual cross-module errors, and finish — make no code edit.
    If residual errors appear: fix each one annotation-only, following the same house patterns as the owning plans (concrete types per D-10, explicit None-narrowing, no new ignores without a D-09 code+reason). For EACH fix, record in the SUMMARY: the file, the mypy error code and message, and a one-line rationale. This plan runs alone in Wave 6, so cross-file fixes are collision-free — but if any residual error demands a BEHAVIOR change (not annotation), STOP and surface it as a D-08 finding instead of fixing it silently: a behavior bug found this late needs its own test-first commit and explicit visibility.
    After any fixes: re-run bare `uv run mypy` to exit 0, then re-run `uv run pytest -q -m "not integration and not live_llm"` and compare counts against the baseline.
  </action>
  <acceptance_criteria>
    - `uv run mypy` (bare, zero arguments) exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 with passed/skipped counts IDENTICAL to the in-session baseline captured before any edit
    - If any file was modified: the SUMMARY lists every touched file with the exact residual error it resolved; `git diff` for each shows annotation-only changes
    - If zero files were modified: the SUMMARY states the combined run was clean on first pass, closing RESEARCH.md Open Question 1
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy</automated>
  </verify>
  <done>Bare `uv run mypy` exits 0 with `Success: no issues found` and the hermetic suite matches the captured baseline</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Full-repo integration point | The first point where the ENTIRE repo is checked together — any cross-module error only visible in combination surfaces here, potentially in money-path files |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-16 | Repudiation | Bare `uv run mypy` surfacing a residual cross-module error not caught by any single directory-scoped plan | medium | mitigate | This plan explicitly owns the combined check; every residual fix is individually documented (file + error code + rationale) in the SUMMARY, and behavior-changing findings are escalated to the D-08 test-first protocol rather than fixed silently |
| T-14-16b | Tampering | Late annotation fixes touching money-path files without their owning plan's test discipline | medium | mitigate | Residual fixes must be annotation-only; the full hermetic suite with in-session baseline comparison is a hard acceptance criterion, and any assertion/behavior delta blocks the plan |
</threat_model>

<verification>
Run `uv run mypy` (bare) — exits 0, `Success: no issues found`.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, counts match baseline.
Confirm the SUMMARY documents either "clean on first pass" or an itemized residual-fix list.
</verification>

<success_criteria>
- Bare `uv run mypy` (the exact future CI command) exits 0 over the entire repo for the first time in the phase.
- TYPE-01 and TYPE-02 fully satisfied: zero errors across app/, eval/, scripts/, tests/ in one combined run.
- Residual-error ownership handled without breaking file ownership (solo wave, documented fixes).
- Full hermetic test suite green, counts identical to the dynamically captured baseline.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-09-SUMMARY.md` when done
</output>
