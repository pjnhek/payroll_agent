---
phase: 14-full-type-checking-mypy
plan: 09
type: execute
wave: 4
depends_on: ["14-06", "14-07", "14-08"]
files_modified:
  - .github/workflows/ci.yml
  - .planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md
autonomous: false
requirements: [TYPE-03]

must_haves:
  truths:
    - "ci.yml has a third parallel job named typecheck that runs uv run mypy, mirroring the lint job's exact recipe"
    - "A push with a deliberately injected type error demonstrably fails the typecheck job (red), captured as evidence"
    - "The real master branch's typecheck job run is green, captured as evidence"
    - "The throwaway red-proof branch is deleted after evidence capture, per Phase 12's D-14 precedent"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "third typecheck job alongside lint and test"
      contains: "Run mypy"
    - path: ".planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md"
      provides: "red-proof evidence: red run URL + green run URL for the typecheck job"
  key_links:
    - from: ".github/workflows/ci.yml typecheck job"
      to: "pyproject.toml [tool.mypy]"
      via: "bare uv run mypy command, config-driven scope"
      pattern: "run: uv run mypy"
---

<objective>
Wire mypy into CI as a third, blocking, named job (`typecheck`) alongside the existing `lint` and `test` jobs, and produce the same red/green red-proof evidence pattern Phase 12 used for its own gates — completing TYPE-03 and closing out the phase.

Purpose: TYPE-01 and TYPE-02 (landed across Plans 01-08) prove the codebase IS type-clean; TYPE-03 proves that guarantee is ENFORCED and can't silently regress. This is the last plan in the phase — it only runs after all of `app/`, `eval/`, `scripts/`, `tests/` are confirmed mypy-clean (Wave 3's Plan 08 already ran the full-repo `uv run mypy .` check as its final task).

Output: `.github/workflows/ci.yml` with a new `typecheck` job; a red-proof branch pushed, its failing run URL captured, then deleted; the real master branch's green `typecheck` run URL captured; both recorded in `14-VERIFICATION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@.planning/phases/12-ci-quality-gates/12-CONTEXT.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the typecheck job to ci.yml, mirroring the lint job's exact recipe</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (the existing lint job, lines 17-33 — the exact copy-paste template per D-05)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (the typecheck job YAML already drafted in the pattern map)
    .planning/phases/12-ci-quality-gates/12-CONTEXT.md (the house CI recipe: pinned action SHAs, --locked sync, named jobs)
  </read_first>
  <action>
    Add a new `typecheck` job to `.github/workflows/ci.yml`, placed after the existing `test` job, with the exact same shape as `lint`: `name: "Type check (mypy --strict)"`, `runs-on: ubuntu-latest`, checkout step using the SAME pinned SHA already in the file (`actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5`), `astral-sh/setup-uv` step using the SAME pinned SHA already in the file (`astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86`) with `python-version: "3.12"`, `uv sync --locked` install step, then a final step `name: Run mypy` with `run: uv run mypy` (bare command, no directory arguments — D-06 requires scope to live only in `pyproject.toml`'s `files=` key so local and CI invocations agree byte-for-byte).
    Do not modify the file-level `permissions: contents: read` or `concurrency:` blocks — these already apply repo-wide to every job.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a `typecheck:` job with `name: "Type check (mypy --strict)"`
    - The `typecheck` job's checkout and setup-uv steps use the identical pinned SHAs as the `lint`/`test` jobs (byte-for-byte match on the `uses:` lines)
    - The `typecheck` job's final step is `run: uv run mypy` with no directory arguments
    - `git diff .github/workflows/ci.yml` shows only the new job added, zero changes to `lint`/`test`/file-level blocks
  </acceptance_criteria>
  <verify>
    <automated>grep -q "typecheck:" .github/workflows/ci.yml && grep -q "uv run mypy" .github/workflows/ci.yml</automated>
  </verify>
  <done>`.github/workflows/ci.yml` contains a `typecheck:` job with `name: "Type check (mypy --strict)"`</done>
</task>

<task type="auto">
  <name>Task 2: Push master, confirm the real typecheck job runs green</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    .github/workflows/ci.yml (post-Task-1 state, to confirm the job is correctly wired before pushing)
  </read_first>
  <action>
    Commit Task 1's ci.yml change (and any final loose-end fixes from Wave 3, if the executor discovers the full-repo mypy run has any residual issue not yet committed) to master. Push to origin. Poll the GitHub Actions run for this push (via `gh run list`/`gh run view` or by checking the Actions tab) until it completes. Confirm all three jobs (`lint`, `test`, `typecheck`) report green.
    Record the run's URL for `14-VERIFICATION.md` in the next task.
  </action>
  <acceptance_criteria>
    - `git push` to master succeeds
    - `gh run list --branch master --limit 1` (or equivalent) shows the triggered run
    - `gh run view <run-id>` (or the Actions UI) shows all three jobs (`lint`, `test`, `typecheck`) with conclusion `success`
  </acceptance_criteria>
  <verify>
    <automated>git push</automated>
  </verify>
  <done>`git push` to master succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Red-proof the typecheck CI gate</name>
  <files>.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md</files>
  <what-built>The typecheck CI job is wired and green on master. This checkpoint red-proofs it per Phase 12's D-07/D-14 pattern: push a throwaway branch with a deliberately injected type error, confirm the typecheck job goes red (and only that job — lint/test should stay unaffected by a pure type error), capture both run URLs, then delete the branch.</what-built>
  <how-to-verify>
    1. Create a throwaway branch (e.g. `red-proof/mypy-14`) off master.
    2. Inject a single, clearly-scoped type error — e.g. in `app/config.py`, change one function's return type annotation to a type its body cannot satisfy (an incompatible return statement), or add a new function with a mismatched parameter/return type. Choose an error that ONLY affects mypy (not ruff, not pytest) so the red run isolates the `typecheck` job exactly as Phase 12's D-14 isolated single-cause failures.
    3. Push the branch. Confirm via `gh run list --branch red-proof/mypy-14` that the run's `typecheck` job fails while `lint` and `test` stay green (single-cause isolation, matching Phase 12 D-14's precedent).
    4. Capture the red run's URL.
    5. Delete the throwaway branch (`git push origin --delete red-proof/mypy-14` and `git branch -D red-proof/mypy-14`) — branch deletion does not delete the recorded Actions run history (Phase 12 D-14 precedent).
    6. Write `.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` recording: the green master run URL (from Task 2), the red red-proof-branch run URL (this checkpoint), the exact single-cause type error injected, and confirmation that lint/test stayed green on the red run.
  </how-to-verify>
  <resume-signal>Reply "approved" once both run URLs are captured in 14-VERIFICATION.md and the throwaway branch is deleted, or describe any issue (e.g. the injected error also broke ruff/pytest, meaning it wasn't single-cause and needs a different injection).</resume-signal>
  <verify>
    <automated>MISSING — this is a CI-level red-proof exercise with no pytest equivalent; verification is the human confirming both run URLs in 14-VERIFICATION.md per how-to-verify above</automated>
  </verify>
  <done>14-VERIFICATION.md contains the green master run URL, the red red-proof-branch run URL, the exact injected error, and confirmation lint/test stayed green on the red run; the throwaway branch is deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CI workflow file (.github/workflows/ci.yml) | Modifying CI configuration is itself a supply-chain-sensitive change — a malicious or careless edit here could weaken the security posture of every future push |
| Throwaway red-proof branch | A temporary branch pushed to the real origin remote, deleted after evidence capture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-17 | Tampering | ci.yml typecheck job addition | mitigate | The new job reuses the exact same pinned action SHAs already vetted for `lint`/`test` (Phase 12's own supply-chain review) — no new third-party Action is introduced, only a new step running an already-vetted-this-phase tool (mypy) |
| T-14-18 | Elevation of Privilege | Red-proof branch push to origin | accept | Push-then-delete is the same pattern Phase 12 D-14 already used and the user already accepted; the `permissions: contents: read` block at file level means the workflow itself has no write access regardless |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new package-manager installs in this plan (mypy was already added in Plan 01, verified via slopcheck-equivalent research); this plan only adds a CI step invoking the already-installed tool |
</threat_model>

<verification>
`gh run view` on the real master push shows `lint`, `test`, `typecheck` all green.
`gh run view` on the red-proof branch push shows `typecheck` red while `lint`/`test` stay green (single-cause isolation).
`.planning/phases/14-full-type-checking-mypy/14-VERIFICATION.md` records both run URLs and the exact injected error.
</verification>

<success_criteria>
- `.github/workflows/ci.yml` has a third, blocking, named `typecheck` job identical in shape to `lint`/`test`.
- Real master push is green across all three jobs.
- A red-proof push demonstrates the `typecheck` job fails on a genuine type error, isolated from `lint`/`test`.
- Evidence (both run URLs, the injected error) is committed in `14-VERIFICATION.md`.
- Throwaway branch is deleted after evidence capture.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-09-SUMMARY.md` when done
</output>
