---
phase: 14-full-type-checking-mypy
plan: 07
type: execute
wave: 3
depends_on: ["14-02", "14-03", "14-04", "14-05"]
files_modified:
  - tests/conftest.py
  - tests/test_bootstrap_timeouts.py
  - tests/test_calculate.py
  - tests/test_claim_status.py
  - tests/test_clarify.py
  - tests/test_combined_context.py
  - tests/test_cr01_classify_union.py
  - tests/test_dashboard.py
  - tests/test_decide_field_regression.py
  - tests/test_gateway.py
  - tests/test_ingest.py
  - tests/test_models_contracts.py
  - tests/test_multi_employee_delivery.py
  - tests/test_reconcile.py
  - tests/test_reply_redelivery.py
  - tests/test_retrigger_epoch.py
  - tests/test_status_drift.py
  - tests/test_webhook_dedup_race.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to this plan's 18 test files (including conftest.py), under the tests.* relaxed override from Plan 01"
    - "conftest.py's shared fixtures are correctly typed so files depending on it in other test-group plans do not regress"
    - "No test assertion changes — only type-level fixes"
  artifacts: []
  key_links: []
---

<objective>
Bring the second third (18 files including `conftest.py`, ~8945 lines, balanced group 2) of `tests/` to zero mypy errors under Plan 01's `tests.*` relaxed override.

Purpose: `conftest.py` (1226 lines) is the shared fixture file every other test file in the suite implicitly depends on via pytest's fixture injection — it lands in this plan since its own line count balances this group, but its correctness affects mypy results across ALL test-group plans equally (fixture type shapes are read by mypy per-file at the import site, not globally cached in a way that would create a plan-ordering dependency; each test-group plan already depends on all of Wave 2, and mypy re-checks conftest.py as part of any `files=` run that includes tests/ regardless of which plan "owns" the file).

Output: Zero mypy errors across this plan's 18 test files; full hermetic suite green with zero assertion changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve mypy errors in conftest.py and the gateway/webhook/dashboard test cluster</name>
  <files>tests/conftest.py, tests/test_gateway.py, tests/test_webhook_dedup_race.py, tests/test_dashboard.py, tests/test_ingest.py, tests/test_reply_redelivery.py</files>
  <read_first>
    tests/conftest.py (all shared fixtures — read once here in full, since this is the file every other test-group plan's files implicitly depend on for fixture shapes)
    tests/test_gateway.py (the FakeReceivedEmail fixture around line 1144 already read this session — the fixture mirroring resend.EmailsReceiving.get's shape)
    tests/test_dashboard.py (1016 lines, largest file in this task)
  </read_first>
  <action>
    Run `uv run mypy tests/conftest.py tests/test_gateway.py tests/test_webhook_dedup_race.py tests/test_dashboard.py tests/test_ingest.py tests/test_reply_redelivery.py` first to get the real current error list under the relaxed `tests.*` override.
    Fix reported errors in `conftest.py` first (before the other 5 files) since fixtures defined here are consumed everywhere — annotate fixture functions' return types concretely (e.g. `-> Generator[psycopg.Connection, None, None]` for a connection fixture, matching `app/db/supabase.py`'s own pattern) rather than leaving them inferred as `Any`. For `FakeReceivedEmail` (test_gateway.py), confirm its shape still matches what Plan 01's gateway.py cast/Protocol expects — if Plan 01 introduced a `Protocol` type for the gateway boundary, `FakeReceivedEmail` may need to structurally satisfy it (duck-typing) rather than needing an inheritance change.
    Fix remaining reported errors in the other 5 files using the same discipline: only annotate what's needed to resolve a real error.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/conftest.py tests/test_gateway.py tests/test_webhook_dedup_race.py tests/test_dashboard.py tests/test_ingest.py tests/test_reply_redelivery.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_gateway.py tests/test_webhook_dedup_race.py tests/test_dashboard.py tests/test_ingest.py tests/test_reply_redelivery.py -q -m "not integration and not live_llm"` exits 0 with unchanged pass/skip counts
    - `uv run pytest -q -m "not integration and not live_llm"` (full suite) exits 0 — confirms conftest.py fixture typing changes did not break any OTHER test file relying on the same fixtures
    - `git diff` shows zero assertion changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/conftest.py tests/test_gateway.py tests/test_webhook_dedup_race.py tests/test_dashboard.py tests/test_ingest.py tests/test_reply_redelivery.py</automated>
  </verify>
  <done>`uv run mypy tests/conftest.py tests/test_gateway.py tests/test_webhook_dedup_race.py tests/test_dashboard.py tests/test_ingest.py tests/test_reply_redelivery.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Resolve mypy errors in the remaining test files in this group</name>
  <files>tests/test_bootstrap_timeouts.py, tests/test_calculate.py, tests/test_claim_status.py, tests/test_clarify.py, tests/test_combined_context.py, tests/test_cr01_classify_union.py, tests/test_decide_field_regression.py, tests/test_models_contracts.py, tests/test_multi_employee_delivery.py, tests/test_reconcile.py, tests/test_retrigger_epoch.py, tests/test_status_drift.py</files>
  <read_first>
    tests/test_models_contracts.py (651 lines — Pydantic contract model tests, must stay aligned with Plan 02's contracts.py annotations)
    tests/test_calculate.py, tests/test_decide_field_regression.py (money-path unit tests — highest-sensitivity test files in this group)
  </read_first>
  <action>
    Run `uv run mypy tests/test_bootstrap_timeouts.py tests/test_calculate.py tests/test_claim_status.py tests/test_clarify.py tests/test_combined_context.py tests/test_cr01_classify_union.py tests/test_decide_field_regression.py tests/test_models_contracts.py tests/test_multi_employee_delivery.py tests/test_reconcile.py tests/test_retrigger_epoch.py tests/test_status_drift.py` first to get the real current error list.
    Fix reported errors following the same discipline as Task 1 — only annotate what's needed to resolve a real error, never add an ignore without a documented reason, never touch assertions.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_bootstrap_timeouts.py tests/test_calculate.py tests/test_claim_status.py tests/test_clarify.py tests/test_combined_context.py tests/test_cr01_classify_union.py tests/test_decide_field_regression.py tests/test_models_contracts.py tests/test_multi_employee_delivery.py tests/test_reconcile.py tests/test_retrigger_epoch.py tests/test_status_drift.py` exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 with the same pass/skip counts as the pre-plan baseline
    - `git diff` shows zero assertion changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_bootstrap_timeouts.py tests/test_calculate.py tests/test_claim_status.py tests/test_clarify.py tests/test_combined_context.py tests/test_cr01_classify_union.py tests/test_decide_field_regression.py tests/test_models_contracts.py tests/test_multi_employee_delivery.py tests/test_reconcile.py tests/test_retrigger_epoch.py tests/test_status_drift.py</automated>
  </verify>
  <done>`uv run mypy tests/test_bootstrap_timeouts.py tests/test_calculate.py tests/test_claim_status.py tests/test_clarify.py tests/test_combined_context.py tests/test_cr01_classify_union.py tests/test_decide_field_regression.py tests/test_models_contracts.py tests/test_multi_employee_delivery.py tests/test_reconcile.py tests/test_retrigger_epoch.py tests/test_status_drift.py` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only code, but conftest.py is shared infrastructure | A typing mistake in conftest.py's fixtures could mask a real bug across every other test file in the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-14 | Tampering | conftest.py fixture return-type annotation | mitigate | Full suite run (not just this plan's 18 files) required in Task 1's acceptance criteria specifically because conftest.py changes have blast radius across all other test-group plans' files |
</threat_model>

<verification>
Run `uv run mypy tests/conftest.py` plus this plan's 17 other files — exits 0.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, same pass/skip counts.
</verification>

<success_criteria>
- Zero mypy errors across this plan's 18 test files (including conftest.py) under the tests.* relaxed override.
- Full hermetic test suite green, identical pass/skip counts, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-07-SUMMARY.md` when done
</output>
