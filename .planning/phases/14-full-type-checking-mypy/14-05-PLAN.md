---
phase: 14-full-type-checking-mypy
plan: 05
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - eval/run_eval.py
  - eval/draft_candidate_emails.py
  - eval/judge.py
  - scripts/demo_reset.py
  - scripts/reset_stuck_runs.py
  - scripts/show_confirmation_subject.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to eval/ and scripts/"
    - "No annotation introduces a behavior change to the eval harness or the operational scripts — all remain runnable as-is"
  artifacts:
    - path: "eval/run_eval.py"
      provides: "fully strict-annotated eval harness (scoring, --check regression, --record live-extraction path)"
  key_links:
    - from: "eval/run_eval.py"
      to: "app/pipeline/extract.py"
      via: "extract() call on the --record path"
      pattern: "extract\\(email, roster, run_id=run_id, llm=llm_client\\)"
---

<objective>
Bring `eval/` (run_eval.py, draft_candidate_emails.py, judge.py) and `scripts/` (demo_reset.py, reset_stuck_runs.py, show_confirmation_subject.py) to zero mypy errors under the strict config from Plan 01.

Purpose: These are the smallest-surface directories in the phase (RESEARCH.md's census: `eval/`+`scripts/` combined produce far fewer NEW errors than `app/`'s transitively-checked count once Plan 01's llm_client import fix is applied) — annotating them completes TYPE-02's "no blind spot anywhere in the codebase" requirement outside `app/` and `tests/`.

Output: Zero mypy errors across `eval/` and `scripts/`; full hermetic suite green; eval harness and operational scripts remain runnable unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate eval/run_eval.py, draft_candidate_emails.py, judge.py to mypy-clean</name>
  <files>eval/run_eval.py, eval/draft_candidate_emails.py, eval/judge.py</files>
  <read_first>
    eval/run_eval.py (1088 lines — scoring, --check regression, --record live-extraction path already fixed for its import bug in Plan 01)
    eval/draft_candidate_emails.py, eval/judge.py (smaller supporting eval modules)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns: dict[str, Any] at genuinely dynamic edges — eval fixture JSON loading is a canonical dynamic-edge case per D-10)
  </read_first>
  <action>
    Run `uv run mypy eval/run_eval.py eval/draft_candidate_emails.py eval/judge.py` first to get the real current error list (Plan 01 already fixed the llm_client import bug — this task covers everything else: no-untyped-def, type-arg, and any remaining Optional-narrowing).
    Fix every reported error. Fixture JSON loading (`_load_fixture`, raw dict parsing before `InboundEmail.model_validate`) is a genuinely dynamic edge per D-10 — `dict[str, Any]` is acceptable there; once a value is validated into an `InboundEmail`/`Employee`/model instance, subsequent code should use the concrete model type, not `Any`. Scoring/aggregation functions that produce the eval chart's summary.json should have concrete return types (e.g. `dict[str, float]` or a small TypedDict/dataclass if the shape is stable, not a bare `dict`).
  </action>
  <acceptance_criteria>
    - `uv run mypy eval/run_eval.py eval/draft_candidate_emails.py eval/judge.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_eval_wiring.py -q` exits 0
    - `uv run python -m eval.run_eval --check` (or the project's documented equivalent invocation) still runs and reports "no regression" or the pre-existing expected output — confirm via reading the file's argparse/CLI entrypoint if the exact invocation is unclear, do not guess a flag that doesn't exist
    - `git diff eval/` shows only annotation additions/narrowing, no scoring-logic or fixture-parsing changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy eval/run_eval.py eval/draft_candidate_emails.py eval/judge.py</automated>
  </verify>
  <done>`uv run mypy eval/run_eval.py eval/draft_candidate_emails.py eval/judge.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Annotate scripts/ (demo_reset, reset_stuck_runs, show_confirmation_subject) to mypy-clean</name>
  <files>scripts/demo_reset.py, scripts/reset_stuck_runs.py, scripts/show_confirmation_subject.py</files>
  <read_first>
    scripts/demo_reset.py (202 lines — demo-state reset CLI)
    scripts/reset_stuck_runs.py (87 lines — stranded-run sweep CLI)
    scripts/show_confirmation_subject.py (48 lines — small diagnostic CLI)
  </read_first>
  <action>
    Run `uv run mypy scripts/` first to get the real current error list.
    Fix every reported error — these are small operational CLI scripts; annotate their argparse setup, DB-connection usage (matching `app/db/repo`'s `conn: psycopg.Connection | None = None` pattern where applicable), and any print/output formatting functions with concrete types.
  </action>
  <acceptance_criteria>
    - `uv run mypy scripts/` exits 0 with `Success: no issues found`
    - `uv run python scripts/show_confirmation_subject.py --help` (or equivalent no-side-effect invocation) still runs without a Python-level error, confirming the script remains executable post-annotation
    - `git diff scripts/` shows only annotation additions/narrowing, no CLI-behavior changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy scripts/</automated>
  </verify>
  <done>`uv run mypy scripts/` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Eval fixture loading (eval/run_eval.py) | Local JSON fixture files parsed into typed models — no untrusted network input |
| Operational scripts (scripts/*) | Local-only CLI tools run by the developer/operator, not exposed to any network boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Tampering | eval/run_eval.py scoring-function annotation | accept | Eval harness is a local dev-time tool, not part of the production pipeline; a behavior slip here would surface immediately as a changed eval chart, not a live money-path bug |
| T-14-12 | Tampering | scripts/reset_stuck_runs.py annotation | mitigate | This script issues real DB writes (status resets) — verify via a dry-run/--help invocation post-annotation that argparse wiring is unchanged, since this script has no dedicated pytest coverage |
</threat_model>

<verification>
Run `uv run mypy eval/ scripts/` — exits 0.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green.
</verification>

<success_criteria>
- Zero mypy errors across all of `eval/` and `scripts/`.
- Eval harness and operational scripts remain runnable, behavior-identical.
- Full hermetic test suite green.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-05-SUMMARY.md` when done
</output>
