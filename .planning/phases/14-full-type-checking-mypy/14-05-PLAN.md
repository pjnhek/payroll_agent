---
phase: 14-full-type-checking-mypy
plan: 05
type: execute
wave: 4
depends_on: ["14-02", "14-03"]
files_modified:
  - eval/run_eval.py
  - eval/draft_candidate_emails.py
  - eval/judge.py
  - scripts/demo_reset.py
  - scripts/reset_stuck_runs.py
  - scripts/show_confirmation_subject.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to eval/ and scripts/"
    - "No annotation introduces a behavior change to the eval harness or the operational scripts — all remain runnable as-is"
    - "No operational DB script is executed to prove annotations preserved executability — side-effect-free checks (py_compile) are used instead"
  artifacts:
    - path: "eval/run_eval.py"
      provides: "fully strict-annotated eval harness (scoring, --check regression, --record live-extraction path)"
  key_links:
    - from: "eval/run_eval.py"
      to: "app/pipeline/decide.py, app/pipeline/reconcile_names.py, app/pipeline/validate.py, app/pipeline/orchestrator.py, app/db/seed.py, app/models/"
      via: "production judgment-spine imports at eval/run_eval.py:34-41 — the reason this plan depends on 14-02 and 14-03"
      pattern: "from app.pipeline"
---

<objective>
Bring `eval/` (run_eval.py, draft_candidate_emails.py, judge.py) and `scripts/` (demo_reset.py, reset_stuck_runs.py, show_confirmation_subject.py) to zero mypy errors under the strict config from Plan 14-01.

Purpose: These are the smallest-surface directories in the phase — annotating them completes TYPE-02's "no blind spot anywhere in the codebase" requirement outside `app/` and `tests/`. `eval/run_eval.py` imports the production judgment spine (`app/pipeline/decide`, `reconcile_names`, `validate`, `orchestrator`, `app/db/seed`, `app/models` — lines 34-41) and the scripts import `app/db/repo`, so mypy's import traversal makes this plan schedulable only after Plans 14-02/14-03 (addresses Codex review HIGH). It runs in Wave 4 parallel to Plan 14-04 — neither imports the other's files (eval/scripts never import app/routes or app/main), and there is zero file overlap.

Output: Zero mypy errors across `eval/` and `scripts/`; full hermetic suite green; eval harness and operational scripts remain runnable unchanged.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **HIGH — eval checking traverses pipeline and DB code owned by other plans:** depends_on is now `["14-02", "14-03"]` (wave 4).
- **MEDIUM — `show_confirmation_subject.py --help` is NOT side-effect-free:** the script has no argument parser and `main()` opens a live DB connection when executed (`scripts/show_confirmation_subject.py:15-26`, invoked under the `__main__` guard at lines 47-48, and any CLI flag is ignored). The executability check is replaced with `uv run python -m py_compile` — the executor must NOT run this script or pass any flag to it.
- **LOW — "argparse setup" wording was wrong for scripts/:** corrected — `eval/run_eval.py` uses argparse (line 24), but `scripts/demo_reset.py:109` and `scripts/reset_stuck_runs.py:31` parse `sys.argv` manually, and `show_confirmation_subject.py` takes no arguments at all.

## Artifacts this phase produces (Plan 05)

- No new public symbols — annotations only. Scoring/aggregation return types may gain small TypedDict/dataclass shapes ONLY if a stable dict shape warrants it (executor's judgment per D-10); any such new type name must be recorded in the SUMMARY.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate eval/run_eval.py, draft_candidate_emails.py, judge.py to mypy-clean</name>
  <files>eval/run_eval.py, eval/draft_candidate_emails.py, eval/judge.py</files>
  <read_first>
    eval/run_eval.py (1088 lines — argparse CLI with --check/--record flags, scoring, DB-free --check regression path; its import bug was already fixed in Plan 14-01)
    eval/draft_candidate_emails.py, eval/judge.py (smaller supporting eval modules)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns: dict[str, Any] at genuinely dynamic edges — eval fixture JSON loading is a canonical dynamic-edge case per D-10)
  </read_first>
  <action>
    Run `uv run mypy eval/` first to get the real current error list (Plan 14-01 already fixed the llm_client import bug, and the traversed app/ substrate is clean from Plans 14-02/14-03 — this task covers everything else: no-untyped-def, type-arg, and any remaining Optional-narrowing).
    Fix every reported error. Fixture JSON loading (`_load_fixture`, raw dict parsing before `InboundEmail.model_validate`) is a genuinely dynamic edge per D-10 — `dict[str, Any]` is acceptable there; once a value is validated into an `InboundEmail`/`Employee`/model instance, subsequent code should use the concrete model type, not `Any`. Scoring/aggregation functions that produce the eval chart's summary.json should have concrete return types (e.g. `dict[str, float]` or a small TypedDict/dataclass if the shape is stable, not a bare `dict`). Annotate the argparse entrypoint (`eval/run_eval.py` uses argparse — line 24) with its real types.
  </action>
  <acceptance_criteria>
    - `uv run mypy eval/` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_eval_wiring.py -q` exits 0
    - `uv run python eval/run_eval.py --check` (the invocation documented in the file's own header, line 11 — a DB-free path by design) still runs and reports no regression against the committed summary.json
    - `git diff eval/` shows only annotation additions/narrowing, no scoring-logic or fixture-parsing changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy eval/</automated>
  </verify>
  <done>`uv run mypy eval/` exits 0 with `Success: no issues found` and the --check regression gate still passes</done>
</task>

<task type="auto">
  <name>Task 2: Annotate scripts/ (demo_reset, reset_stuck_runs, show_confirmation_subject) to mypy-clean</name>
  <files>scripts/demo_reset.py, scripts/reset_stuck_runs.py, scripts/show_confirmation_subject.py</files>
  <read_first>
    scripts/demo_reset.py (202 lines — demo-state reset CLI; parses sys.argv manually at line ~109, no argparse)
    scripts/reset_stuck_runs.py (87 lines — stranded-run sweep CLI; parses sys.argv manually at line ~31, no argparse)
    scripts/show_confirmation_subject.py (48 lines — small diagnostic CLI; takes NO arguments; main() opens a live DB connection via repo._conn_ctx when executed)
  </read_first>
  <action>
    Run `uv run mypy scripts/` first to get the real current error list.
    Fix every reported error — these are small operational CLI scripts; annotate their manual `sys.argv` parsing (demo_reset.py and reset_stuck_runs.py — NOT argparse; correction per Codex review), DB-connection usage (matching `app/db/repo`'s `conn: psycopg.Connection | None = None` pattern where applicable), and any print/output formatting functions with concrete types.
    Executability check (addresses Codex review MEDIUM): do NOT execute any of these scripts and do NOT pass `--help` to them — `show_confirmation_subject.py` has no argument parser, so any invocation runs `main()` and opens a live DB connection, and `reset_stuck_runs.py`/`demo_reset.py` issue real DB writes. Prove the annotated files remain syntactically valid Python with `uv run python -m py_compile scripts/demo_reset.py scripts/reset_stuck_runs.py scripts/show_confirmation_subject.py` instead (side-effect-free byte-compilation).
  </action>
  <acceptance_criteria>
    - `uv run mypy scripts/` exits 0 with `Success: no issues found`
    - `uv run python -m py_compile scripts/demo_reset.py scripts/reset_stuck_runs.py scripts/show_confirmation_subject.py` exits 0 (side-effect-free executability proof — no script is actually run)
    - `git diff scripts/` shows only annotation additions/narrowing, no CLI-behavior changes (sys.argv handling logic byte-for-byte unchanged)
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy eval/ scripts/ && uv run python -m py_compile scripts/demo_reset.py scripts/reset_stuck_runs.py scripts/show_confirmation_subject.py</automated>
  </verify>
  <done>`uv run mypy eval/ scripts/` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Eval fixture loading (eval/run_eval.py) | Local JSON fixture files parsed into typed models — no untrusted network input |
| Operational scripts (scripts/*) | Local-only CLI tools run by the developer/operator, not exposed to any network boundary — but two of them issue real DB writes when executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-11 | Tampering | eval/run_eval.py scoring-function annotation | low | accept | Eval harness is a local dev-time tool, not part of the production pipeline; a behavior slip here would surface immediately as a changed eval chart or a failing --check gate, not a live money-path bug |
| T-14-12 | Tampering | scripts/reset_stuck_runs.py annotation | medium | mitigate | This script issues real DB writes (status resets) and has no dedicated pytest coverage — verification is deliberately side-effect-free (py_compile + git-diff review showing sys.argv logic unchanged); the script is never executed during this plan |
</threat_model>

<verification>
Run `uv run mypy eval/ scripts/` — exits 0.
Run `uv run python eval/run_eval.py --check` — no regression (DB-free path).
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green.
</verification>

<success_criteria>
- Zero mypy errors across all of `eval/` and `scripts/`.
- Eval harness and operational scripts remain runnable, behavior-identical — proven without executing any DB-touching script.
- Full hermetic test suite green.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-05-SUMMARY.md` when done
</output>
