---
phase: 14-full-type-checking-mypy
plan: 06
type: execute
wave: 5
depends_on: ["14-04", "14-05"]
files_modified:
  - tests/__init__.py
  - tests/test_alias_full_loop.py
  - tests/test_bound01_private_imports.py
  - tests/test_check_schema_cli.py
  - tests/test_compose_email_field_regression.py
  - tests/test_concurrency_proof.py
  - tests/test_delivery.py
  - tests/test_demo_landing.py
  - tests/test_extract.py
  - tests/test_gate.py
  - tests/test_health_schema.py
  - tests/test_live_llm.py
  - tests/test_needs_operator.py
  - tests/test_pdf.py
  - tests/test_resume_pipeline.py
  - tests/test_seed_roundtrip.py
  - tests/test_suggest.py
  - tests/test_tax_tables_2026.py
  - tests/test_threading.py
  - tests/test_webhook.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to this plan's 20 files (19 test files + tests/__init__.py), under the tests.* relaxed override from Plan 14-01"
    - "check_untyped_defs still catches real type errors in these test bodies — the relaxed override is not a blanket exclusion"
    - "No test assertion is weakened — proven by a dynamic before/after pass-skip baseline PLUS a diff review showing zero removed/modified assertion lines (unchanged counts alone do not prove assertions weren't weakened)"
  artifacts: []
  key_links: []
---

<objective>
Bring the first third (19 test files + `tests/__init__.py`, ~8950 lines, alphabetically/by-size-balanced group 1) of `tests/` to zero mypy errors under Plan 14-01's `tests.*` relaxed override (`check_untyped_defs = true`, `disallow_untyped_defs = false`).

Purpose: TYPE-02 requires the entire repo — including tests — to have no type-checking blind spot. The relaxed override means most of these ~785 unannotated test defs do NOT need hand-annotation, but `check_untyped_defs = true` still type-checks every test body, so real errors (bad fixture shapes, wrong mock return types, incorrect monkeypatch targets) must still be fixed. Test files import `app/` (including routes/main via TestClient) and `eval/` — all clean after Waves 2-4 — so per-file mypy gates here report only test-file errors. The three test-group plans (06/07/08) are mutually mypy-independent: mypy only follows imports, and test modules do not import each other or conftest.py, so they run in parallel in Wave 5.

Output: Zero mypy errors across this plan's 20 files; full hermetic suite green with zero assertion changes.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **MEDIUM — stale pinned `614 passed / 20 skipped` baseline:** all pinned counts removed. The baseline is captured dynamically at task start (Phase 13's verified full-suite reference was 615 passed / 50 skipped via `uv run pytest -q`, but the hermetic-subset counts differ and drift — Phase 12 explicitly rejected permanent pinned-count gates). Before/after counts from the SAME command in the SAME session must match.
- **MEDIUM — unchanged pass/skip counts don't prove assertions weren't weakened:** added a diff-level criterion — zero removed or modified assertion lines in `git diff` (an added `x is not None`-style narrowing assertion is permitted and must be listed in the SUMMARY).
- **LOW — `tests/__init__.py` declared in scope but omitted from the mypy command:** it is now included in the Task 1 command explicitly.

## Artifacts this phase produces (Plan 06)

- No new public symbols — type-level fixes in existing test files only (fixture/mock/monkeypatch annotations added solely to resolve real reported errors).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve mypy errors in the concurrency/delivery/resume/threading test cluster</name>
  <files>tests/__init__.py, tests/test_alias_full_loop.py, tests/test_bound01_private_imports.py, tests/test_concurrency_proof.py, tests/test_delivery.py, tests/test_resume_pipeline.py, tests/test_threading.py, tests/test_needs_operator.py</files>
  <read_first>
    tests/conftest.py (shared fixtures every test file draws from — read once here, do not re-read in Task 2)
    tests/test_resume_pipeline.py, tests/test_threading.py (the two largest files in this task — 1682 and 763 lines respectively)
    .planning/phases/14-full-type-checking-mypy/14-CONTEXT.md (D-02 — every test body still type-checked, only annotation requirement relaxed)
  </read_first>
  <action>
    FIRST, before any edit: run `uv run pytest -q -m "not integration and not live_llm"` and record the exact passed/skipped counts — this is the dynamic baseline for the whole plan (addresses Codex review MEDIUM: do not pin counts from planning docs; capture them live).
    Run `uv run mypy tests/__init__.py tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py` to get the real current error list under the relaxed `tests.*` override. `tests/__init__.py` is empty (0 lines) and needs no changes — including it in this command (addresses Codex review LOW) closes the one file in the repo not otherwise measured by any Phase 14 plan.
    Fix reported errors: `type-arg` (bare dict/list needing type args in fixture builders), `no-untyped-call` (calling an unannotated helper — usually resolved once the helper itself gets annotated, or by annotating the call site's expected return type), `attr-defined`/`index` errors from monkeypatch/mock usage (annotate `monkeypatch: pytest.MonkeyPatch` parameters explicitly where mypy can't infer the type from an untyped test function signature under `check_untyped_defs`), and any genuine fixture-shape mismatches. Do NOT add full parameter annotations to every test function purely for style — the D-02 override exists specifically so untyped test defs are not required; only annotate what's needed to resolve an actual reported error.
    `test_concurrency_proof.py` and `test_threading.py` use `threading.Barrier`/thread-pool patterns from the Phase 10 concurrency-proof work — annotate thread target functions' signatures if mypy flags them, matching the actual repo helper signatures they call (`repo.insert_inbound_email`, `repo.create_run`, etc., already typed in Plan 14-02).
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/__init__.py tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py -q -m "not integration and not live_llm"` exits 0
    - `git diff -- tests/` piped through a grep for removed assertion lines (diff lines starting with a single minus that contain an assertion statement) produces no output — no assertion was removed or modified; any ADDED narrowing assertion (is-not-None style) is listed in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/__init__.py tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py</automated>
  </verify>
  <done>`uv run mypy` over this task's 8 files exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Resolve mypy errors in the remaining test files in this group</name>
  <files>tests/test_check_schema_cli.py, tests/test_compose_email_field_regression.py, tests/test_demo_landing.py, tests/test_extract.py, tests/test_gate.py, tests/test_health_schema.py, tests/test_live_llm.py, tests/test_pdf.py, tests/test_seed_roundtrip.py, tests/test_suggest.py, tests/test_tax_tables_2026.py, tests/test_webhook.py</files>
  <read_first>
    tests/test_demo_landing.py (1210 lines, largest file in this task)
    tests/test_webhook.py, tests/test_extract.py, tests/test_suggest.py (LLM-facing test seams — must keep stubbing LLM calls per the execute-phase-integration-hazards memory note, never rely on env emptiness)
  </read_first>
  <action>
    Run `uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py` first to get the real current error list.
    Fix reported errors following the same discipline as Task 1: annotate only what's needed to resolve a real error, prefer fixing the underlying fixture/mock type over adding an ignore, and confirm `test_live_llm.py` (the live-LLM-gated test file) still correctly skips under the two-factor guard (no DATABASE_URL/ALLOW_LIVE_LLM in the test environment) after any annotation changes.
    After all edits, re-run `uv run pytest -q -m "not integration and not live_llm"` and compare against the baseline captured at Task 1 start.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py` exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 and reports passed/skipped counts IDENTICAL to the dynamic baseline captured before any edit in Task 1 (both counts recorded in the SUMMARY); any legitimate delta must be documented and justified in the SUMMARY
    - `git diff -- tests/` shows zero removed/modified assertion lines (same diff-review rule as Task 1)
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py</automated>
  </verify>
  <done>`uv run mypy` over this task's 12 files exits 0 with `Success: no issues found` and the full hermetic suite matches the captured baseline</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only code (no production trust boundary) | This plan touches only `tests/`, never exercised in the deployed app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-13 | Tampering | Test-file annotation edits accidentally weakening an assertion | medium | mitigate | Two-layer proof: (1) dynamic before/after pass-skip baseline captured in-session (not pinned from docs), (2) git-diff review requiring zero removed/modified assertion lines — because identical counts alone cannot prove an assertion wasn't weakened (Codex review finding) |
</threat_model>

<verification>
Run `uv run mypy` over this plan's 20 files — exits 0 (full tests/ check happens in Plan 14-09's integration gate; this plan verifies its own file set in isolation).
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, counts identical to the in-session baseline.
Review `git diff -- tests/` for assertion-line changes — none removed/modified.
</verification>

<success_criteria>
- Zero mypy errors across this plan's 20 files under the tests.* relaxed override.
- Full hermetic test suite green, counts identical to the dynamically captured baseline, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-06-SUMMARY.md` when done
</output>
