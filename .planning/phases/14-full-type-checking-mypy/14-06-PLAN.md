---
phase: 14-full-type-checking-mypy
plan: 06
type: execute
wave: 3
depends_on: ["14-02", "14-03", "14-04", "14-05"]
files_modified:
  - tests/__init__.py
  - tests/test_alias_full_loop.py
  - tests/test_bound01_private_imports.py
  - tests/test_check_schema_cli.py
  - tests/test_compose_email_field_regression.py
  - tests/test_concurrency_proof.py
  - tests/test_delivery.py
  - tests/test_demo_landing.py
  - tests/test_extract.py
  - tests/test_gate.py
  - tests/test_health_schema.py
  - tests/test_live_llm.py
  - tests/test_needs_operator.py
  - tests/test_pdf.py
  - tests/test_resume_pipeline.py
  - tests/test_seed_roundtrip.py
  - tests/test_suggest.py
  - tests/test_tax_tables_2026.py
  - tests/test_threading.py
  - tests/test_webhook.py
autonomous: true
requirements: [TYPE-02]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to this plan's 19 test files, under the tests.* relaxed override from Plan 01"
    - "check_untyped_defs still catches real type errors in these test bodies — the relaxed override is not a blanket exclusion"
    - "No test assertion changes — only type-level fixes (fixture typing, mock/monkeypatch typing, real errors surfaced by check_untyped_defs)"
  artifacts: []
  key_links: []
---

<objective>
Bring the first third (19 files, ~8950 lines, alphabetically/by-size-balanced group 1) of `tests/` to zero mypy errors under Plan 01's `tests.*` relaxed override (`check_untyped_defs = true`, `disallow_untyped_defs = false`).

Purpose: TYPE-02 requires the entire repo — including tests — to have no type-checking blind spot. The relaxed override means most of these ~785 unannotated test defs do NOT need hand-annotation, but `check_untyped_defs = true` still type-checks every test body, so real errors (bad fixture shapes, wrong mock return types, incorrect monkeypatch targets) must still be fixed.

Output: Zero mypy errors across this plan's 19 test files; full hermetic suite green with zero assertion changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve mypy errors in the concurrency/delivery/resume/threading test cluster</name>
  <files>tests/__init__.py, tests/test_alias_full_loop.py, tests/test_bound01_private_imports.py, tests/test_concurrency_proof.py, tests/test_delivery.py, tests/test_resume_pipeline.py, tests/test_threading.py, tests/test_needs_operator.py</files>
  <read_first>
    tests/conftest.py (shared fixtures every test file draws from — read once here, do not re-read in Task 2)
    tests/test_resume_pipeline.py, tests/test_threading.py (the two largest files in this task — 1682 and 763 lines respectively)
    .planning/phases/14-full-type-checking-mypy/14-CONTEXT.md (D-02 — every test body still type-checked, only annotation requirement relaxed)
  </read_first>
  <action>
    Run `uv run mypy tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py` first to get the real current error list under the relaxed `tests.*` override. `tests/__init__.py` is empty (0 lines) and needs no changes — confirm it exits 0 trivially as part of this same command, closing the one file in the repo not yet assigned to any Phase 14 plan.
    Fix reported errors: `type-arg` (bare dict/list needing type args in fixture builders), `no-untyped-call` (calling an unannotated helper — usually resolved once the helper itself gets annotated, or by annotating the call site's expected return type), `attr-defined`/`index` errors from monkeypatch/mock usage (annotate `monkeypatch: pytest.MonkeyPatch` parameters explicitly where mypy can't infer the type from an untyped test function signature under `check_untyped_defs`), and any genuine fixture-shape mismatches. Do NOT add full parameter annotations to every test function purely for style — the D-02 override exists specifically so untyped test defs are not required; only annotate what's needed to resolve an actual reported error.
    `test_concurrency_proof.py` and `test_threading.py` use `threading.Barrier`/thread-pool patterns from the Phase 10 concurrency-proof work — annotate thread target functions' signatures if mypy flags them, matching the actual repo helper signatures they call (`repo.insert_inbound_email`, `repo.create_run`, etc., already typed in Plan 02).
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py -q -m "not integration and not live_llm"` exits 0 with the same pass/skip counts as before this task's changes
    - `git diff` shows zero assertion changes — only type annotations on fixtures/mocks/thread-target signatures where needed to resolve a real reported error
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py</automated>
  </verify>
  <done>`uv run mypy tests/test_alias_full_loop.py tests/test_bound01_private_imports.py tests/test_concurrency_proof.py tests/test_delivery.py tests/test_resume_pipeline.py tests/test_threading.py tests/test_needs_operator.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Resolve mypy errors in the remaining test files in this group</name>
  <files>tests/test_check_schema_cli.py, tests/test_compose_email_field_regression.py, tests/test_demo_landing.py, tests/test_extract.py, tests/test_gate.py, tests/test_health_schema.py, tests/test_live_llm.py, tests/test_pdf.py, tests/test_seed_roundtrip.py, tests/test_suggest.py, tests/test_tax_tables_2026.py, tests/test_webhook.py</files>
  <read_first>
    tests/test_demo_landing.py (1210 lines, largest file in this task)
    tests/test_webhook.py, tests/test_extract.py, tests/test_suggest.py (LLM-facing test seams — must keep stubbing LLM calls per the execute-phase-integration-hazards memory note, never rely on env emptiness)
  </read_first>
  <action>
    Run `uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py` first to get the real current error list.
    Fix reported errors following the same discipline as Task 1: annotate only what's needed to resolve a real error, prefer fixing the underlying fixture/mock type over adding an ignore, and confirm `test_live_llm.py` (the live-LLM-gated test file) still correctly skips under the two-factor guard (no DATABASE_URL/ALLOW_LIVE_LLM in the test environment) after any annotation changes.
  </action>
  <acceptance_criteria>
    - `uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py` exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 with the same 614-passed/20-skipped baseline (or documents any legitimate count change in the plan SUMMARY)
    - `git diff` shows zero assertion changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py</automated>
  </verify>
  <done>`uv run mypy tests/test_check_schema_cli.py tests/test_compose_email_field_regression.py tests/test_demo_landing.py tests/test_extract.py tests/test_gate.py tests/test_health_schema.py tests/test_live_llm.py tests/test_pdf.py tests/test_seed_roundtrip.py tests/test_suggest.py tests/test_tax_tables_2026.py tests/test_webhook.py` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only code (no production trust boundary) | This plan touches only `tests/`, never exercised in the deployed app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | Test-file annotation edits accidentally weakening an assertion | mitigate | Every task requires the exact same pass/skip count post-change (614 passed baseline) — any drop in assertions executed is caught by this count check, not just an exit-0 pass |
</threat_model>

<verification>
Run `uv run mypy tests/` (this plan's 19 files are a subset — full tests/ mypy check happens once all 3 test-group plans land; this plan verifies its own file set exits 0 in isolation).
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, same pass/skip counts as before this plan.
</verification>

<success_criteria>
- Zero mypy errors across this plan's 19 test files under the tests.* relaxed override.
- Full hermetic test suite green, identical pass/skip counts, zero assertion changes.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-06-SUMMARY.md` when done
</output>
