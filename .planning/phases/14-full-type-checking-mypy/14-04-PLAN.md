---
phase: 14-full-type-checking-mypy
plan: 04
type: execute
wave: 4
depends_on: ["14-02", "14-03"]
files_modified:
  - app/main.py
  - app/routes/__init__.py
  - app/routes/dashboard.py
  - app/routes/demo.py
  - app/routes/health.py
  - app/routes/pipeline_glue.py
  - app/routes/runs.py
  - app/routes/templating.py
  - app/routes/webhook.py
autonomous: true
requirements: [TYPE-01]

must_haves:
  truths:
    - "uv run mypy exits 0 (no errors) when scoped to app/routes/ and app/main.py"
    - "FastAPI route handler signatures and Jinja2Templates usage type-check cleanly against their shipped stubs"
    - "No annotation introduces a behavior change — the full hermetic test suite passes unmodified"
  artifacts:
    - path: "app/routes/runs.py"
      provides: "fully strict-annotated operator-gate flow (approve/reject/retrigger/paystub PDF)"
    - path: "app/routes/pipeline_glue.py"
      provides: "fully strict-annotated HTTP-to-orchestrator bridge functions (row_to_inbound, reply_sender_ok, finish_reply_resume, route_reply, resume_pipeline_bg, run_pipeline_bg, operator_resume_bg)"
    - path: "app/main.py"
      provides: "thin 16-line app-assembly module (router registration + static mount), fully annotated"
  key_links:
    - from: "app/routes/webhook.py"
      to: "app/routes/pipeline_glue.py"
      via: "shared bridge functions route_reply / run_pipeline_bg"
      pattern: "from app.routes import pipeline_glue"
    - from: "app/main.py"
      to: "app/routes/dashboard, demo, health, runs, webhook"
      via: "router registration — app.include_router(x.router) for each of the five route modules"
      pattern: "include_router"
---

<objective>
Bring `app/routes/` (all 7 files: dashboard, demo, health, pipeline_glue, runs, templating, webhook) and `app/main.py` to zero mypy errors under the strict config from Plan 14-01.

Purpose: `app/routes/` is the HTTP-facing surface — FastAPI route handlers, the operator-gate approval flow, and the pipeline_glue bridge functions Phase 13 carved out. Routes import DB, pipeline, models, and gateway modules (`app/routes/runs.py:19-24` crosses all of those boundaries), so this plan depends on Plans 14-02 and 14-03 — mypy traverses those imports, and file non-overlap alone does NOT imply mypy-analysis independence (addresses Codex review HIGH + MEDIUM). `app/main.py` lives here because it imports the five route modules this plan cleans: it is 16 lines of thin assembly — `from app.routes import dashboard, demo, health, runs, webhook`, five `app.include_router(...)` calls, and one `StaticFiles` mount; it has NO startup/shutdown event handlers (stale description corrected per review).

`app/email/` is NOT in this plan — it moved to Plan 14-03 (gateway ownership consolidation).

Output: Zero mypy errors across all of `app/routes/` and `app/main.py`; full hermetic suite green with zero assertion changes.
</objective>

## Review Feedback Incorporated (Codex, 2026-07-10)

- **HIGH — routes import DB and pipeline modules owned by other plans:** depends_on is now `["14-02", "14-03"]` (wave 4); every traversed module is clean before this plan runs.
- **MEDIUM — stale `read_first` symbol list (`_run_pipeline`/`_route_reply` don't exist):** replaced with the actual Phase 13 public names verified against source: `row_to_inbound`, `reply_sender_ok`, `finish_reply_resume`, `route_reply`, `resume_pipeline_bg`, `run_pipeline_bg`, `operator_resume_bg` (`app/routes/pipeline_glue.py:27-226`).
- **MEDIUM — "no file overlap therefore parallel-safe" claim:** removed; scheduling now follows the import graph, not file ownership alone.
- **Suggestion — move `app/main.py` into this plan:** done (Task 3), so its imported routes are typed in the same dependency unit.

## Artifacts this phase produces (Plan 04)

- No new public symbols — annotations and narrowing only (FastAPI response types, uuid.UUID/RunStatus domain types on existing functions).

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-full-type-checking-mypy/14-CONTEXT.md
@.planning/phases/14-full-type-checking-mypy/14-RESEARCH.md
@.planning/phases/14-full-type-checking-mypy/14-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Annotate app/routes/pipeline_glue.py, webhook.py, health.py, templating.py to mypy-clean</name>
  <files>app/routes/pipeline_glue.py, app/routes/webhook.py, app/routes/health.py, app/routes/templating.py, app/routes/__init__.py</files>
  <read_first>
    app/routes/pipeline_glue.py (the shared HTTP-to-orchestrator bridge — public functions: row_to_inbound, reply_sender_ok, finish_reply_resume, route_reply, resume_pipeline_bg, run_pipeline_bg, operator_resume_bg — Phase 13's public-name promotions)
    app/routes/webhook.py (the inbound webhook route — FastAPI Request/Response handling, reply-routing-before-first-ingest)
    app/routes/health.py, app/routes/templating.py (Jinja2Templates object + badge filters)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns: dict[str, Any] at genuinely dynamic edges, conn: psycopg.Connection | None = None)
  </read_first>
  <action>
    Run `uv run mypy app/routes/pipeline_glue.py app/routes/webhook.py app/routes/health.py app/routes/templating.py app/routes/__init__.py` first to get the real current error list for exactly this file set (traversed imports — db, pipeline, models, gateway — are clean since Plans 14-02/14-03, so only this set's own errors appear).
    Fix every reported error using FastAPI's own shipped types for route handlers (Request, Response, RedirectResponse, HTTPException), Jinja2Templates' shipped types for templating.py, and the house dict[str, Any]/conn: psycopg.Connection | None patterns elsewhere. Annotate every function currently missing param/return types.
    `pipeline_glue.py`'s bridge functions are called from both `webhook.py` and `runs.py` (Task 2 of this same plan) — annotate their signatures to the concrete types their actual bodies use (InboundEmail, uuid.UUID, RunStatus, dict[str, str] for operator overrides, etc.), not Any, since these are well-typed domain objects, not dynamic edges. The DB-row `row: dict` parameters on `row_to_inbound`/`reply_sender_ok` are genuine psycopg-boundary dynamic edges — `dict[str, Any]` is the sanctioned D-10 type there.
  </action>
  <acceptance_criteria>
    - `uv run mypy app/routes/pipeline_glue.py app/routes/webhook.py app/routes/health.py app/routes/templating.py app/routes/__init__.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_webhook.py tests/test_webhook_dedup_race.py tests/test_health_schema.py tests/test_ingest.py tests/test_reply_redelivery.py -q` exits 0
    - `git diff` shows only annotation additions/narrowing, no assertion or routing-logic changes
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/routes/pipeline_glue.py app/routes/webhook.py app/routes/health.py app/routes/templating.py app/routes/__init__.py</automated>
  </verify>
  <done>`uv run mypy` over pipeline_glue/webhook/health/templating exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 2: Annotate app/routes/runs.py, dashboard.py, demo.py to mypy-clean</name>
  <files>app/routes/runs.py, app/routes/dashboard.py, app/routes/demo.py</files>
  <read_first>
    app/routes/runs.py (lines 95-135, the approve/reject/retrigger operator-gate flow; the getattr(exc, "payroll_roster", None) read site at ~line 129)
    app/routes/dashboard.py (landing page, /eval, /eval/chart.svg routes)
    app/routes/demo.py (/demo/* bind, compose, send-test routes)
    .planning/phases/14-full-type-checking-mypy/14-PATTERNS.md (Shared Patterns section)
  </read_first>
  <action>
    Run `uv run mypy app/routes/runs.py app/routes/dashboard.py app/routes/demo.py` first to get the real current error list.
    Fix every reported error using FastAPI's shipped route-handler types and the house dict[str, Any]/concrete-type patterns. The `getattr(exc, "payroll_roster", None)` call in `runs.py` (~line 129) already handles the dynamic-attribute-on-Exception case safely via `getattr`'s optional-default form — this should type-check cleanly without needing any ignore, since `getattr` with a default is generically typed by mypy already; do not add an ignore here unless a real error is reported (verify first, per the same measure-before-fixing discipline as Plan 14-02 Task 3's no_implicit_reexport check).
    Annotate the full operator-gate flow in runs.py (approve, reject, resolve, retrigger, paystub PDF streaming response, simulate-reply) with concrete FastAPI response types (RedirectResponse, StreamingResponse) and domain types (uuid.UUID, RunStatus) rather than Any.
  </action>
  <acceptance_criteria>
    - `uv run mypy app/routes/runs.py app/routes/dashboard.py app/routes/demo.py` exits 0 with `Success: no issues found`
    - `uv run pytest tests/test_dashboard.py tests/test_demo_landing.py tests/test_demo_fixtures.py tests/test_hitl.py tests/test_needs_operator.py tests/test_retrigger_epoch.py tests/test_claim_status.py tests/test_gate.py -q` exits 0
    - `git diff app/routes/runs.py` shows the `getattr(exc, "payroll_roster", None)` line is unchanged (no ignore added) unless a real mypy error is documented in the plan SUMMARY justifying one
    - No new `# type: ignore` comments in this task's diff without a documented real error justifying each one
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/routes/runs.py app/routes/dashboard.py app/routes/demo.py</automated>
  </verify>
  <done>`uv run mypy app/routes/runs.py app/routes/dashboard.py app/routes/demo.py` exits 0 with `Success: no issues found`</done>
</task>

<task type="auto">
  <name>Task 3: Annotate app/main.py to mypy-clean</name>
  <files>app/main.py</files>
  <read_first>
    app/main.py (16 lines: module docstring, `from app.routes import dashboard, demo, health, runs, webhook`, `app = FastAPI(title="Payroll Agent")`, one StaticFiles mount, five include_router calls — NO startup/shutdown event handlers exist in this file)
    app/routes/__init__.py (the router modules main.py registers, cleaned in Tasks 1-2 of this plan)
  </read_first>
  <action>
    Run `uv run mypy app/main.py` to get the real current error list (expected to be very small or zero — this file is 16 lines of thin app assembly whose imports are all clean after Tasks 1-2; FastAPI's `include_router` and `StaticFiles` carry shipped types).
    Fix any reported error — module-level statements here rarely need annotation; if mypy is already clean, record that in the SUMMARY and make no edit. Do not add speculative annotations to a file with zero reported errors, and do not describe or add event handlers — the file has none (stale-description fix per Codex review LOW).
  </action>
  <acceptance_criteria>
    - `uv run mypy app/main.py` exits 0 with `Success: no issues found`
    - `uv run pytest -q -m "not integration and not live_llm"` exits 0 (full hermetic suite — main.py wires every router, so this is the correct regression check)
    - `git diff app/main.py` is empty OR shows only annotation additions, no changes to router registration order or app-creation logic
  </acceptance_criteria>
  <verify>
    <automated>uv run mypy app/routes/ app/main.py</automated>
  </verify>
  <done>`uv run mypy app/routes/ app/main.py` exits 0 with `Success: no issues found`</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Inbound HTTP -> webhook.py / runs.py | Untrusted request bodies and form posts cross into route handlers |
| Operator-gate flow (runs.py approve/reject) | The single human-in-the-loop money-approval gate — annotation edits here must not weaken claim_status's atomic CAS semantics |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-09 | Tampering | runs.py operator-gate route annotations | high | mitigate | Full operator-gate test suite (test_hitl, test_needs_operator, test_claim_status, test_gate) re-run after annotation; any assertion diff signals a behavior leak |
| T-14-10 | Elevation of Privilege | webhook.py reply-routing annotations | low | accept | Annotation-only changes cannot alter FIX-5 sender revalidation logic since no branching/comparison code is touched — verified via test_reply_redelivery.py staying green |
</threat_model>

<verification>
Run `uv run mypy app/routes/ app/main.py` — exits 0.
Run `uv run pytest -q -m "not integration and not live_llm"` — full hermetic suite green, no assertion changes.
</verification>

<success_criteria>
- Zero mypy errors across all of `app/routes/` (7 files) and `app/main.py`.
- Full hermetic test suite green, zero assertion changes.
- The operator-gate flow (runs.py) and reply-routing (webhook.py) remain byte-for-byte behavior-identical.
</success_criteria>

<output>
Create `.planning/phases/14-full-type-checking-mypy/14-04-SUMMARY.md` when done
</output>
