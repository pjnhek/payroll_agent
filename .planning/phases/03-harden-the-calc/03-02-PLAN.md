---
phase: 03-harden-the-calc
plan: 02
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-03
files_modified:
  - tests/test_federal_withholding.py
  - README.md
autonomous: false
requirements:
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06

# FIX D: wave 3, depends_on 03-03.
# Reason: this plan's Task 2 calls calculate() for the 401k-base-split and
# Step-4b interaction assertions, references the additional_medicare_not_modeled
# flag on PaystubLineItem, and tests integration behavior that 03-03 implements.
# Making 03-02 depend on 03-03 eliminates the residual forward dependency that
# Codex identified: the golden suite is now free of any reference to calculate()
# or PaystubLineItem that isn't yet shipped.
#
# R2-7: python-taxes dev dep REMOVED from this plan.
# Reason (per Round-2 Codex re-review): the python-taxes structural test was
# dropped in FIX F (engine is 2026-keyed; python-taxes ships 2023–2025 only).
# Installing the package despite no test using it adds supply-chain/lockfile
# churn with zero verification value. The wave-bracket PRIMARY oracle already
# provides the structural independence that python-taxes was added to contribute.
# pyproject.toml and uv.lock are therefore removed from files_modified.
# If year-injection is ever added, python-taxes can be re-introduced then.

user_setup:
  - service: layer-b-oracle-verification
    why: "For over-ceiling high earners (Thomas Bergmann, ~$9,231 biweekly, above the $3,875 biweekly wage-bracket ceiling), hand-computed expected values must be confirmed against two independent online percentage-method calculators (D-01 Decision 1, Decision 2). The wage-bracket primary oracle cannot reach these cases. This is a blocking gate — no over-ceiling golden value goes into the test module before BOTH calculators agree within ±$1. If the two calculators disagree with each other by more than $1, the Thomas Bergmann fixture is SKIPPED (pytest.mark.skip with explicit reason text: 'OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified') — it must NOT be written using a lone hand-trace as authoritative. A skipped fixture means over-ceiling coverage is UNRESOLVED (not passed) — this must be noted in the plan summary and SUMMARY.md."
    dashboard_config:
      - task: "Open usapaycheck.org (biweekly variant) and confirm it is in IRS Pub 15-T percentage-method mode by entering: Gross $800, pay frequency Weekly, Filing Status Single, Step 2 Checkbox NOT checked, Step 3 = $0, Step 4a/4b/4c = $0. The calculator should return federal withholding = $54.08 (hand-computed calibration case). If it returns a different value, investigate the method delta before using it as a layer-B oracle."
      - task: "Open paycheckcity.com/calculator/salary (or /hourly) with the same inputs (Weekly, Single, no Step 2, $800 gross, no Step 3/4 adjustments). Confirm it returns $54.08. If either calculator disagrees, note which and do NOT mark the disagreeing fixture as TRUSTED."
      - task: "Using both calculators in IRS-percentage-method mode, compute the Thomas Bergmann over-ceiling case: biweekly, married_jointly, standard (Step-2 unchecked), gross ≈ $9,230.77 (=$240k/26), Step-3=$8,000 annual, 401k=8% (so federal_taxable ≈ $8,492.31). Record both calculator results."
      - task: "AGREEMENT RULE (FIX — replaces prior 'hand trace authoritative' fallback): if both calculators AGREE within ±$1 of each other AND with the hand-computed trace → type 'approved' with both results; executor writes the fixture. If the calculators DISAGREE with each other by more than $1 (regardless of hand trace) → type 'method-delta: [describe what each tool returned]'. The executor marks the Thomas Bergmann over-ceiling fixture with @pytest.mark.skip(reason='OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified') and does NOT write a golden value. The plan summary MUST state 'over-ceiling coverage is UNRESOLVED — Thomas Bergmann fixture skipped.' A lone hand trace is NOT authoritative when two independent calculators disagree — it cannot break the tie."
      - task: "Record all confirmations (tool name, input used, result, date 2026-06-22) in the module docstring of tests/test_federal_withholding.py under a 'Layer-B Oracle Verification' section."

must_haves:
  truths:
    - "Federal withholding amounts are independently verified — a transcription error in tax_tables_2026.py causes the wage-bracket cross-check to fail, not silently pass"
    - "All 6 Worksheet 1A schedule columns are covered by the wage-bracket cross-check sweep: single-standard, single-step2, married_jointly-standard, married_jointly-step2, married_separately-standard, married_separately-step2"
    - "The engine correctly floors withholding at $0.00 for any wage/credit combination — no golden case can produce a negative result"
    - "A filing_status='head_of_household' input to federal_withholding_2026 raises an error rather than silently mapping to the wrong schedule"
    - "The Additional Medicare 0.9% limitation is observable: a (ytd_ss_wages+gross) > $200k proxy case carries a documented 'additional_medicare_not_modeled' flag rather than asserting flat 1.45% is correct"
    - "SS wage-base straddle case: Thomas Bergmann-like fixture asserts partial SS-taxable amount"
    - "401k-reduces-federal-not-FICA case: fixture asserts FICA uses gross base, federal uses gross-minus-401k base"
    - "Bracket boundary tests: _find_bracket() is verified DIRECTLY with adjusted-annual-wage inputs at exact lower bound B, B minus $0.01, and B plus $0.01 for at least one schedule — these inputs bypass the annualization-rounding confound"
    - "Whole-dollar comparison in wage-bracket cross-check uses quantize(Decimal('1'), ROUND_HALF_UP) not Python round()"
    - "Wage-bracket cross-check uses EXACT equality (engine_whole_dollar == published_cell) by default; ±$1 tolerance is only allowed on a SPECIFICALLY-NAMED fixture row with a documented extraction/rounding-anomaly reason in a comment"
    - "If the Thomas Bergmann over-ceiling fixture is skipped, the plan summary and SUMMARY.md explicitly state 'over-ceiling coverage is UNRESOLVED — Thomas Bergmann fixture skipped' — a skip is not treated as coverage"
  artifacts:
    - path: "tests/test_federal_withholding.py"
      provides: "Golden suite for Pub 15-T 2026 Worksheet 1A engine"
      exports: ["test_wage_bracket_cross_check", "test_federal_withholding_golden", "test_bracket_boundary", "test_additional_medicare_limitation_is_flagged", "test_hoh_reject_guard"]
  key_links:
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/federal_withholding.py"
      via: "from app.pipeline.federal_withholding import federal_withholding_2026, _find_bracket"
      pattern: "from app.pipeline.federal_withholding import"
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/calculate.py"
      via: "from app.pipeline.calculate import calculate (for the 401k-interaction and Additional Medicare flag cases only — 03-03 must be complete first)"
      pattern: "from app.pipeline.calculate import calculate"
---

<objective>
Write the golden-value test suite for the Pub 15-T 2026 Worksheet 1A engine, implementing the revised oracle hierarchy (User Decision 2): wage-bracket tables (Deliverable 5) as the PRIMARY independent oracle for under-ceiling fixtures; online calculators as SECONDARY corroboration for over-ceiling high earners (with a hard two-calculator agreement requirement — FIX: no lone-hand-trace fallback). Also implements the Additional Medicare limitation flag test (User Decision 1, FIX B — proxy-based trigger from 03-03), the HoH reject-guard test (Fix 5), bracket boundary tests (_find_bracket() tested DIRECTLY with adjusted-annual-wage inputs — R2-4), and the Step-4b FICA-base assertion (Fix 7b). python-taxes structural test is DROPPED (FIX F — not runnable against 2026-keyed engine; independence for 2026 fixtures already provided by the wage-bracket PRIMARY oracle). python-taxes dev dep is ALSO removed (R2-7 — zero verification value after structural test drop; supply-chain/lockfile churn with no benefit).

Purpose: This is the correctness oracle for the payroll calc — the most-tested unit in the repo (CLAUDE.md §6). After this plan, every CALC-04/CALC-05/CALC-06 claim is backed by tests with genuinely independently-derived expected values. The wage-bracket cross-check provides structural independence: a transcription typo in tax_tables_2026.py cannot simultaneously corrupt both the percentage-table rows (Deliverable 1) and the separately-transcribed wage-bracket cells (Deliverable 5).

Output: tests/test_federal_withholding.py with:
- A wage-bracket cross-check sweeping all 6 schedule columns (PRIMARY oracle — in-PDF independent), with EXACT whole-dollar equality via quantize(Decimal("1"), ROUND_HALF_UP); ±$1 only on named-exception rows (R2-1, R2-8)
- Explicit bracket boundary tests that call _find_bracket() DIRECTLY with adjusted-annual-wage inputs at B, B-$0.01, B+$0.01 (R2-4)
- The manual layer-B calculator verification checkpoint (SECONDARY — blocks the over-ceiling high-earner fixture only; requires two-calculator agreement or the fixture is SKIPPED with reason "OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified")
- The full D-04 golden matrix (penny-exact traces for named edge cases)
- User Decision 1: Additional Medicare limitation flag test using REALISTIC SS-capped YTD values (R2-2)
- HoH reject-guard test
- README "Known Limitations" disclaimer update
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/models/roster.py
@app/models/contracts.py
@app/db/seed.py
@app/pipeline/federal_withholding.py
@app/pipeline/tax_tables_2026.py
@tests/test_calculate.py
@.planning/phases/03-harden-the-calc/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The executor has constructed the wage-bracket cross-check fixture (PRIMARY oracle — in-PDF independent, no calculator needed) for all under-ceiling schedules covering all 6 column combinations. For the over-ceiling high earner (Thomas Bergmann, ~$9,231 biweekly, above the $3,875 biweekly ceiling), the executor has computed a hand-traced expected value. That hand-traced value CANNOT be entered into the test file until it is confirmed by TWO independent layer-B calculators that AGREE within ±$1 of each other. If they disagree, the fixture is SKIPPED with skip reason "OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified" — a lone hand trace is not authoritative when calculators disagree (FIX: removes the prior "hand trace authoritative if calculators disagree" language). The wage-bracket primary oracle (in-PDF) does not require human verification.
  </what-built>
  <how-to-verify>
    Step 1 — Verify usapaycheck.org is in IRS Pub 15-T percentage-method mode (calibration check):
      Open https://usapaycheck.org/weekly-paycheck-calculator/ (or biweekly variant).
      Enter: Gross wage $800, Pay frequency Weekly, Filing status Single, Step 2 checkbox NOT checked, Step 3 dependents/credits $0, Step 4a other income $0, Step 4b deductions $0, Step 4c extra $0.
      Expected federal withholding: $54.08 (calibration case — must match to confirm the tool is in IRS percentage-method mode).
      If the tool returns $54.08 → CONFIRMED in IRS percentage-method mode.
      If it returns a different value → fall back to paycheckcity.com and document the discrepancy.

    Step 2 — Verify paycheckcity.com (calibration check):
      Open https://www.paycheckcity.com/calculator/salary (or hourly variant).
      Same calibration inputs: Weekly, Single, no Step 2, $800 gross, no adjustments.
      Expected: $54.08. Confirm agreement.

    Step 3 — Thomas Bergmann over-ceiling case (biweekly, MFJ standard):
      Thomas Bergmann: annual_salary=$240,000, pay_periods=26, retirement_contribution_pct=0.08, step_2_checkbox=False, step_3_dependents=$8,000, step_4a_other_income=$0, step_4b_deductions=$0.
      Per-period gross = $240,000 / 26 = $9,230.769... → $9,230.77 (rounded to cents).
      Federal taxable (after 401k) = $9,230.77 × (1 − 0.08) = $8,492.31 (rounded to cents).
      CONFIRM: both usapaycheck.org (biweekly) and paycheckcity.com return the same federal withholding for these inputs.
      Record both tool results.

    Step 4 — AGREEMENT RULE (FIX — replaces prior "hand trace authoritative" fallback):
      If both calculators AGREE with each other within ±$1:
        → Type "approved: [usapaycheck result], [paycheckcity result]". The executor writes the Thomas Bergmann fixture with the agreed value.
      If the calculators DISAGREE with each other by more than $1 (regardless of hand trace):
        → Type "method-delta: [describe what each tool returned]".
        → The executor marks the Thomas Bergmann fixture with:
            @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified")
          and does NOT write a golden value.
        → The executor records in the plan summary and SUMMARY.md: "Over-ceiling coverage is UNRESOLVED — Thomas Bergmann fixture skipped. This is not treated as coverage."
        → A lone hand trace cannot break a two-calculator disagreement — investigate: confirm both tools are in 2026 IRS percentage-method mode (re-run calibration); check for calculator stale-table risk (pre-OBBBA tables).
        → Note: the calibration case ($800/week/Single → $54.08) is under-ceiling and is NOT affected by calculator disagreement on the over-ceiling case.

    Step 5 — Record in the test module docstring (the executor does this after "approved" or "method-delta"):
      "Layer-B Oracle Verification (2026-06-22): usapaycheck.org returned $XX.XX for Single/Standard/Weekly/$800 [calibration]; paycheckcity.com returned $XX.XX [calibration]. Thomas Bergmann biweekly MFJ over-ceiling: usapaycheck.org returned $XX.XX; paycheckcity.com returned $XX.XX. [Both agree (within ±$1) with hand-computed trace → oracle TRUSTED for Thomas Bergmann fixture.] OR [Calculators disagree by $X.XX → Thomas Bergmann fixture marked @pytest.mark.skip(reason='OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified').]"
  </how-to-verify>
  <resume-signal>Type "approved: [usapaycheck result], [paycheckcity result]" once both calculators agree within ±$1 and the Thomas Bergmann verification record is ready. Or type "method-delta: [describe what each tool returned]" if the two calculators disagree by more than $1 — this triggers the skip path with reason "OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified".</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_federal_withholding.py — wage-bracket PRIMARY oracle (exact equality) + _find_bracket() direct boundary tests + full D-04 golden suite (CALC-03/04/05/06, R2-1/R2-2/R2-4/R2-5/R2-6/R2-7/R2-8, FIX D/E/F/G/H)</name>
  <files>tests/test_federal_withholding.py, README.md</files>

  <read_first>
    RESEARCH.md §"MANDATORY DELIVERABLE 5: Wage Bracket Method Tables" — the full Deliverable 5 section: midpoint cross-check rule, ceiling boundaries per frequency (weekly $1,925 / biweekly $3,875 / semimonthly $4,185 / monthly $8,395), the verbatim sample block (pp.14–16 weekly Single/MFS Standard rows), and the all-six-columns structure (MFJ Standard, MFJ Checkbox, HoH Standard, HoH Checkbox, Single/MFS Standard, Single/MFS Checkbox).
    RESEARCH.md §"Finding 5" — the construction convention ([ASSUMED] midpoint + whole-dollar half-up). This is why midpoint + quantize(Decimal("1"), ROUND_HALF_UP) should produce EXACT equality against the published cell — the same convention the IRS uses. A blanket ±$1 tolerance would mask a real $1 transcription or line-1g bug.
    RESEARCH.md §"Finding 4" — all four frequencies (52/26/24/12) are covered in the wage-bracket tables; all 6 column combinations exist per row.
    RESEARCH.md §"Finding 3" — exact ceiling values per frequency; Thomas Bergmann is ABOVE the $3,875 biweekly ceiling.
    RESEARCH.md §"MANDATORY DELIVERABLE 1: Worksheet 1A Bracket Tables" — bracket rows for hand-computing penny-exact expected values and for deriving direct _find_bracket() boundary inputs.
    RESEARCH.md §"Code Examples: Golden Test Pattern" — the parametrize table shape, _make_employee() factory, Decimal assertion style.
    PATTERNS.md §"tests/test_federal_withholding.py" — imports pattern, file header docstring, _make_employee() helper, seeded-employee fixture pattern, assertion style.
    PATTERNS.md §"Synthetic Employee Fixtures" — the _make_employee() factory signature (all 15 fields required; pay_type/hourly_rate/annual_salary invariant from @model_validator).
    app/db/seed.py — all 7 seeded employees with exact W-4 field values.
    app/pipeline/federal_withholding.py — confirm _find_bracket() is importable and its signature (needed for R2-4 direct boundary tests).
    app/pipeline/tax_tables_2026.py — read STANDARD_BRACKETS["single"] bracket rows to get exact boundary values B for the direct _find_bracket() tests (R2-4).
    app/models/contracts.py — confirm additional_medicare_not_modeled: bool field is present (added by 03-03 Task 1).
    tests/test_calculate.py — existing pattern to follow (import style, fixture convention, Decimal arithmetic).
    .planning/phases/03-harden-the-calc/03-03-SUMMARY.md — confirm calculate() signature, _raise_if_reconciliation_drift() helper, and PayrollCalculationError are available (03-03 must be complete before this plan runs).
  </read_first>

  <behavior>
    WAGE-BRACKET PRIMARY ORACLE — EXACT EQUALITY (R2-1, R2-8):

    test_wage_bracket_cross_check: A property-style parametrized test sweeping MANY wage points per schedule covering ALL 6 schedule columns (R2-8: the objective claims all 6 — acceptance criteria must match). For each covered schedule, walk across multiple wage-bracket intervals, evaluate the engine at the interval midpoint, convert to whole dollar using quantize(Decimal("1"), rounding=ROUND_HALF_UP) (NOT Python builtin round()), and assert EXACT EQUALITY (==) against the published wage-bracket cell.

    R2-1 EXACT EQUALITY RULE — override from prior ±$1 blanket tolerance:
    - The IRS builds each wage-bracket cell by applying the percentage method to the midpoint of the wage interval and rounding to the whole dollar (Finding 5 [ASSUMED] convention).
    - Therefore: evaluating the engine at the interval midpoint and applying quantize(Decimal("1"), ROUND_HALF_UP) SHOULD produce the EXACT published cell.
    - A blanket abs(cell_whole_dollar - published_cell) <= 1 tolerance masks a real $1 transcription or line-1g bug in the PRIMARY oracle — which defeats the independence guarantee.
    - DEFAULT ASSERTION: assert engine_whole_dollar == published_cell (exact equality, no tolerance).
    - NAMED EXCEPTION ONLY: if a specific fixture row is known to have a documented extraction anomaly or rounding discrepancy (e.g., a wage interval whose midpoint falls on a half-dollar boundary that diverges from the IRS's actual calculation convention), that SINGLE row may use abs(...) <= 1 with a mandatory inline comment:
        # NAMED EXCEPTION: interval [$X–$Y], midpoint $M.50, half-dollar boundary
        # produces ±$1 divergence from the IRS cell due to [specific reason].
        # This is the only row with tolerance; all other rows use exact equality.
    - If no anomaly is documented for any row in this suite, use exact equality everywhere. Do NOT apply ±$1 as a default.

    Add acceptance criterion comment in the test function:
    # R2-1: Exact equality (engine_whole_dollar == published_cell) is the DEFAULT.
    # ±$1 tolerance is ONLY permitted on a specifically-named fixture row with a
    # documented extraction/rounding-anomaly reason. A blanket ±$1 tolerance
    # would mask a real $1 bracket-table or line-1g transcription bug in the
    # PRIMARY oracle — defeating the independence guarantee.
    # Rounding note: quantize(Decimal("1"), ROUND_HALF_UP) (not Python round()
    # which is half-even/banker's rounding) matches the IRS's own whole-dollar
    # convention per Finding 5 [ASSUMED].
    # Per-step cent quantization in federal_withholding.py is a CHOSEN ENGINE
    # CONVENTION (IRS whole-dollar rounding is optional per Pub 15-T Deliverable 2;
    # ROUND_HALF_UP is the project pin per D-03/WR-06). This is NOT IRS-mandated
    # per-step behavior.

    Schedule column coverage — ALL 6 (R2-8):
    - Weekly (52) Single/MFS Standard: 6–8 rows from Deliverable 5 verbatim sample (pp.14)
    - Weekly (52) MFJ Standard: 4–6 rows
    - Weekly (52) Single/MFS Step-2 Checkbox: 4–6 rows (under ceiling)
    - Weekly (52) MFJ Step-2 Checkbox: 4–6 rows (under ceiling)
    - Biweekly (26) Single/MFS Standard: 4–6 rows (all under $3,875)
    - Biweekly (26) or Semimonthly (24) or Monthly (12): 4–6 rows for any remaining coverage
    HoH columns are NOT covered (reject-guard; document in comment).
    This covers all 6 project-relevant columns (MFJ Std, MFJ Chk, Single/MFS Std, Single/MFS Chk — project uses only these 4 filing-status combinations, but the Step-2 variants count as separate columns per the 6-column structure).

    BRACKET BOUNDARY TESTS — DIRECT _find_bracket() (R2-4):

    test_bracket_boundary: _find_bracket() is lower-bound-driven. Prior approach (constructing boundary wages via (B + STEP1_STANDARD) / 52) may NOT land on the exact annual boundary B after the engine's _money(line_1a * p) annualization rounding — the boundary test could silently test the wrong bracket.

    FIX (R2-4): Test _find_bracket() DIRECTLY with adjusted-annual-wage inputs. Import _find_bracket from app.pipeline.federal_withholding (same package, acceptable). For at least one schedule (Single/Standard), pick one internal bracket boundary B (e.g., the $19,900 boundary between the 0% and 10% brackets, or the $57,900 boundary between 10% and 12% brackets — read from tax_tables_2026.STANDARD_BRACKETS["single"]).

    Three direct test cases per boundary B:
    (a) annual_wage = B exactly → call _find_bracket(B, STANDARD_BRACKETS["single"]) → assert it returns the row whose lower == B (the bracket starting at B, not the one ending just below it). This pins the >= lower-bound-inclusive semantics.
    (b) annual_wage = B - Decimal("0.01") → assert it returns the row whose lower is the bracket just BELOW B (still in the lower bracket).
    (c) annual_wage = B + Decimal("0.01") → assert it returns the row whose lower == B (in the new bracket, same as case a — confirm the engine is stable just above the boundary).

    For each case, also hand-compute the expected federal_withholding_2026 result (using the correct bracket row's base + rate) and assert the engine returns that value. This gives both a _find_bracket() structural test and a penny-exact withholding test at the boundary, using adjusted-annual-wage inputs that bypass the annualization-rounding confound entirely.

    Add comment block per behavior:
    # R2-4 boundary tests: _find_bracket() is lower-bound-driven (>= row.lower).
    # Prior approach constructing per-period wages via (B + STEP1_STANDARD) / 52
    # may not land on the exact annual boundary B after _money(line_1a * p) rounding.
    # These tests call _find_bracket() DIRECTLY with adjusted-annual-wage inputs
    # at exactly B, B - $0.01, and B + $0.01 — no annualization rounding confound.
    # A >= vs > regression in _find_bracket() passes all midpoint checks but fails here.

    ADDITIONAL MEDICARE TESTS — REALISTIC SS-CAPPED FIXTURES (R2-2):

    test_additional_medicare_limitation_is_flagged: The test MUST use POSSIBLE (realistic) SS YTD values. SS wages are capped at $184,500 for 2026 — ytd_ss_wages CANNOT legitimately exceed this cap in a real run. The prior fixtures used ytd_ss_wages=196000/197000 (IMPOSSIBLE — above the SS wage base), which meant the test exercised the Boolean expression but not the real proxy semantics.

    R2-2 FIX — use realistic capped values:

    Flag-FIRES case:
      ytd_ss_wages = Decimal("184500")  # AT the SS wage base (realistic maximum)
      Current period: hourly_rate=Decimal("100.00"), 40 hours → gross=$4,000
      Proxy: 184500 + 4000 = $188,500 — this does NOT cross $200,000, so choose a higher gross.
      Use hourly_rate=Decimal("500.00"), 40 hours → gross=$20,000 → proxy=184500+20000=$204,500 > $200,000.
      Assert result.additional_medicare_not_modeled is True.
      Add comment: "# R2-2: ytd_ss_wages=$184,500 is the MAXIMUM possible SS YTD (the 2026 SS wage base cap).
      # Any value above $184,500 is IMPOSSIBLE in a real run — SS wages are capped.
      # Using a high current-period gross ($20,000) to push (ytd + gross) above $200k."

    Flag-DOES-NOT-fire case:
      ytd_ss_wages = Decimal("0")  # or any low normal value
      Current period: hourly_rate=Decimal("100.00"), 40 hours → gross=$4,000
      Proxy: 0 + 4000 = $4,000 << $200,000 → flag does NOT fire.
      Assert result.additional_medicare_not_modeled is False.
      Add comment: "# R2-2: normal employee — (ytd_ss_wages + gross) well below $200k threshold."

    Keep the documented-limitation comment:
    # User Decision 1 (FIX B): proxy trigger fires on (ytd_ss_wages + gross) > $200,000.
    # ytd_ss_wages is a lower-bound proxy for YTD Medicare wages (Medicare has no cap,
    # so Medicare YTD >= SS YTD always). Under-flags high earners whose true Medicare
    # YTD already exceeds their capped SS YTD — accepted limitation of the static-seed model.
    # Keep it flag-only — no 0.9% modeling.

    HoH reject-guard test:
    test_hoh_reject_guard: Monkey-patch an Employee with filing_status outside the supported set. Assert ValueError raised. Assert message contains "head_of_household".

    python-taxes DROPPED (FIX F, R2-7):
    Do NOT add a python-taxes structural test (FIX F: engine is 2026-keyed; python-taxes ships 2023–2025).
    Do NOT run "uv add --dev python-taxes" (R2-7: no test uses it; installs only supply-chain/lockfile churn with zero verification value; the wave-bracket PRIMARY oracle already provides the structural independence python-taxes was added to contribute). pyproject.toml and uv.lock are NOT modified by this plan.
    Add a one-line comment at the top of the test file explaining the absence:
    # python-taxes (PyPI 0.7.0, MIT) was considered but not installed or used in this suite.
    # Reason (FIX F + R2-7): the engine is keyed to 2026 tables; python-taxes ships 2023–2025 only.
    # A structural comparison requires injectable year tables not in scope.
    # The wage-bracket PRIMARY oracle (Deliverable 5) already provides the structural independence.
    # Supply-chain/lockfile churn with no verification value (R2-7) — see 03-REVIEWS.md Round 2.

    README disclaimer:
    Update README.md to add or update a "Known Limitations" section with:
    "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation (additional_medicare_not_modeled=True on PaystubLineItem when the Medicare-wage proxy exceeds $200,000)."
    Verify the line is present after writing.
  </behavior>

  <action>
    MODULE DOCSTRING (required — Decision 1 and User Decision 2):
    State: (a) PRIMARY oracle is the independently-transcribed wage-bracket fixture set (RESEARCH.md Deliverable 5, Pub 15-T pp.13–27). A transcription error in tax_tables_2026.py causes the wage-bracket cross-check to disagree — structural enforcement of Decision 2's self-derivation ban for all under-ceiling schedules. (b) NO expected value in the parametrize table was derived by calling tax_tables_2026 or federal_withholding_2026. (c) For over-ceiling fixtures (Thomas Bergmann ~$9,231 biweekly), expected values are hand-computed and confirmed against usapaycheck.org and paycheckcity.com; BOTH must agree within ±$1 or the fixture is @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified"). A lone hand trace is not authoritative. (d) python-taxes is NOT installed or used (FIX F + R2-7 — see comment). (e) Whole-dollar comparison uses quantize(Decimal("1"), rounding=ROUND_HALF_UP), not Python round() (FIX E). (f) EXACT equality is the default for wage-bracket assertions (R2-1); ±$1 only on named-exception rows with documented reason. (g) The per-step cent quantization in federal_withholding.py is a CHOSEN ENGINE CONVENTION (IRS whole-dollar rounding is optional; ROUND_HALF_UP is the project pin per D-03/WR-06). (h) Ceiling boundary is explicit: weekly $1,925 / biweekly $3,875 / semimonthly $4,185 / monthly $8,395. (i) User Decision 1: Additional Medicare 0.9% surtax over $200k YTD is NOT modeled; test_additional_medicare_limitation_is_flagged asserts the flag using REALISTIC capped SS YTD values (R2-2). (j) OBBBA qualified-tips and overtime deductions are disclaimed and NOT implemented. (k) All 6 schedule columns are covered in the wage-bracket sweep (R2-8).

    FIX E — ROUNDING MODULE HEADER:
    Check app/pipeline/federal_withholding.py for a note about per-step cent quantization being a chosen convention. If missing, add a one-line docstring update in that file: "Per-step rounding: _money() carries cents at each step (ROUND_HALF_UP). This is a CHOSEN engine convention — Pub 15-T permits optional whole-dollar rounding; the per-step approach eliminates step-boundary ambiguity (see RESEARCH.md Deliverable 2)." Add federal_withholding.py to this task's <files> if edited (it is in 03-01 files_modified but is now complete — editing the docstring only is a low-risk additive change).

    WAGE-BRACKET FIXTURE (independently transcribed — separate from Deliverable 1):
    Transcribe wage-bracket cells from RESEARCH.md Deliverable 5 verbatim sample block (pp.14 weekly Single/MFS Standard) and extend to cover ALL 6 schedule columns (R2-8). Structure as a list of (frequency, interval_lower, interval_upper, filing_status, step2, published_cell) tuples. Cover ALL of:
    - Weekly (52) Single/MFS Standard: 6–8 rows from Deliverable 5 verbatim sample (pp.14)
    - Weekly (52) MFJ Standard: 4–6 rows
    - Weekly (52) Single/MFS Step-2 Checkbox: 4–6 rows (under $1,925 ceiling)
    - Weekly (52) MFJ Step-2 Checkbox: 4–6 rows (under $1,925 ceiling)
    - Biweekly (26) Single/MFS Standard: 4–6 rows (all under $3,875)
    - Semimonthly (24) or Monthly (12) Single/MFS Standard: 4–6 rows (synthetic frequency)
    A comment on each tuple: "# Source: Pub 15-T 2026 p.XX, [frequency] table, [status] [step2] column, row [$at_least–$but_less_than] → $cell"

    test_wage_bracket_cross_check parametrize: for each tuple:
    1. Compute midpoint = (interval_lower + interval_upper) / 2
    2. Create a synthetic employee with: filing_status=status, step_2_checkbox=step2, step_3_dependents=Decimal("0"), step_4a_other_income=Decimal("0"), step_4b_deductions=Decimal("0"), pay_periods_per_year=frequency
    3. Call federal_withholding_2026(midpoint_per_period, synthetic_employee)
    4. cell_whole_dollar = engine_result.quantize(Decimal("1"), rounding=ROUND_HALF_UP)  # FIX E + R2-1
    5. DEFAULT: assert cell_whole_dollar == published_cell  # R2-1 EXACT EQUALITY
       (Only use abs(...) <= 1 if this specific row has a documented named exception with inline comment explaining the anomaly — otherwise exact equality always.)

    CEILING BOUNDARY CONSTANT: WAGE_BRACKET_CEILINGS = {52: Decimal("1925"), 26: Decimal("3875"), 24: Decimal("4185"), 12: Decimal("8395")}

    BRACKET BOUNDARY TESTS — DIRECT _find_bracket() (R2-4):
    Import: from app.pipeline.federal_withholding import _find_bracket
    Import: from app.pipeline.tax_tables_2026 import STANDARD_BRACKETS

    Pick one internal bracket boundary B for Single/Standard schedule — read the exact value from STANDARD_BRACKETS["single"] in tax_tables_2026.py (e.g., the boundary at lower=Decimal("19900") between the 0% and 10% brackets, or lower=Decimal("57900") between 10% and 12%). Choose whichever is easiest to verify manually.

    Three direct _find_bracket() assertions for boundary B:
    (a) result_at_B = _find_bracket(B, STANDARD_BRACKETS["single"])
        assert result_at_B.lower == B  # bracket starting at B is selected (>= inclusive)
    (b) result_below_B = _find_bracket(B - Decimal("0.01"), STANDARD_BRACKETS["single"])
        assert result_below_B.lower < B  # still in the lower bracket
    (c) result_above_B = _find_bracket(B + Decimal("0.01"), STANDARD_BRACKETS["single"])
        assert result_above_B.lower == B  # same bracket as (a)

    For each, also call federal_withholding_2026 with a synthetic employee where step_2=False, status="single", step_3=0, step_4a=0, step_4b=0, and a per-period wage computed as:
      adjusted_annual = B  (or B±0.01)
      annualized_gross = adjusted_annual + STEP1_STANDARD["single"]  (= B + 8600 for single/standard)
      per_period_gross = annualized_gross / Decimal(pay_periods_per_year)
    Use p=52 (weekly) for simplicity. Hand-compute the expected withholding from the correct bracket row and assert exact Decimal equality. Add the R2-4 comment block per behavior section.

    Note on step_4b interaction when working backwards: if using step_4b_deductions=Decimal("0") and step_4a_other_income=Decimal("0"), line_1i = _money(annualized_gross) - STEP1_STANDARD["single"] = B (or B±0.01). The per_period_gross to feed the engine = annualized_gross / p where annualized_gross = B + STEP1_STANDARD["single"] (so the engine computes line_1c = _money(per_period_gross * p) = annualized_gross, and line_1i = annualized_gross - 8600 = B). Use exact Decimal division; for p=52 this may produce a repeating decimal — feed the EXACT quotient to the engine; the engine's _money() will handle cents. The test asserts _find_bracket() structural behavior, not just the withholding output, so BOTH assertions are required.

    _make_employee() helper (PATTERNS.md §"Synthetic Employee Fixtures"):
    Keyword-only factory; defaults: pay_type="hourly", hourly_rate=Decimal("100.00"), annual_salary=None, ytd_ss_wages=Decimal("0"), retirement_contribution_pct=Decimal("0"). All 15 Employee fields passed explicitly.

    Seeded-employee fixture functions: @pytest.fixture() for maria_chen, james_okafor, thomas_bergmann, sandra_kim, priya_nair (filter by field predicate from seed(dry_run=True), never by index).

    Hand-compute all PENNY-EXACT expected values for the D-04 golden matrix using RESEARCH.md Deliverable 1. For each named edge case add a comment block:
    # Hand-computation trace:
    # 1a=$X, 1c=$X*P, 1d=$X, 1e=$X, 1f=$X, 1g=$X, 1h=$X, 1i=$X
    # bracket: [$lower, $upper), base=$C, rate=D%
    # 2e=$X, 2f=$X, 2g=$X, 2h=$X
    # 3b=$X, 3c=$X
    # 4b=$X (= expected)
    # [For over-ceiling] Layer-B: usapaycheck.org=$X.XX, paycheckcity.com=$X.XX (within ±$1 of hand trace). BOTH agree.
    # [For under-ceiling] Primary oracle: wage-bracket cross-check covers this cell

    FULL D-04 GOLDEN MATRIX (penny-exact):

    test_federal_withholding_golden parametrize table covers all cells in the D-04 matrix:
    - Single + Standard (step_2=False): at least 1 fixture per frequency from seed (Maria Chen weekly, Priya Nair biweekly)
    - Single + Step-2 checkbox: Priya Nair-like fixture (married_separately + step_2=True maps to Single/MFS Step-2 table)
    - MFJ + Standard (step_2=False): James Okafor (weekly) and Thomas Bergmann (biweekly, over-ceiling — secondary oracle, or skipped per checkpoint)
    - MFJ + Step-2 checkbox: SYNTHETIC fixture under the weekly $1,925 ceiling (wage-bracket cross-checkable)
    - married_separately + Standard: synthetic or Priya Nair-equivalent without checkbox
    - married_separately + Step-2 checkbox: Priya Nair (seeded, MFS + Step-2)
    - At least one semi-monthly (24) or monthly (12) synthetic fixture (under ceiling — wage-bracket cross-checkable)

    D-04 edge cases (all required):
    - SS wage-base straddle: uses Thomas Bergmann (ytd_ss_wages=$183,900) via the full calculate() call to assert partial SS; verify fica_ss = $600 × 0.062 = $37.20 (CALC-04)
    - 401k-reduces-federal-not-FICA (CALC-03): fixture with retirement_contribution_pct > 0 where federal_withholding uses gross-minus-401k while fica_ss uses gross; verify fica_ss == _money(gross * SS_RATE) ignoring 401k
    - Step-4b deduction does NOT reduce the FICA base (Fix 7b): fixture with step_4b_deductions > 0 asserts fica_ss is computed on gross, while federal withholding IS lowered
    - Multi-bracket high earner: Thomas Bergmann crossing into the 22% bracket (over-ceiling, secondary oracle — skipped if checkpoint not approved)
    - Below-threshold zero-withholding: wage so low that annualized wage < STEP1_STANDARD → line_1i = 0 → $0.00 withholding
    - Step-3 partial credit: step_3_dependents > 0 but not large enough to zero out withholding
    - Credit-exceeds-tentative → floor at $0.00
    - Step-4a addition: step_4a_other_income > 0 raises the annualized base
    - Step-4b deduction: step_4b_deductions > 0 lowers the annualized base
    - Hourly-with-explicit-OT gross: use calculate() result as the wages input to verify end-to-end
    NOTE: "salaried-with-leave gross" integration case belongs in tests/test_calculate.py (Plan 03) — NOT here.

    ADDITIONAL MEDICARE TESTS (R2-2 — realistic SS-capped fixtures):
    test_additional_medicare_limitation_is_flagged:
    Flag-FIRES case:
      Create synthetic employee with ytd_ss_wages=Decimal("184500") (AT the SS wage base — maximum possible real value), hourly_rate=Decimal("500.00"). Call calculate() with 40 hours → gross=$20,000 → proxy=184500+20000=$204,500 > $200k. Assert result.additional_medicare_not_modeled is True.
    Flag-DOES-NOT-fire case:
      Create normal employee (ytd_ss_wages=Decimal("0"), hourly_rate=Decimal("100.00"), 40 hours → gross=$4,000 → proxy=$4,000 << $200k). Assert result.additional_medicare_not_modeled is False.
    Include the R2-2 comment blocks per behavior section. Do NOT assert flat 1.45% is correct above $200k — flag-only.

    HoH reject-guard test:
    test_hoh_reject_guard: Monkey-patch or construct an Employee with filing_status outside the three supported values. Assert ValueError is raised. Assert message contains "head_of_household".

    README disclaimer:
    Read README.md. Add or update "Known Limitations" section with the Additional Medicare disclaimer. Verify the line is present.

    R2-5 OVER-CEILING UNRESOLVED TRACKING:
    After the checkpoint and before writing the Thomas Bergmann fixture: if the checkpoint produced "method-delta" (calculators disagree), annotate the Thomas Bergmann fixture location with:
      @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified")
    The plan SUMMARY.md output section MUST include this line if Thomas is skipped:
      "OVER-CEILING COVERAGE: UNRESOLVED — Thomas Bergmann fixture skipped. CI passing without this fixture does not constitute over-ceiling verification."
    This is not a failure state — it is an explicit acknowledgment that over-ceiling coverage is unresolved, matching the test's skip reason.
  </action>

  <acceptance_criteria>
    - The wage-bracket cross-check covers ALL 6 schedule columns (MFJ Standard, MFJ Step-2, Single/MFS Standard, Single/MFS Step-2 — and their frequency variants) per R2-8. The objective, behavior, and acceptance criteria now all say "all 6 schedule columns."
    - Wage-bracket cross-check uses EXACT equality (cell_whole_dollar == published_cell) as the DEFAULT (R2-1). ±$1 tolerance is only permitted on a SPECIFICALLY-NAMED fixture row with a documented inline comment stating the extraction/rounding anomaly reason. If no anomaly is documented, all assertions use ==.
    - Whole-dollar comparison in the wage-bracket cross-check uses quantize(Decimal("1"), rounding=ROUND_HALF_UP), NOT Python round() (FIX E). A comment documents the distinction and documents that per-step cent quantization is a CHOSEN engine convention.
    - test_bracket_boundary tests _find_bracket() DIRECTLY with adjusted-annual-wage inputs at exactly B, B-$0.01, and B+$0.01 for at least one schedule boundary (R2-4). No annualization-rounding confound. Includes the R2-4 comment block.
    - test_additional_medicare_limitation_is_flagged uses REALISTIC SS-capped fixtures: flag-fires case uses ytd_ss_wages=Decimal("184500") (at cap) plus a large current gross; flag-does-not-fire case uses a normal low-YTD employee (R2-2). No impossible ytd_ss_wages value above $184,500.
    - The over-ceiling Thomas Bergmann fixture requires BOTH calculators to agree within ±$1 or is @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified") — no lone-hand-trace fallback (FIX). If skipped, SUMMARY.md states "OVER-CEILING COVERAGE: UNRESOLVED" (R2-5).
    - No expected value in the parametrize table is produced by calling tax_tables_2026 or federal_withholding_2026 — the module docstring asserts this ban.
    - test_additional_medicare_limitation_is_flagged does NOT assert flat 1.45% is correct (User Decision 1, FIX B).
    - The README carries "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation" (FIX H).
    - test_hoh_reject_guard passes — ValueError message contains "head_of_household".
    - Step-4b deduction case asserts FICA base is gross (not gross minus Step-4b), same shape as 401k-base-split (Fix 7b).
    - No python-taxes structural test is present (FIX F); no python-taxes install step runs (R2-7); a comment explains why.
    - pyproject.toml and uv.lock are NOT modified by this plan (R2-7).
    - The salaried-with-leave integration case does NOT appear in this file — it belongs in test_calculate.py (Plan 03, Fix 9).
    - At least two distinct pay frequencies (52 + 26 from seed; plus at least one synthetic 24 or 12 in the wage-bracket sweep).
    - The module docstring explicitly names the wage-bracket cross-check as the structural independence mechanism, notes exact equality as the default assertion rule (R2-1), and notes python-taxes absence (FIX F + R2-7).
  </acceptance_criteria>

  <verify>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_federal_withholding.py -q
    </automated>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest -q
    </automated>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && grep -c "wage_bracket" tests/test_federal_withholding.py && grep -c "ROUND_HALF_UP" tests/test_federal_withholding.py && grep -c "quantize" tests/test_federal_withholding.py && grep -c "bracket_boundary" tests/test_federal_withholding.py && grep -c "additional_medicare_not_modeled" tests/test_federal_withholding.py && grep -c "head_of_household" tests/test_federal_withholding.py && grep -c "additional_medicare" README.md
    </automated>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && grep -c "_find_bracket" tests/test_federal_withholding.py
    </automated>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && python -c "
# R2-1: verify no blanket abs()<=1 tolerance exists in the cross-check test
import pathlib, re
src = pathlib.Path('tests/test_federal_withholding.py').read_text()
# The abs(...) <= 1 pattern should only appear inside a comment block or next to a NAMED_EXCEPTION marker
bare_tolerance = re.findall(r'abs\(.+\)\s*<=\s*1(?!.*NAMED EXCEPTION)', src)
if bare_tolerance:
    print(f'WARNING: {len(bare_tolerance)} unnamed ±1 tolerance(s) found — all must have NAMED EXCEPTION comment')
else:
    print('R2-1 OK: no unnamed ±1 tolerances found')
# R2-2: verify impossible SS YTD values not in the test
if 'ytd_ss_wages=Decimal(\"196000\")' in src or 'ytd_ss_wages=Decimal(\"197000\")' in src:
    print('R2-2 FAIL: impossible ytd_ss_wages values found (above 184500 cap)')
else:
    print('R2-2 OK: no impossible ytd_ss_wages values above SS cap')
# R2-7: verify python-taxes not installed
import subprocess
result = subprocess.run(['grep', '-c', 'python-taxes', 'pyproject.toml'], capture_output=True, text=True)
print(f'R2-7: python-taxes entries in pyproject.toml: {result.stdout.strip()}')
"
    </automated>
  </verify>

  <done>
    tests/test_federal_withholding.py exits with 0 failures. Wage-bracket cross-check sweeps all 6 schedule columns using exact equality (cell_whole_dollar == published_cell) by default; ±$1 only on named-exception rows (R2-1, R2-8). test_bracket_boundary tests _find_bracket() directly with adjusted-annual-wage inputs at B, B-$0.01, B+$0.01 — no annualization confound (R2-4). test_additional_medicare_limitation_is_flagged uses realistic SS-capped YTD values (ytd_ss_wages=184500 + high gross; not impossible 196000/197000) (R2-2). HoH reject-guard test passes. No python-taxes structural test; no python-taxes install; pyproject.toml and uv.lock unchanged (FIX F, R2-7). Over-ceiling Thomas Bergmann fixture: approved with golden value OR @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified") with SUMMARY.md noting "OVER-CEILING COVERAGE: UNRESOLVED" (R2-5). README disclaimer present. The full suite (uv run pytest -q) stays green. No expected value derived from the engine under test. Salaried-with-leave integration case is NOT in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External calculator output | usapaycheck.org and paycheckcity.com are external, unverifiable at CI time; their role is human-verified oracle confirmation for over-ceiling fixtures only, not CI-time computation |
| Wage-bracket fixture independence | The wage-bracket fixture is transcribed from a different PDF section (Deliverable 5 pp.13–27) than the percentage tables (Deliverable 1 p.12); a single transcription session cannot corrupt both simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Golden expected values (over-ceiling) | mitigate | Blocking human checkpoint requires BOTH calculator results to agree within ±$1 before fixture is written; if they disagree, fixture is @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified") — no lone-hand-trace-authoritative path (FIX); SUMMARY.md notes unresolved coverage (R2-5) |
| T-03-02-02 | Tampering | Wage-bracket fixture (under-ceiling) | mitigate | Fixtures transcribed independently from Deliverable 5 (separate PDF section); EXACT equality assertion (R2-1) means a $1 transcription typo in Deliverable 1 (tax_tables_2026.py) causes the cross-check to FAIL — stronger structural control than ±$1 blanket tolerance |
| T-03-02-03 | Tampering | Stale calculator oracle | mitigate | Checkpoint Step 1 calibration case ($800/week/Single → $54.08) must confirm the calculator is in IRS percentage-method mode AND using 2026 tables; disagreement between calculators triggers skip with explicit unresolved reason, not hand-trace fallback |
| T-03-02-04 | Information Disclosure | N/A | accept | Test-only file; no PII; no network calls at test runtime |
| T-03-02-05 | Denial of Service | Additional Medicare test-trap (R2-2) | mitigate | R2-2: test uses realistic capped SS YTD (ytd_ss_wages=184500, max possible) + large gross to cross $200k; impossible ytd_ss_wages values above $184,500 are prohibited — they would exercise the Boolean without testing the real proxy semantics |
| T-03-02-06 | Tampering | Rounding divergence at .5 (FIX E, R2-1) | mitigate | Whole-dollar comparison uses quantize(Decimal("1"), ROUND_HALF_UP); builtin round() (half-even) is explicitly forbidden; EXACT equality as default (R2-1) makes any $1 divergence a failing test, not a passing one |
| T-03-02-SC | Tampering | supply-chain (python-taxes) | accept | R2-7: python-taxes is NOT installed — supply-chain surface eliminated. The dev dep was removed because no test references it; the wave-bracket PRIMARY oracle provides the structural independence it would have contributed |

Note: This plan contains a blocking human checkpoint (autonomous: false). The checkpoint gate confirms the over-ceiling layer-B oracle before Thomas Bergmann's fixture value is written. Under-ceiling fixtures use the in-PDF wage-bracket PRIMARY oracle and do not require human verification. FIX D: this plan is wave 3 (depends on 03-01 and 03-03) because Task 2 calls calculate() for the 401k-base and Additional Medicare tests — calculate() is not complete until 03-03 ships. R2-7: pyproject.toml and uv.lock are NOT in files_modified (python-taxes dep removed).
</threat_model>

<verification>
cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_federal_withholding.py -q
cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest -q
grep -c "wage_bracket" tests/test_federal_withholding.py
grep -c "ROUND_HALF_UP" tests/test_federal_withholding.py
grep -c "quantize" tests/test_federal_withholding.py
grep -c "bracket_boundary" tests/test_federal_withholding.py
grep -c "_find_bracket" tests/test_federal_withholding.py
grep -c "additional_medicare_not_modeled" tests/test_federal_withholding.py
grep -c "head_of_household" tests/test_federal_withholding.py
grep -c "python.taxes" tests/test_federal_withholding.py
grep -c "additional_medicare" README.md
</verification>

<success_criteria>
1. tests/test_federal_withholding.py exits with 0 failures on uv run pytest tests/test_federal_withholding.py -q.
2. Wage-bracket cross-check covers ALL 6 schedule columns (MFJ Std, MFJ Chk, Single/MFS Std, Single/MFS Chk — and frequency variants) (R2-8).
3. Wage-bracket assertions use EXACT equality (cell_whole_dollar == published_cell) by default; ±$1 only on a named-exception row with an inline documented reason (R2-1). No blanket abs(…) <= 1 tolerance in the sweep.
4. Whole-dollar comparison uses quantize(Decimal("1"), rounding=ROUND_HALF_UP) — NOT Python round() (FIX E). Per-step cent convention documented as CHOSEN.
5. test_bracket_boundary calls _find_bracket() DIRECTLY with adjusted-annual-wage inputs at B, B-$0.01, B+$0.01 — no annualization-rounding confound (R2-4). Includes R2-4 comment block.
6. test_additional_medicare_limitation_is_flagged uses REALISTIC SS-capped fixtures: ytd_ss_wages=Decimal("184500") + large gross for fires case; normal low YTD for does-not-fire case. No impossible ytd_ss_wages above $184,500 (R2-2).
7. Ceiling boundary explicit: WAGE_BRACKET_CEILINGS constant; Thomas Bergmann identified as over-ceiling.
8. Over-ceiling fixture: either both calculators agree and fixture is written, or fixture is @pytest.mark.skip(reason="OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified") (R2-5). If skipped, SUMMARY.md explicitly states "OVER-CEILING COVERAGE: UNRESOLVED."
9. All 6 Worksheet 1A schedules (3 filing statuses × 2 Step-2 branches) represented in the golden parametrize table.
10. test_additional_medicare_limitation_is_flagged: asserts proxy-based flag fires when (ytd_ss_wages + gross) > $200k; does NOT assert flat 1.45% is correct (User Decision 1, FIX B).
11. README carries the Additional Medicare disclaimer with "flags it as a known limitation" (FIX H).
12. test_hoh_reject_guard passes; ValueError message contains "head_of_household".
13. Step-4b: FICA base is gross (not gross minus Step-4b).
14. No python-taxes structural test; no python-taxes install; a comment explains FIX F + R2-7.
15. pyproject.toml and uv.lock are NOT modified by this plan (R2-7).
16. Salaried-with-leave integration case is NOT in this file (belongs in test_calculate.py per Fix 9).
17. At least two distinct pay frequencies (52 + 26 from seed; synthetic 24 or 12 in wage-bracket sweep).
18. Module docstring identifies wage-bracket cross-check as structural independence mechanism, names exact equality as the default (R2-1), and notes python-taxes absence (FIX F + R2-7).
19. Full suite uv run pytest -q is green.
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-02-SUMMARY.md when done.

If the Thomas Bergmann over-ceiling fixture is skipped (checkpoint produced "method-delta"), include this line verbatim in the SUMMARY.md:
"OVER-CEILING COVERAGE: UNRESOLVED — Thomas Bergmann fixture marked @pytest.mark.skip(reason='OVER-CEILING ORACLE UNRESOLVED — high-earner withholding not independently verified'). CI passing without this fixture does NOT constitute over-ceiling verification. This must be resolved before any claim of full D-04 matrix coverage."
</output>
