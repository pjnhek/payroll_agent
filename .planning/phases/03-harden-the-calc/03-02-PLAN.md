---
phase: 03-harden-the-calc
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_federal_withholding.py
autonomous: false
requirements:
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06

user_setup:
  - service: layer-b-oracle-verification
    why: "For over-ceiling high earners (Thomas Bergmann, ~$9,231 biweekly, above the $3,875 biweekly wage-bracket ceiling), hand-computed expected values must be confirmed against two independent online percentage-method calculators (D-01 Decision 1, Decision 2). The wage-bracket primary oracle cannot reach these cases. This is a blocking gate — no over-ceiling golden value goes into the test module before this confirmation."
    dashboard_config:
      - task: "Open usapaycheck.org (biweekly variant) and confirm it is in IRS Pub 15-T percentage-method mode by entering: Gross $800, pay frequency Weekly, Filing Status Single, Step 2 Checkbox NOT checked, Step 3 = $0, Step 4a/4b/4c = $0. The calculator should return federal withholding = $54.08 (hand-computed). If it returns a different value, investigate the method delta before using it as a layer-B oracle."
      - task: "Open paycheckcity.com/calculator/salary (or /hourly) with the same inputs (Weekly, Single, no Step 2, $800 gross, no Step 3/4 adjustments). Confirm it returns the same $54.08. If either calculator disagrees, note which and do NOT mark the disagreeing fixture as TRUSTED."
      - task: "Using both calculators in IRS-percentage-method mode, compute the Thomas Bergmann over-ceiling case: biweekly, married_jointly, standard (Step-2 unchecked), gross ≈ $9,230.77 (=$240k/26), Step-3=$8,000 annual, 401k=8% (so federal_taxable ≈ $8,492.31). Record both calculator results. They become the secondary oracle for Thomas Bergmann. If they disagree with each other by more than $1, document the discrepancy and use the hand-computed trace (see Task 2 action) as the authoritative fixture."
      - task: "Record all confirmations (tool name, input used, result, date 2026-06-22) in the module docstring of tests/test_federal_withholding.py under a 'Layer-B Oracle Verification' section."

must_haves:
  truths:
    - "Federal withholding amounts are independently verified — a transcription error in tax_tables_2026.py causes the wage-bracket cross-check to fail, not silently pass"
    - "All 6 Worksheet 1A schedules are covered: single-standard, single-step2, married_jointly-standard, married_jointly-step2, married_separately-standard, married_separately-step2"
    - "The engine correctly floors withholding at $0.00 for any wage/credit combination — no golden case can produce a negative result"
    - "A filing_status='head_of_household' input to federal_withholding_2026 raises an error rather than silently mapping to the wrong schedule"
    - "The Additional Medicare 0.9% limitation is observable: a $200k+ YTD case carries a documented 'additional_medicare_not_modeled' flag rather than asserting flat 1.45% is correct"
    - "SS wage-base straddle case: Thomas Bergmann-like fixture asserts partial SS-taxable amount"
    - "401k-reduces-federal-not-FICA case: fixture asserts FICA uses gross base, federal uses gross-minus-401k base"
  artifacts:
    - path: "tests/test_federal_withholding.py"
      provides: "Golden suite for Pub 15-T 2026 Worksheet 1A engine"
      exports: ["test_wage_bracket_cross_check", "test_federal_withholding_golden", "test_structural_python_taxes_2024", "test_additional_medicare_limitation_is_flagged", "test_hoh_reject_guard"]
  key_links:
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/federal_withholding.py"
      via: "from app.pipeline.federal_withholding import federal_withholding_2026"
      pattern: "from app.pipeline.federal_withholding import"
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/calculate.py"
      via: "from app.pipeline.calculate import calculate (for the 401k-interaction cases and the $200k flag case)"
      pattern: "from app.pipeline.calculate import calculate"
---

<objective>
Write the golden-value test suite for the Pub 15-T 2026 Worksheet 1A engine, implementing the revised oracle hierarchy (User Decision 2): wage-bracket tables (Deliverable 5) as the PRIMARY independent oracle for under-ceiling fixtures; online calculators as SECONDARY corroboration for over-ceiling high earners; python-taxes as TERTIARY structural sanity only. Also implements User Decision 1 ($200k Additional Medicare limitation guard), the HoH reject-guard test (Fix 5), and the Step-4b FICA-base assertion (Fix 7b).

Purpose: This is the correctness oracle for the payroll calc — the most-tested unit in the repo (CLAUDE.md §6). After this plan, every CALC-04/CALC-05/CALC-06 claim is backed by tests with genuinely independently-derived expected values: the wage-bracket cross-check provides structural independence because a separate transcription typo cannot pass both checks simultaneously.

Output: tests/test_federal_withholding.py with:
- A wage-bracket cross-check sweeping many wage points per schedule (PRIMARY oracle — in-PDF independent)
- The manual layer-B calculator verification checkpoint (SECONDARY — blocks the over-ceiling high-earner fixture only)
- The full D-04 golden matrix (penny-exact traces for named edge cases)
- The python-taxes structural test (TERTIARY — 2024/2025 only, never 2026)
- User Decision 1: $200k Additional Medicare limitation flag test
- HoH reject-guard test
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/models/roster.py
@app/db/seed.py
@app/pipeline/federal_withholding.py
@tests/test_calculate.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The executor has constructed the wage-bracket cross-check fixture (PRIMARY oracle — in-PDF independent, no calculator needed) for all under-ceiling schedules. For the over-ceiling high earner (Thomas Bergmann, ~$9,231 biweekly, above the $3,875 biweekly ceiling), the executor has computed a hand-traced expected value. That hand-traced value CANNOT be entered into the test file until it is confirmed by two independent layer-B calculators. This checkpoint gates only the over-ceiling oracle trust. The wage-bracket primary oracle (in-PDF) does not require human verification.
  </what-built>
  <how-to-verify>
    Step 1 — Verify usapaycheck.org is in IRS Pub 15-T percentage-method mode (calibration check):
      Open https://usapaycheck.org/weekly-paycheck-calculator/ (or biweekly variant).
      Enter: Gross wage $800, Pay frequency Weekly, Filing status Single, Step 2 checkbox NOT checked, Step 3 dependents/credits $0, Step 4a other income $0, Step 4b deductions $0, Step 4c extra $0.
      Expected federal withholding: $54.08 (this is the calibration case — it must match to confirm the tool is in IRS percentage-method mode, not annual-liability mode).
      If the tool returns $54.08 → CONFIRMED in IRS percentage-method mode.
      If it returns a different value (e.g. ~$69 or similar) → the tool is using a different method; fall back to paycheckcity.com and document the discrepancy.

    Step 2 — Verify paycheckcity.com (calibration check):
      Open https://www.paycheckcity.com/calculator/salary (or hourly variant).
      Same calibration inputs: Weekly, Single, no Step 2, $800 gross, no adjustments.
      Expected: $54.08. Confirm agreement.

    Step 3 — Thomas Bergmann over-ceiling case (biweekly, MFJ standard):
      Thomas Bergmann: annual_salary=$240,000, pay_periods=26, retirement_contribution_pct=0.08, step_2_checkbox=False, step_3_dependents=$8,000, step_4a_other_income=$0, step_4b_deductions=$0.
      Per-period gross = $240,000 / 26 = $9,230.769... → $9,230.77 (rounded to cents).
      Federal taxable (after 401k) = $9,230.77 × (1 − 0.08) = $8,492.31 (rounded to cents).
      CONFIRM: both usapaycheck.org (biweekly) and paycheckcity.com return the same federal withholding for these inputs.
      Record both tool results.

    Step 4 — Agreement test (Fix 3 — escalation path):
      If both calculators AGREE with each other AND with the hand computation (within ±$1 tolerance):
        → Type "approved" with both tool results. The executor writes the fixture.
      If the calculators DISAGREE with each other OR with the hand computation by more than $1:
        → Type "method-delta: [describe what each tool returned and the hand-computed value]".
        → The executor MUST: (a) re-examine the Deliverable-1 percentage-table transcription for the MFJ bracket range used, (b) re-examine the Deliverable-5 wage-bracket cells for any adjacent biweekly MFJ entry (even though $9,231 is over-ceiling, nearby cells confirm the table), (c) confirm both calculators are in IRS percentage-method mode and using 2026 tables (not 2025/pre-OBBBA — a calculator silently running stale tables is the documented "calibrating to a stale oracle" hazard), then EITHER correct the transcription OR record that calculator as untrusted for this cell. The operator decides; the suite MUST NOT be silently calibrated to any calculator output that disagrees with the hand trace.

    Step 5 — Record in the test module docstring (the executor does this after "approved"):
      "Layer-B Oracle Verification (2026-06-22): usapaycheck.org returned $XX.XX for Single/Standard/Weekly/$800 [calibration]; paycheckcity.com returned $XX.XX [calibration]. Thomas Bergmann biweekly MFJ over-ceiling: usapaycheck.org returned $XX.XX; paycheckcity.com returned $XX.XX. Both agree (within ±$1) with hand-computed trace → oracle TRUSTED for Thomas Bergmann fixture."
  </how-to-verify>
  <resume-signal>Type "approved: [usapaycheck result], [paycheckcity result]" once both calculators are confirmed and the Thomas Bergmann verification record is ready. Or type "method-delta: [describe what each tool returned vs hand computation]" if any disagreement exceeds $1 — this triggers the escalation path described above before the fixture is written.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_federal_withholding.py — wage-bracket primary oracle + full D-04 golden suite + structural test (CALC-03/04/05/06)</name>
  <files>tests/test_federal_withholding.py</files>

  <read_first>
    RESEARCH.md §"MANDATORY DELIVERABLE 5: Wage Bracket Method Tables" (the full Deliverable 5 section) — the midpoint cross-check rule, the ceiling boundaries per frequency (weekly $1,925 / biweekly $3,875 / semimonthly $4,185 / monthly $8,395), the construction convention ([ASSUMED] midpoint rounded to whole dollar), the ±$1 tolerance for the cross-check assertion, and the verbatim sample block (pp.14–16 weekly Single/MFS Standard rows $625–$725).
    RESEARCH.md §"Finding 5" — why ±$1 tolerance is documented for the wage-bracket cross-check: the midpoint-round convention is [ASSUMED] (not stated in the PDF), so assert at whole-dollar granularity with ±$1 tolerance.
    RESEARCH.md §"Finding 4" — all four frequencies (52/26/24/12) are covered in the wage-bracket tables (pp.14–27), resolving the MFJ+Step-2-checkbox gap.
    RESEARCH.md §"Finding 3" — exact ceiling values per frequency; Thomas Bergmann ($9,231 biweekly) is ABOVE the $3,875 biweekly ceiling → cannot use wage-bracket oracle; falls back to hand computation + layer-B checkpoint.
    RESEARCH.md §"MANDATORY DELIVERABLE 1: Worksheet 1A Bracket Tables" — all bracket rows needed for hand-computing penny-exact expected values for the named edge cases.
    RESEARCH.md §"Oracle Tool 3 (Tertiary/Backup): python-taxes" — how to use python-taxes for the structural test (2024/2025 ONLY; never 2026).
    RESEARCH.md §"Code Examples: Golden Test Pattern" — the @pytest.mark.parametrize table shape, _make_employee() factory, Decimal assertion style.
    PATTERNS.md §"tests/test_federal_withholding.py" — imports pattern, file header docstring, _make_employee() helper, seeded-employee fixture pattern, assertion style (never float, always Decimal to Decimal).
    PATTERNS.md §"Synthetic Employee Fixtures" — the _make_employee() factory signature and full Employee construction (extra="forbid" — all 15 fields required; pay_type/hourly_rate/annual_salary invariant from @model_validator).
    app/db/seed.py — all 7 seeded employees with exact W-4 field values needed for hand-computation.
    tests/test_calculate.py — existing pattern to follow (import style, fixture convention, Decimal arithmetic).
  </read_first>

  <behavior>
    WAGE-BRACKET PRIMARY ORACLE (User Decision 2 — the new top tier):

    test_wage_bracket_cross_check: A property-style test that sweeps MANY wage points per schedule. For each covered schedule, walk across multiple wage-bracket intervals (from RESEARCH.md Deliverable 5 verbatim transcription — SEPARATELY from Deliverable 1 percentage tables, different PDF rows), evaluate the engine at the interval midpoint, round to the whole dollar, and assert it equals the published wage-bracket cell exactly (whole-dollar ==) with a ±$1 tolerance documented.
    - Sweep at least 6–8 wage intervals per schedule (not just 1–2 spot checks) so a single mis-keyed bracket row is caught.
    - Cover all 4 project frequencies: weekly (52), biweekly (26), semimonthly (24), monthly (12).
    - Cover all 6 column combinations: MFJ Standard, MFJ Step-2, Single/MFS Standard, Single/MFS Step-2 (HoH columns are not tested — no seeded HoH employees and the engine's reject-guard blocks HoH). HoH exclusion is documented explicitly in a comment.
    - The wage-bracket fixture is transcribed INDEPENDENTLY from the percentage tables in tax_tables_2026.py (second-pass transcription from different PDF rows / different PDF section). This independence is the structural enforcement of Decision 2 / Fix 6: a transcription typo in tax_tables_2026.py makes the engine disagree with the separately-transcribed wage-bracket cell, causing the test to go red. Document this in a comment.
    - Wage-bracket cells are transcribed from RESEARCH.md Deliverable 5 verbatim sample block (pp.14–27 of the 2026 Pub 15-T). The executor may also use additional cells from the same PDF section.
    - The midpoint → round-to-whole-dollar rule is documented in the test: "wage bracket cells are [ASSUMED] computed at the interval midpoint (not stated in PDF); assert round(engine_result) == cell with ±$1 tolerance."
    - CEILING BOUNDARY: clearly documented in the test file and in a constant. For the biweekly schedule, the ceiling is $3,875 (above this → Thomas Bergmann territory → not wage-bracket checkable). The test must NOT attempt to wage-bracket-check any wage above the respective frequency ceiling.

    FULL D-04 GOLDEN MATRIX (penny-exact for named edge cases):

    test_federal_withholding_golden parametrize table covers all cells in the D-04 matrix:
    - Single + Standard (step_2=False): at least 1 fixture per frequency (52 weekly from seed, 26 biweekly from seed)
    - Single + Step-2 checkbox: Priya Nair-like fixture (married_separately + step_2=True maps to Single/MFS Step-2 table)
    - MFJ + Standard (step_2=False): James Okafor (weekly) and Thomas Bergmann (biweekly, over-ceiling — secondary oracle)
    - MFJ + Step-2 checkbox: SYNTHETIC fixture under the weekly $1,925 ceiling (wage-bracket cross-checkable — no longer a gap per RESEARCH.md Finding 4)
    - married_separately + Standard: synthetic or use Priya Nair-equivalent without checkbox
    - married_separately + Step-2 checkbox: Priya Nair (seeded, MFS + Step-2)
    - At least one semi-monthly (24) or monthly (12) synthetic fixture (under ceiling — wage-bracket cross-checkable)

    D-04 edge cases (all required):
    - SS wage-base straddle: uses Thomas Bergmann (ytd_ss_wages=$183,900) via the full calculate() call to assert partial SS; verify fica_ss = $600 × 0.062 = $37.20 (CALC-04)
    - 401k-reduces-federal-not-FICA (CALC-03): fixture with retirement_contribution_pct > 0 where the same gross gives different federal_withholding (uses gross - pretax_401k) vs fica_ss (uses gross); verify fica_ss == _money(gross * SS_RATE) ignoring 401k
    - Step-4b deduction does NOT reduce the FICA base (Fix 7b): a fixture with step_4b_deductions > 0 asserts that fica_ss is computed on gross (not on gross minus the Step-4b deduction), while federal withholding IS lowered. Reuses the 401k-base-split test shape — a cheap structural assertion confirming the FICA/federal base distinction applies equally to Step-4b.
    - Multi-bracket high earner: Thomas Bergmann crossing into the 22% bracket (biweekly, over-ceiling, secondary oracle)
    - Below-threshold zero-withholding: wage so low that annualized wage < STEP1_STANDARD → line_1i = 0 → $0.00 withholding
    - Step-3 partial credit: step_3_dependents > 0 but not large enough to zero out withholding
    - Credit-exceeds-tentative → floor at $0.00
    - Step-4a addition: step_4a_other_income > 0 raises the annualized base
    - Step-4b deduction: step_4b_deductions > 0 lowers the annualized base
    - Hourly-with-explicit-OT gross: use calculate() result as the wages input to verify end-to-end
    NOTE: "salaried-with-leave gross" integration case belongs in tests/test_calculate.py (Plan 03) — NOT here. test_federal_withholding.py tests the federal_withholding engine; integrate-through-calculate cases belong in test_calculate.py.

    USER DECISION 1 — Additional Medicare limitation guard:
    test_additional_medicare_limitation_is_flagged:
    Create a synthetic employee with ytd_ss_wages just over $200,000 (e.g., $201,000). Call calculate() with some gross. Assert that the result carries an observable "additional_medicare_not_modeled" flag/marker (the exact mechanism is the executor's discretion — e.g., a field on PaystubLineItem, a structured warning returned alongside the result, or a logged annotation — but it MUST be observable and testable). Do NOT assert "fica_medicare == gross * 0.0145 is correct" — that assertion certifies a defect as green. Assert instead that the engine surface the limitation as a known unmodeled case.
    Add comment: "# User Decision 1: The employer Additional Medicare obligation (0.9% on wages over $200k YTD) is NOT modeled. The engine must FLAG this case, not silently under-withhold it as if flat 1.45% were correct. A test asserting flat 1.45% is the right amount at >$200k YTD would encode a defect as green — this test asserts the flag instead."
    Note to executor: Implementing the flag requires a small addition to calculate.py or federal_withholding.py (e.g., add an "additional_medicare_not_modeled" bool field to PaystubLineItem, or return a warning alongside the result). Coordinate with Plan 03 task 1 which touches calculate.py. The flag mechanism is the executor's discretion — pick the approach that is most observable and testable, and keep it out of the withheld amount.

    README disclaimer task:
    Update the Phase-2 README stub area (the D-A6-01 disclaimer section) to include: "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation." This makes the disclaimer test-backed (the test above asserts the flag). File path: find the README or the Phase-2 stub and update the relevant line. If the README does not yet have a disclaimer section, add one under a "Known Limitations" heading.

    HoH reject-guard test:
    test_hoh_reject_guard: Construct or monkey-patch an Employee with filing_status outside the supported set. Assert federal_withholding_2026 raises ValueError with "head_of_household" in the message. This confirms the Plan 01 Task 2 guard fires correctly.

    python-taxes structural test (TERTIARY — demoted per User Decision 2):
    test_structural_python_taxes_2024: A SEPARATE test function using python-taxes for STRUCTURE verification only. Import python_taxes. For a 2024 Single/Standard/Weekly/$800 fixture, compare the computation structure (annualize → bracket → de-annualize → step3 → step4) with python-taxes' own 2024 output. This is explicitly TERTIARY — it validates computation shape, NOT 2026 numbers. Add comment: "# TERTIARY structural sanity only — python-taxes ships 2023-2025; it does NOT produce 2026 ground truth (D-01). Independence for 2026 fixtures comes from the wage-bracket cross-check (PRIMARY oracle), not from this test."

    Self-derivation ban (Fix 2 — structural enforcement):
    No expected value in the parametrize table is produced by importing or calling tax_tables_2026 or federal_withholding_2026. For under-ceiling fixtures, the wage-bracket cross-check IS the structural enforcement: a transcription typo in tax_tables_2026.py makes the engine disagree with the separately-transcribed wage-bracket cell → the test goes red. Document this mechanism explicitly in the module docstring as the independence guarantee.

  </behavior>

  <action>
    Create tests/test_federal_withholding.py following PATTERNS.md §test_federal_withholding.py.

    First, run: uv add --dev python-taxes (for the structural test).

    MODULE DOCSTRING (required per Decision 1 and User Decision 2):
    State: (a) The PRIMARY oracle is the independently-transcribed wage-bracket fixture set (RESEARCH.md Deliverable 5, Pub 15-T pp.13–27). A transcription error in tax_tables_2026.py causes the wage-bracket cross-check to disagree — this is the structural enforcement of Decision 2's self-derivation ban for all under-ceiling schedules. (b) NO expected value in the parametrize table was derived by calling tax_tables_2026 or federal_withholding_2026. (c) For over-ceiling fixtures (Thomas Bergmann ~$9,231 biweekly), expected values are hand-computed and confirmed against usapaycheck.org and paycheckcity.com; the layer-B oracle record is filled in after the blocking checkpoint. (d) python-taxes is TERTIARY structural sanity only (2024/2025); it is NOT the 2026 independence guarantee. (e) The wage-bracket PRIMARY oracle covers all under-ceiling schedules with ±$1 tolerance (midpoint-round convention is [ASSUMED] — not stated in PDF). (f) The ceiling boundary is explicit: weekly $1,925 / biweekly $3,875 / semimonthly $4,185 / monthly $8,395 — fixtures above these ceilings use the secondary oracle only. (g) User Decision 1: Additional Medicare 0.9% surtax over $200k YTD is NOT modeled; the engine flags it as a known limitation; test_additional_medicare_limitation_is_flagged asserts the flag. (h) OBBBA qualified-tips and overtime deductions are disclaimed and NOT implemented.

    WAGE-BRACKET FIXTURE (independently transcribed — separate from Deliverable 1):
    Transcribe wage-bracket cells from RESEARCH.md Deliverable 5 verbatim sample block (pp.14 weekly Single/MFS Standard) and extend to at least 3–4 additional schedule columns from the same Deliverable 5 source. Structure the fixture as a list of (frequency, adjusted_per_period_wage_interval, status, step2, published_cell) tuples — one per schedule/interval combination. The interval lower/upper are taken directly from the Deliverable 5 table rows; the executor reads the midpoint. Do NOT re-derive these cells from the Deliverable 1 percentage method — they must be read from the Deliverable 5 wage-bracket table (separate PDF section). At minimum cover:
    - Weekly Single/MFS Standard: 6–8 rows from Deliverable 5 verbatim sample (pp.14)
    - Weekly MFJ Standard: 4–6 rows
    - Biweekly Single/MFS Standard: 4–6 rows (all under $3,875)
    - Biweekly MFJ Standard: 4–6 rows
    - Semimonthly (24) or Monthly (12) Single/MFS Standard: 4–6 rows (synthetic frequency coverage)
    - At least 2 Step-2-checkbox columns (any frequency under ceiling)
    A comment on each tuple: "# Source: Pub 15-T 2026 p.XX, [frequency] table, [status] [step2] column, row [$at_least–$but_less_than] → $cell"

    test_wage_bracket_cross_check parametrize:
    For each (frequency, interval_lower, interval_upper, status, step2, published_cell) tuple:
    1. Compute midpoint = (interval_lower + interval_upper) / 2
    2. Call federal_withholding_2026(midpoint_per_period, synthetic_employee) where the synthetic employee has: filing_status=status, step_2_checkbox=step2, step_3_dependents=Decimal("0"), step_4a_other_income=Decimal("0"), step_4b_deductions=Decimal("0"), pay_periods_per_year=frequency, and compensation consistent with the fixture (hourly or salary, doesn't matter for the withholding calculation since the input is the per-period adjusted wage)
    3. Assert abs(round(engine_result) - published_cell) <= 1 — the ±$1 documented tolerance
    4. Add a comment: "# Independence: this wage-bracket cell is separately transcribed from Deliverable 5; a typo in tax_tables_2026.py Deliverable 1 rows causes this to fail."

    CEILING BOUNDARY CONSTANT: define WAGE_BRACKET_CEILINGS = {52: Decimal("1925"), 26: Decimal("3875"), 24: Decimal("4185"), 12: Decimal("8395")}. Use in a parameterize ID or comment to make the boundary explicit.

    _make_employee() helper (PATTERNS.md §"Synthetic Employee Fixtures"):
    Keyword-only factory; defaults: pay_type="hourly", hourly_rate=Decimal("100.00"), annual_salary=None, ytd_ss_wages=Decimal("0"), retirement_contribution_pct=Decimal("0"). All 15 Employee fields passed explicitly; uuid.uuid4() for id and business_id. The @model_validator in Employee enforces pay_type ↔ compensation invariant at construction.

    Seeded-employee fixture functions (PATTERNS.md §"pytest fixture pattern"):
    @pytest.fixture() functions that call seed(dry_run=True) and filter by field predicate (never by index). Create separate fixtures for: maria_chen, james_okafor, thomas_bergmann, sandra_kim, priya_nair.

    Hand-compute all PENNY-EXACT expected values for the D-04 golden matrix using RESEARCH.md Deliverable 1 bracket tables. For each named edge case add a comment block:
    # Hand-computation trace:
    # 1a=$X, 1c=$X*P, 1d=$X, 1e=$X, 1f=$X, 1g=$X, 1h=$X, 1i=$X
    # bracket: [$lower, $upper), base=$C, rate=D%
    # 2e=$X, 2f=$X, 2g=$X, 2h=$X
    # 3b=$X, 3c=$X
    # 4b=$X (= expected)
    # For over-ceiling: Layer-B: usapaycheck.org=$X.XX, paycheckcity.com=$X.XX (within ±$1 of hand trace)
    # For under-ceiling: Primary oracle: wage-bracket cross-check (test_wage_bracket_cross_check covers this cell)

    NOTE on 401k-reduces-federal-not-FICA and Step-4b FICA-base case (CALC-03, Fix 7b): these tests must call calculate() (not just federal_withholding_2026) to assert the FULL interaction. For the Step-4b case: verify item.fica_ss == _money(min(gross, remaining_cap) * SS_RATE) where gross is the pre-deduction gross, AND that item.federal_withholding < what it would be without step_4b (confirming Step-4b lowers the federal base) — same structural assertion shape as the 401k case but for Step-4b deductions. Import calculate from app.pipeline.calculate for these assertions only.

    test_additional_medicare_limitation_is_flagged:
    Create a synthetic employee with ytd_ss_wages=Decimal("201000"). Call calculate() with gross above $0. Assert the result carries the "additional_medicare_not_modeled" flag/marker (whatever mechanism Plan 03 implements). Do NOT assert "fica_medicare == gross * 0.0145 is the correct withholding amount" — that assertion encodes the defect as green. Add the User Decision 1 comment block as specified in the behavior section.

    README disclaimer (additional subtask within Task 2):
    After writing the test file, update the README (or Phase-2 stub) to add: "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation." Verify the line is present after writing.

    test_hoh_reject_guard:
    Monkey-patch or construct an Employee with filing_status outside the three supported values. Assert ValueError is raised. Assert the error message contains "head_of_household".

    python-taxes structural test (separate function, TERTIARY):
    uv add --dev python-taxes has already been run. Import python_taxes. For a 2024 Single/Standard/Weekly/$800 fixture, call both the engine (with manually-transcribed 2024 bracket entry OR python-taxes' own 2024 constants as a reference, not derived from tax_tables_2026) and python-taxes' employer_withholding() with 2024 inputs. Assert both return the same value (within $0.01). Add explicit comment: "# TERTIARY structural sanity only. Independence for 2026 fixtures comes from the wage-bracket PRIMARY oracle, not this test."
  </action>

  <acceptance_criteria>
    - The wage-bracket cross-check sweeps at least 6–8 intervals per schedule for the weekly Single/MFS column and covers at least 4 schedule columns total (review Fix 2, User Decision 2). This is the structural independence enforcement for under-ceiling fixtures.
    - The wage-bracket fixture is transcribed independently from tax_tables_2026.py (different PDF section — Deliverable 5 not Deliverable 1). A comment on each fixture row cites the PDF page and row (review Fix 6).
    - The ceiling boundary is explicit: WAGE_BRACKET_CEILINGS constant defines the per-frequency max adjusted wage; Thomas Bergmann is explicitly identified as over-ceiling in a comment and uses only the secondary oracle.
    - No expected value in the parametrize table is produced by calling tax_tables_2026 or federal_withholding_2026 — the module docstring asserts this ban. The wage-bracket cross-check provides structural enforcement (review Fix 2).
    - test_additional_medicare_limitation_is_flagged asserts the engine surface a "additional_medicare_not_modeled" flag for >$200k YTD — it does NOT assert flat 1.45% is the correct withholding amount (User Decision 1 — no test-trap).
    - The README/Phase-2 stub carries "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation."
    - test_hoh_reject_guard passes — confirms Plan 01 Task 2 guard fires correctly.
    - Step-4b deduction case asserts FICA base is gross (not gross minus Step-4b deduction), same shape as 401k-base-split (review Fix 7b).
    - python-taxes structural test explicitly labeled TERTIARY with a comment stating it validates shape not 2026 numbers (User Decision 2 — demoted).
    - The salaried-with-leave integration case does NOT appear in this file — it belongs in test_calculate.py (review Fix 9).
    - At least two distinct pay frequencies (52 + 26 from seed; plus at least one synthetic 24 or 12 in the wage-bracket sweep).
  </acceptance_criteria>

  <verify>
    <automated>
      cd /Users/pnhek/usf\ msds/github/payroll_agent && uv run pytest tests/test_federal_withholding.py -q
    </automated>
  </verify>

  <done>
    tests/test_federal_withholding.py exits with 0 failures. Wage-bracket cross-check sweeps many wage points per schedule using an independently-transcribed fixture. All D-04 matrix cells covered. Additional Medicare limitation is flagged, not asserted as correct. HoH reject-guard test passes. python-taxes structural test passes (labeled TERTIARY). The full suite (uv run pytest -q) stays green. No expected value derived from the engine under test. Salaried-with-leave integration case is NOT in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External calculator output | usapaycheck.org and paycheckcity.com are external, unverifiable at CI time; their role is human-verified oracle confirmation for over-ceiling fixtures only, not CI-time computation |
| Wage-bracket fixture independence | The wage-bracket fixture is transcribed from a different PDF section (Deliverable 5 pp.13–27) than the percentage tables (Deliverable 1 p.12); a single transcription session cannot corrupt both simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Golden expected values (over-ceiling) | mitigate | Blocking human checkpoint requires manual calculator confirmation for Thomas Bergmann before the fixture is written; Decision 2 acceptance criterion (no self-derived value) asserted in module docstring |
| T-03-02-02 | Tampering | Wage-bracket fixture (under-ceiling) | mitigate | Fixtures transcribed independently from Deliverable 5 (separate PDF section); a transcription typo in Deliverable 1 (tax_tables_2026.py) causes the cross-check to fail — this is the structural independence control |
| T-03-02-03 | Tampering | Stale calculator oracle | mitigate | Checkpoint Step 1 calibration case ($800/week/Single → $54.08) must confirm the calculator is in IRS percentage-method mode AND using 2026 tables; step 4 escalation path handles disagreement without silent calibration |
| T-03-02-04 | Information Disclosure | N/A | accept | Test-only file; no PII; no network calls at test runtime |
| T-03-02-05 | Denial of Service | Additional Medicare test-trap | mitigate | User Decision 1 explicitly prohibits asserting flat 1.45% as correct at >$200k YTD; test asserts the limitation flag instead |
| T-03-02-SC | Tampering | uv add --dev python-taxes install | mitigate | python-taxes 0.7.0 is audited (RESEARCH.md Package Legitimacy Audit — PyPI, MIT, existing reference in CLAUDE.md); dev-only dep, not in runtime image |

Note: This plan contains a blocking human checkpoint (autonomous: false). The checkpoint gate confirms the over-ceiling layer-B oracle before Thomas Bergmann's fixture value is written. Under-ceiling fixtures use the in-PDF wage-bracket PRIMARY oracle and do not require human verification.
</threat_model>

<verification>
uv run pytest tests/test_federal_withholding.py -q  # golden suite passes
uv run pytest -q                                     # full suite stays green (195+ existing)
grep -c "wage_bracket" tests/test_federal_withholding.py  # at least 1 (PRIMARY oracle function)
grep -c "TERTIARY" tests/test_federal_withholding.py  # at least 1 (python-taxes demoted label)
grep -c "additional_medicare_not_modeled" tests/test_federal_withholding.py  # at least 1
grep -c "head_of_household" tests/test_federal_withholding.py  # at least 1 (HoH reject-guard test)
python -c "import python_taxes; print('python-taxes installed')"  # dev dep present
</verification>

<success_criteria>
1. tests/test_federal_withholding.py exits with 0 failures on uv run pytest tests/test_federal_withholding.py -q.
2. Wage-bracket cross-check sweeps at least 6–8 weekly Single/MFS intervals and covers at least 4 schedule columns total — independently transcribed from Deliverable 5 (not derived from Deliverable 1).
3. Ceiling boundary explicit: WAGE_BRACKET_CEILINGS constant; Thomas Bergmann identified as over-ceiling with secondary oracle only.
4. All 6 Worksheet 1A schedules (3 filing statuses × 2 Step-2 branches) represented in the golden parametrize table.
5. test_additional_medicare_limitation_is_flagged asserts the limitation flag — NOT flat 1.45% as correct.
6. README/stub carries the Additional Medicare disclaimer with "flags it as a known limitation."
7. test_hoh_reject_guard passes; ValueError message contains "head_of_household".
8. Step-4b deduction: assertion confirms FICA base is gross (not gross minus Step-4b).
9. python-taxes structural test explicitly labeled TERTIARY in a comment; passes.
10. No salaried-with-leave integration case in this file.
11. At least two distinct pay frequencies (52 + 26 from seed; synthetic 24 or 12 in wage-bracket sweep).
12. Module docstring identifies wage-bracket cross-check as the structural independence mechanism.
13. Full suite uv run pytest -q is green.
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-02-SUMMARY.md when done.
</output>
