---
phase: 03-harden-the-calc
plan: 02
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-03
files_modified:
  - tests/test_federal_withholding.py
  - pyproject.toml
  - uv.lock
  - README.md
autonomous: false
requirements:
  - CALC-03
  - CALC-04
  - CALC-05
  - CALC-06

# FIX D: wave 3, depends_on 03-03.
# Reason: this plan's Task 2 calls calculate() for the 401k-base-split and
# Step-4b interaction assertions, references the additional_medicare_not_modeled
# flag on PaystubLineItem, and tests integration behavior that 03-03 implements.
# Making 03-02 depend on 03-03 eliminates the residual forward dependency that
# Codex identified: the golden suite is now free of any reference to calculate()
# or PaystubLineItem that isn't yet shipped.
# Alternative considered (splitting Task 2 into pure engine vs calculate()-integration):
# rejected as higher churn — the dependency is already clean given the wave ordering.

user_setup:
  - service: layer-b-oracle-verification
    why: "For over-ceiling high earners (Thomas Bergmann, ~$9,231 biweekly, above the $3,875 biweekly wage-bracket ceiling), hand-computed expected values must be confirmed against two independent online percentage-method calculators (D-01 Decision 1, Decision 2). The wage-bracket primary oracle cannot reach these cases. This is a blocking gate — no over-ceiling golden value goes into the test module before BOTH calculators agree within ±$1. If the two calculators disagree with each other by more than $1, the Thomas Bergmann fixture is SKIPPED (pytest.mark.skip) — it must NOT be written using a lone hand-trace as authoritative."
    dashboard_config:
      - task: "Open usapaycheck.org (biweekly variant) and confirm it is in IRS Pub 15-T percentage-method mode by entering: Gross $800, pay frequency Weekly, Filing Status Single, Step 2 Checkbox NOT checked, Step 3 = $0, Step 4a/4b/4c = $0. The calculator should return federal withholding = $54.08 (hand-computed calibration case). If it returns a different value, investigate the method delta before using it as a layer-B oracle."
      - task: "Open paycheckcity.com/calculator/salary (or /hourly) with the same inputs (Weekly, Single, no Step 2, $800 gross, no Step 3/4 adjustments). Confirm it returns $54.08. If either calculator disagrees, note which and do NOT mark the disagreeing fixture as TRUSTED."
      - task: "Using both calculators in IRS-percentage-method mode, compute the Thomas Bergmann over-ceiling case: biweekly, married_jointly, standard (Step-2 unchecked), gross ≈ $9,230.77 (=$240k/26), Step-3=$8,000 annual, 401k=8% (so federal_taxable ≈ $8,492.31). Record both calculator results."
      - task: "AGREEMENT RULE (FIX — replaces prior 'hand trace authoritative' fallback): if both calculators AGREE within ±$1 of each other AND with the hand-computed trace → type 'approved' with both results; executor writes the fixture. If the calculators DISAGREE with each other by more than $1 (regardless of hand trace) → the Thomas Bergmann over-ceiling fixture is MARKED @pytest.mark.skip(reason='Layer-B calculators disagree >$1 on over-ceiling fixture; requires manual resolution') and NOT written with a golden value. A lone hand trace is NOT authoritative when two independent calculators disagree — it cannot break the tie. Investigate: confirm both tools are in 2026 IRS percentage-method mode (re-run the $54.08 calibration); check for calculator stale-table risk (pre-OBBBA tables)."
      - task: "Record all confirmations (tool name, input used, result, date 2026-06-22) in the module docstring of tests/test_federal_withholding.py under a 'Layer-B Oracle Verification' section."

must_haves:
  truths:
    - "Federal withholding amounts are independently verified — a transcription error in tax_tables_2026.py causes the wage-bracket cross-check to fail, not silently pass"
    - "All 6 Worksheet 1A schedules are covered: single-standard, single-step2, married_jointly-standard, married_jointly-step2, married_separately-standard, married_separately-step2"
    - "The engine correctly floors withholding at $0.00 for any wage/credit combination — no golden case can produce a negative result"
    - "A filing_status='head_of_household' input to federal_withholding_2026 raises an error rather than silently mapping to the wrong schedule"
    - "The Additional Medicare 0.9% limitation is observable: a (ytd_ss_wages+gross) > $200k proxy case carries a documented 'additional_medicare_not_modeled' flag rather than asserting flat 1.45% is correct"
    - "SS wage-base straddle case: Thomas Bergmann-like fixture asserts partial SS-taxable amount"
    - "401k-reduces-federal-not-FICA case: fixture asserts FICA uses gross base, federal uses gross-minus-401k base"
    - "Bracket boundary tests: _find_bracket() is verified at exact lower bound, upper-bound minus $0.01, and exact upper bound (next bracket) for at least one schedule"
    - "Whole-dollar comparison in wage-bracket cross-check uses quantize(Decimal('1'), ROUND_HALF_UP) not Python round()"
  artifacts:
    - path: "tests/test_federal_withholding.py"
      provides: "Golden suite for Pub 15-T 2026 Worksheet 1A engine"
      exports: ["test_wage_bracket_cross_check", "test_federal_withholding_golden", "test_bracket_boundary", "test_additional_medicare_limitation_is_flagged", "test_hoh_reject_guard"]
  key_links:
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/federal_withholding.py"
      via: "from app.pipeline.federal_withholding import federal_withholding_2026"
      pattern: "from app.pipeline.federal_withholding import"
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/calculate.py"
      via: "from app.pipeline.calculate import calculate (for the 401k-interaction and Additional Medicare flag cases only — 03-03 must be complete first)"
      pattern: "from app.pipeline.calculate import calculate"
---

<objective>
Write the golden-value test suite for the Pub 15-T 2026 Worksheet 1A engine, implementing the revised oracle hierarchy (User Decision 2): wage-bracket tables (Deliverable 5) as the PRIMARY independent oracle for under-ceiling fixtures; online calculators as SECONDARY corroboration for over-ceiling high earners (with a hard two-calculator agreement requirement — FIX: no lone-hand-trace fallback). Also implements the Additional Medicare limitation flag test (User Decision 1, FIX B — proxy-based trigger from 03-03), the HoH reject-guard test (Fix 5), bracket boundary tests (_find_bracket() bounds — FIX G), and the Step-4b FICA-base assertion (Fix 7b). python-taxes structural test is DROPPED (FIX F — not runnable against 2026-keyed engine; independence for 2026 fixtures already provided by the wage-bracket PRIMARY oracle).

Purpose: This is the correctness oracle for the payroll calc — the most-tested unit in the repo (CLAUDE.md §6). After this plan, every CALC-04/CALC-05/CALC-06 claim is backed by tests with genuinely independently-derived expected values. The wage-bracket cross-check provides structural independence: a transcription typo in tax_tables_2026.py cannot simultaneously corrupt both the percentage-table rows (Deliverable 1) and the separately-transcribed wage-bracket cells (Deliverable 5).

Output: tests/test_federal_withholding.py with:
- A wage-bracket cross-check sweeping many wage points per schedule (PRIMARY oracle — in-PDF independent), with whole-dollar comparison via quantize(Decimal("1"), ROUND_HALF_UP)
- Explicit bracket boundary tests at lower bound, upper-minus-$0.01, and exact upper bound (FIX G)
- The manual layer-B calculator verification checkpoint (SECONDARY — blocks the over-ceiling high-earner fixture only; requires two-calculator agreement or the fixture is marked skip)
- The full D-04 golden matrix (penny-exact traces for named edge cases)
- User Decision 1: Additional Medicare limitation flag test (proxy-based from 03-03)
- HoH reject-guard test
- uv add --dev python-taxes (mutates pyproject.toml + uv.lock) is run but the python-taxes structural test is SKIPPED/DROPPED (FIX F)
- README "Known Limitations" disclaimer update
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/models/roster.py
@app/models/contracts.py
@app/db/seed.py
@app/pipeline/federal_withholding.py
@app/pipeline/tax_tables_2026.py
@tests/test_calculate.py
@.planning/phases/03-harden-the-calc/03-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The executor has constructed the wage-bracket cross-check fixture (PRIMARY oracle — in-PDF independent, no calculator needed) for all under-ceiling schedules. For the over-ceiling high earner (Thomas Bergmann, ~$9,231 biweekly, above the $3,875 biweekly ceiling), the executor has computed a hand-traced expected value. That hand-traced value CANNOT be entered into the test file until it is confirmed by TWO independent layer-B calculators that AGREE within ±$1 of each other. If they disagree, the fixture is SKIPPED — a lone hand trace is not authoritative when calculators disagree (FIX: removes the prior "hand trace authoritative if calculators disagree" language). The wage-bracket primary oracle (in-PDF) does not require human verification.
  </what-built>
  <how-to-verify>
    Step 1 — Verify usapaycheck.org is in IRS Pub 15-T percentage-method mode (calibration check):
      Open https://usapaycheck.org/weekly-paycheck-calculator/ (or biweekly variant).
      Enter: Gross wage $800, Pay frequency Weekly, Filing status Single, Step 2 checkbox NOT checked, Step 3 dependents/credits $0, Step 4a other income $0, Step 4b deductions $0, Step 4c extra $0.
      Expected federal withholding: $54.08 (calibration case — must match to confirm the tool is in IRS percentage-method mode).
      If the tool returns $54.08 → CONFIRMED in IRS percentage-method mode.
      If it returns a different value → fall back to paycheckcity.com and document the discrepancy.

    Step 2 — Verify paycheckcity.com (calibration check):
      Open https://www.paycheckcity.com/calculator/salary (or hourly variant).
      Same calibration inputs: Weekly, Single, no Step 2, $800 gross, no adjustments.
      Expected: $54.08. Confirm agreement.

    Step 3 — Thomas Bergmann over-ceiling case (biweekly, MFJ standard):
      Thomas Bergmann: annual_salary=$240,000, pay_periods=26, retirement_contribution_pct=0.08, step_2_checkbox=False, step_3_dependents=$8,000, step_4a_other_income=$0, step_4b_deductions=$0.
      Per-period gross = $240,000 / 26 = $9,230.769... → $9,230.77 (rounded to cents).
      Federal taxable (after 401k) = $9,230.77 × (1 − 0.08) = $8,492.31 (rounded to cents).
      CONFIRM: both usapaycheck.org (biweekly) and paycheckcity.com return the same federal withholding for these inputs.
      Record both tool results.

    Step 4 — AGREEMENT RULE (FIX — replaces prior "hand trace authoritative" fallback):
      If both calculators AGREE with each other within ±$1:
        → Type "approved: [usapaycheck result], [paycheckcity result]". The executor writes the Thomas Bergmann fixture with the agreed value.
      If the calculators DISAGREE with each other by more than $1 (regardless of hand trace):
        → Type "method-delta: [describe what each tool returned]".
        → The executor marks the Thomas Bergmann fixture with @pytest.mark.skip(reason="Layer-B calculators disagree >$1 on over-ceiling fixture; requires manual resolution before enabling"). The fixture is NOT written with a golden value.
        → A lone hand trace cannot break a two-calculator disagreement — investigate: confirm both tools are in 2026 IRS percentage-method mode (re-run calibration), check for stale-table risk (pre-OBBBA calculators running 2025 tables will differ from 2026).
        → Note: the calibration case ($800/week/Single → $54.08) is under-ceiling and is NOT affected by calculator disagreement on the over-ceiling case.

    Step 5 — Record in the test module docstring (the executor does this after "approved" or "method-delta"):
      "Layer-B Oracle Verification (2026-06-22): usapaycheck.org returned $XX.XX for Single/Standard/Weekly/$800 [calibration]; paycheckcity.com returned $XX.XX [calibration]. Thomas Bergmann biweekly MFJ over-ceiling: usapaycheck.org returned $XX.XX; paycheckcity.com returned $XX.XX. [Both agree (within ±$1) with hand-computed trace → oracle TRUSTED for Thomas Bergmann fixture.] OR [Calculators disagree by $X.XX → Thomas Bergmann fixture marked @pytest.mark.skip.]"
  </how-to-verify>
  <resume-signal>Type "approved: [usapaycheck result], [paycheckcity result]" once both calculators agree within ±$1 and the Thomas Bergmann verification record is ready. Or type "method-delta: [describe what each tool returned]" if the two calculators disagree by more than $1 — this triggers the skip path (fixture is NOT written with a golden value, only a skip annotation).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_federal_withholding.py — wage-bracket PRIMARY oracle + bracket boundary tests + full D-04 golden suite (CALC-03/04/05/06, FIX D/E/F/G/H)</name>
  <files>tests/test_federal_withholding.py, pyproject.toml, uv.lock, README.md</files>

  <read_first>
    RESEARCH.md §"MANDATORY DELIVERABLE 5: Wage Bracket Method Tables" — the full Deliverable 5 section: midpoint cross-check rule, ceiling boundaries per frequency (weekly $1,925 / biweekly $3,875 / semimonthly $4,185 / monthly $8,395), construction convention ([ASSUMED] midpoint rounded to whole dollar), the verbatim sample block (pp.14–16 weekly Single/MFS Standard rows).
    RESEARCH.md §"Finding 5" — why ±$1 tolerance is documented for the wage-bracket cross-check.
    RESEARCH.md §"Finding 4" — all four frequencies (52/26/24/12) are covered in the wage-bracket tables.
    RESEARCH.md §"Finding 3" — exact ceiling values per frequency; Thomas Bergmann is ABOVE the $3,875 biweekly ceiling.
    RESEARCH.md §"MANDATORY DELIVERABLE 1: Worksheet 1A Bracket Tables" — bracket rows for hand-computing penny-exact expected values.
    RESEARCH.md §"Code Examples: Golden Test Pattern" — the parametrize table shape, _make_employee() factory, Decimal assertion style.
    PATTERNS.md §"tests/test_federal_withholding.py" — imports pattern, file header docstring, _make_employee() helper, seeded-employee fixture pattern, assertion style.
    PATTERNS.md §"Synthetic Employee Fixtures" — the _make_employee() factory signature (all 15 fields required; pay_type/hourly_rate/annual_salary invariant from @model_validator).
    app/db/seed.py — all 7 seeded employees with exact W-4 field values.
    app/pipeline/federal_withholding.py — confirm _find_bracket() implementation and module structure (needed for FIX G boundary tests).
    app/pipeline/tax_tables_2026.py — read bracket tables to construct hand-computed boundary test values.
    app/models/contracts.py — confirm additional_medicare_not_modeled: bool field is present (added by 03-03 Task 1).
    tests/test_calculate.py — existing pattern to follow (import style, fixture convention, Decimal arithmetic).
    .planning/phases/03-harden-the-calc/03-03-SUMMARY.md — confirm calculate() signature and PayrollCalculationError are available (03-03 must be complete before this plan runs).
  </read_first>

  <behavior>
    WAGE-BRACKET PRIMARY ORACLE (FIX E — quantize rounding):

    test_wage_bracket_cross_check: A property-style parametrized test sweeping MANY wage points per schedule. For each covered schedule, walk across multiple wage-bracket intervals, evaluate the engine at the interval midpoint, convert to whole dollar using quantize(Decimal("1"), rounding=ROUND_HALF_UP) (NOT Python builtin round()), and assert == the published wage-bracket cell with ±$1 tolerance documented.

    FIX E details for the whole-dollar comparison:
    - Use: from decimal import ROUND_HALF_UP; cell_whole_dollar = engine_result.quantize(Decimal("1"), rounding=ROUND_HALF_UP)
    - NOT: round(engine_result) which uses Python half-even (banker's rounding) and diverges at .5
    - Add acceptance criterion note in the test: "# Whole-dollar rounding uses ROUND_HALF_UP (not Python round() which is half-even).
      # Per-step cent quantization in federal_withholding.py is a CHOSEN ENGINE CONVENTION
      # (IRS whole-dollar rounding is optional per Pub 15-T; ROUND_HALF_UP is the project pin
      # per D-03/WR-06). This is NOT presented as IRS-mandated per-step behavior."
    - The module header of federal_withholding.py should also carry a one-line note to this effect (add if missing; see action).

    BRACKET BOUNDARY TESTS (FIX G):

    test_bracket_boundary: _find_bracket() is lower-bound-driven; midpoint wage-bracket sweeps do NOT catch boundary regressions (a >= vs > error in the lookup logic passes all midpoint checks). Add explicit assertions for at least one schedule (e.g. Single/Standard) at:
    - Exact lower bound of a non-first bracket (e.g. if bracket 2 starts at L, test federal_withholding_2026 at a wage that annualizes to exactly L) → assert it lands in bracket 2, not bracket 1
    - Upper bound minus $0.01 (the last cent before the bracket boundary) → assert it still lands in the lower bracket
    - Exact upper bound / lower bound of next bracket (same wage as the upper limit) → assert it has moved to the next bracket
    Hand-compute expected values for each boundary case. Add a comment for each: "# FIX G boundary test: _find_bracket() is lower-bound-driven; >= vs > regression at this boundary would pass all midpoint checks but fail here."

    - Cover at least 6–8 wage intervals per schedule for the weekly Single/MFS cross-check.
    - Cover all 4 project frequencies: weekly (52), biweekly (26), semimonthly (24), monthly (12).
    - Cover all 6 column combinations: MFJ Standard, MFJ Step-2, Single/MFS Standard, Single/MFS Step-2 (HoH excluded — reject guard; document in comment).

    FULL D-04 GOLDEN MATRIX (penny-exact):

    test_federal_withholding_golden parametrize table covers all cells in the D-04 matrix:
    - Single + Standard (step_2=False): at least 1 fixture per frequency from seed (Maria Chen weekly, Priya Nair biweekly)
    - Single + Step-2 checkbox: Priya Nair-like fixture (married_separately + step_2=True maps to Single/MFS Step-2 table)
    - MFJ + Standard (step_2=False): James Okafor (weekly) and Thomas Bergmann (biweekly, over-ceiling — secondary oracle, or skipped if calculators disagree per checkpoint)
    - MFJ + Step-2 checkbox: SYNTHETIC fixture under the weekly $1,925 ceiling (wage-bracket cross-checkable)
    - married_separately + Standard: synthetic or Priya Nair-equivalent without checkbox
    - married_separately + Step-2 checkbox: Priya Nair (seeded, MFS + Step-2)
    - At least one semi-monthly (24) or monthly (12) synthetic fixture (under ceiling — wage-bracket cross-checkable)

    D-04 edge cases (all required):
    - SS wage-base straddle: uses Thomas Bergmann (ytd_ss_wages=$183,900) via the full calculate() call to assert partial SS; verify fica_ss = $600 × 0.062 = $37.20 (CALC-04)
    - 401k-reduces-federal-not-FICA (CALC-03): fixture with retirement_contribution_pct > 0 where federal_withholding uses gross-minus-401k while fica_ss uses gross; verify fica_ss == _money(gross * SS_RATE) ignoring 401k
    - Step-4b deduction does NOT reduce the FICA base (Fix 7b): fixture with step_4b_deductions > 0 asserts fica_ss is computed on gross, while federal withholding IS lowered
    - Multi-bracket high earner: Thomas Bergmann crossing into the 22% bracket (over-ceiling, secondary oracle — skipped if checkpoint not approved)
    - Below-threshold zero-withholding: wage so low that annualized wage < STEP1_STANDARD → line_1i = 0 → $0.00 withholding
    - Step-3 partial credit: step_3_dependents > 0 but not large enough to zero out withholding
    - Credit-exceeds-tentative → floor at $0.00
    - Step-4a addition: step_4a_other_income > 0 raises the annualized base
    - Step-4b deduction: step_4b_deductions > 0 lowers the annualized base
    - Hourly-with-explicit-OT gross: use calculate() result as the wages input to verify end-to-end
    NOTE: "salaried-with-leave gross" integration case belongs in tests/test_calculate.py (Plan 03) — NOT here.

    USER DECISION 1 — Additional Medicare limitation guard (FIX B proxy trigger):
    test_additional_medicare_limitation_is_flagged:
    Create a synthetic employee where (ytd_ss_wages + current_gross) > Decimal("200000") (e.g., ytd_ss_wages=Decimal("197000"), hourly_rate=Decimal("100.00"), 40 hours → gross=$4,000 → proxy=$201,000 > $200k). Call calculate() with this employee. Assert that result.additional_medicare_not_modeled is True.
    Also create a normal employee (ytd_ss_wages=Decimal("0")) and assert additional_medicare_not_modeled is False.
    Do NOT assert "fica_medicare == gross * 0.0145 is correct" — that assertion certifies a defect as green.
    Add comment: "# User Decision 1 (FIX B): The proxy trigger fires on (ytd_ss_wages + gross) > $200,000.
    # ytd_ss_wages is the lower-bound proxy for YTD Medicare wages (Medicare has no cap,
    # so Medicare YTD >= SS YTD always). The prior plan used ytd_ss_wages alone, which
    # can NEVER exceed $184,500 (the SS wage base cap) — that trigger was dead code.
    # The proxy-based trigger CAN cross $200k and correctly identifies the flagged case."

    HoH reject-guard test:
    test_hoh_reject_guard: Monkey-patch an Employee with filing_status outside the supported set. Assert ValueError raised. Assert message contains "head_of_household".

    python-taxes DROPPED (FIX F):
    Do NOT add a python-taxes structural test. Reason:
    (a) The engine is keyed to 2026 tables; a 2024/2025 comparison requires injectable year tables or a tax_year parameter — adding that to avoid a skip adds scope not present in any locked decision.
    (b) The wage-bracket PRIMARY oracle already provides the structural independence that python-taxes was added to contribute; demoting python-taxes to TERTIARY and then skipping it is equivalent to dropping it given that the PRIMARY oracle already covers its role.
    (c) Dropping is lower-risk than adding a skip that may confuse future readers about what is intentional vs broken.
    However: still run "uv add --dev python-taxes" because the RESEARCH.md Package Legitimacy Audit has it as an audited dev dep. The package is installed but no test references it. This mutates pyproject.toml and uv.lock (FIX H — these are now in files_modified).
    Add a comment at the top of the test file explaining the absence: "# python-taxes (dev dep, installed) is NOT used in this test suite. Reason (FIX F):
    # the engine is keyed to 2026 tables; python-taxes ships 2023–2025 only. A structural
    # comparison requires injectable year tables not currently in scope. The wage-bracket
    # PRIMARY oracle (Deliverable 5, below) already provides the structural independence
    # that python-taxes was added to contribute. If year-injection is added in a future
    # phase, a python-taxes structural test can be re-enabled."

    README disclaimer (FIX H):
    Update README.md to add or update a "Known Limitations" section with:
    "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation (additional_medicare_not_modeled=True on PaystubLineItem when the Medicare-wage proxy exceeds $200,000)."
    Verify the line is present after writing.
  </behavior>

  <action>
    FIRST: run uv add --dev python-taxes (mutates pyproject.toml + uv.lock — FIX H: both now listed in files_modified). Even though no test uses it, the RESEARCH.md Package Legitimacy Audit includes it and the install is required per planning artifacts.

    MODULE DOCSTRING (required — Decision 1 and User Decision 2):
    State: (a) PRIMARY oracle is the independently-transcribed wage-bracket fixture set (RESEARCH.md Deliverable 5, Pub 15-T pp.13–27). A transcription error in tax_tables_2026.py causes the wage-bracket cross-check to disagree — structural enforcement of Decision 2's self-derivation ban for all under-ceiling schedules. (b) NO expected value in the parametrize table was derived by calling tax_tables_2026 or federal_withholding_2026. (c) For over-ceiling fixtures (Thomas Bergmann ~$9,231 biweekly), expected values are hand-computed and confirmed against usapaycheck.org and paycheckcity.com; BOTH must agree within ±$1 or the fixture is skipped. A lone hand trace is not authoritative. (d) python-taxes is NOT used in this test suite (see FIX F comment). (e) Whole-dollar comparison uses quantize(Decimal("1"), rounding=ROUND_HALF_UP), not Python round() (FIX E). (f) The per-step cent quantization in federal_withholding.py is a CHOSEN ENGINE CONVENTION (IRS whole-dollar rounding is optional; ROUND_HALF_UP is the project pin per D-03/WR-06). (g) Ceiling boundary is explicit: weekly $1,925 / biweekly $3,875 / semimonthly $4,185 / monthly $8,395. (h) User Decision 1: Additional Medicare 0.9% surtax over $200k YTD is NOT modeled; test_additional_medicare_limitation_is_flagged asserts the flag. (i) OBBBA qualified-tips and overtime deductions are disclaimed and NOT implemented.

    FIX E — ROUNDING MODULE HEADER:
    Check app/pipeline/federal_withholding.py for a note about per-step cent quantization being a chosen convention. If missing, add a one-line docstring update in that file: "Per-step rounding: _money() carries cents at each step (ROUND_HALF_UP). This is a CHOSEN engine convention — Pub 15-T permits optional whole-dollar rounding; the per-step approach eliminates step-boundary ambiguity (see RESEARCH.md Deliverable 2)." Add federal_withholding.py to the list of files touched if edited.

    WAGE-BRACKET FIXTURE (independently transcribed — separate from Deliverable 1):
    Transcribe wage-bracket cells from RESEARCH.md Deliverable 5 verbatim sample block (pp.14 weekly Single/MFS Standard) and extend to at least 3–4 additional schedule columns. Structure as a list of (frequency, interval_lower, interval_upper, filing_status, step2, published_cell) tuples. At minimum cover:
    - Weekly Single/MFS Standard: 6–8 rows from Deliverable 5 verbatim sample (pp.14)
    - Weekly MFJ Standard: 4–6 rows
    - Biweekly Single/MFS Standard: 4–6 rows (all under $3,875)
    - Biweekly MFJ Standard: 4–6 rows
    - Semimonthly (24) or Monthly (12) Single/MFS Standard: 4–6 rows
    - At least 2 Step-2-checkbox columns (any frequency under ceiling)
    A comment on each tuple: "# Source: Pub 15-T 2026 p.XX, [frequency] table, [status] [step2] column, row [$at_least–$but_less_than] → $cell"

    test_wage_bracket_cross_check parametrize: for each tuple:
    1. Compute midpoint = (interval_lower + interval_upper) / 2
    2. Call federal_withholding_2026(midpoint_per_period, synthetic_employee) where the synthetic employee has: filing_status=status, step_2_checkbox=step2, step_3_dependents=Decimal("0"), step_4a_other_income=Decimal("0"), step_4b_deductions=Decimal("0"), pay_periods_per_year=frequency
    3. cell_whole_dollar = engine_result.quantize(Decimal("1"), rounding=ROUND_HALF_UP)  # FIX E
    4. Assert abs(cell_whole_dollar - published_cell) <= 1

    CEILING BOUNDARY CONSTANT: WAGE_BRACKET_CEILINGS = {52: Decimal("1925"), 26: Decimal("3875"), 24: Decimal("4185"), 12: Decimal("8395")}

    BRACKET BOUNDARY TESTS (FIX G):
    test_bracket_boundary parametrize: For at least one schedule (Single/Standard weekly) pick one internal bracket boundary B (a value where bracket k ends and bracket k+1 begins at lower=B). Construct:
    (a) A test at adjusted_annual_wage = B exactly → verify engine assigns bracket k+1 (lower-bound-inclusive)
    (b) A test at adjusted_annual_wage = B - Decimal("0.01") → verify engine assigns bracket k (still below the boundary)
    (c) A test at adjusted_annual_wage = B + Decimal("0.01") → verify engine assigns bracket k+1
    To derive the per-period wage that produces an adjusted annual wage of B, work backwards through the Worksheet 1A Step 1 formula given a specific employee setup (e.g., step_2=False, status="single", step_3=0, step_4a=0, step_4b=0, p=52): line_1i = annualized_gross - STEP1_STANDARD["single"]. Set line_1i = B, so annualized_gross = B + STEP1_STANDARD["single"], so per_period_gross = annualized_gross / 52. Hand-compute the expected withholding for each case against the correct bracket (lower, rate, base from RESEARCH.md Deliverable 1). Use penny-exact Decimal comparison (no ±$1 tolerance — these are hand-computed, not wage-bracket table lookups). Add the FIX G comment block per behavior section.

    _make_employee() helper (PATTERNS.md §"Synthetic Employee Fixtures"):
    Keyword-only factory; defaults: pay_type="hourly", hourly_rate=Decimal("100.00"), annual_salary=None, ytd_ss_wages=Decimal("0"), retirement_contribution_pct=Decimal("0"). All 15 Employee fields passed explicitly.

    Seeded-employee fixture functions: @pytest.fixture() for maria_chen, james_okafor, thomas_bergmann, sandra_kim, priya_nair (filter by field predicate from seed(dry_run=True), never by index).

    Hand-compute all PENNY-EXACT expected values for the D-04 golden matrix using RESEARCH.md Deliverable 1. For each named edge case add a comment block:
    # Hand-computation trace:
    # 1a=$X, 1c=$X*P, 1d=$X, 1e=$X, 1f=$X, 1g=$X, 1h=$X, 1i=$X
    # bracket: [$lower, $upper), base=$C, rate=D%
    # 2e=$X, 2f=$X, 2g=$X, 2h=$X
    # 3b=$X, 3c=$X
    # 4b=$X (= expected)
    # [For over-ceiling] Layer-B: usapaycheck.org=$X.XX, paycheckcity.com=$X.XX (within ±$1 of hand trace). BOTH agree.
    # [For under-ceiling] Primary oracle: wage-bracket cross-check covers this cell

    NOTE on 401k-reduces-federal-not-FICA and Step-4b FICA-base case (CALC-03, Fix 7b):
    These tests call calculate() (not just federal_withholding_2026) to assert the FULL interaction. calculate() is now available because this plan is wave 3 (depends on 03-03). For the Step-4b case: verify item.fica_ss == _money(min(gross, remaining_cap) * SS_RATE) where gross is the pre-deduction gross, AND that item.federal_withholding < what it would be without step_4b.

    test_additional_medicare_limitation_is_flagged:
    Create synthetic employee with ytd_ss_wages=Decimal("197000"), hourly_rate=Decimal("100.00"). Call calculate() with 40 hours → gross=$4,000 → proxy=$201,000 > $200k. Assert result.additional_medicare_not_modeled is True. Also create normal employee (ytd_ss_wages=Decimal("0")) and assert False. Include the FIX B comment block per behavior section.

    README disclaimer:
    Read README.md. Add or update "Known Limitations" section with the Additional Medicare disclaimer. Verify the line is present.

    test_hoh_reject_guard:
    Monkey-patch or construct an Employee with filing_status outside the three supported values. Assert ValueError is raised. Assert message contains "head_of_household".
  </action>

  <acceptance_criteria>
    - The wage-bracket cross-check sweeps at least 6–8 intervals per schedule for the weekly Single/MFS column and covers at least 4 schedule columns total (User Decision 2 independence).
    - Whole-dollar comparison in the wage-bracket cross-check uses quantize(Decimal("1"), rounding=ROUND_HALF_UP), NOT Python round() (FIX E). A comment documents the distinction.
    - A note is present (in the test module docstring or the wage-bracket fixture comment) stating that per-step cent quantization is a CHOSEN engine convention, not IRS-mandated (FIX E).
    - test_bracket_boundary covers exact lower bound, upper-minus-$0.01, and exact upper bound for at least one schedule, with hand-computed expected values and the FIX G comment (FIX G).
    - The over-ceiling Thomas Bergmann fixture requires BOTH calculators to agree within ±$1 or is marked @pytest.mark.skip — no lone-hand-trace fallback (FIX: over-ceiling criterion update).
    - No expected value in the parametrize table is produced by calling tax_tables_2026 or federal_withholding_2026 — the module docstring asserts this ban.
    - test_additional_medicare_limitation_is_flagged: asserts proxy-based flag (ytd_ss_wages + gross > $200k) fires correctly; asserts it does NOT fire for normal employee; does NOT assert flat 1.45% is correct (User Decision 1, FIX B).
    - The README carries "Additional Medicare 0.9% over $200k YTD is not modeled; the engine flags it as a known limitation" (FIX H).
    - test_hoh_reject_guard passes — ValueError message contains "head_of_household".
    - Step-4b deduction case asserts FICA base is gross (not gross minus Step-4b), same shape as 401k-base-split (Fix 7b).
    - No python-taxes structural test is present (FIX F); a comment explains why.
    - The salaried-with-leave integration case does NOT appear in this file — it belongs in test_calculate.py (Plan 03, Fix 9).
    - pyproject.toml and uv.lock are updated by uv add --dev python-taxes (FIX H — now in files_modified).
    - At least two distinct pay frequencies (52 + 26 from seed; plus at least one synthetic 24 or 12 in the wage-bracket sweep).
    - The module docstring explicitly names the wage-bracket cross-check as the structural independence mechanism, and explicitly states python-taxes is not used (FIX F).
  </acceptance_criteria>

  <verify>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_federal_withholding.py -q
    </automated>
  </verify>

  <done>
    tests/test_federal_withholding.py exits with 0 failures. Wage-bracket cross-check sweeps many wage points per schedule using an independently-transcribed fixture; whole-dollar comparison uses quantize(Decimal("1"), ROUND_HALF_UP) (FIX E). test_bracket_boundary covers exact lower bound, upper-minus-$0.01, and exact upper bound for at least one schedule (FIX G). All D-04 matrix cells covered. Additional Medicare limitation is flagged by the proxy-based trigger (FIX B). HoH reject-guard test passes. No python-taxes structural test (FIX F — comment explains). Over-ceiling Thomas Bergmann fixture requires two-calculator agreement or is skipped (FIX). pyproject.toml and uv.lock updated. README disclaimer present (FIX H). The full suite (uv run pytest -q) stays green. No expected value derived from the engine under test. Salaried-with-leave integration case is NOT in this file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External calculator output | usapaycheck.org and paycheckcity.com are external, unverifiable at CI time; their role is human-verified oracle confirmation for over-ceiling fixtures only, not CI-time computation |
| Wage-bracket fixture independence | The wage-bracket fixture is transcribed from a different PDF section (Deliverable 5 pp.13–27) than the percentage tables (Deliverable 1 p.12); a single transcription session cannot corrupt both simultaneously |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Golden expected values (over-ceiling) | mitigate | Blocking human checkpoint requires BOTH calculator results to agree within ±$1 before fixture is written; if they disagree, fixture is @pytest.mark.skip — no lone-hand-trace-authoritative path (FIX) |
| T-03-02-02 | Tampering | Wage-bracket fixture (under-ceiling) | mitigate | Fixtures transcribed independently from Deliverable 5 (separate PDF section); a transcription typo in Deliverable 1 (tax_tables_2026.py) causes the cross-check to fail — structural independence control |
| T-03-02-03 | Tampering | Stale calculator oracle | mitigate | Checkpoint Step 1 calibration case ($800/week/Single → $54.08) must confirm the calculator is in IRS percentage-method mode AND using 2026 tables; disagreement between calculators triggers skip, not hand-trace fallback |
| T-03-02-04 | Information Disclosure | N/A | accept | Test-only file; no PII; no network calls at test runtime |
| T-03-02-05 | Denial of Service | Additional Medicare test-trap | mitigate | FIX B: test asserts flag fires on proxy trigger (ytd_ss_wages+gross > $200k), NOT on ytd_ss_wages alone (dead code); does NOT assert flat 1.45% is correct at >$200k |
| T-03-02-06 | Tampering | Rounding divergence at .5 (FIX E) | mitigate | Whole-dollar comparison uses quantize(Decimal("1"), ROUND_HALF_UP); builtin round() (half-even) is explicitly forbidden; comment documents distinction |
| T-03-02-SC | Tampering | uv add --dev python-taxes install | mitigate | python-taxes 0.7.0 is audited (RESEARCH.md Package Legitimacy Audit — PyPI, MIT, existing reference in CLAUDE.md); dev-only dep, not in runtime image |

Note: This plan contains a blocking human checkpoint (autonomous: false). The checkpoint gate confirms the over-ceiling layer-B oracle before Thomas Bergmann's fixture value is written. Under-ceiling fixtures use the in-PDF wage-bracket PRIMARY oracle and do not require human verification. FIX D: this plan is now wave 3 (depends on 03-01 and 03-03) because Task 2 calls calculate() for the 401k-base and Additional Medicare tests — calculate() is not complete until 03-03 ships.
</threat_model>

<verification>
cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_federal_withholding.py -q
cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest -q
grep -c "wage_bracket" tests/test_federal_withholding.py
grep -c "ROUND_HALF_UP" tests/test_federal_withholding.py
grep -c "quantize" tests/test_federal_withholding.py
grep -c "bracket_boundary" tests/test_federal_withholding.py
grep -c "additional_medicare_not_modeled" tests/test_federal_withholding.py
grep -c "head_of_household" tests/test_federal_withholding.py
grep -c "python.taxes" tests/test_federal_withholding.py
grep -c "additional_medicare" README.md
</verification>

<success_criteria>
1. tests/test_federal_withholding.py exits with 0 failures on uv run pytest tests/test_federal_withholding.py -q.
2. Wage-bracket cross-check sweeps at least 6–8 weekly Single/MFS intervals and covers at least 4 schedule columns — independently transcribed from Deliverable 5 (not derived from Deliverable 1).
3. Whole-dollar comparison uses quantize(Decimal("1"), rounding=ROUND_HALF_UP) — NOT Python round() (FIX E).
4. Per-step cent convention documented as CHOSEN in test module docstring and/or federal_withholding.py header (FIX E).
5. test_bracket_boundary covers exact lower bound, upper-minus-$0.01, and exact upper bound for at least one schedule with hand-computed penny-exact expected values (FIX G).
6. Ceiling boundary explicit: WAGE_BRACKET_CEILINGS constant; Thomas Bergmann identified as over-ceiling.
7. Over-ceiling fixture: either both calculators agree and fixture is written, or fixture is @pytest.mark.skip — no lone-hand-trace fallback (FIX).
8. All 6 Worksheet 1A schedules (3 filing statuses × 2 Step-2 branches) represented in the golden parametrize table.
9. test_additional_medicare_limitation_is_flagged: asserts proxy-based flag fires when (ytd_ss_wages + gross) > $200k; does NOT assert flat 1.45% is correct (User Decision 1, FIX B).
10. README carries the Additional Medicare disclaimer with "flags it as a known limitation" (FIX H).
11. test_hoh_reject_guard passes; ValueError message contains "head_of_household".
12. Step-4b: FICA base is gross (not gross minus Step-4b).
13. No python-taxes structural test; FIX F comment explains the absence.
14. pyproject.toml + uv.lock updated by uv add --dev python-taxes (FIX H).
15. Salaried-with-leave integration case is NOT in this file (belongs in test_calculate.py per Fix 9).
16. At least two distinct pay frequencies (52 + 26 from seed; synthetic 24 or 12 in wage-bracket sweep).
17. Module docstring identifies wage-bracket cross-check as structural independence mechanism and notes python-taxes absence.
18. Full suite uv run pytest -q is green.
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-02-SUMMARY.md when done.
</output>
