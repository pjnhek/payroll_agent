---
phase: 03-harden-the-calc
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - tests/test_federal_withholding.py
autonomous: false
requirements:
  - CALC-03
  - CALC-05
  - CALC-06

user_setup:
  - service: layer-b-oracle-verification
    why: "Hand-computed golden values must be confirmed against two independent online percentage-method calculators before being trusted as golden fixtures (D-01 Decision 1, Decision 2). This is a blocking gate — no golden value goes into the test module before this confirmation."
    dashboard_config:
      - task: "Open usapaycheck.org (try weekly variant at usapaycheck.org/weekly-paycheck-calculator/ and biweekly at usapaycheck.org/biweekly-paycheck-calculator/) and confirm it is in IRS Pub 15-T percentage-method mode by entering: Gross $800, pay frequency Weekly, Filing Status Single, Step 2 Checkbox NOT checked, Step 3 = $0, Step 4a/4b/4c = $0. The calculator should return federal withholding = $54.08 (hand-computed). If it returns a different value, investigate the method delta before using it as a layer-B oracle."
      - task: "Open paycheckcity.com/calculator/salary (or /hourly) with the same inputs (Weekly, Single, no Step 2, $800 gross, no Step 3/4 adjustments). Confirm it returns the same $54.08. If either calculator disagrees, note which and do NOT mark the disagreeing fixture as TRUSTED."
      - task: "Record the confirmation (tool name, input used, result, date 2026-06-22) in the module docstring of tests/test_federal_withholding.py under a 'Layer-B Oracle Verification' section."

must_haves:
  truths:
    - "The golden suite is the most-tested unit in the repo (CLAUDE.md §6)"
    - "All 6 Worksheet 1A schedules are covered to the penny: single-standard, single-step2, married_jointly-standard, married_jointly-step2, married_separately-standard, married_separately-step2"
    - "Every expected value has a hand-computation trace in a comment — not derived from the engine (D-01 Decision 2)"
    - "Layer-B oracle confirmation is recorded in the test module docstring with date"
    - "SS wage-base straddle case: Thomas Bergmann-like fixture asserts partial SS-taxable amount"
    - "401k-reduces-federal-not-FICA case: fixture asserts FICA uses gross base, federal uses gross-minus-401k base"
    - "Step-3 credit floor case: credit exceeding tentative withholding returns Decimal('0.00')"
    - "python-taxes structural test runs against 2024/2025 tables only (NEVER 2026)"
    - "Negative withholding can never appear anywhere in the parametrize table"
    - "MFJ + Step-2 checkbox is covered by a synthetic fixture (no seeded employee covers this schedule)"
    - "At least two distinct pay frequencies appear in the matrix (52 weekly + 26 biweekly from seed; at least one of 24/12 from synthetic)"
    - "Additional Medicare 0.9% surtax is verified-absent: a $200k+ YTD case asserts Medicare == gross * Decimal('0.0145') with no surtax"
    - "OBBBA provisions are verified-absent by a disclaimer assertion in the test module"
  artifacts:
    - path: "tests/test_federal_withholding.py"
      provides: "Golden suite for Pub 15-T 2026 Worksheet 1A engine"
      exports: ["test_federal_withholding_golden", "test_structural_python_taxes_2024", "test_no_additional_medicare_surtax"]
  key_links:
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/federal_withholding.py"
      via: "from app.pipeline.federal_withholding import federal_withholding_2026"
      pattern: "from app.pipeline.federal_withholding import"
    - from: "tests/test_federal_withholding.py"
      to: "app/pipeline/calculate.py"
      via: "from app.pipeline.calculate import calculate (for the 401k-interaction cases that need full paystub)"
      pattern: "from app.pipeline.calculate import calculate"
---

<objective>
Write the golden-value test suite for the Pub 15-T 2026 Worksheet 1A engine, implementing the hybrid oracle strategy from Decision 1 (hand-computed + layer-B cross-check for all values; python-taxes structure test for 2024/2025 independence; manual calculator-verification checkpoint gate).

Purpose: This is the correctness oracle for the payroll calc — the most-tested unit in the repo (CLAUDE.md §6). After this plan, every CALC-05/CALC-06 claim is backed by a penny-exact test with an independently-derived expected value.

Output: tests/test_federal_withholding.py with the full D-04 matrix plus edge cases, the manual oracle verification checkpoint (blocking), and the python-taxes structural test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/models/roster.py
@app/db/seed.py
@app/pipeline/federal_withholding.py
@tests/test_calculate.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The executor has computed hand-traced expected values for the golden suite using the bracket tables in RESEARCH.md Deliverable 1. These CANNOT be entered into the test file until they are confirmed by two independent layer-B calculators. This checkpoint gates the oracle trust.
  </what-built>
  <how-to-verify>
    Step 1 — Verify usapaycheck.org is in IRS Pub 15-T percentage-method mode:
      Open https://usapaycheck.org/weekly-paycheck-calculator/ (or biweekly variant).
      Enter: Gross wage $800, Pay frequency Weekly, Filing status Single, Step 2 checkbox NOT checked, Step 3 dependents/credits $0, Step 4a other income $0, Step 4b deductions $0, Step 4c extra $0.
      Expected federal withholding: $54.08.
      If the tool returns $54.08 → CONFIRMED (it is in IRS percentage-method mode).
      If it returns a different value (e.g. ~$69 or similar) → the tool is using annual-liability or another method; fall back to paycheckcity.com and document the discrepancy.

    Step 2 — Verify paycheckcity.com:
      Open https://www.paycheckcity.com/calculator/hourly (or salary variant).
      Same inputs: Weekly, Single, no Step 2, $800 gross, no adjustments.
      Expected: $54.08.
      Confirm agreement.

    Step 3 — Record in the test module docstring (the executor does this after you type "approved"):
      "Layer-B Oracle Verification (2026-06-22): usapaycheck.org returned $XX.XX for Single/Standard/Weekly/$800; paycheckcity.com returned $XX.XX. Both agree with hand-computed $54.08 → oracle TRUSTED."

    Step 4 — If either tool returns a DIFFERENT value:
      Do not mark the $54.08 fixture as TRUSTED.
      Investigate whether the disagreement is a method delta (tool uses annual liability instead of employer percentage method) or a genuine discrepancy.
      Document the finding and proceed only with fixtures that two tools agree on.
  </how-to-verify>
  <resume-signal>Type "approved" once both calculators are confirmed and the verification record is ready to be pasted into the docstring. Or type "disagreement: [describe what each tool returned]" so the executor can investigate before writing fixtures.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/test_federal_withholding.py — full D-04 golden suite + structural test (CALC-03/05/06)</name>
  <files>tests/test_federal_withholding.py</files>

  <read_first>
    RESEARCH.md §"Hand-Computation Worked Example (Single/Standard, Weekly $800)" — the $54.08 template showing the full 1a→4b trace; all other expected values must follow the same trace-in-comment structure.
    RESEARCH.md §"Seed Employee Coverage for Layer-B Fixtures" — which seeded employees cover which D-04 matrix cells; specifically: MFJ + Step-2 checkbox has NO seeded employee → requires synthetic fixture.
    RESEARCH.md §"MANDATORY DELIVERABLE 1: Worksheet 1A Bracket Tables" — all bracket rows needed for hand-computing expected values.
    RESEARCH.md §"Oracle Tool 3 (Tertiary/Backup): python-taxes" — how to use python-taxes for the structural test (2024/2025 ONLY; never 2026).
    RESEARCH.md §"Code Examples: Golden Test Pattern" — the @pytest.mark.parametrize table shape, _make_employee() factory, Decimal assertion style.
    RESEARCH.md §"Common Pitfalls" — all 7 pitfalls inform the edge cases; especially Pitfall #1 (401k/FICA base), Pitfall #3 (floor at $0), Pitfall #6 (self-derived oracle forbidden).
    PATTERNS.md §"tests/test_federal_withholding.py" — imports pattern, file header docstring, _make_employee() helper, seeded-employee fixture pattern, assertion style (never float, always Decimal to Decimal).
    PATTERNS.md §"Synthetic Employee Fixtures" — the _make_employee() factory signature and full Employee construction (extra="forbid" — all 15 fields required; pay_type/hourly_rate/annual_salary invariant from @model_validator).
    app/db/seed.py — all 7 seeded employees with their filing_status, step_2_checkbox, step_3_dependents, step_4a_other_income, step_4b_deductions, pay_periods_per_year values (confirmed exact values needed for hand-computation).
    tests/test_calculate.py — existing pattern to follow (import style, fixture convention, Decimal arithmetic).
  </read_first>

  <behavior>
    Golden parametrize table covers all cells in the D-04 matrix:
    - Single + Standard (step_2=False): at least 1 fixture per pay frequency (52 weekly from seed, 26 biweekly from seed)
    - Single + Step-2 checkbox: Priya Nair-like fixture (married_separately + step_2=True maps to Single/MFS Step-2 table)
    - MFJ + Standard (step_2=False): James Okafor (weekly) and Thomas Bergmann (biweekly)
    - MFJ + Step-2 checkbox: SYNTHETIC fixture (no seeded employee covers this)
    - married_separately + Standard: synthetic or use Priya Nair-equivalent without checkbox
    - married_separately + Step-2 checkbox: Priya Nair (seeded, MFS + Step-2)
    - At least one semi-monthly (24) or monthly (12) synthetic fixture

    D-04 edge cases (all required):
    - SS wage-base straddle: uses Thomas Bergmann (ytd_ss_wages=$183,900) via the full calculate() call to assert partial SS; verify FICA SS = $600 * 0.062 = $37.20
    - 401k-reduces-federal-not-FICA: fixture with retirement_contribution_pct > 0 where the same gross gives different federal_withholding (uses gross - pretax_401k) vs fica_ss (uses gross); verify fica_ss == _money(gross * SS_RATE) ignoring 401k
    - Multi-bracket high earner: a high annualized wage crossing into the 22% or 24% bracket
    - Below-threshold zero-withholding: wage so low that annualized wage < STEP1_STANDARD → line_1i = 0 → $0.00 withholding
    - Step-3 partial credit: step_3_dependents > 0 but not large enough to zero out withholding → withholding equals tentative minus per-period credit
    - Credit-exceeds-tentative → floor at $0.00 (the Thomas Bergmann case with $8,000 step_3 may trigger; ensure at least one explicit case)
    - Step-4a addition: step_4a_other_income > 0 raises the annualized base
    - Step-4b deduction: step_4b_deductions > 0 lowers the annualized base (but not below $0 for line_1i)
    - Hourly-with-explicit-OT gross: use calculate() result as the wages input to verify end-to-end
    - Salaried-with-leave gross: after Plan 03 ships, verify salaried employee with leave hours produces higher gross → higher withholding

    Negative/disclaimer assertions:
    - Additional Medicare surtax NOT modeled: a synthetic employee with ytd_ss_wages > $200,000 via calculate() asserts fica_medicare == _money(gross * Decimal("0.0145")) exactly; no 0.9% surtax is present
    - OBBBA provisions NOT applied: assert in module docstring + one nominal test that the standard method (no qualified-tips or overtime deductions) is the only method implemented

    python-taxes structural test (Decision 1 Layer C):
    - uv add --dev python-taxes first
    - In a SEPARATE test function (test_structural_python_taxes_2024), import python_taxes and compute withholding for a 2024 case using both the engine (with 2024 bracket tables from python-taxes or manually transcribed) AND python_taxes itself, comparing the STRUCTURE (annualize → bracket → de-annualize → step3 → step4), NOT the 2026 numbers
    - This test asserts the computation structure is correct against an independent 2024/2025 implementation
    - NEVER call this test with 2026 expected values; it is a structure test only
    - If the year-keyed engine only supports 2026 at this stage, the structural test can compare against python-taxes' own output for a 2024 fixture to confirm the formula is the same

    All expected values in the parametrize table must:
    - Have a hand-computation trace comment showing all 1a→4b steps
    - Have a "Layer-B: [tool] returned $X.XX" comment confirming oracle agreement
    - Never be derived by calling federal_withholding_2026 or tax_tables_2026 (Decision 2)
  </behavior>

  <action>
    Create tests/test_federal_withholding.py following PATTERNS.md §test_federal_withholding.py.

    First, run: uv add --dev python-taxes (for the structural test).

    MODULE DOCSTRING (required per Decision 1):
    State: (a) ALL expected values are hand-computed from RESEARCH.md Deliverable 1 bracket tables, (b) no expected value was derived from tax_tables_2026.py or federal_withholding_2026(), (c) each expected value was cross-checked against usapaycheck.org and paycheckcity.com, (d) the layer-B oracle verification record (tool name, input, result, date — filled after the checkpoint), (e) the 2026 PDF contains NO Worksheet 1A worked examples — the hybrid hand-computed + calculator oracle is the chosen substitute (Decision 1), (f) Additional Medicare 0.9% surtax is NOT modeled (demo wages never reach the $200k threshold; test_no_additional_medicare_surtax asserts absence), (g) OBBBA qualified-tips and overtime deductions are disclaimed and NOT implemented.

    _make_employee() helper (PATTERNS.md §"Synthetic Employee Fixtures"):
    Keyword-only factory; defaults: pay_type="hourly", hourly_rate=Decimal("100.00"), annual_salary=None, ytd_ss_wages=Decimal("0"), retirement_contribution_pct=Decimal("0"). All 15 Employee fields passed explicitly; uuid.uuid4() for id and business_id. The @model_validator in Employee enforces pay_type ↔ compensation invariant at construction.

    Seeded-employee fixture (PATTERNS.md §"pytest fixture pattern"):
    @pytest.fixture() functions that call seed(dry_run=True) and filter by field predicate (never by index). Create separate fixtures for: maria_chen (single, std, weekly, no 401k, no credits), james_okafor (mfj, std, weekly, 401k=4%, step3=$4000), thomas_bergmann (mfj, std, biweekly, high earner, step3=$8000, ytd_ss=$183,900), sandra_kim (single, std, biweekly, 401k=5%), priya_nair (married_separately, step_2=True, weekly, 401k=6%, step4a=$2000).

    Hand-compute all expected values for the parametrize table using RESEARCH.md Deliverable 1 bracket tables with the same 1a→4b trace shown in RESEARCH.md §Hand-Computation. For each case add a comment block:
    # Hand-computation trace:
    # 1a=$X, 1c=$X*P, 1d=$X, 1e=$X, 1f=$X, 1g=$X, 1h=$X, 1i=$X
    # bracket: [$lower, $upper), base=$C, rate=D%
    # 2e=$X, 2f=$X, 2g=$X, 2h=$X
    # 3b=$X, 3c=$X
    # 4b=$X (= expected)
    # Layer-B: usapaycheck.org=$X.XX, paycheckcity.com=$X.XX

    The main parametrize table (test_federal_withholding_golden) uses the signature:
    @pytest.mark.parametrize("desc,wages_this_period,emp_fixture_or_kwargs,expected_wh", [...])
    For seeded employees, pass the fixture value; for synthetic cases, pass kwargs to _make_employee() inside the parametrize list.

    NOTE on 401k-reduces-federal-not-FICA case (CALC-03): this test must call calculate() (not just federal_withholding_2026) to assert the FULL interaction — verify item.fica_ss == _money(min(gross, remaining_cap) * SS_RATE) using the raw gross, AND item.federal_withholding == federal_withholding_2026(gross - item.pretax_401k, emp). Import calculate from app.pipeline.calculate for this assertion only.

    python-taxes structural test (separate function):
    uv add --dev python-taxes has already been run. Import python_taxes. For a 2024 Single/Standard/Weekly/$800 fixture, call both your engine (with 2024 bracket tables from python-taxes' constants, or hard-code one 2024 bracket manually) and python_taxes' employer_withholding() with the same 2024 inputs. Assert both return the same value (or within $0.01). Assert that the 2026 golden values are NOT tested here — add an explicit comment "# This test uses 2024 tables only; never 2026 (python-taxes ships 2023-2025 only)."
    Minimum: test that the COMPUTATION PATH (annualize → bracket → de-annualize → step3 → step4) produces the same result as python-taxes for at least one 2024 case.

    test_no_additional_medicare_surtax (separate function):
    Create a synthetic employee with ytd_ss_wages just over $200,000 (e.g., $201,000). Call calculate() with some gross. Assert item.fica_medicare == _money(item.gross_pay * Decimal("0.0145")), confirming no 0.9% surtax was added. Add comment: "# Additional Medicare 0.9% surtax over $200k YTD is NOT modeled. This test verifies the absence. See README disclaimer."
  </action>

  <verify>
    <automated>
      cd /Users/pnhek/usf\ msds/github/payroll_agent && uv run pytest tests/test_federal_withholding.py -q
    </automated>
  </verify>

  <done>
    tests/test_federal_withholding.py exits with 0 failures. All D-04 matrix cells are covered (6 Worksheet 1A schedules, edge cases, disclaimer negatives). Every expected value has a hand-computation trace + layer-B oracle confirmation in a comment. The python-taxes structural test passes. The full suite (uv run pytest -q) stays green. No expected value was derived from the engine under test.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External calculator output | usapaycheck.org and paycheckcity.com are external, unverifiable at CI time; their role is human-verified oracle confirmation, not CI-time computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | Golden expected values | mitigate | Blocking human checkpoint requires manual calculator confirmation before any fixture value is written; Decision 2 acceptance criterion (no self-derived value) asserted in module docstring |
| T-03-02-02 | Information Disclosure | N/A | accept | Test-only file; no PII; no network calls at test runtime |
| T-03-02-SC | Tampering | uv add --dev python-taxes install | mitigate | python-taxes 0.7.0 is audited (RESEARCH.md Package Legitimacy Audit — PyPI, MIT, existing reference in CLAUDE.md); dev-only dep, not in runtime image |

Note: This plan contains a blocking human checkpoint (autonomous: false). The checkpoint gate confirms layer-B oracle method before golden values are trusted. No golden expected value may be written before this gate is cleared.
</threat_model>

<verification>
uv run pytest tests/test_federal_withholding.py -q  # golden suite passes
uv run pytest -q                                     # full suite stays green (195+ existing)
grep -c "Layer-B" tests/test_federal_withholding.py  # at least 6 layer-B confirmation comments
grep -c "Hand-computation" tests/test_federal_withholding.py  # at least 10 trace comments
python -c "import python_taxes; print('python-taxes installed')"  # dev dep present
</verification>

<success_criteria>
1. tests/test_federal_withholding.py exits with 0 failures on uv run pytest tests/test_federal_withholding.py -q.
2. All 6 Worksheet 1A schedules (3 filing statuses × 2 Step-2 branches) are represented in the parametrize table.
3. Every parametrize case has a hand-computation trace in a comment AND a layer-B oracle confirmation note.
4. The python-taxes structural test (2024/2025 only, never 2026) passes.
5. test_no_additional_medicare_surtax passes, confirming the 0.9% surtax is absent.
6. The MFJ + Step-2 checkbox case uses a synthetic fixture (no seeded employee).
7. At least two distinct pay frequencies (52 + 26 from seed; plus at least one synthetic 24 or 12) appear in the matrix.
8. Module docstring contains the layer-B oracle verification record with date.
9. Full suite uv run pytest -q is green.
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-02-SUMMARY.md when done.
</output>
