---
phase: 03-harden-the-calc
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/pipeline/calculate.py
  - app/models/contracts.py
  - tests/test_calculate.py
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-07
  - CALC-08

must_haves:
  truths:
    - "calculate() returns real federal withholding (never Decimal('0')) for a typical earning employee"
    - "calculate() net_pay == gross - pretax_401k - fica_ss - fica_medicare - federal_withholding"
    - "FICA SS uses gross as its base (not gross - pretax_401k)"
    - "Federal withholding uses gross - pretax_401k as its base (401k reduces federal base, not FICA)"
    - "Salaried employee with leave hours has higher gross than same employee with no leave hours"
    - "Salaried leave pay with semi-monthly pay schedule exercises the non-trivial standard_hours branch"
    - "PRE_FEDERAL_NET_LABEL constant is gone from calculate.py"
    - "calculate.py imports FICA constants from tax_tables_2026 (no inline _SS_RATE/_SS_WAGE_BASE/_MEDICARE_RATE)"
    - "All four existing 401k-override tests in test_calculate.py still pass"
    - "Reconciliation check inside calculate() raises AssertionError on arithmetic drift"
    - "OT stays explicit-only: hours_regular=45 with hours_overtime=0 produces straight-time gross, not 1.5x gross (D-03)"
    - "Salaried leave pay formula documented in a comment (Claude's Discretion convention chosen and justified)"
    - "Salaried-with-leave integration case is tested in test_calculate.py (not test_federal_withholding.py)"
  artifacts:
    - path: "app/pipeline/calculate.py"
      provides: "Full-fidelity payroll calc: gross/FICA/401k/federal/net + reconciliation backstop"
      contains: "from app.pipeline.federal_withholding import federal_withholding_2026"
    - path: "tests/test_calculate.py"
      provides: "Extended test suite: CALC-01/02/07/08 cases added below existing 401k tests; salaried-with-leave integration case; semi-monthly leave-pay branch"
  key_links:
    - from: "app/pipeline/calculate.py"
      to: "app/pipeline/federal_withholding.py"
      via: "federal_withholding_2026(federal_taxable, employee)"
      pattern: "from app.pipeline.federal_withholding import federal_withholding_2026"
    - from: "app/pipeline/calculate.py"
      to: "app/pipeline/tax_tables_2026.py"
      via: "SS_RATE, SS_WAGE_BASE, MEDICARE_RATE imported as _SS_RATE/_SS_WAGE_BASE/_MEDICARE_RATE for backward compat"
      pattern: "from app.pipeline.tax_tables_2026 import"
---

<objective>
Deepen calculate.py from the Phase 2 thin calc (gross + FICA, federal=Decimal("0"), net pre-federal) into the full-fidelity Phase 3 calc: salaried leave pay, migrated FICA constants, real Pub 15-T federal withholding call, real net, and the arithmetic reconciliation backstop. Extend test_calculate.py with CALC-01/02/07/08 cases including: the salaried-with-leave integration case (moved here from the golden suite per Fix 9 — integration-through-calculate belongs in test_calculate.py), and the semi-monthly (pay_periods_per_year=24) leave-pay branch to exercise the non-trivial standard_hours formula (Fix 11).

Purpose: This plan completes the payroll math — after it, every PaystubLineItem produced by calculate() is a correct, real paystub. The PRE_FEDERAL_NET_LABEL is retired. The reconciliation check guards arithmetic integrity.

Output: Modified app/pipeline/calculate.py (deepened, no new file) + modified tests/test_calculate.py (4 existing tests green + new CALC-01/02/07/08/CALC-03 cases including salaried-with-leave and semi-monthly branch).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/pipeline/calculate.py
@app/pipeline/federal_withholding.py
@app/pipeline/tax_tables_2026.py
@app/models/roster.py
@app/db/seed.py
@tests/test_calculate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deepen calculate.py — constants migration, salary leave pay, real federal + net, reconciliation, Additional Medicare flag (CALC-01/02/03/04/07/08, User Decision 1)</name>
  <files>app/pipeline/calculate.py</files>

  <read_first>
    PATTERNS.md §"app/pipeline/calculate.py (modified — exact current lines for in-place editing)" — EXACT current lines for each edit:
      Lines 1–23: module docstring (update to remove "thin" / "pre-federal" / D-A6-01 reference per PATTERNS.md)
      Lines 33–38: PRE_FEDERAL_NET_LABEL block to DELETE
      Lines 41–43: three inline FICA constants to REPLACE with import from tax_tables_2026
      Lines 99–101: salary gross (add leave pay per CALC-02)
      Lines 122–124: federal_withholding = Decimal("0") and net_pay line to REPLACE
    PATTERNS.md §"Constants Migration Pattern (D-02)" — the exact import line to add.
    RESEARCH.md §"Gross Calc Engine — Phase 3 Additions" — salaried leave pay options; confirms pro-rated salary convention (Option 2) is the CHOSEN approach (Claude's Discretion — see Action for the documented choice).
    RESEARCH.md §"401k Interaction — Federal vs FICA Base (CALC-03)" — FICA base is gross (unchanged); federal base is gross - pretax_401k. The existing FICA block (PATTERNS.md lines 105–112) is CORRECT and DOES NOT CHANGE.
    RESEARCH.md §"Net Pay Formula (CALC-07)" — new net_pay = gross - pretax_401k - fica_ss - fica_medicare - federal_withholding.
    RESEARCH.md §"Reconciliation Backstop (CALC-08)" — per-line assertion in calculate() is the chosen location.
    app/pipeline/calculate.py (read the entire current file — 145 lines — before making any edit to know every reference that changes).
    app/pipeline/federal_withholding.py (read to confirm the function signature: federal_withholding_2026(federal_taxable_wages_this_period: Decimal, employee: Employee) -> Decimal).
    app/pipeline/tax_tables_2026.py (read to confirm the public names: SS_RATE, SS_WAGE_BASE, MEDICARE_RATE).
  </read_first>

  <behavior>
    - calculate() produces a PaystubLineItem where federal_withholding is NOT Decimal("0") for a typical earning employee (e.g., Maria Chen at $800/week → $54.08)
    - calculate() net_pay == _money(gross - pretax_401k - fica_ss - fica_medicare - federal_withholding) (real net)
    - calculate() with an hourly employee: hours_regular=45, hours_overtime=0 → gross = rate * 45 (straight time only, D-03)
    - calculate() with an hourly employee: hours_overtime=5 → gross = rate * straight + rate * 1.5 * 5 (CALC-01)
    - calculate() with a salaried employee and leave hours → gross > annual / pay_periods (CALC-02)
    - calculate() with a salaried employee and zero leave hours → gross == _money(annual / pay_periods) exactly
    - FICA SS uses gross base not gross - pretax_401k (existing block unchanged, confirmed)
    - Federal withholding uses federal_taxable = gross - pretax_401k (CALC-03)
    - The reconciliation assertion inside calculate() raises AssertionError if net_pay + pretax_401k + fica_ss + fica_medicare + federal_withholding != gross (arithmetic drift test)
    - PRE_FEDERAL_NET_LABEL is not importable from calculate.py (it is gone)
    - calculate.py has no inline _SS_RATE, _SS_WAGE_BASE, _MEDICARE_RATE literals (migrated to tax_tables_2026.py)
    - All four existing 401k-override test assertions in test_calculate.py still pass without change
    - calculate() for an employee with ytd_ss_wages > $200,000 surfaces an "additional_medicare_not_modeled" indicator on the PaystubLineItem (exact mechanism: see Action; must be observable and testable by test_additional_medicare_limitation_is_flagged in Plan 02)
  </behavior>

  <action>
    Edit app/pipeline/calculate.py in place (the executor must read the current 145-line file first). Changes in order from top to bottom:

    1. UPDATE MODULE DOCSTRING (lines 1–23):
    Replace the Phase 2 docstring ("Thin payroll calc — gross + FICA only, net labeled 'pre-federal'...") with a Phase 3 docstring. New first line: "Payroll calc — full-fidelity gross / FICA / federal / net (Phase 3)." Remove all references to "pre-federal", "D-A6-01", "NO federal withholding", "Federal LEFT OUT". Keep the FICA constants documentation section (update to note constants migrated to tax_tables_2026.py per D-02). Add a note: "User Decision 1: Additional Medicare 0.9% surtax over $200k YTD is NOT modeled. calculate() surfaces an 'additional_medicare_not_modeled' indicator when ytd_ss_wages exceeds $200,000 so the limitation is observable and testable rather than silent."

    2. DELETE PRE_FEDERAL_NET_LABEL BLOCK (PATTERNS.md lines 33–38):
    Remove the four lines: the comment ("# FIX 2: the human-readable..."), the constant assignment, and the closing parenthesis. This constant is retired — net is now real (CALC-07).

    3. REPLACE INLINE FICA CONSTANTS WITH IMPORT (PATTERNS.md lines 41–43):
    Remove: _SS_RATE = Decimal("0.062"), _SS_WAGE_BASE = Decimal("184500"), _MEDICARE_RATE = Decimal("0.0145")
    Add to imports (top of file, after existing imports):
    from app.pipeline.tax_tables_2026 import SS_RATE as _SS_RATE, SS_WAGE_BASE as _SS_WAGE_BASE, MEDICARE_RATE as _MEDICARE_RATE
    from app.pipeline.federal_withholding import federal_withholding_2026
    The existing FICA usages (_SS_RATE, _SS_WAGE_BASE, _MEDICARE_RATE) in the function body are UNCHANGED by this import alias — the values are identical, only the source changes. (PATTERNS.md §"Constants Migration Pattern".)

    4. ADD SALARY LEAVE PAY (PATTERNS.md lines 99–101 — CALC-02):
    The current salary branch:
      annual = employee.annual_salary or Decimal("0")
      gross = annual / Decimal(employee.pay_periods_per_year)

    Phase 3 adds leave pay using the pro-rated salary convention (Claude's Discretion — documented choice):
    Convention chosen: leave_pay = (annual / pay_periods_per_year) × (leave_hours / standard_hours_per_period)
    where standard_hours_per_period = Decimal("40") × Decimal(pay_periods_per_year) / Decimal("52").
    This normalizes to annual_salary / 2080 × leave_hours (standard industry implied hourly rate for salaried employees), expressed as a period proportion to handle all pay frequencies without hardcoding 2080.
    leave_hours = hours["hours_vacation"] + hours["hours_sick"] + hours["hours_holiday"]

    New salary branch:
      annual = employee.annual_salary or Decimal("0")
      p = Decimal(employee.pay_periods_per_year)
      period_salary = _money(annual / p)
      # CALC-02: leave pay uses pro-rated salary convention (Claude's Discretion).
      # Convention: leave_rate = annual / (52 * 40) implied hourly rate.
      # Expressed as proportion: leave_pay = period_salary * (leave_hours / standard_hours_per_period)
      # where standard_hours_per_period = 40 * (pay_periods_per_year / 52).
      # This is equivalent to annual_salary / 2080 * leave_hours, handles all pay frequencies,
      # and requires no Employee contract change.
      # For semi-monthly (24): standard_hours = 40 * 24 / 52 = 18.46 hours/period.
      # For monthly (12): standard_hours = 40 * 12 / 52 = 9.23 hours/period.
      standard_hours = Decimal("40") * p / Decimal("52")
      leave_hours = hours["hours_vacation"] + hours["hours_sick"] + hours["hours_holiday"]
      leave_pay = _money(period_salary * (leave_hours / standard_hours)) if standard_hours > 0 else Decimal("0")
      gross = _money(period_salary + leave_pay)

    Note: for salaried employees with zero leave hours, leave_pay = Decimal("0") and gross = period_salary exactly as before. The existing tests that don't pass leave hours will still pass.

    5. REPLACE federal_withholding = Decimal("0") AND net_pay (PATTERNS.md lines 122–124 — CALC-05/07):
    Remove:
      # Phase 2: NO federal withholding (no fabricated figure). The pre-federal net.
      federal_withholding = Decimal("0")
      net_pay = _money(gross - pretax_401k - fica_ss - fica_medicare)

    Replace with:
      # Phase 3: real IRS Pub 15-T 2026 federal withholding (CALC-05).
      # 401k reduces the federal taxable base but NOT the FICA base (CALC-03).
      federal_taxable = _money(gross - pretax_401k)
      federal_withholding = federal_withholding_2026(federal_taxable, employee)
      net_pay = _money(gross - pretax_401k - fica_ss - fica_medicare - federal_withholding)

    6. ADD ADDITIONAL MEDICARE LIMITATION FLAG (User Decision 1 — immediately after the FICA block, before the reconciliation backstop):
    # User Decision 1: Additional Medicare 0.9% on wages over $200k YTD is NOT modeled.
    # The withheld amount uses flat 1.45% in all cases (including over $200k YTD).
    # This is a KNOWN LIMITATION — employer under-withholds by 0.9% above this threshold.
    # Flag it so the limitation is observable and test-backed, not silent.
    _ADDITIONAL_MEDICARE_THRESHOLD = Decimal("200000")
    additional_medicare_not_modeled = employee.ytd_ss_wages > _ADDITIONAL_MEDICARE_THRESHOLD

    Pass this flag to the PaystubLineItem return value. This requires adding an "additional_medicare_not_modeled: bool" field to PaystubLineItem in app/models/contracts.py IF it does not already have one. Check contracts.py first; if the field is missing, add it as "additional_medicare_not_modeled: bool = False" with a default so existing tests are not broken. The field must be observable to the test in Plan 02 (test_additional_medicare_limitation_is_flagged asserts item.additional_medicare_not_modeled is True for ytd_ss_wages > $200k).

    7. ADD RECONCILIATION BACKSTOP (CALC-08 — per-line in calculate(), immediately before the return statement):
    # CALC-08: arithmetic backstop — net + taxes + deductions must tie back to gross.
    # This is NOT the correctness oracle for tax math (the golden tests are the oracle).
    # A transcription bug in tax_tables_2026.py can still produce internally-consistent
    # but wrong results — the reconciliation check does NOT catch that.
    _state_wh = Decimal("0")  # state_withholding is always None in Phase 3
    _reconstructed = _money(net_pay + pretax_401k + fica_ss + fica_medicare + federal_withholding + _state_wh)
    assert _reconstructed == gross, (
        f"Reconciliation failed: {_reconstructed} != {gross}. "
        "This is an arithmetic bug in calculate(), not a tax-math error."
    )

    IMPORTANT — what does NOT change:
    - The FICA block (lines 105–112 in original) — remaining_cap / ss_taxable / fica_ss / fica_medicare — is CORRECT and UNCHANGED. FICA base is gross (CALC-03 — 401k does NOT reduce FICA base).
    - The calculate() function signature (lines 73–86) — unchanged; contribution_401k_override stays.
    - The pretax_401k calculation — unchanged.
    - The _resolved_hours(), _money(), _CENTS — unchanged.
  </action>

  <verify>
    <automated>
      cd /Users/pnhek/usf\ msds/github/payroll_agent && uv run python -c "
from decimal import Decimal
from app.pipeline.calculate import calculate
from app.db.seed import seed

seeded = seed(dry_run=True)
maria = next(e for e in seeded.employees if e.full_name == 'Maria Chen')

# CALC-07: net is real net
item = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('0'),
                  'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                 maria)
assert item.federal_withholding != Decimal('0'), 'federal_withholding must be real'
expected_net = (item.gross_pay - item.pretax_401k - item.fica_ss - item.fica_medicare - item.federal_withholding).quantize(Decimal('0.01'))
assert item.net_pay == expected_net, f'net_pay {item.net_pay} != {expected_net}'

# CALC-01: OT at 1.5x
maria_ot = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('5'),
                      'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                     maria)
expected_gross = (maria.hourly_rate * Decimal('40') + maria.hourly_rate * Decimal('1.5') * Decimal('5')).quantize(Decimal('0.01'))
assert maria_ot.gross_pay == expected_gross, f'OT gross {maria_ot.gross_pay} != {expected_gross}'

# D-03: OT explicit-only — hours_regular=45, hours_overtime=0 → straight time
maria_no_ot = calculate({'hours_regular': Decimal('45'), 'hours_overtime': Decimal('0'),
                         'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                        maria)
expected_straight = (maria.hourly_rate * Decimal('45')).quantize(Decimal('0.01'))
assert maria_no_ot.gross_pay == expected_straight, f'Straight 45h gross {maria_no_ot.gross_pay} != {expected_straight}'

# CALC-02: salaried leave pay
james = next(e for e in seeded.employees if e.full_name == 'James Okafor')
item_no_leave = calculate({'hours_regular': Decimal('0'), 'hours_overtime': Decimal('0'),
                           'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                          james)
item_with_leave = calculate({'hours_regular': Decimal('0'), 'hours_overtime': Decimal('0'),
                             'hours_vacation': Decimal('8'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                            james)
assert item_with_leave.gross_pay > item_no_leave.gross_pay, 'Leave pay must increase salaried gross'

# User Decision 1: Additional Medicare flag must be observable
import uuid
from app.models.roster import Employee
emp_high_ytd = Employee(
    id=uuid.uuid4(), business_id=uuid.uuid4(), full_name='HighYTD', known_aliases=[],
    pay_type='hourly', hourly_rate=Decimal('100.00'), annual_salary=None,
    retirement_contribution_pct=Decimal('0'),
    filing_status='single', step_2_checkbox=False,
    step_3_dependents=Decimal('0'), step_4a_other_income=Decimal('0'), step_4b_deductions=Decimal('0'),
    ytd_ss_wages=Decimal('201000'), pay_periods_per_year=52,
)
item_high = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('0'),
                       'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                      emp_high_ytd)
assert hasattr(item_high, 'additional_medicare_not_modeled'), 'Flag field must exist on PaystubLineItem'
assert item_high.additional_medicare_not_modeled is True, 'Flag must be True when ytd_ss_wages > 200k'

# PRE_FEDERAL_NET_LABEL must not exist
try:
    from app.pipeline.calculate import PRE_FEDERAL_NET_LABEL
    assert False, 'PRE_FEDERAL_NET_LABEL must be gone'
except ImportError:
    pass

# FICA constants must not be inline
import pathlib
src = pathlib.Path('app/pipeline/calculate.py').read_text()
assert '_SS_RATE = Decimal' not in src, 'inline _SS_RATE must be removed'
assert '_SS_WAGE_BASE = Decimal' not in src, 'inline _SS_WAGE_BASE must be removed'
assert '_MEDICARE_RATE = Decimal' not in src, 'inline _MEDICARE_RATE must be removed'
print('calculate.py deepening OK')
"
    </automated>
  </verify>

  <done>
    calculate() produces real federal withholding and real net for a typical employee. Salaried employees with leave hours have higher gross. OT is explicit-only. PRE_FEDERAL_NET_LABEL is gone. FICA constants import from tax_tables_2026.py. Reconciliation assertion inside calculate() is present. additional_medicare_not_modeled flag is True for ytd_ss_wages > $200k. All changes verified by the automated script above.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_calculate.py — CALC-01/02/07/08 + salaried-with-leave integration + semi-monthly leave-pay branch (Fix 9, Fix 11)</name>
  <files>tests/test_calculate.py</files>

  <read_first>
    tests/test_calculate.py (read the entire current 66-line file — the four existing tests must stay green; add BELOW them, never above or in place of).
    PATTERNS.md §"tests/test_calculate.py (extended)" — the exact new test functions for CALC-01 (hourly OT), CALC-02 (salaried leave pay), CALC-07 (real net), CALC-08 (reconciliation identity + drift test).
    app/pipeline/calculate.py (after Task 1 edits — read to confirm the salaried leave pay formula and the reconciliation assertion location).
    app/db/seed.py — James Okafor (salary employee, weekly, for CALC-02 tests); Thomas Bergmann (high earner, biweekly, for SS straddle).
    RESEARCH.md §"Gross Calc Engine — Phase 3 Additions" — the leave pay formula: standard_hours_per_period = 40 × pay_periods_per_year / 52. For semi-monthly (24): standard_hours = 40 × 24 / 52 ≈ 18.46 hours/period.
  </read_first>

  <behavior>
    Existing tests (all four) still pass without modification:
      test_uses_stored_default_when_no_override
      test_override_replaces_stored_rate_for_this_run
      test_override_does_not_mutate_employee
      test_zero_override_is_honored_not_treated_as_absent

    New test_hourly_overtime_at_1_5x: hourly employee with hours_overtime=5 → gross = rate*40 + rate*1.5*5 (CALC-01)
    New test_leave_hours_excluded_from_ot_threshold: hourly employee with hours_vacation=8, hours_overtime=0 → gross = rate*(40+8) straight time only (leave never triggers OT per D-03/CALC-01)
    New test_salaried_leave_pay_added_to_gross: salaried employee (James Okafor, weekly/52) with hours_vacation=8 → gross > annual/pay_periods (CALC-02)
    New test_salaried_no_leave_gross_unchanged: salaried employee (James Okafor) with zero leave → gross == _money(annual/pay_periods) exactly (CALC-02 baseline)
    New test_salaried_leave_pay_semimonthly_branch: SYNTHETIC salaried employee with pay_periods_per_year=24 (semi-monthly). Add hours_vacation=8 leave. Assert: (a) gross > period_salary (leave pay was added), (b) the leave pay amount equals period_salary * (leave_hours / standard_hours) where standard_hours = 40 * 24 / 52 ≈ 18.46. This exercises the non-trivial formula branch (standard_hours ≠ 40) that weekly (52 periods) makes trivial (standard_hours = 40 exactly). (Fix 11 — CALC-02 semi-monthly coverage)
    New test_salaried_with_leave_gross_integration: salaried employee with leave hours → federal_withholding is higher than for same employee with no leave (because gross is higher → federal_taxable is higher → withholding is higher). This is the INTEGRATION test through calculate() for the salaried-with-leave path end-to-end. (Fix 9 — this test moved here from test_federal_withholding.py where it would have created a wave dependency)
    New test_net_pay_is_real_net: net_pay == gross - pretax_401k - fica_ss - fica_medicare - federal_withholding, AND federal_withholding > 0 for a typical employee (CALC-07)
    New test_reconciliation_identity: sum of all paystub parts equals gross (CALC-08 normal case)
    New test_fica_uses_gross_not_reduced_base: employee with 401k > 0 → fica_ss == _money(min(gross, remaining_cap) * Decimal("0.062")) where the gross used is NOT gross - pretax_401k (CALC-03 interaction guard)
    New test_additional_medicare_flag_present: ytd_ss_wages > $200k → item.additional_medicare_not_modeled is True; ytd_ss_wages <= $200k → item.additional_medicare_not_modeled is False (User Decision 1 — observable flag)
  </behavior>

  <action>
    Edit tests/test_calculate.py by APPENDING new test functions BELOW the four existing ones. Do NOT modify or remove any existing line. Add a section comment before the new block: "# ---- Phase 3 additions: CALC-01 / CALC-02 / CALC-07 / CALC-08 / Fix-9 / Fix-11 ----".

    Add a salary_employee fixture (weekly, 52 periods — James Okafor from seed):
    @pytest.fixture()
    def salary_employee():
        seeded = seed(dry_run=True)
        return next(e for e in seeded.employees if e.pay_type == "salary" and e.pay_periods_per_year == 52)

    Add a _make_salary_employee() helper for synthetic fixtures (pay_type="salary", requires annual_salary, no hourly_rate):
    Construct an Employee with all 15 fields; annual_salary provided, hourly_rate=None. Use pay_periods_per_year as a parameter to allow testing 24 (semi-monthly). All other W-4 fields default to $0/False. uuid.uuid4() for id/business_id.

    test_hourly_overtime_at_1_5x (CALC-01):
    Calls calculate() with hours_overtime=Decimal("5"), hours_regular=Decimal("40").
    Asserts item.gross_pay == _money(hourly_employee.hourly_rate * 40 + hourly_employee.hourly_rate * Decimal("1.5") * 5).
    Compute _money() locally with the same Decimal("0.01") quantize pattern from the file.

    test_leave_hours_excluded_from_ot_threshold (CALC-01 + D-03):
    Hours: hours_regular=40, hours_vacation=8, hours_overtime=0.
    Asserts gross == _money(hourly_employee.hourly_rate * (40 + 8)) — straight time on all leave, no OT premium.

    test_salaried_leave_pay_added_to_gross (CALC-02):
    Call calculate() on salary_employee with hours_vacation=8, everything else 0.
    Call calculate() on same employee with zero hours.
    Assert with_leave.gross_pay > without_leave.gross_pay.

    test_salaried_no_leave_gross_unchanged (CALC-02 baseline):
    Call calculate() on salary_employee with all zero hours.
    Assert gross_pay == (salary_employee.annual_salary / Decimal(salary_employee.pay_periods_per_year)).quantize(Decimal("0.01")).

    test_salaried_leave_pay_semimonthly_branch (Fix 11 — CALC-02):
    Create a synthetic salaried employee with pay_periods_per_year=24 (semi-monthly), annual_salary=Decimal("52000").
    period_salary = _money(Decimal("52000") / Decimal("24"))  # = $2,166.67
    standard_hours = Decimal("40") * Decimal("24") / Decimal("52")  # ≈ 18.46 hours/period
    leave_hours = Decimal("8")
    expected_leave_pay = _money(period_salary * (leave_hours / standard_hours))
    expected_gross = _money(period_salary + expected_leave_pay)
    Call calculate() with hours_vacation=8, everything else 0 on this employee.
    Assert item.gross_pay == expected_gross.
    Assert item.gross_pay > period_salary  (leave pay is positive for the non-trivial branch).
    Add comment: "# Fix 11: exercises standard_hours = 40*24/52 ≈ 18.46 (non-trivial branch). Weekly (52) makes standard_hours=40 trivially, hiding formula bugs."

    test_salaried_with_leave_gross_integration (Fix 9 — integration case):
    Uses salary_employee (James Okafor or any weekly salaried employee).
    item_no_leave = calculate(zero hours, salary_employee)
    item_with_leave = calculate(hours_vacation=8, salary_employee)
    Assert item_with_leave.gross_pay > item_no_leave.gross_pay  (leave increases gross)
    Assert item_with_leave.federal_withholding >= item_no_leave.federal_withholding  (higher gross → higher or equal withholding)
    Add comment: "# Fix 9: salaried-with-leave integration test belongs in test_calculate.py (integration-through-calculate), not test_federal_withholding.py (which tests the withholding engine in isolation). Moving this here prevents a wave-2 forward dependency where 03-02 tests the deepened calculate() before 03-03 ships it."

    test_net_pay_is_real_net (CALC-07):
    Call calculate(_hours(), hourly_employee) for a typical employee.
    Assert item.federal_withholding > Decimal("0") — it is real, not zero.
    Assert item.net_pay == _money(item.gross_pay - item.pretax_401k - item.fica_ss - item.fica_medicare - item.federal_withholding).

    test_reconciliation_identity (CALC-08):
    Call calculate(_hours(), hourly_employee).
    Reconstruct: _money(item.net_pay + item.pretax_401k + item.fica_ss + item.fica_medicare + item.federal_withholding + (item.state_withholding or Decimal("0"))).
    Assert reconstructed == item.gross_pay.

    test_fica_uses_gross_not_reduced_base (CALC-03):
    Use an employee with retirement_contribution_pct > 0 (James Okafor at 4%, or the override mechanism).
    Assert item.fica_ss == _money(min(item.gross_pay, remaining_cap) * Decimal("0.062"))
    where remaining_cap is computed from the same employee's ytd_ss_wages.
    Assert item.fica_medicare == _money(item.gross_pay * Decimal("0.0145")).
    Both FICA amounts must use gross_pay, not (gross_pay - pretax_401k).
    Note: import SS_RATE, SS_WAGE_BASE, MEDICARE_RATE from app.pipeline.tax_tables_2026 for this assertion.

    test_additional_medicare_flag_present (User Decision 1):
    import uuid; from app.models.roster import Employee
    Create emp_over = synthetic employee with ytd_ss_wages=Decimal("201000").
    Create emp_under = synthetic employee with ytd_ss_wages=Decimal("100000").
    item_over = calculate(40 hours, emp_over)
    item_under = calculate(40 hours, emp_under)
    Assert item_over.additional_medicare_not_modeled is True
    Assert item_under.additional_medicare_not_modeled is False
    Add comment: "# User Decision 1: The flag makes the Additional Medicare limitation observable and test-backed. asserting item.fica_medicare == gross * 0.0145 as 'correct' at >$200k YTD would encode the defect as green."

    The _hours() helper already exists in the file — do NOT add a duplicate. The new tests that need hours dicts either reuse _hours() or construct the dict inline.
  </action>

  <verify>
    <automated>
      cd /Users/pnhek/usf\ msds/github/payroll_agent && uv run pytest tests/test_calculate.py -q
    </automated>
  </verify>

  <done>
    uv run pytest tests/test_calculate.py -q exits with 0 failures. All 4 existing tests green. All new CALC-01/02/07/08/CALC-03/Fix-9/Fix-11 tests pass including: test_salaried_leave_pay_semimonthly_branch (semi-monthly non-trivial branch exercised) and test_salaried_with_leave_gross_integration (integration end-to-end). test_additional_medicare_flag_present passes. uv run pytest -q (full suite) is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No external trust boundaries in this plan | calculate.py is a pure function; all inputs are Pydantic-validated Employee objects with ge=0 constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | calculate.py arithmetic (401k base interaction) | mitigate | test_fica_uses_gross_not_reduced_base asserts FICA base is gross; the golden suite in Plan 02 also covers this |
| T-03-03-02 | Tampering | Reconciliation bypass | accept | Reconciliation is per-line AssertionError inside calculate() — it fires on any arithmetic drift; it is NOT a security control but an arithmetic integrity guard |
| T-03-03-03 | Denial of Service | Additional Medicare silent under-withholding | mitigate | User Decision 1 flag (additional_medicare_not_modeled) surfaces the limitation as observable and test-backed; the under-withheld amount is documented, not hidden |
| T-03-03-04 | Information Disclosure | N/A | accept | Pure offline arithmetic; no PII emitted outside the return value |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No new deps added in this plan (python-taxes already handled in Plan 02); the plan only edits existing files |

Note: ASVS V2/V3/V4/V6 do not apply. V5 input validation: Employee ge=0 constraints on W-4 fields are the upstream gate (already shipped in Phase 1). The reconciliation assertion is a correctness backstop (CALC-08), not a security control. The Additional Medicare flag is a limitation-disclosure control per User Decision 1.
</threat_model>

<verification>
uv run pytest tests/test_calculate.py -q   # all existing + new tests pass
uv run pytest -q                            # full suite green (195+ existing + new)
grep -c "PRE_FEDERAL_NET_LABEL" app/pipeline/calculate.py  # must be 0
grep -c "from app.pipeline.tax_tables_2026 import" app/pipeline/calculate.py  # must be >= 1
grep -c "federal_withholding_2026" app/pipeline/calculate.py  # must be >= 1
grep -c "_SS_RATE = Decimal" app/pipeline/calculate.py  # must be 0 (migrated)
grep -c "additional_medicare_not_modeled" app/pipeline/calculate.py  # must be >= 1
grep -c "semimonthly_branch" tests/test_calculate.py  # must be >= 1 (Fix 11)
grep -c "leave_gross_integration" tests/test_calculate.py  # must be >= 1 (Fix 9)
</verification>

<success_criteria>
1. uv run pytest tests/test_calculate.py -q exits with 0 failures.
2. All 4 original 401k-override tests still pass (no regression).
3. test_net_pay_is_real_net passes and confirms federal_withholding > Decimal("0") for a typical employee.
4. test_reconciliation_identity passes — confirming the backstop check is correctly implemented.
5. test_fica_uses_gross_not_reduced_base passes — confirming 401k does NOT reduce the FICA base.
6. test_salaried_leave_pay_added_to_gross passes — confirming CALC-02 leave pay addition (weekly, trivial branch).
7. test_salaried_leave_pay_semimonthly_branch passes — confirming CALC-02 at pay_periods_per_year=24 exercises the non-trivial standard_hours formula (Fix 11).
8. test_salaried_with_leave_gross_integration passes — end-to-end: leave → higher gross → higher withholding (Fix 9).
9. test_additional_medicare_flag_present passes — item.additional_medicare_not_modeled is True for ytd_ss_wages>$200k, False for ytd_ss_wages<=$200k (User Decision 1).
10. PRE_FEDERAL_NET_LABEL is not importable from calculate.py (grep returns 0).
11. calculate.py imports FICA constants from tax_tables_2026 (no inline literals).
12. calculate.py carries additional_medicare_not_modeled flag logic.
13. Full suite uv run pytest -q is green.
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-03-SUMMARY.md when done.
</output>
