---
phase: 03-harden-the-calc
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/pipeline/calculate.py
  - app/models/contracts.py
  - tests/test_calculate.py
autonomous: true
requirements:
  - CALC-01
  - CALC-02
  - CALC-03
  - CALC-04
  - CALC-07
  - CALC-08

must_haves:
  truths:
    - "calculate() returns real federal withholding (never Decimal('0')) for a typical earning employee"
    - "calculate() net_pay == gross - pretax_401k - fica_ss - fica_medicare - federal_withholding"
    - "FICA SS uses gross as its base (not gross - pretax_401k)"
    - "Federal withholding uses gross - pretax_401k as its base (401k reduces federal base, not FICA)"
    - "Salaried employee with leave hours has higher gross than same employee with no leave hours"
    - "Salaried leave pay for semi-monthly (p=24), monthly (p=12), biweekly (p=26), and weekly (p=52) produces the SAME leave_pay per leave_hour for identical annual_salary — the formula is frequency-independent (R2-6: p=26 added to invariance test)"
    - "PRE_FEDERAL_NET_LABEL constant is gone from calculate.py"
    - "calculate.py imports FICA constants from tax_tables_2026 (no inline _SS_RATE/_SS_WAGE_BASE/_MEDICARE_RATE)"
    - "All four existing 401k-override tests in test_calculate.py still pass"
    - "_raise_if_reconciliation_drift() is a named pure helper in calculate.py that raises PayrollCalculationError when net+taxes+deductions does not tie to gross (R2-3)"
    - "test_reconciliation_raises_on_drift exercises the ACTUAL raise path via pytest.raises(PayrollCalculationError), not just a source grep (R2-3)"
    - "OT stays explicit-only: hours_regular=45 with hours_overtime=0 produces straight-time gross, not 1.5x gross (D-03)"
    - "Salaried leave pay formula documented in a comment; uses annual_salary / 2080 * leave_hours (implied hourly form)"
    - "Salaried-with-leave integration case is tested in test_calculate.py (not test_federal_withholding.py)"
    - "additional_medicare_not_modeled fires when (ytd_ss_wages_as_medicare_proxy + current_gross) > 200000, never on ytd_ss_wages alone"
    - "Additional Medicare test fixtures use REALISTIC capped SS YTD values (ytd_ss_wages <= 184500) — no impossible values above the SS wage base (R2-2)"
  artifacts:
    - path: "app/pipeline/calculate.py"
      provides: "Full-fidelity payroll calc: gross/FICA/401k/federal/net + reconciliation backstop via named helper"
      contains: "from app.pipeline.federal_withholding import federal_withholding_2026"
    - path: "app/models/contracts.py"
      provides: "PaystubLineItem with additional_medicare_not_modeled: bool = False field (additive, non-breaking)"
      contains: "additional_medicare_not_modeled"
    - path: "tests/test_calculate.py"
      provides: "Extended test suite: CALC-01/02/07/08 cases added below existing 401k tests; salaried-with-leave integration case; 4-frequency leave-pay invariance test (p=52/26/24/12); ACTUAL reconciliation raise test via pytest.raises"
  key_links:
    - from: "app/pipeline/calculate.py"
      to: "app/pipeline/federal_withholding.py"
      via: "federal_withholding_2026(federal_taxable, employee)"
      pattern: "from app.pipeline.federal_withholding import federal_withholding_2026"
    - from: "app/pipeline/calculate.py"
      to: "app/pipeline/tax_tables_2026.py"
      via: "SS_RATE, SS_WAGE_BASE, MEDICARE_RATE imported as _SS_RATE/_SS_WAGE_BASE/_MEDICARE_RATE for backward compat"
      pattern: "from app.pipeline.tax_tables_2026 import"
---

<objective>
Deepen calculate.py from the Phase 2 thin calc (gross + FICA, federal=Decimal("0"), net pre-federal) into the full-fidelity Phase 3 calc: salaried leave pay using the frequency-independent annual/2080 implied-hourly form, migrated FICA constants, real Pub 15-T federal withholding call, real net, the Additional Medicare limitation flag triggered by a Medicare-wage proxy, and the arithmetic reconciliation backstop as a named pure helper (_raise_if_reconciliation_drift) that raises PayrollCalculationError — tested via pytest.raises on the actual drift path (R2-3). Extend test_calculate.py with CALC-01/02/07/08 cases including the salaried-with-leave integration case (moved here from the golden suite per Fix 9), and a 4-frequency-invariance test (p=52/26/24/12 — R2-6 adds p=26 to the three already required) that proves the /2080 formula is frequency-independent.

Purpose: This plan completes the payroll math — after it, every PaystubLineItem produced by calculate() is a correct, real paystub. The PRE_FEDERAL_NET_LABEL is retired. The reconciliation check guards arithmetic integrity via a real exception raised from a named helper, tested end-to-end. Plan 03-02's golden suite depends on this plan being complete first (FIX D — wave ordering).

Output: Modified app/pipeline/calculate.py (deepened, no new file) + app/models/contracts.py (additional_medicare_not_modeled field added, additive/non-breaking) + modified tests/test_calculate.py (4 existing tests green + new CALC-01/02/07/08/CALC-03 cases including salaried-with-leave and 4-frequency-invariance test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/pipeline/calculate.py
@app/pipeline/federal_withholding.py
@app/pipeline/tax_tables_2026.py
@app/models/roster.py
@app/models/contracts.py
@app/db/seed.py
@tests/test_calculate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deepen calculate.py + contracts.py — constants migration, salary leave pay (/2080 form), real federal + net, Additional Medicare proxy flag, _raise_if_reconciliation_drift() named helper (CALC-01/02/03/04/07/08, R2-2/R2-3, User Decision 1)</name>
  <files>app/pipeline/calculate.py, app/models/contracts.py</files>

  <read_first>
    PATTERNS.md §"app/pipeline/calculate.py (modified — exact current lines for in-place editing)" — EXACT current lines for each edit:
      Lines 1–23: module docstring (update to remove "thin" / "pre-federal" / D-A6-01 reference)
      Lines 33–38: PRE_FEDERAL_NET_LABEL block to DELETE
      Lines 41–43: three inline FICA constants to REPLACE with import from tax_tables_2026
      Lines 99–101: salary gross (add leave pay per CALC-02, FIX A formula)
      Lines 122–124: federal_withholding = Decimal("0") and net_pay line to REPLACE
    PATTERNS.md §"Constants Migration Pattern (D-02)" — the exact import line to add.
    RESEARCH.md §"Gross Calc Engine — Phase 3 Additions" — salaried leave pay; the /2080 form is chosen.
    RESEARCH.md §"401k Interaction — Federal vs FICA Base (CALC-03)" — FICA base is gross (unchanged); federal base is gross - pretax_401k.
    RESEARCH.md §"Net Pay Formula (CALC-07)" — net_pay = gross - pretax_401k - fica_ss - fica_medicare - federal_withholding.
    RESEARCH.md §"Reconciliation Backstop (CALC-08)" — per-line in calculate() is the chosen location; R2-3 requires extracting it as a named helper.
    app/pipeline/calculate.py (read the entire current file before editing — know every reference that changes).
    app/pipeline/federal_withholding.py (confirm signature: federal_withholding_2026(federal_taxable_wages_this_period: Decimal, employee: Employee) -> Decimal).
    app/pipeline/tax_tables_2026.py (confirm public names: SS_RATE, SS_WAGE_BASE, MEDICARE_RATE).
    app/models/contracts.py (read PaystubLineItem — extra="forbid" means ALL fields must be present at construction; adding a field with a default is additive and non-breaking for existing call sites that don't pass the new field).
  </read_first>

  <behavior>
    - calculate() produces a PaystubLineItem where federal_withholding is NOT Decimal("0") for a typical earning employee (e.g., Maria Chen at $800/week → $54.08)
    - calculate() net_pay == _money(gross - pretax_401k - fica_ss - fica_medicare - federal_withholding) (real net)
    - calculate() with an hourly employee: hours_regular=45, hours_overtime=0 → gross = rate * 45 (straight time only, D-03)
    - calculate() with an hourly employee: hours_overtime=5 → gross = rate * 40 + rate * 1.5 * 5 (CALC-01)
    - calculate() with a salaried employee and 8 leave hours: leave_pay == _money((annual_salary / Decimal("2080")) * Decimal("8")) regardless of pay_periods_per_year — identical for p=52, p=26, p=24, p=12 (R2-6: biweekly p=26 added)
    - calculate() with a salaried employee and zero leave hours → gross == _money(annual / pay_periods) exactly
    - FICA SS uses gross base not gross - pretax_401k (existing block unchanged, confirmed)
    - Federal withholding uses federal_taxable = gross - pretax_401k (CALC-03)
    - _raise_if_reconciliation_drift(gross, pretax_401k, fica_ss, fica_medicare, federal_withholding, net_pay) is a named module-level pure helper that raises PayrollCalculationError when the arithmetic identity does not tie (R2-3). Called near the end of calculate() before the return.
    - additional_medicare_not_modeled fires when (ytd_ss_wages + gross) > Decimal("200000"); does NOT fire for a normal employee with ytd_ss_wages=Decimal("0") and typical gross
    - PRE_FEDERAL_NET_LABEL is not importable from calculate.py (it is gone)
    - calculate.py has no inline _SS_RATE, _SS_WAGE_BASE, _MEDICARE_RATE literals (migrated to tax_tables_2026.py)
    - All four existing 401k-override test assertions in test_calculate.py still pass without change
    - contracts.py PaystubLineItem gains "additional_medicare_not_modeled: bool = False" — default=False means existing calculate() callers that do not pass this field are unbroken
  </behavior>

  <action>
    PART A — app/models/contracts.py:

    Read the current file (already in context). Add one field to PaystubLineItem BEFORE the closing of the class definition, after "state_withholding: Decimal | None":

      additional_medicare_not_modeled: bool = False
      # User Decision 1 (FIX B): Additional Medicare 0.9% surtax on wages over $200k YTD
      # is NOT modeled. When this flag is True the engine is known to under-withhold by 0.9%
      # above the threshold. The flag makes the limitation observable and test-backed.
      # Default=False so existing callers are unbroken (additive, non-breaking field addition).

    PART B — app/pipeline/calculate.py:

    Edit in place (read the current 145-line file first). Changes in order from top to bottom:

    1. UPDATE MODULE DOCSTRING (lines 1–23):
    Replace the Phase 2 docstring with a Phase 3 docstring. New first line: "Payroll calc — full-fidelity gross / FICA / federal / net (Phase 3)." Remove all references to "pre-federal", "D-A6-01", "NO federal withholding", "Federal LEFT OUT". Keep the FICA constants documentation section (update to note constants migrated to tax_tables_2026.py per D-02). Add a note about the Additional Medicare flag (User Decision 1) and the reconciliation helper (R2-3).

    2. DELETE PRE_FEDERAL_NET_LABEL BLOCK (PATTERNS.md lines 33–38):
    Remove the four lines: the comment, the constant assignment, and the closing parenthesis. This constant is retired — net is now real (CALC-07).

    3. REPLACE INLINE FICA CONSTANTS WITH IMPORT (PATTERNS.md lines 41–43):
    Remove: _SS_RATE = Decimal("0.062"), _SS_WAGE_BASE = Decimal("184500"), _MEDICARE_RATE = Decimal("0.0145")
    Add to imports (top of file, after existing imports):
      from app.pipeline.tax_tables_2026 import SS_RATE as _SS_RATE, SS_WAGE_BASE as _SS_WAGE_BASE, MEDICARE_RATE as _MEDICARE_RATE
      from app.pipeline.federal_withholding import federal_withholding_2026
    The existing FICA usages (_SS_RATE, _SS_WAGE_BASE, _MEDICARE_RATE) in the function body are UNCHANGED — the values are identical, only the source changes.

    4. ADD PayrollCalculationError EXCEPTION CLASS (add near top of module, before calculate()):

      class PayrollCalculationError(Exception):
          """Raised by _raise_if_reconciliation_drift() when net + taxes + deductions does not tie to gross.

          This is an arithmetic backstop (CALC-08), NOT the correctness oracle for tax math.
          A transcription bug in tax_tables_2026.py can produce internally-consistent but
          wrong numbers — the reconciliation check does NOT catch that. The golden tests
          (test_federal_withholding.py) are the correctness oracle.
          """

    5. ADD _raise_if_reconciliation_drift() NAMED HELPER (R2-3 — add as a module-level function after PayrollCalculationError, before calculate()):

    This is the key R2-3 fix: extracting the reconciliation logic into a named pure helper makes it directly unit-testable on both the pass path and the drift-raises path, without monkeypatching calculate()'s internal state.

      def _raise_if_reconciliation_drift(
          gross: Decimal,
          pretax_401k: Decimal,
          fica_ss: Decimal,
          fica_medicare: Decimal,
          federal_withholding: Decimal,
          net_pay: Decimal,
      ) -> None:
          """Arithmetic backstop (CALC-08): raise PayrollCalculationError if net + taxes + deductions != gross.

          This is a pure arithmetic identity check, NOT a tax-correctness oracle.
          A transcription error in tax_tables_2026.py can produce a self-consistent but
          wrong paystub — this check does NOT catch that. The golden test suite is the oracle.

          CRITICAL (FIX C / R2-3): uses an explicit raise, NOT assert. Python -O strips assert
          statements silently, so the backstop would disappear in optimized deployments.
          PayrollCalculationError is a real exception that cannot be stripped.

          Call signature note: state_withholding is always Decimal("0") / None in Phase 3,
          so it is not included in the parameters — the caller reconstructs from these six values.
          """
          _state_wh = Decimal("0")  # state_withholding is always None in Phase 3
          _reconstructed = _money(net_pay + pretax_401k + fica_ss + fica_medicare + federal_withholding + _state_wh)
          if _reconstructed != gross:
              raise PayrollCalculationError(
                  f"Reconciliation failed: reconstructed={_reconstructed} != gross={gross}. "
                  "This is an arithmetic bug in calculate(), not a tax-math error. "
                  "Check the net_pay, FICA, and federal_withholding formulas."
              )

    6. ADD SALARY LEAVE PAY — FIX A: /2080 FORM (PATTERNS.md lines 99–101 — CALC-02):
    The current salary branch:
      annual = employee.annual_salary or Decimal("0")
      gross = annual / Decimal(employee.pay_periods_per_year)

    Phase 3 adds leave pay using the FREQUENCY-INDEPENDENT implied-hourly form (FIX A).

    CRITICAL: Do NOT use `standard_hours_per_period = 40 * pay_periods / 52`. That expression is
    invertible — for semi-monthly (p=24) it yields ~18.46h/period and leave_pay would be 4.7x too high.

    New salary branch:
      annual = employee.annual_salary or Decimal("0")
      p = Decimal(employee.pay_periods_per_year)
      period_salary = _money(annual / p)
      # CALC-02: salaried leave pay uses the implied-hourly form (FIX A — frequency-independent).
      # implied_hourly = annual_salary / 2080  (where 2080 = 40h/wk * 52 wk/yr).
      # leave_pay = implied_hourly * leave_hours  — identical result for p=52, p=26, p=24, p=12.
      _ANNUAL_WORK_HOURS = Decimal("2080")  # 40h/wk * 52 wk — standard work-year hours
      leave_hours = hours.get("hours_vacation", Decimal("0")) + hours.get("hours_sick", Decimal("0")) + hours.get("hours_holiday", Decimal("0"))
      leave_pay = _money((annual / _ANNUAL_WORK_HOURS) * leave_hours)
      gross = _money(period_salary + leave_pay)

    7. REPLACE federal_withholding = Decimal("0") AND net_pay (PATTERNS.md lines 122–124 — CALC-05/07):
    Remove:
      # Phase 2: NO federal withholding (no fabricated figure). The pre-federal net.
      federal_withholding = Decimal("0")
      net_pay = _money(gross - pretax_401k - fica_ss - fica_medicare)

    Replace with:
      # Phase 3: real IRS Pub 15-T 2026 federal withholding (CALC-05).
      # 401k reduces the federal taxable base but NOT the FICA base (CALC-03).
      federal_taxable = _money(gross - pretax_401k)
      federal_withholding = federal_withholding_2026(federal_taxable, employee)
      net_pay = _money(gross - pretax_401k - fica_ss - fica_medicare - federal_withholding)

    8. ADD ADDITIONAL MEDICARE LIMITATION FLAG — FIX B (immediately after the FICA block, before the reconciliation call):
    # User Decision 1 (FIX B): Additional Medicare 0.9% on wages over $200k YTD is NOT modeled.
    # Trigger: (ytd_ss_wages_as_medicare_proxy + current_gross) > $200,000.
    # Why ytd_ss_wages as proxy: it is a DOCUMENTED LOWER BOUND for YTD Medicare wages
    # (Medicare has no wage cap, so Medicare YTD >= SS YTD always). Adding current gross
    # gives a conservative Medicare-wage estimate that CAN cross $200k.
    # Limitation: if an employee's true Medicare YTD already exceeds their capped SS YTD
    # (wages above $184,500 in prior periods), this proxy UNDER-FLAGS — accepted limitation
    # of the static-seed model (no per-employee YTD Medicare ledger in Phase 3).
    # R2-2 note: ytd_ss_wages is capped at $184,500 (the SS wage base) in any real run —
    # it CANNOT legitimately exceed that cap. Tests must use realistic values (ytd_ss_wages <= 184500).
    _ADDITIONAL_MEDICARE_THRESHOLD = Decimal("200000")
    additional_medicare_not_modeled = (employee.ytd_ss_wages + gross) > _ADDITIONAL_MEDICARE_THRESHOLD

    9. CALL _raise_if_reconciliation_drift() (R2-3 — replace any prior reconciliation block with a call to the named helper):
    # CALC-08: arithmetic backstop — call the named helper (R2-3).
    # The helper is a pure function that raises PayrollCalculationError on drift.
    # Extracted as a named helper so both paths (pass and drift-raises) can be
    # directly unit-tested via pytest.raises without monkeypatching.
    _raise_if_reconciliation_drift(
        gross, pretax_401k, fica_ss, fica_medicare, federal_withholding, net_pay
    )

    Pass additional_medicare_not_modeled to the PaystubLineItem return value.

    IMPORTANT — what does NOT change:
    - The FICA block (remaining_cap / ss_taxable / fica_ss / fica_medicare) — CORRECT and UNCHANGED.
    - The calculate() function signature — unchanged; contribution_401k_override stays.
    - The pretax_401k calculation — unchanged.
    - The _resolved_hours(), _money(), _CENTS — unchanged.
  </action>

  <verify>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run python -c "
from decimal import Decimal
from app.pipeline.calculate import calculate, PayrollCalculationError, _raise_if_reconciliation_drift
from app.db.seed import seed

seeded = seed(dry_run=True)
maria = next(e for e in seeded.employees if e.full_name == 'Maria Chen')

# CALC-07: net is real net
item = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('0'),
                  'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                 maria)
assert item.federal_withholding != Decimal('0'), 'federal_withholding must be real'
expected_net = (item.gross_pay - item.pretax_401k - item.fica_ss - item.fica_medicare - item.federal_withholding).quantize(Decimal('0.01'))
assert item.net_pay == expected_net, f'net_pay {item.net_pay} != {expected_net}'

# CALC-01: OT at 1.5x
maria_ot = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('5'),
                      'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                     maria)
expected_gross = (maria.hourly_rate * Decimal('40') + maria.hourly_rate * Decimal('1.5') * Decimal('5')).quantize(Decimal('0.01'))
assert maria_ot.gross_pay == expected_gross, f'OT gross {maria_ot.gross_pay} != {expected_gross}'

# D-03: OT explicit-only
maria_no_ot = calculate({'hours_regular': Decimal('45'), 'hours_overtime': Decimal('0'),
                         'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                        maria)
expected_straight = (maria.hourly_rate * Decimal('45')).quantize(Decimal('0.01'))
assert maria_no_ot.gross_pay == expected_straight, f'Straight 45h gross {maria_no_ot.gross_pay} != {expected_straight}'

# CALC-02: salaried leave pay (FIX A — frequency-independent /2080 form)
james = next(e for e in seeded.employees if e.full_name == 'James Okafor')
item_no_leave = calculate({'hours_regular': Decimal('0'), 'hours_overtime': Decimal('0'),
                           'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                          james)
item_with_leave = calculate({'hours_regular': Decimal('0'), 'hours_overtime': Decimal('0'),
                             'hours_vacation': Decimal('8'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                            james)
assert item_with_leave.gross_pay > item_no_leave.gross_pay, 'Leave pay must increase salaried gross'

# FIX A + R2-6 invariance: /2080 leave pay is identical at p=52, p=26, p=24, p=12
import uuid
from app.models.roster import Employee
def _make_emp(p):
    return Employee(
        id=uuid.uuid4(), business_id=uuid.uuid4(), full_name='SalTest', known_aliases=[],
        pay_type='salary', hourly_rate=None, annual_salary=Decimal('52000'),
        retirement_contribution_pct=Decimal('0'),
        filing_status='single', step_2_checkbox=False,
        step_3_dependents=Decimal('0'), step_4a_other_income=Decimal('0'), step_4b_deductions=Decimal('0'),
        ytd_ss_wages=Decimal('0'), pay_periods_per_year=p,
    )
leave_hours_input = {'hours_regular': Decimal('0'), 'hours_overtime': Decimal('0'),
                     'hours_vacation': Decimal('8'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')}
no_leave_input = {k: Decimal('0') for k in leave_hours_input}
emp_52 = _make_emp(52)
emp_26 = _make_emp(26)
emp_24 = _make_emp(24)
emp_12 = _make_emp(12)
delta_52 = calculate(leave_hours_input, emp_52).gross_pay - calculate(no_leave_input, emp_52).gross_pay
delta_26 = calculate(leave_hours_input, emp_26).gross_pay - calculate(no_leave_input, emp_26).gross_pay
delta_24 = calculate(leave_hours_input, emp_24).gross_pay - calculate(no_leave_input, emp_24).gross_pay
delta_12 = calculate(leave_hours_input, emp_12).gross_pay - calculate(no_leave_input, emp_12).gross_pay
assert delta_52 == delta_26 == delta_24 == delta_12, f'Leave pay NOT frequency-independent: 52={delta_52} 26={delta_26} 24={delta_24} 12={delta_12}'
assert delta_52 == Decimal('200.00'), f'Expected 200.00 (52000/2080*8), got {delta_52}'

# FIX B + R2-2: Additional Medicare flag — realistic SS-capped fixtures only
emp_at_cap = Employee(
    id=uuid.uuid4(), business_id=uuid.uuid4(), full_name='AtCap', known_aliases=[],
    pay_type='hourly', hourly_rate=Decimal('500.00'), annual_salary=None,
    retirement_contribution_pct=Decimal('0'),
    filing_status='single', step_2_checkbox=False,
    step_3_dependents=Decimal('0'), step_4a_other_income=Decimal('0'), step_4b_deductions=Decimal('0'),
    ytd_ss_wages=Decimal('184500'), pay_periods_per_year=52,
)
item_cap = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('0'),
                      'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                     emp_at_cap)
# ytd_ss_wages=184500 (at cap) + gross=20000 = 204500 > 200000 — MUST fire
assert item_cap.additional_medicare_not_modeled is True, f'Must fire when YTD cap + high gross > 200k: got {item_cap.additional_medicare_not_modeled}'

emp_normal = Employee(
    id=uuid.uuid4(), business_id=uuid.uuid4(), full_name='Normal', known_aliases=[],
    pay_type='hourly', hourly_rate=Decimal('100.00'), annual_salary=None,
    retirement_contribution_pct=Decimal('0'),
    filing_status='single', step_2_checkbox=False,
    step_3_dependents=Decimal('0'), step_4a_other_income=Decimal('0'), step_4b_deductions=Decimal('0'),
    ytd_ss_wages=Decimal('0'), pay_periods_per_year=52,
)
item_normal = calculate({'hours_regular': Decimal('40'), 'hours_overtime': Decimal('0'),
                         'hours_vacation': Decimal('0'), 'hours_sick': Decimal('0'), 'hours_holiday': Decimal('0')},
                        emp_normal)
# ytd_ss_wages=0 + gross=4000 << 200000 — must NOT fire
assert item_normal.additional_medicare_not_modeled is False, 'Must not fire for normal employee'

# R2-3: _raise_if_reconciliation_drift is a named importable helper
assert callable(_raise_if_reconciliation_drift), '_raise_if_reconciliation_drift must be importable'

# R2-3: passing case — correct values do not raise
try:
    g = Decimal('1000.00')
    p401k = Decimal('40.00')
    ss = Decimal('62.00')
    med = Decimal('14.50')
    fed = Decimal('54.08')
    net = g - p401k - ss - med - fed
    _raise_if_reconciliation_drift(g, p401k, ss, med, fed, net)
    print('_raise_if_reconciliation_drift pass path OK')
except PayrollCalculationError as e:
    assert False, f'Pass path should not raise: {e}'

# R2-3: drift case — deliberately wrong net triggers raise
import contextlib
raised = False
try:
    _raise_if_reconciliation_drift(
        Decimal('1000.00'), Decimal('40.00'), Decimal('62.00'),
        Decimal('14.50'), Decimal('54.08'), Decimal('999.99')  # wrong net — drift of ~$170.57
    )
except PayrollCalculationError:
    raised = True
assert raised, 'R2-3: _raise_if_reconciliation_drift must raise PayrollCalculationError on drift'
print('_raise_if_reconciliation_drift drift path OK')

# FIX C: no bare assert in source
import pathlib
src = pathlib.Path('app/pipeline/calculate.py').read_text()
assert 'assert _reconstructed' not in src, 'FIX C: bare assert must be replaced with _raise_if_reconciliation_drift call'
# PRE_FEDERAL_NET_LABEL must not exist
try:
    from app.pipeline.calculate import PRE_FEDERAL_NET_LABEL
    assert False, 'PRE_FEDERAL_NET_LABEL must be gone'
except ImportError:
    pass
# FICA constants must not be inline (grep excludes comment lines)
assert '_SS_RATE = Decimal' not in src, 'inline _SS_RATE must be removed'
assert '_SS_WAGE_BASE = Decimal' not in src, 'inline _SS_WAGE_BASE must be removed'
assert '_MEDICARE_RATE = Decimal' not in src, 'inline _MEDICARE_RATE must be removed'
print('calculate.py + contracts.py deepening OK')
"
    </automated>
  </verify>

  <done>
    calculate() produces real federal withholding and real net for a typical employee. Salaried employees with leave hours have higher gross. Leave pay is frequency-independent: p=52, p=26, p=24, p=12 all return the same leave_pay for the same annual_salary+leave_hours (R2-6: p=26 added). Additional Medicare flag fires when (ytd_ss_wages + gross) > $200k using REALISTIC capped SS YTD values — ytd_ss_wages=184500 (at the SS wage base cap) + high gross is the fires case (R2-2). _raise_if_reconciliation_drift() is a named module-level helper that raises PayrollCalculationError on drift; both the pass path and the drift-raises path are verified in the inline test above (R2-3). PaystubLineItem has additional_medicare_not_modeled: bool = False (additive, non-breaking). PRE_FEDERAL_NET_LABEL is gone. FICA constants import from tax_tables_2026.py.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_calculate.py — CALC-01/02/07/08 + salaried-with-leave integration + 4-frequency-invariance regression guard + ACTUAL reconciliation raise test (FIX A, R2-3/R2-6, Fix 9)</name>
  <files>tests/test_calculate.py</files>

  <read_first>
    tests/test_calculate.py (read the entire current file — the four existing tests must stay green; add BELOW them, never above or in place of).
    PATTERNS.md §"tests/test_calculate.py (extended)" — the new test functions for CALC-01/02/07/08 cases.
    app/pipeline/calculate.py (after Task 1 edits — confirm the /2080 leave pay formula, the _raise_if_reconciliation_drift() helper, and the PayrollCalculationError class).
    app/db/seed.py — James Okafor (salary employee, weekly) for CALC-02 tests; Thomas Bergmann (high earner, biweekly) for SS straddle.
  </read_first>

  <behavior>
    Existing tests (all four) still pass without modification:
      test_uses_stored_default_when_no_override
      test_override_replaces_stored_rate_for_this_run
      test_override_does_not_mutate_employee
      test_zero_override_is_honored_not_treated_as_absent

    New test_hourly_overtime_at_1_5x: hourly employee with hours_overtime=5 → gross = rate*40 + rate*1.5*5 (CALC-01)
    New test_leave_hours_excluded_from_ot_threshold: hours_regular=40, hours_vacation=8, hours_overtime=0 → gross = rate*(40+8) straight time (D-03)
    New test_salaried_leave_pay_added_to_gross: salaried employee (James Okafor, weekly/52) with hours_vacation=8 → gross > annual/pay_periods (CALC-02)
    New test_salaried_no_leave_gross_unchanged: salaried employee with zero leave → gross == _money(annual/pay_periods) exactly (CALC-02 baseline)

    New test_salaried_leave_pay_frequency_invariant (FIX A + R2-6 — 4-frequency regression guard):
    The regression guard that ensures the /2080 implied-hourly form is correct and frequency-independent.
    Create four synthetic salaried employees with annual_salary=Decimal("52000") and pay_periods_per_year in (52, 26, 24, 12).
    R2-6: p=26 (biweekly) is ADDED. The original inverted formula produced a 4x error at p=24 (semi-monthly) and showed the bug was masked at p=52. p=26 (biweekly) is where the original bug was 4× off relative to p=52 — it must be in the invariant test.
    Call calculate() with 8 hours_vacation on each.
    Assert delta_52 == delta_26 == delta_24 == delta_12 == Decimal("200.00").
    Add comment:
    # FIX A + R2-6 regression guard: the prior period-proportion formula (period_salary * leave_h /
    # standard_h_per_period, where standard_h = 40 * p / 52) was INVERTED. For p=24 it produced
    # ~$2,166 instead of $200.00 (4.7x). For p=12 it produced ~$8,666 instead of $200.00 (18.8x).
    # For p=52 it was accidentally correct (40*52/52 = 40 = actual weekly standard hours).
    # R2-6 ADDS p=26 (biweekly): the formula was also wrong at p=26 (40*26/52=20h/period, but
    # the real biweekly standard is 80h/2wks = the /2080 form is also correct at p=26).
    # p=26 was the original reported bug context (4x off) — always include it in the invariant test.
    # This test catches any re-introduction of a frequency-dependent denominator by asserting
    # the invariant: leave_pay is identical across ALL valid pay_periods_per_year values.

    New test_salaried_with_leave_gross_integration (Fix 9 — end-to-end integration test):
    salary_employee with 8 hours_vacation → federal_withholding >= no-leave case (higher gross → higher or equal withholding)
    Add Fix 9 comment.

    New test_net_pay_is_real_net: net_pay == gross - pretax_401k - fica_ss - fica_medicare - federal_withholding, AND federal_withholding > 0 (CALC-07)
    New test_reconciliation_identity: reconstructed sum equals gross (CALC-08 normal passing case)

    New test_reconciliation_raises_on_drift (R2-3 — MUST use pytest.raises, NOT just source grep):
    R2-3 MANDATORY: The prior plan backed off to "if monkeypatching is complex, at minimum assert the class exists." A source grep proves "not this assert string" but does NOT prove that arithmetic drift raises. This test MUST exercise the ACTUAL raise path.
    Use _raise_if_reconciliation_drift() DIRECTLY (it is a named module-level helper, importable):
    (a) Passing path: call _raise_if_reconciliation_drift with a consistent set of values (net_pay = gross - pretax_401k - fica_ss - fica_medicare - federal_withholding). Assert no exception is raised.
    (b) Drift path: call _raise_if_reconciliation_drift with deliberately wrong net_pay (e.g. net_pay off by $1.00). Assert pytest.raises(PayrollCalculationError) is triggered.
    Both paths are mandatory. The source-grep check ("assert _reconstructed not in src") is KEPT as a secondary check that no bare assert remains, but is NO LONGER the primary test — pytest.raises on the drift path is.
    Add comment:
    # R2-3: _raise_if_reconciliation_drift() is a named pure helper (not monkeypatching needed).
    # Both paths are tested directly:
    # (a) pass path — correct arithmetic does not raise
    # (b) drift path — deliberately wrong net triggers pytest.raises(PayrollCalculationError)
    # This exercises the ACTUAL raise, not just a string in source code.
    # FIX C secondary check: source grep confirms no bare 'assert _reconstructed' remains.

    New test_fica_uses_gross_not_reduced_base (CALC-03): employee with 401k > 0 → fica_ss and fica_medicare use gross, not gross-pretax_401k
    New test_additional_medicare_flag_present (User Decision 1 + FIX B + R2-2 — realistic capped values):
    Flag-fires case: ytd_ss_wages=Decimal("184500") (at SS cap — maximum real value) + hourly_rate=Decimal("500.00"), 40 hours → gross=$20,000 → proxy=$204,500 > $200k. Assert additional_medicare_not_modeled is True.
    Flag-does-not-fire case: ytd_ss_wages=Decimal("0"), normal gross. Assert additional_medicare_not_modeled is False.
    Comment:
    # R2-2: ytd_ss_wages=$184,500 is the MAXIMUM possible SS YTD (2026 SS wage base cap).
    # Values above $184,500 are IMPOSSIBLE in a real run. A high current gross ($20k/period)
    # is used to push (ytd + gross) above $200k, testing the real proxy semantics.
    # The prior plan used ytd_ss_wages=197000 (impossible — above the cap). That tested
    # the Boolean expression without testing the real proxy: an employee can NEVER have
    # ytd_ss_wages=197000 in a real run, so the test was exercising dead code.
  </behavior>

  <action>
    Edit tests/test_calculate.py by APPENDING new test functions BELOW the four existing ones. Do NOT modify or remove any existing line.

    Add a section comment before the new block:
    "# ---- Phase 3 additions: CALC-01 / CALC-02 / CALC-07 / CALC-08 / Fix-A / R2-3 / R2-6 / Fix-9 ----"

    Add imports at the top of the new section (check if already present in the file):
      import uuid
      import pytest
      from app.pipeline.calculate import PayrollCalculationError, _raise_if_reconciliation_drift
      from app.pipeline.tax_tables_2026 import SS_RATE as _SS_RATE_REF, SS_WAGE_BASE as _SS_WAGE_BASE_REF, MEDICARE_RATE as _MEDICARE_RATE_REF

    Add salary_employee fixture (James Okafor — weekly salaried):
      @pytest.fixture()
      def salary_employee():
          seeded = seed(dry_run=True)
          return next(e for e in seeded.employees if e.pay_type == "salary" and e.pay_periods_per_year == 52)

    Add _make_salary_employee() helper for synthetic fixtures:
    Factory that creates Employee(pay_type="salary", annual_salary=provided, hourly_rate=None, pay_periods_per_year=provided). All W-4 fields default $0/False. uuid.uuid4() for id/business_id. Accepts annual_salary and pay_periods_per_year as required keyword args; filing_status defaults "single".

    Add _zero_hours() helper returning all-zero hours dict, and _leave_hours() returning dict with hours_vacation=Decimal("8") and rest zero, if not already present.

    test_hourly_overtime_at_1_5x (CALC-01):
    Assert item.gross_pay == _money(hourly_employee.hourly_rate * 40 + hourly_employee.hourly_rate * Decimal("1.5") * 5) with hours_overtime=Decimal("5"), hours_regular=Decimal("40").

    test_leave_hours_excluded_from_ot_threshold (CALC-01 + D-03):
    hours_regular=40, hours_vacation=8, hours_overtime=0.
    Assert gross == _money(hourly_employee.hourly_rate * (40 + 8)) — straight time on all hours, no OT premium.

    test_salaried_leave_pay_added_to_gross (CALC-02):
    salary_employee with _leave_hours() vs _zero_hours(). Assert with_leave.gross_pay > without_leave.gross_pay.

    test_salaried_no_leave_gross_unchanged (CALC-02 baseline):
    salary_employee with _zero_hours(). Assert gross_pay == _money(salary_employee.annual_salary / Decimal(salary_employee.pay_periods_per_year)).

    test_salaried_leave_pay_frequency_invariant (FIX A + R2-6 — 4-frequency regression guard):
    Use _make_salary_employee with annual_salary=Decimal("52000") at p=52, p=26, p=24, p=12.
    Compute leave_pay_delta = calculate(_leave_hours(), emp).gross_pay - calculate(_zero_hours(), emp).gross_pay for each.
    Assert delta_52 == delta_26 == delta_24 == delta_12 == Decimal("200.00").
    Include the full R2-6 comment block as specified in the behavior section.

    test_salaried_with_leave_gross_integration (Fix 9):
    salary_employee: compare federal_withholding with and without leave hours.
    Assert item_with_leave.gross_pay > item_no_leave.gross_pay.
    Assert item_with_leave.federal_withholding >= item_no_leave.federal_withholding.
    Include Fix 9 comment.

    test_net_pay_is_real_net (CALC-07):
    Calculate for a typical employee. Assert federal_withholding > Decimal("0").
    Assert net_pay == _money(item.gross_pay - item.pretax_401k - item.fica_ss - item.fica_medicare - item.federal_withholding).

    test_reconciliation_identity (CALC-08 normal case):
    Calculate for a typical employee. Reconstruct: _money(net_pay + pretax_401k + fica_ss + fica_medicare + federal_withholding + (state_withholding or Decimal("0"))). Assert reconstructed == gross_pay.

    test_reconciliation_raises_on_drift (R2-3 — MUST use pytest.raises on BOTH paths):
    (a) Passing path:
      gross = Decimal("1000.00")
      pretax_401k = Decimal("40.00")
      fica_ss = Decimal("62.00")
      fica_medicare = Decimal("14.50")
      federal_wh = Decimal("54.08")
      net = gross - pretax_401k - fica_ss - fica_medicare - federal_wh  # correct net
      _raise_if_reconciliation_drift(gross, pretax_401k, fica_ss, fica_medicare, federal_wh, net)
      # no exception raised — assert by reaching this line
    (b) Drift path:
      with pytest.raises(PayrollCalculationError):
          _raise_if_reconciliation_drift(
              Decimal("1000.00"), Decimal("40.00"), Decimal("62.00"),
              Decimal("14.50"), Decimal("54.08"),
              Decimal("999.99")  # deliberately wrong net — drift of ~$170.57
          )
    (c) Secondary source-grep check (FIX C):
      import pathlib
      src = pathlib.Path("app/pipeline/calculate.py").read_text()
      assert "assert _reconstructed" not in src, "FIX C: bare assert must not remain in source"
      assert issubclass(PayrollCalculationError, Exception)
    Include the R2-3 comment block per behavior section.

    test_fica_uses_gross_not_reduced_base (CALC-03):
    Use an employee with retirement_contribution_pct > 0 (James Okafor at 4%, or override).
    Assert item.fica_ss == _money(min(item.gross_pay, remaining_ss_cap) * _SS_RATE_REF) where remaining_ss_cap = _SS_WAGE_BASE_REF - employee.ytd_ss_wages.
    Assert item.fica_medicare == _money(item.gross_pay * _MEDICARE_RATE_REF).
    Both use gross_pay, not (gross_pay - pretax_401k).

    test_additional_medicare_flag_present (User Decision 1 + FIX B + R2-2 — realistic capped values):
    Flag-fires case:
      ytd_ss_wages=Decimal("184500"), hourly_rate=Decimal("500.00"), 40 hours → gross=$20,000 → proxy=$204,500 > $200k.
      Assert item.additional_medicare_not_modeled is True.
    Flag-does-not-fire case:
      ytd_ss_wages=Decimal("0"), hourly_rate=Decimal("100.00"), 40 hours → proxy=$4,000 << $200k.
      Assert item.additional_medicare_not_modeled is False.
    Include the R2-2 comment block per behavior section.
  </action>

  <verify>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_calculate.py -q
    </automated>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest -q
    </automated>
    <automated>
      cd "/Users/pnhek/usf msds/github/payroll_agent" && grep -c "frequency_invariant" tests/test_calculate.py && grep -c "delta_26" tests/test_calculate.py && grep -c "_raise_if_reconciliation_drift" tests/test_calculate.py && grep -c "pytest.raises" tests/test_calculate.py && grep -c "leave_gross_integration" tests/test_calculate.py && grep -c "assert _reconstructed" app/pipeline/calculate.py
    </automated>
  </verify>

  <done>
    uv run pytest tests/test_calculate.py -q exits with 0 failures. All 4 existing tests green. All new Phase 3 tests pass including:
    - test_salaried_leave_pay_frequency_invariant asserting delta_52 == delta_26 == delta_24 == delta_12 == Decimal("200.00") (FIX A + R2-6 regression guard with p=26 biweekly included)
    - test_reconciliation_raises_on_drift using pytest.raises(PayrollCalculationError) on the ACTUAL drift path via _raise_if_reconciliation_drift() directly (R2-3 — not just source grep)
    - test_additional_medicare_flag_present using realistic capped SS YTD (ytd_ss_wages=184500 + high gross for fires case; no impossible ytd_ss_wages above $184,500) (R2-2)
    - test_salaried_with_leave_gross_integration (Fix 9)
    Full suite uv run pytest -q is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No external trust boundaries in this plan | calculate.py is a pure function; all inputs are Pydantic-validated Employee objects with ge=0 constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | calculate.py arithmetic (401k base interaction) | mitigate | test_fica_uses_gross_not_reduced_base asserts FICA base is gross; the golden suite in Plan 02 also covers this |
| T-03-03-02 | Tampering | Reconciliation bypass (FIX C / R2-3) | mitigate | _raise_if_reconciliation_drift() is a named pure helper raising PayrollCalculationError — cannot be stripped by python -O unlike bare assert. Both pass and drift-raises paths are directly unit-tested via pytest.raises (R2-3 — not just source grep). |
| T-03-03-03 | Denial of Service | Additional Medicare silent under-withholding (FIX B / R2-2) | mitigate | User Decision 1 flag (additional_medicare_not_modeled) triggers on (ytd_ss_wages+gross) > $200k proxy; tests use REALISTIC capped SS YTD values (ytd_ss_wages=184500 max, not impossible 196000); documented lower-bound limitation in comment; observable and test-backed, not silent |
| T-03-03-04 | Information Disclosure | N/A | accept | Pure offline arithmetic; no PII emitted outside the return value |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No new deps added in this plan; the plan only edits existing files |

Note: ASVS V2/V3/V4/V6 do not apply. V5 input validation: Employee ge=0 constraints on W-4 fields are the upstream gate (already shipped in Phase 1). The reconciliation raise is a correctness backstop (CALC-08), not a security control. The Additional Medicare flag is a limitation-disclosure control per User Decision 1. The contracts.py addition (additional_medicare_not_modeled: bool = False) is additive/non-breaking.
</threat_model>

<verification>
cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_calculate.py -q
cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest -q
grep -c "PRE_FEDERAL_NET_LABEL" app/pipeline/calculate.py
grep -c "from app.pipeline.tax_tables_2026 import" app/pipeline/calculate.py
grep -c "federal_withholding_2026" app/pipeline/calculate.py
grep -v "^#" app/pipeline/calculate.py | grep -c "_SS_RATE = Decimal"
grep -c "additional_medicare_not_modeled" app/pipeline/calculate.py
grep -c "PayrollCalculationError" app/pipeline/calculate.py
grep -c "_raise_if_reconciliation_drift" app/pipeline/calculate.py
grep -c "frequency_invariant" tests/test_calculate.py
grep -c "delta_26" tests/test_calculate.py
grep -c "leave_gross_integration" tests/test_calculate.py
grep -c "assert _reconstructed" app/pipeline/calculate.py
grep -c "pytest.raises" tests/test_calculate.py
</verification>

<success_criteria>
1. uv run pytest tests/test_calculate.py -q exits with 0 failures.
2. All 4 original 401k-override tests still pass (no regression).
3. test_salaried_leave_pay_frequency_invariant passes — delta_52 == delta_26 == delta_24 == delta_12 == Decimal("200.00") (FIX A + R2-6: p=26 biweekly explicitly included).
4. test_reconciliation_raises_on_drift passes with BOTH paths tested: (a) passing path — _raise_if_reconciliation_drift with consistent values does not raise; (b) drift path — pytest.raises(PayrollCalculationError) on deliberately wrong net; (c) secondary source grep confirms no bare "assert _reconstructed" in source (R2-3).
5. test_net_pay_is_real_net passes — confirms federal_withholding > Decimal("0") for a typical employee.
6. test_reconciliation_identity passes — reconstructed sum equals gross (CALC-08 normal case).
7. test_fica_uses_gross_not_reduced_base passes — FICA base is gross (CALC-03).
8. test_salaried_leave_pay_added_to_gross passes — CALC-02 leave pay addition (James Okafor).
9. test_salaried_with_leave_gross_integration passes — end-to-end: leave → higher gross → higher or equal withholding (Fix 9).
10. test_additional_medicare_flag_present passes — fires case uses ytd_ss_wages=Decimal("184500") (at SS cap, REALISTIC) + high gross ($500/hr × 40h = $20k) to cross $200k proxy; does-not-fire case uses normal low YTD; NO impossible ytd_ss_wages values above $184,500 (R2-2).
11. PRE_FEDERAL_NET_LABEL is not importable from calculate.py (grep returns 0).
12. calculate.py imports FICA constants from tax_tables_2026 (no inline literals).
13. calculate.py has _raise_if_reconciliation_drift() as a named module-level helper (importable).
14. calculate.py reconciliation block calls _raise_if_reconciliation_drift() — no bare assert (grep for "assert _reconstructed" returns 0).
15. calculate.py carries additional_medicare_not_modeled flag logic with proxy-based trigger and R2-2 comment.
16. contracts.py PaystubLineItem has additional_medicare_not_modeled: bool = False field.
17. Full suite uv run pytest -q is green.
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-03-SUMMARY.md when done.
</output>
