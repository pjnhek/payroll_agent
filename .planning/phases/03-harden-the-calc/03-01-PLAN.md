---
phase: 03-harden-the-calc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/tax_tables_2026.py
  - app/pipeline/federal_withholding.py
autonomous: true
requirements:
  - CALC-04
  - CALC-05
  - CALC-06

must_haves:
  truths:
    - "tax_tables_2026.py imports cleanly with no DB or network access"
    - "All 6 Worksheet 1A bracket tables are present (3 statuses × 2 Step-2 branches)"
    - "married_separately maps to the Single/MFS table, never to MFJ"
    - "FICA constants (SS_RATE, SS_WAGE_BASE, MEDICARE_RATE) live in tax_tables_2026.py, not calculate.py"
    - "Module header carries both source URLs and retrieval date 2026-06-22"
    - "federal_withholding_2026 returns Decimal('0.00') when annualized wage is below the first bracket"
    - "federal_withholding_2026 never returns a negative Decimal (line_1i and line_3c floor at $0)"
    - "federal_withholding_2026 is importable by the eval with zero DB access (pure function)"
    - "The $54.08 hand-computation example (Single/Standard/Weekly/$800) matches what the engine returns"
  artifacts:
    - path: "app/pipeline/tax_tables_2026.py"
      provides: "All 2026 year-keyed federal + FICA constants"
      contains: "TAX_YEAR = 2026"
    - path: "app/pipeline/federal_withholding.py"
      provides: "Isolated Worksheet 1A pure function"
      exports: ["federal_withholding_2026"]
  key_links:
    - from: "app/pipeline/federal_withholding.py"
      to: "app/pipeline/tax_tables_2026.py"
      via: "import STANDARD_BRACKETS, STEP2_BRACKETS, STEP1_STANDARD"
      pattern: "from app.pipeline.tax_tables_2026 import"
    - from: "app/pipeline/federal_withholding.py"
      to: "app/models/roster.py"
      via: "Employee parameter (filing_status, step_2_checkbox, step_3_dependents, step_4a_other_income, step_4b_deductions, pay_periods_per_year)"
      pattern: "from app.models.roster import Employee"
---

<objective>
Create the two new pure-function modules: the dated year-keyed tax-constants module and the isolated Worksheet 1A federal withholding engine.

Purpose: These are the foundation of all Phase 3 correctness — the constants module is the audit point for every 2026 number; the withholding engine is the highest-bug-risk unit that the golden suite (Plan 02) and calculate.py deepening (Plan 03) both depend on.

Output: app/pipeline/tax_tables_2026.py (all 2026 bracket tables + FICA constants, dated header) and app/pipeline/federal_withholding.py (Worksheet 1A engine, pure function, per D-01 Decision 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-harden-the-calc/03-CONTEXT.md
@.planning/phases/03-harden-the-calc/03-RESEARCH.md
@.planning/phases/03-harden-the-calc/03-PATTERNS.md
@app/models/roster.py
@app/pipeline/calculate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create app/pipeline/tax_tables_2026.py — dated year-keyed constants (D-02, CALC-06)</name>
  <files>app/pipeline/tax_tables_2026.py</files>

  <read_first>
    RESEARCH.md §"MANDATORY DELIVERABLE 1: Worksheet 1A Bracket Tables" — ALL bracket rows for the 6 schedules (Tables 1A.1 and 1A.2 for MFJ-Standard, Single/MFS-Standard, HoH-Standard, MFJ-Step2, Single/MFS-Step2, HoH-Step2). Do NOT copy bracket numbers from memory — use EXACTLY the rows in RESEARCH.md Deliverable 1.
    RESEARCH.md §"1A.3 Step 1 Standard Amounts" — the $12,900 (MFJ) / $8,600 (Single/MFS/HoH) proxy amounts for Worksheet 1A line 1g.
    RESEARCH.md §"FICA Constants (2026)" — SS_RATE=0.062, SS_WAGE_BASE=184500, MEDICARE_RATE=0.0145.
    RESEARCH.md §"Architecture Patterns" Pattern 1 — the BracketRow NamedTuple, STANDARD_BRACKETS / STEP2_BRACKETS dict shape, STEP1_STANDARD dict, FICA constants.
    PATTERNS.md §"app/pipeline/tax_tables_2026.py" — exact imports pattern, module-level docstring convention (dated source-URL header), BracketRow definition, year-keying shape.
    app/pipeline/calculate.py lines 41–43 — the three inline FICA constants being migrated (values confirmed: _SS_RATE=0.062, _SS_WAGE_BASE=184500, _MEDICARE_RATE=0.0145).
  </read_first>

  <behavior>
    - Module imports cleanly: `from app.pipeline.tax_tables_2026 import SS_RATE, SS_WAGE_BASE, MEDICARE_RATE, STANDARD_BRACKETS, STEP2_BRACKETS, STEP1_STANDARD, TAX_YEAR, BracketRow`
    - TAX_YEAR == 2026
    - SS_RATE == Decimal("0.062"), SS_WAGE_BASE == Decimal("184500"), MEDICARE_RATE == Decimal("0.0145")
    - STEP1_STANDARD["married_jointly"] == Decimal("12900")
    - STEP1_STANDARD["single"] == Decimal("8600")
    - STEP1_STANDARD["married_separately"] == Decimal("8600")
    - STANDARD_BRACKETS["married_separately"] is the same object as STANDARD_BRACKETS["single"] (or identical rows) — MFS uses Single/MFS table per RESEARCH.md Pitfall #4
    - STEP2_BRACKETS["married_separately"] is the same object as STEP2_BRACKETS["single"]
    - All BracketRow.rate values are Decimal fractions (e.g., Decimal("0.12") for 12%), not percentages
    - The top bracket in each table has upper=None
    - Module header contains the string "irs.gov/pub/irs-pdf/p15t.pdf" and "ssa.gov/oact/cola/cbb.html" and "Retrieved: 2026-06-22"
    - Adding 2027 constants in a new file or new dict key does NOT require editing any 2026 constant row
  </behavior>

  <action>
    Create app/pipeline/tax_tables_2026.py following PATTERNS.md §tax_tables_2026.py and RESEARCH.md Architecture Patterns Pattern 1.

    Module-level docstring (CALC-06 MANDATORY — include verbatim source URLs + date):
    Title: "2026 Federal Tax Constants for Payroll Engine." Sources block: IRS Publication 15-T (2026) at irs.gov/pub/irs-pdf/p15t.pdf and SSA Contribution and Benefit Base at ssa.gov/oact/cola/cbb.html, Retrieved: 2026-06-22. OBBBA note: standard method only, qualified-tips/overtime deductions disclaimed.

    TAX_YEAR: int constant = 2026.

    BracketRow: NamedTuple with four fields in this order: lower (Decimal), upper (Decimal | None), base (Decimal), rate (Decimal). The rate field stores a FRACTION (Decimal("0.12") for the 12% bracket), not the percentage integer.

    STANDARD_BRACKETS: dict[str, list[BracketRow]] with keys "married_jointly", "single", "married_separately". Populate from RESEARCH.md Deliverable 1, Table 1A.1. The "married_separately" value MUST be the same list as "single" — alias them in the dict literal (per Pitfall #4: MFS and Single share one table). Transcribe all 8 rows for MFJ and all 8 rows for Single/MFS; include HoH rows under "head_of_household" with a comment "out of scope — no seeded HoH employees" for completeness. Do NOT omit rows — all 8 per schedule.

    STEP2_BRACKETS: dict[str, list[BracketRow]] with keys "married_jointly", "single", "married_separately". Populate from RESEARCH.md Deliverable 1, Table 1A.2. Same aliasing for "married_separately" → "single". Include HoH under "head_of_household" with out-of-scope comment.

    STEP1_STANDARD: dict[str, Decimal] — line 1g proxy amounts when step_2_checkbox is False. Keys: "married_jointly" → Decimal("12900"), "single" → Decimal("8600"), "married_separately" → Decimal("8600"), "head_of_household" → Decimal("8600"). Source: RESEARCH.md Deliverable 1, Section 1A.3.

    FICA constants (migrated per D-02, values unchanged from calculate.py lines 41–43):
    SS_RATE = Decimal("0.062")
    SS_WAGE_BASE = Decimal("184500")
    MEDICARE_RATE = Decimal("0.0145")

    Year-keying: the module IS the 2026 data. A future 2027 module would be tax_tables_2027.py imported by federal_withholding.py alongside this one. No selector function needed in Phase 3.

    Do NOT include any function — this is a pure constants module. No DB access, no network, no I/O.
  </action>

  <verify>
    <automated>
      cd /Users/pnhek/usf\ msds/github/payroll_agent && uv run python -c "
from app.pipeline.tax_tables_2026 import (
    TAX_YEAR, SS_RATE, SS_WAGE_BASE, MEDICARE_RATE,
    STANDARD_BRACKETS, STEP2_BRACKETS, STEP1_STANDARD, BracketRow
)
from decimal import Decimal
assert TAX_YEAR == 2026
assert SS_RATE == Decimal('0.062')
assert SS_WAGE_BASE == Decimal('184500')
assert MEDICARE_RATE == Decimal('0.0145')
assert STEP1_STANDARD['married_jointly'] == Decimal('12900')
assert STEP1_STANDARD['single'] == Decimal('8600')
assert STEP1_STANDARD['married_separately'] == Decimal('8600')
# MFS must alias the Single/MFS table
assert STANDARD_BRACKETS['married_separately'] is STANDARD_BRACKETS['single']
assert STEP2_BRACKETS['married_separately'] is STEP2_BRACKETS['single']
# All schedule present
for k in ['married_jointly', 'single', 'married_separately']:
    assert k in STANDARD_BRACKETS
    assert k in STEP2_BRACKETS
    assert len(STANDARD_BRACKETS[k]) == len(STEP2_BRACKETS['married_jointly'])
# rates are fractions, not percentages
for row in STANDARD_BRACKETS['single']:
    assert row.rate < Decimal('1'), f'rate {row.rate} looks like a percentage'
print('tax_tables_2026 OK')
"
    </automated>
  </verify>

  <done>
    tax_tables_2026.py exists, imports with no DB/network access, carries the dated source-URL header, exports TAX_YEAR=2026 and all 6 bracket schedule dicts, STEP1_STANDARD with correct proxy amounts, and the three FICA constants. "married_separately" aliases the Single/MFS rows. All BracketRow.rate values are decimal fractions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create app/pipeline/federal_withholding.py — isolated Worksheet 1A engine (CALC-05, D-01 Decision 2)</name>
  <files>app/pipeline/federal_withholding.py</files>

  <read_first>
    RESEARCH.md §"Worksheet 1A Computation Flow (End-to-End)" — the exact 1a→4b step sequence with floor conditions for line_1i and line_3c.
    RESEARCH.md §"MANDATORY DELIVERABLE 2: Rounding Convention" — Option A (carry cents via _money() at each step, never round to whole dollars mid-calc); the module header must document this choice.
    RESEARCH.md Architecture Patterns Pattern 2 — full code excerpt for federal_withholding_2026 including _money() local copy, _find_bracket() helper, step annotations.
    PATTERNS.md §"app/pipeline/federal_withholding.py" — imports pattern, _money() copy-verbatim instruction, filing-status table mapping, _find_bracket() reversed-list scan, function signature and docstring.
    PATTERNS.md §"Shared Patterns: Decimal + _money()" — the exact _CENTS and _money() implementation to copy locally (NOT imported from calculate.py).
    app/models/roster.py lines 57–70 — confirmed Employee field names: filing_status, step_2_checkbox, step_3_dependents, step_4a_other_income, step_4b_deductions, pay_periods_per_year.
    RESEARCH.md §"Common Pitfalls" — Pitfall #3 (Step-3 credit goes negative → floor at $0), Pitfall #4 (filing status mapping), Pitfall #5 (rounding accumulation), Pitfall #6 (self-derived oracle — forbidden).
    RESEARCH.md §"Hand-Computation Worked Example" — Single/Standard/Weekly/$800 → expected $54.08 (use as a smoke-test, NOT as a self-derived oracle).
  </read_first>

  <behavior>
    - federal_withholding_2026(Decimal("800.00"), single_std_weekly_emp) returns Decimal("54.08")
    - federal_withholding_2026(Decimal("0.00"), any_emp) returns Decimal("0.00") — zero wages, zero withholding
    - federal_withholding_2026 with step_3_dependents large enough to exceed tentative withholding returns Decimal("0.00"), not a negative
    - federal_withholding_2026 with step_2_checkbox=True uses STEP2_BRACKETS, not STANDARD_BRACKETS
    - federal_withholding_2026 with filing_status="married_separately" uses the Single/MFS table (same as "single")
    - federal_withholding_2026 with filing_status="married_jointly" uses the MFJ table
    - Never returns a Decimal less than Decimal("0.00")
    - Module has no DB access, no network calls, no side effects — pure function importable by eval
  </behavior>

  <action>
    Create app/pipeline/federal_withholding.py following PATTERNS.md §federal_withholding.py and RESEARCH.md Architecture Patterns Pattern 2.

    Imports (per PATTERNS.md):
    from __future__ import annotations
    from decimal import ROUND_HALF_UP, Decimal
    from app.models.roster import Employee
    from app.pipeline.tax_tables_2026 import STANDARD_BRACKETS, STEP2_BRACKETS, STEP1_STANDARD, TAX_YEAR

    Local _CENTS and _money() (PATTERNS.md §"_money() helper pattern" — copy VERBATIM from calculate.py, do NOT import it from calculate.py; this keeps federal_withholding.py independently importable by the eval without pulling in calculate.py's UUID/datetime imports).

    _find_bracket(annual_wage: Decimal, brackets: list) function: linear scan of the bracket list in reverse order; return the first row where annual_wage >= row.lower. Return brackets[0] as a fallback for wages below the first bracket lower bound. See PATTERNS.md §"Bracket lookup helper".

    federal_withholding_2026(federal_taxable_wages_this_period: Decimal, employee: Employee) -> Decimal:
    Docstring: state that the input is gross - pretax_401k (NOT raw gross), that it returns per-period withholding in cents (ROUND_HALF_UP), that it never returns negative, that rounding follows Option A (carry cents via _money() at each step — see RESEARCH.md Deliverable 2).

    Implement the EXACT 1a→4b flow from RESEARCH.md §"Worksheet 1A Computation Flow":

    Step 1 — annualize:
      p = Decimal(employee.pay_periods_per_year)
      status = employee.filing_status
      checkbox = employee.step_2_checkbox
      line_1a = federal_taxable_wages_this_period
      line_1c = _money(line_1a * p)
      line_1d = employee.step_4a_other_income
      line_1e = _money(line_1c + line_1d)
      line_1f = employee.step_4b_deductions
      line_1g = Decimal("0") if checkbox else STEP1_STANDARD[status]
      line_1h = _money(line_1f + line_1g)
      line_1i = max(Decimal("0"), _money(line_1e - line_1h))  — FLOOR AT $0

    Step 2 — bracket lookup:
      brackets = STEP2_BRACKETS[status] if checkbox else STANDARD_BRACKETS[status]
      row = _find_bracket(line_1i, brackets)
      line_2e = _money(line_1i - row.lower)
      line_2f = _money(line_2e * row.rate)
      line_2g = _money(row.base + line_2f)
      line_2h = _money(line_2g / p)

    Step 3 — Step-3 credit, floor at $0:
      line_3b = _money(employee.step_3_dependents / p)
      line_3c = max(Decimal("0"), _money(line_2h - line_3b))  — FLOOR AT $0, NEVER NEGATIVE

    Step 4 — final (no step_4c field on Employee; treat as $0 per RESEARCH.md A5):
      line_4b = line_3c

    return line_4b

    Add a comment above Step 4 explaining that step_4c_extra_per_period is not modeled (no Employee field for it); line_4b = line_3c is correct for all seeded employees.

    Do NOT use float anywhere. Do NOT import from calculate.py. Do NOT call tax_tables_2026 to produce expected values in tests (that is forbidden by Decision 2).
  </action>

  <verify>
    <automated>
      cd /Users/pnhek/usf\ msds/github/payroll_agent && uv run python -c "
from decimal import Decimal
from app.pipeline.federal_withholding import federal_withholding_2026
from app.db.seed import seed

seeded = seed(dry_run=True)
# Employee 1: Maria Chen — single, step_2=False, step_3=0, step_4a=0, step_4b=0, 52 periods
maria = next(e for e in seeded.employees if e.full_name == 'Maria Chen')

# Hand-computed: Single/Standard/Weekly/\$800 = \$54.08 (RESEARCH.md §Hand-Computation)
result = federal_withholding_2026(Decimal('800.00'), maria)
assert result == Decimal('54.08'), f'Expected 54.08, got {result}'

# Zero wages -> zero withholding
result0 = federal_withholding_2026(Decimal('0.00'), maria)
assert result0 == Decimal('0.00'), f'Expected 0.00, got {result0}'

# Step-3 floor: large credit must floor at 0.00 not go negative
import uuid
from app.models.roster import Employee
emp_big_credit = Employee(
    id=uuid.uuid4(), business_id=uuid.uuid4(), full_name='T', known_aliases=[],
    pay_type='hourly', hourly_rate=Decimal('10.00'), annual_salary=None,
    retirement_contribution_pct=Decimal('0'),
    filing_status='single', step_2_checkbox=False,
    step_3_dependents=Decimal('5000.00'),
    step_4a_other_income=Decimal('0'), step_4b_deductions=Decimal('0'),
    ytd_ss_wages=Decimal('0'), pay_periods_per_year=52,
)
result_floor = federal_withholding_2026(Decimal('150.00'), emp_big_credit)
assert result_floor == Decimal('0.00'), f'Expected floor 0.00, got {result_floor}'
assert result_floor >= Decimal('0'), 'withholding must never be negative'
print('federal_withholding_2026 smoke tests OK')
"
    </automated>
  </verify>

  <done>
    federal_withholding.py exists; federal_withholding_2026 returns $54.08 for the Single/Standard/Weekly/$800 hand-computed case; returns $0.00 for zero wages; floors at $0.00 when Step-3 credit exceeds tentative withholding; never returns a negative Decimal; is importable with zero DB/network access.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No external trust boundaries in this plan | Both modules are pure constants + arithmetic with no input from untrusted sources at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | tax_tables_2026.py bracket numbers | accept | Numbers transcribed from irs.gov/pub/irs-pdf/p15t.pdf (2026-06-22); golden tests in Plan 02 are the integrity control; source URL + retrieval date in module header enables re-audit |
| T-03-02 | Information Disclosure | N/A | accept | Pure offline arithmetic; no PII processed, no network, no DB |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 adds no runtime deps; python-taxes is dev-only (Plan 02); no install-time supply chain risk in this plan |

Note: ASVS V2/V3/V4/V6 do not apply (no auth, no network, no session, no crypto). V5 input validation is handled by Employee Pydantic ge=0 constraints on all W-4 fields (already in app/models/roster.py). The integrity control for this phase is the golden test suite (Plan 02), not a runtime security control.
</threat_model>

<verification>
uv run python -c "from app.pipeline.tax_tables_2026 import TAX_YEAR, SS_RATE, STANDARD_BRACKETS, STEP2_BRACKETS, STEP1_STANDARD; print('tax_tables OK')"
uv run python -c "from app.pipeline.federal_withholding import federal_withholding_2026; print('federal_withholding OK')"
uv run pytest -q  # full suite — 195+ existing tests must stay green (no calculate.py changes in this plan)
</verification>

<success_criteria>
1. tax_tables_2026.py imports cleanly with all 6 bracket schedules, STEP1_STANDARD proxy amounts, and FICA constants — all from RESEARCH.md Deliverable 1 (not from memory).
2. federal_withholding_2026 returns Decimal("54.08") for Single/Standard/Weekly/$800 (hand-computed per RESEARCH.md).
3. Both modules are pure functions with no DB, no network, no side effects.
4. Full pytest suite stays green (uv run pytest -q exits 0).
5. Module header of tax_tables_2026.py contains "irs.gov/pub/irs-pdf/p15t.pdf" and "Retrieved: 2026-06-22".
</success_criteria>

<output>
Create .planning/phases/03-harden-the-calc/03-01-SUMMARY.md when done.
</output>
