---
phase: 20-exactly-once-send
plan: 17
type: execute
wave: 11
depends_on: [20-14, 20-16]
files_modified:
  - app/db/repo/emails.py
  - tests/test_send_idempotency.py
autonomous: true
gap_closure: true
requirements: [SEND-01]
must_haves:
  truths:
    - "SEND-01: a sent confirmation from epoch 0 cannot count as proof after the run advances to reply_epoch 1."
    - "The current-epoch proof lookup remains purpose- and send_state-scoped, so a sent row from another epoch is invisible while the epoch-1 slot remains eligible."
  artifacts:
    - path: app/db/repo/emails.py
      provides: "get_outbound_message_id with an em.epoch-to-payroll_runs.reply_epoch predicate"
    - path: tests/test_send_idempotency.py
      provides: "Hermetic SQL-shape and guarded live regression for sent proof after an epoch bump"
  key_links:
    - "get_outbound_message_id joins or correlates email_messages to payroll_runs on the same run_id and requires the outbound row epoch to equal the run's current reply_epoch."
    - "The delivery proof consumer receives None for a stale sent row and therefore does not suppress the new epoch's confirmation reservation."
---

<objective>
Close the SEND-01 sent-proof epoch leak without changing the already-verified immutable reservation path.

Purpose: a successful confirmation from an earlier reply epoch must not suppress a newly authorized confirmation after a human retrigger.
Output: one current-epoch SQL predicate, one SQL-shape guard, and one real-Postgres epoch regression.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-14-SUMMARY.md
@app/db/repo/emails.py
@tests/test_send_idempotency.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope sent-proof lookup to the run's current epoch</name>
  <files>app/db/repo/emails.py, tests/test_send_idempotency.py</files>
  <read_first>Read get_outbound_message_id, get_outbound_for_round, the existing current-epoch correlated predicates in app/db/repo/emails.py, and the hermetic FakeConnection/live-Postgres epoch tests in tests/test_send_idempotency.py.</read_first>
  <behavior>
    - A sent outbound row matches get_outbound_message_id only when its purpose, direction, send_state, run_id, and epoch all match the run's current reply_epoch.
    - A sent epoch-0 confirmation returns no proof after the run is advanced to epoch 1, while a sent epoch-1 confirmation is returned.
    - The SQL-shape regression fails if the current-epoch comparison is removed.
  </behavior>
  <action>Per SEND-01 and D-07, change only get_outbound_message_id's proof query so email_messages.epoch is compared to payroll_runs.reply_epoch for the same run, preserving the existing purpose validation, outbound direction, sent-state filter, ordering, and parameterization. Mirror the existing get_outbound_for_round epoch predicate rather than inventing a second epoch source. Add a hermetic test named test_get_outbound_message_id_sql_shape_requires_current_epoch that asserts the recorded SQL contains the payroll_runs reply_epoch comparison, and add a guarded integration/queueproof test named test_sent_confirmation_proof_is_scoped_to_current_reply_epoch: create a run, persist and mark an epoch-0 confirmation sent, advance reply_epoch to 1 through the existing human context-clear seam, prove the epoch-0 Message-ID is not returned, persist an epoch-1 sent confirmation, and prove only the epoch-1 Message-ID is returned. Do not alter snapshot schema, provider calls, or the already-verified unconfirmed lookup.</action>
  <acceptance_criteria>The sent-proof repository lookup cannot treat a prior-epoch confirmation as delivery proof, and the live regression proves an epoch-1 confirmation is not suppressed by epoch-0 history.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_send_idempotency.py</automated></verify>
  <done>Both the SQL-shape guard and the guarded epoch regression exercise the exact proof lookup and fail if its current-epoch predicate disappears.</done>
</task>

<task type="auto">
  <name>Task 2: Reconfirm the delivery guard's current-epoch call contract</name>
  <files>tests/test_send_idempotency.py</files>
  <read_first>Read app/pipeline/delivery.py's already-sent confirmation branch and the existing tests that distinguish get_outbound_message_id proof from get_unconfirmed_outbound ambiguity.</read_first>
  <action>Keep the verified delivery call site purpose-aware and add a focused regression named `test_delivery_confirmation_uses_current_epoch_sent_proof` around the epoch-1 branch that invokes the same repository proof seam rather than a broad run-level query. The test must assert that the stale epoch-0 sent Message-ID is not treated as the current delivery, while the current epoch's sent proof is treated as already delivered. Cite D-04 and D-07 in the test boundary to document that this is only proof selection; it must not redraft, regenerate a PDF, mint a new key, or change the immutable snapshot contract.</action>
  <acceptance_criteria>The confirmation delivery path consumes the corrected current-epoch proof result and retains the existing frozen replay behavior for all other epochs.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_send_idempotency.py tests/test_delivery.py</automated></verify>
  <done>The current-epoch regression covers the repository-to-delivery proof link, not merely the SQL string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Postgres proof query -> delivery guard | Historical outbound rows influence whether a confirmation is considered sent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-17-01 | Tampering | `get_outbound_message_id` | high | mitigate | Require `email_messages.epoch = payroll_runs.reply_epoch` and prove both stale suppression and current-epoch visibility against Postgres. |
| T-20-17-02 | Denial of service | confirmation delivery proof branch | medium | mitigate | Keep purpose/send-state filters and add a non-vacuous epoch-1 sent-proof assertion so valid current delivery remains visible. |
| T-20-17-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; retain the repository's existing slopcheck and human package gate policy. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_send_idempotency.py tests/test_delivery.py</automated>
<automated>uv run ruff check app/db/repo/emails.py tests/test_send_idempotency.py</automated>
</verification>

<success_criteria>
SEND-01 proof lookup is current-epoch scoped in production SQL, and a sent epoch-0 confirmation cannot suppress the epoch-1 confirmation path.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-17-SUMMARY.md` when done.</output>
