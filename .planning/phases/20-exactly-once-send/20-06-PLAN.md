---
phase: 20-exactly-once-send
plan: "06"
type: execute
wave: 7
depends_on: [20-04, 20-05]
files_modified:
  - app/routes/runs.py
  - app/templates/run_detail.html
  - app/static/style.css
  - tests/test_dashboard.py
  - tests/test_needs_operator.py
autonomous: true
requirements: [SEND-03]
must_haves:
  truths:
    - "D-08: stale or non-replayable delivery uncertainty appears as needs_operator delivery review, never a silent approved/error state."
    - "D-09: Mark delivered performs no provider call, while typed authorization creates one clearly distinct human-approved send slot."
    - "D-10: review shows safe basis and exact frozen artifact without raw provider request/response dumps."
    - "D-11: human authorization copies the original frozen snapshot bytes rather than current payroll/contact values."
  artifacts:
    - app/routes/runs.py
    - app/templates/run_detail.html
    - app/static/style.css
  key_links:
    - "POST actions use validation, CAS, transaction, enqueue, then post-commit wake; template receives only safe review projection."
---

<objective>
Deliver the human delivery-review controls and a small progressive enhancement for legible durable status.

Purpose: make non-replayable ambiguity actionable without exposing provider diagnostics or silently sending again.
Output: bounded delivery-review card, two safe actions, and compact no-build-step queue enhancement.

Artifacts this phase produces: bounded review readers from Plan 01; `GET /runs/{run_id}/delivery-review/email`; `GET /runs/{run_id}/delivery-review/attachments/{attachment_id}`; review projection/action routes; and a server-rendered delivery-review card with progressive queue status presentation.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-09-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-05-SUMMARY.md
@.planning/todos/pending/260623-02-frontend-progressive-enhancement.md
@app/routes/runs.py
@app/templates/run_detail.html
@app/static/style.css
@tests/test_dashboard.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded delivery-review projection and transactional operator actions</name>
  <files>app/routes/runs.py, tests/test_needs_operator.py, tests/test_dashboard.py</files>
  <read_first>Read _safe_run_for_browser, _safe_run_with_queue_projection, resolve, retrigger, run_detail, and the existing needs_operator route tests.</read_first>
  <behavior>
    - A delivery-review projection allowlists recipient, subject, reservation time, attempt count, safe category, Message-ID/key, and frozen artifact references only.
    - Mark delivered settles without calling gateway and redirects safely on stale/double submissions.
    - Authorize a new confirmation requires an exact server-validated acknowledgement, clones original snapshot bytes into a distinct human-authorized epoch/slot, enqueues one job transactionally, then wakes after commit.
    - The frozen email route and attachment-stream route each prove run ownership plus delivery-review eligibility before reading bounded snapshot fields/bytes, return a safe Content-Disposition filename, and never regenerate a PDF or consult mutable payroll/contact data.</behavior>
  <action>Implement dedicated route/service actions for D-08 through D-11 using the existing resolve/retrigger CAS and redirect discipline. Build a separate safe review projection; exclude raw provider requests, responses, exception text, queue identifiers, attachment bytes, and arbitrary form content. Add `GET /runs/{run_id}/delivery-review/email`, which validates that the run is in the delivery-review state and belongs to its frozen reservation before returning stored subject/body/thread facts, and `GET /runs/{run_id}/delivery-review/attachments/{attachment_id}`, which validates the same run/review/snapshot ownership and streams only the stored attachment bytes with a sanitized ASCII-safe Content-Disposition filename. Both readers must use Plan 01's bounded snapshot APIs, not current payroll, roster, contact, PDF generation, or raw provider diagnostics. `retry now` must call only `advance_existing_send_job_due_now` inside the caller transaction, wake only after its commit, and never call the gateway, enqueue another job, or advance a leased/expired reservation. `Mark delivered` records a bounded resolution with no provider call. The new-authorization action must validate the exact acknowledgement server-side, make a visibly/auditably distinct human slot, copy only the original persisted snapshot/attachment bytes, enqueue in the same transaction, and wake after commit. Add stale/lost-CAS/double-submit, retry-now fence/cutoff, no-provider-call, byte-identity, and historical-payroll-change tests.</action>
  <acceptance_criteria>Delivery ambiguity is reviewable through exactly two explicit outcomes and bounded frozen-email/attachment evidence routes; neither action or reader can leak raw diagnostics, bypass the queue, silently rebuild/send current payroll data, or return an artifact outside its run/review ownership.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_needs_operator.py tests/test_dashboard.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Route tests prove typed authorization is required and generates a distinct slot from frozen bytes, mark-delivered and retry-now never send synchronously, and frozen email/PDF attachment responses remain byte-identical after later payroll/contact changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the review card with narrow progressive enhancement</name>
  <files>app/templates/run_detail.html, app/static/style.css, tests/test_dashboard.py</files>
  <read_first>Read existing needs_operator banner/forms, sent-email card, queue badge markup/polling, style rules near sent-email-card, and the selected frontend-progressive-enhancement todo.</read_first>
  <behavior>
    - The card presents the D-10 safe facts and links/serves frozen email/PDF artifacts without raw provider dumps.
    - The two actions are plainly labeled and the authorization acknowledgement is visible before submission.
    - Existing vanilla status polling improves this review surface without an SPA, bundler, or automatic recovery action.</behavior>
  <action>Add a focused delivery-review card beside existing status/banner patterns, with clear Mark delivered and Authorize a new confirmation controls and accessible explanatory copy about duplicate risk. Render only the route's allowlisted fields and frozen artifact links. Apply the folded frontend progressive-enhancement todo only as a small vanilla-JS/status-card improvement that makes queued/review state legible; retain server-rendered no-build architecture, bounded polling, and read-only behavior. Add template assertions that refuse raw diagnostic/provider-data names and verify the card/action labels and bounded queue presentation.</action>
  <acceptance_criteria>The review UI is understandable in a browser, shows the frozen evidence and explicit choices, and cannot expose unbounded provider data or initiate recovery by polling.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_dashboard.py tests/test_needs_operator.py -q</automated></verify>
  <done>The rendered detail view has a tested safe delivery-review card and preserves the existing compact durable queue feedback without introducing a frontend build step.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Form tampering creates a duplicate without conscious operator authorization | Elevation of privilege | high | mitigate | D-09 exact server-side acknowledgement, transactional CAS, and distinct audit slot. |
| Provider diagnostics or payload bytes are rendered in the browser | Information disclosure | high | mitigate | D-10 allowlisted review projection and template negative-exposure tests. |
| Retry-now races active work or invokes provider directly | Tampering | high | mitigate | D-01/D-07 existing-job-only due-now operation and post-commit wake. |
</threat_model>

<verification>
Run `uv run pytest tests/test_dashboard.py tests/test_needs_operator.py tests/test_send_idempotency.py -q`, then `uv run mypy app/routes/runs.py` and complete the manual browser check in 20-VALIDATION.md before phase verification.
</verification>

<success_criteria>
An operator can understand and resolve every unsafe delivery ambiguity through safe, explicit, auditable controls without a second automatic email.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-06-SUMMARY.md` when done.</output>
