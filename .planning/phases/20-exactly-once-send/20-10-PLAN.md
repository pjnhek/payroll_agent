---
phase: 20-exactly-once-send
plan: "10"
type: execute
wave: 6
depends_on: [20-01, 20-02, 20-03, 20-05, 20-11]
files_modified:
  - app/pipeline/clarification.py
  - tests/test_clarify.py
  - tests/test_alias_full_loop.py
autonomous: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-01/D-07: clarification initial sends, automatic recovery, and any retry-now work converge on the same executable identifier-only job."
    - "D-04: repeat clarification entry reads the existing snapshot/job before drafting, suggesting, or provider work."
    - "D-12/D-13: clarification envelope, threading, and attachments are frozen per purpose/round/epoch slot."
  artifacts:
    - app/pipeline/clarification.py
  key_links:
    - "Producer migration happens only after Plan 05's registered handler and Plan 11's purpose-safe settlement are executable."
---

<objective>
Move clarification and field-regression sends onto the now-executable immutable reservation job path.

Purpose: every outbound purpose gets the same durable replay contract without turning a clarification into a confirmation or alias approval.
Output: clarification producer handoff, RFC-thread/round preservation, and alias-safe replay regressions.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-01-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-03-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-05-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-11-SUMMARY.md
@app/pipeline/clarification.py
@tests/test_clarify.py
@tests/test_alias_full_loop.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reserve and enqueue standard clarification only after the shared consumer exists</name>
  <files>app/pipeline/clarification.py, tests/test_clarify.py</files>
  <read_first>Read clarify, defer_field_regression_clarification, the per-purpose/per-round idempotency guard, alias-candidate persistence, Plan 01 reserve API, Plan 02 identifier-only job contract, and Plans 05/11 handler-plus-settlement behavior.</read_first>
  <behavior>
    - Plain clarifications reserve their provider-ready envelope and enqueue one SEND_OUTBOUND job using the existing purpose/round/epoch slot.
    - A repeat entry for the same clarification round reuses its reservation, RFC Message-ID, In-Reply-To/References chain, and job; it does not re-draft, re-suggest, or mint a new anchor.
    - Round, pre-clarify snapshot, alias-candidate facts, and awaiting_reply remain coherent while delivery is pending and are not mistaken for confirmation delivery.</behavior>
  <action>Replace clarification.py's direct caller-argument `gateway.send_outbound` invocation with the same immutable reservation path used by confirmation, while keeping clarification-specific subject, recipient, stored reply-to, In-Reply-To/References headers, purpose, current round, and epoch as frozen envelope fields. This producer is intentionally after Plans 05 and 11: atomically persist alias-candidate and pre-clarify facts, reserve the clarification snapshot, enqueue the identifier-only SEND_OUTBOUND job, advance the clarification round from the reserved slot, and retain awaiting_reply; wake only after commit. Reentry must load the existing reservation/job before any draft, suggestion, or provider work. Preserve the round cap. Add standard clarification tests for replayed RFC threading, stable round/awaiting_reply state, and one job per slot. Plan 12 is the required post-migration gate that removes or fail-closes the now-unused compatibility method after both this producer and Plan 04 confirmation pass.</action>
  <acceptance_criteria>Standard clarification uses the executable durable send contract while preserving reply-routing and round boundaries.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_clarify.py tests/test_clarify_rounds.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Clarification tests prove retry uses the original snapshot/key/thread headers and one identifier-only job while remaining awaiting_reply.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry field-regression clarification through the same alias-safe path</name>
  <files>app/pipeline/clarification.py, tests/test_clarify.py, tests/test_alias_full_loop.py</files>
  <read_first>Read defer_field_regression_clarification, the completed standard clarification branch, field-regression fixtures, and alias full-loop assertions.</read_first>
  <behavior>
    - Field-regression clarification uses the same frozen purpose/round/epoch reservation and one identifier-only job without crossing into confirmation delivery.
    - Scheduled clarification delivery cannot create or confirm an alias.</behavior>
  <action>Route field-regression clarification through the completed reservation/enqueue seam without changing its purpose boundary, alias-candidate facts, or existing round cap. Add alias full-loop regressions that a field-regression replay preserves the original frozen snapshot/key/thread headers and that scheduled delivery cannot write a confirmed alias; only the established human reply-resolution flow may do so.</action>
  <acceptance_criteria>Field-regression and standard clarification share the immutable job path, while no delivery worker can cross the alias-confirmation boundary.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_clarify.py tests/test_alias_full_loop.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Both clarification forms are replay-safe and alias-safe under their original durable slots.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Producer creates a job before a safe consumer can settle it | Denial of service | high | mitigate | Depend on registered Plan 05 handler and purpose-safe Plan 11 settlement. |
| Repeated clarification draft changes a provider payload/key | Tampering | high | mitigate | D-04 load-before-draft reservation/job proof and RFC-thread identity tests. |
| Scheduled send writes a confirmed alias | Elevation of privilege | high | mitigate | Alias non-mutation tests and Plan 11 purpose-specific settlement. |
</threat_model>

<verification>
Run `uv run pytest tests/test_clarify.py tests/test_clarify_rounds.py tests/test_alias_full_loop.py tests/test_send_idempotency.py -q` and `uv run mypy app/pipeline/clarification.py`.
</verification>

<success_criteria>
Clarification begins only after a fully executable durable send consumer exists, and every replay remains frozen, thread-correct, and alias-safe.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-10-SUMMARY.md` when done.</output>
