---
phase: 20-exactly-once-send
plan: 20
type: execute
wave: 14
depends_on: [20-16, 20-19]
files_modified:
  - app/routes/runs.py
  - app/db/repo/jobs.py
  - tests/conftest.py
  - tests/test_repo_jobs_sql.py
  - tests/test_phase20_clarification_review.py
  - tests/test_phase20_fake_parity.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-09/D-16: mark-delivered and authorize-new-confirmation direct POST actions reject clarification delivery reviews without changing run status, creating a slot, or enqueueing work."
    - "D-01/D-07: generic confirmation retry-now requires confirmation review ownership and cannot advance a clarification SEND_OUTBOUND row; clarification retry remains on its dedicated purpose-checked seam."
    - "The exact configured bare `uv run mypy` command is green, including the Phase 20 fake connection argument and the typed route repository seam."
    - "All six verification gaps have default-running regressions, and the full repository suite remains green without a schema push or new dependency."
  artifacts:
    - path: app/routes/runs.py
      provides: "Typed module export and explicit confirmation review-kind guards on confirmation-only POST actions"
    - path: app/db/repo/jobs.py
      provides: "Confirmation purpose and DeliveryReview ownership predicates in advance_existing_send_job_due_now"
    - path: tests/conftest.py
      provides: "Fake confirmation retry purpose/review ownership parity"
    - path: tests/test_repo_jobs_sql.py
      provides: "Repository SQL regressions for confirmation-only retry fencing"
    - path: tests/test_phase20_clarification_review.py
      provides: "Negative direct-POST regressions for clarification versus confirmation actions"
    - path: tests/test_phase20_fake_parity.py
      provides: "Fake purpose-isolation and typed connection regression coverage"
  key_links:
    - "The generic retry route checks review_kind == confirmation and calls advance_existing_send_job_due_now only for a DeliveryReview-owned confirmation."
    - "The repository operation independently checks confirmation purpose, reserved send state, needs_operator status, DeliveryReview ownership, and the 20-hour reservation window."
    - "Clarification routes continue to call advance_existing_clarification_delivery_review_job_due_now and never use the confirmation operation."
---

<objective>
Close the purpose-isolation and strict-quality gaps, then prove the complete Phase 20 closure set together.

Purpose: a clarification delivery review must never be reconciled or retried through confirmation controls, and the repository must finish with the configured type gate green.
Output: confirmation-only route/repository/fake contracts, negative direct-POST proofs, and final integration/type/lint evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-16-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-19-SUMMARY.md
@app/routes/runs.py
@app/db/repo/jobs.py
@tests/test_phase20_clarification_review.py
@tests/test_repo_jobs_sql.py
@tests/test_phase20_fake_parity.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce confirmation-only review actions and retry ownership</name>
  <files>app/routes/runs.py, app/db/repo/jobs.py, tests/test_repo_jobs_sql.py, tests/test_phase20_clarification_review.py</files>
  <read_first>Read `_load_delivery_review`, `retry_delivery_now`, `mark_delivery_delivered`, `authorize_new_confirmation`, the clarification-specific retry route, `advance_existing_send_job_due_now`, and existing confirmation/clarification review tests. Read the module `__all__` declaration in app/routes/runs.py because the current mypy failures use runs_mod.repo.</read_first>
  <behavior>
    - Generic retry-now advances only a confirmation-owned DeliveryReview and refuses ClarificationDeliveryReview before repository mutation or wake.
    - mark-delivered and authorize-new-confirmation require review_kind == confirmation and are no-ops for clarification reviews, including direct POST requests.
    - The generic repository retry operation rejects clarification purpose, non-DeliveryReview ownership, non-needs_operator status, non-reserved state, leased jobs, and expired reservations while the dedicated clarification operation remains unchanged.
  </behavior>
  <action>Per D-01, D-07, D-09, D-16, and SEND-03, add an explicit `review_kind == "confirmation"` guard to retry_delivery_now, mark_delivery_delivered, and authorize_new_confirmation after the shared loader returns but before any CAS, clone, enqueue, or wake. Preserve the dedicated clarification retry/handled/reject routes and their purpose-specific repository method. Strengthen advance_existing_send_job_due_now in app/db/repo/jobs.py so its locked reservation query and locked run query require message.purpose = 'confirmation', message.send_state = 'reserved', payroll_runs.status = 'needs_operator', and payroll_runs.error_reason = 'DeliveryReview' before the existing open-window/pending-job update; it must never insert a job or alter a clarification row. Add the repository SQL assertion `test_advance_existing_send_job_due_now_requires_confirmation_review_ownership`. Add direct POST tests named `test_confirmation_retry_now_rejects_clarification_review`, `test_mark_delivery_delivered_rejects_clarification_review_without_mutation`, and `test_authorize_new_confirmation_rejects_clarification_review_without_mutation`; assert 303 response, unchanged status/error marker, unchanged snapshot/Message-ID, unchanged job count, no wake, and no provider call. Export the imported repository seam in runs.py's `__all__` so the existing runs_mod.repo test monkeypatches are type-visible without changing runtime routing behavior.</action>
  <acceptance_criteria>Clarification reviews cannot be retried, marked delivered, or cloned through confirmation endpoints, and the generic repository seam independently enforces confirmation purpose and review ownership.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_clarification_review.py tests/test_repo_jobs_sql.py tests/test_dashboard.py</automated></verify>
  <done>Direct POST and repository SQL tests prove purpose isolation before any mutation, while clarification keeps its dedicated same-row retry path.</done>
</task>

<task type="auto">
  <name>Task 2: Close fake typing and run the final integration/type-check gate</name>
  <files>tests/conftest.py, tests/test_phase20_fake_parity.py</files>
  <read_first>Read the fake advance_existing_send_job_due_now and clarification retry methods, the Phase 20 fake parity tests around their connection sentinel, and the configured pyproject.toml mypy overrides. Read all new closure test names before updating the final command list.</read_first>
  <action>Mirror the production confirmation-only retry ownership checks in InMemoryRepo.advance_existing_send_job_due_now while retaining the clarification method's separate purpose/review contract. Add `test_fake_confirmation_retry_rejects_clarification_review` to prove no job timing, status, snapshot, or Message-ID mutation. Fix the test-only fake connection argument in `test_fake_clarification_review_retry_advances_the_same_row_only` using an explicit typing cast for the sentinel accepted by the fake seam; do not weaken production signatures or add a global mypy ignore. Then run the complete closure evidence in this order: focused Phase 20 closure tests, `uv run ruff check` on every modified Python file, bare `uv run mypy`, and bare `uv run pytest -q`. Also run the guarded `integration and queueproof` command for the affected files and report skips as unavailable evidence when database configuration is absent. No schema push, dependency install, or hand-maintained requirements file is part of this closure.</action>
  <acceptance_criteria>The fake cannot advance clarification through the confirmation retry operation; the sentinel remains type-safe; Ruff, bare mypy, the full suite, and the guarded live command produce explicit results.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_clarification_review.py tests/test_phase20_fake_parity.py tests/test_repo_jobs_sql.py tests/test_send_idempotency.py tests/test_queue_durability.py tests/test_queue_drain.py</automated>
<automated>uv run ruff check app/routes/runs.py app/db/repo/jobs.py app/db/repo/job_settlement.py app/db/repo/emails.py app/queue/handlers/send_outbound.py app/queue/drain.py tests/conftest.py tests/test_phase20_clarification_review.py tests/test_phase20_fake_parity.py tests/test_repo_jobs_sql.py tests/test_send_idempotency.py tests/test_queue_durability.py tests/test_queue_drain.py</automated>
<automated>uv run mypy</automated>
<automated>uv run pytest -q</automated>
<automated>uv run pytest -q -m 'integration and queueproof' tests/test_send_idempotency.py tests/test_queue_durability.py tests/test_queue_drain.py tests/test_threading.py</automated></verify>
  <done>All six reported gaps have executable regression coverage, the repository-wide bare mypy command exits successfully, the full suite is green, and any unavailable live-Postgres evidence is explicitly distinguishable from a pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser POST -> review repository seam | Untrusted route input could select the wrong delivery purpose or create a second slot. |
| In-memory fake -> quality gate | A permissive fake could hide production purpose or type errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-20-01 | Elevation of privilege | confirmation review POST routes | critical | mitigate | Require explicit confirmation review kind before CAS, clone, enqueue, or wake; negative direct-POST tests assert no mutation. |
| T-20-20-02 | Tampering | advance_existing_send_job_due_now | high | mitigate | Require confirmation purpose plus DeliveryReview-owned needs_operator state under locks; keep clarification on its dedicated operation. |
| T-20-20-03 | Repudiation | fake repository parity | medium | mitigate | Mirror purpose ownership and append no fake evidence on rejected clarification retry. |
| T-20-20-04 | Denial of service | bare mypy quality gate | medium | mitigate | Fix the typed export/cast at the specific Phase 20 seams and execute the configured command without suppression. |
| T-20-20-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; retain the repository's existing slopcheck and human package gate policy. |
</threat_model>

<verification>
<automated>uv run pytest -q</automated>
<automated>uv run ruff check app/routes/runs.py app/db/repo/jobs.py app/db/repo/job_settlement.py app/db/repo/emails.py app/queue/handlers/send_outbound.py app/queue/drain.py tests/conftest.py tests/test_phase20_clarification_review.py tests/test_phase20_fake_parity.py tests/test_repo_jobs_sql.py tests/test_send_idempotency.py tests/test_queue_durability.py tests/test_queue_drain.py</automated>
<automated>uv run mypy</automated>
</verification>

<success_criteria>
The six verification gaps are closed: current-epoch proof and stale-job fencing are enforced, invalid leases are durably retired, confirmation actions are purpose-isolated, and the configured quality gates are green.
</success_criteria>

## Artifacts this phase produces

- Symbols and production seams:
  - `get_outbound_message_id` current-epoch proof predicate in `app/db/repo/emails.py`.
  - Handler current-epoch validation in `app/queue/handlers/send_outbound.py`.
  - Locked reservation/run epoch validation and invalid-context retirement helper in `app/db/repo/job_settlement.py`.
  - `SettlementOutcome.LOST_LEASE` and `SettlementOutcome.INVALID_CONTEXT` plus matching drain outcomes in `app/queue/drain.py`.
  - Confirmation-purpose/review-ownership predicates in `advance_existing_send_job_due_now`.
  - Type-visible `repo` module export in `app/routes/runs.py`.
- Regression tests:
  - `test_get_outbound_message_id_sql_shape_requires_current_epoch`.
  - `test_sent_confirmation_proof_is_scoped_to_current_reply_epoch`.
  - `test_delivery_confirmation_uses_current_epoch_sent_proof`.
  - `test_send_handler_noops_before_gateway_for_stale_epoch`.
  - `test_delivery_settlement_retires_stale_epoch_without_current_reservation_mutation`.
  - `test_final_send_lease_reap_retires_stale_epoch_without_current_review_mutation`.
  - `test_fake_stale_epoch_send_settlement_is_invalid_context_no_mutation`.
  - `test_fake_stale_epoch_final_lease_is_invalid_context_no_mutation`.
  - `test_drain_invalid_context_durably_retires_lease_before_token_discard`.
  - `test_drain_distinguishes_lost_lease_from_invalid_context`.
  - `test_invalid_context_settlement_retires_exact_leased_row`.
  - `test_advance_existing_send_job_due_now_requires_confirmation_review_ownership`.
  - `test_confirmation_retry_now_rejects_clarification_review`.
  - `test_mark_delivery_delivered_rejects_clarification_review_without_mutation`.
  - `test_authorize_new_confirmation_rejects_clarification_review_without_mutation`.
  - `test_fake_confirmation_retry_rejects_clarification_review` and the typed-sentinel correction in `tests/test_phase20_fake_parity.py`.
- Files touched by closure plans:
  - `app/db/repo/emails.py`
  - `app/db/repo/job_settlement.py`
  - `app/db/repo/jobs.py`
  - `app/queue/handlers/send_outbound.py`
  - `app/queue/drain.py`
  - `app/routes/runs.py`
  - `tests/conftest.py`
  - `tests/test_phase20_clarification_review.py`
  - `tests/test_phase20_fake_parity.py`
  - `tests/test_queue_drain.py`
  - `tests/test_queue_durability.py`
  - `tests/test_repo_jobs_sql.py`
  - `tests/test_send_idempotency.py`

## Source coverage audit

| Source | ID / item | Closure coverage | Status |
|---|---|---|---|
| GOAL | Phase 20 goal | Plans 17-20 jointly fence proof, provider, settlement, queue, and review paths per epoch. | COVERED |
| REQ | SEND-01 | Plan 17 current-epoch sent proof; Plan 18 stale epoch provider/settlement fence; Plan 20 final integration. | COVERED |
| REQ | SEND-02 | Plan 18 frozen snapshot stale-context fence; Plan 19 safe queue retirement; Plan 20 full regression gate. | COVERED |
| REQ | SEND-03 | Plans 18-19 preserve bounded durable queue/idempotency behavior; Plan 20 enforces purpose-safe retry and quality gates. | COVERED |
| RESEARCH | Current epoch, identifier-only jobs, locked settlement, 20-hour cutoff, four-reason replay allowlist, and PII-safe projections | Plans 17-20 preserve verified behavior and add only the missing epoch, fence, purpose, and lease checks. | COVERED |
| RESEARCH | No schema push or new dependency for closure | All closure files are application/repository/tests; no schema or dependency artifact is planned. | COVERED |
| CONTEXT | D-01/D-07 | Plans 19-20 keep durable same-row retry and separate invalid-context/lost-lease handling. | COVERED |
| CONTEXT | D-02/D-03/D-04/D-05/D-06 | Plan 18 explicitly preserves replay classification, reservation cutoff, frozen content, approved state, and mismatch escalation. | COVERED |
| CONTEXT | D-08/D-09/D-10/D-11 | Plan 20 preserves purpose-specific review outcomes and blocks confirmation controls for clarification. | COVERED |
| CONTEXT | D-12/D-13 | Plans 17-18 never mutate immutable snapshots and keep append-only evidence separate. | COVERED |
| CONTEXT | D-16 closure boundary | Plan 20 adds explicit confirmation-only direct-POST guards. | COVERED |

<output>Create `.planning/phases/20-exactly-once-send/20-20-SUMMARY.md` when done.</output>
