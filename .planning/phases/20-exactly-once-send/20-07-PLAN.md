---
phase: 20-exactly-once-send
plan: "07"
type: execute
wave: 8
depends_on: [20-04, 20-12]
files_modified:
  - app/pipeline/pdf.py
  - app/pipeline/delivery.py
  - tests/test_pdf.py
  - tests/test_delivery.py
autonomous: true
requirements: [SEND-02]
must_haves:
  truths:
    - "D-04: YTD presentation affects only future snapshot creation and cannot regenerate an existing reserved PDF."
    - "D-11: any human-authorized repeat remains the original frozen attachment bytes, not a fresh YTD rendering."
    - "D-12/D-13: paystub presentation is captured before provider call and later snapshot bytes remain append-only."
  artifacts:
    - app/pipeline/pdf.py
    - tests/test_pdf.py
  key_links:
    - "YTD derivation is used only by first-time snapshot creation; send handler and authorization clone use persisted bytes."
---

<objective>
Add the selected YTD paystub presentation without weakening immutable outbound replay.

Purpose: fulfill the folded payroll-document enhancement while keeping delivery bytes and historical audits stable.
Output: current/YTD PDF layout, complete YTD derivation, and snapshot-boundary regression tests.

Artifacts this phase produces: accurate all-category YTD paystub data/layout for new reservations and proof that existing delivery snapshots are unchanged.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-04-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-12-SUMMARY.md
@.planning/todos/pending/260623-03-paystub-ytd-v2.md
@app/pipeline/pdf.py
@app/pipeline/delivery.py
@tests/test_pdf.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive complete per-category YTD totals for new paystub generation</name>
  <files>app/pipeline/delivery.py, tests/test_delivery.py</files>
  <read_first>Read the YTD todo, current line-item/reconciled-run repository queries, delivery's initial snapshot creation boundary, and existing delivery tests.</read_first>
  <behavior>
    - New paystubs receive prior reconciled totals for gross, FIT, FICA-SS, Medicare, state, and net for the employee/pay period scope.
    - The calculation never uses a partially populated single YTD field as a complete YTD view.
    - Existing reserved/sent snapshots bypass all YTD derivation.</behavior>
  <action>Implement the selected all-category YTD data derivation at the first-time confirmation snapshot boundary, using reconciled historical payroll records with explicit employee/category aggregation and clear zero-prior-run behavior. Preserve tax calculation inputs and avoid using the result to mutate line items or historical runs. Per D-04, D-12, and D-13, call this work only when creating an absent snapshot; loading, retrying, marking delivered, or authorizing a repeat must use persisted attachment bytes and must not calculate fresh YTD data.</action>
  <acceptance_criteria>Only a new reservation can receive current/YTD display inputs; all established delivery slots replay byte-identically even if payroll history later changes.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_delivery.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Tests cover complete category totals, zero history, and prove snapshot replay/authorization does not call the YTD derivation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render honest current-versus-YTD paystub columns</name>
  <files>app/pipeline/pdf.py, tests/test_pdf.py</files>
  <read_first>Read generate_paystub_pdf, existing PDF extraction assertions, and the selected YTD todo's all-or-none rationale.</read_first>
  <behavior>
    - Earnings and deductions show aligned Current and YTD values for every supported category.
    - Generated PDF text/layout remains readable when a category has zero prior value.
    - The PDF API consumes supplied YTD values and does not query or mutate delivery snapshots.</behavior>
  <action>Extend the ReportLab paystub layout and typed input contract to show a complete Current/YTD earnings and deductions presentation, including gross, FIT, FICA-SS, Medicare, state where applicable, and net. Keep generation pure/in-memory and preserve existing filename and money formatting conventions. Add content/layout tests using extracted PDF text rather than relying on a wall-clock-dependent byte comparison. The delivery safety rule is D-04/D-11: this generator is allowed only before the first snapshot persists, never in queue replay or human-authorized clone paths.</action>
  <acceptance_criteria>The new paystub contains a complete, honest Current/YTD table with no misleading lone YTD value, while frozen attachments remain a separate immutable artifact.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_pdf.py tests/test_delivery.py -q</automated></verify>
  <done>PDF tests verify all Current/YTD labels and values, and delivery tests confirm the generator cannot be reached after a snapshot exists.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Re-rendered paystub changes an idempotent provider payload | Tampering | high | mitigate | D-04/D-11 guards and replay tests prohibit generator/YTD calls after reservation. |
| Incomplete YTD data misrepresents payroll history | Tampering | high | mitigate | Aggregate every displayed category from reconciled history or display an honest zero. |
| Historical YTD data leaks across employees | Information disclosure | high | mitigate | Employee-scoped aggregation and fixture tests for cross-employee exclusion. |
</threat_model>

<verification>
Run `uv run pytest tests/test_pdf.py tests/test_delivery.py tests/test_send_idempotency.py -q` and `uv run mypy app/pipeline/pdf.py app/pipeline/delivery.py`.
</verification>

<success_criteria>
New confirmations get complete YTD paystubs, while every already-reserved confirmation retains its exact original PDF bytes.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-07-SUMMARY.md` when done.</output>
