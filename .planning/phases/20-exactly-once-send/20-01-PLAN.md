---
phase: 20-exactly-once-send
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/schema.sql
  - app/db/repo/emails.py
  - app/db/repo/__init__.py
  - tests/conftest.py
  - tests/test_send_idempotency.py
autonomous: true
requirements: [SEND-01, SEND-02]
must_haves:
  truths:
    - "D-12: a provider-ready confirmation envelope and its ordered attachment bytes are committed before any provider request."
    - "D-13: a retry reads the original immutable snapshot; it cannot overwrite the Message-ID, body, headers, recipient, or bytes."
    - "SEND-01: one logical run/purpose/round/epoch slot keeps its original reserved Message-ID."
  artifacts:
    - app/db/schema.sql
    - app/db/repo/emails.py
  key_links:
    - "The read-or-reserve repository API writes the logical email row, snapshot, and ordered byte attachments in one caller-owned transaction."
---

<objective>
Create the immutable database and repository foundation for a provider-ready outbound reservation.

Purpose: make the original Message-ID and exact payload durable before the queue or Resend can act.
Output: append-only outbound snapshot, attachment, and bounded-attempt persistence plus fake-repository parity.

Artifacts this phase produces: durable logical send-slot snapshot records; ordered raw attachment rows; append-only `outbound_delivery_attempts`; deployed-schema triggers `trg_outbound_email_snapshots_append_only`, `trg_outbound_email_attachments_append_only`, and `trg_outbound_delivery_attempts_append_only`; repository APIs `reserve_outbound_snapshot`, `load_outbound_snapshot`, `load_delivery_review_snapshot`, and `load_snapshot_attachment`; and immutable-reservation regression proofs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@app/db/schema.sql
@app/db/repo/emails.py
@tests/test_send_idempotency.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add append-only outbound snapshot, attachment, and attempt storage</name>
  <files>app/db/schema.sql, tests/test_send_idempotency.py</files>
  <read_first>Read the email_messages definition and its deployed-schema repair blocks in app/db/schema.sql, then the live epoch-scoping proof in tests/test_send_idempotency.py and the Phase 20 pattern map.</read_first>
  <behavior>
    - A snapshot has one parent per logical outbound email and ordered attachment filename/byte children.
    - A duplicate logical slot cannot mutate its original Message-ID or snapshot fields.
    - A delivery attempt records only fixed category/state metadata and cannot contain provider bodies.
    - Direct SQL UPDATE and DELETE against snapshot, attachment, and attempt rows are rejected after bootstrap, including on an already-deployed schema.</behavior>
  <action>Per D-12 and D-13, extend the idempotent schema/bootstrap repair path with an immutable outbound snapshot parent keyed to the existing outbound email row, ordered attachment rows containing filename plus BYTEA content, and append-only PII-safe `outbound_delivery_attempts` rows. Preserve email_messages as the logical audit and its run/purpose/round/epoch identity. Add foreign keys, ordinal uniqueness, explicit bounded category/state checks, and deployed-database repair SQL in the same current-install/repair style. Install `trg_outbound_email_snapshots_append_only`, `trg_outbound_email_attachments_append_only`, and `trg_outbound_delivery_attempts_append_only` as database-enforced rejection triggers for UPDATE or DELETE, while keeping mutable send/review state on separate existing/state tables. Do not put envelope or attachment content on jobs, and do not add a purge rule. First add SQL-shape, schema-repair, and live-schema-guarded tests that prove slot identity, byte ordering, immutable columns, and direct SQL mutation rejection.</action>
  <acceptance_criteria>D-12/D-13 storage can represent sender, recipient, reply-to, threading headers, subject, text, ordered attachment filenames and exact bytes before a provider call; state history is separate and bounded; direct SQL cannot update or delete immutable snapshot, attachment, or attempt evidence.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_send_idempotency.py -q</automated></verify>
  <done>The schema installs and repairs idempotently for new and deployed databases, and focused tests fail if an immutable snapshot field, attachment ordering constraint, or append-only trigger is removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace outbound conflict overwrite with read-or-reserve repository APIs</name>
  <files>app/db/repo/emails.py, app/db/repo/__init__.py, tests/conftest.py, tests/test_send_idempotency.py</files>
  <read_first>Read insert_email_message, get_unconfirmed_outbound, load_outbound_emails, _conn_ctx, and the matching InMemoryRepo methods in tests/conftest.py.</read_first>
  <behavior>
    - The absent branch reserves a generated Message-ID and full snapshot once in a caller-owned transaction.
    - The conflict branch locks and returns stored snapshot data byte-identically without applying caller content.
    - Fake and production APIs expose the same `reserve_outbound_snapshot`, `load_outbound_snapshot`, `load_delivery_review_snapshot`, and `load_snapshot_attachment` operations.</behavior>
  <action>Implement the purpose/round/epoch-scoped `reserve_outbound_snapshot` API that selects and locks an existing logical slot before returning it unchanged, or inserts its email row, immutable envelope, and attachment bytes exactly once when absent. Implement explicit `load_outbound_snapshot` handler data, `load_delivery_review_snapshot` bounded review facts, and `load_snapshot_attachment` byte retrieval scoped to the owning snapshot/run; do not use broad reads. Remove caller-argument conflict updates from the outbound path; retain separate state-transition APIs. Re-export the public functions and extend InMemoryRepo plus fake-pairing inventories in lockstep. Cite D-12 and D-13 in docstrings/tests and make all PII-facing projections explicit-column, rather than broad reads.</action>
  <acceptance_criteria>A repeated reservation receives the original Message-ID, headers, body, recipient, and attachment bytes; no repository conflict path changes them; D-13 attempt facts remain separate; the review and attachment readers return only the owned frozen record requested.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_send_idempotency.py tests/test_delivery.py -q</automated></verify>
  <done>Repository and fake contracts preserve an immutable snapshot under an existing send-slot conflict and expose no raw provider payload in their safe review projection.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Retry-supplied content overwrites the original logical email | Tampering | high | mitigate | D-12/D-13 read-or-reserve locking, immutable schema constraints, and byte-identity tests. |
| Attachment content leaks through transport rows or review queries | Information disclosure | high | mitigate | Keep jobs identifier-only; use dedicated explicit-column safe projection and raw bytes only in persistence/authorized download path. |
| Attempt diagnostics retain provider payload or exception text | Information disclosure | medium | mitigate | CHECK-backed fixed categories and repository validation only. |
</threat_model>

<verification>
Run `uv run pytest tests/test_send_idempotency.py tests/test_delivery.py -q`, then `uv run mypy app/db/repo/emails.py tests/conftest.py`.
</verification>

<success_criteria>
An outbound logical slot has one durable immutable snapshot and ordered byte attachments, and a retry cannot replace any provider-visible field.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-01-SUMMARY.md` when done.</output>
