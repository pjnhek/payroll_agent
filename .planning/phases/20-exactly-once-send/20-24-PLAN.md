---
phase: 20-exactly-once-send
plan: 24
type: execute
wave: 17
depends_on: [20-22]
files_modified:
  - app/db/repo/job_settlement.py
  - tests/conftest.py
  - tests/test_phase20_fake_parity.py
  - tests/test_queue_durability.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "Every SEND_OUTBOUND result finalizes or preserves only its exact current durable handoff before its job lease changes; a classified retry releases that exact active handoff into auditable history in the same transaction before the job becomes claimable, while adoption is completed only by the pre-provider authorizer."
    - "An authorization-expired result releases the matching handoff, records a bounded deadline category, and enters D-08 delivery review without another provider attempt."
    - "The InMemoryRepo exposes the same pre-provider handoff adoption and exact-owner release/result behavior as production, so hermetic tests cannot hide a provider-fence failure."
  artifacts:
    - path: app/db/repo/job_settlement.py
      provides: "Exact-handoff delivery settlement and final-lease reaper behavior."
    - path: tests/conftest.py
      provides: "In-memory exact-handoff lifecycle parity."
  key_links:
    - "authorize_outbound_provider_handoff adopts an eligible crash-reclaimed matching handoff before provider work; a classified timeout/5xx settlement exact-owner-releases its handoff to `retry_scheduled` before atomically rescheduling the job, and the next claim reauthorizes the original frozen snapshot/Message-ID under the same reservation-derived deadline."
    - "DELIVERY_AUTHORIZATION_EXPIRED is routed to exact release and purpose-aware delivery review, never to a retry schedule."
---

<objective>
Settle every durable provider-handoff outcome against the exact job, snapshot, epoch, and lease without letting fake behavior drift from production.

Purpose: Plan 20-22 safely prevents expired provider I/O; this plan ensures that safe result neither strands the handoff nor gets treated as an eligible retry, and proves a crash after authorization resumes the same adopted slot rather than permanently no-oping.
Output: exact settlement/reaper lifecycle and production/fake parity for the adopted-handoff and new outcome vocabulary.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-21-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-22-SUMMARY.md
@app/db/repo/job_settlement.py
@tests/conftest.py
@tests/test_phase20_fake_parity.py
@tests/test_queue_durability.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Finalize only the current exact handoff and prove reclaimed adoption parity</name>
  <files>app/db/repo/job_settlement.py, tests/conftest.py, tests/test_phase20_fake_parity.py, tests/test_queue_durability.py</files>
  <read_first>Read `_locked_job`, `_lock_outbound_reservation`, `_lock_run_status`, `settle_outbound_delivery_job`, `reap_expired_final_attempt`, the Plan 20-21 exact-owner/adoption APIs, and InMemoryRepo's job settlement/final-lease methods. Read the stale-epoch and lost-lease queue durability tests before changing result mapping.</read_first>
  <behavior>
    - `DELIVERY_RECORD_ONLY` completes the exact job/snapshot without a provider-attempt event or active-handoff release because it never created a provider handoff.
    - `DELIVERY_AUTHORIZATION_EXPIRED` locks/releases the matching active handoff, writes one fixed deadline category, and transitions only the matching purpose/epoch into D-08 review with no retry job.
    - A reclaimed current epoch adopts only through `authorize_outbound_provider_handoff` before the gateway. Settlement accepts the adopted current token but a lost lease, stale epoch, malformed context, or replaced predecessor token cannot finalize, release, or mutate its record.
    - For each classified `DELIVERY_TIMEOUT` and `DELIVERY_SERVER_FAILURE`, settlement locks the exact current active handoff and releases it to the fixed `retry_scheduled` history state before it clears that exact lease and makes the job due. The next exact claim obtains a fresh active authorization from the unchanged frozen snapshot and original reservation-derived `not_after`; the former token cannot settle or release either the historical or new active row.
    - After a simulated crash immediately after production/fake authorization, the exact send job can be reclaimed only after the recorded predecessor lease expiry; authorization retains the original handoff id, snapshot Message-ID, `authorized_at`, and `not_after`, the reclaimed handler performs one provider call, and the old token cannot settle or release the adopted handoff.
  </behavior>
  <action>Extend `settle_outbound_delivery_job` and `reap_expired_final_attempt` after Plan 20-22's new result vocabulary. Preserve the established jobs → reservation → run lock order, then lock/finalize the exact handoff identified by handoff id, job id, current lease token, snapshot/email id, run id, and epoch before any sent transition, retry reschedule, review transition, final-lease release, or invalid-context retirement. Settlement and reaping must never adopt an active handoff: adoption belongs exclusively to Plan 20-21's pre-provider `authorize_outbound_provider_handoff` transaction, so a worker cannot obtain provider authority after its handler has already chosen a no-op/result path. For an allowed D-02 timeout or 5xx retry inside D-03's reservation-time window, call Plan 20-21's exact-owner `release_outbound_provider_handoff_for_retry` while the exact job/reservation/run/handoff locks are held, append the bounded retry attempt, and only then update that same leased job to pending and clear its token. These release, attempt, and reschedule writes are one transaction: if the handoff release cannot match every identity and current token, roll back rather than create a claimable job behind an unexpired active fence. The released row remains append-only/auditable with the fixed retry reason; a due claimant must reauthorize a new active handoff from the exact original frozen snapshot, Message-ID, and `reserved_at + 20 hours` deadline. Successful sends and terminal/review outcomes likewise exact-owner-finalize or release the current active row before changing their job state; neither a stale predecessor nor LOST_LEASE/INVALID_CONTEXT may release a newer active row. Treat `DELIVERY_RECORD_ONLY` as a no-provider terminal settlement with no active-handoff lookup or provider-attempt row. Treat `DELIVERY_AUTHORIZATION_EXPIRED` as D-08 delivery review: release only its exact current-owner handoff, append a fixed PII-safe deadline category, keep D-05's approved status only while a safe retry exists, and never reschedule. Preserve D-02 retry classification, D-03 reservation-time cutoff, D-06 payload-mismatch escalation, and D-07 bounded attempts. Extend InMemoryRepo with the same handoff data and authority/retry-release method: it must retain predecessor lease expiry, admit adoption only when the fake claimant owns the current exact leased job and the matching prior owner lease has expired, update only owner token/expiry, preserve handoff id, snapshot, epoch, `authorized_at`, and `not_after` on adoption, and mirror the production retry release before it clears the fake lease. Add parameterized production-repository and InMemoryRepo parity regressions for `DELIVERY_TIMEOUT` and `DELIVERY_SERVER_FAILURE`: authorize the original frozen send, settle the classified retry, assert historical release before the pending job, reclaim the due job under a new token, reauthorize the original snapshot/Message-ID and unchanged reservation-derived deadline, perform exactly one frozen-snapshot provider replay, then prove the former token cannot settle or release while the new token can settle. Also retain the crash-after-authorization adoption regression, record-only completion, authorization-expired review/no-reschedule, unrelated/unexpired active rejection, and stale/foreign token rejection. Use the existing guarded queueproof fixtures for SQL/lock-order coverage and keep diagnostics IDs/categories only.</action>
  <acceptance_criteria>All provider, no-provider, retry, review, reaper, and reclaim paths leave exactly the handoff owned by the current leased job in a coherent state. Every classified timeout/5xx retry first converts its active fence into auditable history, then becomes claimable so the new lease can reauthorize and replay only the original frozen snapshot under its original deadline; old tokens cannot mutate either row. A crash after authorization resumes the same original frozen slot only after safe adoption, and expired authorization is visibly reviewable and never auto-retried.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_fake_parity.py tests/test_queue_durability.py tests/test_queue_drain.py</automated></verify>
  <done>Settlement cannot orphan a handoff or release an adopted/replacement owner's handoff, and production/fake tests exercise the same crash-reclaim-send-once and deadline-review behavior.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Gateway result → repository settlement | A bounded result must change only the durable handoff that authorized its send. |
| Reclaimed lease → active handoff | The pre-provider authorizer may transfer only an expired matching handoff to the exact new lease; settlement cannot adopt it. |
| Production repository → InMemoryRepo | Hermetic tests must retain ownership and review semantics rather than silently simplify them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-24-01 | Tampering | delivery settlement | critical | mitigate | Lock and finalize exact handoff identity before every state or lease write. |
| T-20-24-02 | Tampering | reaper/reclaim | critical | mitigate | Require the authorizer's prior-expiry/current-lease adoption predicate and reject foreign or stale owners from settlement/release. |
| T-20-24-03 | Repudiation | delivery review | high | mitigate | Persist fixed deadline category and purpose-aware D-08 review, not raw provider material. |
| T-20-24-04 | Information disclosure | fake diagnostics | medium | mitigate | Keep tests and fake outputs to identifiers and bounded reasons. |
| T-20-24-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; use `uv run` only. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_phase20_fake_parity.py tests/test_queue_durability.py tests/test_queue_drain.py</automated>
<automated>uv run ruff check app/db/repo/job_settlement.py tests/conftest.py tests/test_phase20_fake_parity.py tests/test_queue_durability.py</automated>
<automated>uv run mypy</automated>
</verification>

<success_criteria>
Every exact-handoff result has one durable, fenced settlement outcome, and an expired authorization becomes a deliberate delivery-review state without provider retry.
</success_criteria>

## Source coverage audit

| Source | ID / item | Coverage | Status |
|---|---|---|---|
| GOAL | At most one confirmation per approved run and epoch | Exact pre-provider adoption plus exact finalization prevents old result/reaper work from affecting a reclaimed or replacement epoch. | COVERED |
| REQ | SEND-01 | Original slot identity remains bound through retry, adoption, and terminal settlement. | COVERED |
| REQ | SEND-02 | Settlement uses frozen snapshot identity only and never composes a replacement payload. | COVERED |
| REQ | SEND-03 | Deadline expiry enters review, while only classified safe faults remain retryable inside the reservation window. | COVERED |
| RESEARCH | Fenced durable queue and PII-safe diagnostics | Uses existing lock order/exact-token semantics and bounded categories. | COVERED |
| CONTEXT | D-01 through D-13 | D-01/D-02/D-03/D-05/D-06/D-07/D-08 govern settlement; D-04/D-12/D-13 preserve immutable snapshots and audit. | COVERED |

<output>Create `.planning/phases/20-exactly-once-send/20-24-SUMMARY.md` when done.</output>
