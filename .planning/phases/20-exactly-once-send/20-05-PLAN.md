---
phase: 20-exactly-once-send
plan: "05"
type: execute
wave: 5
depends_on: [20-03, 20-09]
files_modified:
  - app/queue/handlers/send_outbound.py
  - app/queue/dispatch.py
  - app/queue/drain.py
  - tests/conftest.py
  - tests/test_queue_drain.py
autonomous: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-01/D-07: automatic and retry-now paths have an executable identifier-only consumer backed by fenced settlement before a producer may enqueue work."
    - "D-04: the handler loads a persisted snapshot and cannot call composition/PDF code."
  artifacts:
    - app/queue/handlers/send_outbound.py
    - app/queue/dispatch.py
  key_links:
    - "Late-bound dispatch loads the handler module only after it exists; the handler loads snapshot by email_id, invokes the snapshot-only gateway, then uses Plan 09's exact-token settlement."
---

<objective>
Register and execute frozen send jobs only after their delivery-specific lease-fenced settlement coordinator exists.

Purpose: eliminate the missing-import/stranded-job interval before any producer can create SEND_OUTBOUND work.
Output: late-bound handler registration, snapshot-only handler, and anti-vacuous queue proofs.

Artifacts this phase produces: the identifier-only `app/queue/handlers/send_outbound.py` handler and its `HANDLERS` registration. Plan 09 provides its purpose-aware `settle_outbound_delivery_job` coordinator first; Plan 10 migrates clarification producers after this executable consumer exists.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-04-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-09-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-09-SUMMARY.md
@app/queue/handlers/resume_reply.py
@app/queue/dispatch.py
@app/queue/drain.py
@tests/test_queue_durability.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add and register a snapshot-only SEND_OUTBOUND handler</name>
  <files>app/queue/handlers/send_outbound.py, app/queue/dispatch.py, app/queue/drain.py, tests/test_queue_drain.py</files>
  <read_first>Read the resume_reply and operator_resume handlers, dispatch behavior, drain result normalization, snapshot repository APIs, and the Plan 09 fenced settlement contract.</read_first>
  <behavior>
    - The handler verifies job.run_id/email_id ownership and snapshot state before invoking the provider.
    - It uses only the persisted snapshot and stored reservation timestamp.
    - Stale, missing, superseded, or fenced context returns a bounded no-op/result before provider work.
    - A clarification-purpose job retains its immutable purpose/round/threading context and can never be treated as confirmation delivery or alias approval.</behavior>
  <action>Create the `send_outbound` handler using the established identifier-only handler shape, then register it by module object and function name in `HANDLERS` only after the module exists. Validate that the job email_id loads one outbound immutable snapshot belonging to job.run_id and current authorized slot, including purpose/round/epoch, determine D-03 eligibility from database reserved_at, and call the snapshot-only gateway from Plan 03. Pass the classified result and exact lease token to Plan 09's `settle_outbound_delivery_job`; wire it through the shared drain without calling delivery.deliver, compose_confirmation, generate_paystub_pdf, current payroll loaders, or a direct route helper. Treat a clarification purpose as an identifier-only snapshot send with its stored reply chain, not a confirmation path; it must not mutate aliases. Add non-vacuous handler tests with provider/draft/PDF fail-if-called seams to prove D-04/D-07 ownership before effects.</action>
  <acceptance_criteria>A claimed send job cannot issue a provider request until it proves durable ownership and replay eligibility; replays use only stored bytes and fixed key.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_queue_drain.py tests/test_gateway.py -q</automated></verify>
  <done>Handler tests cover valid send, stale/missing context no-op, and a replay path where content-generation functions are unreachable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete fake parity and dispatch equality for the live handler</name>
  <files>tests/conftest.py, tests/test_queue_drain.py</files>
  <read_first>Read InMemoryRepo handler seams, dispatch's monkeypatch rationale, and the exact enum/dispatch equality tests delayed from Plan 02.</read_first>
  <behavior>
    - Dispatch contains one module/name handler entry for every JobKind, including SEND_OUTBOUND, after the module exists.
    - A test double can patch the send handler at runtime through the module lookup seam.
    - Fake jobs preserve run_id/email_id and the bounded result plumbing required for D-07.</behavior>
  <action>Extend InMemoryRepo and fake-pairing inventories for the handler's bounded snapshot/settlement calls. Replace Plan 02's temporary pre-handler assertion with the normal exact JobKind-to-HANDLERS equality proof and prove removing the send handler or miswiring its identifier context fails. Keep test doubles late-bound through the module lookup seam. This task does not implement a producer: Plans 04 and 10 are the first enqueueing plans and both depend on this completed handler.</action>
  <acceptance_criteria>Every JobKind now has an importable late-bound consumer, tests can patch SEND_OUTBOUND at module lookup time, and no producer plan precedes this executable handler.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_queue_drain.py tests/test_job_kind_drift.py -q</automated></verify>
  <done>Dispatch equality is non-vacuous, fake parity covers the handler boundary, and SEND_OUTBOUND is executable and fenced before any application producer enqueues it.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| A dispatch entry imports a missing handler or receives unowned context | Denial of service | high | mitigate | Create the module before registration and prove ownership/no-op behavior through late-bound dispatch tests. |
| Handler bypasses exact-token settlement | Tampering | high | mitigate | Require Plan 09's fenced coordinator and pass its lease token explicitly from the handler. |
| Handler recomposes or regenerates provider content | Tampering | high | mitigate | D-04 fail-if-called seams for delivery/PDF/current-data functions. |
</threat_model>

<verification>
Run `uv run pytest tests/test_queue_drain.py tests/test_job_kind_drift.py tests/test_gateway.py -q` and `uv run mypy app/queue/handlers/send_outbound.py app/queue/dispatch.py app/queue/drain.py`.
</verification>

<success_criteria>
The queue has an importable, snapshot-only, lease-fenced send consumer before the first confirmation or clarification producer may enqueue a job.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-05-SUMMARY.md` when done.</output>
