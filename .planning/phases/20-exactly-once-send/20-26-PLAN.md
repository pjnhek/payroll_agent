---
phase: 20-exactly-once-send
plan: 26
type: execute
wave: 20
depends_on: [20-25]
files_modified:
  - app/queue/handlers/send_outbound.py
  - app/db/repo/job_settlement.py
  - tests/conftest.py
  - tests/test_gateway.py
  - tests/test_phase20_fake_parity.py
  - tests/test_send_idempotency.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "A SEND_OUTBOUND job whose locked reservation has already passed its 20-hour replay window reaches the purpose-aware delivery-review state, rather than being retired as generic invalid context."
    - "The pre-provider expiry path calls neither the frozen-snapshot gateway nor Resend, creates no provider handoff, appends exactly one bounded `authorization_expired` review attempt, completes the exact leased job, and keeps the immutable snapshot unchanged."
    - "The same pre-provider expiry behavior is represented by production settlement SQL and the InMemoryRepo, for confirmation and clarification purposes."
  artifacts:
    - path: app/queue/handlers/send_outbound.py
      provides: "A bounded translation from the authorizer's replay-window-closed no-provider outcome to DELIVERY_AUTHORIZATION_EXPIRED."
    - path: app/db/repo/job_settlement.py
      provides: "Fenced, no-handoff settlement of a specifically pre-provider authorization expiry into purpose-aware delivery review."
    - path: tests/test_phase20_fake_parity.py
      provides: "Offline parity regressions for zero-provider pre-authorization expiry review."
  key_links:
    - "handle_send_outbound preserves the distinct `ProviderHandoffActive('replay_window_closed')` reason as DELIVERY_AUTHORIZATION_EXPIRED before its generic no-op branch can discard it."
    - "settle_outbound_delivery_job accepts that exact bounded result only after locking the exact lease, frozen reservation, current run generation, and expected purpose status; it does not require or release an authority record that was never created."
---

<objective>
Close CR-01's pre-provider expiry branch by preserving its bounded reason and settling it directly into delivery review.

Purpose: an automatically replayed confirmation must never be silently retired after Resend's deduplication safety window. The reservation is already too old to call the provider, so the safe result is explicit, purpose-aware human review with no replacement key or provider side effect.
Output: a delivery-specific pre-authorization-expired result, exact no-handoff review settlement, fake parity, and focused regressions.

Artifacts this phase produces: a handler translation for `replay_window_closed`; a narrowly fenced no-handoff `authorization_expired` review branch; confirmation and clarification fake/production-seam regressions; and provider-spy assertions that expiry is settled without outbound I/O.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-21-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-22-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-24-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-25-SUMMARY.md
@app/queue/handlers/send_outbound.py
@app/db/repo/outbound_handoffs.py
@app/db/repo/job_settlement.py
@tests/test_gateway.py
@tests/test_phase20_fake_parity.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve the bounded pre-provider replay-window-expired result</name>
  <files>app/queue/handlers/send_outbound.py, tests/test_gateway.py</files>
  <read_first>Read `handle_send_outbound`, the typed `ProviderHandoffAuthorization`, `ProviderHandoffRecordOnly`, and `ProviderHandoffActive` outcomes in app/db/repo/outbound_handoffs.py, the DELIVERY_AUTHORIZATION_EXPIRED result definition, and the current handler authority/expiry tests in tests/test_gateway.py. Read CR-01 in 20-VERIFICATION.md before changing the generic no-provider branch.</read_first>
  <behavior>
    - An authorizer result whose bounded reason is replay-window-closed returns `PipelineResult(outcome=TERMINAL, stage=DELIVERY, reason=DELIVERY_AUTHORIZATION_EXPIRED)`.
    - This result returns before the gateway seam and therefore causes zero Resend/provider calls, no draft/PDF composition, and no provider-handoff creation.
    - Record-only and unrelated active/invalid contexts retain their established bounded no-provider behavior; no generic active result may accidentally become a delivery-review authorization expiry.
  </behavior>
  <action>Per D-02, D-03, D-04, D-06, D-07, D-08, D-12, D-13, and SEND-03, add an explicit structural check for the typed/no-provider authority outcome carrying the fixed replay-window-closed reason before the existing generic snapshot-less no-op branch. Translate only that reason to the existing bounded DELIVERY_AUTHORIZATION_EXPIRED terminal result; keep record-only mapped to DELIVERY_RECORD_ONLY and leave unrelated active or invalid context as their existing non-provider no-op. Do not mint a Message-ID, derive content, enqueue a replacement job, call `gateway.send_reserved_outbound_snapshot`, import delivery composition/PDF functions, or expose persisted email values in the result. First add a handler test that injects the authorizer's replay-window-closed outcome and a passive gateway/Resend spy, then proves the returned reason, terminal outcome, exact job forwarding to the authorizer, and zero provider calls. Add a negative companion for an unrelated active reason so the mapping cannot become an overbroad conversion of generic invalid context into operator review.</action>
  <acceptance_criteria>A stale reservation produces a distinct bounded delivery expiry result before any provider boundary; only the authoritative replay-window-closed reason is eligible for that mapping.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_gateway.py -k 'replay_window_closed or authorization_expired or send_handler'</automated></verify>
  <done>Handler tests prove pre-provider expiry has no external side effect and generic no-provider authority outcomes retain their prior semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settle pre-provider authorization expiry directly to purpose-aware review</name>
  <files>app/db/repo/job_settlement.py, tests/conftest.py, tests/test_phase20_fake_parity.py, tests/test_send_idempotency.py</files>
  <read_first>Read `settle_outbound_delivery_job`, `_lock_outbound_reservation`, `_lock_current_provider_handoff`, `_append_delivery_attempt`, and the existing final delivery-review branch in app/db/repo/job_settlement.py. Read the InMemoryRepo authorization and delivery-settlement equivalents in tests/conftest.py, the existing expiry fake-parity test, and the SQL-shape settlement tests in tests/test_send_idempotency.py.</read_first>
  <behavior>
    - An exact leased SEND_OUTBOUND job with DELIVERY_AUTHORIZATION_EXPIRED, a current reserved snapshot, matching epoch, expected purpose status, and no current provider handoff appends one `needs_operator` attempt with `authorization_expired`, changes only that purpose's run to `needs_operator` with its existing purpose-specific review reason, and marks the exact job done.
    - The no-handoff branch is unavailable to every other result reason, stale lease, mismatched run/email/epoch/status, or active/foreign handoff; those retain their established fenced or invalid-context handling and cannot write review evidence.
    - Confirmation uses DeliveryReview and clarification uses ClarificationDeliveryReview; neither path calls the provider, regenerates frozen content, changes the Message-ID, schedules another job, or treats the run as ordinary transient error.
  </behavior>
  <action>Per D-01, D-03, D-04, D-05, D-07, D-08, D-10, D-12, D-13, and SEND-01 through SEND-03, refactor the delivery-review write sequence only as needed so `settle_outbound_delivery_job` can handle the exact DELIVERY_AUTHORIZATION_EXPIRED result after it has locked the leased job, immutable reservation, current run, expected purpose status, and current generation but before it requires an exact provider handoff. This path represents authorization that was denied before a handoff existed: confirm no active handoff belongs to the locked delivery identity, append the bounded review fact, perform the existing purpose-aware status CAS, and complete the same exact lease in one transaction. It must not call any handoff release/finalize API, since there is no authority to release. Keep the existing handoff-required success/retry/post-provider-review flow unchanged and fail closed for every other result lacking authority. Mirror exactly that narrow pre-provider branch in InMemoryRepo; do not make the fake accept arbitrary terminal results without an active handoff. Add focused production SQL-shape/seam tests and fake parity tests for confirmation and clarification, including stale/foreign/active-handoff rejection, one append-only attempt fact, no pending/leased send job left, unchanged frozen snapshot identity/content, and zero provider calls from Task 1's handler path. The schema vocabulary deployment proof is deliberately owned by Plan 20-27; do not claim these tests prove a live database accepts the new category yet.</action>
  <acceptance_criteria>Only the bounded pre-provider expiry result can settle a leased reservation into purpose-aware delivery review without a handoff; it remains fenced, auditable, snapshot-preserving, and provider-free in both repository implementations.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_fake_parity.py tests/test_send_idempotency.py tests/test_gateway.py</automated></verify>
  <done>Pre-authorization expiry no longer retires a valid delivery job as invalid context, while all non-expiry missing-handoff results remain unable to mutate delivery state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provider-authorizer result → queue handler | A bounded no-provider result must not be erased or mistaken for an authorization to send. |
| Exact leased job/snapshot/run → review settlement | A review transition must be bound to the current delivery generation even though no provider handoff exists. |
| Production repository → InMemoryRepo | Offline test behavior must not hide a missing fence or an overbroad review transition. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-26-01 | Tampering | handler result normalization | critical | mitigate | Preserve only replay-window-closed as a typed authorization-expired delivery result and deny provider entry. |
| T-20-26-02 | Tampering | no-handoff settlement | critical | mitigate | Require exact lease, reserved snapshot, current epoch, expected purpose status, and absence of a current handoff before the direct review CAS. |
| T-20-26-03 | Repudiation | delivery evidence | high | mitigate | Append one fixed `authorization_expired` review event and retain immutable snapshot/message identity. |
| T-20-26-04 | Information disclosure | browser/review diagnostics | medium | mitigate | Reuse only fixed delivery category and purpose-specific review fields; no provider dump or snapshot body crosses the result boundary. |
| T-20-26-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; use the committed uv environment only. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_gateway.py tests/test_phase20_fake_parity.py tests/test_send_idempotency.py</automated>
<automated>uv run ruff check app/queue/handlers/send_outbound.py app/db/repo/job_settlement.py tests/conftest.py tests/test_gateway.py tests/test_phase20_fake_parity.py tests/test_send_idempotency.py</automated>
<automated>uv run mypy</automated>
</verification>

<success_criteria>
The expired pre-provider path is a bounded, zero-provider, fenced delivery-review outcome rather than a generic successful no-op or an invalid-context retirement.
</success_criteria>

## Source coverage audit

| Source | ID / item | Coverage | Status |
|---|---|---|---|
| GOAL | At most one confirmation per approved run and epoch | Past-window work cannot silently retire or issue a replacement send; it is reviewed against its original frozen slot. | COVERED |
| REQ | SEND-01 | No new Message-ID or slot is minted when the old reservation is expired. | COVERED |
| REQ | SEND-02 | The pre-provider branch does not draft, regenerate, alter, or render the frozen payload. | COVERED |
| REQ | SEND-03 | Replay-window expiry is classified before provider I/O and enters explicit human review. | COVERED |
| RESEARCH | Identifier-only handler, reservation-time cutoff, exact fenced settlement, PII-safe categories | The plan maps the typed authorizer outcome through an exact lease/snapshot/run transaction and stores only the fixed category. | COVERED |
| CONTEXT | D-01 | Queue-owned delivery remains the sole side-effect path; no browser/synchronous resend is introduced. | COVERED |
| CONTEXT | D-02 | The result is a classified non-replayable condition, not a retry. | COVERED |
| CONTEXT | D-03 | The authorizer's reservation-time 20-hour cutoff is preserved, never reset. | COVERED |
| CONTEXT | D-04 | No payload recomposition or regeneration occurs. | COVERED |
| CONTEXT | D-05 | The run moves to review only when automatic replay is no longer eligible. | COVERED |
| CONTEXT | D-06 | No alternate idempotency key is minted. | COVERED |
| CONTEXT | D-07 | One bounded attempt-history event is retained. | COVERED |
| CONTEXT | D-08 | Expiry becomes delivery review rather than ordinary error or silent approval. | COVERED |
| CONTEXT | D-09 | The existing purpose-aware review state remains the route to its two explicit outcomes. | COVERED |
| CONTEXT | D-10 | Only fixed category/purpose diagnostics are recorded for later safe projection. | COVERED |
| CONTEXT | D-11 | No new confirmation is automatically authorized. | COVERED |
| CONTEXT | D-12 | The existing immutable snapshot is the only content identity consulted. | COVERED |
| CONTEXT | D-13 | Attempt state is appended separately; snapshot content is untouched. | COVERED |

<output>Create `.planning/phases/20-exactly-once-send/20-26-SUMMARY.md` when done.</output>
