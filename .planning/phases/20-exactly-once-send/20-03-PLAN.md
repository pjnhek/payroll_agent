---
phase: 20-exactly-once-send
plan: "03"
type: execute
wave: 3
depends_on: [20-01, 20-02]
files_modified:
  - app/pipeline/result.py
  - app/email/gateway.py
  - tests/test_gateway.py
  - tests/test_orchestrator_states.py
autonomous: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-02: only timeout, connection, Resend 5xx, and classified rate-limit failures are eligible for replay."
    - "D-03: the reservation timestamp, not restart or attempt time, limits automatic replay to 20 hours."
    - "D-04: the gateway sends only a persisted provider-ready snapshot and never recomposes content."
    - "D-06: an idempotency payload mismatch is terminal delivery review, never an automatic replacement key."
  artifacts:
    - app/pipeline/result.py
    - app/email/gateway.py
  key_links:
    - "Every Resend request gets the stored Message-ID-derived idempotency option and stored headers/bytes."
---

<objective>
Make the provider gateway replay an immutable reservation with explicit bounded failure classification.

Purpose: Resend can deduplicate only when the same key is paired with the same frozen payload.
Output: additive snapshot-only send API, idempotency-key wiring, and safe delivery result vocabulary.

Artifacts this phase produces: the additive snapshot-only `send_reserved_outbound_snapshot` gateway adapter and classified delivery outcomes. The existing caller-argument `gateway.send_outbound` remains intact because the current confirmation and clarification producers still call it; Plans 04 and 10 migrate those producers only after Plan 05 registers its executable handler backed by Plan 09's fenced settlement, and Plan 12 removes or fail-closes the legacy path after both migrations.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-02-SUMMARY.md
@app/pipeline/result.py
@app/email/gateway.py
@tests/test_gateway.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend bounded delivery classification for Resend responses</name>
  <files>app/pipeline/result.py, tests/test_orchestrator_states.py, tests/test_gateway.py</files>
  <read_first>Read PipelineReason, classify_pipeline_exception, existing delivery ambiguity behavior, and Phase 20 research sections on Resend error classes and the 20-hour ladder.</read_first>
  <behavior>
    - Timeout, connection, 5xx, and the documented retryable rate-limit category map to retryable delivery results.
    - Payload mismatch, validation, authentication, authorization, configuration, quota, and unknown provider failures map to bounded terminal review categories.
    - Classification persists no exception string, response body, recipient, or provider payload.</behavior>
  <action>Add delivery-specific bounded reasons and a classifier that understands the installed Resend exception/status/code shape while preserving the terminal-safe default. Implement D-02 and D-06: distinguish retryable rate-limit pressure from quota exhaustion, classify 409 payload mismatch as non-replayable, and retain only stage/reason values. Define a pure reservation-time schedule helper with the documented immediate/1m/5m/15m/1h/3h/8h/16h ladder; it must refuse a claim or next schedule at or beyond D-03's 20-hour cutoff. Do not let generic job attempts or process time reset that clock.</action>
  <acceptance_criteria>The classifier produces only bounded codes, retries only approved transient classes, and treats mismatch/quota/auth/validation as review-worthy terminal outcomes.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_orchestrator_states.py tests/test_gateway.py -q</automated></verify>
  <done>Focused tests cover every replayable and non-replayable provider category plus both sides of the 20-hour boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a snapshot-only Resend capability alongside the live legacy caller path</name>
  <files>app/email/gateway.py, tests/test_gateway.py</files>
  <read_first>Read gateway.send_outbound, Resend SendOptions in the installed SDK, the snapshot repository API from Plan 01, and gateway threading/Reply-To rules.</read_first>
  <behavior>
    - The provider call rehydrates sender, recipient, reply-to, headers, text, filename ordering, and bytes from the persisted snapshot.
    - The Message-ID header and SDK idempotency option both use the stored logical Message-ID on every call.
    - The gateway returns a classified result and does not flip database state around the external call.</behavior>
  <action>Introduce the narrow `send_reserved_outbound_snapshot` gateway method alongside the existing caller-argument `gateway.send_outbound`; do not delete, rename, or change the legacy method yet because the current confirmation and clarification producers still invoke it. Preserve API-key setup, existing RFC threading and Reply-To behavior as stored reservation data, byte encoding at the provider boundary, and ID/category-only logging. The new method alone passes the stored Message-ID through Resend's idempotency option on every call and returns success or the D-02/D-06 bounded classification for later fenced settlement; hold no transaction across Resend and do not mint IDs, rewrite reservations, regenerate attachments, or persist raw provider material in this layer. Do not enqueue a SEND_OUTBOUND job or change any producer in this plan; Plans 04 and 10 perform those migrations after Plan 05, while Plan 12 closes the compatibility path.</action>
  <acceptance_criteria>Two calls for the same snapshot produce byte-equivalent Resend params and the same idempotency key; the additive capability creates no producer-side queue work and leaves the still-live legacy caller path importable until both producers migrate.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_gateway.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Gateway tests prove the new adapter sends fixed-key, byte-identical snapshot payloads while the existing caller-argument method remains callable for the unmigrated producers.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Same key is sent with drifted payload and loses provider deduplication | Tampering | high | mitigate | D-04 snapshot-only gateway input and byte-identical request tests. |
| Expired idempotency key permits an automatic duplicate email | Repudiation | high | mitigate | D-03 reservation-time ladder refuses any send at/after 20 hours. |
| Payload mismatch triggers automatic fresh key creation | Tampering | high | mitigate | D-06 terminal bounded classification with no ID generation in gateway. |
</threat_model>

<verification>
Run `uv run pytest tests/test_gateway.py tests/test_send_idempotency.py tests/test_orchestrator_states.py -q` and `uv run mypy app/email/gateway.py app/pipeline/result.py`. Do not enqueue SEND_OUTBOUND in this wave; Plan 05 is the first executable consumer, Plans 04 and 10 migrate the confirmation and clarification producers afterward, and Plan 12 removes or fail-closes the old caller-argument route only when both migrations pass.
</verification>

<success_criteria>
Every provider request is a deterministic projection of one persisted reservation under its original key, with only classified transient failures eligible for later replay.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-03-SUMMARY.md` when done.</output>
