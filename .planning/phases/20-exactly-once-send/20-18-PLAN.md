---
phase: 20-exactly-once-send
plan: 18
type: execute
wave: 12
depends_on: [20-17]
files_modified:
  - app/queue/handlers/send_outbound.py
  - app/db/repo/job_settlement.py
  - tests/conftest.py
  - tests/test_queue_durability.py
  - tests/test_phase20_fake_parity.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-07/D-08: a SEND_OUTBOUND snapshot whose epoch differs from the locked run.reply_epoch cannot reach the provider, settle as current delivery, or enter the current run's delivery review."
    - "Under the locked settlement transaction, stale-epoch handling performs no attempt, send-state, payroll-status, or review write and preserves the current epoch's reservation and Message-ID."
    - "The final-lease reaper applies the same epoch fence before appending review evidence or changing payroll_runs."
  artifacts:
    - path: app/queue/handlers/send_outbound.py
      provides: "Pre-provider snapshot/message epoch validation against the loaded run reply_epoch"
    - path: app/db/repo/job_settlement.py
      provides: "Locked-run epoch validation and stale-context no-write boundary in delivery settlement and final lease reaping"
    - path: tests/conftest.py
      provides: "InMemoryRepo stale-epoch parity for handler settlement and final reaping"
    - path: tests/test_queue_durability.py
      provides: "Provider-free stale epoch settlement and final-lease regressions"
    - path: tests/test_phase20_fake_parity.py
      provides: "Ordinary stale-epoch fake proof with no current reservation mutation"
  key_links:
    - "send_outbound.handle_send_outbound loads the run before gateway.send_reserved_outbound_snapshot and compares snapshot epoch with run.reply_epoch."
    - "settle_outbound_delivery_job and reap_expired_final_attempt lock message epoch plus payroll_runs.reply_epoch and reject stale work without touching the current slot; Plan 20-19 owns outcome mapping and exact-token retirement."
---

<objective>
Fence stale-epoch outbound jobs at every delivery boundary after a human retrigger.

Purpose: a durable job for epoch 0 must not send or settle epoch-0 bytes after the run has moved to epoch 1, while the newly reserved epoch-1 slot remains intact.
Output: handler, settlement, final-reaper, fake-parity, and provider-spy regressions for stale epochs.
Plan boundary: this plan establishes epoch fencing and provider-free/no-write stale handling; Plan 20-19 introduces the distinct queue outcomes and durable exact-token retirement.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-13-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-15-SUMMARY.md
@app/queue/handlers/send_outbound.py
@app/db/repo/job_settlement.py
@tests/test_queue_durability.py
@tests/test_phase20_fake_parity.py
@tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reject stale send epochs before provider work and settlement writes</name>
  <files>app/queue/handlers/send_outbound.py, app/db/repo/job_settlement.py, tests/test_queue_durability.py</files>
  <read_first>Read _snapshot_matches_job, handle_send_outbound, _lock_outbound_reservation, _lock_run_status, settle_outbound_delivery_job, reap_expired_final_attempt, and the existing exact-email fence tests in tests/test_queue_durability.py.</read_first>
  <behavior>
    - A handler with snapshot/message epoch 0 and loaded run reply_epoch 1 returns a bounded no-op before gateway.send_reserved_outbound_snapshot is called.
    - Settlement locks the immutable message epoch and the run's reply_epoch, and an old-epoch exact lease cannot append an attempt, mark the old email sent, or mutate the current run.
    - Final-lease reaping rejects an old-epoch send job without creating DeliveryReview/ClarificationDeliveryReview evidence for the current run.
  </behavior>
  <action>Per D-07, D-08, and SEND-02, extend the handler's `_snapshot_matches_job`/loaded-run authorization boundary to require the snapshot's `epoch` (the persisted email_messages epoch) to equal the loaded run's integer `reply_epoch` before invoking the snapshot-only gateway; return the existing bounded no-op for stale context. In `job_settlement.py`, extend `_lock_outbound_reservation` to return the persisted message epoch, lock the run context including `reply_epoch`, and reject an epoch mismatch before `_append_delivery_attempt`, send_state mutation, payroll status mutation, or provider-result review writes. Apply the same comparison in `reap_expired_final_attempt`; stale final leases must be rejected without changing the current reservation or run state. Keep D-03's reservation timestamp cutoff, D-04's frozen snapshot, D-05's approved state, and the four-reason replay allowlist unchanged. Add tests named `test_send_handler_noops_before_gateway_for_stale_epoch`, `test_delivery_settlement_rejects_stale_epoch_without_current_reservation_mutation`, and `test_final_send_lease_rejects_stale_epoch_without_current_review_mutation`; use a provider spy and assert no gateway call, no attempt insert, no old/current email mutation, and no current-run review transition. Leave distinct queue-outcome mapping and exact-token retirement to Plan 20-19.</action>
  <acceptance_criteria>Every stale epoch is rejected before provider work and again under the locked settlement/reaper transaction; no attempt, send-state, payroll-status, or review write occurs, and the current epoch's reservation remains available.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_queue_durability.py tests/test_send_idempotency.py</automated></verify>
  <done>Handler, normal settlement, and final-lease reaping each enforce the epoch fence with non-vacuous provider-free and no-mutation regressions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror stale-epoch no-write handling in the in-memory repository</name>
  <files>tests/conftest.py, tests/test_phase20_fake_parity.py</files>
  <read_first>Read InMemoryRepo.load_outbound_snapshot, settle_outbound_delivery_job, reap_expired_final_attempt, claim_job, current-epoch header routing, and the existing fake parity tests for purpose-aware final leases.</read_first>
  <behavior>
    - A fake stale-epoch send settlement follows the pre-20-19 fenced/no-op boundary, does not append delivery evidence, and does not change the current run or snapshot.
    - A fake stale-epoch final lease is rejected without entering a current-purpose review.
    - A fake stale-epoch handler path is provider-free and preserves the epoch-1 reservation.
  </behavior>
  <action>Update the InMemoryRepo mirrors in tests/conftest.py to read the run's current `reply_epoch` alongside the snapshot/message epoch before fake send settlement and final reaping, matching the production stale-job disposition and keeping the current row untouched. Extend tests/test_phase20_fake_parity.py with `test_fake_stale_epoch_send_settlement_rejects_without_mutation` and `test_fake_stale_epoch_final_lease_rejects_without_mutation`; assert `delivery_attempts` is unchanged and the epoch-1 snapshot/Message-ID remains reserved. Preserve existing fake behavior for current-epoch confirmation and clarification purposes; do not make fake-only decisions about provider delivery or introduce Plan 20-19's named settlement outcomes.</action>
  <acceptance_criteria>Ordinary tests cannot pass through a stale fake send that production would reject, and fake evidence proves stale no-write handling while preserving the current reservation.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_fake_parity.py tests/test_send_idempotency.py tests/test_clarify.py</automated></verify>
  <done>The fake reproduces the production epoch fence and remains suitable for route/queue regressions without pretending to be live Postgres evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Queue job/snapshot -> provider | Durable identifiers and persisted epoch facts authorize an external send. |
| Provider result -> settlement/reaper | An ambiguous result can otherwise mutate durable payroll and delivery state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-18-01 | Tampering | `send_outbound.py` handler | critical | mitigate | Compare snapshot/message epoch with loaded `payroll_runs.reply_epoch` before the gateway call and prove the gateway spy is untouched. |
| T-20-18-02 | Tampering | `settle_outbound_delivery_job` | critical | mitigate | Lock persisted epoch and current run epoch, and reject stale context before attempt/send/status/review writes; Plan 20-19 owns lease retirement. |
| T-20-18-03 | Denial of service | `reap_expired_final_attempt` | high | mitigate | Apply the same epoch predicate and leave stale send work outside current review mutation; Plan 20-19 owns durable queue retirement. |
| T-20-18-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; retain the repository's existing slopcheck and human package gate policy. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_queue_durability.py tests/test_phase20_fake_parity.py tests/test_send_idempotency.py</automated>
<automated>uv run ruff check app/queue/handlers/send_outbound.py app/db/repo/job_settlement.py tests/conftest.py tests/test_phase20_fake_parity.py tests/test_queue_durability.py</automated>
</verification>

<success_criteria>
No old-epoch SEND_OUTBOUND job can reach the provider, settle as the current delivery, or create current-epoch review evidence; locked stale handling preserves the current reservation and key. Durable retirement and named fencing outcomes are introduced by Plan 20-19.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-18-SUMMARY.md` when done.</output>
