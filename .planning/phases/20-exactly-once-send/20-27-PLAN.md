---
phase: 20-exactly-once-send
plan: 27
type: execute
wave: 21
depends_on: [20-26]
files_modified:
  - app/db/schema.sql
  - tests/test_send_idempotency.py
  - tests/test_queue_durability.py
autonomous: false
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
user_setup:
  - service: resettable PostgreSQL
    why: "The fresh/deployed schema and two-connection queueproof acceptance checks are destructive by design and must execute with a real DATABASE_URL plus ALLOW_DB_RESET=1."
    env_vars:
      - name: DATABASE_URL
        source: "A disposable/resettable PostgreSQL database"
      - name: ALLOW_DB_RESET
        source: "Set exactly to 1 only for that disposable database"
must_haves:
  truths:
    - "Both a fresh schema and the idempotent deployed-schema repair permit the bounded `authorization_expired` outbound-delivery attempt category emitted by settlement."
    - "On the disposable seeded_db target, a legacy failure_category-only CHECK that omits `authorization_expired` is repaired by `bootstrap(reset=False)`; the Postgres catalog then exposes the authoritative vocabulary and accepts a real `authorization_expired` attempt INSERT."
    - "Real PostgreSQL proves both pre-authorization expiry and provider-boundary expiry append the permitted category and enter purpose-aware review with zero provider call for the former."
    - "The already collected two-connection provider-handoff queueproof remains an explicitly guarded acceptance criterion: it is executed with DATABASE_URL and ALLOW_DB_RESET=1 and is not represented as passing until it reports zero skips."
  artifacts:
    - path: app/db/schema.sql
      provides: "Fresh-install and deployed-schema repair checks with identical outbound-delivery failure-category vocabulary including authorization_expired."
    - path: tests/test_queue_durability.py
      provides: "Guarded real-Postgres legacy-schema repair, pre-authorization expiry, and gateway-boundary expiry regressions plus the two-connection proof execution criterion."
    - path: tests/test_send_idempotency.py
      provides: "Static schema-drift guard for fresh/deployed category parity."
  key_links:
    - "The fresh CREATE TABLE check and the deployed constraint-repair block enumerate the same fixed failure categories, so a settlement result accepted by fake tests is also accepted by Postgres."
    - "The legacy-CHECK queueproof mutates only the resettable seeded_db target, calls the production non-reset bootstrap, and proves the repaired pg_constraint catalog and real INSERT rather than inferring repair from schema text."
    - "The integration tests use seeded_db, whose reset bootstrap installs app/db/schema.sql, then assert the actual outbound_delivery_attempts rows and review status after both expiry boundaries."
---

<objective>
Close the deployed-schema vocabulary drift and obtain guarded real-Postgres evidence for both safe-expiry paths.

Purpose: a delivery-review branch is not durable if the attempt-ledger INSERT rolls back under the actual database constraint. This plan gives fresh installs and deployed databases the same bounded category vocabulary, then requires an emulated deployed legacy schema and both expiry boundaries to demonstrate the production repair/result path.
Output: parity-safe schema SQL, a real legacy-CHECK repair queueproof, live-database expiry regressions, and a blocking evidence gate that keeps the existing two-connection queueproof honestly unverified until it runs without skips.

Artifacts this phase produces: schema constraint parity for `authorization_expired`; falsifiable fresh/repair schema guards; a resettable-Postgres deployed-schema repair regression that executes `bootstrap(reset=False)` after a legacy constraint is installed; pre-provider and provider-boundary expiry regressions; and recorded zero-skip acceptance commands for the provider-handoff race proof.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-23-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-26-SUMMARY.md
@app/db/schema.sql
@app/db/bootstrap.py
@app/db/repo/job_settlement.py
@app/queue/handlers/send_outbound.py
@tests/conftest.py
@tests/test_send_idempotency.py
@tests/test_queue_durability.py
@.github/workflows/concurrency-proof.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make fresh and deployed attempt-ledger constraints accept authorization expiry</name>
  <files>app/db/schema.sql, tests/test_send_idempotency.py</files>
  <read_first>Read the fresh `outbound_delivery_attempts` definition and its deployed failure-category constraint repair in app/db/schema.sql. Read `_delivery_failure_category` and the Plan 20-26 pre-provider settlement branch, plus `test_outbound_snapshot_schema_declares_append_only_evidence` and related schema-shape assertions in tests/test_send_idempotency.py.</read_first>
  <behavior>
    - The fresh table CHECK accepts the exact fixed `authorization_expired` failure category.
    - The deployed repair drops every prior failure-category-only CHECK and recreates one identical authoritative vocabulary containing `authorization_expired`; it does not change append-only triggers, attempt states, or payload/PII constraints.
    - Static regression tests fail if fresh and repair vocabularies diverge or if the emitted settlement category is absent from either path.
  </behavior>
  <action>Per D-03, D-07, D-08, D-10, D-12, D-13, and SEND-03, update the outbound_delivery_attempts failure-category list in both the fresh CREATE TABLE constraint and the idempotent deployed-schema repair block so each contains the same bounded authorization-expired value emitted by `_delivery_failure_category`. Keep the migration's existing constraint-column discovery and drop/re-add discipline; do not add an ORM migration, schema push, a hand-maintained requirements file, a raw provider diagnostic column, or any unbounded category. First strengthen the schema test to extract/compare the two relevant lists rather than checking only one literal, and assert the exact emitted fixed category belongs to both. Preserve existing final-attempt lease category coverage and append-only triggers.</action>
  <acceptance_criteria>A database initialized today and an already deployed database repaired by schema.sql enforce the same PII-safe attempt-category contract, including authorization expiry.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_send_idempotency.py -k 'outbound_snapshot_schema or authorization_expired or delivery_settlement'</automated></verify>
  <done>Schema-shape regression proves the fresh and deployed constraint paths cannot drift on the settlement vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add guarded real-Postgres deployed-schema repair and expiry regressions</name>
  <files>tests/test_queue_durability.py</files>
  <read_first>Read `seeded_db` in tests/conftest.py, the module-level integration/queueproof markers and reset/isolation contract in tests/test_queue_durability.py, existing cutoff/final-lease delivery tests, the Plan 20-26 handler/settlement tests, and the current provider-handoff queueproofs from Plan 20-23. Read `bootstrap(reset=False)` and the failure-category constraint-discovery block in app/db/schema.sql to confirm how the production non-reset repair discovers, drops, and recreates the deployed CHECK.</read_first>
  <behavior>
    - On the guarded disposable seeded_db database, a test replaces every failure_category-only CHECK with one legacy CHECK whose otherwise-authoritative vocabulary omits `authorization_expired`, invokes `bootstrap(reset=False)`, then proves from `pg_constraint`/`pg_get_constraintdef` that exactly one repaired failure-category-only CHECK has the full authoritative vocabulary and that a real `needs_operator`/`authorization_expired` attempt INSERT succeeds for a valid snapshot.
    - A reservation made stale before authorization runs through the real handler and settlement with a passive provider spy, produces no provider call, enters the purpose-appropriate delivery review state, writes `needs_operator/authorization_expired`, completes the exact job, and leaves the frozen Message-ID/bytes unchanged.
    - A currently authorized frozen snapshot that expires at the fixed gateway budget boundary likewise settles through the real schema to the same bounded attempt category and purpose-aware review state, without a provider call.
    - All three regressions run under the marker-selected real-Postgres queueproof suite and would fail if the deployed repair does not discover/drop/re-add the legacy constrained column, the repaired catalog omits the category, the handler erases pre-provider expiry, or review settlement becomes generic invalid-context retirement.
  </behavior>
  <action>Add focused seeded_db integration regressions in tests/test_queue_durability.py, using the established confirmation and clarification setup/cleanup patterns and passive gateway spies. First add one isolated deployed-schema queueproof that uses the existing resettable seeded_db connection only: discover and drop the current failure_category-only CHECK(s), install a deliberately legacy failure-category-only CHECK that has every previous category but omits `authorization_expired`, commit that emulation, and call the production `app.db.bootstrap.bootstrap(reset=False)`. After that non-destructive production repair returns, query `pg_constraint` joined to the constrained column and `pg_get_constraintdef`; assert exactly one failure_category-only CHECK remains and extract/assert its complete fixed vocabulary equals the authoritative category set, including `authorization_expired`. Seed or reserve a valid snapshot through the established production helper, then execute a real SQL INSERT of its `needs_operator`/`authorization_expired` attempt and assert the committed row is returned. This narrow direct DDL is permitted only in this legacy-emulation test, only after seeded_db has applied its destructive two-factor reset, and must not point at a separate or unguarded database. Retain the fresh-schema regression and Task 1's fake/static parity test; the new proof is specifically for the deployed repair path.

For the pre-authorization case, persist an otherwise valid frozen slot whose DB reservation timestamp is at or beyond the 20-hour boundary, claim its exact SEND_OUTBOUND job, execute the real handler then the real settlement, and assert zero provider calls, one `needs_operator`/`authorization_expired` attempt row, expected DeliveryReview or ClarificationDeliveryReview reason, a done exact job, no pending/leased replacement, and byte-identical frozen snapshot content. For the provider-boundary case, acquire the real provider authorization while its reservation is still eligible, invoke the real handler/gateway boundary with an injected clock at the strict fixed-timeout-plus-margin boundary, then settle and assert the same live SQL facts and no provider call. Parameterize only across the established purpose/status/review-reason combinations so confirmation and clarification remain purpose-aware. Keep all tests protected by seeded_db and the module's integration/queueproof marker path; do not call an HTTP route or live Resend. Prove each test is non-vacuous by asserting the exact constrained row, and for the repair test the actual Postgres catalog plus the successful INSERT, not merely a run status or schema text.</action>
  <acceptance_criteria>Against a resettable Postgres database, the production non-reset bootstrap demonstrably upgrades an emulated legacy category CHECK and permits a real `authorization_expired` INSERT; both expiry boundaries also prove the production constraint accepts the bounded category and the delivery outcome is review rather than silent retirement or resend.</acceptance_criteria>
  <verify><automated>DATABASE_URL="$DATABASE_URL" ALLOW_DB_RESET=1 uv run pytest -q tests/test_queue_durability.py -m 'integration and queueproof' -k 'authorization_expired or deployed_schema_repair or provider_handoff' -rs</automated></verify>
  <done>The production database—not only the fake—demonstrates that deployed-schema repair accepts the new category and both expiration paths preserve immutable evidence, avoid provider I/O, and commit the bounded review ledger fact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply and verify the resettable production-schema evidence</name>
  <files>app/db/schema.sql, tests/test_queue_durability.py</files>
  <read_first>Read the two-factor `seeded_db` guard, bootstrap reset and non-reset behavior, Task 2's legacy-CHECK repair and live expiry tests, `test_provider_handoff_blocks_epoch_bump_before_gateway`, `test_provider_handoff_race_control_observes_stale_gateway_when_fence_is_released`, and the zero-skip guard in .github/workflows/concurrency-proof.yml.</read_first>
  <action>[BLOCKING] Use only a disposable/resettable PostgreSQL DATABASE_URL and set ALLOW_DB_RESET=1. Execute the focused marker-selected live command from Task 2, including its emulated legacy-CHECK proof that calls `bootstrap(reset=False)` after seeded_db has reset/applied schema.sql, then execute the two existing provider-handoff tests with the same environment. The seeded_db bootstrap is the destructive fresh-schema application mechanism and the repair test's explicit non-reset bootstrap invocation is the deployed-schema verification mechanism; do not substitute an ORM migration, a fake test, static schema parity, or test collection for either execution. Record the exact pytest summaries. A missing DATABASE_URL, missing ALLOW_DB_RESET=1, any skipped selected test, or any failure leaves this task incomplete and leaves the Phase 20 behavior-unverified queueproof item explicitly unclosed; do not claim a pass or deployment repair from an unavailable guarded run.</action>
  <acceptance_criteria>The live legacy-CHECK repair regression, both live expiry-boundary regressions, and the two existing provider-handoff protected/control tests execute against schema.sql with DATABASE_URL and ALLOW_DB_RESET=1, report zero skips, and pass; otherwise the plan summary records unavailable/failed evidence without claiming closure.</acceptance_criteria>
  <verify><automated>DATABASE_URL="$DATABASE_URL" ALLOW_DB_RESET=1 uv run pytest -q tests/test_queue_durability.py -m 'integration and queueproof' -k 'authorization_expired or deployed_schema_repair or provider_handoff' -rs</automated></verify>
  <how-to-verify>1. Point DATABASE_URL only at a disposable database. 2. Set ALLOW_DB_RESET=1. 3. Run the exact automated command. 4. Confirm the legacy-CHECK repair, expiry, and provider_handoff tests all report passed with zero skipped. 5. If credentials are unavailable or pytest skips, retain the behavior-unverified status instead of approving the phase.</how-to-verify>
  <resume-signal>Provide the zero-skip pytest summary, or report the unavailable/failed guard condition.</resume-signal>
  <done>Live Postgres evidence is either recorded as zero-skip passing or truthfully retained as unverified; no skipped proof is treated as a successful verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Repository category → Postgres CHECK constraint | A fixed safe result must be accepted by the production ledger rather than silently rolled back. |
| Expiry boundary → provider gateway | Both pre-authorization and gateway-budget expiry must stop before any email provider side effect. |
| Guarded resettable DB → phase evidence | The reset capability is destructive and the zero-skip status determines whether a real concurrency claim is evidenced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-27-01 | Tampering | schema vocabulary drift | critical | mitigate | Single identical bounded category set is asserted for fresh create and deployed repair; a seeded legacy CHECK is repaired by the real non-reset bootstrap, verified through the Postgres catalog, and exercised by a real INSERT. |
| T-20-27-02 | Repudiation | delivery-review evidence | critical | mitigate | Live tests assert the exact append-only attempt row, job state, review reason, and frozen identity for both expiry boundaries. |
| T-20-27-03 | Tampering | provider boundary | critical | mitigate | Passive spies assert zero provider calls before settlement at both elapsed-time boundaries. |
| T-20-27-04 | Denial of service | database reset | high | mitigate | The blocking task requires the existing DATABASE_URL plus ALLOW_DB_RESET=1 two-factor guard and a disposable target. |
| T-20-27-05 | Repudiation | queueproof evidence | high | mitigate | Treat skips as unavailable evidence; require zero skips for the pre-existing two-connection proof before verification may close. |
| T-20-27-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; all commands use the committed uv environment. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_send_idempotency.py tests/test_gateway.py tests/test_phase20_fake_parity.py</automated>
<automated>uv run pytest tests/ -m queueproof --collect-only -q | rg 'authorization_expired|deployed_schema_repair|test_provider_handoff_(blocks_epoch_bump_before_gateway|race_control_observes_stale_gateway_when_fence_is_released)'</automated>
<automated>DATABASE_URL="$DATABASE_URL" ALLOW_DB_RESET=1 uv run pytest -q tests/test_queue_durability.py -m 'integration and queueproof' -k 'authorization_expired or deployed_schema_repair or provider_handoff' -rs</automated>
<automated>uv run ruff check app/db/schema.sql tests/test_send_idempotency.py tests/test_queue_durability.py</automated>
<automated>uv run mypy</automated>
</verification>

<success_criteria>
Fresh and repaired Postgres schemas accept the bounded authorization-expired ledger event; an emulated deployed legacy CHECK is upgraded by the production non-reset bootstrap and proven by catalog inspection plus a real INSERT; both expiry boundaries reach delivery review without provider I/O; and the guarded two-connection proof remains explicitly unverified until a zero-skip live execution exists.
</success_criteria>

## Source coverage audit

| Source | ID / item | Coverage | Status |
|---|---|---|---|
| GOAL | At most one confirmation per approved run and epoch | Real schema evidence proves stale delivery is reviewed without sending or creating a second slot. | COVERED |
| REQ | SEND-01 | Live expiry paths retain their original frozen Message-ID and do not create a replacement send. | COVERED |
| REQ | SEND-02 | Live assertions preserve snapshot bytes/content and provider spies observe no regeneration/send. | COVERED |
| REQ | SEND-03 | Fresh/deployed constraint parity and both real expiration boundaries implement the bounded provider window and human escalation. | COVERED |
| RESEARCH | Provider deadline, real-Postgres proof, append-only PII-safe evidence, no ORM migration | Tests execute both fresh bootstrap and a legacy-CHECK-to-non-reset-bootstrap repair on seeded_db, then inspect fixed catalog and attempt facts only. | COVERED |
| CONTEXT | D-01 | Only durable queued work is executed; no synchronous fallback is introduced. | COVERED |
| CONTEXT | D-02 | Expiry remains non-replayable classification. | COVERED |
| CONTEXT | D-03 | Tests use the database reservation deadline and the fixed provider budget boundary. | COVERED |
| CONTEXT | D-04 | Frozen snapshots are asserted unchanged and no recomposition is invoked. | COVERED |
| CONTEXT | D-05 | Review occurs only after the automatic replay window has closed. | COVERED |
| CONTEXT | D-06 | No automatic replacement key is created. | COVERED |
| CONTEXT | D-07 | Both paths require exact append-only attempt facts. | COVERED |
| CONTEXT | D-08 | Both paths enter purpose-aware needs_operator delivery review. | COVERED |
| CONTEXT | D-09 | The existing explicit review outcomes remain the only next action after real expiry. | COVERED |
| CONTEXT | D-10 | Live assertions inspect fixed categories and safe review fields, never raw provider data. | COVERED |
| CONTEXT | D-11 | No test or schema path authorizes a new confirmation automatically. | COVERED |
| CONTEXT | D-12 | seeded_db schema and tests retain the immutable envelope/bytes before any provider action. | COVERED |
| CONTEXT | D-13 | The constraint governs append-only attempt evidence separately from immutable content. | COVERED |
| VERIFICATION GAP | CR-01 pre-provider reason loss | Plan 20-26 maps and settles the reason; Task 2 proves the durable production outcome. | COVERED |
| VERIFICATION GAP | authorization_expired schema drift | Task 1 aligns fresh/deployed checks; Task 2 installs a legacy category-only CHECK on seeded_db, invokes `bootstrap(reset=False)`, and proves repaired catalog parity plus a real category INSERT. | COVERED |
| VERIFICATION GAP | provider-handoff behavior unverified | Task 3 preserves the guarded zero-skip real-Postgres acceptance criterion; it does not claim a pass absent evidence. | COVERED |

<output>Create `.planning/phases/20-exactly-once-send/20-27-SUMMARY.md` when done.</output>
