---
phase: 20-exactly-once-send
plan: 25
type: execute
wave: 18
depends_on: [20-24]
files_modified:
  - app/db/repo/pipeline_state.py
  - app/routes/runs.py
  - tests/test_retrigger_epoch.py
  - tests/test_phase20_clarification_review.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "Generic retrigger cannot commit a status claim, reply_epoch increment, job enqueue, or wake while an active exact provider handoff exists."
    - "Only D-09 mark-delivered and D-11 typed new-confirmation authorization can release or supersede a lingering ambiguous handoff."
    - "The explicit new confirmation remains byte-identical to its original frozen snapshot under a distinct human-authorized slot."
  artifacts:
    - path: app/db/repo/pipeline_state.py
      provides: "Active-handoff-aware reply-context clearing guard."
    - path: app/routes/runs.py
      provides: "Rollback-safe generic retrigger and review-only delivery-handoff resolution."
  key_links:
    - "clear_reply_context calls assert_no_active_outbound_provider_handoff while the caller transaction owns the run row and before incrementing reply_epoch."
    - "retrigger lets ProviderHandoffActive escape its transaction so its earlier status CAS rolls back before the bounded 303 response."
---

<objective>
Fence every browser path that can advance an epoch behind the durable provider handoff, while preserving the two deliberate delivery-review outcomes.

Purpose: Plan 20-22 prevents an unauthorized provider call and Plan 20-24 settles it; generic operator retry must still be unable to cross the durable fence between those steps.
Output: rollback-safe retrigger and purpose-aware D-09/D-11 handoff resolution with focused browser-contract regressions.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-21-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-24-SUMMARY.md
@app/db/repo/pipeline_state.py
@app/routes/runs.py
@tests/test_retrigger_epoch.py
@tests/test_phase20_clarification_review.py
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Prevent generic retrigger from crossing an active provider handoff</name>
  <files>app/db/repo/pipeline_state.py, app/routes/runs.py, tests/test_retrigger_epoch.py, tests/test_phase20_clarification_review.py</files>
  <read_first>Read `clear_reply_context`, `retrigger`, `mark_delivery_delivered`, `authorize_new_confirmation`, their transaction/303 boundaries, and the Plan 20-21 `assert_no_active_outbound_provider_handoff` and review-only handoff APIs. Read existing epoch and confirmation-review route tests before editing.</read_first>
  <behavior>
    - Generic retrigger meeting an active handoff returns its established bounded 303 result with no committed status change, epoch increment, run job, or wake.
    - A released/completed handoff retains the normal clear-and-enqueue behavior.
    - D-09 mark-delivered and D-11 typed authorization are the only browser actions allowed to resolve an ambiguous active handoff; the authorization action preserves frozen byte identity in its new explicit slot.
  </behavior>
  <action>Call `assert_no_active_outbound_provider_handoff` from `clear_reply_context` while its caller-owned transaction holds the payroll-run row and before the reply-epoch UPDATE. Raise `ProviderHandoffActive` before any epoch mutation. Refactor retrigger so this exception escapes the transaction block, rolls back an earlier status CAS, then returns the existing bounded 303 no-op without enqueueing or waking; do not catch it inside the transaction. Preserve D-01 automatic queue ownership and D-05 business-status behavior. Wire only D-09 `mark_delivery_delivered` and D-11 `authorize_new_confirmation` to the Plan 20-21 review-only release/supersession seam before their existing final status or snapshot-clone work. Preserve D-10's safe projection and D-11's byte-identical frozen snapshot clone; generic retrigger must never call either override. Add route/repository regressions proving active generic retrigger rolls back all run/job/epoch state, settled handoff permits ordinary retrigger, mark-delivered completes without new send, and only a correct typed acknowledgement can authorize the distinct frozen repeat.</action>
  <acceptance_criteria>No generic browser operation can create a new epoch while an old snapshot's durable handoff is unresolved; the explicit review outcomes remain narrowly authorized and auditable.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_retrigger_epoch.py tests/test_phase20_clarification_review.py tests/test_delivery.py</automated></verify>
  <done>Reply-epoch changes, review actions, and post-commit wakes honor the same provider-handoff fence as the worker.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser retrigger POST → reply epoch | A generic action must not supersede a pending provider handoff. |
| Delivery-review POST → handoff lifecycle | Explicit human actions can resolve ambiguity but require purpose and acknowledgement checks. |
| Review clone → immutable snapshot | New authorization may create a distinct slot but cannot use changed content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-25-01 | Elevation of privilege | generic retrigger | critical | mitigate | Raise active-handoff guard before epoch UPDATE and roll back caller transaction. |
| T-20-25-02 | Tampering | review resolution | critical | mitigate | Permit only D-09/D-11 routes to release/supersede exact ambiguous handoff. |
| T-20-25-03 | Repudiation | authorization acknowledgement | high | mitigate | Retain typed acknowledgement and fixed review actions. |
| T-20-25-04 | Information disclosure | route responses | medium | mitigate | Preserve bounded 303/review projections; do not render provider dumps. |
| T-20-25-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; use `uv run` only. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_retrigger_epoch.py tests/test_phase20_clarification_review.py tests/test_delivery.py</automated>
<automated>uv run ruff check app/db/repo/pipeline_state.py app/routes/runs.py tests/test_retrigger_epoch.py tests/test_phase20_clarification_review.py</automated>
<automated>uv run mypy</automated>
</verification>

<success_criteria>
The durable provider fence is linearizable with every epoch-changing browser action, except for the two deliberately reviewed outcomes mandated by D-09 and D-11.
</success_criteria>

## Source coverage audit

| Source | ID / item | Coverage | Status |
|---|---|---|---|
| GOAL | At most one confirmation per approved run and epoch | Generic retrigger cannot supersede the epoch while provider delivery remains unresolved. | COVERED |
| REQ | SEND-01 | Prevents a generic action from creating a replacement send slot/key mid-handoff. | COVERED |
| REQ | SEND-02 | D-11 explicitly clones the original frozen snapshot, never a new payload. | COVERED |
| REQ | SEND-03 | Uncertain delivery remains delivery review rather than an automatic replacement request. | COVERED |
| RESEARCH | Fenced queue ownership and PII-safe browser boundary | Uses typed durable guard and bounded route outcomes. | COVERED |
| CONTEXT | D-01 through D-13 | D-01/D-05 enforce queue/status rules; D-08/D-09/D-10/D-11 govern review; D-04/D-12/D-13 preserve immutable content/audit. | COVERED |

<output>Create `.planning/phases/20-exactly-once-send/20-25-SUMMARY.md` when done.</output>
