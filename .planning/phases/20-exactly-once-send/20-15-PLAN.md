---
phase: 20-exactly-once-send
plan: 15
type: execute
wave: 10
depends_on: [20-05, 20-12, 20-13, 20-14]
files_modified:
  - tests/conftest.py
  - tests/test_phase20_fake_parity.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "The InMemoryRepo accepts SEND_OUTBOUND only with production-equivalent run/email identifiers, dedup key, and max_attempts=8."
    - "Malformed send context is rejected before fake state changes, and duplicate enqueue returns the same no-new-row behavior as production."
    - "D-02: fake settlement automatically reschedules only DELIVERY_TIMEOUT, DELIVERY_CONNECTION_FAILURE, DELIVERY_RATE_LIMIT, and DELIVERY_SERVER_FAILURE; every other retryable reason goes directly to bounded review."
    - "Fake delivery review attempt_count reflects persisted append-only attempt facts, and fake final-lease/header behavior mirrors the production safety boundaries."
  artifacts:
    - path: tests/conftest.py
      provides: "Production-parity queue, attempt ledger, review projection, final-reaper, and epoch-routing fake"
    - path: tests/test_phase20_fake_parity.py
      provides: "Malformed-context, eight-attempt, strict replay-allowlist, attempt-count, stale-header, and purpose-aware fake regressions"
  key_links:
    - "InMemoryRepo.enqueue_job validation -> fake jobs row shape -> claim/settlement attempt budget"
    - "Fake settlement/reaper attempt ledger -> load_delivery_review_snapshot.attempt_count"
    - "Fake settlement replay gate -> exact four-reason production allowlist -> automatic reschedule only for DELIVERY_TIMEOUT, DELIVERY_CONNECTION_FAILURE, DELIVERY_RATE_LIMIT, and DELIVERY_SERVER_FAILURE; every other retryable reason -> review"
    - "Fake awaiting-reply header routing -> run.reply_epoch and outbound row.epoch"
    - "Fake clarification review retry -> advance_existing_clarification_delivery_review_job_due_now -> same job/reservation/Message-ID with no provider call"

---

<objective>
Bring the in-memory repository used by ordinary tests into behavioral parity with the production SEND_OUTBOUND safety contract.

Purpose: fake-backed tests must be able to falsify malformed queue context, budget drift, stale epoch routing, missing attempt evidence, and purpose-aware final-lease behavior instead of silently accepting an easier model.
Output: a production-shaped fake and paired default-running regressions; guarded database proofs remain separate and are never replaced by fake evidence.
</objective>

## Artifacts this phase produces

- Exact SEND_OUTBOUND validation, deduplication, and forced eight-attempt budget in `InMemoryRepo`.
- An in-memory append-only attempt ledger that drives review counts and purpose-aware final-lease outcomes.
- Fake current-epoch header routing, constrained legacy state mutation, and tests that distinguish ordinary evidence from guarded live evidence.

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-REVIEW.md
@.planning/phases/20-exactly-once-send/20-05-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-12-SUMMARY.md
@tests/conftest.py
@tests/test_repo_jobs_sql.py
@tests/test_send_idempotency.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mirror SEND_OUTBOUND validation, deduplication, and eight-attempt budget in InMemoryRepo</name>
  <files>tests/conftest.py, tests/test_phase20_fake_parity.py</files>
  <read_first>Read production enqueue_job's SEND_OUTBOUND branch and _SEND_OUTBOUND_MAX_ATTEMPTS, the existing InMemoryRepo.enqueue_job and claim_job, and the production/fake pairing conventions.</read_first>
  <action>Per D-01, D-07, D-12, and SEND-03, add the missing fake branch before any fake job mutation: require run_id, email_id, the exact send_outbound:{email_id} dedup key, and no operator-resolution, event, or business context; force max_attempts to 8 and reject caller-provided values other than 8. Preserve validation-before-state-change, duplicate dedup returning None, identifier-only job fields, claim-time attempt increments, and the existing contracts for all other JobKinds. Add parameterized malformed-context tests covering wrong dedup, missing run/email, mixed identifiers, and an invalid attempt budget, plus a paired assertion that a valid send job stores eight attempts and a duplicate creates no second row. Do not let the fake default to the generic five-attempt budget or accept a send payload.</action>
  <acceptance_criteria>Every malformed SEND_OUTBOUND call raises before `self.jobs` changes; a valid call has max_attempts=8 and exact dedup behavior; existing job kinds remain unchanged.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_fake_parity.py tests/test_repo_jobs_sql.py tests/test_job_kind_drift.py</automated></verify>
  <done>Default-running fake tests and production SQL tests agree on send context, dedup, identifier-only shape, and eight-attempt budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fake attempt evidence, purpose-aware reaping, epoch routing, and safe state mutation</name>
  <files>tests/conftest.py, tests/test_phase20_fake_parity.py</files>
  <read_first>Read InMemoryRepo.settle_outbound_delivery_job, reap_expired_final_attempt, load_delivery_review_snapshot, advance_existing_send_job_due_now, find_awaiting_reply_for_header, update_email_message_state, the fake_repo registration tuple, and the corrected production contracts from Plans 13 and 14.</read_first>
  <action>Per D-02, D-07, D-08, D-10, D-12, and D-13, add a fake append-only delivery-attempt collection and append one bounded fact for fake success, replay scheduling, review, and final-lease ambiguity. Make fake settlement enforce the same strict production replay allowlist: automatic reschedule is permitted only when the outcome is RETRYABLE and the reason is exactly DELIVERY_TIMEOUT, DELIVERY_CONNECTION_FAILURE, DELIVERY_RATE_LIMIT, or DELIVERY_SERVER_FAILURE; every other retryable reason must append bounded review evidence and transition directly to needs_operator, never to pending automatic replay. Build the parameterized direct-review test set from every PipelineReason enum member outside that exact four-member allowlist, including DELIVERY_IDEMPOTENCY_PAYLOAD_MISMATCH, DELIVERY_QUOTA_EXHAUSTED, DELIVERY_VALIDATION_FAILURE, DELIVERY_AUTHENTICATION_FAILURE, DELIVERY_AUTHORIZATION_FAILURE, DELIVERY_CONFIGURATION_FAILURE, DELIVERY_PROVIDER_FAILURE, and UNCLASSIFIED, and assert each goes directly to review. Make load_delivery_review_snapshot derive attempt_count from those persisted fake facts and omit body_text from its review mapping while leaving body available through load_outbound_snapshot. Mirror production final-lease behavior for SEND_OUTBOUND: preserve the reservation, append review evidence, set status = needs_operator, set error_reason = DeliveryReview for confirmation or ClarificationDeliveryReview for clarification/clarification_field_regression, and set only the bounded safe final-lease detail; do not route to generic name resolution. Implement the exact InMemoryRepo method `advance_existing_clarification_delivery_review_job_due_now`, register that exact name in the fake_repo facade patch tuple, and return the production AdvanceSendJobOutcome values. It must require the same caller-owned transaction shape, same-row SEND_OUTBOUND identity, needs_operator/ClarificationDeliveryReview ownership, clarification purpose, and open 20-hour reservation; it only moves the existing job due and never calls a provider, creates a row, changes the frozen reservation, or changes Message-ID/round/epoch. Mirror the current-epoch predicate in find_awaiting_reply_for_header so a stale header after reply_epoch changes returns no current run, while find_any_run_for_header remains late-observability behavior. Restrict or retire the fake legacy state mutator in the same shape as production so inbound rows and invalid states cannot change. Add tests with one and multiple attempt facts, both outbound purposes, exact final-lease status/reason/detail, the named same-row retry method, the exact four-reason automatic-replay cases, parameterized direct-review coverage for every other retryable reason, a stale header after epoch bump, a malformed legacy state call, and a negative assertion that the review object has no body_text. Do not treat this fake suite as configured Postgres evidence.</action>
  <acceptance_criteria>Fake review attempt counts are nonzero when facts were appended; automatic fake rescheduling occurs only for DELIVERY_TIMEOUT, DELIVERY_CONNECTION_FAILURE, DELIVERY_RATE_LIMIT, and DELIVERY_SERVER_FAILURE; parameterized tests prove every other retryable reason goes directly to needs_operator review with no automatic replay. Final lease, current epoch, legacy state, and exact `advance_existing_clarification_delivery_review_job_due_now` boundaries match production; no fake review projection returns body_text or resolves aliases, and clarification retry performs no provider work.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_phase20_fake_parity.py tests/test_send_idempotency.py tests/test_clarify.py tests/test_dashboard.py</automated></verify>
  <done>Ordinary fake-backed regressions cover every warning in the verifier report without masking that live queueproof tests still require a configured database.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| A fake accepts a malformed send job that production rejects | Tampering | high | mitigate | Mirror exact identifiers, dedup, and fixed max_attempts=8 before fake mutation with parameterized tests. |
| Fake review evidence reports zero attempts and hides duplicate risk | Repudiation | high | mitigate | Persist bounded fake attempt facts and derive attempt_count from the ledger. |
| Fake tests replay an unsafe retryable delivery reason | Tampering | high | mitigate | Enforce the exact four-reason replay allowlist in fake settlement and parameterize every other retryable reason to direct review. |
| Fake tests authorize stale replies or generic clarification recovery | Tampering | high | mitigate | Mirror epoch/purpose-aware routing and final-reaper outcomes with stale-header and both-purpose tests. |
| Fake projection exposes frozen body text to broad callers | Information disclosure | high | mitigate | Remove body_text from the review mapping and test its absence. |
</threat_model>

<verification>
Run ordinary evidence with `uv run pytest -q tests/test_phase20_fake_parity.py tests/test_repo_jobs_sql.py tests/test_job_kind_drift.py tests/test_send_idempotency.py tests/test_clarify.py tests/test_dashboard.py`. No test in this plan may label a skipped `integration`/`queueproof` result as success; run the separate guarded command `uv run pytest -q -m 'integration and queueproof' tests/test_send_idempotency.py tests/test_queue_durability.py` only when reporting live evidence. Also run `uv run ruff check tests/conftest.py tests/test_phase20_fake_parity.py` and `uv run mypy tests/conftest.py`.
</verification>

<success_criteria>
The fake repository is not a permissive substitute for production: it rejects malformed send jobs, enforces the eight-attempt budget, records attempt history, preserves purpose/epoch boundaries, and hides body text from review projections.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-15-SUMMARY.md` when done.</output>
