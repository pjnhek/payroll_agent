---
phase: 20-exactly-once-send
plan: "02"
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - app/models/job.py
  - app/db/schema.sql
  - app/db/repo/jobs.py
  - app/db/repo/__init__.py
  - tests/conftest.py
  - tests/test_job_kind_drift.py
  - tests/test_repo_jobs_sql.py
autonomous: true
requirements: [SEND-01, SEND-03]
must_haves:
  truths:
    - "D-01: automatic and operator-accelerated retries own one durable send job rather than invoking a provider from a route."
    - "D-07: scheduled and accelerated retry work converges on one identifier-only, lease-fenced send job."
    - "SEND-03: queue vocabulary admits only a run plus persisted email/snapshot identifier for a send."
  artifacts:
    - app/models/job.py
    - app/db/repo/jobs.py
  key_links:
    - "JobKind, SQL checks, enqueue validation, and Job RETURNING mapping stay equal; dispatch registration waits for the executable fenced handler in Plan 05."
---

<objective>
Add the identifier-only durable transport vocabulary for a frozen outbound send.

Purpose: ensure every automatic send attempt is owned by one deduplicated queue job, never a browser or synchronous route.
Output: SEND_OUTBOUND job kind, constraints, validation, fake parity, and drift proofs; dispatch deliberately remains unavailable until Plan 05 supplies an executable fenced handler.

Artifacts this phase produces: a send-job identity contract keyed by the persisted reservation UUID; the existing-job-only `advance_existing_send_job_due_now` repository/facade operation; and queue-vocabulary, SQL-shape, and retry-now tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-01-SUMMARY.md
@app/models/job.py
@app/db/repo/jobs.py
@tests/test_job_kind_drift.py
@tests/test_repo_jobs_sql.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the exact send-outbound job context and SQL mirror</name>
  <files>app/models/job.py, app/db/schema.sql, app/db/repo/jobs.py, app/db/repo/__init__.py, tests/test_repo_jobs_sql.py</files>
  <read_first>Read JobKind/Job, enqueue_job validation, claim_job RETURNING, jobs table CHECK and repair blocks, and the SQL bijection tests.</read_first>
  <behavior>
    - SEND_OUTBOUND requires run_id and email_id, rejects event and operator-resolution identifiers, and uses a snapshot-derived dedup key.
    - Its queue row carries no body, attachment, next-status, or provider-result payload.
    - Existing context kinds retain their exact requirements and claim mapping.
    - `advance_existing_send_job_due_now` locks and advances exactly one already-pending send job for its run/snapshot, never INSERTs a second job, leaves a leased job untouched, and rejects a reservation at or beyond the 20-hour cutoff.</behavior>
  <action>Add the `send_outbound` JobKind across Python and both schema constraint forms, then extend enqueue_job with an exact context branch requiring the persisted outbound email/snapshot UUID and run UUID only. Add a deterministic repository dedup-key helper derived from the immutable send slot; select a max-attempt policy compatible with the reservation-time ladder rather than the generic short queue cap. Add the transaction-aware `advance_existing_send_job_due_now(run_id, email_id, conn=...)` facade/repository operation: lock the exact logical reservation and its one send job, verify the database reservation timestamp remains before D-03's cutoff, update only that existing pending job's due time, and return an outcome for the caller's post-commit wake. It must neither insert/enqueue a job, act on a leased job, invoke a provider, nor bypass later lease-token settlement. Preserve Job's identifier-only projection and update SQL-shape/fake parity tests so malformed or payload-like context is rejected before SQL. This implements D-01 and D-07 without granting a route a provider-send path.</action>
  <acceptance_criteria>Every send job is uniquely identified by persisted reservation context, has no business payload, and cannot be confused with a pipeline/reply/operator job.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_repo_jobs_sql.py tests/test_job_kind_drift.py -q</automated></verify>
  <done>Enum, SQL constraints, validation, and claim projection accept the send kind only with its exact two identifiers and reject all invalid combinations; retry-now can advance only one existing eligible pending job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve fake queue parity while withholding dispatch until settlement exists</name>
  <files>tests/conftest.py, tests/test_job_kind_drift.py, tests/test_repo_jobs_sql.py</files>
  <read_first>Read InMemoryRepo job methods, fake pairing inventories, the enum/dispatch equality tests, and the Plan 05 handler/settlement dependency described in this plan.</read_first>
  <behavior>
    - Fake jobs preserve run_id/email_id and dedup behavior required for D-07.
    - The pre-handler phase has no SEND_OUTBOUND dispatch entry, preventing an import of a nonexistent module or a producer-path claim.
    - After Plan 05, the existing enum/dispatch equality proof will require the executable late-bound handler registration.</behavior>
  <action>Extend the in-memory queue and pairing guards for the new context shape and `advance_existing_send_job_due_now`, then add non-vacuous SQL and fake tests proving that retry-now cannot create another job, cannot make a leased job due, cannot advance an expired reservation, and needs a caller-owned transaction before the route wakes. Keep SEND_OUTBOUND absent from `HANDLERS` and make the dispatch test assert the intentional pre-handler mismatch rather than importing a nonexistent module. No application producer is changed in Plans 02 or 03; Plan 04 is delayed until Plan 05 creates and registers the actual handler backed by Plan 09 settlement. Plan 05 replaces this temporary precondition with the normal enum-to-dispatch equality test; do not add a placeholder handler or provider call here.</action>
  <acceptance_criteria>Fake tests cannot silently omit the new contract, and the only pre-handler behavior for SEND_OUTBOUND is a fail-closed refusal rather than an import-time failure or stranded job.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_job_kind_drift.py tests/test_repo_jobs_sql.py -q</automated></verify>
  <done>Job-kind inventories and fake contracts are exact, retry-now is existing-job-only, and SEND_OUTBOUND remains deliberately non-enqueueable until the executable fenced handler plan lands.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| A route or malformed job injects mutable mail content into the queue | Tampering | high | mitigate | Exact kind/context validation, SQL CHECK mirror, no new payload fields, and fake/SQL tests. |
| A send kind exists before its consumer and is silently stranded | Denial of service | high | mitigate | Fail closed before Plan 05, then require enum-to-dispatch equality and non-vacuous handler-registration tests in Plan 05. |
| A wrong run/email pairing causes cross-run provider action | Elevation of privilege | high | mitigate | Require both identifiers now; handler ownership verification lands before provider calls in Plan 05. |
</threat_model>

<verification>
Run `uv run pytest tests/test_repo_jobs_sql.py tests/test_job_kind_drift.py -q` and `uv run mypy app/models/job.py app/db/repo/jobs.py`.
</verification>

<success_criteria>
The durable queue vocabulary owns one immutable send action identity and cannot transport untrusted mail data; only Plan 05 enables dispatch after fenced settlement is executable.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-02-SUMMARY.md` when done.</output>
