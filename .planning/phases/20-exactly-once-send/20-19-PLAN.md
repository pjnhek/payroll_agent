---
phase: 20-exactly-once-send
plan: 19
type: execute
wave: 13
depends_on: [20-18]
files_modified:
  - app/db/repo/job_settlement.py
  - app/queue/drain.py
  - tests/test_queue_drain.py
  - tests/test_queue_durability.py
autonomous: true
gap_closure: true
requirements: [SEND-02, SEND-03]
must_haves:
  truths:
    - "Queue fencing distinguishes a lost lease from an invalid-context lease held by this worker."
    - "An invalid-context SEND_OUTBOUND lease is durably completed, dead-lettered, or released before drain_once forgets its token."
    - "A lost-lease result performs no delivery evidence or job mutation, while an invalid-context result performs the exact current-lease retirement required for safe drain bookkeeping."
  artifacts:
    - path: app/db/repo/job_settlement.py
      provides: "Distinct LOST_LEASE and INVALID_CONTEXT outcomes and exact-token invalid-context retirement"
    - path: app/queue/drain.py
      provides: "Outcome mapping and token-discard behavior that only forgets a token after durable invalid-context retirement"
    - path: tests/test_queue_drain.py
      provides: "Drain regressions for stale/invalid context versus lost lease"
    - path: tests/test_queue_durability.py
      provides: "Settlement SQL and no-write/retirement proofs"
  key_links:
    - "settle_outbound_delivery_job invalid-context branch updates the exact leased job with state/lease-token predicates before returning INVALID_CONTEXT."
    - "drain_once maps INVALID_CONTEXT and LOST_LEASE separately and marks the held token settled only after the repository operation returns."
---

<objective>
Close the queue-fencing hole where drain_once discarded a token after an invalid-context settlement that had not retired the database lease.

Purpose: stale or malformed delivery context must not strand a leased row, and a genuinely reclaimed lease must remain a no-write loser.
Output: distinct settlement/drain outcomes, durable invalid-context retirement, and direct drain regressions.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-PATTERNS.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-18-SUMMARY.md
@app/db/repo/job_settlement.py
@app/queue/drain.py
@tests/test_queue_drain.py
@tests/test_queue_durability.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate lost-lease and invalid-context settlement outcomes</name>
  <files>app/db/repo/job_settlement.py, app/queue/drain.py, tests/test_queue_durability.py</files>
  <read_first>Read SettlementOutcome, settle_outbound_delivery_job's current FENCED branches, reap_expired_final_attempt, drain DrainOutcome/_map_settlement_outcome, and existing lost-token tests.</read_first>
  <behavior>
    - A missing/reclaimed lease returns LOST_LEASE and does not issue a delivery write.
    - An exact leased row with stale epoch, wrong purpose/status, or mismatched persisted context returns INVALID_CONTEXT only after an exact-token state transition clears or terminalizes the lease.
    - Final-lease stale/invalid context follows the same durable retirement vocabulary and never creates current-run attempt evidence.
  </behavior>
  <action>Per D-07/D-08, replace the delivery-specific overloaded FENCED result with distinct `SettlementOutcome.LOST_LEASE` for a missing or already-reclaimed `(job.id, lease_token)` and `SettlementOutcome.INVALID_CONTEXT` for an exact held lease whose persisted run/email/epoch/purpose/status is no longer authorized. Add a private exact-token retirement helper that marks the obsolete SEND_OUTBOUND row `done` or `dead` with bounded last_error and clears `lease_token`/`leased_until`; it must not touch the immutable snapshot, current reservation, attempt ledger, or payroll status. Use the same disposition from `reap_expired_final_attempt` for stale/invalid send context. Extend DrainOutcome and `_map_settlement_outcome` with the two distinct values while retaining generic pipeline FENCED compatibility where it is still valid. Add a SQL-shape test named `test_invalid_context_settlement_retires_exact_leased_row` proving invalid-context retirement contains the exact state/token predicate, and prove lost leases return without job, attempt, or payroll writes.</action>
  <acceptance_criteria>Settlement results identify whether the worker lost ownership or still owns an invalid lease, and only the latter performs a durable exact-token lease retirement.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_queue_durability.py tests/test_queue_drain.py</automated></verify>
  <done>Production settlement/reaper SQL and tests enforce the two fencing cases without conflating current-context invalidity with zombie-worker loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make drain token bookkeeping safe for invalid context</name>
  <files>app/queue/drain.py, tests/test_queue_drain.py</files>
  <read_first>Read drain_once's lease_settled lifecycle, held_tokens helpers, current SEND_OUTBOUND exception/result branches, and existing tests for complete/fenced/failure settlement.</read_first>
  <behavior>
    - drain_once returns INVALID_CONTEXT for an invalid-context send lease and no longer leaves its token in held_tokens after the repository has durably retired the row.
    - drain_once returns LOST_LEASE for a reclaimed lease and reports that distinction without attempting a second retirement.
    - A settlement exception before durable retirement keeps the token held for graceful shutdown, preserving the existing outage behavior.
  </behavior>
  <action>Update drain_once to map the distinct repository outcomes and set `lease_settled` only after the settlement call has returned a durable INVALID_CONTEXT, DONE, RETRIED, DEAD, or LOST_LEASE result. Preserve the existing exception path that keeps a token held when settlement itself raises. Add `test_drain_invalid_context_durably_retires_lease_before_token_discard` with a fake SEND_OUTBOUND row whose settlement implementation first changes the row to a terminal/released state and returns INVALID_CONTEXT; assert the drain result, row state, and held-token set. Add `test_drain_distinguishes_lost_lease_from_invalid_context` with a no-write LOST_LEASE result and assert no duplicate settlement call or token-stranding cleanup is attempted. Keep provider calls outside drain and preserve post-commit queue semantics.</action>
  <acceptance_criteria>drain_once cannot forget a token for a still-leased invalid-context job, while a lost lease remains a distinct no-write outcome and settlement failures remain recoverable by graceful shutdown.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_queue_drain.py tests/test_queue_durability.py</automated></verify>
  <done>Queue drain behavior is testable for both ownership loss and invalid context, with durable retirement preceding token discard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Worker lease token -> Postgres job row | A worker may mutate transport state only while it owns the exact lease. |
| Settlement outcome -> in-process held-token registry | Drain cleanup must reflect durable state, not merely a returned enum. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-19-01 | Tampering | invalid-context settlement | critical | mitigate | Exact-token terminal/release update before returning INVALID_CONTEXT; SQL and row-state regressions prove it. |
| T-20-19-02 | Denial of service | `drain_once` token bookkeeping | high | mitigate | Keep tokens on settlement exceptions; discard only after durable outcomes and test held-token state. |
| T-20-19-03 | Repudiation | LOST_LEASE versus INVALID_CONTEXT reporting | medium | mitigate | Separate enum values and drain outcomes make zombie ownership loss distinct from stale business context. |
| T-20-19-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; retain the repository's existing slopcheck and human package gate policy. |
</threat_model>

<verification>
<automated>uv run pytest -q tests/test_queue_drain.py tests/test_queue_durability.py</automated>
<automated>uv run ruff check app/db/repo/job_settlement.py app/queue/drain.py tests/test_queue_drain.py tests/test_queue_durability.py</automated>
</verification>

<success_criteria>
An invalid-context fenced SEND_OUTBOUND lease is durably retired before drain forgets its token, and a lost lease remains a separate no-write outcome.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-19-SUMMARY.md` when done.</output>
