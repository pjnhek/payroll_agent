---
phase: 20-exactly-once-send
plan: 23
type: execute
wave: 19
depends_on: [20-25]
files_modified:
  - tests/test_queue_durability.py
autonomous: true
gap_closure: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "Two genuine Postgres connections racing after durable provider authorization cannot advance reply_epoch and then invoke the old snapshot gateway."
    - "The race harness is demonstrably capable of observing the unsafe condition when the active-handoff fence is intentionally removed from the control setup."
    - "The new proof is collected by the marker-selected real-Postgres concurrency workflow and reports unavailable credentials as a skip, never as evidence of success."
  artifacts:
    - path: tests/test_queue_durability.py
      provides: "Barrier-driven real-Postgres authorization-versus-epoch-bump proof and its falsifying control."
  key_links:
    - "A test-only wrapper around the real authorize_outbound_provider_handoff call commits its worker transaction, records that worker's backend PID, and pauses before returning the real authorization to the otherwise unmodified handler; the retrigger thread uses a distinct real connection to call clear_reply_context."
    - "The module-level integration and queueproof markers cause concurrency-proof.yml's marker-selected queue durability step to execute the proof without adding a filename list."
---

<objective>
Prove the provider-handoff fence under a genuine concurrent Postgres interleaving, then execute the guarded live evidence.

Purpose: the earlier proof only advanced the epoch before the handler began. This plan makes the rejected interleaving occur after durable authorization and before gateway entry, with a paired control proving the test can actually observe stale provider work.
Output: non-vacuous queueproof tests and explicit local/CI execution evidence rules.

Artifacts this phase produces: `test_provider_handoff_blocks_epoch_bump_before_gateway` and `test_provider_handoff_race_control_observes_stale_gateway_when_fence_is_released`; real connection backend-PID assertions; barrier/event ordering; and marker-collection verification for the existing `concurrency-proof.yml` queueproof step.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-VERIFICATION.md
@.planning/phases/20-exactly-once-send/20-21-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-22-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-24-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-25-SUMMARY.md
@tests/test_queue_durability.py
@tests/conftest.py
@.github/workflows/concurrency-proof.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the barrier-driven two-connection handoff regression</name>
  <files>tests/test_queue_durability.py</files>
  <read_first>Read the module docstring, module-level `integration`/`queueproof` markers, `seeded_db`, `_isolated_jobs`, `test_genuine_claim_race_exactly_one_winner`, and the existing stale-epoch handler/settlement tests in tests/test_queue_durability.py. Read handle_send_outbound, authorize_outbound_provider_handoff, clear_reply_context, and the retrigger transaction path before writing the test.</read_first>
  <behavior>
    - A leased epoch-0 SEND_OUTBOUND job enters the unmodified handler; a test-only wrapper invokes the real authorization with the worker connection, commits it, then pauses after authorization and before returning the real authorization to that handler.
    - A retrigger-side thread uses a different real PostgreSQL connection, synchronizes through `threading.Barrier`, and attempts clear_reply_context after authorization; it receives ProviderHandoffActive and leaves status, reply_epoch, jobs, and snapshot identity unchanged.
    - Releasing the worker afterward permits its gateway spy to run only while the persisted run epoch is still 0; exact settlement then releases the handoff so fixture cleanup remains quiescent.
  </behavior>
  <action>In tests/test_queue_durability.py, add `test_provider_handoff_blocks_epoch_bump_before_gateway(seeded_db)` under the existing module-level `integration` and `queueproof` markers. Seed an approved current-epoch confirmation snapshot and claim its exact SEND_OUTBOUND job. Run `handle_send_outbound` in a worker OS thread. At the handler's repository import seam only, install a test-only wrapper for `authorize_outbound_provider_handoff`: it opens the worker's explicit `repo.get_connection()` connection, captures `SELECT pg_backend_pid()`, invokes the real authorizer with that connection inside an explicit transaction, lets that transaction commit, then reaches `threading.Barrier(2, timeout=30)` and waits before returning the real authorization to the unmodified handler. This is the required post-authorization/pre-gateway pause; do not use the gateway mock itself as the pause point. The retrigger thread opens a separate explicit `repo.get_connection()`, captures its backend PID, joins that barrier, calls `clear_reply_context` in its own transaction, asserts `ProviderHandoffActive`, verifies reply_epoch remains 0, then signals the wrapper to return. Only then may a simple gateway spy execute; it records the run epoch and frozen Message-ID, but never controls the barrier. Settle its OK result through the real coordinator so the handoff and exact lease are released. Assert distinct backend PIDs, one gateway call, gateway epoch 0, unchanged original snapshot/message id before settlement, no new RUN_PIPELINE job, no wake surrogate, and no skipped barrier participant. Do not use TestClient, a fake repository, a shared async portal, sleeps, direct route calls, or a gateway-monkeypatch pause: this is a sync repository/handler race against Postgres.
  </action>
  <acceptance_criteria>The blocking verifier scenario exercises two actual database connections after authorization and proves no stale gateway call can follow a successful epoch bump, because that bump is rejected before it commits.</acceptance_criteria>
  <verify><automated>uv run pytest -q tests/test_queue_durability.py -m 'integration and queueproof' -k provider_handoff</automated></verify>
  <done>The test fails if clear_reply_context no longer checks the active handoff or if the handler calls the gateway before durable authorization, and it cannot pass by serializing through an HTTP route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the race harness can observe the unsafe control and register live evidence correctly</name>
  <files>tests/test_queue_durability.py</files>
  <read_first>Read Task 1's new test, the queueproof workflow's marker-selected step and its skip/no-pass guards, and the test module's existing falsifying-mutation documentation and cleanup fixtures.</read_first>
  <behavior>
    - A paired control uses the same real-authorizer wrapper to deliberately release the test's active handoff record after authorization and before clear_reply_context, then observes epoch 1 at the provider spy; this demonstrates the barrier schedule would catch the original bug.
    - Both tests retain `integration` and `queueproof` collection through the module-level marker and are found by `uv run pytest tests/ -m queueproof --collect-only -q`.
    - With DATABASE_URL and ALLOW_DB_RESET=1 absent the selected tests skip and the executor records unavailable evidence; with both supplied they must run and pass with zero skips.
  </behavior>
  <action>Add the paired live control `test_provider_handoff_race_control_observes_stale_gateway_when_fence_is_released(seeded_db)` using the same seeded job, two-connection barrier, and real-authorizer wrapper from Task 1. In the control only, after the wrapper has committed the real authorization and before the retrigger-side clear, use test-local direct SQL through the retrigger connection to release that exact active handoff; then call clear_reply_context, assert it advances to epoch 1, signal the wrapper to return the already-issued authorization to the unmodified handler, and assert its provider spy observes epoch 1. The provider spy remains a passive recorder, never the barrier/interception mechanism. This is an intentionally unsafe test setup, not a production path, and it proves the scheduler, connections, and handler seam can detect the forbidden interleaving rather than merely asserting a mock was unused. Keep direct SQL confined to the resettable queueproof fixture and restore/settle state so no worker leaks or jobs survive fixture teardown. Document the two falsifying mutations in the test docstrings: removing the active-handoff predicate from clear_reply_context makes Task 1 fail, while changing the control to retain the fence makes its required epoch-1 observation fail. Do not edit concurrency-proof.yml: its `tests/ -m queueproof` collection already discovers any correctly marked test in this module. Verify collection explicitly, then run the focused command with DATABASE_URL and ALLOW_DB_RESET=1 when available; otherwise report the guard's skips as unavailable evidence and leave Phase 20 verification unclosed until CI or a resettable database produces a no-skip pass.</action>
  <acceptance_criteria>The live proof has a passing protected case and an intentionally unsafe control that proves the race schedule is capable of exposing a stale provider call. CI discovers both through its existing marker-selected queueproof gate.</acceptance_criteria>
  <verify><automated>uv run pytest tests/ -m queueproof --collect-only -q | rg 'test_provider_handoff_(blocks_epoch_bump_before_gateway|race_control_observes_stale_gateway_when_fence_is_released)'</automated>
<automated>DATABASE_URL="$DATABASE_URL" ALLOW_DB_RESET=1 uv run pytest -q tests/test_queue_durability.py -m 'integration and queueproof' -k provider_handoff -rs</automated></verify>
  <done>The test harness is non-vacuous, marker-collected in CI, and reports the real-Postgres credential gate honestly instead of converting skipped proofs into a green completion claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Worker connection → provider spy | The test must establish that only a durably authorized worker can reach the external boundary. |
| Retrigger connection → reply_epoch | A separate connection attempts the operator's epoch-changing write at the only dangerous interleaving point. |
| CI marker selection → durability claim | A missing marker or unavailable database can otherwise make a real-concurrency proof appear to pass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-20-23-01 | Tampering | provider/epoch race | critical | mitigate | Barrier-held real connections prove protected behavior and an unsafe direct-SQL control proves the harness can observe the failure. |
| T-20-23-02 | Repudiation | queueproof evidence | high | mitigate | Assert two backend PIDs, exact barrier ordering, marker collection, and zero skipped tests when credentials are supplied. |
| T-20-23-03 | Denial of service | test cleanup | medium | mitigate | Use existing seeded_db/_isolated_jobs quiescence protocol and settle/release the exact handoff before teardown. |
| T-20-23-04 | Information disclosure | test logs | medium | mitigate | Use fixture-safe UUIDs and fixed addresses/categories only; assert IDs/epochs, not raw provider payloads. |
| T-20-23-SC | Tampering | uv package installation | high | mitigate | No package installation is planned; run tests through uv only. |
</threat_model>

<verification>
<automated>uv run pytest tests/ -m queueproof --collect-only -q | rg 'test_provider_handoff_(blocks_epoch_bump_before_gateway|race_control_observes_stale_gateway_when_fence_is_released)'</automated>
<automated>DATABASE_URL="$DATABASE_URL" ALLOW_DB_RESET=1 uv run pytest -q tests/test_queue_durability.py -m 'integration and queueproof' -k provider_handoff -rs</automated>
<automated>uv run ruff check tests/test_queue_durability.py</automated>
<automated>uv run mypy</automated>
</verification>

<success_criteria>
The blocked interleaving is proven on real Postgres with two connections and a barrier, its unsafe twin is observable, and the marker-selected CI job cannot silently omit the new proof.
</success_criteria>

## Source coverage audit

| Source | ID / item | Coverage | Status |
|---|---|---|---|
| GOAL | At most one confirmation per epoch | Live proof covers the after-authorization retrigger boundary that invalidated the goal. | COVERED |
| REQ | SEND-01 | Proof retains the original message ID/snapshot through the authorization race. | COVERED |
| REQ | SEND-02 | Gateway spy observes only frozen snapshot data and no regenerated content. | COVERED |
| REQ | SEND-03 | Queueproof verifies the durable handoff/retry boundary; credentials are enforced rather than assumed. | COVERED |
| RESEARCH | Sync seam, barrier, real Postgres, non-vacuity | Both test cases are direct sync-seam, two-connection, marker-collected queueproofs. | COVERED |
| CONTEXT | D-01/D-03/D-04/D-07/D-08/D-12/D-13 | The proof preserves durable queue ownership, cutoff/frozen snapshot behavior, bounded audit, review path, and immutable record while exercising the new fence. | COVERED |

<output>Create `.planning/phases/20-exactly-once-send/20-23-SUMMARY.md` when done.</output>
