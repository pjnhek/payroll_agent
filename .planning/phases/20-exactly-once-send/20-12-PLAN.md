---
phase: 20-exactly-once-send
plan: "12"
type: execute
wave: 7
depends_on: [20-04, 20-10]
files_modified:
  - app/email/gateway.py
  - app/pipeline/delivery.py
  - app/pipeline/clarification.py
  - tests/test_gateway.py
  - tests/test_delivery.py
  - tests/test_clarify.py
  - tests/test_send_idempotency.py
autonomous: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-01/D-07: after migration, no producer bypasses one durable identifier-only SEND_OUTBOUND job and its fenced handler."
    - "D-04/D-12/D-13: after migration, every provider call starts from a read-or-reserve immutable snapshot rather than caller-supplied content."
    - "D-02/D-03/D-06: legacy callers cannot evade bounded classification, reservation-age cutoff, or mismatch escalation."
  artifacts:
    - app/email/gateway.py
    - app/pipeline/delivery.py
    - app/pipeline/clarification.py
  key_links:
    - "Both migrated producers reach Resend only through read-or-reserve snapshot plus identifier-only job, Plan 05's handler, and Plan 09's fenced settlement."
---

<objective>
Remove or fail-close the obsolete caller-argument outbound gateway path only after confirmation and clarification both use the durable snapshot/job contract.

Purpose: eliminate the remaining bypass that could send caller-supplied content without the immutable reservation, identifier-only job, bounded replay classification, or lease-fenced settlement.
Output: no live producer can invoke the legacy `gateway.send_outbound` path, and tests prove every confirmation and clarification delivery enters the shared durable seam.

Artifacts this phase produces: a deleted legacy method when no compatibility callers remain, or a fail-closed method that raises before any provider work if a framework boundary requires its symbol; targeted bypass-regression coverage and final full-suite evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-03-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-04-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-05-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-09-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-10-SUMMARY.md
@app/email/gateway.py
@app/pipeline/delivery.py
@app/pipeline/clarification.py
@tests/test_gateway.py
@tests/test_delivery.py
@tests/test_clarify.py
@tests/test_send_idempotency.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close the legacy caller-argument gateway bypass after both producer migrations</name>
  <files>app/email/gateway.py, app/pipeline/delivery.py, app/pipeline/clarification.py, tests/test_gateway.py, tests/test_delivery.py, tests/test_clarify.py, tests/test_send_idempotency.py</files>
  <read_first>Read the completed Plan 04 confirmation producer, Plan 10 standard and field-regression clarification producers, Plan 05 handler, Plan 09 settlement coordinator, and every repository reference to gateway outbound sending before changing compatibility behavior.</read_first>
  <behavior>
    - Confirmation delivery in app/pipeline/delivery.py and standard plus field-regression clarification in app/pipeline/clarification.py have no caller-argument gateway send path.
    - Every provider-bound producer first read-or-reserves its immutable snapshot, then commits exactly one identifier-only SEND_OUTBOUND job and wakes only after commit.
    - If a compatibility symbol must remain for an import boundary, it fails closed before any provider call and directs callers to the durable reservation/job contract.
    - No legacy path can bypass the reservation timestamp cutoff, classified result handling, fixed Message-ID key, or Plan 09 lease-fenced settlement.
  </behavior>
  <action>After confirming Plan 04 has migrated confirmation delivery and Plan 10 has migrated standard plus field-regression clarification, remove `gateway.send_outbound` and its caller-argument provider path. If a nonproducer framework import requires the symbol, retain only a fail-closed compatibility stub that performs no Resend request, no reservation/state mutation, and no fallback send. Update delivery.py and clarification.py imports/call sites so both named producer families reach provider work only by read-or-reserve snapshot, atomic identifier-only SEND_OUTBOUND job, post-commit wake, Plan 05 snapshot handler, and Plan 09 exact-token settlement. Add focused negative and integration-style regression tests in test_gateway.py, test_delivery.py, test_clarify.py, and test_send_idempotency.py: patch the old path to fail if reached; prove confirmation, standard clarification, and field-regression clarification each reserve/load their existing snapshot before drafting or provider work and produce one identifier-only job; prove no caller-supplied envelope can reach Resend; and prove a legacy call fails before any provider effect. Preserve D-01 through D-13, including D-02/D-03 bounded classification/cutoff, D-04 frozen replay, D-06 mismatch escalation, and clarification's round, RFC-thread, awaiting_reply, and alias-safety boundaries.</action>
  <acceptance_criteria>Neither migrated producer nor any send helper can invoke a provider with caller-supplied content; all outbound mail is traceable to one immutable snapshot and one fenced identifier-only job, while any surviving legacy symbol is provably inert.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_gateway.py tests/test_delivery.py tests/test_clarify.py tests/test_send_idempotency.py -q</automated></verify>
  <done>The legacy caller-argument send implementation is absent or fail-closed, and all confirmation plus clarification routes are proved to use the durable read-or-reserve snapshot/job path without a bypass.</done>
</task>

<task type="auto">
  <name>Task 2: Run final no-bypass and whole-suite verification after compatibility removal</name>
  <files>tests/test_gateway.py, tests/test_delivery.py, tests/test_clarify.py, tests/test_send_idempotency.py</files>
  <read_first>Read the completed targeted no-bypass tests, queue drain tests, and Phase 20 validation sampling contract.</read_first>
  <action>Run the focused no-bypass suite and the full repository suite after removing or fail-closing the compatibility path. Investigate and fix only failures caused by this migration; preserve unrelated worktree changes. Record configured database queueproof skips as unavailable evidence, not a pass, and keep final Phase 20 verification dependent on the resulting full-suite outcome.</action>
  <acceptance_criteria>The targeted producer/gateway tests and full suite demonstrate that the compatibility removal did not reintroduce a synchronous sender, import failure, missing handler, mutable snapshot route, or clarification regression.</acceptance_criteria>
  <verify><automated>uv run pytest -q</automated></verify>
  <done>`uv run pytest -q` passes, or any configured-database queueproof absence is explicitly reported as unavailable evidence rather than success; no legacy caller-argument provider bypass remains.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| A forgotten producer sends mutable caller content around the snapshot | Tampering | high | mitigate | Remove or inert the legacy entry point and patch it fail-if-called in confirmation and clarification regressions. |
| A compatibility import turns into a silent provider fallback | Tampering | high | mitigate | Any retained symbol fails before provider setup or state changes; whole-suite evidence prevents an import/runtime gap. |
| Clarification migration loses thread/round or writes alias state | Tampering | high | mitigate | Include standard and field-regression clarification thread, awaiting-reply, and alias-safety proofs in the no-bypass suite. |
</threat_model>

<verification>
Run `uv run pytest tests/test_gateway.py tests/test_delivery.py tests/test_clarify.py tests/test_send_idempotency.py -q`, then `uv run pytest -q` and `uv run mypy app/email/gateway.py app/pipeline/delivery.py app/pipeline/clarification.py`. This is the required post-wave full-suite gate before the remaining nonblocking polish plans proceed.
</verification>

<success_criteria>
After both producer migrations, the caller-argument gateway route is gone or provably inert; all provider effects originate from a persisted snapshot and identifier-only durable job with bounded, fenced settlement.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-12-SUMMARY.md` when done.</output>
