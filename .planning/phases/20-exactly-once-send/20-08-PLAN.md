---
phase: 20-exactly-once-send
plan: "08"
type: execute
wave: 8
depends_on: [20-06, 20-12]
files_modified:
  - eval/run_eval.py
  - eval/chart.svg
  - tests/test_eval.py
autonomous: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-04: the selected evaluation-chart polish has no access to or effect on provider snapshot replay."
    - "D-13: changing a committed evaluation artifact never mutates completed or manually resolved outbound audit records."
    - "SEND-01/SEND-02/SEND-03 safety remains green before this nonblocking visual polish is accepted."
  artifacts:
    - eval/run_eval.py
    - eval/chart.svg
  key_links:
    - "Chart generation remains an offline deterministic eval artifact, separate from delivery/database/queue modules."
---

<objective>
Restyle the selected evaluation chart after outbound-delivery correctness is complete.

Purpose: align recruiter-facing eval presentation with the dashboard without coupling display polish to payroll or email state.
Output: dashboard-aligned committed SVG and regression check.

Artifacts this phase produces: restyled offline evaluation chart and deterministic chart-generation assertion.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-05-SUMMARY.md
@.planning/phases/20-exactly-once-send/20-12-SUMMARY.md
@.planning/todos/pending/260623-04-eval-chart-restyle-v2.md
@eval/run_eval.py
@eval/chart.svg
@tests/test_eval.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply dashboard visual tokens to offline chart generation</name>
  <files>eval/run_eval.py, tests/test_eval.py</files>
  <read_first>Read eval/run_eval.py chart-generation code, test_eval.py check-mode assertions, the selected chart-restyle todo, and existing dashboard palette/style tokens.</read_first>
  <behavior>
    - Chart generation uses an intentional dashboard-aligned palette, legible labels, restrained grid/spines, and stable layout.
    - Score calculation, fixture interpretation, and eval metric semantics remain unchanged.
    - The offline command remains database-free and has no dependency on outbound delivery modules.</behavior>
  <action>Restyle the matplotlib/SVG generation with the dashboard's blue/neutral palette, clean sans-serif typography, lighter visual chrome, and tighter readable labels while leaving metrics, fixture loading, scoring functions, and check-mode semantics untouched. Add tests that pin style metadata/structural SVG features without asserting unstable full-file bytes. Cite D-04 and D-13 in the regression boundary: this task must not import queue, gateway, delivery, snapshot, or database mutation code.</action>
  <acceptance_criteria>The chart looks intentionally dashboard-aligned while all existing evaluation numbers and offline check behavior remain identical.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_eval.py -q</automated></verify>
  <done>Tests prove styling changes leave evaluation score/report semantics untouched and remain offline.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate and verify the committed chart artifact</name>
  <files>eval/chart.svg, tests/test_eval.py</files>
  <read_first>Read the eval --check command documentation, generated-artifact test, and current eval/chart.svg before regeneration.</read_first>
  <action>Regenerate the committed SVG through the repository's normal uv-managed evaluation command, inspect that it contains the intended labels/palette and no input fixture content beyond aggregate metrics, then update regression checks for the generated artifact. Keep this limited to the offline eval artifact; do not touch email, snapshot, queue, or payroll persistence files. This is deliberately after SEND correctness: D-04/D-13 require chart work to remain incapable of regenerating or mutating frozen email/PDF evidence.</action>
  <acceptance_criteria>The checked-in SVG matches the restyled generator and the standard eval check passes without database or provider access.</acceptance_criteria>
  <verify><automated>uv run python eval/run_eval.py --check && uv run pytest tests/test_eval.py -q</automated></verify>
  <done>The committed chart is reproducible from fixtures and preserves every preexisting evaluation metric assertion.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Visual refactor alters reported evaluation truth | Tampering | high | mitigate | Keep scoring untouched and assert existing metrics/check mode before and after regeneration. |
| Eval polish crosses into delivery persistence or frozen snapshots | Tampering | high | mitigate | D-04/D-13 module-boundary tests and zero delivery/database files in scope. |
| SVG exposes fixture-level sensitive text | Information disclosure | medium | mitigate | Inspect/chart-test aggregate-only labels and preserve current fixture-safe rendering pattern. |
</threat_model>

<verification>
Run `uv run python eval/run_eval.py --check`, `uv run pytest tests/test_eval.py -q`, and the phase focused delivery suite `uv run pytest tests/test_send_idempotency.py tests/test_delivery.py -q` before accepting this polish.
</verification>

<success_criteria>
The recruiter-facing chart is visually integrated with the dashboard and entirely isolated from frozen outbound delivery state.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-08-SUMMARY.md` when done.</output>
