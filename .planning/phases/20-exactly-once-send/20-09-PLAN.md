---
phase: 20-exactly-once-send
plan: "09"
type: execute
wave: 4
depends_on: [20-01, 20-02, 20-03]
files_modified:
  - app/db/repo/job_settlement.py
  - app/db/repo/__init__.py
  - tests/test_queue_durability.py
  - tests/test_send_idempotency.py
autonomous: true
requirements: [SEND-01, SEND-02, SEND-03]
must_haves:
  truths:
    - "D-01/D-07: an exact leased send job has a fenced settlement path before any producer may enqueue it."
    - "D-02/D-03: a classified transient outcome reschedules the original reservation only before its fixed 20-hour cutoff."
    - "D-05/D-06: confirmation retry keeps approved, while terminal or expired delivery moves to bounded review without a replacement key."
    - "D-07: a fenced winner appends PII-safe attempt history and a fenced loser writes nothing."
  artifacts:
    - app/db/repo/job_settlement.py
  key_links:
    - "Plan 05's handler passes the exact lease token and Plan 03's bounded result to settle_outbound_delivery_job before any producer path is enabled."
---

<objective>
Build the delivery-specific fenced settlement coordinator before registering or producing send jobs.

Purpose: a handler is safe to expose only when every provider outcome has an exact-token, bounded database settlement.
Output: confirmation delivery settlement, reservation-time retry ladder enforcement, PII-safe attempt ledger writes, and non-vacuous durability proofs.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-exactly-once-send/20-CONTEXT.md
@.planning/phases/20-exactly-once-send/20-RESEARCH.md
@.planning/phases/20-exactly-once-send/20-03-SUMMARY.md
@app/db/repo/job_settlement.py
@app/db/repo/jobs.py
@tests/test_queue_durability.py
@tests/test_send_idempotency.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settle confirmation send outcomes under the exact lease fence</name>
  <files>app/db/repo/job_settlement.py, app/db/repo/__init__.py, tests/test_queue_durability.py, tests/test_send_idempotency.py</files>
  <read_first>Read _locked_job, settle_pipeline_job, existing fenced reschedule rules, run-status CAS helpers, outbound snapshot/attempt APIs from Plan 01, and Plan 03's bounded delivery result and reservation-time ladder.</read_first>
  <behavior>
    - A success atomically marks the logical confirmation reservation sent, appends a bounded attempt event, completes the exact job, and performs the approved post-send transition.
    - An eligible transient result appends a fixed-category attempt and reschedules the same job at the next legal reservation-time slot without changing approved to error.
    - An expired or terminal confirmation result appends a bounded event and CASes approved to needs_operator delivery review without minting a new Message-ID or key.
    - A clarification result preserves awaiting_reply, round, and RFC-thread facts; its terminal branch uses the existing clarification-safe escalation and never writes an alias.
    - A losing lease token returns FENCED without writing the attempt ledger, job, reservation, or run.</behavior>
  <action>Add the purpose-aware `settle_outbound_delivery_job` coordinator rather than routing a send through generic pipeline retry or rewind logic. Under the exact leased job token, lock the reservation and append only fixed-category D-07 attempt facts. For confirmation purpose, atomically mark sent and complete on success; reschedule that same job only at Plan 03's D-03 reservation-time ladder while retaining D-05 approved state; or CAS approved to needs_operator with a bounded delivery-review category for D-06, expiry, and other terminal outcomes. For clarification purpose, preserve awaiting_reply, stored round, and RFC-thread audit on success/retry; route a terminal outcome through the existing clarification-safe escalation and never write aliases or use confirmation review choices. Validate job/run/snapshot ownership and preserve the original immutable key in every branch. A fenced loser must return without any append, state, or queue write. Re-export the facade and add hermetic tests for confirmation success, retry, cutoff, terminal review, and loser no-write behavior; Plan 11 adds focused clarification/alias regressions before its producer migrates.</action>
  <acceptance_criteria>An exact lease winner is the only process that can settle, append, retry, or escalate a confirmation send; automatic work never changes the original slot/key or an approved run's business state while replay remains safe.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_queue_durability.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Confirmation settlement has executable, lease-fenced outcomes for success, retry, and review before Plan 05 registers a handler or Plans 04/10 enqueue a job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register non-vacuous database proof for cutoff and zombie rejection</name>
  <files>tests/test_queue_durability.py, tests/test_send_idempotency.py</files>
  <read_first>Read the project's guarded integration and queueproof conventions plus existing live-Postgres epoch-scoping proof.</read_first>
  <behavior>
    - A real-Postgres proof exercises the same-job retry dedup, 20-hour cutoff, and stale-token rejection path when the database guard is configured.
    - A skipped local guarded proof is reported as unavailable evidence rather than a passing proof.</behavior>
  <action>Add guarded `integration` and `queueproof` tests for the coordinator's same-job dedup, reservation cutoff, and zombie-write rejection. Keep the tests PII-safe and use the repository's established marker/guard reporting so a local database skip is visible as unavailable. Do not create a provider call or producer in this plan; Plan 05 will use this already-tested coordinator to make dispatch executable.</action>
  <acceptance_criteria>The most consequential fence and cutoff invariants have both hermetic and configured-database evidence paths before dispatch is enabled.</acceptance_criteria>
  <verify><automated>uv run pytest tests/test_queue_durability.py tests/test_send_idempotency.py -q</automated></verify>
  <done>Queueproof markers are discoverable, hermetic settlement tests are non-vacuous, and any missing local DB remains explicitly unavailable evidence.</done>
</task>

</tasks>

<threat_model>
ASVS L1 applies; any high-severity finding blocks this plan.

| Threat | STRIDE | Severity | Disposition | Mitigation |
|---|---|---:|---|---|
| Reclaimed zombie lease records a second attempt or reopens review | Tampering | high | mitigate | D-07 exact-token lock/fence for every event, reschedule, sent, and review write. |
| Generic retry rewinds approved payroll state | Tampering | high | mitigate | Delivery-specific settlement retains D-05 approved state until sent or review CAS. |
| Stale provider key is retried after retention safety margin | Repudiation | high | mitigate | D-03 database reservation cutoff and no-next-slot-at-cutoff proof. |
</threat_model>

<verification>
Run `uv run pytest tests/test_queue_durability.py tests/test_send_idempotency.py -q` and `uv run mypy app/db/repo/job_settlement.py`.
</verification>

<success_criteria>
An executable fenced settlement coordinator exists before handler registration, so no dispatchable job can be stranded or mutate state without its lease token.
</success_criteria>

<output>Create `.planning/phases/20-exactly-once-send/20-09-SUMMARY.md` when done.</output>
