---
phase: 07-money-correctness-deepening
plan: "05"
type: execute
wave: 5
depends_on:
  - "07-04"
files_modified:
  - tests/test_resume_pipeline.py
  - eval/run_eval.py
  - eval/fixtures/16_zero_hours_hourly_coastal.json
  - eval/fixtures/16_zero_hours_hourly_coastal_extraction.json
  - eval/fixtures/17_nfd_name_coastal.json
  - eval/fixtures/17_nfd_name_coastal_extraction.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json
  - eval/summary.json
autonomous: false
requirements:
  - MONEY-01
  - MONEY-02
  - MONEY-03

must_haves:
  truths:
    - "Integration tests for snapshot_once, loop_guard, confirmed_dropped_no_reflag are REQUIRED to PASS (not skip) before phase is marked complete — skip ≠ evidence per D-23. Phase gate FAILS if DATABASE_URL is absent at evidence time."
    - "Three new eval fixtures (16, 17, 18) exist in eval/fixtures/ and eval/run_eval.py scores all three correctly"
    - "Fixture 17 (MONEY-02 NFD) is HONEST: it proves the _normalize scorer parity (C-4 fix) using existing coastal employees with whitespace normalization, OR uses a deliberately-added accented seeded employee. The plan does NOT claim eval fixture 17 proves NFD Unicode matching in the reconciler — that proof is test_reconcile.py:test_nfd_name_resolves_same_as_nfc. The plan states this clearly in SUMMARY."
    - "run_eval.py is updated to support prior_extracted: the fixture loader reads a 'prior_extracted' key from the fixture JSON and passes it as prior= to validate(); --check passes with the new summary.json regenerated to include fixtures 16/17/18"
    - "Fixture extraction caches produced via extracted.model_dump_json() — NOT hand-typed JSON (D-24)"
    - "uv run python eval/run_eval.py --check exits 0 with all three new fixtures scoring correctly"
    - "Full test suite green: uv run pytest -q — integration tests PASS (not skip) when DATABASE_URL is set"
  artifacts:
    - path: "tests/test_resume_pipeline.py"
      provides: "3 real integration tests: snapshot_once (DB), loop_guard (two-inbound round 1+2 architecture per decision A), confirmed_dropped_no_reflag"
    - path: "eval/run_eval.py"
      provides: "prior_extracted support in fixture loader + fixture stripping for --check + summary.json regeneration"
    - path: "eval/fixtures/16_zero_hours_hourly_coastal.json"
      provides: "MONEY-01 eval judgment fixture (zero-hours-hourly → request_clarification)"
    - path: "eval/fixtures/17_nfd_name_coastal.json"
      provides: "MONEY-02 eval scorer fixture (proves _normalize parity via C-4 fix, not NFD Unicode proof)"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal.json"
      provides: "MONEY-03 eval judgment fixture (prior= seam test: detect drop → request_clarification)"
    - path: "eval/summary.json"
      provides: "Updated summary.json including fixtures 16/17/18 so --check passes"
  key_links:
    - from: "eval/fixtures/18_field_drop_clarify_carry_coastal.json (prior_extracted key)"
      to: "eval/run_eval.py:validate(prior=prior)"
      via: "fixture loader reads prior_extracted key → Extracted.model_validate → passed as prior= kwarg"
      pattern: "prior_extracted"
    - from: "tests/test_resume_pipeline.py:test_loop_guard"
      to: "app/pipeline/orchestrator.py:resume_pipeline"
      via: "calls resume_pipeline TWICE (Round 1 + Round 2) against a real DB run in AWAITING_REPLY state — two-inbound architecture per decision A"
      pattern: "resume_pipeline"
---

<objective>
Complete Phase 7 evidence: turn the three xfail integration test stubs into real PASSING assertions (D-23 state machine certification), add three eval judgment fixtures certifying MONEY-01/02/03 (D-23 eval layer), update run_eval.py to support prior_extracted fixtures and regenerate summary.json.

Purpose: D-23 requires two layers of evidence. This plan provides both: integration tests (state machine) + eval fixtures (judgment purity). The integration tests MUST PASS before the phase is marked complete — skip is not evidence (finding #9 corrected).

D-23 two-layer split documentation (to appear in SUMMARY):
- Eval layer: certifies pure judgment (validate → decide) given a prior. Proves MONEY-01 ($0 gate), MONEY-02 (NFD scorer parity), and MONEY-03 (field drop detection → clarification). Cannot certify the state machine.
- Integration layer: certifies the state machine (snapshot-once, two-inbound loop guard, confirmed_dropped D-15 interaction). Requires a live DB with the new columns applied.

REVISION NOTES (incorporating Codex review findings #7, #8, #9):

Finding #7 (run_eval.py has no prior_extracted support): run_eval.py must be modified to load prior_extracted from fixture JSON and pass it as prior= to validate(). Also: _load_fixture strips only "expected" and "fixture_category" from the fixture dict before passing to the eval — any new "prior_extracted" key must also be stripped so the fixture passes InboundEmail validation. And --check compares against summary.json which must be regenerated to include fixtures 16/17/18.

Finding #8 (NFD eval fixture honesty): Seed data has NO accented employee names. Fixture 17 CANNOT claim to prove NFD Unicode matching in the reconciler via the eval alone (the LLM extraction path in the eval is not the reconciler). The honest approach: fixture 17 uses a submitted name that differs from a seeded roster employee only in whitespace (tests the _normalize scorer's NFC call for whitespace normalization), OR adds a note to SUMMARY that MONEY-02 Unicode proof is in test_reconcile.py. The eval fixture proves C-4 (_normalize parity with _norm) only. Do NOT claim fixture 17 proves NFD matching — document the scoping honestly.

Finding #9 (integration tests must not silently skip): The phase gate requires the integration tests to PASS. If DATABASE_URL is absent at the time the developer runs the phase gate, they must set it. The pytest marks must be `pytest.mark.skipif` for LOCAL test runs without DB (acceptable during development), but the PHASE GATE command must be run WITH DATABASE_URL set. The phase-completion checkpoint verifies this.

Output: Three integration tests PASSING, three eval fixture pairs, updated run_eval.py, regenerated summary.json.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md

# Source files to read before writing (one pass each)
@tests/test_resume_pipeline.py
@tests/conftest.py
@eval/run_eval.py
@eval/fixtures/07_missing_hours_coastal.json
@eval/fixtures/07_missing_hours_coastal_extraction.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_eval.py prior_extracted support + fixture stripping + summary regen</name>
  <files>eval/run_eval.py, eval/summary.json</files>

  <read_first>
  Read eval/run_eval.py in full — focus on:
  - Lines 100-130: the fixture loader (_load_fixture or equivalent). Identify which keys are currently stripped before the fixture is passed to InboundEmail. "expected" and "fixture_category" are stripped; "prior_extracted" will also need to be stripped.
  - Lines 155-165: where validate() is called (or where the eval pipeline runs). This is where `prior=prior_extracted` needs to be threaded in.
  - Lines 640-660: where extraction caches are written (the `--record` path, via `extracted.model_dump_json(indent=2)`). This confirms D-24 compliance.
  - The `--check` flag behavior: how it loads summary.json and compares against current run results. The summary.json path.

  Read eval/summary.json (first 30 lines) to understand its structure. The file must be regenerated after the new fixtures are added.

  SCOPE: This task is ONLY the run_eval.py changes needed for prior support. It does NOT write the fixture files (that is Task 2).
  </read_first>

  <action>
  Step 1 — Update fixture loader to strip "prior_extracted" key:
  In the fixture loading function (wherever "expected" and "fixture_category" are stripped from the fixture dict before building InboundEmail), add "prior_extracted" to the set of stripped keys. Example: change `{k: v for k, v in fixture.items() if k not in ("expected", "fixture_category")}` to `{k: v for k, v in fixture.items() if k not in ("expected", "fixture_category", "prior_extracted")}`.

  Step 2 — Load prior_extracted from fixture JSON and pass to validate():
  In the eval pipeline function (where validate() is called), before the validate() call:
  - Load `prior_extracted_data = fixture.get("prior_extracted")` (the raw fixture dict, before stripping).
  - If prior_extracted_data is not None: `prior_extracted = Extracted.model_validate(prior_extracted_data)`. Import Extracted from app.models.contracts if not already imported.
  - Call `validate(extracted, roster, matches, prior=prior_extracted)` where `prior_extracted` is None if the fixture has no "prior_extracted" key.
  This preserves all existing fixture behavior (prior_extracted=None is a no-op per D-18).

  Step 3 — Regenerate summary.json:
  After the fixture files in Task 2 are written, run:
  `uv run python eval/run_eval.py --record` (or the equivalent --generate-summary flag — check the actual flag by reading the run_eval.py help/argparse block) to regenerate summary.json including the new fixtures. The summary.json must be committed with the new fixtures so --check passes in CI.

  NOTE: If run_eval.py has no --record or --generate-summary flag that updates summary.json, look at the actual CLI interface and use the correct flag. The goal is: after running the eval with all new fixtures scoring correctly, write the expected results to summary.json so --check validates them deterministically.
  </action>

  <verify>
    <automated>uv run python -c "import ast, sys; src=open('eval/run_eval.py').read(); assert 'prior_extracted' in src, 'prior_extracted not found'; print('prior_extracted wiring found')"</automated>
  </verify>
  <done>"prior_extracted" appears in eval/run_eval.py. The "prior_extracted" key is stripped from the fixture dict before InboundEmail construction. validate() receives prior= kwarg from fixture data. summary.json updated (after Task 2 creates the fixtures and a --record run is done).</done>
</task>

<task type="auto">
  <name>Task 2: Eval fixtures 16/17/18 + eval --check passes</name>
  <files>eval/fixtures/16_zero_hours_hourly_coastal.json, eval/fixtures/16_zero_hours_hourly_coastal_extraction.json, eval/fixtures/17_nfd_name_coastal.json, eval/fixtures/17_nfd_name_coastal_extraction.json, eval/fixtures/18_field_drop_clarify_carry_coastal.json, eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json, eval/summary.json</files>

  <read_first>
  Read eval/fixtures/07_missing_hours_coastal.json and eval/fixtures/07_missing_hours_coastal_extraction.json to understand the exact structure (PATTERNS.md §9 has the full excerpt). Read eval/run_eval.py lines 640-660 (extraction cache writer — `extracted.model_dump_json(indent=2)`). PATTERNS.md §9 documents the naming convention. CONTEXT.md D-24 locks the serialization requirement.

  Check the seed data (app/db/seed.py) for coastal business employees to use plausible names. The accented-name check: does the seed have any accented character employee? If YES, use them for fixture 17 with NFD submitted name. If NO (confirmed: seed has "María García" in one business but check coastal specifically), use a different approach for fixture 17 per finding #8.

  Run: `uv run python -m app.db.seed --dry-run` or `grep -n "coastal" app/db/seed.py` to find the coastal business employee names.

  FIXTURE 17 HONESTY (finding #8): If NO accented coastal employee exists in seed:
  - Fixture 17 proves C-4 (eval _normalize parity with _norm after the import fix). The submitted name can differ from a real coastal employee's name only in whitespace or case — proving _normalize now calls NFC. Document in SUMMARY: "Fixture 17 certifies the eval scorer's _normalize parity with reconcile_names._norm (C-4 fix). Full NFD Unicode proof is in tests/test_reconcile.py:test_nfd_name_resolves_same_as_nfc."
  - If YES, an accented coastal employee exists: fixture 17 uses their NFD-form submitted name; expected reconciliation shows exact match (proves the full NFD path through both _norm and _normalize). Document the employee used.
  </read_first>

  <action>
  Step 1 — Write a generation script to the scratchpad (NOT committed):
  Create `/private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/f8767991-5653-4e3a-9b4b-d4eb596a72c8/scratchpad/gen_fixtures.py` that:
  a. Imports `Extracted, ExtractedEmployee` from `app.models.contracts`
  b. Imports `uuid`, `date`, `Decimal`, `json`, `pathlib`
  c. Creates each `Extracted` object programmatically
  d. Writes `extracted.model_dump_json(indent=2)` to the fixture files (D-24 compliance)

  The script creates:

  Fixture 16 (MONEY-01 — zero-hours hourly):
  - `16_zero_hours_hourly_coastal.json`: Use a coastal business employee. hourly employee with `hours_regular="0.00"` (Decimal("0")), all other hours null. Expected: `final_action="request_clarification"`, `gate_reasons` contains a "missing" reason. `fixture_category="zero-hours-hourly"`.
  - `16_zero_hours_hourly_coastal_extraction.json`: `Extracted` with one employee `hours_regular=Decimal("0")`, all other hours None. Must serialize Decimal correctly via model_dump_json.

  Fixture 17 (MONEY-02 / C-4 eval parity):
  - Inspect seed to determine if a coastal accented employee exists. If yes: use their name with NFD encoding. If no: use an existing coastal employee with a submitted name that has extra whitespace (e.g. "Maria  Chen" → normalizes to "Maria Chen" via _normalize). Either way, the expected result is `final_action="process"`, reconciliation shows `name_category="exact"` or `name_category="alias"` (resolved=True).
  - `fixture_category="nfc-normalizer-parity"` (honest category name — does NOT claim to be "nfd-unicode" unless a real accented employee is used).
  - `17_nfd_name_coastal_extraction.json`: `Extracted` with the submitted name (whitespace or NFD form). Use model_dump_json.

  Fixture 18 (MONEY-03 — field regression → request_clarification with prior):
  - `18_field_drop_clarify_carry_coastal.json`: Use a coastal hourly employee. The extraction (resumed) has `hours_overtime=None` (absent). The prior snapshot has `hours_overtime=Decimal("2")`. Expected: `final_action="request_clarification"` when validate() is called with prior=prior_snapshot (the field regression is detected). `fixture_category="field-regression"`.
  - ADD a `"prior_extracted"` key to the main fixture JSON: the value is the serialized prior Extracted object (the snapshot with OT=2). This key is loaded by the updated run_eval.py (Task 1) and passed as `prior=` to validate().
  - `18_field_drop_clarify_carry_coastal_extraction.json`: `Extracted` with the employee having `hours_overtime=None` (the resumed extraction). Use model_dump_json.
  - NOTE: The prior snapshot embedded in the main fixture JSON is also produced via `Extracted(...).model_dump(mode="json")` (a plain dict, not model_dump_json string — it needs to be a JSON-serializable dict that becomes a nested object in the fixture). Wrap it appropriately.

  Step 2 — Run the generation script:
  `uv run python /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/f8767991-5653-4e3a-9b4b-d4eb596a72c8/scratchpad/gen_fixtures.py`

  Step 3 — D-24 compliance verification:
  `uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/16_zero_hours_hourly_coastal_extraction.json').read())); print('Fixture 16 OK:', len(e.employees), 'employees')"`
  `uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json').read())); print('Fixture 18 OK:', len(e.employees), 'employees')"`

  Step 4 — Run eval and update summary.json:
  `uv run python eval/run_eval.py 2>&1 | tail -30`
  Verify all three new fixtures (16, 17, 18) score correctly. Then regenerate summary.json:
  `uv run python eval/run_eval.py --record` (or equivalent — use the actual flag from the CLI).
  `uv run python eval/run_eval.py --check 2>&1 | tail -20` — must exit 0.
  </action>

  <verify>
    <automated>uv run python eval/run_eval.py 2>&1 | tail -20</automated>
  </verify>
  <done>`uv run python eval/run_eval.py` exits 0 and shows all three new fixtures (16, 17, 18) scoring correctly. `uv run python eval/run_eval.py --check` exits 0. All six fixture files exist. Extraction caches parse via `Extracted.model_validate()` without error. summary.json updated to include fixture 16/17/18 expected results.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — snapshot_once, loop_guard (two-inbound), confirmed_dropped_no_reflag</name>
  <files>tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/test_resume_pipeline.py (current state — 3 xfail stubs from Plan 01). Read tests/conftest.py to understand what DB connection fixtures exist (especially any `skip_if_no_db` or `DATABASE_URL` marker). Read tests/test_clarify.py to understand how existing clarify integration tests set up DB state (the test run creation pattern). CONTEXT.md D-19/D-20/D-21/D-28/D-15 lock the invariants each test must verify. RESEARCH.md §Integration layer confirms these are real DB tests requiring DATABASE_URL.

  CRITICAL per finding #9: These tests must PASS at phase completion time. The pytest.mark.skipif is acceptable for local runs without DB, but the phase gate checkpoint REQUIRES the developer to run WITH DATABASE_URL set. The tests must NOT remain as xfail stubs.
  </read_first>

  <action>
  Replace the three xfail stubs with real integration tests. All three tests use `@pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="requires live DATABASE_URL — set DATABASE_URL before running phase gate")`. This is NOT the same as xfail — skipif preserves the ability to detect failures; xfail masks them.

  Test setup pattern: follow tests/test_clarify.py precedent. Each test:
  1. Creates a real payroll run in AWAITING_REPLY state via direct DB inserts or helper functions (follow the existing pattern).
  2. Asserts the expected behavior.
  3. Cleans up the created run in a `finally` block.
  Use `uuid.uuid4()` for run IDs; prefix logs with "test-" to distinguish from real runs.

  Test 1 — `test_snapshot_once_not_overwritten_on_second_clarify` (D-28):
  - Create a payroll run in AWAITING_REPLY state.
  - Call `repo.set_pre_clarify_extracted(run_id, extracted_v1)` with a known extraction (OT=2).
  - Call `repo.set_pre_clarify_extracted(run_id, extracted_v2)` with a DIFFERENT extraction (OT=0).
  - Assert `repo.load_pre_clarify_extracted(run_id)` returns the FIRST extraction (v1): the IS NULL guard in SQL makes the second write a no-op.
  - PURE DB test: no orchestrator call needed. Verifies D-19.

  Test 2 — `test_loop_guard_fires_exactly_once_then_carry_forward` (decision A, D-13/D-16):
  This test verifies the two-inbound architecture:
  - Round 1 setup: create a run with a seeded coastal roster. Set extracted_data = snapshot with employee OT=2. Set pre_clarify_extracted = the snapshot. Set status = AWAITING_REPLY.
  - Call `resume_pipeline(run_id, reply_email_with_ot_absent, llm=mock_llm)` where the mock LLM returns an extraction with OT=None (absent). (Follow test_clarify.py for mock_llm pattern — pass llm=None or use an existing mock that returns a deterministic extraction.)
  - After Round 1 call: assert:
    - run is in AWAITING_REPLY status (the field regression was detected, clarification sent).
    - `repo.load_clarified_fields(run_id)` has an entry for the OT field with outcome "asked".
  - Round 2 setup: the run is still in AWAITING_REPLY (as if the client replied "yes keep it" / silence again, or replied with the OT included).
  - Call `resume_pipeline(run_id, reply_email_with_ot_still_absent, llm=mock_llm)` (same absent-OT reply — tests the carry-forward path).
  - After Round 2 call: assert:
    - run is NOT in AWAITING_REPLY (advanced to COMPUTED or AWAITING_APPROVAL).
    - `repo.load_clarified_fields(run_id)` shows outcome "carried_forward" for the OT field.
    - The computed paystub (if inspect-able) includes OT=2 from the snapshot (backfill applied).
  NOTE: If the mock_llm approach does not produce a controllable extraction in the test environment, use direct DB inserts to pre-populate `extracted_data` with the test extraction and skip the LLM extraction step. Check test_clarify.py for the established pattern.

  Test 3 — `test_confirmed_dropped_does_not_reflag_money01` (D-15):
  - Create a run with a seeded coastal roster. Snapshot has OT=2. Set pre_clarify_extracted = snapshot, status = AWAITING_REPLY.
  - Pre-populate clarified_fields = `{str(emp_id): {"hours_overtime": "confirmed_dropped"}}` (simulating that Round 1 completed and the client said "remove OT" → confirmed_dropped).
  - Call `resume_pipeline(run_id, reply_email_with_ot_zero, llm=mock_llm)` where mock_llm returns OT=Decimal("0") explicitly.
  - Assert the run advances to COMPUTED/AWAITING_APPROVAL WITHOUT issuing a new field_regression validation issue for OT (the confirmed_dropped entry in resolved_drops short-circuits MONEY-01 and field_regression detection).
  - Assert `repo.load_clarified_fields(run_id)` shows "confirmed_dropped" outcome for OT (unchanged from pre-test value or refreshed correctly).

  All tests import `os` and use `@pytest.mark.skipif(not os.environ.get("DATABASE_URL"), ...)`.
  All tests have `finally:` cleanup blocks to delete inserted runs.
  </action>

  <verify>
    <automated>uv run pytest tests/test_resume_pipeline.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>All three tests PASS (or SKIP if DATABASE_URL is not set — SKIP is acceptable for development runs; PASS is required at phase gate). No xfail stubs remain. `uv run pytest tests/test_resume_pipeline.py -v` shows PASSED or SKIPPED (never xfail, never FAILED). When DATABASE_URL is set: all three tests PASS (not skip).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Integration tests (snapshot_once, loop_guard two-inbound, confirmed_dropped_no_reflag) + three eval fixture pairs + updated run_eval.py with prior support + updated summary.json. Full phase evidence assembled.</what-built>
  <how-to-verify>
  PHASE GATE — all checks must pass WITH DATABASE_URL set. If DATABASE_URL is not in the shell env, set it now from the Supabase connection string (Supavisor transaction mode, port 6543) before running these checks.

  1. Full test suite (with live DB): `uv run pytest -q` — exits 0.
     Integration tests must PASS (not skip). If they skip, DATABASE_URL is not set — fix that first.
     `uv run pytest tests/test_resume_pipeline.py -v` — shows 3 PASSED (not SKIPPED, not xfail).

  2. Eval: `uv run python eval/run_eval.py --check` — exits 0. Fixtures 16/17/18 score correctly.

  3. Per-requirement ROADMAP Success Criteria:
     - SC1 (MONEY-01): `uv run pytest tests/test_validate.py -k "zero_hours" -x` exits 0 AND `eval/fixtures/16_zero_hours_hourly_coastal.json` scores as request_clarification.
     - SC2 (MONEY-02): `uv run pytest tests/test_reconcile.py -k "nfd" -x` exits 0 AND `grep "_norm as _normalize" eval/run_eval.py` exits 0 (C-4 fix in place).
     - SC3 (MONEY-03): `uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -x` PASSES (not skips) AND fixture 18 scores as request_clarification with prior= support.

  4. D-23 two-layer documentation in SUMMARY:
     - Eval layer certifies judgment (validate → decide purity).
     - Integration tests certify state machine (snapshot-once, two-inbound loop guard, D-15 interaction).
     - Fixture 17 is honestly scoped: certifies _normalize/C-4 parity; NFD Unicode proof is in test_reconcile.py (document if seed has no accented coastal employee).

  5. All architectural fixes from the Codex review verified:
     `grep "clarification_field_regression" app/db/schema.sql` — exits 0
     `grep "clarification_field_regression" app/db/repo.py` — exits 0
     `grep "clarification_field_regression" app/pipeline/orchestrator.py` — exits 0
     `grep "set_clarified_fields" app/pipeline/orchestrator.py` — exits 0
     `grep "post_extract_hook" app/pipeline/orchestrator.py` — exits 0
     `grep "def detect_field_regression" app/pipeline/validate.py` — exits 0
     `grep "RawFieldDrop" app/pipeline/validate.py` — exits 0
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass WITH DATABASE_URL set and integration tests PASS (not skip). Describe any failures so they can be diagnosed before marking Phase 7 complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/fixtures/*.json | Read from disk by run_eval.py; Pydantic validates on load via model_validate |
| tests/test_resume_pipeline.py → live DB | Integration tests write/read real DB state; scoped to test run_ids with cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | eval fixture JSON files — prior_extracted key is parsed and passed to validate() | mitigate | `Extracted.model_validate()` with `extra="forbid"` rejects malformed prior_extracted data at load time. D-24 compliance (model_dump(mode="json") serialization) ensures Decimal precision is correct. |
| T-07-14 | Information Disclosure | Integration tests write to real Supabase dev DB | accept | Tests use synthetic run_ids and synthetic extraction data (no real employee PII). Tests clean up inserted rows in `finally` blocks. |
| T-07-15 | Elevation of Privilege | Integration test run_ids colliding with production runs | mitigate | Use uuid.uuid4() for all test run IDs; assert cleanup at test teardown. The dev DB is separate from production. |
| T-07-SC | Tampering | No new package installs | accept | No `uv add` invoked; all imports from existing pyproject.toml deps |
</threat_model>

<verification>
Full phase gate (all plans combined):

1. `uv run pytest -q` — exits 0 (full suite, all plans; integration tests PASS with DATABASE_URL)
2. `uv run python eval/run_eval.py --check` — exits 0 (no failures, fixtures 16/17/18 present and scoring correctly)
3. `uv run pytest tests/test_validate.py -k "zero_hours" -x` — GREEN (MONEY-01)
4. `uv run pytest tests/test_reconcile.py -k "nfd" -x` — GREEN (MONEY-02)
5. `uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -x` — PASSED (not skipped) (MONEY-03)
6. `grep "def _is_paid" app/pipeline/validate.py` — exits 0
7. `grep "unicodedata.normalize" app/pipeline/reconcile_names.py` — exits 0
8. `grep "field_regression" app/pipeline/decide.py` — exits 0
9. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
10. `grep "clarified_fields JSONB" app/db/schema.sql` — exits 0
11. `grep "clarification_field_regression" app/db/schema.sql` — exits 0
12. `grep "clarification_field_regression" app/db/repo.py` — exits 0 (allowlist)
13. `grep "def detect_field_regression" app/pipeline/validate.py` — exits 0
14. `grep "RawFieldDrop" app/pipeline/validate.py` — exits 0
15. `grep "post_extract_hook" app/pipeline/orchestrator.py` — exits 0
16. `grep "set_clarified_fields" app/pipeline/orchestrator.py` — exits 0
</verification>

<success_criteria>
- All three ROADMAP Phase 7 Success Criteria are verifiable:
  1. zero-hours hourly gates (test + fixture 16 both prove it)
  2. NFD name resolves (test_reconcile.py proves it; fixture 17 certifies _normalize parity)
  3. field-regression clarifies once then terminates via two-inbound architecture (integration test + fixture 18 prove it)
- D-23 two-layer split documented honestly in SUMMARY: eval = judgment, test_resume_pipeline = state machine; fixture 17 scoping is explicit
- Integration tests PASS (not skip) when DATABASE_URL is set — skip ≠ evidence (finding #9)
- run_eval.py updated: prior_extracted support, fixture stripping, summary.json regenerated
- Full suite green; eval --check passes; phase ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-05-SUMMARY.md` when done.
</output>
