---
phase: 07-money-correctness-deepening
plan: "05"
type: execute
wave: 5
depends_on:
  - "07-04"
files_modified:
  - tests/test_resume_pipeline.py
  - eval/fixtures/16_zero_hours_hourly_coastal.json
  - eval/fixtures/16_zero_hours_hourly_coastal_extraction.json
  - eval/fixtures/17_nfd_name_coastal.json
  - eval/fixtures/17_nfd_name_coastal_extraction.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json
autonomous: false
requirements:
  - MONEY-01
  - MONEY-02
  - MONEY-03

must_haves:
  truths:
    - "Integration tests for snapshot_once, loop_guard, confirmed_dropped_no_reflag are real assertions (not xfail) and pass against the live dev DB (D-28/D-20/D-15, MONEY-03 state machine certification per D-23)"
    - "Three new eval fixtures (16, 17, 18) exist in eval/fixtures/ and eval/run_eval.py --check passes (MONEY-01/02/03 judgment certification per D-23/D-24)"
    - "Fixture extraction caches produced via extracted.model_dump_json() — NOT hand-typed JSON (D-24)"
    - "uv run python eval/run_eval.py --check exits 0 with all three new fixtures scoring correctly"
    - "Full test suite green: uv run pytest -q"
  artifacts:
    - path: "tests/test_resume_pipeline.py"
      provides: "3 real integration tests (not xfail): snapshot_once, loop_guard, confirmed_dropped_no_reflag"
    - path: "eval/fixtures/16_zero_hours_hourly_coastal.json"
      provides: "MONEY-01 eval judgment fixture"
    - path: "eval/fixtures/17_nfd_name_coastal.json"
      provides: "MONEY-02 eval judgment fixture"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal.json"
      provides: "MONEY-03 eval judgment fixture (prior= seam test)"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json"
      provides: "MONEY-03 prior snapshot fixture (serialized via model_dump_json)"
  key_links:
    - from: "eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json"
      to: "eval/run_eval.py:validate(prior=prior)"
      via: "loaded as Extracted.model_validate(json.loads(...)) per D-24"
      pattern: "prior_extracted"
    - from: "tests/test_resume_pipeline.py"
      to: "app/pipeline/orchestrator.py:resume_pipeline"
      via: "calls resume_pipeline against a real DB run in AWAITING_REPLY state"
      pattern: "resume_pipeline"
---

<objective>
Complete Phase 7 evidence: turn the three xfail integration test stubs into real green assertions (D-23 state machine certification), and add three eval judgment fixtures certifying MONEY-01/02/03 (D-23 eval layer). Together these prove all three ROADMAP Success Criteria at both layers: judgment purity (eval) and state machine correctness (integration).

Purpose: D-23 requires two layers of evidence. This plan provides the second layer (integration tests) plus closes the eval judgment layer with real fixtures. Phase gate: `uv run pytest -q` AND `uv run python eval/run_eval.py --check` both exit 0.
Output: Three integration tests green, three eval fixture pairs, full suite green.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md

# Source files to read before writing (one pass each)
@tests/test_resume_pipeline.py
@tests/conftest.py
@eval/run_eval.py
@eval/fixtures/07_missing_hours_coastal.json
@eval/fixtures/07_missing_hours_coastal_extraction.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests — snapshot_once, loop_guard, confirmed_dropped_no_reflag</name>
  <files>tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/test_resume_pipeline.py (current state — 3 xfail stubs from Plan 01). Read tests/conftest.py to understand what fixtures are available (especially any DB connection fixture, roster_from_seed, or skip-if-no-db markers). Read tests/test_clarify.py to understand how existing clarify integration tests set up DB state. CONTEXT.md D-19/D-20/D-21/D-28/D-15 lock the invariants each test must verify. RESEARCH.md §Validation Architecture §Integration layer confirms these are real DB tests requiring DATABASE_URL.
  </read_first>

  <action>
  Replace the three xfail stubs with real integration tests. All three tests require a live DATABASE_URL (check conftest.py for the existing `skip_if_no_db` pattern — use it). If DATABASE_URL is not available in the test environment, tests must be skipped (not xfail), so they do not give false confidence.

  Test setup pattern (follow test_clarify.py precedent): each test creates a real payroll run, advances it to AWAITING_REPLY state with a known extraction, then calls `resume_pipeline` and asserts the DB state.

  Test 1 — `test_snapshot_once_not_overwritten_on_second_clarify` (D-28):
  - Create a run, advance to AWAITING_REPLY (via existing helpers or direct DB inserts — follow test_clarify.py pattern).
  - Call `repo.set_pre_clarify_extracted(run_id, extracted_v1)` with a known extraction (original with OT=2).
  - Call `repo.set_pre_clarify_extracted(run_id, extracted_v2)` with a DIFFERENT extraction (reply with OT=0).
  - Assert `repo.load_pre_clarify_extracted(run_id)` returns the FIRST extraction (v1), NOT v2.
  - The IS NULL guard in SQL makes the second write a no-op.
  - This test is PURE DB: no orchestrator call needed. Fastest and most reliable for proving D-19.

  Test 2 — `test_loop_guard_fires_exactly_once_then_carry_forward` (D-20/D-13/D-16):
  - Create a run with a roster employee (Maria Chen, hourly, OT=2 hours in extraction).
  - Advance to AWAITING_REPLY with pre_clarify_extracted set (snapshot with OT=2).
  - Write `clarified_fields = {}` (first resume — no previous "asked" entry).
  - Call `resume_pipeline(run_id, reply_email_with_ot_absent, llm=None)` where the reply email produces an extraction with OT absent.
  - After the call: assert run is NOT in AWAITING_REPLY (it should have advanced to COMPUTED or AWAITING_APPROVAL, having carried OT=2 forward — OR it should be in AWAITING_REPLY a second time with clarified_fields showing "asked").
  - If the first call goes to AWAITING_REPLY with "asked" outcome: call resume_pipeline again with the same absent-OT reply.
  - Assert second call results in COMPUTED/AWAITING_APPROVAL (carry-forward applied, loop does not re-clarify).
  - Assert `repo.load_clarified_fields(run_id)` shows outcome `"carried_forward"` for the OT field.
  - This is the critical loop-guard test (D-20 termination). Use mocked LLM (`llm=None`) to force deterministic extraction — but if `llm=None` causes extract to skip, use the `--no-llm` pattern from test_clarify.py.

  Test 3 — `test_confirmed_dropped_does_not_reflag_money01` (D-15):
  - Create a run with extraction `OT=2` snapshot. First reply produces `OT=Decimal("0")` (explicit zero).
  - After first resume: assert `clarified_fields` shows `"confirmed_dropped"` for OT.
  - After second resume (same reply): assert the run advances WITHOUT a new field_regression issue (no second AWAITING_REPLY for OT).
  - Assert `resolved_drops` mechanism works: OT=0 does not re-trigger MONEY-01 clarification.

  All three tests: wrap in `pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="needs live DATABASE_URL")` to prevent false-green in CI without a DB.
  </action>

  <verify>
    <automated>uv run pytest tests/test_resume_pipeline.py -q -v 2>&1 | tail -20</automated>
  </verify>
  <done>All three tests PASS (or SKIP if no DATABASE_URL — acceptable). No xfail stubs remain in the file. `uv run pytest tests/test_resume_pipeline.py -v` shows PASSED or SKIPPED (never xfail or FAILED).</done>
</task>

<task type="auto">
  <name>Task 2: Eval fixtures 16/17/18 + eval --check</name>
  <files>eval/fixtures/16_zero_hours_hourly_coastal.json, eval/fixtures/16_zero_hours_hourly_coastal_extraction.json, eval/fixtures/17_nfd_name_coastal.json, eval/fixtures/17_nfd_name_coastal_extraction.json, eval/fixtures/18_field_drop_clarify_carry_coastal.json, eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json</files>

  <read_first>
  Read eval/fixtures/07_missing_hours_coastal.json and eval/fixtures/07_missing_hours_coastal_extraction.json to understand the exact structure (PATTERNS.md §9 has the full excerpt). Read eval/run_eval.py lines 1-60 (imports, _normalize, fixture loader) and lines 640-660 (extraction cache writer at line 655: `cache_path.write_text(extracted.model_dump_json(indent=2))`). PATTERNS.md §9 documents the naming convention: 16, 17, 18 prefixes; coastal business (from_addr matches the coastal business in the seed). CONTEXT.md D-24 locks the serialization requirement: extraction caches MUST be produced via `extracted.model_dump_json()`, NOT hand-typed. RESEARCH.md §Target 9 §fixture structure note: fixture 18 needs a `prior_extracted` field or sibling file.
  </read_first>

  <action>
  All six fixture files must be written as valid JSON following the 07_* structure. Produce extraction cache JSON via a Python script using `Extracted.model_dump_json()` (D-24 compliance — never hand-type Decimal values).

  Step 1 — Write a temporary generation script (scratchpad only, do NOT commit):
  Create a script at the scratchpad path that:
  a. Imports `Extracted, ExtractedEmployee` from `app.models.contracts`
  b. Imports `uuid`, `date`, `Decimal`
  c. Creates each `Extracted` object programmatically
  d. Writes `extracted.model_dump_json(indent=2)` to the fixture files

  Then run: `uv run python /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/f8767991-5653-4e3a-9b4b-d4eb596a72c8/scratchpad/gen_fixtures.py`

  The script creates:

  Fixture 16 (MONEY-01 — zero-hours hourly, coastal business):
  - `16_zero_hours_hourly_coastal.json`: hourly Maria Chen submitted with `hours_regular=0` (all else absent). Expected: `final_action="request_clarification"`, `missing_fields` contains "hours". `fixture_category="zero-hours-hourly"`.
  - `16_zero_hours_hourly_coastal_extraction.json`: `Extracted` with one employee `hours_regular: "0.00"` (Decimal("0") serializes as "0.00"), all other hours null. Use `extracted.model_dump_json(indent=2)`.

  Fixture 17 (MONEY-02 — NFD name, coastal business):
  - `17_nfd_name_coastal.json`: submitted name is NFD form of "José Martínez" (use `unicodedata.normalize("NFD", "José Martínez")` in the body_text). Expected: `final_action="process"`, reconciliation shows `name_category="exact"`, `expected_source="exact"`, `expected_resolved=True`. `fixture_category="nfd-name"`.
  - `17_nfd_name_coastal_extraction.json`: `Extracted` with the NFD submitted_name, regular hours present. Use `model_dump_json()`.
  NOTE: Fixture 17 needs a roster employee named "José Martínez" (NFC). If no such employee exists in the seed data, use an existing coastal employee name and construct the NFD variant. Choose "María García" (check seed data) or add a plausible Spanish-named employee to the coastal roster — or just use a simpler NFD example where the name IS in the seed (check seed.py for any accented characters).

  ALTERNATIVE: The NFD test proves the NORMALIZER works, not that a specific employee exists. For the eval fixture, the expected `extracted` can use the existing coastal roster employee names (no accents needed for the eval to test normalization scoring). The NFD test in test_reconcile.py (Plan 01/02) already proves the normalization. Fixture 17 can be a simpler case that exercises the scorer's `_normalize` (C-4 fix). Use an existing exact-match employee with a submitted name that differs only in whitespace normalization, which `_normalize` handles — OR keep fixture 17 minimal and note that the full NFD proof is in test_reconcile.py.

  Fixture 18 (MONEY-03 — field drop/carry-forward, coastal business):
  - `18_field_drop_clarify_carry_coastal.json`: submitted "40 regular + 2 OT". Expected: `final_action="request_clarification"` when `prior` is provided with OT=2 and resumed extraction has OT=None. `fixture_category="field-regression"`.
  - `18_field_drop_clarify_carry_coastal_extraction.json`: `Extracted` with employee having `hours_overtime=None` (the resumed extraction). This is the "resumed" snapshot. The "prior" snapshot (OT=2) must also be serialized.
  - For fixture 18, the eval needs BOTH the resumed extraction (standard `_extraction.json`) AND the prior snapshot. Add a `prior_extracted` key to the main fixture JSON file that contains the serialized prior `Extracted` (OR add a `18_field_drop_clarify_carry_coastal_prior.json` sibling file). Check how `run_eval.py` fixture loader handles this — read it before deciding. If the loader only handles `_extraction.json`, the cleanest approach is to embed `prior_extracted` inline in the main fixture JSON.

  Step 2 — Write the fixture files. Before writing, run `ls eval/fixtures/` to confirm numbering (Plan 01 reading confirmed 15 existing fixtures). Numbers 16/17/18 are available.

  Step 3 — Run the eval to confirm new fixtures score correctly:
  `uv run python eval/run_eval.py --check 2>&1 | tail -30`
  If `--check` does not exist, run `uv run python eval/run_eval.py 2>&1 | tail -30` and verify the new fixtures appear in the summary with passing scores.

  If `run_eval.py` needs changes to support the `prior_extracted` key for fixture 18: update `run_eval.py` to load `prior_extracted` from the fixture JSON and pass it as `prior=prior` to `validate()`. This is a small addition to the fixture loader (check lines 100-130 of run_eval.py for the fixture loading logic).

  D-24 compliance check: after writing `_extraction.json` files, verify they parse back correctly:
  `uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/16_zero_hours_hourly_coastal_extraction.json').read())); print('Fixture 16 OK:', len(e.employees), 'employees')"`
  </action>

  <verify>
    <automated>uv run python eval/run_eval.py 2>&1 | tail -20</automated>
  </verify>
  <done>`uv run python eval/run_eval.py` (or `--check`) exits 0 and shows all three new fixtures (16, 17, 18) scoring correctly (no failures). All six fixture files exist. Extraction caches parse via `Extracted.model_validate()` without error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Integration tests (snapshot_once, loop_guard, confirmed_dropped_no_reflag) + three eval fixture pairs. Full phase evidence assembled.</what-built>
  <how-to-verify>
  1. Full test suite: `uv run pytest -q` — exits 0 (integration tests PASS or SKIP; no FAILED)
  2. Eval: `uv run python eval/run_eval.py` — no failures on new fixtures 16/17/18
  3. Phase gate — all three ROADMAP Success Criteria verifiable:
     - SC1 (MONEY-01): `uv run pytest tests/test_validate.py -k "zero_hours" -x` exits 0 AND fixture 16 scores correctly
     - SC2 (MONEY-02): `uv run pytest tests/test_reconcile.py -k "nfd" -x` exits 0 AND fixture 17 scores correctly
     - SC3 (MONEY-03): `uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -x` PASSES AND fixture 18 scores correctly
  4. Confirm D-23 two-layer split in SUMMARY: eval certifies judgment; test_resume_pipeline certifies state machine.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass. Describe any failures so they can be diagnosed before marking Phase 7 complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/fixtures/*.json | Read from disk by run_eval.py; Pydantic validates on load via model_validate |
| test_resume_pipeline.py → live DB | Integration tests write/read real DB state; scoped to test run_ids |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Tampering | eval fixture JSON files — read by Pydantic model_validate | mitigate | `Extracted.model_validate()` with `extra="forbid"` rejects malformed fixture JSON at load time, preventing silent bad data from reaching the test assertion. D-24 compliance (model_dump_json serialization) ensures Decimal precision is correct. |
| T-07-13 | Information Disclosure | Integration tests write to real Supabase dev DB | accept | Tests use synthetic run_ids and synthetic extraction data (no real employee PII). If cleanup fails, only test data leaks — no production PII. Tests should clean up inserted rows in a `finally` block. |
| T-07-14 | Elevation of Privilege | Integration test run_ids colliding with real production runs | mitigate | Use fixed UUID prefixes (e.g. `test-0000-...`) identifiable as test data. Assert cleanup at test teardown. |
| T-07-SC | Tampering | No new package installs | accept | No `uv add` invoked; all imports from existing pyproject.toml deps |
</threat_model>

<verification>
Full phase gate (all plans combined):

1. `uv run pytest -q` — exits 0 (full suite, all plans)
2. `uv run python eval/run_eval.py` — no failures, fixtures 16/17/18 present
3. `uv run pytest tests/test_validate.py -k "zero_hours" -x` — GREEN (MONEY-01)
4. `uv run pytest tests/test_reconcile.py -k "nfd" -x` — GREEN (MONEY-02)
5. `uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -x` — GREEN/SKIP (MONEY-03)
6. `grep "def _is_paid" app/pipeline/validate.py` — exits 0
7. `grep "unicodedata.normalize" app/pipeline/reconcile_names.py` — exits 0
8. `grep "field_regression" app/pipeline/decide.py` — exits 0
9. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
10. `grep "clarified_fields JSONB" app/db/schema.sql` — exits 0
</verification>

<success_criteria>
- All three ROADMAP Phase 7 Success Criteria are verifiable:
  1. zero-hours hourly gates (test + fixture 16 both prove it)
  2. NFD name resolves (test + fixture 17 both prove it)
  3. field-regression clarifies once then terminates (test + fixture 18 both prove it)
- D-23 two-layer split documented: eval = judgment, test_resume_pipeline = state machine
- Full suite green; eval passes; phase ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-05-SUMMARY.md` when done.
</output>
