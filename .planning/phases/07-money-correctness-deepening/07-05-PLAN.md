---
phase: 07-money-correctness-deepening
plan: "05"
type: execute
wave: 5
depends_on:
  - "07-04"
files_modified:
  - tests/test_resume_pipeline.py
  - eval/run_eval.py
  - eval/fixtures/16_zero_hours_hourly_coastal.json
  - eval/fixtures/16_zero_hours_hourly_coastal_extraction.json
  - eval/fixtures/17_nfd_name_coastal.json
  - eval/fixtures/17_nfd_name_coastal_extraction.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json
  - eval/summary.json
autonomous: false
requirements:
  - MONEY-01
  - MONEY-02
  - MONEY-03

must_haves:
  truths:
    - "Integration tests for snapshot_once, loop_guard (two-round single-_run_stages invariant), confirmed_dropped_no_reflag, N1-invariant (single _run_stages call count), N2-ordering (asked-before-send), and D-26 explicit-drop PASS (not skip) when DATABASE_URL is set — skip ≠ evidence per D-23"
    - "D-26 explicit-drop integration test proves: reply with hours_overtime=Decimal('0') → outcome is confirmed_dropped → no backfill → final extracted_data has OT=0 (not the snapshot value) — closes the N3/D-14 overpay bug"
    - "Three new eval fixtures (16, 17, 18) exist in eval/fixtures/ and eval/run_eval.py scores all three correctly"
    - "Fixture 17 (MONEY-02 NFD) is HONEST: certifies the eval scorer _normalize parity (C-4 fix). Full NFD Unicode proof is test_reconcile.py:test_nfd_name_resolves_same_as_nfc. SUMMARY states the scope explicitly."
    - "run_eval.py updated: prior_extracted key stripped from fixture dict before InboundEmail construction; prior_extracted loaded and passed as prior= to validate(); --check passes with updated summary.json including fixtures 16/17/18"
    - "Fixture extraction caches produced via Extracted.model_dump_json() — NOT hand-typed JSON (D-24)"
    - "uv run python eval/run_eval.py --check exits 0 with all three new fixtures scoring correctly"
    - "Full test suite green: uv run pytest -q — integration tests PASS (not skip) when DATABASE_URL is set"
  artifacts:
    - path: "tests/test_resume_pipeline.py"
      provides: "6 real integration tests covering N1/N2/N3/N7/D-26/loop-guard invariants — no xfail stubs"
    - path: "eval/run_eval.py"
      provides: "prior_extracted support in fixture loader + fixture stripping + summary.json regeneration"
    - path: "eval/fixtures/16_zero_hours_hourly_coastal.json"
      provides: "MONEY-01 eval fixture (zero-hours-hourly → request_clarification)"
    - path: "eval/fixtures/17_nfd_name_coastal.json"
      provides: "MONEY-02 eval scorer fixture (proves _normalize parity via C-4 fix)"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal.json"
      provides: "MONEY-03 eval fixture (prior= seam: detect field drop → request_clarification)"
    - path: "eval/summary.json"
      provides: "Updated summary.json including fixtures 16/17/18 so --check passes"
  key_links:
    - from: "eval/fixtures/18_field_drop_clarify_carry_coastal.json (prior_extracted key)"
      to: "eval/run_eval.py:validate(prior=prior)"
      via: "fixture loader reads prior_extracted key → Extracted.model_validate → passed as prior= kwarg"
      pattern: "prior_extracted"
    - from: "tests/test_resume_pipeline.py:test_n1_single_run_stages_call"
      to: "app/pipeline/orchestrator.py:resume_pipeline"
      via: "patches _run_stages; asserts called exactly once regardless of Round 1 or Round 2 path"
      pattern: "_run_stages"
    - from: "tests/test_resume_pipeline.py:test_n3_explicit_zero_confirmed_dropped"
      to: "app/pipeline/orchestrator.py:resume_pipeline (Round 2 classify-from-raw)"
      via: "verifies N3: explicit zero in resumed extraction classified as confirmed_dropped; final extracted_data has OT=0 not snapshot value"
      pattern: "confirmed_dropped"
---

<objective>
Complete Phase 7 evidence: replace all xfail integration test stubs with real PASSING assertions, add new tests for the Round-2 findings (N1/N2/N3/D-26), add three eval judgment fixtures certifying MONEY-01/02/03 (D-23 eval layer), update run_eval.py to support prior_extracted fixtures, and regenerate summary.json.

Purpose: D-23 requires two layers of evidence. This plan provides both: integration tests (state machine) + eval fixtures (judgment purity). The integration tests MUST PASS before the phase is marked complete — skip is not evidence.

D-23 two-layer split:
- Eval layer: certifies pure judgment (validate → decide) given a prior. Proves MONEY-01 ($0 gate), MONEY-02 (NFD scorer parity), MONEY-03 (field drop detection → clarification).
- Integration layer: certifies the state machine invariants (snapshot-once, single-_run_stages-call, N2 asked-before-send ordering, N3 explicit-zero vs silence distinction, confirmed_dropped D-15 interaction). Requires a live DB with the new columns applied.

REVISION NOTES (from Round-1 and Round-2 Codex review):
- Finding #7 (prior_extracted support): run_eval.py must load prior_extracted from fixture JSON and pass as prior= to validate(). The "prior_extracted" key must also be stripped from the fixture dict before InboundEmail construction.
- Finding #8 (NFD fixture honesty): Seed data has no accented coastal employee. Fixture 17 CANNOT claim NFD Unicode matching via the eval alone. Honest scope: fixture 17 proves C-4 (_normalize parity with _norm). NFD Unicode proof is in test_reconcile.py. State this clearly in SUMMARY.
- Finding #9 (integration tests must not skip at phase gate): skipif(not DATABASE_URL) is acceptable during development; the phase gate checkpoint requires the developer to run WITH DATABASE_URL set.
- N1/N2/N3 findings: the xfail stubs in test_resume_pipeline.py must be replaced with tests that validate the structural fixes from Plan 04 — specifically: single _run_stages call count (N1), asked-before-send ordering (N2), and explicit-zero classification before backfill (N3/D-26).

Output: 6 integration tests PASSING, three eval fixture pairs, updated run_eval.py, regenerated summary.json.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md

# Source files to read before writing (one pass each)
@tests/test_resume_pipeline.py
@tests/conftest.py
@eval/run_eval.py
@eval/fixtures/07_missing_hours_coastal.json
@eval/fixtures/07_missing_hours_coastal_extraction.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_eval.py prior_extracted support + fixture stripping + summary regen</name>
  <files>eval/run_eval.py, eval/summary.json</files>

  <read_first>
  Read eval/run_eval.py in full. Focus on:
  - Lines 100-130: the fixture loader (wherever "expected" and "fixture_category" are stripped). Identify the exact dict comprehension or pop() calls used to strip keys before passing to InboundEmail.
  - Lines 155-165: where validate() is called (or the equivalent eval pipeline run point). This is where prior=prior_extracted needs to be threaded in.
  - Lines 640-660: the extraction cache writer (--record path, via extracted.model_dump_json(indent=2)) — confirms D-24 compliance.
  - The --check flag behavior: how it loads summary.json and compares. The summary.json path.
  - Whether _normalize is already updated from Plan 02 (grep: "from app.pipeline.reconcile_names import _norm as _normalize") — it should be; do not revert it.

  Read eval/summary.json (first 30 lines) to understand its structure and update format.
  SCOPE: This task only handles run_eval.py changes for prior support + fixture stripping. Fixture files are written in Task 2.
  </read_first>

  <action>
  Step 1 — Update fixture loader to strip "prior_extracted" key:
  In the fixture loading function (wherever "expected" and "fixture_category" are stripped), add "prior_extracted" to the stripped key set. The fixture dict is filtered before being used to construct InboundEmail — the prior_extracted value is payroll data (an Extracted object shape), not an InboundEmail field, so it must be stripped before InboundEmail validation.

  Step 2 — Load prior_extracted from fixture and pass to validate():
  In the eval pipeline function (where validate() is called), before the call:
  - Load the raw fixture dict (before stripping) to access prior_extracted_data = fixture_raw.get("prior_extracted").
  - If prior_extracted_data is not None: prior_extracted = Extracted.model_validate(prior_extracted_data). Import Extracted from app.models.contracts if not already imported.
  - Call validate(extracted, roster, matches, prior=prior_extracted) where prior_extracted is None if the fixture has no "prior_extracted" key. This preserves all existing fixture behavior (prior_extracted=None is a no-op per D-18).

  Step 3 — Regenerate summary.json AFTER Task 2 creates the fixture files:
  After Task 2 runs, execute: uv run python eval/run_eval.py --record (or the equivalent flag — check the actual CLI by reading the argparse block in run_eval.py). This regenerates summary.json including fixtures 16/17/18. Then verify: uv run python eval/run_eval.py --check exits 0.
  NOTE: Task 2 must complete before this step. The step is recorded here for ordering clarity; perform it at the end of Task 2 after fixture files exist.
  </action>

  <verify>
    <automated>uv run python -c "import ast; src=open('eval/run_eval.py').read(); assert 'prior_extracted' in src, 'prior_extracted not wired'; print('prior_extracted wiring found')"</automated>
  </verify>
  <done>"prior_extracted" appears in eval/run_eval.py. The "prior_extracted" key is stripped from the fixture dict before InboundEmail construction. validate() receives prior= kwarg from fixture data when fixture has "prior_extracted" key. Existing eval fixtures still score correctly (prior=None is a no-op).</done>
</task>

<task type="auto">
  <name>Task 2: Eval fixtures 16/17/18 + eval --check passes</name>
  <files>eval/fixtures/16_zero_hours_hourly_coastal.json, eval/fixtures/16_zero_hours_hourly_coastal_extraction.json, eval/fixtures/17_nfd_name_coastal.json, eval/fixtures/17_nfd_name_coastal_extraction.json, eval/fixtures/18_field_drop_clarify_carry_coastal.json, eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json, eval/summary.json</files>

  <read_first>
  Read eval/fixtures/07_missing_hours_coastal.json and eval/fixtures/07_missing_hours_coastal_extraction.json to capture the exact fixture structure (PATTERNS.md §9 has the full excerpt). Read eval/run_eval.py lines 640-660 (extraction cache writer — extracted.model_dump_json(indent=2)). PATTERNS.md §9 documents the naming convention and D-24 constraint.

  Check the seed data for coastal business employees: grep -n "coastal" app/db/seed.py or run uv run python -m app.db.seed --dry-run. Identify employee names used for coastal business to use plausible real names in fixtures.

  FIXTURE 17 HONESTY check (finding #8): grep the seed for accented characters in coastal employees. If no accented coastal employee exists: fixture 17 uses a submitted name differing from a real coastal employee's name in whitespace only (tests _normalize NFC call on whitespace normalization). Document this scope in SUMMARY.
  </read_first>

  <action>
  Step 1 — Write a generation script to the scratchpad (NOT committed):
  Create /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/f8767991-5653-4e3a-9b4b-d4eb596a72c8/scratchpad/gen_phase7_fixtures.py that:
  a. Imports Extracted, ExtractedEmployee from app.models.contracts
  b. Imports uuid, date, Decimal, json, pathlib
  c. Creates each Extracted object programmatically and writes extraction files using extracted.model_dump_json(indent=2) — D-24 compliance.
  d. Creates each main fixture JSON manually (matching the 07_missing_hours_coastal.json structure).

  Fixture 16 (MONEY-01 — zero-hours hourly):
  - Main: 16_zero_hours_hourly_coastal.json — use a real coastal business employee (from seed). Hourly employee with hours_regular=Decimal("0"), all other hours null. Expected: final_action="request_clarification", gate_reasons_contains=["missing"] or ["hours"]. fixture_category="zero-hours-hourly".
  - Extraction cache: Extracted with one employee hours_regular="0.00" (Decimal("0") serialized), all other hours null. Run the generation script to produce the correct JSON via model_dump_json.

  Fixture 17 (MONEY-02 / C-4 eval parity):
  - Inspect seed for coastal employees. If no accented name exists: use an existing coastal employee's name with extra internal whitespace (e.g. "Maria  Chen" with two spaces) as the submitted name. The eval scorer's _normalize (now aliased to _norm after Plan 02) normalizes whitespace → matches "Maria Chen" in the roster. This proves C-4 (_normalize parity with _norm) since _norm also whitespace-normalizes. Expected: final_action="process", reconciliation shows resolved=True.
  - fixture_category="nfc-normalizer-parity" (honest — does NOT claim "nfd-unicode" unless a real accented employee exists).
  - Document in SUMMARY: "Fixture 17 certifies the eval scorer's _normalize parity with reconcile_names._norm (C-4 fix, Plan 02). Full NFD Unicode proof is tests/test_reconcile.py:test_nfd_name_resolves_same_as_nfc."

  Fixture 18 (MONEY-03 — field regression → request_clarification with prior):
  - Use a coastal hourly employee. The resumed extraction has hours_overtime=None (absent). The prior snapshot Extracted has hours_overtime=Decimal("2").
  - Main fixture: 18_field_drop_clarify_carry_coastal.json — add a "prior_extracted" key containing the prior snapshot as a JSON object (produced by prior_extracted_obj.model_dump(mode="json") — a plain dict, embedded as a nested JSON object in the fixture).
  - Expected: final_action="request_clarification" when validate() is called with prior=prior_extracted.
  - fixture_category="field-regression-carry-forward".
  - Extraction cache: Extracted with the resumed employee (hours_overtime null). Via model_dump_json.

  Step 2 — Run the generation script:
  uv run python /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/f8767991-5653-4e3a-9b4b-d4eb596a72c8/scratchpad/gen_phase7_fixtures.py

  Step 3 — D-24 compliance verification:
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/16_zero_hours_hourly_coastal_extraction.json').read())); print('Fixture 16 OK:', len(e.employees), 'employees')"
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json').read())); print('Fixture 18 OK:', len(e.employees), 'employees')"

  Step 4 — Run eval and verify fixtures score correctly:
  uv run python eval/run_eval.py 2>&1 | tail -30
  Verify fixtures 16, 17, 18 score correctly (16 and 18 → request_clarification; 17 → process with resolved=True).

  Step 5 — Regenerate summary.json (from Task 1 Step 3):
  uv run python eval/run_eval.py --record (or equivalent flag)
  uv run python eval/run_eval.py --check 2>&1 | tail -20 — must exit 0.
  </action>

  <verify>
    <automated>uv run python eval/run_eval.py 2>&1 | tail -20</automated>
  </verify>
  <done>uv run python eval/run_eval.py exits 0 and shows fixtures 16/17/18 scoring correctly. uv run python eval/run_eval.py --check exits 0. All six fixture files exist. Extraction caches parse via Extracted.model_validate() without error. summary.json updated to include fixture 16/17/18 expected results.</done>
</task>

<task type="auto">
  <name>Task 3: Integration tests — 6 tests covering N1/N2/N3/D-26/snapshot_once/confirmed_dropped_no_reflag</name>
  <files>tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/test_resume_pipeline.py (current state — 3 xfail stubs from Plan 01, plus the 2-3 tests added in Plan 04 Task 2 behavior block). Read tests/conftest.py to understand DB connection fixtures and skip markers. Read tests/test_clarify.py to understand how existing clarify integration tests set up DB state (run creation pattern, mock LLM, cleanup). CONTEXT.md D-13/D-14/D-15/D-19/D-26/D-28 lock the invariants each test must verify.

  CRITICAL per finding #9 (Round 1): integration tests must PASS at phase gate. Use @pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="requires live DATABASE_URL") — this is NOT the same as xfail. skipif preserves failure detection; xfail masks it.

  Test setup pattern: follow tests/test_clarify.py precedent. Each test:
  1. Creates a payroll run in the required state via direct DB inserts or existing helper functions.
  2. Asserts the expected behavior.
  3. Cleans up in a finally block.
  </read_first>

  <action>
  Replace ALL xfail stubs with real tests. Add new tests for Round-2 findings. Total: 6 tests.

  All tests: @pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="requires live DATABASE_URL — set before phase gate") and finally: cleanup blocks.

  Test 1 — test_snapshot_once_not_overwritten_on_second_write (D-19/D-28):
  - Create a payroll run in AWAITING_REPLY state via direct DB insert.
  - Call repo.set_pre_clarify_extracted(run_id, extracted_v1) with a known extraction (hours_overtime=Decimal("2")).
  - Call repo.set_pre_clarify_extracted(run_id, extracted_v2) with a DIFFERENT extraction (hours_overtime=Decimal("0")).
  - Assert repo.load_pre_clarify_extracted(run_id) returns extracted_v1 (the IS NULL guard makes the second write a no-op).
  - Pure DB test; no orchestrator call needed. Verifies D-19 snapshot-once.

  Test 2 — test_n1_single_run_stages_call_on_resume (N1):
  - Use unittest.mock.patch to monkeypatch _run_stages in orchestrator, recording call count.
  - The patched _run_stages returns a realistic StageResult (with a mock decision.final_action="process").
  - Create a run in AWAITING_REPLY state; call resume_pipeline(run_id, mock_inbound, llm=mock_llm).
  - Assert the patched _run_stages was called exactly once. This is the N1 structural invariant.
  - This test does NOT require live extraction — the monkeypatch returns a controlled result.

  Test 3 — test_n2_asked_written_before_send (N2):
  - Set up a run in AWAITING_REPLY state with a pre_clarify_extracted snapshot (hours_overtime=Decimal("2")).
  - Monkeypatch _clarify to record (a) whether clarified_fields already has the "asked" entry when called, (b) the purpose it was called with.
  - Call resume_pipeline with a combined email that extracts hours_overtime=None (drop detected).
  - Assert: when _clarify was invoked, repo.load_clarified_fields(run_id) already had {emp_id: {"hours_overtime": "asked"}} (the write happened before the send).
  - Assert: _clarify was called with purpose="clarification_field_regression".

  Test 4 — test_n3_explicit_zero_classified_as_confirmed_dropped and no_backfill (N3/D-26):
  - Set up a run with pre_clarify_extracted (hours_overtime=Decimal("2")) and clarified_fields with "asked" entry for OT (simulating Round 2).
  - Configure mock LLM to return an extraction with hours_overtime=Decimal("0") (explicit zero — client says "remove it").
  - Call resume_pipeline (Round 2 path — clarified has "asked" entries).
  - Assert: clarified_fields after the call has outcome "confirmed_dropped" for OT (not "carried_forward").
  - Assert: repo.load_run(run_id)["extracted_data"] has hours_overtime="0.00" (NOT Decimal("2") from snapshot) — the confirmed_dropped field was NOT backfilled. This is the D-14/D-26 explicit-drop integration proof.

  Test 5 — test_loop_guard_round_1_sets_asked_and_awaits (decision A):
  - Create a run where the first resume arrives with hours_overtime=None (drop vs. snapshot OT=2).
  - The mock LLM returns OT=None. The run has pre_clarify_extracted set (from the prior clarification cycle).
  - Call resume_pipeline (Round 1 path — no "asked" in clarified yet).
  - Assert: run status is AWAITING_REPLY (clarification was sent).
  - Assert: clarified_fields has "asked" for OT (the loop guard fired).
  - Assert: the outbound email_messages row has purpose="clarification_field_regression".

  Test 6 — test_confirmed_dropped_does_not_trigger_money01 (D-15):
  - Create a run with clarified_fields = {str(emp_id): {"hours_overtime": "confirmed_dropped"}} (simulating a completed Round 1/2 cycle where OT was explicitly removed).
  - The resumed extraction has hours_overtime=Decimal("0") and hours_regular=Decimal("40") (normal paid week).
  - Call resume_pipeline.
  - Assert: run does NOT re-clarify for a field_regression on OT (confirmed_dropped suppresses re-flag per D-15, N8 fix).
  - Assert: run advances to COMPUTED/AWAITING_APPROVAL (the payroll processes without re-asking about OT).

  All tests use uuid.uuid4() for run IDs, have finally: cleanup blocks, and import os with the skipif decorator.
  </action>

  <verify>
    <automated>uv run pytest tests/test_resume_pipeline.py -v -q 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 tests PASS (or SKIP if DATABASE_URL not set — SKIP is acceptable for development; PASS is required at phase gate). No xfail stubs remain. When DATABASE_URL is set: uv run pytest tests/test_resume_pipeline.py -v shows 6 PASSED (not SKIPPED, not xfail, not FAILED).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>6 integration tests (N1/N2/N3/D-26/loop-guard/D-15) + three eval fixture pairs (16/17/18) + updated run_eval.py with prior support + updated summary.json. Full phase evidence assembled.</what-built>
  <how-to-verify>
  PHASE GATE — all checks must pass WITH DATABASE_URL set. If DATABASE_URL is not in the shell env, set it from the Supabase connection string (Supavisor transaction mode, port 6543) before running.

  1. Full test suite (with live DB):
     uv run pytest -q — exits 0.
     Integration tests must PASS (not skip). If they skip, DATABASE_URL is not set.
     uv run pytest tests/test_resume_pipeline.py -v — shows 6 PASSED (not SKIPPED, not xfail).

  2. Eval:
     uv run python eval/run_eval.py --check — exits 0. Fixtures 16/17/18 score correctly.

  3. Per-requirement ROADMAP Success Criteria:
     - SC1 (MONEY-01): uv run pytest tests/test_validate.py -k "zero_hours" -x exits 0 AND fixture 16 scores as request_clarification.
     - SC2 (MONEY-02): uv run pytest tests/test_reconcile.py -k "nfd" -x exits 0 AND grep "_norm as _normalize" eval/run_eval.py exits 0 (C-4 fix in place).
     - SC3 (MONEY-03): uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -x PASSES AND fixture 18 scores as request_clarification with prior= support.

  4. Round-2 finding verification (all 8 fixes):
     N1: python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(f'_run_stages calls in resume_pipeline: {len([n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(n.func,\"id\",None)==\"_run_stages\"])}') for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='resume_pipeline']"
     (should print 1)
     N2: uv run pytest tests/test_resume_pipeline.py -k "n2_asked" -v — PASSES.
     N3/D-26: uv run pytest tests/test_resume_pipeline.py -k "n3_explicit_zero" -v — PASSES.
     N4: grep "clarification_field_regression" app/db/schema.sql exits 0 AND insert test from Plan 04 checkpoint succeeds.
     N5: grep "field regression" app/pipeline/compose_email.py exits 0 (unconditional block).
     N6: grep "prior_matches" app/pipeline/validate.py exits 0.
     N7: grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py shows 3+.
     N8: grep "resolved_drops" app/pipeline/validate.py exits 0 (per-field suppression present).

  5. D-23 two-layer documentation in SUMMARY:
     - Eval layer certifies judgment (validate → decide purity given a prior).
     - Integration tests certify state machine (snapshot-once D-19/D-28, N1 single-call invariant, N2 asked-before-send, N3/D-26 explicit-drop classification, D-15 confirmed_dropped no-reflag).
     - Fixture 17 is honestly scoped: certifies _normalize/C-4 parity; NFD Unicode proof is in test_reconcile.py. Documented in SUMMARY if seed has no accented coastal employee.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass WITH DATABASE_URL set and all 6 integration tests PASS (not skip). Describe any failures for diagnosis before marking Phase 7 complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/fixtures/*.json | Read from disk by run_eval.py; Pydantic validates on load via model_validate |
| tests/test_resume_pipeline.py → live DB | Integration tests write/read real DB state; scoped to test run_ids with cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13 | Tampering | eval fixture JSON — prior_extracted key parsed and passed to validate() | mitigate | Extracted.model_validate() with extra="forbid" rejects malformed prior_extracted data at load time. D-24 compliance (model_dump(mode="json") serialization) ensures Decimal precision is correct. |
| T-07-14 | Information Disclosure | Integration tests write to real Supabase dev DB | accept | Tests use synthetic run_ids and synthetic extraction data (no real employee PII). Tests clean up inserted rows in finally blocks. |
| T-07-15 | Elevation of Privilege | Test run_ids colliding with production runs | mitigate | Use uuid.uuid4() for all test run IDs; assert cleanup at test teardown. The dev DB is separate from production. |
| T-07-SC | Tampering | No new package installs | accept | No uv add invoked; all imports from existing pyproject.toml deps |
</threat_model>

<verification>
Full phase gate (all plans combined):

1. uv run pytest -q — exits 0 (full suite; integration tests PASS with DATABASE_URL)
2. uv run python eval/run_eval.py --check — exits 0 (fixtures 16/17/18 present and scoring correctly)
3. uv run pytest tests/test_validate.py -k "zero_hours" -x — GREEN (MONEY-01)
4. uv run pytest tests/test_reconcile.py -k "nfd" -x — GREEN (MONEY-02)
5. uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -x — PASSED/not skipped (MONEY-03)
6. grep "def _is_paid" app/pipeline/validate.py — exits 0
7. grep "unicodedata.normalize" app/pipeline/reconcile_names.py — exits 0
8. grep "field_regression" app/pipeline/decide.py — exits 0
9. grep "pre_clarify_extracted JSONB" app/db/schema.sql — exits 0
10. grep "clarified_fields JSONB" app/db/schema.sql — exits 0
11. grep "clarification_field_regression" app/db/schema.sql — exits 0
12. grep "clarification_field_regression" app/db/repo.py — exits 0 (allowlist)
13. grep "def detect_field_regression" app/pipeline/validate.py — exits 0
14. grep "RawFieldDrop" app/pipeline/validate.py — exits 0
15. grep "StageResult" app/pipeline/orchestrator.py — exits 0
16. grep "set_clarified_fields" app/pipeline/orchestrator.py — exits 0
17. grep "prior_matches" app/pipeline/validate.py — exits 0 (N6 D-11 fix)
18. grep "field regression" app/pipeline/compose_email.py — exits 0 (N5 fix)
19. uv run pytest tests/test_resume_pipeline.py -v — 6 PASSED (not SKIPPED when DATABASE_URL set)
</verification>

<success_criteria>
- All three ROADMAP Phase 7 Success Criteria verifiable:
  1. Zero-hours hourly gates (test_validate.py + fixture 16 both prove it)
  2. NFD name resolves (test_reconcile.py proves it; fixture 17 certifies _normalize parity)
  3. Field-regression clarifies once then terminates via two-round architecture (6 integration tests + fixture 18 prove it)
- All 8 Round-2 findings (N1-N8) closed and verified:
  N1: single _run_stages call in resume_pipeline
  N2: asked written before send
  N3/D-26: explicit-zero classified as confirmed_dropped, no backfill
  N4: DB CHECK constraint expanded on existing DBs
  N5: field-regression question in email regardless of other clarification reasons
  N6: diff enforces same employee_id in both snapshots
  N7: snapshot-once on all 3 AWAITING_REPLY paths
  N8: resolved_drops suppression is per-(employee_id, field), not per-employee
- D-23 two-layer split documented honestly in SUMMARY: eval = judgment, test_resume_pipeline = state machine; fixture 17 scoping explicit
- Integration tests PASS (not skip) when DATABASE_URL is set — skip ≠ evidence (finding #9)
- run_eval.py updated: prior_extracted support, fixture stripping, summary.json regenerated
- Full suite green; eval --check passes; phase ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-05-SUMMARY.md` when done.
</output>
