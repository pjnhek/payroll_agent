---
phase: 07-money-correctness-deepening
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - app/pipeline/validate.py
  - app/pipeline/reconcile_names.py
  - eval/run_eval.py
autonomous: true
requirements:
  - MONEY-01
  - MONEY-02

must_haves:
  truths:
    - "An hourly employee with hours_regular=Decimal('0') and all other hours None gates to request_clarification (never ships $0 paystub) — the MONEY-01 fix (D-02/D-09)"
    - "An hourly employee with hours_regular=Decimal('0') but hours_holiday=Decimal('8') does NOT gate — partial week still processes (D-03)"
    - "NFD 'José Martínez' submitted resolves to the same employee_id as NFC 'José Martínez' in the roster — MONEY-02 fix. Closes the casefold-only `_norm` bug (D-04) with the hardened NFC form (D-05/D-07)."
    - "_is_paid shared predicate exists at module level in validate.py — three call sites (any_hours, detect_field_regression, D-05 OT guard alignment) (D-09)"
    - "eval/run_eval.py:_normalize is DRY with _norm — imported directly so NFD eval fixtures score correctly (C-4)"
  artifacts:
    - path: "app/pipeline/validate.py"
      provides: "_is_paid predicate + fixed any_hours gate"
      contains: "def _is_paid"
    - path: "app/pipeline/reconcile_names.py"
      provides: "NFC-hardened _norm"
      contains: "unicodedata.normalize"
    - path: "eval/run_eval.py"
      provides: "_normalize imported from reconcile_names (_norm)"
      contains: "_norm as _normalize"
  key_links:
    - from: "app/pipeline/validate.py:_is_paid"
      to: "app/pipeline/validate.py:any_hours (line ~84)"
      via: "replaces is not None predicate"
      pattern: "_is_paid"
    - from: "app/pipeline/reconcile_names.py:_norm"
      to: "eval/run_eval.py:_normalize"
      via: "direct import; C-4 fix"
      pattern: "from app.pipeline.reconcile_names import _norm as _normalize"
---

<objective>
Flip the two pure-function money-mover RED tests GREEN: the `_is_paid` shared predicate + `any_hours` fix (MONEY-01) and the NFC `_norm` hardening (MONEY-02). Both are self-contained one-function changes; they share no files and can be executed sequentially in one plan. Also fix `run_eval.py:_normalize` (C-4) in the same wave so the eval scorer does not diverge from `_norm` after MONEY-02.

Purpose: D-30 priority — ship the shared predicate (`_is_paid`) before the loop-guard wiring so MONEY-03's `detect_field_regression` and MONEY-01's `any_hours` cannot disagree on the zero boundary.
Output: Three file edits, all pre-existing RED tests now GREEN, full suite still green.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/pipeline/validate.py
@app/pipeline/reconcile_names.py
@eval/run_eval.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _is_paid predicate + any_hours MONEY-01 fix (validate.py)</name>
  <files>app/pipeline/validate.py</files>

  <read_first>
  Read app/pipeline/validate.py (full file — it is the target). Key anchor lines per RESEARCH.md:
  - Lines 27-33: `_HOURS_FIELDS` tuple — `detect_field_regression` will reuse this; do NOT redefine it.
  - Lines 84-88: `any_hours = any(getattr(emp, f) is not None ...)` — the D-01 bug to replace.
  - Lines 102-140: D-05 OT guard — `ot_missing = ot is None or ot == 0` (D-09 precedent).
  - Lines 69-73: current `validate()` signature — gains `prior=None` and `resolved_drops=None` in Plan 03/04; do NOT add those kwargs yet (they belong to MONEY-03 plans). This plan touches ONLY `_is_paid` + `any_hours`.
  PATTERNS.md §1 has the exact placement: `_is_paid` goes immediately after `_HOURS_FIELDS` (before `_employee_pay_type`). CONTEXT.md D-02/D-03/D-09 lock the rule.
  </read_first>

  <behavior>
  RED tests to turn GREEN (from Plan 01 Wave 1 scaffold — filter string used by VALIDATION.md):
  - test_zero_hours_hourly_gates: hourly employee `hours_regular=Decimal("0")`, all others None → issues contains `issue_type="missing"`. GREEN after fix.
  - test_partial_week_not_gated: hourly employee `hours_regular=Decimal("0")` + `hours_holiday=Decimal("8")` → no missing issue. Must stay correct.
  - test_unknown_pay_type_zero_hours_gates: `pay_type=None` employee + all-zero hours → gates (D-03 fail-safe). GREEN after fix.
  - test_predicate_consistency (D-25): `_is_paid(Decimal("0")) == _is_paid(None) == False` — both treated as absent.
  - Existing tests test_missing_hours_for_hourly_employee, test_salaried_employee_with_no_hours_is_not_missing must stay GREEN (regression guard).
  </behavior>

  <action>
  Step 1 — Add `_is_paid` predicate (per D-09, PATTERNS.md §1):
  After `_HOURS_FIELDS` tuple (line 33) and before `_employee_pay_type`, insert the `_is_paid` helper function:
  - Signature: `def _is_paid(v: Decimal | None) -> bool:`
  - Docstring: "True iff value is present AND > 0 (D-09 shared predicate). Decimal('0') is treated the same as None — both count as absent/dropped."
  - Body: `return v is not None and v > 0`
  - Import: `Decimal` is already imported via `from app.models.contracts import Extracted` which transitively imports it, but verify the file imports `Decimal` explicitly. Add `from decimal import Decimal` if missing.

  Step 2 — Replace `any_hours` predicate (per D-02, RESEARCH.md §Target 1):
  At line ~84-88, change:
  `any_hours = any(getattr(emp, f) is not None for f in _HOURS_FIELDS)`
  to:
  `any_hours = any(_is_paid(getattr(emp, f)) for f in _HOURS_FIELDS)`

  D-03 edge constraints (verify these are enforced by construction after the change):
  - Salaried employees: already skipped earlier in the loop (`_employee_pay_type(emp, roster) == "salary"` branch). Confirm the guard logic is NOT changed — salaried path must remain untouched.
  - Partial week (e.g. `hours_regular=0` + `hours_holiday=8`): `_is_paid(Decimal("8")) = True` → `any_hours=True` → employee skipped (no issue). Correct by construction.
  - `pay_type=None/unknown`: the existing `pay_type` lookup returns None → the code should treat it as "could be hourly" and apply the gate. Verify the existing branch logic; if it currently skips `pay_type=None` employees, adjust so they fall through to the `any_hours` check. Do NOT add a dead guard for negatives (D-03 explicit prohibition).

  Step 3 — Run the target tests:
  `uv run pytest tests/test_validate.py -k "zero_hours or partial_week or unknown_pay_type or predicate_consistency or salaried_no_gate" -x -q`
  All should be GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "zero_hours or partial_week or unknown_pay_type or predicate_consistency or salaried_no_gate or salaried_employee_with_no_hours" -q</automated>
  </verify>
  <done>All targeted tests GREEN. `grep "def _is_paid" app/pipeline/validate.py` exits 0. `grep "_is_paid" app/pipeline/validate.py | grep -c "any_hours"` shows the replacement in place. Full existing validate test suite green: `uv run pytest tests/test_validate.py -q`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NFC _norm fix (reconcile_names.py) + eval _normalize parity (run_eval.py)</name>
  <files>app/pipeline/reconcile_names.py, eval/run_eval.py</files>

  <read_first>
  Read app/pipeline/reconcile_names.py lines 1-40 (imports + `_norm` function at lines 32-34). Read eval/run_eval.py lines 1-60 (imports + `_normalize` at lines 51-53). PATTERNS.md §2 has the target `_norm` form (double NFC around casefold). PATTERNS.md §8 has the `_normalize` fix (Option A: `from app.pipeline.reconcile_names import _norm as _normalize`). RESEARCH.md §Target 2 confirms the fix and lists all 6 `_normalize` call sites (lines 167, 168, 173, 196, 197, 234) — they need NO changes. CONTEXT.md D-05 locks the hardened NFC form `NFC(casefold(NFC(s)))`. C-4 locks that eval/_normalize must be fixed in the same wave.
  </read_first>

  <behavior>
  RED tests to turn GREEN:
  - test_nfd_name_resolves_same_as_nfc (test_reconcile.py): NFD submitted name matches NFC roster employee → `resolved=True`, correct `matched_employee_id`. GREEN after _norm fix.
  - test_eval_normalize_nfd_matches_nfc (test_eval_wiring.py): `_normalize(NFD("José Martínez")) == _normalize(NFC("José Martínez"))`. GREEN after _normalize import fix.
  Existing tests: full `tests/test_reconcile.py` suite must stay GREEN (regression guard).
  </behavior>

  <action>
  Step 1 — Fix `_norm` in reconcile_names.py (per D-05, PATTERNS.md §2):
  At the top of `app/pipeline/reconcile_names.py`, add `import unicodedata` to the imports block (currently only `from __future__ import annotations` and the roster import).

  Replace the body of `_norm` (lines 32-34):
  Old: `return " ".join(name.split()).casefold()`
  New:
  ```
  nfc = unicodedata.normalize("NFC", name)
  return " ".join(unicodedata.normalize("NFC", nfc.casefold()).split())
  ```
  Update the docstring to: `"""Whitespace-normalize + NFC(casefold(NFC(s))) for deterministic comparison (D-05)."""`

  The double-NFC-around-casefold is deliberate (D-05): `casefold()` can de-normalize its output; the canonical caseless form is NFC(casefold(NFC(s))). Do NOT use NFKC (D-06 locks NFC as conservative choice for names).

  DO NOT change any call site — they all benefit automatically (RESEARCH.md §Target 2 confirms `_norm` is the sole chokepoint).

  Step 2 — Fix `_normalize` in eval/run_eval.py (per C-4, PATTERNS.md §8):
  In eval/run_eval.py, replace the `_normalize` function definition (lines 51-53) with a direct import:
  `from app.pipeline.reconcile_names import _norm as _normalize`

  Place this import in the imports block at the top of the file (near the other `from app.pipeline.*` imports at lines 31-36), NOT as a nested function definition. Remove the old `def _normalize(...)` function body entirely. The 6 call sites at lines 167, 168, 173, 196, 197, 234 need no changes — they already call `_normalize(...)`.

  Step 3 — Run targeted tests:
  `uv run pytest tests/test_reconcile.py -k "nfd" -x -q`
  `uv run pytest tests/test_eval_wiring.py -k "nfd" -x -q`
  Both must be GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_reconcile.py -k "nfd" tests/test_eval_wiring.py -k "nfd" -q</automated>
  </verify>
  <done>Both NFD tests GREEN. `grep "unicodedata.normalize" app/pipeline/reconcile_names.py` shows 2 occurrences (one per NFC call). `grep "_norm as _normalize" eval/run_eval.py` exits 0. Full reconcile test suite green: `uv run pytest tests/test_reconcile.py -q`. Full eval wiring suite green: `uv run pytest tests/test_eval_wiring.py -q`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate.py → _is_paid | Pure predicate over Decimal | None; no external input crossing the boundary |
| reconcile_names._norm | Normalizes string from extraction LLM output (untrusted) before comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | `validate.py:_is_paid` — no state, no logging | accept | Pure function; no PII surface. Zero-hours case emits `ValidationIssue` with employee `submitted_name.field` — same pattern as all existing issues (roster.py:93). |
| T-07-04 | Tampering | `reconcile_names._norm` — Unicode normalization of untrusted name string | accept | stdlib `unicodedata.normalize` handles all Unicode edge cases; no code injection vector from name normalization. NFC is the conservative, non-collapsing choice (D-06). |
| T-07-SC | Tampering | No new package installs | accept | `unicodedata` is Python 3.12 stdlib; `uv add` not invoked |
</threat_model>

<verification>
Full plan verification after both tasks:

1. `uv run pytest tests/test_validate.py -k "zero_hours or partial_week or unknown_pay_type or salaried" -q` — all GREEN
2. `uv run pytest tests/test_reconcile.py -k "nfd" -q` — GREEN
3. `uv run pytest tests/test_eval_wiring.py -k "nfd" -q` — GREEN
4. `uv run pytest tests/test_validate.py tests/test_reconcile.py -q` — full suite GREEN (no regressions)
5. `grep "def _is_paid" app/pipeline/validate.py` — exits 0
6. `grep "_norm as _normalize" eval/run_eval.py` — exits 0
</verification>

<success_criteria>
- MONEY-01: `uv run pytest tests/test_validate.py -k "zero_hours" -x` exits 0 (GREEN)
- MONEY-01: `uv run pytest tests/test_validate.py -k "partial_week" -x` exits 0 (not gated, correct)
- MONEY-02: `uv run pytest tests/test_reconcile.py -k "nfd" -x` exits 0 (GREEN)
- C-4: `uv run pytest tests/test_eval_wiring.py -k "nfd" -x` exits 0 (GREEN)
- All existing validate + reconcile tests still pass (no regressions)
- `_is_paid` predicate present in validate.py; `any_hours` uses it
- `run_eval.py:_normalize` is a direct import alias of `_norm`, not a local function
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-02-SUMMARY.md` when done.
</output>
