---
phase: 07-money-correctness-deepening
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - app/pipeline/validate.py
  - app/pipeline/reconcile_names.py
  - eval/run_eval.py
autonomous: true
requirements:
  - MONEY-01
  - MONEY-02

must_haves:
  truths:
    - "An hourly employee with hours_regular=Decimal('0') and all other hours None gates to request_clarification — never ships a $0 paystub (MONEY-01, D-02/D-09)"
    - "An hourly employee with hours_regular=Decimal('0') but hours_holiday=Decimal('8') does NOT gate — partial week still processes (MONEY-01 D-03 edge)"
    - "NFD 'José Martínez' submitted resolves to the same matched_employee_id as NFC 'José Martínez' in the roster — MONEY-02 NFC fix closes the casefold-only _norm bug (D-04/D-05/D-07)"
    - "_is_paid shared predicate exists at module scope in validate.py with signature _is_paid(v: Decimal | None) -> bool returning v is not None and v > 0 (D-09; Phase 7.5 detect_field_regression will add the third call site)"
    - "eval/run_eval.py:_normalize is the direct import alias of _norm from reconcile_names so NFD eval fixtures score correctly after MONEY-02 (C-4)"
  artifacts:
    - path: "app/pipeline/validate.py"
      provides: "_is_paid predicate at module scope + any_hours fixed to use _is_paid"
      contains: "def _is_paid"
    - path: "app/pipeline/reconcile_names.py"
      provides: "NFC-hardened _norm: unicodedata.normalize applied in hardened double-NFC form"
      contains: "unicodedata.normalize"
    - path: "eval/run_eval.py"
      provides: "_normalize imported from reconcile_names._norm (replaces local def)"
      contains: "_norm as _normalize"
  key_links:
    - from: "app/pipeline/validate.py:_is_paid"
      to: "app/pipeline/validate.py:any_hours (~line 84)"
      via: "replaces is not None predicate; D-09 notes Phase 7.5 detect_field_regression will be the second call site"
      pattern: "_is_paid"
    - from: "app/pipeline/reconcile_names.py:_norm"
      to: "eval/run_eval.py:_normalize"
      via: "direct import alias; C-4 ensures eval scorer matches production normalizer"
      pattern: "from app.pipeline.reconcile_names import _norm as _normalize"
---

<objective>
Flip the MONEY-01 and MONEY-02 RED tests GREEN: the _is_paid shared predicate + any_hours fix (MONEY-01) and the NFC _norm hardening (MONEY-02). Both are self-contained one-function changes touching different files; they share no modified code and execute sequentially in one plan. Fix eval/run_eval.py:_normalize (C-4) in the same wave so the eval scorer cannot diverge from _norm after MONEY-02 ships.

Purpose: D-30 priority — ship _is_paid before Phase 7.5 lands detect_field_regression so both call sites agree on the zero boundary from day one. Close both ROADMAP success criteria: SC1 (zero-hours gate) and SC2 (NFC name match).

Output: Three file edits. All pre-existing RED tests from Plan 07-01 now GREEN. Full suite still green. Phase 7 is done.

SCOPE FENCE: This plan contains ZERO MONEY-03 work:
- No detect_field_regression function (Phase 7.5)
- No prior=None kwarg on validate() (Phase 7.5)
- No schema.sql column additions (Phase 7.5)
- No run_eval.py changes beyond the _normalize import alias
- No test_decide.py, no test_resume_pipeline.py, no xfail stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/pipeline/validate.py
@app/pipeline/reconcile_names.py
@eval/run_eval.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _is_paid predicate + any_hours MONEY-01 fix (validate.py)</name>
  <files>app/pipeline/validate.py</files>

  <read_first>
  Read app/pipeline/validate.py (full file — it is the sole target). Key anchors per RESEARCH.md §Target 1:
  - Lines 27-33: _HOURS_FIELDS tuple — already defined; do NOT redefine it. Phase 7.5 detect_field_regression will import or reuse this same constant.
  - Lines 84-88: any_hours = any(getattr(emp, f) is not None ...) — the D-01 bug to replace with _is_paid.
  - Line 113: ot_missing = ot is None or ot == 0 — the D-05 precedent for _is_paid semantics.
  - Lines 69-73: current validate() signature — do NOT add prior=None kwarg here; that belongs to Phase 7.5. This task touches ONLY _is_paid + any_hours.

  Verify that Decimal is importable in validate.py (check the imports at the top). If from decimal import Decimal is not present, add it.

  D-03 edge constraints to honor (verified by construction after the change):
  - Partial week (hours_regular=Decimal("0") + hours_holiday=Decimal("8")): _is_paid(Decimal("8")) = True → any_hours=True → employee skipped (correct).
  - Salaried employees: pay_type != "hourly" → issue never emitted regardless of any_hours (guard is already in validate() body).
  - Unresolved employees (matched_employee_id=None): _employee_pay_type returns None → pay_type != "hourly" → skipped (already safe per RESEARCH.md finding #6).
  - Negative hours: structurally unreachable (ExtractedEmployee ge=0 + extra="forbid" blocks negatives at extraction parse boundary per FIX 1 docstring). Do NOT add a dead negative guard.
  </read_first>

  <behavior>
  RED tests to turn GREEN (written in Plan 07-01):
  - test_zero_hours_hourly_gates: hourly employee hours_regular=Decimal("0"), all others None → issues contains issue_type="missing". GREEN after fix.
  - test_predicate_consistency (D-25): same hourly employee, two separate calls — once with hours_overtime=Decimal("0") all others None, once with hours_overtime=None all others None. Both produce a missing issue (identical treatment). GREEN once _is_paid exists.
  - test_partial_week_not_gated: hours_regular=Decimal("0") + hours_holiday=Decimal("8") → no missing issue. Must stay GREEN (D-03 regression guard — correct by construction with _is_paid).
  - test_salaried_not_gated_regression_guard: salaried employee with hours_regular=Decimal("0") → no missing issue. Must stay GREEN.
  - All existing tests in test_validate.py (test_missing_hours_for_hourly_employee, test_salaried_employee_with_no_hours_is_not_missing, etc.) must remain GREEN.
  </behavior>

  <action>
  Step 1 — Add _is_paid predicate at module scope (per D-09):
  After _HOURS_FIELDS tuple (line ~33) and before _employee_pay_type, insert the _is_paid helper. Signature: def _is_paid(v: Decimal | None) -> bool. Docstring: "True iff value is present AND strictly positive (D-09 shared predicate). Decimal('0') is treated the same as None — both count as absent for the zero-hours gate. Phase 7.5 detect_field_regression will use this same predicate as its second call site." Body: return v is not None and v > 0. Ensure Decimal is imported (add from decimal import Decimal if not present).

  Step 2 — Replace any_hours predicate (per D-02):
  At line ~84, change the generator expression inside any() from:
  getattr(emp, f) is not None for f in _HOURS_FIELDS
  to:
  _is_paid(getattr(emp, f)) for f in _HOURS_FIELDS

  Do NOT change any other logic in validate(). Do NOT touch the OT guard (lines 102-140) — it already has the correct ot_missing = ot is None or ot == 0 form and is independent of _is_paid (D-05 is a separate guard, not a call site for _is_paid).

  Step 3 — Run targeted tests:
  uv run pytest tests/test_validate.py -k "zero_hours or partial_week or predicate_consistency or salaried_not_gated or salaried_employee_with_no_hours" -x -q
  All should be GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "zero_hours or predicate_consistency or partial_week or salaried" -q</automated>
  </verify>
  <done>All targeted tests GREEN. grep "def _is_paid" app/pipeline/validate.py exits 0. grep -c "_is_paid" app/pipeline/validate.py shows 2+ occurrences (definition + any_hours usage). Full existing validate test suite green: uv run pytest tests/test_validate.py -q exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NFC _norm fix (reconcile_names.py) + eval _normalize parity (run_eval.py)</name>
  <files>app/pipeline/reconcile_names.py, eval/run_eval.py</files>

  <read_first>
  Read app/pipeline/reconcile_names.py lines 1-40 (imports + _norm at lines 32-34). Read eval/run_eval.py lines 1-60 (imports block + _normalize function at lines 51-53 and the six call sites at lines 167, 168, 173, 196, 197, 234, 238). RESEARCH.md §Target 2 confirms _norm is the sole normalizer chokepoint and lists all _normalize call sites in run_eval.py — they need NO changes after the import alias replaces the local def.

  Hardened NFC form (D-05, locked): the canonical caseless form is NFC(casefold(NFC(s))). The double NFC is deliberate insurance: casefold() can de-normalize its own output on some Unicode sequences. Cheap to include; the alternative is a subtle latent bug on names casefold de-normalizes. Use NFC not NFKC (D-06 locks the conservative choice: NFKC folds compatibility characters too aggressively for real names).

  SCOPE: run_eval.py modification in this task is ONLY the _normalize import alias (remove the local def, add the import). No other run_eval.py changes belong here — prior_extracted fixture support and eval fixture additions belong to Phase 7.5.
  </read_first>

  <behavior>
  RED tests to turn GREEN (written in Plan 07-01):
  - test_nfd_name_resolves_same_as_nfc (test_reconcile.py): NFD submitted name resolves to the same matched_employee_id as the NFC roster entry. GREEN after _norm fix.
  - test_eval_normalize_nfd_matches_nfc (test_eval_wiring.py): _normalize(NFD form) == _normalize(NFC form). GREEN after _normalize import fix.
  All existing tests in test_reconcile.py must remain GREEN (regression guard — _norm change is backward-compatible for any already-NFC or ASCII names).
  </behavior>

  <action>
  Step 1 — Fix _norm in reconcile_names.py (per D-05):
  Add import unicodedata to the imports block at the top of the file (place it near the standard library imports, before the app imports).

  Replace the body of _norm (currently one line: return " ".join(name.split()).casefold()). New implementation (prose, not code block): first normalize the input to NFC, assigning the result to a local name; then casefold that result; then normalize the casefolded form to NFC again; then whitespace-split and rejoin. The full canonical caseless form is NFC(casefold(NFC(s))). Update the docstring to: "Whitespace-normalize + NFC(casefold(NFC(s))) for deterministic Unicode-safe comparison (D-05). The double NFC is deliberate: casefold can de-normalize its output on some Unicode sequences. NFC chosen over NFKC (D-06: conservative — NFKC over-folds compatibility chars for names)."

  Do NOT change any call site in reconcile_names.py. The fix is transparent to all callers.

  Step 2 — Fix _normalize in eval/run_eval.py (per C-4):
  In the imports block near the other app.pipeline imports (currently: from app.pipeline.reconcile_names import reconcile_names), add the import alias: from app.pipeline.reconcile_names import _norm as _normalize. Then remove the local _normalize function definition (the def _normalize(name: str) -> str block at lines 51-53). The six call sites in the file (lines ~167, 168, 173, 196, 197, 234, 238) reference _normalize by name and need no changes — the name is now satisfied by the import alias.

  Step 3 — Run targeted tests:
  uv run pytest tests/test_reconcile.py -k "nfd" -x -q
  uv run pytest tests/test_eval_wiring.py -k "nfd" -x -q
  Both must be GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_reconcile.py -k "nfd" tests/test_eval_wiring.py -k "nfd" -q</automated>
  </verify>
  <done>Both NFD tests GREEN. grep "unicodedata.normalize" app/pipeline/reconcile_names.py shows 2 occurrences (one per NFC call in _norm). grep "_norm as _normalize" eval/run_eval.py exits 0. Full reconcile test suite green: uv run pytest tests/test_reconcile.py -q exits 0. Full eval wiring suite green: uv run pytest tests/test_eval_wiring.py -q exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate.py → _is_paid | Pure predicate over Decimal or None; typed input from ExtractedEmployee (ge=0 + extra=forbid already blocks negatives upstream) |
| reconcile_names._norm | Normalizes string originating from LLM extraction output (untrusted) before name comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Information Disclosure | validate.py:_is_paid — zero-hours ValidationIssue carries submitted_name.field | accept | Same pattern as all existing ValidationIssue.field values (see roster.py:93 pattern); no new PII surface. Error logging boundary at orchestrator.py captures only type(exc).__name__ |
| T-07-04 | Tampering | reconcile_names._norm — unicodedata.normalize on untrusted name string from LLM extraction | accept | stdlib unicodedata.normalize handles all Unicode edge cases without code injection risk; name normalization cannot execute arbitrary code. NFC is non-lossy and does not collapse semantically-distinct characters |
| T-07-05 | Tampering | eval/run_eval.py:_normalize now imports _norm directly — if _norm behavior changes, eval scoring changes too | accept | This is the DESIRED invariant (C-4): eval scorer must agree with production normalizer. Any future _norm change intentionally propagates to eval scoring. No trust boundary crossed. |
| T-07-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | unicodedata is Python 3.12 stdlib; no uv add invoked |
</threat_model>

<verification>
Full plan verification after both tasks:

1. uv run pytest tests/test_validate.py -k "zero_hours" -x -q — GREEN (MONEY-01 SC1)
2. uv run pytest tests/test_validate.py -k "partial_week" -x -q — GREEN (D-03 edge stays correct)
3. uv run pytest tests/test_validate.py -k "predicate_consistency" -x -q — GREEN (D-25)
4. uv run pytest tests/test_reconcile.py -k "nfd" -x -q — GREEN (MONEY-02 SC2)
5. uv run pytest tests/test_eval_wiring.py -k "nfd" -x -q — GREEN (C-4 parity)
6. uv run pytest tests/test_validate.py tests/test_reconcile.py -q — full suites GREEN (no regressions)
7. grep "def _is_paid" app/pipeline/validate.py — exits 0
8. grep "unicodedata.normalize" app/pipeline/reconcile_names.py — shows 2 occurrences
9. grep "_norm as _normalize" eval/run_eval.py — exits 0
10. grep "def _normalize" eval/run_eval.py — exits 1 (local def removed)
</verification>

<success_criteria>
- MONEY-01 SC1: uv run pytest tests/test_validate.py -k "zero_hours" -x exits 0 (GREEN)
- MONEY-01 D-03: uv run pytest tests/test_validate.py -k "partial_week" -x exits 0 (not gated)
- MONEY-01 D-25: uv run pytest tests/test_validate.py -k "predicate_consistency" -x exits 0 (GREEN)
- MONEY-02 SC2: uv run pytest tests/test_reconcile.py -k "nfd" -x exits 0 (GREEN)
- C-4: uv run pytest tests/test_eval_wiring.py -k "nfd" -x exits 0 (GREEN)
- _is_paid predicate present at module scope in validate.py; any_hours uses it (not is not None)
- _norm in reconcile_names.py calls unicodedata.normalize twice
- eval/run_eval.py:_normalize is the direct import alias of _norm (no local def)
- All existing validate + reconcile + eval_wiring tests still pass: uv run pytest tests/test_validate.py tests/test_reconcile.py tests/test_eval_wiring.py -q exits 0
</success_criteria>

<output>
Create .planning/phases/07-money-correctness-deepening/07-02-SUMMARY.md when done.
</output>
