---
phase: 07-money-correctness-deepening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/contracts.py
  - app/models/roster.py
  - tests/test_validate.py
  - tests/test_reconcile.py
  - tests/test_decide.py
  - tests/test_eval_wiring.py
  - tests/test_resume_pipeline.py
autonomous: true
requirements:
  - MONEY-01
  - MONEY-02
# Note: this plan also lands the `field_regression` ValidationIssue Literal value and the
# FieldDrop/RawFieldDrop models as FORWARD-COMPATIBLE SCAFFOLDING for Phase 7.5 (MONEY-03,
# re-scoped out 2026-06-27). They are harmless no-ops in Phase 7 (nothing emits field_regression
# here) and let Phase 7.5 build on a stable contract. MONEY-03 itself is NOT delivered in Phase 7.

must_haves:
  truths:
    - "ValidationIssue.issue_type Literal includes 'field_regression' so D-17 ValidationIssue(issue_type='field_regression') does not raise a ValidationError at runtime (per D-17, C-1)"
    - "FieldDrop Pydantic model exists in contracts.py with employee_id, field, original_value, resumed_value fields (per D-10, D-11, D-13) — employee_id is always a real resolved UUID; the sentinel trick is REMOVED per decision C (supersedes prior revision)"
    - "All MONEY-01 failing tests exist in test_validate.py and run RED before the Wave 2 fix — the pay_type=None test uses a constructible approach (does NOT attempt Employee(pay_type=None) which violates the Literal — per finding #6)"
    - "NFD name test exists in test_reconcile.py and runs RED before the Wave 2 fix"
    - "field_regression gating test exists in tests/test_decide.py and runs RED before the Wave 3 fix"
    - "Integration test stubs exist in tests/test_resume_pipeline.py marked xfail"
    - "detect_regression test asserts drop.field == 'hours_overtime' (bare field name, not qualified string) — decision C: RawFieldDrop carries submitted_name internally; FieldDrop.field is always a bare field name"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "FieldDrop model (employee_id=real UUID, field=bare name) + widened ValidationIssue Literal"
      contains: "field_regression"
    - path: "tests/test_validate.py"
      provides: "MONEY-01 failing tests (zero_hours, partial_week, pay_type_unknown_gate, predicate_consistency, explicit_drop, no_regression, detect_regression)"
    - path: "tests/test_decide.py"
      provides: "field_regression gate test (C-1 coverage)"
    - path: "tests/test_resume_pipeline.py"
      provides: "integration stubs: snapshot_once, loop_guard, confirmed_dropped_no_reflag"
  key_links:
    - from: "app/models/contracts.py:ValidationIssue.issue_type"
      to: "app/pipeline/decide.py:Rule 2b (Wave 3)"
      via: "Literal must include 'field_regression' before decide rule can reference it"
      pattern: "field_regression"
---

<objective>
Widen `ValidationIssue.issue_type` Literal to include `"field_regression"` and add the new `FieldDrop` Pydantic model to `contracts.py` — the two pre-conditions every subsequent MONEY-03 task depends on. Simultaneously write ALL failing Wave 0 tests so the RED baseline is established before any implementation runs in Wave 2+.

Purpose: TDD discipline (D-30) requires every fix to be preceded by a failing test. Wave 1 establishes the contracts foundation and plants every RED stake so Waves 2-5 can flip them GREEN one by one.

Output: Widened Literal in `app/models/roster.py`, `FieldDrop` in `app/models/contracts.py`, and five test files with failing/xfail tests covering all 14 behaviors in VALIDATION.md.

REVISION NOTES (from Codex review — confirmed against live source):
- Finding #6: `Employee.pay_type` is `Literal["hourly","salary"]` (non-nullable). `Employee(pay_type=None)` fails Pydantic validation. The D-03 "unknown pay_type ⇒ fail-safe gate" test must NOT construct a bare Employee with pay_type=None. Instead test the code path via the validate() function with a NameMatchResult whose matched employee is missing from the roster — or confirm the unknown-pay_type branch is structurally unreachable given typed Employee, and document why the guard still exists (as a defensive Literal width check). See Task 2 action for the constructible approach.
- Decision C (supersedes prior revision's sentinel trick): `FieldDrop.employee_id` is ALWAYS a real resolved UUID (never uuid.UUID(int=0) sentinel); `FieldDrop.field` is ALWAYS a bare field name (never "{submitted_name}.{field}" qualified string). The qualified encoding trick is REMOVED entirely from plans. `detect_field_regression` returns a new internal type `RawFieldDrop(submitted_name, field, original_value, resumed_value)` — NOT `FieldDrop` — and `validate()` maps submitted_name→employee_id internally. The RED tests for detect_regression must assert `drop.field == "hours_overtime"` (bare), not a qualified string.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-VALIDATION.md

# Source files to read before editing (no re-reads)
@app/models/roster.py
@app/models/contracts.py
@tests/test_validate.py
@tests/test_reconcile.py
@tests/test_eval_wiring.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen ValidationIssue Literal + add FieldDrop + add RawFieldDrop</name>
  <files>app/models/roster.py, app/models/contracts.py</files>

  <read_first>
  Read app/models/roster.py lines 200-220 for the ValidationIssue class (PATTERNS.md §3 shows it lives in roster.py at lines 205-216). Read app/models/contracts.py lines 1-30 (imports) and the ExtractedEmployee class (lines 60-80) for the model style. PATTERNS.md §3 has the exact Literal change and FieldDrop shape. CONTEXT.md D-17 locks the new Literal value; D-10/D-11/D-13 lock the FieldDrop fields.

  Decision C (locked by orchestrator + user): FieldDrop.employee_id is ALWAYS a real resolved UUID. FieldDrop.field is ALWAYS a bare field name. The sentinel UUID trick (uuid.UUID(int=0)) from the prior plan revision is REMOVED. A NEW internal type RawFieldDrop is added to contracts.py alongside FieldDrop — RawFieldDrop carries submitted_name (a string) instead of employee_id, and is the type returned by detect_field_regression. validate() maps RawFieldDrop.submitted_name → real employee_id via matches and constructs FieldDrop. RawFieldDrop never escapes validate().
  </read_first>

  <behavior>
  - Test 1 (contracts): Constructing `ValidationIssue(issue_type="field_regression", field="hours_overtime", message="regression")` must NOT raise a ValidationError (currently raises because "field_regression" is not in the Literal).
  - Test 2 (contracts): Constructing `FieldDrop(employee_id=some_uuid, field="hours_overtime", original_value=Decimal("2"), resumed_value=None)` succeeds and `drop.resumed_value is None` → carry_forward semantics (D-14). NOTE: employee_id is a real UUID, NOT uuid.UUID(int=0).
  - Test 3 (contracts): `FieldDrop(..., resumed_value=Decimal("0"))` succeeds and `drop.resumed_value == Decimal("0")` → confirmed_dropped semantics (D-14).
  - Test 4 (contracts): `FieldDrop(employee_id=..., field="x", original_value=Decimal("2"), resumed_value=None, extra_field="bad")` raises ValidationError (extra="forbid").
  - Test 5 (contracts): `RawFieldDrop(submitted_name="Maria Chen", field="hours_overtime", original_value=Decimal("2"), resumed_value=None)` constructs successfully (the internal return type of detect_field_regression before mapping).
  </behavior>

  <action>
  Step 1 — Widen ValidationIssue Literal (per C-1 orchestrator resolution + D-17):
  In `app/models/roster.py`, find the `ValidationIssue` class (lines 205-216). Change the `issue_type` field from `Literal["missing", "out_of_bounds", "non_numeric"]` to `Literal["missing", "out_of_bounds", "non_numeric", "field_regression"]`. Do NOT change any other field. Update the comment to note D-17 adds "field_regression".

  Step 2 — Add FieldDrop model to contracts.py (per D-10/D-11/D-13, decision C):
  In `app/models/contracts.py`, after the `Extracted` class and before the `Decision` class, add TWO models:

  Model A — `RawFieldDrop` (INTERNAL to detect_field_regression, never escapes validate()):
  - `model_config = ConfigDict(extra="forbid")`
  - `submitted_name: str` (the submitted name from extraction — used by validate() to resolve employee_id)
  - `field: str` (bare field name e.g. "hours_overtime", NOT qualified)
  - `original_value: Decimal`
  - `resumed_value: Decimal | None`
  - Docstring: "Internal return type of detect_field_regression. validate() maps submitted_name → employee_id via matches to produce FieldDrop. Never passed outside validate()."

  Model B — `FieldDrop` (the public type stored in clarified_fields, passed to orchestrator):
  - `model_config = ConfigDict(extra="forbid")`
  - `employee_id: UUID` (resolved by validate() from matches — ALWAYS a real UUID, never a sentinel per decision C)
  - `field: str` (bare field name e.g. "hours_overtime" — NEVER qualified with submitted_name)
  - `original_value: Decimal`
  - `resumed_value: Decimal | None`
  - Docstring: "Public field-regression record. employee_id is always a real resolved UUID (decision C). field is always a bare hours-field name. resumed_value=None means carried_forward; Decimal('0') means confirmed_dropped (D-14)."

  Ensure `UUID` and `Decimal` are imported at the top of contracts.py (confirm; add if missing).

  Run: `uv run python -c "from app.models.contracts import FieldDrop, RawFieldDrop; print('OK')"`
  </action>

  <verify>
    <automated>uv run python -c "from app.models.roster import ValidationIssue; from app.models.contracts import FieldDrop, RawFieldDrop; from decimal import Decimal; import uuid; vi = ValidationIssue(issue_type='field_regression', field='hours_overtime', message='test'); fd = FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); rfd = RawFieldDrop(submitted_name='Maria Chen', field='hours_overtime', original_value=Decimal('2'), resumed_value=None); print('ALL OK')"</automated>
  </verify>
  <done>Python import + construction of all three types succeeds without error. `uv run pytest tests/test_models_contracts.py -q` exits 0 (existing tests unbroken).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write all Wave 0 failing tests (RED baseline)</name>
  <files>tests/test_validate.py, tests/test_reconcile.py, tests/test_decide.py, tests/test_eval_wiring.py, tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/test_validate.py (full file — capture existing helpers `_extracted`, `_match`, `_one_employee_roster`, `_make_weekly_hourly_employee`; add new tests at the bottom without rewriting existing ones). Read tests/test_reconcile.py lines 1-50 for file structure and fixtures. Read tests/test_eval_wiring.py (full file) for the existing wiring test structure. VALIDATION.md has the full per-requirement test map. PATTERNS.md §8 has the target `_norm` for the NFD test. CONTEXT.md D-25/D-26/D-27 lock the predicate-consistency, explicit-drop, and determinism tests.

  CRITICAL per finding #6: Employee.pay_type is Literal["hourly","salary"] — it is NOT nullable. ExtractedEmployee has NO pay_type field. The D-03 "unknown pay_type ⇒ fail-safe" test cannot construct Employee(pay_type=None). Instead: read how validate() determines pay_type — it looks up the employee in the roster via NameMatchResult.matched_employee_id. The unknown-pay_type scenario means matched_employee_id is None (unresolved). Read the actual validate() code to see what happens when a match is unresolved before writing the test. If validate() already handles it (unresolved = no gate), document that and write a test proving the EXISTING behavior. Do NOT fabricate a test scenario that fails at construction time.

  CRITICAL per decision C: detect_field_regression returns list[RawFieldDrop] (not list[FieldDrop]). The RED test for detect_regression should import RawFieldDrop and assert on bare field names (drop.field == "hours_overtime"), NOT qualified strings.
  </read_first>

  <behavior>
  All tests in this task MUST be RED before Wave 2/3 implementation (except stubs which are xfail).

  MONEY-01 tests in test_validate.py (append to existing file):
  - test_zero_hours_hourly_gates: hourly employee `hours_regular=Decimal("0")`, all other hours None → issues contains `issue_type="missing"`. RED because current `is not None` lets Decimal("0") pass.
  - test_partial_week_not_gated: hourly employee `hours_regular=Decimal("0")`, `hours_holiday=Decimal("8")` → no missing issue. Assert this explicitly — may currently pass or fail.
  - test_pay_type_unknown_gate: Read app/pipeline/validate.py to understand the actual code path when a match is unresolved (matched_employee_id=None). If the code currently gates unresolved employees or skips them, write a test that asserts the D-03 locked behavior: "unresolved employee with all-zero hours is either gated or handled safely — never silently processed at $0". This test must be achievable with constructible inputs (NameMatchResult with resolved=False). Confirm validate()'s behavior in the D-03 edge case from live code, not assumption.
  - test_predicate_consistency: call validate() twice with the same employee — once with `hours_overtime=Decimal("0")`, once with `hours_overtime=None`. Assert both produce a missing issue for the OT field (identical treatment per D-25 shared predicate). RED because _is_paid does not exist yet.

  MONEY-03 detection tests in test_validate.py (append):
  - test_detect_regression_ot_absent: call `detect_field_regression(original_extracted, resumed_extracted)` where original has `hours_overtime=Decimal("2")` and resumed has `hours_overtime=None`. Import `detect_field_regression` from `app.pipeline.validate` (does not exist yet → ImportError = RED). When it exists, assert returns list with one RawFieldDrop where `drop.field == "hours_overtime"` (BARE NAME — not qualified) and `drop.original_value == Decimal("2")` and `drop.resumed_value is None`.
  - test_explicit_drop_zero_resumed_value (D-26): `detect_field_regression(...)` where resumed has `hours_overtime=Decimal("0")` → returns RawFieldDrop with `resumed_value=Decimal("0")`. RED via ImportError initially.
  - test_no_regression_noop (D-27): original and resumed are identical → `detect_field_regression(...)` returns `[]`. RED via ImportError initially.

  MONEY-02 test in test_reconcile.py (append):
  - test_nfd_name_resolves_same_as_nfc: build a roster with an employee whose `full_name` is NFC "José Martínez"; call `reconcile_names([NFD "José Martínez"], roster)` and assert `matches[0].resolved is True` and same `matched_employee_id`. RED because current _norm has no NFC.

  field_regression decide test in tests/test_decide.py (new file):
  - test_field_regression_issue_gates_to_clarification: build a `ValidationIssue(issue_type="field_regression", field="hours_overtime", message="regression")` and call `decide(extracted, matches, [regression_issue])`. Assert `decision.final_action == "request_clarification"` and `any("field_regression" in r for r in decision.gate_reasons)`. RED because decide.py Rule 2b does not exist.

  eval wiring NFD test in tests/test_eval_wiring.py (append):
  - test_eval_normalize_nfd_matches_nfc: import `_normalize` from `eval.run_eval`; assert `_normalize(NFD "José Martínez") == _normalize(NFC "José Martínez")`. RED because current `_normalize` has no NFC.

  Integration stubs in tests/test_resume_pipeline.py (new file):
  - Create the file with three tests marked `@pytest.mark.xfail(reason="integration: needs live DB + Wave 4 wiring", strict=False)`:
    - `test_snapshot_once_not_overwritten_on_second_clarify` (D-28)
    - `test_loop_guard_fires_exactly_once_then_carry_forward` — two-inbound architecture per decision A (D-20 superseded)
    - `test_confirmed_dropped_does_not_reflag_money01` (D-15)
  These stubs will be replaced with real DB logic in Plan 05.
  </behavior>

  <action>
  Before writing any test that asserts a code path for `pay_type=None` employees, READ `app/pipeline/validate.py` lines 69-100 (the full validate() function body, particularly the pay_type lookup logic). Determine the actual code path for an unresolved employee. Then write `test_pay_type_unknown_gate` to test the real behavior, not a fabricated one. If Employee.pay_type is always a Literal (never None from the roster) and validate() only accesses pay_type via the roster (never from ExtractedEmployee), then the "unknown pay_type" scenario may be structurally unreachable via validate() — if so, write the test to document this: `def test_pay_type_unknown_gate(): pytest.skip("structurally unreachable: Employee.pay_type is Literal['hourly','salary']; validate() never sees pay_type=None from a typed roster employee. D-03 fail-safe is maintained by the Literal itself.")`.

  For all detect_regression tests: import `from app.pipeline.validate import detect_field_regression` at the top of the new test group (inside a try/except ImportError block to allow collection — the ImportError itself is the RED signal until Plan 03). Assert on RawFieldDrop (not FieldDrop), bare field names.

  For test_decide.py: minimal imports (`from app.pipeline.decide import decide`, `from app.models.roster import ValidationIssue`, `from app.models.contracts import Extracted, ExtractedEmployee`). Build a minimal `Extracted` with one employee, build a minimal NameMatchResult, call decide(). Build ValidationIssue from the now-widened Literal (Task 1 must be done first).

  For tests/test_resume_pipeline.py: imports `pytest` only; xfail stubs call `pytest.skip("not yet implemented")`.

  Append to tests/test_eval_wiring.py: import `unicodedata`; add the new test at the bottom.

  After writing all tests, run to confirm RED state:
  `uv run pytest tests/test_validate.py tests/test_reconcile.py tests/test_decide.py tests/test_eval_wiring.py tests/test_resume_pipeline.py -q 2>&1 | tail -20`

  Expected: several FAILED + xfail stubs. ImportError for `detect_field_regression` counts as expected RED — collection errors are also RED signals here. The three xfail stubs must collect without errors (not fail with collection errors).
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "zero_hours or detect_regression or explicit_drop or no_regression" --no-header -q 2>&1 | grep -E "FAILED|ERROR|passed|failed" | head -10</automated>
  </verify>
  <done>At least 3 tests fail RED in test_validate.py (zero_hours, detect_regression/ImportError, explicit_drop/ImportError). test_nfd_name_resolves_same_as_nfc fails RED in test_reconcile.py. test_field_regression_issue_gates_to_clarification fails RED in test_decide.py. test_eval_normalize_nfd_matches_nfc fails RED in test_eval_wiring.py. The three xfail stubs in test_resume_pipeline.py collect without error. Full suite still collects: `uv run pytest --collect-only -q` exits 0 with no collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts.py model construction | FieldDrop + RawFieldDrop accepted as pipeline I/O; Pydantic validates at construction time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `FieldDrop.resumed_value` (Decimal or None) | mitigate | `model_config = ConfigDict(extra="forbid")` on both FieldDrop and RawFieldDrop; Pydantic rejects unexpected fields at construction |
| T-07-02 | Information Disclosure | `ValidationIssue.field` in field_regression issues | accept | Submitted name already in all existing ValidationIssue.field values (roster.py:93 pattern); no new PII surface. Logging boundary at orchestrator.py:239 only captures `type(exc).__name__` |
| T-07-SC | Tampering | No new npm/pip installs in this plan | accept | No new package-manager installs; stdlib `uuid`/`Decimal` only |
</threat_model>

<verification>
Full plan verification (run after both tasks):

1. `uv run python -c "from app.models.roster import ValidationIssue; vi = ValidationIssue(issue_type='field_regression', field='f', message='m'); print('OK')"` — exits 0
2. `uv run python -c "from app.models.contracts import FieldDrop, RawFieldDrop; from decimal import Decimal; import uuid; FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); RawFieldDrop(submitted_name='Bob', field='hours_overtime', original_value=Decimal('2'), resumed_value=None); print('OK')"` — exits 0
3. `uv run pytest tests/test_validate.py -k "zero_hours" -x -q` — FAILS RED (expected)
4. `uv run pytest tests/test_decide.py -k "field_regression" -x -q` — FAILS RED (expected)
5. `uv run pytest tests/test_resume_pipeline.py -q` — all xfail (no errors)
6. `uv run pytest tests/test_models_contracts.py -q` — all pass (regression guard)
</verification>

<success_criteria>
- `ValidationIssue.issue_type` Literal in roster.py contains `"field_regression"` (grep: `grep "field_regression" app/models/roster.py` exits 0)
- `FieldDrop` class exists in contracts.py with `extra="forbid"`, `employee_id: UUID` (real UUID — no sentinel per decision C), `field: str` (bare name), `original_value: Decimal`, `resumed_value: Decimal | None`
- `RawFieldDrop` class exists in contracts.py with `extra="forbid"`, `submitted_name: str`, `field: str`, `original_value: Decimal`, `resumed_value: Decimal | None`
- 3+ tests fail RED in test_validate.py (zero_hours, detect_regression ImportError, explicit_drop ImportError)
- 1 test fails RED in test_decide.py (field_regression gate)
- 1 test fails RED in test_eval_wiring.py (NFD normalize parity)
- 1 test fails RED in test_reconcile.py (NFD name match)
- 3 tests xfail (not error) in test_resume_pipeline.py
- Full test collection succeeds: `uv run pytest --collect-only -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md` when done.
</output>
