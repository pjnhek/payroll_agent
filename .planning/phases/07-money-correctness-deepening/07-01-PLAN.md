---
phase: 07-money-correctness-deepening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/roster.py
  - app/models/contracts.py
  - tests/test_validate.py
  - tests/test_reconcile.py
  - tests/test_eval_wiring.py
autonomous: true
requirements:
  - MONEY-01
  - MONEY-02
# Forward-compat scaffolding note (D-17, CONTEXT.md scope override 2026-06-27):
# This plan widens ValidationIssue.issue_type to include "field_regression" and
# adds FieldDrop to contracts.py as forward-compatible scaffolding for Phase 7.5.
# Both are harmless no-ops in Phase 7 (nothing emits field_regression here).
# Phase 7.5 builds on these stable contracts. MONEY-03 itself is NOT delivered here.
# RawFieldDrop is NOT added here — it is an internal Phase 7.5 implementation detail
# that belongs with detect_field_regression in Phase 7.5.

must_haves:
  truths:
    - "ValidationIssue.issue_type Literal includes 'field_regression' — constructing ValidationIssue(issue_type='field_regression', ...) does not raise (per D-17; forward-compat scaffolding)"
    - "FieldDrop Pydantic model exists in contracts.py with extra='forbid', employee_id: UUID, field: str, original_value: Decimal, resumed_value: Decimal | None (per D-13/D-14; forward-compat scaffolding)"
    - "MONEY-01 RED tests exist in test_validate.py: zero_hours_hourly_gates (fails because is not None lets Decimal(0) pass), partial_week_not_gated (explicit D-03 edge), predicate_consistency (D-25: OT=0 and OT=None treated identically), salaried_not_gated regression guard"
    - "MONEY-02 RED test exists in test_reconcile.py: nfd_name_resolves_same_as_nfc (fails because _norm has no NFC)"
    - "eval _normalize parity RED test exists in test_eval_wiring.py: eval_normalize_nfd_matches_nfc (fails because _normalize has no NFC)"
    - "No MONEY-03 tests anywhere — no detect_field_regression, no test_resume_pipeline.py, no test_decide.py field_regression gate test, no xfail integration stubs"
  artifacts:
    - path: "app/models/roster.py"
      provides: "Widened ValidationIssue.issue_type Literal"
      contains: "field_regression"
    - path: "app/models/contracts.py"
      provides: "FieldDrop forward-compat model"
      contains: "class FieldDrop"
    - path: "tests/test_validate.py"
      provides: "MONEY-01 RED tests (zero_hours_hourly_gates, partial_week_not_gated, predicate_consistency, salaried_not_gated)"
    - path: "tests/test_reconcile.py"
      provides: "MONEY-02 RED test (nfd_name_resolves_same_as_nfc)"
    - path: "tests/test_eval_wiring.py"
      provides: "eval _normalize parity RED test (eval_normalize_nfd_matches_nfc)"
  key_links:
    - from: "app/models/roster.py:ValidationIssue.issue_type"
      to: "app/pipeline/decide.py"
      via: "Literal must include field_regression before Phase 7.5 decide rule can reference it"
      pattern: "field_regression"
    - from: "tests/test_validate.py:test_zero_hours_hourly_gates"
      to: "app/pipeline/validate.py:any_hours"
      via: "RED test proves the is-not-None bug; Plan 07-02 turns it GREEN"
      pattern: "_is_paid"
---

<objective>
Establish the forward-compatible contract foundation (ValidationIssue Literal widening + FieldDrop model) that Phase 7.5 will build on, and write ALL failing RED tests for MONEY-01 and MONEY-02 so the TDD baseline is established before any implementation runs in Wave 2.

Purpose: D-30 requires every fix to be preceded by a failing test. This wave plants the RED stakes so Plan 07-02 can flip them GREEN without ambiguity. The forward-compat scaffolding (field_regression Literal + FieldDrop) lands here as harmless no-ops that make Phase 7.5 independent of Phase 7 mid-plan changes.

Output: Widened ValidationIssue Literal in app/models/roster.py, FieldDrop model in app/models/contracts.py, and three test files with behaviorally-failing tests covering MONEY-01 (D-01/D-02/D-03/D-09/D-25) and MONEY-02 (D-04/D-05/D-07) plus the eval _normalize parity gap (C-4).

SCOPE FENCE: This plan contains ZERO MONEY-03 work. Specifically excluded:
- No detect_field_regression import, call, or test
- No RawFieldDrop model (Phase 7.5 implementation detail)
- No test_resume_pipeline.py (Phase 7.5)
- No test_decide.py field_regression gate test (Phase 7.5)
- No xfail integration stubs for snapshot/loop-guard/confirmed_dropped
- No schema.sql column additions (Phase 7.5)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md

# Source files to read before editing (one pass each, no re-reads)
@app/models/roster.py
@app/models/contracts.py
@tests/test_validate.py
@tests/test_reconcile.py
@tests/test_eval_wiring.py
@app/pipeline/validate.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen ValidationIssue Literal + add FieldDrop forward-compat model</name>
  <files>app/models/roster.py, app/models/contracts.py</files>

  <read_first>
  Read app/models/roster.py lines 200-220 for the ValidationIssue class. Per RESEARCH.md §Target 7, ValidationIssue.issue_type is currently Literal["missing", "out_of_bounds", "non_numeric"] at line 215. This is in roster.py (not contracts.py). Read app/models/contracts.py lines 60-120 for model style, ConfigDict usage, and where to insert FieldDrop (after Extracted, before Decision). Confirm UUID and Decimal are imported at the top of contracts.py — both are needed for FieldDrop. CONTEXT.md D-13/D-14 define the FieldDrop semantics: resumed_value=None means carried_forward; Decimal("0") means confirmed_dropped.
  </read_first>

  <behavior>
  - Test A: Constructing ValidationIssue(issue_type="field_regression", field="hours_overtime", message="regression") must NOT raise a ValidationError. (Currently raises because "field_regression" is absent from the Literal.)
  - Test B: Constructing FieldDrop(employee_id=uuid.uuid4(), field="hours_overtime", original_value=Decimal("2"), resumed_value=None) succeeds; drop.resumed_value is None signals carried_forward (D-14).
  - Test C: FieldDrop(..., resumed_value=Decimal("0")) succeeds; drop.resumed_value == Decimal("0") signals confirmed_dropped (D-14).
  - Test D: FieldDrop(extra_field="bad", ...) raises ValidationError (extra="forbid").
  - Existing ValidationIssue construction with "missing", "out_of_bounds", "non_numeric" continues to work unbroken.
  </behavior>

  <action>
  Step 1 — Widen ValidationIssue Literal in app/models/roster.py (per D-17 forward-compat, RESEARCH.md CORRECTION):
  Locate ValidationIssue class (lines ~205-216). Change the issue_type field from Literal["missing", "out_of_bounds", "non_numeric"] to Literal["missing", "out_of_bounds", "non_numeric", "field_regression"]. Update the docstring comment to note that "field_regression" is a forward-compat value added for Phase 7.5 (D-17); nothing in Phase 7 emits it. Do NOT change any other field.

  Step 2 — Add FieldDrop to app/models/contracts.py (per D-13/D-14, RESEARCH.md §Target 7):
  Confirm UUID and Decimal are imported at the top of contracts.py. If UUID is missing, add it to the existing typing/uuid import line. If Decimal is missing, add from decimal import Decimal. Then after the Extracted class (and its closing line) and before the Decision class, add the FieldDrop model:

  - Class name: FieldDrop
  - model_config = ConfigDict(extra="forbid")
  - Fields: employee_id of type UUID, field of type str (bare field name, e.g. "hours_overtime"), original_value of type Decimal, resumed_value of type Decimal or None
  - Docstring: "Forward-compat scaffolding for Phase 7.5 (MONEY-03). Public field-regression record. employee_id is always a real resolved UUID. field is always a bare hours-field name (never qualified with submitted_name). resumed_value=None means carried_forward; Decimal('0') means confirmed_dropped (D-13/D-14). Nothing in Phase 7 emits this type."

  Do NOT add RawFieldDrop — that is an internal Phase 7.5 implementation detail that belongs alongside detect_field_regression.

  Verify by running: uv run python -c "from app.models.roster import ValidationIssue; from app.models.contracts import FieldDrop; from decimal import Decimal; import uuid; vi = ValidationIssue(issue_type='field_regression', field='f', message='m'); fd = FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); print('OK')"
  </action>

  <verify>
    <automated>uv run python -c "from app.models.roster import ValidationIssue; from app.models.contracts import FieldDrop; from decimal import Decimal; import uuid; vi = ValidationIssue(issue_type='field_regression', field='f', message='m'); fd = FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); fd2 = FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=Decimal('0')); assert fd.resumed_value is None; assert fd2.resumed_value == Decimal('0'); print('ALL OK')"</automated>
  </verify>
  <done>Import + construction succeeds. uv run pytest tests/test_models_contracts.py -q exits 0 (existing tests unbroken). grep "field_regression" app/models/roster.py exits 0. grep "class FieldDrop" app/models/contracts.py exits 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write MONEY-01 and MONEY-02 RED tests (behavioral, not ImportError)</name>
  <files>tests/test_validate.py, tests/test_reconcile.py, tests/test_eval_wiring.py</files>

  <read_first>
  Read tests/test_validate.py (full file — capture existing helpers _extracted, _match, _make_weekly_hourly_employee, _one_employee_roster; append new tests at the bottom without rewriting existing ones). Read tests/test_reconcile.py lines 1-30 for file structure and fixture usage. Read tests/test_eval_wiring.py (full file) to understand where to append the new test. Read app/pipeline/validate.py lines 69-100 (the full validate() function body, particularly the any_hours predicate at line 84 and the pay_type lookup logic) — this is REQUIRED before writing tests to avoid testing structurally-unreachable code paths.

  CRITICAL — pay_type constraint (RESEARCH.md finding #6): Employee.pay_type is Literal["hourly","salary"], non-nullable. ExtractedEmployee has NO pay_type field. The "unknown pay_type" scenario (D-03 fail-safe) means matched_employee_id=None (unresolved employee). Read validate()'s actual code path for unresolved employees (the _employee_pay_type helper returns None when no match found). An unresolved employee with all-zero hours is currently neither gated (pay_type=None → the if pay_type == "hourly" branch is skipped). The D-03 fail-safe is maintained by the existing gate structure: unresolved names are already blocked from processing by decide.py before calc runs. Do NOT write a test that constructs Employee(pay_type=None) — that violates Pydantic validation and will fail at construction, not at the predicate being tested.

  SCOPE FENCE: Do NOT write any of the following — they are Phase 7.5:
  - Any test importing detect_field_regression
  - Any test for field_regression ValidationIssue gating in decide.py
  - Any test in test_decide.py (a new file for this purpose)
  - Any test_resume_pipeline.py file
  - Any xfail integration stubs for snapshot-once, loop-guard, or confirmed_dropped
  </read_first>

  <behavior>
  All new tests MUST fail RED before Plan 07-02 implements the fixes. Tests fail because the current code has the bug (is not None predicate / casefold-only _norm), NOT because of ImportErrors.

  MONEY-01 tests to append to tests/test_validate.py:

  test_zero_hours_hourly_gates (D-01/D-02):
  Hourly employee Maria Chen with hours_regular=Decimal("0") and all other four hours fields absent (None). Call validate(). Assert issues is non-empty and contains at least one issue with issue_type="missing". RED because current any_hours predicate is "is not None" — Decimal("0") is not None → any_hours=True → employee is skipped → no issue emitted → assertion fails.

  test_partial_week_not_gated (D-03 edge):
  Hourly employee Maria Chen with hours_regular=Decimal("0") AND hours_holiday=Decimal("8"). Call validate(). Assert issues is empty (no missing issue). This may be GREEN already (hours_holiday=8 is not None → any_hours=True → skip), but write it explicitly as a D-03 regression guard so it stays green after the fix. Assert on no missing issue for this employee.

  test_predicate_consistency (D-25):
  Build two separate validate() calls for the same hourly employee — once with hours_overtime=Decimal("0"), all other hours None; once with hours_overtime=None, all other hours None. Assert both calls return a non-empty issues list with at least one issue_type="missing". RED because current predicate treats hours_overtime=Decimal("0") as present (is not None → True) and emits no issue for that call.

  test_salaried_not_gated_regression_guard:
  Salaried employee James Okafor with hours_regular=Decimal("0"), all other hours absent. Call validate(). Assert issues is empty. Regression guard — must pass before AND after the MONEY-01 fix. Salaried employees compute from annual_salary; the zero-hours gate must never apply to them.

  MONEY-02 test to append to tests/test_reconcile.py:

  test_nfd_name_resolves_same_as_nfc (D-07):
  Build a roster using model_copy to create an employee whose full_name is the NFC-composed form of "José Martínez" (unicodedata.normalize("NFC", "José Martínez")). Call reconcile_names with the NFD-decomposed form (unicodedata.normalize("NFD", "José Martínez")). Assert matches[0].resolved is True and matches[0].matched_employee_id equals the employee's id. RED because current _norm calls casefold without NFC — NFD and NFC casefold to different byte sequences → no match → resolved=False → assertion fails.

  eval _normalize parity test to append to tests/test_eval_wiring.py:

  test_eval_normalize_nfd_matches_nfc (C-4):
  Import _normalize from eval.run_eval (the function at line 51 of run_eval.py). Import unicodedata. Assert _normalize(unicodedata.normalize("NFD", "José Martínez")) == _normalize(unicodedata.normalize("NFC", "José Martínez")). RED because current _normalize does casefold().split() without NFC — the two forms produce different outputs.

  After writing all tests, run to confirm RED state:
  uv run pytest tests/test_validate.py -k "zero_hours or predicate_consistency or partial_week or salaried_not_gated" -q
  uv run pytest tests/test_reconcile.py -k "nfd" -q
  uv run pytest tests/test_eval_wiring.py -k "nfd" -q
  Expected: zero_hours FAILS, predicate_consistency FAILS, partial_week PASSES (D-03 guard already correct), salaried_not_gated PASSES (regression guard), nfd tests FAIL.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "zero_hours or predicate_consistency" --tb=no -q 2>&1 | grep -E "failed|passed|error" | head -5</automated>
  </verify>
  <done>test_zero_hours_hourly_gates FAILS RED (expected). test_predicate_consistency FAILS RED (expected). test_partial_week_not_gated PASSES (correct D-03 behavior already holds). test_salaried_not_gated_regression_guard PASSES (regression guard). test_nfd_name_resolves_same_as_nfc FAILS RED in test_reconcile.py. test_eval_normalize_nfd_matches_nfc FAILS RED in test_eval_wiring.py. Full test collection succeeds: uv run pytest --collect-only -q exits 0 with no collection errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts.py model construction | FieldDrop accepted as pipeline I/O; Pydantic validates at construction time |
| ValidationIssue.issue_type Literal | Constrains the set of legal issue types at construction; widening is backward-compatible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | FieldDrop fields (employee_id, resumed_value) | mitigate | model_config = ConfigDict(extra="forbid") on FieldDrop; Pydantic rejects unexpected fields at construction; UUID type prevents non-UUID employee_id injection |
| T-07-02 | Information Disclosure | ValidationIssue.field carries submitted employee name | accept | Same pattern as all existing ValidationIssue.field values (roster.py); no new PII surface. Logging boundary at orchestrator.py error handler captures only type(exc).__name__ |
| T-07-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No new package-manager installs; stdlib uuid and Decimal only |
</threat_model>

<verification>
Full plan verification after both tasks complete:

1. uv run python -c "from app.models.roster import ValidationIssue; vi = ValidationIssue(issue_type='field_regression', field='f', message='m'); print('Literal OK')" — exits 0
2. uv run python -c "from app.models.contracts import FieldDrop; from decimal import Decimal; import uuid; FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); print('FieldDrop OK')" — exits 0
3. uv run pytest tests/test_validate.py -k "zero_hours" --tb=short -q — FAILS RED (expected: D-01 bug not yet fixed)
4. uv run pytest tests/test_validate.py -k "partial_week or salaried_not_gated" --tb=short -q — PASSES (D-03 regression guards)
5. uv run pytest tests/test_reconcile.py -k "nfd" --tb=short -q — FAILS RED (expected: _norm has no NFC yet)
6. uv run pytest tests/test_eval_wiring.py -k "nfd" --tb=short -q — FAILS RED (expected: _normalize has no NFC yet)
7. uv run pytest tests/test_models_contracts.py -q — all PASS (no regressions from Literal widening)
8. uv run pytest --collect-only -q — exits 0 with no collection errors
</verification>

<success_criteria>
- grep "field_regression" app/models/roster.py exits 0 (Literal widened)
- grep "class FieldDrop" app/models/contracts.py exits 0 (forward-compat model present)
- grep "class RawFieldDrop" app/models/contracts.py exits 1 (MONEY-03 internal type NOT present — Phase 7.5)
- test_zero_hours_hourly_gates FAILS RED in test_validate.py (MONEY-01 bug not yet fixed)
- test_predicate_consistency FAILS RED in test_validate.py (D-25 shared predicate not yet added)
- test_nfd_name_resolves_same_as_nfc FAILS RED in test_reconcile.py (MONEY-02 not yet fixed)
- test_eval_normalize_nfd_matches_nfc FAILS RED in test_eval_wiring.py (C-4 not yet fixed)
- No test_resume_pipeline.py file created (scope fence)
- No test importing detect_field_regression anywhere (scope fence)
- Full test collection succeeds with no collection errors
</success_criteria>

<output>
Create .planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md when done.
</output>
