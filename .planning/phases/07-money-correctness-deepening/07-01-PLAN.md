---
phase: 07-money-correctness-deepening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/contracts.py
  - app/models/roster.py
  - tests/test_validate.py
  - tests/test_reconcile.py
  - tests/test_decide.py
  - tests/test_eval_wiring.py
  - tests/test_resume_pipeline.py
autonomous: true
requirements:
  - MONEY-01
  - MONEY-02
  - MONEY-03

must_haves:
  truths:
    - "ValidationIssue.issue_type Literal includes 'field_regression' so D-17 ValidationIssue(issue_type='field_regression') does not raise a ValidationError at runtime (per D-17, C-1)"
    - "FieldDrop Pydantic model exists in contracts.py with employee_id, field, original_value, resumed_value fields (per D-10, D-11, D-13)"
    - "All MONEY-01 failing tests exist in test_validate.py and run RED before the Wave 2 fix"
    - "NFD name test exists in test_reconcile.py and runs RED before the Wave 2 fix"
    - "field_regression gating test exists in tests/test_decide.py and runs RED before the Wave 3 fix"
    - "Integration test stubs exist in tests/test_resume_pipeline.py marked xfail"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "FieldDrop model + widened ValidationIssue Literal"
      contains: "field_regression"
    - path: "tests/test_validate.py"
      provides: "MONEY-01 failing tests (zero_hours, partial_week, unknown_pay_type, predicate_consistency, explicit_drop, no_regression, detect_regression)"
    - path: "tests/test_decide.py"
      provides: "field_regression gate test (C-1 coverage)"
    - path: "tests/test_resume_pipeline.py"
      provides: "integration stubs: snapshot_once, loop_guard, confirmed_dropped_no_reflag"
  key_links:
    - from: "app/models/contracts.py:ValidationIssue.issue_type"
      to: "app/pipeline/decide.py:Rule 2b (Wave 3)"
      via: "Literal must include 'field_regression' before decide rule can reference it"
      pattern: "field_regression"
---

<objective>
Widen `ValidationIssue.issue_type` Literal to include `"field_regression"` and add the new `FieldDrop` Pydantic model to `contracts.py` — the two pre-conditions every subsequent MONEY-03 task depends on. Simultaneously write ALL failing Wave 0 tests so the RED baseline is established before any implementation runs in Wave 2+.

Purpose: TDD discipline (D-30) requires every fix to be preceded by a failing test. Wave 1 establishes the contracts foundation and plants every RED stake so Waves 2-5 can flip them GREEN one by one.
Output: Widened Literal in `app/models/roster.py` (where `ValidationIssue` lives per PATTERNS.md), `FieldDrop` in `app/models/contracts.py`, and five test files with failing/xfail tests covering all 14 behaviors in VALIDATION.md.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-VALIDATION.md

# Source files to read before editing (no re-reads)
@app/models/roster.py
@app/models/contracts.py
@tests/test_validate.py
@tests/test_reconcile.py
@tests/test_eval_wiring.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen ValidationIssue Literal + add FieldDrop model</name>
  <files>app/models/roster.py, app/models/contracts.py</files>

  <read_first>
  Read app/models/roster.py lines 200-220 for the ValidationIssue class (PATTERNS.md §3 shows it lives in roster.py at lines 205-216, not contracts.py — confirm the actual location before editing). Read app/models/contracts.py lines 1-30 (imports) and the ExtractedEmployee class near lines 60-80 for the model style to copy. PATTERNS.md §3 has the exact Literal change and FieldDrop shape. CONTEXT.md D-17 locks the new Literal value; D-10/D-11/D-13 lock the FieldDrop fields.
  </read_first>

  <behavior>
  - Test 1 (contracts): Constructing `ValidationIssue(issue_type="field_regression", field="x.hours_overtime", message="regression")` must NOT raise a ValidationError (currently raises because "field_regression" is not in the Literal).
  - Test 2 (contracts): Constructing `FieldDrop(employee_id=some_uuid, field="hours_overtime", original_value=Decimal("2"), resumed_value=None)` succeeds and `drop.resumed_value is None` → carry_forward semantics (D-14).
  - Test 3 (contracts): Constructing `FieldDrop(..., resumed_value=Decimal("0"))` succeeds and `drop.resumed_value == Decimal("0")` → confirmed_dropped semantics (D-14).
  - Test 4 (contracts): `FieldDrop(employee_id=..., field="x", original_value=Decimal("2"), resumed_value=None, extra_field="bad")` raises ValidationError (extra="forbid", per V5 input validation).
  </behavior>

  <action>
  Step 1 — Widen ValidationIssue Literal (per C-1 orchestrator resolution + D-17):
  In `app/models/roster.py`, find the `ValidationIssue` class (RESEARCH.md confirms it is at roster.py:205-216). Change the `issue_type` field from `Literal["missing", "out_of_bounds", "non_numeric"]` to `Literal["missing", "out_of_bounds", "non_numeric", "field_regression"]`. Do NOT change any other field or the docstring beyond updating the comment to note D-17 adds "field_regression".

  Step 2 — Add FieldDrop model to contracts.py (per D-10/D-11/D-13, PATTERNS.md §3):
  In `app/models/contracts.py`, after the `Extracted` class and before the `Decision` class, add the `FieldDrop` BaseModel. Shape (per PATTERNS.md §3):
  - `model_config = ConfigDict(extra="forbid")` (V5 input validation)
  - `employee_id: UUID` (the resolved employee id from the snapshot; D-11)
  - `field: str` (e.g. "hours_overtime")
  - `original_value: Decimal` (value in snapshot where `_is_paid` is True; D-10)
  - `resumed_value: Decimal | None` (None = absent/silence → carried_forward; Decimal("0") = explicit zero → confirmed_dropped; D-14)

  Ensure `UUID` and `Decimal` are imported at the top of contracts.py (confirm; add if missing). The docstring must state the D-14 semantics of `resumed_value`.

  Run tests: `uv run pytest tests/test_models_contracts.py -q` to confirm existing tests still pass. The new FieldDrop tests belong in the failing-test scaffold (Task 2).
  </action>

  <verify>
    <automated>uv run python -c "from app.models.roster import ValidationIssue; from decimal import Decimal; import uuid; vi = ValidationIssue(issue_type='field_regression', field='x.hours_overtime', message='test'); print('Literal OK'); from app.models.contracts import FieldDrop; fd = FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); print('FieldDrop OK')"</automated>
  </verify>
  <done>Python import + construction of both types succeeds without error. `uv run pytest tests/test_models_contracts.py -q` exits 0 (existing tests unbroken).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write all Wave 0 failing tests (RED baseline)</name>
  <files>tests/test_validate.py, tests/test_reconcile.py, tests/test_decide.py, tests/test_eval_wiring.py, tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/test_validate.py (full file already in context) — the existing helper functions `_extracted`, `_match`, and the `roster_from_seed` fixture must be reused; add new tests at the bottom. Read tests/test_reconcile.py lines 1-50 to understand the file structure and which fixtures it uses. Read tests/test_eval_wiring.py (full file in context) to understand the existing wiring test — add the NFD test at the bottom. VALIDATION.md has the full per-requirement test map with exact -k filter strings. PATTERNS.md §8 shows the target form of `_norm` for the NFD test. CONTEXT.md D-25/D-26/D-27 lock the predicate-consistency, explicit-drop, and determinism tests.
  </read_first>

  <behavior>
  All tests in this task MUST be RED (fail) before Wave 2/3 implementation. Write them as genuine assertions, not as xfail with no body.

  MONEY-01 tests in test_validate.py (append to existing file):
  - test_zero_hours_hourly_gates: hourly employee `hours_regular=Decimal("0")`, all other hours None → issues contains `issue_type="missing"`. FAILS because current code uses `is not None` and Decimal("0") passes.
  - test_partial_week_not_gated: hourly employee `hours_regular=Decimal("0")`, `hours_holiday=Decimal("8")` → no missing issue (D-03: partial week still processes). May pass or fail depending on current code — assert explicitly.
  - test_unknown_pay_type_zero_hours_gates: `match` constructed with a roster Employee whose `pay_type=None` (build inline, not from seed; use `_one_employee_roster` helper already in file) + all hours zero → issues contains `issue_type="missing"` (D-03 fail-safe).
  - test_predicate_consistency: `OT=Decimal("2")` snapshot employee becomes `OT=Decimal("0")` resumed → the `_is_paid(Decimal("0"))` result equals `_is_paid(None)` result (both False); both must be treated as "not paid". This tests the `_is_paid` predicate directly once it exists (import from validate.py). For now write to verify the behavior through validate output: two separate extractions (OT=0 vs OT=absent) both gate to clarification.

  MONEY-03 detection tests in test_validate.py (append):
  - test_detect_regression_ot_absent: call `detect_field_regression(original_extracted, resumed_extracted)` where original has `hours_overtime=Decimal("2")` and resumed has `hours_overtime=None` → returns a list with exactly one `FieldDrop` whose hours field is `hours_overtime` (the helper returns a qualified `field` of the form `"{submitted_name}.hours_overtime"`, so assert `drop.field.split(".")[-1] == "hours_overtime"`, not `drop.field == "hours_overtime"`; see Plan 03 LOCKED DESIGN). Import `detect_field_regression` from `app.pipeline.validate` (does not exist yet → ImportError = RED).
  - test_explicit_drop_zero_resumed_value: call `detect_field_regression(...)` where resumed has `hours_overtime=Decimal("0")` → returns `FieldDrop` with `resumed_value=Decimal("0")` (confirms D-14 explicit-drop signal). Will fail RED via ImportError.
  - test_no_regression_noop: original and resumed are identical → `detect_field_regression(...)` returns `[]` (D-27 determinism). Will fail RED via ImportError.

  MONEY-02 test in test_reconcile.py (append):
  - test_nfd_name_resolves_same_as_nfc: build a roster with an employee whose `full_name` is NFC "José Martínez" (construct using `unicodedata.normalize("NFC", "José Martínez")`); call `reconcile_names(["José Martínez" in NFD form], roster)` where the submitted name is `unicodedata.normalize("NFD", "José Martínez")`; assert `matches[0].resolved is True` and `matches[0].matched_employee_id == jose_id`. Fails RED because current `_norm` has no NFC call.

  field_regression decide test in tests/test_decide.py (new file):
  - Create the file with a single test `test_field_regression_issue_gates_to_clarification`. Build a `ValidationIssue(issue_type="field_regression", field="alice.hours_overtime", message="regression")` and call `decide(extracted, matches, [regression_issue])`. Assert `decision.final_action == "request_clarification"` and any gate reason mentions "field_regression". Fails RED because `decide.py` Rule 2b does not exist (the issue is ignored, so `gate_reasons` is empty and `final_action` may be "process").

  eval wiring NFD test in tests/test_eval_wiring.py (append to existing file):
  - test_eval_normalize_nfd_matches_nfc: import `_normalize` from `eval.run_eval` (handle potential import path); assert that `_normalize(unicodedata.normalize("NFD", "José Martínez"))` equals `_normalize(unicodedata.normalize("NFC", "José Martínez"))`. Fails RED because current `_normalize` does not NFC-normalize.

  Integration stubs in tests/test_resume_pipeline.py (new file):
  - Create the file with three tests marked `@pytest.mark.xfail(reason="integration: needs live DB + Wave 4 wiring", strict=False)`:
    - `test_snapshot_once_not_overwritten_on_second_clarify` (D-28)
    - `test_loop_guard_fires_exactly_once_then_carry_forward` (D-20/D-13)
    - `test_confirmed_dropped_does_not_reflag_money01` (D-15)
  These stubs will be filled in with real DB logic in Plan 05 (Wave 5). Mark xfail so the suite stays collectible.
  </behavior>

  <action>
  Append new tests to test_validate.py after the last existing test. The file already has `_extracted`, `_match`, `_one_employee_roster`, and `_make_weekly_hourly_employee` helpers — reuse them. For `test_unknown_pay_type_zero_hours_gates`, build an inline `Employee` with `pay_type=None` using the `_make_weekly_hourly_employee` pattern but set `pay_type=None`.

  For test_reconcile.py: append the NFD test at the bottom. Import `unicodedata` at the top if not already present.

  Create tests/test_decide.py: minimal imports (`from app.pipeline.decide import decide`, `from app.models.roster import ValidationIssue`, `from app.models.contracts import Extracted, ExtractedEmployee`). Build a minimal `Extracted` with one employee and call `decide`. Do NOT import the roster fixture — build inline. The test must import `ValidationIssue` from the widened Literal (Task 1 must be done first; if this plan is executed atomically Task 1 runs first).

  Create tests/test_resume_pipeline.py: imports `pytest` only (the xfail stubs have empty bodies calling `pytest.skip("not yet implemented")`).

  Append to tests/test_eval_wiring.py: import `unicodedata` at the module level; add the new test function at the bottom.

  After writing all tests, run the suite to confirm RED state:
  `uv run pytest tests/test_validate.py tests/test_reconcile.py tests/test_decide.py tests/test_eval_wiring.py tests/test_resume_pipeline.py -q 2>&1 | tail -20`

  Expected: several FAILED + the xfail tests show as xfail (not as errors). `ImportError` for `detect_field_regression` counts as a test error/failure — that is the expected RED baseline.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "zero_hours or detect_regression or explicit_drop or no_regression" --no-header -q 2>&1 | grep -E "FAILED|ERROR|passed|failed" | head -10</automated>
  </verify>
  <done>At least 4 tests fail RED in test_validate.py (zero_hours, detect_regression, explicit_drop, no_regression). test_nfd_name_resolves_same_as_nfc fails RED in test_reconcile.py. test_field_regression_issue_gates_to_clarification fails RED in test_decide.py. test_eval_normalize_nfd_matches_nfc fails RED in test_eval_wiring.py. The three xfail stubs in test_resume_pipeline.py collect without error. Full suite still collects (`uv run pytest --collect-only -q` exits 0 with no collection errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contracts.py model construction | `FieldDrop` accepted as pipeline I/O; Pydantic validates at construction time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | `FieldDrop.resumed_value` (Decimal or None) | mitigate | `model_config = ConfigDict(extra="forbid")` + `ge=0` on `original_value`; Pydantic rejects unexpected fields at construction |
| T-07-02 | Information Disclosure | `ValidationIssue.field` in field_regression issues (contains submitted_name.field) | accept | Submitted name is already in all existing `ValidationIssue.field` values (roster.py line 93 pattern); no new PII surface. Logging boundary at orchestrator.py:239 only captures `type(exc).__name__` |
| T-07-SC | Tampering | No new npm/pip installs in this plan | accept | No new package-manager installs; stdlib `uuid`/`Decimal` only |
</threat_model>

<verification>
Full plan verification (run after both tasks):

1. `uv run python -c "from app.models.roster import ValidationIssue; vi = ValidationIssue(issue_type='field_regression', field='f', message='m'); print('OK')"` — exits 0
2. `uv run python -c "from app.models.contracts import FieldDrop; from decimal import Decimal; import uuid; FieldDrop(employee_id=uuid.uuid4(), field='hours_overtime', original_value=Decimal('2'), resumed_value=None); print('OK')"` — exits 0
3. `uv run pytest tests/test_validate.py -k "zero_hours" -x -q` — FAILS RED (expected)
4. `uv run pytest tests/test_decide.py -k "field_regression" -x -q` — FAILS RED (expected)
5. `uv run pytest tests/test_resume_pipeline.py -q` — all xfail (no errors)
6. `uv run pytest tests/test_models_contracts.py -q` — all pass (regression guard)
</verification>

<success_criteria>
- `ValidationIssue.issue_type` Literal in roster.py contains `"field_regression"` (grep: `grep "field_regression" app/models/roster.py` exits 0)
- `FieldDrop` class exists in contracts.py with `extra="forbid"` and all four fields (grep: `grep "class FieldDrop" app/models/contracts.py` exits 0)
- 4+ tests fail RED in test_validate.py under the new MONEY-01/03 markers
- 1 test fails RED in test_decide.py (field_regression gate)
- 1 test fails RED in test_eval_wiring.py (NFD normalize parity)
- 1 test fails RED in test_reconcile.py (NFD name match)
- 3 tests xfail (not error) in test_resume_pipeline.py
- Full test collection succeeds: `uv run pytest --collect-only -q` exits 0
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md` when done.
</output>
