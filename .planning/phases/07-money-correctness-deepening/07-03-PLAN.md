---
phase: 07-money-correctness-deepening
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - app/pipeline/validate.py
  - app/pipeline/decide.py
autonomous: true
requirements:
  - MONEY-03

must_haves:
  truths:
    - "detect_field_regression(original, resumed) exists as a pure 2-argument helper in validate.py — returns list[RawFieldDrop] (NOT list[FieldDrop]) iterating ALL five watched hours fields via _HOURS_FIELDS using _is_paid (per D-08/D-09/D-10)"
    - "RawFieldDrop carries submitted_name (string), not employee_id — validate() maps submitted_name→employee_id internally via matches to produce FieldDrop (decision C; supersedes prior revision's sentinel UUID trick)"
    - "validate() gains prior: Extracted | None = None kwarg and resolved_drops: set[tuple[UUID, str]] | None = None kwarg — fresh runs (prior=None) are a pure no-op (D-17/D-18)"
    - "validate() maps RawFieldDrop.submitted_name → employee_id via matches INSIDE validate(), never exposes the sentinel UUID outside the function"
    - "decide.py Rule 2b gates request_clarification on issue_type='field_regression' issues (2 lines added after Rule 2) — resolves C-1"
    - "OT 2→0 is detected as a drop identically to OT 2→absent (D-25 predicate-consistency, uses shared _is_paid)"
    - "explicit zero resumed_value=Decimal('0') is returned as RawFieldDrop with resumed_value=Decimal('0'), not None (D-26 explicit-drop signal for D-14)"
    - "no-op reply yields detect_field_regression([]) (D-27 determinism)"
  artifacts:
    - path: "app/pipeline/validate.py"
      provides: "detect_field_regression 2-arg helper returning list[RawFieldDrop] + validate(prior=, resolved_drops=) seam"
      exports: ["detect_field_regression", "validate"]
      contains: "def detect_field_regression"
    - path: "app/pipeline/decide.py"
      provides: "Rule 2b for field_regression issues"
      contains: "field_regression"
  key_links:
    - from: "app/pipeline/validate.py:detect_field_regression"
      to: "app/pipeline/validate.py:validate (prior= kwarg)"
      via: "validate() calls detect_field_regression(prior, extracted) when prior is not None; maps RawFieldDrop→FieldDrop internally"
      pattern: "detect_field_regression"
    - from: "app/pipeline/validate.py:validate"
      to: "app/pipeline/decide.py:Rule 2b"
      via: "ValidationIssue(issue_type='field_regression') emitted by validate → caught by Rule 2b"
      pattern: "field_regression"
---

<objective>
Add the MONEY-03 pure-function judgment layer: the `detect_field_regression` helper (returning `list[RawFieldDrop]`) and the `validate(prior=)` seam in `validate.py`, plus Rule 2b in `decide.py`. These are the judgment-certifiable pieces (D-23 eval layer).

Purpose: Establish the pure judgment path that the eval can certify directly (D-23). With Rule 2b in place, a `field_regression` ValidationIssue flowing out of `validate()` correctly gates to `request_clarification` — proving the detect→validate→decide chain is wired before the orchestrator state machine complexity is added.

Output: detect_field_regression helper (returns RawFieldDrop, not FieldDrop — decision C), validate(prior=, resolved_drops=) seam, decide Rule 2b. All MONEY-03 unit tests in test_validate.py and test_decide.py turn GREEN.

REVISION NOTES (incorporating all Codex HIGH findings and locked architecture decisions):

Decision C (locked): detect_field_regression returns list[RawFieldDrop] (NOT list[FieldDrop]). RawFieldDrop.submitted_name carries the submitted name. The sentinel UUID trick (uuid.UUID(int=0) in employee_id) is REMOVED. validate() maps RawFieldDrop → FieldDrop INTERNALLY using matches. FieldDrop objects emitted by validate() always have a real employee_id (resolved from matches) and a bare field name. Any RawFieldDrop whose submitted_name is not in matches (unresolved employee) is SKIPPED per D-11 — no baseline for unresolved employees.

The prior Plan 03 LOCKED DESIGN block with the qualified-field sentinel trick is SUPERSEDED by decision C. The action below reflects the correct design.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md
@.planning/phases/07-money-correctness-deepening/07-02-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/pipeline/validate.py
@app/pipeline/decide.py
@app/models/contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detect_field_regression (returns RawFieldDrop) + validate(prior=, resolved_drops=) seam</name>
  <files>app/pipeline/validate.py</files>

  <read_first>
  Read app/pipeline/validate.py (full file — already modified in Plan 02; capture current state including _is_paid). Key anchors:
  - Line ~27-33: `_HOURS_FIELDS` tuple — `detect_field_regression` MUST reuse this.
  - `_is_paid` predicate added in Plan 02 — reuse it in detect_field_regression.
  - Lines 69-73: current `validate()` signature — add `prior: Extracted | None = None` and `resolved_drops: set[tuple[UUID, str]] | None = None` as keyword-only params. Keep default=None so every existing caller is unbroken (D-18).
  - PATTERNS.md §1 "New helper placement": place `detect_field_regression` between `_employee_pay_periods_per_year` and `validate()`.
  - app/models/contracts.py — confirm RawFieldDrop and FieldDrop are both importable (added in Plan 01). Add both to the import.
  - D-11: diff set keyed by submitted_name resolved in BOTH snapshots (via matches on the validate side). D-12: last-wins reduction for multi-line. D-10: trigger = present→absent/zero only.

  CRITICAL — decision C design (supersedes prior plan's sentinel trick):
  detect_field_regression returns list[RawFieldDrop] where RawFieldDrop.submitted_name is the key.
  validate() builds name_to_id from matches and maps each RawFieldDrop.submitted_name → real employee_id to construct FieldDrop.
  NO sentinel UUID. NO qualified field strings. RawFieldDrop stays internal to validate().
  </read_first>

  <behavior>
  RED tests to turn GREEN (from Plan 01 scaffold — using the corrected assertion form per decision C):
  - test_detect_regression_ot_absent: `detect_field_regression(original_extracted, resumed_extracted)` where original has `hours_overtime=Decimal("2")` and resumed has `hours_overtime=None` → returns list with exactly one RawFieldDrop where `drop.field == "hours_overtime"` (BARE NAME), `drop.original_value == Decimal("2")`, `drop.resumed_value is None`, `drop.submitted_name == <submitted_name>`.
  - test_explicit_drop_zero_resumed_value (D-26): resumed has `hours_overtime=Decimal("0")` → RawFieldDrop with `resumed_value=Decimal("0")`. `_is_paid(Decimal("0"))==False` means it is a drop; `resumed_value` carries the actual value for D-14 outcome distinction.
  - test_no_regression_noop (D-27): original and resumed identical → `detect_field_regression` returns `[]`.
  - test_predicate_consistency (D-25): `OT=2→0` and `OT=2→absent` both detected as drops (both produce RawFieldDrop with the same field). NOT detected when OT goes `2→3` (increase while paid).

  validate() seam behavior:
  - Fresh run (`prior=None`): `validate(extracted, roster, matches)` behaves identically to today — no field_regression issue emitted. Existing tests must stay GREEN.
  - Resume with prior (`prior=some_extracted`): calls `detect_field_regression(prior, extracted)` → for each RawFieldDrop, maps submitted_name→employee_id via matches → constructs FieldDrop with real employee_id + bare field name → emits `ValidationIssue(issue_type="field_regression", ...)`.
  - resolved_drops kwarg: employees/fields in resolved_drops are SKIPPED in the `any_hours` gate (D-15). Default=None is a no-op.
  </behavior>

  <action>
  Step 1 — Add `detect_field_regression` function (per D-10/D-11/D-12, decision C):

  The PUBLIC signature is exactly: `detect_field_regression(original: Extracted, resumed: Extracted) -> list[RawFieldDrop]`.

  Place it between `_employee_pay_periods_per_year` and `validate()` in validate.py.

  Inside `detect_field_regression`:
  - Build `{submitted_name: ExtractedEmployee}` dicts for BOTH `original.employees` and `resumed.employees` using last-wins reduction (D-12): iterate each list in order; later entries for the same `submitted_name` overwrite earlier ones.
  - For each `submitted_name` present in BOTH dicts (iterate sorted for determinism, D-27), for each `field` in `_HOURS_FIELDS`:
    - `original_val = getattr(original_emp, field)` and `resumed_val = getattr(resumed_emp, field)`
    - If `_is_paid(original_val) and not _is_paid(resumed_val)` → this is a drop.
    - Append `RawFieldDrop(submitted_name=submitted_name, field=field, original_value=original_val, resumed_value=resumed_val)`.
  - Return the list of RawFieldDrop objects.

  Note: field is the BARE hours field name (e.g. "hours_overtime"), NOT a qualified string. This is the correct design per decision C.

  Add `from app.models.contracts import Extracted, FieldDrop, RawFieldDrop` to validate.py imports (confirm Extracted is already imported; add FieldDrop and RawFieldDrop).
  Add `from uuid import UUID` if not already present (needed for resolved_drops type hint).
  Add `from app.models.roster import ExtractedEmployee` only if not importable via the existing Extracted type. Check the import chain first.

  Step 2 — Add `prior=None` and `resolved_drops=None` kwargs to `validate()` (per D-17/D-18):
  Change the signature to include keyword-only params `prior: Extracted | None = None` and `resolved_drops: set[tuple[UUID, str]] | None = None` after `matches`.

  Inside `validate()`, add the following logic (in the correct positions):

  At the START of the function body: `resolved_drops = resolved_drops or set()`

  D-15 resolved_drops guard: inside the `any_hours` loop, after the pay_type check, skip the MONEY-01 gate for an employee if its `(matched_employee_id, field)` pair is in `resolved_drops`. Build `matched_emp_id` from the `matches` list by looking up `submitted_name`. The guard is: `if any((matched_emp_id, f) in resolved_drops for f in _HOURS_FIELDS)` — if any of the employee's hours fields are in resolved_drops, skip the missing-hours gate for those fields.

  When `prior is not None`: 
  - Build `name_to_id = {m.submitted_name: m.matched_employee_id for m in matches if m.resolved and m.matched_employee_id is not None}`.
  - Call `raw_drops = detect_field_regression(prior, extracted)` (the 2-arg form).
  - For each `raw_drop in raw_drops`:
    - Look up `submitted_name = raw_drop.submitted_name` in `name_to_id`.
    - If submitted_name is NOT in name_to_id (unresolved employee): SKIP (no baseline per D-11).
    - Otherwise: construct `FieldDrop(employee_id=name_to_id[submitted_name], field=raw_drop.field, original_value=raw_drop.original_value, resumed_value=raw_drop.resumed_value)`.
    - Emit `ValidationIssue(issue_type="field_regression", field=f"{raw_drop.submitted_name}.{raw_drop.field}", message=f"field regression: {raw_drop.field} was {raw_drop.original_value}, now {'absent' if raw_drop.resumed_value is None else raw_drop.resumed_value}")` for each resolved drop.
  - Note: validate() builds FieldDrop for internal tracking; the ValidationIssue is what gets returned in the list. The FieldDrop is used by the orchestrator in Plan 04 to populate clarified_fields — validate() does NOT need to return FieldDrop objects directly, only the ValidationIssue list.

  The field attribute on ValidationIssue uses the `{submitted_name}.{field}` format for human readability in the gate_reasons — this is in the ValidationIssue message/field (NOT in FieldDrop). The FieldDrop.field is always a bare name. This keeps the existing roster.py:93 pattern for ValidationIssue field values while keeping FieldDrop correct.

  Run: `uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency" -q` to confirm the 4 targeted tests turn GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency" -q</automated>
  </verify>
  <done>All 4 targeted tests GREEN. `grep "def detect_field_regression" app/pipeline/validate.py` exits 0. `grep "prior" app/pipeline/validate.py | grep "def validate"` shows the new signature. `grep "RawFieldDrop" app/pipeline/validate.py` exits 0. Existing validate tests still pass: `uv run pytest tests/test_validate.py -q`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: decide.py Rule 2b for field_regression issues (C-1 resolution)</name>
  <files>app/pipeline/decide.py</files>

  <read_first>
  Read app/pipeline/decide.py lines 110-135 to see the full Rule 2 pattern. PATTERNS.md §4 has the exact 2-line addition. CONTEXT.md D-17 locks this as "Option B" (C-1 orchestrator resolution). The `Decision` model's `missing_fields` field is NOT widened — regressions feed `gate_reasons` only (PATTERNS.md §4 note).
  </read_first>

  <behavior>
  RED test to turn GREEN:
  - test_field_regression_issue_gates_to_clarification (test_decide.py): pass `[ValidationIssue(issue_type="field_regression", field="alice.hours_overtime", message="regression")]` to `decide(extracted, matches, issues)` → `decision.final_action == "request_clarification"` and `any("field_regression" in r for r in decision.gate_reasons)`. Currently fails because Rule 2 only checks `issue_type == "missing"`.
  Existing tests must stay GREEN.
  </behavior>

  <action>
  In app/pipeline/decide.py, immediately after Rule 2 (lines ~123-125), add Rule 2b — exactly 2 lines:

  After the existing `# Rule 2` block:
  `missing = [i.field for i in issues if i.issue_type == "missing"]`
  `gate_reasons += [f"missing required field: {f}" for f in missing]`

  Add these two lines:
  `regressions = [i.field for i in issues if i.issue_type == "field_regression"]`
  `gate_reasons += [f"field regression: {f}" for f in regressions]`

  Include the comment: `# Rule 2b — field regression detected (D-17, C-1 resolution; decide.py needed 2 new lines).`

  That is the complete change to decide.py. The `Decision.missing_fields` field is NOT updated — regressions surface via `gate_reasons` only (per PATTERNS.md §4). Do NOT widen any other part of the decide logic.
  </action>

  <verify>
    <automated>uv run pytest tests/test_decide.py -k "field_regression" -q</automated>
  </verify>
  <done>test_field_regression_issue_gates_to_clarification is GREEN. `grep "Rule 2b" app/pipeline/decide.py` exits 0. `grep "field_regression" app/pipeline/decide.py` exits 0. Full suite still green: `uv run pytest tests/test_validate.py tests/test_reconcile.py tests/test_decide.py -q`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate() → detect_field_regression | Operates on typed Extracted objects (Pydantic-validated at extraction parse boundary); no raw string input |
| ValidationIssue(issue_type="field_regression") | Pydantic validates the Literal at construction; extra="forbid" on ValidationIssue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | `detect_field_regression` — takes two typed `Extracted` objects | mitigate | Both `Extracted` objects are Pydantic-validated with `extra="forbid"` at parse time. `RawFieldDrop` also has `extra="forbid"` (Plan 01). No raw dict/string input; no injection vector. |
| T-07-06 | Information Disclosure | `ValidationIssue(issue_type="field_regression")` field contains submitted_name (PII) | accept | The field attribute uses `{submitted_name}.{field}` — same pattern as all existing ValidationIssue.field values (roster.py:93 pattern, no new PII surface). Gate reason contains field name only (not name), so operator-visible context is limited. |
| T-07-07 | Denial of Service | Loop over `_HOURS_FIELDS` (5 fields) × employees (bounded by extraction) | accept | Loop is O(employees × 5 fields) — trivially bounded for a payroll email. No external input can expand the loop size beyond the extraction result. |
| T-07-SC | Tampering | No new package installs | accept | Pure stdlib + existing Pydantic; no `uv add` invoked |
</threat_model>

<verification>
Full plan verification after both tasks:

1. `uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression" -q` — all GREEN
2. `uv run pytest tests/test_decide.py -k "field_regression" -q` — GREEN
3. `uv run pytest tests/test_validate.py tests/test_reconcile.py -q` — full suite GREEN (no regressions from Plan 02)
4. `grep "def detect_field_regression" app/pipeline/validate.py` — exits 0
5. `grep "RawFieldDrop" app/pipeline/validate.py` — exits 0 (returned by detect_field_regression)
6. `grep "Rule 2b" app/pipeline/decide.py` — exits 0
7. `grep "prior" app/pipeline/validate.py | grep -c "def validate"` — > 0 (signature updated)
</verification>

<success_criteria>
- detect_field_regression exists in validate.py with PUBLIC signature `detect_field_regression(original: Extracted, resumed: Extracted) -> list[RawFieldDrop]` — returns RawFieldDrop (NOT FieldDrop); NO sentinel UUID; NO qualified field string (decision C)
- validate() accepts `prior=None` and `resolved_drops=None` kwargs; existing callers with 3 positional args are unbroken
- validate() maps RawFieldDrop.submitted_name → employee_id internally using matches; the FieldDrop with a real resolved UUID is constructed inside validate() and used to track outcomes
- decide Rule 2b exists (2 lines after Rule 2): `field_regression` issues now gate to `request_clarification`
- D-25 predicate-consistency: OT 2→0 and OT 2→absent produce identical RawFieldDrop (both are drops)
- All prior validate + reconcile + eval-wiring tests remain GREEN
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md` when done.
</output>
