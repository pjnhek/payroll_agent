---
phase: 07-money-correctness-deepening
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - app/pipeline/validate.py
  - app/pipeline/decide.py
  - app/pipeline/compose_email.py
autonomous: true
requirements:
  - MONEY-03

must_haves:
  truths:
    - "detect_field_regression(original, resumed) is a pure 2-argument helper in validate.py returning list[RawFieldDrop] — iterates _HOURS_FIELDS via _is_paid (D-08/D-09/D-10)"
    - "RawFieldDrop carries submitted_name (string); validate() maps submitted_name→employee_id via matches INTERNALLY to produce FieldDrop — decision C; no sentinel UUID; no qualified field string"
    - "N6 FIXED: validate() enforces D-11/D-12 correctly — diff is keyed by employee_id in BOTH snapshot-era matches AND current matches. A RawFieldDrop whose submitted_name resolves to a DIFFERENT employee_id across the two sets is SKIPPED (not a regression — it is a re-resolution). Reduce-by-employee_id last-wins (D-12) on both sides before diffing."
    - "N8 FIXED: resolved_drops suppression in validate() is scoped per-(employee_id, field) pair — the any_hours gate is NOT skipped for the entire employee, only for the specific (employee_id, field) pairs in resolved_drops. An employee with one confirmed_dropped field but otherwise zero hours still gates to MONEY-01."
    - "validate() gains prior: Extracted | None = None kwarg and resolved_drops: set[tuple[UUID, str]] | None = None kwarg — fresh runs (prior=None) are a pure no-op; existing callers are unbroken (D-17/D-18)"
    - "decide.py Rule 2b gates request_clarification on field_regression issues (2 lines after Rule 2, C-1 resolution)"
    - "compose_email.py _template_body emits a field-regression-specific line for each regression issue INDEPENDENTLY of the unresolved_names/missing_fields fallback gate — N5 fix. The line is present even when unresolved_names and/or missing_fields are ALSO non-empty."
    - "D-25: OT 2→0 detected as drop identically to OT 2→absent (shared _is_paid predicate)"
    - "D-26: explicit zero resumed_value=Decimal('0') returned as RawFieldDrop with resumed_value=Decimal('0') (not None) — confirmed_dropped signal for D-14"
    - "D-27: no-op reply yields detect_field_regression([]) empty list"
  artifacts:
    - path: "app/pipeline/validate.py"
      provides: "detect_field_regression returning list[RawFieldDrop]; validate(prior=, resolved_drops=) seam with N6 D-11/D-12 and N8 per-field suppression"
      exports: ["detect_field_regression", "validate"]
      contains: "def detect_field_regression"
    - path: "app/pipeline/decide.py"
      provides: "Rule 2b for field_regression issues"
      contains: "field_regression"
    - path: "app/pipeline/compose_email.py"
      provides: "field-regression-specific email line independent of unresolved/missing gate (N5 fix)"
      contains: "field_regression"
  key_links:
    - from: "app/pipeline/validate.py:detect_field_regression"
      to: "app/pipeline/validate.py:validate (prior= kwarg)"
      via: "validate() calls detect_field_regression(prior, extracted), maps RawFieldDrop→FieldDrop using employee_id-in-BOTH-snapshots check (N6/D-11), then emits ValidationIssue(issue_type='field_regression')"
      pattern: "detect_field_regression"
    - from: "app/pipeline/validate.py:validate resolved_drops"
      to: "app/pipeline/validate.py:any_hours gate"
      via: "resolved_drops suppression is per-(employee_id,field) NOT per-employee (N8 fix)"
      pattern: "resolved_drops"
    - from: "app/pipeline/decide.py:Rule 2b"
      to: "app/pipeline/compose_email.py:_template_body"
      via: "field_regression gate_reason → compose surfaces the question regardless of other clarification reasons (N5 fix)"
      pattern: "field_regression"
---

<objective>
Add the MONEY-03 pure-function judgment layer: `detect_field_regression`, `validate(prior=)`, Rule 2b, and the field-regression email copy.

This plan also closes two Round-2 findings in the pure-function layer before the orchestrator wiring in Plan 04:

N6 [HIGH] — detect_field_regression keyed by submitted_name, not employee_id-in-both snapshots (D-11/D-12 violation). Fix: validate() must enforce that a RawFieldDrop is only promoted to a FieldDrop when the submitted_name resolves to the SAME employee_id in BOTH the snapshot-era matches AND the current matches. Re-resolutions are skipped.

N8 [MEDIUM] — resolved_drops suppression skips the whole any_hours gate for an employee when ANY (employee_id, field) pair matches. Fix: scope the suppression to the specific field only; an employee with one confirmed_dropped field but otherwise zero paid hours still gates to MONEY-01.

N5 [HIGH] — field-regression question never reaches the email when a normal clarification coexists. Fix: compose_email.py _template_body must emit the field-regression lines INDEPENDENTLY of the unresolved_names/missing_fields gate — always, whenever gate_reasons contains a "field regression:" entry.

Purpose: Establish the pure-judgment and email-body paths that the eval can certify (D-23 eval layer). With Rule 2b and the N5 email fix in place, a field_regression ValidationIssue flowing from validate() correctly gates AND produces a correct email body regardless of what other clarification reasons exist.

Output: detect_field_regression (returns RawFieldDrop, N6-safe D-11/D-12), validate(prior=, resolved_drops=) seam (N8-safe per-field suppression), decide Rule 2b, compose_email _template_body N5 fix. All MONEY-03 unit tests in test_validate.py and test_decide.py turn GREEN.

SUPERSEDED from prior Plan 03: The prior plan's validate() action described a resolved_drops guard as skipping "any_hours at employee level" — that is the N8 bug. This plan uses per-(employee_id, field) suppression exclusively.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md
@.planning/phases/07-money-correctness-deepening/07-02-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/pipeline/validate.py
@app/pipeline/decide.py
@app/pipeline/compose_email.py
@app/models/contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detect_field_regression (returns RawFieldDrop, N6 D-11/D-12) + validate(prior=, resolved_drops=) seam (N8 per-field)</name>
  <files>app/pipeline/validate.py</files>

  <read_first>
  Read app/pipeline/validate.py in full (already modified in Plan 02; capture current state with _is_paid and any_hours fix). Key anchors:
  - Line ~27-33: _HOURS_FIELDS tuple — detect_field_regression MUST reuse this, not redefine it.
  - _is_paid predicate added in Plan 02 — reuse in detect_field_regression.
  - Lines ~69-73: current validate() signature — gains prior and resolved_drops kwargs.
  - PATTERNS.md §1 and §6 have the exact helper placement and repo patterns.
  - app/models/contracts.py — RawFieldDrop and FieldDrop added in Plan 01 (read to confirm exact field names and import path).

  CRITICAL N6 design (D-11/D-12): detect_field_regression takes two Extracted objects and returns list[RawFieldDrop] keyed by submitted_name — this is correct for the pure-function layer (it has no access to matches). The N6 fix happens ONE LEVEL UP in validate(), which HAS access to both the snapshot-era matches (passed via a new kwarg) AND the current matches. validate() must:
  1. Build name_to_id_SNAPSHOT from snapshot-era matches (new kwarg: prior_matches, passed by Plan 04's StageResult)
  2. Build name_to_id_CURRENT from current matches
  3. For each RawFieldDrop, look up submitted_name in BOTH maps
  4. Skip (not a regression) if the submitted_name maps to a DIFFERENT employee_id across the two, or is unresolved in either
  5. Only promote to FieldDrop when employee_id is THE SAME in both maps

  However: Plan 04 provides prior_matches via StageResult. Plan 03's validate() must define the interface correctly. For now, add a prior_matches: list[NameMatchResult] | None = None kwarg to validate(). When prior_matches is None (fresh run), the N6 check is a no-op. When prior_matches is provided (resume path), it is the matches from the StageResult returned by the FIRST (Round 1) _run_stages call.

  CRITICAL N8 design: The resolved_drops set contains (employee_id, field) tuples for confirmed_dropped fields. The current plan had: "skip any_hours gate if any (employee_id, field) pair for this employee is in resolved_drops." This is WRONG — it skips the entire gate. The correct behavior: inside the any_hours loop, compute any_hours normally across ALL fields. Then when about to emit a MISSING issue for field F for employee E, check if (employee_id, F) is in resolved_drops — if yes, skip ONLY that field's issue. An employee with confirmed_dropped=OT but regular=0 and all other hours None still has any_hours=False (no _is_paid field), so the MONEY-01 gate fires, but it should NOT be suppressed by resolved_drops on OT.

  WAIT — re-read the MONEY-01 gate logic: the gate is at the EMPLOYEE level (if not any_hours: emit missing issue). resolved_drops should NOT suppress the employee-level gate; it should suppress a RE-FLAG of the specific (employee_id, field) as a FIELD REGRESSION issue. The any_hours MONEY-01 gate runs independently. The resolved_drops suppression applies only to the field_regression ValidationIssue path (D-15 says confirmed_dropped short-circuits MONEY-01 re-flag for that specific field — meaning: don't re-emit a field_regression issue for a field that was already confirmed_dropped). So the N8 fix is: in the field_regression emission code path inside validate(), skip emitting a field_regression ValidationIssue for (employee_id, field) pairs in resolved_drops — not in the any_hours gate. Read the prior plan's intended D-15 wording carefully and apply the correct per-field scope.
  </read_first>

  <behavior>
  RED tests to turn GREEN (from Plan 01 scaffold):
  - test_detect_regression_ot_absent: detect_field_regression(original_extracted, resumed_extracted) where original has hours_overtime=Decimal("2") and resumed has hours_overtime=None → returns list with exactly one RawFieldDrop where drop.field == "hours_overtime" (BARE NAME), drop.original_value == Decimal("2"), drop.resumed_value is None, drop.submitted_name == the submitted name.
  - test_explicit_drop_zero_resumed_value (D-26): resumed has hours_overtime=Decimal("0") → RawFieldDrop with resumed_value=Decimal("0"). D-14: the explicit zero is preserved in resumed_value so the orchestrator can distinguish it from None (silence).
  - test_no_regression_noop (D-27): original and resumed identical → detect_field_regression returns [].
  - test_predicate_consistency (D-25): OT 2→0 and OT 2→absent both produce a RawFieldDrop for hours_overtime.
  - New N8 test (add to test_validate.py): test_resolved_drops_suppression_is_per_field — build a run where employee has confirmed_dropped=hours_overtime AND hours_regular=Decimal("0") (all other hours None). Build resolved_drops={(employee_id, "hours_overtime")}. Call validate(extracted, roster, matches, prior=None, resolved_drops=resolved_drops). Assert: the MONEY-01 any_hours gate FIRES (all fields are not _is_paid → any_hours=False → missing issue emitted). The confirmed_dropped only prevents re-emitting a field_regression issue for OT, not the any_hours gate. Expected: issues contains a MONEY-01 "missing" issue for the employee (not suppressed by resolved_drops).
  - New N6 test (add to test_validate.py): test_re_resolution_skip — build a scenario where submitted_name "M. Chen" resolves to employee_A in snapshot-era matches but to employee_B in current matches. Pass prior_matches pointing to employee_A, current matches pointing to employee_B. Call validate(..., prior=prior_extracted, prior_matches=prior_matches_with_A, ...). Assert: no field_regression ValidationIssue is emitted for "M. Chen" (the re-resolution is skipped per N6/D-11).
  </behavior>

  <action>
  Step 1 — Add detect_field_regression function (per D-10/D-11/D-12, decision C):

  Place between the last private helper (before validate()) in validate.py.

  Public signature: detect_field_regression(original: Extracted, resumed: Extracted) -> list[RawFieldDrop].

  Import RawFieldDrop from app.models.contracts at the top of validate.py (add to the existing imports line). Import FieldDrop as well (needed in validate()). Import UUID from uuid if not already present.

  Inside detect_field_regression:
  - Build {submitted_name: ExtractedEmployee} dicts for original.employees and resumed.employees using LAST-WINS reduction (D-12): iterate each list; later entries for the same submitted_name overwrite earlier ones.
  - For each submitted_name present in BOTH dicts (iterate sorted(intersection) for determinism, D-27), for each field in _HOURS_FIELDS:
    - original_val = getattr(original_emp, field); resumed_val = getattr(resumed_emp, field)
    - If _is_paid(original_val) and not _is_paid(resumed_val): this is a drop.
    - Append RawFieldDrop(submitted_name=submitted_name, field=field, original_value=original_val, resumed_value=resumed_val).
  - Return the list. (The reduction-by-employee_id D-11/D-12 enforcement for the DIFF is handled in validate() via the N6 fix — detect_field_regression is a pure submitted_name-keyed helper.)

  Step 2 — Update validate() signature (per D-17/D-18) with N6 and N8 fixes:

  New signature (add three keyword-only params after matches):
  prior: Extracted | None = None
  prior_matches: list[NameMatchResult] | None = None
  resolved_drops: set[tuple[UUID, str]] | None = None

  At the start of the function body: resolved_drops = resolved_drops or set()

  N8 fix — resolved_drops suppression is ONLY for field_regression issues, NOT for any_hours gate:
  The any_hours gate (MONEY-01) runs unchanged on every employee. The resolved_drops set is used ONLY in the field_regression emission block (Step 2b below) to skip re-emitting a field_regression issue for an already-resolved (employee_id, field) pair. Do NOT skip any_hours for any employee based on resolved_drops.

  Step 2a — When prior is not None:
  Build name_to_id_CURRENT from current matches: {m.submitted_name: m.matched_employee_id for m in matches if m.resolved and m.matched_employee_id is not None}.
  Build name_to_id_SNAPSHOT from prior_matches (the snapshot-era matches): {m.submitted_name: m.matched_employee_id for m in prior_matches if m.resolved and m.matched_employee_id is not None} — if prior_matches is None, treat as empty dict (no N6 check possible, promote all resolved drops).
  Call raw_drops = detect_field_regression(prior, extracted).

  For each raw_drop in raw_drops:
  - Look up current_emp_id = name_to_id_CURRENT.get(raw_drop.submitted_name). If None: skip (unresolved in current — D-11).
  - Look up snapshot_emp_id = name_to_id_SNAPSHOT.get(raw_drop.submitted_name). If prior_matches is None: skip the N6 check (treat as same employee). If prior_matches is not None and snapshot_emp_id is None: skip (unresolved in snapshot — D-11).
  - N6 check: if snapshot_emp_id is not None and current_emp_id != snapshot_emp_id: SKIP (re-resolution — different employee in each snapshot, not a regression per D-11).
  - Build employee_id = current_emp_id (a real UUID).
  - N8 check: if (employee_id, raw_drop.field) in resolved_drops: SKIP (already resolved/confirmed_dropped for this exact field — D-15 prevents re-flagging).
  - Emit ValidationIssue(issue_type="field_regression", field=f"{raw_drop.submitted_name}.{raw_drop.field}", message=f"field regression: {raw_drop.field} was {raw_drop.original_value}, now {'absent' if raw_drop.resumed_value is None else raw_drop.resumed_value}").

  Note on FieldDrop: validate() also constructs FieldDrop objects internally for any future callers that need the structured employee_id+field record. For now, the ValidationIssue list is what is returned. The FieldDrop construction can be added here for completeness but need not be returned from validate() — Plan 04 uses detect_field_regression + name_to_id maps directly to build clarified_fields.

  Step 2b — Run targeted tests:
  uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency or re_resolution or resolved_drops_per_field" -q
  All should be GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency or re_resolution or resolved_drops_per_field" -q</automated>
  </verify>
  <done>All targeted tests GREEN. grep "def detect_field_regression" app/pipeline/validate.py exits 0. grep "prior_matches" app/pipeline/validate.py exits 0. grep "RawFieldDrop" app/pipeline/validate.py exits 0. Full validate test suite green: uv run pytest tests/test_validate.py -q.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: decide Rule 2b (C-1) + compose_email N5 field-regression line fix</name>
  <files>app/pipeline/decide.py, app/pipeline/compose_email.py</files>

  <read_first>
  Read app/pipeline/decide.py lines 110-135 for the full Rule 2 pattern. PATTERNS.md §4 has the exact 2-line addition for Rule 2b. The Decision model's missing_fields field is NOT widened — regressions feed gate_reasons only (PATTERNS.md §4 note).

  Read app/pipeline/compose_email.py lines 47-102 (the _template_body function). The N5 bug is at line 93: "if not decision.unresolved_names and not decision.missing_fields:" — the gate_reasons fallback ONLY fires when both lists are empty. This means a field_regression reason coexisting with a normal name clarification is DROPPED from the email body. Fix: emit field-regression lines UNCONDITIONALLY whenever gate_reasons contains entries matching "field regression:" — do NOT rely on the existing fallback gate.

  The email copy for the field-regression line (Claude's discretion per CONTEXT.md): must phrase the question so "yes, remove it" lands as an explicit 0 in re-extraction per D-14. Example: "  - We noticed that {field} was {original_value} in the original but is now absent or zero — did you mean to remove it? If so, please reply with {field}=0; if it should still be included, please provide the correct value."
  </read_first>

  <behavior>
  RED test to turn GREEN:
  - test_field_regression_issue_gates_to_clarification (test_decide.py): pass [ValidationIssue(issue_type="field_regression", field="alice.hours_overtime", message="regression")] to decide(extracted, matches, issues) → decision.final_action == "request_clarification" AND any("field_regression" in r for r in decision.gate_reasons). Currently fails because Rule 2 only checks issue_type == "missing".
  - New N5 test (add to test_validate.py or new test_compose_email.py): test_field_regression_line_present_when_other_clarification_coexists — build a Decision with unresolved_names=["Bob Smith"] AND gate_reasons=["field regression: alice.hours_overtime"]. Call _template_body(decision) and assert the result contains text about "hours_overtime" (the field-regression line) AND also contains "Bob Smith" (the name clarification line). Previously the field-regression question was dropped because unresolved_names was non-empty.
  Existing tests must stay GREEN.
  </behavior>

  <action>
  Change A — decide.py Rule 2b (per C-1, PATTERNS.md §4):
  In app/pipeline/decide.py, immediately after Rule 2 (lines ~123-125), add:
  A comment: # Rule 2b — field regression detected (D-17, C-1 resolution).
  Two lines mirroring Rule 2 exactly:
  regressions = [i.field for i in issues if i.issue_type == "field_regression"]
  gate_reasons += [f"field regression: {f}" for f in regressions]
  Do NOT change any other part of decide.py. The Decision.missing_fields field is NOT updated.

  Change B — compose_email.py N5 fix:
  In app/pipeline/compose_email.py, inside _template_body (lines 47-102):

  After the existing missing_fields block (around line 92), ADD a new unconditional block for field-regression lines BEFORE the "if not unresolved_names and not missing_fields" fallback:

  The new block iterates gate_reasons and for each reason starting with "field regression: ":
  - Extract the qualified field string from the reason (everything after "field regression: ").
  - Parse the submitted_name and bare field name from the qualified string (it has the form "submitted_name.field_name" — split on the last "." since field names never contain dots).
  - Append a line like: f"  - The field '{field_name}' for {submitted_name} was present in the original but is now absent or zero. If you meant to remove it, please reply confirming {field_name}=0. Otherwise, provide the correct value."

  Place this BEFORE the existing "if not decision.unresolved_names and not decision.missing_fields:" fallback block (so field-regression lines are emitted regardless of whether name or field clarifications coexist).

  The "if not unresolved_names and not missing_fields:" fallback block remains for other generic gate_reason entries that are not field_regression type.

  After the change, run:
  uv run pytest tests/test_decide.py -k "field_regression" -q  # must GREEN
  uv run pytest tests/ -k "field_regression_line" -q  # N5 test must GREEN
  </action>

  <verify>
    <automated>uv run pytest tests/test_decide.py -k "field_regression" -q 2>&1; uv run pytest tests/ -k "field_regression_line or compose" -q 2>&1 | tail -10</automated>
  </verify>
  <done>test_field_regression_issue_gates_to_clarification GREEN. N5 coexistence test GREEN. grep "Rule 2b" app/pipeline/decide.py exits 0. grep "field regression" app/pipeline/compose_email.py exits 0 (the new unconditional block). Full suite still green: uv run pytest tests/test_validate.py tests/test_reconcile.py tests/test_decide.py -q.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate() → detect_field_regression | Operates on typed Extracted objects (Pydantic-validated at extraction parse boundary); no raw string input |
| ValidationIssue(issue_type="field_regression") | Pydantic validates the Literal at construction; extra="forbid" on ValidationIssue |
| compose_email._template_body | gate_reasons contains only strings built by decide.py from typed ValidationIssue.field values — no raw user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | detect_field_regression — takes two typed Extracted objects | mitigate | Both Extracted objects are Pydantic-validated with extra="forbid" at parse time. RawFieldDrop also has extra="forbid" (Plan 01). No raw dict/string input; no injection vector. |
| T-07-06 | Information Disclosure | ValidationIssue(issue_type="field_regression") field contains submitted_name (PII) | accept | The field attribute uses {submitted_name}.{field} — same pattern as all existing ValidationIssue.field values (roster.py:93). Gate reason contains field name; operator-visible context is bounded. |
| T-07-07 | Denial of Service | Loop over _HOURS_FIELDS (5 fields) × employees (bounded by extraction) | accept | O(employees × 5 fields) — trivially bounded for a payroll email. No external input can expand the loop size beyond the extraction result. |
| T-07-N5 | Information Disclosure | compose_email field-regression line parses gate_reason strings | accept | gate_reasons are built from typed ValidationIssue.field values in decide.py (code-owned, not raw user input). The split-on-dot parsing operates on internal strings never from external input directly. |
| T-07-SC | Tampering | No new package installs | accept | Pure stdlib + existing Pydantic; no uv add invoked |
</threat_model>

<verification>
Full plan verification after both tasks:

1. uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency or re_resolution or resolved_drops_per_field" -q — all GREEN
2. uv run pytest tests/test_decide.py -k "field_regression" -q — GREEN
3. uv run pytest tests/ -k "field_regression_line or compose" -q — GREEN (N5 coexistence test)
4. uv run pytest tests/test_validate.py tests/test_reconcile.py -q — full suite GREEN (no regressions from Plan 02)
5. grep "def detect_field_regression" app/pipeline/validate.py — exits 0
6. grep "RawFieldDrop" app/pipeline/validate.py — exits 0
7. grep "Rule 2b" app/pipeline/decide.py — exits 0
8. grep "prior_matches" app/pipeline/validate.py — exits 0 (N6 D-11 fix)
9. grep "field regression" app/pipeline/compose_email.py — exits 0 (N5 fix, unconditional block)
</verification>

<success_criteria>
- detect_field_regression exists in validate.py with public signature detect_field_regression(original: Extracted, resumed: Extracted) -> list[RawFieldDrop] — returns RawFieldDrop (NOT FieldDrop); no sentinel UUID; no qualified field string (decision C)
- validate() accepts prior=None, prior_matches=None, and resolved_drops=None kwargs; existing callers with 3 positional args are unbroken (D-18)
- N6 ENFORCED: validate() maps RawFieldDrop via BOTH snapshot-era matches (prior_matches) AND current matches; re-resolutions (different employee_id in the two sets) are SKIPPED (D-11)
- N8 ENFORCED: resolved_drops suppression is per-(employee_id, field) pair in the field_regression emission path — NOT at the any_hours/employee level; MONEY-01 still fires for an employee whose only confirmed_dropped field doesn't satisfy any_hours
- N5 ENFORCED: _template_body emits field-regression lines unconditionally whenever gate_reasons contains "field regression:" entries, regardless of unresolved_names/missing_fields
- decide Rule 2b exists (2 lines after Rule 2): field_regression issues gate to request_clarification
- D-25 predicate-consistency: OT 2→0 and OT 2→absent produce identical RawFieldDrop
- All prior validate + reconcile + eval-wiring tests remain GREEN
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md` when done.
</output>
