---
phase: 07-money-correctness-deepening
plan: "03"
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - app/pipeline/validate.py
  - app/pipeline/decide.py
autonomous: true
requirements:
  - MONEY-03

must_haves:
  truths:
    - "detect_field_regression(original, resumed) exists as a pure 2-argument helper in validate.py — returns list[FieldDrop] iterating ALL five watched hours fields via _HOURS_FIELDS (hours_regular/overtime/vacation/sick/holiday; 401k+salary excluded, D-08) using _is_paid (per D-08/D-09/D-10/D-11)"
    - "validate() gains prior: Extracted | None = None kwarg and resolved_drops: set[tuple[UUID, str]] | None = None kwarg — fresh runs (prior=None) are a pure no-op (D-17/D-18)"
    - "decide.py Rule 2b gates request_clarification on issue_type='field_regression' issues (2 lines added after Rule 2, line ~125) — resolves C-1"
    - "OT 2→0 is detected as a drop identically to OT 2→absent (D-25 predicate-consistency, uses shared _is_paid)"
    - "explicit zero resumed_value=Decimal('0') is returned as FieldDrop with resumed_value=Decimal('0'), not None (D-26 explicit-drop signal for D-14)"
    - "no-op reply yields detect_field_regression([]) (D-27 determinism)"
  artifacts:
    - path: "app/pipeline/validate.py"
      provides: "detect_field_regression pure 2-arg helper + validate(prior=, resolved_drops=) seam"
      exports: ["detect_field_regression", "validate"]
      contains: "def detect_field_regression"
    - path: "app/pipeline/decide.py"
      provides: "Rule 2b for field_regression issues"
      contains: "field_regression"
  key_links:
    - from: "app/pipeline/validate.py:detect_field_regression"
      to: "app/pipeline/validate.py:validate (prior= kwarg)"
      via: "called inside validate() when prior is not None, before any_hours check"
      pattern: "detect_field_regression"
    - from: "app/pipeline/validate.py:validate"
      to: "app/pipeline/decide.py:Rule 2b"
      via: "ValidationIssue(issue_type='field_regression') emitted by validate → caught by Rule 2b"
      pattern: "field_regression"
---

<objective>
Add the MONEY-03 pure-function judgment layer: the `detect_field_regression` helper and the `validate(prior=)` seam in `validate.py`, plus Rule 2b in `decide.py`. These are the judgment-certifiable pieces (D-23 eval layer). The state machine wiring (snapshot write in `_clarify`, `clarified_fields` outcome tracking, backfill, orchestrator threading) is Plan 04.

Purpose: Establish the pure judgment path that the eval can certify directly (D-23). With Rule 2b in place, a `field_regression` ValidationIssue flowing out of `validate()` correctly gates to `request_clarification` — proving the detect→validate→decide chain is wired before the orchestrator state machine complexity is added.
Output: detect_field_regression helper, validate(prior=, resolved_drops=) seam, decide Rule 2b. All MONEY-03 unit tests in test_validate.py and test_decide.py turn GREEN.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-01-SUMMARY.md
@.planning/phases/07-money-correctness-deepening/07-02-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/pipeline/validate.py
@app/pipeline/decide.py
@app/models/contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detect_field_regression helper + validate(prior=, resolved_drops=) seam</name>
  <files>app/pipeline/validate.py</files>

  <read_first>
  Read app/pipeline/validate.py (full file — already modified in Plan 02; capture current state). Key anchors per RESEARCH.md + PATTERNS.md:
  - Line ~27-33: `_HOURS_FIELDS` tuple — `detect_field_regression` MUST reuse this, not redefine it (DRY, D-09).
  - `_is_paid` predicate added in Plan 02 — reuse it in detect_field_regression.
  - Lines 69-73: current `validate()` signature — add `prior: Extracted | None = None` and `resolved_drops: set[tuple[UUID, str]] | None = None` as keyword-only params after `matches`. Keep default=None so every existing caller (orchestrator.py:273, run_eval.py:159) is unbroken (D-18).
  - PATTERNS.md §1 "New helper placement": place `detect_field_regression` between `_employee_pay_periods_per_year` and `validate()`.
  - app/models/contracts.py — confirm FieldDrop is imported (added in Plan 01). Add `FieldDrop` to the import if not present.
  - CONTEXT.md D-11 locks the diff keyed by employee_id (resolved in both snapshots). D-10 locks trigger = present→absent/zero only (not increases). D-12 locks last-wins reduction for multi-line.
  </read_first>

  <behavior>
  RED tests to turn GREEN (from Plan 01 scaffold):
  - test_detect_regression_ot_absent: `detect_field_regression(original_extracted, resumed_extracted)` where original has `hours_overtime=Decimal("2")` and resumed has `hours_overtime=None` → returns list with exactly one FieldDrop for "hours_overtime" with `original_value=Decimal("2")` and `resumed_value=None`.
  - test_explicit_drop_zero_resumed_value (D-26): resumed has `hours_overtime=Decimal("0")` → FieldDrop with `resumed_value=Decimal("0")`. Key: `_is_paid(Decimal("0"))=False` means it is a drop; but `resumed_value` carries the actual value so D-14 can distinguish silence from explicit zero.
  - test_no_regression_noop (D-27): original and resumed identical → `detect_field_regression` returns `[]`.
  - test_predicate_consistency (D-25): `OT=2→0` and `OT=2→absent` both detected as drops (both produce FieldDrop); NOT detected as drops when OT goes `2→3` (an increase while paid).

  validate() seam behavior:
  - Fresh run (`prior=None`): `validate(extracted, roster, matches)` (3 args, no prior) behaves identically to today — no field_regression issue emitted. Existing tests must stay GREEN (no regression).
  - Resume with prior (`prior=some_extracted`): `validate(extracted, roster, matches, prior=prior)` calls `detect_field_regression(prior, extracted)` and emits `ValidationIssue(issue_type="field_regression", ...)` for each drop in the returned list.
  - resolved_drops kwarg: employees/fields in resolved_drops are SKIPPED in the `any_hours` gate (D-15). Default=None/empty set is a no-op.
  </behavior>

  <action>
  Step 1 — Add `detect_field_regression` function (per D-10/D-11/D-12, PATTERNS.md §1):

  The PUBLIC signature is exactly: `detect_field_regression(original: Extracted, resumed: Extracted) -> list[FieldDrop]` — TWO arguments only. No third argument. `validate()` builds the `name_to_id` mapping INTERNALLY from `matches` and does NOT pass it into `detect_field_regression`.

  Place it between `_employee_pay_periods_per_year` and `validate()` in validate.py.

  Inside `detect_field_regression`:
  - Build `{submitted_name: ExtractedEmployee}` dicts for both `original.employees` and `resumed.employees` using last-wins reduction (D-12): for each list, iterate in order — later entries for the same `submitted_name` overwrite earlier ones.
  - The function keys the diff by `submitted_name` (not employee_id) because it only has access to the two `Extracted` objects and their `submitted_name` fields. Employee_id is resolved by `validate()` from `matches` — that mapping stays in `validate()`, not here.
  - For each `submitted_name` present in BOTH dicts (iterate the names in sorted order for determinism, D-27), for each `field` in `_HOURS_FIELDS`:
    - `original_val = getattr(original_emp, field)` and `resumed_val = getattr(resumed_emp, field)`
    - If `_is_paid(original_val) and not _is_paid(resumed_val)` → this is a drop.
    - Set `field=f"{submitted_name}.{field}"` on the `FieldDrop` so the originating `submitted_name` travels WITH the drop inside the existing 4-field model (no new model field needed). Append `FieldDrop(employee_id=uuid.UUID(int=0), field=f"{submitted_name}.{field}", original_value=original_val, resumed_value=resumed_val)`.

  LOCKED DESIGN (the single design — keeps the 2-arg public API clean, keeps Plan 01's 4-field `FieldDrop` model UNCHANGED, and makes the RED tests pass with the exact signature `detect_field_regression(original, resumed)`):

  `detect_field_regression(original: Extracted, resumed: Extracted) -> list[FieldDrop]` returns `FieldDrop` objects whose `employee_id` is the placeholder sentinel `uuid.UUID(int=0)` and whose `field` is the qualified `"{submitted_name}.{hours_field}"` string — so the originating name travels inside the existing model with NO new field. `validate()` then splits `field` on the first `.` to recover `submitted_name`, resolves the real UUID from `matches`, and reconstructs the final `FieldDrop` (resolved `employee_id`, bare `field`) before emitting any issue. The sentinel UUID never escapes `validate()`. Do NOT add a `name_to_id` third parameter to `detect_field_regression`, and do NOT add a `submitted_name` field to `FieldDrop` — the mapping lives entirely inside `validate()` and the model stays exactly as Plan 01 defines it (`employee_id`, `field`, `original_value`, `resumed_value`).

  This means the RED tests in test_validate.py that call `detect_field_regression(original, resumed)` directly receive `FieldDrop` objects with `employee_id=uuid.UUID(int=0)` and a qualified `field` like `"José Martínez.hours_overtime"` — acceptable for the unit tests, which assert on the `hours_field` portion of `field`, `original_value`, `resumed_value`, and list length, not on `employee_id`. (Plan 01 Task 2 `test_detect_regression_ot_absent` asserts the drop is "for `field='hours_overtime'`" — it must match on the suffix after the `.`, i.e. `drop.field.endswith("hours_overtime")` or `drop.field.split(".")[-1] == "hours_overtime"`.)

  Add `from uuid import UUID` to validate.py imports if not already present (needed for `resolved_drops` type hint). Add `FieldDrop` to the `from app.models.contracts import ...` line.

  Step 2 — Add `prior=None` and `resolved_drops=None` kwargs to `validate()` (per D-17/D-18):
  Change the signature to:
  ```
  def validate(
      extracted: Extracted,
      roster: Roster,
      matches: list[NameMatchResult],
      *,
      prior: Extracted | None = None,
      resolved_drops: set[tuple[UUID, str]] | None = None,
  ) -> list[ValidationIssue]:
  ```
  Inside `validate()`, after the existing validation logic and before the return:
  - Build `resolved_drops = resolved_drops or set()` at the start of the function body.
  - D-15 `resolved_drops` guard: inside the `any_hours` loop, after `_employee_pay_type` check, skip MONEY-01 gate for employee/field if `(resolved_emp_id, field)` pair is in `resolved_drops`. Build `emp_id_from_matches: UUID | None` from the `matches` list (lookup by `submitted_name`) to form the tuple key.
  - When `prior is not None`: build `name_to_id = {m.submitted_name: m.matched_employee_id for m in matches if m.resolved and m.matched_employee_id is not None}`, call `raw_drops = detect_field_regression(prior, extracted)` (the 2-arg form — `name_to_id` is NOT passed in). For each `raw_drop`: split `raw_drop.field` on the first `.` into `(submitted_name, hours_field)`, look up `submitted_name` in `name_to_id` to get the real `employee_id`, and reconstruct the final `FieldDrop(employee_id=<resolved>, field=hours_field, original_value=..., resumed_value=...)` (replacing the `uuid.UUID(int=0)` sentinel and the qualified field) per the LOCKED DESIGN above. Skip any drop whose `submitted_name` is not in `name_to_id` (unresolved employee — no baseline per D-11).
  - Emit `ValidationIssue(issue_type="field_regression", field=f"{submitted_name}.{drop_field}", message=f"field regression: {drop_field} was {original_value}, now {'absent' if resumed_value is None else resumed_value}")` for each resolved drop.

  D-20 termination guarantee: when `prior is not None` on the second resume pass (called from Plan 04's second `_run_stages` invocation with `prior=None`), detect does not run — no stray `field_regression` issue can re-trigger clarification. This halt mechanism is enforced in Plan 04's orchestrator wiring, not here.

  Run `uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency" -q` to confirm the 4 targeted tests turn GREEN.
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency" -q</automated>
  </verify>
  <done>All 4 targeted tests GREEN. `grep "def detect_field_regression" app/pipeline/validate.py` exits 0. `grep "prior" app/pipeline/validate.py | grep "def validate"` shows the new signature. Existing validate tests still pass: `uv run pytest tests/test_validate.py -q`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: decide.py Rule 2b for field_regression issues (C-1 resolution)</name>
  <files>app/pipeline/decide.py</files>

  <read_first>
  Read app/pipeline/decide.py lines 110-135 to see the full Rule 2 pattern (`missing = [i.field for i in issues if i.issue_type == "missing"]` at line ~123-125). PATTERNS.md §4 has the exact 2-line addition to copy. CONTEXT.md D-17 locks this as "Option B" (C-1 orchestrator resolution). The `Decision` model's `missing_fields` field is NOT widened — regressions feed `gate_reasons` only (PATTERNS.md §4 note).
  </read_first>

  <behavior>
  RED test to turn GREEN:
  - test_field_regression_issue_gates_to_clarification (test_decide.py): pass `[ValidationIssue(issue_type="field_regression", field="alice.hours_overtime", message="regression")]` to `decide(extracted, matches, issues)` → `decision.final_action == "request_clarification"` and `any("field_regression" in r for r in decision.gate_reasons)`. Currently fails because Rule 2 only checks `issue_type == "missing"`.
  Existing tests in tests/ that call decide() must stay GREEN.
  </behavior>

  <action>
  In app/pipeline/decide.py, immediately after Rule 2 (lines 123-125), add Rule 2b — exactly 2 lines:

  After the existing block:
  ```
  # Rule 2 — missing required field.
  missing = [i.field for i in issues if i.issue_type == "missing"]
  gate_reasons += [f"missing required field: {f}" for f in missing]
  ```

  Add:
  ```
  # Rule 2b — field regression detected (D-17, C-1 resolution: decide.py needed 2 new lines).
  regressions = [i.field for i in issues if i.issue_type == "field_regression"]
  gate_reasons += [f"field regression: {f}" for f in regressions]
  ```

  That is the complete change to decide.py. The `Decision.missing_fields` field is NOT updated — regressions surface via `gate_reasons` only (per PATTERNS.md §4). Do NOT widen any other part of the decide logic.
  </action>

  <verify>
    <automated>uv run pytest tests/test_decide.py -k "field_regression" -q</automated>
  </verify>
  <done>test_field_regression_issue_gates_to_clarification is GREEN. `grep "Rule 2b" app/pipeline/decide.py` exits 0. `grep "field_regression" app/pipeline/decide.py` exits 0. Full suite still green: `uv run pytest tests/test_validate.py tests/test_reconcile.py tests/test_decide.py -q`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validate() → detect_field_regression | Operates on typed Extracted objects (Pydantic-validated at extraction parse boundary); no raw string input crossing here |
| ValidationIssue(issue_type="field_regression") | Pydantic validates the Literal at construction; extra="forbid" on ValidationIssue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | `detect_field_regression` — takes two typed `Extracted` objects | mitigate | Both `Extracted` objects are Pydantic-validated with `extra="forbid"` at parse time. `FieldDrop` also has `extra="forbid"` (Plan 01). No raw dict/string input; no injection vector. |
| T-07-06 | Information Disclosure | `ValidationIssue(issue_type="field_regression")` message contains `original_value` (a Decimal, not a name) | accept | The message contains only numbers and field names (e.g., "hours_overtime was 2, now absent"). No PII (submitted_name is in the `field` attribute following existing roster.py:93 pattern, not the message). PII-safe logging at orchestrator.py:239 unchanged. |
| T-07-07 | Denial of Service | Loop over `_HOURS_FIELDS` (5 fields) × employees (bounded by extraction) | accept | Loop is O(employees × 5 fields) — trivially bounded for a payroll email (< 50 employees). No external input can expand the loop size beyond the extraction result. |
| T-07-SC | Tampering | No new package installs | accept | Pure stdlib + existing Pydantic; no `uv add` invoked |
</threat_model>

<verification>
Full plan verification after both tasks:

1. `uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression" -q` — all GREEN
2. `uv run pytest tests/test_decide.py -k "field_regression" -q` — GREEN
3. `uv run pytest tests/test_validate.py tests/test_reconcile.py -q` — full suite GREEN (no regressions from Plan 02)
4. `grep "def detect_field_regression" app/pipeline/validate.py` — exits 0
5. `grep "Rule 2b" app/pipeline/decide.py` — exits 0
6. `grep "prior" app/pipeline/validate.py | grep -c "def validate"` — > 0 (signature updated)
</verification>

<success_criteria>
- detect_field_regression helper exists in validate.py with PUBLIC signature of exactly 2 arguments: detect_field_regression(original: Extracted, resumed: Extracted) -> list[FieldDrop]; validate() builds the name_to_id mapping internally from matches and does not pass it into detect_field_regression
- validate() accepts `prior=None` and `resolved_drops=None` kwargs; existing callers with 3 positional args are unbroken
- decide Rule 2b exists (2 lines after Rule 2): `field_regression` issues now gate to `request_clarification`
- D-25 predicate-consistency: OT 2→0 and OT 2→absent produce identical behavior
- All prior validate + reconcile + eval-wiring tests remain GREEN
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md` when done.
</output>
