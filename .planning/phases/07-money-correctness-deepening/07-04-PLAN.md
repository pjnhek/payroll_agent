---
phase: 07-money-correctness-deepening
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so new JSONB columns (pre_clarify_extracted, clarified_fields) AND the updated purpose CHECK constraint exist before integration tests run"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Settings → Database → Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local .env and shell env"
        location: "local .env file"

must_haves:
  truths:
    - "StageResult dataclass/NamedTuple exists with fields: extracted: Extracted, matches: list[NameMatchResult], issues: list[ValidationIssue], decision: Decision, clarify_deferred: bool — _run_stages returns it, BOTH callers updated (N1 fix)"
    - "N1 FIXED: resume_pipeline calls _run_stages EXACTLY ONCE per invocation. The alias-diff STEP B (formerly a second _run_stages call) is REFACTORED to reuse the StageResult from the single call. Pre-resume resolved-id set is captured BEFORE the single _run_stages call (STEP A runs first); post-resume set comes FROM StageResult.matches. No second _run_stages call anywhere in resume_pipeline."
    - "N2 FIXED: clarified_fields[emp_id][field]='asked' is written BEFORE the field-regression clarification is sent. clarify_deferred=True in StageResult signals that _run_stages skipped _clarify for all-field_regression decisions; resume_pipeline then writes 'asked' DURABLY (set_clarified_fields) BEFORE calling _clarify with purpose='clarification_field_regression'."
    - "N3 FIXED: Round-2 outcome classification uses the RAW resumed extraction (StageResult.extracted BEFORE any backfill) to distinguish confirmed_dropped (explicit Decimal('0')) from carried_forward (absent/None). Backfill is applied ONLY to carried_forward fields AFTER classification. The outcome determines whether backfill runs at all for each field."
    - "N4 FIXED: schema.sql has an EXPLICIT DO $$ migration block that DROPS the existing email_messages purpose CHECK constraint (by introspecting its name from pg_constraint) and RE-ADDS it with the expanded value list ('clarification','confirmation','clarification_field_regression'). The inline CREATE TABLE check at line 147 is also updated. Bootstrap applies this. An existing DB that previously had only ('clarification','confirmation') now accepts 'clarification_field_regression' inserts."
    - "N7 FIXED: set_pre_clarify_extracted is called BEFORE EACH of the THREE AWAITING_REPLY paths in _clarify — the idempotency early-return path (~line 325), the record_only/collision path (~line 438), and the live path (~line 451). The IS NULL guard in SQL makes all three calls idempotent (only the first write succeeds — snapshot-once D-19)."
    - "payroll_runs has pre_clarify_extracted JSONB column (nullable, D-19) applied to dev DB via bootstrap"
    - "payroll_runs has clarified_fields JSONB column (nullable, D-13) applied to dev DB via bootstrap"
    - "email_messages.purpose CHECK includes 'clarification_field_regression' on BOTH fresh AND existing DBs"
    - "get_outbound_message_id() allowlist includes 'clarification_field_regression'"
    - "Four new repo helpers exist: set_pre_clarify_extracted (IS NULL CAS), load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields"
    - "_clarify() accepts a purpose kwarg (default 'clarification'); uses purpose variable for all three hardcoded 'clarification' strings inside it"
    - "ROUND 1 (partial reply, field regression detected): resume_pipeline — STEP A (pre-state capture) FIRST → single _run_stages call → inspects StageResult.clarify_deferred=True → writes clarified_fields[emp_id][field]='asked' DURABLY (BEFORE triggering the clarification path) → calls _clarify with purpose='clarification_field_regression' → sets AWAITING_REPLY → RETURNS. Does not proceed to alias-diff or calc."
    - "ROUND 2 (client answers 'did you forget OT?'): resume_pipeline — STEP A FIRST → single _run_stages call → detects 'asked' entries in clarified_fields → classifies outcomes from RAW StageResult.extracted (before backfill) → backfills carried_forward values into extracted AFTER classification → proceeds normally through validate/decide/calc without re-calling _run_stages"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields inline + ALTER blocks; email_messages purpose CHECK expanded via DROP/RE-ADD migration (N4 fix)"
      contains: "clarification_field_regression"
    - path: "app/db/repo.py"
      provides: "4 new repo helpers; get_outbound_message_id allowlist expanded to 'clarification_field_regression'"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "StageResult (with clarify_deferred field) + _run_stages returns StageResult + BOTH callers updated + _clarify snapshot on all 3 AWAITING_REPLY paths (N7) + resume_pipeline single-call + N2 asked-before-send + N3 classify-from-raw-before-backfill"
      contains: "StageResult"
  key_links:
    - from: "app/pipeline/orchestrator.py:_run_stages"
      to: "app/pipeline/orchestrator.py:resume_pipeline"
      via: "returns StageResult with clarify_deferred; resume_pipeline uses StageResult.matches for alias-diff (no second _run_stages call)"
      pattern: "StageResult"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 1)"
      to: "app/db/repo.py:set_clarified_fields"
      via: "writes 'asked' BEFORE calling _clarify (N2 fix); triggered by clarify_deferred=True"
      pattern: "set_clarified_fields"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 2)"
      to: "app/pipeline/orchestrator.py (classify from raw)"
      via: "classify outcomes from StageResult.extracted BEFORE backfill; backfill only carried_forward fields; proceed to existing calc path"
      pattern: "confirmed_dropped"
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called BEFORE EACH of the 3 AWAITING_REPLY set_status calls (N7 fix); IS NULL guard in SQL = snapshot-once"
      pattern: "set_pre_clarify_extracted"
---

<objective>
Wire the MONEY-03 state machine with a structural refactor that fixes all six Round-2 HIGH findings at their root.

THE STRUCTURAL FIX (user accepted): Refactor _run_stages to RETURN a StageResult dataclass instead of returning None. StageResult carries: extracted, matches, issues, decision, clarify_deferred — everything the caller needs to inspect what happened WITHOUT re-running the pipeline. Keep all existing side effects (persist_extracted/decision/reconciliation + the process/clarify branch). Both callers are updated.

N1 fix: resume_pipeline calls _run_stages EXACTLY ONCE. The alias-diff STEP B (a second _run_stages call in the original code) is refactored to reuse StageResult.matches instead of re-running. Pre-resume set captured before the single call (STEP A runs FIRST, before the detection block and before _run_stages); post-resume set from StageResult.matches.

N2 fix: clarified_fields "asked" is written BEFORE the field-regression send. In _run_stages, when all request_clarification issues are field_regression type, _run_stages sets clarify_deferred=True and does NOT call _clarify. resume_pipeline inspects StageResult.clarify_deferred, writes clarified_fields first, THEN calls _clarify with purpose="clarification_field_regression".

N3 fix: Round-2 outcomes are classified from the RAW StageResult.extracted (before any backfill). Only after classifying each asked field as carried_forward (absent/None in raw) or confirmed_dropped (explicit Decimal("0") in raw) does backfill run — and only for carried_forward fields. confirmed_dropped fields skip backfill entirely.

N4 fix: schema.sql gets an explicit DO $$ migration block that introspects pg_constraint to find and DROP the existing email_messages purpose CHECK by name, then RE-ADDS it with the expanded value list. ADD COLUMN IF NOT EXISTS alone is not enough for an existing DB.

N7 fix: set_pre_clarify_extracted (with IS NULL guard) is called before EACH of the three set_status(AWAITING_REPLY) paths in _clarify — the idempotency early-return, the record_only path, and the live path.

Architecture after this plan:
- ROUND 1 call to resume_pipeline: claim_status → load → combined_email → STEP A (pre-state capture: _pre_candidates, _pre_reconciliation, _pre_resolved_ids) → load snapshot + clarified → _run_stages ONCE → inspect StageResult.clarify_deferred → if True: write asked → call _clarify(purpose="clarification_field_regression") → RETURN.
- ROUND 2 call to resume_pipeline: claim_status → load → combined_email → STEP A (pre-state capture) → load snapshot + clarified → detect "asked" entries → _run_stages ONCE → classify outcomes from raw StageResult.extracted BEFORE backfill → backfill carried_forward into extracted → update clarified dict → set_clarified_fields → proceed to alias-diff (STEP C/D using stage_result.matches) + normal flow.

NO second _run_stages call. NO post-backfill re-detection. NO blunt prior=None suppression.
STEP A ALWAYS runs before the detection block and before _run_stages — this is the invariant that makes pre-vs-post diff correct.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/db/schema.sql
@app/db/repo.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema DDL (N4 DROP/RE-ADD migration) + repo helpers + purpose allowlist</name>
  <files>app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/db/schema.sql in full. Key anchors:
  - payroll_runs inline: alias_candidates column at ~line 90; ALTER block at ~lines 105-108 — pattern to mirror for two new columns.
  - email_messages CREATE TABLE block ~lines 134-158: the purpose CHECK constraint at line 147 is CHECK (purpose IN ('clarification','confirmation')). This INLINE check applies to FRESH DBs. The ALTER TABLE at line 164-165 adds the column to EXISTING DBs with the SAME old check.
  - The DO $$ block at lines 173-184 adds the UNIQUE constraint idempotently — THIS IS THE PATTERN TO COPY for the N4 DROP/RE-ADD CHECK constraint migration.
  - N4 critical: an existing DB has a purpose column with an old CHECK constraint. The constraint name may be auto-generated (e.g. email_messages_purpose_check) or named. The migration must introspect pg_constraint to find the correct name and drop it.

  Read app/db/repo.py lines 85-100 (imports + RUN_COLS), lines 340-375 (claim_status CAS pattern), lines 405-430 (persist_extracted serializer), lines 505-530 (set_alias_candidates — exact template), lines 659-686 (get_outbound_message_id — THE ALLOWLIST TO UPDATE).

  PATTERNS.md §5 has the SQL for the two new payroll_runs columns. PATTERNS.md §6 has all four repo helper implementations.

  CRITICAL N4 fix: The DO $$ idempotency pattern is the only correct way to modify a named constraint on an existing DB. The block must:
  1. DROP the existing CHECK constraint on email_messages.purpose (by introspecting its actual name from pg_constraint — do not hardcode a name; look for contype='c' AND conrelid='email_messages'::regclass AND conname LIKE '%purpose%' OR inspect for the constraint that references the purpose column).
  2. ADD CONSTRAINT with the new expanded CHECK.
  The block must be idempotent: if the new constraint already exists, skip. Structure:
  - DROP IF EXISTS the old purpose check (by the introspected name).
  - ADD CONSTRAINT with the new value list.
  Because Postgres auto-names inline CHECKs as {table}_{column}_check, the old constraint is likely named "email_messages_purpose_check". Use a DO $$ block that looks up the constraint name from pg_constraint rather than hardcoding, to be safe.
  </read_first>

  <action>
  Step 1 — Update app/db/schema.sql:

  a. In the CREATE TABLE IF NOT EXISTS payroll_runs block, after alias_candidates JSONB, (~line 90), add two new columns (inline for fresh DB):
     pre_clarify_extracted JSONB,    -- D-19: snapshot of extracted_data at awaiting_reply pause (MONEY-03)
     clarified_fields JSONB,          -- D-13: {employee_id: {field: outcome}} field-regression outcomes (MONEY-03)

  b. In the idempotent ALTER TABLE block for payroll_runs (after the alias_candidates ALTER at ~line 107), add:
     ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03
     ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields JSONB;       -- D-13 MONEY-03

  c. In the CREATE TABLE IF NOT EXISTS email_messages block at line 147, update the purpose inline check:
     Change: purpose TEXT CHECK (purpose IN ('clarification','confirmation'))
     To: purpose TEXT CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'))

  d. In the idempotent ALTER TABLE for email_messages (lines ~164-165), update the purpose ADD COLUMN check to match:
     Change: ALTER TABLE email_messages ADD COLUMN IF NOT EXISTS purpose TEXT CHECK (purpose IN ('clarification','confirmation'));
     To: ALTER TABLE email_messages ADD COLUMN IF NOT EXISTS purpose TEXT CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'));

  e. N4 FIX — Add a new DO $$ block after the existing purpose ALTER (after line 165, before the uq_email_run_purpose DO $$ block) to DROP the old CHECK constraint and RE-ADD the expanded one on EXISTING DBs:
     The block structure:
     - Declare a variable for the old constraint name.
     - SELECT conname FROM pg_constraint WHERE contype = 'c' AND conrelid = 'email_messages'::regclass AND conname LIKE '%purpose%' into the variable.
     - If found: ALTER TABLE email_messages DROP CONSTRAINT <name>.
     - Check if a constraint with the new expected name already exists (in case bootstrap ran twice).
     - ALTER TABLE email_messages ADD CONSTRAINT email_messages_purpose_check CHECK (purpose IN ('clarification','confirmation','clarification_field_regression')).
     Make the ADD idempotent by checking pg_constraint first (same pattern as the uq_email_run_purpose block).

  Step 2 — Add four repo helpers to app/db/repo.py after set_alias_candidates (~line 524). Follow the exact transaction/connection pattern from set_alias_candidates and claim_status. Always %s placeholders, never f-string SQL (V5 security rule). Always str(run_id) for UUID params. Always json.dumps(obj) for JSONB writes.

  a. set_pre_clarify_extracted(run_id, extracted, conn=None) -> bool:
     SQL: UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id
     Serialize: json.dumps(extracted.model_dump(mode="json"))
     Returns True if written, False if already set (snapshot-once, D-19).

  b. load_pre_clarify_extracted(run_id, conn=None) -> Extracted | None:
     SQL: SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s
     If NULL: return None. Deserialize: Extracted.model_validate(json.loads(row[0]) if isinstance(row[0], str) else row[0]).

  c. set_clarified_fields(run_id, clarified: dict, conn=None) -> None:
     SQL: UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s
     Serialize: json.dumps(clarified).

  d. load_clarified_fields(run_id, conn=None) -> dict:
     SQL: SELECT clarified_fields FROM payroll_runs WHERE id = %s
     Returns {} on NULL. Deserialize: json.loads(row[0]) if isinstance(row[0], str) else row[0].

  Import Extracted in repo.py if not already imported at module level. Check imports block at ~lines 85-100. If Extracted is only importable at module level without circular import issues (verify: does repo.py currently import from app.models.contracts?), add it there. If circular: use a TYPE_CHECKING import or local import inside the function — follow the existing pattern for any conditional imports in repo.py.

  Step 3 — Expand get_outbound_message_id() allowlist (repo.py ~line 670):
  Change: if purpose not in ("clarification", "confirmation"):
  To: if purpose not in ("clarification", "confirmation", "clarification_field_regression"):
  Update the docstring to note the new purpose value.

  Step 4 — Apply schema to dev DB:
  uv run python -m app.db.bootstrap
  If DATABASE_URL is not set: skip bootstrap and note as pending in SUMMARY. Must be applied before Plan 05 integration tests.
  </action>

  <verify>
    <automated>
    uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; print('imports OK')" && \
    python3 -c "
    import os, subprocess
    if not os.environ.get('DATABASE_URL'):
        print('DATABASE_URL not set — skipping live DB constraint verify (run bootstrap + insert test manually before phase gate)')
    else:
        # Run bootstrap
        subprocess.run(['uv', 'run', 'python', '-m', 'app.db.bootstrap'], check=True)
        # Verify the DO \$\$ migration applied: insert with new purpose and rollback
        import psycopg, uuid
        conn = psycopg.connect(os.environ['DATABASE_URL'])
        try:
            cur = conn.cursor()
            # Check constraint lists the new value
            cur.execute(\"SELECT check_clause FROM information_schema.check_constraints WHERE constraint_name LIKE '%purpose%' OR constraint_name LIKE '%email_messages%'\")
            rows = cur.fetchall()
            assert any('clarification_field_regression' in str(r) for r in rows), f'N4 constraint not in check_clause: {rows}'
            # Prove a live INSERT succeeds and roll it back
            test_id = str(uuid.uuid4())
            cur.execute(\"INSERT INTO email_messages (id, run_id, direction, message_id, purpose) VALUES (%s, NULL, 'outbound', %s, 'clarification_field_regression')\", (test_id, '<n4-verify@test.local>'))
            conn.rollback()
            print('N4 constraint verify PASSED — insert with clarification_field_regression succeeded (rolled back)')
        except Exception as e:
            conn.rollback()
            raise AssertionError(f'N4 constraint verify FAILED: {e}') from e
        finally:
            conn.close()
    "
    </automated>
  </verify>
  <done>Four repo helpers import. grep "pre_clarify_extracted JSONB" app/db/schema.sql exits 0. grep "clarified_fields JSONB" app/db/schema.sql exits 0. grep "clarification_field_regression" app/db/schema.sql exits 2+ occurrences (inline check + ALTER + DO $$ migration). grep "clarification_field_regression" app/db/repo.py exits 0 (allowlist). The DO $$ N4 migration block exists in schema.sql. When DATABASE_URL is set: bootstrap runs without error AND the inline INSERT test (rolled back) with purpose='clarification_field_regression' succeeds — confirming the CHECK constraint accepted the new value. When DATABASE_URL is absent: a clear note is printed and bootstrap is flagged as pending for the phase gate checkpoint.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StageResult + _run_stages refactor (incl. Change D clarify_deferred branching) + _clarify N7 snapshot-on-all-3-paths + purpose kwarg</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py in full — this is a complete refactor of _run_stages and partial refactor of _clarify. Key anchors:
  - _run_stages lines 260-289: current signature def _run_stages(run_id, email, roster, *, llm) -> None. Current callers: run_pipeline line 91 (_run_stages(run_id, email, roster, llm=llm)); resume_pipeline line 165 (_run_stages(run_id, combined_email, roster, llm=llm)).
  - _clarify lines 292-451: THREE set_status(AWAITING_REPLY) paths — idempotency early-return at line ~325 (existing_clari is not None), record_only path at line ~438, live path at line ~451.
  - imports block lines 35-57: existing imports; confirm NameMatchResult, ValidationIssue, Decision are importable for StageResult type hints.

  The StageResult is a lightweight named container. Use a dataclass or NamedTuple (NOT Pydantic) since it is an internal return value, not a serialized model. A dataclass with __slots__=False is fine. It holds five fields.

  N7 critical: _clarify currently writes pre_clarify_extracted only around the two AWAITING_REPLY paths identified in prior plans, but the idempotency early-return at line ~325 also calls set_status(AWAITING_REPLY) and RETURNS — the snapshot-write was missed there. Fix: call set_pre_clarify_extracted(run_id, extracted) BEFORE ALL THREE set_status(AWAITING_REPLY) calls. Since _clarify now receives extracted as a parameter (needed for the snapshot write), thread it in.

  CRITICAL — _clarify purpose kwarg: the existing hardcoded "clarification" strings inside _clarify are at three locations:
  1. get_outbound_message_id(run_id, purpose="clarification") at line ~318
  2. purpose="clarification" in repo.insert_email_message(...) at line ~435
  3. purpose="clarification" in gateway.send_outbound(...) at line ~449
  Replace all three with the purpose variable (default "clarification").

  Note: _run_stages' callers: run_pipeline calls _run_stages and ignores the return (existing behavior, fine). resume_pipeline MUST capture the return and use StageResult — this is the N1 fix.
  </read_first>

  <behavior>
  Add to tests/test_resume_pipeline.py (replacing/augmenting the xfail stubs where relevant):
  - test_run_stages_returns_stage_result: import StageResult from orchestrator; monkeypatch extract/reconcile_names/validate/decide to return known values; call _run_stages(...); assert the return is a StageResult with the expected extracted, matches, issues, decision fields.
  - test_resume_pipeline_calls_run_stages_exactly_once: monkeypatch _run_stages to count calls (using a counter); call resume_pipeline with a run in AWAITING_REPLY state (use a mock run + DB-free setup if possible, or mark skipif no DB). Assert _run_stages was called exactly once. This is the N1 invariant test.
  - test_clarify_deferred_true_when_all_issues_are_field_regression (BRANCHING behavior for Change D): build a mock that returns issues=[ValidationIssue(issue_type="field_regression", ...)] (all field_regression, no other types). Monkeypatch validate to return those issues and decide to return final_action="request_clarification". Call _run_stages(...). Assert: returned StageResult has clarify_deferred=True AND _clarify was NOT called (verify via a spy/monkeypatch on _clarify that records whether it was invoked).
  - test_clarify_deferred_false_when_mixed_issues (BRANCHING behavior for Change D, negative case): build issues that mix field_regression with a normal "missing" type. Assert: returned StageResult has clarify_deferred=False AND _clarify WAS called (normal path).
  These tests can be unit-style (no live DB) if _run_stages and resume_pipeline are mockable. Add them as regular tests (not xfail) with skipif(not DATABASE_URL) only for the DB-dependent parts.
  </behavior>

  <action>
  Change A — Define StageResult near the top of orchestrator.py (after the imports, before run_pipeline):

  Add a dataclass definition:
  from dataclasses import dataclass
  (add this to the import block)

  After the logger line: define StageResult as a plain dataclass (not Pydantic) with FIVE fields:
  - extracted: Extracted
  - matches: list (type annotation from NameMatchResult — import NameMatchResult from app.pipeline.reconcile_names or app.models.roster as appropriate; check existing imports)
  - issues: list (ValidationIssue from app.models.roster)
  - decision: Decision (from app.pipeline.decide — check if Decision is already imported)
  - clarify_deferred: bool = False

  Use from __future__ import annotations (already present) so forward refs work. The dataclass needs no __eq__ or frozen — it is a transient return value.

  Change B — Refactor _run_stages to return StageResult:

  1. Add three new keyword-only kwargs after *, llm: prior=None, prior_matches=None, resolved_drops=None. Thread them through to the validate() call.
  2. Change validate call to: issues = validate(extracted, roster, matches, prior=prior, prior_matches=prior_matches, resolved_drops=resolved_drops).
  3. Thread extracted to _clarify: change the _clarify call to _clarify(run_id, email, decision, roster, extracted, llm=llm, purpose=_clarify_purpose). The purpose is determined in Change D below.
  4. Change return type from -> None to -> StageResult and add return StageResult(extracted=extracted, matches=matches, issues=issues, decision=decision, clarify_deferred=clarify_deferred) at the END of the function body (after the branch). Both branches — process AND clarify — must reach this return.
  5. Update the run_pipeline call site (inside _run): change _run_stages(run_id, email, roster, llm=llm) to _ = _run_stages(run_id, email, roster, llm=llm) (silently discards the StageResult — existing behavior unchanged).

  Change C — Update _clarify: add extracted parameter + purpose kwarg + N7 snapshot-on-all-3-paths:

  1. Add extracted: Extracted as a positional parameter before *, llm: def _clarify(run_id, email, decision, roster, extracted, *, llm, purpose="clarification") -> None.
  2. Replace all three hardcoded "clarification" strings inside _clarify with the purpose variable (see Read_first for the three locations).
  3. N7 fix — add set_pre_clarify_extracted(run_id, extracted) call IMMEDIATELY BEFORE each of the three set_status(AWAITING_REPLY) calls:
     a. Before the idempotency early-return set_status at line ~325: add repo.set_pre_clarify_extracted(run_id, extracted) immediately before repo.set_status(run_id, RunStatus.AWAITING_REPLY).
     b. Before the record_only path set_status at line ~438: add repo.set_pre_clarify_extracted(run_id, extracted) immediately before repo.set_status(run_id, RunStatus.AWAITING_REPLY).
     c. Before the live path set_status at line ~451: add repo.set_pre_clarify_extracted(run_id, extracted) immediately before repo.set_status(run_id, RunStatus.AWAITING_REPLY).
  The IS NULL guard in set_pre_clarify_extracted's SQL makes all three calls idempotent — only the first succeeds. This is snapshot-once (D-19/D-28).

  Change D — StageResult clarify_deferred field + _run_stages branching (ALL of Change D lives HERE in Task 2):

  In _run_stages, in the else branch (request_clarification):
  - Determine whether ALL issues that caused request_clarification are of type field_regression:
    all_field_regression = all(i.issue_type == "field_regression" for i in issues if i.issue_type != "missing" or True)
    More precisely: check that every issue is field_regression — i.e., there are no "missing" or other issue types mixed in: all_field_regression = bool(issues) and all(i.issue_type == "field_regression" for i in issues).
  - If all_field_regression is True: set clarify_deferred = True. Do NOT call _clarify. The caller (resume_pipeline) will handle the send AFTER writing "asked" (N2 ordering fix).
  - If all_field_regression is False (mixed issues or no issues): set clarify_deferred = False. Call _clarify(run_id, email, decision, roster, extracted, purpose="clarification", llm=llm) as today.
  - Return StageResult(extracted=extracted, matches=matches, issues=issues, decision=decision, clarify_deferred=clarify_deferred).

  The clarify_deferred field is set in the else branch (request_clarification path) only. On the "process" branch, clarify_deferred=False (default).

  Import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields from app.db.repo in the imports block (add to the existing from app.db import repo line if they are module-level functions on repo, OR import them explicitly if needed).

  Add import for dataclass and StageResult types as described in Change A.

  All new code must stay inside the existing except Exception as exc boundary (orchestrator.py:235-241) for resume_pipeline code, and the existing try/except in run_pipeline.
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import StageResult, _run_stages, resume_pipeline; print('OK')" && uv run pytest tests/test_resume_pipeline.py -k "clarify_deferred_true or clarify_deferred_false" -q 2>&1 | tail -10</automated>
  </verify>
  <done>StageResult is importable with clarify_deferred field. _run_stages returns StageResult. grep "return StageResult" app/pipeline/orchestrator.py exits 0. grep "clarify_deferred" app/pipeline/orchestrator.py exits 0 (the field AND the branching logic). grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py exits 3+ occurrences (one before each AWAITING_REPLY path). grep "purpose=" app/pipeline/orchestrator.py | grep "_clarify" exits 0. Behavioral tests GREEN: test_clarify_deferred_true_when_all_issues_are_field_regression confirms _clarify is NOT called and clarify_deferred=True; test_clarify_deferred_false_when_mixed_issues confirms _clarify IS called and clarify_deferred=False. Existing orchestrator tests pass: uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py -q.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resume_pipeline — single _run_stages call + N1 alias-diff refactor + N2 asked-before-send + N3 classify-from-raw-before-backfill</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py lines 119-241 in a SINGLE PASS (the full resume_pipeline body). Do NOT re-read; extract everything needed in one pass:
  - Line 124: claim_status CAS — DO NOT TOUCH (D-22).
  - Line 133: load_run; line 136: load_roster_for_business; line 139-140: original_body + combined_email.
  - Lines 153-162: STEP A — pre-resume alias-diff data (_pre_candidates, _pre_reconciliation, _pre_resolved_ids). These capture the pre-state from the DB. KEEP THIS STEP EXACTLY. STEP A must run BEFORE the detection block and BEFORE _run_stages — this ordering is invariant and must not change.
  - Line 165: STEP B — the second _run_stages call. THIS IS THE N1 BUG. REPLACE this with the Round 1/2 detection block (see below). The alias-diff STEP C that follows (lines 167-234) used to read post-reconciliation from the DB. Now it must use StageResult.matches directly — no DB re-read needed for the post-resume resolved-id set.
  - Lines 167-234: STEP C — alias binding. The _post_resolved_ids set is built from post_run_data.get("reconciliation"). REFACTOR: build _post_resolved_ids from StageResult.matches directly: _post_resolved_ids = {str(m.matched_employee_id) for m in stage_result.matches if m.matched_employee_id is not None}. This avoids the post-run DB read for reconciliation — the StageResult has the data already. The rest of alias-diff logic (STEP D, misname guard) remains unchanged.
  - Line 235-241: except boundary — all new code stays inside here.

  CRITICAL ORDERING (BLOCKER 3 fix — silent alias-binding regression prevention):
  The CORRECT ordering in resume_pipeline MUST be:
  1. combined_email build (existing, ~line 140)
  2. STEP A: capture PRE-resume state (pre_run_data, _pre_candidates, _pre_reconciliation, _pre_resolved_ids) — UNCHANGED, runs FIRST before anything else
  3. ROUND 1/2 DETECTION BLOCK: load snapshot + clarified; build _asked_fields, _confirmed_drops; the SINGLE _run_stages call lives here, capturing its StageResult; on Round 2 classify-from-raw-before-backfill, etc.
  4. STEP C: build _post_resolved_ids FROM stage_result.matches (NOT a fresh load_run) — avoids the post-run DB read
  5. STEP D: diff _pre_resolved_ids vs _post_resolved_ids, bind alias

  STEP A MUST run BEFORE the detection block so _pre_resolved_ids is captured BEFORE _run_stages overwrites the reconciliation column. If detection (and thus _run_stages) ran before STEP A, then STEP A would read the already-overwritten post-resume reconciliation, making _pre_resolved_ids == _post_resolved_ids and _newly_resolved_ids empty — alias binding would silently never fire. This is the regression being prevented.

  STEP C derives _post_resolved_ids from stage_result.matches (the single call's return), NOT a second load_run — stage_result already has this data.

  ARCHITECTURE — two-inbound design (Round 1 vs Round 2 detection):
  Insert ROUND 1/2 DETECTION BLOCK after STEP A (after line ~162) and before the old STEP B (line 165, which is being replaced).

  ROUND 1/2 DETECTION BLOCK:
  1. snapshot = repo.load_pre_clarify_extracted(run_id) — the pre-clarify snapshot (None on first resume).
  2. clarified = repo.load_clarified_fields(run_id) — the field-regression outcomes dict (empty on first resume).
  3. _asked_fields: dict = {(str(emp_id), field): outcome for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "asked"} — which (emp_id, field) pairs are in "asked" state.
  4. _confirmed_drops: set[tuple[UUID, str]] = {(uuid.UUID(emp_id), field) for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "confirmed_dropped"} — skip these in validate (D-15).

  ROUND 2 DETECTION: if _asked_fields is non-empty, this is a Round 2 resume.
  - Call stage_result = _run_stages(run_id, combined_email, roster, prior=None, prior_matches=None, resolved_drops=_confirmed_drops, llm=llm).
  - N3 FIX: Classify outcomes from the RAW stage_result.extracted BEFORE any backfill. For each (emp_id_str, field) in _asked_fields: find the emp in stage_result.extracted.employees whose submitted_name matches (use the same name_to_id map approach). Get the raw resumed value = getattr(emp, field) from stage_result.extracted. If _is_paid(raw_value): the field is present in the reply → the drop is resolved, update outcome to "carried_forward" (the client answered and the field is now paid — backfill not needed since it is already in the extraction). If not _is_paid(raw_value) AND raw_value == Decimal("0"): outcome is "confirmed_dropped" (explicit zero — D-14; no backfill). If not _is_paid(raw_value) AND raw_value is None: outcome is "carried_forward" (silence — backfill the original value from snapshot).
  - BACKFILL (only for carried_forward with original value to restore): for each (emp_id_str, field) classified as carried_forward where the raw resumed value is None (silence, not paid in resume): find the matching employee in stage_result.extracted.employees (by submitted_name via name_to_id_current); find the matching employee in snapshot.employees (by submitted_name); if snapshot_emp has _is_paid(getattr(snapshot_emp, field)): setattr(extracted_emp, field, getattr(snapshot_emp, field)). Apply the backfill to stage_result.extracted IN PLACE. Then call repo.persist_extracted(run_id, stage_result.extracted) to commit the backfilled value.
  - Update clarified with final outcomes; call repo.set_clarified_fields(run_id, clarified).
  - Continue to the alias-diff block (STEP C/D) using stage_result.matches for _post_resolved_ids.

  ROUND 1 PATH: if _asked_fields is empty (no asked entries — this is a first or fresh resume):
  - Call stage_result = _run_stages(run_id, combined_email, roster, prior=snapshot, prior_matches=None, resolved_drops=_confirmed_drops, llm=llm).
  - N2 FIX — check stage_result.clarify_deferred: if stage_result.clarify_deferred is True (all issues are field_regression, _clarify was NOT called inside _run_stages):
    a. Build name_to_id_current from stage_result.matches.
    b. For each raw_drop from detect_field_regression(snapshot, stage_result.extracted) (if snapshot is not None): look up emp_id via name_to_id_current. Build clarified.setdefault(str(emp_id), {})[raw_drop.field] = "asked".
    c. repo.set_clarified_fields(run_id, clarified). ("asked" is now durable BEFORE the send.)
    d. THEN call _clarify(run_id, combined_email, stage_result.decision, roster, stage_result.extracted, llm=llm, purpose="clarification_field_regression").
    e. RETURN from resume_pipeline (do NOT continue to alias-diff).
  - If stage_result.clarify_deferred is False (normal path — _clarify already sent or decision is "process"): fall through to alias-diff STEP C/D.

  For the alias-diff STEP C refactor: replace the post_run_data DB load with stage_result.matches directly:
  _post_resolved_ids = {str(m.matched_employee_id) for m in stage_result.matches if m.matched_employee_id is not None}

  Import detect_field_regression from app.pipeline.validate in the imports block. Import _is_paid from app.pipeline.validate as well (needed for Round 2 outcome classification). Import Decimal from decimal (for the explicit-zero check).

  All new code stays inside the existing except Exception as exc boundary.
  </read_first>

  <behavior>
  Tests to add/update in tests/test_resume_pipeline.py:
  - Update test_run_stages_returns_stage_result from Task 2 (already added, verify it passes).
  - test_resume_pipeline_single_run_stages_call: mock _run_stages to count invocations; call resume_pipeline with a mocked run in AWAITING_REPLY; assert _run_stages called exactly once (N1 invariant). Use unittest.mock.patch for _run_stages.
  - test_n2_asked_written_before_send: use a test double for _clarify that records call order; assert that by the time _clarify is called for purpose="clarification_field_regression", clarified_fields in the DB already has "asked" for the relevant (emp_id, field) pairs. This is the N2 ordering invariant.
  - test_n3_explicit_zero_is_confirmed_dropped: set up a Round 2 scenario where the reply explicitly includes hours_overtime=Decimal("0"). Assert outcome classified as "confirmed_dropped" and no backfill is applied (the extracted value after backfill is still 0.00, not the snapshot value). The D-26 explicit-drop integration test (from Plan 05 stubs) can be promoted here.
  </behavior>

  <action>
  Implement the resume_pipeline refactor as described in read_first. The key structural changes:

  Change E — resume_pipeline ROUND 1 / ROUND 2 block:
  The correct final ordering in resume_pipeline after this task:
  1. combined_email build
  2. STEP A (pre-resume state capture: _pre_candidates, _pre_reconciliation, _pre_resolved_ids) — UNCHANGED, runs FIRST
  3. ROUND 1/2 DETECTION BLOCK (load snapshot + clarified; build _asked_fields, _confirmed_drops; the SINGLE _run_stages call lives here; on Round 2 classify-from-raw-before-backfill, etc.)
  4. STEP C (build _post_resolved_ids from stage_result.matches, NOT a second load_run)
  5. STEP D (diff _pre_resolved_ids vs _post_resolved_ids, bind alias)

  STEP A runs BEFORE the detection block so _pre_resolved_ids is captured pre-_run_stages; STEP C derives _post_resolved_ids from stage_result.matches (the single call's return), not a second load_run. This ordering is invariant — do not reorder.

  Insert the full ROUND 1/2 detection block after STEP A and remove the old standalone STEP B (_run_stages call at line 165). For Round 1: stage_result is captured in the Round 1 block. For Round 2: stage_result is captured in the Round 2 block. Both paths produce one stage_result; STEP C/D consume it.

  Change F — alias-diff STEP B replacement:
  Remove the old STEP B (_run_stages call at line 165). The single _run_stages call is now at the top of the Round 1 or Round 2 path, which runs after STEP A. STEP C now uses stage_result.matches for _post_resolved_ids instead of loading from DB.

  Add all imports at the top of orchestrator.py: detect_field_regression and _is_paid from app.pipeline.validate, Decimal from decimal.

  The existing except boundary (lines 235-241) wraps all of resume_pipeline's body — the new code is already inside it.

  After implementing, verify imports and run existing tests:
  uv run python -c "from app.pipeline.orchestrator import StageResult, resume_pipeline; print('OK')"
  uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import StageResult, resume_pipeline; print('OK')" && uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>resume_pipeline imports. StageResult is defined with clarify_deferred field. grep "set_clarified_fields" app/pipeline/orchestrator.py exits 0. grep "clarification_field_regression" app/pipeline/orchestrator.py exits 0. grep "clarify_deferred" app/pipeline/orchestrator.py exits 0. No second _run_stages call in resume_pipeline body — verified via AST: python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(f'_run_stages calls in resume_pipeline: {len([n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(n.func,\"id\",None)==\"_run_stages\"])}') for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='resume_pipeline']" must print 1. Total _run_stages occurrences in orchestrator.py (grep -c) should show exactly 3 — the def (~line 260), the call in _run (~line 91), and the one call in resume_pipeline. Full existing orchestrator test suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Schema DDL with N4 DROP/RE-ADD constraint migration applied to dev DB (Task 1). Repo helpers + allowlist expansion (Task 1). StageResult + _run_stages refactor + Change D clarify_deferred branching + _clarify N7 snapshot-on-all-3-paths + purpose kwarg (Task 2). resume_pipeline single-call N1 + N2 asked-before-send + N3 classify-from-raw-before-backfill with correct STEP A → detection → STEP C/D ordering (Task 3). All existing tests should be GREEN.</what-built>
  <how-to-verify>
  1. Verify repo helpers and StageResult importable:
     uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.pipeline.orchestrator import StageResult, _run_stages, resume_pipeline; print('ALL OK')"

  2. Verify schema N4 fix applied (requires DATABASE_URL):
     uv run python -m app.db.bootstrap
     uv run python -c "
     import psycopg, os, json
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     cur.execute(\"SELECT check_clause FROM information_schema.check_constraints WHERE constraint_name LIKE '%purpose%' OR constraint_name LIKE '%email_messages%' LIMIT 5\")
     rows = cur.fetchall()
     print('purpose constraints:', rows)
     # assert 'clarification_field_regression' appears
     assert any('clarification_field_regression' in str(r) for r in rows), 'N4 constraint not applied!'
     cur.execute(\"SELECT 1 FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name='pre_clarify_extracted'\")
     assert cur.fetchone(), 'pre_clarify_extracted column missing!'
     cur.execute(\"SELECT 1 FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name='clarified_fields'\")
     assert cur.fetchone(), 'clarified_fields column missing!'
     print('ALL SCHEMA CHECKS PASSED')
     conn.close()
     "

  3. Verify N7 snapshot-on-all-3-paths:
     grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py
     (Should show at least 3 occurrences — one before each AWAITING_REPLY path.)

  4. Verify N1 — only one _run_stages call in resume_pipeline:
     python3 -c "
     import ast, sys
     src = open('app/pipeline/orchestrator.py').read()
     tree = ast.parse(src)
     # Count _run_stages calls inside resume_pipeline function body
     for node in ast.walk(tree):
         if isinstance(node, ast.FunctionDef) and node.name == 'resume_pipeline':
             calls = [n for n in ast.walk(node) if isinstance(n, ast.Call) and hasattr(n.func, 'id') and n.func.id == '_run_stages']
             print(f'_run_stages calls in resume_pipeline: {len(calls)}')
             assert len(calls) == 1, f'N1 violated: expected 1, got {len(calls)}'
             print('N1 PASSED')
     "

  5. Full suite: uv run pytest -q (integration tests SKIP if no DATABASE_URL is fine; all others must PASS).

  6. Verify N4 — direct insert with new purpose value succeeds (requires DATABASE_URL):
     uv run python -c "
     import psycopg, os, uuid
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     try:
         test_id = str(uuid.uuid4())
         cur.execute(\"INSERT INTO email_messages (id, run_id, direction, message_id, purpose) VALUES (%s, NULL, 'outbound', %s, 'clarification_field_regression')\", (test_id, '<test-n4@test.local>'))
         conn.commit()
         print('N4 INSERT SUCCEEDED')
         cur.execute('DELETE FROM email_messages WHERE id = %s', (test_id,))
         conn.commit()
     except Exception as e:
         conn.rollback()
         print(f'N4 INSERT FAILED: {e}')
     conn.close()
     "
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass and the N4 insert test succeeds, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StageResult | Internal return value — typed dataclass, not Pydantic; never serialized to DB or external |
| clarified_fields JSONB writes | Written via json.dumps with %s placeholders — no f-string SQL injection vector |
| orchestrator resume_pipeline new code | Reads clarified_fields from DB; keys are emp_id strings and bare field names — no arbitrary code execution risk |
| email_messages.purpose CHECK | DB-enforced allowlist now includes 'clarification_field_regression'; unexpected purpose raises constraint violation at INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | clarified_fields JSONB via set_clarified_fields | mitigate | json.dumps(clarified) with %s placeholder — no f-string SQL. The dict is built from typed RawFieldDrop output + name_to_id resolution, not raw user input. |
| T-07-09 | Tampering | pre_clarify_extracted IS NULL guard | mitigate | Guard is in SQL: WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id — atomic check-and-write, no Python read-then-write race (snapshot-once D-19). |
| T-07-10 | Information Disclosure | PII in resume_pipeline new code | mitigate | All new code stays inside the existing except boundary (lines 235-241) which persists only type(exc).__name__. str(exc) never logged for new code. |
| T-07-11 | Denial of Service | set_pre_clarify_extracted called on every _clarify invocation (now 3x per _clarify) | accept | IS NULL guard means only the first of the 3 calls does a write; the other 2 are CAS misses returning 0 rows. O(3) = O(1) per clarification cycle. |
| T-07-12 | Spoofing | New purpose value in email_messages | mitigate | DB CHECK constraint (N4 DROP/RE-ADD migration) explicitly enforces the expanded list. get_outbound_message_id allowlist updated. |
| T-07-N2 | Tampering | asked-before-send ordering — crash between write and send leaves asked but no send | accept | Phase 7 specifies ordering invariant; full crash-idempotency (retry after crash) is Phase 9 atomicity scope. The asked entry durable before send is strictly better than send-before-asked (the N2 bug). |
| T-07-SC | Tampering | No new package installs | accept | No uv add invoked; psycopg and dataclasses are already available |
</threat_model>

<verification>
Full plan verification:

1. grep "pre_clarify_extracted JSONB" app/db/schema.sql — exits 0
2. grep "clarified_fields JSONB" app/db/schema.sql — exits 0
3. grep "clarification_field_regression" app/db/schema.sql — exits 2+ occurrences (inline + ALTER + DO $$ migration)
4. grep "clarification_field_regression" app/db/repo.py — exits 0 (allowlist)
5. grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py — exits 0 (CAS guard)
6. grep "def set_pre_clarify_extracted" app/db/repo.py — exits 0
7. grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py — 3+ (N7: all 3 AWAITING_REPLY paths)
8. grep "StageResult" app/pipeline/orchestrator.py — exits 0
9. grep "return StageResult" app/pipeline/orchestrator.py — exits 0 (_run_stages returns it)
10. grep "clarify_deferred" app/pipeline/orchestrator.py — exits 0 (Change D: field definition AND branching logic in _run_stages)
11. grep "set_clarified_fields" app/pipeline/orchestrator.py — exits 0 (N2 asked-before-send)
12. grep "clarification_field_regression" app/pipeline/orchestrator.py — exits 0
13. python3 AST check: _run_stages calls in resume_pipeline == 1 (N1)
14. grep -c "_run_stages" app/pipeline/orchestrator.py — total occurrences == 3 (def ~260 + call in _run ~91 + call in resume_pipeline); comments also contain the token so the raw count may be higher — the AST check at item 13 is authoritative for the N1 invariant
15. uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q — all GREEN
</verification>

<success_criteria>
- StageResult dataclass defined in orchestrator.py with extracted, matches, issues, decision, clarify_deferred fields
- _run_stages returns StageResult; both callers updated (run_pipeline silently discards, resume_pipeline captures)
- N1: resume_pipeline calls _run_stages exactly once — verified by AST (not grep -c which counts comment lines); STEP A (pre-capture) runs BEFORE the detection block and before _run_stages
- N2: clarified_fields "asked" written BEFORE _clarify is called for field-regression send (clarify_deferred=True → write asked → THEN call _clarify)
- N3: Round-2 outcome classification uses RAW stage_result.extracted BEFORE any backfill; confirmed_dropped (Decimal("0")) never backfilled; carried_forward (None) backfilled only after classification
- N4: schema.sql DO $$ migration drops old purpose CHECK and re-adds with 'clarification_field_regression'; existing DBs accept the new purpose value; live-DB verify (rollback insert) confirms N4 when DATABASE_URL is set
- N7: set_pre_clarify_extracted called before EACH of the 3 AWAITING_REPLY paths in _clarify; IS NULL guard = snapshot-once
- Change D lives entirely in Task 2 (_run_stages else-branch): clarify_deferred=True when all issues are field_regression; _clarify not called; clarify_deferred=False otherwise; behavioral tests confirm both branches
- Two new JSONB columns in schema.sql (inline + ALTER) and applied to dev DB
- Four repo helpers importable from app.db.repo
- Full existing test suite green (no regressions)
- Human checkpoint confirms all N-finding fixes verified against live DB
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md` when done.
</output>
