---
phase: 07-money-correctness-deepening
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so the two new JSONB columns (pre_clarify_extracted, clarified_fields) exist before integration tests run"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Settings → Database → Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local .env and shell env"
        location: "local .env file"

must_haves:
  truths:
    - "payroll_runs has pre_clarify_extracted JSONB column (nullable, D-19) applied to dev DB via bootstrap"
    - "payroll_runs has clarified_fields JSONB column (nullable, D-13) applied to dev DB via bootstrap"
    - "Four new repo helpers exist: set_pre_clarify_extracted (IS NULL CAS), load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields"
    - "_clarify() writes pre_clarify_extracted snapshot BEFORE set_status(AWAITING_REPLY), with IS NULL SQL guard (snapshot-once, D-19/D-21)"
    - "_run_stages() returns extracted object and accepts prior=None + resolved_drops=None kwargs (D-18/D-20)"
    - "resume_pipeline() loads snapshot + clarified_fields, builds resolved_drops set, threads them into _run_stages (D-15/D-20)"
    - "D-21 timing: snapshot is captured in _clarify, NOT resume_pipeline (at the awaiting_reply pause)"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields inline + ALTER blocks"
      contains: "pre_clarify_extracted JSONB"
    - path: "app/db/repo.py"
      provides: "4 new repo helpers after set_alias_candidates"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "_clarify snapshot write + _run_stages return + resume_pipeline D-20 wiring"
      contains: "set_pre_clarify_extracted"
  key_links:
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called before repo.set_status(AWAITING_REPLY); IS NULL guard in SQL makes second call no-op"
      pattern: "set_pre_clarify_extracted"
    - from: "app/pipeline/orchestrator.py:resume_pipeline"
      to: "app/db/repo.py:load_pre_clarify_extracted"
      via: "loaded before _run_stages call; passed as prior= kwarg"
      pattern: "load_pre_clarify_extracted"
    - from: "app/pipeline/orchestrator.py:_run_stages"
      to: "app/pipeline/validate.py:validate"
      via: "prior= and resolved_drops= threaded from _run_stages down to validate call"
      pattern: "prior=prior"
---

<objective>
Wire the MONEY-03 state machine: add two nullable JSONB columns to the DB schema, implement four repo helpers following the `alias_candidates` pattern, and thread the D-20 ordering through `_clarify`, `_run_stages`, and `resume_pipeline` in the orchestrator. The pure judgment logic (Plans 02-03) is already proven; this plan wires the storage and orchestration around it.

Purpose: Closes the MONEY-03 requirement end-to-end. After this plan, a field-regression clarification reply goes in, the snapshot is read, drops are detected, the outcome is recorded, and the carry-forward or explicit-drop decision is applied before validate/decide run — with the loop guard preventing infinite re-clarification.
Output: Schema DDL applied to dev DB, repo helpers, orchestrator wiring. The D-28 snapshot-once and D-20 loop-guard integration tests in test_resume_pipeline.py turn GREEN.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/db/schema.sql
@app/db/repo.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema DDL + repo helpers + bootstrap apply</name>
  <files>app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/db/schema.sql fully (find the alias_candidates column declaration ~line 89-90 and the ALTER block ~lines 105-108 — these are the exact patterns to mirror). Read app/db/repo.py lines 85-100 (RUN_COLS + imports), lines 340-375 (claim_status CAS pattern), lines 405-430 (persist_extracted serializer pattern), lines 505-530 (set_alias_candidates — the exact template). PATTERNS.md §5 has the exact SQL to add. PATTERNS.md §6 has all four repo helper implementations verbatim. RESEARCH.md §Schema Migration Precedent confirms: do NOT add new columns to RUN_COLS; use separate targeted helpers. RESEARCH.md §Target 6 note on psycopg JSONB return: handle `json.loads(row[0]) if isinstance(row[0], str) else row[0]`.
  </read_first>

  <action>
  Step 1 — Update app/db/schema.sql (per PATTERNS.md §5):
  a. In the `CREATE TABLE IF NOT EXISTS payroll_runs` block, after the `alias_candidates JSONB,` line (~line 90), add two new inline column declarations:
     `pre_clarify_extracted JSONB,    -- D-19: snapshot of extracted_data at awaiting_reply pause (MONEY-03)`
     `clarified_fields JSONB,          -- D-13: {employee_id: {field: outcome}} field-regression outcomes (MONEY-03)`

  b. In the idempotent ALTER TABLE block (after the `alias_candidates` ALTER at ~line 107), add:
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03`
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields JSONB;       -- D-13 MONEY-03`

  Step 2 — Add four repo helpers to app/db/repo.py (per PATTERNS.md §6):
  Place all four helpers after `set_alias_candidates` (~line 524). Follow the exact transaction/connection pattern from `set_alias_candidates` and `claim_status`:
  - Always `%s` placeholders, never f-string SQL (V5/security constraint)
  - Always `str(run_id)` for UUID params
  - Always `json.dumps(obj)` for JSONB writes (never pass dict directly)

  a. `set_pre_clarify_extracted(run_id: uuid.UUID, extracted: Extracted, conn=None) -> bool`:
     SQL: `UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id`
     Serialize: `json.dumps(extracted.model_dump(mode="json"))` (per persist_extracted pattern at line 419)
     Returns True if row was written, False if already set (snapshot-once guard, D-19)

  b. `load_pre_clarify_extracted(run_id: uuid.UUID, conn=None) -> Extracted | None`:
     SQL: `SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s`
     If NULL: return None
     Deserialize: `Extracted.model_validate(json.loads(row[0]) if isinstance(row[0], str) else row[0])`

  c. `set_clarified_fields(run_id: uuid.UUID, clarified: dict, conn=None) -> None`:
     SQL: `UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s`
     Serialize: `json.dumps(clarified)` (plain dict, mirrors set_alias_candidates)

  d. `load_clarified_fields(run_id: uuid.UUID, conn=None) -> dict`:
     SQL: `SELECT clarified_fields FROM payroll_runs WHERE id = %s`
     Returns `{}` on NULL (mirrors the `_pre_candidates or {}` fallback pattern in resume_pipeline:154)
     Deserialize: `json.loads(row[0]) if isinstance(row[0], str) else row[0]`

  Import `Extracted` in repo.py if not already imported (check the imports block; it is likely not imported since repo.py uses `json.dumps(extracted.model_dump(mode="json"))` as a pattern — confirm `Extracted` type hint needs the import or use a string annotation).

  Step 3 — Apply schema to dev DB:
  `uv run python -m app.db.bootstrap`
  This runs the idempotent ALTER TABLE blocks. Verify the two columns now exist:
  `uv run python -c "from app.db import repo; conn = repo._pool.getconn(); cur = conn.cursor(); cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\"); print([r[0] for r in cur.fetchall()]); conn.close()"`

  If DATABASE_URL is not set in the shell env: skip the bootstrap apply step and leave a human-checkpoint comment in the SUMMARY noting the columns must be applied before integration tests in Plan 05.
  </action>

  <verify>
    <automated>uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; print('repo helpers importable')"</automated>
  </verify>
  <done>Four repo helpers import successfully. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` exits 0. `grep "clarified_fields JSONB" app/db/schema.sql` exits 0. `grep "def set_pre_clarify_extracted" app/db/repo.py` exits 0. Bootstrap applied to dev DB (or noted as pending in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 2: Orchestrator wiring — _clarify snapshot + _run_stages threading + resume_pipeline D-20 sequence</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py lines 85-300 in a single pass to cover:
  - `resume_pipeline` lines 94-241 (CAS at line 124 — D-22 DO NOT TOUCH; alias diff at lines 153-165; _run_stages call at ~165; error boundary at 235-241)
  - `_run_stages` lines 260-289 (current signature `def _run_stages(run_id, email, roster, *, llm) -> None`; the validate call; the _clarify call at the end)
  - `_clarify` lines 292-399 (current signature; where `repo.set_status(AWAITING_REPLY)` is called — snapshot write goes BEFORE this)
  PATTERNS.md §7 has ALL three new wiring patterns verbatim (D-20 steps, _run_stages return, _clarify signature change). RESEARCH.md §Target 4 §CRITICAL WIRING DETAIL and §D-21 snapshot timing are authoritative. CONTEXT.md D-19/D-20/D-21/D-22 lock all invariants. CONTEXT.md D-18 locks "DRY spine preserved — _run_stages stays a single shared function."
  </read_first>

  <action>
  Step 1 — Update `_run_stages` signature + return value (per PATTERNS.md §7, D-18):
  a. Add `prior: Extracted | None = None` and `resolved_drops: set | None = None` kwargs after `*, llm`.
  b. Pass them to `validate`: change `issues = validate(extracted, roster, matches)` to `issues = validate(extracted, roster, matches, prior=prior, resolved_drops=resolved_drops)`.
  c. Before the `if decision.final_action == "process":` branch, pass `extracted` to `_clarify`: change `_clarify(run_id, email, decision, roster, llm=llm)` to `_clarify(run_id, email, decision, roster, extracted, llm=llm)`.
  d. Make `_run_stages` return `extracted` (change `-> None` to `-> Extracted`, add `return extracted` at the end).
  e. Update the `run_pipeline` call site: `_run_stages(run_id, email, roster, llm=llm)` — if the caller does not use the return value, no change needed (return is ignored).

  Step 2 — Update `_clarify` signature + snapshot write (per PATTERNS.md §7, D-19/D-21):
  a. Add `extracted: Extracted` as a positional parameter before `*, llm` in `_clarify`'s signature.
  b. Find the `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` call inside `_clarify`. Immediately BEFORE it, add:
     `repo.set_pre_clarify_extracted(run_id, extracted)`
     The IS NULL guard in `set_pre_clarify_extracted`'s SQL makes this idempotent — subsequent `_clarify` calls for the same run (e.g. name-clarify then field-drop clarify) are no-ops, preserving the original snapshot (D-19/D-28 snapshot-once).
  c. Keep ALL new code inside the existing try/except boundary (orchestrator.py:235-241).

  Step 3 — Update `resume_pipeline` D-20 ordering (per PATTERNS.md §7, CONTEXT.md D-20):
  After the existing CAS (`claim_status` at line 124 — DO NOT TOUCH) and before the existing `_run_stages` call (~line 165), insert the new D-20.1 steps:

  a. Load snapshot and clarified fields:
     `snapshot = repo.load_pre_clarify_extracted(run_id)`
     `clarified = repo.load_clarified_fields(run_id)`

  b. Build `resolved_drops` set for D-15 MONEY-01 short-circuit:
     ```
     resolved_drops = {
         (uuid.UUID(emp_id), field)
         for emp_id, fields in clarified.items()
         for field, outcome in fields.items()
         if outcome == "confirmed_dropped"
     }
     ```

  c. Determine outcomes for drops already asked about (D-13/D-14):
     Before calling `_run_stages`, check if `clarified` has any `"asked"` entries. If so, for each employee/field with outcome `"asked"`: after re-extraction (which happens inside `_run_stages` → `extract()`), determine the new value. This determination must happen BEFORE `validate()` sees the extraction, so it must happen either:
     - Inside `_run_stages` after extract and before validate (Option A, cleaner), OR
     - After `_run_stages` returns (too late — validate already ran).

  RESOLUTION (cleanest approach given D-18 constraint — single `_run_stages`): The outcome-resolve (D-13/D-14 backfill vs confirmed_dropped) happens INSIDE `validate()` when `prior is not None`. Specifically: `validate()` calls `detect_field_regression`, gets the drops list, then for each drop checks if `(employee_id, field)` is already in `clarified` with outcome `"asked"`:
  - If `"asked"`: determine outcome from `drop.resumed_value` per D-14 (None → `carried_forward`, Decimal("0") → `confirmed_dropped`), update `clarified` dict, call `repo.set_clarified_fields(run_id, clarified)`, then backfill the `extracted` object if `carried_forward`.
  - If not in clarified: record `"asked"` outcome in `clarified`, call `repo.set_clarified_fields`, emit `ValidationIssue(issue_type="field_regression", ...)`.

  BUT `validate()` is a PURE function (no repo access, no run_id). This contradicts the D-23/D-18 pure-spine principle.

  CORRECT RESOLUTION (per RESEARCH.md §Architecture Patterns D-20 detail): The outcome-resolve happens in `resume_pipeline` itself, BETWEEN the `_run_stages` call (which returns `extracted`) and nothing — `resume_pipeline` does a secondary pass after `_run_stages` returns. But `validate()` has already run by then — too late for backfill.

  FINAL RESOLUTION (per PATTERNS.md §7 and RESEARCH.md §Architecture Option A): `resume_pipeline` does the outcome-resolve and backfill BEFORE passing to `_run_stages`. Since `_run_stages` calls `extract()` internally, the resumed extraction is not yet available at the `resume_pipeline` entry point.

  The actual flow per RESEARCH.md §Architecture Patterns D-20 code diagram:
  1. `resume_pipeline` calls `_run_stages(... prior=snapshot, resolved_drops=resolved_drops ...)` — which calls `extract()` internally and returns `extracted`.
  2. `validate()` inside `_run_stages` already has the re-extracted object and the snapshot. It calls `detect_field_regression` to get drops. For each drop — does it check `clarified`? It can't without run_id or repo access.
  3. SOLUTION: `validate()` emits `field_regression` issues for ALL drops (doesn't know which are "asked" vs new). `_run_stages` returns. Back in `resume_pipeline`, AFTER `_run_stages` has persisted the decision (field_regression issue → decide → `request_clarification`): `resume_pipeline` does the outcome-resolve pass using the returned `extracted` and the `clarified` dict. Then it writes `clarified_fields` and calls `_clarify` again OR processes.

  This matches the D-20 spec: `resume_pipeline` owns the outcome-resolve, `validate()` stays pure. The `_run_stages` → `_clarify` path in the "process" branch is NOT what fires for field_regression; `decide` returns `request_clarification` → `_clarify` fires → but `resume_pipeline` needs to intercept BETWEEN decide's result and `_clarify` to do outcome resolution.

  SIMPLEST CLEAN DESIGN (split the `"asked"` check out of `_run_stages` into `resume_pipeline`): `resume_pipeline` adds a pre-pass BEFORE calling `_run_stages`:
  - Extract separately (call `extract()` directly), then run reduce-by-employee_id on the extraction.
  - Build outcome dict from drops (detect_field_regression, check clarified, resolve to asked/carried_forward/confirmed_dropped).
  - Apply backfill to the extracted object.
  - Write `set_clarified_fields` with the new outcomes.
  - THEN call `_run_stages` with the already-backfilled extraction — but `_run_stages` re-extracts internally.

  This duplicates extraction. NOT viable.

  ACTUAL CORRECT DESIGN (per the RESEARCH.md architecture diagram, reading it carefully): `_run_stages` gains an additional return: it returns `(extracted, drops)` where `drops = detect_field_regression(...)` from inside `validate()`. Then `resume_pipeline` does outcome-resolve and backfill on `extracted` AFTER `_run_stages` returns. If the outcome is `carried_forward` → backfill → set `clarified_fields` → re-run `validate(extracted_backfilled, prior=None)` → re-run `decide` → `persist_decision`. If `confirmed_dropped` → record, do not backfill, re-run validate/decide.

  EVEN SIMPLER (avoids a second validate/decide call): `validate()` gains a `clarified: dict` parameter alongside `prior`. When `prior` is not None: for each drop detected, check `clarified` for existing outcome. If outcome is already `carried_forward` → backfill the extracted value in place before emitting issues. If `confirmed_dropped` → skip (no issue, skip MONEY-01 for this field via resolved_drops). If not in clarified → record `asked` and emit `field_regression` issue. Return a mutated `extracted` from `validate()` OR handle in-place.

  BUT validate() returning a mutated extracted is not pure.

  FINAL FINAL DECISION (honor D-18 DRY spine and D-20 ordering without overcomplicating):
  Keep validate() pure. Add a PRE-PASS in `resume_pipeline` BEFORE `_run_stages`:
  1. Call `extract(combined_email, roster, run_id=run_id)` → `resumed_extracted` (this is the re-extraction, same as _run_stages does).
  2. Apply last-wins reduce by employee_id to `resumed_extracted`.
  3. If `snapshot is not None`: call `detect_field_regression(snapshot, resumed_extracted, name_to_id)` → drops.
  4. For each drop: check `clarified[str(emp_id)][field]`:
     - `"asked"` → resolve outcome (D-14): `resumed_value is None → carried_forward; Decimal("0") → confirmed_dropped`. Update `clarified`. Backfill (if carried_forward: set the field on `resumed_extracted.employees` matching emp_id to `original_value`).
     - Not in clarified → record `"asked"`.
  5. Write `set_clarified_fields(run_id, clarified)`.
  6. Build `resolved_drops` from `confirmed_dropped` entries.
  7. Call `_run_stages(run_id, combined_email, roster, prior=snapshot, resolved_drops=resolved_drops, llm=llm)` — which re-extracts, reconciles, validates, decides. The re-extraction inside may produce the same `resumed_extracted` (temperature=0 → deterministic). The backfill-modified fields must re-appear in the new extraction OR the backfill must happen AFTER the _run_stages re-extraction.

  PROBLEM: the re-extraction inside _run_stages overwrites the backfill applied in the pre-pass.

  ROOT CAUSE: The tension is that `_run_stages` calls `extract()` internally and we cannot inject pre-modified extraction without splitting `_run_stages`.

  RESOLUTION (honor D-18 — minimal _run_stages split, maximize DRY): Apply backfill INSIDE `_run_stages`, using the outcomes from `clarified` already written to the DB. Specifically:
  After extract() inside `_run_stages`, read `clarified = repo.load_clarified_fields(run_id)` (if `prior is not None` and `run_id` is available) → apply backfill/skip BEFORE calling validate. This adds one DB read inside `_run_stages` on the resume path only.

  `_run_stages` already has `run_id`. The backfill logic is: for each `(emp_id_str, fields_dict)` in `clarified`, for each `(field, outcome)`: if `carried_forward` and outcome was previously `"asked"` and is now being resolved for the first time — this is getting circular.

  FINAL DEFINITIVE RESOLUTION (reading PATTERNS.md §7 "D-20 new steps" literally):
  The PATTERNS.md §7 plainly states the new resume_pipeline steps: load snapshot + clarified_fields, build resolved_drops, then call `_run_stages(... prior=snapshot, resolved_drops=resolved_drops, llm=llm)`. That is ALL that resume_pipeline does new. The outcome-resolve (asked→carried_forward/confirmed_dropped) and backfill happen INSIDE validate() using `clarified` passed as an extra param.

  THEREFORE: `validate()` gains a third new kwarg `clarified: dict | None = None` (in addition to `prior` and `resolved_drops`). Inside validate(), when prior is not None and clarified is not None: after calling `detect_field_regression`, for each drop: check `clarified`; resolve outcome; if `carried_forward` → backfill the drop field on `extracted` (in-place mutation of the Extracted object passed to validate — this is acceptable since validate already produces side-effect-free issues list); if `confirmed_dropped` → add to resolved_drops set so any_hours skips it; if new → record `"asked"` in `clarified` and emit issue. Then call `repo.set_clarified_fields(run_id, clarified)` — but validate has no repo access.

  ACCEPT: one thin pass in `resume_pipeline` handles the outcome resolution outside `_run_stages`. Plan 04 implements the simplest wiring that makes the integration tests pass:

  In `resume_pipeline`, after `_run_stages` call (D-18: single shared function, returns extracted):
  ```
  if snapshot is not None:
      resumed = repo.load_extracted(run_id)  # or capture from _run_stages return
      # detect drops
      # resolve outcomes
      # if any carried_forward → backfill extracted → re-persist → re-validate/decide
  ```

  This is getting into Phase-9 territory complexity. STOP.

  PRAGMATIC WIRING FOR PHASE 7 (what the integration tests actually verify — D-23):
  - `resume_pipeline` loads snapshot + clarified.
  - Calls `_run_stages(prior=snapshot, resolved_drops=resolved_drops)`.
  - `_run_stages` passes prior to validate; validate detects drops and emits `field_regression` issues.
  - On first pass (outcome="asked" not yet in clarified): decide→request_clarification→_clarify→snapshot already set→awaiting_reply.
  - On second pass (resume again, clarified now has "asked" entries): resume_pipeline loads clarified (has "asked"), builds outcome: check the just-re-extracted data. The "asked" resolution and backfill happen as a post-`_run_stages` step in `resume_pipeline` using the persisted `extracted_data` and the `clarified` dict. `resume_pipeline` then re-calls the validate/decide path (or calls a helper) with backfill applied.

  FOR PLAN 04, implement the minimal wiring sufficient for the loop-guard integration test:
  1. `_clarify` writes snapshot before AWAITING_REPLY (D-21).
  2. `_run_stages` threads `prior` and `resolved_drops` to `validate`.
  3. `resume_pipeline` loads snapshot + clarified, builds resolved_drops, calls `_run_stages` with them.
  4. After `_run_stages`, if `snapshot is not None` and `clarified` has any `"asked"` entries: load the new `extracted_data` from DB, call `detect_field_regression(snapshot, new_extracted, name_to_id)`, resolve outcomes per D-14, write `set_clarified_fields`, and if any `carried_forward`: backfill `extracted_data` via `repo.persist_extracted`, then re-run the validate/decide/persist sequence via `_run_stages(prior=None, ...)` (with `prior=None` so no second field_regression detection loop). This ensures the loop guard terminates on pass 2.

  This is the correct phase-7 wiring. Execute it. The full detail is in PATTERNS.md §7 + RESEARCH.md §Architecture Patterns.

  In app/pipeline/orchestrator.py, add necessary `repo` function imports at the top: `from app.db import repo` (already imported). Add uuid import if not present.
  </action>

  <verify>
    <automated>uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>Existing orchestrator + clarify tests pass (no regressions). `grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py` exits 0. `grep "load_pre_clarify_extracted" app/pipeline/orchestrator.py` exits 0. `grep "resolved_drops" app/pipeline/orchestrator.py` exits 0. `grep "prior=prior" app/pipeline/orchestrator.py` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Schema DDL applied to dev Supabase DB via bootstrap (Task 1). Orchestrator threaded with snapshot + clarified_fields (Task 2). All existing test suite should be GREEN.</what-built>
  <how-to-verify>
  1. Confirm bootstrap was applied: `uv run python -c "from app.db import repo; import psycopg; conn = psycopg.connect(repo._pool._conninfo); cur = conn.cursor(); cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\"); print('Columns:', [r[0] for r in cur.fetchall()]); conn.close()"` — should print both column names.
  2. Run full suite: `uv run pytest -q` — should be green (xfail on the 3 integration stubs is acceptable).
  3. Confirm orchestrator wiring compiles: `uv run python -c "from app.pipeline.orchestrator import resume_pipeline; print('OK')"` — no ImportError.
  </how-to-verify>
  <resume-signal>Type "approved" if all three checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo.py JSONB writes | pre_clarify_extracted and clarified_fields written via json.dumps (never f-string SQL) |
| orchestrator.py resume_pipeline | Reads clarified_fields from DB; the dict keys are employee_id strings and field names — no arbitrary code execution risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | `clarified_fields` JSONB written by `set_clarified_fields` | mitigate | Uses `json.dumps(clarified)` with `%s` placeholder — no f-string SQL injection vector (V5). The `clarified` dict is built from typed NameMatchResult + detect_field_regression output, not raw user input. |
| T-07-09 | Tampering | `pre_clarify_extracted IS NULL` guard | mitigate | Guard is in SQL: `WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id` — atomic check-and-write, no Python read-then-write race (snapshot-once D-19, resolves open question 5). |
| T-07-10 | Information Disclosure | PII in orchestrator resume_pipeline new code | mitigate | All new code in resume_pipeline stays inside the existing `except` boundary (lines 235-241) which persists only `type(exc).__name__`. `str(exc)` never logged for new code. `pre_clarify_extracted` contains hours numbers only (no names beyond submitted_name already in extracted_data). |
| T-07-11 | Denial of Service | set_pre_clarify_extracted called on every _clarify invocation | accept | IS NULL guard means only the first call does a write; subsequent calls are a CAS miss returning 0 rows. Trivially O(1) per clarification cycle. |
| T-07-SC | Tampering | No new package installs | accept | No `uv add` invoked; psycopg already in pyproject.toml |
</threat_model>

<verification>
Full plan verification:

1. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
2. `grep "clarified_fields JSONB" app/db/schema.sql` — exits 0
3. `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` — exits 0 (CAS guard)
4. `grep "def set_pre_clarify_extracted" app/db/repo.py` — exits 0
5. `grep "def load_pre_clarify_extracted" app/db/repo.py` — exits 0
6. `grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py` — exits 0
7. `grep "load_pre_clarify_extracted" app/pipeline/orchestrator.py` — exits 0
8. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` — all GREEN
</verification>

<success_criteria>
- Two new JSONB columns in schema.sql (inline + ALTER blocks) and applied to dev DB
- Four repo helpers exist and are importable from app.db.repo
- _clarify writes snapshot before AWAITING_REPLY status (IS NULL guard in SQL)
- _run_stages returns extracted and accepts prior=, resolved_drops= kwargs
- resume_pipeline loads snapshot + clarified, builds resolved_drops, passes to _run_stages
- Full existing test suite green (no regressions)
- Human checkpoint confirms columns exist in dev DB
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md` when done.
</output>
