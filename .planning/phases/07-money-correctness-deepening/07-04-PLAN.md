---
phase: 07-money-correctness-deepening
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so the new JSONB columns (pre_clarify_extracted, clarified_fields) AND the new purpose value 'clarification_field_regression' exist before integration tests run"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Settings → Database → Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local .env and shell env"
        location: "local .env file"

must_haves:
  truths:
    - "payroll_runs has pre_clarify_extracted JSONB column (nullable, D-19) applied to dev DB via bootstrap"
    - "payroll_runs has clarified_fields JSONB column (nullable, D-13) applied to dev DB via bootstrap"
    - "email_messages.purpose CHECK constraint includes 'clarification_field_regression' (schema DDL updated + ALTER applied) — required so the Round 1 send does not violate the DB constraint"
    - "get_outbound_message_id() allowlist includes 'clarification_field_regression' (repo.py updated) — required so the Round 1 idempotency guard does not raise ValueError"
    - "Four new repo helpers exist: set_pre_clarify_extracted (IS NULL CAS), load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields"
    - "ROUND 1 (partial reply arrives, field regression detected): resume_pipeline detects drop → writes clarified_fields[employee_id][field]='asked' DURABLY (via set_clarified_fields) BEFORE the send → sends with purpose='clarification_field_regression' → sets AWAITING_REPLY → RETURNS (does NOT continue to calc) — decision A"
    - "ROUND 2 (client answers 'did you forget OT?'): resume_pipeline loads clarified_fields with 'asked' entries → resolves per D-14 (silence=carried_forward, Decimal('0')=confirmed_dropped) → post-extract backfill hook: set carried_forward fields AFTER extract() returns but BEFORE persist_extracted — so re-extraction cannot clobber the backfilled value (decision B) → validate(prior=None) → decide → process — decision A Round 2"
    - "Termination is guaranteed by purpose+asked state: Round 2 sees an existing purpose='clarification_field_regression' row AND clarified_fields entries no longer 'asked' → resolves and proceeds instead of re-asking — NO infinite loop, NO prior=None blunt suppression (D-20 superseded by decision A)"
    - "_clarify() writes pre_clarify_extracted snapshot BEFORE set_status(AWAITING_REPLY), with IS NULL SQL guard (snapshot-once, D-19/D-21)"
    - "_run_stages() returns extracted object and accepts prior=None + resolved_drops=None kwargs (D-18)"
    - "D-15 interaction: confirmed_dropped entries in clarified_fields build resolved_drops set passed to validate() so MONEY-01 does not re-flag a deliberately-removed field"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields inline + ALTER blocks; email_messages.purpose CHECK updated to include 'clarification_field_regression'; ALTER blocks for both"
      contains: "pre_clarify_extracted JSONB"
    - path: "app/db/repo.py"
      provides: "4 new repo helpers after set_alias_candidates; get_outbound_message_id allowlist expanded to include 'clarification_field_regression'"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "_clarify snapshot write + _run_stages return + resume_pipeline two-inbound round-1/round-2 logic per decision A/B"
      contains: "set_pre_clarify_extracted"
  key_links:
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called before repo.set_status(AWAITING_REPLY); IS NULL guard in SQL makes second call no-op"
      pattern: "set_pre_clarify_extracted"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 1)"
      to: "app/db/repo.py:set_clarified_fields"
      via: "writes clarified_fields[emp_id][field]='asked' BEFORE sending clarification_field_regression"
      pattern: "set_clarified_fields"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 2)"
      to: "app/pipeline/validate.py:validate"
      via: "backfill hook applies after extract() but before persist_extracted; then validate(prior=None, resolved_drops=confirmed_set)"
      pattern: "prior=None"
---

<objective>
Wire the MONEY-03 state machine using the TWO-INBOUND architecture (decision A, locked by orchestrator + user, superseding D-20's single-call design).

Architecture summary:
- ROUND 1: a partial reply arrives (e.g. "40", no OT). resume_pipeline re-extracts, detects the field drop, writes clarified_fields[emp_id][field]="asked" to DB, sends the question ("did you forget the OT?") with a NEW distinct purpose="clarification_field_regression" (so the existing purpose="clarification" idempotency guard cannot suppress it), sets AWAITING_REPLY, and RETURNS. Does not process.
- ROUND 2: the client's answer arrives. resume_pipeline re-extracts, sees clarified_fields has "asked" entries, resolves per D-14 (silence=carry_forward, explicit_zero=confirmed_dropped), applies the post-extract backfill hook for carried_forward values BEFORE persist_extracted (so re-extraction cannot clobber them — decision B), then validate(prior=None, resolved_drops=confirmed_set) → decide → process.
- Termination guarantee: Round 2 sees purpose="clarification_field_regression" already sent AND clarified_fields no longer has "asked" entries → resolves instead of re-asking. No infinite loop.

Required infra:
- Schema: two new JSONB columns on payroll_runs; email_messages.purpose CHECK expanded to include "clarification_field_regression"; ALTER blocks for both.
- Repo: four new helpers; get_outbound_message_id() allowlist expanded.
- Orchestrator: _clarify() snapshot write; _run_stages() returns extracted; resume_pipeline() round-1/round-2 logic.

REVISION — ALL prior Plan 04 design is SUPERSEDED by this revision:
- SUPERSEDED: D-20 single-resume-call with prior=snapshot + second _run_stages(prior=None) — REPLACED by two-inbound model (decision A).
- SUPERSEDED: the qualified-field string trick — REMOVED (decision C, already fixed in Plan 03).
- SUPERSEDED: "backfill → persist → second _run_stages" in one call — REPLACED by post-extract backfill hook on Round 2 AFTER extract() returns but BEFORE persist_extracted (decision B).
- SUPERSEDED: prior=None as termination mechanism — REPLACED by purpose+asked state (decision A).
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/db/schema.sql
@app/db/repo.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema DDL + repo helpers + purpose allowlist expansion</name>
  <files>app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/db/schema.sql fully — capture:
  - alias_candidates column declaration (~line 90) and the ALTER block (~lines 105-108) — patterns to mirror for the two new payroll_runs columns.
  - email_messages CREATE TABLE block (~lines 134-158): the purpose CHECK constraint at line 147 is `CHECK (purpose IN ('clarification','confirmation'))` and the UNIQUE constraint at line 157 is `CONSTRAINT uq_email_run_purpose UNIQUE (run_id, purpose)`. The CHECK MUST be updated to add 'clarification_field_regression'. The UNIQUE constraint means each run can have at most ONE row per purpose value — which is correct: exactly one Round 1 send per run.
  - The ALTER TABLE blocks for email_messages at lines 160-184: note the DO $$ pattern for the constraint (Postgres does not support IF NOT EXISTS for named constraints).

  Read app/db/repo.py lines 85-100 (RUN_COLS + imports), lines 340-375 (claim_status CAS pattern), lines 405-430 (persist_extracted serializer pattern), lines 505-530 (set_alias_candidates — the exact template), lines 659-685 (get_outbound_message_id — THE PURPOSE ALLOWLIST to update).

  PATTERNS.md §5 has the SQL for the new payroll_runs columns. PATTERNS.md §6 has all four repo helper implementations.

  CRITICAL: get_outbound_message_id at repo.py:670 has a hardcoded allowlist: `if purpose not in ("clarification", "confirmation"): raise ValueError(...)`. This MUST be expanded to include "clarification_field_regression" or the orchestrator's Round 1 idempotency check will raise ValueError.

  CRITICAL: insert_email_message at repo.py:568 uses ON CONFLICT (run_id, purpose) — this works correctly for the new purpose value because the uq_email_run_purpose UNIQUE constraint covers all non-NULL purpose values including the new one.
  </read_first>

  <action>
  Step 1 — Update app/db/schema.sql:

  a. In the `CREATE TABLE IF NOT EXISTS payroll_runs` block, after `alias_candidates JSONB,` (line ~90), add:
     `pre_clarify_extracted JSONB,    -- D-19: snapshot of extracted_data at awaiting_reply pause (MONEY-03)`
     `clarified_fields JSONB,          -- D-13: {employee_id: {field: outcome}} field-regression outcomes (MONEY-03)`

  b. In the idempotent ALTER TABLE block for payroll_runs (after the alias_candidates ALTER at ~line 107), add:
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03`
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields JSONB;       -- D-13 MONEY-03`

  c. In the `CREATE TABLE IF NOT EXISTS email_messages` block, update the purpose CHECK constraint:
     Change: `purpose TEXT CHECK (purpose IN ('clarification','confirmation'))`
     To: `purpose TEXT CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'))`

  d. In the idempotent ALTER TABLE for email_messages (lines ~164-166), update the ALTER ... ADD COLUMN IF NOT EXISTS for purpose to match the new CHECK:
     Change: `ALTER TABLE email_messages ADD COLUMN IF NOT EXISTS purpose TEXT CHECK (purpose IN ('clarification','confirmation'));`
     To: `ALTER TABLE email_messages ADD COLUMN IF NOT EXISTS purpose TEXT CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'));`

  Note: The DO $$ block for the UNIQUE constraint (uq_email_run_purpose) does NOT need to change — the constraint is on (run_id, purpose) and already handles any non-NULL purpose value. Do NOT modify that block.

  Step 2 — Add four repo helpers to app/db/repo.py (per PATTERNS.md §6):
  Place all four helpers after `set_alias_candidates` (~line 524). Follow the exact transaction/connection pattern from `set_alias_candidates` and `claim_status`:
  - Always `%s` placeholders, never f-string SQL (V5/security constraint)
  - Always `str(run_id)` for UUID params
  - Always `json.dumps(obj)` for JSONB writes (never pass dict directly)

  a. `set_pre_clarify_extracted(run_id: uuid.UUID, extracted: Extracted, conn=None) -> bool`:
     SQL: `UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id`
     Serialize: `json.dumps(extracted.model_dump(mode="json"))` (per persist_extracted pattern)
     Returns True if row was written, False if already set (snapshot-once guard, D-19)

  b. `load_pre_clarify_extracted(run_id: uuid.UUID, conn=None) -> Extracted | None`:
     SQL: `SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s`
     If NULL: return None
     Deserialize: `Extracted.model_validate(json.loads(row[0]) if isinstance(row[0], str) else row[0])`

  c. `set_clarified_fields(run_id: uuid.UUID, clarified: dict, conn=None) -> None`:
     SQL: `UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s`
     Serialize: `json.dumps(clarified)` (plain dict, mirrors set_alias_candidates)

  d. `load_clarified_fields(run_id: uuid.UUID, conn=None) -> dict`:
     SQL: `SELECT clarified_fields FROM payroll_runs WHERE id = %s`
     Returns `{}` on NULL (mirrors the `_pre_candidates or {}` fallback pattern in resume_pipeline:154)
     Deserialize: `json.loads(row[0]) if isinstance(row[0], str) else row[0]`

  Import `Extracted` in repo.py if not already imported. Confirm the imports block at line ~85-100. Use string annotation `Extracted` if the type is imported inside a function to avoid circular imports — check whether it's safe to import at module level (validate: does repo.py already import from app.models.contracts?).

  Step 3 — Expand get_outbound_message_id() allowlist (repo.py:670):
  Change: `if purpose not in ("clarification", "confirmation"):`
  To: `if purpose not in ("clarification", "confirmation", "clarification_field_regression"):`

  Update the docstring to note the new purpose value.

  Step 4 — Apply schema to dev DB:
  `uv run python -m app.db.bootstrap`
  If DATABASE_URL is not set: skip bootstrap and note it as pending in SUMMARY. The columns and CHECK update must be applied before integration tests in Plan 05.
  </action>

  <verify>
    <automated>uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; print('OK')"</automated>
  </verify>
  <done>Four repo helpers import successfully. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` exits 0. `grep "clarified_fields JSONB" app/db/schema.sql` exits 0. `grep "clarification_field_regression" app/db/schema.sql` exits 0 (CHECK constraint updated). `grep "clarification_field_regression" app/db/repo.py` exits 0 (allowlist updated). Bootstrap applied to dev DB (or noted as pending in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 2: _run_stages threading + _clarify snapshot write (D-21 timing)</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py lines 260-290 in a single pass:
  - `_run_stages` lines 260-289: current signature `def _run_stages(run_id, email, roster, *, llm) -> None`; the validate call at ~line 273; the _clarify call at ~line 289.
  - `_clarify` lines 292-451: current signature `def _clarify(run_id, email, decision, roster, *, llm) -> None`; the idempotency guard at lines 318-326 (`get_outbound_message_id(run_id, purpose="clarification")`); where `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` is called (two places: line 438 for record_only path and line 451 for live path).
  PATTERNS.md §7 has both wiring patterns. CONTEXT.md D-18 locks "_run_stages stays a single shared function." CONTEXT.md D-19/D-21 lock snapshot timing: snapshot in _clarify, BEFORE AWAITING_REPLY. CONTEXT.md D-22 locks the CAS in resume_pipeline — DO NOT TOUCH it.
  </read_first>

  <action>
  This task makes two targeted changes to orchestrator.py:

  Change A — Update `_run_stages` signature and return (per D-18):
  1. Add `prior: Extracted | None = None` and `resolved_drops: set | None = None` keyword-only kwargs after `*, llm` in the `_run_stages` signature.
  2. Change `issues = validate(extracted, roster, matches)` to `issues = validate(extracted, roster, matches, prior=prior, resolved_drops=resolved_drops)`.
  3. Change the `_clarify(run_id, email, decision, roster, llm=llm)` call to `_clarify(run_id, email, decision, roster, extracted, llm=llm)` so the snapshot can be written inside _clarify (per D-21).
  4. Change the return type annotation from `-> None` to `-> Extracted` and add `return extracted` at the end of `_run_stages` (after the branch). The existing `run_pipeline` call site does `_run_stages(...)` without assignment — the return is silently dropped, which is fine. The `resume_pipeline` call site will be updated in Task 3 to capture the return value.

  Change B — Update `_clarify` signature and add snapshot write (per D-19/D-21):
  1. Add `extracted: Extracted` as a positional parameter before `*, llm` in `_clarify`'s signature: `def _clarify(run_id, email, decision, roster, extracted, *, llm) -> None`.
  2. Locate BOTH `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` calls inside `_clarify` (one in the record_only path at ~line 438, one in the live path at ~line 451). Immediately BEFORE EACH, add: `repo.set_pre_clarify_extracted(run_id, extracted)`. The IS NULL guard in set_pre_clarify_extracted makes this idempotent — if _clarify is called twice, only the first write succeeds (snapshot-once, D-19/D-28).

  NOTE: Do NOT change the existing idempotency guard at lines 318-326 (`get_outbound_message_id(run_id, purpose="clarification")`). This guard protects against duplicate name-clarification sends. The field-regression clarification uses `purpose="clarification_field_regression"` which is a different purpose value — the existing guard does NOT suppress it. The guard for field-regression is handled in Task 3's resume_pipeline logic.

  Add `from app.models.contracts import Extracted` to the orchestrator import block if not already imported (check the current imports — it may already be imported). Keep all new code inside the existing `except Exception as exc` boundary (orchestrator.py:235-241).
  </action>

  <verify>
    <automated>uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>Existing orchestrator + clarify tests pass (no regressions). `grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py` exits 0. `grep "prior=prior" app/pipeline/orchestrator.py` exits 0. `grep "return extracted" app/pipeline/orchestrator.py` exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: resume_pipeline two-inbound Round 1 / Round 2 logic (decision A + B)</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py lines 94-241 in a single pass to cover the full resume_pipeline body:
  - CAS at line 124 — DO NOT TOUCH (D-22).
  - load_run at line 133; load_roster_for_business at line 136; load_source_email at line 139; combined_email build at line 140.
  - Pre-vs-post alias diff block: STEP A at lines 153-162 (_pre_candidates, _pre_reconciliation, _pre_resolved_ids), STEP B at line 165 (_run_stages call), STEP C at lines 167-234 (post-stages alias binding + error boundary).
  - The error boundary at line 235-241.

  CRITICAL constraints from live code:
  - get_outbound_message_id has a purpose allowlist (updated in Task 1 to include "clarification_field_regression").
  - _run_stages now returns extracted (after Task 2). Capture it.
  - set_clarified_fields and load_clarified_fields are now available (after Task 1).
  - The Round 1 detection + write + send + return sequence must happen BEFORE the existing alias-diff STEP C block, or the alias-diff will run unnecessarily for a Round 1 return.

  ARCHITECTURE (decision A + B — TWO separate inbound calls, locked):

  ROUND 1 (partial reply arrives, field regression detected):
  resume_pipeline is called. After claim_status + load_run + combined_email build:
  1. Load snapshot and clarified_fields from DB.
  2. Call extracted = _run_stages(run_id, combined_email, roster, prior=snapshot, resolved_drops=resolved_drops_from_confirmed, llm=llm).
     _run_stages with prior=snapshot detects drops, emits field_regression ValidationIssues, decide returns request_clarification, and _clarify is called inside _run_stages.
     HOWEVER: _clarify must send with purpose="clarification_field_regression" when called from a field-regression context. The question is how _clarify knows to use the new purpose. Design choice: _clarify detects this by checking whether the decision's gate_reasons contain "field_regression" — or accept a new `purpose` kwarg parameter. The cleanest approach: add `purpose: str = "clarification"` kwarg to _clarify (default preserves all existing behavior); _run_stages passes purpose="clarification_field_regression" when any field_regression issue exists in the issues list. This keeps _clarify's interface clean and the purpose explicit.
  3. BEFORE _clarify sends: write clarified_fields[employee_id][field]="asked" to DB for each detected drop. This is the durable loop-guard state that prevents re-asking on Round 2. The drop details are available from validate()'s output — specifically, the ValidationIssue.field values contain the qualified name which can be split to extract submitted_name and field_name, then resolved to employee_id via matches (same name_to_id logic as validate() uses internally). Alternatively, since validate() emits ValidationIssues whose field attribute is "{submitted_name}.{field}", resume_pipeline can extract the structured information from the decision's gate_reasons or from an additional return value.

  DESIGN DECISION for clarified_fields write: The cleanest approach that does not require validate() to return extra data: resume_pipeline can call detect_field_regression(snapshot, extracted) AGAIN after _run_stages returns (since detect_field_regression is a pure function, calling it twice is safe). The second call gives resume_pipeline the RawFieldDrop list it needs to build the {employee_id: {field: "asked"}} dict using the same name_to_id lookup pattern. This avoids threading extra return values through _run_stages.

  After writing clarified_fields with "asked" entries:
  - Resume_pipeline does NOT proceed further. The "request_clarification" branch already triggered _clarify inside _run_stages, which set AWAITING_REPLY. resume_pipeline returns after writing clarified_fields.

  ROUND 2 (client answers the "did you forget OT?" question):
  resume_pipeline is called again. After claim_status + load_run + combined_email build:
  1. Load snapshot and clarified_fields from DB. If clarified_fields has "asked" entries, this is a Round 2 resume.
  2. Call extracted = _run_stages(run_id, combined_email, roster, prior=None, resolved_drops=resolved_drops_from_confirmed, llm=llm). NOTE: prior=None here — detect_field_regression does NOT run, no field_regression issues emitted. This is the Round 2 termination guarantee.
  3. HOWEVER: _run_stages calls extract() and persist_extracted which would overwrite any backfilled values. The backfill hook (decision B) must run AFTER extract() but BEFORE persist_extracted. This requires splitting _run_stages OR having resume_pipeline call extract() + reconcile + detect + backfill + then the remaining stages manually.

  DECISION B IMPLEMENTATION: The post-extract backfill hook cannot be cleanly inserted between extract() and persist_extracted() from outside _run_stages without splitting the function. The cleanest solution: add a `post_extract_hook: Callable[[Extracted], Extracted] | None = None` kwarg to _run_stages. When provided, _run_stages calls `extracted = post_extract_hook(extracted)` IMMEDIATELY after `extracted = extract(...)` and BEFORE `repo.persist_extracted(run_id, extracted)`. resume_pipeline constructs this hook on Round 2 to backfill carried_forward fields from the snapshot.

  The post_extract_hook for Round 2:
  - Iterates each "asked" entry in clarified_fields that was resolved as "carried_forward".
  - Finds the matching ExtractedEmployee in extracted.employees by submitted_name.
  - Sets `setattr(emp, field, snapshot_value)` where snapshot_value is found in the pre_clarify_extracted snapshot.
  - Returns the modified extracted.
  - After the hook runs and persist_extracted runs with the backfilled extracted, resolve the outcomes: update clarified_fields outcomes from "asked" to "carried_forward" or "confirmed_dropped" by re-running detect_field_regression(snapshot, extracted_after_hook) and checking resumed_value is None vs Decimal("0").
  - Write updated clarified_fields to DB via set_clarified_fields.

  TERMINATION: On Round 2, _run_stages is called with prior=None → no field_regression issues → decide returns "process" (or clarifies for a DIFFERENT reason) → no second AWAITING_REPLY for field regression. The existing purpose="clarification_field_regression" row prevents a duplicate send via the idempotency guard in _clarify (which now knows about this purpose via the updated allowlist). The loop terminates naturally.
  </read_first>

  <action>
  This task implements the two-inbound architecture in resume_pipeline and the post_extract_hook mechanism in _run_stages.

  Change A — Add `purpose` kwarg to `_clarify` and update `_run_stages` to pass it:
  In _clarify: add `purpose: str = "clarification"` as a keyword-only parameter (after `*, llm` becomes `*, llm, purpose="clarification"`). Use `purpose` everywhere the string `"clarification"` is hardcoded inside _clarify: the `get_outbound_message_id(run_id, purpose="clarification")` guard at line 318, the `insert_email_message(..., purpose="clarification", ...)` call at line 436, and the `gateway.send_outbound(..., purpose="clarification", ...)` call at line 449. Replace all three hardcoded "clarification" strings with the `purpose` variable. The default value "clarification" preserves all existing call behavior.

  In _run_stages: detect whether any issue in `issues` has `issue_type == "field_regression"`. If yes, pass `purpose="clarification_field_regression"` to the `_clarify(...)` call. Otherwise pass the default (`purpose="clarification"` or simply omit the kwarg to use the default). The issues list is already available at line ~275 (result of validate()).

  Change B — Add `post_extract_hook` kwarg to `_run_stages`:
  Add `post_extract_hook=None` as a keyword-only kwarg to _run_stages. After `extracted = extract(email, roster, **extract_kwargs)` (line ~269), and BEFORE `repo.persist_extracted(run_id, extracted)` (line ~278), add: `if post_extract_hook is not None: extracted = post_extract_hook(extracted)`.

  Change C — Update resume_pipeline to implement Round 1 and Round 2 logic:

  INSERT after `combined_email = _combined_context_email(inbound, original_body)` (line ~140) and before the existing alias-diff pre-data load (line ~153):

  ROUND 1 / ROUND 2 DETECTION:
  `snapshot = repo.load_pre_clarify_extracted(run_id)`
  `clarified = repo.load_clarified_fields(run_id)`
  `_asked_fields = {(emp_id, field): outcome for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "asked"}`
  `_confirmed_drops = {(uuid.UUID(emp_id), field) for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "confirmed_dropped"}`

  ROUND 2 BACKFILL HOOK (if any "asked" entries exist from a prior Round 1):
  If `_asked_fields` is not empty (this is a Round 2 resume):
  Build the post_extract_hook closure that captures `snapshot` and `_asked_fields`:
  The hook function iterates through `_asked_fields` keys `(emp_id_str, field)`, finds the matching employee in the snapshot by employee_id (requires matching submitted_name → snapshot employee), and if `_is_paid(getattr(snapshot_emp, field))` is True (the field had a value in the snapshot), sets that value on the resumed extracted employee. The hook returns the modified extracted. Note: matching snapshot employees to resumed employees requires submitted_name as the join key (both extractions use the same submitted_name from the same email chain). Look up snapshot employee by submitted_name; look up resumed employee by submitted_name.

  Construct the ROUND 2 _run_stages call:
  `extracted = _run_stages(run_id, combined_email, roster, prior=None, resolved_drops=_confirmed_drops, post_extract_hook=backfill_hook, llm=llm)`
  After this call: resolve outcomes for all "asked" entries by calling `detect_field_regression(snapshot, extracted)` to get current drops, then for each asked field, check whether the resumed extracted has the field restored (carry_forward was effective) or still absent/zero. Update clarified dict with final outcomes ("carried_forward" or "confirmed_dropped"). Call `repo.set_clarified_fields(run_id, clarified)`.
  Then continue to the existing alias-diff block (STEP A/B/C) — alias learning still applies on Round 2.

  ROUND 1 DETECTION AND SEND (if no "asked" entries — this is a first or fresh resume):
  `extracted = _run_stages(run_id, combined_email, roster, prior=snapshot, resolved_drops=_confirmed_drops, llm=llm)`
  After _run_stages: check whether the run is now in AWAITING_REPLY status (a field_regression issue triggered _clarify inside _run_stages).
  If AWAITING_REPLY: write clarified_fields "asked" entries and RETURN (do not continue to alias diff).
    - Re-call `detect_field_regression(snapshot, extracted)` to get the RawFieldDrop list.
    - Build `name_to_id` from the post-run reconciliation (load_run → reconciliation → build map).
    - For each raw_drop: look up employee_id via name_to_id[raw_drop.submitted_name] (skip if unresolved per D-11).
    - Update clarified dict: `clarified.setdefault(str(emp_id), {})[raw_drop.field] = "asked"`.
    - Call `repo.set_clarified_fields(run_id, clarified)`.
    - RETURN from resume_pipeline (the run is already in AWAITING_REPLY; the alias-diff is skipped on Round 1).
  If NOT in AWAITING_REPLY (no field regression or other clarification reason): continue to alias-diff block normally. The existing STEP A/B/C alias binding logic runs as before.

  STATUS CHECK: to detect AWAITING_REPLY after _run_stages, call `run_data = repo.load_run(run_id)` and check `run_data.get("status") == "awaiting_reply"`. This is a cheap targeted read; the run data is already being loaded anyway for the alias diff.

  All new code stays inside the existing `except Exception as exc` boundary (orchestrator.py:235-241).

  Import `detect_field_regression` from `app.pipeline.validate` at the top of orchestrator.py if not already imported. Add to the `from app.pipeline.validate import validate` import line.
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import resume_pipeline; print('OK')"</automated>
  </verify>
  <done>`from app.pipeline.orchestrator import resume_pipeline` imports without error. `grep "clarification_field_regression" app/pipeline/orchestrator.py` exits 0. `grep "set_clarified_fields" app/pipeline/orchestrator.py` exits 0. `grep "post_extract_hook" app/pipeline/orchestrator.py` exits 0. `grep "load_pre_clarify_extracted" app/pipeline/orchestrator.py` exits 0. Existing orchestrator tests still pass: `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Schema DDL applied to dev Supabase DB via bootstrap (Task 1). Repo helpers + purpose allowlist expanded (Task 1). Orchestrator threaded with two-inbound Round 1/Round 2 logic (Tasks 2-3). All existing test suite should be GREEN.</what-built>
  <how-to-verify>
  1. Confirm repo helpers + updated allowlist are importable:
     `uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; print('OK')"` — prints "OK".
  2. Confirm schema update (if DATABASE_URL is set):
     `uv run python -m app.db.bootstrap` exits 0.
     Then verify all new columns + purpose value exist:
     `uv run python -c "import psycopg, os, json; conn = psycopg.connect(os.environ['DATABASE_URL']); cur = conn.cursor(); cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\"); print('payroll_runs columns:', [r[0] for r in cur.fetchall()]); cur.execute(\"SELECT check_clause FROM information_schema.check_constraints WHERE constraint_name LIKE '%purpose%' OR constraint_name LIKE '%email%' LIMIT 5\"); print('purpose constraints:', [r[0] for r in cur.fetchall()]); conn.close()"`
  3. Run full suite: `uv run pytest -q` — should be green (3 xfail stubs in test_resume_pipeline.py are acceptable).
  4. Confirm orchestrator wiring compiles: `uv run python -c "from app.pipeline.orchestrator import resume_pipeline; print('OK')"` — no ImportError.
  5. Confirm the new purpose value does not crash get_outbound_message_id:
     `uv run python -c "from app.db.repo import get_outbound_message_id; import uuid; print(get_outbound_message_id.__doc__[:50])"` — does not raise.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo.py JSONB writes | pre_clarify_extracted and clarified_fields written via json.dumps (never f-string SQL) |
| orchestrator.py resume_pipeline | Reads clarified_fields from DB; the dict keys are employee_id strings and field names — no arbitrary code execution risk |
| email_messages.purpose CHECK | DB-enforced allowlist now includes 'clarification_field_regression'; any unexpected purpose value raises a constraint violation at INSERT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | `clarified_fields` JSONB written by `set_clarified_fields` | mitigate | Uses `json.dumps(clarified)` with `%s` placeholder — no f-string SQL injection vector (V5). The `clarified` dict is built from typed RawFieldDrop output + name_to_id resolution, not raw user input. |
| T-07-09 | Tampering | `pre_clarify_extracted IS NULL` guard | mitigate | Guard is in SQL: `WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id` — atomic check-and-write, no Python read-then-write race (snapshot-once D-19). |
| T-07-10 | Information Disclosure | PII in orchestrator resume_pipeline new code | mitigate | All new code in resume_pipeline stays inside the existing `except` boundary (lines 235-241) which persists only `type(exc).__name__`. `str(exc)` never logged for new code. |
| T-07-11 | Denial of Service | `set_pre_clarify_extracted` called on every `_clarify` invocation | accept | IS NULL guard means only the first call does a write; subsequent calls are a CAS miss returning 0 rows. O(1) per clarification cycle. |
| T-07-12 | Spoofing | New purpose value "clarification_field_regression" in email_messages | mitigate | DB CHECK constraint now explicitly includes the new value. `get_outbound_message_id` allowlist updated. Any caller passing an unknown purpose raises ValueError at the Python layer before touching the DB. |
| T-07-SC | Tampering | No new package installs | accept | No `uv add` invoked; psycopg already in pyproject.toml |
</threat_model>

<verification>
Full plan verification:

1. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
2. `grep "clarified_fields JSONB" app/db/schema.sql` — exits 0
3. `grep "clarification_field_regression" app/db/schema.sql` — exits 0 (CHECK constraint)
4. `grep "clarification_field_regression" app/db/repo.py` — exits 0 (allowlist)
5. `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` — exits 0 (CAS guard)
6. `grep "def set_pre_clarify_extracted" app/db/repo.py` — exits 0
7. `grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py` — exits 0
8. `grep "set_clarified_fields" app/pipeline/orchestrator.py` — exits 0
9. `grep "clarification_field_regression" app/pipeline/orchestrator.py` — exits 0
10. `grep "post_extract_hook" app/pipeline/orchestrator.py` — exits 0
11. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` — all GREEN
</verification>

<success_criteria>
- Two new JSONB columns in schema.sql (inline + ALTER blocks) and applied to dev DB
- email_messages.purpose CHECK in schema.sql includes 'clarification_field_regression'
- get_outbound_message_id() allowlist includes 'clarification_field_regression'
- Four repo helpers exist and are importable from app.db.repo
- _clarify() accepts `purpose` kwarg (default "clarification"); writes snapshot before AWAITING_REPLY (IS NULL guard)
- _run_stages() returns extracted; accepts prior=, resolved_drops=, post_extract_hook= kwargs; passes purpose="clarification_field_regression" to _clarify when field_regression issues detected
- resume_pipeline implements the two-inbound architecture (decision A):
  - Round 1: detect drops → write clarified_fields "asked" BEFORE send → send with purpose="clarification_field_regression" → AWAITING_REPLY → RETURN
  - Round 2: detect "asked" state → build backfill hook → _run_stages(prior=None, post_extract_hook) → resolve outcomes → set_clarified_fields → proceed to calc
- Termination via purpose+asked state (NOT via prior=None blunt suppression — D-20 superseded by decision A)
- Full existing test suite green (no regressions)
- Human checkpoint confirms columns + purpose CHECK update exist in dev DB
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md` when done.
</output>
