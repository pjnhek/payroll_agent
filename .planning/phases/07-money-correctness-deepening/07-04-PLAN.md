---
phase: 07-money-correctness-deepening
plan: "04"
type: execute
wave: 4
depends_on:
  - "07-03"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so the two new JSONB columns (pre_clarify_extracted, clarified_fields) exist before integration tests run"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Settings → Database → Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local .env and shell env"
        location: "local .env file"

must_haves:
  truths:
    - "payroll_runs has pre_clarify_extracted JSONB column (nullable, D-19) applied to dev DB via bootstrap"
    - "payroll_runs has clarified_fields JSONB column (nullable, D-13) applied to dev DB via bootstrap"
    - "Four new repo helpers exist: set_pre_clarify_extracted (IS NULL CAS), load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields"
    - "_clarify() writes pre_clarify_extracted snapshot BEFORE set_status(AWAITING_REPLY), with IS NULL SQL guard (snapshot-once, D-19/D-21)"
    - "_run_stages() returns extracted object and accepts prior=None + resolved_drops=None kwargs (D-18/D-20)"
    - "resume_pipeline() loads snapshot + clarified_fields, builds resolved_drops set, threads them into _run_stages (D-15/D-20)"
    - "D-21 timing: snapshot is captured in _clarify, NOT resume_pipeline (at the awaiting_reply pause)"
    - "D-20 termination: second _run_stages call in resume_pipeline uses prior=None so detect_field_regression does not run again — no infinite loop"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields inline + ALTER blocks"
      contains: "pre_clarify_extracted JSONB"
    - path: "app/db/repo.py"
      provides: "4 new repo helpers after set_alias_candidates"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "_clarify snapshot write + _run_stages return + resume_pipeline D-20 5-step sequence"
      contains: "set_pre_clarify_extracted"
  key_links:
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called before repo.set_status(AWAITING_REPLY); IS NULL guard in SQL makes second call no-op"
      pattern: "set_pre_clarify_extracted"
    - from: "app/pipeline/orchestrator.py:resume_pipeline"
      to: "app/db/repo.py:load_pre_clarify_extracted"
      via: "loaded before _run_stages call; passed as prior= kwarg"
      pattern: "load_pre_clarify_extracted"
    - from: "app/pipeline/orchestrator.py:_run_stages"
      to: "app/pipeline/validate.py:validate"
      via: "prior= and resolved_drops= threaded from _run_stages down to validate call"
      pattern: "prior=prior"
---

<objective>
Wire the MONEY-03 state machine: add two nullable JSONB columns to the DB schema, implement four repo helpers following the `alias_candidates` pattern, and thread the D-20 ordering through `_clarify`, `_run_stages`, and `resume_pipeline` in the orchestrator. The pure judgment logic (Plans 02-03) is already proven; this plan wires the storage and orchestration around it.

Purpose: Closes the MONEY-03 requirement end-to-end. After this plan, a field-regression clarification reply goes in, the snapshot is read, drops are detected, the outcome is recorded, and the carry-forward or explicit-drop decision is applied before validate/decide run — with the loop guard preventing infinite re-clarification.
Output: Schema DDL applied to dev DB, repo helpers, orchestrator wiring. The D-28 snapshot-once and D-20 loop-guard integration tests in test_resume_pipeline.py turn GREEN.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07-money-correctness-deepening/07-RESEARCH.md
@.planning/phases/07-money-correctness-deepening/07-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-03-SUMMARY.md

# Source files to read before editing (one pass each, no re-reads)
@app/db/schema.sql
@app/db/repo.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema DDL + repo helpers + bootstrap apply</name>
  <files>app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/db/schema.sql fully (find the alias_candidates column declaration ~line 89-90 and the ALTER block ~lines 105-108 — these are the exact patterns to mirror). Read app/db/repo.py lines 85-100 (RUN_COLS + imports), lines 340-375 (claim_status CAS pattern), lines 405-430 (persist_extracted serializer pattern), lines 505-530 (set_alias_candidates — the exact template). PATTERNS.md §5 has the exact SQL to add. PATTERNS.md §6 has all four repo helper implementations verbatim. RESEARCH.md §Schema Migration Precedent confirms: do NOT add new columns to RUN_COLS; use separate targeted helpers. RESEARCH.md §Target 6 note on psycopg JSONB return: handle `json.loads(row[0]) if isinstance(row[0], str) else row[0]`.
  </read_first>

  <action>
  Step 1 — Update app/db/schema.sql (per PATTERNS.md §5):
  a. In the `CREATE TABLE IF NOT EXISTS payroll_runs` block, after the `alias_candidates JSONB,` line (~line 90), add two new inline column declarations:
     `pre_clarify_extracted JSONB,    -- D-19: snapshot of extracted_data at awaiting_reply pause (MONEY-03)`
     `clarified_fields JSONB,          -- D-13: {employee_id: {field: outcome}} field-regression outcomes (MONEY-03)`

  b. In the idempotent ALTER TABLE block (after the `alias_candidates` ALTER at ~line 107), add:
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03`
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields JSONB;       -- D-13 MONEY-03`

  Step 2 — Add four repo helpers to app/db/repo.py (per PATTERNS.md §6):
  Place all four helpers after `set_alias_candidates` (~line 524). Follow the exact transaction/connection pattern from `set_alias_candidates` and `claim_status`:
  - Always `%s` placeholders, never f-string SQL (V5/security constraint)
  - Always `str(run_id)` for UUID params
  - Always `json.dumps(obj)` for JSONB writes (never pass dict directly)

  a. `set_pre_clarify_extracted(run_id: uuid.UUID, extracted: Extracted, conn=None) -> bool`:
     SQL: `UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id`
     Serialize: `json.dumps(extracted.model_dump(mode="json"))` (per persist_extracted pattern at line 419)
     Returns True if row was written, False if already set (snapshot-once guard, D-19)

  b. `load_pre_clarify_extracted(run_id: uuid.UUID, conn=None) -> Extracted | None`:
     SQL: `SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s`
     If NULL: return None
     Deserialize: `Extracted.model_validate(json.loads(row[0]) if isinstance(row[0], str) else row[0])`

  c. `set_clarified_fields(run_id: uuid.UUID, clarified: dict, conn=None) -> None`:
     SQL: `UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s`
     Serialize: `json.dumps(clarified)` (plain dict, mirrors set_alias_candidates)

  d. `load_clarified_fields(run_id: uuid.UUID, conn=None) -> dict`:
     SQL: `SELECT clarified_fields FROM payroll_runs WHERE id = %s`
     Returns `{}` on NULL (mirrors the `_pre_candidates or {}` fallback pattern in resume_pipeline:154)
     Deserialize: `json.loads(row[0]) if isinstance(row[0], str) else row[0]`

  Import `Extracted` in repo.py if not already imported (check the imports block; it is likely not imported since repo.py uses `json.dumps(extracted.model_dump(mode="json"))` as a pattern — confirm `Extracted` type hint needs the import or use a string annotation).

  Step 3 — Apply schema to dev DB:
  `uv run python -m app.db.bootstrap`
  This runs the idempotent ALTER TABLE blocks. If DATABASE_URL is not set in the shell env: skip the bootstrap apply step and leave a human-checkpoint comment in the SUMMARY noting the columns must be applied before integration tests in Plan 05.
  </action>

  <verify>
    <automated>uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; print('OK')"</automated>
  </verify>
  <done>Four repo helpers import successfully (the import check above exits 0 and prints "OK"). `grep "pre_clarify_extracted JSONB" app/db/schema.sql` exits 0. `grep "clarified_fields JSONB" app/db/schema.sql` exits 0. `grep "def set_pre_clarify_extracted" app/db/repo.py` exits 0. Bootstrap applied to dev DB (or noted as pending in SUMMARY).</done>
</task>

<task type="auto">
  <name>Task 2a: _run_stages threading + _clarify snapshot write (D-21 timing)</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py lines 260-399 in a single pass to cover:
  - `_run_stages` lines 260-289: current signature `def _run_stages(run_id, email, roster, *, llm) -> None`; the validate call at ~line 277; the _clarify call at the end of the else-branch (~line 291).
  - `_clarify` lines 292-399: current signature; where `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` is called — snapshot write goes BEFORE this call.
  PATTERNS.md §7 has both wiring patterns verbatim. CONTEXT.md D-18 locks "_run_stages stays a single shared function." CONTEXT.md D-19/D-21 lock snapshot timing: snapshot in _clarify, BEFORE AWAITING_REPLY. CONTEXT.md D-22 locks the CAS in resume_pipeline — DO NOT TOUCH it.
  </read_first>

  <action>
  This task makes two targeted changes to orchestrator.py, each isolated from the other:

  Change A — Update `_run_stages` signature and return (per PATTERNS.md §7, D-18):
  1. Add `prior: Extracted | None = None` and `resolved_drops: set | None = None` keyword-only kwargs after `*, llm` in the `_run_stages` signature.
  2. Change `issues = validate(extracted, roster, matches)` to `issues = validate(extracted, roster, matches, prior=prior, resolved_drops=resolved_drops)`.
  3. Change the `_clarify(run_id, email, decision, roster, llm=llm)` call to `_clarify(run_id, email, decision, roster, extracted, llm=llm)` so the snapshot can be written inside _clarify (per D-21).
  4. Change the return type annotation from `-> None` to `-> Extracted` and add `return extracted` at the end of `_run_stages` (after all persist calls and both branches). The existing `run_pipeline` call site assigns or ignores the return value — if it does not use it, no change needed at the call site; if it does `_run_stages(...)` without assignment, the return is silently dropped, which is fine.

  Change B — Update `_clarify` signature and add snapshot write (per PATTERNS.md §7, D-19/D-21):
  1. Add `extracted: Extracted` as a positional parameter before `*, llm` in `_clarify`'s signature: `def _clarify(run_id, email, decision, roster, extracted, *, llm) -> None`.
  2. Locate the `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` call inside `_clarify`. Immediately BEFORE it, add: `repo.set_pre_clarify_extracted(run_id, extracted)`. The IS NULL guard in set_pre_clarify_extracted's SQL makes this idempotent — if _clarify is called twice (e.g. name-clarify then field-drop clarify), only the first write succeeds, preserving the original snapshot (D-19/D-28 snapshot-once).
  3. Keep ALL new code inside the existing try/except boundary (orchestrator.py:235-241).

  Add `from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields` to the repo import block at the top of orchestrator.py if those names are not already accessible via the existing `from app.db import repo` import.
  </action>

  <verify>
    <automated>uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>Existing orchestrator + clarify tests pass (no regressions). `grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py` exits 0. `grep "prior=prior" app/pipeline/orchestrator.py` exits 0. `grep "return extracted" app/pipeline/orchestrator.py` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2b: resume_pipeline D-20 5-step post-stages sequence (loop-guard)</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py lines 94-241 in a single pass to cover:
  - `resume_pipeline` full body: CAS at line 124 (DO NOT TOUCH, D-22), alias diff at lines 153-165, existing `_run_stages` call (~line 165), error boundary at 235-241.
  PATTERNS.md §7 D-20 new steps for resume_pipeline. CONTEXT.md D-13/D-14/D-15/D-20 for the outcome-resolve logic.
  This task depends on Task 2a completing first (repo helpers importable, _run_stages returns extracted, _clarify takes extracted).
  </read_first>

  <action>
  This is the locked, numbered implementation of the D-20 loop-guard sequence in resume_pipeline. Execute these steps in ORDER — the ordering is the termination guarantee (per D-20).

  Step 1 — Before the existing `_run_stages` call in resume_pipeline, insert the D-20.1 load block:
  Load the snapshot and clarified outcomes written by the earlier clarification round:
  `snapshot = repo.load_pre_clarify_extracted(run_id)`
  `clarified = repo.load_clarified_fields(run_id)`
  Build the resolved_drops set for D-15 MONEY-01 short-circuit:
  `resolved_drops = {(uuid.UUID(emp_id), field) for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "confirmed_dropped"}`
  Add `import uuid` at the top of orchestrator.py if not already imported.

  Step 2 — Replace the existing `_run_stages(run_id, combined_email, roster, llm=llm)` call with:
  `extracted = _run_stages(run_id, combined_email, roster, prior=snapshot, resolved_drops=resolved_drops, llm=llm)`
  This is the first stages pass. With `prior=snapshot`, `validate()` runs `detect_field_regression(snapshot, extracted)` and emits `field_regression` issues for any drops not yet resolved. `decide()` sees those issues and returns `request_clarification` — triggering `_clarify`, which writes the `clarified_fields` outcome as `asked`. Capture the return value (`extracted`) for use in step 3.

  Step 3 — Post-stages outcome-resolve. Immediately after the first `_run_stages` call (inside the same try block, after the `extracted = _run_stages(...)` line): if `snapshot is not None` AND `clarified` contains any field with outcome `asked`, resolve the drop outcomes now. Load the freshly-overwritten `extracted_data` from DB via `repo.load_run` or use the `extracted` object returned from `_run_stages` directly (prefer the in-memory return value — no extra DB round-trip). Reduce it by employee_id (D-12 last-wins): build `{submitted_name: ExtractedEmployee}` by iterating `extracted.employees` in order, last-wins. Call `detect_field_regression(snapshot, extracted)` to get the current drops list. For each drop whose field appears as `asked` in `clarified[str(emp_id)][drop.field]` (look up `emp_id` from the matches persisted to DB or from the `extracted.employees` submitted_name → resolved employee_id mapping): resolve per D-14 — if `drop.resumed_value is None`: outcome is `carried_forward`; if `drop.resumed_value == Decimal("0")`: outcome is `confirmed_dropped`. Update the `clarified` dict with the resolved outcome.

  Step 4 — Backfill carried-forward values. For each drop resolved as `carried_forward` in step 3: backfill the field's original value from `snapshot` into `extracted`. Find the matching `ExtractedEmployee` in `extracted.employees` by `submitted_name` and set `setattr(emp, drop.field, drop.original_value)`. Then call `repo.persist_extracted(run_id, extracted)` once after all backfills so the DB row, paystub, and reconciliation agree on one source of truth (D-16). Write the final outcomes to DB: `repo.set_clarified_fields(run_id, clarified)` with all outcomes now set to `carried_forward` or `confirmed_dropped` (no more `asked` entries for this round).

  Step 5 — Second stages pass with prior=None (the termination guarantee per D-20). Build the `resolved_drops` set again from the updated `clarified` dict (now includes `confirmed_dropped` entries from this round). Call: `_run_stages(run_id, combined_email, roster, prior=None, resolved_drops=resolved_drops, llm=llm)`. The `prior=None` argument is what makes the loop terminate — `detect_field_regression` does not run on this second pass, so no stray `field_regression` issue can re-trigger `decide` → no infinite loop. The `resolved_drops` set carries `confirmed_dropped` pairs so MONEY-01 does not re-flag them (D-15). The second pass re-extracts, reconciles, validates, decides, and persists the final decision — this time without field_regression issues, so decide proceeds to `process` (or clarifies for a different reason if the reply introduced new problems).

  Note: steps 3-5 only execute when `snapshot is not None` AND the first `_run_stages` pass resulted in `request_clarification` due to `field_regression`. When `snapshot is None` (first-time processing), or when `clarified` has no `asked` entries, skip steps 3-5 entirely and let the first `_run_stages` result stand. Guard with: `if snapshot is not None and any(outcome == "asked" for fields in clarified.values() for outcome in fields.values()):`.

  All new code stays inside the existing `except Exception as exc` boundary at orchestrator.py:235-241 (the D-A1-03 PII-safe error boundary). Never log `str(exc)` — only `type(exc).__name__`.
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import resume_pipeline; print('OK')"</automated>
  </verify>
  <done>`from app.pipeline.orchestrator import resume_pipeline` imports without error. `grep "resolved_drops" app/pipeline/orchestrator.py` exits 0. `grep "load_pre_clarify_extracted" app/pipeline/orchestrator.py` exits 0. `grep "prior=None" app/pipeline/orchestrator.py` exits 0 (the second _run_stages call). Existing orchestrator tests still pass: `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Schema DDL applied to dev Supabase DB via bootstrap (Task 1). Orchestrator threaded with snapshot + clarified_fields (Tasks 2a/2b). All existing test suite should be GREEN.</what-built>
  <how-to-verify>
  1. Confirm repo helpers are importable: `uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; print('OK')"` — should print "OK".
  2. Confirm bootstrap was applied (if DATABASE_URL is set): `uv run python -m app.db.bootstrap` exits 0. Then verify columns exist via a psycopg query: `uv run python -c "import psycopg, os; conn = psycopg.connect(os.environ['DATABASE_URL']); cur = conn.cursor(); cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\"); print('Columns:', [r[0] for r in cur.fetchall()]); conn.close()"` — should print both column names.
  3. Run full suite: `uv run pytest -q` — should be green (xfail on the 3 integration stubs in test_resume_pipeline.py is acceptable).
  4. Confirm orchestrator wiring compiles: `uv run python -c "from app.pipeline.orchestrator import resume_pipeline; print('OK')"` — no ImportError.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo.py JSONB writes | pre_clarify_extracted and clarified_fields written via json.dumps (never f-string SQL) |
| orchestrator.py resume_pipeline | Reads clarified_fields from DB; the dict keys are employee_id strings and field names — no arbitrary code execution risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Tampering | `clarified_fields` JSONB written by `set_clarified_fields` | mitigate | Uses `json.dumps(clarified)` with `%s` placeholder — no f-string SQL injection vector (V5). The `clarified` dict is built from typed NameMatchResult + detect_field_regression output, not raw user input. |
| T-07-09 | Tampering | `pre_clarify_extracted IS NULL` guard | mitigate | Guard is in SQL: `WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id` — atomic check-and-write, no Python read-then-write race (snapshot-once D-19, resolves open question 5). |
| T-07-10 | Information Disclosure | PII in orchestrator resume_pipeline new code | mitigate | All new code in resume_pipeline stays inside the existing `except` boundary (lines 235-241) which persists only `type(exc).__name__`. `str(exc)` never logged for new code. `pre_clarify_extracted` contains hours numbers only (no names beyond submitted_name already in extracted_data). |
| T-07-11 | Denial of Service | set_pre_clarify_extracted called on every _clarify invocation | accept | IS NULL guard means only the first call does a write; subsequent calls are a CAS miss returning 0 rows. Trivially O(1) per clarification cycle. |
| T-07-SC | Tampering | No new package installs | accept | No `uv add` invoked; psycopg already in pyproject.toml |
</threat_model>

<verification>
Full plan verification:

1. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
2. `grep "clarified_fields JSONB" app/db/schema.sql` — exits 0
3. `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` — exits 0 (CAS guard)
4. `grep "def set_pre_clarify_extracted" app/db/repo.py` — exits 0
5. `grep "def load_pre_clarify_extracted" app/db/repo.py` — exits 0
6. `grep "set_pre_clarify_extracted" app/pipeline/orchestrator.py` — exits 0
7. `grep "load_pre_clarify_extracted" app/pipeline/orchestrator.py` — exits 0
8. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` — all GREEN
</verification>

<success_criteria>
- Two new JSONB columns in schema.sql (inline + ALTER blocks) and applied to dev DB
- Four repo helpers exist and are importable from app.db.repo (import-check command exits 0)
- _clarify writes snapshot before AWAITING_REPLY status (IS NULL guard in SQL)
- _run_stages returns extracted and accepts prior=, resolved_drops= kwargs
- resume_pipeline implements the D-20 locked 5-step sequence: load snapshot + clarified → first _run_stages(prior=snapshot) → outcome-resolve → backfill carried_forward + persist → second _run_stages(prior=None, resolved_drops=confirmed_dropped_set)
- The second _run_stages call uses prior=None — this is the termination guarantee (detect_field_regression does not run on pass 2, no infinite loop)
- Full existing test suite green (no regressions)
- Human checkpoint confirms columns exist in dev DB
</success_criteria>

<output>
Create `.planning/phases/07-money-correctness-deepening/07-04-SUMMARY.md` when done.
</output>
