---
phase: 18-failure-policy-sweep-deletion
plan: 10
type: execute
wave: 7
depends_on: [18-04]
files_modified:
  - app/pipeline/orchestrator.py
  - tests/test_orchestrator_states.py
  - tests/test_resume_pipeline.py
autonomous: true
requirements: [FAIL-01, FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Only after every BackgroundTask wrapper, queue handler, dispatch seam, and drain settlement consumer exists, run_pipeline and resume_pipeline both return PipelineResult on every control path (D-01)."
    - "Neither orchestrator entry point persists terminal/error state; wrappers own background terminal CAS and drain owns queued terminal settlement, so one failure has one persistence owner (D-06, D-07, D-16)."
    - "Both producers stop returning None in this plan; Plan 18-11 immediately narrows every compatibility-era consumer, forwarder, and normalizer annotation/branch after this producer cutover."
    - "Completed process, completed request_clarification, and lost resume claims return coarse OK; stage-aware exceptions return bounded RETRYABLE or TERMINAL without PII (D-02, D-03)."
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "Final shared PipelineResult producer contract for initial and resume entry points"
    - path: "tests/test_resume_pipeline.py"
      provides: "Both producer entry points return explicit bounded outcomes on every control path"
  key_links:
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/result.py"
      via: "both catch boundaries classify by active bounded stage and all success/no-op paths return OK"
      pattern: "PipelineResult"
    - from: "app/routes/pipeline_glue.py|app/queue/drain.py"
      to: "app/db/repo/job_settlement.py"
      via: "existing consumers already map RETRYABLE and TERMINAL; Plan 18-11 narrows their temporary normalizer/union seams"
      pattern: "normalize_pipeline_result|settle_pipeline_job"
---

<objective>
Perform the producer cutover after every failure-result consumer is installed and before the focused compatibility-closure plan narrows those seams.

Purpose: The additive sequence stays green in every earlier wave, then FAIL-01 becomes real without any interval where failures are swallowed or kinds are unhandled.
Output: two explicit orchestrator producers with no terminal persistence; Plan 18-11 owns compatibility closure and caller-inventory proof.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-01-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-04-SUMMARY.md

<interfaces>
- Plans 18-03/18-04/18-09 already consume explicit outcomes and temporarily treat None as legacy OK only through normalize_pipeline_result.
- Background wrappers settle terminal runs without a leased job; drain settles queued job/run outcomes atomically.
- The orchestrator still owns legacy record_run_error calls until this plan removes them in the same change that makes every path return PipelineResult.
</interfaces>
</context>

<artifacts_this_phase_produces>
- run_pipeline, _run, and resume_pipeline return PipelineResult on every path.
- Stage-aware classification at load/extract/persist/clarification/compute/delivery boundaries.
- Removal of orchestrator-owned record_run_error persistence.
- Existing consumers continue to accept the temporary compatibility union only until the immediately dependent Plan 18-11; no new compatibility branch is added here.
- No new job kind, queue surface, route, status, schema object, dependency, or Phase 19 producer migration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut both orchestrator entry points over to the explicit result</name>
  <files>app/pipeline/orchestrator.py, tests/test_orchestrator_states.py, tests/test_resume_pipeline.py</files>
  <read_first>
    - app/pipeline/orchestrator.py run_pipeline/_run and resume_pipeline catch boundaries plus clarification return seam.
    - app/pipeline/result.py classifier and temporary normalize_pipeline_result None branch, which remains until Plan 18-11.
    - tests/test_orchestrator_states.py result/classification matrix from Plan 18-01.
    - tests/test_resume_pipeline.py claim-loser, field-regression, and single-send cases.
  </read_first>
  <behavior>
    - Initial process and request_clarification success return OK; clarification sends exactly once.
    - Resume success and lost claim return OK.
    - Retryable extraction exceptions return RETRYABLE without writing payroll_runs.error.
    - Terminal exceptions return TERMINAL without writing payroll_runs.error.
    - Producer behavior and annotations contain no None return; the temporary normalizer remains unchanged until Plan 18-11 narrows all consumers together.
  </behavior>
  <action>
    Change run_pipeline, _run, and resume_pipeline to return PipelineResult on every control path. Track the active bounded PipelineStage immediately before load, extract, persist, clarification, compute, and delivery boundaries and classify only at the existing catch boundaries. Preserve roster-aware type-only logging, deterministic decisions, transaction scope, and exactly-one clarification send. Remove record_run_error and every other terminal-status persistence from both entry points in the same task: pre-cutover BackgroundTask wrappers already own background terminal CAS, and queued work already reaches atomic drain settlement. Return coarse OK after process, completed request_clarification, and duplicate/lost resume claim. Do not edit normalize_pipeline_result or consumer annotations here; Plan 18-11 narrows all compatibility seams together so this producer task boundary stays green. Replace the Plan 18-01 legacy source assertion with behavioral proof that neither producer entry point returns None and neither persists ERROR for retryable or terminal results.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_orchestrator_states.py tests/test_resume_pipeline.py -k "result or classification or clarification or claim"</automated>
  </verify>
  <acceptance_criteria>
    - Both public entry points and _run have PipelineResult annotations and no implicit return.
    - record_run_error is absent from both catch boundaries.
    - Every tested success/failure path returns a bounded result and exposes no injected sensitive text.
    - No producer returns None; the sole legacy adapter remains explicitly temporary and is removed by Plan 18-11.
  </acceptance_criteria>
  <done>The producer contract is explicit only after all consumers are capable of preserving its failure policy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator catch boundary to caller | Provider/DB exceptions become bounded policy values instead of persisted raw details. |
| explicit result to background/queue consumer | A discarded return would recreate swallowed failure. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-27 | Denial of Service | producer cutover | high | mitigate | Cut over only after consumer plans; both entry points have exhaustive return tests. |
| T-18-28 | Tampering | active caller result handling | high | mitigate | Non-vacuous AST caller inventory plus behavioral RETRYABLE/TERMINAL matrices. |
| T-18-29 | Information Disclosure | classified diagnostics | high | mitigate | Bounded stage/reason only and hostile-text absence tests. |
| T-18-10-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run the two task commands.
- Run uv run ruff check app/pipeline/orchestrator.py tests/test_orchestrator_states.py tests/test_resume_pipeline.py.
- Run uv run mypy app/pipeline/orchestrator.py.
</verification>

<success_criteria>
- FAIL-01 and D-01 through D-03 are complete only after consumers are installed.
- D-06/D-07/D-16 retain one persistence owner per execution mode.
- Producer None is eliminated; Plan 18-11 is the sole explicit owner for closing legacy consumer acceptance.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-10-SUMMARY.md when done.
</output>
