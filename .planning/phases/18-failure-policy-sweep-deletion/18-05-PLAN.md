---
phase: 18-failure-policy-sweep-deletion
plan: 05
type: execute
wave: 7
depends_on: [18-04]
files_modified:
  - app/routes/pump.py
  - tests/test_pump_route.py
  - tests/test_queue_durability.py
autonomous: true
requirements: [FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "A final-attempt lease reap increments dead and reaped_final_lease but does not increment claimed (D-14)."
    - "The pump accounting identity excludes reaped rows from claimed work while keeping every pre-existing DONE, RETRIED, DEAD, FENCED, and EMPTY count unchanged."
    - "The pump remains a bounded synchronous drain request and returns HTTP 200 when reaping is the only work available."
    - "No Phase 21 operational dashboard, new route, queue mutation seam, or job state is introduced."
  artifacts:
    - path: "app/routes/pump.py"
      provides: "Honest reaped_final_lease response accounting for shared drain outcomes"
    - path: "tests/test_pump_route.py"
      provides: "Hermetic pump response and arithmetic-invariant coverage"
    - path: "tests/test_queue_durability.py"
      provides: "Live endpoint proof for reap-only pump work"
  key_links:
    - from: "app/routes/pump.py"
      to: "app/queue/drain.py"
      via: "DrainOutcome.REAPED_FINAL_LEASE is counted outside claimed"
      pattern: "reaped_final_lease"
    - from: "tests/test_pump_route.py"
      to: "app/routes/pump.py"
      via: "claimed equals done plus retried plus non-reaped dead plus fenced"
      pattern: "claimed"
---

<objective>
Expose final-lease reaping honestly in the existing pump response without corrupting worker accounting.

Purpose: Operators and tests must be able to distinguish claimed work from maintenance settlement while Phase 17 pump behavior stays stable.
Output: one bounded response counter, revised arithmetic, and hermetic/live proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/17-the-pump/17-03-SUMMARY.md
@.planning/phases/17-the-pump/17-05-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-04-SUMMARY.md

<interfaces>
- drain_once() returns a truthy REAPED_FINAL_LEASE when it settles expired final-attempt work.
- PumpResponse already exposes claimed, done, retried, dead, fenced, and empty counters.
- The pump route remains the only HTTP wrapper over the shared bounded drain loop.
</interfaces>
</context>

<artifacts_this_phase_produces>
- New PumpResponse field reaped_final_lease with a zero default.
- Revised pump accounting that keeps reaped maintenance outside claimed.
- Hermetic response and invariant tests plus a live reap-only endpoint proof.
- No dashboard, new endpoint, worker loop, job kind, schema object, or dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Count reaped final leases without inflating claimed work</name>
  <files>app/routes/pump.py, tests/test_pump_route.py</files>
  <read_first>
    - app/routes/pump.py current PumpResponse and bounded loop accounting.
    - app/queue/drain.py DrainOutcome values and truthiness contract.
    - tests/test_pump_route.py existing endpoint, limit, and counter expectations.
  </read_first>
  <behavior>
    - REAPED_FINAL_LEASE increments dead and reaped_final_lease exactly once.
    - REAPED_FINAL_LEASE leaves claimed unchanged.
    - Existing outcomes retain their exact counter behavior.
    - Pump stops only on EMPTY or its configured bound and returns HTTP 200.
  </behavior>
  <action>
    Add reaped_final_lease: int = 0 to PumpResponse. Extend the existing outcome switch so REAPED_FINAL_LEASE increments both dead and reaped_final_lease but not claimed. Preserve the current counts for DONE, RETRIED, DEAD, FENCED, and EMPTY, including the bounded loop and authentication behavior. Encode the revised identity in focused tests: claimed == done + retried + (dead - reaped_final_lease) + fenced. Cover mixed sequences, reap-only work, zero-work, and response serialization. Keep this response narrowly operational; do not add per-job details or Phase 21 queue observability.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_pump_route.py</automated>
  </verify>
  <acceptance_criteria>
    - Response JSON contains reaped_final_lease.
    - Reaped rows are included in dead and excluded from claimed.
    - The revised accounting identity holds for every tested outcome sequence.
    - Existing pump authentication, bound, and status-code tests remain green.
  </acceptance_criteria>
  <done>The pump reports maintenance settlement separately while preserving its claimed-work meaning.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove reap-only pump work against live Postgres</name>
  <files>tests/test_queue_durability.py</files>
  <read_first>
    - tests/test_queue_durability.py Phase 17 pump and Phase 18 reaper fixtures.
    - app/routes/pump.py revised response model.
    - app/db/repo/job_settlement.py exact final-attempt reaper predicate.
  </read_first>
  <behavior>
    - A database containing only one exact eligible final lease yields dead=1, reaped_final_lease=1, claimed=0.
    - The job is dead and the run is error after the response.
    - An immediate second request reports no additional work.
  </behavior>
  <action>
    Add one queueproof endpoint test that seeds and directly backdates an exact attempts=max_attempts leased job, calls the real pump route through the existing test client, and asserts response accounting plus durable job/run state. Reuse the Phase 18 reaper setup helpers rather than sleeps or a second implementation. Call the endpoint again to prove the row is not counted twice. Keep live-environment skip behavior and diagnostics consistent with the current queueproof suite.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest tests/test_queue_durability.py -m queueproof -k "pump and reap" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - Live endpoint accounting matches D-14 exactly.
    - Durable state matches the already-proven reaper settlement.
    - A second call is idempotent at the response-counting layer.
  </acceptance_criteria>
  <done>The HTTP surface truthfully accounts for a real final-lease reap without claiming work no worker claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drain outcome to HTTP accounting | Internal settlement outcomes become operator-visible counters. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-16 | Repudiation | pump counters | high | mitigate | Separate reap counter, explicit invariant, and hermetic plus live proof. |
| T-18-17 | Tampering | claimed arithmetic | medium | mitigate | Table-driven mixed-outcome tests preserve every existing mapping. |
| T-18-05-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_pump_route.py.
- Run uv run pytest tests/test_queue_durability.py -m queueproof -k "pump and reap" -v -rs.
- Run uv run ruff check app/routes/pump.py tests/test_pump_route.py.
</verification>

<success_criteria>
- D-14 is directly represented in response fields and tests.
- Existing Phase 17 pump behavior is unchanged for non-reaper outcomes.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-05-SUMMARY.md when done.
</output>
