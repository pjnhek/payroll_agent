---
phase: 18-failure-policy-sweep-deletion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/result.py
  - tests/test_orchestrator_states.py
autonomous: true
requirements: [FAIL-01]
nyquist_compliant: true

must_haves:
  truths:
    - "One bounded ok/retryable/terminal contract exists for both orchestrator entry points, but this additive wave does not change their legacy return/persistence behavior (D-01, D-02)."
    - "The contract can represent completed deterministic request_clarification as ok without duplicating the business action (D-03)."
    - "Only extraction-stage OpenAI connection/timeout/rate-limit/5xx failures are retryable; schema/parse failures, ordinary 4xx responses, unclassified exceptions, and ambiguous clarification/delivery sends fail closed as terminal classifications without pre-writing run error."
    - "The result contains only bounded outcome, stage, and reason values; no exception string, model output, employee name, submitted name, or provider body crosses the boundary."
    - "A single explicitly named compatibility adapter maps the legacy None return to OK until Plan 18-10 performs the producer cutover; no consumer may invent its own None policy."
  artifacts:
    - path: "app/pipeline/result.py"
      provides: "PipelineOutcome, PipelineStage, PipelineReason, PipelineResult, and contextual exception classification"
    - path: "tests/test_orchestrator_states.py"
      provides: "Contract, classifier, and temporary compatibility-adapter matrix"
  key_links:
    - from: "app/pipeline/result.py"
      to: "Plans 18-03, 18-04, and 18-09"
      via: "one temporary normalize_pipeline_result adapter accepts None as legacy success while preserving every explicit PipelineResult"
      pattern: "normalize_pipeline_result"
---

<objective>
Add the typed, stage-aware, PII-safe result contract without cutting over either producer yet.

Purpose: FAIL-01 needs a shared vocabulary before any caller changes; keeping legacy persistence/None behavior intact makes this first wave green while later plans install every consumer.
Output: app/pipeline/result.py, one temporary compatibility adapter, and a focused classification matrix.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-VALIDATION.md
@.planning/phases/17-the-pump/17-01-SUMMARY.md

<interfaces>
- app/pipeline/orchestrator.py continues to return None and persist terminal errors in this wave; Plan 18-10 changes that only after all consumers exist.
- app/pipeline/orchestrator.py: _run_stages branches solely on Decision.final_action, which remains separate from transport outcome.
- app/llm/client.py disables SDK retries and raises Pydantic ValidationError after its one application-level parse retry.
</interfaces>
</context>

<artifacts_this_phase_produces>
- New file app/pipeline/result.py.
- New enum PipelineOutcome with OK, RETRYABLE, TERMINAL.
- New enum PipelineStage with UNKNOWN, LOAD, EXTRACT, PERSIST, CLARIFICATION, COMPUTE, DELIVERY.
- New bounded enum PipelineReason covering UNCLASSIFIED, provider connection/timeout/rate-limit/server failure, schema-or-parse failure, client request failure, ambiguous send failure, and INVALID_OPERATOR_OVERRIDE_CONTEXT for a malformed durable operator-resume mapping.
- New frozen dataclass PipelineResult with terminal-safe defaults and a bounded diagnostic-code property or equivalent formatter.
- New public function classify_pipeline_exception(stage, exc) returning PipelineResult without serializing exc.
- New temporary normalize_pipeline_result(value) adapter: None maps to OK only during the additive compatibility waves; explicit values pass through unchanged and every other type fails closed. Plan 18-10 tightens this same seam to PipelineResult-only.
- No orchestrator signature, return, catch boundary, or terminal-persistence change in this plan.
- No new dependency, job kind, database object, route, or UI surface.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the safe-default result contract and contextual classifier</name>
  <files>app/pipeline/result.py, tests/test_orchestrator_states.py</files>
  <read_first>
    - app/models/status.py for the bounded StrEnum convention.
    - app/models/job.py for the frozen dataclass convention.
    - app/llm/client.py for the one parse retry and Pydantic ValidationError behavior.
    - app/pipeline/send_guard.py for the ambiguous-send fail-closed boundary.
    - tests/test_orchestrator_states.py for existing fake-run and clarification tests.
  </read_first>
  <behavior>
    - PipelineResult() is terminal/unknown/unclassified.
    - Extraction APITimeoutError or APIConnectionError is retryable with a bounded provider reason.
    - Extraction RateLimitError and APIStatusError with status 500 or greater are retryable.
    - Extraction Pydantic ValidationError, 4xx other than 429, and every unclassified failure are terminal.
    - Clarification and delivery-stage provider ambiguity is terminal until Phase 20.
    - Formatting or serializing a result cannot expose str(exc) or any input content.
  </behavior>
  <action>
    Create app/pipeline/result.py using stdlib enum.StrEnum and dataclasses.dataclass(frozen=True). Implement PipelineOutcome, PipelineStage, PipelineReason, and PipelineResult exactly as bounded code values. The dataclass defaults must be TERMINAL, UNKNOWN, and UNCLASSIFIED so a missed classification fails closed in the money system. Add a diagnostic-code property or function that combines only stage.value and reason.value. Include INVALID_OPERATOR_OVERRIDE_CONTEXT as the bounded terminal reason used when a durable operator-resume handler cannot prove its persisted mapping is complete and server-valid; the contextual provider classifier does not emit it. Implement classify_pipeline_exception(stage, exc) contextually: at EXTRACT only, map OpenAI API connection/timeouts, rate limits, and status codes 500 or greater to RETRYABLE; keep exhausted Pydantic parse/schema failures, non-rate-limit 4xx, and unknown failures TERMINAL. At CLARIFICATION and DELIVERY, provider ambiguity remains TERMINAL because Phase 20 has not supplied replay/idempotency. Do not place queue timing, database writes, raw exception text, model response content, names, overrides, or PII in this module. Add contract/classifier tests before implementation and keep all plan decision labels in planning/tests only; source comments explain safety constraints without copying D-01/D-02/D-03 provenance tokens.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_orchestrator_states.py -k "pipeline_result or classification"</automated>
  </verify>
  <acceptance_criteria>
    - PipelineResult default construction is terminal-safe.
    - Every result field is a bounded enum or derived bounded string.
    - The extraction retryable matrix and terminal counterexamples pass without network or database access.
    - A test proves arbitrary sensitive exception text is absent from the result representation and diagnostic code.
  </acceptance_criteria>
  <done>The bounded result type and contextual safe-terminal classifier exist with a hermetic retryability matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add one temporary legacy-result compatibility adapter</name>
  <files>app/pipeline/result.py, tests/test_orchestrator_states.py</files>
  <read_first>
    - app/pipeline/result.py from Task 1.
    - app/pipeline/orchestrator.py run_pipeline and resume_pipeline annotations/catch boundaries to confirm legacy None behavior remains untouched.
    - tests/test_orchestrator_states.py for the contract-focused hermetic test style.
  </read_first>
  <behavior>
    - None maps to the singleton/coarse OK result only through the named compatibility adapter.
    - Explicit OK, RETRYABLE, and TERMINAL values pass through unchanged.
    - Any non-None, non-PipelineResult value raises TypeError rather than becoming success.
    - Source guards prove neither orchestrator entry point is modified in this plan.
  </behavior>
  <action>
    Add normalize_pipeline_result in app/pipeline/result.py as the only temporary bridge for the current None-returning producers. Map None to a coarse PipelineOutcome.OK result because legacy None means the existing orchestrator completed or already recorded its own terminal error; preserve explicit PipelineResult objects exactly, and reject every other runtime value. Mark the None branch as compatibility-only with Plan 18-10 as its deletion/tightening owner. Plans 18-03, 18-04, and 18-09 must call or feed this seam rather than duplicating an `if result is None` branch. Add direct tests for the adapter and a source-level assertion that run_pipeline/resume_pipeline still retain their existing None annotations and record_run_error ownership at the end of this plan. Do not edit app/pipeline/orchestrator.py or require any active caller to consume PipelineResult yet.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_orchestrator_states.py -k "pipeline_result or classification or legacy_result"</automated>
  </verify>
  <acceptance_criteria>
    - There is exactly one named legacy-None compatibility policy.
    - Explicit RETRYABLE and TERMINAL cannot be collapsed to OK.
    - Invalid runtime values fail loudly.
    - Both orchestrator entry points and their existing persistence behavior remain unchanged and green.
  </acceptance_criteria>
  <done>All consumers can be installed against one temporary adapter before the producer cutover, without creating an intermediate swallowed-failure wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider/LLM exception to pipeline result | Untrusted exception objects can contain prompt, response, email, or provider details. |
| legacy producer return to future consumers | None must have one explicit temporary meaning until producer cutover. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-01 | Information Disclosure | PipelineResult/classifier | high | mitigate | Bounded enums only; tests inject sensitive exception text and prove it never reaches result fields, diagnostics, or persisted reason. |
| T-18-02 | Tampering | compatibility adapter | high | mitigate | One named None-to-OK bridge, exact pass-through for explicit results, invalid-type rejection, and a Plan 18-10 removal owner. |
| T-18-03 | Denial of Service | retry classifier | medium | mitigate | Retry only the named extraction provider classes/statuses; safe-terminal fallback for every other exception. |
| T-18-01-SC | Tampering | package installs | low | accept | This plan adds no package or dependency. |
</threat_model>

<verification>
- Run the FAIL-01 foundation row: uv run pytest -q tests/test_orchestrator_states.py -k "pipeline_result or classification or legacy_result".
- Run uv run ruff check app/pipeline/result.py tests/test_orchestrator_states.py.
- Run uv run mypy app/pipeline/result.py.
</verification>

<success_criteria>
- FAIL-01's bounded contract and D-01/D-02/D-03 vocabulary are established without changing producers.
- The temporary compatibility surface is singular, tested, and explicitly owned by Plan 18-10.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-01-SUMMARY.md when done.
</output>
