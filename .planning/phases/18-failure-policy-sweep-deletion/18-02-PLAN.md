---
phase: 18-failure-policy-sweep-deletion
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - app/models/job.py
  - app/db/schema.sql
  - app/db/repo/jobs.py
  - app/db/repo/emails.py
  - app/db/repo/operator_resume_resolutions.py
  - app/db/repo/__init__.py
  - tests/test_repo_jobs_sql.py
  - tests/conftest.py
autonomous: true
requirements: [FAIL-01, FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Job remains an identifier-only typed record, gains email_id in exact claim order, and never gains a generic payload (D-01, D-04)."
    - "Every valid operator resolution has a durable UUID generation independent of reply_epoch; typed child rows preserve the complete submitted-name-to-employee-id mapping without JSON or alias_candidates authority."
    - "Plan 18-09 can add RESUME_REPLY and OPERATOR_RESUME independently at task boundaries because their context repositories and claim projection already exist in a green foundation wave."
  artifacts:
    - path: "app/models/job.py"
      provides: "email_id-bearing identifier-only Job claim contract"
    - path: "app/db/schema.sql"
      provides: "operator_resume_resolutions parent generations plus typed operator_resume_overrides child rows; kind widening remains atomic with handlers in Plan 18-09"
    - path: "app/db/repo/operator_resume_resolutions.py"
      provides: "Strict create/load functions for immutable operator resolution generations and complete mappings"
    - path: "app/db/repo/emails.py"
      provides: "Persisted inbound lookup by email row id"
    - path: "tests/test_repo_jobs_sql.py"
      provides: "Ordered claim projection, parameterization, and override-store SQL-shape proofs"
  key_links:
    - from: "app/db/repo/jobs.py"
      to: "app/models/job.py"
      via: "claim_job RETURNING id, kind, run_id, email_id, operator_resolution_id, attempts, max_attempts, lease_token in exact dataclass order"
      pattern: "RETURNING.*email_id.*operator_resolution_id"
    - from: "app/db/repo/operator_resume_resolutions.py"
      to: "operator_resume_resolutions/operator_resume_overrides"
      via: "resolution UUID scopes the immutable parent and complete typed submitted_name/employee_id rows independently of reply_epoch"
      pattern: "operator_resolution_id|run_id|submitted_name|employee_id"
---

<objective>
Create the green identifier/context persistence foundation for lossless clarification and operator-resume retries.

Purpose: Durable retry cannot discard a clarification reply or the operator's complete validated override mapping, and Phase 17 forbids solving that by adding an arbitrary jobs payload.
Output: email/resolution-aware claims, normalized operator resolution tables, repository primitives, and SQL-shape guards that remain green before either new kind is registered.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-01-SUMMARY.md

<interfaces>
- Job currently mirrors six claim RETURNING columns and jobs.email_id already exists.
- jobs rows carry typed identifiers and transport state only; no payload or next business state is allowed.
- /runs/{run_id}/resolve builds the complete server-validated submitted_name -> employee_id mapping; alias_candidates intentionally persists only remember-checked learning choices and is not authoritative for resumption.
- app/db/bootstrap.py reapplies schema.sql atomically to fresh and live databases.
</interfaces>
</context>

<artifacts_this_phase_produces>
- Job.email_id and Job.operator_resolution_id between run_id and attempts, with an exact eight-column claim projection.
- Optional bounded delay and safe diagnostic inputs on enqueue_job, preserving all existing defaults.
- New parent table operator_resume_resolutions(id, run_id, created_at) and child table operator_resume_overrides(operator_resolution_id, submitted_name, employee_id, created_at), with history-preserving foreign keys and an index on the parent run id.
- New create_operator_resume_resolution(run_id, operator_resolution_id, overrides, conn=None) and load_operator_resume_resolution(run_id, operator_resolution_id, conn=None).
- New get_inbound_email_by_id(email_id, conn=None).
- No JSON payload, reply-epoch mutation, generic serializer, dependency, environment variable, route, handler, or producer cutover.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen identifier-only Job claim and prewire kind validation</name>
  <files>app/models/job.py, app/db/repo/jobs.py, tests/test_repo_jobs_sql.py</files>
  <read_first>
    - app/models/job.py for the canonical enum and ordered frozen dataclass contract.
    - app/db/repo/jobs.py enqueue_job and claim_job SQL/fencing conventions.
    - tests/test_repo_jobs_sql.py RETURNING-to-dataclass and parameter guards.
    - app/pipeline/result.py for bounded diagnostic-code inputs.
  </read_first>
  <behavior>
    - Job carries email_id/operator_resolution_id identifiers but no dict/JSON payload, override mapping, submitted name, or business next-state value.
    - Existing RUN_PIPELINE validation remains green; value-based validation branches are ready for resume_reply run_id/email_id and operator_resume run_id once Plan 18-09 adds the enum members.
    - Delayed enqueue parameterizes available_at and accepts only a bounded safe diagnostic code.
    - claim_job maps nullable email_id/operator_resolution_id without reordering any other field.
  </behavior>
  <action>
    Add email_id and operator_resolution_id to Job in exact claim-projection order. Extend enqueue_job with optional available_in_seconds and safe_last_error inputs used by classified retry bridges; validate and parameterize both. Prewire kind-specific identifier validation by bounded kind.value strings so the current RUN_PIPELINE-only enum and full suite remain green in this wave; Plan 18-09 adds both enum members and proves those dormant branches. Extend claim_job RETURNING/construction to eight fields. Do not add a payload, mapping, retry loop, or run write.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_repo_jobs_sql.py</automated>
  </verify>
  <acceptance_criteria>
    - dataclasses.fields(Job) and claim RETURNING are ordered-equal with email_id and operator_resolution_id exactly once.
    - Existing RUN_PIPELINE missing-id behavior remains green and future value branches are source/SQL-shape pinned for Plan 18-09 activation.
    - Delay and diagnostic values are parameters, not interpolation.
    - No generic payload or operator mapping appears on Job/jobs.
  </acceptance_criteria>
  <done>The queue can identify all Phase 18 work without carrying business payload data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add immutable typed operator-resolution generations without widening JobKind early</name>
  <files>app/db/schema.sql, tests/test_repo_jobs_sql.py</files>
  <read_first>
    - app/db/schema.sql jobs definition and email_messages purpose CHECK replacement analog.
    - app/db/bootstrap.py whole-file transactional bootstrap behavior.
    - tests/test_repo_jobs_sql.py DDL inventory patterns.
  </read_first>
  <behavior>
    - Current jobs.kind remains run_pipeline in this green wave; Plan 18-09 widens Python/SQL/dispatch in one atomic handler plan.
    - Each parent resolution UUID belongs to one run, and its child rows map every non-empty submitted name to one employee UUID.
    - Two valid resolutions for the same run and reply_epoch can coexist under distinct UUIDs; duplicate insertion of the same UUID is idempotent only when the complete mapping is identical.
    - Deleting/reopening a dead job is not part of this table contract.
  </behavior>
  <action>
    After jobs, create operator_resume_resolutions with UUID primary key id, NOT NULL run_id, created_at, a history-preserving run FK, and idx_operator_resume_resolutions_run_id. Create operator_resume_overrides with NOT NULL operator_resolution_id/submitted_name/employee_id, primary key (operator_resolution_id, submitted_name), history-preserving FKs to the parent and employees, and a nonblank name CHECK. Add nullable jobs.operator_resolution_id with a history-preserving FK so OPERATOR_RESUME remains identifier-only. Use idempotent ALTER/constraint installation for live databases, but keep jobs.kind unchanged so Python, SQL, and dispatch remain equal until Plan 18-09. Do not add JSONB payloads or a migration framework.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_job_kind_drift.py tests/test_repo_jobs_sql.py tests/test_schema_introspect.py</automated>
  </verify>
  <acceptance_criteria>
    - The full current suite remains green with one kind; Plan 18-09 owns the atomic three-kind widening.
    - The normalized parent/child store has no JSON payload and preserves audit generations.
    - A same-epoch pair of distinct resolution UUIDs is representable without a uniqueness collision.
    - Constraint and index tests fail if any typed key or FK disappears.
  </acceptance_criteria>
  <done>Postgres can durably represent the complete operator mapping as typed rows scoped to a retry generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add strict persisted-context repository primitives</name>
  <files>app/db/repo/emails.py, app/db/repo/operator_resume_resolutions.py, app/db/repo/__init__.py, tests/test_repo_jobs_sql.py, tests/conftest.py</files>
  <read_first>
    - app/db/repo/emails.py get_inbound_by_message_id explicit projection.
    - app/db/repo/_shared.py conn ownership convention.
    - app/db/repo/jobs.py identifier parameterization style.
    - app/routes/runs.py resolve mapping validation contract.
  </read_first>
  <behavior>
    - get_inbound_email_by_id loads only an inbound row by exact UUID.
    - create_operator_resume_resolution rejects empty mappings, blank/duplicate names, malformed employee UUIDs, and mixed run/resolution identifiers before or atomically with SQL.
    - load_operator_resume_resolution returns an exact dict from explicit columns for one run_id/operator_resolution_id pair.
    - Reusing one resolution UUID with the same complete mapping is idempotent; a conflicting duplicate fails closed, while a fresh UUID in the same reply_epoch is independent.
    - Arbitrary diagnostic text and alias_candidates never enter this store.
  </behavior>
  <action>
    Add get_inbound_email_by_id and the strict resolution create/load functions, export them through app.db.repo, and add matching stateful InMemoryRepo methods plus fake_repo registrations in this foundation wave. The create call accepts a caller-generated operator_resolution_id, inserts one immutable parent plus all child rows in one transaction, enforces nonempty keys/UUID values, treats an exact duplicate as idempotent, and rejects a conflicting duplicate. The loader scopes by run_id/operator_resolution_id and rejects missing, partial, duplicate, or corrupt rows. Keep passed-connection composition and never inspect alias_candidates.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_repo_jobs_sql.py -k "inbound_email_by_id or operator_resume_resolution or claim_projection or enqueue"</automated>
  </verify>
  <acceptance_criteria>
    - Exact resolution UUID/run scoping and explicit columns are proven.
    - Duplicate same-resolution writes are idempotent, conflicting duplicates fail closed, and two same-epoch resolutions remain distinct.
    - Malformed or duplicate mapping input cannot partially persist.
    - Loader reconstruction is exact and detects corrupt duplicates.
    - Every write is joinable into the Plan 18-03 outer transaction.
  </acceptance_criteria>
  <done>The two resume handlers can reconstruct durable context from typed repository reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs row to handler | Typed identifiers select persisted resume context. |
| validated operator POST to durable generation rows | A complete money-moving mapping crosses from request memory into an immutable UUID generation in Postgres. |
| schema.sql to deployed Postgres | CREATE TABLE IF NOT EXISTS alone cannot widen a live CHECK. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-04 | Tampering | operator resolution store | high | mitigate | Immutable UUID parent, typed per-name rows, run/resolution scoping, employee FKs, strict loader, and atomic insertion contract. |
| T-18-05 | Tampering | JobKind/schema | high | mitigate | Python/SQL/dispatch set equality plus idempotent deployed CHECK replacement. |
| T-18-06 | Information Disclosure | transport schema | high | mitigate | Identifiers only on jobs; no arbitrary JSON, raw mapping, exception, or alias-candidate authority. |
| T-18-07 | Injection | context repository SQL | medium | mitigate | Explicit columns and parameterized identifiers/names/UUIDs. |
| T-18-02-SC | Tampering | package installs | low | accept | No package or dependency is added. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_job_kind_drift.py tests/test_repo_jobs_sql.py tests/test_schema_introspect.py tests/test_fake_repo_pairing.py.
- Run uv run ruff check app/models/job.py app/db/repo/jobs.py app/db/repo/emails.py app/db/repo/operator_resume_resolutions.py app/db/repo/__init__.py.
- Run uv run mypy on those Python files.
</verification>

<success_criteria>
- D-01, D-02, D-04, and Phase 17's identifier-only transport invariant are preserved.
- Both reply context and the complete operator mapping have lossless typed persistence.
- Plan scope is eight files, below the prior 10-file warning threshold.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md when done.
</output>
