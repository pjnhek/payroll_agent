---
phase: 18-failure-policy-sweep-deletion
plan: 12
type: execute
wave: 3
depends_on: [18-02]
files_modified:
  - app/db/schema_introspect.py
  - tests/test_schema_introspect.py
autonomous: true
requirements: [FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Schema health expects operator_resume_resolutions, operator_resume_overrides, and jobs.operator_resolution_id after the foundation schema lands."
    - "A live database missing or malformed in either typed operator-resolution table reports out of sync before any handler/use plan executes."
    - "The introspection check remains explicit and non-vacuous; it does not weaken existing payroll_runs, email_messages, jobs, CHECK-value, or unique-constraint coverage."
  artifacts:
    - path: "app/db/schema_introspect.py"
      provides: "Expected/live typed operator-resolution table, column, and critical index/constraint coverage"
    - path: "tests/test_schema_introspect.py"
      provides: "Missing-table/column/index drift regressions and updated scripted-query inventory"
  key_links:
    - from: "app/db/schema_introspect.py"
      to: "app/db/schema.sql"
      via: "expected_schema parses both operator-resolution tables and jobs.operator_resolution_id"
      pattern: "operator_resume_resolutions|operator_resume_overrides|operator_resolution_id"
---

<objective>
Make deployment schema health cover the typed operator-resolution foundation before handlers depend on it.

Purpose: Fresh-schema tests alone cannot detect a live deploy missing the parent/mapping tables or resolution identifier column.
Output: exact expected/live introspection and hermetic drift regressions.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md

<interfaces>
- Plan 18-02 adds operator_resume_resolutions, operator_resume_overrides, jobs.operator_resolution_id, and named indexes/constraints.
- expected_schema currently inventories payroll_runs, email_messages, and jobs columns plus selected check/unique definitions.
- tests/test_schema_introspect.py scripts one live-column query per expected table in insertion order.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register typed operator-resolution tables in schema health</name>
  <files>app/db/schema_introspect.py, tests/test_schema_introspect.py</files>
  <read_first>
    - app/db/schema_introspect.py ExpectedSchema, expected_schema, live-column, and required-constraint queries.
    - app/db/schema.sql Plan 18-02 parent/child definitions and named indexes/constraints.
    - tests/test_schema_introspect.py scripted query ordering and anti-vacuity tests.
  </read_first>
  <behavior>
    - expected_schema includes both new tables, their exact typed columns, and jobs.operator_resolution_id.
    - diff_against_live marks missing parent table, child table, key column, employee FK/critical named constraint, or required index as out of sync.
    - Existing checks remain unchanged and extra live columns remain non-drift.
  </behavior>
  <action>
    Extend ExpectedSchema and expected_schema with operator_resume_resolutions and operator_resume_overrides column sets parsed from schema.sql, including jobs.operator_resolution_id. Add narrow required index/constraint inventory for the parent run lookup and child identity/employee-integrity surfaces using named catalog queries rather than assuming column presence implies a valid relationship. Update diff/as_missing_dict without changing existing status/purpose/unique semantics. Update the FakeConnection script order and add anti-vacuous regressions for missing parent table, missing child table, missing resolution key column, malformed/missing required index/constraint, and the all-in-sync case.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_schema_introspect.py</automated>
  </verify>
  <acceptance_criteria>
    - Either typed operator-resolution table being absent or malformed makes is_in_sync false.
    - jobs.operator_resolution_id drift is detected.
    - Scripted query count/order is explicit and existing drift tests remain green.
  </acceptance_criteria>
  <done>Schema health fails closed before runtime handlers encounter missing operator-resolution persistence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.sql expectation to live Postgres | A deploy can appear healthy while typed recovery authority is missing or malformed. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-32 | Tampering | live schema drift | high | mitigate | Explicit parent/child/job-column plus named index/constraint inventory and negative tests. |
| T-18-33 | Denial of Service | handler persistence dependency | high | mitigate | Handler plans depend on this green schema-health wave. |
| T-18-12-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_schema_introspect.py tests/test_repo_jobs_sql.py.
- Run uv run ruff check app/db/schema_introspect.py tests/test_schema_introspect.py and uv run mypy app/db/schema_introspect.py.
</verification>

<success_criteria>
- Live schema health covers both typed resolution tables before Plans 18-09/18-03/18-06 use them.
- Existing schema-introspection guarantees remain intact.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-12-SUMMARY.md when done.
</output>
