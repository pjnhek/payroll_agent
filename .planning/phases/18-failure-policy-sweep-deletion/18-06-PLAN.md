---
phase: 18-failure-policy-sweep-deletion
plan: 06
type: execute
wave: 6
depends_on: [18-03]
files_modified:
  - app/db/repo/demo.py
  - app/routes/runs.py
  - app/templates/runs_list.html
  - app/templates/run_detail.html
  - tests/test_dashboard.py
  - tests/test_hitl.py
  - tests/test_alias_and_run_column_regressions.py
autonomous: true
requirements: [FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "The existing run list and detail surfaces show the real run status as Error and add only a bounded secondary Retries exhausted label where applicable (D-09, D-12)."
    - "Visible terminal diagnostics are limited to stable reason, stage, and attempt count; raw exceptions, submitted names, email bodies, and provider text never render (D-11)."
    - "The existing Retrigger action CASes the same run from error to received, bumps reply_epoch, and inserts a fresh dedup-keyed job generation while the prior dead job remains immutable (D-10)."
    - "Polling updates the secondary label consistently without adding a queue-operations dashboard, new run status, or retry button."
  artifacts:
    - path: "app/db/repo/demo.py"
      provides: "Bounded run-list diagnostic projection"
    - path: "app/routes/runs.py"
      provides: "Stable UI presentation fields derived from persisted safe diagnostics"
    - path: "app/templates/runs_list.html"
      provides: "Error badge plus secondary retry-exhaustion label in list and polling states"
    - path: "app/templates/run_detail.html"
      provides: "Bounded stage/reason/attempt detail beside the existing Retrigger action"
    - path: "tests/test_dashboard.py"
      provides: "Safe rendering and polling parity proofs"
    - path: "tests/test_hitl.py"
      provides: "Existing retrigger endpoint and fresh epoch/dedup generation proofs"
    - path: "tests/test_alias_and_run_column_regressions.py"
      provides: "Reply-context reset and retrigger provenance regression proofs"
  key_links:
    - from: "app/db/repo/demo.py"
      to: "app/routes/runs.py"
      via: "safe persisted error_reason, error_detail, and attempt projection"
      pattern: "error_reason"
    - from: "app/routes/runs.py"
      to: "app/templates/runs_list.html"
      via: "bounded secondary status presentation"
      pattern: "Retries exhausted"
    - from: "app/templates/run_detail.html"
      to: "app/routes/runs.py"
      via: "existing Retrigger POST resets the same run to received and enqueues a fresh job generation"
      pattern: "retrigger"
    - from: "app/routes/runs.py"
      to: "jobs"
      via: "same-run Retrigger transaction bumps reply_epoch and inserts a distinct dedup-keyed job without mutating the prior dead row"
      pattern: "reply_epoch|enqueue_job"
---

<objective>
Surface terminal failure policy through the existing operator workflow while preserving immutable recovery history.

Purpose: An operator should understand that a run is truly Error, whether retries exhausted, and how to restart that same run with a fresh job generation without seeing unsafe diagnostics.
Output: bounded list/detail/poll labels and same-run retrigger immutability tests.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/17-the-pump/17-SECURITY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md

<interfaces>
- Atomic settlement persists a stable safe run error_reason/error_detail and queue attempts.
- The list route polls an existing status endpoint; detail already exposes Retrigger.
- Retrigger CASes the existing failed run to received, bumps reply_epoch, and enqueues a new dedup-keyed job generation while prior dead jobs remain immutable.
</interfaces>
</context>

<artifacts_this_phase_produces>
- Bounded list projection and presentation helpers for terminal diagnostics.
- Secondary Retries exhausted label while the canonical run badge remains Error.
- Safe stage/reason/attempt rendering on list, polling, and detail.
- Regression proof that Retrigger keeps the same run id, CASes ERROR to RECEIVED, bumps reply_epoch, creates a fresh job/dedup generation, and leaves prior dead history immutable.
- No queue dashboard, new run status, new retry action, schema object, or dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render bounded terminal diagnostics on existing run surfaces</name>
  <files>app/db/repo/demo.py, app/routes/runs.py, app/templates/runs_list.html, app/templates/run_detail.html, tests/test_dashboard.py</files>
  <read_first>
    - app/db/repo/demo.py run-list projection and query ordering.
    - app/routes/runs.py list, detail, status polling, and error-detail parsing.
    - app/templates/runs_list.html badge markup and polling JavaScript.
    - app/templates/run_detail.html error and Retrigger sections.
    - tests/test_dashboard.py current list/detail/status rendering assertions.
  </read_first>
  <behavior>
    - Error remains the canonical status badge for terminal and retry-exhausted runs.
    - RetryExhausted and FinalAttemptLeaseExpired display a secondary Retries exhausted label.
    - Only allowlisted stable reason, stage, and bounded attempts display.
    - Polling and initial HTML render produce equivalent labels.
    - Unknown or malformed diagnostics degrade to a generic safe Error presentation.
  </behavior>
  <action>
    Extend the existing run-list projection only with the persisted fields needed to derive a safe presentation. Centralize an allowlisted presentation mapping in runs.py for stable Phase 18 reason/stage codes and bounded integer attempts; never pass arbitrary error_detail or last_error through to templates. Update list HTML, polling JSON/JavaScript, and detail HTML so the real badge stays Error and only recognized exhaustion codes add the secondary Retries exhausted label plus safe stage/reason/attempt context. Unknown values fall back to the existing generic error copy. Add tests with hostile raw provider/exception/PII-like text to prove it is absent from HTML and JSON while recognized codes render consistently. Do not add a queue operations page or expose payloads.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_dashboard.py -k "error or retried or polling or safe"</automated>
  </verify>
  <acceptance_criteria>
    - Canonical Error and secondary Retries exhausted meanings are visually distinct.
    - Initial list, polled list, and detail use one safe mapping.
    - Raw diagnostic text and PII-like fixtures do not appear in responses.
    - Existing non-error dashboard states remain unchanged.
  </acceptance_criteria>
  <done>Operators see a concise stable explanation of terminal failures on the surfaces they already use.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restart the same run with a fresh immutable-history job generation</name>
  <files>app/routes/runs.py, tests/test_hitl.py, tests/test_alias_and_run_column_regressions.py</files>
  <read_first>
    - app/routes/runs.py current retrigger endpoint and generation/linkage behavior.
    - app/db/repo/pipeline_state.py clear_reply_context versus rewind_for_reclaim epoch semantics.
    - app/models/status.py canonical RunStatus business vocabulary; preserve ERROR/RECEIVED and add no queue-derived status.
    - tests/test_hitl.py existing same-run retrigger enqueue/drain, second-generation, and epoch-keyed dedup analogs from 18-PATTERNS.
    - tests/test_alias_and_run_column_regressions.py existing full reply-context reset and stale-provenance analogs from 18-PATTERNS.
    - app/db/repo/job_settlement.py dead-letter settlement ownership.
  </read_first>
  <behavior>
    - Retrigger preserves the payroll run id and atomically moves that run from error to received.
    - Retrigger bumps reply_epoch and inserts a distinct dedup-keyed job row with a fresh bounded attempt budget/fencing lifecycle.
    - The prior dead job remains dead with unchanged attempts, reason, lease history, and identifier.
    - Retrigger remains the only operator recovery action exposed here.
  </behavior>
  <action>
    Preserve the established same-run Retrigger transaction exactly per D-10: CAS the existing payroll run from ERROR to RECEIVED, call clear_reply_context so reply_epoch advances (never substitute automatic rewind_for_reclaim, which intentionally does not bump), and enqueue a new run_pipeline job whose dedup key includes the same run_id plus the new epoch. Keep RunStatus.ERROR and RECEIVED canonical; add no transport-derived business status. Do not create a new inbound email or payroll_runs row. Extend the actual behavioral analogs in tests/test_hitl.py and tests/test_alias_and_run_column_regressions.py rather than inventing dashboard authorization coverage that does not exist. Snapshot the failed run and prior dead job before POST; after POST assert the run id is unchanged, status is RECEIVED, reply_epoch/dedup generation changed, and the new job has a distinct id plus a fresh bounded attempt budget/fencing lifecycle. Re-read the prior dead job to prove its state, attempts, reason, token/history fields, and id are unchanged. Assert that no action reopens the dead row and no new public mutation endpoint is introduced. Preserve the route's current unauthenticated authorization posture without claiming authentication guarantees that do not exist; operator authentication remains an accepted existing gap outside this phase's scope. Do not implement Phase 19 producer cutover or Phase 20 replay semantics.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_hitl.py tests/test_alias_and_run_column_regressions.py -k "retrigger"</automated>
  </verify>
  <acceptance_criteria>
    - The payroll run id is unchanged and its status moves ERROR to RECEIVED with a bumped reply_epoch.
    - The new job/dedup generation has a distinct identifier and fresh attempt/fencing fields.
    - The prior dead job is byte-for-byte unchanged in the asserted audit fields.
    - No new inbound email or payroll_runs row is created.
    - No action reopens a dead job.
  </acceptance_criteria>
  <done>Manual recovery reuses the same run with a fresh auditable job generation and never mutates prior dead history.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted diagnostics to browser | Database values cross into operator-visible HTML and JSON. |
| retrigger action to immutable history | A human recovery action creates new work beside terminal records. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-18 | Information Disclosure | diagnostic rendering | high | mitigate | Allowlisted codes and bounded fields only; hostile raw-text absence tests. |
| T-18-19 | Tampering | retrigger history | high | mitigate | Same-run ERROR-to-RECEIVED CAS, epoch/dedup generation assertions, and prior dead-job immutability proof. |
| T-18-20 | Spoofing | retrigger route | medium | accept | Phase 18 preserves the existing route and adds no public mutation surface; operator authentication is an accepted existing gap and this plan does not claim nonexistent authorization tests. |
| T-18-06-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_dashboard.py tests/test_hitl.py tests/test_alias_and_run_column_regressions.py -k "error or polling or retrigger".
- Run uv run ruff check app/db/repo/demo.py app/routes/runs.py tests/test_dashboard.py.
- Run uv run mypy app/db/repo/demo.py app/routes/runs.py.
</verification>

<success_criteria>
- D-09 through D-12 are directly represented in safe UI and recovery tests.
- Terminal history is immutable across operator recovery.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-06-SUMMARY.md when done.
</output>
