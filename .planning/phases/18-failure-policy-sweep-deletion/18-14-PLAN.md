---
phase: 18-failure-policy-sweep-deletion
plan: 14
type: execute
wave: 12
depends_on: [18-09, 18-11, 18-13]
files_modified:
  - app/queue/handlers/resume_reply.py
  - tests/test_resume_pipeline.py
  - tests/test_queue_durability.py
autonomous: true
requirements: [FAIL-01, FAIL-02]
gap_closure: true
nyquist_compliant: true

must_haves:
  truths:
    - "A RESUME_REPLY job may replay persisted email content only when the row has a non-null canonical run_id equal to job.run_id; validation happens before row_to_inbound or orchestration (CR-02, D-01, D-04)."
    - "Same-business wrong-run, cross-business wrong-run, null run_id, and malformed run_id context return one bounded terminal load-stage result and never enter the money-moving resume pipeline."
    - "Invalid-context logging contains only the bounded reason code; it contains no run, job, email, business, employee, submitted-name, subject, body, or UUID identifiers (D-02)."
    - "All fake-repository resume-handler regressions run by default with DATABASE_URL absent, and the reclaimed success assertion requires an explicit PipelineOutcome.OK rather than legacy None (WR-01, FAIL-01)."
    - "The always-run and guarded-live suites prove valid same-run context still resumes from RECEIVED without advancing reply_epoch (D-05)."
  artifacts:
    - path: "app/queue/handlers/resume_reply.py"
      provides: "Canonical same-run persisted-email validation before replay and identifier-free invalid-context diagnostics"
    - path: "tests/test_resume_pipeline.py"
      provides: "Always-run valid/wrong-run/cross-business handler matrix and explicit OK reclaim assertion"
    - path: "tests/test_queue_durability.py"
      provides: "Guarded live Postgres wrong-run and cross-business association proofs"
  key_links:
    - from: "app/queue/handlers/resume_reply.py"
      to: "app/db/repo/emails.py"
      via: "row run_id is canonicalized and compared to job.run_id before row_to_inbound"
      pattern: "get_inbound_email_by_id|run_id|row_to_inbound"
    - from: "tests/test_resume_pipeline.py"
      to: "app/queue/handlers/resume_reply.py"
      via: "the handler matrix runs with and without DATABASE_URL and forbids orchestration on association mismatch"
      pattern: "PipelineOutcome.OK|cross_business|wrong_run"
---

<objective>
Close CR-02 and WR-01 by binding every persisted reply to its claimed run before replay and making the handler regressions unconditional.

Purpose: Independent jobs.run_id and jobs.email_id foreign keys do not prove cross-row ownership. A mismatched reply can cross runs or businesses unless the durable handler validates the persisted association before constructing InboundEmail. The proof is incomplete while a module-wide environment skip hides its stale None expectation.
Output: canonical same-run validation, bounded identifier-free failure handling, always-run hermetic coverage, and guarded live Postgres counterexamples.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-VERIFICATION.md
@.planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-11-SUMMARY.md

<interfaces>
- repo.get_inbound_email_by_id returns id, run_id, message headers, addresses, cleaned body_text, consumed_round, and created_at for an inbound row; run_id can be NULL and is not cross-constrained to jobs.run_id.
- row_to_inbound validates and constructs InboundEmail but intentionally has no payroll-run ownership authority.
- handle_resume_reply currently logs three identifiers on invalid context and calls row_to_inbound without checking row run ownership.
- tests/test_resume_pipeline.py is hermetic by design through fake_repo and mock_llm, but a module-load DATABASE_URL skip currently suppresses the entire file. The reclaim test still expects None even though the exact result contract returns PipelineResult with outcome OK.
</interfaces>
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- Changed private app/queue/handlers/resume_reply.py::_invalid_context behavior: emit the bounded invalid-context code without logging any identifiers or row content.
- New private helper app/queue/handlers/resume_reply.py::_canonical_row_run_id, or an equivalently named bounded parser, returning UUID only for a present canonical persisted run id and failing closed otherwise.
- Changed public handle_resume_reply behavior: validate persisted row ownership before row_to_inbound, rewind_for_reclaim, or orchestrator.resume_pipeline.
- New always-run tests in tests/test_resume_pipeline.py covering same-run success, null/malformed ownership, same-business wrong-run, cross-business wrong-run, identifier/body log absence, and explicit OK reclaim.
- New guarded queueproof in tests/test_queue_durability.py covering real Postgres same-business and cross-business mismatches.
- No new production file, database constraint, schema migration, route, dependency, environment variable, job kind, generic payload, or business status. No schema push task is required because the fix is an application-level cross-row validation over existing columns.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce same-run persisted reply ownership in always-run handler tests</name>
  <files>app/queue/handlers/resume_reply.py, tests/test_resume_pipeline.py</files>
  <read_first>
    - app/queue/handlers/resume_reply.py complete handler order and _invalid_context logging.
    - app/db/repo/emails.py get_inbound_email_by_id explicit projection.
    - app/routes/pipeline_glue.py row_to_inbound validation contract.
    - app/queue/handlers/operator_resume.py for the established persisted-context fail-closed pattern.
    - tests/test_resume_pipeline.py module docstring/pytestmark, handler helpers, persisted-body test, reclaim test, and invalid-context log proof.
    - tests/conftest.py InMemoryRepo.insert_inbound_email, link_email_to_run, create_run, and get_inbound_email_by_id semantics.
    - .planning/phases/18-failure-policy-sweep-deletion/18-VERIFICATION.md CR-02 and WR-01.
  </read_first>
  <behavior>
    - A valid persisted row linked to the exact job run is converted and resumed once from RECEIVED.
    - A row with run_id NULL, a malformed UUID value, or a canonical UUID unequal to job.run_id returns terminal load:invalid_operator_override_context before row_to_inbound.
    - A same-business wrong-run reply and a cross-business reply both return the same bounded terminal result and never call row_to_inbound or orchestrator.resume_pipeline.
    - Invalid-context logs contain the bounded code only; UUIDs, body, subject, addresses, names, and business identifiers are absent.
    - attempts=2 preserves reply_epoch and returns a PipelineResult whose outcome is PipelineOutcome.OK.
    - The handler-focused and full resume module execute when DATABASE_URL is absent and when it is a harmless stub value.
  </behavior>
  <action>
    Write the wrong-run, cross-business, null/malformed-run, log-absence, and explicit-OK reclaim tests first. Remove the module-wide DATABASE_URL skip and its explanatory premise from tests/test_resume_pipeline.py; remove the os import if it becomes unused. Preserve hermetic fake_repo/mock_llm isolation and do not move these handler tests behind a new marker. Update valid handler fixtures to call fake_repo.link_email_to_run(email_id, run_id), matching the real webhook persistence contract. Create a second same-business run and a seeded Business 2 run for the mismatch counterexamples; patch row_to_inbound and orchestrator.resume_pipeline with fail-if-called spies so the test proves ordering, not merely a terminal return.

    In resume_reply.py, canonicalize row.get("run_id") through UUID parsing and require equality with the already-validated job.run_id before row_to_inbound. Null, malformed, wrong-run, and cross-business rows all return the existing bounded terminal load-stage result. Change _invalid_context so it logs only a static event name plus PipelineReason.INVALID_OPERATOR_OVERRIDE_CONTEXT.value; do not log job.run_id, job.id, email_id, row values, or exception text. Preserve missing job.run_id/email_id programming errors as loud ValueError, attempts-greater-than-one rewind semantics, RECEIVED entry, and strict PipelineResult runtime validation. Update the reclaim assertion to require result.outcome is PipelineOutcome.OK, not None.
  </action>
  <verify>
    <automated>env -u DATABASE_URL UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_resume_pipeline.py -k "resume_reply_handler or persisted or reclaim" &amp;&amp; DATABASE_URL=postgresql://stub:stub@localhost/stub UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_resume_pipeline.py -k "resume_reply_handler or persisted or reclaim"</automated>
  </verify>
  <acceptance_criteria>
    - Same-run association is checked before row_to_inbound and before any business-state mutation.
    - Wrong-run and cross-business content cannot reach resume_pipeline.
    - Invalid-context caplog assertions exclude every injected UUID and sensitive body/subject/name token.
    - test_resume_reply_handler_reclaims_without_advancing_epoch asserts explicit OK and passes.
    - No module-wide DATABASE_URL skip remains; the focused suite passes with the variable unset and set.
  </acceptance_criteria>
  <done>Every hermetic resume-handler regression runs by default and proves a reply cannot cross run or business boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove reply association rejection against real Postgres</name>
  <files>tests/test_queue_durability.py</files>
  <read_first>
    - tests/test_queue_durability.py seeded_db guard, _isolated_jobs fixture, _seed_run_for_queue_proof, and existing Phase 18 live context tests.
    - app/db/repo/runs.py insert_inbound_email and link_email_to_run.
    - app/db/repo/emails.py get_inbound_email_by_id projection.
    - app/queue/handlers/resume_reply.py Task 1 ownership check.
    - app/db/seed.py canonical Business 1 and Business 2 identifiers used by the live schema.
  </read_first>
  <behavior>
    - A real inbound row linked to run B cannot be replayed by a RESUME_REPLY Job whose run_id is run A, even when A and B belong to the same business.
    - The same rejection holds when run B belongs to a different seeded business.
    - Both cases return the bounded terminal result before orchestration and expose no identifiers/content in logs.
    - A real same-run control reaches the stubbed orchestrator exactly once with the persisted email id and RECEIVED status.
  </behavior>
  <action>
    Add a guarded table-driven queueproof that creates two real runs and inbound reply rows through existing repository seams. Cover same-business wrong-run and Business 1 versus Business 2 cross-business mismatch. Construct identifier-only Job values without enqueuing provider work, call handle_resume_reply directly, and patch orchestrator.resume_pipeline to raise if a mismatch reaches it. Assert terminal load:invalid_operator_override_context, no row body/subject/address/UUID in caplog, and no run-state change. Add a positive same-run control linked through repo.link_email_to_run that reaches a stubbed explicit OK orchestrator once. Reuse the module's integration/queueproof markers and seeded_db reset guard; call no LLM, gateway, or external provider. Record a missing reset-enabled database as unavailable evidence, never as a pass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest tests/test_queue_durability.py -m queueproof -k "resume_reply and association" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - Real database rows prove independent foreign keys do not bypass the application ownership check.
    - Same-business and cross-business mismatches are both rejected before orchestration.
    - The positive same-run control proves the test does not pass by disabling the handler.
    - No external provider is called and guarded-live availability is reported honestly.
  </acceptance_criteria>
  <done>CR-02 has an always-run behavioral proof and a guarded real-Postgres counterexample at the exact cross-row trust boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs identifiers to persisted email row | Independent foreign keys select two records whose same-run association must be proven before replay. |
| persisted reply to deterministic payroll pipeline | Email body and sender context can affect money-moving reconciliation if routed to the wrong run. |
| invalid durable context to logs | Stored identifiers and email content must not escape through diagnostics. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-37 | Tampering | RESUME_REPLY ownership | high | mitigate | Canonical non-null row-run equality check before conversion/replay, with same-business and cross-business counterexamples. |
| T-18-38 | Information Disclosure | invalid-context logging | high | mitigate | Bounded code-only log event and hostile UUID/body/name absence assertions in hermetic and live tests. |
| T-18-39 | Repudiation | skipped handler regressions | high | mitigate | Remove module-wide environment skip, assert explicit OK, and run focused coverage with DATABASE_URL both absent and set. |
| T-18-14-SC | Tampering | package installs | low | accept | No dependency or package installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Gap requirement | Plan | Status
GOAL | — | Durable queue recovery must replay the correct persisted context and classify failure honestly | 18-14 | COVERED
REQ | FAIL-01 | Resume path returns explicit bounded PipelineResult; no None success | 18-14 | COVERED
REQ | FAIL-02 | Durable retry context is reconstructable and safe for money-moving replay | 18-14 | COVERED
RESEARCH | — | RESUME_REPLY reloads persisted email_id and re-enters RECEIVED without losing context | 18-14 | COVERED
CONTEXT | D-01/D-02/D-04/D-05 | Shared explicit contract, bounded diagnostics, durable bridge, RECEIVED retry seam | 18-14 | COVERED
VERIFICATION | CR-02/WR-01 | Same-run association, wrong-run/cross-business proof, unconditional handler coverage, explicit OK | 18-14 | COVERED
</source_audit>

<verification>
- Run the Task 1 focused module twice, once with env -u DATABASE_URL and once with a stub DATABASE_URL.
- Run env -u DATABASE_URL UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_resume_pipeline.py.
- Run DATABASE_URL=postgresql://stub:stub@localhost/stub UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_resume_pipeline.py.
- Run uv run pytest tests/test_queue_durability.py -m queueproof -k "resume_reply and association" -v -rs.
- Run uv run ruff check app/queue/handlers/resume_reply.py tests/test_resume_pipeline.py tests/test_queue_durability.py.
- Run uv run mypy app/queue/handlers/resume_reply.py.
- Run uv run pytest -q, then git diff --check, before phase re-verification.
</verification>

<success_criteria>
- CR-02 is closed: no persisted reply lacking exact canonical run ownership can reach row_to_inbound or resume_pipeline.
- Same-business and cross-business mismatch cases are terminal, bounded, and identifier/PII-free.
- WR-01 is closed: tests/test_resume_pipeline.py runs by default and its reclaim regression requires explicit PipelineOutcome.OK.
- Both DATABASE_URL states pass the hermetic module, while guarded live evidence is reported honestly.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-14-SUMMARY.md when done.
</output>
