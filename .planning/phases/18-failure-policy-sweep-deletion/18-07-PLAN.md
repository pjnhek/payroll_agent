---
phase: 18-failure-policy-sweep-deletion
plan: 07
type: execute
wave: 9
depends_on: [18-04, 18-06, 18-11]
files_modified:
  - app/routes/runs.py
  - app/routes/pipeline_glue.py
  - tests/test_stuck_run_recovery.py
  - tests/test_reply_redelivery.py
  - tests/test_needs_operator.py
  - tests/test_retrigger_epoch.py
autonomous: true
requirements: [FAIL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "GET /runs is read-only: it loads/renders data and performs no status transition, reply consumption, BackgroundTask scheduling, or queue enqueue (D-18)."
    - "All route callers of sweep_stranded_runs and find_stranded_unconsumed_replies are removed while the repository APIs still exist, so this plan's intermediate test wave remains green."
    - "Webhook redelivery, spoofed-sender rejection, consumed/late reply handling, needs_operator behavior, durable operator resume, and manual Retrigger remain covered."
    - "A route AST guard plus mutation/scheduling spies prove the page-load recovery path cannot return."
  artifacts:
    - path: "app/routes/runs.py"
      provides: "Pure read-and-render GET /runs with existing Retrigger unchanged"
    - path: "app/routes/pipeline_glue.py"
      provides: "Inbound conversion documentation for webhook and durable resume handlers only"
    - path: "tests/test_stuck_run_recovery.py"
      provides: "Read-only route and retired-caller subtraction gates"
    - path: "tests/test_reply_redelivery.py"
      provides: "Preserved webhook redelivery and sender/consumption coverage"
    - path: "tests/test_needs_operator.py"
      provides: "Preserved human-gate and durable operator-resume coverage"
  key_links:
    - from: "app/routes/runs.py"
      to: "app/db/repo/demo.py"
      via: "GET /runs performs only list-data reads and template rendering"
      pattern: "runs_list"
    - from: "tests/test_stuck_run_recovery.py"
      to: "app/routes/runs.py"
      via: "route AST inventory and mutation-spy request test"
      pattern: "read_only"
---

<objective>
Remove every route caller of the legacy recovery sweep before deleting its APIs.

Purpose: The dependency order must stay green: page-load mutation and sweep-only tests disappear first while repository/fake symbols remain available until Plan 18-08.
Output: side-effect-free GET /runs, caller/test subtraction, and preserved supported recovery regressions.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-06-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-11-SUMMARY.md

<interfaces>
- Plans 18-03/18-04 install queue settlement/reaping, Plan 18-10 cuts over producers, and Plan 18-11 closes every compatibility-era consumer/normalizer seam before sweep callers are removed.
- Plan 18-06 has already added bounded diagnostics to list/detail/polling in app/routes/runs.py.
- Legacy repository/fake functions intentionally remain through this wave and are deleted only after callers/tests are green.
</interfaces>
</context>

<artifacts_this_phase_produces>
- GET /runs without BackgroundTasks or recovery side effects.
- Deleted request-path age sweep, stranded-reply replay loop, helpers/imports, and sweep-only expectations.
- Updated row_to_inbound wording that names webhook redelivery and durable resume handlers, not page-load recovery.
- Positive supported-path tests plus route-local negative/behavioral gates.
- No repository/facade/fake symbol deletion until dependent Plan 18-08.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove page-load recovery callers and prove GET /runs is read-only</name>
  <files>app/routes/runs.py, app/routes/pipeline_glue.py, tests/test_stuck_run_recovery.py</files>
  <read_first>
    - app/routes/runs.py runs_list signature, stale cutoff, sweep/reply loop, and diagnostic presentation from Plan 18-06.
    - app/routes/pipeline_glue.py row_to_inbound supported callers.
    - tests/test_stuck_run_recovery.py sweep mutation/scheduling tests.
    - app/db/repo/demo.py bounded list projection.
  </read_first>
  <behavior>
    - GET /runs returns the same list/status presentation without accepting BackgroundTasks.
    - It calls only read-side projection/presentation/template seams.
    - Mutating, enqueueing, reply-consuming, and scheduling spies are never called.
    - Retired symbols are absent from route source/imports, though repository definitions remain until Plan 18-08.
  </behavior>
  <action>
    Remove BackgroundTasks from runs_list and delete the complete age cutoff, sweep call, stranded-reply lookup, reply conversion/sender replay branch, background resume scheduling, recovery counts, and related imports. Preserve only list/status reads, safe presentation derivation, and rendering. Update row_to_inbound documentation/caller inventory for webhook redelivery plus RESUME_REPLY; it must not claim GET /runs as a caller. Rewrite test_stuck_run_recovery into a route-scoped AST/import guard and a successful GET with every mutation/scheduling seam patched to raise. Do not delete the repository/fake definitions in this plan and do not replace the sweep with an enqueue.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_stuck_run_recovery.py tests/test_dashboard.py -k "runs or retired or read_only"</automated>
  </verify>
  <acceptance_criteria>
    - GET /runs has no BackgroundTasks parameter or side-effect branch.
    - Route source contains neither retired recovery call.
    - Mutation/scheduling spies remain untouched during a 200 response.
    - Plan 18-06 list/detail/poll rendering stays green.
  </acceptance_criteria>
  <done>Opening the run list cannot mutate payroll, reply, or queue state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove caller-only tests while preserving supported recovery paths</name>
  <files>tests/test_stuck_run_recovery.py, tests/test_reply_redelivery.py, tests/test_needs_operator.py, tests/test_retrigger_epoch.py</files>
  <read_first>
    - tests/test_reply_redelivery.py webhook duplicate, sender, consumed, and page-load cases.
    - tests/test_needs_operator.py human-gate and operator-resume cases.
    - tests/test_retrigger_epoch.py epoch-scoped append-only audit proofs; preserve its current clear_reply_context and load_consumed_replies behavioral analogs while removing retired lookup prose only.
    - app/queue/handlers/resume_reply.py and operator_resume.py supported durable handlers.
  </read_first>
  <behavior>
    - No test expects viewing /runs to recover work.
    - Webhook duplicate/redelivery, spoofed sender, consumed/late replies, and needs_operator remain deterministic.
    - Durable reply/operator handlers and manual Retrigger remain separate supported entry points.
    - Tests do not import/call retired repository APIs after this task.
  </behavior>
  <action>
    Delete tests whose sole contract was request-path recovery or stranded-reply replay. Re-home only still-valid redelivery, sender, consumption, late-reply, needs_operator, and durable resume assertions in their domain files. Follow tests/test_retrigger_epoch.py's existing real-seam pattern: update only its retired lookup prose while preserving clear_reply_context, append-only email history, current-epoch get_outbound_for_round, and load_consumed_replies behavior. Add an explicit supported-entry-point inventory rejecting background list-page resume. Keep manual Retrigger tests and the complete operator mapping proofs. Do not edit repository/fake APIs yet.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_stuck_run_recovery.py tests/test_reply_redelivery.py tests/test_needs_operator.py tests/test_retrigger_epoch.py tests/test_hitl.py</automated>
  </verify>
  <acceptance_criteria>
    - Wave 6 is green while legacy APIs still exist but have zero live callers.
    - No page-view recovery expectation remains.
    - Legitimate reply/operator/manual recovery safeguards remain directly covered.
  </acceptance_criteria>
  <done>Legacy callers are gone before their APIs, with supported recovery behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated GET to durable state | A read page previously triggered mutation and scheduling. |
| inbound reply/operator mapping to resume | Supported durable handlers must retain sender/consumption/validation gates. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-24 | Tampering | GET /runs | high | mitigate | Structural caller deletion, route AST guard, and mutating/scheduling spies. |
| T-18-25 | Spoofing | supported resume paths | high | mitigate | Preserve sender, consumed, late-reply, operator-map, and manual retrigger tests. |
| T-18-26 | Repudiation | hidden compatibility caller | medium | mitigate | Explicit supported-entry-point inventory and caller-only test deletion. |
| T-18-07-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_stuck_run_recovery.py tests/test_reply_redelivery.py tests/test_needs_operator.py tests/test_retrigger_epoch.py tests/test_hitl.py tests/test_dashboard.py.
- Run uv run ruff check app/routes/runs.py app/routes/pipeline_glue.py and changed tests.
- Run uv run mypy app/routes/runs.py app/routes/pipeline_glue.py.
</verification>

<success_criteria>
- D-18 caller deletion is complete and GET /runs is proven read-only.
- Plan 18-08 can delete repository/fake APIs without breaking route/test imports.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-07-SUMMARY.md when done.
</output>
