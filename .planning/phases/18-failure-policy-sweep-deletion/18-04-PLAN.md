---
phase: 18-failure-policy-sweep-deletion
plan: 04
type: execute
wave: 6
depends_on: [18-02, 18-03, 18-09]
files_modified:
  - app/queue/handlers/pipeline.py
  - app/queue/drain.py
  - tests/test_queue_drain.py
  - tests/test_queue_durability.py
autonomous: true
requirements: [FAIL-01, FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Queued consumers are green before producer cutover: handlers/dispatch may still return legacy None, which drain accepts only through normalize_pipeline_result as OK; explicit results already use atomic settlement."
    - "drain_once maps an explicit handler PipelineResult through the atomic settlement seam: ok done, retryable retried/dead, terminal transport done plus run error; injected explicit-result tests prove the future producer contract before Plan 18-10 removes legacy orchestrator persistence (D-07, D-16)."
    - "When normal claim finds nothing, every shared drain path attempts exactly one final-attempt lease reap before reporting EMPTY (D-13)."
    - "The exact expired leased and attempts=max_attempts row is dead-lettered; attempts-below-cap and unexpired rows are not (D-17)."
    - "A reaped final lease is a distinct truthy REAPED_FINAL_LEASE outcome with FinalAttemptLeaseExpired, preserving earlier last_error as history without misattribution (D-15)."
    - "Safe classified infrastructure exceptions use the same atomic retry/dead coordinator: below cap pending/received, at cap dead/error; only failure of that settlement write re-raises and retains the held token."
  artifacts:
    - path: "app/queue/drain.py"
      provides: "Result-aware settlement plus shared final-attempt reaper and DrainOutcome.REAPED_FINAL_LEASE"
    - path: "app/queue/handlers/pipeline.py"
      provides: "Initial handler forwards PipelineResult after CAS-only run claim"
    - path: "tests/test_queue_drain.py"
      provides: "Hermetic result/outcome and normal-claim-before-reap matrix"
    - path: "tests/test_queue_durability.py"
      provides: "Live exact-predicate, rollback, and zombie-fence reaper proofs"
  key_links:
    - from: "app/queue/drain.py"
      to: "app/db/repo/job_settlement.py"
      via: "settle_pipeline_job for returned results, bounded atomic infrastructure retry/dead settlement for escaped dispatch failures, and reap_expired_final_attempt only after claim_job returns None"
      pattern: "REAPED_FINAL_LEASE"
    - from: "app/queue/worker.py"
      to: "app/queue/drain.py"
      via: "every outcome except EMPTY stays truthy so workers continue draining"
      pattern: "drain_once"
---

<objective>
Make the shared drain path enforce the explicit failure policy and close the accepted final-attempt lease strand.

Purpose: Workers and the pump must observe identical retry/dead/terminal semantics, and queue empty must mean neither executable nor reapable work remains.
Output: result-aware drain settlement, exact final-lease reaping, and hermetic/live proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/17-the-pump/17-SECURITY.md
@.planning/phases/17-the-pump/17-01-SUMMARY.md
@.planning/phases/17-the-pump/17-05-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md

<interfaces>
- dispatch.handle(job) forwards PipelineResult | None from a registered handler during the compatibility wave; normalize_pipeline_result is the only None policy.
- settle_pipeline_job(job, result, backoff_seconds) owns cross-aggregate atomic settlement.
- reap_expired_final_attempt() returns a bounded settlement result for one exact eligible row.
- DrainOutcome.EMPTY is the only falsy member; worker.py relies on this.
</interfaces>
</context>

<artifacts_this_phase_produces>
- New enum member DrainOutcome.REAPED_FINAL_LEASE with truthy behavior.
- Changed handle_run_pipeline(job) to forward PipelineResult | None before cutover; CAS-loser returns explicit PipelineResult.OK.
- Changed drain_once result mapping for normalized PipelineResult outcomes, including legacy None as compatibility-only OK.
- Changed escaping-infrastructure handling to use bounded atomic retry/dead settlement rather than jobs.fail_job; settlement-write failure alone re-raises with held-token retention.
- Shared drain invocation of reap_expired_final_attempt after an empty normal claim.
- New hermetic and live tests for result mapping, exact reaper ordering/predicate, final reason, and fencing.
- No new job kind, schema object, route, run status, dashboard, or dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map explicit handler results through fenced atomic settlement</name>
  <files>app/queue/handlers/pipeline.py, app/queue/drain.py, tests/test_queue_drain.py, tests/test_queue_durability.py</files>
  <read_first>
    - app/queue/handlers/pipeline.py current CAS-only J-1 handler.
    - app/queue/drain.py held-token, exception, and outcome logic.
    - app/db/repo/job_settlement.py Plan 18-03 public settlement contract.
    - tests/test_queue_drain.py current DONE/RETRIED/DEAD/FENCED and status-writer guards.
  </read_first>
  <behavior>
    - A legacy None returned through initial, reply, or operator dispatch normalizes to OK and settles DONE in this wave.
    - CAS-loser handler returns OK, and drain settles the transport row done.
    - OK result returns DONE.
    - RETRYABLE below cap returns RETRIED; at cap returns DEAD.
    - TERMINAL begins with a leased job and active run, then atomically returns DONE while the run becomes error without a false FENCED result.
    - Settlement FENCED returns FENCED and releases this process's held-token record.
    - Escaping safe infrastructure exception below cap atomically returns the job pending and run received; at cap it atomically commits job dead and run error.
    - Infrastructure settlement rollback or stale token changes neither aggregate; failure of the settlement write itself re-raises and retains the token.
  </behavior>
  <action>
    Change handle_run_pipeline to forward pipeline_glue.run_pipeline_now's temporary PipelineResult | None; if its run-status CAS loses, return explicit PipelineResult.OK so at-least-once delivery becomes a completed no-op under J-1. Keep the Plan 18-09 reply/operator handlers and dispatch return-forwarding compatible with the same union. In drain_once, capture dispatch.handle(job)'s value, pass it through normalize_pipeline_result, then pass the normalized PipelineResult plus _backoff_seconds(job.attempts) to settle_pipeline_job. No consumer gets a private `if result is None` branch. Map SettlementOutcome to existing DrainOutcome values, with explicit terminal mapping to DONE because the transport successfully classified the run even though business status becomes ERROR in that same transaction. Add a legacy-None DONE case plus a queued-terminal regression that starts from an active run and leased job and proves DONE plus ERROR commit together without a false FENCED result. For an exception that safely escapes dispatch, derive only a bounded infrastructure reason/type code and route it through settle_infrastructure_failure (or the coordinator's equivalent shared retry/dead primitive): below cap atomically fence job to PENDING and CAS run to RECEIVED; at cap atomically fence job to DEAD and CAS run to ERROR with bounded exhaustion detail (D-06, D-07). Do not call repo.fail_job for this path. A stale token or lost run CAS returns FENCED/no-op with neither aggregate changed. Reserve re-raise and held-token retention for an exception thrown by the settlement write itself, since releasing the token would hide an unpersisted failure. Update the static J-1 guard so the new coordinator is the only additional sanctioned queue-tier status-writing seam and it fails closed on alias/getattr/import tricks; no unconditional set_status is introduced. Add hermetic cases for every result/fence and live at-cap, rollback, and stale-token infrastructure counterexamples. Plan 18-10 tightens the adapter only after this consumer path is complete.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_queue_drain.py -k "pipeline_result or infrastructure or drain_once or status_writers" && uv run pytest tests/test_queue_durability.py -m queueproof -k "terminal_result or infrastructure" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - All three PipelineOutcome values map to the locked transport/run matrix.
    - Legacy None maps to DONE only through the shared compatibility adapter, and a static assertion finds no duplicate None policy in queue consumers.
    - Clarification OK is DONE and never RETRIED.
    - Terminal is transport DONE, not DEAD.
    - An at-cap escaping infrastructure exception commits DEAD/ERROR together; below-cap commits PENDING/RECEIVED.
    - Rollback, stale-token, and settlement-write-failure tests prove neither partial writes nor silent token release.
    - J-1 static guard remains fail-closed and non-vacuous.
  </acceptance_criteria>
  <done>The queue interprets explicit pipeline results without confusing business clarification or terminal classification with transport failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reap the exact expired final-attempt lease before queue-empty</name>
  <files>app/queue/drain.py, tests/test_queue_drain.py, tests/test_queue_durability.py</files>
  <read_first>
    - app/db/repo/job_settlement.py reap function and exact SQL predicate.
    - app/queue/drain.py claim-before-empty control flow.
    - tests/test_queue_durability.py existing lease-expiry tests that manipulate leased_until without sleeps.
    - .planning/phases/17-the-pump/17-SECURITY.md T-17-16 and AR-17-03.
  </read_first>
  <behavior>
    - Normal due work is claimed before any reap attempt.
    - No claim plus one exact eligible row returns REAPED_FINAL_LEASE, not EMPTY.
    - Next call continues reaping until none remain; only then returns EMPTY.
    - Exact predicate: leased, attempts equal max_attempts, leased_until expired.
    - Reap stores FinalAttemptLeaseExpired and does not reuse earlier last_error as the final reason.
  </behavior>
  <action>
    Add DrainOutcome.REAPED_FINAL_LEASE and keep __bool__ defined as self is not EMPTY. In drain_once, after claim_job returns None, call reap_expired_final_attempt once; map its successful result to REAPED_FINAL_LEASE and only return EMPTY when neither normal claim nor reap settles a row. Keep normal executable work first and at most one settlement per call. Hermetic tests must assert call ordering and that the worker loop remains truthy. Live tests must backdate leased_until directly and prove the exact predicate, atomic job dead/run error, bounded FinalAttemptLeaseExpired reason, rollback on injected failure, and no effect on below-cap or unexpired final leases. Include a lost run-CAS case per research and prove it leaves no partial write. Do not add a separate timer, route sweep, dashboard hook, or maintenance worker.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_queue_drain.py && uv run pytest tests/test_queue_durability.py -m queueproof -k "final_attempt or reap" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - REAPED_FINAL_LEASE is truthy and EMPTY remains the only falsy member.
    - Live tests prove the exact D-17 row is settled dead/error and near-miss rows are untouched.
    - dead/error transition is atomic and fenced/CAS-safe.
    - Earlier last_error is retained only as history, never copied into final run diagnostic.
  </acceptance_criteria>
  <done>Every shared drain closes the final-attempt lease strand before honestly reporting the queue empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| handler result to transport settlement | A business result controls retry/dead/done state. |
| expired lease to reaper authority | Expiry plus row lock authorizes settlement without a live worker token. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-12 | Tampering | drain result mapping | high | mitigate | Exhaustive typed mapping with clarification OK, atomic terminal DONE/ERROR, and bounded infrastructure retry/dead counterexamples. |
| T-18-13 | Denial of Service | final-attempt lease | high | mitigate | Exact reaper predicate on every shared drain plus live Postgres proof. |
| T-18-14 | Tampering | reaper/run state | high | mitigate | FOR UPDATE SKIP LOCKED, one atomic job/run transaction, run CAS, and rollback/fence tests. |
| T-18-15 | Repudiation | final reason | medium | mitigate | Dedicated bounded FinalAttemptLeaseExpired code; never reinterpret earlier last_error. |
| T-18-04-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_queue_drain.py.
- Run uv run pytest tests/test_queue_durability.py -m queueproof -v -rs.
- Run uv run ruff check app/queue/handlers/pipeline.py app/queue/drain.py and uv run mypy on them.
</verification>

<success_criteria>
- D-13, D-15, D-16, and D-17 are directly proven.
- The Phase 17 accepted final-attempt strand is closed, not hidden from queue depth.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-04-SUMMARY.md when done.
</output>
