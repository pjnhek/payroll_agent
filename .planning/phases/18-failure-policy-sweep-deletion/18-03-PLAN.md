---
phase: 18-failure-policy-sweep-deletion
plan: 03
type: execute
wave: 5
depends_on: [18-01, 18-02, 18-09, 18-12]
files_modified:
  - app/db/repo/job_settlement.py
  - app/db/repo/__init__.py
  - app/routes/runs.py
  - app/routes/pipeline_glue.py
  - tests/test_queue_drain.py
  - tests/test_resume_pipeline.py
  - tests/test_needs_operator.py
  - tests/test_queue_durability.py
  - tests/conftest.py
autonomous: true
requirements: [FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "All three BackgroundTask consumers are installed before producer cutover: legacy None is accepted only through normalize_pipeline_result as legacy success, while explicit retryable/terminal results already take the durable policy path."
    - "Every retryable pre-cutover BackgroundTask result enters durable queue work in the same transaction that returns the run to received; no in-memory retry loop or sweep fallback exists (D-04, D-05)."
    - "At this plan boundary, every valid /runs/{run_id}/resolve POST atomically creates a fresh immutable operator_resolution_id plus the complete server-validated mapping, commits it, and schedules operator_resume_bg(run_id, operator_resolution_id); no active caller passes raw overrides to the identifier-only wrapper."
    - "Operator-resume RETRYABLE receives only run_id plus a previously persisted operator_resolution_id, reloads the complete typed mapping, and enqueues one identifier-only OPERATOR_RESUME job deduped by that resolution UUID; it is never keyed only by reply_epoch or reconstructed from alias_candidates."
    - "Duplicate handling of one resolution UUID produces one durable retry job, while two valid resolutions created in the same reply_epoch produce distinct jobs and mappings."
    - "Transient stage/reason diagnostics live only on jobs.last_error; payroll_runs.error fields remain untouched until terminal or exhaustion (D-06, D-08)."
    - "Queued exhaustion atomically commits job dead plus run error, while queued explicit terminal atomically commits job done plus run error under the lease/run fences (D-07, D-16)."
    - "Dead history remains immutable and bounded diagnostics contain only reason, optional safe stage, and attempts/max_attempts (D-09, D-10, D-11)."
    - "Every coordinator seam has a strict registered InMemoryRepo mirror, including enqueue_operator_resume_retry."
  artifacts:
    - path: "app/db/repo/job_settlement.py"
      provides: "Atomic classified retry bridges over already-persisted operator authority, queued settlement, infrastructure failure settlement, and final-lease reaper"
    - path: "app/routes/runs.py"
      provides: "Complete valid /resolve transaction: fresh immutable resolution generation, typed mapping persistence, and identifier-only post-commit handoff"
    - path: "app/routes/pipeline_glue.py"
      provides: "Return-aware initial, reply-resume, and operator-resume BackgroundTask wrappers"
    - path: "tests/test_needs_operator.py"
      provides: "OK/retryable/terminal operator bridge proofs with mixed remember choices"
    - path: "tests/test_queue_durability.py"
      provides: "Live Postgres mapping+enqueue atomicity, rollback, fencing, and settlement proofs"
    - path: "tests/conftest.py"
      provides: "Strict stateful coordinator fakes and patch registrations"
  key_links:
    - from: "app/routes/pipeline_glue.py"
      to: "app/db/repo/job_settlement.py"
      via: "each RETRYABLE result selects the matching durable bridge"
      pattern: "enqueue_classified_retry|enqueue_operator_resume_retry"
    - from: "app/routes/runs.py"
      to: "app/db/repo/operator_resume_resolutions.py|app/routes/pipeline_glue.py"
      via: "one valid /resolve transaction persists the complete mapping under a fresh UUID before scheduling operator_resume_bg with identifiers only"
      pattern: "create_operator_resume_resolution|operator_resolution_id|operator_resume_bg"
    - from: "app/db/repo/job_settlement.py"
      to: "operator_resume_resolutions/operator_resume_overrides/jobs/payroll_runs"
      via: "one transaction reloads the immutable resolution, CASes EXTRACTING to RECEIVED, and inserts OPERATOR_RESUME keyed/deduped by operator_resolution_id"
      pattern: "enqueue_operator_resume_retry"
    - from: "app/db/repo/job_settlement.py"
      to: "jobs/payroll_runs"
      via: "one transaction combines exact lease-token fence and run-status CAS"
      pattern: "lease_token"
    - from: "tests/conftest.py"
      to: "app/db/repo/job_settlement.py"
      via: "six strict public coordinator mirrors are registered in fake_repo"
      pattern: "enqueue_operator_resume_retry|settle_pipeline_job|reap_expired_final_attempt"
---

<objective>
Create one atomic failure-policy coordinator and bridge all three pre-cutover background entry points into durable retries.

Purpose: FAIL-02 requires classified retries to survive process loss without dropping reply/operator context, competing with business-state writers, or erasing dead history.
Output: fenced cross-aggregate settlement, an atomic identifier-only /resolve handoff, lossless initial/reply/operator bridges, strict fakes, and hermetic/live transaction proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-VALIDATION.md
@.planning/phases/18-failure-policy-sweep-deletion/18-01-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md

<interfaces>
- PipelineResult carries only bounded outcome/stage/reason and defaults terminal; normalize_pipeline_result is the sole temporary None-to-OK bridge until Plan 18-10 tightens it.
- Plans 18-02/18-09 provide RUN_PIPELINE, RESUME_REPLY, OPERATOR_RESUME plus lossless handlers and typed context repositories.
- run_pipeline_bg, resume_pipeline_bg, and operator_resume_bg remain first-attempt BackgroundTask wrappers until Phase 19; in this wave their producers still return None, but tests inject explicit future PipelineResult values to prove retry/terminal handling before cutover.
- alias_candidates is intentionally partial optional learning state and must never be used as the operator override mapping.
</interfaces>
</context>

<artifacts_this_phase_produces>
- New SettlementOutcome with DONE, RETRIED, DEAD, FENCED, REAPED_FINAL_LEASE.
- New enqueue_classified_retry for initial/persisted-reply jobs.
- New enqueue_operator_resume_retry(run_id, operator_resolution_id, result, conn=None) that reloads the complete immutable typed resolution and inserts OPERATOR_RESUME under a resolution-UUID dedup generation.
- New settle_pipeline_job, settle_background_terminal, settle_infrastructure_failure, and reap_expired_final_attempt.
- Valid /resolve creates a fresh UUID, persists the complete server-validated mapping and remember-only alias state in one route transaction, commits, then schedules operator_resume_bg(run_id, operator_resolution_id).
- run_pipeline_now/resume_pipeline_now temporarily return PipelineResult | None; all three background wrappers normalize through the single legacy adapter and consume OK/RETRYABLE/TERMINAL.
- Strict fakes for all six coordinator functions and live rollback/fence proofs.
- No terminal conversion of a retryable provider result, generic jobs payload, producer cutover, send replay, queue dashboard, or new status.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement fenced atomic settlement and complete operator retry persistence</name>
  <files>app/db/repo/job_settlement.py, app/db/repo/__init__.py, tests/conftest.py, tests/test_queue_durability.py</files>
  <read_first>
    - app/db/repo/_shared.py transaction ownership helpers.
    - app/db/repo/jobs.py fenced transport primitives.
    - app/db/repo/operator_resume_resolutions.py typed immutable create/load contract.
    - app/db/repo/runs.py claim_status and bounded error persistence.
    - tests/test_queue_durability.py rollback, lease-expiry, and zombie-fence analogs.
    - tests/conftest.py strict fake/public patch pairing.
  </read_first>
  <behavior>
    - OK: leased job -> done, run remains business-authoritative.
    - RETRYABLE below cap: leased job -> pending with delay/safe last_error and run extracting -> received atomically.
    - RETRYABLE at cap: job dead and eligible run error commit together with RetryExhausted and bounded attempts.
    - TERMINAL: job done and eligible run error commit together; dead is not used.
    - Operator BackgroundTask RETRYABLE: the route-persisted resolution reloads completely, the run returns to received, and one OPERATOR_RESUME job carrying only that resolution id is inserted in the same transaction.
    - Duplicate same-resolution enqueue is idempotent; a second resolution id in the same reply_epoch creates a distinct job. Lost run CAS or missing/wrong-run mapping returns FENCED/no-op; injected half-failure rolls back run and job without changing the immutable resolution.
  </behavior>
  <action>
    Create job_settlement.py as the sole cross-aggregate coordinator, with conn=None and one outer transaction per operation. Implement the locked settlement matrix and bounded final diagnostics. Add enqueue_operator_resume_retry(run_id, operator_resolution_id, result, conn=None): reload the complete immutable resolution by run/id, CAS EXTRACTING to RECEIVED, and enqueue JobKind.OPERATOR_RESUME with operator_resolution_id under dedup operator_resume:{run_id}:{operator_resolution_id}. An existing row for the same identifiers is idempotent; a different resolution UUID in the same epoch creates a distinct job. Any CAS loss or enqueue failure rolls back run/job while leaving the immutable resolution unchanged. Do not log mapping values, pass raw mappings to jobs, key dedup by reply_epoch, or consult alias_candidates. Implement the normal initial/reply enqueue bridge, queued settlement, background-only terminal CAS, bounded infrastructure settlement, and exact final-attempt reaper. Export the six public coordinator functions/types.

    Extend InMemoryRepo with exact stateful mirrors for enqueue_classified_retry, enqueue_operator_resume_retry, settle_pipeline_job, settle_background_terminal, settle_infrastructure_failure, and reap_expired_final_attempt; register every name. The operator fake atomically models run rewind plus identifier-only job while reading the immutable resolution store, dedupes the same resolution id, keeps a second same-epoch resolution distinct, and supports rollback assertions. Live tests cover normal retry, at-cap dead/error, terminal done/error, complete-map reload, mixed remember selections, duplicate same-resolution idempotency, two same-epoch resolution ids/jobs, missing/wrong-run resolution fence, injected half-failure, stale token, lost run CAS, and immutable dead history. Persist only bounded codes/integers.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_fake_repo_pairing.py tests/test_queue_drain.py -k "pairing or settlement or operator_resume or final_attempt" && uv run pytest tests/test_queue_durability.py -m queueproof -k "settlement or operator_resume or exhaustion or terminal_result or infrastructure or final_attempt" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - Job/run settlement is one fenced transaction for every queued outcome.
    - Operator retry consumes the already-committed full mapping and atomically writes only the identifier-only job plus RECEIVED run.
    - Rollback leaves the immutable route-persisted resolution unchanged; duplicate mismatch cannot replace authority.
    - Diagnostics exclude injected sensitive names/employee ids/raw exceptions.
    - All six coordinator fake methods are defined, registered, strict, and positively paired.
  </acceptance_criteria>
  <done>One transaction seam owns classified settlement and every durable retry bridge, including complete operator authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bridge initial and persisted-reply BackgroundTask results</name>
  <files>app/routes/pipeline_glue.py, tests/test_queue_drain.py, tests/test_resume_pipeline.py</files>
  <read_first>
    - app/routes/pipeline_glue.py current wrappers and row_to_inbound.
    - app/pipeline/orchestrator.py shared PipelineResult returns.
    - app/queue/handlers/resume_reply.py retry-only RECEIVED seam.
    - app/db/repo/job_settlement.py Task 1 bridge functions.
  </read_first>
  <behavior>
    - Legacy None from either unchanged orchestrator entry point normalizes to OK and preserves all existing behavior during this wave.
    - Initial/reply OK performs no enqueue; TERMINAL performs one background-only bounded run CAS and no enqueue.
    - Initial RETRYABLE creates one delayed RUN_PIPELINE job; reply RETRYABLE creates one delayed RESUME_REPLY with inbound.id.
    - Both RETRYABLE paths leave the run received atomically and wake only after commit.
    - No wrapper sleeps, loops, schedules recursive BackgroundTasks, or calls a sweep.
  </behavior>
  <action>
    Make run_pipeline_now and new resume_pipeline_now expose the producer's temporary PipelineResult | None return. Update run_pipeline_bg and resume_pipeline_bg to pass that value through normalize_pipeline_result before exhaustive outcome handling; do not add local None branches. Legacy None must behave as the pre-change completed path, while injected explicit values prove the future contract: for OK return; for TERMINAL use settle_background_terminal because no leased job exists; for RETRYABLE call enqueue_classified_retry with the correct kind, persisted email id where required, bounded diagnostic, first backoff+jitter, and a dedup key scoped to run/current reply generation; wake after commit only. Keep the queued handler path separate so settle_pipeline_job remains its atomic owner. Add legacy-None, idempotency, exact reply-id/body, terminal-owner, no-recursion, and post-commit wake tests. Plan 18-10 tightens the shared adapter after producers change.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_fake_repo_pairing.py tests/test_queue_drain.py tests/test_resume_pipeline.py -k "pairing or background or resume or reply"</automated>
  </verify>
  <acceptance_criteria>
    - Each retryable result creates exactly one reconstructable durable row and leaves run received.
    - OK/TERMINAL create no retry job; terminal writes bounded run error once.
    - Reply retries preserve the exact persisted reply id and never route to run_pipeline.
    - Wake ordering and no in-memory retry are directly proven.
  </acceptance_criteria>
  <done>Initial and clarification BackgroundTasks hand every retryable result to durable work without context loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Atomically persist valid operator resolutions and bridge identifier-only retries</name>
  <files>app/routes/runs.py, app/routes/pipeline_glue.py, tests/test_needs_operator.py, tests/test_queue_durability.py</files>
  <read_first>
    - app/routes/runs.py all-or-nothing roster validation, remember-checkbox behavior, transaction helpers, and current raw-overrides BackgroundTask handoff.
    - app/db/repo/operator_resume_resolutions.py immutable create/load contract from Plan 18-02.
    - app/routes/pipeline_glue.py operator_resume_bg.
    - app/queue/handlers/operator_resume.py complete durable reconstruction contract.
    - tests/test_needs_operator.py route-to-real-resume and mixed remember cases.
  </read_first>
  <behavior>
    - Invalid, missing, incomplete, or cross-business POST mappings create no resolution row, mutate no alias state, and schedule no work.
    - Every valid POST generates a fresh operator_resolution_id independent of reply_epoch, persists the complete server-validated mapping plus remember-only alias state in one caller-owned transaction, commits, then schedules operator_resume_bg(run_id, operator_resolution_id).
    - At task completion, no active /resolve caller passes a raw overrides mapping to the identifier-only wrapper; a focused signature/caller test fails if that incompatibility returns.
    - Legacy None from operator resume normalizes to OK and preserves the current completed path during this wave.
    - operator_resume_bg OK does nothing further; TERMINAL performs one bounded background-only run CAS.
    - RETRYABLE passes only the persisted operator_resolution_id to enqueue_operator_resume_retry and creates one OPERATOR_RESUME job for that immutable generation.
    - Mixed remember choices do not affect durable mapping completeness; alias_candidates is never read by the bridge/handler.
    - Reclaimed operator jobs reconstruct the exact mapping and invoke resume_pipeline from RECEIVED; two valid same-epoch POSTs retain distinct resolution ids/jobs.
  </behavior>
  <action>
    Make this task atomically own the complete valid /runs/{run_id}/resolve transition before changing the wrapper signature. Preserve the existing all-or-nothing roster validation, then generate operator_resolution_id with uuid.uuid4 and open one caller-owned transaction. Within it, call create_operator_resume_resolution(run_id, operator_resolution_id, overrides, conn=conn) with every validated submitted-name/employee-id pair and apply only remember-checked alias-candidate updates through the same connection; any persistence or alias write failure rolls back the parent, all child rows, and alias state. Exit the transaction before background scheduling, then add exactly operator_resume_bg(run_id, operator_resolution_id). Invalid/missing/cross-business mappings and stale/non-NEEDS_OPERATOR runs create no generation and schedule nothing. A later valid POST gets a fresh UUID even in the same reply_epoch; duplicate execution of one committed background item reuses its identifier.

    In the same task, update operator_resume_bg to accept run_id plus operator_resolution_id, reload the complete mapping through load_operator_resume_resolution, call resume_pipeline_now with that mapping, and exhaustively consume the temporary normalized result. Do not retain an overload/raw-mapping compatibility branch or duplicate a None branch. For RETRYABLE call enqueue_operator_resume_retry with only the same identifiers; do not reclassify, key dedup by reply_epoch, serialize a payload, or infer from alias_candidates. Wake only after commit. Missing/wrong-run/incomplete context fails closed with the bounded operator-context reason. Add focused AST/signature and route tests proving every active caller passes exactly the two identifiers, commit happens before scheduling, existing resolve behavior crosses the new seam at the task boundary, mixed remember choices do not truncate durable authority, persistence failure schedules nothing, duplicate same-resolution execution is idempotent, two same-epoch resolutions/jobs remain distinct, and raw mappings/PII never enter BackgroundTask args, jobs, logs, or diagnostics.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_fake_repo_pairing.py tests/test_needs_operator.py -k "pairing or resolve or resolution or operator_resume or override or retryable or signature or caller" && uv run pytest tests/test_queue_durability.py -m queueproof -k "operator_resume" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - Complete server-validated authority and remember-only alias state commit atomically before background scheduling.
    - No active caller passes raw overrides to operator_resume_bg; the route and wrapper signatures agree at this task boundary.
    - Retryable operator work remains retryable and durable; no unavailable/fail-closed terminal conversion exists.
    - Reloaded key/value rows exactly equal the validated POST mapping, including unchecked remember names.
    - Handler/reclaim tests consume that exact mapping from RECEIVED.
    - Jobs and diagnostics contain no mapping or PII.
  </acceptance_criteria>
  <done>Operator resume has the same durable retry guarantee as every other classified pipeline entry point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BackgroundTask result to Postgres | A process-local first attempt hands retry authority to durable state. |
| valid operator POST to durable resolution generation | Money-moving authority and remember-only alias state must persist completely before identifier-only scheduling. |
| leased job/run pair to settlement | A zombie token or newer run state must fence both writes. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-12 | Tampering | /resolve persistence and operator retry | high | mitigate | Route-owned atomic UUID/mapping persistence, post-commit identifier-only handoff, resolution-scoped dedup, caller/signature guard, and rollback proofs. |
| T-18-13 | Denial of Service | background retry bridge | high | mitigate | Durable delayed enqueue for all three entry points; no terminal reclassification or in-memory loop. |
| T-18-14 | Tampering | classified settlement | high | mitigate | Exact lease-token fence, run CAS, atomic rollback, and immutable dead history. |
| T-18-15 | Information Disclosure | diagnostics/override rows | high | mitigate | Bounded codes in diagnostics; mapping only in typed access-controlled DB rows and never logs/jobs/UI. |
| T-18-03-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run the FAIL-02 focused suite and live queueproof commands from each task.
- Run uv run ruff check app/db/repo/job_settlement.py app/routes/pipeline_glue.py tests/conftest.py.
- Run uv run mypy app/db/repo/job_settlement.py app/routes/pipeline_glue.py.
</verification>

<success_criteria>
- D-04 through D-11 and D-16 are executable and lossless for initial, reply, and operator retries, with no incompatible raw-override caller at the Plan 18-03 boundary.
- The highest-priority checker blocker is eliminated: operator RETRYABLE remains retryable with complete durable authority.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md when done.
</output>
