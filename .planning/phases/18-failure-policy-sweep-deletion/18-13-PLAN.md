---
phase: 18-failure-policy-sweep-deletion
plan: 13
type: execute
wave: 11
depends_on: [18-04]
files_modified:
  - app/db/repo/job_settlement.py
  - tests/conftest.py
  - tests/test_queue_drain.py
  - tests/test_queue_durability.py
autonomous: true
requirements: [FAIL-02]
gap_closure: true
nyquist_compliant: true

must_haves:
  truths:
    - "Every exact expired final-attempt lease reaches dead transport state in one reap; no valid RunStatus can leave the selected oldest row leased or starve the next candidate (CR-01, D-13, D-17)."
    - "The reaper locks the associated payroll run and applies one exhaustive status matrix inside the same transaction as the job dead-letter write (D-07)."
    - "Active crash states RECEIVED, EXTRACTING, COMPUTED, and APPROVED become ERROR with the bounded FinalAttemptLeaseExpired diagnostic, while SENT, AWAITING_REPLY, AWAITING_APPROVAL, NEEDS_OPERATOR, RECONCILED, REJECTED, and existing ERROR remain authoritative and unchanged."
    - "All matrix branches preserve jobs.last_error as prior-attempt history, clear the lease, return REAPED_FINAL_LEASE, and allow a second eligible row to progress (D-15)."
  artifacts:
    - path: "app/db/repo/job_settlement.py"
      provides: "Exhaustive run-locked final-lease settlement matrix with no valid-status FENCED strand"
    - path: "tests/conftest.py"
      provides: "Stateful hermetic reaper mirror with the same exhaustive status semantics"
    - path: "tests/test_queue_drain.py"
      provides: "Always-run status-matrix, exhaustiveness, and second-candidate liveness regressions"
    - path: "tests/test_queue_durability.py"
      provides: "Guarded live Postgres matrix, atomicity, rollback, and starvation proofs"
  key_links:
    - from: "app/db/repo/job_settlement.py"
      to: "payroll_runs and jobs"
      via: "FOR UPDATE locks on the oldest exact job candidate and its associated run before one atomic matrix branch"
      pattern: "FOR UPDATE SKIP LOCKED|FOR UPDATE|REAPED_FINAL_LEASE"
    - from: "app/queue/drain.py"
      to: "app/db/repo/job_settlement.py"
      via: "the existing reap-before-EMPTY seam receives a truthy result for every valid run status"
      pattern: "reap_expired_final_attempt"
---

<objective>
Close CR-01 by making final-attempt lease reaping status-aware, atomic, and starvation-free.

Purpose: FAIL-02 and D-17 are not achieved while the oldest expired final-attempt row can return FENCED forever after the business run advances. The transport row must always settle without erasing authoritative completed or human-wait business state.
Output: an exhaustive run-locked settlement matrix, a faithful fake, and hermetic plus guarded-live liveness proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-VERIFICATION.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-04-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-05-SUMMARY.md

<interfaces>
- app/db/repo/job_settlement.py::reap_expired_final_attempt currently locks the oldest exact jobs row but assumes EXTRACTING -> ERROR; a false run CAS returns FENCED without changing the job.
- app/queue/drain.py already calls one reaper after an empty normal claim and maps REAPED_FINAL_LEASE to a truthy drain outcome; pump accounting already records it as dead maintenance outside claimed work.
- RunStatus is the exhaustive business-state vocabulary. The final-lease matrix must be disjoint and set-equal to every RunStatus member so a future status cannot silently recreate starvation.
- tests/conftest.py::InMemoryRepo.reap_expired_final_attempt mirrors the same current EXTRACTING-only defect and is the hermetic behavioral seam.
</interfaces>
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

- New private constants app/db/repo/job_settlement.py::_FINAL_LEASE_ERROR_STATUSES and _FINAL_LEASE_PRESERVE_STATUSES, disjoint and exhaustive over RunStatus.
- New private helper app/db/repo/job_settlement.py::_lock_run_status, or an equivalently named single-purpose helper, which locks the associated payroll_runs row and returns its canonical RunStatus inside the reaper transaction.
- Changed public behavior of reap_expired_final_attempt: every exact eligible row with a valid associated run status is dead-lettered and returns SettlementOutcome.REAPED_FINAL_LEASE; no valid status returns FENCED while leaving the row leased.
- New or expanded hermetic tests named for final-lease status-matrix exhaustiveness and second-candidate progress in tests/test_queue_drain.py.
- Expanded guarded-live test coverage in tests/test_queue_durability.py for COMPUTED, SENT, AWAITING_REPLY, AWAITING_APPROVAL, active crash states, rollback, and the next-oldest candidate.
- No new production file, route, schema object, dependency, environment variable, job kind, run status, or operator surface. No schema push task is required because no planned file matches a supported schema pattern.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the EXTRACTING-only reap with an exhaustive run-locked matrix</name>
  <files>app/db/repo/job_settlement.py, tests/conftest.py, tests/test_queue_drain.py</files>
  <read_first>
    - app/db/repo/job_settlement.py SettlementOutcome, _set_run_error, and reap_expired_final_attempt transaction/locking order.
    - app/models/status.py for the complete canonical RunStatus set and the semantics of human-wait and completed states.
    - app/db/repo/pipeline_state.py rewind_for_reclaim status rationale, especially SENT and human-owned exclusions.
    - tests/conftest.py InMemoryRepo.reap_expired_final_attempt and queue row representation.
    - tests/test_queue_drain.py normal-claim-before-reap, near-miss, truthiness, and one-row-per-call coverage.
    - .planning/phases/18-failure-policy-sweep-deletion/18-VERIFICATION.md CR-01 reason and missing evidence.
  </read_first>
  <behavior>
    - RECEIVED, EXTRACTING, COMPUTED, and APPROVED are active crash states: the run becomes ERROR with FinalAttemptLeaseExpired and bounded attempts detail, and the job becomes dead.
    - SENT, AWAITING_REPLY, AWAITING_APPROVAL, NEEDS_OPERATOR, RECONCILED, REJECTED, and ERROR are authoritative completed/human-wait/terminal states: their status and existing error fields remain unchanged while the job becomes dead.
    - The two status sets are disjoint and their union equals set(RunStatus); adding a new status without an explicit disposition fails an always-run test.
    - Every valid branch clears lease_token and leased_until, preserves last_error, and returns REAPED_FINAL_LEASE.
    - With an oldest preserved-state candidate followed by an active-state candidate, two calls settle both rows in order and the third returns EMPTY/None; the first row cannot starve the second.
  </behavior>
  <action>
    Write the failing hermetic matrix and liveness tests first. In job_settlement.py, keep the existing exact candidate predicate and job row lock, then lock the associated payroll_runs row with SELECT status ... FOR UPDATE before deciding business-state treatment. Define _FINAL_LEASE_ERROR_STATUSES as RECEIVED, EXTRACTING, COMPUTED, and APPROVED; define _FINAL_LEASE_PRESERVE_STATUSES as SENT, AWAITING_REPLY, AWAITING_APPROVAL, NEEDS_OPERATOR, RECONCILED, REJECTED, and ERROR. Assert or test that the sets are disjoint and exhaust RunStatus. For an error-set status, perform a CAS scoped to the locked observed status and write only the bounded FinalAttemptLeaseExpired reason/detail. For a preserve-set status, do not update payroll_runs at all. In both cases, update the already-locked jobs row from leased to dead, clear lease fields, preserve last_error, and return REAPED_FINAL_LEASE. A lost locked-job update is an invariant failure that rolls back; it must not be reported as a successful/fenced no-op. Do not weaken the exact expiry predicate, add a second sweep, reopen reply epochs, or reinterpret prior diagnostics.

    Mirror the exact matrix in InMemoryRepo rather than making the fake permissive. Expand tests/test_queue_drain.py with an all-status parameter matrix and an oldest-preserved-then-active sequence. The tests must assert business status/error-field preservation for SENT and both AWAITING states, ERROR transition for COMPUTED and all active states, transport DEAD for every row, last_error preservation, and eventual EMPTY only after both eligible candidates settle.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_queue_drain.py -k "final_attempt or reap or starvation or status_matrix"</automated>
  </verify>
  <acceptance_criteria>
    - CR-01's COMPUTED strand is inverted: the selected job becomes dead and cannot be selected again.
    - SENT, AWAITING_REPLY, and AWAITING_APPROVAL preserve their authoritative business state while their transport row settles dead.
    - Every current RunStatus has exactly one matrix disposition and the test fails on vocabulary drift.
    - The second eligible candidate progresses on the next drain call.
    - No valid status returns FENCED with the job still leased.
  </acceptance_criteria>
  <done>The shared reaper can no longer strand the oldest exact candidate or starve later work, while authoritative business states remain untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the matrix, rollback, and second-candidate progress against Postgres</name>
  <files>tests/test_queue_durability.py</files>
  <read_first>
    - tests/test_queue_durability.py test_final_attempt_reap_exact_predicate_fence_and_rollback and its _leased_job helper.
    - app/db/repo/job_settlement.py final matrix from Task 1.
    - tests/conftest.py seeded_db two-factor reset guard and queueproof marker conventions.
    - app/routes/pump.py reaped_final_lease accounting, which depends on every successful maintenance settlement returning REAPED_FINAL_LEASE.
  </read_first>
  <behavior>
    - Real Postgres proves COMPUTED becomes ERROR plus dead transport.
    - Real Postgres proves SENT, AWAITING_REPLY, and AWAITING_APPROVAL remain unchanged while their jobs become dead.
    - Real Postgres proves RECEIVED, EXTRACTING, and APPROVED follow the active crash branch.
    - An oldest preserved-state candidate does not prevent a later active candidate from being reaped on the next call.
    - Injected failure after either run handling or job handling rolls the whole transaction back; near-miss attempts/lease rows remain untouched.
  </behavior>
  <action>
    Replace the live test that currently expects a COMPUTED row to remain leased. Build a table-driven queueproof over the named matrix states using direct leased_until backdating rather than sleeps. Assert the exact persisted job and run rows after each reap, including preserved error fields and last_error history. Add two eligible rows with deterministic lease ordering: the oldest in AWAITING_APPROVAL and the second in EXTRACTING. Prove the first call returns REAPED_FINAL_LEASE and preserves AWAITING_APPROVAL, the second call returns REAPED_FINAL_LEASE and moves EXTRACTING to ERROR, and no third eligible row remains. Retain and adapt the existing near-miss and injected-half-failure rollback proofs. Keep both integration and queueproof markers, use no provider/LLM call, and report a missing guarded database as unavailable evidence rather than a pass.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest tests/test_queue_durability.py -m queueproof -k "final_attempt or reap or starvation" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - The prior live assertion that COMPUTED remains leased is gone.
    - Named live cases cover COMPUTED, SENT, AWAITING_REPLY, AWAITING_APPROVAL, and all active crash states without network calls.
    - Second-candidate progress is proven against real row locks and ordering.
    - Atomic rollback and exact-predicate near misses remain covered.
    - Guarded live evidence is recorded honestly when DATABASE_URL/ALLOW_DB_RESET are unavailable.
  </acceptance_criteria>
  <done>CR-01 is closed by both always-run behavioral coverage and the guarded real-Postgres proof that the oldest row cannot strand or starve the queue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| expired leased job to reaper authority | Expiry, attempt cap, and row lock authorize terminal transport settlement without a live worker token. |
| transport settlement to business state | The reaper may mark active crashes ERROR but must not overwrite authoritative human-wait or completed states. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-34 | Denial of Service | oldest final-attempt lease | high | mitigate | Every valid status dead-letters the selected transport row; hermetic and live second-candidate proofs block starvation. |
| T-18-35 | Tampering | payroll_runs status during reap | high | mitigate | Lock associated run, use disjoint exhaustive status sets, CAS active states only, and preserve authoritative states. |
| T-18-36 | Repudiation | final lease diagnostics | medium | mitigate | Preserve jobs.last_error as history and write only bounded FinalAttemptLeaseExpired detail on active-state error transitions. |
| T-18-13-SC | Tampering | package installs | low | accept | No dependency or package installation is planned. |
</threat_model>

<source_audit>
SOURCE | ID | Gap requirement | Plan | Status
GOAL | — | Queue lease recovery is the sole honest recovery mechanism | 18-13 | COVERED
REQ | FAIL-02 | Attempt-capped work reaches visible terminal transport state | 18-13 | COVERED
RESEARCH | — | Exact final-attempt predicate, one transaction, shared drain, bounded final diagnostic | 18-13 | COVERED
CONTEXT | D-07/D-13/D-15/D-17 | Atomic dead-lettering, shared reaper, dedicated reason, no accepted strand | 18-13 | COVERED
VERIFICATION | CR-01 | Status matrix, no oldest-row strand, second-candidate progress, hermetic/live proof | 18-13 | COVERED
</source_audit>

<verification>
- Run uv run pytest -q tests/test_queue_drain.py -k "final_attempt or reap or starvation or status_matrix".
- Run uv run pytest tests/test_queue_durability.py -m queueproof -k "final_attempt or reap or starvation" -v -rs.
- Run uv run ruff check app/db/repo/job_settlement.py tests/conftest.py tests/test_queue_drain.py tests/test_queue_durability.py.
- Run uv run mypy app/db/repo/job_settlement.py.
- Run uv run pytest -q tests/test_pump_route.py to preserve truthful reap accounting.
</verification>

<success_criteria>
- CR-01 is closed: no exact eligible final-attempt lease remains leased for any valid RunStatus.
- Active crash states reach bounded ERROR; SENT and every human-wait/completed state retain authoritative business state.
- A preserved-state oldest row cannot starve the next candidate.
- D-07 atomicity, D-15 diagnostic history, pump accounting, and the exact D-17 predicate remain intact.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-13-SUMMARY.md when done.
</output>
