---
phase: 18-failure-policy-sweep-deletion
plan: 09
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-12]
files_modified:
  - app/queue/handlers/resume_reply.py
  - app/queue/handlers/operator_resume.py
  - app/queue/dispatch.py
  - app/models/job.py
  - app/db/schema.sql
  - tests/conftest.py
  - tests/test_job_kind_drift.py
  - tests/test_resume_pipeline.py
  - tests/test_needs_operator.py
autonomous: true
requirements: [FAIL-01, FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "A resume_reply job reconstructs the exact persisted reply by email_id and invokes resume_pipeline from RECEIVED; it never falls back to the original inbound or run_pipeline (D-01, D-04, D-05)."
    - "An operator_resume job loads the complete mapping by job.operator_resolution_id plus run_id, verifies its key set against decision.unresolved_names and employee ids against the run's roster, then invokes the same resume seam from RECEIVED."
    - "Duplicate delivery of one operator_resolution_id is idempotent, while two valid operator resolutions in the same reply_epoch remain distinct jobs because queue dedup is resolution-UUID scoped."
    - "On attempts greater than one both handlers use rewind_for_reclaim before the RECEIVED-to-EXTRACTING claim; automatic reclaim never bumps reply_epoch."
    - "Malformed/missing durable context returns a bounded terminal INVALID_OPERATOR_OVERRIDE_CONTEXT or equivalent safe result and never leaks names, ids, provider text, or mapping contents."
    - "After Task 1, JobKind/SQL/dispatch are exactly set-equal for RUN_PIPELINE plus RESUME_REPLY; after Task 2 they are exactly set-equal for all three kinds, so no task boundary exposes a kind without its real handler."
    - "Dispatch forwards PipelineResult | None during the compatibility wave; valid handlers may return legacy None until Plan 18-10, while invalid context already returns bounded terminal and drain owns the sole normalization policy."
  artifacts:
    - path: "app/queue/handlers/resume_reply.py"
      provides: "Dedicated persisted-reply retry handler"
    - path: "app/queue/handlers/operator_resume.py"
      provides: "Dedicated durable operator-resume handler over the complete typed mapping"
    - path: "app/queue/dispatch.py"
      provides: "Three-kind module/name dispatch that forwards PipelineResult"
    - path: "tests/conftest.py"
      provides: "Strict email_id/operator-resolution store mirrors and eight-field Job fake"
    - path: "tests/test_needs_operator.py"
      provides: "Complete mapping, malformed context, first claim, and reclaim proofs"
  key_links:
    - from: "app/queue/handlers/resume_reply.py"
      to: "app/db/repo/emails.py"
      via: "job.email_id selects the persisted cleaned reply"
      pattern: "get_inbound_email_by_id"
    - from: "app/queue/handlers/operator_resume.py"
      to: "app/db/repo/operator_resume_resolutions.py"
      via: "job.operator_resolution_id and run_id reconstruct the exact full mapping; alias_candidates is never consulted"
      pattern: "load_operator_resume_resolution"
    - from: "app/queue/handlers/operator_resume.py"
      to: "app/pipeline/orchestrator.py"
      via: "resume_pipeline(from_status=RunStatus.RECEIVED, overrides=complete_mapping)"
      pattern: "from_status.*RECEIVED"
    - from: "app/queue/dispatch.py"
      to: "app/models/job.py"
      via: "set(JobKind) equals set(HANDLERS) and each handler return reaches drain_once"
      pattern: "OPERATOR_RESUME|RESUME_REPLY"
---

<objective>
Integrate lossless retry handlers and strict fakes on top of the typed transport foundation.

Purpose: A durable identifier is useful only if its handler reconstructs the exact persisted context and re-enters the shared resume state machine without weakening validation or reclaim semantics.
Output: resume_reply/operator_resume handlers, dispatch registration, complete strict fakes, and hermetic handler proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-01-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-12-SUMMARY.md

<interfaces>
- Plan 18-02 provides Job.email_id/operator_resolution_id, get_inbound_email_by_id, and the typed immutable operator-resolution store; Plan 18-12 proves the new tables/columns are visible to live schema drift checks.
- resume_pipeline(run_id, inbound, from_status, overrides) is the shared state-machine seam and still returns legacy None in this wave; handlers forward PipelineResult | None so Plan 18-04 can normalize it centrally before Plan 18-10 cutover.
- rewind_for_reclaim is the established attempts>1 rewind and does not advance reply_epoch.
- decision.unresolved_names is the authoritative complete submitted-name set for operator resolution; alias_candidates contains only optional learning choices.
</interfaces>
</context>

<artifacts_this_phase_produces>
- handle_resume_reply(job) -> PipelineResult | None during compatibility.
- handle_operator_resume(job) -> PipelineResult | None during compatibility.
- Both handlers use attempts>1 rewind_for_reclaim, then the RECEIVED-to-EXTRACTING resume seam.
- Operator context validation loads by operator_resolution_id, requires exact unresolved-name key equality and employee membership in the run business roster before calling resume_pipeline.
- dispatch.HANDLERS entries for all three kinds and return forwarding.
- Strict InMemoryRepo email lookup and per-resolution row store, eight-field Job claim construction, patch registrations, and positive pairing guards.
- No enqueue/settlement logic, new route, generic payload, alias-candidate reconstruction, provider call, or producer migration.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Atomically add RESUME_REPLY with its real handler and drift proof</name>
  <files>app/models/job.py, app/db/schema.sql, app/queue/handlers/resume_reply.py, app/queue/dispatch.py, tests/test_job_kind_drift.py, tests/test_resume_pipeline.py</files>
  <read_first>
    - app/queue/handlers/pipeline.py missing-id, reclaim, and CAS-loser conventions.
    - app/routes/pipeline_glue.py row_to_inbound persisted-body conversion.
    - app/db/repo/pipeline_state.py rewind_for_reclaim scope.
    - app/queue/dispatch.py module/name monkeypatch-preserving dispatch.
    - tests/test_resume_pipeline.py current persisted-reply and claim-loser tests.
  </read_first>
  <behavior>
    - Missing run_id/email_id fails loudly before orchestration.
    - Missing or non-inbound persisted row returns a bounded terminal result.
    - A valid row uses persisted body_text and resume_pipeline from RECEIVED, forwarding legacy None or a future explicit PipelineResult unchanged.
    - attempts=2 rewinds an eligible EXTRACTING/COMPUTED/SENT run before the forward claim and processes the same email id.
    - The handler never calls run_pipeline or uses the default AWAITING_REPLY claim.
  </behavior>
  <action>
    Add only RESUME_REPLY to JobKind in this task. In the same task, widen the fresh/deployed jobs_kind_check atomically from {run_pipeline} to {run_pipeline, resume_reply}, add the RESUME_REPLY run_id/email_id context CHECK, create the real handle_resume_reply(job) -> PipelineResult | None, and register its module/name dispatch entry. Require run_id/email_id, use attempts>1 rewind, load exact persisted email_id, and call resume_pipeline from RECEIVED. Forward its legacy None or future explicit PipelineResult unchanged; malformed durable context returns a bounded terminal result. Update drift tests so Python kind values, the deployed SQL CHECK parser, and dispatch are exactly set-equal at the end of Task 1. Do not add OPERATOR_RESUME anywhere in this task; Task 2 owns that second atomic bijection expansion.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_job_kind_drift.py tests/test_resume_pipeline.py -k "job_kind or resume_reply or persisted or received or reclaim"</automated>
  </verify>
  <acceptance_criteria>
    - The dedicated handler consumes exact persisted reply context from RECEIVED.
    - attempts=2 reuses the same context after rewind and never bumps reply_epoch.
    - Missing context is terminal-safe and PII-free.
    - Dispatch forwards PipelineResult and contains no bound-function monkeypatch trap.
  </acceptance_criteria>
  <done>Clarification retries can re-enter losslessly from a claimed identifier-only job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Atomically add OPERATOR_RESUME with its real handler and drift proof</name>
  <files>app/models/job.py, app/db/schema.sql, app/queue/handlers/operator_resume.py, app/queue/dispatch.py, tests/test_job_kind_drift.py, tests/test_needs_operator.py</files>
  <read_first>
    - app/routes/runs.py resolve route's full server validation and remember-checkbox split.
    - app/pipeline/orchestrator.py resume_pipeline override threading.
    - app/db/repo/operator_resume_resolutions.py Plan 18-02 loader.
    - tests/test_needs_operator.py real resolve/resume and mixed remember-choice cases.
  </read_first>
  <behavior>
    - Handler requires job.operator_resolution_id, loads its immutable parent/rows by resolution id and run id, and never reads alias_candidates as mapping authority.
    - Loaded keys equal decision.unresolved_names exactly; every employee id belongs to the run business roster.
    - Valid first and reclaimed attempts call resume_pipeline from RECEIVED with the complete dict, forwarding legacy None or a future explicit PipelineResult unchanged.
    - Missing, partial, extra, cross-business, wrong-run, or corrupt rows return INVALID_OPERATOR_OVERRIDE_CONTEXT terminal without logging values.
    - Re-dispatch of the same resolution id reloads the same mapping; two resolution ids created in one reply_epoch remain distinct durable jobs.
  </behavior>
  <action>
    Add OPERATOR_RESUME to JobKind only in this task; atomically widen the fresh/deployed jobs_kind_check from the Task 1 two-kind set to all three values and add the OPERATOR_RESUME constraint requiring run_id plus operator_resolution_id while forbidding email_id. Create handle_operator_resume(job) -> PipelineResult | None. Require both identifiers, load the run and immutable typed resolution by operator_resolution_id/run_id, compare mapping keys exactly to decision.unresolved_names, and validate every employee UUID against load_roster_for_business(run.business_id). These checks defend the durable boundary; they do not replace the route's all-or-nothing validation. Never consult alias_candidates because unchecked remember boxes make it intentionally partial. On attempts > 1 call rewind_for_reclaim, then call resume_pipeline(run_id, None, from_status=RunStatus.RECEIVED, overrides=complete_mapping), forwarding legacy None or an explicit PipelineResult unchanged. For any invalid durable context return a bounded terminal INVALID_OPERATOR_OVERRIDE_CONTEXT result and log only run/job/resolution correlation ids plus the bounded code. Update drift tests so Python/SQL/dispatch are exactly set-equal at the Task 2 boundary. Add same-resolution redelivery, two-same-epoch-distinct-resolution, mixed remember-choice, complete/partial/extra/cross-business/wrong-run, first-attempt, reclaim, and PII-absence tests.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_job_kind_drift.py tests/test_needs_operator.py -k "operator_resume or override or reclaim or job_kind"</automated>
  </verify>
  <acceptance_criteria>
    - Complete mapping survives even when only some selections were marked remember.
    - Valid rows invoke resume_pipeline exactly once with from_status RECEIVED.
    - Invalid rows cannot reach reconciliation and expose no token/employee id in result or logs.
    - All JobKind values have exactly one real handler.
  </acceptance_criteria>
  <done>Operator retries reconstruct the complete validated money-moving mapping instead of failing terminal or guessing from partial learning state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mirror every new context seam in strict fakes</name>
  <files>tests/conftest.py, tests/test_resume_pipeline.py, tests/test_needs_operator.py</files>
  <read_first>
    - tests/conftest.py InMemoryRepo queue/email stores and fake_repo patch tuple.
    - tests/test_fake_repo_pairing.py existing reflection guard, which this task runs unchanged as the acceptance oracle.
    - app/db/repo/emails.py and app/db/repo/operator_resume_resolutions.py public functions.
  </read_first>
  <behavior>
    - InMemoryRepo returns eight-field Jobs including email_id and operator_resolution_id.
    - get_inbound_email_by_id and both resolution-store methods are strict, stateful, and registered.
    - Resolution parent/rows are stored under resolution id plus run id and reject malformed/duplicate/mismatched input.
    - Missing patch registration fails before any real database fallthrough.
  </behavior>
  <action>
    Extend InMemoryRepo enqueue/claim to the three kind-specific identifier contracts and eight-field Job. Mirror get_inbound_email_by_id, create_operator_resume_resolution, and load_operator_resume_resolution with explicit state keyed by (operator_resolution_id, run_id), strict UUID/name validation, deterministic copy semantics, same-id idempotency/conflict rejection, and call recording. Register all exact names in fake_repo. Run the existing generic test_fake_repo_pairing.py unchanged to prove every new public fake is registered; the generic reflection contract already covers these methods, so modifying that file would add scope without strengthening the proof. Keep unknown methods fail-closed and preserve every existing sender/redelivery/retrigger fake.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_fake_repo_pairing.py tests/test_resume_pipeline.py tests/test_needs_operator.py -k "pairing or resume_reply or operator_resume or override"</automated>
  </verify>
  <acceptance_criteria>
    - Production/fake public seams are paired in both directions.
    - Hermetic handler tests cannot reach a real repository function.
    - Fake mapping storage preserves complete per-resolution mappings and rejects corruption.
  </acceptance_criteria>
  <done>The test harness enforces the same lossless identifier/context contract as production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted identifiers to resume handlers | Job ids select reply or operator context that changes payroll reconciliation. |
| operator mapping rows to deterministic reconcile | A complete employee assignment becomes money-moving authority. |
| fake repository to test confidence | Missing patch registration could silently invoke production DB code. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-08 | Tampering | operator resume handler | high | mitigate | Exact key-set and roster validation, job/run scoping, bounded invalid-context result, no alias-candidate fallback. |
| T-18-09 | Tampering | resume reclaim | high | mitigate | attempts>1 rewind then authoritative RECEIVED CAS; no epoch bump. |
| T-18-10 | Information Disclosure | handler diagnostics | high | mitigate | Bounded codes and correlation ids only; hostile token/id absence tests. |
| T-18-11 | Tampering | fake fallthrough | high | mitigate | Reflection/AST pairing and strict stateful mirrors for every new public seam. |
| T-18-09-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run uv run pytest -q tests/test_job_kind_drift.py tests/test_fake_repo_pairing.py tests/test_resume_pipeline.py tests/test_needs_operator.py.
- Run uv run ruff check app/queue/handlers/resume_reply.py app/queue/handlers/operator_resume.py app/queue/dispatch.py tests/conftest.py.
- Run uv run mypy app/queue/handlers/resume_reply.py app/queue/handlers/operator_resume.py app/queue/dispatch.py.
</verification>

<success_criteria>
- Durable resume handlers preserve D-01, D-04, and D-05 for both entry points.
- Operator retry never becomes terminal merely because its request-local map needed persistence.
- Plan owns exactly nine files; tests/test_fake_repo_pairing.py is an unchanged verification oracle, not an owned file.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md when done.
</output>
