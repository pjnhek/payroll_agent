---
phase: 18-failure-policy-sweep-deletion
plan: 08
type: execute
wave: 10
depends_on: [18-02, 18-03, 18-07, 18-09, 18-11]
files_modified:
  - app/db/repo/runs.py
  - app/db/repo/emails.py
  - app/db/repo/pipeline_state.py
  - app/db/repo/__init__.py
  - app/models/status.py
  - tests/conftest.py
  - tests/test_fake_repo_pairing.py
autonomous: true
requirements: [FAIL-03]
nyquist_compliant: true

must_haves:
  truths:
    - "After all callers are gone, sweep_stranded_runs and find_stranded_unconsumed_replies are deleted from production repositories, facade, documentation, fakes, and patch tuples (D-18)."
    - "Durable replacements remain public and paired: persisted email lookup, typed operator override store, six coordinator seams, and queue reaper."
    - "Webhook redelivery, sender/consumed checks, late replies, durable operator resume, and Retrigger repository support remain intact."
    - "A source-aware production/fake inventory proves retired names cannot return and is paired with positive replacement assertions."
  artifacts:
    - path: "app/db/repo/runs.py"
      provides: "Run repository without age-based automatic recovery"
    - path: "app/db/repo/emails.py"
      provides: "Email repository without stranded-reply scan"
    - path: "app/db/repo/__init__.py"
      provides: "Facade exposing durable settlement/context seams only"
    - path: "tests/conftest.py"
      provides: "Strict fake surface with legacy mirrors removed and replacements preserved"
    - path: "tests/test_fake_repo_pairing.py"
      provides: "Final non-vacuous source/fake negative gate plus positive replacement pairing"
  key_links:
    - from: "app/db/repo/__init__.py"
      to: "app/db/repo/job_settlement.py"
      via: "classified settlement and final-lease reaping remain public"
      pattern: "reap_expired_final_attempt"
    - from: "tests/test_fake_repo_pairing.py"
      to: "app/db/repo|tests/conftest.py"
      via: "source-aware retired-symbol absence and replacement presence"
      pattern: "retired_recovery_symbols"
---

<objective>
Delete legacy recovery APIs and fakes after their callers have already been removed.

Purpose: This final ordering keeps every wave runnable while leaving exactly one automatic recovery policy: the durable queue.
Output: complete repository/facade/fake subtraction and final source-aware negative gates.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-07-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md

<interfaces>
- Plan 18-07 removed every live/test caller while the APIs still existed.
- app.db.repo now exposes typed resume context plus atomic settlement/reaping.
- tests/conftest.py must remain strict: creator-plan fake methods and patch entries survive this subtraction.
</interfaces>
</context>

<artifacts_this_phase_produces>
- Complete deletion of the two legacy repository functions, facade exports, prose references, fake methods/state, and patch names.
- Final production-source negative grep/AST gate and strict fake/public inventory.
- Positive pairing for get_inbound_email_by_id, create/load_operator_resume_resolution, enqueue_classified_retry, enqueue_operator_resume_retry, settle_pipeline_job, settle_background_terminal, settle_infrastructure_failure, and reap_expired_final_attempt.
- No alias, deprecated stub, replacement sweep, compatibility import, timer, migration tool, or dependency.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete retired repository/facade symbols after caller removal</name>
  <files>app/db/repo/runs.py, app/db/repo/emails.py, app/db/repo/pipeline_state.py, app/db/repo/__init__.py, app/models/status.py, tests/test_fake_repo_pairing.py</files>
  <read_first>
    - app/db/repo/runs.py sweep constant/function and neighboring retrigger APIs.
    - app/db/repo/emails.py stranded lookup and neighboring sender/consumption APIs.
    - app/db/repo/pipeline_state.py rewind_for_reclaim and clear_reply_context epoch distinction from 18-PATTERNS.
    - app/db/repo/__init__.py explicit imports/exports.
    - app/models/status.py canonical RunStatus and NEEDS_OPERATOR business-state comments from 18-PATTERNS.
    - app/db/repo/job_settlement.py and operator_resume_resolutions.py durable replacements.
    - Plan 18-07 summary proving callers/tests are already removed.
  </read_first>
  <behavior>
    - Retired definitions/exports/prose are absent from production source.
    - Replacement context/settlement/reaper symbols remain public.
    - Adjacent sender, consumed-reply, redelivery, retrigger, and rewind APIs remain.
  </behavior>
  <action>
    Delete only _STRANDED_SCOPE_STATUSES/sweep_stranded_runs, _STRANDED_REPLY_SCOPE_STATUS/find_stranded_unconsumed_replies, their facade imports/exports, and stale prose references in pipeline_state/status. In pipeline_state preserve the semantic distinction that automatic rewind_for_reclaim never bumps reply_epoch while human clear_reply_context does; in status preserve the canonical ERROR/RECEIVED/NEEDS_OPERATOR vocabulary and remove only references to the deleted sweep. Leave no alias, wrapper, commented implementation, or compatibility import. Preserve every adjacent supported API and all typed Phase 18 replacements. Extend test_fake_repo_pairing with a source-aware production inventory whose retired literals are test data only, plus explicit positive assertions for the persisted-email, operator-store, settlement, and reaper functions. The gate must scan real definitions/exports and assert scanned files are nonempty.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_fake_repo_pairing.py</automated>
  </verify>
  <acceptance_criteria>
    - Production source contains no retired symbol or fallback.
    - Negative gate fails on a reintroduced definition/export and cannot pass vacuously.
    - All durable replacements pass positive facade assertions.
  </acceptance_criteria>
  <done>The production repository exposes one durable recovery policy and no legacy sweep API.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete retired fake mirrors and run the final source-aware gate</name>
  <files>tests/conftest.py, tests/test_fake_repo_pairing.py</files>
  <read_first>
    - tests/conftest.py retired methods/state/patch entries and Phase 18 replacement fakes.
    - tests/test_fake_repo_pairing.py public/fake reflection guard.
    - Plan 18-02/18-03/18-09 summaries for the exact replacement fake inventory.
  </read_first>
  <behavior>
    - InMemoryRepo and fake_repo tuple contain no legacy sweep/stranded lookup.
    - Every persisted context/coordinator replacement remains strict, stateful, and registered.
    - Unknown retired calls fail rather than falling through.
    - Final app-source negative shell gate is literal runnable text.
  </behavior>
  <action>
    Remove only the two retired fake methods, their sweep-only state/comments, and patch entries. Preserve email_by_id/jobs/operator override rows and all strict replacement coordinator methods. Update pairing tests for the smaller surface and positive replacement list. Run the full caller-preservation suite from Plan 18-07, then the final source-aware shell gate over app Python/HTML/SQL only; tests may contain retired literals as gate fixtures. Do not delete or weaken sender/redelivery/retrigger/operator fakes.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_fake_repo_pairing.py tests/test_stuck_run_recovery.py tests/test_reply_redelivery.py tests/test_needs_operator.py && ! grep -R -n --exclude-dir='__pycache__' --include='*.py' --include='*.html' --include='*.sql' 'sweep_stranded_runs\|find_stranded_unconsumed_replies' app</automated>
  </verify>
  <acceptance_criteria>
    - Final app-source gate returns zero matches and uses literal shell &&.
    - Retired fake state/methods/patch names are absent.
    - Replacement fakes remain paired and supported recovery tests stay green.
  </acceptance_criteria>
  <done>Production and test harness contain no alternate automatic recovery mechanism.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository facade to callers | Public methods define durable state mutation authority. |
| strict fakes to production confidence | Obsolete/permissive fake methods can mask missing production contracts. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-21 | Tampering | duplicate recovery APIs | high | mitigate | Caller-first order, complete definition/export deletion, and source-aware inventory. |
| T-18-22 | Tampering | permissive fakes | high | mitigate | Remove legacy mirrors, preserve strict fallback, assert positive/negative pairing. |
| T-18-23 | Spoofing | reply/operator safeguards | high | mitigate | Narrow subtraction plus preserved sender/consumed/operator/retrigger tests. |
| T-18-08-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run the Task 2 focused suite and final literal negative shell gate.
- Run uv run ruff check app/db/repo/runs.py app/db/repo/emails.py app/db/repo/pipeline_state.py app/db/repo/__init__.py app/models/status.py tests/conftest.py tests/test_fake_repo_pairing.py.
- Run uv run mypy on changed production Python files.
</verification>

<success_criteria>
- D-18 and FAIL-03 are complete in caller, production, facade, fake, and test surfaces.
- Deletion ordering is forward, green, and has no compatibility gap.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-08-SUMMARY.md when done.
</output>
