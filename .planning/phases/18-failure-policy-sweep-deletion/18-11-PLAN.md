---
phase: 18-failure-policy-sweep-deletion
plan: 11
type: execute
wave: 8
depends_on: [18-10]
files_modified:
  - app/pipeline/result.py
  - app/routes/pipeline_glue.py
  - app/queue/handlers/pipeline.py
  - app/queue/handlers/resume_reply.py
  - app/queue/handlers/operator_resume.py
  - app/queue/dispatch.py
  - tests/test_queue_drain.py
  - tests/test_resume_pipeline.py
  - tests/test_needs_operator.py
autonomous: true
requirements: [FAIL-01, FAIL-02]
nyquist_compliant: true

must_haves:
  truths:
    - "Every active producer, wrapper, handler, dispatcher, and normalizer seam is PipelineResult-only after producer cutover; no compatibility-era PipelineResult | None contract remains (D-01)."
    - "All active callers propagate or exhaustively consume OK, RETRYABLE, and TERMINAL; no result is discarded and no None value is accepted as success."
    - "Initial, persisted-reply, and operator-resolution paths retain their durable retry, terminal settlement, exact identifier reconstruction, and bounded-diagnostic behavior (D-02, D-04, D-07, D-16)."
  artifacts:
    - path: "app/pipeline/result.py"
      provides: "Strict PipelineResult-only normalizer"
    - path: "app/routes/pipeline_glue.py"
      provides: "Three PipelineResult-only background/now seams"
    - path: "app/queue/dispatch.py"
      provides: "PipelineResult-only handler forwarding"
    - path: "tests/test_queue_drain.py"
      provides: "Non-vacuous caller inventory and strict-mypy closure proof"
  key_links:
    - from: "app/routes/pipeline_glue.py|app/queue/handlers/*.py|app/queue/dispatch.py"
      to: "app/pipeline/result.py|app/queue/drain.py"
      via: "PipelineResult-only returns reach exhaustive wrapper or atomic drain settlement"
      pattern: "PipelineResult"
---

<objective>
Close every compatibility-era result seam immediately after the producers become explicit.

Purpose: Producer cutover is incomplete while active consumers still advertise or normalize legacy None; this plan makes the shared contract exact across the whole call graph.
Output: strict normalizer, narrowed wrappers/handlers/dispatch, non-vacuous caller guards, and strict mypy proof.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-failure-policy-sweep-deletion/18-CONTEXT.md
@.planning/phases/18-failure-policy-sweep-deletion/18-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-10-SUMMARY.md

<interfaces>
- Plan 18-10 guarantees run_pipeline, _run, and resume_pipeline return PipelineResult on every path.
- Plans 18-03/18-04 already implement exhaustive durable wrapper/drain settlement behavior while temporarily accepting legacy None through one adapter.
- All three queue handlers and dispatch currently forward PipelineResult | None only for that pre-cutover compatibility window.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow every compatibility-era producer and forwarding seam</name>
  <files>app/pipeline/result.py, app/routes/pipeline_glue.py, app/queue/handlers/pipeline.py, app/queue/handlers/resume_reply.py, app/queue/handlers/operator_resume.py, app/queue/dispatch.py, tests/test_resume_pipeline.py, tests/test_needs_operator.py</files>
  <read_first>
    - app/pipeline/result.py normalize_pipeline_result temporary branch.
    - app/routes/pipeline_glue.py all now/background return annotations and exhaustive outcome branches.
    - app/queue/handlers/pipeline.py, resume_reply.py, and operator_resume.py forward/no-op returns.
    - app/queue/dispatch.py dynamic module/name handler forwarding.
  </read_first>
  <behavior>
    - normalize_pipeline_result accepts PipelineResult only and rejects None/other values.
    - All now/background/handler/dispatch annotations return PipelineResult where they forward a value; background functions that settle and return remain explicitly None-returning only as terminal procedures, not as pipeline-result producers.
    - CAS-loser and invalid-context handler paths return explicit OK/TERMINAL.
    - Reply email_id and operator_resolution_id reconstruction behavior is unchanged.
  </behavior>
  <action>
    Remove the compatibility None branch from normalize_pipeline_result and narrow its parameter to PipelineResult. Narrow run_pipeline_now/resume_pipeline_now and all three queue handlers plus dispatch.handle to PipelineResult-only; update background wrappers to consume a PipelineResult exhaustively without union annotations or local None branches. Keep their procedure-level -> None annotation only where the wrapper itself intentionally settles/schedules and returns no policy value. Replace every compatibility prose branch with the final explicit contract. Preserve identifier-only operator_resolution_id loading, reply email reconstruction, CAS-loser explicit OK, bounded invalid-context TERMINAL, and existing settlement ownership. Add focused tests that inject None at the strict adapter/forwarding seams and require loud failure while all three real outcomes retain their prior behavior.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_resume_pipeline.py tests/test_needs_operator.py -k "result or resume or operator or context"</automated>
  </verify>
  <acceptance_criteria>
    - No active forwarding signature contains PipelineResult | None.
    - None is not converted to OK anywhere.
    - All handler no-op/error paths return explicit bounded results.
    - Reply/operator durable identifiers and PII-safe diagnostics remain intact.
  </acceptance_criteria>
  <done>The entire active pipeline-result call graph has one exact return type.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove no active consumer accepts or ignores None</name>
  <files>tests/test_queue_drain.py</files>
  <read_first>
    - tests/test_queue_drain.py existing AST status-writer and anti-vacuity guards.
    - app/routes/pipeline_glue.py, app/queue/handlers/*.py, and app/queue/dispatch.py narrowed seams from Task 1.
    - app/queue/drain.py exhaustive normalization and settlement mapping.
  </read_first>
  <behavior>
    - A non-vacuous AST inventory covers run_pipeline, resume_pipeline, both now seams, three handlers, dispatch, and drain.
    - Every call is returned or assigned then exhaustively consumed; expression-statement result sinks fail.
    - No union-with-None annotation, None-success branch, or optional normalizer input remains on active result seams.
    - Repo-wide strict mypy passes after closure.
  </behavior>
  <action>
    Extend the source-aware caller inventory to scan every active result producer/forwarder/consumer and assert the scanned set is nonempty. Reject discarded call expressions, direct truthiness shortcuts, union-with-None return/input annotations, and None-to-success branches. Pair the static inventory with behavioral OK/RETRYABLE/TERMINAL matrices and run repo-wide mypy so every compatibility-era type seam is closed, not merely hidden from one test. Do not alter drain settlement behavior or broaden Phase 19 producer scope.
  </action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/payroll-agent-uv-cache uv run --offline pytest -q tests/test_queue_drain.py tests/test_resume_pipeline.py tests/test_needs_operator.py -k "result or caller or background or resume or operator" && uv run mypy</automated>
  </verify>
  <acceptance_criteria>
    - Caller inventory is positive and covers every active seam.
    - No active consumer accepts, ignores, or treats None as success.
    - Repo-wide mypy and the focused regression suite pass.
  </acceptance_criteria>
  <done>Compatibility closure is mechanically proven across runtime behavior, AST inventory, and strict typing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| explicit producer to consumer graph | A discarded or optionalized result recreates swallowed failure. |

## STRIDE Threat Register — OWASP ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-18-30 | Denial of Service | compatibility result sink | high | mitigate | PipelineResult-only annotations, strict normalizer, exhaustive behavior tests, and non-vacuous AST inventory. |
| T-18-31 | Tampering | handler/dispatch forwarding | high | mitigate | Explicit bounded no-op/error returns and repo-wide mypy closure. |
| T-18-11-SC | Tampering | package installs | low | accept | No dependency changes. |
</threat_model>

<verification>
- Run the focused caller/behavior suite and repo-wide uv run mypy.
- Run uv run ruff check on all nine owned files.
</verification>

<success_criteria>
- FAIL-01's one shared result contract is exact across producers and every active consumer.
- No active compatibility-era None acceptance or ignored-result seam remains.
- No high-severity threat remains unmitigated.
</success_criteria>

<output>
Create .planning/phases/18-failure-policy-sweep-deletion/18-11-SUMMARY.md when done.
</output>
