---
phase: 09-atomic-data-integrity
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/main.py
  - tests/test_webhook_dedup_race.py
autonomous: true
requirements: [DATA-02, DATA-03]

must_haves:
  truths:
    - "Two concurrent duplicate webhook deliveries for the same inbound message_id result in exactly one payroll run — the loser's response reports the winner's run_id instead of creating a second run"
    - "The webhook's dedup-insert + sender-routing + create_run sequence commits as one transaction BEFORE background_tasks.add_task is ever called, in both TestClient (synchronous BackgroundTasks) and real ASGI serving"
    - "GET /runs sweeps stranded in-flight runs (received/extracting/computed older than the shared threshold) to ERROR before rendering the runs list, using ONE shared threshold constant with retrigger's existing stale-in-flight claim logic"
    - "A run whose background task died mid-flight becomes visible as an ERROR with a distinguishing error_reason on the very next dashboard load, and is then retriggerable via the existing ERROR-claim path with no new UI"
  artifacts:
    - path: "app/main.py"
      provides: "inbound() webhook route wraps insert_inbound_email + find_business_by_sender + create_run in one repo.get_connection()/conn.transaction() block, committed before background_tasks.add_task; runs_list() calls repo.sweep_stranded_runs(STALE_THRESHOLD_SECONDS) before repo.load_all_runs()"
      contains: "with conn.transaction():"
    - path: "tests/test_webhook_dedup_race.py"
      provides: "SC2 concurrency race proof — two real OS threads racing two real Postgres transactions, asserting exactly one run exists"
      contains: "def test_duplicate_webhook_delivery_creates_exactly_one_run"
  key_links:
    - from: "app/main.py::inbound"
      to: "app/db/repo.py (insert_inbound_email/find_business_by_sender/create_run/find_run_by_message_id)"
      via: "one with repo.get_connection() as conn: with conn.transaction(): block; background_tasks.add_task called strictly AFTER the with block exits; the loser branch calls find_run_by_message_id(email.message_id, conn=conn) — NOT email_id, which insert_inbound_email returns as None on conflict"
      pattern: "background_tasks.add_task"
    - from: "app/main.py::runs_list"
      to: "app/db/repo.py::sweep_stranded_runs"
      via: "one-line call before repo.load_all_runs(), sharing STALE_THRESHOLD_SECONDS with retrigger's stale-claim logic"
      pattern: "sweep_stranded_runs"
---

<objective>
Wire DATA-02 (transactional webhook dedup, D-9-09) and DATA-03 (stuck-run recovery,
D-9-10/11/12/13) into `app/main.py`. DATA-02: wrap the webhook's dedup-insert +
sender-routing + `create_run` sequence in one transaction, committed BEFORE
`background_tasks.add_task` is ever scheduled — closing the orphan window where a
crash mid-ingest could leave an email row with no run, and making the two-concurrent-
duplicates race resolve to exactly one run via Postgres's own MVCC blocking behavior
on the `uq_message_id` unique index (no new locking primitive). DATA-03: derive the
corrected threshold (informed by RESEARCH.md's Pitfall 1 finding that the naive
90s-3min estimate under-counts the real ~30-minute LLM worst-case) and wire
`repo.sweep_stranded_runs` (built in 09-01) into the `GET /runs` dashboard route —
the one HTTP entry point Render's free tier guarantees will be hit periodically.

Purpose: close audit finding HIGH-04 (webhook dedup race) and MED-05 (stuck-run
recovery, 5-minute threshold too long with no visible signal) — the two remaining
DATA requirements not covered by 09-02's orchestrator work.

Output: `app/main.py`'s `inbound()` route and `runs_list()` route wired per D-9-09/
D-9-11; a shared `STALE_THRESHOLD_SECONDS` constant sized against the corrected LLM
worst-case; `tests/test_webhook_dedup_race.py` proving SC2 via real concurrent
threads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atomic-data-integrity/09-CONTEXT.md
@.planning/phases/09-atomic-data-integrity/09-RESEARCH.md
@.planning/phases/09-atomic-data-integrity/09-PATTERNS.md
@.planning/phases/09-atomic-data-integrity/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wrap the webhook's dedup+routing+create_run sequence in one transaction</name>
  <files>app/main.py</files>
  <read_first>
    app/main.py (already read this session — the full `inbound()` route body lines
    231-359, particularly the dedup-insert at lines 311-320, the duplicate-response
    branch at lines 322-328, the reply-routing call at lines 336-339 which must stay
    OUTSIDE this transaction (it does its own separate reads/writes via
    `_route_reply`, unchanged by this plan), the sender-check + `create_run` +
    `background_tasks.add_task` sequence at lines 341-354; `_route_reply` full body
    lines 362-441 — UNCHANGED by this task). app/db/repo.py's `insert_inbound_email`
    (already read — lines 140-184, `ON CONFLICT (message_id) DO NOTHING RETURNING id`),
    `find_business_by_sender` (lines 187-210), `create_run` (lines 228-264),
    `find_run_by_message_id` (new in 09-01 — read 09-01-SUMMARY.md for its exact
    signature: keyed on `message_id: str`, joining `email_messages` to
    `payroll_runs.source_email_id` — NOT `email_id`, since `insert_inbound_email`
    returns `(None, False)` on `ON CONFLICT DO NOTHING` and the loser never has an
    `email_id` to pass). tests/test_webhook.py (already read this session, full
    file — the existing `test_duplicate_delivery_idempotent` and
    `test_unknown_sender_no_run` tests this task must not break).
  </read_first>
  <behavior>
    - Test 1 (offline, existing `fake_repo`+TestClient): `test_duplicate_delivery_idempotent`
      (already exists in tests/test_webhook.py) continues to pass unmodified —
      exactly one run created across two identical POSTs.
    - Test 2 (offline): a NEW test asserting the duplicate response body now includes
      the existing run's id (`r2.json()["run_id"]` populated, not just
      `{"status": "duplicate"}`) when the first-ever inbound for a business already
      created a run — per D-9-09 "the loser attaches to the existing run: report,
      never create."
    - Test 3 (offline): `test_unknown_sender_no_run` continues to pass unmodified —
      the transaction still commits the email row with no run when
      `find_business_by_sender` returns None.
  </behavior>
  <action>
    In `app/main.py`'s `inbound()` route, replace the three independently-committing
    calls (`repo.insert_inbound_email` at line 311, `repo.find_business_by_sender` at
    line 342, `repo.create_run` at line 350) with ONE
    `with repo.get_connection() as conn: with conn.transaction(): ...` block per
    D-9-09, structured as: inside the block, call
    `email_id, inserted = repo.insert_inbound_email(..., conn=conn)`; if `not
    inserted`, call `existing_run_id = repo.find_run_by_message_id(email.message_id,
    conn=conn)` (the loser path — `email_id` here is `None` per
    `insert_inbound_email`'s existing `(None, False)` return on conflict, so the
    lookup key is `email.message_id`, the RFC header string already parsed by
    `gateway.parse_inbound` earlier in this same route, NOT the unavailable
    `email_id`; `find_run_by_message_id` — built in 09-01 for exactly this call
    site — joins `email_messages` to `payroll_runs.source_email_id` internally, so
    this route never needs its own JOIN or a second ad-hoc query); otherwise call
    `business_id = repo.find_business_by_sender(email.from_addr, conn=conn)` and,
    if not None, `run_id = repo.create_run(business_id=business_id,
    source_email_id=email_id, conn=conn)`. The `with` block ends there — the
    transaction commits on clean exit. AFTER the `with` block (never inside it, per
    the RESEARCH.md anti-pattern list), branch on the captured `inserted`/
    `business_id`/`run_id` locals: keep the existing reply-routing call
    (`_route_reply`, lines 336-339) exactly where it is in the CONTROL FLOW relative
    to the duplicate-check (it currently runs only on the non-duplicate path, before
    the sender check) — `_route_reply` is UNCHANGED and does its own separate
    reads/writes; only the ingest-dedup-create sequence moves inside the new
    transaction. `background_tasks.add_task(_run_pipeline, run_id)` is called ONLY
    when `inserted and business_id is not None`, strictly outside the `with` block.
    The duplicate-response JSONResponse (currently `{"status": "duplicate",
    "message_id": ...}`) gains an optional `"run_id"` key populated from
    `existing_run_id` when found (D-9-09 — upgrade the loser's response, never
    silently the same shape; `existing_run_id` is `None` when the message_id has no
    associated run yet, e.g. a duplicate delivery racing an unknown-sender or
    reply-routed first attempt — return the bare `None` value for `run_id` in that
    case, do not fabricate one). Verify explicitly (per the read_first note above)
    whether `TestClient`'s synchronous BackgroundTasks execution still sees the
    already-committed row when `_run_pipeline`/`_resume_pipeline` runs — since the
    `with` block already closed and committed by the time `background_tasks.add_task`
    is even called, this ordering holds identically under both TestClient and real
    ASGI serving (RESEARCH.md's explicit verification point).
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration" tests/test_webhook.py</automated>
  </verify>
  <done>The webhook's dedup+routing+create_run sequence commits as one transaction before background_tasks.add_task is scheduled; the duplicate-response body reports an existing run_id when found; existing webhook tests plus the new duplicate-run_id test are green.</done>
  <acceptance_criteria>
    - `app/main.py`'s `inbound()` route contains exactly one `with repo.get_connection() as conn: with conn.transaction():` block wrapping `insert_inbound_email`, the loser lookup, `find_business_by_sender`, and `create_run`
    - The loser branch calls `repo.find_run_by_message_id(email.message_id, conn=conn)` — grep confirms `find_run_by_message_id(email.message_id` appears in `app/main.py`; `email_id` is NEVER passed as the lookup key (it is `None` on this branch)
    - `background_tasks.add_task(_run_pipeline, run_id)` appears in source AFTER (not inside) that `with` block
    - The duplicate-response JSONResponse body includes a `run_id` key (may be `None`) when an existing run is found for the message_id
    - `uv run pytest -q -m "not integration" tests/test_webhook.py` exits 0 (both existing tests + the new duplicate-run_id test)
    - `uv run pytest -q -m "not integration"` (full offline suite) exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the recovery sweep into runs_list; derive the shared threshold; SC2 concurrency race test</name>
  <files>app/main.py, tests/test_webhook_dedup_race.py</files>
  <read_first>
    app/main.py (already read this session — `STALE_THRESHOLD = timedelta(minutes=5)`
    at line 64 and its usage in `retrigger()`'s stale-in-flight claim at lines 534-613,
    specifically lines 572-609; `runs_list()` at lines 865-884). 09-CONTEXT.md's
    D-9-13 (the threshold bound discussion) and 09-RESEARCH.md's Pitfall 1 (the
    ~30-minute true LLM worst-case ceiling FIRST computed by RESEARCH.md — NOTE:
    09-04 will correct this further: the `openai` library's own `max_retries=2`
    default COMPOUNDS with the app's 2-attempt reflective retry loop, making the
    true untightened worst case ≈60 min, not 30 — since no explicit `timeout=` or
    `max_retries=` override is passed to `call_structured`'s OpenAI client
    construction as of THIS plan, THIS plan must pick a threshold value that is
    honest about the CURRENT (untightened) worst case, documenting the tension per
    D-9-13's "known accepted tension" language, OR select a value that anticipates
    09-04 landing in the same phase and tightening both the timeout AND the retry
    count). tests/test_claim_status.py (already read — the `@pytest.mark.integration`
    threading race pattern this task's new test file generalizes, exact code cited
    in RESEARCH.md's "Concurrency race test skeleton" example and PATTERNS.md's
    Test File Patterns section).
  </read_first>
  <action>
    Introduce ONE shared threshold constant in app/main.py — rename/repurpose the
    existing `STALE_THRESHOLD = timedelta(minutes=5)` (line 64) to a lower,
    evidence-derived value per D-9-13, and add a companion `STALE_THRESHOLD_SECONDS =
    int(STALE_THRESHOLD.total_seconds())` for `sweep_stranded_runs`'s `int` parameter.
    Document in a code comment directly above the constant: the bound must exceed the
    worst-case legitimate gap between two consecutive DB writes (≈ the longest LLM
    call + its retries, since every stage write bumps `updated_at`); cite the
    RESEARCH.md Pitfall 1 finding that `call_structured` currently has NO explicit
    timeout or `max_retries` override (10-min library default timeout ×
    `max_retries=2`, i.e. 3 library-level attempts, COMPOUNDED with the app's own
    2-attempt reflective retry loop ≈ 60 min true worst case as of THIS plan — note
    for the comment: earlier phase artifacts estimated ~30 min by counting only the
    library's 3-attempt multiplier, not its compounding with the app's own retry;
    state the corrected ~60-min figure here) and that 09-04 tightens both
    `app/llm/client.py`'s timeout AND sets `max_retries=0` to make a much shorter
    threshold safe by construction — choose a concrete numeric value that is honest
    about which of those two states this plan lands in (Claude's Discretion, D-9-13
    — document the choice and its rationale in the comment, do not leave it
    implicit).
    In `runs_list()` (lines 865-884), add `repo.sweep_stranded_runs(STALE_THRESHOLD_SECONDS)`
    wrapped in the SAME try/except-swallow-on-DB-unavailable style the route already
    uses for `repo.load_all_runs()` (a sweep failure must never 500 the dashboard —
    log at debug and continue to render, matching the existing philosophy), called
    BEFORE `repo.load_all_runs()` so freshly-swept ERROR rows appear in the SAME page
    load (D-9-11). Update `retrigger()`'s existing `STALE_THRESHOLD` references
    (lines 545, 579) to use the renamed/repurposed constant — do not introduce a
    second, independently-tunable threshold unless tracing shows the sweep and
    retrigger genuinely need different values (D-9-13 says keep ONE shared constant
    "unless tracing shows they genuinely need different values" — document if you
    diverge). **Checker WARNING 3 (scope acknowledgment, do not "fix"):**
    `retrigger()`'s existing `stale_statuses` set (current lines 579-584) is
    `{received, extracting, computed, sent}` — FOUR statuses, including `sent` (the
    reserved-before-finalize state) — while the sweep's scope (D-9-12, built in
    09-01) is exactly THREE: `{received, extracting, computed}`, deliberately
    excluding `sent` because a `sent` run has already durably committed the
    provider-send evidence (D-13c) and belongs to `_deliver`'s own re-run path, not
    the sweep's "background task died before persisting anything durable" scope.
    "Keep ONE shared constant" (the THRESHOLD VALUE) does NOT mean "keep ONE shared
    scope LIST" — the two scope lists structurally diverge by design and cannot
    converge. Add a one-line code comment directly above `retrigger()`'s
    `stale_statuses` set (or the sweep scope list in `repo.sweep_stranded_runs`, or
    both) stating explicitly: "scope intentionally differs from
    sweep_stranded_runs's D-9-12 scope — retrigger additionally covers `sent` (its
    own re-run path via _deliver's idempotency guard); do not 'fix' this into
    parity." This prevents a future reader from treating the divergence as a bug.
    Create `tests/test_webhook_dedup_race.py` implementing SC2 exactly per
    RESEARCH.md's "Concurrency race test skeleton" / PATTERNS.md's Test File Patterns
    section: `@pytest.mark.integration`, skip-guarded on `DATABASE_URL` (matching
    `test_claim_status.py`'s exact skip shape), using Python's `threading` module to
    fire two `client.post("/webhook/inbound", json={...same message_id...})` calls
    at near-simultaneously against a REAL TestClient hitting a REAL/local Postgres
    (not `fake_repo` — this test proves the actual Postgres MVCC blocking behavior,
    which `FakeConnection` cannot simulate per RESEARCH.md Pitfall 3), then asserts
    `len({r.get("run_id") for r in results if r.get("run_id")}) == 1` across both
    responses.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration"</automated>
  </verify>
  <done>runs_list() sweeps stranded runs before rendering, sharing one threshold constant with retrigger's stale-claim logic; the SC2 concurrency race test exists and passes against a real DB when available; the offline suite has zero regressions.</done>
  <acceptance_criteria>
    - `app/main.py` contains a single named threshold constant used by BOTH `runs_list()`'s sweep call and `retrigger()`'s stale-claim logic (grep confirms one definition, two use sites)
    - The threshold constant's value and the code comment above it are present and state the current (untightened-timeout) worst case explicitly
    - `runs_list()` calls `repo.sweep_stranded_runs(...)` before `repo.load_all_runs()`, wrapped in its own try/except that never raises out of the route
    - A code comment near `retrigger()`'s `stale_statuses` set (or the sweep's scope list) explicitly acknowledges the scope divergence (`sweep_stranded_runs` = 3 statuses, `retrigger`'s `stale_statuses` = 4 statuses including `sent`) and states this is intentional, not a bug to converge (checker WARNING 3)
    - `tests/test_webhook_dedup_race.py` contains `def test_duplicate_webhook_delivery_creates_exactly_one_run` marked `@pytest.mark.integration` with a `DATABASE_URL` skip-guard
    - `uv run pytest -q -m "not integration"` exits 0 (the new test file skips cleanly offline)
    - `uv run pytest -q -m integration tests/test_webhook_dedup_race.py` exits 0 when `DATABASE_URL`+`ALLOW_DB_RESET=1` set
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Resend (external) → `POST /webhook/inbound` | Untrusted network input; the dedup/ingest transaction is the mechanism that guarantees exactly-once run-creation even under adversarial/duplicate/racing delivery. |
| Unauthenticated `GET /runs` → sweep UPDATE | The sweep runs on every dashboard load with no additional auth check (matches the existing route's auth posture, unchanged by this plan) — its blast radius is bounded entirely by the hardcoded scope list built in 09-01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09 | Tampering (duplicate financial run from retried delivery) | `inbound()` dedup+create_run transaction | mitigate | Postgres UNIQUE constraint (`uq_message_id`) + `ON CONFLICT DO NOTHING` inside ONE transaction (D-9-09) — verified Postgres MVCC blocking behavior (RESEARCH.md), not an app-level lock; proven by Task 2's real-thread concurrency test. |
| T-09-10 | Denial of Service (orphaned email row with no run, un-repairable) | `inbound()` ingest transaction | mitigate | The whole dedup+routing+create_run sequence is now one transaction — a crash mid-ingest rolls back the email row itself, so Resend's retry starts clean and creates the run on the next attempt. No repair/adoption path needed (D-9-09 dissolves rather than patches the orphan window). |
| T-09-11 | Repudiation (stuck run invisible to the operator) | `runs_list()` sweep hook | mitigate | Every dashboard load sweeps stranded runs to a visible, diagnosable ERROR state with a distinguishing `error_reason` sentinel (D-9-10/11), closing MED-05's "no visible signal" gap. |
| T-09-12 | Denial of Service (sweep threshold too aggressive, live task swept mid-flight) | Shared `STALE_THRESHOLD_SECONDS` constant | accept (documented tension) | D-9-13's known accepted tension: a pathologically-slow-but-alive task could be swept to ERROR and later have its unguarded `set_status` silently un-sweep it. This plan documents the current true worst case (≈60 min — the library's `max_retries=2` compounds with the app's 2-attempt reflective retry loop, correcting RESEARCH.md Pitfall 1's initial ~30-min estimate, checker BLOCKER 2) in the code comment rather than picking a value that pretends the risk doesn't exist; 09-04 tightens both the timeout and `max_retries` to close this gap; Phase 10's concurrency proof is the designated venue for any further guard-hardening evidence. |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration"` exits 0 (full offline suite — webhook, dashboard, retrigger tests all still pass with the new transaction wrapping and the sweep hook).
- `uv run pytest -q -m integration tests/test_webhook_dedup_race.py` exits 0 when `DATABASE_URL`+`ALLOW_DB_RESET=1` are set; skips cleanly otherwise.
- Manual check: `grep -n "STALE_THRESHOLD" app/main.py` shows exactly one constant definition and its two use sites (runs_list sweep call, retrigger stale-claim).
</verification>

<success_criteria>
- SC2 is proven: two concurrent duplicate webhook deliveries for the same message_id create exactly one run, verified against real Postgres concurrency (not a mocked simulation).
- The orphan window (email row with no run after a mid-ingest crash) is closed — a crash rolls back the whole ingest sequence.
- Stranded in-flight runs become visible as ERROR on the next dashboard load, using a threshold value whose rationale (including its current limitation) is documented in code.
</success_criteria>

<output>
Create `.planning/phases/09-atomic-data-integrity/09-03-SUMMARY.md` when done
</output>
