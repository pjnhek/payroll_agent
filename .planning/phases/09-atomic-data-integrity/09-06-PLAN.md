---
phase: 09-atomic-data-integrity
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/orchestrator.py
  - app/db/repo.py
  - tests/test_atomic_persist.py
autonomous: true
gap_closure: true
requirements: [DATA-01]
must_haves:
  truths:
    - "resume_pipeline's Round-2 non-deferred fall-through persists clarified_fields in a transaction that closes strictly BEFORE _run_stages opens its own persist transaction on that path — no independently-committing write after the status advance"
    - "A crash injected between the two commits (clarified-fields commit and _run_stages' persist-txn commit) leaves the run's clarified_fields already durably 'client_supplied'/'confirmed_dropped'/'carried_forward' (never stale 'asked') even though the run itself never reaches AWAITING_APPROVAL — diagnosable, not half-written"
    - "A genuine DB-level error (not a monkeypatched Python exception) inside _write_aliases_if_safe's alias write no longer poisons _deliver's finalize transaction — the run still reaches RECONCILED"
    - "The alias write runs inside its own nested conn.transaction() (SAVEPOINT) scoped to exactly _write_aliases_if_safe's call, so a DB-level failure there rolls back only the alias work"
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "Round-2 non-deferred clarified_fields persisted before _run_stages call (STEP 3); _deliver's alias write wrapped in a nested conn.transaction() (SAVEPOINT)"
    - path: "tests/test_atomic_persist.py"
      provides: "Two new/modified integration tests: crash-between-commits fault injection for the Round-2 fall-through, and a genuine DB-level (psycopg.errors) alias-write failure test proving RECONCILED is still reached"
  key_links:
    - from: "resume_pipeline (Round-2 non-deferred branch)"
      to: "repo.set_clarified_fields"
      via: "its own with repo.get_connection(): with conn.transaction(): block, called BEFORE _run_stages(...)"
      pattern: "repo\\.set_clarified_fields\\(run_id, clarified, conn=conn\\)"
    - from: "_deliver finalize block"
      to: "_write_aliases_if_safe"
      via: "nested with conn.transaction(): (SAVEPOINT) wrapping ONLY the alias-write call, inside the existing try/except"
      pattern: "with conn\\.transaction\\(\\):\\s*\\n\\s*_write_aliases_if_safe"
---

<objective>
Close the 2 remaining DATA-01 verification gaps from `09-VERIFICATION.md` (both re-confirmed against live source, both independently found by `09-REVIEW.md` WR-01/WR-02):

1. **Gap 1 (WR-02):** `resume_pipeline`'s Round-2 non-deferred fall-through calls `repo.set_clarified_fields(run_id, clarified)` at `orchestrator.py:618` with no `conn=` — an independently-auto-committing write that runs entirely AFTER `_run_stages`' own persist-transaction has already committed (including `set_status(AWAITING_APPROVAL)`). A crash between the two commits leaves an approvable run whose `clarified_fields` still read `asked` for fields the client actually answered (provenance contradicts the paid line items) and silently skips the STEP C/D alias-candidate diff.

2. **Gap 2 (WR-01):** `_deliver`'s alias-write isolation claim ("an alias failure NEVER rolls back a genuine delivery") only holds for pure-Python exceptions. `update_known_alias` (and every other repo helper `_write_aliases_if_safe` calls) runs under `_nulltx()` — a bare no-op, no SAVEPOINT — whenever the caller supplies its own `conn`. A genuine DB-level error (constraint violation, undefined column, lock timeout) inside the alias path poisons the whole finalize transaction; the very next statement, `repo.set_status(run_id, RunStatus.SENT, conn=conn)`, then raises `psycopg.errors.InFailedSqlTransaction`, and the ENTIRE finalize block — including the already-genuinely-sent email's status advance — rolls back. The run ERRORs after a successful send, contradicting both the plan's must-have and the code's own comment.

Purpose: DATA-01's literal wording — "each multi-write pipeline operation is atomic ... a crash mid-sequence never leaves a half-written run" — currently has one falsifying call site (gap 1) and one falsified isolation guarantee (gap 2). Both fixes are surgical: reorder one write (gap 1) and add one nested transaction/SAVEPOINT (gap 2). No new abstractions, no scope beyond these two named gaps (WR-03/WR-04/Info findings are explicitly out of scope per the gap-closure contract).

Output: `orchestrator.py` changes closing both crash windows; two integration tests in `tests/test_atomic_persist.py` proving the fixes against a real/local Postgres connection (not `FakeConnection`, which cannot prove rollback/SAVEPOINT semantics — see `09-RESEARCH.md` Pitfall 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atomic-data-integrity/09-VERIFICATION.md
@.planning/phases/09-atomic-data-integrity/09-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close gap 1 — persist Round-2 non-deferred clarified_fields BEFORE _run_stages, with a crash-window fault-injection test</name>
  <files>app/pipeline/orchestrator.py, tests/test_atomic_persist.py</files>
  <read_first>
    - app/pipeline/orchestrator.py (lines 227-350 for resume_pipeline's Round-1 branch and the `_defer_field_regression_clarification` call pattern; lines 355-620 for the full Round-2 classify-first block, specifically lines 430-483 where `clarified[emp_id_str][field]` is finalized to its terminal outcome during STEP 1, lines 588-598 for the `_run_stages(...)` call, and lines 600-619 for the current buggy fall-through)
    - app/pipeline/orchestrator.py lines 700-751 (`_defer_field_regression_clarification` — the ALREADY-CORRECT sibling pattern: `with repo.get_connection() as conn: with conn.transaction(): repo.set_clarified_fields(run_id, clarified, conn=conn)` committed and closed BEFORE the later `_clarify(...)` call, per the code's own "N2 ordering invariant" docstring)
    - app/db/repo.py lines 844-869 (`set_clarified_fields` signature: `(run_id, clarified, conn=None)`, validates via `ClarifiedFields(outcomes=clarified)` before serializing — raises `pydantic.ValidationError` on a malformed shape, so this call must NOT be silently swallowed)
    - tests/test_atomic_persist.py lines 1-135 (module docstring, `_SKIP_LIVE_DB` two-factor guard, `_seed_live_run`/`_live_inbound`/`_live_snapshot_extracted` helpers) and lines 466-531 (`test_defer_field_regression_write_survives_later_clarify_failure` — the closest existing analog: a live-DB test driving `resume_pipeline`'s Round-1 deferred branch, monkeypatching a downstream call to raise, then asserting the earlier write survived as its own closed transaction)
  </read_first>
  <behavior>
    - Test 1 (fix-proving, live-DB integration): Drive `resume_pipeline` down the Round-2 NON-deferred fall-through (a prior `clarified_fields` entry already exists — e.g. seed `set_clarified_fields(run_id, {chen_id: {"hours_overtime": "asked"}})` before calling `resume_pipeline` — so `is_round_2` is True and the reply answers the asked field, producing a `client_supplied`/`carried_forward`/`confirmed_dropped` terminal outcome with `stage.clarify_deferred == False`). Monkeypatch `_run_stages` (or the specific repo call inside its persist transaction, e.g. `repo.set_status`) to raise AFTER the fix's own `set_clarified_fields` commit but as part of `_run_stages`' own persist transaction. Assert: (a) `repo.load_clarified_fields(run_id)` shows the TERMINAL outcome (e.g. `"client_supplied"`), not `"asked"` — proving the write committed and survived the later crash; (b) the run's status did NOT advance to AWAITING_APPROVAL (the crashed `_run_stages` transaction rolled back cleanly) — proving the two writes are in genuinely separate, independently-verifiable transactions, not silently coupled.
    - Test 2 (regression guard, offline or live-DB): assert `set_clarified_fields(run_id, clarified, conn=conn)` is called and its transaction is CLOSED (committed) strictly before the `_run_stages(...)` call in source order for the Round-2 non-deferred path — either via a source-order assertion (mirrring the existing `test_defer_field_regression_write_survives_later_clarify_failure`'s sibling checks elsewhere in the file, e.g. the ordering check for `_defer_field_regression_clarification`) or by asserting call-order via a monkeypatched spy list in a live-DB run. This pins the fix so a future refactor cannot silently move the write back to the current (buggy) fall-through position.
  </behavior>
  <action>
    In the Round-2 branch of `resume_pipeline`, move the finalization of `clarified` so it is fully computed (STEP 1, lines ~430-483, unchanged — already runs before `_run_stages`) and then PERSISTED via `set_clarified_fields` in its OWN closed transaction BEFORE the `_run_stages(...)` call at line 588, using the exact pattern already established at lines 748-750 in `_defer_field_regression_clarification`: `with repo.get_connection() as conn: with conn.transaction(): repo.set_clarified_fields(run_id, clarified, conn=conn)`. This requires reordering: persist `clarified`'s terminal outcomes (client_supplied/confirmed_dropped/carried_forward, all already resolved in-memory by the end of STEP 1) BEFORE STEP 3's `_run_stages` call, not after it as the current code does at line 618. Delete the now-redundant bare `repo.set_clarified_fields(run_id, clarified)` call at line 618 entirely (the STEP C/D alias-diff fall-through comment at line 619 stays, since STEP C/D still needs to run after `_run_stages` — only the clarified-fields WRITE moves earlier, not the alias-diff logic). Since `clarified`'s values are already finalized in-memory during STEP 1 and do NOT depend on `_run_stages`' return value (`stage.clarify_deferred` only gates whether `_defer_field_regression_clarification` ADDS new 'asked' entries for a NEW regression found this round — the classify-first terminal outcomes from STEP 1 are independent of that), moving the persist earlier is safe and mirrors the deferred branch's own established invariant ("terminal outcomes are already final — do NOT reclassify from stage.issues after the fact", per the existing code comment at line 616). Update the code comment at the new call site to state the invariant this closes: "D-9-06 gap closure: persisted BEFORE _run_stages so a crash in _run_stages' own transaction cannot leave a stale 'asked' outcome — this write and the persist-transaction below are independently committed and independently diagnosable, never coupled."
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -n "repo.set_clarified_fields" app/pipeline/orchestrator.py` shows the Round-2 non-deferred call site now appears BEFORE (lower line number than) the `stage = _run_stages(` call inside the same branch — no bare (no-`conn=`) `set_clarified_fields` call remains anywhere after `_run_stages` returns on this path.
    - Source assertion: the moved call passes `conn=conn` and is nested inside `with repo.get_connection() as conn: with conn.transaction():` — identical shape to `_defer_field_regression_clarification`'s existing Step 3 (lines 748-750).
    - Test command: `uv run pytest -q tests/test_atomic_persist.py -k "round2_clarified_fields_persist_before_run_stages or round2"` — new test(s) present and passing (or cleanly skipped if `DATABASE_URL`/`ALLOW_DB_RESET` absent, per the existing two-factor guard).
    - Behavior assertion (live-DB path, when available): after forcing a crash inside `_run_stages`' own persist transaction on the Round-2 non-deferred path, `repo.load_clarified_fields(run_id)` returns the TERMINAL outcome (not `"asked"`) for the answered field, AND the run's `status` is unchanged from its pre-`_run_stages` value (never reaches `AWAITING_APPROVAL`) — both assertions in the same test, proving neither half is silently coupled to the other's success.
    - `uv run pytest -q -m "not integration"` still passes (no regression to the offline suite).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_atomic_persist.py -k "round2" -m "not integration" && uv run pytest -q -m "not integration"</automated>
  </verify>
  <done>Round-2 non-deferred clarified_fields write commits in its own closed transaction strictly before _run_stages is called on that path; a live-DB crash-injection test proves clarified_fields shows the terminal outcome (not "asked") while the run's status is independently unchanged from its pre-_run_stages value.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close gap 2 — nested SAVEPOINT around the alias write in _deliver's finalize block, with a genuine DB-level fault-injection test</name>
  <files>app/pipeline/orchestrator.py, tests/test_atomic_persist.py</files>
  <read_first>
    - app/pipeline/orchestrator.py lines 1359-1397 (`_deliver`'s finalize block — the exact try/except around `_write_aliases_if_safe(run_id, run, roster, conn=conn)` at lines 1368-1389, followed immediately by `repo.set_status(run_id, RunStatus.SENT, conn=conn)` and `repo.set_status(run_id, RunStatus.RECONCILED, conn=conn)`)
    - app/pipeline/orchestrator.py lines 1122-1202 (`_write_aliases_if_safe` — calls `repo.load_run`, `repo.update_known_alias`, `repo.load_roster_for_business`, all with `conn=conn` threaded through)
    - app/db/repo.py lines 892-925 (`update_known_alias` — uses `with c.transaction() if owns else _nulltx():`; when a caller-supplied `conn` is present (`owns=False`), `_nulltx()` is a bare `yield` — confirmed at repo.py:1416-1418 — meaning NO savepoint exists at this layer; the nesting must be added by the CALLER, i.e. `_deliver`, not inside `update_known_alias` itself, since psycopg3 nested `conn.transaction()` blocks are what create a SAVEPOINT, and only one such nested block is needed around the whole alias-write call, not inside every repo helper it transitively calls)
    - tests/test_atomic_persist.py lines 539-569 (`test_deliver_finalize_alias_failure_still_reaches_reconciled` — the EXISTING test that only proves the pure-Python-exception case via `monkeypatch.setattr(orch_mod, "_write_aliases_if_safe", _boom_alias)`; this task adds a SIBLING test using a genuine DB-level error, it does not replace this one)
  </read_first>
  <behavior>
    - Test (NEW, live-DB integration): seed a run at `RunStatus.APPROVED` with a resolvable `alias_candidates` entry (via `repo.set_alias_candidates(run_id, {token: str(employee_id)}, conn=None)` before calling `_deliver`, using an employee_id present in the seeded Coastal roster and a token that passes `_safe_to_learn_alias`'s collision guard, so `_write_aliases_if_safe` actually reaches `repo.update_known_alias`). Monkeypatch `repo.update_known_alias` to execute a genuinely-invalid SQL statement against the passed `conn` (e.g. `conn.execute("UPDATE employees SET nonexistent_column = 1 WHERE id = %s", (str(employee_id),))`, which raises `psycopg.errors.UndefinedColumn` — a real DB-level failure, not a monkeypatched Python `raise`) instead of calling the real implementation. Call `_deliver(run_id, run)`. Assert `repo.load_run(run_id)["status"] == "reconciled"` — the DB-level alias failure must NOT poison the finalize transaction's later `set_status(SENT)`/`set_status(RECONCILED)` calls.
    - Regression check: the EXISTING `test_deliver_finalize_alias_failure_still_reaches_reconciled` (pure-Python-exception case) must still pass unchanged after the fix — the nested SAVEPOINT must not alter behavior for the already-covered case.
  </behavior>
  <action>
    In `_deliver`'s finalize block (orchestrator.py ~1368-1383), wrap ONLY the `_write_aliases_if_safe(run_id, run, roster, conn=conn)` call in its own nested `with conn.transaction():` block — psycopg3 automatically issues `SAVEPOINT`/`RELEASE SAVEPOINT`/`ROLLBACK TO SAVEPOINT` (instead of `BEGIN`/`COMMIT`/`ROLLBACK`) when `conn.transaction()` is entered while already inside an outer transaction, per `09-RESEARCH.md`'s verified psycopg3 semantics. The try/except stays exactly where it is (nested strictly around the alias call, per the existing D-13b/Pitfall-2 comment) but now wraps the `with conn.transaction():` block, not the bare call: `try: \n    with conn.transaction(): \n        _write_aliases_if_safe(run_id, run, roster, conn=conn) \n except Exception as alias_exc: \n    logger.warning(...)`. This makes a DB-level error inside the alias path roll back ONLY to the savepoint (undoing just the alias write), leaving the OUTER finalize transaction's connection in a clean, still-usable state — so the subsequent `repo.set_status(run_id, RunStatus.SENT, conn=conn)` and `repo.set_status(run_id, RunStatus.RECONCILED, conn=conn)` execute normally instead of raising `InFailedSqlTransaction`. Update the code comment at this site (currently describing D-13b/Pitfall-2 isolation) to state explicitly: "the nested `with conn.transaction()` is a psycopg3 SAVEPOINT — this is what makes the isolation hold for genuine DB-level errors (constraint violations, undefined columns, lock timeouts), not just pure-Python exceptions; without it, a DB-level failure here poisons the WHOLE outer transaction via InFailedSqlTransaction on the next statement (09-REVIEW.md WR-01)."
  </action>
  <acceptance_criteria>
    - Source assertion: `sed -n '1368,1395p' app/pipeline/orchestrator.py` shows `with conn.transaction():` nested directly inside the `try:` block, wrapping ONLY the `_write_aliases_if_safe(...)` call — `repo.set_status(run_id, RunStatus.SENT, conn=conn)` and the RECONCILED call remain OUTSIDE both the try/except and the nested transaction block (unchanged position).
    - Test command: `uv run pytest -q tests/test_atomic_persist.py -k "alias_failure"` — both the existing pure-Python-exception test AND the new genuine-DB-level-error test pass (or cleanly skip together if `DATABASE_URL`/`ALLOW_DB_RESET` absent).
    - Behavior assertion (live-DB path, when available): after monkeypatching `repo.update_known_alias` to raise `psycopg.errors.UndefinedColumn` via a real invalid SQL statement executed against the shared `conn`, `_deliver(run_id, run)` returns without raising, and `repo.load_run(run_id)["status"] == "reconciled"` — the exact assertion the plan's own must-have requires (status VALUE outcome, not just "no exception propagated").
    - `uv run pytest -q -m "not integration"` still passes (no regression to the offline suite); the full `uv run pytest -q` (live-DB gated) passes when `DATABASE_URL`+`ALLOW_DB_RESET=1` are set.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_atomic_persist.py -k "alias_failure" -m "not integration" && uv run pytest -q -m "not integration"</automated>
  </verify>
  <done>_write_aliases_if_safe executes inside a nested conn.transaction() (SAVEPOINT) inside _deliver's finalize block; a live-DB test forcing a genuine psycopg.errors.UndefinedColumn failure in the alias path still results in the run reaching status "reconciled".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| resume_pipeline (background task) → Postgres | Internal-only; the reply body driving this path is already-validated `InboundEmail` from the webhook ingest path (Phase 9's own DATA-02 transaction). No new external input surface. |
| _deliver (approve route) → Postgres → Resend | The alias-write savepoint boundary sits entirely on the DB side of an already-sent, already-durable email (D-9-08 at-least-once semantics, unchanged by this plan). No new network call introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09.06-01 | Tampering | `resume_pipeline` Round-2 clarified_fields write | mitigate | Reordering the write to commit strictly before `_run_stages`' own transaction removes the crash window entirely (no partial/contradictory provenance state is reachable) — verified by Task 1's fault-injection test asserting both halves independently. |
| T-09.06-02 | Repudiation | `_deliver` finalize transaction after a DB-level alias-write failure | mitigate | Nested `conn.transaction()` (SAVEPOINT) isolates a DB-level alias failure from the outer transaction's `InFailedSqlTransaction` cascade, so the run's true delivery outcome (RECONCILED, since the email was genuinely sent) remains observable and not misreported as ERROR — verified by Task 2's genuine-DB-level fault-injection test. |
| T-09.06-03 | Denial of Service (resource exhaustion) | Both new live-DB integration tests | accept | Tests are `@pytest.mark.integration`, two-factor skip-guarded (`DATABASE_URL` + `ALLOW_DB_RESET=1`) — no risk to CI/offline runs; identical existing pattern used throughout `tests/test_atomic_persist.py`. |

No new external packages, no new input-validation surface, no new secrets — this plan is a pure transaction-boundary correction on already-reviewed, already-internal code paths. `security_enforcement` gate is satisfied by the mitigate dispositions above; no `[ASSUMED]`/`[SUS]` package checkpoint applies (zero new dependencies).
</threat_model>

<verification>
Run the full offline suite plus the two targeted live-DB test groups:

```
uv run pytest -q -m "not integration"
uv run pytest -q tests/test_atomic_persist.py -k "round2 or alias_failure"
uv run pytest -q  # full suite, requires DATABASE_URL + ALLOW_DB_RESET=1 for the two new/modified integration tests
```

Both gaps must show:
1. A source-level fix (verifiable by `grep`/`sed` inspection, no ambiguity about location or shape).
2. A live-DB test proving the fix against the SPECIFIC failure mode 09-VERIFICATION.md and 09-REVIEW.md identified (crash-between-commits for gap 1; genuine `psycopg.errors` DB-level failure, not a monkeypatched Python raise, for gap 2).
</verification>

<success_criteria>
- `app/pipeline/orchestrator.py`'s Round-2 non-deferred `resume_pipeline` fall-through persists `clarified_fields` via its own closed transaction strictly BEFORE `_run_stages` is called on that path — no bare, unwrapped `set_clarified_fields` call remains after the persist-transaction commits.
- `_deliver`'s alias write executes inside a nested `conn.transaction()` (SAVEPOINT), so a genuine DB-level error there no longer poisons the finalize transaction — `_deliver` still reaches `RECONCILED` after a real send, even when the alias write fails at the SQL level.
- Both new/modified tests in `tests/test_atomic_persist.py` use a real/local Postgres connection (never `FakeConnection`) and assert the PERSISTED VALUE outcome (status column, `clarified_fields` column) — not merely "no exception propagated."
- `uv run pytest -q -m "not integration"` remains fully green (no regression).
- `uv run pytest -q` (full suite, live-DB gated) is green when run against a real/local Postgres with `DATABASE_URL` + `ALLOW_DB_RESET=1` set.
- DATA-01's literal wording — "each multi-write pipeline operation is atomic ... a crash mid-sequence never leaves a half-written run" — no longer has a confirmed falsifying call site or a falsified isolation guarantee.
</success_criteria>

<output>
Create `.planning/phases/09-atomic-data-integrity/09-06-SUMMARY.md` when done
</output>
