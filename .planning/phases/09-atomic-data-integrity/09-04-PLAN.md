---
phase: 09-atomic-data-integrity
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - app/llm/client.py
  - .env.example
  - app/pipeline/compose_email.py
  - app/main.py
  - tests/test_stuck_run_recovery.py
  - tests/test_llm_client.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "call_structured (used by BOTH extract() and suggest_employees()) passes an explicit, bounded timeout AND max_retries=0 to its OpenAI client construction — the openai library's own default max_retries=2 compounds with the app's 2-attempt reflective retry loop (timeout x 3 x 2 = 6x, not 2x), so timeout= alone is insufficient"
    - "call_text (used by compose_clarification()) ALSO gains an explicit, bounded timeout (via its existing timeout_s= parameter, now always passed with a non-None value by every call site) AND, where the library allows, a suppressed retry count — closing the previously-unbounded clarification-drafting gap Codex HIGH-3 identified (compose_email.py:167 called call_text with no timeout at all)"
    - "resume_pipeline's Round-2 back-to-back double extraction (raw_reply_extracted = extract(inbound, ...) then raw_extracted = extract(combined_email, ...), orchestrator.py:377,380) is explicitly counted in the threshold derivation as a 2x multiplier on call_structured's own bounded ceiling — the D-9-13 threshold is sized against the LONGEST real sequence of LLM calls between DB writes, not a single call (Codex HIGH-3, closed)"
    - "A run stranded in extracting/received/computed with a backdated updated_at is swept to ERROR by sweep_stranded_runs, then successfully retriggered to a progressing state via the EXISTING retrigger claim path — proven end-to-end against a real/local Postgres"
    - "The STALE_THRESHOLD_SECONDS constant in app/main.py is now safely bounded by the FULLY-tightened LLM ceiling (call_structured's timeout x 2 app-attempts x 2 for Round-2's double extraction, PLUS call_text's now-bounded timeout), and the code comment documenting the D-9-13 tension is updated to reflect the closed gap with the corrected, complete multiplier"
    - "The full offline test suite (uv run pytest -q -m \"not integration\") is green, and the full integration suite (uv run pytest -q, live DB) is green for every Phase 9 test file"
  artifacts:
    - path: "app/llm/client.py"
      provides: "call_structured passes an explicit timeout= AND max_retries=0 to the OpenAI(...) client construction, matching the existing timeout_s= precedent in call_text/compose_confirmation and additionally suppressing the library's own retry layer so it cannot compound with the app's reflective retry"
      contains: "timeout="
    - path: "app/pipeline/compose_email.py"
      provides: "compose_clarification's call_text invocation now passes an explicit timeout_s= (previously absent — Codex HIGH-3), bounding the clarification-drafting LLM gap that feeds directly into resume_pipeline's field-regression re-clarification path (the same path 09-02 wrapped in a transaction)"
      contains: "timeout_s="
    - path: "tests/test_stuck_run_recovery.py"
      provides: "SC3 end-to-end integration test: strand a run, sweep it, assert ERROR + error_detail, retrigger it, assert progressing status"
      contains: "def test_stranded_run_swept_and_retriggerable"
  key_links:
    - from: "app/llm/client.py::call_structured"
      to: "openai.OpenAI(...) client construction"
      via: "explicit timeout= AND max_retries=0 kwargs bounding the true worst-case single-stage latency to timeout x 2 app-attempts (not timeout x 3 library-attempts x 2 app-attempts = 6x); this same function serves BOTH extract() and suggest_employees() (app/pipeline/suggest.py:81, Codex HIGH-3) so both gaps are closed by one change"
      pattern: "OpenAI(base_url=cfg.base_url, api_key=cfg.api_key, timeout="
    - from: "app/pipeline/compose_email.py::compose_clarification"
      to: "app/llm/client.py::call_text"
      via: "compose_clarification now passes an explicit timeout_s= (previously omitted, Codex HIGH-3 finding) so the drafting/clarification LLM gap is bounded the same way compose_confirmation's timeout_s=3.0 already bounds its own call"
      pattern: "llm.call_text(\"draft\", messages, temperature=0.3, timeout_s="
    - from: "app/main.py::STALE_THRESHOLD_SECONDS"
      to: "app/llm/client.py::call_structured timeout + resume_pipeline Round-2 double-extraction"
      via: "the sweep threshold now safely exceeds (_STRUCTURED_TIMEOUT_S x 2 app-attempts x 2 for Round-2's double extraction) PLUS compose_clarification's now-bounded call_text timeout, with comfortable margin, documented in a code comment at both seams"
      pattern: "STALE_THRESHOLD"
---

<objective>
Close the findings RESEARCH.md and 09-REVIEWS.md surfaced beyond the three locked
CONTEXT.md decisions.

**Revision note (09-REVIEWS.md Codex HIGH-3 — the prior draft still undercounted
the recovery threshold):** the prior draft of this plan tightened ONLY
`call_structured` and reasoned about the threshold as
`_STRUCTURED_TIMEOUT_S × 2 app-attempts`. Verified against live source this
session: (1) `call_structured` is NOT extraction-only — `app/pipeline/suggest.py:81`
(`suggest_employees`) ALSO calls `llm.call_structured("draft", messages,
NameSuggestionResponse)`, so tightening `call_structured` closes a SECOND gap for
free (already implicitly covered by this plan's Task 1, now stated explicitly);
(2) `app/pipeline/compose_email.py:167` (`compose_clarification`) calls
`llm.call_text("draft", messages, temperature=0.3)` with NO `timeout_s=` argument
at all — `call_text`'s `timeout_s` parameter already exists and is optional
(defaults to `None`, meaning "no client-side timeout override," i.e. the raw
10-minute library default with no app-level retry wrapping it at all) — this gap
was NOT closed by tightening `call_structured` alone, since `compose_clarification`
never goes through `call_structured`; (3) `app/pipeline/orchestrator.py:377,380`
(verified this session): `resume_pipeline`'s Round-2 branch calls `extract(inbound,
...)` then, two lines later, `extract(combined_email, ...)` — TWO separate
`call_structured`-backed extraction calls back-to-back, BEFORE the next DB write
— so the real worst-case gap on THAT specific path is `_STRUCTURED_TIMEOUT_S × 2
app-attempts × 2 extractions` = 4× the single-call ceiling, not 2×. This plan
corrects all three: tightens `call_structured` (closing both the extraction AND
suggestion gaps at once), adds an explicit `timeout_s=` to
`compose_clarification`'s `call_text` invocation (closing the previously-wholly-
unbounded clarification-drafting gap), and re-derives the D-9-13 threshold against
the ACTUAL longest real sequence (Round-2's double extraction), not a
single-call assumption.

Per RESEARCH.md's Pitfall 1 recommendation #1 (the preferred resolution) and
09-REVIEWS.md's suggestion ("Bound all in-flight external gaps used by the sweep
threshold... size the threshold to the longest real sequence between DB writes"),
this plan tightens `call_structured`'s timeout AND suppresses the library's own
retry layer (`max_retries=0`), tightens `compose_clarification`'s `call_text` call
with an explicit bounded `timeout_s=`, and re-derives `STALE_THRESHOLD_SECONDS`
against the corrected, COMPLETE worst-case picture — making the threshold chosen
in 09-03 actually safe by construction rather than by an incomplete assumption.
Then proves DATA-03's full success criterion end-to-end: a run stranded mid-flight
is swept to ERROR and successfully retriggered, against a real/local Postgres.

Purpose: without this plan, 09-03's threshold constant is either uncomfortably
long (once ALL THREE gaps are correctly counted) or unsafe (short enough to
occasionally sweep a legitimately-still-running extraction, suggestion, OR
clarification draft). Tightening every LLM-call gap that can legitimately delay
the next DB write — not just the one RESEARCH.md first flagged — is the surgical
fix that makes a short, useful threshold correct, closing RESEARCH.md's
Assumption A1 AND 09-REVIEWS.md's Codex HIGH-3 explicitly (with the corrected,
complete multiplier) rather than leaving either as an unresolved or
partially-resolved tension.

Output: `call_structured` gains an explicit, bounded `timeout=` AND
`max_retries=0`; `compose_clarification`'s `call_text` invocation gains an
explicit, bounded `timeout_s=`; 09-03's threshold constant and its code comment
are reconciled against the now-fully-tightened, correctly-derived ceiling
(`_STRUCTURED_TIMEOUT_S × 2 app-attempts × 2 for Round-2's double extraction`,
plus the now-bounded `call_text` gap); `tests/test_stuck_run_recovery.py` proves
SC3 end-to-end (strand → sweep → ERROR → retrigger → progressing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atomic-data-integrity/09-CONTEXT.md
@.planning/phases/09-atomic-data-integrity/09-RESEARCH.md
@.planning/phases/09-atomic-data-integrity/09-REVIEWS.md
@.planning/phases/09-atomic-data-integrity/09-01-SUMMARY.md
@.planning/phases/09-atomic-data-integrity/09-02-SUMMARY.md
@.planning/phases/09-atomic-data-integrity/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit bounded timeout to call_structured AND compose_clarification's call_text; reconcile the sweep threshold against the FULL worst-case picture</name>
  <files>app/llm/client.py, app/pipeline/compose_email.py, .env.example, app/main.py, tests/test_llm_client.py</files>
  <read_first>
    app/llm/client.py (already read this session — `call_structured` full body lines
    93-158, specifically the `client = OpenAI(base_url=cfg.base_url,
    api_key=cfg.api_key)` construction at line 110 with NO timeout kwarg AND NO
    `max_retries` kwarg — the `openai` library (2.43.0, pinned) defaults
    `max_retries=2`, applied INDEPENDENTLY, per HTTP call, underneath the app's own
    `for attempt in (1, 2):` reflective-retry loop (line 120); `call_text` lines
    161-201, which ALREADY has the `timeout_s=`/`client_kwargs["timeout"]` precedent
    at lines 165/183-187 — confirm this precedent is unchanged, this task's
    `compose_clarification` fix simply passes a NON-`None` value through the
    parameter that already exists). app/pipeline/suggest.py (already read this
    session, full file, 111 lines) — CONFIRM line 81: `response =
    llm.call_structured("draft", messages, NameSuggestionResponse)` — this is the
    SECOND call site `call_structured`'s timeout/retry fix closes automatically
    (Codex HIGH-3), no separate code change needed in suggest.py itself.
    app/pipeline/compose_email.py (already read this session, lines 140-201) —
    CONFIRM line 167: `body = llm.call_text("draft", messages, temperature=0.3)` —
    NO `timeout_s=` argument passed at all (Codex HIGH-3 finding). Compare against
    `compose_confirmation`'s call (grep for `timeout_s=3.0` elsewhere in this file
    or in `orchestrator.py`'s `_deliver`, which already passes `timeout_s=3.0` to
    `compose_confirmation` — the precedent to scale from). app/pipeline/orchestrator.py
    lines 227-697 (already read this session, full `resume_pipeline` body) —
    CONFIRM lines 377/380: `raw_reply_extracted = extract(inbound, roster,
    **extract_kwargs_r2)` then two lines later `raw_extracted = extract(combined_email,
    roster, **extract_kwargs_r2)` — TWO separate extraction calls (each backed by
    `call_structured`) back-to-back on the Round-2 path, BEFORE the next DB write —
    this DOUBLES the single-call ceiling on that specific path; the threshold
    derivation in this task must count this multiplier explicitly, not just the
    single-call `_STRUCTURED_TIMEOUT_S × 2 app-attempts` figure the prior plan draft
    used. 09-RESEARCH.md's Pitfall 1 in full (the original ~30-minute estimate,
    now superseded by the more complete Codex HIGH-3 picture) and its "How to
    avoid" recommendation #1. 09-03-SUMMARY.md for the exact `STALE_THRESHOLD_SECONDS`
    value and code-comment wording chosen in that plan — this task reconciles that
    value against the newly-tightened AND fully-counted ceiling. tests/test_llm_client.py
    (grep its existing test shape before writing new assertions — match its
    existing mocking/assertion conventions rather than inventing a new style).
  </read_first>
  <action>
    In `app/llm/client.py`'s `call_structured`, add BOTH an explicit `timeout=` kwarg
    AND an explicit `max_retries=0` kwarg to the `OpenAI(base_url=cfg.base_url,
    api_key=cfg.api_key)` construction at line 110 — per checker BLOCKER 2 (prior
    round), setting `timeout=` alone is NOT sufficient: the `openai` library's own
    `max_retries=2` default compounds with the app's existing `for attempt in (1,
    2):` reflective retry loop, so without `max_retries=0` the true worst case is
    `timeout × 3 library-attempts × 2 app-attempts` = 6× the timeout, not 2×.
    Setting `max_retries=0` makes the app's own single reflective retry the ONLY
    retry layer for this call, collapsing the ceiling to `timeout × 2 app-attempts`
    exactly. Mirror `call_text`'s existing `client_kwargs["timeout"] = timeout_s`
    pattern for the `timeout=` kwarg. Choose a concrete numeric timeout value
    bounded above by RESEARCH.md's recommendation (30-60s range, scaled up from
    `compose_confirmation`'s `timeout_s=3.0` for a heavier structured-JSON
    extraction call that must round-trip a full payroll email) — introduce a
    module-level named constant (e.g. `_STRUCTURED_TIMEOUT_S = 45.0`) rather than a
    bare literal, so the value is documented and greppable in one place. Add a code
    comment directly above the constant citing this exact finding, AND noting
    (Codex HIGH-3) that this same function is called by BOTH `extract()` (via
    `app/pipeline/extract.py`) AND `suggest_employees()`
    (`app/pipeline/suggest.py:81`) — so this ONE change bounds both call sites, and
    that `resume_pipeline`'s Round-2 path calls extraction TWICE back-to-back
    (`orchestrator.py:377,380`) before its next DB write, so the real worst-case
    gap on THAT path is `_STRUCTURED_TIMEOUT_S × 2 app-attempts × 2 extractions`.

    In `app/pipeline/compose_email.py`'s `compose_clarification`, change the
    `llm.call_text("draft", messages, temperature=0.3)` call (line 167) to pass an
    explicit `timeout_s=` — introduce (or reuse, if `call_structured`'s constant is
    importable without a circular import; otherwise define a small local constant
    in this module, Claude's discretion, document the choice) a bounded value in
    the same 30-60s range used for `_STRUCTURED_TIMEOUT_S` (a clarification draft is
    free-text, not a heavier structured JSON round-trip, so a value at the LOWER
    end of that range, or matching it exactly, is acceptable — document the
    reasoning in a code comment directly above the call). This closes the
    previously wholly-unbounded `call_text` gap Codex HIGH-3 identified — before
    this fix, `compose_clarification`'s draft call had NO client-side timeout at
    all (10-minute raw library default, no app-level retry wrapping it either,
    since `call_text` itself does not implement the app's own reflective-retry
    loop). Verify (grep `call_text(` across `app/pipeline/`) that no OTHER
    `call_text` call site needs the same fix within this phase's scope — only
    `compose_clarification`'s call feeds the field-regression re-clarification path
    09-02 already wrapped in a transaction; `compose_confirmation` already passes
    `timeout_s=3.0` (unchanged, out of scope for this task).

    Do NOT add a timeout/max_retries override to any OTHER `call_text` call site
    beyond `compose_clarification` unless grep reveals one this read_first missed —
    verify scope is exactly these two functions (`call_structured`,
    `compose_clarification`'s `call_text` invocation) before concluding the task is
    complete. Update `.env.example` if a new env var is warranted (Claude's
    discretion — a hardcoded module constant is acceptable and matches
    `_MAX_TOKENS`'s existing style; only add an env var if config-driven tunability
    is clearly needed, matching the project's existing "config-driven where it
    matters" philosophy from CLAUDE.md).

    Return to `app/main.py`'s `STALE_THRESHOLD`/`STALE_THRESHOLD_SECONDS` constant
    (introduced in 09-03) and update its code comment to state the NEW,
    CORRECTLY-AND-COMPLETELY-derived ceiling this task establishes: the longest
    real sequence of LLM calls between two consecutive DB writes is Round-2's
    double extraction — `_STRUCTURED_TIMEOUT_S × 2 app-attempts × 2 extractions`
    (the library's `max_retries` now forced to 0 so it no longer multiplies either
    extraction's ceiling) — PLUS, on the clarify branch, `compose_clarification`'s
    now-bounded `call_text` timeout as an ADDITIONAL (not multiplied — it runs
    after, not concurrently with, extraction) gap before the next DB write. State
    the combined worst-case ceiling explicitly as a sum where sequential, not a
    product where the calls are on different, mutually-exclusive branches. If
    09-03's threshold value was chosen conservatively assuming the untightened
    ceiling, this task MAY tighten `STALE_THRESHOLD_SECONDS` further now that the
    true ceiling is much lower, as long as the value still exceeds the corrected
    combined ceiling with comfortable margin (D-9-13's bound). Document whichever
    choice is made (tighten further, or leave as-is with an updated comment)
    directly in the code.

    In `tests/test_llm_client.py`, add a test asserting `call_structured` constructs
    its `OpenAI` client with BOTH a `timeout` kwarg present and equal to the new
    constant AND `max_retries=0` present (mock/spy on the `OpenAI` class the same
    way the existing tests in that file already do — read the file first to match
    the convention exactly). Add a test (in `tests/test_llm_client.py` or the
    existing `compose_email` test file, whichever already covers
    `compose_clarification` — grep before choosing) asserting
    `compose_clarification`'s `call_text` invocation is made WITH a non-`None`
    `timeout_s` value (spy on `call_text` via the fake `llm=` injection pattern
    `compose_clarification` already supports).
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration" tests/test_llm_client.py</automated>
  </verify>
  <done>call_structured passes an explicit, named, documented timeout= AND max_retries=0 to its OpenAI client construction (closing the compounding-retry gap for BOTH extraction and suggestion call sites); compose_clarification's call_text invocation now passes an explicit, bounded timeout_s= (closing the previously wholly-unbounded clarification-drafting gap, Codex HIGH-3); the sweep threshold's code comment is reconciled against the correctly-and-completely-derived ceiling (accounting for Round-2's double extraction AND the now-bounded call_text gap); the offline suite is green.</done>
  <acceptance_criteria>
    - `app/llm/client.py` contains a named constant (not a bare literal) for the structured-call timeout, with a code comment citing (a) the 10-min-default/max_retries=2-compounding-with-2-app-attempts finding, (b) that this function serves both `extract()` and `suggest_employees()`, and (c) that Round-2's double extraction doubles the single-call ceiling on that path
    - `call_structured`'s `OpenAI(...)` construction includes both `timeout=<the constant>` AND `max_retries=0`
    - `app/pipeline/compose_email.py`'s `compose_clarification` passes an explicit, non-`None` `timeout_s=` to its `llm.call_text(...)` call, with a code comment documenting the chosen value's rationale
    - `app/main.py`'s threshold code comment (from 09-03) is updated to reference the FULL corrected ceiling: `_STRUCTURED_TIMEOUT_S × 2 app-attempts × 2 (Round-2 double extraction)` PLUS the now-bounded `call_text` gap on the clarify branch, with an explicit statement of the current margin
    - `uv run pytest -q -m "not integration" tests/test_llm_client.py` exits 0, including a new test asserting BOTH the `timeout=` kwarg AND `max_retries=0` are passed to `call_structured`'s `OpenAI(...)` construction, AND a test asserting `compose_clarification`'s `call_text` invocation passes a non-`None` `timeout_s`
    - `uv run pytest -q -m "not integration"` (full offline suite) exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC3 end-to-end integration test — strand, sweep, retrigger</name>
  <files>tests/test_stuck_run_recovery.py</files>
  <read_first>
    tests/test_stuck_run_recovery.py (created in 09-01 — read its current contents
    first; this task ADDS the end-to-end integration test, replacing the stub from
    09-01 Task 2). app/main.py's `retrigger()` route (already read this session, full
    body lines 534-613) — the EXISTING claim-from-ERROR path this test exercises
    unchanged. app/db/repo.py's `sweep_stranded_runs` (built in 09-01) and `load_run`
    (already read — explicit RUN_COLS projection including `updated_at`,
    `error_reason`, `error_detail`). tests/conftest.py's `seeded_db` fixture
    (already read this session, lines 57-73) — the module-scoped live-DB
    reset+bootstrap+seed fixture this integration test uses.
  </read_first>
  <action>
    Implement `test_stranded_run_swept_and_retriggerable` (or equivalently named) in
    tests/test_stuck_run_recovery.py, replacing the 09-01 stub. Using `seeded_db` +
    a real `repo.get_connection()`: create a run via `repo.create_run(...)`, then
    directly backdate its `updated_at` via a raw UPDATE (older than
    `STALE_THRESHOLD_SECONDS` — import the constant from `app.main`) while its
    status is `extracting` (one of the three swept statuses, D-9-12) — set the
    status via `repo.set_status(run_id, RunStatus.EXTRACTING)` then backdate
    `updated_at` with a direct SQL UPDATE (`UPDATE payroll_runs SET updated_at = now()
    - interval '...' WHERE id = %s`) since no repo helper exposes a raw
    `updated_at` write. Call `repo.sweep_stranded_runs(STALE_THRESHOLD_SECONDS)` and
    assert the run_id is IN the returned list. Reload via `repo.load_run(run_id)` and
    assert `status == "error"`, `error_reason == "StrandedRunSwept"`, and
    `error_detail` is non-null and contains the word "stranded" (case-insensitive) —
    proving the diagnostic sentinel is distinguishable from a real exception-driven
    ERROR. Then call the retrigger logic directly (import and call the same claim
    path `main.py`'s `retrigger()` route uses — `repo.claim_status(run_id,
    RunStatus.ERROR, RunStatus.RECEIVED)`) and assert it returns `True` (the claim
    succeeds) and the reloaded run's status is now `received` — a "progressing"
    state per SC3's wording. Also add (or confirm 09-01 already added) the
    scope-pin unit test asserting `awaiting_reply`/`awaiting_approval`/`approved`
    runs are NEVER sweepable — if 09-01 already covers this via FakeConnection,
    add a SECOND, real-DB confirmation here: create one run in each of the three
    excluded statuses with a backdated `updated_at`, run the sweep, and assert NONE
    of their ids appear in the returned list (closing the loop between the
    SQL-shape unit test from 09-01 and a genuine live-DB behavioral proof).
  </action>
  <verify>
    <automated>uv run pytest -q -m integration tests/test_stuck_run_recovery.py</automated>
  </verify>
  <done>A stranded run is swept to ERROR with the StrandedRunSwept sentinel and a non-null error_detail, then successfully retriggered to received via the existing claim path; the three excluded statuses are proven never-swept against a real DB; the full integration suite for Phase 9 is green.</done>
  <acceptance_criteria>
    - `tests/test_stuck_run_recovery.py` contains `def test_stranded_run_swept_and_retriggerable` marked `@pytest.mark.integration`, using `seeded_db`
    - The test asserts `error_reason == "StrandedRunSwept"` and a non-null `error_detail` containing "stranded" after the sweep
    - The test asserts the swept run is retriggerable via `repo.claim_status(run_id, RunStatus.ERROR, RunStatus.RECEIVED)` returning `True` and the reloaded status is `received`
    - A second test (or an extension of the same test) confirms `awaiting_reply`/`awaiting_approval`/`approved` runs with a backdated `updated_at` are NEVER included in the sweep's returned list, against a real DB
    - `uv run pytest -q -m integration tests/test_stuck_run_recovery.py` exits 0 when `DATABASE_URL`+`ALLOW_DB_RESET=1` set
    - `uv run pytest -q` (full suite, live DB) exits 0 — every Phase 9 integration test (`test_atomic_persist.py`, `test_webhook_dedup_race.py`, `test_stuck_run_recovery.py`) is green together
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App → DeepSeek/Kimi (external LLM providers) | The tightened `timeout=`/`timeout_s=` values bound how long the app trusts a slow/hung provider response before failing the stage — this is a resilience boundary, not a data-trust boundary (the provider output is already validated post-hoc by Pydantic). This now covers ALL THREE call surfaces that can delay the next DB write: `call_structured` (extraction + suggestion), `call_text` (clarification drafting), and the Round-2 double-extraction sequence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Denial of Service (a hung/slow LLM provider stalls a run indefinitely, defeating the sweep's short threshold) | `call_structured`'s `OpenAI` client construction | mitigate | Explicit bounded `timeout=` AND `max_retries=0` (this plan) makes the true worst-case single-stage latency small, known, and NOT compounded by the library's own independent retry layer — the ceiling collapses to `_STRUCTURED_TIMEOUT_S × 2` (app-attempts only) per call, correctly counted twice on the Round-2 double-extraction path — so the sweep threshold (09-03) can be short AND safe — closing RESEARCH.md's Assumption A1 AND 09-REVIEWS.md's Codex HIGH-3 explicitly (with the corrected, COMPLETE multiplier across all three gap sources) rather than leaving it open or partially-counted. |
| T-09-20 | Denial of Service (compose_clarification's call_text gap was wholly unbounded — no client timeout, no app-level retry wrapper) | `compose_clarification`'s `llm.call_text(...)` invocation | mitigate | Explicit `timeout_s=` now passed (this plan, Codex HIGH-3) — the clarification-drafting gap that feeds `_defer_field_regression_clarification`'s Step 5 (already wrapped in its own commit-then-post-commit-unit split by 09-02) is now bounded the same way `compose_confirmation`'s existing `timeout_s=3.0` already bounds its own call. |
| T-09-14 | Tampering (a swept-to-ERROR run's still-running background task later overwrites ERROR via an unguarded set_status) | The D-9-13 "known accepted tension" | accept | Explicitly accepted per 09-CONTEXT.md — the margin between the tightened LLM timeout (now correctly and completely derived) and the sweep threshold makes this pathological, not eliminated; Phase 10's concurrency proof is the designated venue for any further guard-hardening evidence, not this phase. |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration"` exits 0 (full offline suite, including the new `call_structured`/`compose_clarification` timeout assertions).
- `uv run pytest -q -m integration` exits 0 when `DATABASE_URL`+`ALLOW_DB_RESET=1` are set — every Phase 9 integration test (SC1 in `test_atomic_persist.py`, SC2 in `test_webhook_dedup_race.py`, SC3 in `test_stuck_run_recovery.py`) green together, proving all three DATA-01/02/03 success criteria end-to-end.
- `uv run pytest -q` (the phase-gate full-suite command per 09-VALIDATION.md) exits 0.
</verification>

<success_criteria>
- RESEARCH.md's Pitfall 1 / Assumption A1 AND 09-REVIEWS.md's Codex HIGH-3 are closed: `call_structured` has an explicit, documented, bounded timeout AND `max_retries=0` (covering both extraction and suggestion call sites); `compose_clarification`'s `call_text` invocation is now bounded too; the sweep threshold's safety is derived against the LONGEST real sequence (Round-2's double extraction), not a single-call assumption.
- The sweep threshold's safety is now provable by construction (`_STRUCTURED_TIMEOUT_S × 2 app-attempts × 2 for Round-2` plus the bounded `call_text` gap, correctly derived + comfortable margin), not merely assumed and not under-counted.
- SC3 is proven end-to-end: a stranded run is swept to a visible ERROR with a distinguishing sentinel, then successfully retriggered to a progressing state — closing MED-05.
- All three Phase 9 requirements (DATA-01, DATA-02, DATA-03) have a passing integration test proving their respective success criterion against a real/local Postgres connection.
</success_criteria>

<output>
Create `.planning/phases/09-atomic-data-integrity/09-04-SUMMARY.md` when done
</output>
</output>
