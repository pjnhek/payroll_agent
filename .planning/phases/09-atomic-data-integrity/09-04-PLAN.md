---
phase: 09-atomic-data-integrity
plan: 04
type: execute
wave: 3
depends_on: ["09-01", "09-02", "09-03"]
files_modified:
  - app/llm/client.py
  - .env.example
  - app/main.py
  - tests/test_stuck_run_recovery.py
  - tests/test_llm_client.py
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "call_structured (used by extraction, and any future decision-tier call) passes an explicit, bounded timeout AND max_retries=0 to its OpenAI client construction — the openai library's own default max_retries=2 compounds with the app's 2-attempt reflective retry loop (timeout x 3 x 2 = 6x, not 2x), so timeout= alone is insufficient; the D-9-13 threshold's safety no longer depends on an unverified 90s-3min assumption nor an under-counted retry ceiling"
    - "A run stranded in extracting/received/computed with a backdated updated_at is swept to ERROR by sweep_stranded_runs, then successfully retriggered to a progressing state via the EXISTING retrigger claim path — proven end-to-end against a real/local Postgres"
    - "The STALE_THRESHOLD_SECONDS constant in app/main.py is now safely bounded by the tightened LLM timeout (_STRUCTURED_TIMEOUT_S x 2 app-attempts, library retries suppressed via max_retries=0), and the code comment documenting the D-9-13 tension is updated to reflect the closed gap with the corrected multiplier"
    - "The full offline test suite (uv run pytest -q -m \"not integration\") is green, and the full integration suite (uv run pytest -q, live DB) is green for every Phase 9 test file"
  artifacts:
    - path: "app/llm/client.py"
      provides: "call_structured passes an explicit timeout= AND max_retries=0 to the OpenAI(...) client construction, matching the existing timeout_s= precedent in call_text/compose_confirmation and additionally suppressing the library's own retry layer so it cannot compound with the app's reflective retry"
      contains: "timeout="
    - path: "tests/test_stuck_run_recovery.py"
      provides: "SC3 end-to-end integration test: strand a run, sweep it, assert ERROR + error_detail, retrigger it, assert progressing status"
      contains: "def test_stranded_run_swept_and_retriggerable"
  key_links:
    - from: "app/llm/client.py::call_structured"
      to: "openai.OpenAI(...) client construction"
      via: "explicit timeout= AND max_retries=0 kwargs bounding the true worst-case single-stage latency to timeout x 2 app-attempts (not timeout x 3 library-attempts x 2 app-attempts = 6x)"
      pattern: "OpenAI(base_url=cfg.base_url, api_key=cfg.api_key, timeout="
    - from: "app/main.py::STALE_THRESHOLD_SECONDS"
      to: "app/llm/client.py::call_structured timeout"
      via: "the sweep threshold now safely exceeds _STRUCTURED_TIMEOUT_S x 2 (the corrected, max_retries=0-bounded ceiling) with comfortable margin, documented in a code comment at both seams"
      pattern: "STALE_THRESHOLD"
---

<objective>
Close the one open finding RESEARCH.md surfaced beyond the three locked CONTEXT.md
decisions: `call_structured` (the extraction-tier structured-output call) has no
explicit `timeout=` on its `OpenAI(...)` client construction, so the true worst-case
single-stage latency is the `openai` library's own default (10 min timeout ×
`max_retries=2`, i.e. 3 library-level attempts) COMPOUNDED with the app's own
2-attempt reflective retry loop — `10 min × 3 × 2` = 60 min true worst case, not
merely the ~30 min RESEARCH.md's Pitfall 1 first computed (which counted the
library's 3-attempt multiplier but not its compounding with the app's own 2
attempts) and far outside the "90s-3min" landing zone 09-CONTEXT.md's D-9-13
discussion assumed (checker BLOCKER 2). Per RESEARCH.md's Pitfall 1 recommendation
#1 (the preferred resolution), tighten this timeout AND suppress the library's own
retry layer (`max_retries=0`) so the app's single reflective retry is the ONLY
retry multiplier left — collapsing the ceiling to exactly
`_STRUCTURED_TIMEOUT_S × 2`, making the threshold chosen in 09-03 actually safe by
construction rather than by assumption. Then prove DATA-03's full success
criterion end-to-end: a run stranded mid-flight is swept to ERROR and successfully
retriggered, against a real/local Postgres.

Purpose: without this plan, 09-03's threshold constant is either uncomfortably long
(≈60+ min once the compounding is correctly counted, weakening DATA-03's practical
value) or unsafe (short enough to occasionally sweep a legitimately-still-running
extraction). Tightening the LLM client's timeout AND its retry count is the
surgical fix that makes a short, useful threshold correct — closing RESEARCH.md's
Assumption A1 risk explicitly (with the corrected multiplier) rather than leaving
it as an unresolved accepted tension.

Output: `call_structured` gains an explicit, bounded `timeout=` AND
`max_retries=0`; 09-03's threshold constant and its code comment are reconciled
against the now-tightened, correctly-derived ceiling (`_STRUCTURED_TIMEOUT_S × 2`);
`tests/test_stuck_run_recovery.py` proves SC3 end-to-end (strand → sweep → ERROR →
retrigger → progressing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-atomic-data-integrity/09-CONTEXT.md
@.planning/phases/09-atomic-data-integrity/09-RESEARCH.md
@.planning/phases/09-atomic-data-integrity/09-01-SUMMARY.md
@.planning/phases/09-atomic-data-integrity/09-02-SUMMARY.md
@.planning/phases/09-atomic-data-integrity/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit bounded timeout to call_structured; reconcile the sweep threshold</name>
  <files>app/llm/client.py, .env.example, app/main.py, tests/test_llm_client.py</files>
  <read_first>
    app/llm/client.py (already read this session — `call_structured` full body lines
    93-158, specifically the `client = OpenAI(base_url=cfg.base_url,
    api_key=cfg.api_key)` construction at line 110 with NO timeout kwarg AND NO
    `max_retries` kwarg — the `openai` library (2.43.0, pinned) defaults
    `max_retries=2`, applied INDEPENDENTLY, per HTTP call, underneath the app's own
    `for attempt in (1, 2):` reflective-retry loop (lines 120+) — these two retry
    layers COMPOUND: each of the app's 2 attempts can itself spawn up to
    `1 + max_retries` library-level HTTP attempts, so the true worst-case ceiling is
    `timeout × (1 + max_retries) × app_attempts` = `timeout × 3 × 2` = 6× the
    per-request timeout, NOT 2× (checker BLOCKER 2 — do not under-count this);
    `call_text` lines 161-200 which ALREADY has the `timeout_s=`/
    `client_kwargs["timeout"]` precedent at lines 165/183-187 to mirror exactly, but
    which ALSO has no explicit `max_retries=` override (same compounding risk exists
    there, though `call_text` is only ever called with `timeout_s=3.0`, keeping its
    absolute worst case small even at 6×). 09-RESEARCH.md's Pitfall 1 in full (the
    ~30-minute true worst-case ceiling derivation: 10-min library default timeout ×
    3 library attempts, BEFORE even counting the app's own 2 reflective-retry
    attempts) and its "How to avoid" recommendation #1 (set an explicit bounded
    timeout, e.g. 30-60s scaled up from `compose_confirmation`'s `timeout_s=3.0`
    precedent for a heavier structured JSON call). 09-03-SUMMARY.md for the exact
    `STALE_THRESHOLD_SECONDS` value and code-comment wording chosen in that plan —
    this task reconciles that value against the newly-tightened AND correctly-
    multiplied ceiling. tests/test_llm_client.py (grep its existing test shape
    before writing new assertions — match its existing mocking/assertion
    conventions rather than inventing a new style).
  </read_first>
  <action>
    In `app/llm/client.py`'s `call_structured`, add BOTH an explicit `timeout=` kwarg
    AND an explicit `max_retries=0` kwarg to the `OpenAI(base_url=cfg.base_url,
    api_key=cfg.api_key)` construction at line 110 — per checker BLOCKER 2, setting
    `timeout=` alone is NOT sufficient: the `openai` library's own `max_retries=2`
    default compounds with the app's existing `for attempt in (1, 2):` reflective
    retry loop, so without `max_retries=0` the true worst case is `timeout × 3
    library-attempts × 2 app-attempts` = 6× the timeout, not 2×. Setting
    `max_retries=0` makes the app's own single reflective retry the ONLY retry
    layer for this call, collapsing the ceiling to `timeout × 2 app-attempts`
    exactly (D-9-13's bound, made true by construction). Mirror `call_text`'s
    existing `client_kwargs["timeout"] = timeout_s` pattern for the `timeout=`
    kwarg. Choose a concrete numeric timeout value bounded above by RESEARCH.md's
    recommendation (30-60s range, scaled up from `compose_confirmation`'s
    `timeout_s=3.0` for a heavier structured-JSON extraction call that must
    round-trip a full payroll email) — introduce a module-level named constant
    (e.g. `_STRUCTURED_TIMEOUT_S = 45.0`) rather than a bare literal, so the value
    is documented and greppable in one place. Add a code comment directly above the
    constant citing this exact finding: the `openai` library's own defaults (10 min
    timeout × `max_retries=2`, i.e. 3 attempts per call) compounded with the app's 2
    reflective-retry attempts gave a true worst case of `10 min × 3 × 2` = 60 min
    (not merely 30 min) before this change; setting `timeout=_STRUCTURED_TIMEOUT_S`
    AND `max_retries=0` collapses this to exactly
    `_STRUCTURED_TIMEOUT_S × 2 app-attempts`, which is what makes 09-03's
    `STALE_THRESHOLD_SECONDS` value safe BY CONSTRUCTION rather than by assumption.
    Do NOT add a timeout/max_retries override to `suggest_employees`'s or `decide`'s
    calls if they route through `call_text` (already timeout-capable via
    `timeout_s=`, though `call_text` itself has the same un-overridden
    `max_retries=2` default — note this in the code comment as a residual, smaller
    risk since `call_text` call sites always pass a small `timeout_s`, out of scope
    for this task since only `call_structured` feeds the D-9-13 threshold
    derivation) — only `call_structured` (used by `extract()`) is the function this
    task changes; verify by grepping `call_structured(` call sites in
    `app/pipeline/` before concluding scope is exactly this one function. Update
    `.env.example` if a new env var is warranted (Claude's discretion — a hardcoded
    module constant is acceptable and matches `_MAX_TOKENS`'s existing style; only
    add an env var if config-driven tunability is clearly needed, matching the
    project's existing "config-driven where it matters" philosophy from
    CLAUDE.md). Return to `app/main.py`'s `STALE_THRESHOLD`/
    `STALE_THRESHOLD_SECONDS` constant (introduced in 09-03) and update its code
    comment to state the NEW, correctly-derived ceiling this task establishes:
    `_STRUCTURED_TIMEOUT_S × 2` (the app's own 2-attempt reflective retry loop,
    with the library's `max_retries` now forced to 0 so it no longer multiplies
    the ceiling) — if 09-03's threshold value was chosen conservatively assuming
    the untightened ~30-60 min ceiling, this task MAY tighten
    `STALE_THRESHOLD_SECONDS` further now that the true ceiling is much lower, as
    long as the value still exceeds `_STRUCTURED_TIMEOUT_S × 2` with comfortable
    margin (D-9-13's bound). Document whichever choice is made (tighten further, or
    leave as-is with an updated comment) directly in the code. In
    `tests/test_llm_client.py`, add a test asserting `call_structured` constructs
    its `OpenAI` client with BOTH a `timeout` kwarg present and equal to the new
    constant AND `max_retries=0` present (mock/spy on the `OpenAI` class the same
    way the existing tests in that file already do — read the file first to match
    the convention exactly).
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration" tests/test_llm_client.py</automated>
  </verify>
  <done>call_structured passes an explicit, named, documented timeout= AND max_retries=0 to its OpenAI client construction (closing checker BLOCKER 2's compounding-retry gap); the sweep threshold's code comment is reconciled against the correctly-derived ceiling (timeout × 2 app-attempts, not × 6); the offline suite is green.</done>
  <acceptance_criteria>
    - `app/llm/client.py` contains a named constant (not a bare literal) for the structured-call timeout, with a code comment citing the 10-min-default/max_retries=2-compounding-with-2-app-attempts finding (the corrected ~60-min pre-fix ceiling, not merely ~30-min)
    - `call_structured`'s `OpenAI(...)` construction includes both `timeout=<the constant>` AND `max_retries=0`
    - `app/main.py`'s threshold code comment (from 09-03) is updated to reference the now-tightened LLM ceiling as `_STRUCTURED_TIMEOUT_S × 2` (app-attempts only, library retries now suppressed via `max_retries=0`), with an explicit statement of the current margin
    - `uv run pytest -q -m "not integration" tests/test_llm_client.py` exits 0, including a new test asserting BOTH the `timeout=` kwarg AND `max_retries=0` are passed to the `OpenAI(...)` construction
    - `uv run pytest -q -m "not integration"` (full offline suite) exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC3 end-to-end integration test — strand, sweep, retrigger</name>
  <files>tests/test_stuck_run_recovery.py</files>
  <read_first>
    tests/test_stuck_run_recovery.py (created in 09-01 — read its current contents
    first; this task ADDS the end-to-end integration test, replacing the stub from
    09-01 Task 2). app/main.py's `retrigger()` route (already read this session, full
    body lines 534-613) — the EXISTING claim-from-ERROR path this test exercises
    unchanged. app/db/repo.py's `sweep_stranded_runs` (built in 09-01) and `load_run`
    (already read — explicit RUN_COLS projection including `updated_at`,
    `error_reason`, `error_detail`). tests/conftest.py's `seeded_db` fixture
    (already read this session, lines 57-73) — the module-scoped live-DB
    reset+bootstrap+seed fixture this integration test uses.
  </read_first>
  <action>
    Implement `test_stranded_run_swept_and_retriggerable` (or equivalently named) in
    tests/test_stuck_run_recovery.py, replacing the 09-01 stub. Using `seeded_db` +
    a real `repo.get_connection()`: create a run via `repo.create_run(...)`, then
    directly backdate its `updated_at` via a raw UPDATE (older than
    `STALE_THRESHOLD_SECONDS` — import the constant from `app.main`) while its
    status is `extracting` (one of the three swept statuses, D-9-12) — set the
    status via `repo.set_status(run_id, RunStatus.EXTRACTING)` then backdate
    `updated_at` with a direct SQL UPDATE (`UPDATE payroll_runs SET updated_at = now()
    - interval '...' WHERE id = %s`) since no repo helper exposes a raw
    `updated_at` write. Call `repo.sweep_stranded_runs(STALE_THRESHOLD_SECONDS)` and
    assert the run_id is IN the returned list. Reload via `repo.load_run(run_id)` and
    assert `status == "error"`, `error_reason == "StrandedRunSwept"`, and
    `error_detail` is non-null and contains the word "stranded" (case-insensitive) —
    proving the diagnostic sentinel is distinguishable from a real exception-driven
    ERROR. Then call the retrigger logic directly (import and call the same claim
    path `main.py`'s `retrigger()` route uses — `repo.claim_status(run_id,
    RunStatus.ERROR, RunStatus.RECEIVED)`) and assert it returns `True` (the claim
    succeeds) and the reloaded run's status is now `received` — a "progressing"
    state per SC3's wording. Also add (or confirm 09-01 already added) the
    scope-pin unit test asserting `awaiting_reply`/`awaiting_approval`/`approved`
    runs are NEVER sweepable — if 09-01 already covers this via FakeConnection,
    add a SECOND, real-DB confirmation here: create one run in each of the three
    excluded statuses with a backdated `updated_at`, run the sweep, and assert NONE
    of their ids appear in the returned list (closing the loop between the
    SQL-shape unit test from 09-01 and a genuine live-DB behavioral proof).
  </action>
  <verify>
    <automated>uv run pytest -q -m integration tests/test_stuck_run_recovery.py</automated>
  </verify>
  <done>A stranded run is swept to ERROR with the StrandedRunSwept sentinel and a non-null error_detail, then successfully retriggered to received via the existing claim path; the three excluded statuses are proven never-swept against a real DB; the full integration suite for Phase 9 is green.</done>
  <acceptance_criteria>
    - `tests/test_stuck_run_recovery.py` contains `def test_stranded_run_swept_and_retriggerable` marked `@pytest.mark.integration`, using `seeded_db`
    - The test asserts `error_reason == "StrandedRunSwept"` and a non-null `error_detail` containing "stranded" after the sweep
    - The test asserts the swept run is retriggerable via `repo.claim_status(run_id, RunStatus.ERROR, RunStatus.RECEIVED)` returning `True` and the reloaded status is `received`
    - A second test (or an extension of the same test) confirms `awaiting_reply`/`awaiting_approval`/`approved` runs with a backdated `updated_at` are NEVER included in the sweep's returned list, against a real DB
    - `uv run pytest -q -m integration tests/test_stuck_run_recovery.py` exits 0 when `DATABASE_URL`+`ALLOW_DB_RESET=1` set
    - `uv run pytest -q` (full suite, live DB) exits 0 — every Phase 9 integration test (`test_atomic_persist.py`, `test_webhook_dedup_race.py`, `test_stuck_run_recovery.py`) is green together
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App → DeepSeek/Kimi (external LLM providers) | The tightened `timeout=` bounds how long the app trusts a slow/hung provider response before failing the stage — this is a resilience boundary, not a data-trust boundary (the provider output is already validated post-hoc by Pydantic). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Denial of Service (a hung/slow LLM provider stalls a run indefinitely, defeating the sweep's short threshold) | `call_structured`'s `OpenAI` client construction | mitigate | Explicit bounded `timeout=` AND `max_retries=0` (this plan) makes the true worst-case single-stage latency small, known, and NOT compounded by the library's own independent retry layer — the ceiling collapses to `_STRUCTURED_TIMEOUT_S × 2` (app-attempts only), so the sweep threshold (09-03) can be short AND safe — closing RESEARCH.md's Assumption A1 explicitly (with the corrected multiplier, checker BLOCKER 2) rather than leaving it open or under-counted. |
| T-09-14 | Tampering (a swept-to-ERROR run's still-running background task later overwrites ERROR via an unguarded set_status) | The D-9-13 "known accepted tension" | accept | Explicitly accepted per 09-CONTEXT.md — the margin between the tightened LLM timeout and the sweep threshold makes this pathological, not eliminated; Phase 10's concurrency proof is the designated venue for any further guard-hardening evidence, not this phase. |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration"` exits 0 (full offline suite, including the new `call_structured` timeout assertion).
- `uv run pytest -q -m integration` exits 0 when `DATABASE_URL`+`ALLOW_DB_RESET=1` are set — every Phase 9 integration test (SC1 in `test_atomic_persist.py`, SC2 in `test_webhook_dedup_race.py`, SC3 in `test_stuck_run_recovery.py`) green together, proving all three DATA-01/02/03 success criteria end-to-end.
- `uv run pytest -q` (the phase-gate full-suite command per 09-VALIDATION.md) exits 0.
</verification>

<success_criteria>
- RESEARCH.md's Pitfall 1 / Assumption A1 is closed: `call_structured` has an explicit, documented, bounded timeout AND `max_retries=0`, so the library's own retry layer no longer compounds with the app's reflective retry (checker BLOCKER 2).
- The sweep threshold's safety is now provable by construction (`_STRUCTURED_TIMEOUT_S × 2` app-attempts, correctly derived + comfortable margin), not merely assumed and not under-counted.
- SC3 is proven end-to-end: a stranded run is swept to a visible ERROR with a distinguishing sentinel, then successfully retriggered to a progressing state — closing MED-05.
- All three Phase 9 requirements (DATA-01, DATA-02, DATA-03) have a passing integration test proving their respective success criterion against a real/local Postgres connection.
</success_criteria>

<output>
Create `.planning/phases/09-atomic-data-integrity/09-04-SUMMARY.md` when done
</output>
