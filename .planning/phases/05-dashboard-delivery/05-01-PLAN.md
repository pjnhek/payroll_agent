---
phase: 05-dashboard-delivery
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_claim_status.py
  - tests/test_alias_write.py
  - tests/test_validate.py
autonomous: true
requirements:
  - FOUND-04
  - CLAR-04
  - INGEST-05

must_haves:
  truths:
    - "tests/test_claim_status.py exists with failing RED tests for claim_status CAS (True/False returns, SQL shape)"
    - "tests/test_alias_write.py exists with failing RED test that _safe_to_learn_alias('D. Reyes', david, seed_roster) returns False (D-01b D. Reyes trap)"
    - "tests/test_validate.py is extended with failing RED tests for D-05 OT rule (weekly >40, biweekly >85, explicit-zero, no-flag cases)"
    - "Full mocked suite still passes (new tests fail RED on missing implementations, existing 195+ tests remain GREEN)"
  artifacts:
    - path: "tests/test_claim_status.py"
      provides: "Wave 0 test stub for FOUND-04, D-12"
      min_lines: 40
    - path: "tests/test_alias_write.py"
      provides: "Wave 0 test stub for D-01b collision exclusion"
      min_lines: 30
    - path: "tests/test_validate.py"
      provides: "Extended with D-05 OT rule cases"
      contains: "hours_overtime"
  key_links:
    - from: "tests/test_claim_status.py"
      to: "app/db/repo.py"
      via: "import repo.claim_status (will fail red until Wave 1 impl)"
      pattern: "claim_status"
    - from: "tests/test_alias_write.py"
      to: "app/pipeline/reconcile_names.py"
      via: "_safe_to_learn_alias wraps deterministic_match"
      pattern: "_safe_to_learn_alias"
---

## Phase Goal

**As an** operator, **I want to** approve payroll runs through a race-safe gate, **so that** duplicate approvals are structurally impossible and a failed delivery never silently strands a run.

<objective>
Wave 0 test stubs — batch A. Three test files written RED-first (failing because the implementations do not yet exist). These establish the verification contract for the highest-risk units: the claim_status CAS helper (FOUND-04/D-12), the alias-write collision guard (D-01b), and the over-40-no-OT validation rule (D-05).

Purpose: Nyquist compliance requires that these tests exist and fail BEFORE the implementation waves build the code they verify. Writing them first forces a concrete, checkable spec for the two correctness-critical units (D-12 race-safe CAS; D-01b D. Reyes money-misroute guard).

Output: Three test files. All new tests fail RED (ImportError or assertion failure). Existing test suite remains GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-delivery/05-CONTEXT.md
@.planning/phases/05-dashboard-delivery/05-RESEARCH.md
@.planning/phases/05-dashboard-delivery/05-PATTERNS.md
@.planning/phases/05-dashboard-delivery/05-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 — test_claim_status.py + test_alias_write.py stubs (RED)</name>
  <files>tests/test_claim_status.py, tests/test_alias_write.py</files>
  <read_first>
    - tests/test_clarify.py (FakeConnection pattern — copy the FakeConnection/FakeCursor fixture that's already used for offline SQL assertion; tests/conftest.py may also have it)
    - app/db/repo.py:267-279 (set_status — claim_status mirrors this exactly; also lines 86-94 _TERMINAL_STATUSES, lines 97-104 _conn_ctx/_nulltx pattern)
    - app/pipeline/reconcile_names.py:37-81 (deterministic_match collision logic — _safe_to_learn_alias wraps this)
    - app/db/seed.py:134-150 and 245-261 (David Reyes e0000003 and Daniel Reyes e0000007 both carrying "D. Reyes" — the D-01b canonical trap)
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 34-85 (claim_status analog) and lines 383-431 (_safe_to_learn_alias analog)
    - .planning/phases/05-dashboard-delivery/05-VALIDATION.md (per-task verification map — FOUND-04 and D-01b rows)
  </read_first>
  <behavior>
    test_claim_status.py — unit tests (no live DB, use FakeConnection):
    - test_claim_status_returns_true_when_row_returned: FakeConnection with fetchone returning a row → claim_status(run_id, AWAITING_APPROVAL, APPROVED, conn=fake) returns True
    - test_claim_status_returns_false_when_no_row: FakeConnection with fetchone returning None → claim_status(...) returns False
    - test_claim_status_sql_contains_where_status_and_returning: assert the SQL executed by FakeConnection contains both "AND status = %s" and "RETURNING id" (the CAS predicate shape)
    - test_claim_status_invariant_doc_updated: grep app/db/repo.py for "two writers" — assert it contains the updated invariant doc string (planted here so the executor knows to update the docstring in Wave 1)
    - Integration variant (mark with @pytest.mark.integration): skip unless live DB available; assert two concurrent claim calls → exactly one True

    test_alias_write.py — unit tests:
    - test_safe_to_learn_alias_refuses_d_reyes_for_david: given the seed roster (David Reyes e0000003 AND Daniel Reyes e0000007 both already carrying "D. Reyes"), calling _safe_to_learn_alias("D. Reyes", david_employee, seed_roster) returns False
    - test_safe_to_learn_alias_accepts_unambiguous_token: a token that only matches one employee (e.g. "David Reyez" → only David Reyes via the synthetic alias) returns True
    - test_safe_to_learn_alias_idempotent: token already in target employee's known_aliases → safe check still passes (not a collision against self)
  </behavior>
  <action>
    Write both test files as pure stubs that will fail RED (ImportError on missing claim_status and _safe_to_learn_alias).

    For test_claim_status.py:
    - Import: `from app.db import repo` and `from app.models.status import RunStatus`
    - Use a FakeConnection stub (copy the existing pattern from conftest.py or test_clarify.py — do NOT duplicate if a shared fixture exists; import from conftest)
    - The FakeConnection must support `fetchone_returns` so the test can control whether a row is returned
    - Do NOT write a real DB connection; all unit tests use FakeConnection with conn= parameter injection
    - Mark integration test: `@pytest.mark.integration` on the two-connection race test; add a skip condition if no live DB env var

    For test_alias_write.py:
    - Import: `from app.pipeline.reconcile_names import _safe_to_learn_alias` (this import will fail RED until Wave 4 adds it — that is expected; the test file itself is the Wave 0 artifact)
    - Build a minimal seed_roster fixture inline (or import from conftest if roster_from_seed is already there) that includes both David Reyes (e0000003, known_aliases=["D. Reyes"]) and Daniel Reyes (e0000007, known_aliases=["D. Reyes"])
    - Use real Employee/Roster Pydantic models from app/models/roster.py

    Per PATTERNS.md lines 383-431: _safe_to_learn_alias will live in app/pipeline/reconcile_names.py. The import path in the test reflects this.
    Per D-01b: the "D. Reyes" trap MUST be an explicit named test case — not just an edge case, a first-class requirement.
    Per VALIDATION.md: the mocked suite command is `uv run pytest -q -m "not integration and not live_llm"` — these new tests must not add live-DB or live-LLM dependencies to the mocked run.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_claim_status.py tests/test_alias_write.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. Running them produces ImportError or NameError (missing implementations) — RED is expected and correct. The existing suite still passes: `uv run pytest -q -m "not integration and not live_llm" --ignore=tests/test_claim_status.py --ignore=tests/test_alias_write.py` exits 0.</done>
</task>

<task type="tdd">
  <name>Task 2: Wave 0 — extend test_validate.py with D-05 OT rule cases (RED)</name>
  <files>tests/test_validate.py</files>
  <read_first>
    - tests/test_validate.py (FULL FILE — read the existing test structure before adding; do not duplicate fixtures or imports already present)
    - app/pipeline/validate.py:50-82 (current validate() seam — the pure function being extended; read the existing _employee_pay_type helper at lines 36-47 and the _HOURS_FIELDS constant)
    - app/db/seed.py:79-100 (Maria Chen — weekly employee pay_periods_per_year=52, hourly) and lines 138-155 (Thomas Bergmann — biweekly, pay_periods_per_year=26)
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 232-316 (validate.py extension pattern — _employee_pay_periods_per_year helper analog + OT rule loop)
    - .planning/phases/05-dashboard-delivery/05-VALIDATION.md (D-05 rows in the per-task verification map — 5 cases)
    - app/models/contracts.py:60 (ExtractedEmployee — hours_overtime field; check if it is Optional)
    - app/models/roster.py:26 (Employee — pay_periods_per_year field name)
  </read_first>
  <behavior>
    D-05 OT rule cases to add to existing test_validate.py:
    - test_ot_rule_weekly_flagged: weekly employee (pay_periods_per_year=52, hourly) with hours_regular=45 and hours_overtime=None → validate() emits at least one ValidationIssue whose message contains "overtime"
    - test_ot_rule_biweekly_flagged: biweekly employee (pay_periods_per_year=26) with hours_regular=85 and hours_overtime=None → validate() emits ValidationIssue
    - test_ot_rule_biweekly_not_flagged_below_threshold: biweekly employee with hours_regular=78 (below 80) and no OT → validate() emits NO OT-related issue
    - test_ot_rule_no_flag_semimonthly: semi-monthly employee (pay_periods_per_year=24) with hours_regular=100 and no OT → no flag (documented limitation)
    - test_ot_rule_explicit_zero_flagged: weekly employee with hours_regular=45 AND hours_overtime=0 (explicit zero, not None) → validate() DOES emit a ValidationIssue (per D-05 recommended decision: treat explicit 0 same as absent)
  </behavior>
  <action>
    EXTEND tests/test_validate.py — do NOT rewrite; ADD the five new test functions at the bottom of the file after the existing tests.

    Build minimal Employee + NameMatchResult fixtures inline (reuse the roster_from_seed fixture if it is already in conftest.py or test_validate.py — read the file first). If the existing test file builds mock employees ad-hoc, follow that exact same pattern.

    The new OT tests will produce RED failures because validate.py does not yet have the _employee_pay_periods_per_year helper or the OT rule loop (those land in Wave 1 Plan 03). This RED state is the correct Wave 0 outcome.

    CRITICAL: Do NOT change any existing tests. Only add new test functions. Verify existing tests still pass after the extension: `uv run pytest tests/test_validate.py -q -k "not ot_rule"` must exit 0.

    Per D-05 edge: explicitly-zero hours_overtime (Decimal("0") or 0) must trigger the flag — the test_ot_rule_explicit_zero_flagged case puts this on record per the "recommended: flag it" decision.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_validate.py -q -k "not ot_rule" 2>&1 | tail -5 && echo "--- existing pass; new OT tests expected RED ---" && uv run pytest tests/test_validate.py -q -k "ot_rule" 2>&1 | tail -10</automated>
  </verify>
  <done>Existing test_validate.py tests still pass (no regressions). The 5 new ot_rule tests fail RED (the OT rule helper does not yet exist in validate.py). test_validate.py contains the string "hours_overtime" and at least 5 new test functions prefixed with "test_ot_rule_".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test stubs → production imports | Tests import from app.* — only test files modified in this plan, no production code touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | test_claim_status.py — CAS SQL predicate | mitigate | Test asserts the exact `AND status = %s RETURNING id` SQL shape so a regression that drops the predicate is caught immediately |
| T-05-02 | Tampering | test_alias_write.py — D. Reyes trap | mitigate | Named test explicitly verifies the D. Reyes money-misroute scenario; any weakening of the collision guard fails this test |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; uv.lock unchanged |
</threat_model>

<verification>
`uv run pytest tests/test_claim_status.py tests/test_alias_write.py tests/test_validate.py -q 2>&1 | tail -20` — new tests fail RED on missing implementations.
`uv run pytest -q -m "not integration and not live_llm" --ignore=tests/test_claim_status.py --ignore=tests/test_alias_write.py -k "not ot_rule" 2>&1 | tail -5` — existing suite stays GREEN.
</verification>

<success_criteria>
- tests/test_claim_status.py contains: claim_status CAS True/False unit tests + SQL shape assertion + @pytest.mark.integration race variant
- tests/test_alias_write.py contains: D. Reyes trap test (asserts False) + unambiguous token test (asserts True) + idempotency test
- tests/test_validate.py extended with 5 D-05 OT-rule test functions (all RED on missing impl)
- All existing tests (195+) still GREEN after the extensions
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-delivery/05-01-SUMMARY.md` when done.
</output>
