---
phase: 05-dashboard-delivery
plan: 02
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/test_pdf.py
  - tests/test_compose_confirmation.py
  - tests/test_delivery.py
  - tests/test_dashboard.py
  - tests/conftest.py
autonomous: true
requirements:
  - HITL-02
  - HITL-03
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - CLAR-04
  - INGEST-05

must_haves:
  truths:
    - "tests/test_pdf.py exists with RED tests asserting generate_paystub_pdf() returns bytes starting with b'%PDF'"
    - "tests/test_compose_confirmation.py exists with RED tests asserting the template floor fires when LLM raises or returns None; fake-LLM stubs accept **kwargs so timeout_s does not break them (finding MEDIUM)"
    - "tests/test_delivery.py exists with RED tests for D-13b error boundary (exception after claim → run advances to ERROR, not stuck in approved)"
    - "tests/test_delivery.py RED test for CLAR-04 purpose-aware idempotent send (confirmation guard uses purpose='confirmation' — finding HIGH #1)"
    - "tests/test_delivery.py RED test for _clarify idempotency (clarification send guard uses purpose='clarification' — finding HIGH #2)"
    - "tests/test_delivery.py RED test for stale-state recovery: retrigger can claim from stale 'extracting'/'computed'/'received'/'sent' states with staleness threshold (finding HIGH #6); plus test_two_concurrent_stale_retriggers_only_one_wins (R2-HIGH stale CAS exclusivity) [computed, not computing — no such status]"
    - "tests/test_dashboard.py exists with RED behavior smoke tests for GET /runs, GET /runs/{id}, GET /eval, POST /demo/send-test; includes test that /demo/send-test mints fresh Message-ID per click (finding MEDIUM)"
    - "tests/conftest.py extended with seed_roster fixture including David+Daniel Reyes collision pair if not already present"
  artifacts:
    - path: "tests/test_pdf.py"
      provides: "Wave 0 stub for HITL-03"
      contains: "b'%PDF'"
    - path: "tests/test_compose_confirmation.py"
      provides: "Wave 0 stub for HITL-02 template floor + timeout_s **kwargs compatibility"
      contains: "compose_confirmation"
    - path: "tests/test_delivery.py"
      provides: "Wave 0 stub for D-13b, CLAR-04 (purpose-aware), INGEST-05, _clarify idempotency, stale-state recovery"
      contains: "purpose"
    - path: "tests/test_dashboard.py"
      provides: "Wave 0 stub for DASH-01/02/03/04/05 + Message-ID uniqueness"
      contains: "/runs"
  key_links:
    - from: "tests/test_pdf.py"
      to: "app/pipeline/pdf.py"
      via: "import generate_paystub_pdf (fails RED until Wave 2)"
      pattern: "generate_paystub_pdf"
    - from: "tests/test_compose_confirmation.py"
      to: "app/pipeline/compose_email.py"
      via: "import compose_confirmation (fails RED until Wave 2)"
      pattern: "compose_confirmation"
    - from: "tests/test_delivery.py"
      to: "app/db/repo.py"
      via: "import claim_status + get_outbound_message_id (fails RED until Wave 1)"
      pattern: "claim_status"
    - from: "tests/test_dashboard.py"
      to: "app/main.py"
      via: "FastAPI TestClient — routes do not yet exist until Wave 3"
      pattern: "GET /runs"
---

## Phase Goal

**As an** operator, **I want to** approve payroll runs through a race-safe gate, **so that** duplicate approvals are structurally impossible and a failed delivery never silently strands a run.

<objective>
Wave 0 test stubs — batch B. Four new test files that will fail RED because the production implementations don't yet exist (pdf.py, compose_confirmation, delivery path, dashboard routes). Plus conftest.py extension for shared fixtures.

This batch incorporates all Codex review-finding stubs not covered by Plan 01:
- Purpose-aware outbound idempotency (finding #1): test_delivery.py stubs get_outbound_message_id(run_id, purpose='confirmation') call
- _clarify idempotency (finding #2): test_delivery.py stub asserts duplicate clarification send skipped
- compose_confirmation fake-LLM **kwargs fix (MEDIUM): fake-LLM stubs accept **kwargs so timeout_s does not raise TypeError
- Demo Message-ID uniqueness (MEDIUM): test_dashboard.py stub asserts /demo/send-test never reuses the same Message-ID across two clicks
- Stale-state recovery (finding #6): test_delivery.py stub for claim from stale in-flight states

Purpose: Completing the Nyquist Wave 0 contract. These stubs specify the expected behavior of the delivery pipeline (HITL-02/03, D-13b, CLAR-04, INGEST-05) and the dashboard routes (DASH-01..05) before any production code is written.

Output: Four test files + conftest extension. All new tests fail RED. Existing suite stays GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-delivery/05-CONTEXT.md
@.planning/phases/05-dashboard-delivery/05-RESEARCH.md
@.planning/phases/05-dashboard-delivery/05-PATTERNS.md
@.planning/phases/05-dashboard-delivery/05-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Wave 0 — test_pdf.py + test_compose_confirmation.py stubs (RED)</name>
  <files>tests/test_pdf.py, tests/test_compose_confirmation.py</files>
  <read_first>
    - tests/test_clarify.py (FULL FILE — _DraftLLM + _RaisingDraftLLM stub pattern at lines 31-44; compose_confirmation mirrors compose_clarification, so test_compose_confirmation mirrors test_clarify exactly)
    - app/pipeline/compose_email.py:88-132 (compose_clarification — the exact pattern compose_confirmation clones; read the try/except + fallback structure to know what behaviors to test; read call_text call signature including any timeout_s parameter)
    - app/models/contracts.py:149-184 (PaystubLineItem fields — the data in → bytes out for PDF; read all fields available for the test fixture)
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 320-380 (pdf.py analog — pure-stage seam pattern, generate_paystub_pdf signature)
    - .planning/phases/05-dashboard-delivery/05-RESEARCH.md lines 376-422 (D-11 PaystubLineItem fields confirmed; reportlab BytesIO pattern; generate_paystub_pdf signature with pay_period_start/end params)
    - .planning/phases/05-dashboard-delivery/05-VALIDATION.md (HITL-02 and HITL-03 rows in the per-task verification map)
  </read_first>
  <behavior>
    test_pdf.py:
    - test_generate_paystub_pdf_returns_bytes: call generate_paystub_pdf(item, "Test Employee", pay_period_start, pay_period_end) with a minimal PaystubLineItem fixture → returns non-empty bytes
    - test_generate_paystub_pdf_valid_pdf_magic: the bytes returned start with b'%PDF' (valid PDF magic bytes)
    - test_generate_paystub_pdf_pure_no_db: call with no DB connection available; must not raise (pure function, no DB calls)

    test_compose_confirmation.py (mirror of test_clarify.py structure):
    - test_compose_confirmation_template_floor_on_llm_exception: patch llm.call_text to raise RuntimeError → compose_confirmation(paystubs, run, llm=raising_llm) returns a non-empty string (the template floor, not an exception)
    - test_compose_confirmation_template_floor_on_empty_draft: patch llm.call_text to return "" or None → compose_confirmation returns the floor string (not empty)
    - test_compose_confirmation_uses_draft_when_present: patch llm.call_text to return "custom body" → compose_confirmation returns "custom body"
    - test_confirmation_floor_contains_net_pay: floor string contains each employee's net_pay formatted as a dollar amount

    CRITICAL fake-LLM **kwargs fix (MEDIUM finding): the _DraftLLM and _RaisingDraftLLM stubs in test_compose_confirmation.py MUST define call_text with `**kwargs` in their signature — i.e., `def call_text(self, tier, messages, **kwargs)`. This is load-bearing: compose_confirmation passes `timeout_s=3.0` as a keyword argument; a stub without **kwargs raises TypeError and forces the fallback to fire in the "uses draft when present" test, producing a false positive failure. The test_compose_confirmation_uses_draft_when_present case is the one that would spuriously fail if the stub lacks **kwargs.
  </behavior>
  <action>
    For test_pdf.py:
    - Import: `from app.pipeline.pdf import generate_paystub_pdf` (will fail RED until Wave 2 creates app/pipeline/pdf.py)
    - Build a minimal PaystubLineItem fixture using real Pydantic model; use Decimal("0") for all monetary fields, minimal integer hours
    - The test must call the pure function with no side effects — no DB, no file I/O after the test

    For test_compose_confirmation.py:
    - Import: `from app.pipeline.compose_email import compose_confirmation` (will fail RED until Wave 2)
    - Copy the _DraftLLM and _RaisingDraftLLM stubs verbatim from tests/test_clarify.py (or import from conftest if already there — check conftest first)
    - MANDATORY CHANGE from the test_clarify.py pattern: both _DraftLLM and _RaisingDraftLLM must define `def call_text(self, tier, messages, **kwargs)` with `**kwargs` in the signature. This absorbs `timeout_s=3.0` without raising TypeError (MEDIUM finding fix — compose_confirmation passes this kwarg, the fake must accept it).
    - The run fixture is a minimal dict with keys business_name and pay_period_label (the template floor uses these per COPYWRITING CONTRACT in 05-UI-SPEC.md)
    - The paystubs fixture is a list of 1-2 minimal PaystubLineItem objects with net_pay set to a readable value (e.g. Decimal("1234.56"))

    NEVER duplicate the _DraftLLM stub if it can be imported from conftest. Read conftest.py first to check. If conftest has a shared stub, ensure IT also accepts **kwargs — update conftest if needed.
    Do NOT add any live-LLM or live-DB dependencies to these tests.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_pdf.py tests/test_compose_confirmation.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>Both test files exist and fail RED (ImportError on missing modules). File test_pdf.py contains "b'%PDF'". File test_compose_confirmation.py contains "compose_confirmation" and at least one test asserting the template floor on LLM failure. Both _DraftLLM and _RaisingDraftLLM stubs define call_text with **kwargs. Existing suite still GREEN when these files are excluded.</done>
</task>

<task type="tdd">
  <name>Task 2: Wave 0 — test_delivery.py + test_dashboard.py stubs + conftest extension (RED)</name>
  <files>tests/test_delivery.py, tests/test_dashboard.py, tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (FULL FILE — check what fixtures already exist, especially roster_from_seed and FakeConnection; only add what is missing)
    - tests/test_clarify.py (FakeConnection pattern — if not in conftest, the pattern to use for offline SQL assertion)
    - app/main.py (FULL FILE — current routes; TestClient import pattern; FastAPI app instance name; existing test_hitl.py may show the TestClient setup pattern already used in this project)
    - app/db/repo.py:86-94 (_TERMINAL_STATUSES — the test_delivery.py D-13b test must assert that approved is NOT in _TERMINAL_STATUSES after Wave 1; stub this assertion now)
    - app/db/repo.py:282-316 (record_run_error — what it writes; the D-13b test checks this fires after a delivery exception)
    - app/db/repo.py:467-484 (get_outbound_message_id — the purpose-aware signature change lands in Plan 03; test stubs here must reference the NEW signature get_outbound_message_id(run_id, purpose='confirmation'))
    - app/db/schema.sql:129-142 (email_messages table — direction CHECK constraint: only 'inbound' or 'outbound' are valid; 'outbound-pending' is SCHEMA-INVALID per the verified constraint; purpose and send_state columns do not yet exist — they land in Plan 03 DDL)
    - .planning/phases/05-dashboard-delivery/05-VALIDATION.md (DASH-01/02/03/04/05, D-13b, CLAR-04, INGEST-05 rows)
    - app/db/seed.py:134-150 and 245-261 (David e0000003 + Daniel e0000007 — for conftest seed_roster if it needs the collision pair)
  </read_first>
  <behavior>
    test_delivery.py (D-13b, CLAR-04, INGEST-05, finding #1 purpose-aware, finding #2 _clarify idempotency, finding #6 stale-state):
    - test_approved_not_in_terminal_statuses: assert RunStatus.APPROVED not in repo._TERMINAL_STATUSES (will PASS after Wave 1 removes it; stub it now as RED since current code has approved as terminal)
    - test_delivery_error_converts_approved_to_error: simulate a delivery failure after claim → after Wave 1+2 implement _deliver, verify exception in _deliver → record_run_error called → run transitions to ERROR not stuck in approved (use FakeConnection + monkeypatch)
    - test_idempotent_confirmation_skips_if_confirmation_outbound_exists: when get_outbound_message_id(run_id, purpose='confirmation') returns an existing message_id, the delivery path skips send and advances directly (CLAR-04 guard — NOTE: signature uses purpose='confirmation' to distinguish from a prior clarification row; this is the finding #1 fix)
    - test_clarify_idempotency_skips_if_clarification_already_sent: when get_outbound_message_id(run_id, purpose='clarification') returns an existing row, re-calling _clarify does NOT call gateway.send_outbound (finding #2 — idempotent clarification send; will fail RED until Wave 3 Plan 05 Task 1 adds the guard to _clarify)
    - test_retrigger_claims_from_error_state: claim_status(run_id, ERROR, RECEIVED) → returns True when run is in error state
    - test_retrigger_claims_from_approved_state: claim_status(run_id, APPROVED, RECEIVED) → returns True when run is in approved-but-delivery-died state (D-13b)
    - test_retrigger_claims_from_stale_extracting_state: when a run is stuck in 'extracting' with updated_at older than a staleness threshold (e.g. 5 minutes), the retrigger route can claim it → assert claim_status(run_id, EXTRACTING, RECEIVED) succeeds (finding #6 stale-state recovery; guards against force-restarting a fresh in-flight run; the staleness check is in the route handler, not in claim_status itself)

    test_dashboard.py (DASH-01/02/03/04/05 route smoke tests + Message-ID uniqueness):
    - test_runs_list_returns_200: GET /runs → 200 (will be RED until Wave 3 adds the route)
    - test_run_detail_returns_200: GET /runs/{valid_uuid} → 200 or 404 for non-existent run (will be RED until Wave 3)
    - test_eval_view_returns_200: GET /eval → 200 and response contains "chart.svg" (will be RED until Wave 3)
    - test_send_test_returns_303: POST /demo/send-test → 303 redirect (will be RED until Wave 3)
    - test_eval_returns_200_no_summary_json: GET /eval with no eval/summary.json → 200 with "No eval results" message
    - test_send_test_mints_fresh_message_id_each_click: two consecutive POST /demo/send-test calls must produce different Message-IDs (the uq_message_id unique constraint drops repeat clicks if the same fixture Message-ID is reused; the route must mint a fresh synthetic ID per click, finding MEDIUM; will be RED until Wave 3 Plan 06 implements this)

    conftest.py extension (ONLY ADD, do not remove existing fixtures):
    - If FakeConnection is not already in conftest.py, add it (copy from test_clarify.py or wherever it lives)
    - If roster_from_seed is not already in conftest.py, add a @pytest.fixture that builds a Roster with at minimum: David Reyes (e0000003, known_aliases=["D. Reyes"], pay_periods_per_year=52) and Daniel Reyes (e0000007, known_aliases=["D. Reyes"], pay_periods_per_year=52) and Maria Chen (e0000001, known_aliases=[], pay_periods_per_year=52, pay_type=hourly)
  </behavior>
  <action>
    For test_delivery.py:
    - Import from app.db.repo: claim_status, _TERMINAL_STATUSES, record_run_error, get_outbound_message_id
    - Import from app.models.status: RunStatus
    - Use FakeConnection from conftest (or inline if conftest does not have it)
    - Do NOT call real DB in unit variants — use FakeConnection with conn= parameter injection
    - The D-13b test: monkeypatch the delivery callable (or use a FakeConnection that raises on PDF step) then assert record_run_error was called with `type(exc).__name__` (per PII-safe logging rule: D-A1-03 pattern, orchestrator.py:68)
    - test_idempotent_confirmation_skips_if_confirmation_outbound_exists: call get_outbound_message_id(run_id, purpose='confirmation') — note this signature change is the CORE of finding #1. The stub references the new signature even though the implementation doesn't change until Plan 03 adds the purpose column. The test will fail RED on the wrong signature until Plan 03 ships.
    - test_retrigger_claims_from_stale_extracting_state: comment "# Staleness threshold check is in the route handler (updated_at < now() - interval '5 minutes'); claim_status itself is unchanged. Plan 05 Task 2 adds the staleness gate in the retrigger route."

    For test_dashboard.py:
    - Import: `from fastapi.testclient import TestClient` and `from app.main import app`
    - `client = TestClient(app)` at module level or in a fixture
    - These tests will fail RED because /runs, /eval, /demo/send-test routes do not yet exist in main.py — that is the correct Wave 0 state
    - Mark route tests needing a live DB as `@pytest.mark.integration`; GET /eval and the no-summary test can run without a live DB if the route handles the missing file gracefully
    - test_send_test_mints_fresh_message_id_each_click: call POST /demo/send-test twice; assert the two runs created have different message_id values (query the DB via repo after each POST; mark @pytest.mark.integration since it needs a live DB to check the message_ids)

    For conftest.py:
    - Read the full file FIRST to avoid duplication
    - Only add fixtures that are genuinely absent
    - The roster_from_seed fixture must include the David+Daniel Reyes collision pair with both carrying "D. Reyes" in known_aliases — this is required for test_alias_write.py (Plan 01 Task 1)

    Security: test_dashboard.py uses FastAPI UUID path param validation — verify by checking that GET /runs/not-a-uuid returns 422 (test this if the route exists; skip until Wave 3 otherwise).
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_delivery.py tests/test_dashboard.py -q 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist and fail RED (missing routes/implementations). tests/conftest.py contains roster_from_seed fixture with David+Daniel Reyes collision pair. tests/test_delivery.py contains "purpose='confirmation'" and "purpose='clarification'" and "record_run_error" and "stale_extracting". tests/test_dashboard.py contains "GET /runs", "chart.svg", and "fresh_message_id". Existing mocked suite still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TestClient → FastAPI routes | Test stubs use TestClient; routes do not yet exist; stubs document expected behavior only |
| FakeConnection → psycopg3 | Unit tests use FakeConnection to assert SQL shape without a live DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Information Disclosure | test_delivery.py — PII in error_reason | mitigate | D-13b test asserts error_reason is `type(exc).__name__` only (not `str(exc)`) — pins the PII-safe logging rule from D-A1-03 |
| T-05-04 | Repudiation | test_delivery.py — double-approval race | mitigate | test_retrigger_claims_from_approved_state verifies the recovery path from a claimed-but-not-delivered state; paired with test_claim_status.py unit tests from Plan 01 |
| T-05-04b | Integrity | test_delivery.py — purpose-blind idempotency | mitigate | test_idempotent_confirmation_skips_if_confirmation_outbound_exists pins purpose='confirmation' call; a future regression to purpose-blind call would fail this test (finding #1) |
| T-05-04c | Availability | test_delivery.py — clarify double-send | mitigate | test_clarify_idempotency_skips_if_clarification_already_sent pins purpose='clarification' guard; prevents duplicate clarification on re-trigger (finding #2) |
| T-05-05 | Tampering | test_dashboard.py — SQLi via run_id | mitigate | Test document asserts that FastAPI UUID path validation rejects non-UUID strings (422 expected) — planted as a behavior assertion for Wave 3 |
| T-05-05b | Integrity | test_dashboard.py — Message-ID reuse | mitigate | test_send_test_mints_fresh_message_id_each_click pins that /demo/send-test mints a new ID per click; prevents the uq_message_id constraint from silently dropping a demo click (MEDIUM finding) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages; test infrastructure uses only existing pytest dep |
</threat_model>

<verification>
`uv run pytest tests/test_pdf.py tests/test_compose_confirmation.py tests/test_delivery.py tests/test_dashboard.py -q 2>&1 | tail -20` — all new tests fail RED (expected).
`uv run pytest -q -m "not integration and not live_llm" --ignore=tests/test_pdf.py --ignore=tests/test_compose_confirmation.py --ignore=tests/test_delivery.py --ignore=tests/test_dashboard.py 2>&1 | tail -5` — existing suite stays GREEN.
</verification>

<success_criteria>
- tests/test_pdf.py: imports generate_paystub_pdf, asserts b'%PDF' magic bytes, RED on missing app/pipeline/pdf.py
- tests/test_compose_confirmation.py: mirrors test_clarify.py structure; asserts template floor on LLM exception/None; both fake-LLM stubs define call_text with **kwargs (MEDIUM finding fix); RED on missing compose_confirmation
- tests/test_delivery.py: asserts APPROVED not in _TERMINAL_STATUSES (RED now); D-13b error boundary stub; CLAR-04 purpose-aware confirmation guard (purpose='confirmation'); _clarify idempotency guard (purpose='clarification'); INGEST-05 retrigger from both error and approved states; stale-state recovery stub (finding #6)
- tests/test_dashboard.py: TestClient GET /runs → RED (route missing); GET /eval → RED; POST /demo/send-test → RED; fresh-Message-ID-per-click stub
- tests/conftest.py: roster_from_seed fixture with David Reyes (known_aliases=["D. Reyes"]) AND Daniel Reyes (known_aliases=["D. Reyes"]) both present
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-delivery/05-02-SUMMARY.md` when done.
</output>
