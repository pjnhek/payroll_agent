---
phase: 05-dashboard-delivery
plan: 07
type: execute
wave: 4
depends_on:
  - "05-05"
  - "05-03"
files_modified:
  - app/pipeline/reconcile_names.py
  - app/pipeline/orchestrator.py
  - app/db/repo.py
autonomous: true
requirements:
  - DASH-01
  - DASH-02

must_haves:
  truths:
    - "_safe_to_learn_alias('D. Reyes', david_employee, seed_roster) returns False (D-01b D. Reyes trap — the write-side collision guard refuses ambiguous tokens)"
    - "_safe_to_learn_alias('Dave Reyez', david_employee, seed_roster) returns True when only David resolves to it (unambiguous token is safe to learn)"
    - "At clarify-emit time in _clarify, alias_candidates {original_token: None} is written to payroll_runs.alias_candidates column (D-04 — captured before send, keyed by original unresolved token)"
    - "At resume time, alias_candidates dict is updated with {original_token: resolved_employee_id_str} when the reply resolves the name"
    - "At approval time, _write_aliases_if_safe reads alias_candidates, calls _safe_to_learn_alias for each candidate, and appends only unambiguous tokens to employees.known_aliases via update_known_alias (D-01, D-02)"
    - "tests/test_alias_write.py passes GREEN"
  artifacts:
    - path: "app/pipeline/reconcile_names.py"
      provides: "_safe_to_learn_alias helper"
      contains: "def _safe_to_learn_alias("
    - path: "app/pipeline/orchestrator.py"
      provides: "_clarify alias_candidates capture + resume_pipeline alias_candidates update + _deliver alias write call"
      contains: "alias_candidates"
    - path: "app/db/repo.py"
      provides: "update_known_alias idempotent JSONB append"
      contains: "def update_known_alias("
  key_links:
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_alias_candidates"
      via: "captures {original_token: None} at clarify-emit time (D-04 TIMING constraint)"
      pattern: "alias_candidates"
    - from: "app/pipeline/orchestrator.py:resume_pipeline"
      to: "app/db/repo.py:set_alias_candidates"
      via: "updates alias_candidates with resolved employee_id after resume _run_stages"
      pattern: "alias_candidates"
    - from: "app/pipeline/orchestrator.py:_deliver"
      to: "app/pipeline/reconcile_names.py:_safe_to_learn_alias"
      via: "collision guard before update_known_alias write (D-01b)"
      pattern: "_safe_to_learn_alias"
    - from: "app/pipeline/reconcile_names.py:_safe_to_learn_alias"
      to: "app/pipeline/reconcile_names.py:deterministic_match"
      via: "synthetic roster with alias appended; collision detected if deterministic_match returns None"
      pattern: "deterministic_match"
---

## Phase Goal

**As an** operator, **I want to** approve payroll runs through a race-safe gate, **so that** duplicate approvals are structurally impossible and a failed delivery never silently strands a run.

<objective>
The alias WRITE-side learning loop slice (Beat 3 — D-15 independently droppable). When the operator approves a resolved run, the original unresolved shorthand that the client's reply corrected is learned — but ONLY if it is unambiguous across the full roster. "Clarifies once, then learns."

This is the phase's most schedule-risky item (D-15) and is structured as a fully independent Wave 4 that can be dropped without affecting Beats 1 and 2. The executor should confirm time budget is adequate before starting.

Three interconnected changes:
1. `_safe_to_learn_alias` helper in reconcile_names.py (the write-side collision guard, D-01b)
2. alias_candidates capture in `_clarify` + update at resume (D-04 timing constraint: captured at clarify-emit, bound at resume)
3. `_write_aliases_if_safe` called in `_deliver` + `update_known_alias` repo helper (D-01, D-02)

After this plan: Beat 3 is live — approve a resolved run → "David Reyez" is learned as an alias for David Reyes → the next run with "David Reyez" resolves at the stored-alias fast-path with no clarification. The D. Reyes trap is blocked by the write-side collision guard.

Purpose: Complete the "clarifies once, then learns" narrative payoff. Demo's third beat.
Output: reconcile_names.py (_safe_to_learn_alias), orchestrator.py (_clarify + resume updates + _deliver hook), repo.py (update_known_alias).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-dashboard-delivery/05-CONTEXT.md
@.planning/phases/05-dashboard-delivery/05-RESEARCH.md
@.planning/phases/05-dashboard-delivery/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _safe_to_learn_alias + update_known_alias (reconcile_names.py + repo.py)</name>
  <files>app/pipeline/reconcile_names.py, app/db/repo.py</files>
  <read_first>
    - app/pipeline/reconcile_names.py (FULL FILE — deterministic_match at lines 37-81 (the exact function _safe_to_learn_alias wraps); _norm helper; NameMatchResult model usage; existing imports)
    - app/db/repo.py (FULL FILE — set_alias_candidates already added in Plan 03; replace_line_items at line 372 as the CRUD write pattern for update_known_alias; _conn_ctx + _nulltx at lines 97-104; existing imports — check if json already imported)
    - app/models/roster.py:26 (Employee model — known_aliases: list[str] field; full_name; id)
    - app/db/seed.py:134-150 (David Reyes e0000003, known_aliases=["D. Reyes"]) and 245-261 (Daniel Reyes e0000007, known_aliases=["D. Reyes"]) — the trap
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 383-431 (_safe_to_learn_alias pattern — synthetic roster + deterministic_match delegation; exact code in PATTERNS.md)
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 112-159 (update_known_alias pattern — JSONB idempotent append SQL)
    - .planning/phases/05-dashboard-delivery/05-RESEARCH.md lines 175-199 (D-01b Open Item Resolution — CONFIRMED: the David/Daniel Reyes trap; _would_collide / _safe_to_learn_alias implementation)
  </read_first>
  <behavior>
    _safe_to_learn_alias:
    - _safe_to_learn_alias("D. Reyes", david, seed_roster) → False (both David and Daniel carry it — ambiguous post-append)
    - _safe_to_learn_alias("Dave Reyez", david, seed_roster) → True (synthetic roster adds "Dave Reyez" to David only; deterministic_match resolves uniquely to David)
    - _safe_to_learn_alias(token_already_in_david_aliases, david, seed_roster) → True (idempotent — token already present; synthetic roster has it once; resolves uniquely)
    - _safe_to_learn_alias(ambiguous_that_resolves_to_different_emp, david, seed_roster) → False (result.matched_employee_id != david.id)

    update_known_alias:
    - update_known_alias(employee_id, "Dave Reyez") appends "Dave Reyez" to employees.known_aliases JSONB array (idempotent DISTINCT append)
    - update_known_alias(employee_id, "Dave Reyez") second call → returns False (already present; no double-add)
    - update_known_alias(employee_id, "D. Reyes") would append it only if called (caller is responsible for running _safe_to_learn_alias first — not double-gated here)
  </behavior>
  <action>
    In app/pipeline/reconcile_names.py — ADD _safe_to_learn_alias (per PATTERNS.md lines 383-431):
    Location: at the bottom of the file, after all existing functions.
    This is a module-level function (NOT private with double underscore — it is callable from the orchestrator and tests).

    Imports needed at top of reconcile_names.py (add only if not already present):
    - `from app.models.roster import Employee, Roster` (may already be present — check)
    - The function also calls `deterministic_match` which is already in this file — no new import

    `def _safe_to_learn_alias(token: str, target_employee: Employee, roster: Roster) -> bool:`
    Body per PATTERNS.md lines 409-430:
    1. Build synthetic_employees: for each emp in roster.employees, if emp.id == target_employee.id, create emp.model_copy(update={"known_aliases": list(emp.known_aliases) + [token]}); else keep as-is
    2. Build synthetic_roster: roster.model_copy(update={"employees": synthetic_employees})
    3. result = deterministic_match(token, synthetic_roster)
    4. return result is not None and result.matched_employee_id == target_employee.id
    Add docstring: "Return True only if token uniquely resolves to target_employee on the FULL roster AFTER the alias is appended (D-01b write-side collision guard, per D-01b). Uses deterministic_match on a synthetic roster to simulate the post-write state."

    In app/db/repo.py — ADD update_known_alias (per PATTERNS.md lines 112-159):
    After the existing set_alias_candidates helper (added in Plan 03).

    `def update_known_alias(employee_id: uuid.UUID, new_alias: str, conn=None) -> bool:`
    SQL: idempotent JSONB append using `jsonb_agg(DISTINCT elem)` per PATTERNS.md lines 144-158.
    The SQL checks `NOT (known_aliases @> to_jsonb(ARRAY[%s::text]))` in the WHERE clause — returns a row only if the alias was actually new. Returns True if added, False if already present.
    Add docstring: "Idempotently append new_alias to employees.known_aliases (D-01). Caller MUST have already called _safe_to_learn_alias() — this function does NOT re-check collision."

    CRITICAL: _safe_to_learn_alias must use model_copy on Employee (Pydantic v2 method). Do NOT mutate the actual roster objects. The synthetic roster is a temporary computation object only.
    CRITICAL: Do NOT add double underscore prefix to _safe_to_learn_alias. It must be importable from tests/test_alias_write.py as `from app.pipeline.reconcile_names import _safe_to_learn_alias`.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_alias_write.py -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>
    - tests/test_alias_write.py passes GREEN (D. Reyes trap returns False, unambiguous token returns True, idempotency passes)
    - app/pipeline/reconcile_names.py contains def _safe_to_learn_alias(
    - app/db/repo.py contains def update_known_alias(
    - `python -c "from app.pipeline.reconcile_names import _safe_to_learn_alias"` exits 0
    - Full mocked suite GREEN: `uv run pytest -q -m "not integration and not live_llm"` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: alias_candidates capture + resume update + _deliver alias write (orchestrator.py)</name>
  <files>app/pipeline/orchestrator.py</files>
  <read_first>
    - app/pipeline/orchestrator.py (FULL FILE — _clarify at lines 200-245 (the alias_candidates capture insertion point: write BEFORE gateway.send_outbound); resume_pipeline (already has claim_status from Plan 03 — now add the alias_candidates update after _run_stages completes); _deliver (added in Plan 05 — add _write_aliases_if_safe call after successful send))
    - app/db/repo.py (set_alias_candidates signature added in Plan 03; update_known_alias added in Task 1; load_run for reading alias_candidates from the run dict; the alias_candidates column in load_run result — check if it's included)
    - app/pipeline/reconcile_names.py:_safe_to_learn_alias (just added in Task 1 — import it here)
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 435-503 (orchestrator._clarify alias_candidates capture insertion; set_alias_candidates call; resume update pattern)
    - .planning/phases/05-dashboard-delivery/05-PATTERNS.md lines 545-616 (_deliver with _write_aliases_if_safe call)
    - .planning/phases/05-dashboard-delivery/05-RESEARCH.md lines 134-172 (D-04 storage spot confirmed — separate alias_candidates JSONB column; unrecoverability of original tokens by approval time; timing constraint verified)
    - .planning/phases/05-dashboard-delivery/05-CONTEXT.md decisions D-01, D-01b, D-02, D-04 (the authoritative timing/keying/collision constraints)
  </read_first>
  <action>
    THREE INSERTION POINTS in orchestrator.py:

    INSERTION 1 — _clarify (D-04 capture at clarify-emit time):
    In `_clarify`, BEFORE the `gateway.send_outbound(...)` call, insert (per PATTERNS.md lines 462-483):
    If `decision.unresolved_names` is non-empty:
    - Build candidates dict: `{name: None for name in decision.unresolved_names}`
    - Call `repo.set_alias_candidates(run_id, candidates)` (writes {token: None} to alias_candidates column)
    Add import at the top of orchestrator.py if not already present: nothing new needed (repo is already imported; decision.unresolved_names is already available in _clarify scope).

    INSERTION 2 — resume_pipeline alias_candidates update (D-04 binding at resume):
    After `_run_stages(run_id, combined_email, roster, llm=llm)` completes (i.e., after the pipeline re-runs and has resolved names), update alias_candidates with the resolved employee IDs:
    - Load the updated reconciliation: read the matches from the just-completed run's reconciliation JSONB or from the in-scope `matches` variable if available (check what _run_stages returns and what is in scope after it runs — adapt to the actual code)
    - For each name in alias_candidates: if a NameMatchResult in the post-resume matches has resolved=True for that name, update candidates[name] = str(matched_employee_id)
    - Call `repo.set_alias_candidates(run_id, updated_candidates)` with the updated dict
    NOTE: if _run_stages does not return matches directly, load them from `repo.load_run(run_id)["reconciliation"]` after the call. Adapt to the actual code structure seen in the full file read.

    INSERTION 3 — _deliver alias write (D-01, D-02):
    After `repo.set_status(run_id, RunStatus.SENT)` (post-successful send, before RECONCILED), insert a call to `_write_aliases_if_safe(run_id, run, roster)`.
    Define `_write_aliases_if_safe(run_id, run, roster)` as a module-level helper in orchestrator.py:
    - Load the run's alias_candidates: `run_data = repo.load_run(run_id)` and read `run_data.get("alias_candidates") or {}`
    - For each token, employee_id_str in alias_candidates.items():
      - If employee_id_str is None: skip (never got resolved — name wasn't clarified to a specific employee)
      - employee_id = uuid.UUID(employee_id_str)
      - Find target_employee = next(e for e in roster.employees if e.id == employee_id, None); if None: skip
      - Call `_safe_to_learn_alias(token, target_employee, roster)` — if False: log.info("alias write skipped for %r → %s (collision guard)", token, employee_id) and skip
      - If True: `repo.update_known_alias(employee_id, token)` — log.info("alias learned: %r → %s", token, employee_id)
    Import at top of orchestrator.py: `from app.pipeline.reconcile_names import _safe_to_learn_alias`

    CRITICAL D-04 timing: The alias_candidates write in _clarify happens BEFORE gateway.send_outbound. Do NOT move it after the send. The D-04 timing constraint is load-bearing: by the time resume re-extracts from the reply, the original unresolved token is only recoverable from alias_candidates — not from extracted_data or reconciliation (which reflect the POST-resume state).

    CRITICAL D-01b: _write_aliases_if_safe calls _safe_to_learn_alias BEFORE calling update_known_alias. The collision guard is on the WRITE path. A token that passes _safe_to_learn_alias is then written idempotently (update_known_alias handles deduplication in the DB). Never call update_known_alias without _safe_to_learn_alias first.

    CRITICAL D-02: Alias write fires ONLY at delivery time (in _deliver, after send, at approval gate) — NOT on the clarification reply. The reply might correct the name; auto-writing from the reply would silently poison aliases with potentially wrong tokens.
  </action>
  <verify>
    <automated>cd "/Users/pnhek/usf msds/github/payroll_agent" && uv run pytest tests/test_alias_write.py tests/test_delivery.py -x -q 2>&1 | tail -15 && grep -c "alias_candidates" app/pipeline/orchestrator.py</automated>
  </verify>
  <done>
    - tests/test_alias_write.py GREEN (collision guard works)
    - tests/test_delivery.py GREEN (delivery path unchanged)
    - app/pipeline/orchestrator.py contains "alias_candidates" in _clarify (before send_outbound), in resume_pipeline (after _run_stages), and in _write_aliases_if_safe
    - app/pipeline/orchestrator.py imports `_safe_to_learn_alias`
    - `grep -c "alias_candidates" app/pipeline/orchestrator.py` returns ≥ 3 (three insertion points)
    - Full mocked suite GREEN: `uv run pytest -q -m "not integration and not live_llm"` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alias write → employees.known_aliases | A write here permanently changes future name resolution for ALL clients — highest-risk mutation |
| _clarify → alias_candidates column | Captures original tokens before send; these are the inputs to the learning loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-25 | Integrity | alias write — "D. Reyes" money-misroute | mitigate | _safe_to_learn_alias calls deterministic_match on synthetic roster; if the token would collide with 2+ employees, returns False → skip write; the D. Reyes trap is explicitly unit-tested (test_alias_write.py) |
| T-05-26 | Integrity | alias write — auto-write on wrong reply | mitigate | D-02: write fires ONLY at operator-approval gate (in _deliver, which runs only after claim_status(AWAITING_APPROVAL → APPROVED) wins); no auto-write on the clarification reply |
| T-05-27 | Integrity | alias_candidates capture — original token recoverable | mitigate | Captured at clarify-emit time (BEFORE send_outbound) in _clarify; the separate alias_candidates column is not overwritten by persist_reconciliation on resume (D-04 separate column decision, Plan 03 DDL) |
| T-05-28 | Integrity | update_known_alias — duplicate alias | mitigate | JSONB idempotent append SQL: `NOT (known_aliases @> to_jsonb(ARRAY[%s::text]))` in WHERE clause; returns False if already present; no double-add (D-01 idempotency) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
`uv run pytest tests/test_alias_write.py -x -q` — GREEN (D. Reyes trap False, unambiguous True).
`grep -c "_safe_to_learn_alias" app/pipeline/orchestrator.py` — returns ≥ 1 (imported and called in _write_aliases_if_safe).
`grep -c "alias_candidates" app/pipeline/orchestrator.py` — returns ≥ 3 (clarify capture, resume update, deliver write).
`python -c "from app.pipeline.reconcile_names import _safe_to_learn_alias"` exits 0.
`uv run pytest -q -m "not integration and not live_llm"` — full mocked suite GREEN.
</verification>

<success_criteria>
- _safe_to_learn_alias in reconcile_names.py: returns False for "D. Reyes" on full seed roster (both Reyes employees carry it); returns True for unambiguous token
- update_known_alias in repo.py: idempotent JSONB append; returns True on first add, False on re-add
- _clarify extension: writes {original_token: None} to alias_candidates BEFORE send_outbound (D-04 timing)
- resume_pipeline extension: updates alias_candidates with resolved employee_id after _run_stages
- _deliver extension: _write_aliases_if_safe runs collision guard for each candidate and writes only safe aliases
- tests/test_alias_write.py GREEN
- Full mocked suite GREEN
- D-15 lever: this entire plan (Wave 4) can be dropped without affecting Beats 1 or 2 — verified by the depends_on structure (Plans 06 and 03 do not depend on Plan 07)
</success_criteria>

<output>
Create `.planning/phases/05-dashboard-delivery/05-07-SUMMARY.md` when done.
</output>
