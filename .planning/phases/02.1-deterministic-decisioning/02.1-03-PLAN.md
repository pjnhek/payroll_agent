---
phase: 02.1-deterministic-decisioning
plan: 03
type: execute
wave: 3
depends_on: ["02.1-01", "02.1-02"]
files_modified:
  - app/pipeline/orchestrator.py
  - app/pipeline/calculate.py
  - app/db/repo.py
  - app/db/schema.sql
  - app/db/bootstrap.py
  - app/config.py
  - app/llm/client.py
  - .env.example
  - tests/test_orchestrator_states.py
  - tests/test_persistence.py
  - tests/test_threading.py
  - tests/test_status_drift.py
autonomous: false
requirements: [LLM-04, LLM-05, LLM-07, LLM-09]
user_setup:
  - service: local_and_supabase_postgres
    why: "Drop the dead name_matches table on both live DBs (CREATE IF NOT EXISTS cannot alter an existing table); remove the decision/mid model tier from the human's .env"
    env_vars:
      - name: DATABASE_URL
        source: "Existing local .env (and the Supabase pooler URL when running against Supabase)"
    dashboard_config:
      - task: "Remove DECISION_MODEL / DECISION_BASE_URL / DECISION_API_KEY from your local .env (the mid tier is gone — D-21-05)"
        location: "Local .env file"
must_haves:
  truths:
    - "The orchestrator calls reconcile_names/decide with no llm argument and persists Extracted + Decision (with resolutions) + reconciliation on every run"
    - "persist_reconciliation no longer writes match_confidence; no name_matches relational write path remains"
    - "name_matches table is dropped from the live local AND Supabase DBs via bootstrap (DROP TABLE IF EXISTS), and schema.sql no longer creates it"
    - "paystub_line_items.match_confidence column is gone; PaystubLineItem inserts no longer reference it"
    - "Config has TWO model tiers (extraction + draft); the decision/mid tier is removed from Settings and .env.example"
    - "The status-drift guard stays green after the schema change"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "schema without name_matches and without match_confidence"
    - path: "app/config.py"
      provides: "two-tier Settings (extraction + draft)"
  key_links:
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/decide.py"
      via: "decide(extracted, matches, issues) — no llm arg"
      pattern: "decide\\(extracted"
---

<objective>
Wire the deterministic stages through the orchestrator, repo, schema, and config, and physically remove the dead relational + config artifacts (D-21-04, D-21-05, D-21-06). This wave updates every consumer of the old confidence/model_action/name_matches surface, drops the `name_matches` table from the LIVE local AND Supabase DBs (not just schema.sql — CREATE IF NOT EXISTS cannot alter an existing table), drops the `paystub_line_items.match_confidence` column, removes the mid/decision model tier, and keeps the status-drift guard green. Depends on the new contracts (Plan 01) and stages (Plan 02).

This plan is NOT autonomous: it includes a blocking checkpoint to apply the live-DB DROP and confirm the human removed the mid tier from their `.env`.

Purpose: Make the deterministic decisioning real end-to-end in the running app + persisted state, with no dead relational/config residue.
Output: Updated orchestrator/repo/schema/bootstrap/config/client/.env.example; dropped name_matches + match_confidence; two model tiers; green drift guard; updated orchestrator/persistence/threading/drift tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md
@.planning/phases/02.1-deterministic-decisioning/02.1-01-SUMMARY.md
@.planning/phases/02.1-deterministic-decisioning/02.1-02-SUMMARY.md
@app/db/repo.py
@app/db/schema.sql
@app/db/bootstrap.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Orchestrator + calculate stop passing llm/confidence; persist resolutions</name>
  <files>app/pipeline/orchestrator.py, app/pipeline/calculate.py, tests/test_orchestrator_states.py</files>
  <read_first>
    - app/pipeline/orchestrator.py (the whole file — _run_stages 160-193 calls reconcile_names/decide; _compute_line_items 223-263 stamps match_confidence at line 259)
    - app/pipeline/calculate.py (PaystubLineItem construction at 112-131; match_confidence=Decimal("1.0") at line 117)
    - app/pipeline/decide.py + app/pipeline/reconcile_names.py (Plan 02 — both now take no llm arg)
    - app/models/contracts.py (Plan 01 — PaystubLineItem has no match_confidence; Decision has resolutions)
    - tests/test_orchestrator_states.py (the whole file — assertions on decide/reconcile wiring and decision fields)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-03, D-21-04, D-21-09)
  </read_first>
  <action>
    In app/pipeline/orchestrator.py `_run_stages`: call `reconcile_names(submitted_names, roster)` with NO llm kwarg (it is pure now); call `decide(extracted, matches, issues)` with NO llm kwarg and remove `decide_kwargs`. The persist calls stay (persist_extracted/persist_decision/persist_reconciliation) — Decision now carries resolutions internally; reconciliation is still persisted separately for the run-level list. In `_compute_line_items`, REMOVE the `"match_confidence": m.confidence` entry from the model_copy update (NameMatchResult has no confidence; PaystubLineItem has no match_confidence — Plan 01). Keep the run_id/submitted_name stamping. In app/pipeline/calculate.py, REMOVE `match_confidence=Decimal("1.0")` from the PaystubLineItem(...) construction (line ~117). Update the orchestrator module docstring where it mentions "per-name confidence" / "model's advisory action" to reflect deterministic decisioning (final_action is computed purely; there is no model action). Update tests/test_orchestrator_states.py: remove any assertion referencing decision.model_action/confidence/gate_triggered or match_confidence; assert branching on final_action and that resolutions/reconciliation persist on every run.
  </action>
  <verify>
    <automated>uv run pytest tests/test_orchestrator_states.py -q 2>&1 | tail -3 && grep -v '^#' app/pipeline/orchestrator.py app/pipeline/calculate.py | grep -c -iE "match_confidence|m\.confidence|model_action" | grep -qx 0 && echo CLEAN</automated>
  </verify>
  <done>Orchestrator calls reconcile_names/decide with no llm arg; no match_confidence/m.confidence/model_action in orchestrator or calculate; test_orchestrator_states.py passes.</done>
</task>

<task type="auto">
  <name>Task 2: Repo + schema drop name_matches write path + match_confidence column; two-tier config</name>
  <files>app/db/repo.py, app/db/schema.sql, app/db/bootstrap.py, app/config.py, app/llm/client.py, .env.example, tests/test_persistence.py, tests/test_threading.py, tests/test_status_drift.py</files>
  <read_first>
    - app/db/repo.py (persist_reconciliation 344-354 — writes list[NameMatchResult] JSONB, confirm it no longer references match_confidence; replace_line_items 357-404 INSERT references match_confidence at lines 374/390)
    - app/db/schema.sql (paystub_line_items.match_confidence line 105; confirm there is NO name_matches CREATE in this file)
    - app/db/bootstrap.py (the _DROP_ORDER 35-42 + the apply path 68-104)
    - app/config.py (Settings — decision tier at lines 34-37)
    - app/llm/client.py (Tier Literal at line 40; _resolve_tier decision branch 74-79; the "0.8 reconciliation gate" docstring lines 13-15)
    - .env.example (DECISION_* lines)
    - tests/test_persistence.py + tests/test_threading.py (match_confidence / name_matches / decision-tier references)
    - tests/test_status_drift.py (the drift guard — must stay green)
    - tests/test_llm_client.py (decision-tier routing references — update if present)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-05, D-21-06)
  </read_first>
  <action>
    Repo (app/db/repo.py): confirm persist_reconciliation writes the list[NameMatchResult] JSONB only — the new NameMatchResult has no confidence, so the JSON payload is automatically confidence-free; no code change beyond the docstring. In replace_line_items, REMOVE `match_confidence` from the INSERT column list (line ~374) and remove the `it.match_confidence` value (line ~390); adjust the VALUES placeholder count accordingly. Schema (app/db/schema.sql): REMOVE the `match_confidence NUMERIC(4,3) NOT NULL DEFAULT 0,` column from the paystub_line_items table (line 105). There is no name_matches CREATE in schema.sql (resolutions live in the payroll_runs.reconciliation JSONB) — add a short comment near payroll_runs documenting that reconciliation JSONB holds the per-name resolutions and that name_matches is intentionally absent (dropped in Phase 2.1). Bootstrap (app/db/bootstrap.py): add `name_matches` to the FRONT of `_DROP_ORDER` (it is dropped first) so a `--reset` drops it; AND add an unconditional `DROP TABLE IF EXISTS name_matches CASCADE;` in the default (non-reset) apply path BEFORE applying schema.sql, because schema.sql's CREATE IF NOT EXISTS cannot remove an already-existing table on the live DBs — this is the explicit live-DB migration (D-21-06). Config (app/config.py): REMOVE the decision tier block (decision_model/decision_base_url/decision_api_key). LLM client (app/llm/client.py): REMOVE "decision" from the `Tier` Literal (leaving `"extraction" | "draft"`), REMOVE the decision branch in `_resolve_tier`, and rewrite the temperature-0 docstring bullet that references "the 0.8 reconciliation gate keys off the model's reported confidence" → temperature 0 for deterministic, eval-stable extraction (no confidence gate exists). .env.example: REMOVE the DECISION_MODEL/DECISION_BASE_URL/DECISION_API_KEY lines. Tests: update test_persistence.py (drop match_confidence assertions; assert reconciliation JSONB has no confidence key), test_threading.py (NOT a mere reference-strip — plan-check WARNING 1: this file builds full `Decision(model_action=.../gate_triggered=.../confidence=...)` objects (~lines 636-659) AND carries layer-2 reconcile mock JSON with match_type/model_action (~lines 78-85, 106-121). REWRITE every `Decision(...)` to the new field set (final_action/gate_reasons/unresolved_names/missing_fields/resolutions — drop model_action/confidence/gate_triggered), DELETE the dead layer-2 reconcile/decision mock JSON, and let the now-deterministic gate run on the fixture inputs), test_llm_client.py (drop "decision" tier routing tests), and confirm test_status_drift.py still passes unchanged (the schema change touches no CHECK value set, so the guard is green).
  </action>
  <verify>
    <automated>uv run pytest tests/test_persistence.py tests/test_threading.py tests/test_status_drift.py tests/test_llm_client.py -q 2>&1 | tail -3 && grep -c "match_confidence" app/db/schema.sql app/db/repo.py | grep -qE ":0$" && grep -q "DROP TABLE IF EXISTS name_matches" app/db/bootstrap.py && grep -c "decision_model\|DECISION_MODEL" app/config.py .env.example | grep -qE ":0$" && echo CLEAN</automated>
  </verify>
  <done>match_confidence gone from schema + repo INSERT; bootstrap drops name_matches on live DBs; decision tier removed from config/.env.example/client; drift guard + persistence/threading/llm-client tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (checkpoint): Apply the live-DB name_matches DROP + remove the mid tier from .env</name>
  <action>Human-verify checkpoint — apply the DROP on the live DB(s) and edit .env per the steps below. Claude cannot drop a live table for the human nor edit the untracked .env.</action>
  <what-built>
    Task 2 added the live-DB DROP for the dead `name_matches` table to bootstrap and removed the mid/decision model tier from Settings/.env.example. The DROP must be APPLIED to the running local DB (and Supabase when credentials are present), and the human must remove the DECISION_* keys from their local `.env` (not tracked, not editable by Claude).
  </what-built>
  <how-to-verify>
    1. Apply the schema (which now also drops name_matches) to the local DB: `uv run python -m app.db.bootstrap`. Expect "Bootstrap complete."
    2. Confirm the table is gone locally: `uv run python -c "import psycopg; from app.config import get_settings; c=psycopg.connect(get_settings().database_url, prepare_threshold=None); print(c.execute(\"SELECT to_regclass('public.name_matches')\").fetchone())"` — expect `(None,)`.
    3. Confirm paystub_line_items has no match_confidence column: `uv run python -c "import psycopg; from app.config import get_settings; c=psycopg.connect(get_settings().database_url, prepare_threshold=None); print([r[0] for r in c.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='paystub_line_items'\").fetchall()])"` — `match_confidence` must NOT appear.
    4. If you have a Supabase pooler DATABASE_URL, repeat steps 1-2 against it (the same bootstrap, pointed at the pooler host).
    5. Edit your local `.env`: delete the DECISION_MODEL / DECISION_BASE_URL / DECISION_API_KEY lines (the mid tier is gone — D-21-05). Confirm the app still imports: `uv run python -c "from app.config import get_settings; get_settings(); print('config ok')"`.
  </how-to-verify>
  <verify><human-check>name_matches dropped on live DB(s); match_confidence column gone from paystub_line_items; DECISION_* keys removed from .env; app still imports</human-check></verify>
  <done>name_matches is gone on the live DB(s), match_confidence is gone from paystub_line_items, and the DECISION_* keys are removed from .env.</done>
  <resume-signal>Type "approved" once name_matches is gone on the live DB(s), match_confidence is gone from paystub_line_items, and the DECISION_* keys are removed from .env — or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Postgres (local + Supabase) | DDL migration crosses into live state; an unapplied DROP leaves a stale relational source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-04 | Tampering/Repudiation | name_matches stale table | mitigate | Explicit DROP TABLE IF EXISTS in bootstrap applied to live local + Supabase via a blocking human-verify checkpoint; resolutions live only in payroll_runs.reconciliation JSONB (single source of truth) |
| T-021-05 | Information Disclosure | DATABASE_URL in bootstrap output | mitigate | bootstrap already strips the password via _safe_db_url before printing (existing T-02-01) — unchanged |
| T-021-SC | Tampering | npm/pip/cargo installs | accept | No package installs — edits to existing modules only; no new dependency, no requirements/lock change |
</threat_model>

<verification>
- `uv run pytest -q` (full mocked suite) passes green.
- `grep -rE "match_confidence|decision_model|DECISION_MODEL|m\.confidence" app/ .env.example` returns nothing.
- `grep "DROP TABLE IF EXISTS name_matches" app/db/bootstrap.py` matches.
- `uv run pytest tests/test_status_drift.py -q` passes (drift guard green).
- Checkpoint confirms name_matches dropped on live DB(s) and match_confidence column gone.
</verification>

<success_criteria>
The deterministic stages are wired through the orchestrator and persisted; name_matches is dropped from live local + Supabase and from schema.sql; match_confidence is gone from contract/schema/repo; config has two tiers; drift guard stays green. (Maps to CONTEXT success criteria 1, 3, 5.)
</success_criteria>

<output>
Create `.planning/phases/02.1-deterministic-decisioning/02.1-03-SUMMARY.md` when done.
</output>
