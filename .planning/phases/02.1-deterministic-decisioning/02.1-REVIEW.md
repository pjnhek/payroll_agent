---
phase: 02.1
reviewer: codex (gpt-5.5, xhigh)
reviewed_at: 2026-06-22
scope: 17 changed source files (deterministic-decisioning refactor)
status: issues_found_and_fixed
findings: {high: 1, medium: 3, low: 1}
---

# Codex Code Review — Phase 2.1 (Deterministic Decisioning)

Reviewed the 17 changed source files + the design contract against the intended deterministic design. All 5 findings verified real against the code and FIXED with regression tests (199 passed, +4).

## Findings (all confirmed + fixed)

### HIGH — cross-tier exact-vs-alias collision (reconcile_names.py)
A name that was one employee's exact `full_name` AND a *different* employee's `known_alias` resolved to the first (source=exact, resolved=True) without noticing the second candidate — uniqueness was enforced WITHIN each tier, not ACROSS them. So an ambiguous name could reach `process` and pay the wrong employee. **Fix:** `deterministic_match` now computes the set of DISTINCT candidate employees across BOTH full_name and alias matches; resolves only if exactly one, else `none`. Test: `test_name_is_one_employees_fullname_and_another_employees_alias_does_not_resolve`.

### MEDIUM — decide() didn't fail closed on a non-one-for-one match set (decide.py)
`decide` is a pure public function the eval calls with arbitrary inputs; it trusted that `matches` had exactly one entry per extracted employee. A missing record would let an employee be silently dropped from a `process` run (orchestrator `continue`s past unmatched). **Fix:** Rule 0b gates closed when the submitted-name multiset of `matches` != that of `extracted.employees`. Tests: missing-record + extra-record clarify.

### MEDIUM — NameMatchResult had no semantic invariant (roster.py)
Impossible states (`source=none, resolved=True`) were constructible and `decide` trusts `resolved`. **Fix:** `model_validator` — source=none ⟺ resolved=False + id=None; source=exact|alias ⟺ resolved=True + non-null id. Test: rejects 3 impossible combos.

### MEDIUM — PII in suggestion logs (suggest.py)
Failure log used `exc_info=True` (traceback can echo names) and the hallucinated-suggestion warning logged submitted + suggested employee names. **Fix:** log failure TYPE only, no exc_info; count dropped hallucinations instead of naming them.

### LOW — f-string SQL in bootstrap --reset (bootstrap.py)
Reset DROP used an f-string (trusted table list, no injection risk, but violates the 'never f-string SQL' rule). **Fix:** `psycopg.sql.SQL(...).format(sql.Identifier(table))`.

## Verified clean by Codex
decide.py has no LLM call/import; reconcile_names has no llm/DB seam; orchestrator branches only on final_action; suggest_employees is called only in _clarify after decide returns.

_Note: Codex ran static-only (could not import deps in its sandbox). All findings were re-verified against the code and by the test suite here._

## Codex Review — ROUND 2 (re-review after Round-1 fixes)

Round-1 fixes all CONFIRMED correct (incl. the NameMatchResult validator — no invalid constructors, JSONB deserialization safe; one-for-one handles duplicate names). 3 NEW findings, all fixed:

- **MEDIUM — internal employee UUID leaked into client-facing clarification copy** (decide.py). The 'two names → one employee' collision gate_reason appended `-> {emp_id}`, and gate_reasons are copied verbatim into the clarification email. **Fixed:** dropped the UUID from the reason (the submitted names are what the client needs).
- **MEDIUM — PII-safe logging fixed in suggest.py but not adjacent paths.** compose_email used `exc_info=True`; the orchestrator built `reason = f'{type}: {exc}'` and BOTH logged it AND persisted it to error_reason — str(exc) can echo prompt text / names. **Fixed:** compose_email logs type only; orchestrator error_reason is now the exception TYPE only (run_id is the correlation key).
- **LOW — duplicate roster IDs could collapse during reconciliation** (the set-based uniqueness in deterministic_match). Real DB rows are PK-protected but eval fixtures could construct it. **Fixed:** Roster model_validator rejects duplicate employee ids (narrowed to ids only — business_id consistency intentionally not enforced).

+2 regression tests. Full suite 200 passed. (Codex ran static-only again; all findings re-verified against the code + suite here.)

## Codex Review — ROUND 3 (broadened scope: validate/calculate/extract/orchestrator/repo)

Decision logic re-verified CLEAN (cross-tier collision, duplicate-name gating, zero-employee fail-closed, FICA cap math, Decimal serialization all confirmed good). 2 HIGH + 1 MED + 1 LOW found in the under-reviewed plumbing:

- **HIGH — `contribution_401k_override` parsed but silently IGNORED** (calculate.py always used the stored pct). A client-supplied current-run 401k rate did nothing (D-A3-04/LLM-03 was effectively unimplemented). **FIXED:** threaded the override into `calculate` (override if not None else stored default — 0.0 honored as a real override); added `le=1` bound on the contract field; orchestrator passes `ee.contribution_401k_override`. +4 calc tests.
- **HIGH — resume/approve status race** (load-then-set, not atomic): two replies can both see awaiting_reply and double-resume. **DEFERRED (documented):** this is the CR-02 residual already noted in-code as Phase 5 idempotency work (INGEST-05/CLAR-04) — the atomic claim (UPDATE...WHERE status=? RETURNING / FOR UPDATE) lands there with the operator-gate idempotency. Not pulled into 2.1 (deliberate scope boundary, confirmed with the user).
- **MEDIUM — resume_pipeline still persisted `str(exc)` to error_reason** (the round-2 PII fix missed the resume path — its comment differed so replace_all skipped it). **FIXED:** type-only, matching run_pipeline.
- **LOW — `persist_extracted` never populated payroll_runs.pay_period_start/end** (left null; the columns exist for the dashboard to read off the run row). **FIXED:** persist_extracted now writes them from the extraction.

Full suite 204 passed (+4). Codex ran static-only; all findings re-verified here. The status-race is the only open item and is a tracked Phase 5 deliverable.
