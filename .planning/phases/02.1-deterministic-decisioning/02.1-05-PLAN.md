---
phase: 02.1-deterministic-decisioning
plan: 05
type: execute
wave: 5
depends_on: ["02.1-01", "02.1-02", "02.1-03", "02.1-04"]
files_modified:
  - fixtures/gate_block_hero.json
  - fixtures/collision_safety.json
  - tests/test_demo_fixtures.py
  - tests/test_validate.py
  - tests/test_gateway.py
  - tests/test_ingest.py
  - tests/test_webhook.py
  - tests/test_live_llm.py
  - tests/test_seed_roundtrip.py
  - app/db/seed.py
  - CLAUDE.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LLM-04, LLM-05, LLM-07, DEMO-01]
must_haves:
  truths:
    - "The reframed hero fixture proves 'never guesses, clarifies with a specific suggested employee' (unknown shorthand → unresolved → clarify-with-suggestion)"
    - "A collision-safety fixture proves the system never guesses and always clarifies on two plausible matches"
    - "The full mocked suite is green with no confidence/model_action/gate_triggered/match_type/0.8 references remaining in app/ or tests/"
    - "CLAUDE.md, REQUIREMENTS.md, PROJECT.md no longer reference the 0.8 confidence gate; the thesis reads as deterministic auditable decisioning + the learning loop (WRITE in P5)"
    - "The eval taxonomy spec covers exact / stored-alias / first-time-alias / typo / collision / unknown"
  artifacts:
    - path: "fixtures/gate_block_hero.json"
      provides: "reframed unknown-shorthand-clarify-with-suggestion hero"
    - path: "fixtures/collision_safety.json"
      provides: "two-plausible-matches → always clarify"
    - path: "CLAUDE.md"
      provides: "deterministic decisioning description, no 0.8 constraint"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "app/pipeline/decide.py"
      via: "LLM-07 describes deterministic final_action, no model action"
      pattern: "deterministic"
---

<objective>
Reframe the demo fixtures, finish the residual test sweep, and rewrite the planning docs so nothing references the dead 0.8 confidence gate (D-21-04, DEMO-01 reframe, doc_changes). This final wave reflects the fully-assembled deterministic system: the hero fixture becomes "never guesses, clarifies with a specific suggested employee," a collision-safety fixture is added, every remaining test carrying confidence/model_action/match_type is updated, and CLAUDE.md/REQUIREMENTS.md/PROJECT.md + the eval taxonomy are rewritten. Closes with the grep-clean acceptance gate over the whole codebase.

Purpose: Make the docs, demo, eval spec, and entire test suite consistent with deterministic decisioning — no contradictory 0.8/confidence residue anywhere.
Output: Reframed + new fixtures; green full suite; rewritten docs + eval taxonomy; grep-clean confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md
@.planning/phases/02.1-deterministic-decisioning/02.1-01-SUMMARY.md
@.planning/phases/02.1-deterministic-decisioning/02.1-02-SUMMARY.md
@.planning/phases/02.1-deterministic-decisioning/02.1-03-SUMMARY.md
@.planning/phases/02.1-deterministic-decisioning/02.1-04-SUMMARY.md
@app/db/seed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the hero fixture + add a collision-safety fixture; reframe DEMO-01 fixture tests</name>
  <read_first>
    - fixtures/gate_block_hero.json (the David Reyez submission — repurpose)
    - fixtures/clean_happy_path.json (the clean fixture — unchanged, for reference)
    - tests/test_demo_fixtures.py (the whole file — the sub-0.8/llm_typo/model_action hero mocks at lines 52-160 to rewrite)
    - app/db/seed.py (Business 2 Metro Deli: David Reyes e0000003 + Priya Nair e0000004; the collision pair is seeded in Task 2)
    - app/pipeline/decide.py + app/pipeline/reconcile_names.py (Plan 02 — the deterministic behavior the fixtures now exercise)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (DEMO-01 reframe, D-21-02)
  </read_first>
  <action>
    Reframe fixtures/gate_block_hero.json: keep the "David Reyez" unknown-shorthand submission (it now resolves to source="none"/resolved=False deterministically — no model, no confidence). Update only stale framing in any embedded comment/subject so it represents the NEW hero: "never guesses; clarifies with a specific suggested employee (did you mean David Reyes?)". The fixture JSON body stays largely the same (the unresolved variant is what matters). Add a NEW fixture fixtures/collision_safety.json: a submission for Business 2 (hr@metrodeli.example) that triggers the deterministic run-level collision check (D-21-02) — either a single name that two roster employees share, or two submitted names both mapping to one employee (matched to whatever collision pair Task 2 seeds) — so the run forces clarification and the system never guesses. Rewrite tests/test_demo_fixtures.py: remove ALL sub-0.8/llm_typo/match_type/model_action mock scripting (lines ~52-160); replace with deterministic assertions — the hero fixture's "David Reyez" → reconcile gives resolved=False → decide gives final_action="request_clarification" with a gate_reason naming the unresolved name (no model, no confidence); the collision fixture → final_action="request_clarification" with a collision gate_reason. The two canonical demo fixtures remain clean_happy_path (→ process→approval) and the reframed hero (→ clarify-with-suggestion).
  </action>
  <verify>
    <automated>uv run pytest tests/test_demo_fixtures.py -q 2>&1 | tail -3 && test -f fixtures/collision_safety.json && grep -c -E "model_action|confidence|match_type|0\.8|llm_typo" tests/test_demo_fixtures.py | grep -qx 0 && echo CLEAN</automated>
  </verify>
  <done>gate_block_hero reframed to unknown-shorthand-clarify; collision_safety.json added; test_demo_fixtures.py proves both deterministically with no confidence/model_action/match_type references.</done>
</task>

<task type="auto">
  <name>Task 2: Seed a collision pair; finish the residual test + live-LLM sweep</name>
  <read_first>
    - app/db/seed.py (the David Reyes comment block lines 116-138 — purpose changes; the collision pair to add)
    - tests/test_validate.py (the _match helper lines 26-31 — match_type/conf to source/resolved)
    - tests/test_gateway.py (NameMatchResult/Decision construction lines 24-46, 137-138, 186-205 — confidence/match_type to remove)
    - tests/test_ingest.py + tests/test_webhook.py (the decision-mock JSON at test_ingest.py:82, test_webhook.py:45 — model_action mocks no longer apply since decide is pure)
    - tests/test_live_llm.py (the whole file — the live hero exit gate that asserts model_action/confidence/<0.8; the decision tier is gone)
    - tests/test_seed_roundtrip.py (lines 135, 424-425 — David Reyez/model_action/confidence framing)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-02, D-21-05, D-21-07)
  </read_first>
  <action>
    Seed (app/db/seed.py): rewrite the David Reyes comment block — its purpose is now the deterministic unresolved-shorthand hero (the "David Reyez" variant resolves to none and clarifies; no gate-fires-on-confidence framing). Add the COLLISION pair needed by fixtures/collision_safety.json (Task 1): minimal and documented — e.g. a second Business-2 employee whose full_name or alias collides with another so a single submitted name cannot uniquely resolve, OR alias data such that two submitted names map to one employee. Keep the existing 6 employees' identities/UUIDs stable; if adding an employee, give it a fixed UUID and the full FOUND-06 calc-input set (the Employee validator requires it). Re-validate via the seed dry-run. Tests sweep — update each to the deterministic shapes: test_validate.py `_match` → source/resolved (drop match_type/conf params); test_gateway.py NameMatchResult/Decision constructions → source/resolved + the new Decision fields (drop confidence/match_type/model_action; redirect the JSON round-trip assertions to a field that still exists — e.g. resolutions, or a PaystubLineItem Decimal like gross_pay); test_ingest.py + test_webhook.py → decide is now pure (no model call), so remove the decision-advisory mock JSON and let the deterministic gate run (the run reaches process or clarify based on the fixture's resolvability); test_live_llm.py → the live exit gate no longer asserts model_action/confidence (no decision tier exists) — reframe it to assert the LIVE EXTRACTION works and (optionally) the LIVE SUGGESTION call returns a plausible employee, and that deterministic decide on the "David Reyez" variant yields request_clarification; rewrite its docstring (the live proof is extraction + suggestion quality, NOT a confidence gate); drop the _THRESHOLD/0.8 constant and the decision-tier marker. test_seed_roundtrip.py → rewrite the lines 135/424-425 framing from "model returns model_action/confidence<0.8" to "David Reyez resolves to none deterministically → request_clarification" (Phase 1 still owns only seeding).
  </action>
  <verify>
    <automated>uv run python -c "from app.db.seed import seed; r=seed(dry_run=True); print(len(r.employees),'employees ok')" && uv run pytest tests/test_validate.py tests/test_gateway.py tests/test_ingest.py tests/test_webhook.py tests/test_seed_roundtrip.py -q 2>&1 | tail -3 && echo SWEPT</automated>
  </verify>
  <done>A collision pair is seeded (dry-run validates); test_validate/test_gateway/test_ingest/test_webhook/test_seed_roundtrip/test_live_llm updated to deterministic shapes with no confidence/model_action/match_type references; those suites pass.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the docs + eval taxonomy; final grep-clean acceptance over the whole codebase</name>
  <read_first>
    - CLAUDE.md (the "Confidence threshold: ... 0.8" constraint line; the Core Value + decisioning description; the "name reconcile = strong/mid; decision = mid (gated)" model-tiering line; the LLM-05/LLM-07 framing references)
    - .planning/REQUIREMENTS.md (LLM-04 line 43, LLM-05 line 44, LLM-07 line 46, LLM-08 line 47; Core Value line 4)
    - .planning/PROJECT.md (Core Value lines 9-11; the 3-layer decisioning model lines 82-91; model tiering line 87; the Key Decisions confidence/0.8 rows; the DRY-seam note)
    - .planning/ROADMAP.md (Phase 2 success criterion 2 line 62 references "0.8 confidence"; the Phase 2.1 line 18; the eval/Phase 4 taxonomy lines 105-108)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (doc_changes, eval_changes — the authoritative new wording)
    - app/pipeline/decide.py + app/pipeline/reconcile_names.py (the SHIPPED deterministic behavior the docs must now describe accurately)
  </read_first>
  <action>
    CLAUDE.md: REMOVE the "Confidence threshold: name-reconciliation auto-clarify starts at 0.8" constraint bullet. Rewrite the Core Value / decisioning description: every judgment call (name match, process-vs-clarify) is resolved DETERMINISTICALLY by code (exact / stored-alias / none + run-level collision checks); the system never guesses on a money-moving decision; the LLM does extraction and an optional clarification SUGGESTION only (it never decides). Note the human-confirmation learning loop: alias READ-side ships in Phase 2.1; the WRITE side (persist a confirmed alias at the operator-approval gate) is Phase 5. Rewrite the model-tiering line to TWO tiers (extraction = DeepSeek; suggestion/draft = Kimi); remove the "decision = mid (gated)" tier. REQUIREMENTS.md: rewrite LLM-05 (was "LLM reconciliation classifies residual + returns confidence") → the deterministic resolver / stored-alias read + the optional suggestion-only call (no LLM classification, no confidence); rewrite LLM-07 (was "LLM proposes process/clarify; decide gates on 0.8") → deterministic decisioning: decide.py computes final_action purely from resolution facts + run-level collisions + missing fields; there is no model action and no confidence; rewrite LLM-08 to drop model_action/gate_triggered/confidence/reasons from the persisted Decision (keep final_action/gate_reasons/unresolved_names/missing_fields + resolutions); adjust LLM-04 wording so deterministic match is the WHOLE matcher (not just layer 1); update the Core Value line to "gated by code — deterministic, auditable, never guesses." Mark in the traceability/notes that Phase 2.1 supersedes the confidence framing. PROJECT.md: rewrite Core Value → "deterministic, auditable decisioning that never guesses on a money-moving decision, with a human-confirmation learning loop"; rewrite the 3-layer decisioning model so layer 2 is deterministic resolution (the LLM's only judgment roles are extraction + the clarification suggestion) and the "hard gates" layer is the pure decide; remove the 0.8 references in the decisioning model + Key Decisions (replace the "auto-clarify threshold 0.8" decision row with the deterministic-resolution decision); update model tiering to two tiers. ROADMAP.md: fix Phase 2 success criterion 2 (line 62) so it no longer says "below 0.8 confidence" — describe deterministic resolution + collision gating; and update the eval/Phase 4 taxonomy (lines 105-108) to the case taxonomy: exact / stored-alias / first-time-alias / typo / collision / unknown (eval_changes). Keep edits surgical — do not rewrite unrelated sections. After all edits, run the FINAL grep-clean acceptance over the codebase (app/ must be fully clean; planning docs may retain HISTORICAL mentions only inside the 02.1-CONTEXT.md and superseded 02-CONTEXT.md, which are intentionally left as-is).
  </action>
  <verify>
    <automated>grep -rvE "^[[:space:]]*#" app/ --include=*.py | grep -iE "confidence|model_action|gate_triggered|_THRESHOLD" ; test $? -eq 1 && grep -rE "0\.8" app/ --include=*.py | grep -iv "0\.8[0-9]" | grep -i confidence; test $? -eq 1 && grep -niE "0\.8 confidence|confidence threshold|auto-clarify.*0\.8" CLAUDE.md .planning/REQUIREMENTS.md .planning/PROJECT.md; test $? -eq 1 && echo DOCS-CLEAN</automated>
  </verify>
  <done>CLAUDE.md/REQUIREMENTS.md/PROJECT.md/ROADMAP.md rewritten to deterministic decisioning + learning loop (no 0.8 gate); eval taxonomy updated; app/ is fully grep-clean of confidence/model_action/gate_triggered/_THRESHOLD; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ code | Planning docs that assert a 0.8 gate after the code is deterministic mislead future agents into reintroducing confidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-09 | Repudiation/Tampering | doc-code drift | mitigate | The final grep-clean acceptance over app/ + the doc rewrite ensure no live artifact asserts the dead 0.8 gate; historical mentions are confined to the (intentionally immutable) phase CONTEXT files |
| T-021-10 | Tampering | collision fixture/seed | mitigate | The collision-safety fixture + seeded collision pair prove the deterministic run-level collision check forces clarification — the system never guesses on ambiguous identity (D-21-02) |
| T-021-SC | Tampering | npm/pip/cargo installs | accept | No package installs — fixture JSON, test edits, seed edits, and doc edits only; no new dependency |
</threat_model>

<verification>
- `uv run pytest -q` (FULL mocked suite) passes green.
- `grep -rvE "^[[:space:]]*#" app/ --include=*.py | grep -iE "confidence|model_action|gate_triggered|_THRESHOLD|match_type|llm_typo|llm_nickname"` returns nothing.
- `grep -rE "model_action|gate_triggered|match_type|llm_typo" tests/ | grep -v __pycache__` returns nothing.
- `grep -niE "0\.8 confidence|confidence threshold" CLAUDE.md .planning/REQUIREMENTS.md .planning/PROJECT.md` returns nothing.
- `fixtures/collision_safety.json` exists; `fixtures/gate_block_hero.json` reframed.
- The eval taxonomy in ROADMAP/Phase 4 lists exact / stored-alias / first-time-alias / typo / collision / unknown.
</verification>

<success_criteria>
DEMO-01 fixtures reframed (unknown-shorthand-clarify-with-suggestion + collision-safety; learning beat deferred to P5); the full mocked suite is green; the codebase is grep-clean of confidence/model_action/0.8; CLAUDE.md/REQUIREMENTS.md/PROJECT.md no longer reference the 0.8 confidence gate and read as deterministic auditable decisioning + learning loop; the eval taxonomy is updated. (Maps to CONTEXT success criteria 1, 7, 8, 9.)
</success_criteria>

<output>
Create `.planning/phases/02.1-deterministic-decisioning/02.1-05-SUMMARY.md` when done.
</output>
