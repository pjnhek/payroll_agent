---
phase: 02.1-deterministic-decisioning
verified: 2026-06-21T00:00:00Z
status: passed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  note: initial verification
deferred:
  - truth: "Atomic status claim (SELECT ... FOR UPDATE) closing the resume/approve double-resume race"
    addressed_in: "Phase 5 (idempotency)"
    evidence: "Accepted deferral tracked in .planning/backlog.md (FOUND-04 / CLAR-04 / INGEST-05) and in-code comment app/pipeline/orchestrator.py:118 + app/main.py:163,228. NOT a Phase 2.1 deliverable per 02.1-REVIEW.md."
---

# Phase 2.1: Deterministic Decisioning Verification Report

**Phase Goal:** Replace the confidence-threshold gate with DETERMINISTIC decisioning — each submitted name resolves against the roster in pure code (exact / stored-alias / none), run-level collisions always clarify, decide() computes final_action with NO LLM call and NO confidence number, and the system never guesses on a money-moving decision. LLM kept for extraction + optional clarification-suggestion only. Alias-learning READ side ships; WRITE side deferred to Phase 5.

**Verified:** 2026-06-21
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (the 9 CONTEXT success criteria, D-21-01..09)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | No `confidence`/`model_action`/`gate_triggered`/`0.8`/`_THRESHOLD` as LIVE logic in app/ | ✓ VERIFIED | Grep returns 6 hits, ALL comments/docstrings describing the removal (client.py:14 docstring, bootstrap.py:122-126 dead-column migration, seed.py:121 comment, repo.py:360 docstring). Zero live-logic references. No `_THRESHOLD`, no `0.8` constant, no `model_action`/`gate_triggered` symbols. |
| 2 | decide.py is pure code: resolution + collisions + missing-field + empty-extraction (CR-01) → final_action; no LLM call; final_action sole branch | ✓ VERIFIED | `app/pipeline/decide.py` imports only contracts + roster models (no llm, no db). Rule 0 empty-extraction (line 101), Rule 0b one-for-one safety (line 111), Rule 1 unresolved (line 117), Rule 2 missing field (line 124), Rule 3 `check_one_to_one` collisions (line 129). `final_action = "request_clarification" if gate_reasons else "process"` (line 131). Orchestrator branches SOLELY on `decision.final_action` (orchestrator.py:191). |
| 3 | NameMatchResult = source/resolved (no confidence/match_type) + validator; Decision shape correct; name_matches table + match_confidence dropped | ✓ VERIFIED | `roster.py` NameMatchResult: `source: Literal["exact","alias","none"]`, explicit `resolved: bool`, `_check_resolution_invariant` validator rejects impossible states (line 177). Decision (contracts.py:137): `final_action`/`gate_reasons`/`unresolved_names`/`missing_fields`/`resolutions`; zero forbidden fields. schema.sql: name_matches ABSENT (only comment line 84); bootstrap.py drops `match_confidence` column. Tests: `test_name_match_result_rejects_confidence_kwarg`, `test_paystub_line_item_rejects_match_confidence_kwarg`, `test_name_reconciliation_response_module_gone`. |
| 4 | Clarification-suggestion call exists (draft tier, suggestion-only), wired into clarify AFTER decide, NEVER feeds decide; decide.py must not import suggest | ✓ VERIFIED | `suggest.py`: `call_structured("draft", ...)` (line 81), suggestion-only, degrades to `{}` on any failure. orchestrator._clarify calls `suggest_employees` strictly after `decide` returned (orchestrator.py:228, decision is a parameter). decide.py grep for `suggest` → no import. Tests: `test_decide_never_references_suggest`, `test_clarify_suggestion_never_reaches_the_decision`, `test_orchestrator_suggest_called_after_decide`. |
| 5 | Config has TWO tiers (extraction + draft); mid/decision tier removed from config.py + .env.example | ✓ VERIFIED | `config.py`: `extraction_*` + `draft_*` only, explicit comment "mid/decision tier was removed in Phase 2.1". `.env.example`: only `EXTRACTION_*` + `DRAFT_*` (no DECISION_*). `llm/client.py`: `Tier = Literal["extraction", "draft"]` (line 40), `_resolve_tier` handles exactly those two. |
| 6 | reconcile_names + decide remain pure importable functions (no DB, no llm param) — Phase 4 eval reuses | ✓ VERIFIED | `reconcile_names.py` imports only `app.models.roster` (no db, no llm). `decide.py` imports only contracts + roster. Tests: `test_reconcile_is_pure_no_llm_no_db`, `test_decide_is_pure_no_llm`. |
| 7 | Full mocked suite green; deterministic-resolution suite covers taxonomy | ✓ VERIFIED | `uv run pytest -m "not integration and not live_llm" -q` → **204 passed, 12 deselected**. Taxonomy: test_reconcile.py (exact, normalized-exact, stored-alias, single-word-alias, first-time-alias-unresolved, typo-unresolved, unknown-unresolved, 3 collision shapes); test_gate.py (clean-process, unresolved, unknown, missing-field, empty-extraction, two-names-one-employee, duplicate-name). |
| 8 | DEMO-01 fixtures reframed (unknown-shorthand-clarify-with-suggestion + collision-safety); "learns and stops asking" deferred to P5 | ✓ VERIFIED | `fixtures/`: `clean_happy_path.json`, `collision_safety.json`, `gate_block_hero.json`, `clarify_reply.json`. test_demo_fixtures.py: `test_hero_fixture_replays_to_deterministic_clarify`, `test_collision_fixture_replays_to_deterministic_clarify`, `test_all_three_fixtures_replay_end_to_end`. |
| 9 | CLAUDE.md / REQUIREMENTS.md / PROJECT.md no longer reference the 0.8 gate; thesis reads as deterministic auditable decisioning + learning loop | ✓ VERIFIED | CLAUDE.md:31 = rewritten deterministic thesis ("no LLM call and no confidence number"). PROJECT.md:102,115 frame 0.8 only as the SUPERSEDED decision. REQUIREMENTS.md LLM-08 documents dead fields removed. Remaining "confidence" hits in CLAUDE.md are IRS-tax-numbers / confidence-summary table — unrelated to the name-match gate. |

**Score:** 9/9 truths verified

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Atomic status claim closing the resume/approve double-resume race | Phase 5 (idempotency) | Accepted deferral. backlog.md §"Atomic status claim" maps to FOUND-04 / CLAR-04 / INGEST-05; in-code comments orchestrator.py:118 + main.py:163. Explicitly an ACCEPTED deferral per 02.1-REVIEW.md, NOT a Phase 2.1 gap. |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/pipeline/decide.py` | Pure deterministic decision | ✓ VERIFIED | No llm/db/suggest import; all 5 gate rules present; final_action sole branch. |
| `app/pipeline/reconcile_names.py` | Pure 3-source resolver | ✓ VERIFIED | Imports only roster models; exact/alias/none. |
| `app/pipeline/suggest.py` | Draft-tier suggestion-only | ✓ VERIFIED | call_structured("draft"); degrades to {}; never reaches decide. |
| `app/pipeline/orchestrator.py` | Branch on final_action; suggest after decide | ✓ VERIFIED | Branches solely on final_action; suggest wired in _clarify after decide. |
| `app/models/roster.py` | NameMatchResult source/resolved + validator | ✓ VERIFIED | Literal source, explicit resolved, invariant validator. |
| `app/models/contracts.py` | Decision shape, no legacy fields | ✓ VERIFIED | Exact 5-field shape; no model_action/confidence/gate_triggered. |
| `app/config.py` + `.env.example` | Two tiers only | ✓ VERIFIED | extraction + draft; decision tier removed. |
| `app/llm/client.py` | Two-tier Literal | ✓ VERIFIED | Tier = Literal["extraction","draft"]. |
| `app/db/schema.sql` + `bootstrap.py` | name_matches + match_confidence dropped | ✓ VERIFIED | Table absent in schema; column drop in bootstrap. |

### Key Link Verification

| From | To | Via | Status |
|------|-----|-----|--------|
| orchestrator | decide | `decision = decide(...)` then branch on `decision.final_action` | ✓ WIRED |
| orchestrator._clarify | suggest_employees | called after decide, result → compose only | ✓ WIRED |
| decide | suggest | (must be absent) | ✓ CORRECTLY UNWIRED (no import) |
| reconcile_names | NameMatchResult | returns per-name results | ✓ WIRED |
| llm.client | config tiers | _resolve_tier(extraction/draft) | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full mocked suite | `uv run pytest -m "not integration and not live_llm" -q` | 204 passed, 12 deselected | ✓ PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | TBD/FIXME/XXX scan of app/ | — | Clean — no debt markers. |

### Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| LLM-08 (deterministic decision object, legacy fields removed) | ✓ SATISFIED | Decision shape + contract tests; REQUIREMENTS.md updated. |
| D-21-01..09 (the 9 SCs) | ✓ SATISFIED | All 9 verified above. |

### Human Verification Required

None. All criteria verified programmatically (grep + file read + green suite). A live LLM suggestion-quality check is gated behind `live_llm` marker and is out of scope for goal verification.

### Gaps Summary

No gaps. All 9 success criteria are observably true in the codebase: the confidence gate is fully removed (only removal-describing comments remain), decide.py is pure code with final_action as the sole branch source, the contracts enforce the new source/resolved shape with a validator rejecting impossible states, the suggestion call is draft-tier/suggestion-only/post-decide and never feeds the decision, config is two-tier, and the docs thesis is rewritten. The 204-test mocked suite is green and covers the full resolution taxonomy plus the gate taxonomy. The single open item — the resume/approve status race — is an ACCEPTED, documented deferral to Phase 5 (FOUND-04/CLAR-04/INGEST-05 in backlog.md + in-code comments), not a Phase 2.1 gap.

---

_Verified: 2026-06-21_
_Verifier: Claude (gsd-verifier)_
