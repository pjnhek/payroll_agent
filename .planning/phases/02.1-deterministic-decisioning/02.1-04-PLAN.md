---
phase: 02.1-deterministic-decisioning
plan: 04
type: execute
wave: 4
depends_on: ["02.1-01", "02.1-02", "02.1-03"]
files_modified:
  - app/pipeline/suggest.py
  - app/llm/prompts/suggest.py
  - app/pipeline/compose_email.py
  - app/pipeline/orchestrator.py
  - tests/test_suggest.py
  - tests/test_clarify.py
autonomous: true
requirements: [LLM-05, CLAR-01]
must_haves:
  truths:
    - "A suggestion-only LLM call (cheap/draft tier) suggests the likely intended employee for an unresolved name"
    - "The suggestion NEVER feeds decide / final_action — it is purely clarification-email copy"
    - "The clarification email names a specific suggested employee ('did you mean David Reyes?') when a suggestion is available"
    - "A suggestion failure (empty/API error) degrades to the generic clarification body — it never strands the run"
  artifacts:
    - path: "app/pipeline/suggest.py"
      provides: "suggestion-only call: unresolved name → likely employee, copy only"
      exports: ["suggest_employees"]
    - path: "app/pipeline/compose_email.py"
      provides: "clarification body that names the suggested employee"
  key_links:
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/suggest.py"
      via: "_clarify calls suggest then composes with suggestions; decide never sees it"
      pattern: "suggest"
---

<objective>
Add the NEW suggestion-only LLM call (D-21-05): for an unresolved name, one cheap-tier call suggests the most likely intended roster employee, used ONLY to make the clarification email specific ("did you mean David Reyes?"). It is wired AFTER the deterministic gate has already decided request_clarification and NEVER feeds `decide` or `final_action`. This is where nickname knowledge (Bob→Robert) beats plain string distance — keeping the LLM doing real judgment beyond extraction. Depends on the deterministic Decision/orchestrator from Plans 01-03.

Purpose: Deliver the new Phase 2 hero — "never guesses, clarifies intelligently with a specific suggested employee" — without ever letting the LLM touch the money-moving decision.
Output: New `suggest.py` stage + prompt; `compose_clarification` that names the suggested employee; orchestrator `_clarify` wiring; suggestion + clarify tests proving the suggestion never reaches decide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md
@.planning/phases/02.1-deterministic-decisioning/02.1-02-SUMMARY.md
@.planning/phases/02.1-deterministic-decisioning/02.1-03-SUMMARY.md
@app/pipeline/compose_email.py
@app/pipeline/orchestrator.py
@app/llm/client.py
@app/models/roster.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New suggestion-only stage + prompt (cheap tier, never feeds decide)</name>
  <read_first>
    - app/llm/client.py (call_structured signature; the "draft" tier is the cheap tier after Plan 03; Tier Literal is now "extraction" | "draft")
    - app/models/roster.py (NameMatchResult source/resolved; Roster/Employee; the suggestion maps an unresolved submitted_name to a roster employee id+name)
    - app/llm/prompts/clarify.py (existing free-text prompt style to mirror for the new suggest prompt)
    - app/db/seed.py (David Reyes id e0000003-...; the "David Reyez" unresolved variant the suggestion should map back to David Reyes)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-05, D-21-09)
  </read_first>
  <behavior>
    - suggest_employees(unresolved_names=["David Reyez"], roster, llm=<stub>) → {"David Reyez": "David Reyes"} (or a NameSuggestion mapping the submitted name to a roster full_name).
    - suggest_employees([], roster) → {} with NO LLM call (nothing unresolved → nothing to suggest).
    - A suggestion the model omits, or an empty/None LLM response, yields no entry for that name (the caller falls back to a generic ask) — a suggestion failure never raises out of this stage.
    - suggest_employees is pure (Roster + names in, mapping out, no DB) so the eval could reuse it (D-21-09); it imports the LLM client but is only called on the clarify branch.
    - The returned suggestion is advisory copy ONLY — there is no path from this function into decide/final_action.
  </behavior>
  <action>
    Create app/pipeline/suggest.py with `suggest_employees(unresolved_names: list[str], roster: Roster, *, llm=llm_client) -> dict[str, str]`. It returns a mapping of each unresolved submitted name → the suggested roster employee's full_name (only entries the model is reasonably sure about; omit a name when the model returns null/unknown). Use a structured `call_structured("draft", messages, NameSuggestionResponse)` where NameSuggestionResponse is a small Pydantic wrapper `{"suggestions": [{"submitted_name": str, "suggested_full_name": str | None}]}` (extra="forbid"), validated like the old reconcile wrapper but for COPY only. Wrap the call so any exception or empty content degrades to `{}` (mirror compose_email's WR-03 "never strands the run" pattern) and log a warning. Create app/llm/prompts/suggest.py with `build_messages(unresolved_names, roster)` — system prompt: you are helping write a clarification email; given submitted names that did NOT match the roster and the full roster, suggest which roster employee each likely meant (nicknames/typos welcome), or null if genuinely unknown; this is for email copy only, it does NOT decide anything. Include the literal word "json" + an example shape (DeepSeek JSON-mode requirement — even though the draft tier is Kimi, keep the convention). Define NameSuggestionResponse in suggest.py (or a tiny models module) — your discretion, but keep it confidence-free. Add a test file tests/test_suggest.py: stub LLM returns a scripted suggestion → assert the mapping; empty unresolved list → no LLM call; LLM raises → returns {}; assert suggest.py is never imported by decide.py (grep-level: `import app.pipeline.decide` source has no "suggest").
  </action>
  <verify>
    <automated>uv run pytest tests/test_suggest.py -q 2>&1 | tail -3 && grep -v '^#' app/pipeline/decide.py | grep -c -i "suggest" | grep -qx 0 && echo "decide-clean"</automated>
  </verify>
  <done>suggest_employees maps unresolved names → roster full_names via the cheap tier; degrades to {} on failure; decide.py never references suggest; test_suggest.py passes.</done>
</task>

<task type="auto">
  <name>Task 2: Clarification email names the suggested employee; orchestrator wires suggest into _clarify</name>
  <read_first>
    - app/pipeline/compose_email.py (compose_clarification 67-98; _template_body 30-64; the unresolved_names/missing_fields ask)
    - app/llm/prompts/clarify.py (build_messages — to extend with suggestions)
    - app/pipeline/orchestrator.py (_clarify 195-220 — where the clarification is built and sent)
    - app/models/contracts.py (Decision.unresolved_names — the names needing suggestions)
    - tests/test_clarify.py (the whole file — compose_clarification + _template_body tests to extend)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-05, DEMO-01 reframe)
  </read_first>
  <action>
    Extend compose_clarification to accept an optional `suggestions: dict[str, str] | None` (submitted_name → suggested_full_name). Thread it into both the LLM draft prompt (clarify.build_messages gains the suggestions so the model can write "did you mean David Reyes?") AND the deterministic _template_body fallback (when a suggestion exists for an unresolved name, render "We could not match 'David Reyez' — did you mean David Reyes?" instead of the bare name). Keep the WR-03 guarantee: a draft/suggestion failure still produces a sendable body. In app/pipeline/orchestrator.py `_clarify`, BEFORE composing, call `suggest.suggest_employees(decision.unresolved_names, roster, llm=llm)` (the roster must be in scope — pass it down from _run_stages into _clarify; it is already loaded in _run / resume) and pass the result as `suggestions=` to compose_clarification. CRITICAL: this happens only on the request_clarification branch, strictly AFTER `decide` has returned — the suggestion result is never passed to decide and never influences final_action (D-21-05). Import suggest in orchestrator. Update tests/test_clarify.py — note (plan-check WARNING 2) it ALSO carries pre-existing dead-field code that breaks against the Plan 01 contract: a `Decision(model_action=.../gate_triggered=.../confidence=...)` construction (~line 44) and a layer-2 reconcile mock with match_type:llm_typo/model_action:process (~lines 215-222). REWRITE that Decision to the new field set (final_action/gate_reasons/unresolved_names/missing_fields/resolutions) and DELETE the dead reconcile/decision mock JSON FIRST, then: assert the body names the suggested employee when a suggestion is supplied, assert the generic fallback when suggestions is None/empty, and add/adjust an orchestrator-level test (or extend test_orchestrator_states via a clarify-path assertion if that is the natural home) proving suggest is called after decide on the clarify branch and its output never reaches the Decision.
  </action>
  <verify>
    <automated>uv run pytest tests/test_clarify.py -q 2>&1 | tail -3 && uv run python -c "import inspect; from app.pipeline.compose_email import compose_clarification; assert 'suggestions' in inspect.signature(compose_clarification).parameters; print('ok')"</automated>
  </verify>
  <done>The clarification body names a specific suggested employee when available and degrades cleanly otherwise; _clarify calls suggest after decide; the suggestion never feeds the Decision; test_clarify.py passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| suggestion LLM → clarification email copy | The cheap-tier suggestion is untrusted model output used only in human-facing email text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-021-06 | Elevation of Privilege | suggestion → decision | mitigate | suggest_employees runs ONLY on the already-decided clarify branch, after decide returns; its output is passed to compose_clarification, never to decide — enforced by call ordering + a test asserting decide.py never imports suggest (D-21-05) |
| T-021-07 | Tampering | suggestion strands run | mitigate | A suggestion failure degrades to {} and the clarification falls back to the generic ask (WR-03 pattern) — a draft/suggestion failure never strands the run |
| T-021-08 | Information Disclosure | suggestion prompt leaks roster | accept | The roster is the business's own data echoed back to the business's own contact in a clarification — no cross-tenant exposure |
| T-021-SC | Tampering | npm/pip/cargo installs | accept | No package installs — new modules use the existing openai client; no new dependency |
</threat_model>

<verification>
- `uv run pytest tests/test_suggest.py tests/test_clarify.py -q` passes.
- `grep -v '^#' app/pipeline/decide.py | grep -i suggest` returns nothing (the suggestion never touches the decision).
- `uv run python -c "from app.pipeline.suggest import suggest_employees; print('ok')"`.
- The David Reyez clarification body, when a suggestion is supplied, contains "David Reyes".
</verification>

<success_criteria>
A suggestion-only call exists (cheap tier, never feeds decide) and the clarification email names a specific suggested employee, degrading cleanly on failure. (Maps to CONTEXT success criterion 4.)
</success_criteria>

<output>
Create `.planning/phases/02.1-deterministic-decisioning/02.1-04-SUMMARY.md` when done.
</output>
