---
phase: 02.1-deterministic-decisioning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/contracts.py
  - app/models/roster.py
  - app/models/reconcile.py
  - tests/test_models_contracts.py
autonomous: true
requirements: [LLM-04, LLM-05, LLM-07]
must_haves:
  truths:
    - "NameMatchResult carries source + resolved (no confidence, no match_type)"
    - "Decision carries final_action/gate_reasons/unresolved_names/missing_fields + per-name resolutions (no model_action/confidence/gate_triggered/reasons)"
    - "PaystubLineItem no longer carries match_confidence"
    - "NameReconciliationResponse is deleted (no layer-2 LLM wrapper)"
  artifacts:
    - path: "app/models/roster.py"
      provides: "NameMatchResult with source/resolved shape"
      contains: 'source: Literal["exact", "alias", "none"]'
    - path: "app/models/contracts.py"
      provides: "deterministic Decision contract + confidence-free PaystubLineItem"
      contains: "resolutions"
  key_links:
    - from: "app/models/contracts.py"
      to: "app/models/roster.py"
      via: "Decision.resolutions is list[NameMatchResult]"
      pattern: "list\\[NameMatchResult\\]"
---

<objective>
Re-shape the shared Pydantic contracts that are the DRY seam for the whole pipeline + eval, removing every confidence/model_action vestige and replacing the LLM-judgment name-match shape with a deterministic resolution shape (D-21-01, D-21-04, D-21-06). This is the FIRST wave because every downstream stage, the orchestrator, the repo, and the tests import these types — the interface must be defined before any consumer is rewritten.

Purpose: Lock the deterministic-decisioning contract so consumers in later waves build against a stable, confidence-free interface (interface-first ordering — no scavenger hunt).
Output: Rewritten `NameMatchResult`, `Decision`, `PaystubLineItem`; deleted `NameReconciliationResponse`; updated `test_models_contracts.py` proving the new shapes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-shape NameMatchResult to deterministic source/resolved</name>
  <read_first>
    - app/models/roster.py (the current NameMatchResult — lines 133-147; confidence/match_type to remove)
    - app/models/contracts.py (PaystubLineItem.match_confidence at lines 156-160; Decision at 114-134 — referenced, edited in Task 2)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-01, D-21-04)
  </read_first>
  <behavior>
    - NameMatchResult(submitted_name="Maria Chen", matched_employee_id=<uuid>, source="exact", resolved=True, reason="exact match") validates.
    - NameMatchResult(submitted_name="Dave", matched_employee_id=None, source="none", resolved=False, reason="no deterministic or alias match") validates.
    - source only accepts "exact" | "alias" | "none" — any other value raises ValidationError.
    - A `confidence=` or `match_type=` kwarg raises ValidationError (extra="forbid").
    - resolved is an explicit bool field (not derived), legible per D-21-04.
  </behavior>
  <action>
    In app/models/roster.py, rewrite NameMatchResult per D-21-04. REMOVE the `confidence: Decimal` field and the `match_type: Literal[...]` field. ADD `source: Literal["exact", "alias", "none"]` and `resolved: bool`. Keep `submitted_name: str`, `matched_employee_id: UUID | None`, `reason: str`. Keep `model_config = ConfigDict(extra="forbid")`. Rewrite the class docstring: it is the per-name deterministic-resolution result (exact normalized match / stored known_alias match / none), with `resolved` explicit for legibility; there is no confidence and no LLM match_type — resolution is pure code (D-21-01). Remove the now-unused `Decimal` import only if no other symbol in the file uses it (Employee fields use Decimal — keep the import). Do NOT touch the Employee/Roster/ValidationIssue classes.
  </action>
  <verify>
    <automated>uv run python -c "from app.models.roster import NameMatchResult; import uuid; NameMatchResult(submitted_name='x', matched_employee_id=uuid.uuid4(), source='exact', resolved=True, reason='r'); print('ok')" && grep -c -E "confidence|match_type" app/models/roster.py | grep -qx 0 && echo CLEAN</automated>
  </verify>
  <done>NameMatchResult has source/resolved and no confidence/match_type; grep for confidence|match_type in roster.py returns 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deterministic Decision contract + confidence-free PaystubLineItem; delete NameReconciliationResponse</name>
  <read_first>
    - app/models/contracts.py (Decision lines 114-134; PaystubLineItem lines 141-174; module docstring D-08 at lines 1-9)
    - app/models/reconcile.py (NameReconciliationResponse — the whole file, to delete)
    - app/models/roster.py (the NameMatchResult shape from Task 1 — Decision.resolutions references it)
    - tests/test_models_contracts.py (Decision/PaystubLineItem/NameMatchResult tests at lines ~71, 155-213, 279-300, 457-476)
    - .planning/phases/02.1-deterministic-decisioning/02.1-CONTEXT.md (D-21-01, D-21-04, D-21-06)
  </read_first>
  <behavior>
    - Decision(final_action="process", gate_reasons=[], unresolved_names=[], missing_fields=[], resolutions=[<NameMatchResult>]) validates.
    - Decision with final_action="request_clarification" + gate_reasons=["Dave: unresolved (no roster match)"] validates.
    - A `model_action=` / `confidence=` / `gate_triggered=` / `reasons=` kwarg on Decision raises ValidationError (extra="forbid").
    - PaystubLineItem constructs WITHOUT a match_confidence kwarg; supplying match_confidence raises ValidationError.
    - `from app.models.reconcile import NameReconciliationResponse` raises ModuleNotFoundError (file deleted).
    - Decision.model_dump(mode="json")["resolutions"] is a list of dicts each with submitted_name/source/resolved/reason.
  </behavior>
  <action>
    Per D-21-04 and D-21-06, in app/models/contracts.py rewrite Decision: REMOVE `model_action`, `gate_triggered`, `confidence`, `reasons`. KEEP `final_action: Literal["process","request_clarification"]`, `gate_reasons: list[str]`, `unresolved_names: list[str]`, `missing_fields: list[str]`. ADD `resolutions: list[NameMatchResult]` (the per-name resolution detail folded into the decision JSONB so dashboard/eval can read why each name resolved — D-21-04/D-21-06). Add `from app.models.roster import NameMatchResult` at the top of contracts.py. Rewrite the Decision docstring: deterministic decision object; final_action is the SOLE branch source and is computed purely by code; there is no model action to diverge from. In PaystubLineItem, REMOVE the `match_confidence: Decimal` field and its comment block (lines ~156-160) — confidence is gone everywhere (D-21-01); the employee_id + submitted_name already provide provenance. Rewrite the module docstring (lines 1-9): drop the D-08 "model_action AND final_action structurally separate" note; replace with: Decision is purely code-owned (D-21-01); resolutions carries per-name detail. DELETE the file app/models/reconcile.py entirely (NameReconciliationResponse is dead — no layer-2 LLM step, D-21-05). Then update tests/test_models_contracts.py: rewrite every Decision construction to the new field set (remove model_action/gate_triggered/confidence/reasons, add resolutions/final_action), rewrite the NameMatchResult test(s) to source/resolved, remove the PaystubLineItem match_confidence bound tests (lines ~457-476) and remove match_confidence from `_paystub_kwargs`, and replace `test_decision_gate_shape`/`test_decision_pass_through` with tests asserting the deterministic Decision shape (final_action present, no model_action attribute). Keep test_decimal_json_serialization but assert against a remaining Decimal-bearing contract (e.g. PaystubLineItem.gross_pay) since Decision no longer has confidence.
  </action>
  <verify>
    <automated>test ! -f app/models/reconcile.py && grep -c -E "confidence|model_action|gate_triggered|match_confidence" app/models/contracts.py | grep -qx 0 && uv run pytest tests/test_models_contracts.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>reconcile.py deleted; contracts.py has no confidence/model_action/gate_triggered/match_confidence; test_models_contracts.py passes against the new Decision/NameMatchResult/PaystubLineItem shapes.</done>
</task>

</tasks>

<verification>
- `grep -rE "confidence|model_action|gate_triggered|match_type|match_confidence" app/models/` returns nothing.
- `app/models/reconcile.py` does not exist.
- `uv run pytest tests/test_models_contracts.py -q` passes.
- `uv run python -c "from app.models.contracts import Decision; from app.models.roster import NameMatchResult; print(Decision.model_fields.keys())"` lists only final_action/gate_reasons/unresolved_names/missing_fields/resolutions.
</verification>

<success_criteria>
The shared contract substrate is deterministic and confidence-free: NameMatchResult = source/resolved, Decision = the 4 kept fields + resolutions, PaystubLineItem has no match_confidence, NameReconciliationResponse is gone. Downstream waves build against this interface. (Maps to CONTEXT success criteria 1 and 3.)
</success_criteria>

<output>
Create `.planning/phases/02.1-deterministic-decisioning/02.1-01-SUMMARY.md` when done.
</output>
