---
phase: 12-ci-quality-gates
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - app/main.py
  - app/llm/client.py
  - app/email/gateway.py
  - app/models/status.py
  - app/pipeline/decide.py
  - app/db/repo.py
  - app/db/seed.py
  - app/pipeline/orchestrator.py
  - eval/run_eval.py
  - scripts/show_confirmation_subject.py
  - tests/test_stuck_run_recovery.py
  - tests/conftest.py
  - tests/test_calculate.py
  - tests/test_detect_field_regression.py
  - tests/test_validate.py
  - tests/test_atomic_persist.py
  - tests/test_clarify_rounds.py
  - tests/test_needs_operator.py
  - tests/test_demo_landing.py
  - tests/test_gateway.py
  - tests/test_ingest.py
  - tests/test_live_llm.py
  - tests/test_resume_pipeline.py
  - tests/test_dashboard.py
  - tests/test_alias_write.py
  - tests/test_federal_withholding.py
  - tests/test_compose_email_field_regression.py
  - tests/test_alias_full_loop.py
  - tests/test_threading.py
  - eval/**/*.py
autonomous: true
requirements: [CI-01, CI-03]

must_haves:
  truths:
    - "`uv run ruff check .` (config-driven, no flags) exits 0 with zero violations under the curated ruleset"
    - "All ~46 SIM117 nested-with sites, across all 6 affected files, are resolved by collapsing each nested pair into a single combined with-statement — zero `# ruff: noqa: SIM117` directives anywhere in the repo (per user decision resolving BLOCKER 1 / WARNING 1, superseding the original per-file-noqa approach)"
    - "The 663-test suite is fully green (613 passed, 50 skipped) after every hand-fix commit, proving the cleanup changed no behavior"
    - "The 7 F821 quoted-forward-reference errors are resolved via TYPE_CHECKING imports (D-04), not suppressions"
  artifacts:
    - path: "pyproject.toml"
      provides: "unchanged ruleset from Plan 12-01 — no new ignore/per-file-ignore additions"
      contains: "[tool.ruff.lint]"
  key_links:
    - from: "tests/test_calculate.py, tests/test_detect_field_regression.py, tests/test_validate.py"
      to: "app/models/roster.py Employee/Roster"
      via: "if TYPE_CHECKING: from app.models.roster import Employee, Roster"
      pattern: "if TYPE_CHECKING:"
---

<objective>
Hand-fix every ruff violation left after Plan 12-01's mechanical autofix (revised to exclude SIM117 via `--unfixable SIM117` per user decision), so `uv run ruff check .` exits 0 with the curated ruleset (E, F, I, B, UP, SIM) at line-length 100 and zero blanket ignores — satisfying CI-01 (a push-time lint gate has something real to enforce) and CI-03 (local and CI agree byte-for-byte because there is nothing left to diverge on). This closes the remaining 222 violations (empirically measured with SIM117 excluded from 12-01's autofix): 134 long lines (E501), 46 SIM117 nested-`with` sites — all fully intact, none silently collapsed by 12-01 — resolved here by structural collapse across all 6 affected files — `app/db/repo.py` (33), `app/pipeline/orchestrator.py` (9), `app/db/seed.py` (1), `app/main.py` (1), `eval/run_eval.py` (1, a distinct 3-level nesting collapsing to a 3-clause combined with-statement, not the 2-level shape the other 5 files use), `tests/test_stuck_run_recovery.py` (1) — 8 B904 (raise-without-from), 7 B007 (unused loop vars), 7 F821 (quoted forward-refs — TYPE_CHECKING fix per D-04), 6 SIM115 (bare `open()` without context manager), 3 B905 (`zip()` without `strict=`), 5 E402 (deliberate late imports needing individually-justified noqa — 2 more than originally estimated, a knock-on effect of 12-01's I001/UP037 autofix reformatting a noqa'd single-line import into multi-line form and orphaning the noqa), 2 F841 (unused variable), and one each of B017, SIM108, UP042, UP047.

Purpose: This is the judgment-requiring half of the cleanup — every fix here is a structural code change (context managers, exception chaining, TYPE_CHECKING, combined with-statements) or, for the narrow E402 case, a deliberately justified noqa — never a suppression of convenience, and never a blanket ignore. Keeping it in its own plan (after the mechanical autofix in 12-01) means every commit in this pass is independently reviewable and bisectable.
Output: A fully green `uv run ruff check .` with zero blanket ignores in pyproject.toml and zero `# ruff: noqa: SIM117` directives anywhere in the repo, the 613/50 test suite unchanged, and every individually-justified noqa (E402 only) carrying an inline reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ci-quality-gates/12-CONTEXT.md
@.planning/phases/12-ci-quality-gates/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix F821 forward-refs, B904/B007/B905/B017/SIM108/SIM115/UP042/UP047/E402/F841 violations</name>
  <files>app/main.py, app/llm/client.py, app/email/gateway.py, app/models/status.py, app/pipeline/decide.py, eval/run_eval.py, scripts/show_confirmation_subject.py, tests/conftest.py, tests/test_calculate.py, tests/test_detect_field_regression.py, tests/test_validate.py, tests/test_atomic_persist.py, tests/test_clarify_rounds.py, tests/test_needs_operator.py, tests/test_demo_landing.py, tests/test_gateway.py, tests/test_ingest.py, tests/test_live_llm.py</files>
  <read_first>
app/models/status.py, app/pipeline/decide.py, app/llm/client.py, app/email/gateway.py, app/main.py (only the flagged line ranges: ~274, ~294, ~873, ~1407, ~1428, ~1655), tests/conftest.py (~line 435), tests/test_calculate.py (~line 89 for F821; ~lines 70-84 for the post-12-01-autofix E402 sites at 81/84), tests/test_detect_field_regression.py (~line 65), tests/test_validate.py (~lines 99, 122, 145, 173), tests/test_atomic_persist.py (~lines 246, 430, 686), tests/test_clarify_rounds.py (~lines 289, 378), tests/test_needs_operator.py (~line 237), tests/test_demo_landing.py (~line 52), tests/test_gateway.py (~lines 142, 460, 877), tests/test_ingest.py (~line 125), tests/test_live_llm.py (~line 196)
  </read_first>
  <action>
**Preflight (cross-AI review finding, HIGH):** Run `git status --short` before making any edits. Confirm the only dirty files are the ones this task is about to touch (the `<files>` list above) — do not stage or commit any pre-existing unrelated dirty files (e.g. `.planning` deletions from another in-flight session) that may already be sitting in the working tree. Stage this task's commit(s) by explicit pathspec or `git add -u -- <touched files>`, never `git add -A` / `git commit -a`.

**F821 (7 sites, D-04 — TYPE_CHECKING, not suppression):** In tests/test_calculate.py, tests/test_detect_field_regression.py, and tests/test_validate.py, the functions `_make_salary_employee`, `_make_roster`, `_make_weekly_hourly_employee`, `_make_biweekly_hourly_employee`, `_make_semimonthly_salary_employee`, and `_one_employee_roster` return a quoted forward-reference type (`"Employee"` or `tuple[Roster, "Employee"]`) but only import `Employee`/`Roster` locally inside the function body. Add a module-level `from typing import TYPE_CHECKING` import and a guarded block `if TYPE_CHECKING:\n    from app.models.roster import Employee, Roster` (adjust the exact names imported per file) near the top of each of the 3 files, after existing imports. Confirm `from __future__ import annotations` is present at the top of each file (already present in at least tests/test_calculate.py per 12-PATTERNS.md; add if missing in the other two). Leave the function-body-local imports in place only if they are still needed for runtime construction (e.g. instantiating `Employee(...)`) — the TYPE_CHECKING block only satisfies the annotation resolution, not runtime construction.

**B904 (8 sites in app/main.py + app/llm/client.py):** Each `raise HTTPException(...)` or `raise last_error` inside an `except Exception as exc:` / `except Exception:` block must chain the original exception: `raise HTTPException(status_code=..., detail=...) from exc` (use the bound exception variable name already in scope at each site — some sites currently use bare `except Exception:` with no bound name; add `as exc` to the except clause and chain `from exc`). For `app/llm/client.py:176`'s `raise last_error`, change to `raise last_error from None` only if `last_error` is itself already a full exception object being re-raised as the final one in a retry loop (read the surrounding function to confirm this is the terminal re-raise, not a chained cause) — otherwise chain `from` whatever the immediately preceding caught exception variable is.

**B007 (7 sites):** Rename unused loop-control variables to a leading-underscore name at each site: `app/pipeline/decide.py:63` `emp_id` → `_emp_id`; `scripts/show_confirmation_subject.py:34` `created_at` → `_created_at` and `purpose` → `_purpose`; `tests/conftest.py:435` `run_id_str` → `_run_id_str`; `tests/test_demo_landing.py:52` `params` → `_params`; `tests/test_gateway.py:142` `params` → `_params`; `tests/test_gateway.py:877` `sql` → `_sql`.

**SIM115 (6 sites, all `open(...).read()` without a context manager):** In tests/test_atomic_persist.py (lines ~246, ~430, ~686), tests/test_clarify_rounds.py (lines ~289, ~378), and tests/test_needs_operator.py (line ~237), replace the bare `src = open(src_path).read()` / `src = open(orch_mod.__file__).read()` pattern with `with open(src_path) as f:\n    src = f.read()` (or the equivalent variable name already used at each site).

**B905 (3 sites in eval/run_eval.py, lines ~786, ~796, ~824):** Add explicit `strict=False` to each `zip(bars_fa, field_values)`, `zip(bars_f1, f1_values)`, `zip(bars_rec, rec_data_sorted)` call — these zip matplotlib bar-container objects against parallel value lists that are already guaranteed equal-length by construction (same list comprehension source), so `strict=False` documents "already known equal-length, not a data-integrity check" rather than `strict=True` which would be the safety-critical choice for untrusted-length inputs.

**B017 (1 site, tests/test_live_llm.py:196):** `with pytest.raises(Exception):` around `Decision.model_validate({**base, "source": "live"})` — narrow to the specific exception Pydantic raises for a forbidden extra field: `pydantic.ValidationError`. Read the surrounding test to confirm `pydantic` is already imported in this file; add the import if not.

**SIM108 (1 site, app/email/gateway.py:125):** Replace the `if isinstance(raw, (str, bytes)): data = json.loads(raw) else: data = raw` block with the ternary `data = json.loads(raw) if isinstance(raw, (str, bytes)) else raw`.

**UP042 (1 site, app/models/status.py:9):** `class RunStatus(str, enum.Enum):` → `class RunStatus(enum.StrEnum):`. Since this changes the base class, verify no code relies on `RunStatus` NOT being a `StrEnum` subtlety (e.g. `.value` access, JSON serialization via Pydantic) — `enum.StrEnum` (Python 3.11+, available under the project's 3.12 pin) behaves as a drop-in for `(str, Enum)` for `==`, string formatting, and JSON serialization, so this is behavior-neutral. Remove the now-unused `import enum` reference only if `enum` is still used elsewhere in the file for other enums — check before removing the import.

**UP047 (1 site, app/llm/client.py:120, `call_structured`):** Ruff wants PEP 695 generic type parameter syntax (`def call_structured[T](...)`) instead of a separately-declared `TypeVar` bound as `T`. The existing declaration is `T = TypeVar("T", bound=BaseModel)` (app/llm/client.py:42) — **the PEP 695 rewrite MUST preserve this bound**: `def call_structured[T: BaseModel](tier: Tier, messages: list[dict], response_model: type[T]) -> T:`, NOT an unbounded `def call_structured[T](...)`. Dropping the `BaseModel` bound would silently widen the generic to `object` and lose the type-checker's guarantee that `T` is a Pydantic model — a regression that would only surface as mypy friction in Phase 14, not as a ruff or test failure now, so it must be caught here at review time. Since the project's Python floor is 3.12 (supports PEP 695 bounded type params), apply the bounded form and remove the standalone `T = TypeVar("T", bound=BaseModel)` declaration this function's generic previously relied on IF that TypeVar is not reused elsewhere in the file — check other usages of `T` in app/llm/client.py before removing its declaration.

**E402 (5 sites — individually-justified noqa per D-03, NOT structural fixes, since the late-import position is deliberate; 2 more sites than originally estimated because Plan 12-01's autofix reformatting created them — see below):** tests/test_gateway.py:460 already carries `# noqa: F401` for the same line; add `E402` to the same noqa directive so it reads `import resend  # noqa: F401, E402 — installed via 06-01 Task 1; needed for monkeypatching, imported late to keep the patch target order documented above`. tests/test_ingest.py:125-126 (`import os` / `import uuid as _uuid_module`) currently has no noqa — read the `# ===...` banner comment immediately above (line 123) to confirm it documents a deliberate late-import reason, then add `# noqa: E402 — <the stated reason from the banner comment>` to both import lines. tests/test_calculate.py:81/84 are new post-autofix sites: Plan 12-01's I001/UP037 pass reformatted the originally single-line `from app.pipeline.calculate import PayrollCalculationError, _raise_if_reconciliation_drift  # noqa: E402` and a sibling import into multi-line parenthesized form, which orphaned the noqa comment from the newly-created continuation lines. Read the surrounding `# ---- Phase 3 additions ----` banner comment (a few lines above) to confirm it documents the same deliberate late-import reason already used by the file's other noqa'd imports (`uuid`, `pytest` — appended after existing imports, safe stdlib/already-a-dependency re-imports), then add `# noqa: E402 — <the stated reason from the banner comment>` to both of these now-multi-line import statements' opening lines.

**F841 (2 sites — find via `uv run ruff check --select F841 --line-length 100 .` since exact sites were not read during planning):** Read each flagged line; if the unused variable is genuinely dead code, delete the assignment. If it exists for readability/documentation purposes (e.g. capturing an intermediate value for a debugger breakpoint or clarity), prefix with `_` instead of deleting.
  </action>
  <acceptance_criteria>
    - `uv run ruff check --select F821,B904,B007,B905,B017,SIM108,SIM115,UP042,UP047,E402,F841 --line-length 100 .` exits 0 with "All checks passed!"
    - `uv run pytest -q` exits 0, reports "613 passed, 50 skipped"
    - `grep -rn "if TYPE_CHECKING:" tests/test_calculate.py tests/test_detect_field_regression.py tests/test_validate.py` returns at least one match per file
    - `grep -n "noqa: E402" tests/test_gateway.py tests/test_ingest.py tests/test_calculate.py` returns matches on all 5 flagged lines (460 in test_gateway.py; 125 and 126 in test_ingest.py; 81 and 84 in test_calculate.py — the latter two are new post-12-01-autofix sites, not part of the original 3-site estimate)
    - `grep -n "class RunStatus" app/models/status.py` shows `class RunStatus(enum.StrEnum):`
    - `grep -n "def call_structured\[T: BaseModel\]" app/llm/client.py` matches — the PEP 695 generic bound survives the rewrite (NOT an unbounded `def call_structured[T](...)`); `grep -c "TypeVar" app/llm/client.py` reflects the standalone `TypeVar("T", bound=BaseModel)` declaration being removed only if `T` has no other usages in the file
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check --select F821,B904,B007,B905,B017,SIM108,SIM115,UP042,UP047,E402,F841 --line-length 100 . && uv run pytest -q 2>&1 | tail -1 | grep -q "613 passed, 50 skipped"</automated>
  </verify>
  <done>All 12 non-E501/non-SIM117 violation categories from the post-autofix baseline are resolved — either by structural fix (TYPE_CHECKING, exception chaining, context managers, strict= kwarg, StrEnum, PEP 695 generics, ternary) or by an individually-justified inline noqa naming the rule code and reason — and the 613/50 test suite is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Collapse all SIM117 nested-with sites repo-wide and fix E501 (long lines repo-wide), verify fully green ruff + suite</name>
  <files>app/db/repo.py, app/pipeline/orchestrator.py, app/db/seed.py, app/main.py, eval/run_eval.py, tests/test_stuck_run_recovery.py, app/pipeline/calculate.py, app/pipeline/validate.py, app/db/bootstrap.py, scripts/reset_stuck_runs.py, tests/test_demo_landing.py, tests/test_resume_pipeline.py, tests/test_calculate.py, tests/test_detect_field_regression.py, tests/test_dashboard.py, tests/test_alias_write.py, tests/conftest.py, tests/test_federal_withholding.py, tests/test_gateway.py, tests/test_compose_email_field_regression.py, tests/test_alias_full_loop.py, tests/test_threading.py</files>
  <read_first>app/db/repo.py (lines 180-430 for the repeated `_conn_ctx(conn) as (c, owns): with c.transaction() if owns else _nulltx():` idiom), app/pipeline/orchestrator.py (line ~809 for the `with repo.get_connection() as conn: with conn.transaction():` idiom, repeated at the other 8 flagged sites), app/db/seed.py (line ~307 for the `with get_connection() as conn: with conn.transaction():` idiom — this is the one SIM117 site ruff does NOT mark auto-fixable, so hand-collapse it carefully), app/main.py (line ~405), eval/run_eval.py (lines ~953-965 — 3-level nesting: `psycopg.connect` -> `conn.transaction()` -> `conn.cursor()`, distinct shape from the other 2-level sites), tests/test_stuck_run_recovery.py (line ~271)</read_first>
  <action>
**Preflight (cross-AI review finding, HIGH):** Run `git status --short` before making any edits. Confirm the only dirty files are the ones this task is about to touch (the `<files>` list above) — do not stage or commit any pre-existing unrelated dirty files (e.g. `.planning` deletions from another in-flight session) that may already be sitting in the working tree. Stage this task's commit(s) by explicit pathspec or `git add -u -- <touched files>`, never `git add -A` / `git commit -a`.

**SIM117 (46 sites across 6 files — structural collapse repo-wide per user decision superseding the original noqa approach; NO `# ruff: noqa: SIM117` directive anywhere, module-level or per-line):** Distribution — `app/db/repo.py` (33), `app/pipeline/orchestrator.py` (9), `app/db/seed.py` (1), `app/main.py` (1), `eval/run_eval.py` (1), `tests/test_stuck_run_recovery.py` (1). Collapse every nested pair into a single combined with-statement, left-to-right, where the second context manager references the first's `as` binding — this is semantically identical to the nesting (both managers still enter/exit in the same order, and the second still sees the first's bound name).

Two shapes recur:
1. `app/db/repo.py`'s dominant idiom: `with _conn_ctx(conn) as (c, owns):
    with c.transaction() if owns else _nulltx():` (and its cursor variant `with _conn_ctx(conn) as (c, _owns):
    with c.cursor(row_factory=...) as cur:`) collapses to `with _conn_ctx(conn) as (c, owns), (c.transaction() if owns else _nulltx()):` (or `with _conn_ctx(conn) as (c, _owns), c.cursor(row_factory=...) as cur:` for the cursor variant) — the second manager's expression is unchanged, only the `with`/indentation nesting is removed. Apply this at all 33 sites in this file.
2. `app/pipeline/orchestrator.py`, `app/db/seed.py`, `app/main.py`, `tests/test_stuck_run_recovery.py`'s shared idiom: `with <get-connection-call> as conn:
    with conn.transaction():` collapses to `with <get-connection-call> as conn, conn.transaction():` — the second manager references `conn`, the first's binding. Apply this at all remaining 2-level sites (9 in orchestrator.py, 1 each in the other three files).

3. `eval/run_eval.py`'s site (line ~953-955) is a 3-level nesting, not the 2-level shape 2 above — `with psycopg.connect(db_url) as conn:` → `with conn.transaction():` → `with conn.cursor() as cur:` — and collapses to a single 3-clause combined with-statement: `with psycopg.connect(db_url) as conn, conn.transaction(), conn.cursor() as cur:`. The body (the `cur.executemany(...)` call and its multi-line SQL string) is unchanged, only re-indented one level up to sit directly under the new single `with`. While inspecting this diff, spot-check the SQL string's existing indentation — the triple-quoted `INSERT INTO eval_results ...` string has a pre-existing indentation artifact (extra leading whitespace inside the string literal, independent of this collapse) worth a visual check to confirm the collapse doesn't accidentally reformat the string content itself (it must not — only the `with` structure and the block's indentation level change, never string literal contents).

For the 45 sites ruff marks auto-fixable (`[*]`), running `uv run ruff check --select SIM117 --line-length 100 --fix --unsafe-fixes .` performs this exact left-to-right collapse mechanically and is the fastest correct path — inspect the diff afterward to confirm no logic changed, only the `with` structure. The one remaining site (`app/db/seed.py:307`, not marked auto-fixable) must be hand-collapsed following the same shape-2 pattern above. After the fix (auto or hand), re-run the body of each collapsed block: the code inside must be unchanged (same statements, same indentation level relative to the new single `with`), so a pure structural diff — no SQL string changes, no reordered statements. Do not touch `_conn_ctx`, `_nulltx`, or `conn.transaction()` themselves; only the call-site `with` nesting changes.

**E501 (134 lines across ~19 files, purely mechanical — no logic changes):** Run `uv run ruff check --select E501 --line-length 100 --output-format concise .` to get the current file:line list (baseline concentration: tests/test_demo_landing.py ~20, tests/test_resume_pipeline.py ~19, tests/test_calculate.py ~13, eval/run_eval.py ~10, tests/test_detect_field_regression.py ~7, tests/test_dashboard.py ~7, tests/test_alias_write.py ~7, app/pipeline/orchestrator.py ~6, tests/conftest.py ~5, app/main.py ~5, app/pipeline/calculate.py ~4, tests/test_federal_withholding.py ~3, app/db/repo.py ~3, tests/test_gateway.py ~2, tests/test_compose_email_field_regression.py ~2, tests/test_alias_full_loop.py ~2, scripts/reset_stuck_runs.py ~2, app/pipeline/validate.py ~2, app/db/bootstrap.py ~2, tests/test_threading.py ~1). For each flagged line, wrap manually using standard Python continuation (parenthesized multi-line expressions, breaking at logical operators/commas, or splitting long string literals with implicit concatenation) so the line fits within 100 characters. Do NOT change any string content, log message text, comment meaning, or code semantics — this is pure re-wrapping. Prefer wrapping at existing argument boundaries (function calls, dict/list literals) over introducing backslash continuations.
  </action>
  <acceptance_criteria>
    - `uv run ruff check .` (config-driven, no flags) exits 0 and prints "All checks passed!"
    - `uv run ruff check --select SIM117 --line-length 100 .` reports "All checks passed!" (zero SIM117 violations remaining across all 6 previously-flagged files)
    - `grep -rn "ruff: noqa: SIM117" .` returns zero matches anywhere in the repo (no module-level or per-line SIM117 suppression exists)
    - `uv run ruff check --statistics .` reports zero total errors (down from the 222 remaining after Plan 12-01's autofix pass, empirically measured with SIM117 excluded via `--unfixable SIM117`)
    - `uv run pytest -q` exits 0, reports "613 passed, 50 skipped"
    - `git diff --stat` for this task's commit(s) shows no changes to files outside the listed `<files>` and no changes to string literal contents (spot-check: `git diff app/db/repo.py | grep -E '^[+-]' | grep -v '^+++\|^---'` shows only whitespace/wrapping and with-statement-merge changes, not altered SQL strings or log messages)
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check . && uv run ruff check --select SIM117 --line-length 100 . && uv run pytest -q 2>&1 | tail -1 | grep -q "613 passed, 50 skipped"</automated>
  </verify>
  <done>`uv run ruff check .` exits 0 with zero violations under the committed config — the full curated ruleset is clean repo-wide with zero blanket ignores in pyproject.toml and zero `# ruff: noqa: SIM117` directives anywhere in the repo (all 46 SIM117 sites structurally collapsed into single combined with-statements) — and the 613/50 test suite is unchanged. Commit this work (autofix-adjacent hand-fixes from Task 1, then SIM117+E501 from this task) as one or more behavior-neutral commits, e.g. `style(12): hand-fix remaining ruff violations — TYPE_CHECKING, exception chaining, line length, SIM117 collapse`, staged by explicit pathspec/`git add -u` and never `-A`/`-a`, so no unrelated dirty files from the working tree are swept in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Hand-edited source → committed repo | Manual code changes across app/eval/scripts/tests; test suite is the safety net |
| Structural SIM117 collapse across 6 files (33 in repo.py, 9 in orchestrator.py, 1 each in seed.py/main.py/run_eval.py/test_stuck_run_recovery.py) | Nested `with` blocks merged into single combined with-statements; connection/transaction/cursor semantics must be preserved exactly |
| Dirty working tree → git commit | Unrelated pre-existing dirty/staged files (e.g. mid-flight `.planning` deletions from another session) could be accidentally swept into this plan's commits if staged carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Tampering | Manual raise/except chaining edits (B904) in app/main.py, app/llm/client.py | mitigate | Each `from exc` addition is verified not to swallow or alter the original exception's status code / detail message; suite re-run green after |
| T-12-04 | Tampering | `enum.StrEnum` base-class change on RunStatus | mitigate | Verified behavior-neutral for `==`, string formatting, and Pydantic/JSON serialization before commit; full suite re-run green (this status enum is used across dashboard/DB/webhook state-machine code, so a subtle behavior change here would be high-blast-radius) |
| T-12-05 | Tampering | Structural SIM117 collapse (46 sites, 6 files) merging connection-ownership and transaction/cursor scoping into one with-statement | mitigate | Left-to-right binding order preserved exactly (second manager references the first's `as` name, matching nesting semantics); auto-fix used for the 45 `[*]`-eligible sites with post-fix diff inspection, the 1 non-auto-fixable site (app/db/seed.py:307) hand-collapsed against the same pattern; full 613/50 suite re-run green after, per D-03 (zero noqa, zero blanket ignores — every site genuinely fixed) |
| T-12-12 | Tampering | Dirty working tree causing unrelated files to be swept into this plan's commits | mitigate | Both tasks preflight with `git status --short` before editing, and stage only this task's own explicit paths (pathspec or `git add -u -- <touched files>`) — never `git add -A` / `git commit -a` |
</threat_model>

<verification>
- `uv run ruff check .` exits 0, "All checks passed!"
- `uv run pytest -q` exits 0, "613 passed, 50 skipped"
- `uv run ruff check --select SIM117 --line-length 100 .` exits 0, "All checks passed!"
- `grep -rn "ruff: noqa: SIM117" .` — zero matches anywhere in the repo
- No `ignore = [...]` or `[tool.ruff.lint.per-file-ignores]` added to pyproject.toml (still matches Plan 12-01's Task 1 output)
</verification>

<success_criteria>
`uv run ruff check .` is fully green under the committed curated ruleset with zero pyproject.toml-level ignores and zero `# ruff: noqa: SIM117` directives anywhere in the repo — all 46 SIM117 sites across all 6 affected files were structurally fixed, not suppressed (CI-01's future gate has a clean baseline to enforce; CI-03's byte-for-byte local/CI agreement holds because nothing is suppressed at any layer). The 613/50 test suite is unchanged throughout.
</success_criteria>

<output>
Create `.planning/phases/12-ci-quality-gates/12-02-SUMMARY.md` when done
</output>
