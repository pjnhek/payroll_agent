---
phase: 12-ci-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/**/*.py
  - eval/**/*.py
  - scripts/**/*.py
  - tests/**/*.py
autonomous: true
requirements: [CI-03]

must_haves:
  truths:
    - "Running `uv run ruff check .` locally uses the exact curated ruleset (E,F,I,B,UP,SIM) and line-length 100 the committed config defines — no ad-hoc CLI flags needed to reproduce CI's result"
    - "The mechanical, safe-to-autofix violation classes (unsorted imports, datetime.UTC modernization, unused imports, quoted-annotation modernization, yoda conditions, deprecated typing imports) are gone from the repo"
    - "The 663-test suite is still fully green after the autofix pass — the cleanup changed no behavior"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.ruff] and [tool.ruff.lint] sections defining line-length=100 and select=[E,F,I,B,UP,SIM]"
      contains: "[tool.ruff.lint]"
  key_links:
    - from: "pyproject.toml [tool.ruff]"
      to: "uv run ruff check ."
      via: "ruff auto-discovers pyproject.toml config with no CLI flags"
      pattern: "line-length = 100"
---

<objective>
Add the committed ruff configuration (curated ruleset + line length, per D-01/D-02/D-06) to `pyproject.toml`, then run ruff's mechanical autofixer across the whole repo to clear the ~241 auto-fixable violations (of 416 total baseline) — import sorting, `datetime.timezone.utc` → `datetime.UTC` modernization, unused imports, quoted-annotation cleanup, yoda conditions, deprecated typing imports. Confirm the 613-passed/50-skipped hermetic suite stays green throughout.

Purpose: This is the mechanical bulk of the phase's cleanup diff — get it landed, verified, and committed in isolation before the smaller set of hand-fixes (Plan 12-02) that require real judgment calls (exception chaining, TYPE_CHECKING imports, SIM117 review). Splitting mechanical-autofix from hand-fix keeps each commit behavior-neutral and independently bisectable if the suite ever regresses.
Output: A committed `[tool.ruff]` / `[tool.ruff.lint]` config block in `pyproject.toml`, and a repo where all `--fix`-eligible violations under the curated ruleset are resolved, with the test suite still at 613 passed / 50 skipped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ci-quality-gates/12-CONTEXT.md
@.planning/phases/12-ci-quality-gates/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add committed ruff config to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
Add two new sections to pyproject.toml, placed after the existing `[tool.pytest.ini_options]` block (which ends at line 43), following that section's house style of a `#` comment above each option explaining why it's set:

`[tool.ruff]` with `line-length = 100` (D-02: measured tradeoff between the 160 lines that exceed 100 vs the 1,297 that exceed 88).

`[tool.ruff.lint]` with `select = ["E", "F", "I", "B", "UP", "SIM"]` (D-01: curated production ruleset — pycodestyle errors, pyflakes, isort import-sorting, flake8-bugbear, pyupgrade, flake8-simplify — not the bare defaults). Do NOT add any `ignore` list and do NOT add a `[tool.ruff.lint.per-file-ignores]` table — D-03 forbids blanket ignores and per-file-ignore blocks; every violation gets fixed or an individually-justified inline `# noqa: <CODE> — <reason>` in Plan 12-02.

`ruff` is already declared in `[dependency-groups].dev` (line 27) — no dependency change, config-only addition. Do not touch `[tool.pytest.ini_options]` or any other existing section.
  </action>
  <acceptance_criteria>
    - pyproject.toml contains a `[tool.ruff]` section with `line-length = 100`
    - pyproject.toml contains a `[tool.ruff.lint]` section with `select = ["E", "F", "I", "B", "UP", "SIM"]`
    - pyproject.toml contains no `[tool.ruff.lint.ignore]`, no bare `ignore = [...]` under `[tool.ruff.lint]`, and no `[tool.ruff.lint.per-file-ignores]` table
    - `uv run ruff check --statistics .` (no extra flags) reports "Found 416 errors" (or fewer, if uv/ruff version drift changes counts slightly) matching the same rule mix measured in 12-PATTERNS.md, proving the config is picked up with zero CLI flags
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check --statistics . | grep -q "Found"</automated>
  </verify>
  <done>pyproject.toml has both new sections committed; `uv run ruff check .` with no flags reproduces the same violation set as `uv run ruff check --select E,F,I,B,UP,SIM --line-length 100 .` (i.e. the config file alone drives the result — no divergence between config-driven and flag-driven output).</done>
</task>

<task type="auto">
  <name>Task 2: Run mechanical autofix pass, verify suite green, commit</name>
  <files>app/**/*.py, eval/**/*.py, scripts/**/*.py, tests/**/*.py</files>
  <read_first>pyproject.toml</read_first>
  <action>
Run `uv run ruff check --fix .` (no extra flags — the config from Task 1 supplies the ruleset and line-length). This resolves the safe autofixable classes measured in 12-PATTERNS.md: I001 (unsorted-imports, 82), UP017 (datetime-timezone-utc, 64), F401 (unused-import, 30), UP037 (quoted-annotation, 16), SIM300 (yoda-conditions, 3), UP035 (deprecated-import, 1) — 196 violations in total. Do NOT pass `--unsafe-fixes` (that would also attempt SIM117's 46 unsafe fixes, which Plan 12-02 handles by hand-review per D-03's "individually justified" rule, since collapsing nested `with` blocks in app/db/repo.py's transaction-context idiom needs a human-legible judgment call, not a blind autofix).

After the fix runs, run `uv run pytest -q` and confirm it still reports 613 passed, 50 skipped (the same baseline captured during planning) — zero assertion changes, since this is purely import/syntax-shape cleanup with no logic touched.

Then run `uv run ruff check --statistics .` again and confirm the remaining violation count has dropped by the ~196 autofixed classes (E501 134, SIM117 46, B904 8, B007 7, F821 7, SIM115 6, B905 3, E402 3, F841 2, B017 1, SIM108 1, UP042 1, UP047 1 should remain — approximately 220 violations left for Plan 12-02 to hand-fix).

Commit this autofix diff as its own commit, separate from the config commit and separate from the upcoming hand-fix commits (Claude's Discretion, 12-CONTEXT.md) — e.g. `git commit -m "style(12): ruff autofix — imports, datetime.UTC, unused imports"`.
  </action>
  <acceptance_criteria>
    - `uv run ruff check --statistics .` no longer lists I001, UP017, F401, UP037, SIM300, or UP035 in its output
    - `uv run pytest -q` exits 0 and reports "613 passed, 50 skipped" (no new failures, no new skips, no fewer tests collected)
    - `git log --oneline -3` shows a distinct commit for the autofix diff, separate from the pyproject.toml config commit
    - `git diff` for the autofix commit touches only import ordering, `datetime.timezone.utc` → `datetime.UTC` rewrites, removed unused imports, unquoted type annotations, non-yoda comparisons, and modern typing imports — no changes to function bodies' control flow or return values
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -1 | grep -q "613 passed, 50 skipped"</automated>
  </verify>
  <done>The mechanical autofix is applied, committed on its own, and the test suite is verified green at 613 passed / 50 skipped immediately after — proving the autofix pass was behavior-neutral before any hand-fix work begins in Plan 12-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine → committed pyproject.toml | Config change only; no runtime/network surface |
| ruff autofix → source tree | Automated code rewrite; test suite is the safety net that catches any accidental behavior change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ruff `--fix` rewriting source files | mitigate | Task 2 restricts to safe autofixes only (no `--unsafe-fixes`), and the full test suite is re-run and asserted green (613/50) immediately after the fix, before commit |
| T-12-02 | Information Disclosure | pyproject.toml config commit | accept | Config-only change, no secrets or credentials touched |
</threat_model>

<verification>
- `uv run ruff check --statistics .` output no longer lists any of the six autofixed rule codes (I001, UP017, F401, UP037, SIM300, UP035)
- `uv run pytest -q` exits 0, reports "613 passed, 50 skipped"
- `git log` shows two distinct commits: config addition, then autofix diff
</verification>

<success_criteria>
pyproject.toml carries the committed ruff config (CI-03) with zero ad-hoc CLI flags needed to reproduce results; the ~196 mechanically-autofixable violations are resolved repo-wide; the 613-test suite is unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/12-ci-quality-gates/12-01-SUMMARY.md` when done
</output>
