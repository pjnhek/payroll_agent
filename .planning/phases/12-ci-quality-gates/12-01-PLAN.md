---
phase: 12-ci-quality-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - app/**/*.py
  - eval/**/*.py
  - scripts/**/*.py
  - tests/**/*.py
autonomous: true
requirements: [CI-03]

must_haves:
  truths:
    - "Running `uv run ruff check .` locally uses the exact curated ruleset (E,F,I,B,UP,SIM) and line-length 100 the committed config defines — no ad-hoc CLI flags needed to reproduce CI's result"
    - "The mechanical, safe-to-autofix violation classes (unsorted imports, datetime.UTC modernization, unused imports, quoted-annotation modernization, yoda conditions, deprecated typing imports) are gone from the repo"
    - "The 663-test suite is still fully green after the autofix pass — the cleanup changed no behavior"
  artifacts:
    - path: "pyproject.toml"
      provides: "[tool.ruff] and [tool.ruff.lint] sections defining line-length=100 and select=[E,F,I,B,UP,SIM]"
      contains: "[tool.ruff.lint]"
  key_links:
    - from: "pyproject.toml [tool.ruff]"
      to: "uv run ruff check ."
      via: "ruff auto-discovers pyproject.toml config with no CLI flags"
      pattern: "line-length = 100"
---

<objective>
Add the committed ruff configuration (curated ruleset + line length, per D-01/D-02/D-06) to `pyproject.toml`, then run ruff's mechanical autofixer — with SIM117 explicitly excluded via `--unfixable SIM117` — across the whole repo to clear the safe auto-fixable violations except SIM117 (of 416 total baseline). Empirically measured against ruff 0.15.18 (the version pinned in uv.lock): `uv run ruff check --fix --unfixable SIM117 .` fixes 269 violations (measured 2026-07-08), leaving 222 (measured 2026-07-08) — import sorting, `datetime.timezone.utc` → `datetime.UTC` modernization, unused imports, quoted-annotation cleanup, yoda conditions, deprecated typing imports are all resolved; all 46 SIM117 nested-`with` sites are deliberately left untouched for Plan 12-02. Confirm the 613-passed/50-skipped hermetic suite stays green throughout.

Purpose: This is the mechanical bulk of the phase's cleanup diff — get it landed, verified, and committed in isolation before the smaller set of hand-fixes (Plan 12-02) that require real judgment calls (exception chaining, TYPE_CHECKING imports, and — per user decision — the entire SIM117 structural collapse). SIM117 is excluded from this task's autofix specifically because, contrary to 12-PATTERNS.md's original baseline claim, SIM117 is a SAFE fix under ruff 0.15.18 (not unsafe) — so a plain `--fix .` here would silently collapse 45 of 46 nested-`with` sites as an undocumented side effect of the "mechanical" commit, breaking the plan's own acceptance criterion that this commit touches no control-flow structure, and making Plan 12-02's deliberate, diff-inspected, bisectable SIM117 pass vacuous for all but 1 site. Splitting mechanical-autofix from hand-fix (with SIM117 fully reserved for the hand-fix pass) keeps each commit behavior-neutral and independently bisectable if the suite ever regresses.
Output: A committed `[tool.ruff]` / `[tool.ruff.lint]` config block in `pyproject.toml`, and a repo where all `--fix`-eligible violations under the curated ruleset (except SIM117) are resolved, with the test suite still at 613 passed / 50 skipped, and all 46 SIM117 sites intact for Plan 12-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ci-quality-gates/12-CONTEXT.md
@.planning/phases/12-ci-quality-gates/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add committed ruff config to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>
**Preflight (cross-AI review finding, HIGH):** Before editing anything, run `git status --short`. The working tree may already carry unrelated dirty/staged files from other in-flight work (e.g. `.planning` deletions from a prior milestone-archival session) — confirm which files are pre-existing dirty vs. which you are about to create, and never use `git add -A` or `git commit -a` for this plan's commits. Stage only the exact paths this task touches (`pyproject.toml`) by explicit pathspec.

Add two new sections to pyproject.toml, placed after the existing `[tool.pytest.ini_options]` block (which ends at line 43), following that section's house style of a `#` comment above each option explaining why it's set:

`[tool.ruff]` with `line-length = 100` (D-02: measured tradeoff between the 160 lines that exceed 100 vs the 1,297 that exceed 88) and `target-version = "py312"` (explicit pin matching the project's `.python-version`/Docker target, even though ruff can infer this from `requires-python` — makes the target version legible directly in the ruff config block rather than only indirectly via `[project]`).

`[tool.ruff.lint]` with `select = ["E", "F", "I", "B", "UP", "SIM"]` (D-01: curated production ruleset — pycodestyle errors, pyflakes, isort import-sorting, flake8-bugbear, pyupgrade, flake8-simplify — not the bare defaults). Do NOT add any `ignore` list and do NOT add a `[tool.ruff.lint.per-file-ignores]` table — D-03 forbids blanket ignores and per-file-ignore blocks; every violation gets fixed or an individually-justified inline `# noqa: <CODE> — <reason>` in Plan 12-02.

`ruff` is already declared in `[dependency-groups].dev` (line 27) — no dependency change, config-only addition. Do not touch `[tool.pytest.ini_options]` or any other existing section.
  </action>
  <acceptance_criteria>
    - `git status --short` was run before editing and only expected files are treated as this task's own dirty set — the commit for this task stages `pyproject.toml` only, by explicit pathspec (`git add pyproject.toml`), never `git add -A` / `git commit -a`
    - pyproject.toml contains a `[tool.ruff]` section with `line-length = 100` and `target-version = "py312"`
    - pyproject.toml contains a `[tool.ruff.lint]` section with `select = ["E", "F", "I", "B", "UP", "SIM"]`
    - pyproject.toml contains no `[tool.ruff.lint.ignore]`, no bare `ignore = [...]` under `[tool.ruff.lint]`, and no `[tool.ruff.lint.per-file-ignores]` table
    - `uv run ruff check --statistics .` (no extra flags) reports "Found 416 errors" (or fewer, if uv/ruff version drift changes counts slightly) matching the same rule mix measured in 12-PATTERNS.md, proving the config is picked up with zero CLI flags
  </acceptance_criteria>
  <verify>
    <automated>uv run ruff check --statistics . | grep -q "Found"</automated>
  </verify>
  <done>pyproject.toml has both new sections committed (staged by explicit pathspec, not `-A`/`-a`); `uv run ruff check .` with no flags reproduces the same violation set as `uv run ruff check --select E,F,I,B,UP,SIM --line-length 100 --target-version py312 .` (i.e. the config file alone drives the result — no divergence between config-driven and flag-driven output).</done>
</task>

<task type="auto">
  <name>Task 2: Run mechanical autofix pass, verify suite green, commit</name>
  <files>app/**/*.py, eval/**/*.py, scripts/**/*.py, tests/**/*.py</files>
  <read_first>pyproject.toml</read_first>
  <action>
**Preflight (cross-AI review finding, HIGH):** Run `git status --short` again before running the autofixer. Confirm the only files that become dirty as a result of this task's `ruff --fix` run are `.py` files under `app/`, `eval/`, `scripts/`, `tests/` — do not stage or commit any pre-existing unrelated dirty files (e.g. `.planning` deletions) that may already be sitting in the working tree from other in-flight work. Stage only the files this task's autofix run actually touched, by explicit pathspec or `git add -u -- app eval scripts tests` (never `git add -A` / `git commit -a`, which would sweep in unrelated staged/unstaged deletions elsewhere in the tree).

Run `uv run ruff check --fix --unfixable SIM117 .` — NOT plain `--fix .` (empirically verified against ruff 0.15.18, the version pinned in uv.lock: plain `--fix .` silently collapses 45 of 46 SIM117 nested-`with` sites as a side effect, because SIM117 is a SAFE fix in this ruff version, not unsafe as 12-PATTERNS.md's original baseline table claimed — the `[-]` marker in `ruff check --statistics` output means "fixable but hidden from the default fix pass count," not "unsafe"; it is unrelated to `--unsafe-fixes`, which only gates the 15 fixes ruff considers behaviorally risky). `--unfixable SIM117` is the verified flag that blocks ruff from touching SIM117 while still fixing everything else the curated ruleset marks safe: I001 (unsorted-imports, 82), UP017 (datetime-timezone-utc, 64), F401 (unused-import, 30), UP037 (quoted-annotation, 16), SIM300 (yoda-conditions, 3), UP035 (deprecated-import, 1), plus additional E501/E402 shape changes triggered as knock-on effects of those rewrites — measured (2026-07-08) as **269 fixed, 222 remaining** with all 46 SIM117 sites intact (confirmed via `ruff check --select SIM117 --output-format concise .` returning all 46 original sites, not 1). This CLI flag is the one-time, non-committed choice (see Task 1 addendum below on why this isn't expressed as a committed `pyproject.toml` config): once Plan 12-02 finishes collapsing every SIM117 site, an `unfixable` config entry would be permanent dead weight for a rule class that no longer has any live violations to guard.

Do NOT pass `--unsafe-fixes` (that would also attempt the 15 unsafe fixes ruff separately flags, unrelated to SIM117, which stay in scope for Plan 12-02's hand-review per D-03's "individually justified" rule).

After the fix runs, run `uv run pytest -q` and confirm it still reports 613 passed, 50 skipped (the same baseline captured during planning) — zero assertion changes, since this is purely import/syntax-shape cleanup with no logic touched.

Then run the rule-absence gate (NOT an exact-count assertion — ruff's count semantics across autofixed knock-on effects make exact totals brittle; the six autofixed rule codes being absent is the real, robust signal): `uv run ruff check --select I001,UP017,F401,UP037,SIM300,UP035 .` must report zero violations ("All checks passed!"). Separately, `uv run ruff check --select SIM117 .` must still report exactly 46 violations (proving the exclusion held, not a silent partial collapse). As informational context only (not a pass/fail gate), `uv run ruff check --statistics .` was measured at "222 violations remaining" on 2026-07-08 — E501 134, SIM117 46 (untouched, all 46 sites preserved for Plan 12-02's deliberate collapse pass), B904 8, B007 7, F821 7, SIM115 6, E402 5 (up from the original 3-site estimate — two new sites appear in tests/test_calculate.py:81/84 as a knock-on effect of I001/UP037 reformatting a single-line `# noqa: E402`-commented import into a multi-line parenthesized import, which orphans the noqa from the newly-created import line; Plan 12-02 Task 1's per-file E402 handling covers this), B905 3, F841 2, B017 1, SIM108 1, UP042 1, UP047 1. Record the actual measured count in the SUMMARY, but do not gate pass/fail on it matching exactly — ruff's own count semantics (e.g. how knock-on E501/E402 reshaping interacts with rule counting) make a hard-coded total brittle across ruff versions.

Commit this autofix diff as its own commit, separate from the config commit and separate from the upcoming hand-fix commits (Claude's Discretion, 12-CONTEXT.md) — e.g. `git commit -m "style(12): ruff autofix — imports, datetime.UTC, unused imports (SIM117 excluded)"`. Stage only the modified `.py` files (by pathspec or `git add -u -- app eval scripts tests`), never `-A`/`-a`.
  </action>
  <acceptance_criteria>
    - `uv run ruff check --select I001,UP017,F401,UP037,SIM300,UP035 .` reports "All checks passed!" (rule-absence gate — the six autofixed codes are fully gone; this replaces any exact-total assertion, which is brittle across ruff versions/knock-on effects)
    - `uv run ruff check --select SIM117 .` reports exactly 46 violations (proves the exclusion worked — SIM117 was NOT silently fixed by this task; measured baseline 46 sites as of 2026-07-08)
    - `uv run ruff check --statistics .` reports "Found 222 errors" as measured on 2026-07-08 with SIM117 excluded — recorded as informational context in the SUMMARY, not treated as a strict pass/fail gate (ruff count semantics can shift slightly across versions; the rule-absence gates above are the actual acceptance signal)
    - `uv run pytest -q` exits 0 and reports "613 passed, 50 skipped" (no new failures, no new skips, no fewer tests collected)
    - `git log --oneline -3` shows a distinct commit for the autofix diff, separate from the pyproject.toml config commit, staged by explicit pathspec/`-u` (not `-A`/`-a`) and touching only `.py` files under app/eval/scripts/tests
    - `git diff` for the autofix commit touches import ordering, `datetime.timezone.utc` → `datetime.UTC` rewrites, removed unused imports, unquoted type annotations, non-yoda comparisons, modern typing imports, and (as knock-on reformatting) the shape of a small number of already-multi-line-eligible import statements — it must NOT touch any nested `with`-statement structure anywhere in the repo (SIM117 is fully excluded from this commit; verify via `git diff | grep -c "^\+.*with "` staying at 0, or manual scan if that grep is noisy) and must NOT change any function body's control flow or return values
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -1 | grep -q "613 passed, 50 skipped"</automated>
  </verify>
  <done>The mechanical autofix is applied via `uv run ruff check --fix --unfixable SIM117 .` (verified against ruff 0.15.18: rule-absence gate confirms I001/UP017/F401/UP037/SIM300/UP035 all clear, SIM117 still reports exactly 46, measured total 222 recorded as informational context), committed on its own by explicit pathspec, and the test suite is verified green at 613 passed / 50 skipped immediately after — proving the autofix pass was behavior-neutral AND left every SIM117 site untouched for Plan 12-02's deliberate, diff-inspected, separately bisectable structural-collapse pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local dev machine → committed pyproject.toml | Config change only; no runtime/network surface |
| ruff autofix → source tree | Automated code rewrite; test suite is the safety net that catches any accidental behavior change |
| Dirty working tree → git commit | Unrelated pre-existing dirty/staged files (e.g. mid-flight `.planning` deletions from another session) could be accidentally swept into this plan's commits if staged carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ruff `--fix` rewriting source files | mitigate | Task 2 restricts to safe autofixes only, explicitly excluding SIM117 via `--unfixable SIM117` (empirically verified: plain `--fix .` would silently collapse 45/46 nested-`with` sites under ruff 0.15.18, since SIM117 is a safe fix in this version, not unsafe) and no `--unsafe-fixes`; the full test suite is re-run and asserted green (613/50) immediately after the fix, before commit |
| T-12-02 | Information Disclosure | pyproject.toml config commit | accept | Config-only change, no secrets or credentials touched |
| T-12-11 | Tampering | Dirty working tree causing unrelated files to be swept into this plan's commits | mitigate | Both tasks preflight with `git status --short` before editing/running autofix, and stage only this task's own explicit paths (`git add pyproject.toml`, or `git add -u -- app eval scripts tests`) — never `git add -A` / `git commit -a` |
</threat_model>

<verification>
- `uv run ruff check --select I001,UP017,F401,UP037,SIM300,UP035 .` reports "All checks passed!" (rule-absence gate for the six autofixed codes)
- `uv run ruff check --select SIM117 .` reports exactly 46 violations (proves exclusion held, not a silent collapse)
- `uv run ruff check --statistics .` reports "Found 222 errors" as measured 2026-07-08 (informational context, not a strict gate)
- `uv run pytest -q` exits 0, reports "613 passed, 50 skipped"
- `git log` shows two distinct commits: config addition, then autofix diff — each staged by explicit pathspec, not `-A`/`-a`
- `git status --short` was checked before each commit to confirm no unrelated dirty files were swept in
</verification>

<success_criteria>
pyproject.toml carries the committed ruff config (CI-03) with zero ad-hoc CLI flags needed to reproduce results; the mechanically-autofixable violations (269, empirically measured 2026-07-08) are resolved repo-wide via `--fix --unfixable SIM117`, satisfying rule-absence gates for all six autofixed codes and leaving all 46 SIM117 sites fully intact for Plan 12-02's deliberate structural-collapse pass (222 total remaining, measured as informational context); the 613-test suite is unchanged and green. Both commits are staged by explicit pathspec, with no unrelated dirty files from the working tree swept in.
</success_criteria>

<output>
Create `.planning/phases/12-ci-quality-gates/12-01-SUMMARY.md` when done
</output>
</output>
