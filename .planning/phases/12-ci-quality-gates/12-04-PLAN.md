---
phase: 12-ci-quality-gates
plan: 04
type: execute
wave: 4
depends_on: ["12-02", "12-03"]
files_modified:
  - .planning/phases/12-ci-quality-gates/12-VERIFICATION.md
autonomous: false
requirements: [CI-01, CI-02]

must_haves:
  truths:
    - "A throwaway branch with a deliberately injected lint error (unused import) was pushed and its ci.yml run shows the lint job RED while the test job stays GREEN"
    - "A throwaway branch with a deliberately broken test was pushed and its ci.yml run shows the test job RED while the lint job stays GREEN"
    - "The current master HEAD's ci.yml run (from Plan 12-03's merge/push) shows BOTH jobs GREEN"
    - "All three run URLs (lint-red, test-red, master-green) are captured in a phase verification record"
    - "Both throwaway branches are deleted from the remote after their red runs are captured, per D-14 (red runs remaining in GitHub Actions history are acceptable and expected)"
  artifacts:
    - path: ".planning/phases/12-ci-quality-gates/12-VERIFICATION.md"
      provides: "Captured run URLs proving criterion 4 (red-proof) was demonstrated, not just configured"
      contains: "Run URL"
  key_links:
    - from: "throwaway branch push"
      to: "GitHub Actions run"
      via: "ci.yml on: push with no branches filter (D-07) means every branch triggers a run"
      pattern: "actions/workflows/ci.yml"
---

<objective>
Demonstrate ROADMAP.md Phase 12 success criterion 4 for real: push one throwaway branch with a deliberately injected lint error and confirm its `ci.yml` run goes RED on the lint job; push a second throwaway branch with a deliberately broken test and confirm its run goes RED on the test job; confirm the current green master run shows both jobs GREEN. Capture all three run URLs in `12-VERIFICATION.md`, then delete both throwaway branches (D-14 — the red runs staying in Actions history is fine and expected; deleting the branches is just repo hygiene).

Purpose: CI-01/CI-02 aren't actually proven until the gate has been seen catching a real regression — this plan is the live demonstration the hiring-manager audience needs to trust the gate is real, not just YAML that happens to parse. It requires observing live GitHub Actions runs, which Claude cannot fully automate (waiting for/reading a live run's status page) — hence the human-verify checkpoint. This plan now runs in wave 4 (revised per cross-AI review: 12-03 moved from wave 2 to wave 3, `depends_on: ["12-01", "12-02"]`, so `ci.yml` only lands once the repo is fully green) — 12-04 still depends on both 12-02 and 12-03 directly, and transitively on 12-01 through 12-02/12-03's own dependency chain, guaranteeing the master-green run this plan captures is genuinely the repo's first `ci.yml` run, not an interim red one.
Output: `.planning/phases/12-ci-quality-gates/12-VERIFICATION.md` recording three run URLs (lint-red, test-red, master-green) and confirmation both throwaway branches were deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-ci-quality-gates/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push lint-red and test-red throwaway branches</name>
  <files>none (transient branch content only, reverted/discarded before merge)</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
**Preflight (cross-AI review finding, HIGH):** Run `git status --short` before creating either throwaway branch. This plan's throwaway branches must carry ONLY the single deliberate regression each is designed to demonstrate — confirm no pre-existing unrelated dirty/staged files (e.g. `.planning` deletions from another in-flight session) are sitting in the working tree that could get swept into a throwaway branch's commit. If the tree is dirty with unrelated changes, do not commit them as part of either red-proof branch; stash or leave them untouched on master and only commit this task's single-file regression by explicit pathspec.

**Branch hygiene (cross-AI review finding, LOW):** Before creating each throwaway branch, run `git switch master && git pull --ff-only` to guarantee the branch forks from the true, up-to-date master tip (not a stale local master or a leftover checkout from a prior branch).

Create and push branch `ci-redproof-lint` from current master HEAD: `git switch master && git pull --ff-only`, then `git switch -c ci-redproof-lint`. Add one deliberately unused import to any already-clean Python file (e.g. add `import sys` at the top of `app/main.py` without using it anywhere) — this must trigger F401 under the ruleset from Plan 12-01/12-02. Stage by explicit pathspec (`git add app/main.py`, never `-A`/`-a`) and commit with message `test(12): inject lint error for CI red-proof (throwaway)`. **Single-cause sanity check (cross-AI review suggestion) before pushing:** run `uv run ruff check .` on this branch and confirm it now fails (the injected import is the only lint error) — this is a proxy for the lint job; separately, mentally confirm nothing else on this branch would break the test job (no test files touched), so the eventual CI run isolates the failure to lint only. Then push the branch with `git push origin ci-redproof-lint`.

Create and push a second branch `ci-redproof-test` from current master HEAD (not from `ci-redproof-lint`): `git switch master && git pull --ff-only`, then `git switch -c ci-redproof-test`. Modify one existing, currently-passing test assertion to a value guaranteed false (e.g. change an `assert result == expected_value` to `assert result == "deliberately-wrong-value-for-ci-redproof"` in any simple, low-blast-radius test file) — this must fail under `pytest`. Stage by explicit pathspec (`git add <the one modified test file>`, never `-A`/`-a`) and commit with message `test(12): inject test failure for CI red-proof (throwaway)`. **Single-cause sanity check (cross-AI review suggestion) before pushing:** optionally run `uv run pytest -q` on this branch and confirm it now fails (only the one deliberately-broken assertion); also run `uv run ruff check .` and confirm it still passes (no lint regression introduced), so the eventual CI run isolates the failure to the test job only. Then push with `git push origin ci-redproof-test`.

Do not open pull requests for either branch — a bare push is sufficient to trigger `ci.yml` per D-07's `on: push` with no branch filter. After both branches are pushed, `git switch master` to return to master before proceeding to the next task (do not stay checked out on a throwaway branch).
  </action>
  <acceptance_criteria>
    - `git branch -a` (or `git ls-remote origin`) shows both `ci-redproof-lint` and `ci-redproof-test` present on the remote
    - `ci-redproof-lint`'s single commit diff shows exactly one added unused import line, nothing else (staged by explicit pathspec, not `-A`/`-a` — verify no unrelated pre-existing dirty files were swept in)
    - `ci-redproof-test`'s single commit diff shows exactly one modified assertion value, nothing else (staged by explicit pathspec, not `-A`/`-a`)
    - Each branch was created via `git switch master && git pull --ff-only && git switch -c <branch>` from the true master tip, not a stale local ref
    - `git branch --show-current` shows `master` after this task completes (executor switched back before moving on)
  </acceptance_criteria>
  <verify>
    <automated>git ls-remote origin | grep -q "refs/heads/ci-redproof-lint" && git ls-remote origin | grep -q "refs/heads/ci-redproof-test"</automated>
  </verify>
  <done>Both throwaway branches are pushed to origin, each carrying exactly one deliberate, isolated regression (lint error / test failure) and nothing else, staged by explicit pathspec — the executor is switched back to `master` before proceeding.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task: Human-verify red/green CI runs across throwaway branches and master</name>
  <action>
Open the Actions tab and visually confirm the red/green pattern described in `<how-to-verify>` across the three relevant runs (ci-redproof-lint, ci-redproof-test, master HEAD), then report the three run URLs back — this step requires a human to read live GitHub Actions run status pages, which Claude cannot observe directly.
  </action>
  <what-built>Two throwaway branches pushed to trigger `ci.yml`: `ci-redproof-lint` (unused import, should fail the lint job) and `ci-redproof-test` (broken assertion, should fail the test job). The current master HEAD already has a green `ci.yml` run from Plan 12-03 landing.</what-built>
  <how-to-verify>
    1. Open `https://github.com/pjnhek/payroll_agent/actions/workflows/ci.yml` and find the run triggered by `ci-redproof-lint`.
    2. Confirm that run's `lint` job shows a red X / failure, and its `test` job shows a green check / success (proving the two jobs are genuinely independent — the injected lint error did not fail the test job).
    3. Find the run triggered by `ci-redproof-test`. Confirm that run's `test` job shows a red X / failure, and its `lint` job shows a green check / success.
    4. Find the most recent run on `master` (from Plan 12-03's merge). Confirm both `lint` and `test` jobs show green checks.
    5. Copy all three run URLs (visible in the browser address bar or via the run's "..." menu → "Copy link").
  </how-to-verify>
  <resume-signal>Reply with the three run URLs (lint-red run, test-red run, master-green run), or describe what went wrong if any job's color doesn't match the expected red/green pattern above.</resume-signal>
  <done>Human has confirmed the lint-red run (lint FAIL / test PASS), test-red run (test FAIL / lint PASS), and master run (both PASS), and supplied all three run URLs.</done>
  <verify>
    <human-check>Human confirms lint-red run shows lint FAIL/test PASS, test-red run shows test FAIL/lint PASS, and master run shows both PASS, then supplies all three run URLs.</human-check>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Record verification, delete throwaway branches</name>
  <files>.planning/phases/12-ci-quality-gates/12-VERIFICATION.md</files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>
**Preflight (cross-AI review finding, HIGH):** Run `git status --short` before creating `12-VERIFICATION.md`. Confirm the only new file introduced is this VERIFICATION.md — stage it by explicit pathspec (`git add .planning/phases/12-ci-quality-gates/12-VERIFICATION.md`), never `git add -A` / `git commit -a`, so any pre-existing unrelated dirty files elsewhere in the tree are not swept into this commit.

Create `.planning/phases/12-ci-quality-gates/12-VERIFICATION.md` recording, verbatim, the three run URLs supplied by the human in the prior checkpoint task: the `ci-redproof-lint` run URL (lint RED / test GREEN), the `ci-redproof-test` run URL (test RED / lint GREEN), and the master-HEAD run URL (both GREEN). State explicitly which job was red on each throwaway run, satisfying ROADMAP.md Phase 12 success criterion 4 ("Pushing a branch with a deliberately injected lint error... shows CI going red on the lint step, and a deliberately broken test shows CI going red on the test step — both demonstrated, not just configured").

After recording the URLs, stage and commit the verification record: `git add .planning/phases/12-ci-quality-gates/12-VERIFICATION.md && git commit -m "docs(12): record CI red-proof verification"` (pathspec-only staging, per this plan's existing preflight rule — never `git add -A` / `git commit -a`). Then delete both throwaway branches from the remote: `git push origin --delete ci-redproof-lint` and `git push origin --delete ci-redproof-test`. Per D-14, the red runs themselves remain visible in the repo's GitHub Actions run history permanently (deleting a branch does not delete its historical workflow runs) — this is acceptable and expected; only the branches (not the run history) are being cleaned up. **Branch hygiene (cross-AI review finding, LOW):** before deleting local branches, run `git switch master` first (deleting the currently-checked-out branch fails or leaves cleanup incomplete) — then `git branch -D ci-redproof-lint ci-redproof-test` (ignore errors if not present locally).
  </action>
  <acceptance_criteria>
    - `.planning/phases/12-ci-quality-gates/12-VERIFICATION.md` exists and contains three distinct "Run URL" entries (one per: lint-red, test-red, master-green), staged by explicit pathspec (not `-A`/`-a`)
    - The file states, in plain text, which job (lint or test) was red on each of the two throwaway runs
    - `git ls-remote origin` no longer lists `ci-redproof-lint` or `ci-redproof-test`
    - `git branch --show-current` was `master` (or switched to `master`) before the local `git branch -D` deletion ran, so the deletion could not fail from being on the branch being deleted
    - `git log --oneline -1` shows the `docs(12): record CI red-proof verification` commit, and `git status --short` no longer lists `12-VERIFICATION.md` as staged/uncommitted — the record is actually committed, not merely staged
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/12-ci-quality-gates/12-VERIFICATION.md && ! git ls-remote origin | grep -qE "refs/heads/ci-redproof-(lint|test)"</automated>
  </verify>
  <done>12-VERIFICATION.md permanently records the three run URLs proving criterion 4 was demonstrated live (not just configured), staged by explicit pathspec, and both throwaway branches are deleted from the remote and locally (after switching to master first) — their run history remains in Actions per D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Throwaway branch push → GitHub Actions | Deliberately broken code triggers a real CI run; branch is discarded after, run history persists |
| Human-observed run URLs → committed VERIFICATION.md | Human-supplied strings (URLs) written into a tracked file — no code execution risk, but urls should be sanity-checked as github.com links before commit |
| Dirty working tree → git commit | Unrelated pre-existing dirty/staged files (e.g. mid-flight `.planning` deletions from another session) could be accidentally swept into a throwaway branch's commit or the VERIFICATION.md commit if staged carelessly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | Tampering | Deliberately broken throwaway branches left on remote indefinitely | mitigate | Task 2 explicitly deletes both branches from origin immediately after the run URLs are captured; only the (harmless) Actions run history persists, per D-14 |
| T-12-10 | Spoofing | Human-supplied run URLs could be pasted incorrectly or point off-repo | mitigate | Task 2's acceptance criteria requires three distinct URL entries under the confirmed `pjnhek/payroll_agent` slug; executor should sanity-check each URL string starts with `https://github.com/pjnhek/payroll_agent/actions/` before writing to VERIFICATION.md |
| T-12-14 | Tampering | Dirty working tree causing unrelated files to be swept into a throwaway branch's commit or the VERIFICATION.md commit | mitigate | Both tasks preflight with `git status --short` before committing, and stage only this task's own explicit paths (`git add app/main.py`, `git add <test file>`, `git add .planning/phases/12-ci-quality-gates/12-VERIFICATION.md`) — never `git add -A` / `git commit -a`; each throwaway branch is created via `git switch master && git pull --ff-only && git switch -c <branch>` and local branches are deleted only after `git switch master` |
</threat_model>

<verification>
- `.planning/phases/12-ci-quality-gates/12-VERIFICATION.md` contains 3 run URLs with stated pass/fail-per-job outcomes matching the expected red/green pattern
- `git ls-remote origin` shows neither throwaway branch present
</verification>

<success_criteria>
ROADMAP.md Phase 12 success criterion 4 is satisfied with live evidence: a lint-only failure and a test-only failure were each independently demonstrated on real GitHub Actions runs, alongside a fully-green master run, with all three run URLs permanently recorded in the phase's VERIFICATION.md. Both throwaway branches were created from a freshly-pulled master tip and staged by explicit pathspec, and the executor returns to `master` before each branch-deletion step — no unrelated dirty files from the working tree were swept into any of this plan's commits.
</success_criteria>

<output>
Create `.planning/phases/12-ci-quality-gates/12-04-SUMMARY.md` when done
</output>
