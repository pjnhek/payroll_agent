---
phase: 07.5-clarification-reply-field-regression
plan: "02"
type: execute
wave: 2
depends_on:
  - "07.5-01"
files_modified:
  - app/models/contracts.py
  - app/pipeline/validate.py
  - app/pipeline/decide.py
  - app/pipeline/compose_email.py
autonomous: true
requirements:
  - MONEY-03

must_haves:
  truths:
    - "RawFieldDrop model exists in contracts.py (before FieldDrop) with submitted_name: str, field: str, original_value: Decimal (ge=0), resumed_value: Decimal | None (ge=0), ConfigDict(extra='forbid') — the internal raw-keyed counterpart to FieldDrop (D-7.5-05)"
    - "detect_field_regression(original, resumed, prior_matches, current_matches) is a pure function in validate.py returning list[RawFieldDrop] — reduces BOTH Extracted to {employee_id: ExtractedEmployee} dicts (last-wins, D-12) BEFORE diffing; never redefines _HOURS_FIELDS (DRY, D-09); satisfies SC2's restated-name requirement (M. Chen->Maria Chen, same employee_id = same diff slot)"
    - "validate() prior=/prior_matches=/resolved_drops= kwargs are NOW FUNCTIONAL: when prior is not None, detect_field_regression is called with employee-id-keyed maps; each RawFieldDrop is promoted to a ValidationIssue(issue_type='field_regression'); re-resolution to a different employee is skipped (D-11); resolved_drops suppression is per-(employee_id,field) in the field_regression path ONLY — NOT at the any_hours gate (N8)"
    - "decide.py Rule 2b (2 lines after Rule 2): field_regression issues gate to request_clarification; Decision.missing_fields is NOT widened (D-7.5 PATTERNS §4)"
    - "_template_body in compose_email.py emits field-regression lines UNCONDITIONALLY whenever gate_reasons contains 'field regression:' entries — BEFORE the unresolved/missing fallback gate (N5 fix); uses rsplit('.', 1) last-dot split so dotted submitted names (e.g. 'M. Chen') parse correctly (WARNING 1 lock)"
    - "D-7.5-09 wording lock is DETERMINISTIC regardless of LLM draft path: the field-regression question ('Reply with the {field_name} hours for {submitted_name}, or none to confirm zero.') is APPENDED after any LLM draft body — the deterministic line always appears in the final composed email even when the LLM returns non-empty content"
    - "D-26: explicit zero resumed_value=Decimal('0') returned as RawFieldDrop with resumed_value=Decimal('0') (not None) — confirmed_dropped signal preserved"
    - "D-25: OT 2->0 and OT 2->absent both produce a RawFieldDrop (shared _is_paid predicate)"
    - "uv run pytest tests/test_validate.py tests/test_decide.py -q exits 0"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "RawFieldDrop model (submitted_name-keyed, internal)"
      contains: "class RawFieldDrop"
    - path: "app/pipeline/validate.py"
      provides: "detect_field_regression(original, resumed, prior_matches, current_matches) — employee-id-keyed diff (SC2/R3-3 correct) + validate(prior=, prior_matches=, resolved_drops=) seam (N6/N8 correct)"
      exports: ["detect_field_regression", "validate"]
      contains: "def detect_field_regression"
    - path: "app/pipeline/decide.py"
      provides: "Rule 2b — field_regression issues gate to request_clarification"
      contains: "Rule 2b"
    - path: "app/pipeline/compose_email.py"
      provides: "N5 field-regression email line (unconditional, before fallback) PLUS deterministic D-7.5-09 append after any LLM draft body"
      contains: "field regression"
  key_links:
    - from: "app/pipeline/validate.py:detect_field_regression"
      to: "app/pipeline/validate.py:validate (prior= kwarg)"
      via: "validate() calls detect_field_regression(prior, extracted, prior_matches, matches); keys by employee_id BEFORE diffing (SC2/R3-3); maps result to ValidationIssue(issue_type='field_regression')"
      pattern: "detect_field_regression"
    - from: "app/pipeline/decide.py:Rule 2b"
      to: "app/pipeline/compose_email.py:_template_body + compose_clarification"
      via: "field_regression gate_reason -> N5 unconditional block emits D-7.5-09 question AND deterministic append after LLM draft (Finding 4 fix)"
      pattern: "field regression"
---

<objective>
Implement the MONEY-03 pure-judgment layer: `RawFieldDrop` model, `detect_field_regression` pure helper (employee-id-keyed diff — SC2/R3-3 fix), `validate(prior=)` seam implementation (N6/N8-correct), decide Rule 2b, and the field-regression email copy (N5/D-7.5-09, deterministic regardless of LLM draft path).

**Reviews addressed in this plan:**
- **Finding 2 (HIGH — R3-3 restated-name):** `detect_field_regression` now reduces BOTH original and resumed `Extracted` to `{employee_id: ExtractedEmployee}` maps (last-wins, D-12) BEFORE diffing — using `prior_matches` and `current_matches` provided by the caller. "M. Chen" in original and "Maria Chen" in resumed, both resolving to the same `employee_id`, now land in the same diff slot and produce a `RawFieldDrop`. The previous `submitted_name` intersection approach is replaced entirely.
- **Finding 4 (HIGH — deterministic D-7.5-09 wording):** The field-regression question is appended AFTER any LLM-drafted body in `compose_clarification`, not only placed in the `_template_body` fallback. The deterministic line appears on the real path regardless of whether the LLM returns non-empty content.

Purpose: Establish and test the pure-function judgment layer that the eval can certify (D-23 eval layer). All changes are pure-function or data-model changes — no DB writes, no orchestrator changes.

D-7.5-05 reality check (verified against live source): Phase 7 shipped `FieldDrop` (contracts.py:124), the `field_regression` ValidationIssue Literal (roster.py:219), and `_is_paid` (validate.py:38). `RawFieldDrop` was NOT shipped — created here. `detect_field_regression` does NOT exist yet — created here. Both `FieldDrop` and `_is_paid` are reused without modification.

Output: Pure-function judgment layer passing targeted unit tests; eval can certify it with prior= fixtures.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md

@app/models/contracts.py
@app/pipeline/validate.py
@app/pipeline/decide.py
@app/pipeline/compose_email.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RawFieldDrop model + detect_field_regression (employee-id-keyed) + validate(prior=) seam</name>
  <files>app/models/contracts.py, app/pipeline/validate.py</files>

  <read_first>
  Read app/models/contracts.py in full. Key anchors:
  - Lines 60-80: ExtractedEmployee — ConfigDict(extra="forbid"), Decimal | None + ge=0. Style template for RawFieldDrop.
  - Lines 124-146: FieldDrop (shipped Phase 7) — the PUBLIC employee_id-keyed model. RawFieldDrop is the INTERNAL submitted_name-keyed peer. Add RawFieldDrop BEFORE FieldDrop (~line 119-120).
  - Lines 13-20: existing imports — UUID, Decimal, Field, BaseModel, ConfigDict are all already present.

  Read app/pipeline/validate.py in full (Plan 01 modified the signature; re-read to get the current state). Key anchors:
  - Lines 29-35: _HOURS_FIELDS tuple — detect_field_regression MUST reuse this constant, never redefine it.
  - Line 38: _is_paid predicate — the third call site is detect_field_regression (D-09); reuse it.
  - Lines 81-85: validate() signature after Plan 01's addition — now has prior=None, prior_matches=None, resolved_drops=None keyword-only kwargs.
  - Lines ~96-100: any_hours gate — N8: do NOT touch this gate for resolved_drops; it runs unchanged on every employee.
  - Lines ~103-112: ValidationIssue emission pattern — template for the field_regression emission.
  - Current import block (~lines 1-27) — add RawFieldDrop, UUID imports here.

  CRITICAL R3-3 FIX (Finding 2 — CONFIRMED BUG): The old approach of intersecting by submitted_name misses the restated-name case.
  New approach for detect_field_regression: the function takes FOUR arguments — original Extracted, resumed Extracted, prior_matches (list[NameMatchResult] | None), current_matches (list[NameMatchResult]).

  Build two employee_id-keyed maps BEFORE diffing:
  1. id_to_orig: {employee_id: ExtractedEmployee} — from original.employees + prior_matches
     - For each employee in original.employees: look up employee_id via prior_matches; if prior_matches is None, fall back to submitted_name key (deliberate no-op scope per D-7.5-01d)
     - Last-wins on duplicate employee_ids (D-12)
  2. id_to_resumed: {employee_id: ExtractedEmployee} — from resumed.employees + current_matches
     - For each employee in resumed.employees: look up employee_id via current_matches
     - Last-wins on duplicate employee_ids (D-12)

  Then diff: for each employee_id in sorted(intersection of id_to_orig and id_to_resumed):
    For each field in _HOURS_FIELDS:
      If _is_paid(orig_val) and not _is_paid(resumed_val): produce RawFieldDrop.
      The submitted_name in the RawFieldDrop comes from the CURRENT (resumed) employee (so the email references the name the client just used).

  N6 cross-check: when prior_matches is provided AND current_matches is provided, the same employee_id appears in BOTH maps only if the name resolved to the same entity — the re-resolution guard (D-11) is inherently satisfied by keying on employee_id from the start. No separate cross-check loop needed; the employee_id intersection IS the cross-check.

  When prior_matches is None: fall back to a submitted_name key for original.employees (the deliberate no-op scope — D-7.5-01d). This preserves the accepted limited-scope behavior while enabling the full restated-name case when prior_matches IS threaded through (which Plan 03 will do on Round-2 resume).

  validate() body additions:
  - When prior is not None:
    a. _resolved_drops = resolved_drops or set()
    b. raw_drops = detect_field_regression(prior, extracted, prior_matches, matches)
    c. For each raw_drop: look up current_emp_id from matches; if current_emp_id is None, skip (unresolved in current run); if (current_emp_id, raw_drop.field) in _resolved_drops, skip (N8 per-field suppression); else emit ValidationIssue(issue_type="field_regression", field=f"{raw_drop.submitted_name}.{raw_drop.field}", message=...)
  - any_hours gate (lines ~96-100): UNCHANGED — runs on every employee regardless of _resolved_drops.
  </read_first>

  <behavior>
  Tests to write (TDD — write failing tests FIRST, then implement).
  Place new tests in tests/test_detect_field_regression.py OR append to tests/test_validate.py.

  test_detect_regression_ot_absent:
    prior_matches = [match for "Alice", employee_id=alice_id]
    current_matches = [match for "Alice", employee_id=alice_id]
    original has Alice with hours_overtime=Decimal("2")
    resumed has Alice with hours_overtime=None
    detect_field_regression(original, resumed, prior_matches, current_matches) returns one RawFieldDrop:
      drop.field == "hours_overtime", drop.original_value == Decimal("2"), drop.resumed_value is None, drop.submitted_name == "Alice"

  test_explicit_drop_zero_resumed_value (D-26):
    resumed has hours_overtime=Decimal("0") -> RawFieldDrop with resumed_value=Decimal("0") (NOT None, explicit zero preserved)

  test_no_regression_noop (D-27):
    original and resumed identical (same employee, same hours) -> detect_field_regression returns []

  test_predicate_consistency (D-25):
    OT 2->0 and OT 2->absent BOTH produce a RawFieldDrop for hours_overtime (shared _is_paid predicate, same code path)

  test_restated_name_same_employee_id_is_detected (SC2 — the headline restated-name case, previously missing):
    prior_matches = [match for submitted_name="M. Chen", employee_id=chen_id]
    current_matches = [match for submitted_name="Maria Chen", employee_id=chen_id]  # same employee_id
    original.employees has {"M. Chen": {hours_overtime: Decimal("2")}}
    resumed.employees has {"Maria Chen": {hours_overtime: None}}
    detect_field_regression(original, resumed, prior_matches, current_matches) returns one RawFieldDrop:
      drop.field == "hours_overtime", drop.original_value == Decimal("2"), drop.submitted_name == "Maria Chen"
    PREVIOUSLY FAILED because the submitted_name intersection ("M. Chen" vs "Maria Chen") was empty.
    NOW PASSES because both map to the same employee_id before diffing.

  test_re_resolution_different_employee_id_is_skipped (D-11):
    prior_matches = [match for "M. Chen", employee_id=employee_A]
    current_matches = [match for "M. Chen", employee_id=employee_B]  # different employee
    original has "M. Chen" with hours_overtime=Decimal("2")
    resumed has "M. Chen" with hours_overtime=None
    detect_field_regression returns [] — no drop produced (different employee_id means re-resolution, not regression)

  test_prior_matches_none_falls_back_to_name_key:
    prior_matches=None, current_matches=[match for "Alice", alice_id]
    Same-name drop (original and resumed both use "Alice"): detect_field_regression still returns a RawFieldDrop
    (deliberate accepted-scope no-op — prior_matches=None bypasses the id lookup for original side)

  test_validate_field_regression_emitted_with_prior:
    validate(extracted_ot_absent, roster, matches, prior=extracted_ot_2, prior_matches=matches)
    -> issues contains at least one ValidationIssue(issue_type="field_regression") for hours_overtime

  test_resolved_drops_suppression_is_per_field (N8):
    Employee has confirmed_dropped=hours_overtime AND hours_regular=Decimal("0").
    resolved_drops={(employee_id, "hours_overtime")}.
    validate(extracted, roster, matches, prior=prior, resolved_drops=resolved_drops):
    - MONEY-01 any_hours gate fires (missing issue produced — regular=0 means not paid)
    - NO field_regression issue emitted for hours_overtime (suppressed by resolved_drops)
    resolved_drops ONLY suppresses the field_regression re-flag; the MONEY-01 gate is completely unaffected.
  </behavior>

  <action>
  Step 1 — Add RawFieldDrop to app/models/contracts.py:

  Insert before the FieldDrop class (~line 119-120, after the last existing model before FieldDrop):
  - Class name: RawFieldDrop
  - model_config = ConfigDict(extra="forbid")
  - submitted_name: str (raw submitted name from the CURRENT/resumed extraction — the name the client used in their reply)
  - field: str (bare field name, e.g. "hours_overtime")
  - original_value: Decimal = Field(ge=0)
  - resumed_value: Decimal | None = Field(default=None, ge=0)
  - Add docstring: "Internal raw record keyed by submitted_name (D-7.5-05). detect_field_regression returns list[RawFieldDrop]. The submitted_name is from the CURRENT (resumed) extraction — the name the client used in the reply. validate() has access to employee_id via matches and uses it for the N8 resolved_drops suppression check. resumed_value=None means absent (silence); resumed_value=Decimal('0') means explicit zero (D-14 confirmed_dropped signal)."
  No new imports needed — all types (Decimal, Field, BaseModel, ConfigDict) are already imported.

  Step 2 — Add detect_field_regression to app/pipeline/validate.py:

  Add to the import block: `from app.models.contracts import Extracted, RawFieldDrop` (extend existing Extracted import), and `from uuid import UUID`.

  Place detect_field_regression between the last private helper function and validate(). Full signature:
  `def detect_field_regression(original: Extracted, resumed: Extracted, prior_matches: list | None, current_matches: list) -> list[RawFieldDrop]:`

  Body — the R3-3 fix (employee-id-keyed diff, not submitted_name intersection):

  1. Build id_to_orig: {employee_id: ExtractedEmployee}
     - If prior_matches is not None:
       Build name_to_id_prior = {m.submitted_name: m.matched_employee_id for m in prior_matches if m.resolved and m.matched_employee_id is not None}
       For each emp in original.employees: emp_id = name_to_id_prior.get(emp.submitted_name); if emp_id is not None: id_to_orig[emp_id] = emp (last-wins)
     - If prior_matches is None (deliberate no-op scope — D-7.5-01d):
       For each emp in original.employees: id_to_orig[emp.submitted_name] = emp (key by name as fallback; last-wins)
     Note: the fallback name-key is only used when prior_matches is None; it handles the same-name case but cannot handle restated names.

  2. Build id_to_resumed: {employee_id: ExtractedEmployee}
     Build name_to_id_current = {m.submitted_name: m.matched_employee_id for m in current_matches if m.resolved and m.matched_employee_id is not None}
     For each emp in resumed.employees: emp_id = name_to_id_current.get(emp.submitted_name); if emp_id is not None: id_to_resumed[emp_id] = emp (last-wins)

  3. Diff: for each emp_id in sorted(intersection of id_to_orig and id_to_resumed, key=str) (sorted for determinism, D-27):
     orig_emp = id_to_orig[emp_id]
     resumed_emp = id_to_resumed[emp_id]
     current_name = resumed_emp.submitted_name  (the name the client used in the reply)
     For each field in _HOURS_FIELDS (reuse the module-level constant):
       original_val = getattr(orig_emp, field)
       resumed_val = getattr(resumed_emp, field)
       If _is_paid(original_val) and not _is_paid(resumed_val):
         Append RawFieldDrop(submitted_name=current_name, field=field, original_value=original_val, resumed_value=resumed_val)

  4. Return the list.

  Step 3 — Implement validate() kwargs in app/pipeline/validate.py:

  The signature was already extended by Plan 01 with `*, prior=None, prior_matches=None, resolved_drops=None`.

  Add the following logic at the start of validate(), before the existing per-employee loop:

  a. `_resolved_drops = resolved_drops or set()`

  b. When `prior is not None`:
     - Build `name_to_id_current: dict[str, UUID] = {m.submitted_name: m.matched_employee_id for m in matches if m.resolved and m.matched_employee_id is not None}`
     - `raw_drops = detect_field_regression(prior, extracted, prior_matches, matches)`
     - For each raw_drop in raw_drops:
       1. current_emp_id = name_to_id_current.get(raw_drop.submitted_name) — if None: skip (submitted_name not resolved in current run)
       2. N8 check: if (current_emp_id, raw_drop.field) in _resolved_drops: skip (confirmed_dropped per D-15)
       3. Otherwise emit: ValidationIssue(issue_type="field_regression", field=f"{raw_drop.submitted_name}.{raw_drop.field}", message=f"field regression: {raw_drop.field} was {raw_drop.original_value}, now {'absent' if raw_drop.resumed_value is None else str(raw_drop.resumed_value)}")

  c. The per-employee any_hours gate (lines ~96-100) runs UNCHANGED for all employees regardless of _resolved_drops. Do NOT skip or suppress it.

  d. _resolved_drops is used ONLY in the field_regression emission path (step b above). It does NOT touch the any_hours/missing path.

  Note: the N6 same-employee_id cross-check from the old approach is now inherent — detect_field_regression only diffs employees with the SAME employee_id in both maps, so a re-resolution to a different employee automatically produces no RawFieldDrop for that name (test_re_resolution_different_employee_id_is_skipped proves this).

  After implementing, run:
  uv run pytest tests/test_validate.py tests/test_detect_field_regression.py -q 2>/dev/null || uv run pytest tests/test_validate.py -q
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "class RawFieldDrop" app/models/contracts.py` exits 0
  - `grep "def detect_field_regression" app/pipeline/validate.py` exits 0
  - `grep "prior_matches" app/pipeline/validate.py` exits 0 (employee-id-keyed diff takes prior_matches parameter)
  - `grep "resolved_drops" app/pipeline/validate.py` exits 0 (N8 per-field suppression in field_regression path)
  - `grep "rsplit\|submitted_name.*intersection\|intersection.*submitted" app/pipeline/validate.py` exits 1 (no submitted_name intersection — the R3-3 bug is gone)
  - test_restated_name_same_employee_id_is_detected PASSES: "M. Chen" (OT=2) + "Maria Chen" (no OT), both employee_id=chen_id, produces a RawFieldDrop for hours_overtime
  - test_re_resolution_different_employee_id_is_skipped PASSES: same submitted_name resolving to different employee_ids produces NO RawFieldDrop
  - test_resolved_drops_suppression_is_per_field PASSES: MONEY-01 any_hours gate fires even when (employee_id, "hours_overtime") is in resolved_drops
  - `uv run pytest tests/test_validate.py -q` exits 0 — all new tests pass plus existing tests green
  </acceptance_criteria>

  <done>RawFieldDrop exists in contracts.py before FieldDrop. detect_field_regression exists in validate.py with employee-id-keyed diff (R3-3 fix — restated-name case now detected). validate() prior= kwargs are functional with N8 per-field suppression. All new tests GREEN including the restated-name test. Full validate test suite GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: decide Rule 2b + compose_email N5 + deterministic D-7.5-09 wording append (Finding 4 fix)</name>
  <files>app/pipeline/decide.py, app/pipeline/compose_email.py</files>

  <read_first>
  Read app/pipeline/decide.py in full in one pass. Key anchors:
  - Lines 123-125: Rule 2 (the 2-line template to mirror): `missing = [i.field for i in issues if i.issue_type == "missing"]` / `gate_reasons += [f"missing required field: {f}" for f in missing]`. Rule 2b is 2 lines placed IMMEDIATELY AFTER these two.
  - Lines 127-129: Rule 3 — stays unchanged; Rule 2b sits between Rule 2 and Rule 3.
  - Confirm Decision.missing_fields is NOT updated — regressions feed gate_reasons only (PATTERNS.md §3 note).

  Read app/pipeline/compose_email.py in full in one pass. Key anchors:
  - Lines 63-102: _template_body function. Current structure:
    - lines ~71: if decision.unresolved_names: ... block
    - lines ~87: if decision.missing_fields: ... block
    - lines ~93-96: "if not unresolved_names and not missing_fields:" fallback
    - lines ~97-101: the "Could you please reply..." closing line
  - The compose_clarification function (wherever the LLM draft is incorporated and returned) — CRITICAL: find WHERE compose_clarification returns, because the D-7.5-09 deterministic line must be APPENDED after the LLM draft body here (Finding 4 fix).

  FINDING 4 FIX (confirmed HIGH): The D-7.5-09 wording ("Reply with the {field_name} hours...") currently lives only in _template_body's fallback/inline block. If the LLM draft returns non-empty text, compose_clarification returns it directly WITHOUT the field-regression question. The fix: in compose_clarification (or wherever the draft is assembled before return), detect whether any gate_reason is a field_regression reason and if so APPEND the deterministic field-regression lines to the body, AFTER any LLM draft content. This means the operator-visible email ALWAYS contains the field-regression question regardless of what the LLM drafted.

  Gate-reason string format (PATTERNS.md §7): the format written by decide.py is "field regression: {ValidationIssue.field}" where ValidationIssue.field = "{submitted_name}.{field_name}". compose_email MUST use rsplit(".", 1) — last-dot split — NOT split(".", 1) which would break "M. Chen.hours_overtime".

  D-7.5-09 wording lock: the field-regression email line MUST be:
  "  - Reply with the {field_name} hours for {submitted_name}, or 'none' to confirm zero."
  This phrasing elicits a value (new hours) or explicit zero/none (confirmed_dropped), avoiding the yes/no trap.
  </read_first>

  <behavior>
  Tests to write (TDD — write FIRST, then implement):

  test_field_regression_issue_gates_to_clarification (in tests/test_decide.py):
    Build issues=[ValidationIssue(issue_type="field_regression", field="alice.hours_overtime", message="regression")] with extracted and matches for a valid run. Call decide(extracted, matches, issues). Assert: decision.final_action == "request_clarification" AND any("field regression" in r for r in decision.gate_reasons). Currently FAILS (Rule 2 only checks "missing").

  test_field_regression_line_present_when_other_clarification_coexists (in tests/test_compose_email.py or new test file):
    Build a Decision with unresolved_names=["Bob Smith"] AND gate_reasons=["field regression: alice.hours_overtime"]. Call compose_clarification (or the top-level compose function). Assert: result contains text about "hours_overtime" (field-regression line) AND contains "Bob Smith" (unresolved name line). Tests the N5 fix (field-regression line even when other clarification reasons exist).

  test_field_regression_line_dotted_submitted_name:
    Build a Decision with gate_reasons=["field regression: M. Chen.hours_overtime"]. Call compose_clarification / _template_body. Assert: rendered email contains "M. Chen" as the name and "hours_overtime" as the field (NOT "M" as name and "Chen.hours_overtime" as field). This locks the rsplit(".", 1) semantics.

  test_field_regression_line_d7509_wording:
    Build a Decision with gate_reasons=["field regression: Alice Johnson.hours_overtime"]. Call compose_clarification. Assert: rendered line contains "Reply with the hours_overtime hours for Alice Johnson, or 'none' to confirm zero." (D-7.5-09 exact wording).

  test_d7509_wording_present_even_when_llm_draft_nonempty (Finding 4 fix — NEW test):
    Mock the LLM to return a NON-EMPTY draft body ("Hi, please check your submission.").
    Build a Decision with gate_reasons=["field regression: Alice.hours_overtime"].
    Call compose_clarification with this decision and a mock LLM that returns non-empty content.
    Assert: the returned email body contains "Reply with the hours_overtime hours for Alice, or 'none' to confirm zero."
    — The D-7.5-09 line is present EVEN THOUGH the LLM returned non-empty content (the deterministic append fires on the real path).
    Previously would FAIL because compose_clarification returned the LLM draft directly.
  </behavior>

  <action>
  Change A — app/pipeline/decide.py Rule 2b:

  In decide.py, immediately after Rule 2 (lines ~123-125), add:
  - Comment: `# Rule 2b — field regression detected (D-17, C-1 resolution, MONEY-03).`
  - Line 1: `regressions = [i.field for i in issues if i.issue_type == "field_regression"]`
  - Line 2: `gate_reasons += [f"field regression: {f}" for f in regressions]`

  This is exactly 2 new lines plus 1 comment, placed between Rule 2 and Rule 3. Decision.missing_fields is NOT changed. The gate_reason format is `"field regression: {submitted_name}.{field_name}"` (the qualified field string from ValidationIssue.field).

  Change B — app/pipeline/compose_email.py N5 fix + D-7.5-09 wording (two-part fix):

  Part B1 — N5 fix in _template_body: Add a new unconditional block BEFORE the `if not decision.unresolved_names and not decision.missing_fields:` fallback.

  The new block in _template_body (before the fallback at line ~93):
  For each reason in decision.gate_reasons:
    If reason.startswith("field regression: "):
      - qualified = reason[len("field regression: "):]
      - submitted_name, field_name = qualified.rsplit(".", 1)  (LAST-DOT SPLIT — rsplit not split)
      - Append: `f"  - Reply with the {field_name} hours for {submitted_name}, or 'none' to confirm zero."`

  Place this block BEFORE the fallback so field-regression lines fire regardless of whether unresolved_names or missing_fields are also present. The existing fallback remains for other gate_reasons.

  Part B2 — Deterministic D-7.5-09 append in compose_clarification (Finding 4 fix):
  Find compose_clarification (the function that calls the LLM for a draft and assembles the final body). After obtaining the draft body (whether from LLM or fallback), and BEFORE returning, check whether any gate_reason in decision.gate_reasons starts with "field regression: ". If so, build the field-regression lines using the same rsplit(".", 1) logic and APPEND them to the body:

  - Collect field_regression_lines = [] by iterating decision.gate_reasons
  - For each reason starting with "field regression: ": qualified = reason[len("field regression: "):]; submitted_name, field_name = qualified.rsplit(".", 1); field_regression_lines.append(f"  - Reply with the {field_name} hours for {submitted_name}, or 'none' to confirm zero.")
  - If field_regression_lines is non-empty: append them to the email body after the LLM draft, separated by a blank line

  This ensures the D-7.5-09 wording appears on the real (LLM-draft) path AND on the template fallback path.

  Extract the rsplit logic into a small private helper `_field_regression_lines(gate_reasons: list[str]) -> list[str]` to avoid duplication between _template_body and compose_clarification (DRY).

  After implementing both changes, run:
  uv run pytest tests/test_decide.py -k "field_regression" -q
  uv run pytest tests/ -k "field_regression_line or dotted_submitted_name or d7509 or llm_draft" -q
  </action>

  <verify>
    <automated>uv run pytest tests/test_decide.py tests/test_validate.py -q 2>&1 | tail -5; uv run pytest -q 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
  - `grep "Rule 2b" app/pipeline/decide.py` exits 0
  - `grep "field_regression" app/pipeline/decide.py` exits 0 (the regressions list comprehension)
  - `grep "field regression" app/pipeline/compose_email.py` exits 0 (the N5 unconditional block in _template_body)
  - `grep "rsplit" app/pipeline/compose_email.py` exits 0 (last-dot split — guards against first-dot regression)
  - `grep "none.*to confirm zero\|confirm zero" app/pipeline/compose_email.py` exits 0 (D-7.5-09 wording present)
  - `grep "_field_regression_lines\|field_regression_lines" app/pipeline/compose_email.py` exits 0 (helper exists or inline, DRY)
  - test_field_regression_issue_gates_to_clarification GREEN: field_regression issue -> final_action="request_clarification"
  - test_field_regression_line_present_when_other_clarification_coexists GREEN: N5 fix confirmed
  - test_field_regression_line_dotted_submitted_name GREEN: rsplit semantics confirmed for "M. Chen.hours_overtime"
  - test_field_regression_line_d7509_wording GREEN: exact D-7.5-09 wording
  - test_d7509_wording_present_even_when_llm_draft_nonempty GREEN: deterministic append fires on LLM-draft path (Finding 4 fix confirmed)
  - `uv run pytest -q` full suite exits 0 — no regressions
  </acceptance_criteria>

  <done>Rule 2b (2 lines) added to decide.py after Rule 2. _template_body in compose_email.py emits field-regression lines unconditionally (N5). compose_clarification APPENDS the deterministic D-7.5-09 field-regression lines after any LLM draft body (Finding 4 fix — the wording is now guaranteed on the real path). rsplit last-dot split guards dotted submitted names. All new tests GREEN. Full suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detect_field_regression inputs | Both Extracted objects are Pydantic-validated with extra="forbid" at parse time; prior_matches/current_matches are typed NameMatchResult lists from reconcile_names |
| ValidationIssue(issue_type="field_regression") field | Contains submitted_name.field_name — same pattern as all existing ValidationIssue.field values; operator-visible context bounded |
| compose_email._template_body + compose_clarification gate_reason parse | gate_reasons are built from typed ValidationIssue.field values in decide.py (code-owned, never raw user input); rsplit operates on internal strings only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-02-01 | Tampering | detect_field_regression — input Extracted objects | mitigate | Both typed Pydantic models with extra="forbid" at extraction boundary. RawFieldDrop also has extra="forbid". No raw dict or string input. |
| T-7.5-02-02 | Information Disclosure | ValidationIssue.field contains submitted_name (PII) | accept | Same pattern as all existing ValidationIssue.field values (roster.py:93); gate reason contains field name; operator-visible context is bounded. |
| T-7.5-02-03 | Denial of Service | Loop over _HOURS_FIELDS (5 fields) × employees (bounded by extraction) | accept | O(employees × 5 fields) — trivially bounded for a payroll email; no external input can expand the loop. |
| T-7.5-02-04 | Tampering | compose_email rsplit parse of gate_reason string | accept | gate_reasons are built from typed ValidationIssue.field values in decide.py; rsplit(".", 1) produces at most 2 parts and the last part is always the field name; dotted-name test locks the semantics. |
| T-7.5-02-05 | Tampering | Finding 4 fix — appending deterministic wording after LLM draft | accept | Append is pure string concatenation on code-owned gate_reason strings; LLM draft is already operator-visible in the 3-column gate; the append cannot inject unexpected values beyond what decide.py constructed. |
| T-7.5-02-SC | Tampering | No new package installs | accept | Pure stdlib + existing Pydantic; no uv add invoked. |
</threat_model>

<verification>
1. `grep "class RawFieldDrop" app/models/contracts.py` — exits 0
2. `grep "def detect_field_regression" app/pipeline/validate.py` — exits 0
3. `grep "prior_matches" app/pipeline/validate.py` — exits 0 (employee-id-keyed diff takes prior_matches parameter — R3-3 fix)
4. `grep "resolved_drops" app/pipeline/validate.py` — exits 0 (N8 per-field suppression)
5. `grep "Rule 2b" app/pipeline/decide.py` — exits 0
6. `grep "field regression" app/pipeline/compose_email.py` — exits 0 (N5 unconditional block)
7. `grep "rsplit" app/pipeline/compose_email.py` — exits 0 (last-dot split)
8. `grep "none.*to confirm zero\|confirm zero" app/pipeline/compose_email.py` — exits 0 (D-7.5-09 wording)
9. `uv run pytest tests/test_validate.py -k "detect_regression or field_regression_emitted or re_resolution or resolved_drops or restated_name" -q` — all GREEN including the new restated-name test
10. `uv run pytest tests/test_decide.py -k "field_regression" -q` — GREEN
11. `uv run pytest tests/ -k "field_regression_line or dotted_submitted_name or d7509 or llm_draft_nonempty" -q` — GREEN
12. `uv run pytest -q` — full suite exits 0
</verification>

<success_criteria>
- RawFieldDrop (submitted_name-keyed, extra="forbid") exists in contracts.py before FieldDrop
- detect_field_regression(original, resumed, prior_matches, current_matches) reduces BOTH Extracted to employee_id-keyed maps BEFORE diffing (R3-3 fix — restated-name case detected); last-wins; reuses _HOURS_FIELDS + _is_paid; D-25/D-26/D-27 proven by tests; SC2 restated-name case proven by test_restated_name_same_employee_id_is_detected
- validate() prior=/prior_matches=/resolved_drops= kwargs FUNCTIONAL: detect_field_regression called with employee-id-keyed approach; N8 per-field resolved_drops suppression in field_regression path only; any_hours gate unchanged
- decide.py Rule 2b (2 lines): field_regression issues gate to request_clarification; Decision.missing_fields unchanged
- compose_email: N5 unconditional _template_body block (field-regression lines before fallback, rsplit last-dot split, D-7.5-09 wording) PLUS deterministic D-7.5-09 append in compose_clarification after LLM draft — wording guaranteed on real path (Finding 4 fix)
- All tests GREEN; full suite GREEN
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-02-SUMMARY.md` when done.
</output>
