---
phase: 07.5-clarification-reply-field-regression
plan: "02"
type: execute
wave: 2
depends_on:
  - "07.5-01"
files_modified:
  - app/models/contracts.py
  - app/pipeline/validate.py
  - app/pipeline/decide.py
  - app/pipeline/compose_email.py
autonomous: true
requirements:
  - MONEY-03

must_haves:
  truths:
    - "RawFieldDrop model exists in contracts.py (before FieldDrop) with submitted_name: str, field: str, original_value: Decimal (ge=0), resumed_value: Decimal | None (ge=0), ConfigDict(extra='forbid') — the internal raw-keyed counterpart to FieldDrop (D-7.5-05)"
    - "detect_field_regression(original, resumed) is a pure function in validate.py returning list[RawFieldDrop] — iterates _HOURS_FIELDS via _is_paid; last-wins reduction (D-12) on both sides; never redefines _HOURS_FIELDS (DRY, D-09)"
    - "validate() prior=/prior_matches=/resolved_drops= kwargs are NOW FUNCTIONAL: when prior is not None, detect_field_regression is called and each RawFieldDrop is promoted to a ValidationIssue(issue_type='field_regression') via N6 same-employee_id-in-both check; resolved_drops suppression is per-(employee_id,field) in the field_regression path only — NOT at the any_hours gate (N8 fix)"
    - "decide.py Rule 2b (2 lines after Rule 2): field_regression issues gate to request_clarification; Decision.missing_fields is NOT widened (D-7.5 PATTERNS §4)"
    - "_template_body in compose_email.py emits field-regression lines UNCONDITIONALLY whenever gate_reasons contains 'field regression:' entries — BEFORE the unresolved/missing fallback gate (N5 fix); uses rsplit('.', 1) last-dot split so dotted submitted names (e.g. 'M. Chen') parse correctly (WARNING 1 lock)"
    - "D-7.5-09 wording lock: field-regression email line reads 'Reply with the {field_name} hours for {submitted_name}, or none to confirm zero.' — elicits a value, not a yes/no"
    - "D-26: explicit zero resumed_value=Decimal('0') returned as RawFieldDrop with resumed_value=Decimal('0') (not None) — confirmed_dropped signal preserved"
    - "D-25: OT 2->0 and OT 2->absent both produce a RawFieldDrop (shared _is_paid predicate)"
    - "uv run pytest tests/test_validate.py tests/test_decide.py -q exits 0"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "RawFieldDrop model (submitted_name-keyed, internal)"
      contains: "class RawFieldDrop"
    - path: "app/pipeline/validate.py"
      provides: "detect_field_regression + validate(prior=, prior_matches=, resolved_drops=) implementation (N6/N8 correct)"
      exports: ["detect_field_regression", "validate"]
      contains: "def detect_field_regression"
    - path: "app/pipeline/decide.py"
      provides: "Rule 2b — field_regression issues gate to request_clarification"
      contains: "Rule 2b"
    - path: "app/pipeline/compose_email.py"
      provides: "N5 field-regression email line (unconditional, before fallback) with D-7.5-09 wording and rsplit last-dot split"
      contains: "field regression"
  key_links:
    - from: "app/pipeline/validate.py:detect_field_regression"
      to: "app/pipeline/validate.py:validate (prior= kwarg)"
      via: "validate() calls detect_field_regression(prior, extracted) when prior is not None; maps RawFieldDrop -> FieldDrop via employee_id-in-both N6 check; emits ValidationIssue(issue_type='field_regression')"
      pattern: "detect_field_regression"
    - from: "app/pipeline/decide.py:Rule 2b"
      to: "app/pipeline/compose_email.py:_template_body"
      via: "field_regression gate_reason -> N5 unconditional block emits the D-7.5-09 question regardless of other clarification reasons"
      pattern: "field regression"
---

<objective>
Implement the MONEY-03 pure-judgment layer: `RawFieldDrop` model, `detect_field_regression` pure helper, `validate(prior=)` seam implementation (N6/N8-correct), decide Rule 2b, and the field-regression email copy (N5/D-7.5-09).

Purpose: Establish and test the pure-function judgment layer that the eval can certify (D-23 eval layer). Plan 01 added the kwarg slots as no-ops; this plan implements the actual logic. All changes are pure-function or data-model changes — no DB writes, no orchestrator changes. The eval layer (fixtures 16/17/18) and integration wiring come in later plans.

D-7.5-05 reality check (verified against live source): Phase 7 shipped `FieldDrop` (contracts.py:124), the `field_regression` ValidationIssue Literal (roster.py:219), and `_is_paid` (validate.py:38). `RawFieldDrop` was NOT shipped — it is created here. `detect_field_regression` does NOT exist yet — created here. Both `FieldDrop` and `_is_paid` are reused without modification.

N6 design (D-11): `detect_field_regression` returns list[RawFieldDrop] keyed by submitted_name — correct for a pure helper with no access to matches. The N6 same-employee_id cross-check happens one level up in `validate()` which has both prior_matches and current matches. When prior_matches is None (accepted D-7.5-01d scope), the cross-check is a deliberate no-op — the drop is promoted on the submitted_name match alone.

N8 design: `resolved_drops` suppression applies ONLY to the field_regression ValidationIssue emission path inside validate(), NOT to the any_hours gate (MONEY-01). An employee with one confirmed_dropped field but all other hours zero still triggers MONEY-01.

Output: Pure-function judgment layer passing targeted unit tests; eval can certify it with prior= fixtures.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md

@app/models/contracts.py
@app/pipeline/validate.py
@app/pipeline/decide.py
@app/pipeline/compose_email.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RawFieldDrop model + detect_field_regression + validate(prior=) seam implementation</name>
  <files>app/models/contracts.py, app/pipeline/validate.py</files>

  <read_first>
  Read app/models/contracts.py in full. Key anchors:
  - Lines 60-80: ExtractedEmployee — ConfigDict(extra="forbid"), Decimal | None + ge=0. Style template for RawFieldDrop.
  - Lines 124-146: FieldDrop (shipped Phase 7) — the PUBLIC employee_id-keyed model. RawFieldDrop is the INTERNAL submitted_name-keyed peer. Add RawFieldDrop BEFORE FieldDrop (~line 119-120).
  - Lines 13-20: existing imports — UUID, Decimal, Field, BaseModel, ConfigDict are all already present.

  Read app/pipeline/validate.py in full (Plan 01 modified the signature; re-read to get the current state). Key anchors:
  - Lines 29-35: _HOURS_FIELDS tuple — detect_field_regression MUST reuse this constant, never redefine it.
  - Line 38: _is_paid predicate — the third call site is detect_field_regression (D-09); reuse it.
  - Lines 81-85: validate() signature after Plan 01's addition — now has prior=None, prior_matches=None, resolved_drops=None keyword-only kwargs.
  - Lines ~96-100: any_hours gate — N8: do NOT touch this gate for resolved_drops; it runs unchanged on every employee.
  - Lines ~103-112: ValidationIssue emission pattern — template for the field_regression emission.
  - Current import block (~lines 1-27) — add RawFieldDrop, FieldDrop, UUID imports here.

  From PATTERNS.md §1: Place detect_field_regression between `_employee_pay_periods_per_year` (which ends before validate()) and validate(). Use last-wins reduction on both original and resumed employee lists before diffing (D-12).

  From PATTERNS.md §2: The updated validate() signature (from Plan 01) already matches what is needed. Implement the body additions:
  - When prior is not None: build name_to_id_CURRENT from matches; build name_to_id_SNAPSHOT from prior_matches (empty dict if None); call raw_drops = detect_field_regression(prior, extracted); for each raw_drop apply N6 cross-check; emit ValidationIssue(issue_type="field_regression", field="{submitted_name}.{field}", ...) for non-skipped drops not in resolved_drops.

  N6 cross-check steps for each raw_drop:
  1. current_emp_id = name_to_id_CURRENT.get(raw_drop.submitted_name) — if None: skip (unresolved in current)
  2. If prior_matches is not None: snapshot_emp_id = name_to_id_SNAPSHOT.get(raw_drop.submitted_name) — if None: skip (unresolved in snapshot)
  3. If prior_matches is not None AND snapshot_emp_id != current_emp_id: skip (re-resolution, D-11)
  4. N8 check: if (current_emp_id, raw_drop.field) in (resolved_drops or set()): skip (confirmed_dropped, D-15)
  5. Otherwise emit: ValidationIssue(issue_type="field_regression", field=f"{raw_drop.submitted_name}.{raw_drop.field}", message=f"field regression: {raw_drop.field} was {raw_drop.original_value}, now {'absent' if raw_drop.resumed_value is None else raw_drop.resumed_value}")

  Gate-reason string format from PATTERNS.md §7: `f"field regression: {validated_issue.field}"` — where field = "{submitted_name}.{field_name}". This format feeds directly to compose_email.py which uses rsplit(".", 1) to parse it.
  </read_first>

  <behavior>
  Tests to write (TDD — write failing tests FIRST, then implement):

  test_detect_regression_ot_absent (in tests/test_validate.py or new tests/test_detect_field_regression.py):
    detect_field_regression(original_extracted_with_ot=2, resumed_extracted_with_ot=None) returns list with exactly one RawFieldDrop where drop.field == "hours_overtime", drop.original_value == Decimal("2"), drop.resumed_value is None, drop.submitted_name == the employee's submitted name.

  test_explicit_drop_zero_resumed_value (D-26):
    resumed_extracted has hours_overtime=Decimal("0") -> RawFieldDrop with resumed_value=Decimal("0") (explicit zero preserved — confirmed_dropped signal for D-14).

  test_no_regression_noop (D-27):
    original and resumed identical -> detect_field_regression returns [].

  test_predicate_consistency (D-25):
    OT 2->0 and OT 2->absent both produce a RawFieldDrop for hours_overtime (shared _is_paid predicate).

  test_validate_field_regression_emitted_with_prior (in tests/test_validate.py):
    call validate(extracted_ot_absent, roster, matches, prior=extracted_ot_2, prior_matches=matches) -> issues contains ValidationIssue(issue_type="field_regression") for hours_overtime.

  test_re_resolution_skip (N6 test):
    submitted_name "M. Chen" resolves to employee_A in prior_matches but to employee_B in current matches. validate() with prior_matches pointing to A and current matches to B emits NO field_regression for "M. Chen" (re-resolution is skipped per D-11).

  test_prior_matches_none_promotes_drop (N6 no-op / accepted scope):
    Same drop scenario but prior_matches=None. validate() with prior=..., prior_matches=None DOES emit the field_regression issue (the cross-check is a deliberate no-op when prior_matches is None).

  test_resolved_drops_suppression_is_per_field (N8):
    Employee has confirmed_dropped=hours_overtime AND hours_regular=Decimal("0"). Build resolved_drops={(employee_id, "hours_overtime")}. Call validate(extracted, roster, matches, prior=prior, resolved_drops=resolved_drops). Assert: MONEY-01 any_hours gate fires (issues contains "missing" for the employee — regular=0 means not paid). Assert: NO field_regression issue emitted for hours_overtime (suppressed by resolved_drops). resolved_drops ONLY suppresses the field_regression re-flag, NOT the MONEY-01 gate.
  </behavior>

  <action>
  Step 1 — Add RawFieldDrop to app/models/contracts.py:

  Insert before the FieldDrop class (~line 119-120, after the last existing model before FieldDrop):
  - Class name: RawFieldDrop
  - model_config = ConfigDict(extra="forbid")
  - submitted_name: str (raw submitted name from extraction — not a UUID)
  - field: str (bare field name, e.g. "hours_overtime")
  - original_value: Decimal = Field(ge=0)
  - resumed_value: Decimal | None = Field(default=None, ge=0)
  - Add docstring: "Internal raw record keyed by submitted_name (D-7.5-05). detect_field_regression returns list[RawFieldDrop]. validate() maps submitted_name->employee_id via matches to produce FieldDrop. resumed_value=None means absent (silence); resumed_value=Decimal('0') means explicit zero (D-14 confirmed_dropped signal)."
  No new imports needed — all types (Decimal, Field, BaseModel, ConfigDict) are already imported.

  Step 2 — Add detect_field_regression to app/pipeline/validate.py:

  Add to the import block: `from app.models.contracts import Extracted, RawFieldDrop, FieldDrop` (replace the existing Extracted-only import if present), and `from uuid import UUID`.

  Place detect_field_regression between the last private helper function and validate(). Public signature:
  `def detect_field_regression(original: Extracted, resumed: Extracted) -> list[RawFieldDrop]:`

  Body:
  - Build {submitted_name: ExtractedEmployee} dicts for original.employees and resumed.employees using last-wins reduction (D-12): iterate each list; later entries for the same submitted_name overwrite earlier ones.
  - For each submitted_name in sorted(intersection of both dicts) (sorted for determinism, D-27):
    - For each field in _HOURS_FIELDS (reuse the module-level constant — do NOT redefine it):
      - original_val = getattr(original_emp, field); resumed_val = getattr(resumed_emp, field)
      - If _is_paid(original_val) and not _is_paid(resumed_val): drop detected
      - Append RawFieldDrop(submitted_name=submitted_name, field=field, original_value=original_val, resumed_value=resumed_val)
  - Return the list. (N6 employee_id cross-check happens in validate() one level up — not here.)

  Step 3 — Implement validate() kwargs in app/pipeline/validate.py:

  The signature was already extended by Plan 01 with `*, prior=None, prior_matches=None, resolved_drops=None`.

  Add the following logic at the start of validate(), before the existing per-employee loop:

  a. `_resolved_drops = resolved_drops or set()`

  b. When `prior is not None`:
     - Build `name_to_id_current: dict[str, UUID] = {m.submitted_name: m.matched_employee_id for m in matches if m.resolved and m.matched_employee_id is not None}`
     - Build `name_to_id_snapshot: dict[str, UUID] = {m.submitted_name: m.matched_employee_id for m in prior_matches if m.resolved and m.matched_employee_id is not None}` (empty dict if prior_matches is None)
     - `raw_drops = detect_field_regression(prior, extracted)`
     - For each raw_drop in raw_drops, apply N6 and N8 checks (see read_first steps 1-5 above); emit ValidationIssue(issue_type="field_regression", field=f"{raw_drop.submitted_name}.{raw_drop.field}", message=f"field regression: {raw_drop.field} was {raw_drop.original_value}, now {'absent' if raw_drop.resumed_value is None else str(raw_drop.resumed_value)}")

  c. In the per-employee loop, the any_hours gate (lines ~96-100) runs UNCHANGED for all employees regardless of _resolved_drops — do NOT skip or suppress it.

  d. The `_resolved_drops` set is used ONLY in the field_regression emission (step b above) to skip re-emitting a regression for (employee_id, field) pairs already confirmed_dropped (D-15/N8). It does NOT touch the any_hours/missing path.

  After implementing, run tests:
  uv run pytest tests/test_validate.py -k "detect_regression or explicit_drop or no_regression or predicate_consistency or re_resolution or resolved_drops_per_field or field_regression_emitted or prior_matches_none" -q
  </action>

  <verify>
    <automated>uv run pytest tests/test_validate.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "class RawFieldDrop" app/models/contracts.py` exits 0
  - `grep "def detect_field_regression" app/pipeline/validate.py` exits 0
  - `grep "RawFieldDrop" app/pipeline/validate.py` exits 0 (import present)
  - `grep "prior_matches" app/pipeline/validate.py` exits 0 (N6 cross-check implemented)
  - `grep "resolved_drops" app/pipeline/validate.py` exits 0 (N8 per-field suppression in field_regression path)
  - `uv run pytest tests/test_validate.py -q` exits 0 — all 8 new tests pass plus existing tests green
  - test_detect_regression_ot_absent passes: detect_field_regression returns 1 RawFieldDrop with field="hours_overtime", original_value=Decimal("2"), resumed_value=None
  - test_explicit_drop_zero_resumed_value passes: resumed OT=Decimal("0") returns RawFieldDrop with resumed_value=Decimal("0") — NOT None (D-26 preserved)
  - test_resolved_drops_suppression_is_per_field passes: MONEY-01 any_hours gate fires even when (employee_id, "hours_overtime") is in resolved_drops (N8 per-field scope confirmed)
  - test_re_resolution_skip passes: re-resolution to different employee_id skips the field_regression issue (N6/D-11)
  - test_prior_matches_none_promotes_drop passes: prior_matches=None is a deliberate no-op for the cross-check; drop is still detected
  </acceptance_criteria>

  <done>RawFieldDrop exists in contracts.py before FieldDrop. detect_field_regression exists in validate.py with N6/N8-correct implementation. validate() prior= kwargs are functional. All new tests GREEN. Full validate test suite GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: decide Rule 2b + compose_email N5 field-regression line (D-7.5-09 wording)</name>
  <files>app/pipeline/decide.py, app/pipeline/compose_email.py</files>

  <read_first>
  Read app/pipeline/decide.py in full in one pass. Key anchors:
  - Lines 123-125: Rule 2 (the 2-line template to mirror): `missing = [i.field for i in issues if i.issue_type == "missing"]` / `gate_reasons += [f"missing required field: {f}" for f in missing]`. Rule 2b is 2 lines placed IMMEDIATELY AFTER these two.
  - Lines 127-129: Rule 3 — stays unchanged; Rule 2b sits between Rule 2 and Rule 3.
  - Confirm Decision.missing_fields is NOT updated — regressions feed gate_reasons only (PATTERNS.md §3 note).

  Read app/pipeline/compose_email.py in full in one pass. Key anchors:
  - Lines 63-102: _template_body function. Current structure:
    - lines ~71: if decision.unresolved_names: ... block
    - lines ~87: if decision.missing_fields: ... block
    - lines ~93-96: "if not unresolved_names and not missing_fields:" fallback
    - lines ~97-101: the "Could you please reply..." closing line
  - N5 bug: field-regression lines are currently eaten by the fallback gate condition. Fix: add a new unconditional block BEFORE the fallback (lines ~93) that iterates gate_reasons and emits field-regression lines.

  Gate-reason string format (PATTERNS.md §7): the format written by decide.py is "field regression: {ValidationIssue.field}" where ValidationIssue.field = "{submitted_name}.{field_name}". The submitted_name may contain dots (e.g. "M. Chen"), so compose_email MUST use rsplit(".", 1) — last-dot split — NOT split(".", 1) which would break "M. Chen.hours_overtime" into submitted_name="M" and field="Chen.hours_overtime".

  D-7.5-09 wording lock: the field-regression email line MUST be:
  "  - Reply with the {field_name} hours for {submitted_name}, or 'none' to confirm zero."
  This phrasing elicits a value (new hours) or explicit zero/none (confirmed_dropped), avoiding the yes/no trap.
  </read_first>

  <behavior>
  Tests to write (TDD — write FIRST, then implement):

  test_field_regression_issue_gates_to_clarification (in tests/test_decide.py):
    Build issues=[ValidationIssue(issue_type="field_regression", field="alice.hours_overtime", message="regression")] with extracted and matches for a valid run. Call decide(extracted, matches, issues). Assert: decision.final_action == "request_clarification" AND any("field regression" in r for r in decision.gate_reasons). Currently FAILS (Rule 2 only checks "missing").

  test_field_regression_line_present_when_other_clarification_coexists (in tests/test_compose_email.py or tests/test_validate.py):
    Build a Decision with unresolved_names=["Bob Smith"] AND gate_reasons=["field regression: alice.hours_overtime"]. Call _template_body(decision). Assert: result contains text about "hours_overtime" (field-regression line) AND contains "Bob Smith" (unresolved name line). Currently FAILS (N5 bug: field-regression line is dropped when unresolved_names is non-empty).

  test_field_regression_line_dotted_submitted_name (in same test file):
    Build a Decision with gate_reasons=["field regression: M. Chen.hours_overtime"]. Call _template_body(decision). Assert: rendered email line contains "M. Chen" as the name and "hours_overtime" as the field (NOT "M" as the name and "Chen.hours_overtime" as the field). This locks the rsplit(".", 1) semantics and catches a first-dot split regression.

  test_field_regression_line_d7509_wording (in same test file):
    Build a Decision with gate_reasons=["field regression: Alice Johnson.hours_overtime"]. Call _template_body(decision). Assert: rendered line contains "Reply with the hours_overtime hours for Alice Johnson, or 'none' to confirm zero." (D-7.5-09 exact wording).
  </behavior>

  <action>
  Change A — app/pipeline/decide.py Rule 2b:

  In decide.py, immediately after Rule 2 (lines ~123-125), add:
  - Comment: `# Rule 2b — field regression detected (D-17, C-1 resolution, MONEY-03).`
  - Line 1: `regressions = [i.field for i in issues if i.issue_type == "field_regression"]`
  - Line 2: `gate_reasons += [f"field regression: {f}" for f in regressions]`

  This is exactly 2 new lines plus 1 comment, placed between Rule 2 and Rule 3. Decision.missing_fields is NOT changed. The gate_reason format is `"field regression: {submitted_name}.{field_name}"` (the qualified field string from ValidationIssue.field).

  Change B — app/pipeline/compose_email.py N5 fix + D-7.5-09 wording:

  In _template_body, add a new unconditional block BEFORE the `if not decision.unresolved_names and not decision.missing_fields:` fallback. The new block:

  For each reason in decision.gate_reasons:
    If reason.startswith("field regression: "):
      - qualified = reason[len("field regression: "):] — strips the "field regression: " prefix; result is e.g. "M. Chen.hours_overtime"
      - submitted_name, field_name = qualified.rsplit(".", 1) — LAST-DOT SPLIT (submitted_name may contain dots); rsplit(".", 1) splits at the rightmost dot
      - Append: `f"  - Reply with the {field_name} hours for {submitted_name}, or 'none' to confirm zero."` (D-7.5-09 locked wording)

  Place the block BEFORE the existing fallback gate so field-regression lines fire regardless of whether unresolved_names or missing_fields are also present (N5 fix).

  The existing "if not unresolved_names and not missing_fields:" fallback block remains unchanged for other generic gate_reasons that are not field_regression type.

  After implementing both changes, run:
  uv run pytest tests/test_decide.py -k "field_regression" -q
  uv run pytest tests/ -k "field_regression_line or dotted_submitted_name or d7509" -q
  </action>

  <verify>
    <automated>uv run pytest tests/test_decide.py tests/test_validate.py -q 2>&1 | tail -5; uv run pytest -q 2>&1 | tail -3</automated>
  </verify>

  <acceptance_criteria>
  - `grep "Rule 2b" app/pipeline/decide.py` exits 0
  - `grep "field_regression" app/pipeline/decide.py` exits 0 (the regressions list comprehension)
  - `grep "field regression" app/pipeline/compose_email.py` exits 0 (the new unconditional block)
  - `grep "rsplit" app/pipeline/compose_email.py` exits 0 (last-dot split — guards against first-dot regression)
  - `grep "none.*to confirm zero\|confirm zero" app/pipeline/compose_email.py` exits 0 (D-7.5-09 wording present)
  - test_field_regression_issue_gates_to_clarification GREEN: field_regression issue -> final_action="request_clarification" with gate_reasons containing "field regression:"
  - test_field_regression_line_present_when_other_clarification_coexists GREEN: field-regression line appears even when unresolved_names is non-empty (N5 fix confirmed)
  - test_field_regression_line_dotted_submitted_name GREEN: "M. Chen.hours_overtime" parses to submitted_name="M. Chen" and field="hours_overtime" — rsplit semantics confirmed
  - test_field_regression_line_d7509_wording GREEN: exact D-7.5-09 wording in rendered line
  - `uv run pytest -q` full suite exits 0 — no regressions
  </acceptance_criteria>

  <done>Rule 2b (2 lines) added to decide.py after Rule 2. _template_body in compose_email.py emits field-regression lines unconditionally via rsplit last-dot split with D-7.5-09 wording. All new tests GREEN. Full suite GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| detect_field_regression inputs | Both Extracted objects are Pydantic-validated with extra="forbid" at parse time — no raw string input |
| ValidationIssue(issue_type="field_regression") field | Contains submitted_name.field_name — same pattern as all existing ValidationIssue.field values; operator-visible context bounded |
| compose_email._template_body gate_reason parse | gate_reasons are built from typed ValidationIssue.field values in decide.py (code-owned, never raw user input); rsplit operates on internal strings only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-02-01 | Tampering | detect_field_regression — input Extracted objects | mitigate | Both typed Pydantic models with extra="forbid" at extraction boundary. RawFieldDrop also has extra="forbid". No raw dict or string input. |
| T-7.5-02-02 | Information Disclosure | ValidationIssue.field contains submitted_name (PII) | accept | Same pattern as all existing ValidationIssue.field values (roster.py:93); gate reason contains field name; operator-visible context is bounded. |
| T-7.5-02-03 | Denial of Service | Loop over _HOURS_FIELDS (5 fields) × employees (bounded by extraction) | accept | O(employees × 5 fields) — trivially bounded for a payroll email; no external input can expand the loop. |
| T-7.5-02-04 | Tampering | compose_email N5 rsplit parse of gate_reason string | accept | gate_reasons are built from typed ValidationIssue.field values in decide.py; rsplit(".", 1) produces at most 2 parts and the last part is always the field name; dotted-name test locks the semantics. |
| T-7.5-02-SC | Tampering | No new package installs | accept | Pure stdlib + existing Pydantic; no uv add invoked. |
</threat_model>

<verification>
1. `grep "class RawFieldDrop" app/models/contracts.py` — exits 0
2. `grep "def detect_field_regression" app/pipeline/validate.py` — exits 0
3. `grep "prior_matches" app/pipeline/validate.py` — exits 0 (N6 cross-check)
4. `grep "resolved_drops" app/pipeline/validate.py` — exits 0 (N8 per-field suppression)
5. `grep "Rule 2b" app/pipeline/decide.py` — exits 0
6. `grep "field regression" app/pipeline/compose_email.py` — exits 0 (N5 unconditional block)
7. `grep "rsplit" app/pipeline/compose_email.py` — exits 0 (last-dot split)
8. `grep "none.*to confirm zero\|confirm zero" app/pipeline/compose_email.py` — exits 0 (D-7.5-09 wording)
9. `uv run pytest tests/test_validate.py -k "detect_regression or field_regression_emitted or re_resolution or resolved_drops" -q` — all GREEN
10. `uv run pytest tests/test_decide.py -k "field_regression" -q` — GREEN
11. `uv run pytest tests/ -k "field_regression_line or dotted_submitted_name or d7509" -q` — GREEN
12. `uv run pytest -q` — full suite exits 0
</verification>

<success_criteria>
- RawFieldDrop (submitted_name-keyed, extra="forbid") exists in contracts.py before FieldDrop
- detect_field_regression(original, resumed) returns list[RawFieldDrop], reuses _HOURS_FIELDS + _is_paid, last-wins reduction, D-25/D-26/D-27 proven by tests
- validate() prior=/prior_matches=/resolved_drops= kwargs are FUNCTIONAL with N6 (same-employee_id-in-both cross-check) and N8 (per-field resolved_drops suppression in field_regression path only, any_hours gate unchanged)
- decide.py Rule 2b (2 lines): field_regression issues gate to request_clarification; Decision.missing_fields unchanged
- compose_email._template_body N5 fix: field-regression lines unconditional, before fallback gate, rsplit last-dot split, D-7.5-09 wording locked
- All tests GREEN; full suite GREEN
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-02-SUMMARY.md` when done.
</output>
