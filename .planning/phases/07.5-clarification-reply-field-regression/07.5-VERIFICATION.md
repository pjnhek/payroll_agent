---
phase: 07.5-clarification-reply-field-regression
verified: 2026-06-28T12:00:00Z
status: passed
score: 6/6
overrides_applied: 0
re_verification: null
gaps: []
deferred: []
human_verification: []
---

# Phase 7.5: Clarification-Reply Field-Regression Verification Report

**Phase Goal:** A clarification reply that drops a money-affecting field the original submission stated (original '40 + 2 OT', reply '40' with no OT) is detected as a regression, clarifies exactly once, then carries the original value forward — or honors an explicit removal — and never enters an infinite re-clarify loop. This phase is built on a foundational refactor first: `_run_stages` is split so the carry-forward backfill lands between reconcile and validate/decide/calc.

**Verified:** 2026-06-28T12:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Field-regression DETECTION: a dropped money field is detected on RAW (pre-backfill) extracted, employee-id-keyed via prior_matches | VERIFIED | `app/pipeline/validate.py:82-157` — `detect_field_regression` builds `id_to_orig` and `id_to_resumed` dicts keyed by `employee_id` (UUID), using `prior_matches` and `current_matches` respectively. Called in `_run_stages` at line 655 BEFORE `backfill_extracted` at line 662 (D-7.5-10 three-phase ordering). Test `test_detect_fired_on_raw` PASSES: spy on `detect_field_regression` confirms the drop is visible on raw data; `test_restated_name_prior_matches_threading` PASSES: 'M. Chen' (prior) and 'Maria Chen' (current) resolve to the same employee_id — regression detected. |
| 2 | CLARIFY EXACTLY ONCE: the run clarifies once and does not re-clarify a field already answered (no infinite loop) | VERIFIED | `app/pipeline/orchestrator.py:356-507` — `is_round_2 = bool(clarified)` branches at Step E4/E5. Round-2 path builds `suppress_detection` (ALL answered fields) forwarded to `validate(resolved_drops=suppress_detection)` where N8 suppression (`(str(current_emp_id), field) in _resolved_drops`) prevents re-emission. Test `test_loop_guard_fires_exactly_once` PASSES: Round-2 silence reaches AWAITING_APPROVAL with zero field_regression outbound rows. |
| 3 | CARRY FORWARD / HONOR EXPLICIT REMOVAL / USE CLIENT VALUE: three outcomes are correctly implemented | VERIFIED | `app/pipeline/orchestrator.py:422-434` — classify-first logic: `raw_val > 0` → `client_supplied`; `raw_val == Decimal('0')` → `confirmed_dropped`; `None/absent` → `carried_forward`. `backfill_extracted` (`orchestrator.py:82-170`) uses `resolved_drops` (backfill_skip): `confirmed_dropped` and `client_supplied` are in `backfill_skip` (skipped); `carried_forward` is absent (backfill fires, OT=2 from snapshot). Tests: `test_answered_silence_reaches_approval` PASSES (OT=2 in paystub, 'carried_forward'); `test_answered_explicit_zero_not_rebackfilled` PASSES (OT not re-backfilled to 2, 'confirmed_dropped'); `test_answered_positive_uses_client_value` PASSES (OT=5 in paystub, 'client_supplied'). |
| 4 | NO OVERPAY / NO UNDERPAY (money-path correctness) | VERIFIED | Overpay guard: `backfill_extracted` at `orchestrator.py:154` checks `(emp_id_str, field) in _resolved_drops` before filling — `confirmed_dropped` is always in `backfill_skip` even when `_is_paid(Decimal('0'))` is False. CR-01 fix: `name_to_id_for_classify` built from UNION of `current_matches_for_classify + prior_matches` (`orchestrator.py:383-387`) — restated name cannot escape classification. R2-2 fix: `prior_matches_for_backfill = list(snapshot_matches) + list(prior_matches)` (`orchestrator.py:488`) so 'M. Chen' in snapshot is resolved via alias to CHEN_ID in backfill. Tests: `test_confirmed_dropped_no_reloop_on_round2` PASSES (OT not re-backfilled); `test_restated_name_prior_matches_threading` Part B PASSES (paystub OT=2 after restated name); `tests/test_cr01_classify_union.py` — 5 tests all PASS covering restated-name + explicit-zero classification. |
| 5 | FOUNDATIONAL REFACTOR: `_run_stages` split with correct seams, shared by `run_pipeline` and `resume_pipeline` (DRY) | VERIFIED | `app/pipeline/orchestrator.py:602-709` — `_run_stages` accepts `prior=`, `prior_matches=`, `resolved_drops=`, `suppress_detection=`, `extracted=` kwargs. Called from `_run` at line 202 (run_pipeline) and from Round-1 path at line 305 and Round-2 path at line 491 (resume_pipeline). Two-set MODEL: `suppress_detection` → `validate(resolved_drops=suppress_detection)` at line 676; `resolved_drops` → `backfill_extracted` at line 662. These are never conflated. D-7.5-10 three-phase ordering within `_run_stages`: detect (line 655) < backfill (line 662) < validate/decide/calc (lines 670+). |
| 6 | EVIDENCE: 15 tests in test_resume_pipeline.py PASS, eval fixtures 16/17/18 exist and --check passes, run_detail.html has four provenance badges | VERIFIED | Live test run: `DATABASE_URL=postgresql://test/test uv run pytest tests/test_resume_pipeline.py -v` → 15 passed, 0 skipped, 0 failed. Additional MONEY-03 tests: `test_detect_field_regression.py` (10 tests), `test_decide_field_regression.py` (4), `test_compose_email_field_regression.py` (6), `test_cr01_classify_union.py` (5) — all 26 PASS. Full suite: 506 passed, 17 skipped (ALLOW_DB_RESET / ALLOW_LIVE_LLM guards), 0 failed. `uv run python eval/run_eval.py --check` → exit 0. Fixtures 16/17/18 all present (6 files: .json + _extraction.json each). `run_detail.html` lines 144-156 show four badges (carried-forward, client-removed, client-supplied, awaiting-reply) and line 223 shows `clarification_field_regression` purpose badge. |

**Score:** 6/6 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/pipeline/orchestrator.py` | `_run_stages` split with prior/prior_matches/resolved_drops/suppress_detection/extracted seams; resume_pipeline with Round-1/Round-2 branches; classify-first; R2-2 snapshot-name fix | VERIFIED | 1155 lines; all seams present at lines 602-709 (`_run_stages`), 305-352 (Round-1), 356-506 (Round-2 classify-first); `backfill_extracted` at lines 82-170 |
| `app/pipeline/validate.py` | `detect_field_regression` employee-id-keyed, called before backfill | VERIFIED | Lines 82-157; builds `id_to_orig` and `id_to_resumed` keyed by UUID; `prior_matches is None` returns `[]` (documented no-op); `validate()` accepts `raw_field_drops=` kwarg (lines 160-284) |
| `app/db/schema.sql` | `pre_clarify_extracted` and `clarified_fields` JSONB columns; N4 purpose CHECK | VERIFIED | Lines 91-92 (column definitions); lines 111-112 (ADD COLUMN IF NOT EXISTS migrations); lines 151-196 (N4 purpose CHECK with `clarification_field_regression`) |
| `app/db/repo.py` | `set_pre_clarify_extracted` (IS NULL CAS), `load_pre_clarify_extracted`, `set_clarified_fields` (typed via ClarifiedFields), `load_clarified_fields` | VERIFIED | Lines 527-628; IS NULL WHERE guard at line 543-547; ClarifiedFields validation at line 591 |
| `app/models/contracts.py` | `RawFieldDrop` model, `ClarifiedFields` typed validator | VERIFIED | Lines 120-192; `RawFieldDrop` at line 124; `ClarifiedFields` at line 171 |
| `tests/test_resume_pipeline.py` | 15 hermetic integration tests, pytestmark skipif, fake_repo+mock_llm pattern | VERIFIED | 1148 lines; 15 `def test_` functions; `pytestmark` at line 41; `fake_repo` + `mock_llm` fixtures throughout; all 15 PASS with `DATABASE_URL` set |
| `tests/test_cr01_classify_union.py` | CR-01 fix regression tests | VERIFIED | 5 tests, all PASS; covers restated-name classify, explicit-zero confirmed_dropped, WR-01 unresolvable field, IN-03 malformed rsplit guard |
| `tests/test_detect_field_regression.py` | detect_field_regression unit tests | VERIFIED | 10 tests, all PASS |
| `eval/run_eval.py` | `detect_field_regression` import and D-7.5-10 three-phase wiring; `prior_extracted` + `prior_matches` stripped from InboundEmail | VERIFIED | Line 34 imports `detect_field_regression`; lines 98-184 strip and deserialize `prior_extracted`/`prior_matches`; three-phase call at lines 178-186 |
| `eval/fixtures/18_field_drop_clarify_carry_coastal.json` | MONEY-03 eval fixture with `prior_extracted` + `prior_matches` keys | VERIFIED | `prior_extracted` key at line 12; `prior_matches` array at line 28; `expected.decision.final_action = "request_clarification"` confirmed via Python; fixture scores correctly |
| `eval/fixtures/16_*.json` + `eval/fixtures/17_*.json` | MONEY-01 and MONEY-02 eval fixtures | VERIFIED | All 6 fixture files (3 pairs) present; eval `--check` passes |
| `app/templates/run_detail.html` | Four provenance badges (carried-forward, client-removed, client-supplied, awaiting-reply) + `clarification_field_regression` purpose badge | VERIFIED | Lines 144-156 (four outcome badges via Jinja2 macro); line 223 (purpose badge) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `orchestrator._run_stages` line 655 | `validate.detect_field_regression` | Called BEFORE `backfill_extracted` at line 662 | WIRED | D-7.5-10 three-phase: detect on RAW, then backfill, then validate. Confirmed in source and by `test_detect_fired_on_raw` spy |
| `orchestrator.resume_pipeline` Step E5 | `orchestrator.backfill_extracted` | Called inside `_run_stages` with `resolved_drops=backfill_skip` | WIRED | `backfill_extracted` at line 662; `resolved_drops` is the backfill-skip set (confirmed_dropped + client_supplied + prior terminals, NOT carried_forward) |
| `orchestrator.resume_pipeline` Round-2 classify | `validate.validate(resolved_drops=suppress_detection)` | `suppress_detection` (ALL answered) forwarded to `_run_stages` at line 498 → `validate` at line 676 | WIRED | N8 suppression in `validate.py:211`; str key match confirmed; `test_loop_guard_fires_exactly_once` proves no re-clarify |
| `eval/fixtures/18` `prior_extracted`/`prior_matches` | `eval/run_eval.py` `detect_field_regression` → `validate(raw_field_drops=)` | Fixture loader strips + deserializes at lines 162-171; three-phase path at lines 178-186 | WIRED | `eval --check` passes; fixture 18 produces `final_action=request_clarification` |
| `app/main.py` run_detail route | `app/templates/run_detail.html` clarified_fields_by_name | Route loads `clarified_fields` and builds `clarified_fields_by_name` for template context | WIRED | SUMMARY confirms `app/main.py` modified for run_detail route; template renders badges from `clarified_fields_by_name` |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `_run_stages` detect phase | `raw_drops` | `detect_field_regression(prior, extracted, prior_matches, matches)` — pure function over real Extracted objects | Yes — diffing actual snapshot vs raw reply | FLOWING |
| `_run_stages` backfill phase | `extracted` (backfilled) | `backfill_extracted(extracted, snapshot, prior_matches, matches, resolved_drops)` — fills from real snapshot JSONB | Yes — reads `pre_clarify_extracted` persisted at first clarify send | FLOWING |
| `resume_pipeline` Round-2 | `raw_extracted` | `extract(combined_email, roster, ...)` — real LLM call (mocked in tests) | Yes — scripted in tests; real LLM in production | FLOWING |
| `test_resume_pipeline.py` paystub assertions | `line_items` | `fake_repo.load_line_items(run_id)` → set by `_compute_line_items` which calls `calculate()` with backfilled `extracted` | Yes — calculate() is real pure function; fake_repo in-memory | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 15 MONEY-03 integration tests pass | `DATABASE_URL=postgresql://test/test uv run pytest tests/test_resume_pipeline.py -v` | 15 passed, 0 skipped, 0 failed in 0.62s | PASS |
| 26 other MONEY-03 unit/integration tests pass | `uv run pytest tests/test_cr01_classify_union.py tests/test_detect_field_regression.py tests/test_decide_field_regression.py tests/test_compose_email_field_regression.py -v` | 26 passed, 0 failed | PASS |
| Full suite still green | `DATABASE_URL=postgresql://test/test uv run pytest -q` | 506 passed, 17 skipped (ALLOW_DB_RESET/ALLOW_LIVE_LLM guards), 0 failed | PASS |
| Eval --check passes with fixtures 16/17/18 | `uv run python eval/run_eval.py --check` | exit 0; "no regression against committed summary.json" | PASS |
| Fixture 18 produces request_clarification | Python inline verify | `final_action=request_clarification`; `prior_extracted` and `prior_matches` keys present | PASS |

---

### Probe Execution

Step 7c: SKIPPED — no probe scripts declared in PLAN files; this phase does not use the `scripts/*/tests/probe-*.sh` convention.

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MONEY-03 | Plans 01-04 (07.5-01 through 07.5-04) | When a clarification reply drops or changes a money-affecting field the original submission stated, the system detects the regression, clarifies once, then carries the original value forward if still unaddressed (no infinite loop) | SATISFIED | `detect_field_regression` in `validate.py`; three-phase ordering in `_run_stages`; four-outcome classify-first in `resume_pipeline`; 15+26 passing tests; eval fixture 18 |

REQUIREMENTS.md traceability: MONEY-03 is listed as Phase 7.5 with status "Pending" in the requirements file (the file was not updated to "Complete" — this is a documentation trailing artifact, not a code deficiency; the implementation is verified above).

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `app/pipeline/validate.py` | 109 | `return []` | Info | Documented honest no-op: `prior_matches is None` guard — no prior matches means no regression possible. Not a stub; the function has 70 lines of real implementation below this guard. NOT a blocker. |

No `TBD`, `FIXME`, or `XXX` debt markers found in any phase-modified file.
No placeholder or stub implementations found in core pipeline code.

---

### Human Verification Required

None. All critical behaviors (three-phase ordering, loop guard, carry-forward, overpay guard, client_supplied, restated-name threading) are verified by hermetic integration tests with deterministic assertions on paystub line items, clarified_fields outcomes, run status, and outbound email counts.

The only items that would benefit from human testing are visual/UX:

1. **Provenance badges in run_detail.html** — the Jinja2 template logic is verified to contain the four badge branches (`carried-forward`, `client removed`, `client supplied`, `awaiting reply`). Visual rendering in a browser with a real run in field-regression state cannot be verified programmatically.
   - Why deferred: purely cosmetic; the badge text and branch conditions are code-verified. No money-correctness risk.

These are informational only and do not block phase completion — the money-path invariants are fully tested in code.

---

### Gaps Summary

No gaps. All 6 must-have truths are VERIFIED with direct code evidence and passing tests.

**CR-01 (overpay fix):** Present in `orchestrator.py:383-387` — classify lookup uses UNION of `current_matches_for_classify + prior_matches`. Covered by `tests/test_cr01_classify_union.py` (5 tests, all PASS).

**R2-2 (underpay fix):** Present in `orchestrator.py:480-490` — `prior_matches_for_backfill = list(snapshot_matches) + list(prior_matches)`. Covered by `test_restated_name_prior_matches_threading` Part B (PASSES: paystub OT=2).

**CR-02 (schema migration atomicity):** Correctly identified as FALSE POSITIVE in REVIEW.md — `bootstrap.py` applies `schema.sql` as a single `conn.execute()` + single `conn.commit()` in a non-autocommit connection; the DO $$ DROP+RE-ADD is atomic.

---

_Verified: 2026-06-28T12:00:00Z_
_Verifier: Claude (gsd-verifier)_
