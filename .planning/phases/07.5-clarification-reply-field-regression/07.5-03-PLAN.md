---
phase: 07.5-clarification-reply-field-regression
plan: "03"
type: execute
wave: 3
depends_on:
  - "07.5-01"
  - "07.5-02"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/models/contracts.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so new JSONB columns (pre_clarify_extracted, clarified_fields) AND the updated purpose CHECK constraint exist before integration tests run in Plan 04"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local shell env (not just .env) before the checkpoint"
        location: "local shell — run: export DATABASE_URL=<pooler connection string>"

must_haves:
  truths:
    - "ClarifiedFields Pydantic model exists in contracts.py with ConfigDict(extra='forbid') and a nested structure: {employee_id_str: {field: ClarifiedFieldOutcome}} where ClarifiedFieldOutcome = Literal['asked','carried_forward','confirmed_dropped','client_supplied'] — four outcomes: silence->carried_forward, explicit-zero->confirmed_dropped, positive-replacement->client_supplied (Finding 6 fix: 'carried_forward' now means ONLY that the value came from the snapshot, not from the client's reply)"
    - "schema.sql has pre_clarify_extracted JSONB and clarified_fields JSONB inline in payroll_runs CREATE TABLE and in the ADD COLUMN IF NOT EXISTS ALTER block"
    - "schema.sql has a DO $$ block that DROPs the old email_messages purpose CHECK constraint (by name from pg_constraint, matched defensively by table + contype + conname LIKE '%purpose%') and RE-ADDs with the expanded value list ('clarification','confirmation','clarification_field_regression') IN ONE TRANSACTION (D-7.5-03a atomic DROP+ADD)"
    - "app/db/repo.py has 4 new helpers: set_pre_clarify_extracted (IS NULL CAS, snapshot-once D-19), load_pre_clarify_extracted, set_clarified_fields (validates via ClarifiedFields model before serializing), load_clarified_fields (returns {} on NULL)"
    - "get_outbound_message_id() allowlist expanded to include 'clarification_field_regression'"
    - "_clarify() gains extracted: Extracted positional parameter and purpose: str = 'clarification' kwarg; all three hardcoded 'clarification' strings inside _clarify are replaced with the purpose variable; set_pre_clarify_extracted is called BEFORE EACH of the three set_status(AWAITING_REPLY) paths (N7 fix)"
    - "_run_stages now performs the backfill INLINE in the in-memory seam (between reconcile_names and validate) when prior= is not None: calls a pure backfill_extracted(extracted, snapshot, resolved_drops) -> Extracted' helper that mutates a copy, THEN passes the backfilled extracted to validate(); the carry-forward value exists in extracted BEFORE _compute_line_items runs — this is the R3-1 fix (Finding 1)"
    - "prior_matches IS THREADED to _run_stages on the Round-2 resume path: resume_pipeline builds prior_matches from _pre_reconciliation (already loaded in STEP A) by deserializing each dict to NameMatchResult, then passes prior_matches=prior_matches to _run_stages; _run_stages threads it to validate() which passes it to detect_field_regression — this is the R3-3 fix ensuring the employee_id-keyed diff fires on the real path (not just in unit tests)"
    - "resolved_drops IS THREADED to _run_stages on the Round-2 resume path: resume_pipeline builds _resolved_by_name (a set of (submitted_name, field) pairs where outcome is confirmed_dropped or client_supplied) from the pre-resume reconciliation + clarified dict, then passes resolved_drops=_resolved_by_name to _run_stages on Round-2; backfill_extracted honors this set so confirmed_dropped fields are NOT re-backfilled (D-14 honored on real path)"
    - "resume_pipeline Round-1 path: snapshot is None, calls _run_stages(prior=None, prior_matches=None, resolved_drops=None); if clarify_deferred=True, writes asked to clarified_fields DURABLY BEFORE calling _clarify (N2); uses purpose='clarification_field_regression'"
    - "resume_pipeline Round-2 path: snapshot is not None, calls _run_stages(prior=snapshot, prior_matches=prior_matches, resolved_drops=_resolved_by_name); the in-seam backfill in _run_stages ensures carried_forward values are in extracted BEFORE calc; Round-2 classifies outcomes AFTER _run_stages returns using the ALREADY-BACKFILLED extracted from _run_stages — but only for labeling clarified_fields, NOT for re-applying backfill"
    - "_run_stages returns _RunStagesResult(clarify_deferred: bool, matches: list, issues: list) — a minimal shape carrying only what resume_pipeline actually needs; NOT the extracted object (which is already persisted by _run_stages itself, and re-loading from DB avoids the stale-reference trap)"
    - "R3-2 FIX: clarification is deferred whenever ANY field_regression issue exists (not only when ALL issues are field_regression); the deferred _clarify call uses purpose='clarification_field_regression' and bundles BOTH normal asks AND field-regression asks in ONE email; get_outbound_message_id idempotency check uses purpose='clarification_field_regression' so a prior 'clarification' row does NOT suppress the new send"
    - "uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q exits 0"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "ClarifiedFields Pydantic model (typed outcome validator) with 4 outcomes: asked/carried_forward/confirmed_dropped/client_supplied"
      contains: "class ClarifiedFields"
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields columns + N4 DO $$ purpose CHECK migration"
      contains: "clarification_field_regression"
    - path: "app/db/repo.py"
      provides: "4 new JSONB helpers + allowlist expansion"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "inline backfill_extracted in _run_stages seam (R3-1 fix) + prior_matches threading to _run_stages + validate (R3-3 fix) + resolved_drops threading on Round-2 (D-14 fix) + _clarify extracted param + purpose kwarg + N7 snapshot + resume_pipeline Round-1/Round-2 block (N1/N2/N3) + R3-2 any-field_regression deferral"
      contains: "clarification_field_regression"
  key_links:
    - from: "app/pipeline/orchestrator.py:_run_stages (in-memory seam)"
      to: "app/pipeline/orchestrator.py:_compute_line_items"
      via: "backfill_extracted called BETWEEN reconcile_names and validate — the carry-forward value is in extracted BEFORE line_items are computed (R3-1 fix: backfill-before-calc ordering invariant)"
      pattern: "backfill_extracted"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 2, STEP A _pre_reconciliation)"
      to: "app/pipeline/orchestrator.py:_run_stages (prior_matches= kwarg)"
      via: "resume_pipeline deserializes _pre_reconciliation dicts to NameMatchResult list and passes as prior_matches=prior_matches; _run_stages threads it to validate() -> detect_field_regression, enabling employee_id-keyed diff on the real path (R3-3 fix)"
      pattern: "prior_matches"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 2, _resolved_by_name set)"
      to: "app/pipeline/orchestrator.py:_run_stages (resolved_drops= kwarg)"
      via: "resume_pipeline builds _resolved_by_name from pre-resume reconciliation + clarified dict; passes as resolved_drops=_resolved_by_name; backfill_extracted guards confirmed_dropped fields from re-backfill (D-14 honored on real path)"
      pattern: "resolved_drops"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 1)"
      to: "app/db/repo.py:set_clarified_fields"
      via: "writes asked BEFORE calling _clarify (N2 ordering, asked-before-send)"
      pattern: "set_clarified_fields"
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called BEFORE EACH of the 3 AWAITING_REPLY set_status calls; IS NULL guard = snapshot-once"
      pattern: "set_pre_clarify_extracted"
    - from: "app/models/contracts.py:ClarifiedFields"
      to: "app/db/repo.py:set_clarified_fields"
      via: "every write validates shape through ClarifiedFields before json.dumps — typed-on-write D-7.5-03b"
      pattern: "ClarifiedFields"
---

<objective>
Wire the MONEY-03 two-inbound state machine: schema DDL (two JSONB columns + N4 purpose CHECK migration), ClarifiedFields typed model (four outcomes), four repo helpers, _clarify snapshot + purpose kwarg, and the resume_pipeline Round-1/Round-2 detection block.

**Reviews addressed in this plan:**

- **Finding 1 (HIGH — R3-1, the load-bearing fix):** The backfill of carried-forward values now happens INLINE inside `_run_stages`, in the in-memory seam between `reconcile_names` and `validate`. A pure helper `backfill_extracted(extracted, snapshot, resolved_drops) -> Extracted` is called at the seam — it returns a new Extracted object with any silence-field values backfilled from the snapshot. This backfilled copy then flows into `validate()`, `decide()`, `_compute_line_items()`, and ultimately `repo.replace_line_items()`. The carried-forward value MUST exist in `extracted` BEFORE `_compute_line_items` runs. The Round-2 path passes `prior=snapshot` (NOT None) to `_run_stages` so the in-seam backfill fires. The old pattern (call `_run_stages(prior=None)`, then post-return classify and re-persist extracted) is eliminated — it was the R3-1 trap.

- **R3-3 BLOCKER FIX (prior_matches threading):** `detect_field_regression` is employee_id-keyed (Plan 02), but Plan 03 MUST thread `prior_matches` from the pre-resume reconciliation all the way to `_run_stages` → `validate()` → `detect_field_regression`. Without this, `detect_field_regression` receives `prior_matches=None` on every real resume call and returns `[]` (honest no-op per Plan 02) — the entire R3-3 fix is a unit test exercise, not a production fix. The pre-resume reconciliation (`_pre_reconciliation`) is already loaded in STEP A; this plan deserializes it to `list[NameMatchResult]` and passes it as `prior_matches=prior_matches`. The argument flow is: `_pre_reconciliation dicts → prior_matches list → _run_stages(prior_matches=prior_matches) → validate(prior_matches=prior_matches) → detect_field_regression(prior_matches=prior_matches)`.

- **WARNING FIX (resolved_drops threading — confirmed_dropped honored on real path):** Step E2 of resume_pipeline ALREADY BUILDS `_resolved_by_name` (the set of (submitted_name, field) pairs that are `confirmed_dropped` or `client_supplied`) BUT the old plan passed `resolved_drops=None` to `_run_stages` on Round-2. This plan passes `resolved_drops=_resolved_by_name` on Round-2 so `backfill_extracted`'s guard `(submitted_name, field) in (resolved_drops or set())` actually fires. The confirmed_dropped path is now honored on the real path (D-14), not just in unit tests.

- **Finding 3 (HIGH — R3-2 mixed-issue path):** Clarification is now deferred whenever ANY `field_regression` issue exists in issues (not only when ALL are field_regression). When deferred, a single email with `purpose="clarification_field_regression"` is composed that bundles BOTH the normal unresolved/missing asks AND the field-regression ask. The `get_outbound_message_id` idempotency check uses the new purpose value so a pre-existing plain `"clarification"` row does NOT suppress the field-regression send.

- **Finding 5 (MEDIUM — _RunStagesResult shape):** The return shape carries `clarify_deferred: bool`, `matches: list[NameMatchResult]`, and `issues: list[ValidationIssue]` — exactly what `resume_pipeline` needs for Round-1 outcome classification. The `extracted` object is NOT returned (it is already persisted by `_run_stages` itself; callers that need it reload from DB to avoid stale references).

- **Finding 6 (MEDIUM — `carried_forward` overloaded):** A fourth outcome `"client_supplied"` is added to `ClarifiedFieldOutcome` for the case where the client replied with a POSITIVE replacement value. `"carried_forward"` now means ONLY that the value came from the snapshot (the client was silent). Three distinct outcomes: `carried_forward` (silence → backfill from snapshot), `confirmed_dropped` (explicit zero/none), `client_supplied` (positive replacement value). All three are loop-terminal (no path back to `"asked"`).

- **Finding 9 (LOW — schema CHECK migration matcher):** The `pg_constraint` lookup uses both `contype = 'c'` (CHECK) and `conrelid = 'email_messages'::regclass` to narrow the search, making the `LIKE '%purpose%'` pattern less likely to hit a wrong constraint on schemas with similarly-named tables.

This plan closes:
- N4: schema.sql gets an explicit DO $$ block that DROPs the existing email_messages purpose CHECK and RE-ADDs it with 'clarification_field_regression' in ONE TRANSACTION (D-7.5-03a).
- N7: set_pre_clarify_extracted (IS NULL CAS) is called before each of the THREE AWAITING_REPLY set_status paths in _clarify (idempotency/snapshot-once D-19).
- N2: asked-before-send ordering — resume_pipeline writes clarified_fields "asked" entry DURABLY before triggering _clarify.
- R3-1: backfill_extracted runs INLINE in _run_stages seam BEFORE validate/decide/calc.
- R3-2: ANY field_regression issue defers clarification; mixed-issue email bundles all asks under "clarification_field_regression".
- R3-3: prior_matches loaded from pre-resume reconciliation (STEP A, already present) and threaded to _run_stages → validate → detect_field_regression on Round-2 path.
- D-14 (WARNING fix): resolved_drops=_resolved_by_name passed to _run_stages on Round-2 so confirmed_dropped fields are not re-backfilled.
- Single-_run_stages-call invariant (N1): resume_pipeline calls _run_stages exactly once.

Output: full two-inbound state machine wired; existing test suites green; schema applied to dev DB.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-01-SUMMARY.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-02-SUMMARY.md

@app/db/schema.sql
@app/db/repo.py
@app/models/contracts.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClarifiedFields model (4 outcomes) + schema DDL (N4 migration) + repo helpers + allowlist</name>
  <files>app/models/contracts.py, app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/models/contracts.py. Add ClarifiedFields AFTER FieldDrop and RawFieldDrop (both added in Plan 02). ClarifiedFields is the typed-on-write validator for the clarified_fields JSONB column (D-7.5-03b). Shape: a validated dict wrapper. The outcome Literal has FOUR values now (Finding 6 fix): "asked", "carried_forward", "confirmed_dropped", "client_supplied". All existing imports (UUID, Decimal, Literal, BaseModel, ConfigDict) should already be present — verify before adding.

  Read app/db/schema.sql in full. Key anchors:
  - Line 90: alias_candidates JSONB — add pre_clarify_extracted JSONB and clarified_fields JSONB on the next two lines (inline for fresh DBs)
  - Lines 105-108: ADD COLUMN IF NOT EXISTS block — add two lines for the new columns after line 108
  - Line 147: purpose inline CHECK — update to include 'clarification_field_regression'
  - Lines 164-165: purpose ALTER TABLE ADD COLUMN — update CHECK to include 'clarification_field_regression'
  - Lines 169-184: DO $$ uq_email_run_purpose block — THIS IS THE EXACT PATTERN to copy for the N4 DROP+RE-ADD block; add the N4 block BEFORE this existing block (i.e. after line 165 but before line 169)

  Read app/db/repo.py lines 85-100 (imports + RUN_COLS), 340-375 (claim_status CAS — the IS NULL RETURNING id pattern), 405-430 (persist_extracted — serialization with model_dump(mode="json")), 505-530 (set_alias_candidates — EXACT template for the new helpers), 659-686 (get_outbound_message_id — the allowlist to expand).

  PATTERNS.md §5 and §6 have the exact SQL and helper implementations. Critical:
  - RUN_COLS (repo.py:90-93) does NOT include alias_candidates. Follow the same pattern — do NOT add pre_clarify_extracted or clarified_fields to RUN_COLS.
  - All SQL uses %s placeholders and str(run_id) for UUID params. No f-string SQL.
  - JSONB serialization: Extracted objects use json.dumps(extracted.model_dump(mode="json")); plain dicts use json.dumps(the_dict); read-backs use json.loads(row[0]) if isinstance(row[0], str) else row[0].
  - set_pre_clarify_extracted uses the RETURNING id CAS pattern: `WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id` — atomic check-and-write, snapshot-once (D-19).
  - set_clarified_fields MUST validate through ClarifiedFields model before serializing (D-7.5-03b).
  </read_first>

  <action>
  Step 1 — Add ClarifiedFields model to app/models/contracts.py:

  After FieldDrop and RawFieldDrop (added in Plan 02), add:

  - Type alias: `ClarifiedFieldOutcome = Literal["asked", "carried_forward", "confirmed_dropped", "client_supplied"]`
    Four outcomes (Finding 6 fix):
    - "asked": the field-regression question was sent; awaiting client reply
    - "carried_forward": the client was SILENT; the value came from the pre-clarify snapshot (not from the client's reply)
    - "confirmed_dropped": the client explicitly zeroed or said "none" — honor the removal
    - "client_supplied": the client replied with a POSITIVE replacement value — the reply itself is the source

  - Class ClarifiedFields(BaseModel):
    - model_config = ConfigDict(extra="forbid")
    - outcomes: dict[str, dict[str, ClarifiedFieldOutcome]]
    - Docstring: "Typed validator for the clarified_fields JSONB column (D-7.5-03b). Shape: {employee_id_str: {field_name: outcome}}. Validate on every write — a mislabeled carried_forward->confirmed_dropped silently underpays. Four outcomes: asked (awaiting reply), carried_forward (client silent; value from snapshot), confirmed_dropped (explicit zero/none from client), client_supplied (positive replacement from client — Finding 6 fix)."
    - Class method `from_dict(cls, d: dict) -> ClarifiedFields`: validates `outcomes=d`
    - Method `to_dict(self) -> dict`: returns `self.outcomes`

  Add `Literal` to the existing import from typing if not present; it may already be there.

  Step 2 — Update app/db/schema.sql:

  a. In CREATE TABLE IF NOT EXISTS payroll_runs, after `alias_candidates JSONB,` (~line 90), add:
     `    pre_clarify_extracted JSONB,    -- D-19 MONEY-03: snapshot at awaiting_reply (IS NULL write-once guard)`
     `    clarified_fields      JSONB,    -- D-13 MONEY-03: {employee_id: {field: outcome}} field-regression outcomes`

  b. In the ADD COLUMN IF NOT EXISTS ALTER block (~lines 105-108), add after the existing lines:
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03`
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields      JSONB;  -- D-13 MONEY-03`

  c. At line 147, update inline email_messages purpose CHECK to include 'clarification_field_regression'.

  d. At lines 164-165, update the ALTER TABLE ADD COLUMN purpose CHECK to include 'clarification_field_regression'.

  e. N4 DO $$ migration block — add BEFORE the existing uq_email_run_purpose block (after line 165, before line 169). Uses D-7.5-03a atomic DROP+ADD. Finding 9 fix: the pg_constraint lookup is narrowed by BOTH contype = 'c' AND conrelid = 'email_messages'::regclass before applying the LIKE pattern:

     DO $$
     DECLARE
         _con_name TEXT;
     BEGIN
         SELECT conname INTO _con_name
         FROM pg_constraint
         WHERE contype = 'c'
           AND conrelid = 'email_messages'::regclass
           AND conname LIKE '%purpose%';
         IF _con_name IS NOT NULL THEN
             EXECUTE 'ALTER TABLE email_messages DROP CONSTRAINT ' || quote_ident(_con_name);
         END IF;
         IF NOT EXISTS (
             SELECT 1 FROM pg_constraint
             WHERE conname = 'email_messages_purpose_check'
               AND conrelid = 'email_messages'::regclass
         ) THEN
             ALTER TABLE email_messages ADD CONSTRAINT email_messages_purpose_check
                 CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'));
         END IF;
     END;
     $$;

  Step 3 — Add four repo helpers to app/db/repo.py after set_alias_candidates (~line 524):
  Follow the patterns from PATTERNS.md §5/§6 exactly. Use _conn_ctx / _nulltx, %s placeholders, str(run_id).

  a. set_pre_clarify_extracted(run_id, extracted, conn=None) -> bool:
     SQL: UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id
     Returns True if written (first write), False if already set.

  b. load_pre_clarify_extracted(run_id, conn=None) -> Extracted | None:
     SQL: SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s
     Returns None on NULL. Deserializes via Extracted.model_validate.

  c. set_clarified_fields(run_id, clarified: dict, conn=None) -> None:
     FIRST validate: ClarifiedFields(outcomes=clarified) — raises ValidationError if shape wrong (D-7.5-03b).
     SQL: UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s
     Docstring must note: "D-7.5-03b: shape validated through ClarifiedFields before persisting — a mislabeled carried_forward->confirmed_dropped silently underpays. Four outcomes: asked, carried_forward, confirmed_dropped, client_supplied."

  d. load_clarified_fields(run_id, conn=None) -> dict:
     SQL: SELECT clarified_fields FROM payroll_runs WHERE id = %s
     Returns {} on NULL. Deserializes via json.loads.

  Step 4 — Expand get_outbound_message_id allowlist (~line 670):
  Change: `if purpose not in ("clarification", "confirmation"):`
  To: `if purpose not in ("clarification", "confirmation", "clarification_field_regression"):`

  Step 5 — Verify imports (dry run, no DB needed yet):
  uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; from app.models.contracts import ClarifiedFields; print('ALL OK')"
  </action>

  <verify>
    <automated>uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; from app.models.contracts import ClarifiedFields; print('imports OK')" 2>&1</automated>
  </verify>

  <acceptance_criteria>
  - `grep "class ClarifiedFields" app/models/contracts.py` exits 0
  - `grep "client_supplied" app/models/contracts.py` exits 0 (fourth outcome — Finding 6 fix)
  - `grep "pre_clarify_extracted JSONB" app/db/schema.sql` exits 0 (inline column)
  - `grep "clarified_fields.*JSONB" app/db/schema.sql` exits 0
  - `grep -c "clarification_field_regression" app/db/schema.sql` returns 3+ (inline CHECK + ALTER + DO $$ migration)
  - `grep "DO \$\$" app/db/schema.sql | wc -l` returns 2 (the new N4 block AND the existing uq_email_run_purpose block)
  - `grep "contype = 'c'" app/db/schema.sql` exits 0 (Finding 9 — narrower pg_constraint lookup)
  - `grep "def set_pre_clarify_extracted\|def load_pre_clarify_extracted\|def set_clarified_fields\|def load_clarified_fields" app/db/repo.py` exits 0 (all four helpers)
  - `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` exits 0 (IS NULL CAS guard)
  - `grep "clarification_field_regression" app/db/repo.py` exits 0 (allowlist expansion)
  - `grep "ClarifiedFields" app/db/repo.py` exits 0 (typed-on-write validation in set_clarified_fields)
  - `uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.models.contracts import ClarifiedFields; print('OK')"` exits 0
  </acceptance_criteria>

  <done>ClarifiedFields model with 4 outcomes exists. Four repo helpers importable. Schema updated with new columns + N4 migration block (Finding 9 defensive matcher). Allowlist expanded. All imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: backfill_extracted inline seam + prior_matches/resolved_drops threading + _run_stages return + _clarify N7 + resume_pipeline Round-1/Round-2 (R3-1/R3-2/R3-3/D-14/N1/N2 fixes)</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py in full in ONE PASS. Extract every anchor needed:

  _run_stages (lines 260-290 after Plan 01):
  - Line 260: signature `def _run_stages(run_id, email, roster, *, llm, prior=None, prior_matches=None, resolved_drops=None) -> None` — note: Plan 01 added all THREE kwargs including prior_matches
  - Lines 269-275: four pure stages: extract(line 269), reconcile_names(line 272), validate(line 273 — now has prior=, prior_matches=, resolved_drops= kwargs per Plan 01), decide(line 275)
  - Lines 278-289: persist block + branch — MUST remain functionally identical for the process path
  - The clarify branch at line 289: `_clarify(run_id, email, decision, roster, llm=llm)` — gains extracted + purpose args

  _clarify (lines 292-451):
  - Line 292: current signature `def _clarify(run_id, email, decision, roster, *, llm) -> None`
  - Line 318: `repo.get_outbound_message_id(run_id, purpose="clarification")` — gains purpose= variable
  - Line 325: first `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 1 (idempotency early-return)
  - Line 435: `purpose="clarification"` inside repo.insert_email_message — gains variable
  - Line 438: second `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 2 (record_only)
  - Line 448: `purpose="clarification"` inside gateway.send_outbound — gains variable
  - Line 451: third `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 3 (live gateway)

  resume_pipeline (lines 94-241):
  - Line 124: claim_status CAS — DO NOT TOUCH (D-22)
  - Lines 153-162: STEP A pre-resume alias capture — MUST run FIRST before any _run_stages call. CRITICAL: this block ALREADY loads _pre_reconciliation (line 155), which is the raw list[dict] form of the pre-resume reconciliation. This is the source for prior_matches deserialization.
  - Line 164-165: STEP B current single `_run_stages(run_id, combined_email, roster, llm=llm)` call — REPLACE with detection block
  - Lines 167-234: STEP C/D alias-diff — reads from DB via load_run after _run_stages writes reconciliation
  - Lines 235-241: except boundary — ALL new code stays inside here

  STEP A _pre_reconciliation (line 155, already loaded):
  `_pre_reconciliation = (pre_run_data.get("reconciliation") or []) if pre_run_data else []`
  This is a list of dicts with keys: submitted_name, matched_employee_id, resolved, field_regression_source (optional). Each dict corresponds to a NameMatchResult. Deserialize to NameMatchResult objects for prior_matches:
  `from app.models.roster import NameMatchResult`
  `prior_matches = [NameMatchResult.model_validate(m) for m in _pre_reconciliation if isinstance(m, dict)]`
  This is the R3-3 fix: prior_matches is now a real list[NameMatchResult] built from the pre-resume reconciliation that was persisted by the previous _run_stages call. Pass it to _run_stages as prior_matches=prior_matches on Round-2.

  Imports block (lines 35-57): confirm detect_field_regression is importable from app.pipeline.validate (add to imports). Add dataclass from dataclasses. Add NameMatchResult import from app.models.roster.

  CRITICAL DESIGN (R3-1 FIX — the north star): the carry-forward value MUST be in `extracted` BEFORE `_compute_line_items` runs (line 284). The only correct place to inject it is INLINE in `_run_stages`, between `reconcile_names` (line 272) and `validate` (line 273). Plan 01 established the `prior=` kwarg slot for this exact purpose.
  </read_first>

  <action>
  Change A — Add backfill_extracted pure helper near top of orchestrator.py (after imports, before run_pipeline):

  def backfill_extracted(extracted, snapshot, resolved_drops):

  This is a pure function with no DB calls. It accepts:
  - extracted: Extracted — the current (resumed) extraction (will NOT be mutated)
  - snapshot: Extracted — the pre-clarify snapshot (loaded by resume_pipeline)
  - resolved_drops: set — the set of (submitted_name, field) pairs already confirmed_dropped or client_supplied; those fields are NOT backfilled

  Returns a new Extracted object (copy with silence fields filled from snapshot).

  Body:
  1. Build name_to_snapshot_emp: {submitted_name: ExtractedEmployee} from snapshot.employees (last-wins)
  2. Build a new employees list by iterating extracted.employees:
     - For each emp in extracted.employees:
       - Find snap_emp = name_to_snapshot_emp.get(emp.submitted_name)
       - If snap_emp is None: keep emp as-is (no snapshot match — cannot backfill)
       - Build a dict of the emp's field values, then for each field in _HOURS_FIELDS:
         - current_val = getattr(emp, field)
         - If not _is_paid(current_val) (silence or explicit zero):
           - snap_val = getattr(snap_emp, field)
           - If _is_paid(snap_val) (snapshot had a value):
             - Check if (emp.submitted_name, field) is in (resolved_drops or set()) — if so, do NOT backfill (it was confirmed_dropped or client_supplied)
             - Otherwise: set field in the new emp dict to snap_val (backfill from snapshot — silence path)
       - Append a new ExtractedEmployee built from the updated dict
  3. Return a new Extracted with the updated employees list and the same pay_period_start/pay_period_end/run_id

  Note: resolved_drops is keyed by (submitted_name, field) — consistent with backfill_extracted's name-based resolution. The _run_stages caller (resume_pipeline) builds resolved_drops using submitted_name keys derived from the pre-resume reconciliation + clarified dict.

  Change B — Add _RunStagesResult lightweight container (after imports, before run_pipeline):

  from dataclasses import dataclass

  @dataclass
  class _RunStagesResult:
      """Minimal return value from _run_stages (MONEY-03 D-7.5-02).
      Finding 5 fix: shape carries exactly what resume_pipeline needs — clarify_deferred,
      matches, and issues. Does NOT carry extracted (already persisted by _run_stages;
      callers reload from DB to avoid stale references and the R3-1 trap).
      clarify_deferred=True: ANY field_regression issue exists; _clarify deferred so
      resume_pipeline can write 'asked' BEFORE the send (N2 ordering).
      """
      clarify_deferred: bool = False
      matches: object = None    # list[NameMatchResult]
      issues: object = None     # list[ValidationIssue]

  Change C — Update _run_stages (R3-1 fix + _RunStagesResult return):

  1. Change return type annotation to `-> _RunStagesResult`.

  2. INLINE BACKFILL (R3-1 fix): After `matches = reconcile_names(...)` (line 272) and BEFORE `issues = validate(...)` (line 273), add:
     `if prior is not None:`
     `    extracted = backfill_extracted(extracted, prior, resolved_drops)`
     This is the in-memory seam established by Plan 01. The backfilled `extracted` then flows into validate(), decide(), _compute_line_items(), and repo.replace_line_items() in the correct order.

  3. The `validate(extracted, roster, matches, prior=prior, prior_matches=prior_matches, resolved_drops=resolved_drops)` call (already updated by Plan 01 to include all three kwargs) now passes the BACKFILLED extracted as the first arg and the original prior as the prior= kwarg. The prior_matches= kwarg reaches detect_field_regression — this is the R3-3 fix: on Round-2, prior_matches is a real list[NameMatchResult] (not None), so the employee_id-keyed diff fires.

  4. In the else branch (request_clarification, line ~289):
     - R3-2 fix (Finding 3): defer whenever ANY field_regression issue exists (not only all):
       `has_field_regression = any(i.issue_type == "field_regression" for i in issues)`
     - If has_field_regression is True: set `clarify_deferred = True`. Do NOT call _clarify here. The caller (resume_pipeline) will write asked then call _clarify with the field_regression purpose.
     - If has_field_regression is False: set `clarify_deferred = False`. Call `_clarify(run_id, email, decision, roster, extracted, llm=llm, purpose="clarification")` with the extracted positional arg (Change D).

  5. On the process branch: `clarify_deferred = False`.

  6. At the end: `return _RunStagesResult(clarify_deferred=clarify_deferred, matches=matches, issues=issues)`

  7. Update run_pipeline call site (line ~91): `_ = _run_stages(run_id, email, roster, llm=llm)` (discard return — behavior unchanged for run_pipeline).

  Change D — Update _clarify: add extracted positional param + purpose kwarg + N7 snapshot on 3 paths:

  1. New signature: `def _clarify(run_id, email, decision, roster, extracted, *, llm, purpose="clarification") -> None:`

  2. Replace ALL THREE hardcoded "clarification" strings with the `purpose` variable:
     - Line 318: `repo.get_outbound_message_id(run_id, purpose=purpose)`
     - Line 435: `purpose=purpose` in repo.insert_email_message call
     - Line 448: `purpose=purpose` in gateway.send_outbound call

  3. N7 fix — add `repo.set_pre_clarify_extracted(run_id, extracted)` IMMEDIATELY BEFORE each of the THREE set_status(AWAITING_REPLY) calls:
     - Before line 325 (PATH 1 idempotency early-return)
     - Before line 438 (PATH 2 record_only)
     - Before line 451 (PATH 3 live gateway)
  The IS NULL guard in SQL makes all three calls idempotent — only the first write succeeds (snapshot-once D-19).

  Change E — Add Round-1/Round-2 detection block in resume_pipeline (R3-1/R3-2/R3-3/D-14/N1/N2 fixes):

  STEP A (lines 153-162) remains UNCHANGED and runs FIRST. After STEP A completes, _pre_reconciliation is available as a raw list[dict].

  Replace old STEP B (lines 164-165, the bare `_run_stages(...)` call) with the following detection block. All code stays inside the existing except boundary (lines 235-241).

  Step E0: Deserialize prior_matches from _pre_reconciliation (R3-3 fix):
    from app.models.roster import NameMatchResult
    prior_matches: list[NameMatchResult] = [
        NameMatchResult.model_validate(m) for m in _pre_reconciliation if isinstance(m, dict)
    ]
    # Empty list on first-ever resume (no prior reconciliation); None would trigger the honest no-op,
    # so use [] here — detect_field_regression with empty prior_matches produces no drops, which is correct
    # for a first run with no prior reconciliation data.

  Step E1: load snapshot and clarified state:
    snapshot = repo.load_pre_clarify_extracted(run_id)    # None on first resume
    clarified = repo.load_clarified_fields(run_id)          # {} on first resume

  Step E2: build _resolved_by_name from clarified for resolved_drops kwarg (D-14 WARNING fix):
    This set tells backfill_extracted which (submitted_name, field) pairs NOT to backfill — confirmed_dropped
    or client_supplied fields must be honored, not overwritten with the snapshot value.

    Build a reverse lookup from employee_id_str to submitted_name using _pre_reconciliation:
    id_to_submitted_name: dict[str, str] = {}
    for m in _pre_reconciliation:
        if isinstance(m, dict) and m.get("matched_employee_id") and m.get("submitted_name"):
            id_to_submitted_name[str(m["matched_employee_id"])] = m["submitted_name"]

    Build the _resolved_by_name set:
    _resolved_by_name: set[tuple[str, str]] = set()
    for emp_id_str, field_outcomes in clarified.items():
        submitted_name = id_to_submitted_name.get(emp_id_str)
        if submitted_name is None:
            continue  # no reverse lookup available; cannot key by name
        for field, outcome in field_outcomes.items():
            if outcome in ("confirmed_dropped", "client_supplied"):
                _resolved_by_name.add((submitted_name, field))

    This set is passed as resolved_drops=_resolved_by_name on Round-2 so backfill_extracted
    does NOT overwrite a field the client explicitly dropped or replaced.

  Step E3: determine which round this is:
    is_round_2 = bool(clarified)  # any clarified_fields entries = at least Round 2

  Step E4: Round-1 path (no clarified entries — first resume or non-field-regression resume):
    If NOT is_round_2:
      stage = _run_stages(run_id, combined_email, roster, llm=llm, prior=snapshot, prior_matches=prior_matches, resolved_drops=None)
      # prior_matches=prior_matches: threads the pre-resume reconciliation to detect_field_regression (R3-3)
      # resolved_drops=None: no confirmed_dropped fields yet on Round-1
      # prior=snapshot: may be None on first resume; backfill_extracted no-ops when snapshot is None

      If stage.clarify_deferred is True (ANY field_regression issue; R3-2 fix):
        - Build asked entries: reload the run to get fresh matches from DB after _run_stages wrote reconciliation
          post_run = repo.load_run(run_id)
          name_to_id = {m["submitted_name"]: m["matched_employee_id"] for m in (post_run.get("reconciliation") or []) if m.get("matched_employee_id")}
        - For each issue in stage.issues where issue.issue_type == "field_regression":
          - Parse submitted_name from issue.field (issue.field = "{submitted_name}.{field_name}"; use rsplit(".", 1) for last-dot split)
          - current_emp_id = name_to_id.get(submitted_name)
          - If current_emp_id: clarified.setdefault(str(current_emp_id), {})[field_name] = "asked"
        - Write asked durably BEFORE send (N2):
          repo.set_clarified_fields(run_id, clarified)
        - THEN call _clarify with purpose="clarification_field_regression" (R3-2: bundles all asks — decision already has all gate_reasons including normal ones):
          Load the decision and extracted from DB (already persisted by _run_stages):
          run_row = repo.load_run(run_id)
          from app.models.contracts import Extracted
          from app.pipeline.decide import decide as _decide
          persisted_decision = Decision.model_validate(run_row["decision"]) if run_row and run_row.get("decision") else None
          persisted_extracted = Extracted.model_validate(run_row["extracted_data"]) if run_row and run_row.get("extracted_data") else None
          _clarify(run_id, combined_email, persisted_decision, roster, persisted_extracted, llm=llm, purpose="clarification_field_regression")
        - RETURN (do not continue to alias-diff; the run is at AWAITING_REPLY)
      If stage.clarify_deferred is False: fall through to STEP C/D alias-diff (normal path)

  Step E5: Round-2 path (clarified entries present — client answered a field-regression clarification):
    If is_round_2:
      stage = _run_stages(run_id, combined_email, roster, llm=llm, prior=snapshot, prior_matches=prior_matches, resolved_drops=_resolved_by_name)
      # prior=snapshot: in-seam backfill fires for silence fields (R3-1 fix)
      # prior_matches=prior_matches: employee_id-keyed diff in detect_field_regression fires (R3-3 fix)
      # resolved_drops=_resolved_by_name: confirmed_dropped fields are NOT re-backfilled (D-14 WARNING fix)
      #
      # ARGUMENT FLOW PROOF (R3-3):
      # _pre_reconciliation (STEP A) -> prior_matches (Step E0) -> _run_stages(prior_matches=prior_matches)
      # -> validate(prior_matches=prior_matches) [Plan 01 threads it] -> detect_field_regression(prior_matches)
      # -> employee_id-keyed diff fires (not the honest no-op []). detect_field_regression receives a
      # non-None prior_matches on every Round-2 call.
      #
      # ARGUMENT FLOW PROOF (D-14 WARNING):
      # clarified dict + id_to_submitted_name -> _resolved_by_name set -> _run_stages(resolved_drops=_resolved_by_name)
      # -> backfill_extracted(resolved_drops=_resolved_by_name) -> (submitted_name, field) in resolved_drops
      # -> confirmed_dropped fields NOT overwritten with snapshot value.

      VERIFY the ordering: backfill_extracted runs at line 272.5 (between reconcile and validate), BEFORE _compute_line_items at line 284. This is the R3-1 guarantee.

      After _run_stages returns (the run is at AWAITING_APPROVAL or AWAITING_REPLY):
      - N3: classify outcomes from clarified dict using the ALREADY-BACKFILLED data (load from DB):
        post_run = repo.load_run(run_id)
        name_to_id = {m["submitted_name"]: m["matched_employee_id"] for m in (post_run.get("reconciliation") or []) if m.get("matched_employee_id")}
        For each emp_id_str in clarified:
          For each field, outcome in clarified[emp_id_str].items():
            If outcome == "asked":  # reclassify from "asked" to terminal outcome
              - Find submitted_name with this employee_id in name_to_id (reverse lookup)
              - Load extracted_data from post_run; find the emp's field value
              - raw_value = the field value in the (already backfilled) extracted
              - If raw_value is not None and raw_value > 0:
                - If snapshot is not None:
                    snap_emp = find emp in snapshot by name; snap_val = getattr(snap_emp, field)
                    If raw_value == snap_val: outcome = "carried_forward" (silence; value from snapshot)
                    Else: outcome = "client_supplied" (positive replacement from client — Finding 6)
                  Else: outcome = "client_supplied"
              - Elif raw_value == Decimal("0") or raw_value is None: outcome = "confirmed_dropped"
              clarified[emp_id_str][field] = outcome
      - Persist final outcomes: repo.set_clarified_fields(run_id, clarified)
      Fall through to STEP C/D alias-diff.

  STEP C/D alias-diff (lines 167-234) remains as today — reading from DB via load_run after _run_stages has written reconciliation. No change needed here.

  Add imports at the top of orchestrator.py:
  - `from app.models.roster import NameMatchResult` (for prior_matches deserialization)
  - `from app.pipeline.validate import _is_paid` (for backfill_extracted helper; detect_field_regression is called via validate, not directly in orchestrator)
  - `from decimal import Decimal`
  - `from dataclasses import dataclass`
  - `from app.models.contracts import Extracted, ExtractedEmployee` (for backfill_extracted type hints; add if not already imported)

  After implementing, run:
  uv run python -c "from app.pipeline.orchestrator import _run_stages, resume_pipeline, _clarify, _RunStagesResult, backfill_extracted; print('OK')"
  uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import _run_stages, resume_pipeline, _clarify, _RunStagesResult, backfill_extracted; print('OK')" && uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "def backfill_extracted" app/pipeline/orchestrator.py` exits 0 (R3-1 fix: the in-seam pure helper)
  - `grep "extracted = backfill_extracted" app/pipeline/orchestrator.py` exits 0 (inline call BEFORE validate in _run_stages)
  - Ordering check (the critical R3-1 invariant): in _run_stages, `backfill_extracted` is called BEFORE `validate(` AND BEFORE `_compute_line_items` — verify with:
    `python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print([n.lineno for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(getattr(n,'func',None),'id',None) in ('backfill_extracted','validate','_compute_line_items')]) for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='_run_stages']"` — backfill_extracted line < validate line < _compute_line_items line
  - `grep "prior_matches" app/pipeline/orchestrator.py` exits 0 — prior_matches appears in _run_stages signature, the Round-2 _run_stages call, and the Round-1 _run_stages call (R3-3 threading fix — not just in Plan 01's signature, but in actual call sites)
  - `grep "prior_matches=prior_matches" app/pipeline/orchestrator.py` exits 0 — the argument is threaded at the call site, not defaulted to None (this is the key grep that catches the R3-3 bug)
  - `grep "_resolved_by_name" app/pipeline/orchestrator.py` exits 0 — the set is built from clarified + pre-resume reconciliation
  - `grep "resolved_drops=_resolved_by_name" app/pipeline/orchestrator.py` exits 0 — the set is PASSED to _run_stages on Round-2 (WARNING fix: not resolved_drops=None)
  - `grep "def _clarify" app/pipeline/orchestrator.py` shows `extracted` positional param and `purpose="clarification"` default kwarg
  - `grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py` returns 3+ (one before each AWAITING_REPLY path in _clarify — N7)
  - `grep "has_field_regression\|any.*field_regression" app/pipeline/orchestrator.py` exits 0 (R3-2 fix: any-not-all deferral logic)
  - `grep "clarification_field_regression" app/pipeline/orchestrator.py` exits 0 (the Round-1 _clarify call with purpose=)
  - `grep "set_clarified_fields" app/pipeline/orchestrator.py` exits 0 (N2 asked-before-send write in Round-1 path)
  - `grep "client_supplied" app/pipeline/orchestrator.py` exits 0 (Finding 6: positive-replacement outcome classification in Round-2)
  - `grep "_RunStagesResult" app/pipeline/orchestrator.py` exits 0 (Finding 5: correct return shape)
  - AST check (N1 single-call invariant): `python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(f'_run_stages calls: {len([n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(n.func,\"id\",None)==\"_run_stages\"])}') for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='resume_pipeline']"` prints `_run_stages calls: 1`
  - `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` exits 0
  - `uv run pytest -q` full suite exits 0
  </acceptance_criteria>

  <done>backfill_extracted pure helper exists and is called INLINE in _run_stages BEFORE validate (R3-1 fix: carry-forward value in extracted before calc). prior_matches deserialized from _pre_reconciliation and passed as prior_matches=prior_matches to _run_stages on both Round-1 and Round-2 (R3-3 fix: detect_field_regression receives non-None prior_matches on real resume path). _resolved_by_name set built from clarified+reconciliation and passed as resolved_drops=_resolved_by_name on Round-2 (D-14 WARNING fix: confirmed_dropped fields not re-backfilled). _clarify has extracted param, purpose kwarg, set_pre_clarify_extracted on all 3 AWAITING_REPLY paths (N7). _run_stages returns _RunStagesResult(clarify_deferred, matches, issues). resume_pipeline has Round-1/Round-2 block with single _run_stages call (N1), asked-before-send (N2), any-field_regression deferral (R3-2 fix), client_supplied outcome classification (Finding 6). Existing test suites green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1: ClarifiedFields model (4 outcomes: asked/carried_forward/confirmed_dropped/client_supplied — Finding 6 fix), schema DDL (two new JSONB columns + N4 purpose CHECK DO $$ migration with defensive pg_constraint lookup — Finding 9 fix), four repo helpers, allowlist expansion.
  Task 2: backfill_extracted pure helper called inline in _run_stages BEFORE validate (R3-1 fix — the load-bearing ordering invariant). prior_matches deserialized from _pre_reconciliation (STEP A) and threaded to _run_stages → validate → detect_field_regression on both Round-1 and Round-2 (R3-3 fix — employee_id-keyed diff fires on real path, not just unit tests). _resolved_by_name set built and passed as resolved_drops=_resolved_by_name on Round-2 (D-14 WARNING fix — confirmed_dropped fields not re-backfilled on real path). _clarify gains extracted param + purpose kwarg + N7 snapshot on 3 paths. _run_stages returns _RunStagesResult(clarify_deferred, matches, issues). resume_pipeline Round-1/Round-2 detection block: any-field_regression deferral (R3-2 fix), asked-before-send (N2), single _run_stages call (N1).
  </what-built>
  <how-to-verify>
  Requires DATABASE_URL set in shell env (Supavisor transaction mode port 6543).

  1. Verify imports:
     uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.models.contracts import ClarifiedFields; from app.pipeline.orchestrator import _clarify, resume_pipeline, backfill_extracted, _RunStagesResult; print('ALL IMPORTS OK')"

  2. Apply schema to dev DB:
     uv run python -m app.db.bootstrap
     Verify new columns exist:
     uv run python -c "
     import psycopg, os
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\")
     cols = [r[0] for r in cur.fetchall()]
     assert 'pre_clarify_extracted' in cols and 'clarified_fields' in cols, f'MISSING: {cols}'
     cur.execute(\"SELECT check_clause FROM information_schema.check_constraints WHERE constraint_name LIKE '%purpose%'\")
     rows = cur.fetchall()
     assert any('clarification_field_regression' in str(r) for r in rows), f'N4 not applied: {rows}'
     print('Schema PASSED')
     conn.close()
     "

  3. Verify R3-1 ordering invariant (the load-bearing fix):
     python3 -c "
     import ast
     src = open('app/pipeline/orchestrator.py').read()
     tree = ast.parse(src)
     for fn in ast.walk(tree):
         if isinstance(fn, ast.FunctionDef) and fn.name == '_run_stages':
             calls = [(n.lineno, getattr(getattr(n, 'func', None), 'id', None)) for n in ast.walk(fn) if isinstance(n, ast.Call) and getattr(getattr(n, 'func', None), 'id', None) in ('backfill_extracted', 'validate', '_compute_line_items')]
             calls.sort()
             print('Call order in _run_stages:', calls)
             names = [c[1] for c in calls]
             assert names.index('backfill_extracted') < names.index('validate'), 'R3-1 VIOLATED: backfill after validate'
             assert names.index('backfill_extracted') < names.index('_compute_line_items'), 'R3-1 VIOLATED: backfill after calc'
             print('R3-1 ORDERING PASSED')
     "

  4. Verify R3-3 threading (prior_matches actually reaches the call sites — not defaulted to None):
     grep "prior_matches=prior_matches" app/pipeline/orchestrator.py
     # Must show the kwarg being PASSED at the _run_stages call sites (not just declared in the signature)
     grep -c "prior_matches=prior_matches" app/pipeline/orchestrator.py
     # Should print 2 (Round-1 call and Round-2 call in resume_pipeline)

  5. Verify D-14 WARNING fix (resolved_drops=_resolved_by_name, NOT resolved_drops=None, on Round-2):
     grep "resolved_drops=_resolved_by_name" app/pipeline/orchestrator.py
     # Must show the set being PASSED on Round-2, not None
     grep "_resolved_by_name" app/pipeline/orchestrator.py
     # Must show the set being BUILT from clarified + pre-resume reconciliation

  6. Verify N1 (single _run_stages call in resume_pipeline):
     python3 -c "
     import ast
     src = open('app/pipeline/orchestrator.py').read()
     tree = ast.parse(src)
     for fn in ast.walk(tree):
         if isinstance(fn, ast.FunctionDef) and fn.name == 'resume_pipeline':
             calls = [n for n in ast.walk(fn) if isinstance(n, ast.Call) and getattr(getattr(n, 'func', None), 'id', None) == '_run_stages']
             print(f'_run_stages calls in resume_pipeline: {len(calls)}')
             assert len(calls) == 1, f'N1 VIOLATED: {len(calls)}'
             print('N1 PASSED')
     "

  7. Verify R3-2 (any-not-all field_regression deferral):
     grep "has_field_regression\|any.*field_regression" app/pipeline/orchestrator.py
     Should show the any() check, NOT an all() check.

  8. Verify N7 (snapshot on 3 paths):
     grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py
     (should print 3 or more)

  9. Verify N4 insert test (rolled back):
     uv run python -c "
     import psycopg, os, uuid
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     cur.execute(\"INSERT INTO email_messages (id, run_id, direction, message_id, purpose) VALUES (%s, NULL, 'outbound', %s, 'clarification_field_regression')\", (str(uuid.uuid4()), '<n4-test@test.local>'))
     conn.rollback()
     print('N4 INSERT PASSED (rolled back)')
     conn.close()
     "

  10. Verify Finding 6 (client_supplied outcome):
     grep "client_supplied" app/pipeline/orchestrator.py app/models/contracts.py
     Should appear in both files.

  11. Full test suite:
     uv run pytest -q
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass: R3-1 ordering (backfill before validate before calc), R3-3 threading (`prior_matches=prior_matches` at call sites — not defaulted), D-14 WARNING fix (`resolved_drops=_resolved_by_name` on Round-2), N1 AST (1 call), R3-2 any() not all(), N7 grep 3+, N4 insert passes (rolled back), schema columns exist. Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clarified_fields JSONB writes | Written via json.dumps with %s placeholders through ClarifiedFields validator (4 outcomes) — no f-string SQL injection |
| pre_clarify_extracted IS NULL guard | Atomic check-and-write in SQL — no Python read-then-write race (snapshot-once D-19) |
| email_messages.purpose CHECK | DB-enforced allowlist now includes 'clarification_field_regression'; unexpected purpose raises constraint violation at INSERT |
| N4 DO $$ migration block | Runs in a single transaction — failure cannot leave the constraint dropped-but-not-re-added (D-7.5-03a); pg_constraint lookup narrowed by contype='c' AND conrelid (Finding 9) |
| backfill_extracted in-seam helper | Pure function, no DB calls, operates on Pydantic-validated Extracted objects; resolved_drops set gates which fields are backfilled; result is a new Extracted instance (copy, not mutation); validate() receives the backfilled value and apply extra="forbid" validation |
| prior_matches deserialization in resume_pipeline | NameMatchResult.model_validate rejects malformed dicts with extra="forbid"; only dicts from the reconciliation column (Pydantic-validated at persist time) are deserialized |
| resume_pipeline new code | All new code stays inside the existing except boundary (lines 235-241); PII-safe error boundary preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-03-01 | Tampering | clarified_fields JSONB via set_clarified_fields | mitigate | json.dumps(clarified) with %s placeholder; ClarifiedFields.model_validate validates shape before serializing — mislabeled outcome raises ValidationError before reaching the DB (D-7.5-03b). Four outcomes (Finding 6) all in Literal — no free-form string accepted. |
| T-7.5-03-02 | Tampering | pre_clarify_extracted IS NULL guard | mitigate | Guard is in SQL: WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id — atomic check-and-write, no read-then-write race (snapshot-once D-19). |
| T-7.5-03-03 | Tampering | N4 DROP+ADD in DO $$ block | mitigate | Whole block is one transaction; failure leaves old constraint in place (not dropped). Explicit declaration of the new CHECK (not text-reconstruction) per D-7.5-03a. pg_constraint lookup narrowed by contype='c' + conrelid (Finding 9 — less likely to hit wrong constraint). |
| T-7.5-03-04 | Spoofing | New purpose value 'clarification_field_regression' in email_messages | mitigate | DB CHECK constraint (N4 migration) enforces the expanded list; get_outbound_message_id allowlist updated; both gates active. |
| T-7.5-03-05 | Tampering | backfill_extracted in-seam helper | mitigate | Pure function on Pydantic-validated objects; resolved_drops set gates which fields are backfilled; result is a new Extracted instance (copy, not mutation); validate() receives the backfilled value with extra="forbid" validation. |
| T-7.5-03-06 | Information Disclosure | PII in new resume_pipeline code | mitigate | All new code stays inside the existing except boundary (lines 235-241) which persists only type(exc).__name__. str(exc) never logged in new code. |
| T-7.5-03-07 | Elevation of Privilege | Operator approving a run while AWAITING_REPLY | accept | Structurally blocked: claim_status(AWAITING_APPROVAL, APPROVED) at main.py:492 cannot fire on an AWAITING_REPLY run — no override path (CONTEXT verified, D-7.5-04b). |
| T-7.5-03-08 | Denial of Service | set_pre_clarify_extracted called 3x per _clarify invocation (N7) | accept | IS NULL guard means only the first of the 3 calls writes; the other 2 return 0 rows. O(3) = O(1) per clarification cycle. |
| T-7.5-03-09 | Tampering | R3-2 mixed-issue: single _clarify call with purpose="clarification_field_regression" | mitigate | The decision object passed to _clarify carries ALL gate_reasons (both field_regression and normal ones); compose_clarification generates a unified email. The get_outbound_message_id idempotency check uses the new purpose so a prior plain-clarification row does NOT suppress the field-regression send. |
| T-7.5-03-10 | Tampering | prior_matches deserialization from _pre_reconciliation dicts | mitigate | NameMatchResult.model_validate with extra="forbid" rejects unexpected fields; only dicts from the reconciliation JSONB column are deserialized (Pydantic-validated at persist_reconciliation time). An empty list on first resume correctly produces no drops. |
| T-7.5-03-SC | Tampering | No new package installs | accept | No uv add invoked; psycopg + dataclasses already available. |
</threat_model>

<verification>
1. `grep "def backfill_extracted" app/pipeline/orchestrator.py` — exits 0 (R3-1 pure helper)
2. `grep "extracted = backfill_extracted" app/pipeline/orchestrator.py` — exits 0 (inline call in _run_stages seam)
3. R3-1 AST ordering check (see acceptance_criteria) — backfill_extracted line < validate line < _compute_line_items line
4. `grep "prior_matches=prior_matches" app/pipeline/orchestrator.py` — exits 0 (R3-3: prior_matches at call sites, not defaulted)
5. `grep -c "prior_matches=prior_matches" app/pipeline/orchestrator.py` — returns 2 (Round-1 and Round-2 call sites)
6. `grep "_resolved_by_name" app/pipeline/orchestrator.py` — exits 0 (set built)
7. `grep "resolved_drops=_resolved_by_name" app/pipeline/orchestrator.py` — exits 0 (D-14 WARNING fix: set passed on Round-2)
8. `grep "class ClarifiedFields" app/models/contracts.py` — exits 0
9. `grep "client_supplied" app/models/contracts.py app/pipeline/orchestrator.py` — exits 0 in both (Finding 6)
10. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
11. `grep "clarified_fields.*JSONB" app/db/schema.sql` — exits 0
12. `grep -c "clarification_field_regression" app/db/schema.sql` — 3+ occurrences
13. `grep "contype = 'c'" app/db/schema.sql` — exits 0 (Finding 9 defensive matcher)
14. `grep "def set_pre_clarify_extracted\|def load_pre_clarify_extracted\|def set_clarified_fields\|def load_clarified_fields" app/db/repo.py` — 4 hits
15. `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` — exits 0
16. `grep "clarification_field_regression" app/db/repo.py` — exits 0 (allowlist)
17. `grep "ClarifiedFields" app/db/repo.py` — exits 0 (typed-on-write validation)
18. `grep "def _clarify" app/pipeline/orchestrator.py` — shows `extracted` param and `purpose=` kwarg
19. `grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py` — 3+ (N7 all 3 paths)
20. `grep "has_field_regression\|any.*field_regression" app/pipeline/orchestrator.py` — shows any() not all() (R3-2 fix)
21. `grep "clarification_field_regression" app/pipeline/orchestrator.py` — exits 0
22. `grep "set_clarified_fields" app/pipeline/orchestrator.py` — exits 0 (N2 asked-before-send)
23. AST N1 check: prints `_run_stages calls: 1`
24. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` — exits 0
25. `uv run pytest -q` — exits 0
26. Human checkpoint: bootstrap applied, columns exist, R3-1 ordering verified, R3-3 `prior_matches=prior_matches` at call sites, D-14 `resolved_drops=_resolved_by_name` on Round-2, N1 AST 1, R3-2 any() verified, N7 grep 3+, N4 INSERT passes (rolled back)
</verification>

<success_criteria>
- backfill_extracted pure helper in orchestrator.py called INLINE in _run_stages BETWEEN reconcile_names and validate — carry-forward value in extracted BEFORE _compute_line_items (R3-1 fix — the root cause ordering invariant)
- prior_matches threaded from _pre_reconciliation (STEP A) → resume_pipeline → _run_stages(prior_matches=prior_matches) → validate → detect_field_regression — employee_id-keyed diff fires on the REAL Round-2 resume path (R3-3 fix — not just unit tests); grep "prior_matches=prior_matches" shows actual call-site threading
- _resolved_by_name set built from clarified + pre-resume reconciliation and passed as resolved_drops=_resolved_by_name on Round-2 — confirmed_dropped fields not re-backfilled (D-14 WARNING fix); grep "resolved_drops=_resolved_by_name" shows the set is PASSED, not None
- ClarifiedFields model with 4 outcomes: asked/carried_forward/confirmed_dropped/client_supplied (Finding 6); validates on every write (D-7.5-03b)
- Two JSONB columns in schema.sql (inline + ALTER) and DO $$ N4 migration block (atomic DROP+RE-ADD, D-7.5-03a); Finding 9 defensive pg_constraint lookup narrowed by contype + conrelid
- Four repo helpers: set_pre_clarify_extracted (IS NULL CAS), load_pre_clarify_extracted, set_clarified_fields (typed-on-write, 4 outcomes), load_clarified_fields
- get_outbound_message_id allowlist includes 'clarification_field_regression'
- _clarify: extracted positional param + purpose="clarification" kwarg + set_pre_clarify_extracted on all 3 AWAITING_REPLY paths (N7)
- _run_stages: inline backfill before validate (R3-1); returns _RunStagesResult(clarify_deferred, matches, issues) (Finding 5); defers when ANY field_regression issue exists (R3-2)
- resume_pipeline: STEP A before detection block; single _run_stages call (N1); Round-1 writes asked before send (N2); Round-2 passes prior=snapshot + prior_matches=prior_matches + resolved_drops=_resolved_by_name to _run_stages; client_supplied outcome for positive replacements (Finding 6)
- Existing test suites green; schema applied to dev DB; human checkpoint confirms R3-1 ordering + R3-3 threading + D-14 fix + N4 + columns + N1/N7 invariants
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-03-SUMMARY.md` when done.
</output>
