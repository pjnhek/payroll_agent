---
phase: 07.5-clarification-reply-field-regression
plan: "03"
type: execute
wave: 3
depends_on:
  - "07.5-01"
  - "07.5-02"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/models/contracts.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so new JSONB columns (pre_clarify_extracted, clarified_fields) AND the updated purpose CHECK constraint exist before integration tests run in Plan 04"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local shell env (not just .env) before the checkpoint"
        location: "local shell — run: export DATABASE_URL=<pooler connection string>"

must_haves:
  truths:
    - "ClarifiedFields Pydantic model exists in contracts.py with ConfigDict(extra='forbid') and a nested structure: {employee_id_str: {field: ClarifiedFieldOutcome}} where ClarifiedFieldOutcome = Literal['asked','carried_forward','confirmed_dropped','client_supplied'] — four outcomes: silence->carried_forward, explicit-zero->confirmed_dropped, positive-replacement->client_supplied (Finding 6 fix: 'carried_forward' now means ONLY that the value came from the snapshot, not from the client's reply)"
    - "schema.sql has pre_clarify_extracted JSONB and clarified_fields JSONB inline in payroll_runs CREATE TABLE and in the ADD COLUMN IF NOT EXISTS ALTER block"
    - "schema.sql has a DO $$ block that DROPs the old email_messages purpose CHECK constraint (by name from pg_constraint, matched defensively by table + contype + conname LIKE '%purpose%') and RE-ADDs with the expanded value list ('clarification','confirmation','clarification_field_regression') IN ONE TRANSACTION (D-7.5-03a atomic DROP+ADD)"
    - "app/db/repo.py has 4 new helpers: set_pre_clarify_extracted (IS NULL CAS, snapshot-once D-19), load_pre_clarify_extracted, set_clarified_fields (validates via ClarifiedFields model before serializing), load_clarified_fields (returns {} on NULL)"
    - "get_outbound_message_id() allowlist expanded to include 'clarification_field_regression'"
    - "_clarify() gains extracted: Extracted positional parameter and purpose: str = 'clarification' kwarg; all three hardcoded 'clarification' strings inside _clarify are replaced with the purpose variable; set_pre_clarify_extracted is called BEFORE EACH of the three set_status(AWAITING_REPLY) paths (N7 fix)"
    - "D-7.5-10 THREE-PHASE ORDERING IN _run_stages (the load-bearing invariant, R2-1 fix): when prior is not None, _run_stages executes DETECT → BACKFILL → CALC in this exact order: (1) raw_extracted captured = extracted (reference to pre-backfill object); (2) raw_drops = detect_field_regression(prior, raw_extracted, prior_matches, matches) called BEFORE any backfill — detection sees the original drop; (3) extracted = backfill_extracted(extracted, prior, matches, resolved_drops) — AFTER detection, BEFORE validate; (4) validate(extracted, roster, matches, prior_matches=prior_matches, resolved_drops=resolved_drops, raw_field_drops=raw_drops) — validate receives BACKFILLED extracted + PRE-COMPUTED drops; (5) decide/calc see backfilled extracted. The drop OT 2→None is visible at step 2 (raw), is carried forward at step 3 (backfill), and reaches the paystub at step 5 (calc). Detection never fires on backfilled data."
    - "_run_stages gains extracted: Extracted | None = None kwarg (D-7.5-11 mechanism). When provided by the caller, the internal extract() call is SKIPPED and this pre-extracted value is used directly. This is the seam that allows resume_pipeline Round-2 to supply the already-extracted raw reply to _run_stages without double-LLM-extraction. run_pipeline and Round-1 pass no extracted kwarg (uses internal extract() as today)."
    - "backfill_extracted is EMPLOYEE_ID-KEYED on BOTH sides (R2-2 fix — D-7.5-10a): snapshot lookup is NOT submitted_name-keyed. Instead, backfill_extracted receives prior_matches (not just matches) to resolve snapshot employees by employee_id. Both sides (snapshot via prior_matches, current via matches) map submitted_name → employee_id, then match on employee_id. 'M. Chen' in snapshot + 'Maria Chen' in reply, same employee_id → carry-forward fires."
    - "D-7.5-11 CLASSIFY-FIRST ON ANSWERED ROUNDS: the Round-2 path in resume_pipeline calls extract() ONCE up front to get raw_extracted, then CLASSIFIES per-(employee_id, field) outcomes for asked fields from raw_extracted BEFORE calling _run_stages. This is the fix for the Round-2 stranding bug: without classify-first, the just-answered field is still 'asked' (not yet terminal) so _resolved_by_name omits it, _run_stages re-detects the drop, decide→request_clarification, run stranded. With classify-first, the full suppress_detection set (ALL answered asked fields, not just prior terminal ones) AND the backfill_set (only carried_forward fields) are computed before _run_stages, so the run reaches AWAITING_APPROVAL in a single pass."
    - "D-7.5-11 CLASSIFICATION SCOPED TO ASKED FIELDS ONLY: the Round-2 classifier iterates ONLY the fields whose current clarified outcome is 'asked'. It does NOT classify all snapshot-paid fields (which would mislabel untouched fields like hours_regular as client_supplied — the spike's all-fields shortcut). For each (emp_id_str, field) with outcome 'asked': look up the raw extracted value from raw_extracted for the matched employee; present-positive → client_supplied; Decimal('0') → confirmed_dropped; None/absent → carried_forward."
    - "D-7.5-11 suppress_detection set = ALL answered asked fields (union of prior terminal outcomes from _resolved_by_name AND newly-classified outcomes from this round). The suppress_detection set is passed as resolved_drops= to _run_stages so N8 in validate() suppresses re-emission of field_regression for all resolved fields. No answered field re-triggers clarification."
    - "D-7.5-11 backfill_set: only carried_forward fields are backfilled. confirmed_dropped and client_supplied are NOT backfilled. The backfill_extracted call in _run_stages uses resolved_drops=suppress_detection (which now includes confirmed_dropped AND client_supplied), so those fields skip backfill. _is_paid(Decimal('0')) is False — explicit zero LOOKS backfillable by value alone; the classification is what protects it. A confirmed_dropped field in suppress_detection is NOT re-backfilled even if the raw value is None/0."
    - "D-7.5-11 PERSIST: the terminal outcomes computed in the classify-first step are persisted to clarified_fields (via set_clarified_fields) AFTER _run_stages returns but BEFORE the run reaches AWAITING_APPROVAL — the clarified_fields column reflects the final outcomes from step 1's classification, not a post-decide reclassification."
    - "Round-1 path (no clarified entries — first resume or non-field-regression resume): snapshot may be None, calls _run_stages(prior=snapshot, prior_matches=prior_matches, resolved_drops=None) WITHOUT the extracted= kwarg (internal extract() runs). If clarify_deferred=True, writes asked to clarified_fields DURABLY BEFORE calling _clarify (N2); uses purpose='clarification_field_regression'. Round-1 is unchanged by D-7.5-11."
    - "_run_stages returns _RunStagesResult(clarify_deferred: bool, matches: list, issues: list) — a minimal shape; does NOT carry extracted (already persisted); classify-first approach means raw_extracted is captured in resume_pipeline BEFORE the _run_stages call — no need to return it."
    - "R3-2 FIX: clarification is deferred whenever ANY field_regression issue exists; deferred _clarify uses purpose='clarification_field_regression'; get_outbound_message_id idempotency check uses new purpose so prior 'clarification' row does NOT suppress the send"
    - "uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q exits 0"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "ClarifiedFields Pydantic model (typed outcome validator) with 4 outcomes: asked/carried_forward/confirmed_dropped/client_supplied"
      contains: "class ClarifiedFields"
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields columns + N4 DO $$ purpose CHECK migration"
      contains: "clarification_field_regression"
    - path: "app/db/repo.py"
      provides: "4 new JSONB helpers + allowlist expansion"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "D-7.5-10 three-phase DETECT→BACKFILL→CALC ordering in _run_stages + _run_stages extracted= kwarg (D-7.5-11 mechanism: Round-2 passes pre-extracted raw reply, skips double-extraction) + employee_id-keyed backfill_extracted (R2-1/R2-2 fix) + D-7.5-11 classify-first on Round-2 (extract-up-front → classify asked fields from raw → build suppress_detection and backfill_set → single _run_stages call reaches AWAITING_APPROVAL) + _clarify extracted param + purpose kwarg + N7 snapshot + resume_pipeline Round-1/Round-2 block"
      contains: "clarification_field_regression"
  key_links:
    - from: "app/pipeline/orchestrator.py:resume_pipeline Round-2 (D-7.5-11 classify-first)"
      to: "app/pipeline/extract:extract() called once up front"
      via: "D-7.5-11: extract(combined_email, roster, run_id=run_id, llm=llm) called BEFORE _run_stages on Round-2. raw_extracted captured here; classification reads raw_extracted for asked fields. _run_stages receives extracted=raw_extracted kwarg to skip double-extraction."
      pattern: "extracted="
    - from: "app/pipeline/orchestrator.py:resume_pipeline Round-2 classifier"
      to: "raw_extracted (captured before _run_stages call)"
      via: "D-7.5-11 + D-7.5-10b: classifier iterates ONLY asked fields; reads raw value from raw_extracted for that employee/field; present-positive → client_supplied; Decimal('0') → confirmed_dropped; None → carried_forward. NOT all snapshot-paid fields (spike's shortcut avoided)."
      pattern: "client_supplied"
    - from: "app/pipeline/orchestrator.py:resume_pipeline Round-2 (suppress_detection)"
      to: "app/pipeline/orchestrator.py:_run_stages (resolved_drops= kwarg)"
      via: "D-7.5-11: suppress_detection = _resolved_by_name (prior terminals) UNION newly-classified asked fields. Passed as resolved_drops=suppress_detection. N8 in validate() suppresses field_regression re-emission for ALL resolved fields. Answered fields cannot re-trigger clarification."
      pattern: "suppress_detection"
    - from: "app/pipeline/orchestrator.py:_run_stages detect step"
      to: "app/pipeline/validate.py:detect_field_regression"
      via: "D-7.5-10 step (1-2): raw_extracted captured; detect_field_regression(prior, raw_extracted, prior_matches, matches) called BEFORE backfill_extracted. Detection sees the OT 2→None drop while it is still visible in the raw extraction."
      pattern: "detect_field_regression"
    - from: "app/pipeline/orchestrator.py:_run_stages backfill step"
      to: "app/pipeline/orchestrator.py:_compute_line_items"
      via: "D-7.5-10 step (3-5): backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops) fires AFTER detection, BEFORE validate/calc. Backfilled extracted flows into validate(raw_field_drops=raw_drops), decide, and _compute_line_items."
      pattern: "backfill_extracted"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 2, STEP A _pre_reconciliation)"
      to: "app/pipeline/orchestrator.py:_run_stages (prior_matches= kwarg)"
      via: "resume_pipeline deserializes _pre_reconciliation to NameMatchResult list; passes prior_matches=prior_matches; _run_stages uses it in both detect_field_regression and backfill_extracted (R3-3 fix + R2-2 fix)"
      pattern: "prior_matches"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 2, suppress_detection set)"
      to: "app/pipeline/orchestrator.py:_run_stages (resolved_drops= kwarg)"
      via: "D-7.5-11 + D-14: suppress_detection = all resolved asked fields (prior terminals + newly-classified). backfill_extracted guards confirmed_dropped/client_supplied fields from re-backfill. Key type (employee_id_str, field) matches Plan 02 validate() N8 check."
      pattern: "suppress_detection"
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 1)"
      to: "app/db/repo.py:set_clarified_fields"
      via: "writes asked BEFORE calling _clarify (N2 ordering, asked-before-send)"
      pattern: "set_clarified_fields"
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called BEFORE EACH of the 3 AWAITING_REPLY set_status calls; IS NULL guard = snapshot-once"
      pattern: "set_pre_clarify_extracted"
    - from: "app/models/contracts.py:ClarifiedFields"
      to: "app/db/repo.py:set_clarified_fields"
      via: "every write validates shape through ClarifiedFields before json.dumps — typed-on-write D-7.5-03b"
      pattern: "ClarifiedFields"
---

<objective>
Wire the MONEY-03 two-inbound state machine: schema DDL (two JSONB columns + N4 purpose CHECK migration), ClarifiedFields typed model (four outcomes), four repo helpers, _clarify snapshot + purpose kwarg, and the resume_pipeline Round-1/Round-2 detection block.

**Reviews addressed in this plan:**

- **R3 (HIGH — D-7.5-11 classify-first on answered rounds, the Round-2 stranding fix):** Plan 03's previous Round-2 path classified outcomes AFTER `_run_stages`/`decide`, so the just-answered field was still "asked" in `_resolved_by_name`, `_run_stages` re-detected the drop, `decide → request_clarification`, run stranded — the "AWAITING_APPROVAL" comment was false. Fix: on Round-2, call `extract()` once up front to get `raw_extracted`, classify per-(employee_id, field) outcomes for asked fields only from `raw_extracted`, build `suppress_detection` (ALL answered asked fields) and the effective backfill scope (only `carried_forward` fields), then call `_run_stages` ONCE with `resolved_drops=suppress_detection` and `extracted=raw_extracted` — the run reaches AWAITING_APPROVAL in a single pass. The `extracted=` kwarg on `_run_stages` skips the internal `extract()` call to avoid double-LLM-extraction.

- **R2-1 (HIGH — D-7.5-10 three-phase ordering, the load-bearing structural fix):** This plan enforces the invariant: DETECT-on-RAW → BACKFILL → CALC. In `_run_stages`, when `prior is not None`, the sequence is:
  1. Capture `raw_extracted = extracted` (reference to pre-backfill state)
  2. `raw_drops = detect_field_regression(prior, raw_extracted, prior_matches, matches)` — called BEFORE backfill; the OT 2→None drop is visible here
  3. `extracted = backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops)` — AFTER detection
  4. `validate(extracted, roster, matches, prior_matches=prior_matches, resolved_drops=resolved_drops, raw_field_drops=raw_drops)` — receives backfilled extracted AND pre-computed drops; does NOT self-detect
  5. decide, _compute_line_items — see backfilled extracted (R3-1 satisfied)

- **R2-2 (HIGH — backfill is now employee_id-keyed — D-7.5-10a):** `backfill_extracted` receives `prior_matches` as a new parameter to resolve snapshot employees by employee_id. Both sides (snapshot via prior_matches, current via matches) resolve submitted_name → employee_id before matching. "M. Chen" in snapshot + "Maria Chen" in reply, same employee_id → snapshot is found → carry-forward fires.

- **R2-3 (MED — provenance classifier reads raw, not backfilled — D-7.5-10b):** Classification reads `raw_extracted` (captured before backfill / before `_run_stages` on Round-2). A client who re-supplies the same positive value is labeled `client_supplied`, NOT `carried_forward`. Now: raw_value present-positive → client_supplied; None/absent → carried_forward; explicit-0 → confirmed_dropped.

- **Explicit-zero overpay guard (D-7.5-11):** `_is_paid(Decimal('0'))` is False, so explicit-zero LOOKS backfillable by value alone. The classification step labels it `confirmed_dropped` and includes it in `suppress_detection`. `backfill_extracted` guards fields in `resolved_drops`/`suppress_detection` from re-backfill. A client who explicitly zeroed OT is not re-backfilled to OT=2.

- **Finding 1 (HIGH — R3-1, backfill before calc):** Satisfied by the three-phase ordering above.
- **R3-3 BLOCKER FIX (prior_matches threading):** detect_field_regression now receives prior_matches from the pre-resume reconciliation (STEP A). backfill_extracted also uses prior_matches for employee_id-keyed snapshot lookup.
- **Finding 3 (HIGH — R3-2 mixed-issue path), Finding 5 (MEDIUM — _RunStagesResult shape), Finding 6 (MEDIUM — carried_forward overloaded):** Unchanged from previous revision.

This plan closes:
- N4: schema DO $$ block (D-7.5-03a atomic DROP+RE-ADD)
- N7: set_pre_clarify_extracted before each AWAITING_REPLY path
- N2: asked-before-send ordering
- R3: D-7.5-11 classify-first on answered rounds (the Round-2 stranding fix)
- R3-1: backfill before calc (via three-phase ordering)
- R2-1: detect before backfill (three-phase ordering)
- R2-2: employee_id-keyed backfill_extracted (prior_matches parameter)
- R2-3: raw_extracted-based provenance classification (classify-first reads raw before backfill)
- R3-2: ANY field_regression defers; mixed-issue email under "clarification_field_regression"
- R3-3: prior_matches threaded from pre-resume reconciliation to detect + backfill
- D-14 (WARNING fix): resolved_drops=suppress_detection on Round-2; (employee_id_str, field) key type
- N1 single-_run_stages-call invariant

Output: full two-inbound state machine wired; existing test suites green; schema applied to dev DB.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-01-SUMMARY.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-02-SUMMARY.md

@app/db/schema.sql
@app/db/repo.py
@app/models/contracts.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClarifiedFields model (4 outcomes) + schema DDL (N4 migration) + repo helpers + allowlist</name>
  <files>app/models/contracts.py, app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/models/contracts.py. Add ClarifiedFields AFTER FieldDrop and RawFieldDrop (both added in Plan 02). ClarifiedFields is the typed-on-write validator for the clarified_fields JSONB column (D-7.5-03b). Shape: a validated dict wrapper. The outcome Literal has FOUR values now (Finding 6 fix): "asked", "carried_forward", "confirmed_dropped", "client_supplied". All existing imports (UUID, Decimal, Literal, BaseModel, ConfigDict) should already be present — verify before adding.

  Read app/db/schema.sql in full. Key anchors:
  - Line 90: alias_candidates JSONB — add pre_clarify_extracted JSONB and clarified_fields JSONB on the next two lines (inline for fresh DBs)
  - Lines 105-108: ADD COLUMN IF NOT EXISTS block — add two lines for the new columns after line 108
  - Line 147: purpose inline CHECK — update to include 'clarification_field_regression'
  - Lines 164-165: purpose ALTER TABLE ADD COLUMN — update CHECK to include 'clarification_field_regression'
  - Lines 169-184: DO $$ uq_email_run_purpose block — THIS IS THE EXACT PATTERN to copy for the N4 DROP+RE-ADD block; add the N4 block BEFORE this existing block (i.e. after line 165 but before line 169)

  Read app/db/repo.py lines 85-100 (imports + RUN_COLS), 340-375 (claim_status CAS — the IS NULL RETURNING id pattern), 405-430 (persist_extracted — serialization with model_dump(mode="json")), 505-530 (set_alias_candidates — EXACT template for the new helpers), 659-686 (get_outbound_message_id — the allowlist to expand).

  PATTERNS.md §5 and §6 have the exact SQL and helper implementations. Critical:
  - RUN_COLS (repo.py:90-93) does NOT include alias_candidates. Follow the same pattern — do NOT add pre_clarify_extracted or clarified_fields to RUN_COLS.
  - All SQL uses %s placeholders and str(run_id) for UUID params. No f-string SQL.
  - JSONB serialization: Extracted objects use json.dumps(extracted.model_dump(mode="json")); plain dicts use json.dumps(the_dict); read-backs use json.loads(row[0]) if isinstance(row[0], str) else row[0].
  - set_pre_clarify_extracted uses the RETURNING id CAS pattern: `WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id` — atomic check-and-write, snapshot-once (D-19).
  - set_clarified_fields MUST validate through ClarifiedFields model before serializing (D-7.5-03b).
  </read_first>

  <action>
  Step 1 — Add ClarifiedFields model to app/models/contracts.py:

  After FieldDrop and RawFieldDrop (added in Plan 02), add:

  - Type alias: `ClarifiedFieldOutcome = Literal["asked", "carried_forward", "confirmed_dropped", "client_supplied"]`
    Four outcomes (Finding 6 fix):
    - "asked": the field-regression question was sent; awaiting client reply
    - "carried_forward": the client was SILENT; the value came from the pre-clarify snapshot (not from the client's reply). This label means the RAW reply had None/absent for the field — NOT that the client resupplied the same value (which is "client_supplied"). D-7.5-10b and D-7.5-11: classification reads the RAW reply before backfill, scoped to asked fields.
    - "confirmed_dropped": the client explicitly zeroed or said "none" — honor the removal; NOT re-backfilled even though _is_paid(Decimal('0')) is False
    - "client_supplied": the client replied with a POSITIVE replacement value — the raw extraction for this employee/field was present-positive before backfill; NOT same-value resupply mislabeled (D-7.5-10b)

  - Class ClarifiedFields(BaseModel):
    - model_config = ConfigDict(extra="forbid")
    - outcomes: dict[str, dict[str, ClarifiedFieldOutcome]]
    - Docstring: "Typed validator for the clarified_fields JSONB column (D-7.5-03b). Shape: {employee_id_str: {field_name: outcome}}. Validate on every write — a mislabeled carried_forward->confirmed_dropped silently underpays. Four outcomes: asked (awaiting reply), carried_forward (client silent; value from snapshot; RAW reply had None — D-7.5-10b/D-7.5-11), confirmed_dropped (explicit zero/none from client; protected from re-backfill even though _is_paid(0) is False — D-7.5-11 overpay guard), client_supplied (positive replacement from client — raw reply had the value before backfill)."
    - Class method `from_dict(cls, d: dict) -> ClarifiedFields`: validates `outcomes=d`
    - Method `to_dict(self) -> dict`: returns `self.outcomes`

  Add `Literal` to the existing import from typing if not present; it may already be there.

  Step 2 — Update app/db/schema.sql:

  a. In CREATE TABLE IF NOT EXISTS payroll_runs, after `alias_candidates JSONB,` (~line 90), add:
     `    pre_clarify_extracted JSONB,    -- D-19 MONEY-03: snapshot at awaiting_reply (IS NULL write-once guard)`
     `    clarified_fields      JSONB,    -- D-13 MONEY-03: {employee_id: {field: outcome}} field-regression outcomes`

  b. In the ADD COLUMN IF NOT EXISTS ALTER block (~lines 105-108), add after the existing lines:
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03`
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields      JSONB;  -- D-13 MONEY-03`

  c. At line 147, update inline email_messages purpose CHECK to include 'clarification_field_regression'.

  d. At lines 164-165, update the ALTER TABLE ADD COLUMN purpose CHECK to include 'clarification_field_regression'.

  e. N4 DO $$ migration block — add BEFORE the existing uq_email_run_purpose block (after line 165, before line 169). Uses D-7.5-03a atomic DROP+ADD. Finding 9 fix: the pg_constraint lookup is narrowed by BOTH contype = 'c' AND conrelid = 'email_messages'::regclass before applying the LIKE pattern:

     DO $$
     DECLARE
         _con_name TEXT;
     BEGIN
         SELECT conname INTO _con_name
         FROM pg_constraint
         WHERE contype = 'c'
           AND conrelid = 'email_messages'::regclass
           AND conname LIKE '%purpose%';
         IF _con_name IS NOT NULL THEN
             EXECUTE 'ALTER TABLE email_messages DROP CONSTRAINT ' || quote_ident(_con_name);
         END IF;
         IF NOT EXISTS (
             SELECT 1 FROM pg_constraint
             WHERE conname = 'email_messages_purpose_check'
               AND conrelid = 'email_messages'::regclass
         ) THEN
             ALTER TABLE email_messages ADD CONSTRAINT email_messages_purpose_check
                 CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'));
         END IF;
     END;
     $$;

  Step 3 — Add four repo helpers to app/db/repo.py after set_alias_candidates (~line 524):
  Follow the patterns from PATTERNS.md §5/§6 exactly. Use _conn_ctx / _nulltx, %s placeholders, str(run_id).

  a. set_pre_clarify_extracted(run_id, extracted, conn=None) -> bool:
     SQL: UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id
     Returns True if written (first write), False if already set.

  b. load_pre_clarify_extracted(run_id, conn=None) -> Extracted | None:
     SQL: SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s
     Returns None on NULL. Deserializes via Extracted.model_validate.

  c. set_clarified_fields(run_id, clarified: dict, conn=None) -> None:
     FIRST validate: ClarifiedFields(outcomes=clarified) — raises ValidationError if shape wrong (D-7.5-03b).
     SQL: UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s
     Docstring must note: "D-7.5-03b: shape validated through ClarifiedFields before persisting — a mislabeled carried_forward->confirmed_dropped silently underpays. Four outcomes: asked, carried_forward, confirmed_dropped, client_supplied. 'carried_forward' means client was SILENT (raw reply had None/absent — D-7.5-10b/D-7.5-11). 'confirmed_dropped' is protected from re-backfill (D-7.5-11 overpay guard). 'client_supplied' means client resupplied a positive value (raw reply had the value before backfill)."

  d. load_clarified_fields(run_id, conn=None) -> dict:
     SQL: SELECT clarified_fields FROM payroll_runs WHERE id = %s
     Returns {} on NULL. Deserializes via json.loads.

  Step 4 — Expand get_outbound_message_id allowlist (~line 670):
  Change: `if purpose not in ("clarification", "confirmation"):`
  To: `if purpose not in ("clarification", "confirmation", "clarification_field_regression"):`

  Step 5 — Verify imports (dry run, no DB needed yet):
  uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; from app.models.contracts import ClarifiedFields; print('ALL OK')"
  </action>

  <verify>
    <automated>uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; from app.models.contracts import ClarifiedFields; print('imports OK')" 2>&1</automated>
  </verify>

  <acceptance_criteria>
  - `grep "class ClarifiedFields" app/models/contracts.py` exits 0
  - `grep "client_supplied" app/models/contracts.py` exits 0 (fourth outcome — Finding 6 fix)
  - `grep "pre_clarify_extracted JSONB" app/db/schema.sql` exits 0 (inline column)
  - `grep "clarified_fields.*JSONB" app/db/schema.sql` exits 0
  - `grep -c "clarification_field_regression" app/db/schema.sql` returns 3+ (inline CHECK + ALTER + DO $$ migration)
  - `grep "DO \$\$" app/db/schema.sql | wc -l` returns 2 (the new N4 block AND the existing uq_email_run_purpose block)
  - `grep "contype = 'c'" app/db/schema.sql` exits 0 (Finding 9 — narrower pg_constraint lookup)
  - `grep "def set_pre_clarify_extracted\|def load_pre_clarify_extracted\|def set_clarified_fields\|def load_clarified_fields" app/db/repo.py` exits 0 (all four helpers)
  - `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` exits 0 (IS NULL CAS guard)
  - `grep "clarification_field_regression" app/db/repo.py` exits 0 (allowlist expansion)
  - `grep "ClarifiedFields" app/db/repo.py` exits 0 (typed-on-write validation in set_clarified_fields)
  - `uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.models.contracts import ClarifiedFields; print('OK')"` exits 0
  </acceptance_criteria>

  <done>ClarifiedFields model with 4 outcomes exists. 'carried_forward' docstring clarifies it means silent client (raw None/absent — D-7.5-10b/D-7.5-11); 'confirmed_dropped' docstring notes overpay guard (protected from re-backfill per D-7.5-11). Four repo helpers importable. Schema updated with new columns + N4 migration block (Finding 9 defensive matcher). Allowlist expanded. All imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: D-7.5-11 classify-first + D-7.5-10 three-phase _run_stages + employee_id-keyed backfill (R2-1/R2-2) + _run_stages extracted= kwarg (D-7.5-11 mechanism) + resume_pipeline Round-1/Round-2</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py in full in ONE PASS. Extract every anchor needed:

  _run_stages (lines 260-290 after Plan 01):
  - Line 260: current signature `def _run_stages(run_id, email, roster, *, llm, prior=None, prior_matches=None, resolved_drops=None) -> None` — Plan 01 added all THREE kwargs including prior_matches; this task adds extracted=None
  - Lines 265-269: extract_kwargs + extract() call — this is what extracted=None kwarg will optionally skip
  - Lines 269-275: four pure stages: extract(line 269), reconcile_names(line 272), validate(line 273 — now has prior=, prior_matches=, resolved_drops= kwargs per Plan 01), decide(line 275)
  - Lines 278-289: persist block + branch — MUST remain functionally identical for the process path
  - The clarify branch at line 289: `_clarify(run_id, email, decision, roster, llm=llm)` — gains extracted + purpose args

  _clarify (lines 292-451):
  - Line 292: current signature `def _clarify(run_id, email, decision, roster, *, llm) -> None`
  - Line 318: `repo.get_outbound_message_id(run_id, purpose="clarification")` — gains purpose= variable
  - Line 325: first `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 1 (idempotency early-return)
  - Line 435: `purpose="clarification"` inside repo.insert_email_message — gains variable
  - Line 438: second `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 2 (record_only)
  - Line 448: `purpose="clarification"` inside gateway.send_outbound — gains variable
  - Line 451: third `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 3 (live gateway)

  resume_pipeline (lines 94-241):
  - Line 124: claim_status CAS — DO NOT TOUCH (D-22)
  - Lines 153-162: STEP A pre-resume alias capture — MUST run FIRST before any _run_stages call. CRITICAL: this block ALREADY loads _pre_reconciliation (line 155), which is the raw list[dict] form of the pre-resume reconciliation. This is the source for prior_matches deserialization.
  - Line 164-165: STEP B current single `_run_stages(run_id, combined_email, roster, llm=llm)` call — REPLACE with D-7.5-11 + D-7.5-10 detection block
  - Lines 167-234: STEP C/D alias-diff — reads from DB via load_run after _run_stages writes reconciliation
  - Lines 235-241: except boundary — ALL new code stays inside here

  STEP A _pre_reconciliation (line 155, already loaded):
  `_pre_reconciliation = (pre_run_data.get("reconciliation") or []) if pre_run_data else []`
  This is a list of dicts. Deserialize to NameMatchResult objects for prior_matches:
  `prior_matches = [NameMatchResult.model_validate(m) for m in _pre_reconciliation if isinstance(m, dict)]`

  Imports block (lines 35-57): add detect_field_regression and _HOURS_FIELDS and _is_paid imports from app.pipeline.validate, dataclass from dataclasses, NameMatchResult import from app.models.roster, Extracted + ExtractedEmployee from app.models.contracts.

  D-7.5-10 THREE-PHASE ORDERING (the load-bearing invariant this task implements):
  In _run_stages, when prior is not None:
    Phase 1 — DETECT on raw: raw_drops = detect_field_regression(prior, extracted, prior_matches, matches)
              NOTE: extracted here is STILL the raw, pre-backfill extraction — the OT 2→None drop is visible.
    Phase 2 — BACKFILL: extracted = backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops)
    Phase 3 — CALC: validate(extracted, ..., raw_field_drops=raw_drops) → decide → _compute_line_items(extracted, ...)
  When prior is None (first run or non-field-regression): raw_drops = None; no backfill.

  D-7.5-11 CLASSIFY-FIRST MECHANISM (the Round-2 stranding fix):
  On the Round-2 path in resume_pipeline, BEFORE calling _run_stages:
  1. Call extract(combined_email, roster, run_id=run_id, ...) UP FRONT to get raw_extracted.
  2. Classify from raw_extracted for asked fields ONLY → compute suppress_detection and update clarified dict.
  3. Call _run_stages(..., extracted=raw_extracted, resolved_drops=suppress_detection).
     The extracted= kwarg causes _run_stages to SKIP its internal extract() call (no double-extraction).
  This ensures the just-answered field is in suppress_detection before _run_stages runs — so N8 in validate()
  suppresses re-emission of field_regression, decide → process, run reaches AWAITING_APPROVAL.

  CRITICAL: classification is scoped to asked fields ONLY.
  Do NOT classify all snapshot-paid fields — that would mislabel untouched fields (e.g. hours_regular
  that was always present) as client_supplied. Iterate ONLY fields whose current clarified outcome is "asked".

  CRITICAL: suppress_detection = _resolved_by_name (prior terminals) UNION newly-classified answered fields.
  Not just newly-classified. Prior confirmed_dropped/client_supplied pairs must also be suppressed.

  CRITICAL: _is_paid(Decimal('0')) is False. Explicit zero LOOKS backfillable by value alone.
  confirmed_dropped in suppress_detection is what protects it from re-backfill in backfill_extracted.
  Never add a "also suppress on asked" patch to backfill_extracted — that causes the same overpay.
  The classify-first ordering is the fix, not a patch to backfill_extracted's value test.
  </read_first>

  <action>
  Change A — Add backfill_extracted pure helper near top of orchestrator.py (after imports, before run_pipeline):

  def backfill_extracted(extracted, snapshot, prior_matches, matches, resolved_drops):

  This is a pure function with no DB calls. It accepts:
  - extracted: Extracted — the current (resumed) extraction (will be replaced, not mutated in-place)
  - snapshot: Extracted — the pre-clarify snapshot
  - prior_matches: list[NameMatchResult] — the SNAPSHOT round's reconciliation results; used to resolve
    snapshot employees by employee_id (R2-2 fix — employee_id-keyed on BOTH sides)
  - matches: list[NameMatchResult] — the CURRENT round's reconciliation results; resolves each emp's
    employee_id_str for the resolved_drops guard
  - resolved_drops: set — set of (employee_id_str, field) pairs NOT to backfill.
    KEY TYPE: (employee_id_str, field) — NOT (submitted_name, field).
    Includes: confirmed_dropped, client_supplied, AND (per D-7.5-11) any newly-classified answered field.

  Returns a new Extracted object (copy with silence fields filled from snapshot).

  Body — R2-2 fix (employee_id-keyed on BOTH sides):
  1. Build id_to_snapshot_emp: {employee_id_str: ExtractedEmployee} from snapshot.employees + prior_matches
     Build name_to_id_prior = {m.submitted_name: str(m.matched_employee_id) for m in (prior_matches or []) if m.resolved and m.matched_employee_id is not None}
     For each emp in snapshot.employees:
       emp_id_str = name_to_id_prior.get(emp.submitted_name)
       if emp_id_str is not None: id_to_snapshot_emp[emp_id_str] = emp  (last-wins, D-12)
     NOTE: If prior_matches is None or empty, id_to_snapshot_emp will be empty and no backfill occurs
     (same behavior as when no snapshot exists — safe no-op).

  2. Build name_to_id_current: {submitted_name: employee_id_str} from current matches
     name_to_id_current = {m.submitted_name: str(m.matched_employee_id) for m in matches if m.resolved and m.matched_employee_id is not None}

  3. Build a new employees list by iterating extracted.employees:
     - For each emp in extracted.employees:
       - emp_id_str = name_to_id_current.get(emp.submitted_name) — may be None if unresolved
       - snap_emp = id_to_snapshot_emp.get(emp_id_str) if emp_id_str else None  (R2-2 FIX: keyed by employee_id_str)
       - If snap_emp is None: keep emp as-is (no snapshot match by employee_id — cannot backfill)
       - Build a dict of the emp's field values, then for each field in _HOURS_FIELDS:
         - current_val = getattr(emp, field)
         - If not _is_paid(current_val) (silence or explicit zero):
           - snap_val = getattr(snap_emp, field)
           - If _is_paid(snap_val) (snapshot had a value):
             - If emp_id_str is not None and (emp_id_str, field) in (resolved_drops or set()):
               Do NOT backfill — field is in resolved_drops (confirmed_dropped, client_supplied, OR
               a D-7.5-11 newly-classified answered field). This is the overpay guard for explicit-zero:
               _is_paid(Decimal('0')) is False so the explicit-zero branch reaches here, but
               (emp_id_str, "hours_overtime") in resolved_drops → skip backfill → paystub OT=0.
             - Otherwise (not in resolved_drops): backfill
               Set field in the new emp dict to snap_val (carry-forward)
       - Append a new ExtractedEmployee built from the updated dict
  4. Return a new Extracted with the updated employees list and the same pay_period_start/pay_period_end/run_id

  Change B — Add _RunStagesResult lightweight container (after imports, before run_pipeline):

  from dataclasses import dataclass

  @dataclass
  class _RunStagesResult:
      """Minimal return value from _run_stages (MONEY-03 D-7.5-02).
      Finding 5 fix: shape carries exactly what resume_pipeline needs — clarify_deferred,
      matches, and issues. Does NOT carry extracted (already persisted by _run_stages).
      D-7.5-11: raw_extracted is captured in resume_pipeline BEFORE the _run_stages call
      (extracted= kwarg), so it never needs to be returned here.
      clarify_deferred=True: ANY field_regression issue exists; _clarify deferred so
      resume_pipeline can write 'asked' BEFORE the send (N2 ordering).
      """
      clarify_deferred: bool = False
      matches: object = None    # list[NameMatchResult]
      issues: object = None     # list[ValidationIssue]

  Change C — Update _run_stages (D-7.5-10 three-phase ordering + D-7.5-11 extracted= kwarg + _RunStagesResult return):

  1. Change signature to: `def _run_stages(run_id, email, roster, *, llm, prior=None, prior_matches=None, resolved_drops=None, extracted=None) -> _RunStagesResult:`

  2. D-7.5-11 EXTRACTED= KWARG (the mechanism that avoids double-extraction):
     Replace the extract block (lines 265-269) with:

     # D-7.5-11: if caller supplies pre-extracted data (Round-2 classify-first path),
     # skip the LLM extraction call — extracted is already the raw reply extraction.
     # run_pipeline and Round-1 pass no extracted kwarg (None → internal extract() runs).
     if extracted is None:
         extract_kwargs = {"run_id": run_id}
         if llm is not None:
             extract_kwargs["llm"] = llm
         extracted = extract(email, roster, **extract_kwargs)
     # else: use the supplied pre-extracted value directly (no LLM call)

  3. Add import at top of orchestrator.py:
     `from app.pipeline.validate import detect_field_regression, _is_paid, _HOURS_FIELDS`
     (detect_field_regression and _is_paid and _HOURS_FIELDS are public per Plan 02)

  4. D-7.5-10 THREE-PHASE ORDERING (R2-1 fix — the structural north star):

     After `matches = reconcile_names(...)` and BEFORE `issues = validate(...)`, add:

     # D-7.5-10: DETECT-on-RAW → BACKFILL → CALC (three-phase ordering invariant)
     raw_drops = None
     if prior is not None:
         # Phase 1 — DETECT on raw extracted (pre-backfill): the OT 2→None drop is visible here
         raw_drops = detect_field_regression(prior, extracted, prior_matches, matches)
         # Phase 2 — BACKFILL: fill silence fields from snapshot (employee_id-keyed via prior_matches)
         # resolved_drops guards confirmed_dropped, client_supplied, AND D-7.5-11 newly-classified
         # answered fields from re-backfill — this is the overpay guard for explicit-zero.
         extracted = backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops)
         # extracted is now the BACKFILLED version; validate/decide/calc will use it

  5. Update the validate() call to:
     `issues = validate(extracted, roster, matches, prior_matches=prior_matches, resolved_drops=resolved_drops, raw_field_drops=raw_drops)`
     NOTE: `extracted` here is the BACKFILLED version (step 2 above).
           `raw_field_drops=raw_drops` carries the pre-computed drops (step 1 above) to validate().

  6. In the else branch (request_clarification):
     - R3-2 fix: defer whenever ANY field_regression issue exists:
       `has_field_regression = any(i.issue_type == "field_regression" for i in issues)`
     - If has_field_regression is True: set `clarify_deferred = True`. Do NOT call _clarify here.
     - If has_field_regression is False: set `clarify_deferred = False`. Call `_clarify(run_id, email, decision, roster, extracted, llm=llm, purpose="clarification")`.

  7. On the process branch: `clarify_deferred = False`.

  8. At the end: `return _RunStagesResult(clarify_deferred=clarify_deferred, matches=matches, issues=issues)`

  9. Update run_pipeline call site: `_ = _run_stages(run_id, email, roster, llm=llm)` (discard return — behavior unchanged).

  Change D — Update _clarify: add extracted positional param + purpose kwarg + N7 snapshot on 3 paths:

  1. New signature: `def _clarify(run_id, email, decision, roster, extracted, *, llm, purpose="clarification") -> None:`

  2. Replace ALL THREE hardcoded "clarification" strings with the `purpose` variable:
     - Line 318: `repo.get_outbound_message_id(run_id, purpose=purpose)`
     - Line 435: `purpose=purpose` in repo.insert_email_message call
     - Line 448: `purpose=purpose` in gateway.send_outbound call

  3. N7 fix — add `repo.set_pre_clarify_extracted(run_id, extracted)` IMMEDIATELY BEFORE each of the THREE set_status(AWAITING_REPLY) calls:
     - Before line 325 (PATH 1 idempotency early-return)
     - Before line 438 (PATH 2 record_only)
     - Before line 451 (PATH 3 live gateway)
  The IS NULL guard in SQL makes all three calls idempotent — only the first write succeeds (snapshot-once D-19).

  Change E — Add Round-1/Round-2 detection block in resume_pipeline (D-7.5-11 + D-7.5-10 + all prior fixes):

  STEP A (lines 153-162) remains UNCHANGED and runs FIRST. After STEP A, _pre_reconciliation is available as a raw list[dict].

  Replace old STEP B (lines 164-165, the bare `_run_stages(...)` call) with the following detection block. All code stays inside the existing except boundary (lines 235-241).

  Step E0: Deserialize prior_matches from _pre_reconciliation (R3-3 fix):
    prior_matches: list[NameMatchResult] = [
        NameMatchResult.model_validate(m) for m in _pre_reconciliation if isinstance(m, dict)
    ]
    # Empty list on first-ever resume (no prior reconciliation); [] → detect_field_regression returns [] (correct).

  Step E1: load snapshot and clarified state:
    snapshot = repo.load_pre_clarify_extracted(run_id)    # None on first resume
    clarified = repo.load_clarified_fields(run_id)          # {} on first resume

  Step E2: build _resolved_by_name from clarified TERMINAL outcomes for resolved_drops base (D-14 + KEY TYPE):
    KEY TYPE: (employee_id_str, field) — NOT (submitted_name, field). See Plan 02 verification.
    clarified.items() already yields emp_id_str keys — NO reverse-lookup needed.

    _resolved_by_name: set[tuple[str, str]] = set()
    for emp_id_str, field_outcomes in clarified.items():
        for field, outcome in field_outcomes.items():
            if outcome in ("confirmed_dropped", "client_supplied"):
                _resolved_by_name.add((emp_id_str, field))

  Step E3: determine which round this is:
    is_round_2 = bool(clarified)  # any clarified_fields entries = at least Round 2

  Step E4: Round-1 path (no clarified entries — first resume or non-field-regression resume):
    If NOT is_round_2:
      stage = _run_stages(run_id, combined_email, roster, llm=llm,
                          prior=snapshot,                   # None on very first resume
                          prior_matches=prior_matches,      # [] on very first resume
                          resolved_drops=None)              # no confirmed_dropped pairs yet
      # NOTE: no extracted= kwarg on Round-1. _run_stages calls extract() internally as today.

      If stage.clarify_deferred is True (ANY field_regression issue; R3-2 fix):
        - Build asked entries: reload the run to get fresh matches from DB after _run_stages wrote reconciliation
          post_run = repo.load_run(run_id)
          name_to_id = {m["submitted_name"]: m["matched_employee_id"] for m in (post_run.get("reconciliation") or []) if m.get("matched_employee_id")}
        - For each issue in stage.issues where issue.issue_type == "field_regression":
          - Parse submitted_name and field_name from issue.field (format: "{submitted_name}.{field_name}"; use rsplit(".", 1))
          - current_emp_id = name_to_id.get(submitted_name)
          - If current_emp_id: clarified.setdefault(str(current_emp_id), {})[field_name] = "asked"
        - Write asked durably BEFORE send (N2):
          repo.set_clarified_fields(run_id, clarified)
        - THEN call _clarify with purpose="clarification_field_regression":
          run_row = repo.load_run(run_id)
          from app.models.contracts import Extracted
          persisted_decision = Decision.model_validate(run_row["decision"]) if run_row and run_row.get("decision") else None
          persisted_extracted = Extracted.model_validate(run_row["extracted_data"]) if run_row and run_row.get("extracted_data") else None
          _clarify(run_id, combined_email, persisted_decision, roster, persisted_extracted, llm=llm, purpose="clarification_field_regression")
        - RETURN (do not continue to alias-diff; the run is at AWAITING_REPLY)
      If stage.clarify_deferred is False: fall through to STEP C/D alias-diff (normal path)

  Step E5: Round-2 path — D-7.5-11 CLASSIFY-FIRST (answered rounds):
    If is_round_2:

      # D-7.5-11: extract the raw reply ONCE UP FRONT, before _run_stages.
      # This is the key mechanism that enables classify-first ordering.
      # run_pipeline and Round-1 do NOT use this pattern (their _run_stages call has no extracted= kwarg).
      extract_kwargs_r2 = {"run_id": run_id}
      if llm is not None:
          extract_kwargs_r2["llm"] = llm
      raw_extracted = extract(combined_email, roster, **extract_kwargs_r2)
      # raw_extracted is the raw reply extraction, before any backfill.

      # D-7.5-11 STEP 1: CLASSIFY the raw reply for asked fields ONLY.
      # Do NOT classify all snapshot-paid fields — that mislabels untouched fields (e.g. hours_regular
      # that was always present) as client_supplied. Only fields currently "asked" get classified.
      #
      # Build a lookup from current submitted_name → employee_id_str using the snapshot reconciliation
      # (prior_matches) to resolve the current reply's employees by id.
      # Note: we do not yet have current-round matches (not reconciled yet). Use a name→id lookup
      # derived from prior_matches as a best-effort, since the employee names are usually stable.
      # If a name in raw_extracted is not in prior_matches (e.g. truly new name), classification
      # cannot resolve it and no outcome update is made — safe no-op for that field.
      name_to_id_for_classify: dict[str, str] = {
          m.submitted_name: str(m.matched_employee_id)
          for m in prior_matches
          if m.resolved and m.matched_employee_id is not None
      }
      # Also build a lookup from raw_extracted: {submitted_name: ExtractedEmployee}
      raw_name_to_emp: dict[str, object] = {
          emp.submitted_name: emp for emp in raw_extracted.employees
      }

      # Classify each (emp_id_str, field) with outcome "asked".
      newly_classified: set[tuple[str, str]] = set()  # all answered asked fields for suppress_detection
      for emp_id_str, field_outcomes in list(clarified.items()):
          for field, outcome in list(field_outcomes.items()):
              if outcome != "asked":
                  continue  # only reclassify fields that were asked

              # Find the raw extracted employee for this emp_id_str via prior_matches.
              # We look for a submitted_name whose prior_matches entry resolves to this emp_id_str.
              # On the Round-2 reply, the same employee may have a different submitted_name (restated).
              # We match by employee_id_str using prior_matches (the snapshot reconciliation).
              raw_emp = None
              for raw_name, raw_e in raw_name_to_emp.items():
                  if name_to_id_for_classify.get(raw_name) == emp_id_str:
                      raw_emp = raw_e
                      break

              if raw_emp is None:
                  # Cannot resolve the employee in the raw reply — no update (safe no-op).
                  # The field remains "asked"; it will be handled by the next round or the loop guard.
                  continue

              raw_val = getattr(raw_emp, field, None)
              # D-7.5-10b + D-7.5-11: classify from RAW reply, before any backfill.
              if raw_val is not None and raw_val > 0:
                  # Present-positive in raw reply → client supplied a value
                  clarified[emp_id_str][field] = "client_supplied"
              elif raw_val is not None and raw_val == 0:
                  # Explicit Decimal('0') in raw reply → client explicitly zeroed the field
                  clarified[emp_id_str][field] = "confirmed_dropped"
              else:
                  # None/absent in raw reply → client was silent → carry forward from snapshot
                  clarified[emp_id_str][field] = "carried_forward"

              newly_classified.add((emp_id_str, field))

      # D-7.5-11 STEP 2: build suppress_detection (ALL resolved asked fields).
      # suppress_detection = prior terminal outcomes (confirmed_dropped/client_supplied from previous
      # rounds) UNION newly-classified outcomes from this round.
      # This set is passed as resolved_drops= to _run_stages so N8 in validate() suppresses
      # field_regression re-emission for all resolved fields, including the just-answered ones.
      # The run reaches AWAITING_APPROVAL in a single pass (no re-clarify loop).
      suppress_detection: set[tuple[str, str]] = set(_resolved_by_name)
      for emp_id_str, field in newly_classified:
          suppress_detection.add((emp_id_str, field))

      # D-7.5-11 STEP 3: call _run_stages ONCE with:
      # - extracted=raw_extracted: skip double-extraction (LLM already called once above)
      # - resolved_drops=suppress_detection: N8 suppresses re-detection for all answered fields
      # - prior=snapshot, prior_matches=prior_matches: three-phase ordering (D-7.5-10)
      # The three-phase ordering inside _run_stages:
      #   Phase 1: detect_field_regression(snapshot, raw_extracted, prior_matches, matches) on RAW
      #            — suppress_detection is in resolved_drops, so answered fields are suppressed in N8
      #   Phase 2: backfill_extracted(raw_extracted, snapshot, prior_matches, matches, suppress_detection)
      #            — only carried_forward fields backfilled; confirmed_dropped/client_supplied in
      #              suppress_detection are NOT re-backfilled (overpay guard)
      #   Phase 3: validate(backfilled, ..., raw_field_drops=raw_drops) → decide → calc
      #            — no field_regression issues survive for answered fields → decide → process
      #            → run reaches AWAITING_APPROVAL in a single pass
      stage = _run_stages(run_id, combined_email, roster, llm=llm,
                          prior=snapshot,
                          prior_matches=prior_matches,
                          resolved_drops=suppress_detection,
                          extracted=raw_extracted)   # D-7.5-11: pre-extracted raw reply

      # D-7.5-11 STEP 4: persist the terminal outcomes computed in step 1 (classify-first).
      # These are already final — we do NOT reclassify from stage.issues after the fact.
      # A post-decide reclassification would reproduce the D-7.5-11 stranding bug
      # (the just-answered field would be re-detected as a drop and stage.issues would not
      # contain it — the classification would be wrong in the opposite direction).
      repo.set_clarified_fields(run_id, clarified)
      # Fall through to STEP C/D alias-diff (the run is at AWAITING_APPROVAL)

  STEP C/D alias-diff (lines 167-234) remains as today — reading from DB via load_run after _run_stages has written reconciliation. No change needed here.

  Add imports at the top of orchestrator.py:
  - `from app.models.roster import NameMatchResult` (for prior_matches deserialization)
  - `from app.pipeline.validate import _is_paid, _HOURS_FIELDS, detect_field_regression`
  - `from decimal import Decimal`
  - `from dataclasses import dataclass`
  - `from app.models.contracts import Extracted, ExtractedEmployee` (for backfill_extracted type hints)

  After implementing, run:
  uv run python -c "from app.pipeline.orchestrator import _run_stages, resume_pipeline, _clarify, _RunStagesResult, backfill_extracted; print('OK')"
  uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import _run_stages, resume_pipeline, _clarify, _RunStagesResult, backfill_extracted; print('OK')" && uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "def backfill_extracted" app/pipeline/orchestrator.py` exits 0 (R3-1 fix: the in-seam pure helper)
  - `grep "extracted = backfill_extracted" app/pipeline/orchestrator.py` exits 0 (inline call BEFORE validate in _run_stages)
  - `grep "backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops)" app/pipeline/orchestrator.py` exits 0 (R2-2 FIX: prior_matches is THIRD arg — snapshot side is employee_id-keyed via prior_matches)
  - `grep "extracted=None" app/pipeline/orchestrator.py` exits 0 (D-7.5-11 mechanism: _run_stages extracted= kwarg)
  - `grep "extracted=raw_extracted" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: Round-2 resume passes pre-extracted raw reply to _run_stages)
  - `grep "if extracted is None:" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: _run_stages skips internal extract() when caller provides extracted)
  - `grep "raw_extracted = extract(" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: up-front extraction in Round-2 path)
  - `grep "newly_classified" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: classify-first newly_classified set)
  - `grep "suppress_detection" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: suppress_detection = prior terminals + newly_classified)
  - `grep "resolved_drops=suppress_detection" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: suppress_detection passed as resolved_drops to _run_stages on Round-2)
  - `grep "outcome != .asked." app/pipeline/orchestrator.py` exits 0 (D-7.5-11: classification scoped to asked fields only)
  - Ordering check (the R2-1 + R3-1 invariant): in _run_stages, detect_field_regression is called BEFORE backfill_extracted, which is called BEFORE validate, which is BEFORE _compute_line_items
  - `grep "prior_matches=prior_matches" app/pipeline/orchestrator.py` exits 0 (R3-3: prior_matches at call sites)
  - `grep -c "prior_matches=prior_matches" app/pipeline/orchestrator.py` returns 2+ (Round-1 and Round-2 call sites in resume_pipeline)
  - `grep "_resolved_by_name" app/pipeline/orchestrator.py` exits 0 (set built from clarified.items() for prior terminals)
  - `grep "id_to_submitted_name" app/pipeline/orchestrator.py` exits 1 (BLOCKER FIX: reverse-lookup eliminated)
  - `grep "resolved_drops=suppress_detection\|resolved_drops=_resolved_by_name\|resolved_drops=None" app/pipeline/orchestrator.py` — Round-2 uses suppress_detection; Round-1 uses None
  - `grep "def _clarify" app/pipeline/orchestrator.py` shows `extracted` positional param and `purpose="clarification"` default kwarg
  - `grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py` returns 3+ (N7: one before each AWAITING_REPLY path)
  - `grep "has_field_regression\|any.*field_regression" app/pipeline/orchestrator.py` exits 0 (R3-2 fix: any-not-all deferral)
  - `grep "clarification_field_regression" app/pipeline/orchestrator.py` exits 0 (Round-1 _clarify call with purpose=)
  - `grep "set_clarified_fields" app/pipeline/orchestrator.py` exits 0 (N2 asked-before-send in Round-1; D-7.5-11 persist in Round-2)
  - `grep "client_supplied" app/pipeline/orchestrator.py` exits 0 (D-7.5-11 + R2-3 fix: classification in Round-2 classify-first)
  - `grep "carried_forward" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: carried_forward outcome in Round-2 classifier)
  - `grep "confirmed_dropped" app/pipeline/orchestrator.py` exits 0 (D-7.5-11: confirmed_dropped outcome + overpay guard comment)
  - `grep "_RunStagesResult" app/pipeline/orchestrator.py` exits 0 (Finding 5: correct return shape)
  - `grep "raw_field_drops=raw_drops" app/pipeline/orchestrator.py` exits 0 (D-7.5-10: pre-computed drops passed to validate)
  - `grep "detect_field_regression" app/pipeline/orchestrator.py` exits 0 (direct call in _run_stages — D-7.5-10 detect step)
  - `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` exits 0
  - `uv run pytest -q` full suite exits 0
  </acceptance_criteria>

  <done>D-7.5-11 classify-first implemented: Round-2 resume calls extract() once up front (raw_extracted), classifies asked fields from raw_extracted before _run_stages, builds suppress_detection (prior terminals + newly_classified), calls _run_stages ONCE with extracted=raw_extracted + resolved_drops=suppress_detection — run reaches AWAITING_APPROVAL in a single pass. Explicit-zero overpay guard: confirmed_dropped in suppress_detection prevents re-backfill even though _is_paid(Decimal('0')) is False. Classification scoped to asked fields only (not all snapshot-paid fields). _run_stages extracted= kwarg avoids double-LLM-extraction. D-7.5-10 three-phase ordering in _run_stages (detect on raw → backfill → calc). R2-2: employee_id-keyed backfill_extracted via prior_matches. D-7.5-11 persist: set_clarified_fields after _run_stages with classify-first outcomes (not post-decide reclassification). Round-1 unchanged. Existing test suites green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1: ClarifiedFields model (4 outcomes: asked/carried_forward/confirmed_dropped/client_supplied). Schema DDL (two new JSONB columns + N4 purpose CHECK DO $$ migration). Four repo helpers. Allowlist expansion.

  Task 2: D-7.5-11 CLASSIFY-FIRST on answered rounds (the Round-2 stranding fix):
  - Round-2 path: extract() called ONCE up front → raw_extracted captured → classify asked fields from raw_extracted (scoped to asked fields only, not all snapshot-paid fields) → suppress_detection = prior terminals UNION newly_classified → _run_stages called ONCE with extracted=raw_extracted + resolved_drops=suppress_detection → run reaches AWAITING_APPROVAL in single pass.
  - Explicit-zero overpay guard: confirmed_dropped in suppress_detection prevents re-backfill (_is_paid(Decimal('0')) is False by value, but resolved_drops gates it).
  - _run_stages extracted= kwarg: when supplied, skips internal extract() (no double-LLM-extraction).
  - Persist: set_clarified_fields after _run_stages with classify-first outcomes (NOT post-decide reclassification).

  D-7.5-10 THREE-PHASE ORDERING in _run_stages: detect_field_regression(prior, RAW extracted, prior_matches, matches) BEFORE backfill_extracted(extracted, prior, PRIOR_MATCHES, matches, resolved_drops) BEFORE validate(BACKFILLED extracted, raw_field_drops=drops) BEFORE calc.
  R2-2: backfill_extracted employee_id-keyed on BOTH sides (prior_matches resolves snapshot employees).
  R3-3: prior_matches deserialized from _pre_reconciliation and threaded to _run_stages.
  _resolved_by_name built directly from clarified.items() emp_id_str keys (no reverse-lookup).
  _clarify gains extracted param, purpose kwarg, set_pre_clarify_extracted on all 3 paths (N7).
  _run_stages returns _RunStagesResult; defers when ANY field_regression (R3-2).
  Round-1 unchanged: single _run_stages call (N1), asked-before-send (N2), any-field_regression deferral.
  </what-built>
  <how-to-verify>
  Requires DATABASE_URL set in shell env (Supavisor transaction mode port 6543).

  1. Verify imports:
     uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.models.contracts import ClarifiedFields; from app.pipeline.orchestrator import _clarify, resume_pipeline, backfill_extracted, _RunStagesResult; print('ALL IMPORTS OK')"

  2. Apply schema to dev DB:
     uv run python -m app.db.bootstrap
     Verify new columns exist:
     uv run python -c "
     import psycopg, os
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\")
     cols = [r[0] for r in cur.fetchall()]
     assert 'pre_clarify_extracted' in cols and 'clarified_fields' in cols, f'MISSING: {cols}'
     cur.execute(\"SELECT check_clause FROM information_schema.check_constraints WHERE constraint_name LIKE '%purpose%'\")
     rows = cur.fetchall()
     assert any('clarification_field_regression' in str(r) for r in rows), f'N4 not applied: {rows}'
     print('Schema PASSED')
     conn.close()
     "

  3. Verify D-7.5-11 mechanism — extracted= kwarg and classify-first:
     grep "extracted=None" app/pipeline/orchestrator.py
     grep "extracted=raw_extracted" app/pipeline/orchestrator.py
     grep "if extracted is None:" app/pipeline/orchestrator.py
     grep "raw_extracted = extract(" app/pipeline/orchestrator.py
     grep "newly_classified" app/pipeline/orchestrator.py
     grep "suppress_detection" app/pipeline/orchestrator.py
     grep "resolved_drops=suppress_detection" app/pipeline/orchestrator.py
     # All must exit 0

  4. Verify D-7.5-11 classification scoped to asked fields only:
     grep "outcome != .asked." app/pipeline/orchestrator.py
     # Must appear in the Round-2 classifier loop (not all fields classified)

  5. Verify D-7.5-10 three-phase ordering invariant (the load-bearing R2-1 fix):
     python3 -c "
     import ast
     src = open('app/pipeline/orchestrator.py').read()
     tree = ast.parse(src)
     for fn in ast.walk(tree):
         if isinstance(fn, ast.FunctionDef) and fn.name == '_run_stages':
             calls = [(n.lineno, getattr(getattr(n, 'func', None), 'id', None)) for n in ast.walk(fn) if isinstance(n, ast.Call) and getattr(getattr(n, 'func', None), 'id', None) in ('detect_field_regression', 'backfill_extracted', 'validate', '_compute_line_items')]
             calls.sort()
             print('Call order in _run_stages:', calls)
             names = [c[1] for c in calls]
             assert names.index('detect_field_regression') < names.index('backfill_extracted'), 'R2-1 VIOLATED: detect after backfill'
             assert names.index('backfill_extracted') < names.index('validate'), 'R3-1 VIOLATED: backfill after validate'
             assert names.index('backfill_extracted') < names.index('_compute_line_items'), 'R3-1 VIOLATED: backfill after calc'
             print('D-7.5-10 THREE-PHASE ORDERING PASSED')
     "

  6. Verify R2-2 (employee_id-keyed backfill — prior_matches as third arg):
     grep "backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops)" app/pipeline/orchestrator.py

  7. Verify R3-3 threading (prior_matches at call sites):
     grep "prior_matches=prior_matches" app/pipeline/orchestrator.py
     grep -c "prior_matches=prior_matches" app/pipeline/orchestrator.py
     # Should print 2+ (Round-1 and Round-2 call sites in resume_pipeline)

  8. Verify D-14 KEY TYPE FIX (no reverse-lookup):
     grep "id_to_submitted_name" app/pipeline/orchestrator.py
     # MUST return no lines — reverse-lookup eliminated

  9. Verify N1 (single _run_stages call in resume_pipeline):
     python3 -c "
     import ast
     src = open('app/pipeline/orchestrator.py').read()
     tree = ast.parse(src)
     for fn in ast.walk(tree):
         if isinstance(fn, ast.FunctionDef) and fn.name == 'resume_pipeline':
             calls = [n for n in ast.walk(fn) if isinstance(n, ast.Call) and isinstance(getattr(n, 'func', None), ast.Name) and n.func.id == '_run_stages']
             print(f'_run_stages calls in resume_pipeline: {len(calls)}')
             assert len(calls) == 1, f'N1 VIOLATED: {len(calls)}'
             print('N1 PASSED')
     "

  10. Verify R3-2 (any-not-all field_regression deferral):
      grep "has_field_regression\|any.*field_regression" app/pipeline/orchestrator.py
      # Should show the any() check, NOT an all() check.

  11. Verify N7 (snapshot on 3 paths):
      grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py
      (should print 3 or more)

  12. Verify N4 insert test (rolled back):
      uv run python -c "
      import psycopg, os, uuid
      conn = psycopg.connect(os.environ['DATABASE_URL'])
      cur = conn.cursor()
      cur.execute(\"INSERT INTO email_messages (id, run_id, direction, message_id, purpose) VALUES (%s, NULL, 'outbound', %s, 'clarification_field_regression')\", (str(uuid.uuid4()), '<n4-test@test.local>'))
      conn.rollback()
      print('N4 INSERT PASSED (rolled back)')
      conn.close()
      "

  13. Verify all four ClarifiedFieldOutcome values present:
      grep "client_supplied\|carried_forward\|confirmed_dropped\|asked" app/pipeline/orchestrator.py app/models/contracts.py
      Should appear in both files.

  14. Full test suite:
      uv run pytest -q
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass:
  D-7.5-11 classify-first mechanism: raw_extracted = extract() up front, newly_classified set built, suppress_detection = prior terminals UNION newly_classified, _run_stages called with extracted=raw_extracted + resolved_drops=suppress_detection, set_clarified_fields persisted with classify-first outcomes (not post-decide reclassification), classification scoped to asked fields only (outcome != 'asked' guard present).
  D-7.5-10 THREE-PHASE ORDERING (detect < backfill < validate < calc — R2-1 + R3-1).
  R2-2 (prior_matches as third arg to backfill_extracted — employee_id-keyed snapshot).
  R3-3 threading (prior_matches=prior_matches at call sites — not defaulted).
  D-14 + KEY TYPE FIX (no id_to_submitted_name reverse-lookup).
  N1 AST (1 call to _run_stages in resume_pipeline).
  R3-2 any() not all().
  N7 grep 3+.
  N4 insert passes (rolled back).
  Schema columns exist.
  Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clarified_fields JSONB writes | Written via json.dumps with %s placeholders through ClarifiedFields validator (4 outcomes) — no f-string SQL injection |
| pre_clarify_extracted IS NULL guard | Atomic check-and-write in SQL — no Python read-then-write race (snapshot-once D-19) |
| email_messages.purpose CHECK | DB-enforced allowlist now includes 'clarification_field_regression'; unexpected purpose raises constraint violation at INSERT |
| N4 DO $$ migration block | Runs in a single transaction — failure cannot leave the constraint dropped-but-not-re-added (D-7.5-03a); pg_constraint lookup narrowed by contype='c' AND conrelid (Finding 9) |
| D-7.5-11 classify-first in resume_pipeline | Classification reads raw_extracted (from extract(), before backfill); scoped to asked fields only — untouched fields like hours_regular cannot be mislabeled client_supplied. suppress_detection is code-owned set (employee_id_str, field tuples) — no user-controlled strings |
| backfill_extracted in-seam helper | Pure function, no DB calls, operates on Pydantic-validated Extracted objects; prior_matches + matches used to resolve employee_id_str on BOTH sides (R2-2 fix); resolved_drops set (keyed by employee_id_str, field) gates which fields are backfilled; result is a new Extracted instance (copy, not mutation) |
| D-7.5-11 overpay guard | confirmed_dropped in suppress_detection prevents re-backfill of explicitly-zeroed fields. _is_paid(Decimal('0')) is False so explicit zero looks backfillable by value; the resolved_drops gate is the protection — not a value test |
| detect_field_regression in _run_stages | Called on RAW (pre-backfill) extracted; prior_matches used for employee_id-keyed diff; result (raw_drops) passed to validate() as raw_field_drops — never merged with backfilled data before detection |
| prior_matches deserialization in resume_pipeline | NameMatchResult.model_validate rejects malformed dicts with extra="forbid"; only dicts from the reconciliation column (Pydantic-validated at persist time) are deserialized |
| resume_pipeline new code | All new code stays inside the existing except boundary (lines 235-241); PII-safe error boundary preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-03-01 | Tampering | clarified_fields JSONB via set_clarified_fields | mitigate | json.dumps(clarified) with %s placeholder; ClarifiedFields.model_validate validates shape before serializing — mislabeled outcome raises ValidationError before reaching the DB (D-7.5-03b). Four outcomes (Finding 6) all in Literal — no free-form string accepted. |
| T-7.5-03-02 | Tampering | pre_clarify_extracted IS NULL guard | mitigate | Guard is in SQL: WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id — atomic check-and-write, no read-then-write race (snapshot-once D-19). |
| T-7.5-03-03 | Tampering | N4 DROP+ADD in DO $$ block | mitigate | Whole block is one transaction; failure leaves old constraint in place (not dropped). Explicit declaration of the new CHECK (not text-reconstruction) per D-7.5-03a. pg_constraint lookup narrowed by contype='c' + conrelid (Finding 9). |
| T-7.5-03-04 | Spoofing | New purpose value 'clarification_field_regression' in email_messages | mitigate | DB CHECK constraint (N4 migration) enforces the expanded list; get_outbound_message_id allowlist updated; both gates active. |
| T-7.5-03-05 | Tampering | backfill_extracted in-seam helper | mitigate | Pure function on Pydantic-validated objects; employee_id-keyed on BOTH sides via prior_matches + matches (R2-2 fix); resolved_drops set (employee_id_str, field) gates which fields are backfilled — includes D-7.5-11 suppress_detection so explicit-zero is NOT re-backfilled (overpay guard); result is a new Extracted instance (copy). |
| T-7.5-03-06 | Tampering | detect_field_regression in _run_stages seam | mitigate | Called BEFORE backfill (D-7.5-10 phase 1) on raw Pydantic-validated extraction; prior_matches employee_id-keyed (R3-3 fix); result passed as raw_field_drops to validate() — detection cannot be masked by backfill. |
| T-7.5-03-07 | Information Disclosure | PII in new resume_pipeline code | mitigate | All new code stays inside the existing except boundary (lines 235-241) which persists only type(exc).__name__. str(exc) never logged in new code. |
| T-7.5-03-08 | Elevation of Privilege | Operator approving a run while AWAITING_REPLY | accept | Structurally blocked: claim_status(AWAITING_APPROVAL, APPROVED) at main.py:492 cannot fire on an AWAITING_REPLY run — no override path (CONTEXT verified, D-7.5-04b). |
| T-7.5-03-09 | Denial of Service | set_pre_clarify_extracted called 3x per _clarify invocation (N7) | accept | IS NULL guard means only the first of the 3 calls writes; the other 2 return 0 rows. O(3) = O(1) per clarification cycle. |
| T-7.5-03-10 | Tampering | R3-2 mixed-issue: single _clarify call with purpose="clarification_field_regression" | mitigate | The decision object passed to _clarify carries ALL gate_reasons (both field_regression and normal ones); compose_clarification generates a unified email. The get_outbound_message_id idempotency check uses the new purpose so a prior plain-clarification row does NOT suppress the field-regression send. |
| T-7.5-03-11 | Tampering | prior_matches deserialization from _pre_reconciliation dicts | mitigate | NameMatchResult.model_validate with extra="forbid" rejects unexpected fields; only dicts from the reconciliation JSONB column are deserialized (Pydantic-validated at persist_reconciliation time). An empty list on first resume correctly produces no drops. |
| T-7.5-03-12 | Tampering | D-7.5-11 classify-first scoped to asked fields | mitigate | Classification loop guard `if outcome != "asked": continue` ensures only fields currently "asked" are reclassified. Untouched fields (e.g. hours_regular with no prior "asked" outcome) cannot be mislabeled. The spike's all-fields shortcut is explicitly avoided. |
| T-7.5-03-13 | Tampering | D-7.5-11 explicit-zero overpay via re-backfill | mitigate | confirmed_dropped is included in suppress_detection; backfill_extracted checks resolved_drops before backfilling. _is_paid(Decimal('0')) is False (looks backfillable by value alone) — the resolved_drops gate is the protection. |
| T-7.5-03-14 | Tampering | D-7.5-11 double-LLM-extraction via extracted= kwarg | mitigate | _run_stages: `if extracted is None: [call extract()]`. Round-2 supplies extracted=raw_extracted (already extracted up front). Round-1 and run_pipeline supply no extracted kwarg → internal extract() runs as today. No double-extraction on any path. |
| T-7.5-03-SC | Tampering | No new package installs | accept | No uv add invoked; psycopg + dataclasses already available. |
</threat_model>

<verification>
1. `grep "def backfill_extracted" app/pipeline/orchestrator.py` — exits 0
2. `grep "backfill_extracted(extracted, prior, prior_matches, matches, resolved_drops)" app/pipeline/orchestrator.py` — exits 0 (R2-2 FIX: prior_matches is third arg)
3. D-7.5-10 AST ordering check — detect_field_regression line < backfill_extracted line < validate line < _compute_line_items line in _run_stages
4. `grep "raw_field_drops=raw_drops" app/pipeline/orchestrator.py` — exits 0
5. `grep "detect_field_regression" app/pipeline/orchestrator.py` — exits 0
6. `grep "prior_matches=prior_matches" app/pipeline/orchestrator.py` — exits 0 (R3-3)
7. `grep -c "prior_matches=prior_matches" app/pipeline/orchestrator.py` — returns 2+
8. `grep "_resolved_by_name" app/pipeline/orchestrator.py` — exits 0
9. `grep "id_to_submitted_name" app/pipeline/orchestrator.py` — exits 1 (BLOCKER FIX: reverse-lookup eliminated)
10. `grep "extracted=None" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11 mechanism: _run_stages extracted= kwarg)
11. `grep "extracted=raw_extracted" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: Round-2 passes pre-extracted)
12. `grep "if extracted is None:" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: skip internal extract() when provided)
13. `grep "raw_extracted = extract(" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: up-front extraction)
14. `grep "newly_classified" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: classify-first newly_classified set)
15. `grep "suppress_detection" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: suppress_detection set)
16. `grep "resolved_drops=suppress_detection" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: passed to _run_stages)
17. `grep "outcome != .asked." app/pipeline/orchestrator.py` — exits 0 (D-7.5-11: scoped to asked fields only)
18. `grep "client_supplied" app/models/contracts.py app/pipeline/orchestrator.py` — exits 0 in both (Finding 6 + D-7.5-11)
19. `grep "carried_forward" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11 classifier)
20. `grep "confirmed_dropped" app/pipeline/orchestrator.py` — exits 0 (D-7.5-11 classifier + overpay guard comment)
21. `grep "class ClarifiedFields" app/models/contracts.py` — exits 0
22. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
23. `grep "clarified_fields.*JSONB" app/db/schema.sql` — exits 0
24. `grep -c "clarification_field_regression" app/db/schema.sql` — 3+ occurrences
25. `grep "contype = 'c'" app/db/schema.sql` — exits 0 (Finding 9 defensive matcher)
26. `grep "def set_pre_clarify_extracted\|def load_pre_clarify_extracted\|def set_clarified_fields\|def load_clarified_fields" app/db/repo.py` — 4 hits
27. `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` — exits 0
28. `grep "clarification_field_regression" app/db/repo.py` — exits 0 (allowlist)
29. `grep "ClarifiedFields" app/db/repo.py` — exits 0 (typed-on-write validation)
30. `grep "def _clarify" app/pipeline/orchestrator.py` — shows `extracted` param and `purpose=` kwarg
31. `grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py` — 3+ (N7 all 3 paths)
32. `grep "has_field_regression\|any.*field_regression" app/pipeline/orchestrator.py` — shows any() not all() (R3-2)
33. `grep "clarification_field_regression" app/pipeline/orchestrator.py` — exits 0
34. `grep "set_clarified_fields" app/pipeline/orchestrator.py` — exits 0 (N2 Round-1 + D-7.5-11 Round-2)
35. AST N1 check: `_run_stages calls: 1` in resume_pipeline
36. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` — exits 0
37. `uv run pytest -q` — exits 0
38. Human checkpoint: bootstrap applied, columns exist, D-7.5-11 mechanism verified (extracted= kwarg, suppress_detection, classify-first scoped to asked fields, set_clarified_fields after _run_stages), D-7.5-10 three-phase ordering (detect < backfill < validate < calc), R2-2 (prior_matches third arg in backfill call), R3-3 prior_matches=prior_matches at call sites, D-14 no id_to_submitted_name, N1 AST 1, R3-2 any(), N7 grep 3+, N4 INSERT passes (rolled back), schema columns exist
</verification>

<success_criteria>
- D-7.5-11 CLASSIFY-FIRST on Round-2: extract() called once up front → raw_extracted → classify asked fields from raw (scoped to asked fields only) → suppress_detection = prior terminals UNION newly_classified → _run_stages called ONCE with extracted=raw_extracted + resolved_drops=suppress_detection → run reaches AWAITING_APPROVAL in a single pass. No post-decide reclassification.
- D-7.5-11 explicit-zero overpay guard: confirmed_dropped in suppress_detection prevents re-backfill of Decimal('0') fields (_is_paid(0) is False by value; the resolved_drops gate protects it). Client who explicitly zeroed OT: paystub OT=0 (NOT re-backfilled to snapshot OT=2).
- D-7.5-11 no double-extraction: _run_stages extracted= kwarg skips internal extract() when caller supplies pre-extracted. Round-1 and run_pipeline unaffected (no extracted kwarg → internal extract() as today).
- D-7.5-10 THREE-PHASE ORDERING in _run_stages: detect_field_regression(prior, RAW extracted, prior_matches, matches) BEFORE backfill_extracted(extracted, prior, PRIOR_MATCHES, matches, resolved_drops) BEFORE validate(BACKFILLED extracted, raw_field_drops=drops) BEFORE _compute_line_items.
- R2-2 FIX: backfill_extracted is employee_id-keyed on BOTH sides. prior_matches resolves snapshot employees. 'M. Chen' in snapshot + 'Maria Chen' in reply, same employee_id → carry-forward fires.
- R3-3 fix: prior_matches deserialized from _pre_reconciliation and passed as prior_matches=prior_matches to _run_stages.
- D-14 + KEY TYPE: suppress_detection and _resolved_by_name built from clarified.items() emp_id_str keys (no reverse-lookup). (employee_id_str, field) key type consistent with Plan 02 N8 str(current_emp_id).
- ClarifiedFields with 4 outcomes: 'carried_forward' docstring says raw None/absent (D-7.5-10b/D-7.5-11); 'confirmed_dropped' docstring notes overpay guard.
- Two JSONB columns, DO $$ N4 migration (atomic DROP+RE-ADD), four repo helpers, allowlist expanded.
- _clarify: extracted param + purpose kwarg + set_pre_clarify_extracted on all 3 paths (N7).
- _run_stages returns _RunStagesResult; defers when ANY field_regression (R3-2).
- Round-1: single _run_stages call (N1), asked-before-send (N2), unchanged by D-7.5-11.
- Existing test suites green; schema applied to dev DB; human checkpoint confirms all invariants.
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-03-SUMMARY.md` when done.
</output>
