---
phase: 07.5-clarification-reply-field-regression
plan: "03"
type: execute
wave: 3
depends_on:
  - "07.5-01"
  - "07.5-02"
files_modified:
  - app/db/schema.sql
  - app/db/repo.py
  - app/models/contracts.py
  - app/pipeline/orchestrator.py
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Schema bootstrap must be applied to the live dev Supabase DB so new JSONB columns (pre_clarify_extracted, clarified_fields) AND the updated purpose CHECK constraint exist before integration tests run in Plan 04"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Supavisor transaction mode, port 6543)"
    dashboard_config:
      - task: "Confirm DATABASE_URL is set in local shell env (not just .env) before the checkpoint"
        location: "local shell — run: export DATABASE_URL=<pooler connection string>"

must_haves:
  truths:
    - "ClarifiedFields Pydantic model exists in contracts.py with ConfigDict(extra='forbid') and a nested structure: {employee_id_str: {field: ClarifiedFieldOutcome}} where ClarifiedFieldOutcome = Literal['asked','carried_forward','confirmed_dropped'] — typed on every write per D-7.5-03b"
    - "schema.sql has pre_clarify_extracted JSONB and clarified_fields JSONB inline in payroll_runs CREATE TABLE and in the ADD COLUMN IF NOT EXISTS ALTER block"
    - "schema.sql has a DO $$ block that DROPs the old email_messages purpose CHECK constraint (by name from pg_constraint) and RE-ADDs with the expanded value list ('clarification','confirmation','clarification_field_regression') IN ONE TRANSACTION (D-7.5-03a atomic DROP+ADD)"
    - "schema.sql inline email_messages purpose CHECK and ALTER TABLE purpose ADD COLUMN are both updated to include 'clarification_field_regression'"
    - "app/db/repo.py has 4 new helpers: set_pre_clarify_extracted (IS NULL CAS, snapshot-once D-19), load_pre_clarify_extracted, set_clarified_fields (validates via ClarifiedFields model before serializing), load_clarified_fields (returns {} on NULL)"
    - "get_outbound_message_id() allowlist expanded to include 'clarification_field_regression'"
    - "_clarify() gains extracted: Extracted positional parameter and purpose: str = 'clarification' kwarg; all three hardcoded 'clarification' strings inside _clarify are replaced with the purpose variable"
    - "set_pre_clarify_extracted is called BEFORE EACH of the three set_status(AWAITING_REPLY) paths in _clarify (N7 fix); IS NULL guard in SQL makes all three calls idempotent (snapshot-once)"
    - "resume_pipeline contains the Round-1 / Round-2 detection block: loads snapshot + clarified; calls _run_stages EXACTLY ONCE; on Round 1 with clarify_deferred detection writes asked BEFORE calling _clarify; on Round 2 classifies outcomes from raw extracted BEFORE backfill and applies carried_forward backfill inline"
    - "_run_stages gains clarify_deferred bool return: returns (None, clarify_deferred_bool) — or a simple dataclass — so resume_pipeline can inspect whether _run_stages deferred the clarification call (per D-7.5-02 DISCARD note: NOT the StageResult-returns-data model from the seed; a simpler shape that does not persist before returning)"
    - "uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q exits 0 — existing suites stay green"
  artifacts:
    - path: "app/models/contracts.py"
      provides: "ClarifiedFields Pydantic model (typed outcome validator)"
      contains: "class ClarifiedFields"
    - path: "app/db/schema.sql"
      provides: "pre_clarify_extracted + clarified_fields columns + N4 DO $$ purpose CHECK migration"
      contains: "clarification_field_regression"
    - path: "app/db/repo.py"
      provides: "4 new JSONB helpers + allowlist expansion"
      exports: ["set_pre_clarify_extracted", "load_pre_clarify_extracted", "set_clarified_fields", "load_clarified_fields"]
    - path: "app/pipeline/orchestrator.py"
      provides: "_clarify extracted param + purpose kwarg + N7 snapshot + resume_pipeline Round-1/Round-2 block + single _run_stages call invariant"
      contains: "clarification_field_regression"
  key_links:
    - from: "app/pipeline/orchestrator.py:resume_pipeline (Round 1)"
      to: "app/db/repo.py:set_clarified_fields"
      via: "writes asked BEFORE calling _clarify (N2 ordering, asked-before-send)"
      pattern: "set_clarified_fields"
    - from: "app/pipeline/orchestrator.py:_clarify"
      to: "app/db/repo.py:set_pre_clarify_extracted"
      via: "called BEFORE EACH of the 3 AWAITING_REPLY set_status calls; IS NULL guard = snapshot-once"
      pattern: "set_pre_clarify_extracted"
    - from: "app/models/contracts.py:ClarifiedFields"
      to: "app/db/repo.py:set_clarified_fields"
      via: "every write validates shape through ClarifiedFields before json.dumps — typed-on-write D-7.5-03b"
      pattern: "ClarifiedFields"
---

<objective>
Wire the MONEY-03 two-inbound state machine: schema DDL (two JSONB columns + N4 purpose CHECK migration), ClarifiedFields typed model, four repo helpers, _clarify snapshot + purpose kwarg, and the resume_pipeline Round-1/Round-2 detection block.

This plan closes:
- N4: schema.sql gets an explicit DO $$ block that DROPS the existing email_messages purpose CHECK and RE-ADDS it with 'clarification_field_regression' in ONE TRANSACTION (D-7.5-03a).
- N7: set_pre_clarify_extracted (IS NULL CAS) is called before each of the THREE AWAITING_REPLY set_status paths in _clarify (idempotency/snapshot-once D-19).
- N2: asked-before-send ordering — resume_pipeline writes clarified_fields "asked" entry DURABLY before triggering _clarify.
- N3: Round-2 outcome classification uses RAW extracted BEFORE any backfill; carried_forward backfill injected in the in-memory seam.
- Single-_run_stages-call invariant (N1): resume_pipeline calls _run_stages exactly once.

Critical design decision (D-7.5-01a / D-7.5-02 DISCARD): this plan does NOT use the seed's StageResult-returns-data model. Instead, _run_stages returns a lightweight `(clarify_deferred: bool)` signal only (via a simple dataclass or named tuple) — it still executes all side effects internally (persist + branch). The N2 fix uses clarify_deferred=True to signal that _run_stages detected a field-regression-only clarification and deferred the send; resume_pipeline then writes asked and calls _clarify directly. This avoids R3-1 (post-return backfill too late) by injecting the backfill inline in _run_stages' in-memory seam (between reconcile and validate), which Plan 01 already established.

Note: The schema plan requires a live DB apply step — this plan is `autonomous: false`. The blocking checkpoint verifies the migration, confirms the DATABASE_URL gate, and runs bootstrap before Plan 04's integration tests.

Output: full two-inbound state machine wired; existing test suites green; schema applied to dev DB.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-01-SUMMARY.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-02-SUMMARY.md

@app/db/schema.sql
@app/db/repo.py
@app/models/contracts.py
@app/pipeline/orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClarifiedFields model + schema DDL (N4 migration) + repo helpers + allowlist</name>
  <files>app/models/contracts.py, app/db/schema.sql, app/db/repo.py</files>

  <read_first>
  Read app/models/contracts.py. Add ClarifiedFields AFTER FieldDrop (which ends around line 146) and AFTER RawFieldDrop (added in Plan 02). ClarifiedFields is the typed-on-write validator for the clarified_fields JSONB column (D-7.5-03b). Shape: a validated dict wrapper. Model the inner outcome as a Literal type alias. All existing imports (UUID, Decimal, Literal, BaseModel, ConfigDict) should already be present — verify before adding.

  Read app/db/schema.sql in full. Key anchors:
  - Line 90: alias_candidates JSONB — add pre_clarify_extracted JSONB and clarified_fields JSONB on the next two lines (inline for fresh DBs)
  - Lines 105-108: ADD COLUMN IF NOT EXISTS block — add two lines for the new columns after line 108
  - Line 147: purpose inline CHECK — update to include 'clarification_field_regression'
  - Lines 164-165: purpose ALTER TABLE ADD COLUMN — update CHECK to include 'clarification_field_regression'
  - Lines 169-184: DO $$ uq_email_run_purpose block — THIS IS THE EXACT PATTERN to copy for the N4 DROP+RE-ADD block; add the N4 block BEFORE this existing block (i.e. after line 165 but before line 169)

  Read app/db/repo.py lines 85-100 (imports + RUN_COLS), 340-375 (claim_status CAS — the IS NULL RETURNING id pattern), 405-430 (persist_extracted — serialization with model_dump(mode="json")), 505-530 (set_alias_candidates — EXACT template for the new helpers), 659-686 (get_outbound_message_id — the allowlist to expand).

  PATTERNS.md §5 and §6 have the exact SQL and helper implementations. Critical:
  - RUN_COLS (repo.py:90-93) does NOT include alias_candidates. Follow the same pattern — do NOT add pre_clarify_extracted or clarified_fields to RUN_COLS; load them via separate targeted helpers.
  - All SQL uses %s placeholders and str(run_id) for UUID params (V5 security rule). No f-string SQL.
  - JSONB serialization: Extracted objects use json.dumps(extracted.model_dump(mode="json")); plain dicts use json.dumps(the_dict); read-backs use json.loads(row[0]) if isinstance(row[0], str) else row[0].
  - set_pre_clarify_extracted uses the RETURNING id CAS pattern from claim_status: `WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id` — atomic check-and-write, snapshot-once (D-19).
  - set_clarified_fields MUST validate the input through ClarifiedFields model before serializing (D-7.5-03b). Import ClarifiedFields from app.models.contracts inside set_clarified_fields (or at module top if no circular import issue).
  </read_first>

  <action>
  Step 1 — Add ClarifiedFields model to app/models/contracts.py:

  After FieldDrop and RawFieldDrop (roughly after line 146+RawFieldDrop lines), add:

  - Type alias: `ClarifiedFieldOutcome = Literal["asked", "carried_forward", "confirmed_dropped"]`
  - Class ClarifiedFields(BaseModel):
    - model_config = ConfigDict(extra="forbid")
    - outcomes: dict[str, dict[str, ClarifiedFieldOutcome]]
    - Docstring: "Typed validator for the clarified_fields JSONB column (D-7.5-03b). Shape: {employee_id_str: {field_name: outcome}}. Validate on every write — a mislabeled carried_forward->confirmed_dropped silently underpays."
    - Class method `from_dict(cls, d: dict) -> ClarifiedFields`: validates `outcomes=d`
    - Method `to_dict(self) -> dict`: returns `self.outcomes`

  Add `Literal` to the existing import from typing if not present; it may already be there.

  Step 2 — Update app/db/schema.sql:

  a. In CREATE TABLE IF NOT EXISTS payroll_runs, after `alias_candidates JSONB,` (~line 90), add two lines:
     `    pre_clarify_extracted JSONB,    -- D-19 MONEY-03: snapshot at awaiting_reply (IS NULL write-once guard)`
     `    clarified_fields      JSONB,    -- D-13 MONEY-03: {employee_id: {field: outcome}} field-regression outcomes`

  b. In the ADD COLUMN IF NOT EXISTS ALTER block (~lines 105-108), after the alias_candidates and record_only lines, add:
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS pre_clarify_extracted JSONB;  -- D-19 MONEY-03`
     `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields      JSONB;  -- D-13 MONEY-03`

  c. At line 147, update inline email_messages purpose CHECK from:
     `purpose          TEXT        CHECK (purpose IN ('clarification','confirmation')),`
     to:
     `purpose          TEXT        CHECK (purpose IN ('clarification','confirmation','clarification_field_regression')),`

  d. At lines 164-165, update the ALTER TABLE ADD COLUMN purpose CHECK to match:
     `ALTER TABLE email_messages ADD COLUMN IF NOT EXISTS purpose`
     `    TEXT CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'));`

  e. N4 DO $$ migration block — add BEFORE the existing uq_email_run_purpose block (after line 165, before line 169). This block runs in a single transaction, DROPs the old purpose CHECK constraint by introspecting its name from pg_constraint, and RE-ADDs with the expanded value list (D-7.5-03a atomic drop+add, explicit declaration):

     DO $$
     DECLARE
         _con_name TEXT;
     BEGIN
         -- Find the existing purpose CHECK constraint name
         SELECT conname INTO _con_name
         FROM pg_constraint
         WHERE contype = 'c'
           AND conrelid = 'email_messages'::regclass
           AND conname LIKE '%purpose%';
         -- Drop the old constraint if found (leaves constraint absent momentarily inside this transaction)
         IF _con_name IS NOT NULL THEN
             EXECUTE 'ALTER TABLE email_messages DROP CONSTRAINT ' || quote_ident(_con_name);
         END IF;
         -- Re-add with expanded value list (explicit declaration, not reconstructed text -- D-7.5-03a)
         IF NOT EXISTS (
             SELECT 1 FROM pg_constraint
             WHERE conname = 'email_messages_purpose_check'
               AND conrelid = 'email_messages'::regclass
         ) THEN
             ALTER TABLE email_messages ADD CONSTRAINT email_messages_purpose_check
                 CHECK (purpose IN ('clarification','confirmation','clarification_field_regression'));
         END IF;
     END;
     $$;

  Step 3 — Add four repo helpers to app/db/repo.py after set_alias_candidates (~line 524):

  a. set_pre_clarify_extracted(run_id: uuid.UUID, extracted: Extracted, conn=None) -> bool:
     - Serialize: json.dumps(extracted.model_dump(mode="json"))
     - SQL: UPDATE payroll_runs SET pre_clarify_extracted = %s, updated_at = now() WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id
     - Returns True if written (first write), False if already set (IS NULL CAS, snapshot-once D-19)
     - Import Extracted from app.models.contracts — check if it is importable at module level without circular import; if so add to top-level imports; if not, use local import inside the function.

  b. load_pre_clarify_extracted(run_id: uuid.UUID, conn=None) -> Extracted | None:
     - SQL: SELECT pre_clarify_extracted FROM payroll_runs WHERE id = %s
     - If row is None or row[0] is None: return None
     - Deserialize: Extracted.model_validate(json.loads(row[0]) if isinstance(row[0], str) else row[0])

  c. set_clarified_fields(run_id: uuid.UUID, clarified: dict, conn=None) -> None:
     - FIRST validate through ClarifiedFields model: `from app.models.contracts import ClarifiedFields; ClarifiedFields(outcomes=clarified)` — raises ValidationError if shape is wrong (D-7.5-03b; import locally if needed to avoid circular)
     - Serialize: json.dumps(clarified)
     - SQL: UPDATE payroll_runs SET clarified_fields = %s, updated_at = now() WHERE id = %s
     - Docstring must note: "D-7.5-03b: shape validated through ClarifiedFields before persisting — a mislabeled carried_forward->confirmed_dropped silently underpays."

  d. load_clarified_fields(run_id: uuid.UUID, conn=None) -> dict:
     - SQL: SELECT clarified_fields FROM payroll_runs WHERE id = %s
     - Returns {} on NULL (not None — callers iterate the dict)
     - Deserialize: json.loads(row[0]) if isinstance(row[0], str) else row[0]

  All four helpers use the _conn_ctx / _nulltx pattern from set_alias_candidates. Always %s and str(run_id).

  Step 4 — Expand get_outbound_message_id allowlist (~line 670):
  Change: `if purpose not in ("clarification", "confirmation"):`
  To: `if purpose not in ("clarification", "confirmation", "clarification_field_regression"):`

  Step 5 — Verify imports (dry run, no DB needed yet):
  uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; from app.models.contracts import ClarifiedFields; print('ALL OK')"
  </action>

  <verify>
    <automated>uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields, get_outbound_message_id; from app.models.contracts import ClarifiedFields; print('imports OK')" 2>&1</automated>
  </verify>

  <acceptance_criteria>
  - `grep "class ClarifiedFields" app/models/contracts.py` exits 0
  - `grep "ClarifiedFieldOutcome" app/models/contracts.py` exits 0 (the Literal type alias)
  - `grep "pre_clarify_extracted JSONB" app/db/schema.sql` exits 0 (inline column)
  - `grep "clarified_fields.*JSONB" app/db/schema.sql` exits 0
  - `grep -c "clarification_field_regression" app/db/schema.sql` returns 3+ (inline CHECK + ALTER + DO $$ migration)
  - `grep "DO \$\$" app/db/schema.sql | wc -l` returns 2 (the new N4 block AND the existing uq_email_run_purpose block)
  - `grep "def set_pre_clarify_extracted\|def load_pre_clarify_extracted\|def set_clarified_fields\|def load_clarified_fields" app/db/repo.py` exits 0 (all four helpers)
  - `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` exits 0 (IS NULL CAS guard)
  - `grep "clarification_field_regression" app/db/repo.py` exits 0 (allowlist expansion)
  - `grep "ClarifiedFields" app/db/repo.py` exits 0 (typed-on-write validation in set_clarified_fields)
  - `uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.models.contracts import ClarifiedFields; print('OK')"` exits 0
  </acceptance_criteria>

  <done>ClarifiedFields model exists. Four repo helpers importable. Schema updated with new columns + N4 migration block. Allowlist expanded. All imports clean.</done>
</task>

<task type="auto">
  <name>Task 2: _clarify N7 snapshot+purpose kwarg + _run_stages clarify_deferred + resume_pipeline Round-1/Round-2 block</name>
  <files>app/pipeline/orchestrator.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py in full in ONE PASS. Extract every anchor needed:

  _run_stages (lines 260-290):
  - Line 260: signature `def _run_stages(run_id, email, roster, *, llm, prior=None, resolved_drops=None) -> None` (after Plan 01)
  - Lines 269-275: four pure stages
  - Lines 278-289: persist block + branch — the clarify branch at line 289 calls `_clarify(run_id, email, decision, roster, llm=llm)` — this must gain `extracted` + `purpose` args

  _clarify (lines 292-451):
  - Line 292: current signature `def _clarify(run_id, email, decision, roster, *, llm) -> None`
  - Line 318: `repo.get_outbound_message_id(run_id, purpose="clarification")` — gains purpose= variable
  - Line 325: first `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 1 (idempotency early-return)
  - Line 435: `purpose="clarification"` inside repo.insert_email_message — gains variable
  - Line 438: second `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 2 (record_only)
  - Line 448: `purpose="clarification"` inside gateway.send_outbound — gains variable
  - Line 451: third `repo.set_status(run_id, RunStatus.AWAITING_REPLY)` — PATH 3 (live gateway)

  resume_pipeline (lines 94-241):
  - Line 124: claim_status CAS — DO NOT TOUCH (D-22)
  - Line 153-162: STEP A pre-resume alias capture — MUST run FIRST before any _run_stages call
  - Line 164-165: STEP B comment + current single `_run_stages(run_id, combined_email, roster, llm=llm)` call — this is where the Round-1/Round-2 detection block replaces the old simple call
  - Lines 167-234: STEP C/D alias-diff — post-reconciliation currently read from load_run; refactor to use the clarify_deferred result's matches if available, OR keep the existing DB read (simpler — the alias-diff already reads from DB after _run_stages writes reconciliation)
  - Lines 235-241: except boundary — ALL new code stays inside here

  Imports block (lines 35-57): confirm detect_field_regression and _is_paid are importable from app.pipeline.validate (add to imports). Add Decimal from decimal. Add dataclass from dataclasses if using a dataclass return.

  CRITICAL N1 invariant: resume_pipeline calls _run_stages EXACTLY ONCE per invocation. The Round-1 path calls it once and returns after writing asked + calling _clarify. The Round-2 path calls it once and continues to alias-diff. No second call anywhere.

  CRITICAL ordering in resume_pipeline after this task:
  1. combined_email build
  2. STEP A (pre-state capture: _pre_candidates, _pre_reconciliation, _pre_resolved_ids) — UNCHANGED, runs FIRST
  3. Round-1/Round-2 detection block (load snapshot + clarified; single _run_stages call here)
  4. STEP C/D alias-diff (post-reconciliation from DB load as today, OR from detection block result)
  </read_first>

  <action>
  Change A — Add _RunStagesResult lightweight container near top of orchestrator.py (after imports, before run_pipeline):

  Add a dataclass or NamedTuple (NOT Pydantic — transient internal value):
  from dataclasses import dataclass (add to imports if not present)

  @dataclass
  class _RunStagesResult:
      """Lightweight return value from _run_stages (MONEY-03 D-7.5-02).
      clarify_deferred=True means: all clarification issues are field_regression type
      and _run_stages deliberately deferred calling _clarify so resume_pipeline can
      write 'asked' to clarified_fields BEFORE triggering the send (N2 ordering).
      """
      clarify_deferred: bool = False
      extracted: object = None   # type: Extracted — avoids forward-ref import issue; typed loosely here

  Change B — Update _run_stages:

  1. Change return type annotation to `-> _RunStagesResult` (or `-> None` with a return statement — either is fine; returning the dataclass is cleaner for resume_pipeline to consume without re-reading the DB).

  2. In the else branch (request_clarification, line ~289):
     - Check whether ALL issues are field_regression type:
       `all_field_regression = bool(issues) and all(i.issue_type == "field_regression" for i in issues)`
     - If all_field_regression is True: set `clarify_deferred = True`. Do NOT call _clarify. The caller (resume_pipeline) will write asked then call _clarify with the correct purpose.
     - If all_field_regression is False: set `clarify_deferred = False`. Call `_clarify(run_id, email, decision, roster, extracted, llm=llm)` as today (with the new extracted positional arg from Change C).

  3. On the process branch: `clarify_deferred = False`.

  4. At the end: `return _RunStagesResult(clarify_deferred=clarify_deferred, extracted=extracted)`

  5. Update run_pipeline call site (line ~91): `_ = _run_stages(run_id, email, roster, llm=llm)` (discard return — behavior unchanged for run_pipeline).

  Change C — Update _clarify: add extracted positional param + purpose kwarg + N7 snapshot on 3 paths:

  1. New signature: `def _clarify(run_id, email, decision, roster, extracted, *, llm, purpose="clarification") -> None:`

  2. Replace ALL THREE hardcoded "clarification" strings with the `purpose` variable:
     - Line 318: `repo.get_outbound_message_id(run_id, purpose=purpose)`
     - Line 435: `purpose=purpose` in repo.insert_email_message call
     - Line 448: `purpose=purpose` in gateway.send_outbound call

  3. N7 fix — add `repo.set_pre_clarify_extracted(run_id, extracted)` IMMEDIATELY BEFORE each of the THREE set_status(AWAITING_REPLY) calls:
     - Before line 325 (PATH 1 idempotency early-return): add the call before `repo.set_status(run_id, RunStatus.AWAITING_REPLY)`
     - Before line 438 (PATH 2 record_only): add the call before `repo.set_status(run_id, RunStatus.AWAITING_REPLY)`
     - Before line 451 (PATH 3 live gateway): add the call before `repo.set_status(run_id, RunStatus.AWAITING_REPLY)`
  The IS NULL guard in SQL makes all three calls idempotent — only the first write succeeds (snapshot-once D-19).

  Change D — Add Round-1/Round-2 detection block in resume_pipeline:

  STEP A (lines 153-162) remains UNCHANGED and runs FIRST (before _run_stages).

  Replace old STEP B (lines 164-165, the bare `_run_stages(...)` call) with the following detection block. All code stays inside the existing except boundary (lines 235-241).

  Step 1: load snapshot and clarified state:
  `snapshot = repo.load_pre_clarify_extracted(run_id)   # None on first resume`
  `clarified = repo.load_clarified_fields(run_id)         # {} on first resume`

  Step 2: build _asked_fields and _confirmed_drops from clarified:
  `_asked_fields = {(str(emp_id), field): outcome for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "asked"}`
  `_confirmed_drops: set = {(emp_id, field) for emp_id, fields in clarified.items() for field, outcome in fields.items() if outcome == "confirmed_dropped"}`
  (Note: _confirmed_drops uses string emp_id keys to match the dict structure; convert to UUID when passing to resolved_drops if validate() requires UUID)

  Step 3: Round-1 path (no asked fields — first resume or non-field-regression resume):
  If _asked_fields is empty:
  - `stage = _run_stages(run_id, combined_email, roster, llm=llm, prior=snapshot, resolved_drops=_confirmed_drops or None)`
  - If stage.clarify_deferred is True (all issues are field_regression, _clarify was deferred):
    - Build name_to_id_current from post-reconciliation: read from DB via `post_run_data = repo.load_run(run_id)` and build the map from the freshly-written reconciliation column
    - For each raw_drop in detect_field_regression(snapshot, stage.extracted) if snapshot is not None:
      - Look up current_emp_id = name_to_id_current.get(raw_drop.submitted_name)
      - If current_emp_id is not None: clarified.setdefault(str(current_emp_id), {})[raw_drop.field] = "asked"
    - Write asked durably: `repo.set_clarified_fields(run_id, clarified)`  (N2: asked written BEFORE send)
    - THEN call: `_clarify(run_id, combined_email, stage.decision, roster, stage.extracted, llm=llm, purpose="clarification_field_regression")`
    - RETURN (do not continue to alias-diff)
  - If stage.clarify_deferred is False: fall through to STEP C/D alias-diff (normal path)

  Step 4: Round-2 path (asked fields present — client answered a field-regression clarification):
  If _asked_fields is non-empty:
  - `stage = _run_stages(run_id, combined_email, roster, llm=llm, prior=None, resolved_drops=_confirmed_drops or None)`
    (prior=None on Round 2: backfill already applied inline in _run_stages before validate; the round-2 detection is handled via clarified dict below)
  - N3 FIX: classify outcomes from stage.extracted BEFORE any manual backfill:
    - Build name_to_id_current from DB (load_run reconciliation) or from the most recent reconcile call
    - For each (emp_id_str, field) in _asked_fields:
      - Find emp in stage.extracted.employees that matches emp_id_str via the current name_to_id_current map
      - raw_value = getattr(emp, field) if emp found else None
      - If _is_paid(raw_value): outcome = "carried_forward" (client answered; field is paid — no backfill needed since already in extraction)
      - Elif raw_value is not None and raw_value == Decimal("0"): outcome = "confirmed_dropped" (explicit zero per D-14)
      - Else (raw_value is None — silence): outcome = "carried_forward" (must backfill from snapshot)
    - Backfill carried_forward fields where raw_value was None:
      For each (emp_id_str, field) classified as carried_forward where raw_value is None:
      - Find emp in stage.extracted.employees; find snapshot_emp by name_to_id match
      - If snapshot_emp and _is_paid(getattr(snapshot_emp, field)): setattr(found_emp, field, getattr(snapshot_emp, field))
      (Backfill happens to stage.extracted in memory — the in-memory seam between extract and validate is Plan 01's work)
    - Persist the backfilled extraction: `repo.persist_extracted(run_id, stage.extracted)` (overwrites the post-reconcile write)
    - Update clarified with final outcomes; call `repo.set_clarified_fields(run_id, clarified)`
    - Fall through to STEP C/D alias-diff

  STEP C/D alias-diff (lines 167-234) remains as today — reading from DB via load_run after _run_stages has written reconciliation. No change needed here.

  Add imports at the top of orchestrator.py: `from app.pipeline.validate import detect_field_regression, _is_paid` and `from decimal import Decimal` (add if not present). Add `from dataclasses import dataclass` (if using the dataclass approach for _RunStagesResult). Add `from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields` (or access via `repo.` if they are already module-level on repo).

  After implementing, run:
  uv run python -c "from app.pipeline.orchestrator import _run_stages, resume_pipeline, _clarify, _RunStagesResult; print('OK')"
  uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q
  </action>

  <verify>
    <automated>uv run python -c "from app.pipeline.orchestrator import _run_stages, resume_pipeline, _clarify; print('OK')" && uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "def _clarify" app/pipeline/orchestrator.py` shows `extracted` positional param and `purpose="clarification"` default kwarg
  - `grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py` returns 3+ (one before each AWAITING_REPLY path in _clarify — N7)
  - `grep "clarify_deferred" app/pipeline/orchestrator.py` exits 0 (the branching logic in _run_stages else-branch)
  - `grep "clarification_field_regression" app/pipeline/orchestrator.py` exits 0 (the Round-1 _clarify call with purpose=)
  - `grep "set_clarified_fields" app/pipeline/orchestrator.py` exits 0 (N2 asked-before-send write in Round-1 path)
  - AST check (N1 single-call invariant): `python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(f'_run_stages calls: {len([n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(n.func,\"id\",None)==\"_run_stages\"])}') for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='resume_pipeline']"` prints `_run_stages calls: 1`
  - `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` exits 0 — existing suites green
  - `uv run pytest -q` full suite exits 0
  </acceptance_criteria>

  <done>_clarify has extracted param, purpose kwarg, and set_pre_clarify_extracted on all 3 AWAITING_REPLY paths (N7). _run_stages returns _RunStagesResult with clarify_deferred. resume_pipeline has the Round-1/Round-2 detection block with a single _run_stages call. Existing test suites green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1: ClarifiedFields model, schema DDL (two new JSONB columns + N4 purpose CHECK DO $$ migration), four repo helpers, allowlist expansion.
  Task 2: _clarify gains extracted param + purpose kwarg + N7 snapshot on 3 paths. _run_stages returns _RunStagesResult with clarify_deferred. resume_pipeline Round-1/Round-2 detection block with single _run_stages call (N1).
  </what-built>
  <how-to-verify>
  Requires DATABASE_URL set in shell env (Supavisor transaction mode port 6543).

  1. Verify imports:
     uv run python -c "from app.db.repo import set_pre_clarify_extracted, load_pre_clarify_extracted, set_clarified_fields, load_clarified_fields; from app.models.contracts import ClarifiedFields; from app.pipeline.orchestrator import _clarify, resume_pipeline; print('ALL IMPORTS OK')"

  2. Apply schema to dev DB:
     uv run python -m app.db.bootstrap
     Verify new columns exist:
     uv run python -c "
     import psycopg, os
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     cur.execute(\"SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name IN ('pre_clarify_extracted','clarified_fields')\")
     cols = [r[0] for r in cur.fetchall()]
     assert 'pre_clarify_extracted' in cols, 'pre_clarify_extracted MISSING'
     assert 'clarified_fields' in cols, 'clarified_fields MISSING'
     cur.execute(\"SELECT check_clause FROM information_schema.check_constraints WHERE constraint_name LIKE '%purpose%' OR constraint_name LIKE '%email_messages%'\")
     rows = cur.fetchall()
     assert any('clarification_field_regression' in str(r) for r in rows), f'N4 constraint not applied: {rows}'
     print('Schema checks PASSED')
     conn.close()
     "

  3. Verify N4 insert test (rolled back):
     uv run python -c "
     import psycopg, os, uuid
     conn = psycopg.connect(os.environ['DATABASE_URL'])
     cur = conn.cursor()
     test_id = str(uuid.uuid4())
     cur.execute(\"INSERT INTO email_messages (id, run_id, direction, message_id, purpose) VALUES (%s, NULL, 'outbound', %s, 'clarification_field_regression')\", (test_id, '<n4-test@test.local>'))
     conn.rollback()
     print('N4 INSERT PASSED (rolled back)')
     conn.close()
     "

  4. Verify N7 (snapshot on 3 paths):
     grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py
     (should print 3 or more)

  5. Verify N1 (single _run_stages call in resume_pipeline):
     python3 -c "
     import ast
     src = open('app/pipeline/orchestrator.py').read()
     tree = ast.parse(src)
     for fn in ast.walk(tree):
         if isinstance(fn, ast.FunctionDef) and fn.name == 'resume_pipeline':
             calls = [n for n in ast.walk(fn) if isinstance(n, ast.Call) and getattr(getattr(n, 'func', None), 'id', None) == '_run_stages']
             print(f'_run_stages calls in resume_pipeline: {len(calls)}')
             assert len(calls) == 1, f'N1 VIOLATED: expected 1, got {len(calls)}'
             print('N1 PASSED')
     "

  6. Full test suite:
     uv run pytest -q (skip is acceptable for DB-dependent tests if DATABASE_URL not set — but it IS set here so all should run)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass and N4 insert test succeeds, or describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clarified_fields JSONB writes | Written via json.dumps with %s placeholders through ClarifiedFields validator — no f-string SQL injection |
| pre_clarify_extracted IS NULL guard | Atomic check-and-write in SQL — no Python read-then-write race (snapshot-once D-19) |
| email_messages.purpose CHECK | DB-enforced allowlist now includes 'clarification_field_regression'; unexpected purpose raises constraint violation at INSERT |
| N4 DO $$ migration block | Runs in a single transaction — failure cannot leave the constraint dropped-but-not-re-added (D-7.5-03a) |
| resume_pipeline new code | All new code inside the existing except boundary (lines 235-241); PII-safe error boundary preserved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-03-01 | Tampering | clarified_fields JSONB via set_clarified_fields | mitigate | json.dumps(clarified) with %s placeholder; ClarifiedFields.model_validate validates shape before serializing — a mislabeled outcome raises ValidationError before reaching the DB (D-7.5-03b). |
| T-7.5-03-02 | Tampering | pre_clarify_extracted IS NULL guard | mitigate | Guard is in SQL: WHERE id = %s AND pre_clarify_extracted IS NULL RETURNING id — atomic check-and-write, no read-then-write race (snapshot-once D-19). |
| T-7.5-03-03 | Tampering | N4 DROP+ADD in DO $$ block | mitigate | Whole block is one transaction; failure leaves old constraint in place (not dropped). Explicit declaration of the new CHECK (not text-reconstruction) per D-7.5-03a. |
| T-7.5-03-04 | Spoofing | New purpose value 'clarification_field_regression' in email_messages | mitigate | DB CHECK constraint (N4 migration) enforces the expanded list; get_outbound_message_id allowlist updated; both gates active. |
| T-7.5-03-05 | Tampering | Backfill in resume_pipeline Round-2 path | mitigate | Backfill only on carried_forward fields (raw_value is None — silence). confirmed_dropped (Decimal("0")) is never backfilled (D-14/N3). ClarifiedFields types the outcome before the field classification drives the backfill decision. |
| T-7.5-03-06 | Information Disclosure | PII in new resume_pipeline code | mitigate | All new code stays inside the existing except boundary (lines 235-241) which persists only type(exc).__name__. str(exc) never logged in new code. |
| T-7.5-03-07 | Elevation of Privilege | Operator approving a run while AWAITING_REPLY | accept | Structurally blocked: claim_status(AWAITING_APPROVAL, APPROVED) at main.py:492 cannot fire on an AWAITING_REPLY run — no override path (CONTEXT verified, D-7.5-04b). |
| T-7.5-03-08 | Denial of Service | set_pre_clarify_extracted called 3x per _clarify invocation (N7) | accept | IS NULL guard means only the first of the 3 calls writes; the other 2 return 0 rows. O(3) = O(1) per clarification cycle. |
| T-7.5-03-SC | Tampering | No new package installs | accept | No uv add invoked; psycopg + dataclasses already available. |
</threat_model>

<verification>
1. `grep "class ClarifiedFields" app/models/contracts.py` — exits 0
2. `grep "pre_clarify_extracted JSONB" app/db/schema.sql` — exits 0
3. `grep "clarified_fields.*JSONB" app/db/schema.sql` — exits 0
4. `grep -c "clarification_field_regression" app/db/schema.sql` — 3+ occurrences
5. `grep "def set_pre_clarify_extracted\|def load_pre_clarify_extracted\|def set_clarified_fields\|def load_clarified_fields" app/db/repo.py` — 4 hits
6. `grep "AND pre_clarify_extracted IS NULL RETURNING id" app/db/repo.py` — exits 0
7. `grep "clarification_field_regression" app/db/repo.py` — exits 0 (allowlist)
8. `grep "ClarifiedFields" app/db/repo.py` — exits 0 (typed-on-write validation)
9. `grep "def _clarify" app/pipeline/orchestrator.py` — shows `extracted` param and `purpose=` kwarg
10. `grep -c "set_pre_clarify_extracted" app/pipeline/orchestrator.py` — 3+ (N7 all 3 paths)
11. `grep "clarify_deferred" app/pipeline/orchestrator.py` — exits 0
12. `grep "clarification_field_regression" app/pipeline/orchestrator.py` — exits 0
13. `grep "set_clarified_fields" app/pipeline/orchestrator.py` — exits 0 (N2 asked-before-send)
14. AST N1 check: `python3 -c "..."` (see acceptance_criteria) prints `_run_stages calls: 1`
15. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py -q` — exits 0
16. `uv run pytest -q` — exits 0
17. Human checkpoint: bootstrap applied, columns exist, N4 INSERT passes (rolled back), N7 grep 3+, N1 AST 1
</verification>

<success_criteria>
- ClarifiedFields model typed with ClarifiedFieldOutcome Literal; validates shape on every write (D-7.5-03b)
- Two JSONB columns in schema.sql (inline + ALTER) and DO $$ N4 migration block (atomic DROP+RE-ADD, D-7.5-03a)
- Four repo helpers: set_pre_clarify_extracted (IS NULL CAS), load_pre_clarify_extracted, set_clarified_fields (typed-on-write), load_clarified_fields
- get_outbound_message_id allowlist includes 'clarification_field_regression'
- _clarify: extracted positional param + purpose="clarification" kwarg + set_pre_clarify_extracted on all 3 AWAITING_REPLY paths (N7 snapshot-once)
- _run_stages: returns _RunStagesResult with clarify_deferred; defers _clarify when all issues are field_regression type
- resume_pipeline: STEP A before detection block; single _run_stages call (N1); Round-1 writes asked before send (N2); Round-2 classifies from raw extraction then backfills carried_forward in-memory (N3)
- Existing test suites green; schema applied to dev DB; human checkpoint confirms N4 + new columns + N1/N7 invariants
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-03-SUMMARY.md` when done.
</output>
