# Phase 7.5: Clarification-Reply Field-Regression - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-28
**Phase:** 7.5-clarification-reply-field-regression
**Areas discussed:** Refactor seam / sequencing, Seed-plan handling, Schema migration, Test gate, Second-order-drop limitation

> **Pre-decided context:** This phase inherited an unusually complete design — 23 locked
> decisions (D-08..D-30 in 07-CONTEXT.md), three Codex cross-AI review rounds (07-REVIEWS.md),
> and three seed plans. The discussion deliberately did NOT re-litigate that locked design; it
> focused only on the genuinely-open 7.5 implementation choices.

---

## Refactor seam / sequencing

| Option | Description | Selected |
|--------|-------------|----------|
| Split as own plan, pinned first | Plan A = pure refactor (split `_run_stages` → two functions, branch moved out), update both callers, prove behavior-identical via existing suites, committed GREEN before any MONEY-03 code | ✓ |
| Split + detection in one plan | Combine refactor with `detect_field_regression` in one plan | |
| TDD characterization tests first | Pin current behavior with characterization tests, then split against the net | |

**User's choice:** Split as own plan, pinned first (recommended).
**Notes:** Matches ROADMAP SC1 and round-3 R3-1 (the seam must sit between reconcile and validate/decide/calc; a `StageResult`-returning single function that still branches internally is the superseded approach). Characterization-tests-first remains an allowed strengthening at Claude's discretion (existing orchestrator/clarify/persistence suites already serve as the behavior net).

---

## Seed-plan handling

| Option | Description | Selected |
|--------|-------------|----------|
| Design input only, replan fresh | Mine seeds for verified design, author fresh `-PLAN.md` on the split foundation, re-verify line numbers vs live source | ✓ |
| Adapt seeds in place | Edit seed files directly, rename to `-PLAN.md`, fix R3-1/R3-2/R3-3 inline | |

**User's choice:** Design input only, replan fresh (recommended).
**Notes:** Seeds carry the superseded `StageResult`-returns-data model and the R3-1/R3-2/R3-3 HIGH bugs. Reuse the verified design (two-inbound architecture, `RawFieldDrop`, N4/N5/N7/N8 fixes, fixtures 16/17/18); discard the post-return backfill, mixed-issue gap, and build-diff-by-name.

---

## Schema migration

| Option | Description | Selected |
|--------|-------------|----------|
| DO-block migration + live apply | Idempotent `DO $$`: ADD COLUMN IF NOT EXISTS for both JSONB cells + introspect-`pg_constraint`-DROP-and-re-ADD the purpose CHECK; applied to live dev Supabase before integration tests | ✓ |
| Plain ADD COLUMN, defer CHECK | Only ADD COLUMN; handle the purpose CHECK separately/later | |

**User's choice:** DO-block migration + live apply (recommended).
**Notes:** ADD COLUMN alone cannot alter an existing CHECK constraint (seed N4 fix). Plan 04 is `autonomous: false` with a `DATABASE_URL` user-setup gate.

---

## Test gate (D-23 two layers)

| Option | Description | Selected |
|--------|-------------|----------|
| Must PASS with live DB at phase gate | `skipif(not DATABASE_URL)` OK during dev; phase gate runs WITH DATABASE_URL so integration tests execute + pass; skip ≠ evidence | ✓ |
| Allow skip-guarded at gate | Accept skip-guarded integration tests at the gate (like Phase 1 live-DB round-trips) | |

**User's choice:** Must PASS with live DB at phase gate (recommended).
**Notes:** The state-machine claims (loop-guard, snapshot-once, confirmed_dropped, asked-before-send) are exactly what the import-`validate()` eval cannot see (D-23) — they must actually execute. Matches the live-test pattern from earlier phases (keys in shell env).

---

## Second-order-drop limitation (D-30)

| Option | Description | Selected |
|--------|-------------|----------|
| Accept the limitation | Keep the deliberate trade (guaranteed single-clarify termination over chasing drops inside a clarification answer); MONEY-01 backstops the all-zero-hours case; document clearly | ✓ |
| Scope a bounded one-more-round guard now | Add a bounded second round (generation counter) to catch a partial second-order drop once, then hard-stop | |

**User's choice:** Accept the limitation (recommended).
**Notes:** Re-affirmed for 7.5. A *partial* second-order drop (reply restores OT but omits holiday) slips through; MONEY-01's zero-hours gate still catches a reply that leaves an employee with NO paid hours. The bounded "one more round" guard is the natural extension if real usage demands it — deferred.

## Claude's Discretion

- Exact split function names/signatures and the inter-stage return shape.
- Whether to add an explicit Plan A0 characterization-test step before the split.
- Eval fixture authoring details (fixture 17 NFD honesty scope per seed Finding #8).

## Deferred Ideas

- Bounded "one more round" second-order-drop guard (generation counter on `clarified_fields`).
- Sum / explicit-aggregation multi-line reduction (vs locked last-wins, D-12).
- 401k / salary field-regression (D-08 excludes them).
- Full single-transaction wrapping of the resume sequence → Phase 9.
- Crash-idempotency + concurrency proof tests → Phase 9 / Phase 10.
