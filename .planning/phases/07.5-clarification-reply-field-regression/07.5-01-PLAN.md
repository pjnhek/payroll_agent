---
phase: 07.5-clarification-reply-field-regression
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/orchestrator.py
  - app/pipeline/validate.py
autonomous: true
requirements:
  - MONEY-03

must_haves:
  truths:
    - "_run_stages gains prior and resolved_drops kwargs — both default None and both are pure no-ops; all existing callers (run_pipeline, resume_pipeline) pass zero new arguments and observe identical DB writes, identical status transitions, and identical final-state on every path"
    - "validate() gains prior, prior_matches, and resolved_drops kwargs — all default None, all are no-ops; every existing validate() call site in the codebase passes only the original three positional args and still works correctly"
    - "The in-memory seam exists: the kwargs slots are positioned between reconcile_names and validate inside _run_stages, ready to receive backfill logic in Plan 02/03 without touching the persist block or callers"
    - "uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py tests/test_validate.py -q exits 0 — zero regressions from this pure structural change"
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "_run_stages with new no-op prior=/resolved_drops= kwargs"
      contains: "def _run_stages"
    - path: "app/pipeline/validate.py"
      provides: "validate() with new no-op prior=/prior_matches=/resolved_drops= kwargs"
      contains: "def validate"
  key_links:
    - from: "app/pipeline/orchestrator.py:_run_stages"
      to: "app/pipeline/validate.py:validate"
      via: "prior= and resolved_drops= kwargs threaded from _run_stages to validate call — currently None no-ops; Plan 02 wires real values"
      pattern: "prior="
---

<objective>
PLAN A — pure structural refactor that adds the in-memory seam required for the MONEY-03 carry-forward backfill.

This plan makes exactly one structural change: add `prior: Extracted | None = None`, `prior_matches: list | None = None`, and `resolved_drops: set | None = None` keyword-only arguments to `_run_stages` (and threads them into `validate()`), and the same three kwargs to `validate()` itself. Every default is `None`; every path behaves identically to today when no argument is passed. Zero feature code lands here.

Purpose: Establish the seam that allows Plan 02 to inject `detect_field_regression` results as `prior=` without touching callers or the persist block. The inline seam approach (D-7.5-01d) is chosen over the two-function split: it has zero change to the transaction/error boundary, is fully testable as a pure money function, and has the smaller blast radius. Declarative staged pipeline is a future option (D-7.5-01d deferred).

Decision D-7.5-01b verified: the four judgment stages (extract/reconcile/validate/decide) are already pure/in-memory; the first DB write is `persist_extracted` at line 278, AFTER decide. The new kwargs slot into the in-memory seam — between reconcile and validate — with NO new persists added between them. This plan must NOT worsen atomicity (D-7.5-01c): `_run_stages` today makes 5 autocommit writes; after this plan it makes the same 5 in the same order.

Output: `_run_stages` and `validate()` with no-op extension kwargs; existing test suites stay green.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/ROADMAP.md

@app/pipeline/orchestrator.py
@app/pipeline/validate.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-op prior=/resolved_drops= kwargs to _run_stages and validate()</name>
  <files>app/pipeline/orchestrator.py, app/pipeline/validate.py</files>

  <read_first>
  Read app/pipeline/orchestrator.py in full in one pass. Extract:
  - Line 260: _run_stages current signature `def _run_stages(run_id, email, roster, *, llm) -> None`
  - Line 273: `issues = validate(extracted, roster, matches)` — this is the call site to update
  - Lines 278-289: the persist block and branch (must remain unchanged)
  - Lines 91, 165: the two callers — run_pipeline at line 91 and resume_pipeline at line 165

  Read app/pipeline/validate.py in full in one pass. Extract:
  - Lines 29-35: `_HOURS_FIELDS` tuple — do NOT touch
  - Line 38: `_is_paid` predicate — do NOT touch
  - Lines 81-85: current `validate()` signature `def validate(extracted, roster, matches)`
  - Existing imports block (lines ~1-27)

  Key constraint D-7.5-01b: the kwargs slot between the `matches = reconcile_names(...)` line and the `issues = validate(...)` line. After this change, _run_stages internally passes `prior=prior` and `resolved_drops=resolved_drops` to validate, but both callers at lines 91 and 165 still call `_run_stages(run_id, ..., llm=llm)` with no new args — zero caller changes needed.

  Key constraint D-7.5-01c: the persist block at lines 278-287 (persist_extracted, persist_decision, persist_reconciliation, replace_line_items, set_status x2) must remain IDENTICAL — no new persists, no reordering.
  </read_first>

  <action>
  Change 1 — app/pipeline/orchestrator.py:

  Update _run_stages signature from:
    `def _run_stages(run_id, email, roster, *, llm) -> None:`
  to:
    `def _run_stages(run_id, email, roster, *, llm, prior=None, resolved_drops=None) -> None:`

  (Import Extracted for the type annotation is NOT required for the no-op change — add it in Plan 02. For now, bare `prior=None` is sufficient.)

  Update the `validate` call inside _run_stages (currently line 273) from:
    `issues = validate(extracted, roster, matches)`
  to:
    `issues = validate(extracted, roster, matches, prior=prior, resolved_drops=resolved_drops)`

  No other changes to _run_stages. The two callers at lines 91 and 165 do NOT change — they call `_run_stages(run_id, ..., llm=llm)` and `prior` and `resolved_drops` default to `None`.

  Change 2 — app/pipeline/validate.py:

  Update `validate()` signature from:
    `def validate(extracted: Extracted, roster: Roster, matches: list[NameMatchResult]) -> list[ValidationIssue]:`
  to:
    `def validate(extracted: Extracted, roster: Roster, matches: list[NameMatchResult], *, prior=None, prior_matches=None, resolved_drops=None) -> list[ValidationIssue]:`

  (Keyword-only params after `*` so existing positional callers remain valid — D-7.5-01 per D-17/D-18.)

  No changes to the validate() body whatsoever. The new kwargs are declared but not used — they become load-bearing in Plan 02.

  Do NOT import RawFieldDrop, FieldDrop, or UUID in validate.py yet — those imports come with Plan 02 when the args are actually used. Adding unused imports would cause linting failures.

  After both changes, run the full existing suite to confirm zero regressions:
  uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py tests/test_validate.py tests/test_reconcile.py tests/test_decide.py -q
  </action>

  <verify>
    <automated>uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py tests/test_validate.py tests/test_reconcile.py tests/test_decide.py -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "def _run_stages" app/pipeline/orchestrator.py` shows `prior=None, resolved_drops=None` in the signature
  - `grep "def validate" app/pipeline/validate.py` shows `prior=None, prior_matches=None, resolved_drops=None` in the signature
  - `grep "validate(extracted, roster, matches" app/pipeline/orchestrator.py` shows `prior=prior, resolved_drops=resolved_drops` threaded through
  - No change to lines 278-289 of orchestrator.py (persist block unchanged — verify with `git diff app/pipeline/orchestrator.py | grep "^[+-]" | grep -v "prior\|resolved_drops\|def _run_stages\|validate(" | head -5` shows no other diffs)
  - Lines 91 and 165 of orchestrator.py are UNCHANGED (callers pass no new args)
  - `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py tests/test_validate.py tests/test_reconcile.py tests/test_decide.py -q` exits 0 — zero test regressions
  - `uv run pytest -q` full suite exits 0
  </acceptance_criteria>

  <done>_run_stages has prior=None and resolved_drops=None kwargs and threads them into the validate() call. validate() has prior=None, prior_matches=None, resolved_drops=None keyword-only kwargs and ignores them (no-op body). All existing tests pass. The in-memory seam is ready for Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _run_stages new kwargs | Both default None — no new trust boundary opens; purely structural |
| validate() new kwargs | Default None and ignored — no behavior change; no new input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-01-01 | Tampering | _run_stages new kwargs in the persist block path | accept | kwargs default None and are not used in this plan; they never reach the persist block (the seam is before validate, far from the writes at lines 278-287). |
| T-7.5-01-02 | Tampering | validate() new kwargs | accept | All three default None and have no effect on the function body in this plan; zero behavior change; existing tests continue to gate correctness. |
| T-7.5-01-SC | Tampering | No new package installs | accept | Pure Python signature change; no uv add invoked. |
</threat_model>

<verification>
1. `grep -n "def _run_stages" app/pipeline/orchestrator.py` — shows `prior=None, resolved_drops=None`
2. `grep -n "def validate" app/pipeline/validate.py` — shows `prior=None, prior_matches=None, resolved_drops=None`
3. `grep -n "validate(extracted, roster, matches" app/pipeline/orchestrator.py` — shows `prior=prior, resolved_drops=resolved_drops`
4. `git diff app/pipeline/orchestrator.py` — only 2 lines changed (signature + validate call); persist block lines 278-289 are untouched
5. `uv run pytest tests/test_orchestrator_states.py tests/test_clarify.py tests/test_persistence.py tests/test_validate.py -q` — exits 0
6. `uv run pytest -q` — full suite exits 0
</verification>

<success_criteria>
- _run_stages signature extended with `prior=None, resolved_drops=None` (keyword-only); validate call inside threads both through
- validate() signature extended with `prior=None, prior_matches=None, resolved_drops=None` (keyword-only after `*`); body unchanged
- Existing callers (run_pipeline line 91, resume_pipeline line 165) are UNCHANGED — zero signature changes at call sites
- Persist block (lines 278-289) is UNCHANGED — same 5 autocommit writes, same order, same final state (D-7.5-01c)
- Full existing test suite green — proves behavioral identity with today's code
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-01-SUMMARY.md` when done.
</output>
