---
phase: 7.5
reviewers: [codex]
reviewed_at: 2026-06-28T07:08:52Z
rounds: 3
plans_reviewed: [07.5-01-PLAN.md, 07.5-02-PLAN.md, 07.5-03-PLAN.md, 07.5-04-PLAN.md]
---

# Cross-AI Plan Review — Phase 7.5

## ROUND 3 (Codex, delta/confirming — 2026-06-28T07:08Z)

Third delta-scoped round after the round-2 D-7.5-10 three-phase-ordering fixes. Charge: find a NEW failure mode introduced by the round-2 fixes (the reorder, the per-gate raw/backfilled split, employee_id-keyed backfill, raw provenance classifier, eval rewire). **Found ONE new HIGH — a Round-2 sequencing flaw introduced by the three-phase reorder.** Verified against plan text by the orchestrator.

**Final risk: HIGH** — the Round-2 answer path can strand the run before approval/paystub generation; a naive suppression fix risks explicit-zero overpay unless outcome classification is moved BEFORE backfill/validate/calc.

### Round-3 Concern

- **HIGH — Round-2 `asked` answers are classified AFTER `decide()`, so a silence/explicit-zero answer is re-gated as a fresh `field_regression` issue instead of being processed → run re-clarifies (stranded), and the "AWAITING_APPROVAL" comment is false.** Mechanism (verified against Plan 03):
  - `_resolved_by_name` (the suppression set) is built ONLY from terminal outcomes `confirmed_dropped`/`client_supplied` — it EXCLUDES `"asked"` (`07.5-03-PLAN.md:484-487`).
  - On Round-2 the just-answered field is still `"asked"` (it only becomes terminal in the classifier that runs AFTER `_run_stages`). So it is NOT in `_resolved_by_name`.
  - `_run_stages` runs the three phases: `detect_field_regression` sees snapshot OT=2 vs reply OT=None → drop; N8 suppression misses (field not in `_resolved_by_name`) → emits a `field_regression` issue; `decide` → `request_clarification` → `_run_stages` DEFERS and skips `_compute_line_items` (`07.5-03-PLAN.md:417,430,435`).
  - The Round-2 classifier then reclassifies `"asked"` → `carried_forward`/`confirmed_dropped` from `stage.issues` and persists (`07.5-03-PLAN.md:580-606`) — but there is NO second validate/decide/calc pass after that write. The run already branched to clarify. The comment "the run is at AWAITING_APPROVAL" (`07.5-03-PLAN.md:608`) is FALSE → infinite re-clarify on the ANSWERED path.
  - **Explicit-zero edge (additional):** `backfill_extracted` treats "silence OR explicit zero" as backfillable unless the field is already terminal in `resolved_drops` (`07.5-03-PLAN.md:371`). Since `"asked"` is not yet terminal, an explicit-zero removal the client just made is NOT available to suppress backfill before calc → a naive "also suppress on asked" fix would let the just-zeroed field be re-backfilled → OVERPAY.

### Root cause (the missing constraint, one level up from D-7.5-10)
D-7.5-10 ordered detect → backfill → calc. But on a round where the client ANSWERED, there is a prior step: **classify the raw reply into per-field outcomes, and derive the suppression/backfill decisions from that classification, BEFORE detect/backfill/decide/calc run.** The current design classifies AFTER. Correct ordering on an answered round: classify-raw-reply → (carried_forward ⇒ suppress detection + apply backfill; confirmed_dropped ⇒ suppress detection + suppress backfill; client_supplied ⇒ no drop) → detect/backfill/validate/decide/calc ONCE → reach AWAITING_APPROVAL.

### Round-3 Recommendation
This is a structural sequencing change (classification must precede the pipeline on an answered round) — past the revision-loop cap; warrants a new locked CONTEXT decision (a D-7.5-11) pinning "classify-then-suppress before detect/backfill/calc on answered rounds," then a targeted replan of Plan 03's Round-2 path + a test asserting an answered silence reaches AWAITING_APPROVAL (not a second clarification) and an answered explicit-zero is NOT re-backfilled. Do NOT apply the naive "suppress on asked" fix — it causes the explicit-zero overpay.

---

# Cross-AI Plan Review — Phase 7.5

## ROUND 2 (Codex, delta/confirming — 2026-06-28T06:10Z)

Delta-scoped follow-up after round-1 findings were fixed across 3 revision rounds. Charge: find a NEW failure mode introduced by the fixes, or an un-checked consumer of the three contracts (in-seam backfilled `extracted`, `prior_matches`, the `(employee_id_str, field)` resolved_drops set). **It found two HIGH issues — both NEW, both introduced by the R3-1 fix.** Verified against plan text by the orchestrator.

**Final risk: HIGH** — the in-seam backfilled `extracted` has unchecked consumers: first-pass detection and provenance classification both read mutated data and lose the original reply semantics.

### Round-2 Concerns

- **HIGH — Pre-validate backfill MASKS the regression before `detect_field_regression()` sees it (NEW — introduced by the R3-1 fix).** `07.5-03-PLAN.md:384-393`: `backfill_extracted` runs at the seam (rewriting OT silence→snapshot value), THEN `validate(extracted, ..., prior=prior)` runs with the already-backfilled `extracted`. But `validate()` IS the detector — `detect_field_regression(prior, extracted)` now compares snapshot OT=2 against backfilled OT=2 → sees NO drop → emits NO `field_regression` issue → writes NO `asked` state → sends NO clarification. The carried-forward value reaches the paystub and the operator **with no provenance**, as if the client stated it. This breaks the phase's central thesis ("no value reaches approval without visible provenance") and SC2/SC3. **Root cause: the same `validate()` call cannot both (a) consume the backfill and (b) be the detector of what to backfill.** Correct ordering is three-phase: detect-on-raw → backfill → calc — NOT backfill → detect+calc.

- **HIGH — `backfill_extracted` is still submitted_name-keyed for the snapshot lookup (R3-3 fix incomplete).** `07.5-03-PLAN.md:333`: `name_to_snapshot_emp = {submitted_name: ExtractedEmployee}` and `snap_emp = name_to_snapshot_emp.get(emp.submitted_name)`. Detection and resolved_drops were made employee_id-keyed, but the CARRY-FORWARD itself was not. For "M. Chen" (snapshot) → "Maria Chen" (reply), same employee_id: no snapshot match → no backfill → the restated-name silence path does not carry the value forward (or re-defers). The R3-3 fix made detection survive name-restatement but not the backfill.

- **MED — `client_supplied` vs `carried_forward` still not provable after mutation.** `07.5-03-PLAN.md:526`: the Round-2 classifier reads the already-backfilled persisted extraction and labels `raw_value == snap_val` as `carried_forward`. If the client explicitly replies with the SAME positive value as the snapshot, it is mislabeled `carried_forward`. Provenance error, not a pay-calculation error.

### Round-2 Recommendation
Findings 1 + 2 are HIGH and require a structural fix (the detect→backfill→calc ordering) — this is past the 3-iteration revision-loop cap and likely warrants a new CONTEXT decision pinning the three-phase ordering as a locked invariant, plus making backfill employee_id-keyed. Finding 3 is a localized labeling fix. The fixes are real, source-verified, and worth doing before execution on this money path.

---

## ROUND 1 (Codex — 2026-06-28T04:41Z)

## Codex Review

**Summary**
Overall risk is **HIGH**. The plans understand the prior review findings, but Plan 03 reintroduces the core R3-1 failure: Round-2 backfill happens after `_run_stages` has already persisted, decided, and calculated line items in `orchestrator.py:277`. Plan 02 also does not actually satisfy the restated-name / same-`employee_id` diff requirement because detection is still keyed by `submitted_name` before employee-id mapping.

**Strengths**
- Plan 01 is a low-risk seam-prep change and preserves the current persist block/order.
- The plan set correctly identifies the major prior hazards: R3-1 ordering, R3-2 mixed issues, R3-3 name-vs-id diffing.
- `ClarifiedFields` typed-on-write JSONB is a pragmatic mitigation for safety-critical loop state.
- The test plan explicitly includes ordering, loop guard, tri-state, and mixed-issue coverage, which are the right properties to pin.

**Concerns**
- **HIGH: Plan 03 backfill is too late.** The Round-2 action calls `_run_stages(... prior=None ...)`, then classifies/backfills `stage.extracted` afterward. But `_run_stages` already calls `_compute_line_items` and `repo.replace_line_items` before returning on a process decision. Persisting backfilled extraction afterward does not recompute paystubs. This recreates R3-1.
- **HIGH: Plan 03 mixed-issue path still fails R3-2.** `_run_stages` defers `_clarify` only when `all_field_regression` is true. A mixed field-regression + missing/unresolved case calls `_clarify(... purpose="clarification")` immediately, before `asked` is written. Worse, an existing first clarification row can make `get_outbound_message_id(... purpose="clarification")` skip sending entirely.
- **HIGH: Plan 02 does not satisfy restated-name diffing.** `detect_field_regression` intersects original/resumed by `submitted_name`, so original `"M. Chen"` and reply `"Maria Chen"` never produce a raw drop. The required same-`employee_id` comparison must happen before diffing, using prior and current matches.
- **HIGH: Field-regression wording is only guaranteed in `_template_body`.** If the LLM draft returns non-empty text, `compose_clarification` returns it directly. The locked "Reply with the ... or 'none'" wording is not guaranteed on the real path.
- **MEDIUM: Plan 03 return shape is internally inconsistent.** `_RunStagesResult` is defined with `clarify_deferred` and `extracted`, but later uses `stage.decision`. It likely also needs `matches` and `issues`.
- **MEDIUM: `carried_forward` is overloaded.** A client replying with a positive value is classified as `carried_forward`, even though no carry-forward occurred. That weakens the provenance guarantee.
- **MEDIUM: Plan 04 claims hermetic rollback tests but describes live DB writes with manual cleanup.** Use the existing `seeded_db`/transaction fixture pattern or revise the claim.
- **MEDIUM: Approved-bytes test does not prove sent bytes.** Comparing DB before/after approval CAS does not exercise `_deliver`, confirmation composition, or PDF generation.
- **LOW: Schema CHECK migration should target the exact purpose constraint more defensively than `conname LIKE '%purpose%'`.**

**Suggestions**
- Reshape `_run_stages` around an actual pre-validate seam: extract/reconcile, optional backfill/classification, then validate/decide/persist/branch. Do not return to `resume_pipeline` for Round-2 backfill after calculation.
- Move field-regression diffing to employee-id keyed maps: reduce both prior and resumed extraction by resolved `employee_id`, then compare watched fields.
- Defer clarification when **any** field-regression issue exists, write `asked`, then send one clarification with `purpose="clarification_field_regression"` that includes all normal and field-regression asks.
- Make the required field-regression question deterministic: append it after any LLM draft or force it through a non-LLM template block.
- Add a terminal outcome like `client_supplied` or avoid showing "carried-forward" when the client supplied a positive replacement value.
- Make Plan 04 tests genuinely transactional/hermetic, and make the approved/sent invariant capture the actual outbound confirmation body/PDF source.

**Risk Assessment**
**HIGH** until Plans 02 and 03 are revised. As written, the phase can still silently compute paystubs from dropped fields, fail the restated-name requirement, and skip the mixed-issue follow-up email. Plan 01 is safe; Plan 04 has the right test intent but depends on fixing the orchestration design first.

---

## Consensus Summary

Single reviewer (Codex). No cross-reviewer consensus to synthesize, but Codex's findings are notable because **it read the live `orchestrator.py` source** (citing `:277`) rather than reasoning from plan text alone — the exact discipline this phase's history demands.

### Top Concerns (priority order)
1. **R3-1 regression risk (HIGH)** — Plan 03's Round-2 backfill may run *after* `_run_stages` already computed/persisted line items, recreating the silent-underpay bug the phase exists to fix. **Must verify against the actual Plan 03 text — this is the load-bearing finding.**
2. **R3-3 restated-name diffing (HIGH)** — Plan 02's `detect_field_regression` may key on `submitted_name` before employee-id mapping, so `"M. Chen" → "Maria Chen"` produces no drop.
3. **R3-2 mixed-issue path (HIGH)** — clarification may fire immediately with `purpose="clarification"` before `asked` is written, and a pre-existing clarification row could suppress the send.
4. **Deterministic wording (HIGH)** — the locked D-7.5-09 wording may only live in the `_template_body` fallback, not on the LLM-draft path.

### Required Next Step
Every finding MUST be verified against the actual PLAN.md text and live source before acting (the recurring lesson of this thread — Phase 7's review process refuted several plausible-but-wrong findings the same way). Findings that hold → `/gsd-plan-phase 7.5 --reviews`. Findings that are refuted → record the refutation with the source evidence, as Phase 7 did.
