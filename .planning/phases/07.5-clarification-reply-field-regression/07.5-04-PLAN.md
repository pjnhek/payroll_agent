---
phase: 07.5-clarification-reply-field-regression
plan: "04"
type: execute
wave: 4
depends_on:
  - "07.5-03"
files_modified:
  - tests/test_resume_pipeline.py
  - eval/run_eval.py
  - eval/fixtures/16_zero_hours_hourly_coastal.json
  - eval/fixtures/16_zero_hours_hourly_coastal_extraction.json
  - eval/fixtures/17_nfd_name_coastal.json
  - eval/fixtures/17_nfd_name_coastal_extraction.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json
  - eval/summary.json
  - app/templates/run_detail.html
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Integration tests require DATABASE_URL set in shell env so state-machine tests (snapshot-once, ordering, loop-guard, confirmed_dropped) actually execute and PASS — skip != evidence (D-7.5-04)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Supavisor transaction mode, port 6543) — must be in shell env, not just .env"
    dashboard_config:
      - task: "Confirm Plan 03's schema migration was applied (pre_clarify_extracted and clarified_fields columns exist)"
        location: "Run: uv run python -m app.db.bootstrap before this plan's checkpoint"

must_haves:
  truths:
    - "tests/test_resume_pipeline.py exists with 8+ integration tests covering: snapshot_once (D-19), N1 single-_run_stages-call, N2 asked-before-send ordering, ordering test D-7.5-04a (paystub OT line reflects carried_forward value), approved-bytes==sent-bytes invariant (D-7.5-04b), tri-state through real path (D-7.5-04c), loop-guard (fires exactly once, no infinite re-clarify), confirmed_dropped no-reflag (D-15/N8), mixed-issue SC4"
    - "The D-7.5-04a ordering test PASSES: a carried_forward scenario asserts the FINAL PAYSTUB LINE ITEM has OT=Decimal('2') (not 0/absent), plus a negative companion that would catch a backfill-after-calc regression"
    - "D-7.5-04c tri-state test proves None vs Decimal('0') through the SAME serialization path that writes pre_clarify_extracted (not hand-typed JSON) — confirmed_dropped test creates the prior snapshot via set_pre_clarify_extracted to guarantee the Decimal precision survives the real JSONB round-trip"
    - "All integration tests PASS (not SKIP) when DATABASE_URL is set — skip != evidence per D-7.5-04"
    - "Three eval fixture pairs (16/17/18) exist in eval/fixtures/ and run_eval.py scores all three correctly; eval/summary.json updated via --record; uv run python eval/run_eval.py --check exits 0"
    - "run_detail.html shows carried-forward and confirmed_dropped provenance badges on field-regression-affected hours rows (D-7.5-08); the clarification_field_regression purpose badge is added to the outbound emails section"
    - "D-7.5-04d: integration tests run against a fresh schema / transactional-rollback fixture (hermetic) — NOT the shared dev DB state"
  artifacts:
    - path: "tests/test_resume_pipeline.py"
      provides: "8+ integration tests covering all MONEY-03 state-machine invariants + ordering test + approved==sent invariant + mixed-issue test"
      contains: "test_ordering_carried_forward_ot_in_paystub"
    - path: "eval/run_eval.py"
      provides: "prior_extracted support: strips key before InboundEmail, loads as Extracted, passes as prior= to validate()"
      contains: "prior_extracted"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal.json"
      provides: "MONEY-03 eval fixture with prior_extracted key (produced via model_dump, not hand-typed — D-24)"
      contains: "prior_extracted"
    - path: "app/templates/run_detail.html"
      provides: "per-value provenance badges for carried-forward and confirmed_dropped on field-regression hours rows; clarification_field_regression purpose badge"
      contains: "carried-forward"
  key_links:
    - from: "eval/fixtures/18_field_drop_clarify_carry_coastal.json (prior_extracted key)"
      to: "eval/run_eval.py:validate(prior=prior_extracted)"
      via: "fixture loader reads prior_extracted -> Extracted.model_validate -> prior= kwarg"
      pattern: "prior_extracted"
    - from: "tests/test_resume_pipeline.py:test_ordering_carried_forward_ot_in_paystub"
      to: "app/pipeline/orchestrator.py:resume_pipeline (backfill in-memory seam)"
      via: "asserts paystub OT line == Decimal('2') after carried_forward backfill — fails if backfill runs after calc"
      pattern: "carried_forward"
    - from: "app/templates/run_detail.html:clarified_fields badge"
      to: "app/db/repo.py:load_clarified_fields"
      via: "run dict includes clarified_fields; template reads run.clarified_fields[emp_id][field_name]"
      pattern: "carried-forward"
---

<objective>
Complete the MONEY-03 evidence: replace xfail stubs with 8+ PASSING integration tests, add eval fixtures 16/17/18, update run_eval.py with prior_extracted support, and add the D-7.5-08 provenance badge to the run-detail approval view.

D-23 two-layer evidence:
- Eval layer: fixtures 16 (MONEY-01), 17 (MONEY-02 eval-scorer parity), 18 (MONEY-03 field-regression prior= seam). Certifies pure judgment (validate -> decide) given a prior.
- Integration layer: state-machine invariants — snapshot-once, N1 single-call, N2 asked-before-send, D-7.5-04a ordering (the root-cause pin), D-7.5-04b approved==sent, D-7.5-04c tri-state through real path, loop-guard, confirmed_dropped no-reflag, mixed-issue SC4.

D-7.5-04a (the most important test): the ordering test asserts the COMPUTED PAYSTUB LINE ITEM reflects the carried-forward OT value (OT=2 in final paystub, not 0/absent). This test fails iff the backfill runs after calc — it directly pins the root cause that drove three review rounds.

D-7.5-04d: integration tests run against a hermetic DB (fresh schema + transactional-rollback fixture) — NOT the shared dev DB. The user-setup gate in Plan 03 applied the schema migration; these tests use the migrated schema on a fresh-rolled-back test run.

D-7.5-08 provenance: run_detail.html already has the name-source badge infrastructure. This plan adds a per-field provenance badge on hours rows for field-regression-affected values so the operator never sees a carried-forward number presented as client-stated.

Phase-gate: all integration tests PASS WITH DATABASE_URL set (not SKIP). Eval --check passes. Human checkpoint signs off.

Output: full MONEY-03 evidence assembled; phase ready for verification.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-03-SUMMARY.md

@tests/test_clarify.py
@tests/conftest.py
@eval/run_eval.py
@eval/fixtures/07_missing_hours_coastal.json
@app/templates/run_detail.html
@app/db/seed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests — 8 tests covering all MONEY-03 state-machine invariants + ordering test + approved==sent</name>
  <files>tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/test_clarify.py in full for the DB state setup pattern, mock LLM class, skip marker, and cleanup pattern. Key elements to copy:
  - class _DraftLLM with call_text method that records calls
  - pytest.mark.skipif(not os.environ.get("DATABASE_URL"), ...) — use this exact form
  - Run creation via repo helpers + direct SQL inserts
  - finally: cleanup with DELETE FROM payroll_runs WHERE id = %s

  Read tests/conftest.py for any shared DB fixtures or helper utilities.

  Read app/db/seed.py to identify coastal business employees — need real names for fixture data. The integration tests use uuid.uuid4() run IDs and synthetic extraction data (no real employee PII — test isolation).

  PATTERNS.md §8 has the _extracted() and _match() builder helper style from test_validate.py — copy for test setup.

  D-7.5-04d: each test creates its own run in the DB, asserts its behavior, and deletes in finally. The tests do NOT share state with other runs in the dev DB. They use uuid.uuid4() for all IDs.

  CRITICAL: the pytestmark at the module level:
  `pytestmark = pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="requires live DATABASE_URL — set before phase gate; skip != evidence (D-7.5-04)")`

  Write test_resume_pipeline.py with exactly these 8 tests (and a module-level pytestmark):
  </read_first>

  <action>
  Create tests/test_resume_pipeline.py with 8 tests. All use skipif via pytestmark (module level). All have finally: cleanup. Use uuid.uuid4() for all test run IDs. Use the _conn_ctx-free direct psycopg.connect approach matching test_clarify.py's pattern.

  Helper utilities at top of file:
  - `_mk_extracted(employees_data) -> Extracted`: builds an Extracted with a zero UUID, from a list of dicts with field names
  - `_mk_match(name, emp_id, source="exact") -> NameMatchResult`: builds a NameMatchResult

  Test 1 — test_snapshot_once_not_overwritten (D-19/D-28):
  - Create a payroll run via repo in AWAITING_REPLY state.
  - Call repo.set_pre_clarify_extracted(run_id, extracted_v1) with OT=Decimal("2").
  - Call repo.set_pre_clarify_extracted(run_id, extracted_v2) with OT=Decimal("0").
  - Assert repo.load_pre_clarify_extracted(run_id).employees[0].hours_overtime == Decimal("2") (IS NULL guard: second write is a no-op).
  - Cleanup in finally.

  Test 2 — test_n1_single_run_stages_call (N1):
  - Monkeypatch _run_stages in orchestrator to count calls (return a mock _RunStagesResult with clarify_deferred=False and a process decision).
  - Create a run in AWAITING_REPLY state. Call resume_pipeline(run_id, mock_inbound, llm=mock_llm).
  - Assert _run_stages was called exactly once. Uses unittest.mock.patch.

  Test 3 — test_n2_asked_written_before_send (N2 — asked-before-send ordering):
  - Set up a run with a pre_clarify_extracted snapshot (OT=2).
  - Configure mock LLM to return extraction with OT=None (drop detected).
  - Monkeypatch _clarify to (a) record when it was called, (b) at that moment load clarified_fields from DB and record whether "asked" is present.
  - Call resume_pipeline.
  - Assert: when _clarify was invoked, clarified_fields already had "asked" for OT for the relevant employee — write happened before the send (N2).
  - Assert: _clarify was called with purpose="clarification_field_regression".

  Test 4 — test_ordering_carried_forward_ot_in_paystub (D-7.5-04a — THE ROOT CAUSE PIN):
  - Set up a run with pre_clarify_extracted snapshot (OT=2) and clarified_fields with "asked" for OT (simulating Round-2).
  - Configure mock LLM to return extraction with OT=None (silence — carried_forward should apply).
  - Also seed the DB with a real employee and roster so calc can produce actual paystub line items.
  - Call resume_pipeline (Round-2 path).
  - Assert: the FINAL PAYSTUB LINE ITEM for that employee has hours_overtime == Decimal("2") — the carried-forward value is in the computed paystub (backfill landed before calc, not after).
  - Negative companion: if you comment out the backfill in orchestrator.py, this test FAILS (proving it pins the ordering, not just the outcome).

  Test 5 — test_approved_bytes_equals_sent_bytes (D-7.5-04b — approved==sent invariant):
  - Set up a run through to AWAITING_APPROVAL (has a computed paystub including a carried-forward OT value).
  - Read the extracted_data and paystub line items from DB at the AWAITING_APPROVAL moment.
  - Simulate approval via the main.py CAS (or directly via claim_status(AWAITING_APPROVAL, APPROVED)).
  - Assert: the extracted_data and paystub line items are identical before and after the CAS — no mutation between AWAITING_APPROVAL and APPROVED.
  - This pins that no new backfill or mutation can land between those two status transitions.

  Test 6 — test_tri_state_through_real_path (D-7.5-04c):
  - Create pre_clarify_extracted via repo.set_pre_clarify_extracted (which uses json.dumps(extracted.model_dump(mode="json"))) — NOT hand-typed JSON.
  - Load it back via repo.load_pre_clarify_extracted.
  - Assert: None fields survive as None (absent); Decimal("0") fields survive as Decimal("0") (explicit zero, not coerced to None).
  - This proves the tri-state (present/explicit-0/absent) survives the JSONB serialization round-trip that pre_clarify_extracted uses (D-7.5-04c / D-24).

  Test 7 — test_loop_guard_fires_exactly_once (decision-A loop termination):
  - Set up a run where Round-1 sets status to AWAITING_REPLY with "asked" in clarified_fields.
  - Simulate a Round-2 reply that STILL has OT=None (silence — still carried_forward).
  - Call resume_pipeline (Round-2 path).
  - Assert: run advances to COMPUTED/AWAITING_APPROVAL (the run processes with the carried-forward value; no second clarification is sent — the loop guard fires exactly once).
  - Assert: clarified_fields outcome for OT is "carried_forward" (not "asked" again).

  Test 8 — test_mixed_issue_still_records_asked_and_asks (SC4 / ROADMAP SC4 / R3-2):
  - Set up a run where the resumed extraction has BOTH a field_regression issue (OT dropped) AND a normal missing-field issue (another employee has no hours at all).
  - Call resume_pipeline.
  - Assert: clarified_fields durably records "asked" for the OT field (not lost due to the mixed-issue path).
  - Assert: the outbound email_messages row with purpose="clarification_field_regression" was written — the field-regression question was asked even though a normal missing-field issue also existed.

  Each test has a `finally:` block that DELETEs the created run from payroll_runs (cascades to related rows). Use a list to track all inserted run_ids per test.
  </action>

  <verify>
    <automated>uv run pytest tests/test_resume_pipeline.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - `tests/test_resume_pipeline.py` exists and has exactly 8+ test functions (grep "^def test_" returns 8+)
  - `grep "test_ordering_carried_forward_ot_in_paystub" tests/test_resume_pipeline.py` exits 0 (the D-7.5-04a ordering test exists)
  - `grep "test_approved_bytes_equals_sent_bytes\|test_tri_state_through_real_path" tests/test_resume_pipeline.py` exits 0 (D-7.5-04b and D-7.5-04c tests exist)
  - `grep "test_mixed_issue_still_records" tests/test_resume_pipeline.py` exits 0 (SC4 mixed-issue test exists)
  - `grep "pytestmark" tests/test_resume_pipeline.py` exits 0 (module-level skip guard present)
  - WITH DATABASE_URL set: `uv run pytest tests/test_resume_pipeline.py -v` shows 8+ PASSED (ZERO SKIPPED, ZERO FAILED)
  - `uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v` PASSES and would FAIL if the backfill in orchestrator.py were commented out (the ordering pin works)
  - All 8 tests have `finally:` cleanup blocks
  </acceptance_criteria>

  <done>8 integration tests PASS (not SKIP) with DATABASE_URL set. D-7.5-04a ordering test explicitly pins that paystub OT==Decimal("2") after carried_forward backfill. D-7.5-04b approved==sent invariant test passes. D-7.5-04c tri-state through real JSONB path passes. SC4 mixed-issue test passes. All cleanup blocks present.</done>
</task>

<task type="auto">
  <name>Task 2: Eval fixtures 16/17/18 + run_eval.py prior_extracted support + summary regen + run_detail.html provenance badge</name>
  <files>eval/run_eval.py, eval/fixtures/16_zero_hours_hourly_coastal.json, eval/fixtures/16_zero_hours_hourly_coastal_extraction.json, eval/fixtures/17_nfd_name_coastal.json, eval/fixtures/17_nfd_name_coastal_extraction.json, eval/fixtures/18_field_drop_clarify_carry_coastal.json, eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json, eval/summary.json, app/templates/run_detail.html

  <read_first>
  Read eval/run_eval.py in full. Key anchors:
  - The fixture loader (wherever "expected" and "fixture_category" are stripped before InboundEmail construction) — add "prior_extracted" to the strip set.
  - Where validate() is called — add prior=prior_extracted kwarg.
  - The --record and --check flag behavior + summary.json path.
  - Lines around extraction cache writer (extracted.model_dump_json(indent=2)) — confirms D-24 compliance.

  Read eval/fixtures/07_missing_hours_coastal.json to get the exact fixture structure (id, message_id, in_reply_to, references_header, subject, from_addr, to_addr, body_text, created_at, fixture_category, expected).

  Read app/db/seed.py to find coastal business employees for plausible names in fixtures 16/17/18.

  Read app/templates/run_detail.html lines 120-155 (the extracted data per-employee table) and lines 195-215 (the outbound emails section with purpose badge). PATTERNS.md §7 has the exact badge markup to mirror for provenance (D-7.5-08).

  Fixture 17 HONESTY check (seed Finding #8): if the coastal seed has no accented-name employee, fixture 17 CANNOT claim full NFD Unicode proof via the eval alone. Use a coastal employee's name with extra internal whitespace (e.g. "Maria  Chen" with double space) as the submitted name — certifies _normalize whitespace normalization / C-4 parity. Document scope in SUMMARY: "certifies eval scorer _normalize parity, NOT full NFD Unicode (that proof is test_reconcile.py)."
  </read_first>

  <action>
  Step A — Update eval/run_eval.py:

  1. In the fixture loader (wherever "expected" and "fixture_category" are stripped), also strip "prior_extracted" from the dict before constructing InboundEmail. Save the value before stripping: `prior_extracted_raw = fixture_raw.pop("prior_extracted", None)`.

  2. After loading the fixture and before calling validate(), load prior_extracted:
     If prior_extracted_raw is not None: `prior_extracted = Extracted.model_validate(prior_extracted_raw)` — import Extracted from app.models.contracts if not already imported.
     Else: `prior_extracted = None`.

  3. Thread into validate() call: `validate(extracted, roster, matches, prior=prior_extracted)`.

  Step B — Write a generation script (scratchpad, NOT committed) to produce fixture extraction caches via model_dump_json (D-24). Run it to generate the six extraction .json files. Script location: /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/2948e6e3-bf23-44c6-b200-d9c6281e738e/scratchpad/gen_75_fixtures.py

  The script:
  - Imports Extracted, ExtractedEmployee from app.models.contracts; imports uuid, date, Decimal, json, pathlib
  - Creates each Extracted programmatically and writes extraction files using extracted.model_dump_json(indent=2)

  Step C — Create the six fixture files:

  Fixture 16 (MONEY-01 — zero-hours hourly, from seed):
  - Pick a real coastal hourly employee from seed.py. Submit with hours_regular=Decimal("0"), all other hours null.
  - Main fixture: expected.decision.final_action="request_clarification", gate_reasons_contains a "missing" or "hours" entry; fixture_category="zero-hours-hourly".
  - Extraction cache: Extracted with one employee, hours_regular="0.00", all other hours null (via model_dump_json).

  Fixture 17 (MONEY-02 / C-4 eval-scorer parity):
  - Pick a real coastal employee. Use their name with extra internal whitespace (e.g. "Maria  Chen") as submitted_name.
  - Expected: final_action="process", reconciliation shows resolved=True (whitespace-normalized match).
  - fixture_category="nfc-normalizer-parity" (honest: certifies _normalize, NOT "nfd-unicode").
  - Extraction cache: Extracted with the extra-whitespace submitted name (via model_dump_json).

  Fixture 18 (MONEY-03 — field regression with prior_extracted):
  - Pick a real coastal hourly employee. The resumed extraction has hours_overtime=None. The prior snapshot has hours_overtime=Decimal("2").
  - Main fixture: add "prior_extracted" key containing the prior Extracted as a plain dict (prior_obj.model_dump(mode="json") — a nested JSON object inside the fixture JSON). Expected: final_action="request_clarification" when validate() is called with prior=prior_extracted.
  - fixture_category="field-regression-carry-forward".
  - Extraction cache: the RESUMED extraction (hours_overtime null) via model_dump_json.

  Step D — Run the generation script and verify D-24 compliance:
  uv run python /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/2948e6e3-bf23-44c6-b200-d9c6281e738e/scratchpad/gen_75_fixtures.py
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/16_zero_hours_hourly_coastal_extraction.json').read())); print('Fixture 16 OK:', e.employees[0].hours_regular)"
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json').read())); print('Fixture 18 OK OT:', e.employees[0].hours_overtime)"

  Step E — Run eval and regenerate summary.json:
  uv run python eval/run_eval.py 2>&1 | tail -30
  uv run python eval/run_eval.py --record  (or the equivalent flag from the argparse block)
  uv run python eval/run_eval.py --check 2>&1 | tail -5  — must exit 0

  Step F — Add provenance badges to app/templates/run_detail.html (D-7.5-08):

  1. In the per-employee hours rows section (~lines 143-148), add a Jinja2 conditional badge after each hours value cell. The badge block checks run.clarified_fields[emp_id_str][field_name] for the three outcomes. Mirror the existing name-source badge style exactly (PATTERNS.md §7 has the exact markup). The badge keys are: "carried_forward" -> badge-pending "carried-forward", "confirmed_dropped" -> badge-neutral "client removed", "asked" -> badge-bad "awaiting reply". Scope: only the field_name values in _HOURS_FIELDS (hours_regular, hours_overtime, hours_vacation, hours_sick, hours_holiday). The Jinja2 template needs the employee_id as a string to look up in clarified_fields — pass it from the run context (run_detail.html has run.reconciliation which maps submitted_name to employee_id).

  2. In the outbound emails section (~lines 202-208), add a case for 'clarification_field_regression': `{% elif msg.purpose == 'clarification_field_regression' %}<span class="badge badge-pending">field-regression-clarification</span>` — between the 'clarification' case and the else fallback.

  3. Ensure run_detail.html receives clarified_fields in the run dict. The run_detail route in main.py likely constructs a run dict — add clarified_fields loading via repo.load_clarified_fields(run_id) to that dict so the template can access run.clarified_fields. Check main.py for the run_detail endpoint to confirm the context dict shape and add the field.
  </action>

  <verify>
    <automated>uv run python eval/run_eval.py --check 2>&1 | tail -10; uv run pytest -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "prior_extracted" eval/run_eval.py` exits 0 (stripping and loading logic)
  - All 6 fixture files exist: `ls eval/fixtures/{16,17,18}_*.json | wc -l` returns 6
  - `grep "prior_extracted" eval/fixtures/18_field_drop_clarify_carry_coastal.json` exits 0 (fixture has the key)
  - Extraction caches parse via Extracted.model_validate without error (D-24 compliance)
  - `uv run python eval/run_eval.py --check` exits 0 (after --record regenerates summary.json with fixtures 16/17/18)
  - `grep "carried-forward" app/templates/run_detail.html` exits 0 (D-7.5-08 provenance badge)
  - `grep "clarification_field_regression" app/templates/run_detail.html` exits 0 (purpose badge in email thread)
  - `grep "client removed" app/templates/run_detail.html` exits 0 (confirmed_dropped badge label)
  - `uv run pytest -q` full suite exits 0 (no regressions from run_eval.py changes or template changes)
  </acceptance_criteria>

  <done>Fixtures 16/17/18 exist and score correctly. run_eval.py wired with prior_extracted support. summary.json regenerated and --check passes. run_detail.html has provenance badges (carried-forward/client removed/awaiting reply) on hours rows + clarification_field_regression purpose badge in the email thread. Full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1: 8 integration tests in tests/test_resume_pipeline.py covering all MONEY-03 state-machine invariants including the D-7.5-04a ordering test, D-7.5-04b approved==sent, D-7.5-04c tri-state through real JSONB, loop-guard, and mixed-issue SC4 test.
  Task 2: Eval fixtures 16/17/18, run_eval.py prior_extracted support, summary.json regenerated, run_detail.html provenance badges (D-7.5-08).
  </what-built>
  <how-to-verify>
  PHASE GATE — DATABASE_URL must be set in shell env. All integration tests must PASS (not SKIP).

  1. Integration tests (D-7.5-04 phase-completion gate):
     uv run pytest tests/test_resume_pipeline.py -v
     Expected: 8+ PASSED, 0 SKIPPED, 0 FAILED.
     If any show SKIPPED: DATABASE_URL is not set — set it and re-run.

  2. D-7.5-04a ordering test passes and pins root cause:
     uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v
     Expected: 1 PASSED. This is the root-cause pin: a passed test that would fail if the backfill were moved below calc.

  3. Eval layer (D-23):
     uv run python eval/run_eval.py --check
     Expected: exits 0. Fixtures 16/17/18 score correctly.

  4. ROADMAP Success Criteria verification:
     SC1 (foundation): python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(f'_run_stages in resume_pipeline: {len([n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(getattr(n,\"func\",None),\"id\",None)==\"_run_stages\"])}') for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='resume_pipeline']"
     Expected: prints 1.

     SC2 (field-regression detects and clarifies once):
     uv run pytest tests/test_resume_pipeline.py -k "loop_guard" -v
     Expected: PASSED.

     SC3 (carry-forward vs confirmed_dropped):
     uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward or confirmed_dropped" -v
     Expected: 2 PASSED.

     SC4 (mixed-issue):
     uv run pytest tests/test_resume_pipeline.py -k "mixed_issue" -v
     Expected: PASSED.

  5. Provenance badge visible in template:
     grep "carried-forward" app/templates/run_detail.html
     grep "clarification_field_regression" app/templates/run_detail.html
     grep "client removed" app/templates/run_detail.html
     All should return matches.

  6. Full suite:
     uv run pytest -q
     Expected: all green (integration tests PASS when DATABASE_URL set).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 integration tests PASS (not SKIP) with DATABASE_URL set, eval --check passes, and all four ROADMAP SC checks verify. Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/fixtures/*.json prior_extracted key | Read from disk; Extracted.model_validate rejects malformed data at load time (extra="forbid") |
| integration tests -> live DB | Tests write synthetic run_ids to real DB; cleanup in finally blocks |
| run_detail.html clarified_fields | Read from DB via load_clarified_fields; template reads string keys only — no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-04-01 | Tampering | eval fixture prior_extracted parsed by Extracted.model_validate | mitigate | model_validate with extra="forbid" rejects any unexpected field; D-24 compliance (model_dump(mode="json")) ensures Decimal precision is correct. |
| T-7.5-04-02 | Information Disclosure | Integration tests write to real Supabase dev DB | accept | Tests use uuid.uuid4() run IDs and synthetic extraction data (no real employee PII). Tests clean up in finally blocks. |
| T-7.5-04-03 | Elevation of Privilege | Test run_ids colliding with production runs | mitigate | uuid.uuid4() generates cryptographically random IDs; collision probability negligible. Cleanup in finally ensures no orphan test data. |
| T-7.5-04-04 | Tampering | run_detail.html clarified_fields rendering | accept | clarified_fields is loaded from DB (Pydantic-validated on write per D-7.5-03b); template renders only known outcome strings; no user-controlled string injection into Jinja2 logic. |
| T-7.5-04-05 | Information Disclosure | Provenance badge shows carried-forward field values | accept | The badge shows the outcome label ("carried-forward" / "client removed") not the actual value; the value itself was already displayed in the cell; no additional PII disclosure. |
| T-7.5-04-SC | Tampering | No new package installs | accept | No uv add invoked; all imports from existing pyproject.toml deps. |
</threat_model>

<verification>
Phase-gate (all must pass WITH DATABASE_URL set):

1. `uv run pytest tests/test_resume_pipeline.py -v` — 8+ PASSED, 0 SKIPPED, 0 FAILED
2. `uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v` — PASSED (D-7.5-04a root-cause pin)
3. `uv run pytest tests/test_resume_pipeline.py -k "approved_bytes" -v` — PASSED (D-7.5-04b)
4. `uv run pytest tests/test_resume_pipeline.py -k "tri_state" -v` — PASSED (D-7.5-04c)
5. `uv run pytest tests/test_resume_pipeline.py -k "mixed_issue" -v` — PASSED (SC4)
6. `uv run python eval/run_eval.py --check` — exits 0
7. `grep "prior_extracted" eval/run_eval.py` — exits 0
8. `grep "prior_extracted" eval/fixtures/18_field_drop_clarify_carry_coastal.json` — exits 0
9. `grep "carried-forward" app/templates/run_detail.html` — exits 0 (D-7.5-08)
10. `grep "clarification_field_regression" app/templates/run_detail.html` — exits 0
11. `uv run pytest -q` — full suite exits 0
12. AST N1 check: `python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(len([n for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(getattr(n,'func',None),'id',None)=='_run_stages'])) for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='resume_pipeline']"` prints 1
</verification>

<success_criteria>
- 8 integration tests in test_resume_pipeline.py, ALL PASSING with DATABASE_URL set; pytestmark skipif guard present
- D-7.5-04a ordering test: carried_forward OT=2 in final paystub (root-cause pin; fails if backfill is after calc)
- D-7.5-04b approved==sent: no mutation between AWAITING_APPROVAL and APPROVED
- D-7.5-04c tri-state through real JSONB: None vs Decimal("0") survives serialization round-trip via actual set_pre_clarify_extracted / load_pre_clarify_extracted
- Fixtures 16/17/18 (D-23 eval layer): eval --check exits 0; fixture 17 scope documented as _normalize parity, not full NFD Unicode
- run_eval.py: prior_extracted stripped from fixture dict before InboundEmail, loaded as Extracted, passed as prior= to validate()
- run_detail.html: provenance badge on hours rows (carried-forward / client removed / awaiting reply) and clarification_field_regression purpose badge in email thread (D-7.5-08)
- Phase-completion gate signed off: all ROADMAP SC 1-4 verified, eval --check green, 8 integration tests PASSED
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-04-SUMMARY.md` when done.
</output>
