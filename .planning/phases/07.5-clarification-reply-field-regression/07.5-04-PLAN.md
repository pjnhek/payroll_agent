---
phase: 07.5-clarification-reply-field-regression
plan: "04"
type: execute
wave: 4
depends_on:
  - "07.5-03"
files_modified:
  - tests/test_resume_pipeline.py
  - eval/run_eval.py
  - eval/fixtures/16_zero_hours_hourly_coastal.json
  - eval/fixtures/16_zero_hours_hourly_coastal_extraction.json
  - eval/fixtures/17_nfd_name_coastal.json
  - eval/fixtures/17_nfd_name_coastal_extraction.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json
  - eval/summary.json
  - app/templates/run_detail.html
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Integration tests require DATABASE_URL set in shell env so state-machine tests (snapshot-once, ordering, loop-guard, confirmed_dropped, answered-round) actually execute and PASS — skip != evidence (D-7.5-04)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Supavisor transaction mode, port 6543) — must be in shell env, not just .env"
    dashboard_config:
      - task: "Confirm Plan 03's schema migration was applied (pre_clarify_extracted and clarified_fields columns exist)"
        location: "Run: uv run python -m app.db.bootstrap before this plan's checkpoint"

must_haves:
  truths:
    - "tests/test_resume_pipeline.py exists with 15+ integration tests covering: snapshot_once (D-19), N1 single-_run_stages-call, N2 asked-before-send ordering, D-7.5-10 detect-on-raw pin (test_detect_fired_on_raw — R2-1 proof: first-resume OT 2→None asserts field_regression issue emitted + asked written + clarify sent; NEGATIVE COMPANION shows test fails if detect runs on backfilled data), ordering test D-7.5-04a (paystub OT line reflects carried_forward value), approved-bytes==sent-bytes invariant (D-7.5-04b), D-7.5-04c tri-state through real JSONB path, loop-guard (fires exactly once), confirmed_dropped no-reflag (D-15/N8), mixed-issue SC4, restated-name prior_matches threading (R3-3 integration pin), confirmed_dropped no-reloop on Round-2 (BLOCKER FIX cross-plan key-type consistency), client_supplied classification (R2-3 fix proof), AND D-7.5-11 answered-round tests (test_answered_silence_reaches_approval, test_answered_explicit_zero_not_rebackfilled, test_answered_positive_uses_client_value)"
    - "Each integration test uses the seeded_db / transaction-rollback fixture OR creates its own run and deletes in finally — hermetic per D-7.5-04d (Finding 7 fix: either use an existing rollback fixture from conftest.py OR adopt a consistent per-test finally: DELETE pattern — no manual cleanup drift)"
    - "test_detect_fired_on_raw PASSES (D-7.5-10 three-phase ordering R2-1 proof): TWO-PART test: (Part A) First-resume OT 2→None: asserts (1) field_regression issue emitted, (2) clarified_fields has 'asked' for OT, (3) clarification email written with purpose='clarification_field_regression', (4) run at AWAITING_REPLY — detection fired on RAW before backfill. (Part B) Negative companion: documents (via comment) that if backfill_extracted were called BEFORE detect_field_regression, the OT 2→None drop would be masked — the exact R2-1 failure mode the D-7.5-10 ordering prevents."
    - "test_answered_silence_reaches_approval PASSES (NEW — D-7.5-11 proof, first answered-round test): Round-1 asks OT (snapshot OT=2, clarified_fields = {emp_id: {'hours_overtime': 'asked'}}). Round-2 reply is SILENT on OT (OT=None in raw extraction). Call resume_pipeline (Round-2 path). Assert: (1) run reaches AWAITING_APPROVAL (NOT a second clarification — this is the stranding bug D-7.5-11 fixes); (2) final paystub line item for the employee has hours_overtime == Decimal('2') (carry-forward fired); (3) clarified_fields outcome for hours_overtime is 'carried_forward'. Negative companion comment: if classification ran AFTER _run_stages/decide (the old ordering), the just-answered field would still be 'asked' in the suppression set, _run_stages would re-detect the drop, decide → request_clarification, run would strand at AWAITING_REPLY — the exact D-7.5-11 failure mode this test prevents."
    - "test_answered_explicit_zero_not_rebackfilled PASSES (NEW — D-7.5-11 overpay guard proof): Round-1 asks OT (snapshot OT=2, 'asked'). Round-2 reply has OT=Decimal('0') (explicit zero — client is removing the field). Call resume_pipeline (Round-2 path). Assert: (1) run reaches AWAITING_APPROVAL (no second clarification); (2) final paystub line item hours_overtime is None or Decimal('0') — NOT Decimal('2') (the explicit zero is honored, not re-backfilled back to 2); (3) clarified_fields outcome is 'confirmed_dropped'. The overpay guard: _is_paid(Decimal('0')) is False so explicit zero LOOKS backfillable by value alone; the classify-first step labels it confirmed_dropped and includes it in suppress_detection/resolved_drops — backfill_extracted skips it. This test FAILS if the naive 'also suppress on asked' patch is applied (because it would backfill the explicit zero, setting OT=2 in the paystub — overpay)."
    - "test_answered_positive_uses_client_value PASSES (NEW — D-7.5-11 client_supplied proof): Round-1 asks OT (snapshot OT=2, 'asked'). Round-2 reply has OT=Decimal('5') (positive value — client supplies a different amount). Call resume_pipeline (Round-2 path). Assert: (1) run reaches AWAITING_APPROVAL; (2) final paystub line item hours_overtime == Decimal('5') (the client-supplied value is used, not the snapshot OT=2); (3) clarified_fields outcome is 'client_supplied'."
    - "The D-7.5-04a ordering test PASSES: a carried_forward scenario asserts the FINAL PAYSTUB LINE ITEM has OT=Decimal('2') (not 0/absent), plus a negative companion that would catch a backfill-after-calc regression"
    - "test_restated_name_prior_matches_threading PASSES: exercises the REAL resume_pipeline path end-to-end with a restated name; asserts (1) field_regression detected (AWAITING_REPLY, asked written — R3-3 pin), AND (2) carry-forward Round-2 paystub OT=2 (R2-2 employee_id-keyed backfill pin). FAILS if prior_matches defaulted to None (Part A) OR if backfill_extracted still uses submitted_name-keyed snapshot lookup (Part B)."
    - "test_client_supplied_same_value_labeled_correctly PASSES (R2-3 fix proof): client replies with OT=Decimal('2') same as snapshot; clarified_fields outcome is 'client_supplied' NOT 'carried_forward'."
    - "test_confirmed_dropped_no_reloop_on_round2 PASSES (BLOCKER FIX — cross-plan key-type consistency): Round-1 confirmed_dropped for OT; Round-2 OT silent (M. Chen → Maria Chen restated name, same employee_id); run advances to AWAITING_APPROVAL (N8 suppression fired); OT NOT backfilled (D-14 honored)."
    - "D-7.5-04b approved==sent invariant (Finding 8 fix): test asserts actual confirmation email body or PDF source data reflects the same paystub that was at AWAITING_APPROVAL"
    - "D-7.5-04c tri-state test proves None vs Decimal('0') through the SAME serialization path that writes pre_clarify_extracted (not hand-typed JSON)"
    - "All integration tests PASS (not SKIP) when DATABASE_URL is set — skip != evidence per D-7.5-04"
    - "Three eval fixture pairs (16/17/18) exist in eval/fixtures/ and run_eval.py scores all three correctly; eval/summary.json updated via --record; uv run python eval/run_eval.py --check exits 0"
    - "run_detail.html shows carried-forward, confirmed_dropped, client_supplied, and asked provenance badges on field-regression-affected hours rows (D-7.5-08 — four outcomes); the clarification_field_regression purpose badge is added to the outbound emails section"
  artifacts:
    - path: "tests/test_resume_pipeline.py"
      provides: "15+ hermetic integration tests covering all MONEY-03 state-machine invariants + D-7.5-10 detect-on-raw pin (R2-1) + D-7.5-11 answered-round tests (silence reaches approval, explicit-zero not rebackfilled, positive uses client value) + R2-3 client_supplied pin + ordering + approved==sent + R3-3 restated-name + BLOCKER FIX"
      contains: "test_answered_silence_reaches_approval"
    - path: "eval/run_eval.py"
      provides: "prior_extracted + prior_matches support: strips both keys before InboundEmail, loads prior_extracted as Extracted and prior_matches as list[NameMatchResult], calls detect_field_regression(prior_extracted, extracted, prior_matches, current_matches) -> raw_drops, then validate(extracted, roster, matches, raw_field_drops=raw_drops) — mirrors the production path (D-7.5-10)"
      contains: "prior_extracted"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal.json"
      provides: "MONEY-03 eval fixture with prior_extracted + prior_matches keys (both produced via model_dump/model_validate round-trip, not hand-typed — D-24)"
      contains: "prior_extracted"
    - path: "app/templates/run_detail.html"
      provides: "per-value provenance badges for all four outcomes (carried-forward/client-removed/client-supplied/awaiting-reply) on field-regression hours rows; clarification_field_regression purpose badge"
      contains: "carried-forward"
  key_links:
    - from: "eval/fixtures/18_field_drop_clarify_carry_coastal.json (prior_extracted + prior_matches keys)"
      to: "eval/run_eval.py:detect_field_regression -> validate(raw_field_drops=raw_drops)"
      via: "fixture loader reads prior_extracted (-> Extracted) and prior_matches (-> list[NameMatchResult]); computes current_matches via reconcile_names; calls detect_field_regression(prior_extracted, extracted, prior_matches, current_matches) -> raw_drops; calls validate(extracted, roster, matches, raw_field_drops=raw_drops) — the production D-7.5-10 three-phase path"
      pattern: "detect_field_regression"
    - from: "tests/test_resume_pipeline.py:test_answered_silence_reaches_approval"
      to: "app/pipeline/orchestrator.py:resume_pipeline Round-2 D-7.5-11 classify-first path"
      via: "D-7.5-11 proof: Round-1 asks OT; Round-2 reply silent (OT=None) → classify-first labels carried_forward, suppress_detection includes it → _run_stages runs once → AWAITING_APPROVAL, paystub OT=2. Negative companion: classify-after-decide ordering would strand the run (the D-7.5-11 bug)."
      pattern: "answered_silence"
    - from: "tests/test_resume_pipeline.py:test_answered_explicit_zero_not_rebackfilled"
      to: "app/pipeline/orchestrator.py:backfill_extracted (resolved_drops overpay guard)"
      via: "D-7.5-11 overpay guard: OT=Decimal('0') in raw reply → classify-first labels confirmed_dropped, included in suppress_detection → backfill_extracted skips OT → paystub OT=0 (not 2). _is_paid(Decimal('0')) is False so the guard is the resolved_drops gate, not the value test."
      pattern: "answered_explicit_zero"
    - from: "tests/test_resume_pipeline.py:test_answered_positive_uses_client_value"
      to: "app/pipeline/orchestrator.py:resume_pipeline Round-2 classify-first + backfill_extracted"
      via: "D-7.5-11: OT=Decimal('5') in raw reply → classify-first labels client_supplied, included in suppress_detection → backfill_extracted skips OT → paystub OT=5 (client value, not snapshot OT=2)."
      pattern: "answered_positive"
    - from: "tests/test_resume_pipeline.py:test_detect_fired_on_raw"
      to: "app/pipeline/orchestrator.py:_run_stages (detect_field_regression called on raw extracted before backfill_extracted)"
      via: "D-7.5-10 ordering proof: OT 2→None drop is visible to detection on RAW; field_regression issue emitted; asked written; clarify sent. Negative companion proves masking if detect were deferred."
      pattern: "detect_fired_on_raw"
    - from: "tests/test_resume_pipeline.py:test_ordering_carried_forward_ot_in_paystub"
      to: "app/pipeline/orchestrator.py:backfill_extracted in _run_stages seam"
      via: "asserts paystub OT line == Decimal('2') after carried_forward backfill — fails if backfill runs after calc (pins R3-1)"
      pattern: "carried_forward"
    - from: "tests/test_resume_pipeline.py:test_client_supplied_same_value_labeled_correctly"
      to: "app/pipeline/orchestrator.py:resume_pipeline Round-2 classify-first classifier"
      via: "R2-3 fix proof: client supplies OT=2 (same as snapshot) → classify-first sees present-positive in raw → client_supplied. Before D-7.5-11 fix: post-decide classifier saw backfilled data and mislabeled as carried_forward."
      pattern: "client_supplied"
    - from: "tests/test_resume_pipeline.py:test_restated_name_prior_matches_threading"
      to: "app/pipeline/orchestrator.py:resume_pipeline -> _run_stages(prior_matches=prior_matches)"
      via: "asserts field_regression detected (R3-3) on Round-1; then carry-forward Round-2 carries OT=2 to final paystub (R2-2 pin)"
      pattern: "prior_matches"
    - from: "tests/test_resume_pipeline.py:test_confirmed_dropped_no_reloop_on_round2"
      to: "app/pipeline/orchestrator.py:suppress_detection + app/pipeline/validate.py:N8 check"
      via: "Round-1 confirmed_dropped(M. Chen OT) → Round-2 Maria Chen OT=None → suppress_detection includes (chen_id, hours_overtime) → NO re-clarify; FAILS if set keyed by submitted_name OR if str(current_emp_id) missing"
      pattern: "confirmed_dropped_no_reloop"
    - from: "tests/test_resume_pipeline.py:test_approved_bytes_equals_sent_bytes"
      to: "app/pipeline/orchestrator.py:_deliver + compose_confirmation + PDF generation"
      via: "asserts actual confirmation body/PDF source data matches AWAITING_APPROVAL paystub (Finding 8 strengthened invariant)"
      pattern: "confirmation"
---

<objective>
Complete the MONEY-03 evidence: replace xfail stubs with 15+ PASSING hermetic integration tests (including D-7.5-11 answered-round tests), add eval fixtures 16/17/18, update run_eval.py with prior_extracted support, and add the D-7.5-08 provenance badge (four outcomes) to the run-detail approval view.

**Reviews addressed in this plan:**

- **R3 / D-7.5-11 (HIGH — answered-round tests, the Round-2 stranding fix proof):** Three new integration tests prove the classify-first ordering on answered rounds:
  - `test_answered_silence_reaches_approval`: Round-1 asks OT; Round-2 silent on OT → run reaches AWAITING_APPROVAL (NOT a second clarification), paystub OT=2, outcome carried_forward. Negative companion: classify-after-decide ordering would strand the run.
  - `test_answered_explicit_zero_not_rebackfilled`: Round-2 OT=0 → AWAITING_APPROVAL, paystub OT=0 (NOT re-backfilled to 2), outcome confirmed_dropped. Tests the overpay guard: _is_paid(Decimal('0')) is False, but resolved_drops gate protects it.
  - `test_answered_positive_uses_client_value`: Round-2 OT=5 → AWAITING_APPROVAL, paystub OT=5, outcome client_supplied.
  These are the answered-round counterparts to `test_detect_fired_on_raw` (which covers Round-1 first-detection). Together all three rounds are now tested.

- **R2-1 (HIGH — D-7.5-10 three-phase detect-on-raw proof):** `test_detect_fired_on_raw` — unchanged from previous revision. Proves detect fires on RAW before backfill (Part A: field_regression emitted, asked written, clarify sent, AWAITING_REPLY). Negative companion documents R2-1 masking failure mode.

- **R2-3 (MED — client_supplied classification proof):** `test_client_supplied_same_value_labeled_correctly` — unchanged. Client replies OT=2 (same as snapshot) → no field_regression issue → client_supplied (not carried_forward).

- **test_restated_name_prior_matches_threading strengthened:** Part A (Round-1 detection) + Part B (Round-2 carry-forward paystub OT=2) — unchanged from previous revision.

- **Findings 7, 8, R3-3 (prior_matches threading), BLOCKER FIX (key-type consistency):** Unchanged from previous revision.

D-23 two-layer evidence:
- Eval layer: fixtures 16/17/18. Fixture 18 certifies MONEY-03 judgment (detect_field_regression → validate(raw_field_drops=) → decide) via the production D-7.5-10 three-phase path.
- Integration layer: 15+ state-machine invariants — all prior 12 PLUS the three D-7.5-11 answered-round tests (silence/explicit-zero/positive).

Phase-gate: all integration tests PASS WITH DATABASE_URL set (not SKIP). Eval --check passes. Human checkpoint signs off.

Output: full MONEY-03 evidence assembled; phase ready for verification.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-03-SUMMARY.md

@tests/test_clarify.py
@tests/conftest.py
@eval/run_eval.py
@eval/fixtures/07_missing_hours_coastal.json
@app/templates/run_detail.html
@app/db/seed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 15+ hermetic integration tests covering all MONEY-03 invariants + D-7.5-11 answered-round tests + D-7.5-10 detect-on-raw pin + R2-3 client_supplied pin + BLOCKER FIX + approved==sent</name>
  <files>tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/conftest.py in full. Look specifically for:
  - Any transaction-rollback fixture (seeded_db, db_conn, or similar) that rolls back after each test
  - Any helper for creating payroll runs in test state
  - The established DB connection pattern for tests

  Read tests/test_clarify.py in full for the established pattern:
  - class _DraftLLM and its call_text method — copy this mock LLM pattern
  - pytest.mark.skipif(not os.environ.get("DATABASE_URL"), ...) — use this exact module-level form
  - Run creation via repo helpers (repo.create_run or direct INSERT)
  - Finally: cleanup with DELETE FROM payroll_runs WHERE id = %s
  - The established psycopg.connect pattern (direct connection, not pool)

  Read app/db/seed.py to identify coastal business employees — use real names and employee_ids for fixture data. Note which employees are hourly (need for D-7.5-04a ordering test with real calc). Also look for any employee whose name could be abbreviated (e.g., "Maria Chen" could be stored as the full name but submitted as "M. Chen") — needed for tests 9, 10, 11, and the D-7.5-11 answered-round tests.

  PATTERNS.md §8 has the _extracted() and _match() builder helper style from test_validate.py — copy for test setup.

  Finding 7 hermetic discipline: Read conftest.py first. If it has a transaction-rollback fixture, use it consistently in ALL 15 tests. If not, every test uses the finally: DELETE pattern from test_clarify.py. Do NOT mix approaches. Document which approach was chosen in a comment at the top of the file.

  Finding 8 approval path: Read app/main.py around the approval CAS (line ~492) and whatever comes after — the _deliver call, compose_confirmation, PDF generation. Understand what confirmation composition takes as input so test 5 can assert it uses the AWAITING_APPROVAL paystub values.

  For tests 13-15 (D-7.5-11 answered-round tests): These exercise the Round-2 classify-first path in Plan 03. Each sets up a run with pre_clarify_extracted snapshot (OT=2) and clarified_fields with 'asked' for OT (is_round_2=True), then calls resume_pipeline with a Round-2 reply. The three tests differ only in the raw reply's OT value: None (silence), Decimal('0') (explicit zero), Decimal('5') (positive).

  For the negative companion in test 13: document the classify-AFTER-decide failure mode as a comment. The failure: if classify-first is removed and the old classify-after ordering is restored, the just-answered field stays 'asked' in _resolved_by_name (Step E2 builds the set from TERMINAL outcomes only — 'asked' is not terminal), _run_stages runs without suppress_detection for the answered field, detect_field_regression sees OT 2→None as a new drop, decide → request_clarification, run strands at AWAITING_REPLY. The test would then assert AWAITING_APPROVAL and fail.
  </read_first>

  <action>
  Create tests/test_resume_pipeline.py with 15 tests.

  Module header:
  - pytestmark = pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="requires live DATABASE_URL — set before phase gate; skip != evidence (D-7.5-04)")
  - A comment block at the top stating: "Hermetic discipline (D-7.5-04d / Finding 7): [describe chosen approach — rollback fixture or per-test finally DELETE]"

  Helper utilities at top of file (from PATTERNS.md §8):
  - `_mk_extracted(employees_data, pay_period_start=None) -> Extracted`: builds an Extracted from a list of dicts
  - `_mk_match(name, emp_id, source="exact") -> NameMatchResult`: builds a NameMatchResult

  Test 1 — test_snapshot_once_not_overwritten (D-19/D-28):
  Two set_pre_clarify_extracted calls; second is a no-op (IS NULL guard). Assert first value preserved.

  Test 2 — test_n1_single_run_stages_call (N1):
  Monkeypatch _run_stages; assert called exactly once per resume_pipeline invocation.

  Test 3 — test_n2_asked_written_before_send (N2 — asked-before-send ordering):
  Monkeypatch _clarify to capture the moment of its call; assert clarified_fields has "asked" at that instant.
  Assert _clarify called with purpose="clarification_field_regression".

  Test 4 — test_ordering_carried_forward_ot_in_paystub (D-7.5-04a — THE ROOT CAUSE PIN):
  Round-2 path (clarified has 'asked' for OT). Mock LLM returns OT=None (silence).
  Assert final paystub line item hours_overtime == Decimal("2") (backfill landed before calc).
  Negative companion comment: removing backfill_extracted call causes this test to FAIL (proves it pins the ordering).

  Test 5 — test_approved_bytes_equals_sent_bytes (D-7.5-04b — STRENGTHENED per Finding 8):
  Run through to AWAITING_APPROVAL with carried-forward OT=2. Approve. Invoke confirmation path.
  Assert confirmation body OR PDF source data CONTAINS OT=2 (same value that was at AWAITING_APPROVAL).

  Test 6 — test_tri_state_through_real_path (D-7.5-04c):
  set_pre_clarify_extracted then load_pre_clarify_extracted via real JSONB serialization.
  Assert None survives as None; Decimal("0") survives as Decimal("0").

  Test 7 — test_loop_guard_fires_exactly_once:
  Round-1 sets 'asked'. Round-2 reply OT=None (silence). Assert run at AWAITING_APPROVAL and no second clarification email sent.

  Test 8 — test_mixed_issue_records_asked_and_asks_field_regression (SC4 / R3-2):
  Both field_regression issue AND normal missing-field issue. Assert clarified_fields has 'asked', outbound email with purpose='clarification_field_regression' written, email body contains both asks.

  Test 9 — test_restated_name_prior_matches_threading (R3-3 integration pin — STRENGTHENED for R2-2):
  PART A (Round-1): "M. Chen" OT=2 in snapshot; "Maria Chen" OT=None in Round-1 reply, same employee_id.
  Assert run at AWAITING_REPLY, 'asked' written, field_regression detected.
  Comment: R3-3 integration pin — fails if prior_matches is defaulted to None.

  PART B (Round-2, R2-2 fix): Continue to Round-2. "Maria Chen" OT=None still silent.
  Assert final paystub OT=Decimal("2") (employee_id-keyed backfill_extracted carried the value).
  Assert clarified_fields outcome 'carried_forward'.
  Comment: R2-2 fix pin — fails if backfill_extracted uses submitted_name-keyed snapshot lookup.

  Test 10 — test_confirmed_dropped_no_reloop_on_round2 (BLOCKER FIX — cross-plan key-type consistency):
  Round-1 confirmed_dropped for OT (M. Chen → Maria Chen, same employee_id). Round-2 reply OT=None.
  Assert OT NOT backfilled in paystub. Assert run at AWAITING_APPROVAL (N8 suppression fired).
  Comment block: FAILS for any of three regressions: set keyed by submitted_name, str(current_emp_id) missing, backfill_extracted guard keyed by name.

  Test 11 — test_detect_fired_on_raw (D-7.5-10 three-phase ordering R2-1 proof):
  PART A: Round-1 path, "Alice Torres" OT=2 in snapshot, Round-1 reply OT=None.
  Assert field_regression emitted on RAW (run at AWAITING_REPLY, 'asked' written before send, clarify sent).
  Comment: D-7.5-10 detect-on-raw proof — if backfill ran before detect, OT 2 on both sides, no drop, no issue, no clarification.

  PART B: Continue to Round-2 carry-forward. Assert paystub OT=Decimal("2") (full round-trip).
  Comment: Full round-trip proof — both tests together prove D-7.5-10.

  Test 12 — test_client_supplied_same_value_labeled_correctly (R2-3 fix proof):
  Round-2 reply OT=Decimal("2") (same as snapshot OT=2 — client re-confirms).
  Assert clarified_fields outcome is "client_supplied" NOT "carried_forward".
  Assert run at AWAITING_APPROVAL.
  Comment: R2-3 fix proof — classify-first reads raw reply before backfill; present-positive → client_supplied.

  Test 13 — test_answered_silence_reaches_approval (NEW — D-7.5-11 proof):
  Setup:
  - Coastal hourly employee (e.g., "Alice Torres"), employee_id = torres_id.
  - pre_clarify_extracted: "Alice Torres" hours_overtime=Decimal("2"), hours_regular=Decimal("40").
  - Persist reconciliation mapping "Alice Torres" → torres_id (prior_matches source).
  - clarified_fields = {str(torres_id): {"hours_overtime": "asked"}} (is_round_2=True).
  - Configure mock LLM to return extraction for Round-2 with "Alice Torres" OT=None (SILENCE).
  - Seed a real hourly employee record and roster so calc can produce actual paystub line items.
  - Call resume_pipeline (Round-2 path).

  Assertions:
  1. Run status == RunStatus.AWAITING_APPROVAL (NOT AWAITING_REPLY — no second clarification).
  2. Final paystub line item for torres_id has hours_overtime == Decimal("2") (carry-forward fired, not dropped).
  3. clarified_fields outcome for hours_overtime for str(torres_id) is "carried_forward".
  4. No second outbound email_messages row with purpose="clarification_field_regression" created.

  Comment:
  # D-7.5-11 proof: answered-silence reaches AWAITING_APPROVAL in a single _run_stages call.
  # The fix (classify-first): classify-first labels OT 'carried_forward', includes it in
  # suppress_detection. _run_stages: N8 suppresses field_regression re-emission for OT.
  # decide → process → run reaches AWAITING_APPROVAL. Paystub OT=2 (backfill fired in Phase 2).
  #
  # NEGATIVE COMPANION — the D-7.5-11 failure mode (classify-after-decide ordering):
  # If classify-first is removed and the old ordering is restored:
  # Step E2 builds _resolved_by_name from TERMINAL outcomes only ('asked' is not terminal).
  # The just-answered OT field is NOT in _resolved_by_name.
  # _run_stages(resolved_drops=_resolved_by_name) does NOT suppress OT in N8.
  # detect_field_regression sees OT: 2→None (a drop) → field_regression issue emitted.
  # decide → request_clarification → _run_stages DEFERS, skips _compute_line_items.
  # Run re-clarifies (strands at AWAITING_REPLY). This test then FAILS assertion (1)
  # (expected AWAITING_APPROVAL, got AWAITING_REPLY) — the exact D-7.5-11 stranding bug.

  Test 14 — test_answered_explicit_zero_not_rebackfilled (NEW — D-7.5-11 overpay guard proof):
  Setup: same as test 13 (torres_id, OT=2 in snapshot, 'asked' for OT), EXCEPT:
  - Configure mock LLM to return extraction for Round-2 with "Alice Torres" OT=Decimal("0") (EXPLICIT ZERO).
  - Call resume_pipeline (Round-2 path).

  Assertions:
  1. Run status == RunStatus.AWAITING_APPROVAL (no second clarification).
  2. Final paystub line item for torres_id has hours_overtime that is None or Decimal("0") — NOT Decimal("2").
     This is the critical assertion: the explicit zero is honored; the snapshot value is NOT re-backfilled.
  3. clarified_fields outcome for hours_overtime for str(torres_id) is "confirmed_dropped".

  Comment:
  # D-7.5-11 overpay guard proof: explicit-zero answered field is NOT re-backfilled.
  # _is_paid(Decimal('0')) is False — explicit zero LOOKS backfillable by value alone.
  # The classify-first step labels OT 'confirmed_dropped' and includes it in suppress_detection.
  # backfill_extracted: (torres_id, 'hours_overtime') in resolved_drops (suppress_detection) → SKIP backfill.
  # Paystub OT=0 (or absent), not OT=2.
  #
  # The naive "also suppress on asked" patch would NOT protect this: if we had added 'asked'
  # to _resolved_by_name without classify-first, the suppress set is populated but backfill_extracted
  # doesn't know whether to skip because the set only blocks re-detection, not re-backfill.
  # The actual fix is that classify-first determines confirmed_dropped (not just 'suppress for asked')
  # so backfill_extracted's check (emp_id_str, field) in resolved_drops correctly excludes it.
  # Test FAILS if paystub OT=2 (re-backfilled — overpay).

  Test 15 — test_answered_positive_uses_client_value (NEW — D-7.5-11 client_supplied proof):
  Setup: same as test 13 (torres_id, OT=2 in snapshot, 'asked' for OT), EXCEPT:
  - Configure mock LLM to return extraction for Round-2 with "Alice Torres" OT=Decimal("5") (POSITIVE VALUE).
  - Call resume_pipeline (Round-2 path).

  Assertions:
  1. Run status == RunStatus.AWAITING_APPROVAL.
  2. Final paystub line item for torres_id has hours_overtime == Decimal("5") (client-supplied value used, not snapshot OT=2).
  3. clarified_fields outcome for hours_overtime for str(torres_id) is "client_supplied".

  Comment:
  # D-7.5-11 client_supplied proof: positive answered field uses the client's value.
  # classify-first: OT=Decimal('5') in raw reply → present-positive → client_supplied.
  # (torres_id, 'hours_overtime') added to suppress_detection.
  # backfill_extracted: field in resolved_drops (suppress_detection) → SKIP backfill.
  # BUT the raw extracted already has OT=5, so _compute_line_items uses OT=5.
  # Paystub OT=5 (client value), labeled 'client_supplied'.

  Each test has a finally: block performing cleanup via the method chosen in the module header.
  </action>

  <verify>
    <automated>uv run pytest tests/test_resume_pipeline.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - `tests/test_resume_pipeline.py` exists with 15+ test functions
  - `grep "^def test_" tests/test_resume_pipeline.py | wc -l` returns 15+
  - `grep "test_detect_fired_on_raw" tests/test_resume_pipeline.py` exits 0 (D-7.5-10 R2-1 proof)
  - `grep "D-7.5-10 detect-on-raw proof" tests/test_resume_pipeline.py` exits 0
  - `grep "test_answered_silence_reaches_approval" tests/test_resume_pipeline.py` exits 0 (NEW — D-7.5-11 silence)
  - `grep "test_answered_explicit_zero_not_rebackfilled" tests/test_resume_pipeline.py` exits 0 (NEW — D-7.5-11 overpay guard)
  - `grep "test_answered_positive_uses_client_value" tests/test_resume_pipeline.py` exits 0 (NEW — D-7.5-11 client value)
  - `grep "D-7.5-11 proof" tests/test_resume_pipeline.py` exits 0 (comment in test 13)
  - `grep "D-7.5-11 overpay guard proof" tests/test_resume_pipeline.py` exits 0 (comment in test 14)
  - `grep "D-7.5-11 client_supplied proof" tests/test_resume_pipeline.py` exits 0 (comment in test 15)
  - `grep "NEGATIVE COMPANION" tests/test_resume_pipeline.py` exits 0 (negative companion comment in test 13)
  - `grep "classify-after-decide\|classify-first" tests/test_resume_pipeline.py` exits 0 (documents the ordering)
  - `grep "test_client_supplied_same_value_labeled_correctly" tests/test_resume_pipeline.py` exits 0 (R2-3 proof)
  - `grep "R2-3 fix proof" tests/test_resume_pipeline.py` exits 0
  - `grep "test_ordering_carried_forward_ot_in_paystub" tests/test_resume_pipeline.py` exits 0 (D-7.5-04a)
  - `grep "test_approved_bytes_equals_sent_bytes" tests/test_resume_pipeline.py` exits 0 (D-7.5-04b)
  - `grep "test_tri_state_through_real_path" tests/test_resume_pipeline.py` exits 0 (D-7.5-04c)
  - `grep "test_mixed_issue" tests/test_resume_pipeline.py` exits 0 (SC4)
  - `grep "test_restated_name_prior_matches_threading" tests/test_resume_pipeline.py` exits 0 (R3-3)
  - `grep "R3-3 integration pin" tests/test_resume_pipeline.py` exits 0
  - `grep "R2-2 fix pin" tests/test_resume_pipeline.py` exits 0 (test 9 Part B)
  - `grep "test_confirmed_dropped_no_reloop_on_round2" tests/test_resume_pipeline.py` exits 0 (BLOCKER FIX)
  - `grep "BLOCKER FIX cross-plan key-type" tests/test_resume_pipeline.py` exits 0
  - `grep "pytestmark" tests/test_resume_pipeline.py` exits 0
  - `grep "finally:" tests/test_resume_pipeline.py | wc -l` returns 15+ (one per test)
  - `grep "# Hermetic discipline" tests/test_resume_pipeline.py` exits 0 (Finding 7)
  - `grep "confirmation\|compose_confirmation\|_deliver\|PDF\|line_item" tests/test_resume_pipeline.py` exits 0 (Finding 8)
  - WITH DATABASE_URL set: `uv run pytest tests/test_resume_pipeline.py -v` shows 15+ PASSED, 0 SKIPPED, 0 FAILED
  - `uv run pytest tests/test_resume_pipeline.py -k "answered_silence_reaches_approval" -v` PASSES (D-7.5-11 silence)
  - `uv run pytest tests/test_resume_pipeline.py -k "answered_explicit_zero" -v` PASSES (D-7.5-11 overpay guard)
  - `uv run pytest tests/test_resume_pipeline.py -k "answered_positive" -v` PASSES (D-7.5-11 client value)
  - `uv run pytest tests/test_resume_pipeline.py -k "detect_fired_on_raw" -v` PASSES (R2-1 D-7.5-10 proof)
  - `uv run pytest tests/test_resume_pipeline.py -k "client_supplied_same_value" -v` PASSES (R2-3)
  - `uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v` PASSES
  - `uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v` PASSES (R3-3 + R2-2)
  - `uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v` PASSES (BLOCKER FIX)
  </acceptance_criteria>

  <done>15 integration tests PASS (not SKIP) with DATABASE_URL set. D-7.5-11 three answered-round tests: test_answered_silence_reaches_approval (silence → AWAITING_APPROVAL, OT=2, carried_forward, no second clarification; negative companion documents classify-after-decide stranding); test_answered_explicit_zero_not_rebackfilled (explicit-zero → AWAITING_APPROVAL, OT=0/absent, confirmed_dropped; overpay guard: _is_paid(Decimal('0')) is False but resolved_drops gate protects it); test_answered_positive_uses_client_value (OT=5 → AWAITING_APPROVAL, OT=5, client_supplied). test_detect_fired_on_raw (test 11) pins D-7.5-10: OT 2→None visible on RAW (Part A), carry-forward Round-2 paystub OT=2 (Part B). test_client_supplied_same_value (test 12) pins R2-3. test_restated_name (test 9) strengthened with Part B. test_confirmed_dropped_no_reloop (test 10) pins BLOCKER FIX. All 15 tests hermetic. D-7.5-04a/b/c ordering/approved/tri-state. SC4 mixed-issue.</done>
</task>

<task type="auto">
  <name>Task 2: Eval fixtures 16/17/18 + run_eval.py prior_extracted + summary regen + run_detail.html provenance badges (4 outcomes)</name>
  <files>eval/run_eval.py, eval/fixtures/16_zero_hours_hourly_coastal.json, eval/fixtures/16_zero_hours_hourly_coastal_extraction.json, eval/fixtures/17_nfd_name_coastal.json, eval/fixtures/17_nfd_name_coastal_extraction.json, eval/fixtures/18_field_drop_clarify_carry_coastal.json, eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json, eval/summary.json, app/templates/run_detail.html</files>

  <read_first>
  Read eval/run_eval.py in full. Key anchors:
  - The fixture loader (wherever "expected" and "fixture_category" are stripped before InboundEmail construction) — add "prior_extracted" and "prior_matches" to the strip set.
  - Where validate() is called — replace the dead prior= path with the D-7.5-10 three-phase production path.
  - The --record and --check flag behavior + summary.json path.
  - The extraction cache writer (extracted.model_dump_json(indent=2)) — confirms D-24 compliance.

  Read eval/fixtures/07_missing_hours_coastal.json to get the exact fixture structure.

  Read app/db/seed.py to find coastal business employees for plausible names in fixtures 16/17/18.

  Read app/templates/run_detail.html lines 120-155 (the extracted data per-employee table) and lines 195-215 (the outbound emails section with purpose badge).

  Finding 6 note: run_detail.html now needs badges for ALL FOUR outcomes: carried-forward, confirmed_dropped (client removed), client_supplied (new from R2-3 fix), and asked (awaiting reply).

  Fixture 17 HONESTY check: if the coastal seed has no accented-name employee, use a coastal employee's name with extra internal whitespace as the submitted name — certifies _normalize whitespace normalization / C-4 parity. Document scope in SUMMARY: "certifies eval scorer _normalize parity, NOT full NFD Unicode (that proof is test_reconcile.py)."
  </read_first>

  <action>
  Step A — Update eval/run_eval.py (D-7.5-10 eval-wiring fix — replaces dead prior= path):

  1. In the fixture loader dict-stripping block, add BOTH "prior_extracted" and "prior_matches" to the excluded keys. Save both raw values before exclusion:
       prior_extracted_raw = raw.get("prior_extracted")
       prior_matches_raw   = raw.get("prior_matches")
     Extend exclusion set to {"expected", "fixture_category", "prior_extracted", "prior_matches"}.

  2. In _score_fixture, after building expected_extracted and before validate(), add:

     a. Deserialize prior_extracted (if present):
        prior_extracted = (
            Extracted.model_validate(prior_extracted_raw)
            if prior_extracted_raw is not None else None
        )

     b. Deserialize prior_matches (if present):
        prior_matches = (
            [NameMatchResult.model_validate(m) for m in prior_matches_raw]
            if prior_matches_raw is not None else None
        )

  3. Replace the existing validate() call with the three-phase production path:

        if prior_extracted is not None:
            # D-7.5-10 three-phase path: detect on raw -> validate with pre-computed drops
            from app.pipeline.validate import detect_field_regression
            raw_drops = detect_field_regression(
                prior_extracted, expected_extracted, prior_matches, matches
            )
            issues = validate(expected_extracted, roster, matches, raw_field_drops=raw_drops)
        else:
            issues = validate(expected_extracted, roster, matches)

  NOTE: When prior_matches is None, detect_field_regression returns [] (honest no-op). Fixture 18 MUST include prior_matches. Fixtures 16 and 17 have no prior_extracted → else branch fires (no regression possible).

  Step B — Write a generation script (scratchpad, NOT committed) to produce fixture extraction caches via model_dump_json (D-24). Script path: /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/2948e6e3-bf23-44c6-b200-d9c6281e738e/scratchpad/gen_75_fixtures.py

  Step C — Create the six fixture files:

  Fixture 16 (MONEY-01 — zero-hours hourly):
  - Pick a real coastal hourly employee from seed.py.
  - expected.decision.final_action="request_clarification"; fixture_category="zero-hours-hourly".
  - Extraction cache: hours_regular="0.00", all other hours null (via model_dump_json).

  Fixture 17 (MONEY-02 / C-4 eval-scorer parity):
  - Pick a real coastal employee. Use their name with extra internal whitespace as submitted_name.
  - Expected: final_action="process", resolved=True.
  - fixture_category="nfc-normalizer-parity".
  - Extraction cache: Extracted with the extra-whitespace submitted name (via model_dump_json).

  Fixture 18 (MONEY-03 — field regression with prior_extracted + prior_matches):
  - Use a real coastal hourly employee, e.g., "Maria Chen" / chen_id.
  - Scenario: snapshot OT=2; clarification reply (resumed extraction) has OT=None (client silent — drop detected).
  - prior_extracted: Extracted with "Maria Chen" hours_overtime=Decimal("2"), hours_regular=Decimal("40"). Produced via model_dump(mode="json").
  - prior_matches: [NameMatchResult(submitted_name="Maria Chen", matched_employee_id=chen_id, source="exact", resolved=True, reason="exact match").model_dump(mode="json")]. Enables detect_field_regression to resolve chen_id.
  - Resumed extraction (main fixture + extraction cache): "Maria Chen" hours_overtime=null, hours_regular=Decimal("40").
  - expected.decision.final_action = "request_clarification"
  - expected.decision.gate_reasons_contains = ["field regression"]

  Step D — Run the generation script and verify D-24 compliance:
  uv run python /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/2948e6e3-bf23-44c6-b200-d9c6281e738e/scratchpad/gen_75_fixtures.py
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json').read())); print('Fixture 18 OK OT:', e.employees[0].hours_overtime)"

  Step E — Run eval and regenerate summary.json:
  uv run python eval/run_eval.py 2>&1 | tail -30
  uv run python eval/run_eval.py --record
  uv run python eval/run_eval.py --check 2>&1 | tail -5

  Step F — Add provenance badges to app/templates/run_detail.html (D-7.5-08):

  1. In the per-employee hours rows section (~lines 143-150), add a Jinja2 conditional badge after each hours value cell. Mirror the existing name-source badge style exactly (PATTERNS.md §7). FOUR outcomes (Finding 6 fix + R2-3 fix + D-7.5-11):
     - "carried_forward" -> badge-pending "carried-forward" (client was SILENT — D-7.5-10b/D-7.5-11; value from snapshot, NOT same-value resupply which is client_supplied)
     - "confirmed_dropped" -> badge-neutral "client removed" (explicit zero/none from client; D-7.5-11 overpay guard)
     - "client_supplied" -> badge-good "client supplied" (positive replacement from client — present-positive in raw reply before backfill; R2-3 fix)
     - "asked" -> badge-bad "awaiting reply" (field-regression question sent; reply pending)

  2. In the outbound emails section (~lines 202-208), add a case for 'clarification_field_regression':
     `{% elif msg.purpose == 'clarification_field_regression' %}<span class="badge badge-pending">field-regression-clarification</span>`

  3. Ensure run_detail.html receives clarified_fields in the run dict. Check main.py for the run_detail endpoint — add `repo.load_clarified_fields(run_id)` to the run context dict passed to the template, if not already present.
  </action>

  <verify>
    <automated>uv run python eval/run_eval.py --check 2>&1 | tail -10; uv run pytest -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "prior_extracted" eval/run_eval.py` exits 0 (stripping and loading logic)
  - `grep "prior_matches" eval/run_eval.py` exits 0 (prior_matches stripped and deserialized)
  - `grep "detect_field_regression" eval/run_eval.py` exits 0 (production D-7.5-10 path called)
  - `grep -v "prior="` eval/run_eval.py | grep "validate(" exits 0, AND `grep "validate.*prior=" eval/run_eval.py` exits 1 (dead prior= kwarg gone)
  - All 6 fixture files exist: `ls eval/fixtures/{16,17,18}_*.json | wc -l` returns 6
  - `grep "prior_extracted" eval/fixtures/18_field_drop_clarify_carry_coastal.json` exits 0
  - `grep "prior_matches" eval/fixtures/18_field_drop_clarify_carry_coastal.json` exits 0
  - Fixture 18 produces final_action="request_clarification": `uv run python -c "import json; from eval.run_eval import _score_fixture, _load_fixture; import pathlib; p=pathlib.Path('eval/fixtures/18_field_drop_clarify_carry_coastal.json'); r=_load_fixture(p); res=_score_fixture(r,p); assert res['decision']['final_action']=='request_clarification', res['decision']; print('Fixture 18 OK:', res['decision']['final_action'])"` exits 0
  - Extraction caches parse via Extracted.model_validate without error (D-24 compliance)
  - `uv run python eval/run_eval.py --check` exits 0
  - `grep "carried-forward" app/templates/run_detail.html` exits 0 (D-7.5-08)
  - `grep "client.removed\|client removed" app/templates/run_detail.html` exits 0 (confirmed_dropped badge)
  - `grep "client.supplied\|client supplied" app/templates/run_detail.html` exits 0 (client_supplied — R2-3 fix + D-7.5-11)
  - `grep "awaiting.reply\|awaiting reply" app/templates/run_detail.html` exits 0 (asked badge)
  - `grep "clarification_field_regression" app/templates/run_detail.html` exits 0 (purpose badge)
  - `uv run pytest -q` full suite exits 0
  </acceptance_criteria>

  <done>Fixtures 16/17/18 exist and score correctly. run_eval.py: prior_extracted + prior_matches stripped and deserialized; D-7.5-10 three-phase path (detect_field_regression → validate(raw_field_drops=)) called when prior_extracted present; dead prior= kwarg gone; fixtures 16/17 unaffected. Fixture 18 produces final_action="request_clarification". summary.json regenerated, --check passes. run_detail.html: four provenance badges (carried-forward / client-removed / client-supplied / awaiting-reply) + clarification_field_regression purpose badge. Full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1: 15 hermetic integration tests in tests/test_resume_pipeline.py.

  NEW D-7.5-11 TESTS:
  - test_answered_silence_reaches_approval (test 13): Round-1 asks OT; Round-2 silent (OT=None) → AWAITING_APPROVAL, paystub OT=2, carried_forward. Negative companion: classify-after-decide ordering strands the run (AWAITING_REPLY) — the exact D-7.5-11 bug this fixes.
  - test_answered_explicit_zero_not_rebackfilled (test 14): Round-2 OT=Decimal('0') → AWAITING_APPROVAL, paystub OT=0 (NOT re-backfilled to 2), confirmed_dropped. Overpay guard: _is_paid(Decimal('0')) is False but resolved_drops gate protects it.
  - test_answered_positive_uses_client_value (test 15): Round-2 OT=Decimal('5') → AWAITING_APPROVAL, paystub OT=5, client_supplied.

  These three tests are the answered-round counterparts to test_detect_fired_on_raw (which covers Round-1 first-detection). Together all four tests confirm both rounds are tested end-to-end.

  All prior 12 tests preserved: test_detect_fired_on_raw (D-7.5-10 R2-1 proof), test_client_supplied_same_value_labeled_correctly (R2-3), test_restated_name_prior_matches_threading strengthened with Part B (R2-2 + R3-3), test_confirmed_dropped_no_reloop_on_round2 (BLOCKER FIX), test_ordering_carried_forward_ot_in_paystub (D-7.5-04a), test_approved_bytes_equals_sent_bytes (D-7.5-04b), test_tri_state_through_real_path (D-7.5-04c), test_loop_guard_fires_exactly_once, test_mixed_issue (SC4/R3-2). All 15 hermetic.

  Task 2: Fixtures 16/17/18, run_eval.py D-7.5-10 wiring, summary.json, run_detail.html four provenance badges.
  </what-built>
  <how-to-verify>
  PHASE GATE — DATABASE_URL must be set in shell env. All integration tests must PASS (not SKIP).

  1. Integration tests (D-7.5-04 phase-completion gate):
     uv run pytest tests/test_resume_pipeline.py -v
     Expected: 15+ PASSED, 0 SKIPPED, 0 FAILED.

  2. D-7.5-11 answered-round tests (the new tests from Round 3 review):
     uv run pytest tests/test_resume_pipeline.py -k "answered_silence_reaches_approval" -v
     Expected: PASSED. Verify: AWAITING_APPROVAL (NOT AWAITING_REPLY), OT=2 in paystub, 'carried_forward'.
     Verify: negative companion comment present (classify-after-decide failure mode documented).

     uv run pytest tests/test_resume_pipeline.py -k "answered_explicit_zero" -v
     Expected: PASSED. Verify: AWAITING_APPROVAL, OT=0/absent (NOT 2), 'confirmed_dropped'.
     Verify: overpay guard comment present (_is_paid(Decimal('0')) is False; resolved_drops gate protects).

     uv run pytest tests/test_resume_pipeline.py -k "answered_positive" -v
     Expected: PASSED. Verify: AWAITING_APPROVAL, OT=5, 'client_supplied'.

  3. D-7.5-10 three-phase ordering proof (test 11):
     uv run pytest tests/test_resume_pipeline.py -k "detect_fired_on_raw" -v
     Expected: PASSED. Part A: field_regression issued on RAW, asked written before send, clarify sent, AWAITING_REPLY. Part B: carry-forward round paystub OT=2.

  4. R2-3 classification proof (test 12):
     uv run pytest tests/test_resume_pipeline.py -k "client_supplied_same_value" -v
     Expected: PASSED. Outcome is "client_supplied" NOT "carried_forward".

  5. R3-3 + R2-2 integration pin (test 9, both parts):
     uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v
     Expected: PASSED. Part A: AWAITING_REPLY (R3-3). Part B: paystub OT=2 (R2-2 employee_id-keyed backfill).

  6. BLOCKER FIX (test 10):
     uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v
     Expected: PASSED. OT NOT backfilled. AWAITING_APPROVAL (N8 fired). Restated-name variant.

  7. Eval layer:
     uv run python eval/run_eval.py --check
     Expected: exits 0. All three fixtures score correctly.

  8. Comment verification (D-7.5-11 negative companion):
     grep "NEGATIVE COMPANION" tests/test_resume_pipeline.py
     grep "classify-after-decide\|classify-first" tests/test_resume_pipeline.py
     grep "D-7.5-11 proof" tests/test_resume_pipeline.py
     grep "D-7.5-11 overpay guard proof" tests/test_resume_pipeline.py
     grep "D-7.5-11 client_supplied proof" tests/test_resume_pipeline.py
     # All must exit 0

  9. Provenance badge review:
     grep "carried-forward" app/templates/run_detail.html
     grep "client removed" app/templates/run_detail.html
     grep "client supplied" app/templates/run_detail.html
     grep "awaiting reply" app/templates/run_detail.html
     grep "clarification_field_regression" app/templates/run_detail.html

  10. Hermetic confirmation:
      grep "# Hermetic discipline" tests/test_resume_pipeline.py
      grep "finally:" tests/test_resume_pipeline.py | wc -l
      (Should show consistent approach, 15+ finally blocks.)

  11. Full suite:
      uv run pytest -q
      Expected: all green.
  </how-to-verify>
  <resume-signal>Type "approved" if all 15 integration tests PASS (not SKIP) with DATABASE_URL set, eval --check passes, AND:
  - test_answered_silence_reaches_approval PASSES: AWAITING_APPROVAL, OT=2, carried_forward; negative companion comment documents classify-after-decide stranding (D-7.5-11 failure mode)
  - test_answered_explicit_zero_not_rebackfilled PASSES: AWAITING_APPROVAL, OT=0/absent (NOT 2), confirmed_dropped; overpay guard comment present
  - test_answered_positive_uses_client_value PASSES: AWAITING_APPROVAL, OT=5, client_supplied
  - test_detect_fired_on_raw PASSES: Part A (field_regression on RAW), Part B (carry-forward paystub OT=2)
  - test_client_supplied_same_value PASSES: outcome client_supplied not carried_forward
  - test_restated_name_prior_matches Part A + Part B PASS
  - test_confirmed_dropped_no_reloop PASSES (BLOCKER FIX)
  - All four provenance badges in run_detail.html
  Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/fixtures/*.json prior_extracted key | Read from disk; Extracted.model_validate rejects malformed data at load time (extra="forbid") |
| integration tests -> live DB | Tests write synthetic run_ids to real DB; hermetic cleanup in finally blocks or transaction rollback |
| run_detail.html clarified_fields | Read from DB via load_clarified_fields; template renders string outcomes only — no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-04-01 | Tampering | eval fixture prior_extracted parsed by Extracted.model_validate | mitigate | model_validate with extra="forbid" rejects any unexpected field; D-24 compliance (model_dump(mode="json")) ensures Decimal precision is correct. |
| T-7.5-04-02 | Information Disclosure | Integration tests write to real Supabase dev DB | accept | Tests use uuid.uuid4() run IDs and synthetic extraction data (no real employee PII). Hermetic cleanup in finally blocks (Finding 7 fix). |
| T-7.5-04-03 | Elevation of Privilege | Test run_ids colliding with production runs | mitigate | uuid.uuid4() generates cryptographically random IDs; collision probability negligible. Hermetic cleanup ensures no orphan test data. |
| T-7.5-04-04 | Tampering | run_detail.html clarified_fields rendering | accept | clarified_fields is loaded from DB (Pydantic-validated on write per D-7.5-03b, 4 outcomes); template renders only known outcome strings from the Literal; no user-controlled string injection into Jinja2 logic. |
| T-7.5-04-05 | Information Disclosure | Provenance badge shows carried-forward / client_supplied labels | accept | Badges show outcome labels, not the actual values; client_supplied label (R2-3 fix + D-7.5-11) adds no additional PII disclosure beyond what was already operator-visible. |
| T-7.5-04-06 | Tampering | Strengthened approved==sent test exercises _deliver/compose_confirmation | accept | Test exercises the confirmation composition path to verify the invariant holds; no new production code added, only a stronger test assertion. |
| T-7.5-04-07 | Tampering | D-7.5-11 answered-round tests exercise classify-first ordering | accept | Tests are read-only evidence (integration tests). They pin the D-7.5-11 classify-first ordering so a regression to classify-after-decide is immediately detected. No new production code; the fix is in Plan 03. |
| T-7.5-04-08 | Tampering | test_answered_explicit_zero_not_rebackfilled pins overpay guard | accept | Test asserts paystub OT=0 (not 2) when client explicitly zeros the field. Pins the D-7.5-11 overpay guard in backfill_extracted (resolved_drops gate). A regression that re-backfills explicit-zero causes this test to fail with OT=2 in paystub. |
| T-7.5-04-09 | Tampering | Test 10 confirmed_dropped no-reloop exercises resolved_drops cross-plan path | accept | Test is read-only evidence; the fix is in Plans 02/03. Test 10 pins the regression so a future reversal is immediately detected. |
| T-7.5-04-SC | Tampering | No new package installs | accept | No uv add invoked; all imports from existing pyproject.toml deps. |
</threat_model>

<verification>
Phase-gate (all must pass WITH DATABASE_URL set):

1. `uv run pytest tests/test_resume_pipeline.py -v` — 15+ PASSED, 0 SKIPPED, 0 FAILED
2. `uv run pytest tests/test_resume_pipeline.py -k "answered_silence_reaches_approval" -v` — PASSED (D-7.5-11 silence proof: AWAITING_APPROVAL, OT=2, carried_forward; negative companion comment present)
3. `uv run pytest tests/test_resume_pipeline.py -k "answered_explicit_zero" -v` — PASSED (D-7.5-11 overpay guard: AWAITING_APPROVAL, OT=0/absent, confirmed_dropped; NOT re-backfilled to 2)
4. `uv run pytest tests/test_resume_pipeline.py -k "answered_positive" -v` — PASSED (D-7.5-11 client value: AWAITING_APPROVAL, OT=5, client_supplied)
5. `uv run pytest tests/test_resume_pipeline.py -k "detect_fired_on_raw" -v` — PASSED (D-7.5-10 R2-1 proof)
6. `uv run pytest tests/test_resume_pipeline.py -k "client_supplied_same_value" -v` — PASSED (R2-3 proof)
7. `uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v` — PASSED (D-7.5-04a)
8. `uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v` — PASSED (R3-3 + R2-2 paystub)
9. `uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v` — PASSED (BLOCKER FIX)
10. `uv run pytest tests/test_resume_pipeline.py -k "approved_bytes" -v` — PASSED (D-7.5-04b)
11. `uv run pytest tests/test_resume_pipeline.py -k "tri_state" -v` — PASSED (D-7.5-04c)
12. `uv run pytest tests/test_resume_pipeline.py -k "mixed_issue" -v` — PASSED (SC4)
13. `uv run python eval/run_eval.py --check` — exits 0
14. `grep "prior_extracted" eval/run_eval.py` — exits 0
15. `grep "prior_matches" eval/run_eval.py` — exits 0
16. `grep "detect_field_regression" eval/run_eval.py` — exits 0 (production D-7.5-10 path)
17. `grep "validate.*prior=" eval/run_eval.py` — exits 1 (dead prior= kwarg GONE)
18. `grep "prior_extracted" eval/fixtures/18_field_drop_clarify_carry_coastal.json` — exits 0
19. `grep "prior_matches" eval/fixtures/18_field_drop_clarify_carry_coastal.json` — exits 0
20. Fixture 18 field_regression path check: `uv run python -c "import json; from eval.run_eval import _score_fixture, _load_fixture; import pathlib; p=pathlib.Path('eval/fixtures/18_field_drop_clarify_carry_coastal.json'); r=_load_fixture(p); res=_score_fixture(r,p); assert res['decision']['final_action']=='request_clarification', res; print('OK:', res['decision']['final_action'])"` — prints request_clarification
21. `grep "carried-forward" app/templates/run_detail.html` — exits 0
22. `grep "client removed" app/templates/run_detail.html` — exits 0
23. `grep "client supplied" app/templates/run_detail.html` — exits 0 (R2-3 fix + D-7.5-11)
24. `grep "awaiting reply" app/templates/run_detail.html` — exits 0
25. `grep "clarification_field_regression" app/templates/run_detail.html` — exits 0
26. `grep "# Hermetic discipline" tests/test_resume_pipeline.py` — exits 0 (Finding 7)
27. `grep "NEGATIVE COMPANION" tests/test_resume_pipeline.py` — exits 0 (D-7.5-11 negative companion in test 13)
28. `grep "D-7.5-11 proof" tests/test_resume_pipeline.py` — exits 0
29. `grep "D-7.5-11 overpay guard proof" tests/test_resume_pipeline.py` — exits 0
30. `grep "D-7.5-11 client_supplied proof" tests/test_resume_pipeline.py` — exits 0
31. `grep "classify-after-decide\|classify-first" tests/test_resume_pipeline.py` — exits 0
32. `grep "R3-3 integration pin" tests/test_resume_pipeline.py` — exits 0
33. `grep "R2-2 fix pin" tests/test_resume_pipeline.py` — exits 0
34. `grep "BLOCKER FIX cross-plan key-type" tests/test_resume_pipeline.py` — exits 0
35. `grep "str(current_emp_id)" app/pipeline/validate.py` — exits 0 (Plan 02 N8 fix)
36. `grep "id_to_submitted_name" app/pipeline/orchestrator.py` — exits 1 (Plan 03 reverse-lookup eliminated)
37. `uv run pytest -q` — full suite exits 0
38. D-7.5-10 + D-7.5-11 AST ordering in _run_stages: detect_field_regression line < backfill_extracted line < validate line < _compute_line_items line
</verification>

<success_criteria>
- 15 integration tests in test_resume_pipeline.py, ALL PASSING with DATABASE_URL set; pytestmark skipif guard; hermetic discipline documented (Finding 7)
- D-7.5-11 answered-round tests (the Round 3 additions):
  * test_answered_silence_reaches_approval: AWAITING_APPROVAL, OT=2, carried_forward (NOT re-clarified). Negative companion documents the D-7.5-11 stranding failure mode (classify-after-decide → AWAITING_REPLY).
  * test_answered_explicit_zero_not_rebackfilled: AWAITING_APPROVAL, OT=0/absent (NOT 2), confirmed_dropped. Overpay guard: _is_paid(Decimal('0')) is False; resolved_drops gate protects explicit-zero from re-backfill.
  * test_answered_positive_uses_client_value: AWAITING_APPROVAL, OT=5, client_supplied.
  All three correspond to the three outcomes of the classify-first step (carried_forward / confirmed_dropped / client_supplied).
- test_detect_fired_on_raw: D-7.5-10 proof. Part A (RAW detection), Part B (carry-forward paystub). Together with the three D-7.5-11 tests, both Round-1 detection and Round-2 answered behavior are now fully tested.
- test_client_supplied_same_value: R2-3 fix proof (client_supplied, not carried_forward).
- test_restated_name: Part A + Part B (R3-3 + R2-2 paystub).
- test_confirmed_dropped_no_reloop: BLOCKER FIX cross-plan key-type consistency.
- D-7.5-04a/b/c/SC4 tests: ordering, approved==sent, tri-state, mixed-issue.
- Fixtures 16/17/18: eval --check exits 0. Fixture 18 produces request_clarification via field_regression path.
- run_eval.py: D-7.5-10 three-phase path wired; dead prior= kwarg gone.
- run_detail.html: FOUR provenance badges (carried-forward/client-removed/client-supplied/awaiting-reply) + clarification_field_regression purpose badge.
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-04-SUMMARY.md` when done.
</output>
