---
phase: 07.5-clarification-reply-field-regression
plan: "04"
type: execute
wave: 4
depends_on:
  - "07.5-03"
files_modified:
  - tests/test_resume_pipeline.py
  - eval/run_eval.py
  - eval/fixtures/16_zero_hours_hourly_coastal.json
  - eval/fixtures/16_zero_hours_hourly_coastal_extraction.json
  - eval/fixtures/17_nfd_name_coastal.json
  - eval/fixtures/17_nfd_name_coastal_extraction.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal.json
  - eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json
  - eval/summary.json
  - app/templates/run_detail.html
autonomous: false
requirements:
  - MONEY-03

user_setup:
  - service: supabase
    why: "Integration tests require DATABASE_URL set in shell env so state-machine tests (snapshot-once, ordering, loop-guard, confirmed_dropped) actually execute and PASS — skip != evidence (D-7.5-04)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard -> Settings -> Database -> Connection string (Supavisor transaction mode, port 6543) — must be in shell env, not just .env"
    dashboard_config:
      - task: "Confirm Plan 03's schema migration was applied (pre_clarify_extracted and clarified_fields columns exist)"
        location: "Run: uv run python -m app.db.bootstrap before this plan's checkpoint"

must_haves:
  truths:
    - "tests/test_resume_pipeline.py exists with 12+ integration tests covering: snapshot_once (D-19), N1 single-_run_stages-call, N2 asked-before-send ordering, D-7.5-10 detect-on-raw pin (test_detect_fired_on_raw — NEW R2-1 proof: first-resume OT 2→None asserts field_regression issue emitted + asked written + clarify sent; carry-forward round paystub shows OT=2; NEGATIVE COMPANION shows test fails if detect runs on backfilled data), ordering test D-7.5-04a (paystub OT line reflects carried_forward value), approved-bytes==sent-bytes invariant (D-7.5-04b — strengthened per Finding 8 to assert actual confirmation body/PDF source reflects AWAITING_APPROVAL paystub), D-7.5-04c tri-state through real JSONB path, loop-guard (fires exactly once), confirmed_dropped no-reflag (D-15/N8), mixed-issue SC4, restated-name prior_matches threading (R3-3 integration pin), confirmed_dropped no-reloop on Round-2 (BLOCKER FIX cross-plan key-type consistency test), AND client_supplied classification (R2-3 fix proof: same-value resupply labeled client_supplied not carried_forward)"
    - "Each integration test uses the seeded_db / transaction-rollback fixture OR creates its own run and deletes in finally — hermetic per D-7.5-04d (Finding 7 fix: either use an existing rollback fixture from conftest.py OR adopt a consistent per-test finally: DELETE pattern — no manual cleanup drift)"
    - "test_detect_fired_on_raw PASSES (NEW — D-7.5-10 three-phase ordering R2-1 proof): TWO-PART test: (Part A) First-resume OT 2→None: asserts (1) field_regression issue emitted in stage.issues, (2) clarified_fields has 'asked' for OT for the employee's id, (3) clarification email written with purpose='clarification_field_regression', (4) run at AWAITING_REPLY — detection fired on RAW before backfill. (Part B) Negative companion: documents (via comment or parametrized form) that if backfill_extracted were called BEFORE detect_field_regression, the OT 2→None drop would be masked (OT would be 2 in both pre and post-backfill, detect would see no drop, no field_regression issue, no clarification) — this is the exact R2-1 failure mode the D-7.5-10 ordering prevents. The CARRY-FORWARD ROUND assertion is in test_ordering_carried_forward_ot_in_paystub (Test 4/D-7.5-04a), which asserts OT=2 in the final paystub after backfill fires — the two tests together prove the FULL round-trip: detect fires on raw (test_detect_fired_on_raw), carried value reaches paystub (test_ordering_carried_forward_ot_in_paystub)."
    - "The D-7.5-04a ordering test PASSES: a carried_forward scenario asserts the FINAL PAYSTUB LINE ITEM has OT=Decimal('2') (not 0/absent), plus a negative companion that would catch a backfill-after-calc regression"
    - "test_restated_name_prior_matches_threading PASSES: exercises the REAL resume_pipeline path end-to-end with a restated name (M. Chen OT=2 in snapshot → Maria Chen OT=None in Round-1 reply, both same employee_id); asserts (1) clarification is deferred (run at AWAITING_REPLY, 'asked' written — field_regression detected via employee_id-keyed diff), AND (2) an explicit assertion is added for the CARRY-FORWARD ROUND: if the same scenario is continued to Round-2 (Maria Chen OT=None again, carried_forward), the FINAL PAYSTUB LINE ITEM shows OT=Decimal('2'). If prior_matches threading is broken, detect_field_regression returns [] (honest no-op per Plan 02), no field_regression issue emitted, clarification never deferred, run advances with OT=0/absent in the paystub — so this test FAILS for the right reason."
    - "test_client_supplied_same_value_labeled_correctly PASSES (NEW — R2-3 fix proof): client replies with OT=Decimal('2') which is the SAME VALUE as the snapshot OT=2; assert clarified_fields outcome for OT for that employee is 'client_supplied' (NOT 'carried_forward'); assert run_detail.html badge path would show 'client supplied' (via grepping the template). This tests the R2-3 fix: before the fix, the Round-2 classifier read backfilled data and labeled same-value resupply as 'carried_forward'. After the fix (reading from stage.issues derived from RAW detection), the field is NOT in stage.issues as field_regression (because a present-positive raw value emits NO field_regression issue) → the classifier labels it 'client_supplied'."
    - "test_confirmed_dropped_no_reloop_on_round2 PASSES (BLOCKER FIX — cross-plan key-type consistency): Round-1 confirmed_dropped recorded for OT; Round-2 OT silent (M. Chen → Maria Chen restated name, same employee_id); asserts (a) OT is NOT backfilled in the paystub (confirmed_dropped honored per D-14) AND (b) run advances to AWAITING_APPROVAL (N8 suppression fired — no re-emitted field_regression issue). FAILS if resolved_drops set is keyed by submitted_name (R3-3 restated-name instability) or if str(current_emp_id) conversion is missing in Plan 02 validate() N8 check (UUID vs str mismatch — the original infinite-loop bug). This test exercises the FULL cross-plan path: Plan 03 Step E2 builds the set → Plan 02 validate() N8 check tests membership → no field_regression re-emitted → no re-clarify loop."
    - "D-7.5-04b approved==sent invariant (Finding 8 fix): the test asserts that the ACTUAL CONFIRMATION EMAIL BODY produced by compose_confirmation and/or the PDF source data reflects the same paystub that was at AWAITING_APPROVAL — not just that DB columns are unchanged; exercises _deliver, confirmation composition, and PDF field values"
    - "D-7.5-04c tri-state test proves None vs Decimal('0') through the SAME serialization path that writes pre_clarify_extracted (not hand-typed JSON)"
    - "All integration tests PASS (not SKIP) when DATABASE_URL is set — skip != evidence per D-7.5-04"
    - "Three eval fixture pairs (16/17/18) exist in eval/fixtures/ and run_eval.py scores all three correctly; eval/summary.json updated via --record; uv run python eval/run_eval.py --check exits 0"
    - "run_detail.html shows carried-forward, confirmed_dropped, client_supplied, and asked provenance badges on field-regression-affected hours rows (D-7.5-08 — four outcomes from Finding 6 fix); the clarification_field_regression purpose badge is added to the outbound emails section"
  artifacts:
    - path: "tests/test_resume_pipeline.py"
      provides: "12+ hermetic integration tests covering all MONEY-03 state-machine invariants + D-7.5-10 detect-on-raw pin (R2-1) + R2-3 client_supplied classification pin + ordering test + strengthened approved==sent + restated-name R3-3 integration pin + confirmed_dropped no-reloop cross-plan key-type consistency pin"
      contains: "test_detect_fired_on_raw"
    - path: "eval/run_eval.py"
      provides: "prior_extracted support: strips key before InboundEmail, loads as Extracted, passes as prior= to validate()"
      contains: "prior_extracted"
    - path: "eval/fixtures/18_field_drop_clarify_carry_coastal.json"
      provides: "MONEY-03 eval fixture with prior_extracted key (produced via model_dump, not hand-typed — D-24)"
      contains: "prior_extracted"
    - path: "app/templates/run_detail.html"
      provides: "per-value provenance badges for all four outcomes (carried-forward/client-removed/client-supplied/awaiting-reply) on field-regression hours rows; clarification_field_regression purpose badge"
      contains: "carried-forward"
  key_links:
    - from: "eval/fixtures/18_field_drop_clarify_carry_coastal.json (prior_extracted key)"
      to: "eval/run_eval.py:validate(prior=prior_extracted)"
      via: "fixture loader reads prior_extracted -> Extracted.model_validate -> prior= kwarg"
      pattern: "prior_extracted"
    - from: "tests/test_resume_pipeline.py:test_detect_fired_on_raw"
      to: "app/pipeline/orchestrator.py:_run_stages (detect_field_regression called on raw extracted before backfill_extracted)"
      via: "D-7.5-10 ordering proof: OT 2→None drop is visible to detection on RAW; field_regression issue emitted; asked written; clarify sent. Negative companion proves masking if detect were deferred. Together with test_ordering_carried_forward_ot_in_paystub this proves the full round-trip."
      pattern: "detect_fired_on_raw"
    - from: "tests/test_resume_pipeline.py:test_ordering_carried_forward_ot_in_paystub"
      to: "app/pipeline/orchestrator.py:backfill_extracted in _run_stages seam"
      via: "asserts paystub OT line == Decimal('2') after carried_forward backfill — fails if backfill runs after calc (pins R3-1)"
      pattern: "carried_forward"
    - from: "tests/test_resume_pipeline.py:test_client_supplied_same_value_labeled_correctly"
      to: "app/pipeline/orchestrator.py:resume_pipeline Round-2 classifier (reads stage.issues derived from raw_drops)"
      via: "R2-3 fix proof: client supplies OT=2 (same as snapshot) → no field_regression issue in stage.issues (present-positive raw value → issue not emitted) → classifier labels client_supplied (not carried_forward). Before the fix: classifier read backfilled data and labeled same-value resupply as carried_forward."
      pattern: "client_supplied"
    - from: "tests/test_resume_pipeline.py:test_restated_name_prior_matches_threading"
      to: "app/pipeline/orchestrator.py:resume_pipeline -> _run_stages(prior_matches=prior_matches)"
      via: "asserts field_regression detected (R3-3) on Round-1 path (M. Chen → Maria Chen restated-name); then carry-forward Round-2 carries OT=2 to final paystub; FAILS if prior_matches is defaulted to None (R3-3 regression pin)"
      pattern: "prior_matches"
    - from: "tests/test_resume_pipeline.py:test_confirmed_dropped_no_reloop_on_round2"
      to: "app/pipeline/orchestrator.py:Step E2 _resolved_by_name + app/pipeline/validate.py:N8 check"
      via: "Round-1 confirmed_dropped(M. Chen OT) → Round-2 Maria Chen OT=None → NO re-clarify; FAILS if set keyed by submitted_name OR if str(current_emp_id) missing (cross-plan key-type consistency: Plan 03 Step E2 builds set by emp_id_str; Plan 02 validate() N8 uses str(current_emp_id))"
      pattern: "confirmed_dropped_no_reloop"
    - from: "tests/test_resume_pipeline.py:test_approved_bytes_equals_sent_bytes"
      to: "app/pipeline/orchestrator.py:_deliver + compose_confirmation + PDF generation"
      via: "asserts actual confirmation body/PDF source data matches AWAITING_APPROVAL paystub (Finding 8 strengthened invariant)"
      pattern: "confirmation"
---

<objective>
Complete the MONEY-03 evidence: replace xfail stubs with 12+ PASSING hermetic integration tests, add eval fixtures 16/17/18, update run_eval.py with prior_extracted support, and add the D-7.5-08 provenance badge (four outcomes) to the run-detail approval view.

**Reviews addressed in this plan (R2 round — new tests for R2-1 and R2-3 fixes):**

- **R2-1 (HIGH — D-7.5-10 three-phase detect-on-raw proof):** New test `test_detect_fired_on_raw` proves that detection fires on RAW (pre-backfill) extracted. Part A: first-resume OT 2→None — asserts field_regression issue emitted, asked written durably BEFORE send (N2), clarification email sent with purpose='clarification_field_regression', run at AWAITING_REPLY. Part B: negative companion documents the exact masking failure mode (backfill before detect would hide the drop — detection would see OT=2 on both sides, emit no issue, send no clarification). The carry-forward round assertion (paystub OT=2) lives in test_ordering_carried_forward_ot_in_paystub (D-7.5-04a). Together, these two tests prove the full round-trip: detect fires on raw (test_detect_fired_on_raw) → carried value reaches paystub (test_ordering_carried_forward_ot_in_paystub).

- **R2-3 (MED — client_supplied classification proof):** New test `test_client_supplied_same_value_labeled_correctly` proves the Round-2 provenance classifier correctly distinguishes a client who re-supplies the same positive value (→ client_supplied) from a client who was silent (→ carried_forward). The fix in Plan 03 reads from stage.issues (derived from raw_drops computed on RAW data) rather than from the backfilled DB state. When OT=2 is present in the raw reply (same as snapshot), no field_regression issue is emitted for it → classifier labels it client_supplied (not carried_forward).

- **test_restated_name_prior_matches_threading strengthened:** Previously only asserted the Round-1 outcome (AWAITING_REPLY, asked written). Now also asserts the Round-2 carry-forward outcome: after the Round-1 clarification cycle, when Round-2 brings "Maria Chen" OT=None (silence), the FINAL PAYSTUB LINE ITEM shows OT=Decimal('2'). This proves the employee_id-keyed backfill (R2-2 fix) actually carries the value to the paystub on the restated-name path.

- **Findings 7, 8, R3-3 (prior_matches threading), BLOCKER FIX (key-type consistency):** Unchanged from previous revision — same requirements.

D-23 two-layer evidence:
- Eval layer: fixtures 16 (MONEY-01), 17 (MONEY-02 eval-scorer parity), 18 (MONEY-03 field-regression prior= seam). Certifies pure judgment (validate -> decide) given a prior.
- Integration layer: 12+ state-machine invariants — snapshot-once, N1 single-call, N2 asked-before-send, **D-7.5-10 detect-on-raw (NEW R2-1 proof)**, D-7.5-04a ordering (root-cause pin), D-7.5-04b approved==sent (strengthened), D-7.5-04c tri-state through real path, loop-guard, confirmed_dropped no-reflag, mixed-issue SC4, restated-name prior_matches threading (R3-3 pin), confirmed_dropped no-reloop (BLOCKER FIX), **client_supplied classification (NEW R2-3 proof)**.

Phase-gate: all integration tests PASS WITH DATABASE_URL set (not SKIP). Eval --check passes. Human checkpoint signs off.

Output: full MONEY-03 evidence assembled; phase ready for verification.
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.5-clarification-reply-field-regression/07.5-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-PATTERNS.md
@.planning/phases/07-money-correctness-deepening/07-CONTEXT.md
@.planning/phases/07.5-clarification-reply-field-regression/07.5-03-SUMMARY.md

@tests/test_clarify.py
@tests/conftest.py
@eval/run_eval.py
@eval/fixtures/07_missing_hours_coastal.json
@app/templates/run_detail.html
@app/db/seed.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 12 hermetic integration tests covering all MONEY-03 invariants + D-7.5-10 detect-on-raw pin (R2-1) + R2-3 client_supplied pin + strengthened restated-name (R2-2) + BLOCKER FIX + approved==sent</name>
  <files>tests/test_resume_pipeline.py</files>

  <read_first>
  Read tests/conftest.py in full. Look specifically for:
  - Any transaction-rollback fixture (seeded_db, db_conn, or similar) that rolls back after each test
  - Any helper for creating payroll runs in test state
  - The established DB connection pattern for tests

  Read tests/test_clarify.py in full for the established pattern:
  - class _DraftLLM and its call_text method — copy this mock LLM pattern
  - pytest.mark.skipif(not os.environ.get("DATABASE_URL"), ...) — use this exact module-level form
  - Run creation via repo helpers (repo.create_run or direct INSERT)
  - Finally: cleanup with DELETE FROM payroll_runs WHERE id = %s
  - The established psycopg.connect pattern (direct connection, not pool)

  Read app/db/seed.py to identify coastal business employees — use real names and employee_ids for fixture data. Note which employees are hourly (need for D-7.5-04a ordering test with real calc). Also look for any employee whose name could be abbreviated (e.g., "Maria Chen" could be stored as the full name but submitted as "M. Chen") — needed for Tests 9, 10, 11, and 12.

  PATTERNS.md §8 has the _extracted() and _match() builder helper style from test_validate.py — copy for test setup.

  Finding 7 hermetic discipline: Read conftest.py first. If it has a transaction-rollback fixture, use it consistently in ALL 12 tests. If not, every test uses the finally: DELETE pattern from test_clarify.py. Do NOT mix approaches. Document which approach was chosen in a comment at the top of the file.

  Finding 8 approval path: Read app/main.py around the approval CAS (line ~492, claim_status(AWAITING_APPROVAL, APPROVED)) and whatever comes after it — the _deliver call, compose_confirmation, PDF generation. Understand what the confirmation composition takes as input so test 5 can assert it uses the AWAITING_APPROVAL paystub values.

  For test_detect_fired_on_raw (test 11) and test_client_supplied_same_value_labeled_correctly (test 12):
  - Read app/pipeline/orchestrator.py _run_stages implementation (Plan 03 result) to understand exactly what detect_field_regression is called with and what raw_drops contains for the negative companion
  - The negative companion for test 11 documents: if detect_field_regression were called AFTER backfill_extracted, OT would be 2 on both sides → no drop detected → no field_regression issue → no clarification sent

  Write test_resume_pipeline.py with exactly these 12 tests.
  </read_first>

  <action>
  Create tests/test_resume_pipeline.py with 12 tests.

  Module header:
  - pytestmark = pytest.mark.skipif(not os.environ.get("DATABASE_URL"), reason="requires live DATABASE_URL — set before phase gate; skip != evidence (D-7.5-04)")
  - A comment block at the top stating: "Hermetic discipline (D-7.5-04d / Finding 7): [describe chosen approach — rollback fixture or per-test finally DELETE]"

  Helper utilities at top of file (from PATTERNS.md §8):
  - `_mk_extracted(employees_data, pay_period_start=None) -> Extracted`: builds an Extracted from a list of dicts
  - `_mk_match(name, emp_id, source="exact") -> NameMatchResult`: builds a NameMatchResult

  Test 1 — test_snapshot_once_not_overwritten (D-19/D-28):
  - Create a payroll run in AWAITING_REPLY state.
  - Call repo.set_pre_clarify_extracted(run_id, extracted_v1) with OT=Decimal("2").
  - Call repo.set_pre_clarify_extracted(run_id, extracted_v2) with OT=Decimal("0").
  - Assert repo.load_pre_clarify_extracted(run_id).employees[0].hours_overtime == Decimal("2") (IS NULL guard: second write is a no-op).
  - Hermetic cleanup in finally.

  Test 2 — test_n1_single_run_stages_call (N1):
  - Use unittest.mock.patch to monkeypatch _run_stages in orchestrator module.
  - Mock returns a _RunStagesResult(clarify_deferred=False, matches=[], issues=[]).
  - Create a run in AWAITING_REPLY state. Call resume_pipeline(run_id, mock_inbound, llm=mock_llm).
  - Assert _run_stages was called exactly once.
  - Hermetic cleanup.

  Test 3 — test_n2_asked_written_before_send (N2 — asked-before-send ordering):
  - Set up a run with a pre_clarify_extracted snapshot (OT=2).
  - Configure mock LLM to return extraction with OT=None (drop detected).
  - Monkeypatch _clarify to record when it was called, and at that moment load clarified_fields from DB.
  - Call resume_pipeline.
  - Assert: when _clarify was invoked, clarified_fields already had "asked" for OT for the relevant employee.
  - Assert: _clarify was called with purpose="clarification_field_regression".
  - Hermetic cleanup.

  Test 4 — test_ordering_carried_forward_ot_in_paystub (D-7.5-04a — THE ROOT CAUSE PIN):
  - Set up a run with pre_clarify_extracted snapshot (OT=2) and clarified_fields with "asked" for OT (simulating Round-2).
  - Configure mock LLM to return extraction with OT=None (silence — carried_forward should apply via backfill_extracted).
  - Seed the DB with a real coastal hourly employee and roster so calc can produce actual paystub line items.
  - Call resume_pipeline (Round-2 path — clarified dict has "asked" entries).
  - Assert: the FINAL PAYSTUB LINE ITEM for that employee has hours_overtime == Decimal("2") — the carried-forward value is in the computed paystub (backfill landed before calc).
  - Negative companion: document in a comment that commenting out the backfill_extracted call in orchestrator.py would cause this test to FAIL (proving it pins the ordering).
  - Hermetic cleanup.

  Test 5 — test_approved_bytes_equals_sent_bytes (D-7.5-04b — STRENGTHENED per Finding 8):
  - Set up a run through to AWAITING_APPROVAL with a computed paystub including a carried-forward OT value (OT=2).
  - Read the paystub line_items from DB at the AWAITING_APPROVAL moment (capture: employee name, hours_overtime value).
  - Simulate approval via claim_status(AWAITING_APPROVAL, APPROVED) — or through the main.py route.
  - Invoke the confirmation composition path (compose_confirmation or _deliver equivalent) on the APPROVED run.
  - Assert: the confirmation email body OR PDF source data CONTAINS the same OT=2 value that was in the AWAITING_APPROVAL paystub. Specifically: either parse the confirmation body for the OT line, or access the PDF-generating source data and assert the OT hours field equals Decimal("2").
  - This pins that no backfill or mutation occurs between AWAITING_APPROVAL and the confirmation send.
  - Hermetic cleanup.

  Test 6 — test_tri_state_through_real_path (D-7.5-04c):
  - Create pre_clarify_extracted via repo.set_pre_clarify_extracted (uses json.dumps(extracted.model_dump(mode="json"))) — NOT hand-typed JSON.
  - Load it back via repo.load_pre_clarify_extracted.
  - Assert: None fields survive as None; Decimal("0") fields survive as Decimal("0") (not coerced to None).
  - This proves the tri-state (present/explicit-0/absent) survives the JSONB serialization round-trip.
  - Hermetic cleanup.

  Test 7 — test_loop_guard_fires_exactly_once:
  - Set up a run where Round-1 sets status to AWAITING_REPLY with "asked" in clarified_fields.
  - Simulate a Round-2 reply where the mock LLM returns OT=None (silence — still carried_forward).
  - Call resume_pipeline (Round-2 path).
  - Assert: run advances to AWAITING_APPROVAL (not back to AWAITING_REPLY).
  - Assert: clarified_fields outcome for OT is "carried_forward" (not "asked" again).
  - Assert: no second clarification email was sent (no new email_messages row with purpose="clarification_field_regression").
  - Hermetic cleanup.

  Test 8 — test_mixed_issue_records_asked_and_asks_field_regression (SC4 / R3-2):
  - Set up a run where the resumed extraction has BOTH a field_regression issue (OT dropped) AND a normal missing-field issue (another employee has no hours at all).
  - Call resume_pipeline.
  - Assert: clarified_fields durably records "asked" for the OT field (not lost due to the mixed-issue path).
  - Assert: the outbound email_messages row with purpose="clarification_field_regression" was written.
  - Assert: the sent email body contains both the field-regression question AND the normal missing-field/unresolved-name text (one unified email, not two sends).
  - Hermetic cleanup.

  Test 9 — test_restated_name_prior_matches_threading (R3-3 integration pin — STRENGTHENED for R2-2):
  PART A (Round-1 detection pin): Exercises the real resume_pipeline path. Proves that prior_matches from the
  pre-resume reconciliation is threaded through to detect_field_regression, enabling employee_id-keyed diff
  to fire on the restated-name case ("M. Chen" in snapshot → "Maria Chen" in reply, same employee_id).
  If prior_matches is None at any point in the chain, detect_field_regression returns [] (honest no-op per
  Plan 02), no field_regression issue is emitted, and the run advances with OT=0/absent — so this test
  CATCHES the R3-3 regression.

  Setup (ROUND-1 PIN):
  - Set up a FRESH run with NO clarified_fields (is_round_2=False).
  - Create pre_clarify_extracted with "M. Chen" hours_overtime=Decimal("2").
  - Persist reconciliation mapping "M. Chen" → chen_id (resolved=True). This is the prior_matches source.
  - Configure mock LLM to return extraction for Round-1 reply with "Maria Chen" (restated) OT=None.
  - Call resume_pipeline.

  ASSERTIONS FOR PART A (Round-1):
  - The run is set to AWAITING_REPLY (field_regression issue detected — detection fired via employee_id-keyed diff).
  - clarified_fields has "asked" for hours_overtime for str(chen_id).
  - An outbound email_messages row with purpose="clarification_field_regression" was written.
  - Comment: "# R3-3 integration pin: if prior_matches is defaulted to None at any point in the chain
    (Plan 01's _run_stages signature, Plan 01's validate() call, or Plan 03's resume_pipeline call),
    detect_field_regression returns [] (honest no-op), no field_regression issue is emitted, and this
    test fails with the run at AWAITING_APPROVAL instead of AWAITING_REPLY."

  PART B (Round-2 carry-forward paystub pin — STRENGTHENED for R2-2):
  After Part A, continue the same run to Round-2. Simulate the carry-forward round:
  - Set clarified_fields to {str(chen_id): {"hours_overtime": "asked"}} (simulating Part A's outcome).
  - Configure mock LLM to return extraction for Round-2 with "Maria Chen" OT=None (still silent).
  - Call resume_pipeline again (Round-2 path: is_round_2=True, clarified has "asked").
  - ASSERT: the FINAL PAYSTUB LINE ITEM for chen_id shows hours_overtime == Decimal("2") (R2-2 fix: employee_id-keyed backfill_extracted carried the OT from the "M. Chen" snapshot to the "Maria Chen" Round-2 result).
  - ASSERT: clarified_fields outcome for hours_overtime is "carried_forward" (not "asked").
  - Comment: "# R2-2 fix pin: if backfill_extracted used submitted_name-keyed snapshot lookup,
    'M. Chen' (snapshot) ≠ 'Maria Chen' (Round-2 reply), no snapshot found, no backfill, OT=0 in paystub
    — this test fails for the right reason. Employee_id-keyed lookup (via prior_matches + matches) makes
    the carry-forward stable across name restatements."

  - Hermetic cleanup.

  Test 10 — test_confirmed_dropped_no_reloop_on_round2 (BLOCKER FIX — cross-plan key-type consistency):
  This test pins the complete cross-plan path of the resolved_drops key-type fix. Three plan artifacts
  must agree: Plan 03 Step E2 builds the set keyed by (emp_id_str, field); Plan 03 backfill_extracted
  guards by (emp_id_str, field) via matches name→id resolution; Plan 02 validate() N8 check uses
  str(current_emp_id). Any regression in any of the three consumers breaks suppression and causes an
  infinite re-clarify loop on the confirmed_dropped path.

  The restated-name variant is the strongest form: if the set were keyed by submitted_name ("M. Chen")
  but Round-2 submits "Maria Chen", the membership test would miss — re-backfill would fire, re-emitting
  a field_regression issue, re-triggering clarification.

  Setup:
  - Pick a real coastal hourly employee (e.g., "Maria Chen") from seed.py with employee_id = chen_id.
  - Snapshot: create pre_clarify_extracted with "M. Chen" hours_overtime=Decimal("2") and
    hours_regular=Decimal("40").
  - Pre-resume reconciliation: persist reconciliation mapping "M. Chen" → chen_id (resolved=True).
  - clarified_fields: set {str(chen_id): {"hours_overtime": "confirmed_dropped"}} — simulates that
    Round-1 asked about OT and the client explicitly zeroed it out. is_round_2=True.
  - Configure mock LLM to return extraction for Round-2 with "Maria Chen" (restated name) and
    hours_overtime=None (silence — consistent with confirmed_dropped).
    hours_regular=Decimal("40") should be present so the employee is not a zero-hours case.
  - Call resume_pipeline.

  ASSERTIONS:
  a) OT is NOT backfilled — confirmed_dropped is honored (D-14):
     Load final paystub line items from DB.
     Find the line item for chen_id.
     Assert: hours_overtime is None or Decimal("0") (NOT Decimal("2")).
     
  b) Run advances to AWAITING_APPROVAL — N8 suppression fired, no re-clarify loop:
     Assert: run status == RunStatus.AWAITING_APPROVAL (not AWAITING_REPLY).
     Assert: no new email_messages row with purpose="clarification_field_regression" after Round-2.

  Comment in test body:
  "# BLOCKER FIX cross-plan key-type consistency pin (resolved_drops).
  # FAILS for any of these regressions:
  # 1. Plan 03 Step E2 keys set by submitted_name: _resolved_by_name = {('M. Chen', 'hours_overtime')}
  #    -> 'Maria Chen' round-2 name ≠ 'M. Chen' -> backfill guard misses -> OT=2 backfilled (D-14 violated)
  # 2. Plan 02 N8 check uses current_emp_id (UUID) instead of str(current_emp_id):
  #    (UUID, str) ∉ set[tuple[str, str]] -> N8 never matches -> field_regression re-emitted -> loop
  # 3. Plan 03 backfill_extracted guard keys by emp.submitted_name instead of resolving via matches:
  #    ('Maria Chen', field) ∉ {(str(chen_id), field)} -> re-backfills OT against D-14"

  - Hermetic cleanup.

  Test 11 — test_detect_fired_on_raw (NEW — D-7.5-10 three-phase ordering R2-1 proof):
  This test is the primary proof that detection fires on RAW extracted (before backfill_extracted runs).
  The R2-1 finding: after the R3-1 fix (backfill before validate), the validate() call was ALSO the
  detector — so when backfill ran first, it overwrote OT=None back to OT=2, and validate()/detect
  saw OT=2 on both sides → no field_regression issue → no clarification sent. Plan 03 fixes this by
  making detect_field_regression a SEPARATE step (Phase 1) that runs before backfill (Phase 2).

  PART A (detection fires on RAW):
  Setup:
  - Set up a FRESH run with NO clarified_fields (is_round_2=False, Round-1 path).
  - Create pre_clarify_extracted with a coastal hourly employee "Alice Torres" hours_overtime=Decimal("2").
  - Persist reconciliation mapping "Alice Torres" → torres_id (resolved=True). This populates prior_matches.
  - Configure mock LLM to return extraction for Round-1 reply with "Alice Torres" OT=None (silence — the client submitted hours but didn't mention OT this time).
  - Call resume_pipeline.

  ASSERTIONS FOR PART A (detection fired on RAW):
  - Run is at AWAITING_REPLY (field_regression issue was detected and clarification deferred).
  - clarified_fields has "asked" for hours_overtime for str(torres_id) (asked written durably before send — N2).
  - An outbound email_messages row with purpose="clarification_field_regression" exists.
  - Comment: "# D-7.5-10 detect-on-raw proof:
    # The extract for Round-1 has OT=None (raw). detect_field_regression sees OT: 2→None (a drop).
    # field_regression issue emitted. clarification deferred. asked written. _clarify sent.
    # If backfill_extracted ran BEFORE detect_field_regression, OT would be filled back to 2 from snapshot
    # (Phase 2 would run before Phase 1), detect would see OT: 2→2 (no drop), no issue emitted, no
    # clarification, run would advance to AWAITING_APPROVAL with OT silently carried — exactly the R2-1
    # failure mode. The three-phase ordering (D-7.5-10) prevents this."

  PART B (carry-forward round paystub — proves the round-trip with test_ordering_carried_forward_ot_in_paystub):
  After Part A established the clarification was sent and "asked" was written, now simulate Round-2
  (the carry-forward round where the client is still silent):
  - Set clarified_fields to {str(torres_id): {"hours_overtime": "asked"}} (Part A already did this; load and verify).
  - Configure mock LLM to return extraction for Round-2 with "Alice Torres" OT=None (still silent).
  - Call resume_pipeline again (Round-2 path).
  - ASSERT: the FINAL PAYSTUB LINE ITEM for torres_id shows hours_overtime == Decimal("2") (the backfill fired in Phase 2, BEFORE calc in Phase 3 — the carried-forward value reached the paystub).
  - ASSERT: clarified_fields outcome for hours_overtime is "carried_forward".
  - Comment: "# Full round-trip proof:
    # Part A: detect fires on raw (OT 2→None visible, field_regression issue emitted, asked written).
    # Part B: carry-forward round — backfill fires on BACKFILLED extracted (Phase 2 after Phase 1),
    # carried value OT=2 flows through validate/decide/calc to the paystub (Phase 3).
    # Together with test_ordering_carried_forward_ot_in_paystub, these two tests prove D-7.5-10:
    # detect-on-raw → backfill → calc."

  - Hermetic cleanup.

  Test 12 — test_client_supplied_same_value_labeled_correctly (NEW — R2-3 fix proof):
  Before the R2-3 fix, the Round-2 classifier read from the post-run DB data (already backfilled).
  When a client re-supplied the same positive value as the snapshot (e.g., OT=2 in raw reply, OT=2 in
  snapshot), the classifier compared raw_value (from backfilled DB) == snap_val → both were 2 → labeled
  "carried_forward". This was wrong: the client DID supply the value; it should be "client_supplied".

  After the fix: the classifier reads from stage.issues (computed from raw_drops on the RAW extraction).
  A present-positive raw field does NOT cause a field_regression issue to be emitted (there is no drop).
  So when classifying "asked" outcomes: if the field is NOT in stage.issues as field_regression → the raw
  reply had a present-positive value → client_supplied.

  Setup:
  - Set up a run with pre_clarify_extracted snapshot of "Alice Torres" hours_overtime=Decimal("2").
  - Persist reconciliation mapping "Alice Torres" → torres_id (resolved=True).
  - Set clarified_fields = {str(torres_id): {"hours_overtime": "asked"}} (simulating Round-1 asked).
    is_round_2=True (clarified non-empty).
  - Configure mock LLM to return extraction for Round-2 with "Alice Torres" OT=Decimal("2") — the
    client explicitly re-supplied OT=2 in their reply (same value as snapshot — the common case where
    a client confirms by re-stating the number they'd always intended to submit).
  - Call resume_pipeline (Round-2 path).

  ASSERTIONS:
  - clarified_fields outcome for hours_overtime for str(torres_id) is "client_supplied" (NOT "carried_forward").
    This is the primary R2-3 fix assertion: the client supplied a positive value in the raw reply;
    the classifier must label it "client_supplied".
  - Run advances to AWAITING_APPROVAL (no re-clarification).
  - (Optional) Check that OT=Decimal("2") in the final paystub line item (the client-supplied value
    was used as-is, not overwritten by backfill since client_supplied → not silent → not a carry-forward).
  - Comment: "# R2-3 fix proof:
    # client_supplied: client replied with OT=2 (present-positive in raw reply — same as snapshot).
    # RAW extraction has OT=2 → detect_field_regression sees no drop (OT: 2→2) → no field_regression
    # issue emitted for this employee/field → stage.issues does NOT contain a drop for (torres_id, OT).
    # Round-2 classifier: field 'hours_overtime' was 'asked'; no drop in stage.issues → client_supplied.
    #
    # Before the R2-3 fix (reading backfilled DB data):
    # backfill_extracted fills OT=None → OT=2 from snapshot. DB post-run has OT=2.
    # Classifier: raw_value == snap_val (both 2, but raw_value is BACKFILLED, not the raw reply).
    # Mislabeled as 'carried_forward' — wrong: the client explicitly supplied the value.
    #
    # The fix reads from stage.issues (derived from raw detection, Phase 1 of D-7.5-10):
    # present-positive in raw reply → no issue emitted → no entry in surviving_drops → client_supplied."

  - Hermetic cleanup.

  Each test has a finally: block performing cleanup via the method chosen in the module header.
  </action>

  <verify>
    <automated>uv run pytest tests/test_resume_pipeline.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
  - `tests/test_resume_pipeline.py` exists with 12+ test functions
  - `grep "^def test_" tests/test_resume_pipeline.py | wc -l` returns 12+
  - `grep "test_detect_fired_on_raw" tests/test_resume_pipeline.py` exits 0 (NEW — D-7.5-10 R2-1 proof)
  - `grep "D-7.5-10 detect-on-raw proof" tests/test_resume_pipeline.py` exits 0 (comment pinning why test 11 matters)
  - `grep "test_client_supplied_same_value_labeled_correctly" tests/test_resume_pipeline.py` exits 0 (NEW — R2-3 proof)
  - `grep "R2-3 fix proof" tests/test_resume_pipeline.py` exits 0 (comment pinning test 12)
  - `grep "client_supplied" tests/test_resume_pipeline.py` exits 0 (test 12 assertion)
  - `grep "test_ordering_carried_forward_ot_in_paystub" tests/test_resume_pipeline.py` exits 0 (D-7.5-04a)
  - `grep "test_approved_bytes_equals_sent_bytes" tests/test_resume_pipeline.py` exits 0 (D-7.5-04b)
  - `grep "test_tri_state_through_real_path" tests/test_resume_pipeline.py` exits 0 (D-7.5-04c)
  - `grep "test_mixed_issue" tests/test_resume_pipeline.py` exits 0 (SC4)
  - `grep "test_restated_name_prior_matches_threading" tests/test_resume_pipeline.py` exits 0 (R3-3 integration pin)
  - `grep "R3-3 integration pin" tests/test_resume_pipeline.py` exits 0 (comment pinning why test 9 matters)
  - `grep "R2-2 fix pin" tests/test_resume_pipeline.py` exits 0 (test 9 Part B comment — carry-forward paystub)
  - `grep "test_confirmed_dropped_no_reloop_on_round2" tests/test_resume_pipeline.py` exits 0 (BLOCKER FIX)
  - `grep "BLOCKER FIX cross-plan key-type" tests/test_resume_pipeline.py` exits 0 (comment pinning test 10)
  - `grep "pytestmark" tests/test_resume_pipeline.py` exits 0 (module-level skip guard)
  - `grep "finally:" tests/test_resume_pipeline.py | wc -l` returns 12+ (one per test — hermetic cleanup)
  - `grep "# Hermetic discipline" tests/test_resume_pipeline.py` exits 0 (Finding 7 — states chosen approach)
  - `grep "confirmation\|compose_confirmation\|_deliver\|PDF\|line_item" tests/test_resume_pipeline.py` exits 0 (Finding 8)
  - WITH DATABASE_URL set: `uv run pytest tests/test_resume_pipeline.py -v` shows 12+ PASSED, 0 SKIPPED, 0 FAILED
  - `uv run pytest tests/test_resume_pipeline.py -k "detect_fired_on_raw" -v` PASSES (R2-1 D-7.5-10 proof)
  - `uv run pytest tests/test_resume_pipeline.py -k "client_supplied" -v` PASSES (R2-3 proof)
  - `uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v` PASSES
  - `uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v` PASSES (R3-3 end-to-end pin; test 9 Part B asserts OT=2 in paystub on R2-2-fixed carry-forward path)
  - `uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v` PASSES (BLOCKER FIX)
  </acceptance_criteria>

  <done>12 integration tests PASS (not SKIP) with DATABASE_URL set. test_detect_fired_on_raw (test 11) pins D-7.5-10: OT 2→None drop visible to detection on RAW (Part A — field_regression emitted, asked written before send, clarify sent); carry-forward round paystub OT=2 (Part B — backfill fired in Phase 2 before Phase 3 calc). test_client_supplied_same_value_labeled_correctly (test 12) pins R2-3: client resupplies OT=2 → no field_regression issue in stage.issues → classifier labels client_supplied (not carried_forward). test_restated_name_prior_matches_threading (test 9) strengthened with Part B: Round-2 carry-forward round for restated-name path asserts OT=2 in final paystub (R2-2 fix proof — employee_id-keyed backfill). D-7.5-04a ordering test pins carried_forward OT=2. D-7.5-04b approved==sent exercises confirmation path (Finding 8). D-7.5-04c tri-state through real JSONB. SC4 mixed-issue. Test 10 pins BLOCKER FIX cross-plan key-type consistency. All 12 tests hermetic per Finding 7 (consistent cleanup approach documented).</done>
</task>

<task type="auto">
  <name>Task 2: Eval fixtures 16/17/18 + run_eval.py prior_extracted + summary regen + run_detail.html provenance badges (4 outcomes)</name>
  <files>eval/run_eval.py, eval/fixtures/16_zero_hours_hourly_coastal.json, eval/fixtures/16_zero_hours_hourly_coastal_extraction.json, eval/fixtures/17_nfd_name_coastal.json, eval/fixtures/17_nfd_name_coastal_extraction.json, eval/fixtures/18_field_drop_clarify_carry_coastal.json, eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json, eval/summary.json, app/templates/run_detail.html</files>

  <read_first>
  Read eval/run_eval.py in full. Key anchors:
  - The fixture loader (wherever "expected" and "fixture_category" are stripped before InboundEmail construction) — add "prior_extracted" to the strip set.
  - Where validate() is called — add prior=prior_extracted kwarg.
  - The --record and --check flag behavior + summary.json path.
  - The extraction cache writer (extracted.model_dump_json(indent=2)) — confirms D-24 compliance.

  Read eval/fixtures/07_missing_hours_coastal.json to get the exact fixture structure (id, message_id, in_reply_to, references_header, subject, from_addr, to_addr, body_text, created_at, fixture_category, expected).

  Read app/db/seed.py to find coastal business employees for plausible names in fixtures 16/17/18.

  Read app/templates/run_detail.html lines 120-155 (the extracted data per-employee table) and lines 195-215 (the outbound emails section with purpose badge).
  - The per-field row lines (~143-150): these gain inline provenance badges.
  - PATTERNS.md §7 has the exact badge markup to mirror for provenance (D-7.5-08).

  Finding 6 note: run_detail.html now needs badges for ALL FOUR outcomes: carried-forward, confirmed_dropped (client removed), client_supplied (new outcome from R2-3 fix), and asked (awaiting reply). The ClarifiedFieldOutcome Literal was expanded to four values in Plan 03.

  Fixture 17 HONESTY check: if the coastal seed has no accented-name employee, use a coastal employee's name with extra internal whitespace (e.g. "Maria  Chen" with double space) as the submitted name — certifies _normalize whitespace normalization / C-4 parity. Document scope in SUMMARY: "certifies eval scorer _normalize parity, NOT full NFD Unicode (that proof is test_reconcile.py)."
  </read_first>

  <action>
  Step A — Update eval/run_eval.py:

  1. In the fixture loader, also strip "prior_extracted" from the dict before constructing InboundEmail. Save the value before stripping: `prior_extracted_raw = fixture_raw.pop("prior_extracted", None)` (or use .get then exclusion-list).

  2. After loading the fixture and before calling validate(), load prior_extracted:
     If prior_extracted_raw is not None: `prior_extracted = Extracted.model_validate(prior_extracted_raw)`
     Else: `prior_extracted = None`.

  3. Thread into validate() call: `validate(extracted, roster, matches, prior=prior_extracted)`.

  Step B — Write a generation script (scratchpad, NOT committed) to produce fixture extraction caches via model_dump_json (D-24). Script path: /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/2948e6e3-bf23-44c6-b200-d9c6281e738e/scratchpad/gen_75_fixtures.py

  The script imports Extracted, ExtractedEmployee from app.models.contracts and creates each Extracted programmatically via model_dump_json(indent=2).

  Step C — Create the six fixture files:

  Fixture 16 (MONEY-01 — zero-hours hourly):
  - Pick a real coastal hourly employee from seed.py.
  - Main fixture: expected.decision.final_action="request_clarification"; fixture_category="zero-hours-hourly".
  - Extraction cache: hours_regular="0.00", all other hours null (via model_dump_json).

  Fixture 17 (MONEY-02 / C-4 eval-scorer parity):
  - Pick a real coastal employee. Use their name with extra internal whitespace as submitted_name.
  - Expected: final_action="process", resolved=True (whitespace-normalized match).
  - fixture_category="nfc-normalizer-parity" (honest: certifies _normalize, NOT "nfd-unicode").
  - Extraction cache: Extracted with the extra-whitespace submitted name (via model_dump_json).

  Fixture 18 (MONEY-03 — field regression with prior_extracted):
  - Pick a real coastal hourly employee. The resumed extraction has hours_overtime=None. The prior snapshot has hours_overtime=Decimal("2").
  - Main fixture: add "prior_extracted" key containing the prior Extracted as a plain dict (prior_obj.model_dump(mode="json")). Expected: final_action="request_clarification".
  - fixture_category="field-regression-carry-forward".
  - Extraction cache: the RESUMED extraction (hours_overtime null) via model_dump_json.

  Step D — Run the generation script and verify D-24 compliance:
  uv run python /private/tmp/claude-501/-Users-pnhek-usf-msds-github-payroll-agent/2948e6e3-bf23-44c6-b200-d9c6281e738e/scratchpad/gen_75_fixtures.py
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/16_zero_hours_hourly_coastal_extraction.json').read())); print('Fixture 16 OK:', e.employees[0].hours_regular)"
  uv run python -c "from app.models.contracts import Extracted; import json; e = Extracted.model_validate(json.loads(open('eval/fixtures/18_field_drop_clarify_carry_coastal_extraction.json').read())); print('Fixture 18 OK OT:', e.employees[0].hours_overtime)"

  Step E — Run eval and regenerate summary.json:
  uv run python eval/run_eval.py 2>&1 | tail -30
  uv run python eval/run_eval.py --record
  uv run python eval/run_eval.py --check 2>&1 | tail -5

  Step F — Add provenance badges to app/templates/run_detail.html (D-7.5-08):

  1. In the per-employee hours rows section (~lines 143-150), add a Jinja2 conditional badge after each hours value cell. Mirror the existing name-source badge style exactly (PATTERNS.md §7). FOUR outcomes (Finding 6 fix + R2-3 fix — client_supplied is the fourth outcome added by Plan 03):
     - "carried_forward" -> badge-pending "carried-forward" (value from snapshot; client was SILENT — D-7.5-10b clarifies this label means NO raw reply value, not same-value resupply which is client_supplied)
     - "confirmed_dropped" -> badge-neutral "client removed" (explicit zero/none from client)
     - "client_supplied" -> badge-good "client supplied" (positive replacement value from client — R2-3 fix: present-positive in raw reply before backfill; NOT same-value resupply mislabeled)
     - "asked" -> badge-bad "awaiting reply" (field-regression question sent; reply pending)

  2. In the outbound emails section (~lines 202-208), add a case for 'clarification_field_regression':
     `{% elif msg.purpose == 'clarification_field_regression' %}<span class="badge badge-pending">field-regression-clarification</span>`
     Insert between the 'clarification' case and the else fallback.

  3. Ensure run_detail.html receives clarified_fields in the run dict. Check main.py for the run_detail endpoint — add `repo.load_clarified_fields(run_id)` to the run context dict passed to the template, if not already present.
  </action>

  <verify>
    <automated>uv run python eval/run_eval.py --check 2>&1 | tail -10; uv run pytest -q 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
  - `grep "prior_extracted" eval/run_eval.py` exits 0 (stripping and loading logic)
  - All 6 fixture files exist: `ls eval/fixtures/{16,17,18}_*.json | wc -l` returns 6
  - `grep "prior_extracted" eval/fixtures/18_field_drop_clarify_carry_coastal.json` exits 0 (fixture has the key)
  - Extraction caches parse via Extracted.model_validate without error (D-24 compliance)
  - `uv run python eval/run_eval.py --check` exits 0 (after --record regenerates summary.json with fixtures 16/17/18)
  - `grep "carried-forward" app/templates/run_detail.html` exits 0 (D-7.5-08 carried-forward badge)
  - `grep "client.removed\|client removed" app/templates/run_detail.html` exits 0 (confirmed_dropped badge)
  - `grep "client.supplied\|client supplied" app/templates/run_detail.html` exits 0 (client_supplied badge — R2-3 fix fourth outcome)
  - `grep "awaiting.reply\|awaiting reply" app/templates/run_detail.html` exits 0 (asked badge)
  - `grep "clarification_field_regression" app/templates/run_detail.html` exits 0 (purpose badge in email thread)
  - `uv run pytest -q` full suite exits 0 (no regressions from run_eval.py changes or template changes)
  </acceptance_criteria>

  <done>Fixtures 16/17/18 exist and score correctly. run_eval.py wired with prior_extracted support. summary.json regenerated and --check passes. run_detail.html has four provenance badges: carried-forward (client was SILENT — D-7.5-10b; NOT same-value resupply), client-removed (confirmed_dropped), client-supplied (positive replacement from client — R2-3 fix; present-positive in raw reply before backfill), awaiting-reply (asked). Clarification_field_regression purpose badge. Full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task 1: 12 hermetic integration tests in tests/test_resume_pipeline.py.
  
  NEW R2-1 PROOF — test_detect_fired_on_raw (test 11): TWO-PART test. Part A: OT 2→None on Round-1 path — asserts field_regression emitted on RAW extraction (before backfill), asked written durably before send (N2), clarify sent with purpose='clarification_field_regression', run at AWAITING_REPLY. Negative companion documents: if backfill ran before detect, OT would be 2 on both sides, no drop seen, no issue, no clarification — the R2-1 failure mode. Part B: carry-forward Round-2 — asserts OT=2 in final paystub (Phase 2 backfill before Phase 3 calc — full round-trip with test_ordering_carried_forward_ot_in_paystub).
  
  NEW R2-3 PROOF — test_client_supplied_same_value_labeled_correctly (test 12): client replies OT=2 (same as snapshot) → no field_regression issue in stage.issues (present-positive raw → no drop) → classifier labels client_supplied (not carried_forward). Before the fix: backfilled DB had OT=2, raw_value==snap_val both 2 → mislabeled carried_forward.
  
  STRENGTHENED test_restated_name_prior_matches_threading (test 9) with Part B: Round-2 carry-forward round for "M. Chen" → "Maria Chen" restated-name path asserts OT=2 in final paystub — proves R2-2 fix (employee_id-keyed backfill_extracted via prior_matches). FAILS if backfill_extracted still uses submitted_name-keyed snapshot lookup.

  All 10 existing tests from previous revision preserved. D-7.5-04a ordering test. D-7.5-04b approved==sent strengthened (Finding 8). D-7.5-04c tri-state. Loop-guard. SC4 mixed-issue. Test 10 BLOCKER FIX cross-plan key-type consistency. All 12 tests hermetic per Finding 7.
  
  Task 2: Eval fixtures 16/17/18, run_eval.py prior_extracted support, summary.json regenerated, run_detail.html provenance badges (four outcomes: carried-forward/client-removed/client-supplied/awaiting-reply — R2-3 fix and Finding 6 all four ClarifiedFieldOutcome values; client-supplied badge tooltip clarifies it means present-positive in raw reply before backfill, NOT same-value resupply mislabeled; D-7.5-08).
  </what-built>
  <how-to-verify>
  PHASE GATE — DATABASE_URL must be set in shell env. All integration tests must PASS (not SKIP).

  1. Integration tests (D-7.5-04 phase-completion gate):
     uv run pytest tests/test_resume_pipeline.py -v
     Expected: 12+ PASSED, 0 SKIPPED, 0 FAILED.
     If any show SKIPPED: DATABASE_URL is not set — set it and re-run.

  2. NEW R2-1 D-7.5-10 detect-on-raw proof (test 11):
     uv run pytest tests/test_resume_pipeline.py -k "detect_fired_on_raw" -v
     Expected: 1 PASSED.
     Verify Part A: field_regression issued on RAW, asked written, clarify sent, AWAITING_REPLY.
     Verify Part B: carry-forward Round-2 paystub OT=2 (backfill fired in Phase 2 before Phase 3 calc).
     Verify: negative companion comment documents the masking failure mode (backfill before detect → no issue).

  3. NEW R2-3 client_supplied classification proof (test 12):
     uv run pytest tests/test_resume_pipeline.py -k "client_supplied_same_value" -v
     Expected: 1 PASSED.
     Verify: clarified_fields outcome is "client_supplied" NOT "carried_forward".
     Verify: comment explains the R2-3 fix (stage.issues from raw_drops, not backfilled DB data).

  4. D-7.5-04a ordering test (root-cause pin):
     uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v
     Expected: 1 PASSED.

  5. R3-3 integration pin — STRENGTHENED (test 9 Part A + Part B):
     uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v
     Expected: 1 PASSED.
     Verify Part A: AWAITING_REPLY (field_regression detected via employee_id-keyed diff on Round-1).
     Verify Part B (R2-2 fix): carry-forward Round-2 paystub OT=2 (M. Chen snapshot → Maria Chen reply, same employee_id, employee_id-keyed backfill_extracted finds the snapshot — FAILS if backfill still submitted_name-keyed).

  6. BLOCKER FIX cross-plan key-type consistency pin (test 10):
     uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v
     Expected: 1 PASSED.
     Confirm restated-name variant (M. Chen → Maria Chen). Confirm OT NOT backfilled (D-14). Confirm AWAITING_APPROVAL (N8 suppression).

  7. D-7.5-04b approved==sent (strengthened — Finding 8):
     uv run pytest tests/test_resume_pipeline.py -k "approved_bytes" -v
     Expected: 1 PASSED. Confirm: asserts actual confirmation body or PDF field values, not just DB column unchanged.

  8. Eval layer (D-23):
     uv run python eval/run_eval.py --check
     Expected: exits 0. Fixtures 16/17/18 score correctly.

  9. ROADMAP Success Criteria verification:
     SC1 (D-7.5-10 three-phase ordering — R2-1):
     python3 -c "import ast; src=open('app/pipeline/orchestrator.py').read(); tree=ast.parse(src); [print(sorted([(n.lineno, getattr(getattr(n,'func',None),'id',None)) for n in ast.walk(fn) if isinstance(n,ast.Call) and getattr(getattr(n,'func',None),'id',None) in ('detect_field_regression','backfill_extracted','validate','_compute_line_items')])) for fn in ast.walk(tree) if isinstance(fn,ast.FunctionDef) and fn.name=='_run_stages']"
     Expected: detect_field_regression line < backfill_extracted line < validate line < _compute_line_items line.

     SC2 (restated-name detection + carry-forward paystub):
     uv run pytest tests/test_validate.py -k "restated_name" -v
     Expected: PASSED (unit test — detect_field_regression employee_id-keyed diff).
     uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v
     Expected: PASSED (integration — R3-3 threading AND R2-2 carry-forward paystub).

     SC3 (carry-forward vs confirmed_dropped):
     uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward or tri_state" -v
     Expected: 2 PASSED.
     uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v
     Expected: 1 PASSED.

     SC3b (R2-3 client_supplied classification):
     uv run pytest tests/test_resume_pipeline.py -k "client_supplied" -v
     Expected: 1 PASSED (client resupply labeled client_supplied, not carried_forward).

     SC4 (mixed-issue):
     uv run pytest tests/test_resume_pipeline.py -k "mixed_issue" -v
     Expected: PASSED.

  10. Provenance badge review:
      grep "carried-forward" app/templates/run_detail.html
      grep "client removed" app/templates/run_detail.html
      grep "client supplied" app/templates/run_detail.html
      grep "awaiting reply" app/templates/run_detail.html
      grep "clarification_field_regression" app/templates/run_detail.html
      All should return matches.

  11. Finding 7 hermetic confirmation:
      grep "# Hermetic discipline" tests/test_resume_pipeline.py
      grep "finally:" tests/test_resume_pipeline.py | wc -l
      (Should show consistent approach documented, 12+ finally blocks.)

  12. BLOCKER FIX consistency check (cross-plan):
      grep "str(current_emp_id)" app/pipeline/validate.py
      (Plan 02: N8 check uses str() conversion — exits 0)
      grep "id_to_submitted_name" app/pipeline/orchestrator.py
      (Plan 03 Step E2: reverse-lookup eliminated — must return no lines)
      grep "_resolved_by_name" app/pipeline/orchestrator.py
      (Plan 03 Step E2: set built directly from clarified.items() emp_id_str keys)

  13. Full suite:
      uv run pytest -q
      Expected: all green.
  </how-to-verify>
  <resume-signal>Type "approved" if all 12 integration tests PASS (not SKIP) with DATABASE_URL set, eval --check passes, AND:
  - test_detect_fired_on_raw (test 11) PASSES: Part A asserts field_regression issued on RAW (not backfilled data), asked written before send, clarify sent (R2-1 D-7.5-10 proof); Part B asserts paystub OT=2 on carry-forward round; negative companion comment present
  - test_client_supplied_same_value_labeled_correctly (test 12) PASSES: outcome is "client_supplied" NOT "carried_forward" (R2-3 proof); R2-3 fix comment present
  - test_restated_name_prior_matches_threading (test 9) Part B PASSES: carry-forward Round-2 paystub OT=2 for M. Chen → Maria Chen restated name (R2-2 employee_id-keyed backfill proof); R2-2 fix pin comment present
  - SC1 AST ordering (detect < backfill < validate < calc) verified
  - SC2 restated-name at both unit and integration levels
  - SC3b client_supplied classification
  - approved==sent exercises confirmation path (not just DB columns)
  - all four provenance badges in run_detail.html (carrying-forward/client-removed/client-supplied/awaiting-reply)
  Describe any failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval/fixtures/*.json prior_extracted key | Read from disk; Extracted.model_validate rejects malformed data at load time (extra="forbid") |
| integration tests -> live DB | Tests write synthetic run_ids to real DB; hermetic cleanup in finally blocks or transaction rollback |
| run_detail.html clarified_fields | Read from DB via load_clarified_fields; template renders string outcomes only — no code execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7.5-04-01 | Tampering | eval fixture prior_extracted parsed by Extracted.model_validate | mitigate | model_validate with extra="forbid" rejects any unexpected field; D-24 compliance (model_dump(mode="json")) ensures Decimal precision is correct. |
| T-7.5-04-02 | Information Disclosure | Integration tests write to real Supabase dev DB | accept | Tests use uuid.uuid4() run IDs and synthetic extraction data (no real employee PII). Hermetic cleanup in finally blocks (Finding 7 fix). |
| T-7.5-04-03 | Elevation of Privilege | Test run_ids colliding with production runs | mitigate | uuid.uuid4() generates cryptographically random IDs; collision probability negligible. Hermetic cleanup ensures no orphan test data. |
| T-7.5-04-04 | Tampering | run_detail.html clarified_fields rendering | accept | clarified_fields is loaded from DB (Pydantic-validated on write per D-7.5-03b, 4 outcomes); template renders only known outcome strings from the Literal; no user-controlled string injection into Jinja2 logic. |
| T-7.5-04-05 | Information Disclosure | Provenance badge shows carried-forward / client_supplied labels | accept | Badges show outcome labels, not the actual values (values already shown in cell); client_supplied label (R2-3 fix — new fourth outcome) adds no additional PII disclosure beyond what was already operator-visible. |
| T-7.5-04-06 | Tampering | Strengthened approved==sent test exercises _deliver/compose_confirmation | accept | Test exercises the confirmation composition path to verify the invariant holds; no new production code added, only a stronger test assertion. |
| T-7.5-04-07 | Tampering | test_detect_fired_on_raw exercises D-7.5-10 three-phase ordering | accept | Test is read-only evidence (integration test). Part A proves detect fires on RAW. Part B proves carry-forward reaches paystub (backfill before calc). No new production code; the fix is in Plan 03. |
| T-7.5-04-08 | Tampering | test_client_supplied_same_value_labeled_correctly reads clarified_fields outcome | accept | Test reads DB outcome written by Plan 03's classifier; tests the label (client_supplied vs carried_forward) not the value. The R2-3 fix (reading stage.issues from raw detection) is in Plan 03; this test pins the regression. |
| T-7.5-04-09 | Tampering | Test 10 confirmed_dropped no-reloop exercises resolved_drops cross-plan path | accept | Test is read-only evidence (integration test); the fix itself is in Plans 02/03. Test 10 pins the regression so a future reversal is immediately detected. |
| T-7.5-04-SC | Tampering | No new package installs | accept | No uv add invoked; all imports from existing pyproject.toml deps. |
</threat_model>

<verification>
Phase-gate (all must pass WITH DATABASE_URL set):

1. `uv run pytest tests/test_resume_pipeline.py -v` — 12+ PASSED, 0 SKIPPED, 0 FAILED
2. `uv run pytest tests/test_resume_pipeline.py -k "detect_fired_on_raw" -v` — PASSED (D-7.5-10 R2-1 proof — Part A: detect fires on RAW before backfill; Part B: carry-forward round paystub OT=2)
3. `uv run pytest tests/test_resume_pipeline.py -k "client_supplied_same_value" -v` — PASSED (R2-3 proof — outcome is client_supplied not carried_forward for same-value resupply)
4. `uv run pytest tests/test_resume_pipeline.py -k "ordering_carried_forward" -v` — PASSED (D-7.5-04a root-cause pin)
5. `uv run pytest tests/test_resume_pipeline.py -k "restated_name_prior_matches" -v` — PASSED (R3-3 integration pin + R2-2 paystub assertion: OT=2 in final paystub for M. Chen → Maria Chen restated-name carry-forward path)
6. `uv run pytest tests/test_resume_pipeline.py -k "confirmed_dropped_no_reloop" -v` — PASSED (BLOCKER FIX — cross-plan key-type consistency: Plan 03 Step E2 emp_id_str keying + Plan 02 str(current_emp_id) N8 check, restated-name variant)
7. `uv run pytest tests/test_resume_pipeline.py -k "approved_bytes" -v` — PASSED and exercises confirmation path (D-7.5-04b strengthened — Finding 8)
8. `uv run pytest tests/test_resume_pipeline.py -k "tri_state" -v` — PASSED (D-7.5-04c)
9. `uv run pytest tests/test_resume_pipeline.py -k "mixed_issue" -v` — PASSED (SC4 / R3-2)
10. `uv run python eval/run_eval.py --check` — exits 0
11. `grep "prior_extracted" eval/run_eval.py` — exits 0
12. `grep "prior_extracted" eval/fixtures/18_field_drop_clarify_carry_coastal.json` — exits 0
13. `grep "carried-forward" app/templates/run_detail.html` — exits 0 (D-7.5-08)
14. `grep "client removed" app/templates/run_detail.html` — exits 0 (confirmed_dropped badge)
15. `grep "client supplied" app/templates/run_detail.html` — exits 0 (client_supplied badge — R2-3 fix new outcome)
16. `grep "awaiting reply" app/templates/run_detail.html` — exits 0 (asked badge)
17. `grep "clarification_field_regression" app/templates/run_detail.html` — exits 0
18. `grep "# Hermetic discipline" tests/test_resume_pipeline.py` — exits 0 (Finding 7)
19. `grep "R3-3 integration pin" tests/test_resume_pipeline.py` — exits 0 (comment pinning test 9 Part A)
20. `grep "R2-2 fix pin" tests/test_resume_pipeline.py` — exits 0 (comment pinning test 9 Part B — carry-forward paystub)
21. `grep "D-7.5-10 detect-on-raw proof" tests/test_resume_pipeline.py` — exits 0 (test 11 Part A comment)
22. `grep "R2-3 fix proof" tests/test_resume_pipeline.py` — exits 0 (test 12 comment)
23. `grep "BLOCKER FIX cross-plan key-type" tests/test_resume_pipeline.py` — exits 0 (comment pinning test 10)
24. `grep "str(current_emp_id)" app/pipeline/validate.py` — exits 0 (Plan 02 N8 fix — no UUID vs str mismatch)
25. `grep "id_to_submitted_name" app/pipeline/orchestrator.py` — exits 1 (Plan 03 Step E2 fix — reverse-lookup eliminated)
26. `uv run pytest -q` — full suite exits 0
27. R3-1/R2-1 AST ordering check: detect_field_regression line < backfill_extracted line < validate line < _compute_line_items line in _run_stages
</verification>

<success_criteria>
- 12 integration tests in test_resume_pipeline.py, ALL PASSING with DATABASE_URL set; pytestmark skipif guard present; hermetic discipline documented and consistent (Finding 7)
- test_detect_fired_on_raw (test 11): D-7.5-10 three-phase ordering proof. Part A: OT 2→None → field_regression issued on RAW (detection fires before backfill), asked written before send (N2), clarify sent with clarification_field_regression purpose, run at AWAITING_REPLY. Negative companion documents the R2-1 masking failure mode. Part B: carry-forward Round-2 → paystub OT=2 (Phase 2 backfill before Phase 3 calc completes the round-trip). FAILS if detect runs on backfilled data (Part A) or if backfill runs after calc (Part B).
- test_client_supplied_same_value_labeled_correctly (test 12): R2-3 fix proof. Client replies OT=2 (same as snapshot) → no field_regression issue in stage.issues → outcome is "client_supplied" NOT "carried_forward". FAILS if classifier reads backfilled DB data and mislabels same-value resupply.
- test_restated_name_prior_matches_threading (test 9) STRENGTHENED: Part A asserts Round-1 detection fires (AWAITING_REPLY, asked written — R3-3 pin); Part B asserts Round-2 carry-forward paystub OT=2 for M. Chen → Maria Chen restated-name path (R2-2 employee_id-keyed backfill pin). FAILS if prior_matches defaulted to None (Part A) OR if backfill_extracted still uses submitted_name-keyed snapshot lookup (Part B).
- test_confirmed_dropped_no_reloop_on_round2: BLOCKER FIX cross-plan key-type consistency pin; restated-name variant; asserts D-14 honored AND AWAITING_APPROVAL (N8 fired).
- D-7.5-04a ordering: carried_forward OT=2 in final paystub (root-cause pin; fails if backfill after calc).
- D-7.5-04b approved==sent: exercises confirmation composition/PDF source (Finding 8).
- D-7.5-04c tri-state through real JSONB.
- Fixtures 16/17/18 (D-23 eval layer): eval --check exits 0; fixture 17 scope documented as _normalize parity.
- run_eval.py: prior_extracted stripped, loaded as Extracted, passed as prior= to validate().
- run_detail.html: FOUR provenance badges on hours rows: carried-forward (silent client — D-7.5-10b; NOT same-value resupply), client-removed (confirmed_dropped), client-supplied (positive replacement from client — R2-3 fix), awaiting-reply (asked). Clarification_field_regression purpose badge.
- Phase-completion gate signed off: all ROADMAP SC 1-4 verified (SC2 at BOTH unit and integration levels; SC3b: client_supplied classification), eval --check green, 12 integration tests PASSED (including D-7.5-10 detect-on-raw proof, R2-3 client_supplied proof, R2-2 restated-name paystub proof, BLOCKER FIX cross-plan pin).
</success_criteria>

<output>
Create `.planning/phases/07.5-clarification-reply-field-regression/07.5-04-SUMMARY.md` when done.
</output>
