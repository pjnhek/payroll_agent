---
phase: "07.5"
plan: "01"
subsystem: pipeline
tags: [refactor, seam, structural, no-op, validate, orchestrator]
dependency_graph:
  requires: []
  provides: [prior-seam, prior_matches-seam, resolved_drops-seam]
  affects: [app/pipeline/orchestrator.py, app/pipeline/validate.py]
tech_stack:
  added: []
  patterns: [keyword-only kwargs, no-op seam, call-chain threading]
key_files:
  created: []
  modified:
    - app/pipeline/orchestrator.py
    - app/pipeline/validate.py
decisions:
  - "D-7.5-01d (inline seam): kwargs slot inserted between reconcile_names and validate call in _run_stages — zero change to the transaction/error boundary and no new persists"
  - "R3-3 threading fix: prior_matches added to BOTH _run_stages signature AND the internal validate() call — eliminates the permanent-no-op bug from the previous plan version"
  - "No type annotations added for the new kwargs (Extracted import deferred to Plan 02 when prior= becomes load-bearing)"
metrics:
  duration_minutes: 1
  completed_date: "2026-06-28T08:05:11Z"
  tasks_completed: 1
  tasks_total: 1
  files_changed: 2
---

# Phase 7.5 Plan 01: Add no-op prior=/prior_matches=/resolved_drops= seam to _run_stages and validate()

Pure structural refactor that establishes the in-memory seam required for the MONEY-03 carry-forward backfill (Plans 02 and 03), with the R3-3 threading fix: all three kwargs are now correctly wired from `_run_stages` through to the internal `validate()` call.

## What Was Built

Added three keyword-only `None`-defaulted kwargs (`prior`, `prior_matches`, `resolved_drops`) to:
1. `_run_stages` in `app/pipeline/orchestrator.py` — inserted in the keyword-only section after `llm`
2. `validate()` in `app/pipeline/validate.py` — inserted as `*, prior=None, prior_matches=None, resolved_drops=None` after the three positional args

All three kwargs are threaded from `_run_stages` into its internal `validate()` call:
`validate(extracted, roster, matches, prior=prior, prior_matches=prior_matches, resolved_drops=resolved_drops)`

Both callers (`run_pipeline` at line 91 and `resume_pipeline` at line 165) are unchanged — they call `_run_stages(run_id, ..., llm=llm)` and the defaults hold. The persist block (lines 278-289) is identical to before — same 5 autocommit writes in the same order.

## Verification

- `grep "def _run_stages" app/pipeline/orchestrator.py` — shows `prior=None, prior_matches=None, resolved_drops=None` (all three)
- `grep -A7 "def validate" app/pipeline/validate.py` — shows `*, prior=None, prior_matches=None, resolved_drops=None` keyword-only params
- `grep "validate(extracted, roster, matches" app/pipeline/orchestrator.py` — shows all three threaded: `prior=prior, prior_matches=prior_matches, resolved_drops=resolved_drops`
- `git diff HEAD~1 HEAD` — exactly 6 lines changed across 2 files; persist block untouched
- Full test suite: **465 passed, 17 skipped, 0 failures**

## Commits

| Hash | Description |
|------|-------------|
| 74f1f44 | refactor(07.5-01): add no-op prior=/prior_matches=/resolved_drops= seam to _run_stages and validate() |

## Deviations from Plan

None — plan executed exactly as written.

Note: `test_decide.py` referenced in the plan's verification command does not exist in the test suite (the decide logic is tested via `test_gate.py` and related files). The targeted suite was run without it; all 465 passing tests confirm zero regressions.

## Known Stubs

None. This is a pure structural change — no data flows through the new kwargs yet. They become load-bearing in Plan 02 (`prior=`) and Plan 03 (`prior_matches=`).

## Threat Flags

None. The new kwargs default `None` and are not used in the function bodies — no new trust boundary opens.

## Self-Check: PASSED

- `app/pipeline/orchestrator.py` — modified (verified via grep)
- `app/pipeline/validate.py` — modified (verified via grep)
- Commit `74f1f44` exists in git log
- No files deleted
- Full suite 465 passed
