---
phase: 21-durability-proofs-ops-view
plan: 04
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - tests/test_webhook_dedup_race.py
  - app/routes/webhook.py
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "PROOF-02 is identified by a marker id that survives a rename and a file move (D-01/D-02)."
    - "The proof asserts exactly one inbound_events row, one job, one run, and one email survive a redelivery of the same Svix event."
    - "The dedup key is provably derived pre-fetch — from transport headers only, never from a fetched message — which is REQUIREMENTS' named vacuity condition for PROOF-02."
    - "A falsifying mutation to the dedup-key derivation was EXECUTED live against a real Postgres and produced a red in a specifically-named assertion (D-05)."
  artifacts:
    - "tests/test_webhook_dedup_race.py — @pytest.mark.proof(id=\"PROOF-02\") on test_same_svix_redelivery_creates_one_event_one_ingest_job_and_one_run"
    - "a pre-fetch key-derivation assertion in the same proof"
    - "21-04-SUMMARY.md — mutation diff, pasted red, commit SHA, byte-identical-revert confirmation, re-run command"
  key_links:
    - "app/routes/webhook.py's external_event_id derivation ⇄ inbound_events' ON CONFLICT (external_event_id) arbiter — the two-layer dedup link the mutation severs."
---

<objective>
Promote the existing same-Svix-redelivery proof in place (D-01), give it its PROOF-02 identity
(D-02), close REQUIREMENTS' named vacuity condition as a checked fact rather than a claim, and
execute the falsifying mutation live (D-05).

Purpose: ROADMAP criterion 2 requires exactly one `jobs` row, one run and one email against a
real Postgres, **and** that the same test would fail if dedup were keyed on the RFC `Message-ID`
alone — a value unavailable until after the provider fetch. RESEARCH.md Finding 2 verifies the
incumbent already proves the positive half and that the key (`svix-id`) is available pre-fetch.
The remaining work is identity, making the pre-fetch property an assertion instead of an
argument, and executed falsification.

Output: the PROOF-02 marker on the audited incumbent, a source-level pre-fetch assertion, and
publishable mutation evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-durability-proofs-ops-view/21-CONTEXT.md
@.planning/phases/21-durability-proofs-ops-view/21-PATTERNS.md
@.planning/phases/21-durability-proofs-ops-view/21-01-SUMMARY.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit the incumbent, add the PROOF-02 identity, and assert the pre-fetch key property</name>
  <files>tests/test_webhook_dedup_race.py</files>
  <read_first>
    - `tests/test_webhook_dedup_race.py` — `test_same_svix_redelivery_creates_one_event_one_ingest_job_and_one_run`
      in full (around lines 191-311), including its existing per-function marker stack and how it
      drives two threads at the webhook with an identical `svix-id` header.
    - `app/routes/webhook.py` around lines 120-165 — the signed path that reads `svix-id` from the
      request headers and the explicitly-enabled unsigned-fixture path that derives a digest over
      the raw body. Note that neither performs a provider fetch: Phase 19 moved the fetch into the
      durable ingest worker.
    - `app/db/repo/inbound_events.py::insert_inbound_event` — the `ON CONFLICT (external_event_id)
      DO NOTHING` arbiter and the exact-key duplicate lookup.
    - `.planning/ROADMAP.md` — Phase 21 success criterion 2, verbatim.
    - `.planning/research/ARCHITECTURE.md` — the two-layer dedup argument (Svix event id vs RFC
      `Message-ID`); both layers are required and the reasoning belongs in the proof's docstring.
    - `.planning/phases/21-durability-proofs-ops-view/21-01-SUMMARY.md` — the registered marker form.
  </read_first>
  <action>
    Audit the incumbent against ROADMAP criterion 2 and REQUIREMENTS' PROOF-02 vacuity clause
    ("Vacuous if dedup is keyed on something available only post-fetch; must assert exactly one
    `jobs` row survives the `ON CONFLICT`"). Confirm in the live test body that the four "exactly
    one" assertions are present (one `inbound_events` row, one `jobs` row, one `payroll_runs` row,
    one `email_messages` row). Per D-01, add nothing beyond what the criterion requires.

    Add `@pytest.mark.proof(id="PROOF-02")` to that single test function, stacked with its existing
    `integration` and `queueproof` decorators, using the keyword `id=` form registered in plan
    21-01.

    Then close the vacuity clause as a **checked fact**. Today the pre-fetch property is an
    argument in prose; make it an assertion. Add a source-level check to this proof (or to a
    tightly-scoped companion test in the same file, also carrying `integration`/`queueproof` but
    **not** the `proof` marker — exactly one test may carry the PROOF-02 id) establishing that the
    value passed as `external_event_id` at the webhook is derived only from transport-layer inputs
    available at receipt time — the request headers and the raw request body — and never from a
    provider-fetched message. Use `inspect.getsource` plus `ast` over `app/routes/webhook.py`'s
    handler, following the AST-over-source idiom this repo already uses in
    `tests/test_bound01_private_imports.py`, rather than a text search: a text search can be
    satisfied by a comment, and this repo has already been burned by a verification grep that
    silently lied. Record in the docstring why both dedup layers exist and why the RFC
    `Message-ID` cannot serve as this layer's key.
  </action>
  <verify>
    <automated>uv run pytest tests/ -m "proof(id='PROOF-02')" --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -m "proof(id='PROOF-02')" --collect-only -q` lists exactly one node id,
      and it is the same-Svix-redelivery test in `tests/test_webhook_dedup_race.py`.
    - `grep -c 'pytest.mark.proof' tests/test_webhook_dedup_race.py` outputs `1`.
    - The pre-fetch assertion resolves the key-derivation expression through `ast`, not through a
      substring search of the file text; the test fails with a message naming the offending
      expression if the derivation ever reads from a fetched message.
    - The SUMMARY records, per half of criterion 2, the exact assertion that establishes it.
  </acceptance_criteria>
  <done>PROOF-02 carries its id, its four exactly-one assertions are confirmed present, and the pre-fetch key property is an executable assertion rather than prose.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the dedup-key falsifying mutation live and capture the red run</name>
  <files>app/routes/webhook.py, tests/test_webhook_dedup_race.py</files>
  <read_first>
    - `app/routes/webhook.py` around lines 138-165 — the exact statement that assigns
      `external_event_id` before it is handed to the durable receipt insert.
    - `.planning/phases/21-durability-proofs-ops-view/21-CONTEXT.md` — D-05 in full.
    - `tests/conftest.py` — `seeded_db` and its `ALLOW_DB_RESET` two-factor guard.
  </read_first>
  <action>
    **Precondition, blocking.** Confirm `DATABASE_URL` points at a reachable throwaway Postgres and
    that `uv run python -m app.db.bootstrap` succeeds against it. A live-DB module self-skips
    without it and a skip reads as a pass. **Never defer this mutation to CI — that gate runs
    tests, not mutations** (D-05).

    Establish the GREEN baseline first: run the proof, confirm passed and not skipped.

    Then apply the mutation at the `external_event_id` assignment. Criterion 2's named target is
    "dedup keyed on the RFC `Message-ID` alone (unavailable until after the fetch)". That value is
    structurally unreachable at this point in the request — which is itself the proof — so the
    executable mutation is its faithful stand-in: replace the redelivery-stable, authenticated
    transport identity with an identity that is **not stable across a redelivery of the same
    event**, i.e. a per-delivery value rather than a per-event value. Change nothing else: leave
    the signature verification, the `ON CONFLICT` arbiter, and the ingest enqueue untouched, so the
    only severed link is the dedup key's stability.

    Run the proof and capture the FULL output. The expected red is the assertion counting
    `inbound_events` rows (and, downstream, the `jobs`/`payroll_runs` counts) — name which one you
    observe first. Confirm it is an assertion failure, not a skip, a collection error, or a
    signature-verification error; a red for an unrelated reason is visibly not a falsification and
    means the mutation target is wrong.

    Revert byte-identically, verify with `git diff` that the file shows no change at all, and
    re-run to confirm green.

    Record in the SUMMARY per D-05: mutation diff, pasted red, commit SHA, byte-identical-revert
    confirmation, the named failing assertion, and the exact re-run command.
  </action>
  <verify>
    <automated>ALLOW_DB_RESET=1 uv run pytest tests/test_webhook_dedup_race.py -m "proof(id='PROOF-02')" -v -rs</automated>
  </verify>
  <acceptance_criteria>
    - GREEN baseline output shows `1 passed` with an empty `-rs` skip report. A `SKIPPED` line is a
      FAILED criterion.
    - The RED output is pasted in full into the SUMMARY and contains an assertion failure
      originating in the PROOF-02 test, naming a count that came back greater than one.
    - After revert, `git diff --stat app/routes/webhook.py` produces no output.
    - The post-revert run shows `1 passed` with an empty skip report.
    - The SUMMARY records the commit SHA the mutation ran against and the exact re-run command.
  </acceptance_criteria>
  <done>PROOF-02's falsification is executed evidence: a real mutation, a real red naming a count above one, a byte-identical revert, and a real green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider webhook → application | Untrusted inbound HTTP crosses here; the Svix signature and the event identity are both validated at this boundary. |
| Mutation edit → production source | The mutation temporarily weakens the transport dedup identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-04-01 | Spoofing | `external_event_id` derivation | high | mitigate | The mutation touches only the identity value, never the Svix signature verification. Revert is proven byte-identical by an empty `git diff --stat`; a surviving mutation would let a redelivery create a duplicate payroll run. |
| T-21-04-02 | Repudiation | Pre-fetch key claim | high | mitigate | The claim becomes an AST-resolved assertion over the live handler source rather than prose, so a future refactor that starts keying on post-fetch data reds the proof. Text search is explicitly rejected — a comment can satisfy it. |
| T-21-04-03 | Spoofing | A skipped live-DB test reading as a pass | high | mitigate | Explicit `DATABASE_URL` precondition, `-rs` inspected each run, `SKIPPED` defined as a failed criterion. |
| T-21-04-04 | Information Disclosure | Pasted pytest output | medium | mitigate | Paste assertions and tracebacks only; scrub any connection string before the SUMMARY, which plan 21-11 republishes publicly. |
| T-21-04-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No packages installed.** RESEARCH.md Finding 0. No `[ASSUMED]`/`[SUS]` package, therefore no legitimacy checkpoint required. |
</threat_model>

## Artifacts this phase produces (from this plan)

| Symbol | Kind | Location |
|--------|------|----------|
| `@pytest.mark.proof(id="PROOF-02")` | marker application | `tests/test_webhook_dedup_race.py::test_same_svix_redelivery_creates_one_event_one_ingest_job_and_one_run` |
| pre-fetch key-derivation assertion | AST-based test assertion | `tests/test_webhook_dedup_race.py` |
| dedup-key mutation record | evidence (diff + red + SHA + revert proof) | `21-04-SUMMARY.md` |

<verification>
- `uv run pytest tests/ -m "proof(id='PROOF-02')" --collect-only -q` collects exactly one test.
- With `DATABASE_URL` reachable: `ALLOW_DB_RESET=1 uv run pytest tests/ -m queueproof -v -rs` is
  green with an empty skip report.
- `git status --porcelain app/routes/webhook.py` is empty at plan end.
- `uv run pytest tests/ -m "not integration and not live_llm"` is green.
</verification>

<success_criteria>
- The incumbent is audited against criterion 2's exact wording with the audit conclusion recorded.
- PROOF-02's identity is a marker argument on exactly one test.
- The pre-fetch dedup-key property is an AST-resolved assertion, closing REQUIREMENTS' named
  vacuity condition.
- The falsifying mutation was executed live in-phase, produced a named red, and was reverted
  byte-identically.
</success_criteria>

<output>
Create `.planning/phases/21-durability-proofs-ops-view/21-04-SUMMARY.md` when done, in publishable
form: claim, mutation diff, pasted red, byte-identical revert, named failing assertion, commit
SHA, exact re-run command. Plans 21-10 and 21-11 consume it.
</output>
