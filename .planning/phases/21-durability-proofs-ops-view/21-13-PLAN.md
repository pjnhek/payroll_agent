---
phase: 21-durability-proofs-ops-view
plan: 13
type: execute
wave: 0
gap_closure: true
depends_on: []
files_modified:
  - tests/test_queue_durability.py
autonomous: true
requirements: []  # baseline repair — enables PROOF-01..05 and OPS-01; delivers none
must_haves:
  truths:
    - "`ALLOW_DB_RESET=1 uv run pytest tests/ -m queueproof -v -rs` is green with an empty skip report."
    - "The schema-mutating test restores the modern CHECK constraint even when its body raises — proven by forcing a failure inside it and confirming the six downstream tests still pass."
    - "The `worker.start()` AST guard still reds on a genuine direct-start violation after the rename — the fix removes a false positive without removing the guard's teeth."
    - "No production source under `app/` is modified: every failure in this cohort is a test-side contract drift."
  artifacts:
    - tests/test_queue_durability.py (four independent repairs)
  key_links:
    - "Phase 20 made `authorize_outbound_provider_handoff` mandatory (`app/pipeline/send_outbound.py:41` always authorizes) ⇄ four tests still go reserve→enqueue→claim→settle without it, so `_lock_current_provider_handoff` returns None → INVALID_CONTEXT at `app/db/repo/job_settlement.py:493`."
    - "`app/db/repo/job_settlement.py:191-199` SELECT returns 7 columns ⇄ the scripted fake row at `tests/test_queue_durability.py:863` supplies 6, so `int(row[4])` at `:210` parses the string `\"reserved\"`."
    - "The schema poisoner at `tests/test_queue_durability.py:1778-1814` ⇄ six later tests. It has NO try/finally, so a legacy CHECK leaks on failure. Deselecting it alone drops this file's failures 13 → 6."
---

<objective>
Repair the four independent test-side regressions in `tests/test_queue_durability.py` that have kept
`.github/workflows/concurrency-proof.yml` red since Phase 20. Thirteen tests fail; production source
is correct in all four cases.

This plan is a **prerequisite for plans 21-03 and 21-08**, which both edit this same file and both
carry "`-m queueproof` is green with an empty skip report" as an acceptance criterion they cannot
satisfy against today's baseline.

The four causes, in the order you will fix them (cheapest and most independent first):

- **B4 — 1 test.** `test_every_worker_start_call_goes_through_the_live_worker_wrapper` is a pure-AST
  guard (~`:2752`) that matches on the *variable name* `worker`. At `:2077` and `:2192` the local
  named `worker` is a plain `threading.Thread` that is already joined and asserted not-alive. False
  positive.
- **B2 — 1 test.** `test_invalid_context_settlement_retires_exact_leased_row` scripts a 6-tuple fake
  row at `:863`; the production SELECT returns 7 columns. The omitted `epoch` shifts every later
  index, so `int(row[4])` parses `"reserved"`.
- **B1 — 4 tests.** `test_outbound_delivery_settlement_proves_retry_cutoff_and_zombie_fence` and
  `test_final_send_lease_reap_preserves_snapshot_and_enters_purpose_review` (3 params) never call
  `authorize_outbound_provider_handoff`, which Phase 20 made mandatory.
- **B3 — 7 tests.** `test_deployed_schema_repair_accepts_authorization_expired` mutates the schema
  at `:1778-1814` with no `try/finally`. It dies on `UniqueViolation: uq_email_run_purpose_round`
  and leaks a legacy CHECK constraint into six later tests.

Output: a green `-m queueproof` selection, with the ordering dependency between B3 and its six
victims made structurally impossible to recur.
</objective>

<critical_context>
**Do not modify anything under `app/`.** All four causes are test-side drift away from a
production contract that is itself coherent. If a fix seems to require a production change, you have
misdiagnosed it — stop and report rather than editing source. `git status --porcelain app/` being
empty is an explicit acceptance criterion.

**B3 is the one judgement call in this plan.** The other three are mechanical. Read
`app/db/schema.sql:325-348` before you touch it: the widening migration cannot re-add the narrow
3-column unique constraint once epoch-distinct rows exist, which is why `bootstrap(reset=False)`
raises mid-suite. Your teardown must restore the modern CHECK **unconditionally**, including on the
raising path — that is the actual defect. Whether the test's own body can be made to stop raising is
secondary and must not be traded against the teardown.

**Order-dependence is invisible when you test one file at a time.** Six of these thirteen only fail
because another test ran first. Verify with the full `tests/ -m queueproof` selection, never with
`tests/test_queue_durability.py` alone — a single-file run will lie to you here.

**The B4 guard must keep its teeth.** Renaming two locals is the fix, but a guard that no longer
catches anything is worse than a false positive. Truth #3 requires you to demonstrate it still reds
on a genuine violation.
</critical_context>

<environment>
Every test in this plan requires a live Postgres. Your assigned throwaway database:

```bash
export DATABASE_URL="postgresql://pnhek@localhost:5432/pa_p21_13"
export ALLOW_DB_RESET=1
export ALLOW_UNSIGNED_FIXTURES=true
```

It already exists and is bootstrapped. It is a local throwaway — never point any command at
Supabase. `ALLOW_UNSIGNED_FIXTURES=true` matches `concurrency-proof.yml:44`; without it you are not
reproducing CI.

**Executed, never deferred.** Every check below runs in this session. Do not defer any verification
"to the CI gate" — that gate runs tests, not the falsifying mutations this plan asks for, so a
deferred mutation runs nowhere. If a check cannot be run, say so explicitly in SUMMARY.md and mark
the criterion unmet; do not record it as passed.
</environment>

<tasks>

<task id="1" name="B4 + B2 — two single-line repairs">
  <files>tests/test_queue_durability.py</files>

  <context>
  These two are independent of each other and of everything else in the file. Doing them together
  keeps the commit count sane; they are still two distinct logical fixes, so describe both in the
  commit body.

  For B2, read `app/db/repo/job_settlement.py:191-199` and count the selected columns yourself
  before editing the fake row. Do not trust this plan's column count — verify it against live source,
  because a wrong index here produces a passing test that asserts the wrong thing.
  </context>

  <steps>
  1. **B4:** Rename the `threading.Thread` locals named `worker` at ~`:2077` and ~`:2192` to
     something the guard will not match and that reads correctly at the call site (they are the
     handler-running threads in the handoff race/control tests). Rename every reference in those two
     functions, including the `.join()` and any not-alive assertion.
  2. **B4 red-proof:** On a scratch copy (NOT committed), introduce a genuine direct
     `worker.start()` violation and confirm the guard reds on it. Revert the scratch change. Record
     the observed failure message in SUMMARY.md — this is truth #3 and it is the difference between
     fixing the guard and defanging it.
  3. **B2:** At `:863`, insert the missing `epoch` value into the scripted fake row so its shape
     matches the production SELECT. Use an integer epoch consistent with the surrounding fixture.
  4. **B2 red-proof:** Confirm the test fails again if the epoch element is removed. Revert.
  </steps>

  <verification>
    <automated>uv run pytest tests/test_queue_durability.py::test_every_worker_start_call_goes_through_the_live_worker_wrapper -q</automated>
    <expected>1 passed. The guard reports no violations.</expected>
  </verification>

  <verification>
    <automated>uv run pytest tests/test_queue_durability.py::test_invalid_context_settlement_retires_exact_leased_row -q -rs</automated>
    <expected>1 passed, 0 skipped. A SKIPPED result here is a FAILED acceptance criterion — it means DATABASE_URL is not reaching the test.</expected>
  </verification>

  <commit>fix(21-13): unbreak the worker-start AST guard and the 7-column settlement fake row</commit>
</task>

<task id="2" name="B1 — restore the mandatory provider-handoff authorization in four tests">
  <files>tests/test_queue_durability.py</files>

  <context>
  Read `app/pipeline/send_outbound.py:41` to see where production always authorizes, and
  `app/db/repo/job_settlement.py:493` for the INVALID_CONTEXT branch these four tests currently land
  in. The four tests are `test_outbound_delivery_settlement_proves_retry_cutoff_and_zombie_fence`
  and the three params of `test_final_send_lease_reap_preserves_snapshot_and_enters_purpose_review`.

  Find how a currently-passing test in this file performs the authorize step and follow that
  existing idiom — do not invent a new helper.
  </context>

  <steps>
  1. Insert the `authorize_outbound_provider_handoff` call into each of the four tests at the correct
     point in the reserve→enqueue→claim→settle sequence, matching the production ordering in
     `send_outbound.py`.
  2. Confirm each test still asserts what its name claims. These tests exist to prove retry cutoff,
     zombie fencing, and purpose-aware review routing — if adding the authorize step makes an
     assertion trivially true, the assertion has been hollowed out and must be strengthened, not
     left. Note any such case explicitly in SUMMARY.md.
  3. Do not add the authorize step to tests that deliberately exercise the *unauthorized* path, if
     any exist. Check before applying it broadly.
  </steps>

  <verification>
    <automated>uv run pytest tests/test_queue_durability.py -k "retry_cutoff_and_zombie_fence or final_send_lease_reap" -v -rs</automated>
    <expected>4 passed, 0 skipped.</expected>
  </verification>

  <verification>
    <automated>uv run pytest tests/test_queue_durability.py -k "retry_cutoff_and_zombie_fence" -q</automated>
    <expected>
    Falsifying mutation, run now and not deferred: temporarily remove the authorize call you just
    added and confirm the test returns to INVALID_CONTEXT. This proves the added call is load-bearing
    rather than incidental. Revert immediately and record the observed failure in SUMMARY.md.
    </expected>
  </verification>

  <commit>fix(21-13): authorize the provider handoff in four settlement tests</commit>
</task>

<task id="3" name="B3 — make the schema mutation self-restoring">
  <files>tests/test_queue_durability.py</files>

  <context>
  `:1778-1814` drops the modern CHECK, installs `legacy_outbound_delivery_failure_category_check`,
  then calls `bootstrap(reset=False)` — which raises `UniqueViolation: uq_email_run_purpose_round`
  because `app/db/schema.sql:325-348`'s widening block cannot re-add the narrow 3-column unique once
  epoch-distinct rows exist. With no `try/finally`, the legacy CHECK leaks and takes down six later
  tests.

  The six victims are `test_pre_provider_authorization_expired_enters_delivery_review` (3 params)
  and `test_provider_handoff_authorization_expired_at_gateway_boundary_enters_review` (3 params).
  </context>

  <steps>
  1. Wrap the schema mutation in `try/finally` so the modern CHECK is restored on every path,
     including the raising one. The restore must be idempotent — it may run when the drop never
     happened.
  2. Address the `UniqueViolation` itself so the test's own assertion executes rather than merely
     failing cleanly. Prefer isolating this test's data so the widening migration has no
     epoch-distinct rows to trip on, over weakening the migration or the test.
  3. If you conclude the test's assertion cannot be made to run without a production change, STOP —
     do not change `app/db/schema.sql`. Report the finding; a leaked constraint fixed and an
     assertion left failing is still a net improvement worth landing separately.
  </steps>

  <verification>
    <automated>uv run pytest tests/ -m queueproof -v -rs</automated>
    <expected>
    Full marker selection, not a single file — the victims only fail in company. Expect green with
    an empty skip report. Compare against the pre-plan baseline of 13 failed / 50 passed.
    </expected>
  </verification>

  <verification>
    <automated>uv run pytest tests/ -m queueproof -q</automated>
    <expected>
    Teardown proof (truth #2), run now and not deferred: force an exception inside the mutating
    test's body, re-run the full selection, and confirm the six downstream tests STILL pass — the
    leak is structurally gone, not merely absent today. Revert the forced exception and record the
    result in SUMMARY.md. This is the single most important check in the plan; a green run without
    it proves only that today's happy path is clean.
    </expected>
  </verification>

  <commit>fix(21-13): restore the modern CHECK unconditionally so schema repair cannot leak</commit>
</task>

</tasks>

<verification>
- `ALLOW_DB_RESET=1 uv run pytest tests/ -m queueproof -v -rs` is green with an empty skip report.
- The forced-exception teardown proof passed: six downstream tests survive a raising poisoner.
- The `worker.start()` guard still reds on a genuine violation.
- `uv run pytest tests/ -m "not integration and not live_llm"` is green.
- `git status --porcelain app/` is empty.
</verification>

<threat_model>
| ID | Threat | Severity | Response | Mitigation |
|----|--------|----------|----------|------------|
| T-21-13-01 | Spoofing | A skipped live-DB test reading as a pass | high | mitigate | `-rs` inspected on every run; a SKIPPED line is defined as a failed criterion; DATABASE_URL supplied explicitly in `<environment>`. |
| T-21-13-02 | Tampering | B4 "fixed" by weakening or deleting the AST guard | high | mitigate | Truth #3 requires a demonstrated red on a genuine violation before the fix is accepted. |
| T-21-13-03 | Tampering | B1 hollows out assertions — tests pass but prove less | high | mitigate | Task 2 step 2 requires confirming each assertion is still meaningful; task 2's mutation proves the authorize call is load-bearing. |
| T-21-13-04 | Repudiation | B3 appears fixed because today's happy path is clean, while the leak remains on the raising path | high | mitigate | The forced-exception teardown proof in task 3 — the plan's designated most-important check. |
| T-21-13-05 | Tampering | Scope creep into `app/` to make a test pass | high | mitigate | Named as forbidden; `git status --porcelain app/` empty is an acceptance criterion; task 3 step 3 defines the stop condition. |
| T-21-13-06 | Spoofing | Single-file runs hide the six order-dependent failures | medium | mitigate | Every acceptance check specifies the full `tests/ -m queueproof` selection. |
</threat_model>
