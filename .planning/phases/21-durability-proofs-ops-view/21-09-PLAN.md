---
phase: 21-durability-proofs-ops-view
plan: 09
type: execute
wave: 4
depends_on: ["21-01", "21-03", "21-04", "21-05", "21-08"]
files_modified:
  - .github/workflows/concurrency-proof.yml
  - tests/test_proof_inventory.py
  - tests/test_queue_config.py
autonomous: true
requirements: [PROOF-05]
must_haves:
  truths:
    - "Each of PROOF-01..04 is registered exactly once and is collected by CI's real-Postgres job (PROOF-05)."
    - "A typo'd marker id on one proof reds the build, even while every other proof still passes — the gap concurrency-proof.yml documents in its own comment and does not close (D-02)."
    - "The gate verifies the selection CI ACTUALLY RUNS (`queueproof and proof(id=...)`), so a proof with a valid id but a missing queueproof marker cannot pass."
    - "The completeness gate was proven to red against a live typo AND against a live missing `queueproof` marker, not only against synthetic inputs."
    - "The existing by-name integration step and the marker-selected queueproof step are additively unchanged, and are now pinned by a STRUCTURAL FINGERPRINT (name, command, shell, marker, env, skip/pass guards) — the prior tests only checked a command substring."
  artifacts:
    - ".github/workflows/concurrency-proof.yml — a third step running the completeness gate"
    - "tests/test_proof_inventory.py — the live-repository no-false-positive assertion"
    - "tests/test_queue_config.py — TestD02CollectGateStep pinning the new step and re-pinning the untouched ones"
  key_links:
    - "scripts/check_proof_inventory.py's main() ⇄ the new CI step — the selection-layer gate the execution-layer 'N passed' guard cannot provide."
---

<objective>
Wire D-02's completeness gate into CI and prove it non-vacuous against the live repository.

Purpose: PROOF-05's premise as written in REQUIREMENTS is stale. `concurrency-proof.yml` no longer
hard-codes the durability proofs by filename — Phase 16's D-14 already added a marker-selected
`-m queueproof` step that collects any marked test anywhere under `tests/` with zero workflow
edits. What remains is the residual gap the workflow names verbatim in its own comment: *it cannot
detect a typo'd marker on one newly-added test while the other queueproof tests still pass — the
log still says "N passed" and the new proof silently never ran. Nothing in CI closes that.* D-02
closes exactly that, at the selection layer, which is where the gap lives.

This plan runs in wave 4 by necessity: the gate asserts all four PROOF ids exist, so wiring it
before plans 21-03/04/05/08 had tagged them would have redded CI for the rest of the phase.

Output: a third CI step, the live-repository assertion, workflow pins, and an executed proof that
the gate reds on a real typo.

**Corrections applied from cross-AI review (Codex, HIGH ×2 — both independently verified against
live source).**

1. **"Byte-pinned" was false, and the claim had propagated through three artifacts unchallenged.**
   An earlier draft of this plan (and CONTEXT.md, and the planning summary) described the existing
   `concurrency-proof.yml` steps as byte-pinned by `TestD14NoWideningGuard`. They are not.
   `tests/test_queue_config.py:110-135` asserts exactly two things: that one **command substring**
   is present, and that whole-suite `-m integration` is absent. Shell, `set -o pipefail`, `env`,
   the skip/pass log guards, and every comment are **unpinned** — an edit to any of them passes
   today. The workflow's own comment claims the step "stays byte-identical"; the test does not
   enforce that. This plan now says what the guard actually checks and **adds the structural
   fingerprint** that makes the additive-only requirement real.
2. **This plan inherited 21-01's `proof`-vs-`queueproof` blind spot.** CI executes `-m queueproof`;
   a correctly-inventoried proof missing that marker would never run. Plan 21-01 now collects the
   intersection; this plan's live-repository assertion and its executed falsification must exercise
   that same intersection, and must include a **missing-`queueproof`** falsification rather than
   only a typo'd `id=`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-durability-proofs-ops-view/21-CONTEXT.md
@.planning/phases/21-durability-proofs-ops-view/21-PATTERNS.md
@.planning/phases/21-durability-proofs-ops-view/21-01-SUMMARY.md
@.planning/phases/21-durability-proofs-ops-view/21-03-SUMMARY.md
@.planning/phases/21-durability-proofs-ops-view/21-08-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the completeness gate step to concurrency-proof.yml</name>
  <files>.github/workflows/concurrency-proof.yml, tests/test_queue_config.py</files>
  <read_first>
    - `.github/workflows/concurrency-proof.yml` — the whole file, comments included. Two things
      constrain this edit absolutely: `tests/test_queue_config.py` pins the first step's file
      selection and forbids whole-suite collection, and the second step's comment names the gap you
      are closing. Your step is an **addition**; touch neither existing step.
    - `tests/test_queue_config.py` — `TestD14NoWideningGuard` and `TestQueueproofMarkerRegistered`
      in full, plus the module-level `_WORKFLOW_YML` path constant your new class reuses. **Read
      `TestD14NoWideningGuard`'s body carefully and do not repeat the "byte-pinned" description of
      it.** It asserts one command substring is present and that whole-suite `-m integration` is
      absent — nothing more. Shell, `pipefail`, `env`, the skip/pass log guards and all comments are
      unpinned by it. The workflow comment's "stays byte-identical" is an aspiration the test does
      not enforce; Task 1 closes that gap.
    - `.planning/phases/21-durability-proofs-ops-view/21-01-SUMMARY.md` — the checker's module path
      and `main()` contract.
  </read_first>
  <action>
    Append a third step to `concurrency-proof.yml`'s job, after the existing queueproof step. Name
    it for what it establishes: that every durability proof is registered exactly once.

    Its `run` block follows the file's established guard idiom — `shell: bash` and
    `set -o pipefail` with the output teed to a log — and invokes the checker from plan 21-01
    through `uv run`, so a non-zero exit reds the step. Do not reimplement the counting logic in
    bash: the decision function is already red-proofed hermetically, and a second implementation in
    shell would be an unproven guard sitting next to a proven one.

    Add a comment block in the file's established style stating: what this step closes (the
    selection-layer typo gap the step above names and does not close); why it lives at the
    selection layer rather than the execution layer (an execution-layer "N passed" check cannot see
    a proof that was never selected); and why the id is a marker keyword argument rather than a
    naming convention (it survives a rename and a file move, and it is selectable by a marker
    expression, which a positional argument is not).

    Then add a `TestD02CollectGateStep` class to `tests/test_queue_config.py`. Parse the workflow
    with the YAML loader that module already uses and assert structurally: a step exists whose
    `run` invokes the inventory checker; that step appears **after** the existing queueproof step;
    and the job now has exactly three steps that run pytest-or-checker commands, so a future edit
    that deletes one is caught. Also assert the **no-false-positive half of the surrounding pins**:
    re-run the existing `TestD14NoWideningGuard` assertions as part of your verification and
    confirm they still pass unchanged — a new step that accidentally satisfied the forbidden
    whole-suite pattern would otherwise slip through.

    Then add a second new class, `TestPreExistingStepFingerprints`, closing the gap the review
    exposed. `TestD14NoWideningGuard` checks a command substring and an absence; it would not catch
    an edit that dropped `set -o pipefail`, changed `shell:`, altered the `env:` block, or removed
    either the `skipped`-reds or `passed`-required log guard from the two pre-existing steps. Since
    this plan's whole premise is that those steps stay untouched, that premise has to be *enforced*
    rather than believed.

    For **each** of the two pre-existing steps, capture a structural fingerprint from the parsed
    YAML and pin it: the step `name`, its `shell`, the presence of `set -o pipefail` in its `run`,
    its selection arguments (the two named files with `-m integration` for the first step; `-m
    queueproof` for the second), its `env` keys, and the presence of **both** log guards (the
    `skipped` check that reds and the `passed` check that reds when absent). Assert on the parsed
    structure, not on a slice of raw file text. Give each assertion a failure message stating that
    the pre-existing steps are additive-only territory and that a change here means the new step was
    not additive after all.

    Deliberately do **not** hash the whole step: a hash reds on a comment reflow and gets deleted
    for being annoying, which is how a guard dies. Pin the load-bearing fields by name so the guard
    survives formatting changes and still catches semantic ones. Record that reasoning in the class
    docstring.

    Do not modify `TestD14NoWideningGuard`, `TestQueueproofMarkerRegistered`, or any existing
    assertion in that file. Correct any prose in this repo's new artifacts that describes
    `TestD14NoWideningGuard` as "byte-pinning" — describe what it actually checks.
  </action>
  <verify>
    <automated>uv run pytest tests/test_queue_config.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_queue_config.py -v` passes, showing `TestD14NoWideningGuard`,
      `TestQueueproofMarkerRegistered`, `TestProofMarkerRegistered`, `TestD02CollectGateStep` and
      `TestPreExistingStepFingerprints` all green — the pre-existing pins are the no-false-positive
      half and must not have been edited to accommodate the new step.
    - `TestPreExistingStepFingerprints` pins, for **both** pre-existing steps: `name`, `shell`, the
      presence of `set -o pipefail`, the selection arguments, the `env` keys, and both log guards.
      Prove each fingerprint assertion is reachable: temporarily drop `set -o pipefail` from one
      step, observe the red, restore; then temporarily remove one log guard, observe the red,
      restore. A fingerprint never seen failing is not a pin.
    - No artifact produced by this plan describes `TestD14NoWideningGuard` as "byte-pinning" the
      workflow. It checks a command substring and one absence; saying more than that is the
      unverified-belief failure this phase exists to eliminate.
    - `git diff .github/workflows/concurrency-proof.yml` shows **only** added lines; no line inside
      either pre-existing step is modified or removed.
    - `uv run python -c "import yaml,pathlib; s=yaml.safe_load(pathlib.Path('.github/workflows/concurrency-proof.yml').read_text()); st=list(s['jobs'].values())[0]['steps']; print(len(st), st[-1]['name'])"` prints the new step count and the new step's name as the last step.
    - The workflow still parses as valid YAML.
    - `git diff tests/test_queue_config.py` shows only additions.
  </acceptance_criteria>
  <done>The completeness gate runs in the one CI job with a real Postgres, added without disturbing either pre-existing step — and those steps are now genuinely pinned by a structural fingerprint rather than by a command substring.</done>
</task>

<task type="auto">
  <name>Task 2: Assert the gate is clean on the live repository, then prove it reds on a real typo</name>
  <files>tests/test_proof_inventory.py</files>
  <read_first>
    - `tests/test_proof_inventory.py` — the synthetic red-proof tests plan 21-01 landed. This task
      adds the other half: the live-repository assertion.
    - `scripts/check_proof_inventory.py` — `collect_inventory` and `main`, and how `collect_inventory`
      treats pytest's no-tests-collected exit code as a legitimate empty result.
    - Each of the four proof SUMMARYs (21-03, 21-04, 21-05, 21-08) — so you can assert the expected
      node ids, not merely the counts.
  </read_first>
  <action>
    Add a live-repository test to `tests/test_proof_inventory.py` that calls `collect_inventory` and
    `evaluate_inventory` against the real repository and asserts there are no violations. This is
    the **no-false-positive half** the guard-non-vacuity rule requires: plan 21-01 proved the
    detector reds on synthetic bad inputs; this proves it does not red on the conforming real
    thing. Assert additionally that each expected id maps to the specific node id recorded in that
    proof's SUMMARY, so a proof silently migrating to a different test also reds.

    Add a second live-repository assertion covering the review's HIGH finding: **the bare `proof`
    selection and the bare `queueproof` selection agree on the four proof node ids.** Collect both
    and assert every `proof`-marked node id is also present in the `queueproof` set. This is the
    property that makes the inventory mean what it appears to mean — without it, the gate certifies
    that a proof *exists* while CI never *runs* it.

    This test runs pytest collection in a subprocess. It needs no database — none of the proof
    modules skips at import time, so collection succeeds without `DATABASE_URL`; confirm this
    yourself by running it with the variable unset. Mark it so it does not run inside its own
    subprocess recursively if that turns out to be a problem, and keep it out of the `proof` and
    `queueproof` marker selections — it is a guard, not a durability proof, and tagging it would
    corrupt the very inventory it checks.

    Then **execute the gate's own falsification**, live, in this session — the same discipline
    every proof in this phase obeys, applied to the guard itself (D-05, D-06's spirit):

    Run **two** falsifications, not one. They exercise different failure shapes, and the second is
    the one the review found missing — an id-only gate passes it while CI never runs the proof.

    **Falsification 1 — a typo'd marker id.**
    1. Establish the green baseline: run `uv run python -m scripts.check_proof_inventory` and
       confirm it exits 0 with no violations printed.
    2. Introduce a realistic typo on exactly one proof's marker id — change one character of the
       id argument on one of the four tagged tests, leaving the other three untouched and leaving
       that test otherwise fully functional. This reproduces the documented gap precisely: the
       other proofs still pass and an execution-layer log would still say "N passed".
    3. Run the checker again. It must exit non-zero and print two violations: the expected id that
       is now missing, and the stray node id carrying an unrecognised id. Capture the full output.
    4. Revert byte-identically. Confirm with `git diff` that the test file shows no change at all,
       and that the checker exits 0 again.

    **Falsification 2 — a missing `queueproof` marker.** This is the shape that motivated the
    intersection selection, and it must be observed failing rather than argued about.
    1. Confirm the green baseline again.
    2. On exactly one of the four tagged proofs, remove **only** its `queueproof` marker, leaving
       its `proof(id=...)` marker correct and intact. If that proof takes `queueproof` from the
       module-level `pytestmark`, achieve the same effect for that single test in whatever way the
       module's marker structure allows, and record precisely what you did — the point is a test
       that is still a correctly-identified proof and is no longer selected by CI.
    3. Run the checker. It must exit non-zero, and the violation must **name `queueproof`** and the
       affected node id — a violation that merely says "missing id" would be describing the wrong
       defect and would send the next reader to the wrong fix. Capture the full output.
    4. Revert byte-identically; confirm `git diff` is empty and the checker exits 0.

    Record both pasted red outputs, both diffs, the commit SHA and the byte-identical-revert
    confirmations in the SUMMARY, exactly as the four proofs record theirs. A guard that has never
    been observed failing is not a guard — and a guard observed failing on only one of the two
    shapes it claims to catch is a guard for one shape.
  </action>
  <verify>
    <automated>uv run pytest tests/test_proof_inventory.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_proof_inventory.py -v` passes, including both the synthetic tests
      from plan 21-01 and the new live-repository test.
    - `uv run python -m scripts.check_proof_inventory` exits 0 and prints no violations.
    - `uv run pytest tests/ -m proof --collect-only -q` lists exactly four node ids, one per proof.
    - For each of PROOF-01..04, `uv run pytest tests/ -m "queueproof and proof(id='PROOF-0N')"
      --collect-only -q` lists exactly one node id, and it matches the node id recorded in that
      proof's SUMMARY. The **intersection** is the selection asserted, because it is the selection
      CI runs.
    - `uv run pytest tests/ -m proof --collect-only -q` and `uv run pytest tests/ -m queueproof
      --collect-only -q` agree on all four proof node ids — every `proof`-marked node appears in the
      `queueproof` selection.
    - The typo run's output is pasted in full into the SUMMARY and shows a non-zero exit with both
      the missing-id violation and the stray-node-id violation named.
    - **The missing-`queueproof` run's output is pasted in full into the SUMMARY**, shows a non-zero
      exit, and its violation text names `queueproof` and the affected node id. A gate that reds
      here only with a generic "missing id" message is a FAILED criterion: it would point the next
      reader at the wrong defect.
    - After both reverts, `git diff --stat` shows no change to any test file, and the checker
      exits 0.
    - The live-repository test passes with `DATABASE_URL` unset — proving the gate is a
      collection-layer check that does not silently depend on a database.
  </acceptance_criteria>
  <done>The completeness gate is clean on the real repository and has been observed reddening against a real single-character typo — both halves proven, not one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer edit → CI gate | A future edit can disarm the gate, retag a proof, or delete a step. |
| CI runner → repository source | The gate executes repository code under pytest collection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-09-01 | Repudiation | The completeness gate itself | high | mitigate | Both halves proven: synthetic red-proofs (plan 21-01) plus **two** live falsifications executed and reverted here — a single-character id typo and a removed `queueproof` marker. A guard never observed failing is the documented `guard-scope-is-invisible-from-green` class this repo has shipped four times. |
| T-21-09-02 | Tampering | The two pre-existing workflow steps | high | mitigate | The new step is additive only; `git diff` must show added lines exclusively. `TestD14NoWideningGuard` is retained but is **not** a byte-pin — it checks one command substring and one absence. `TestPreExistingStepFingerprints` adds the real enforcement: name, shell, `pipefail`, selection args, `env` keys and both log guards, for both steps, each proven reachable. |
| T-21-09-06 | Repudiation | Gate certifying a proof CI never runs | high | mitigate | `proof` and `queueproof` are independent markers and CI executes `-m queueproof`. The gate collects the intersection, the live-repository test asserts the two selections agree on all four node ids, and the missing-`queueproof` falsification is executed and pasted. Without this the gate would certify presence while the proof silently never ran. |
| T-21-09-03 | Tampering | Gate disarmament (`|| true`, dropped pipefail, deleted step) | high | mitigate | `TestD02CollectGateStep` asserts structurally over parsed YAML that the step exists, runs the checker, and sits after the queueproof step, with an assertion on the total count of proof-running steps so a deletion reds. |
| T-21-09-04 | Spoofing | A proof silently migrating to a different test | medium | mitigate | The live-repository test asserts each id maps to the specific node id recorded in that proof's SUMMARY, not merely that some test carries the id. |
| T-21-09-05 | Denial of Service | Gate runtime in CI | low | accept | Five short `--collect-only` invocations with no database and no network, inside a job that already runs the full live-DB proof suite. |
| T-21-09-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No packages installed.** RESEARCH.md Finding 0. No `[ASSUMED]`/`[SUS]` package, therefore no legitimacy checkpoint required. |
</threat_model>

## Artifacts this phase produces (from this plan)

| Symbol | Kind | Location |
|--------|------|----------|
| proof-completeness CI step | CI step (third in the job) | `.github/workflows/concurrency-proof.yml` |
| `TestD02CollectGateStep` | test class | `tests/test_queue_config.py` |
| `TestPreExistingStepFingerprints` | test class (structural pin for both pre-existing steps) | `tests/test_queue_config.py` |
| live-repository inventory assertion (intersection + selection agreement) | tests | `tests/test_proof_inventory.py` |
| gate typo-falsification record | evidence (diff + red + SHA + revert proof) | `21-09-SUMMARY.md` |
| gate missing-`queueproof`-falsification record | evidence (diff + red + SHA + revert proof) | `21-09-SUMMARY.md` |

<verification>
- `uv run pytest tests/test_queue_config.py tests/test_proof_inventory.py -v` is green.
- `uv run python -m scripts.check_proof_inventory` exits 0.
- `uv run pytest tests/ -m proof --collect-only -q` lists exactly four node ids.
- With `DATABASE_URL` reachable: `ALLOW_DB_RESET=1 uv run pytest tests/ -m queueproof -v -rs` is
  green with an empty skip report.
- `git status --porcelain tests/test_queue_durability.py tests/test_webhook_dedup_race.py tests/test_send_idempotency.py` is empty at plan end.
</verification>

<success_criteria>
- All four proofs are registered exactly once **and are selected by `queueproof and proof(id=...)`**
  — the selection the real-Postgres CI job actually executes, not merely the `proof` marker set.
- The gate runs in CI as an addition, with both pre-existing steps unchanged, their existing pins
  passing unmodified, and a new structural fingerprint enforcing the additive-only requirement that
  the prior substring check could not.
- The gate has been observed reddening against **both** a real id typo and a real missing
  `queueproof` marker, and returning to green after byte-identical reverts.
- No artifact of this phase repeats the false "byte-pinned" description of `TestD14NoWideningGuard`.
</success_criteria>

<output>
Create `.planning/phases/21-durability-proofs-ops-view/21-09-SUMMARY.md` when done. Record the four
node ids the inventory resolved, the new CI step's name, and the pasted typo-falsification output.
</output>
