---
phase: 08-data-layer-hygiene-diagnostics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/schema.sql
  - app/models/status.py
  - tests/test_status_drift.py
  - tests/test_models_contracts.py
autonomous: true
requirements: [OPS2-01, OPS2-02]

must_haves:
  truths:
    - "schema.sql declares a nullable error_detail TEXT column on payroll_runs, additive via ADD COLUMN IF NOT EXISTS"
    - "schema.sql declares the 4 OPS2-02 hot-path index/coverage facts: a new composite index on email_messages(run_id, direction, send_state), a new index on payroll_runs(created_at DESC), a new index on payroll_runs(status), and businesses.contact_email remains covered by its existing NOT NULL UNIQUE constraint (no duplicate index created)"
    - "RunStatus no longer has a NEEDS_CLARIFICATION member; the payroll_runs.status CHECK (both inline CREATE TABLE and the DO-block DROP+ADD migration for existing tables) no longer allows 'needs_clarification'"
    - "The dual-sourced enum drift guard (test_status_drift.py) stays green against the reduced 10-value status set, AND a dedicated guard separately parses the DO-block's re-add value list (not just the inline CREATE TABLE CHECK) so a stale DO-block value list cannot silently pass"
    - "The string 'needs_clarification' does not appear anywhere in schema.sql (zero occurrences file-wide, not just in the inline CHECK)"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "error_detail column, 3 new CREATE INDEX IF NOT EXISTS statements, contact_email coverage comment, payroll_runs status CHECK swap (10 values, DO-block pattern), updated '10 values' comment"
    - path: "app/models/status.py"
      provides: "RunStatus enum with NEEDS_CLARIFICATION removed (10 members)"
    - path: "tests/test_status_drift.py"
      provides: "index static guard (new test class/methods) + DO-block value-list guard (new, separate from the inline-CHECK guard) + updated status-count assertion (11 -> 10) + file-wide needs_clarification-absence assertion"
  key_links:
    - from: "app/models/status.py"
      to: "app/db/schema.sql"
      via: "dual-sourced enum drift guard"
      pattern: "CHECK\\s*\\(\\s*status\\s+IN"
    - from: "app/db/schema.sql DO-block re-add value list"
      to: "app/models/status.py RunStatus"
      via: "dedicated DO-block parser in test_status_drift.py (separate from the inline-CHECK parser, which only ever sees the FIRST CHECK match in the file)"
      pattern: "payroll_runs_status_check"
---

<objective>
Land the Phase 8 schema baseline: the additive `error_detail` column (OPS2-01 storage target), the first three `CREATE INDEX IF NOT EXISTS` statements the project has ever declared (OPS2-02), the `businesses.contact_email` coverage decision (verify, don't duplicate — D-8-09), the `payroll_runs.status` CHECK swap that removes the dead `needs_clarification` value (folded todo 260623-06), and the matching `RunStatus` enum edit. All hermetic/static — no live-DB write happens in this plan; the live apply is a separate blocking checkpoint in 08-03.

Purpose: this is the clean, low-risk data-layer baseline Phase 9's transaction surgery builds on. Landing the DDL and the Python-side enum change together (and proving the drift guard stays green — including against the DO-block re-add, not just the inline CREATE TABLE CHECK, per the codex review finding below) means Plan 08-02 (repo.py logic) and Plan 08-03 (wiring + live checkpoint) never have to touch schema.sql or status.py again.

Review fix folded in (codex review, MEDIUM #5 + LOW #6): `tests/test_status_drift.py`'s existing `_extract_check_in_values` regex finds only the FIRST `CHECK (status IN (...))` match in the file — that's the inline `CREATE TABLE` CHECK, not the new `DO $$` re-add block below it. A stale value list inside the DO-block (e.g. someone edits the inline CHECK but forgets the DO-block's literal re-add list, or vice versa) would silently slip through the existing guard. This plan adds a SEPARATE, dedicated parser/assertion for the DO-block's re-add value list, plus a file-wide `needs_clarification`-absence assertion, so both CHECK definitions (not just the first one found) are drift-guarded. Also folds in the stale "11 values" schema.sql comment (LOW #6).

Output: `app/db/schema.sql` with the new column, 3 new indexes, the swapped CHECK (both inline and DO-block, with a corrected "10 values" comment); `app/models/status.py` with a 10-member `RunStatus`; `tests/test_status_drift.py` extended with an index static guard, a DO-block-specific value-list guard, a file-wide needs_clarification-absence guard, and updated status-count assertions; `tests/test_models_contracts.py` updated to the 10-value expected set.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-CONTEXT.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-PATTERNS.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-REVIEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: schema.sql — error_detail column + 3 hot-path indexes + contact_email coverage comment</name>
  <files>app/db/schema.sql</files>
  <read_first>
    app/db/schema.sql (full file — the idempotent-column-add block at lines 100-112 is the exact template for the new column; the `uq_email_run_purpose` DO-block at lines 201-216 and the deferred-FK DO-block at lines 246-258 are the header-comment-discipline template for the new index statements; `businesses.contact_email` declaration at line 19)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md (Pattern 3 — the traced predicate table justifying `(run_id, direction, send_state)` column order; D-8-09 in 08-CONTEXT.md for the contact_email non-duplication rule)
  </read_first>
  <action>
    In the `payroll_runs` idempotent-column-add block (schema.sql lines 100-112, immediately after the existing `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS clarified_fields JSONB;` line), add one new line: `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS error_detail TEXT;` with a trailing comment citing `-- D-8-01/D-8-02: PII-scrubbed, stage-prefixed, truncated exception detail`. Nullable, no DEFAULT — matches `error_reason`'s own declaration exactly (do not add a NOT NULL or DEFAULT clause). `error_reason` itself is NOT touched by this task — D-8-03 locks it as unchanged (still `type(exc).__name__` only); `error_detail` is purely additive alongside it.

    Immediately after the `email_messages` table's existing DO-blocks (after the `uq_email_run_purpose` block ending at line 216, before the `-- ── 6. eval_results` section header), add a new commented block declaring the first index for `email_messages`:
    ```
    CREATE INDEX IF NOT EXISTS idx_email_messages_run_direction_state
        ON email_messages (run_id, direction, send_state);
    ```
    Column order is `(run_id, direction, send_state)` — locked by D-8-09 and re-verified against live predicates in 08-RESEARCH.md Pattern 3 (do NOT use a different order). Header comment must cite D-8-09 and state the column order was traced against live query predicates in repo.py, not copied from the audit's guess, and must explicitly note `businesses.contact_email` is deliberately excluded because it is already covered by its `NOT NULL UNIQUE` constraint's implicit index.

    Immediately after the `payroll_runs` table's own idempotent-column-add block (after line 112, i.e. right after the new `error_detail` line you just added, before the `-- ── 4. paystub_line_items` section header), add two more index statements for `payroll_runs`:
    ```
    CREATE INDEX IF NOT EXISTS idx_payroll_runs_created_at
        ON payroll_runs (created_at DESC);
    CREATE INDEX IF NOT EXISTS idx_payroll_runs_status
        ON payroll_runs (status);
    ```
    These serve `load_all_runs`'s `ORDER BY created_at DESC` and `find_awaiting_reply_for_header`'s `pr.status = 'awaiting_reply'` filter (repo.py, confirmed in 08-RESEARCH.md).

    Do NOT add any index on `businesses.contact_email` — add a one-line comment directly below the `contact_email TEXT NOT NULL UNIQUE` column declaration (line 19) stating the UNIQUE constraint's implicit index already serves the plain-equality lookup in `find_business_by_sender` (repo.py), per D-8-09 — verify, don't duplicate.

    `CREATE INDEX IF NOT EXISTS` is directly idempotent (unlike named constraints) — no `DO $$` wrapper needed for any of the 3 new index statements, only a flat statement each, matching 08-PATTERNS.md's Pattern guidance.
  </action>
  <verify>
    <automated>grep -c "CREATE INDEX IF NOT EXISTS" app/db/schema.sql | grep -qx 3 && grep -q "idx_email_messages_run_direction_state" app/db/schema.sql && grep -q "idx_payroll_runs_created_at" app/db/schema.sql && grep -q "idx_payroll_runs_status" app/db/schema.sql && grep -q "ADD COLUMN IF NOT EXISTS error_detail" app/db/schema.sql && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CREATE INDEX IF NOT EXISTS" app/db/schema.sql` returns 3
    - `grep -A2 "idx_email_messages_run_direction_state" app/db/schema.sql` shows column order `(run_id, direction, send_state)` exactly
    - `grep "idx_payroll_runs_created_at" app/db/schema.sql` and `grep "idx_payroll_runs_status" app/db/schema.sql` both match
    - `grep "error_detail" app/db/schema.sql` matches exactly one `ALTER TABLE payroll_runs ADD COLUMN IF NOT EXISTS error_detail TEXT;` line (nullable, no DEFAULT, no NOT NULL)
    - `grep -A1 "contact_email" app/db/schema.sql | grep -i "unique"` still shows `NOT NULL UNIQUE` unchanged, plus a new comment nearby referencing D-8-09 / "verify, don't duplicate"
    - No `CREATE INDEX` statement on `businesses (contact_email)` exists anywhere in the file
  </acceptance_criteria>
  <done>schema.sql has exactly 3 new CREATE INDEX IF NOT EXISTS statements (email_messages composite + 2 payroll_runs), the new error_detail nullable TEXT column, and an explicit comment recording why contact_email gets no new index.</done>
</task>

<task type="auto">
  <name>Task 2: NEEDS_CLARIFICATION removal — status.py enum + schema.sql CHECK swap + drift-guard/contract test updates (incl. DO-block-specific guard, review fix #5)</name>
  <files>app/models/status.py, app/db/schema.sql, tests/test_status_drift.py, tests/test_models_contracts.py</files>
  <read_first>
    app/models/status.py (full file — the `RunStatus` enum, docstring lines 1 and 10)
    app/db/schema.sql (the inline `CHECK (status IN (...))` at lines 65-78 inside the `CREATE TABLE IF NOT EXISTS payroll_runs` block — NOTE the stale "The 11 values mirror RunStatus" comment at line 59, update to "10"; AND the `email_messages.purpose` DROP+ADD `DO $$` block at lines 174-199 — this is the EXACT template to adapt)
    tests/test_status_drift.py (full file — `_extract_check_in_values` at lines 34-65 — READ CAREFULLY: this regex-searches for the FIRST `CHECK (<column> IN (...))` match in the whole file via `re.search`, meaning it will keep matching the INLINE CREATE TABLE CHECK and NEVER see the new DO-block's re-add value list; `test_status_exact_count_is_eleven` at lines 118-131; `_DUAL_SOURCED_ENUMS` at lines 81-86; `TestEnumCheckDrift` class shape for the new tests)
    tests/test_models_contracts.py (lines 110-138 — `test_run_status_count` and `test_run_status_values`, the expected-values literal set to edit)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md (Pattern 4 — the adapted DO-block; the pre-migration guard note that belongs to the LIVE checkpoint in 08-03, not this hermetic task)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-REVIEWS.md (Codex 08-01 MEDIUM finding — the exact gap this task's new DO-block guard closes)
  </read_first>
  <action>
    In `app/models/status.py`: delete the `NEEDS_CLARIFICATION = "needs_clarification"` line (line 19) from the `RunStatus` enum. Update the module docstring's "11 pipeline status values" (line 1) to "10 pipeline status values", and the class docstring's "Eleven-state lifecycle" (line 10) to "Ten-state lifecycle". Do not reorder or rename any other member.

    In `app/db/schema.sql`: remove `'needs_clarification',` from the inline `CHECK (status IN (...))` list inside the `CREATE TABLE IF NOT EXISTS payroll_runs` block (lines 65-78) — a fresh bootstrap on an empty DB must create the table with the CHECK already correct, not rely on the DO-block below to self-correct it. Also update the nearby comment at line 59 ("The 11 values mirror RunStatus in app/models/status.py exactly") to say "10 values" (review fix LOW #6 — this is the exact stale comment codex flagged).

    Immediately after the `payroll_runs` idempotent-column-add block (i.e. after the 3 new index statements added in Task 1, still before `-- ── 4. paystub_line_items`), add a new `DO $$ ... END $$;` block adapted verbatim from the `email_messages.purpose` DROP+ADD pattern (schema.sql lines 174-199): narrow the `pg_constraint` lookup by `contype = 'c' AND conrelid = 'payroll_runs'::regclass AND conname LIKE '%status%'`, drop the found constraint via `EXECUTE 'ALTER TABLE payroll_runs DROP CONSTRAINT ' || quote_ident(_con_name)`, then re-add as `payroll_runs_status_check` with the CHECK value list: `'received','extracting','awaiting_reply','computed','awaiting_approval','approved','sent','reconciled','rejected','error'` (10 values, `needs_clarification` removed) — guarded by the same `IF NOT EXISTS (SELECT 1 FROM pg_constraint WHERE conname = 'payroll_runs_status_check' ...)` idempotency check. Header comment must cite the folded todo (260623-06) and the D-7.5-03a atomic DROP+ADD pattern it mirrors, and state explicitly this DO-block only fixes an EXISTING table (the inline CHECK edit above handles a fresh bootstrap).

    Per D-8-10 (static guard + live checkpoint — the live half is 08-03 Task 2), add a new index static-guard test class/methods to `tests/test_status_drift.py` mirroring `TestEnumCheckDrift`'s parse-and-assert shape: parse `schema.sql` for the 3 new `CREATE INDEX IF NOT EXISTS` statements from Task 1 and assert each is present with its exact column list, AND assert the `businesses.contact_email` column declaration still contains `NOT NULL UNIQUE` (the substitute proof for D-8-09's non-duplication decision) plus that `uq_email_run_purpose` is still present. Copy `test_no_db_connection_needed`'s AST-walk verbatim into (or reuse it for) the new test class so it is provably hermetic like its sibling.

    Review fix #5 (codex MEDIUM, HIGHEST-PRIORITY new test in this task): add a SEPARATE, dedicated test — e.g. `test_do_block_status_check_matches_enum` — that does NOT reuse `_extract_check_in_values` (which only ever finds the FIRST `CHECK (status IN (...))` match, i.e. the inline CREATE TABLE CHECK, and structurally cannot see the DO-block's re-add list). Instead, write a new small helper (e.g. `_extract_do_block_status_values(sql: str) -> set[str]`) that locates the `payroll_runs_status_check` DO-block specifically (e.g. search for the substring between `ADD CONSTRAINT payroll_runs_status_check` and the DO-block's closing `END;` / `$$;`, or match on `CHECK (status IN (...))` occurring AFTER the `payroll_runs_status_check` constraint-name literal in the file), extract its value list the same way (strip quotes/whitespace from the CSV), and assert set-equality against `{member.value for member in RunStatus}`. This proves the DO-block's re-add list is drift-guarded independently of the inline CHECK's own guard — a stale value in EITHER location now fails CI. Also add a second new assertion (same test or a sibling one-liner) that `"needs_clarification"` has ZERO occurrences anywhere in the raw `schema.sql` text (file-wide substring count, not scoped to any one CHECK) — this is the belt-and-suspenders proof the value is gone from every location, inline CHECK, DO-block, and any stray comment.

    In `tests/test_status_drift.py`: rename `test_status_exact_count_is_eleven` to `test_status_exact_count_is_ten` and change both `len(sql_values) == 11` and `len(enum_values) == 11` assertions (and their error-message literals) to `10`. The `_DUAL_SOURCED_ENUMS`-driven `test_check_values_match_python` test needs no code change — it will pass automatically once both sides drop the value from the INLINE CHECK, proving the drift guard actually re-verifies rather than being hardcoded (this existing test still only covers the inline CHECK — the new DO-block test above is what closes the remaining gap).

    In `tests/test_models_contracts.py`: remove `"needs_clarification",` from the `expected` set literal in `test_run_status_values` (around line 130) and update `test_run_status_count`'s docstring/assertion from `== 11` to `== 10` (both the assertion value and the `"(D-02 / D-03)"` docstring's numeral if it states "11").
  </action>
  <verify>
    <automated>uv run pytest tests/test_status_drift.py tests/test_models_contracts.py -q 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from app.models.status import RunStatus; assert len(RunStatus) == 10; assert not hasattr(RunStatus, 'NEEDS_CLARIFICATION')"` exits 0
    - `grep -c "needs_clarification" app/db/schema.sql` returns 0 (file-wide, zero occurrences — inline CHECK, DO-block re-add list, and any comment all clear)
    - `grep -c "payroll_runs_status_check" app/db/schema.sql` returns 2 (the `IF NOT EXISTS` guard reference + the `ADD CONSTRAINT` line)
    - `uv run pytest tests/test_status_drift.py -q` passes with all `TestEnumCheckDrift` tests green, including a test named `test_status_exact_count_is_ten` AND a test named `test_do_block_status_check_matches_enum` (or equivalent) that specifically parses the DO-block's re-add value list, independent of `_extract_check_in_values`
    - `uv run pytest tests/test_status_drift.py -k index -q` passes (D-8-10 static index guard: 3 CREATE INDEX statements + contact_email UNIQUE + uq_email_run_purpose coverage facts asserted)
    - `uv run pytest tests/test_models_contracts.py -k run_status -q` passes
    - `grep -n "11 values" app/db/schema.sql` returns no matches (stale comment corrected to "10 values")
  </acceptance_criteria>
  <done>RunStatus has exactly 10 members with NEEDS_CLARIFICATION gone; schema.sql's inline CHECK and the new DO-block CHECK swap both enumerate the same 10-value set; test_status_drift.py's new index static guard (D-8-10), the NEW dedicated DO-block value-list guard (review fix #5 — independent of the inline-CHECK parser), the file-wide needs_clarification-absence assertion, and test_models_contracts.py all pass; the stale "11 values" schema.sql comment is corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| schema.sql -> live Postgres (Supabase) | DDL applied by the idempotent bootstrap; this plan only edits the DDL source file — no live apply happens here (deferred to the 08-03 blocking checkpoint) |
| Python enum <-> SQL CHECK (BOTH the inline CREATE TABLE CHECK and the DO-block re-add) | Dual-sourced value set across TWO separate SQL locations; a drift here (one location updated, the other not) silently permits or rejects the wrong status values in a way the ORIGINAL single-location guard would not catch — closed by this plan's new DO-block-specific test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-03 | Tampering | schema.sql status CHECK swap (DO-block) | mitigate | Guard is `contype='c' AND conrelid='payroll_runs'::regclass AND conname LIKE '%status%'` (narrowed lookup, mirrors the proven email_messages.purpose pattern) so the DROP cannot accidentally target an unrelated constraint; the DROP+ADD is inside one `DO $$ ... END $$;` block so a failed ADD rolls back the DROP too (no half-migrated table) |
| T-8-04 | Information Disclosure | new indexes on `payroll_runs(status)` / `email_messages(...)` | accept | Indexes on status/direction/send_state columns carry no PII themselves (enum-like values); no new information-disclosure surface vs. the existing table data |
| T-8-05 | Denial of Service (schema drift) | dual-sourced RunStatus enum vs. schema.sql CHECK (inline AND DO-block) | mitigate | `tests/test_status_drift.py`'s `TestEnumCheckDrift` parametrized test re-derives the Python value set live via `{member.value for member in RunStatus}` (not hardcoded) and asserts set-equality against the parsed INLINE SQL CHECK; a NEW dedicated test (review fix #5) separately parses and asserts the DO-block's re-add value list against the same live Python set, closing the gap where the original single-parser guard could only ever see the first CHECK match in the file — a future one-sided edit to EITHER location fails CI immediately |
</threat_model>

<verification>
Run `uv run pytest tests/test_status_drift.py tests/test_models_contracts.py -q` after both tasks. Full suite (`uv run pytest -q`) must stay green (baseline: 492 passed, 36 skipped) since this plan changes no application logic that other tests execute — only schema.sql text and the enum member count.
</verification>

<success_criteria>
- `app/db/schema.sql` has the new `error_detail` column, 3 new `CREATE INDEX IF NOT EXISTS` statements with the researched column order, a contact_email non-duplication comment, a 10-value `payroll_runs.status` CHECK (both inline and via the new DO-block swap), and a corrected "10 values" comment (no stale "11 values" text remains).
- `app/models/status.py`'s `RunStatus` has exactly 10 members, `NEEDS_CLARIFICATION` removed.
- `tests/test_status_drift.py` asserts the 10-value set on BOTH the inline CHECK and the DO-block re-add list (two independent parsers), plus a file-wide `needs_clarification`-absence check; `tests/test_models_contracts.py` asserts the 10-value set. All pass.
- Full test suite green, no live-DB call made by this plan.
</success_criteria>

<output>
Create `.planning/phases/08-data-layer-hygiene-diagnostics/08-01-SUMMARY.md` when done
</output>
