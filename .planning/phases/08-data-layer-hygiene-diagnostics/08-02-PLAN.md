---
phase: 08-data-layer-hygiene-diagnostics
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/repo.py
  - tests/test_persistence.py
  - tests/test_dashboard.py
autonomous: true
requirements: [OPS2-01, OPS2-02]

must_haves:
  truths:
    - "A failed run's error_detail contains the scrubbed, stage-prefixed, truncated exception message and excludes seeded PII (roster names/aliases and email addresses)"
    - "A sensitive string straddling the 200-char truncation boundary is still fully redacted (scrub happens BEFORE truncate, never after)"
    - "The scrubber matches roster names case-insensitively and Unicode-normalization-insensitively (NFKC), replacing longest names/aliases first so a short alias cannot over-redact or leave a partial match unredacted"
    - "If the scrub step itself raises, or no roster is available, record_run_error still writes the pre-existing type-name-only error_reason and advances the run to ERROR — the diagnostics feature never blocks the error path it exists to observe"
    - "load_all_runs's SQL names its columns explicitly (no pr.* / no SELECT *) and computes summary_gate_reason + employee_count server-side with NULL-safe, non-array-safe aliases (a JSON scalar/null value in extracted_data->'employees' does not raise)"
  artifacts:
    - path: "app/db/repo.py"
      provides: "_scrub / _build_error_detail helpers (NFKC-normalized, case-insensitive, longest-first name matching), record_run_error(detail_exc=, stage=, roster=, conn=) signature with conn kept positional-compatible, rewritten load_all_runs SQL using a jsonb_typeof guard"
      contains: "_build_error_detail"
    - path: "tests/test_persistence.py"
      provides: "scrub-before-truncate boundary test (D-8-04a), fail-open test (D-8-04b), case/Unicode-normalization scrub test (review fix #4)"
    - path: "tests/test_dashboard.py"
      provides: "load_all_runs projection SQL-assertion test (no pr.*, explicit columns present) + non-array JSONB scalar edge-case test (review fix #2)"
  key_links:
    - from: "app/db/repo.py record_run_error"
      to: "app/db/repo.py _build_error_detail"
      via: "direct function call inside the same transaction, before the UPDATE"
      pattern: "_build_error_detail\\("
    - from: "app/db/repo.py load_all_runs"
      to: "payroll_runs.decision / payroll_runs.extracted_data"
      via: "SQL-computed aliases (jsonb_typeof-guarded), not Python post-processing"
      pattern: "summary_gate_reason|employee_count"
---

<objective>
Implement the OPS2-01 diagnostics core and the OPS2-02 `load_all_runs` projection fix entirely inside `app/db/repo.py` — the single funnel every error boundary and the runs-list route already goes through. Add a centralized, fail-open, scrub-before-truncate PII redaction helper and wire it into `record_run_error`'s existing transaction (no new query, no new transaction — one added `SET` clause on the existing `UPDATE`). Rewrite `load_all_runs` to select an explicit scalar column list plus two NULL-safe, non-array-scalar-safe SQL-computed aliases so no JSONB blob crosses the wire for the list view.

Purpose: this is the data-access layer half of Phase 8 — independent of the schema DDL (08-01, parallel) and independent of wiring the 3 call sites / templates (08-03, depends on this). All new logic is provable offline via `FakeConnection`; no live DB needed.

Review fixes folded in (codex review):
- **MEDIUM #2 (JSONB scalar edge case):** `COALESCE(jsonb_array_length(pr.extracted_data->'employees'), 0)` is NULL-safe but still ERRORS if `extracted_data->'employees'` is a JSON scalar or a JSON `null` literal (non-array) rather than SQL NULL — `jsonb_array_length` on a non-array JSON value raises a Postgres error, not a NULL. Fix: `CASE WHEN jsonb_typeof(pr.extracted_data->'employees') = 'array' THEN jsonb_array_length(pr.extracted_data->'employees') ELSE 0 END AS employee_count`. A corrupt/legacy row must not take down the entire runs list.
- **MEDIUM #4 (scrubber case/Unicode sensitivity):** the original design's exact `str.replace` is case-sensitive and Unicode-normalization-sensitive, so a roster name in different case (e.g. email client capitalizing "Maria Gonzalez" vs stored "maria gonzalez") or a different Unicode normalization form (combining vs precomposed accents) survives unredacted — a real project concern already established by MONEY-02. Fix: normalize (casefold + NFKC) both the message and each candidate name/alias before matching, replace longest-name-first (so a short alias like "Dave" doesn't fragment-redact inside a longer already-matched name and leave the surrounding text mangled), operating on the ORIGINAL (non-normalized) string via matched-span replacement so the stored `error_detail` keeps its original casing/characters outside the redacted spans.
- **LOW #8 (conn positional-compatibility):** keep `conn` positional-compatible in the new `record_run_error` signature — matches every existing call site's convention (`record_run_error(run_id, reason, conn=fake_conn)` used positionally today in some call/test sites) — the 3 NEW params (`detail_exc`, `stage`, `roster`) go keyword-only, placed AFTER `conn` in the signature, not before it.

Output: `app/db/repo.py` with `_scrub`, `_build_error_detail`, an extended `record_run_error` signature, and a rewritten `load_all_runs`; three new/extended tests in `tests/test_persistence.py` (scrub-ordering boundary case, fail-open case, case/Unicode-normalization case); two new tests in `tests/test_dashboard.py` (projection SQL assertion, non-array JSONB scalar edge case).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-CONTEXT.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-PATTERNS.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-REVIEWS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PII scrub helper (case/Unicode-normalized, longest-first) + record_run_error(conn, *, detail_exc=, stage=, roster=)</name>
  <files>app/db/repo.py, tests/test_persistence.py</files>
  <read_first>
    app/db/repo.py (lines 1-110 for the import block + `RUN_COLS`/`_TERMINAL_STATUSES` constants; lines 360-405 for the CURRENT full `record_run_error` body — this is what you extend in place)
    tests/test_persistence.py (lines 1-30 for imports/fixtures used; lines 134-196 for the existing `record_run_error` test suite style — `fake_conn.script_fetchone` + `repo.record_run_error(..., conn=fake_conn)` + `fake_conn.all_sql()` assertions)
    tests/conftest.py (lines 75-170 for `FakeConnection`/`fake_conn` fixture; lines 196-204 for the `roster_from_seed` fixture — a real `Roster` with real employee names/aliases)
    app/models/roster.py (lines 1-40 for the `Employee`/`Roster` shape — `full_name: str`, `known_aliases: list[str]`)
    app/email/clean.py (module-level `re.compile(...)` convention to match for the new regex)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-CONTEXT.md (D-8-01, D-8-01b, D-8-02, D-8-04 — the locked behavioral contract)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md (Pattern 1 code sketch + Pitfall 1/2/4 — the exact failure modes the tests must cover)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-REVIEWS.md (Codex 08-02 MEDIUM #2/#4 + LOW #8 — the exact gaps this task's helper design and signature close)
  </read_first>
  <behavior>
    - Test 1 (D-8-04, PII exclusion + inclusion): seed an exception message containing BOTH a roster employee's `full_name` (from `roster_from_seed`) and an email address (e.g. `"maria.gonzalez@acme.test"`). Call `record_run_error(run_id, "SomeExc", conn=fake_conn, detail_exc=<exc with that message>, stage="extract", roster=roster_from_seed)`. Assert the `error_detail` value bound into the `UPDATE` SQL params CONTAINS the surviving non-PII text and a `"[REDACTED]"` marker, and EXCLUDES both the raw email string and the raw employee full_name string.
    - Test 2 (D-8-04a, scrub-before-truncate boundary straddle): construct an exception message where a sensitive email address is positioned so that a naive truncate-first implementation would slice it in half at the 200-char mark (i.e. place the email string starting around character 190-199 of the composed `"{stage}: {message}"` string). Assert the stored `error_detail` contains NO partial/fragment of that email (neither the full string nor a truncated remnant) — i.e. scrubbing ran on the FULL message before the 200-char slice.
    - Test 3 (D-8-04b, fail-open — scrubber raises): pass a `roster` object (or monkeypatch `repo._scrub`) that raises inside the scrub step. Assert `record_run_error` still completes without raising, still writes the pre-existing `error_reason` value via `SET error_reason = %s` in the SQL, still advances the run to ERROR (`set_status` called / `SET status` present in `fake_conn.all_sql()`), and the bound `error_detail` param is `None` (the fallback).
    - Test 4 (D-8-04b, fail-open — no roster): call `record_run_error` with `roster=None` (no `roster=` kwarg at all, or explicit `None`). Assert it does not raise, `error_detail` is still populated with the regex-only-scrubbed message (email redacted, no roster-name redaction attempted), and no DB/roster-loading call occurs (no new `SELECT` beyond the existing terminal-status check appears in `fake_conn.all_sql()`).
    - Test 5 (review fix #4, case-insensitive + Unicode-normalized + longest-first): seed a roster employee whose `full_name` is e.g. `"Maria Gonzalez"` and construct an exception message containing (a) the SAME name in a DIFFERENT case (e.g. `"MARIA GONZALEZ"` or `"maria gonzalez"`), and (b) a NFD-decomposed Unicode variant of an accented name if the seed roster has one available (skip this sub-case gracefully — e.g. via a locally-constructed `Employee`/`Roster` object with an accented name if `roster_from_seed` has none — the key assertion is case-insensitivity at minimum, Unicode-normalization-insensitivity where a suitable accented name is constructible). Assert both variants are redacted to `"[REDACTED]"` in the stored `error_detail`. Also construct a case where one employee's alias is a strict substring of another employee's full name or a longer alias (e.g. alias `"Dave"` vs full_name `"Dave Reyes"`) both present in the message; assert the LONGER string is redacted as one span (not two overlapping/fragmented redactions that would leave stray characters from the shorter match's boundaries).
    - Test 6 (regression, existing 3 tests in test_persistence.py lines 139-195): `test_record_run_error_skips_terminal_run`, `test_record_run_error_processes_approved_run`, `test_record_run_error_writes_for_non_terminal_run` must still pass unmodified in behavior — calling `record_run_error` WITHOUT `detail_exc`/`stage`/`roster` (positional `run_id, reason` only, as today, and existing call sites using `conn=` as a keyword) must still work exactly as before (all new params are optional keyword-only with `None` defaults, and `conn` stays exactly where it is today in the signature — review fix #8).
  </behavior>
  <action>
    Add `import re` and `import unicodedata` to the stdlib import block in `app/db/repo.py` (alphabetical with `contextlib`, `json`, `logging`, `uuid`).

    Add two module-level helpers directly above `record_run_error` (after the `_TERMINAL_STATUSES` frozenset, before line 370):
    - `_EMAIL_RE = re.compile(r"[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}")` and `_REDACTED = "[REDACTED]"` as module constants.
    - A small normalization helper `_norm_for_match(s: str) -> str` returning `unicodedata.normalize("NFKC", s).casefold()` — used ONLY to build a case/Unicode-insensitive comparison key, never to mutate what gets written back into the message.
    - `_scrub(message: str, roster: Roster | None = None) -> str` — applies `_EMAIL_RE.sub(_REDACTED, message)` first. If `roster is not None`, build the full candidate name list across ALL employees (`employee.full_name` plus every entry in `known_aliases`, skipping falsy names), SORT it by length DESCENDING (review fix #4 — longest-first, so a short alias contained inside a longer name/alias is never independently matched first and does not fragment an already-redacted span), then for each candidate name in that order: build `_norm_for_match(name)` as the needle and repeatedly find case/Unicode-insensitive occurrences of that needle inside the CURRENT (partially redacted) message by normalizing the message with `_norm_for_match` for searching but slicing/replacing on the ORIGINAL message string using the match span's offsets (so casing/characters elsewhere in the message are preserved) — replace each found span with `_REDACTED`. Do this with a simple manual scan (normalized-haystack `str.find` loop tracking offsets) rather than a regex-per-name (names may contain regex metacharacters) — this keeps the helper dependency-free and matches the project's "no PII library" research conclusion. Returns the fully scrubbed string. NEVER queries the DB or calls any `repo.*`/`load_roster_for_business` function — `roster` is only ever the object the caller already has in memory (D-8-01b, non-negotiable).
    - `_build_error_detail(stage: str, exc: Exception, roster: Roster | None = None) -> str | None` — wraps the scrub-then-compose-then-truncate sequence in a `try/except Exception:  # noqa: BLE001 — diagnostics must never break diagnostics` that returns `None` on any failure. Happy path: `scrubbed = _scrub(str(exc), roster=roster)`, then `return f"{stage}: {scrubbed}"[:200]` — the stage prefix is composed BEFORE the 200-char slice (prefix eats into the budget, matching CONTEXT.md's example shape and RESEARCH.md's Open Question 2 resolution).

    Extend `record_run_error`'s signature to `record_run_error(run_id: uuid.UUID, reason: str, conn=None, *, detail_exc: Exception | None = None, stage: str | None = None, roster: Roster | None = None) -> None` — review fix #8: `conn` STAYS in its current positional slot (matches every existing call site's convention, some of which pass `conn=` as a keyword today but rely on it being early in the signature / positionally compatible); the 3 NEW params are keyword-only and placed AFTER `conn`, not before it. Keep the existing docstring content and append a short paragraph documenting the new params citing D-8-01/D-8-01b/D-8-02. Inside the function, AFTER the existing `_TERMINAL_STATUSES` early-return (do not move or weaken that guard — it must still run first and return before any detail-building work), compute `detail = _build_error_detail(stage, detail_exc, roster=roster) if (detail_exc is not None and stage is not None) else None`. Change the `UPDATE` statement to `"UPDATE payroll_runs SET error_reason = %s, error_detail = %s, updated_at = now() WHERE id = %s"` with params `(reason, detail, str(run_id))` — still ONE UPDATE inside the SAME transaction, no new query.
  </action>
  <verify>
    <automated>uv run pytest tests/test_persistence.py -k record_run_error -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_EMAIL_RE\|_build_error_detail\|def _scrub\|_norm_for_match" app/db/repo.py` shows all helpers defined above `record_run_error`
    - `grep -n "def record_run_error" app/db/repo.py` shows `conn=None` appearing BEFORE the `*,` keyword-only marker, and `detail_exc`/`stage`/`roster` appearing AFTER it
    - `uv run pytest tests/test_persistence.py -k "record_run_error or scrub or fail_open or straddle or normaliz" -q` passes, covering all 6 behaviors above
    - `uv run pytest tests/test_persistence.py -q` (full file) passes with zero regressions
  </acceptance_criteria>
  <done>record_run_error keeps conn positional-compatible and accepts optional keyword-only detail_exc/stage/roster after it; scrubs (email regex + case/Unicode-normalized, longest-first in-memory roster name matching) BEFORE truncating to 200 chars; fails open to the pre-existing type-name-only write on any scrub exception or missing roster; and all 6 new/existing test cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: load_all_runs explicit-column projection (D-8-07) with JSONB-scalar-safe employee_count (review fix #2)</name>
  <files>app/db/repo.py, tests/test_dashboard.py</files>
  <read_first>
    app/db/repo.py (lines 1088-1103 for the CURRENT `load_all_runs` body — `SELECT pr.*, b.name as business_name`; lines 1106-1118 for `load_roster_for_business`'s explicit-column-list + `dict_row` cursor convention to match)
    app/templates/runs_list.html (full file — confirms which fields the template actually renders: `run.created_at`, `run.business_name`, `run.status`, `run.id`, and the Summary cell currently reading `run.decision.gate_reasons[0]` / `run.extracted_data.employees | length`)
    tests/test_dashboard.py (lines 1-40 for the existing `test_runs_list_returns_200` pattern and `TestClient` setup; grep the file for any existing `load_all_runs` usage before adding a new test to avoid duplicating a helper)
    tests/conftest.py (lines 75-170 for `FakeConnection`/`fake_conn`; `script_fetchall` is the relevant scripting method for a list-returning query)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-CONTEXT.md (D-8-07, D-8-08 — the locked SQL shape and template-alias contract)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-REVIEWS.md (Codex 08-02 MEDIUM #2 — the exact JSONB-scalar edge case this task's SQL rewrite closes)
  </read_first>
  <behavior>
    - Test 1 (D-8-07, no `pr.*`, explicit columns + aliases): script a `fetchall` result on `fake_conn`, call `repo.load_all_runs(conn=fake_conn)`, then assert on `fake_conn.all_sql()` that the literal substring `"pr.*"` is ABSENT and that `"summary_gate_reason"` and `"employee_count"` ARE present, and that `"pr.id"`, `"pr.status"`, `"pr.created_at"` (or your final chosen scalar list) are present as named columns.
    - Test 2 (D-8-07, NULL-safety + review fix #2 non-array-scalar-safety): assert the SQL text contains `jsonb_typeof(` guarding the `employee_count` expression (NOT a bare `COALESCE(jsonb_array_length(...), 0)` — that form still errors on a non-array JSON scalar/`null` literal, only NULL-safe for SQL NULL, per the codex MEDIUM #2 finding). The exact required shape is `CASE WHEN jsonb_typeof(pr.extracted_data->'employees') = 'array' THEN jsonb_array_length(pr.extracted_data->'employees') ELSE 0 END AS employee_count` — assert this exact `CASE WHEN jsonb_typeof` pattern is present in the SQL text via `fake_conn.all_sql()`.
    - Test 3 (review fix #2, corrupt/non-array value does not raise — hermetic proxy): since `FakeConnection`/`FakeCursor` do not execute real SQL (they replay scripted rows), this test cannot execute the CASE expression against a live Postgres — instead, prove the SQL TEXT is scalar-safe (Test 2 above is the primary proof) AND add a second assertion: script a `fetchall` row where `employee_count` is scripted as `0` (simulating what the CASE expression would return for a non-array `extracted_data->'employees'` value) and assert `repo.load_all_runs(conn=fake_conn)` returns that row with `employee_count == 0` without raising — proving the Python-side return path tolerates the value the new SQL guarantees.
  </behavior>
  <action>
    Rewrite `load_all_runs`'s SQL (repo.py lines 1088-1103) to select an explicit scalar list plus two computed aliases, replacing `"SELECT pr.*, b.name as business_name"`. Verify the exact scalar column set against `app/templates/runs_list.html`'s actual field usage (read_first above) before finalizing — at minimum include `pr.id, pr.business_id, pr.status, pr.created_at, pr.updated_at, b.name AS business_name` (D-8-07's own discretion note: match whatever the template renders; `updated_at` stays for consistency with `RUN_COLS`'s established inclusion). This closes folded todo 260623-07 (load_all_runs SELECT *). Append the two computed aliases: `pr.decision->'gate_reasons'->>0 AS summary_gate_reason` (unchanged — the `->`/`->>` chain is already NULL-safe on a NULL `decision` column per RESEARCH.md Pitfall 3) and, per review fix #2, the JSONB-scalar-safe `employee_count` expression: `CASE WHEN jsonb_typeof(pr.extracted_data->'employees') = 'array' THEN jsonb_array_length(pr.extracted_data->'employees') ELSE 0 END AS employee_count` — this handles THREE cases correctly where a bare `COALESCE(jsonb_array_length(...), 0)` only handled one: (a) `extracted_data` is SQL NULL → `->'employees'` is NULL → `jsonb_typeof(NULL)` is NULL, not `'array'` → ELSE branch → `0`; (b) `extracted_data->'employees'` is a JSON `null` literal or absent key → `jsonb_typeof` is NULL or the type of the JSON null → ELSE branch → `0`; (c) `extracted_data->'employees'` is present but a non-array JSON scalar (e.g. a corrupt/legacy string or number) → `jsonb_typeof` returns that scalar's type string (`'string'`/`'number'`/etc.), never `'array'` → ELSE branch → `0`, where the OLD `jsonb_array_length` form would have raised a Postgres error and taken down the entire runs-list query for every run, not just the corrupt one. Keep the existing `JOIN businesses b ON pr.business_id = b.id` and `ORDER BY pr.created_at DESC` unchanged. Keep the `dict_row` cursor + `_conn_ctx` pattern unchanged — this is a SQL-text-only change, no change to the function's control flow or return type (still `list[dict]`). Update the docstring to cite D-8-07 and the review-fix-#2 jsonb_typeof guard, stating no JSONB blob crosses the wire for the list view AND a corrupt/legacy non-array `employees` value degrades to `employee_count = 0` instead of erroring the whole list.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py -k load_all_runs -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SELECT pr\.\*" app/db/repo.py` returns no matches (the old wildcard is gone)
    - `grep -n "summary_gate_reason\|employee_count" app/db/repo.py` shows both aliases in the `load_all_runs` SQL string
    - `grep -n "jsonb_typeof(pr.extracted_data->'employees')" app/db/repo.py` matches (review fix #2 — the CASE WHEN guard, not a bare COALESCE)
    - `grep -n "COALESCE(jsonb_array_length" app/db/repo.py` returns NO matches (the old, insufficiently-safe form must be fully replaced, not left alongside the new CASE expression)
    - `uv run pytest tests/test_dashboard.py -k load_all_runs -q` passes
    - `uv run pytest tests/test_dashboard.py -q` (full file) passes with zero regressions
  </acceptance_criteria>
  <done>load_all_runs's SQL names its columns explicitly with no pr.* / SELECT *, computes summary_gate_reason and employee_count server-side with a jsonb_typeof-guarded CASE expression (safe against SQL NULL, JSON null, AND non-array JSON scalars — closing the codex MEDIUM #2 gap the original COALESCE-only form left open), and the new projection tests plus the full test_dashboard.py file pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Exception message -> `error_detail` DB column -> dashboard render | The exception message can originate from LLM output, submitted client data, or raw email content — all untrusted from a PII standpoint by the time it reaches `str(exc)`; this is the boundary the scrubber sits on |
| `load_all_runs` JSONB columns -> dashboard list view | `decision`/`extracted_data` may grow new fields over time, AND may contain corrupt/legacy non-array values in `employees`; the explicit projection plus the jsonb_typeof-guarded CASE expression is the boundary preventing both silent schema-creep leakage AND a single corrupt row from erroring the entire list query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Information Disclosure | `_build_error_detail` / `record_run_error` | mitigate | Deterministic scrub-then-truncate (D-8-01): email regex + in-memory roster full_name/known_aliases substring redaction — now case-insensitive and NFKC-Unicode-normalized, longest-name-first (review fix #4) — runs on the FULL message before any truncation, closing the audit HIGH-05 gap; unit tests (D-8-04, D-8-04a, review-fix-#4 case/Unicode test) assert PII exclusion, the boundary-straddle ordering case, and the case/Unicode-normalization/longest-first matching case |
| T-8-02 | Denial of Service (soft — silently-hung run) | `_build_error_detail` internal failure | mitigate | Fail-open design (D-8-01b): any exception inside `_build_error_detail` is caught internally and returns `None`; the existing `error_reason` + `set_status(ERROR)` write path in `record_run_error` proceeds regardless, proven by the D-8-04b unit test |
| T-8-06 | Information Disclosure | `_scrub` querying the DB for roster data during a DB-outage error path | mitigate | `_scrub` takes `roster` as a plain in-memory parameter only — no DB call, no import of `repo.load_roster_for_business` inside `_scrub`/`_build_error_detail`; enforced by code review (no such call exists) and the D-8-04b "no roster" test asserting no new SQL appears |
| T-8-07 | Information Disclosure (schema creep) | `load_all_runs` | mitigate | Explicit scalar column list + 2 computed aliases (D-8-07) replaces `SELECT pr.*`; a new `payroll_runs` column can no longer silently reach the dashboard list view without an explicit, reviewed SQL edit |
| T-8-12 | Denial of Service | `load_all_runs`'s `employee_count` expression against a corrupt/legacy non-array `extracted_data->'employees'` value | mitigate | Review fix #2: `CASE WHEN jsonb_typeof(...) = 'array' THEN jsonb_array_length(...) ELSE 0 END` replaces the bare `COALESCE(jsonb_array_length(...), 0)`, which only guarded SQL-NULL input and still raised a Postgres error on a non-array JSON scalar/null literal — a single corrupt row could otherwise take down the ENTIRE runs-list query (every run, not just the corrupt one) |
</threat_model>

<verification>
Run `uv run pytest tests/test_persistence.py tests/test_dashboard.py -q` after both tasks. Full suite (`uv run pytest -q`) must stay green (baseline: 492 passed, 36 skipped plus the new tests added here).
</verification>

<success_criteria>
- `record_run_error` keeps `conn` positional-compatible and accepts `detail_exc=`/`stage=`/`roster=` (all optional, keyword-only, after `conn`), writes a scrubbed-then-truncated `error_detail` alongside the unchanged `error_reason`, and fails open on any scrub error or missing roster.
- The scrubber matches roster names/aliases case-insensitively and Unicode-normalization-insensitively (NFKC), replacing longest-first to avoid short-alias fragmentation.
- `load_all_runs` selects an explicit scalar column list with two NULL-safe, non-array-scalar-safe SQL-computed aliases (jsonb_typeof-guarded CASE, not a bare COALESCE); no `pr.*` / `SELECT *` remains.
- New tests (D-8-04, D-8-04a, D-8-04b, case/Unicode-normalization, projection assertion, JSONB-scalar-safety assertion) pass; existing `record_run_error`/`load_all_runs`-adjacent tests pass unmodified.
</success_criteria>

<output>
Create `.planning/phases/08-data-layer-hygiene-diagnostics/08-02-SUMMARY.md` when done
</output>
