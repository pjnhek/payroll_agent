---
phase: 08-data-layer-hygiene-diagnostics
plan: 03
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - app/pipeline/orchestrator.py
  - app/main.py
  - app/templates/run_detail.html
  - app/templates/runs_list.html
  - app/db/supabase.py
  - tests/conftest.py
  - tests/test_threading.py
autonomous: false
requirements: [OPS2-01, OPS2-02]
user_setup:
  - service: supabase
    why: "Live-DB migration checkpoint applies the new error_detail column, 3 indexes, and the payroll_runs.status CHECK swap to the real Supabase database"
    env_vars:
      - name: DATABASE_URL
        source: "already configured locally per Phase 2 setup; Supabase pooler connection string"

must_haves:
  truths:
    - "All 3 record_run_error call sites (run_pipeline catch-all, resume_pipeline catch-all, approve() delivery boundary) pass detail_exc=exc and a stage label; a real production failure now produces a non-NULL, PII-scrubbed error_detail"
    - "run_detail.html's error banner shows the existing byte-identical error_reason line, plus a new second line with error_detail ONLY when present; old NULL-detail runs render exactly as before"
    - "runs_list.html's Summary cell renders from the new summary_gate_reason / employee_count aliases, not from run.decision.gate_reasons / run.extracted_data.employees"
    - "The live Supabase schema has the error_detail column, all OPS2-02 hot-path indexes (constraint-backed or new), and the swapped status CHECK applied and verified via pg_indexes — confirmed by a human at a blocking checkpoint"
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "both record_run_error call sites enriched with detail_exc/stage/roster; roster scope guarded against UnboundLocalError; stale needs_clarification docstring updated"
    - path: "app/main.py"
      provides: "approve() delivery-boundary record_run_error call enriched with detail_exc/stage='delivery'"
    - path: "app/templates/run_detail.html"
      provides: "conditional second error-detail line in the error banner"
    - path: "app/templates/runs_list.html"
      provides: "Summary cell switched to summary_gate_reason / employee_count aliases"
  key_links:
    - from: "app/pipeline/orchestrator.py run_pipeline except block"
      to: "app/db/repo.py record_run_error"
      via: "detail_exc=exc, stage='pipeline', roster=None keyword call"
      pattern: "record_run_error\\(run_id, reason, detail_exc=exc"
    - from: "app/templates/run_detail.html"
      to: "payroll_runs.error_detail"
      via: "Jinja2 autoescaped {{ run.error_detail }} conditional render"
      pattern: "run\\.error_detail"
---

<objective>
Wire the OPS2-01 diagnostics helper (built in 08-02) into all 3 production error boundaries, surface `error_detail` on the run-detail dashboard, switch the runs-list Summary cell to the new SQL-computed aliases (08-02's `load_all_runs` rewrite), close the remaining hygiene todos scoped to this phase (WR-02 pool-singleton lock), update the test doubles that would otherwise silently skip coverage of the new call shape, and execute the blocking live-DB checkpoint that applies 08-01's schema changes to Supabase.

Purpose: this is the wiring + proof plan — every piece of logic built in 08-01 (schema) and 08-02 (repo helpers) becomes reachable from a real production failure and visible on the dashboard, and the live schema catches up to schema.sql under human supervision (per project convention: live-DB migrations never run silently in CI).
Output: 3 enriched error-boundary call sites, 2 updated templates, a thread-safe pool singleton, updated test doubles (`InMemoryRepo`, `test_threading.py`'s fake) that accept the new kwargs, and a completed, human-verified live schema apply.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-CONTEXT.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-PATTERNS.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-01-PLAN.md
@.planning/phases/08-data-layer-hygiene-diagnostics/08-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the 3 record_run_error call sites + roster-scope guard + templates + pool lock + test-double updates</name>
  <files>app/pipeline/orchestrator.py, app/main.py, app/templates/run_detail.html, app/templates/runs_list.html, app/db/supabase.py, tests/conftest.py, tests/test_threading.py</files>
  <read_first>
    app/pipeline/orchestrator.py (lines 173-260 for `run_pipeline`, `_run`, and the start of `resume_pipeline` — confirm `roster` is bound INSIDE `_run` at the `roster = repo.load_roster_for_business(...)` line, NOT in `run_pipeline`'s own scope; and confirm `resume_pipeline`'s `roster = repo.load_roster_for_business(run["business_id"])` executes at line 247, INSIDE the same try block the except-clause at line 661 guards, meaning `roster` is unbound if an exception fires before that line; lines 640-668 for the exact `resume_pipeline` except block to edit)
    app/main.py (lines 475-507 for the exact `approve()` try/except block to edit — confirm no roster is loaded anywhere in this function's scope)
    app/db/repo.py (the NEW `record_run_error` signature from Plan 08-02 — `detail_exc`, `stage`, `roster` keyword-only params)
    app/templates/run_detail.html (lines 65-69 for the exact error banner block to edit — the existing `<strong>Error</strong> — {{ run.error_reason }}...` line must stay byte-identical)
    app/templates/runs_list.html (lines 63-71 for the exact Summary cell to edit)
    app/db/supabase.py (full file — `get_pool()` lines 29-51, the unguarded lazy singleton)
    tests/conftest.py (lines 331-339 for `InMemoryRepo.record_run_error` — the fake that must accept the new kwargs without raising `TypeError`)
    tests/test_threading.py (line 700 for its own separate fake `record_run_error` — same requirement)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-CONTEXT.md (D-8-05, D-8-06, D-8-08; folded todo 260623-01 WR-02 slice)
    .planning/phases/08-data-layer-hygiene-diagnostics/08-RESEARCH.md (the roster-availability caveat under "Verified: three call sites" — the exact UnboundLocalError risk and the recommended guard; Pitfall 4 — the full list of test doubles that must be updated)
  </read_first>
  <action>
    **orchestrator.py — 2 call sites:**
    In `run_pipeline`'s except block (around line 181-188): change `repo.record_run_error(run_id, reason)` to `repo.record_run_error(run_id, reason, detail_exc=exc, stage="pipeline")`. `run_pipeline` itself never loads a roster (it's loaded inside `_run`'s own scope, invisible to `run_pipeline`'s except block) — do NOT pass `roster=` here; `roster=None` is implicit and correct (regex-only fallback per D-8-01b).

    In `resume_pipeline`: add `roster = None` as the FIRST statement inside the `try:` block (before line 235's `claimed = repo.claim_status(...)`), so the name is always bound in the enclosing scope. Then at line 247's existing `roster = repo.load_roster_for_business(run["business_id"])`, leave it unchanged (it reassigns the same local). In the except block (around line 661-667), change `repo.record_run_error(run_id, reason)` to `repo.record_run_error(run_id, reason, detail_exc=exc, stage="resume", roster=roster)` — `roster` is now guaranteed bound (either `None` from the top-of-try initialization, or the real `Roster` if the exception fired after line 247).

    Update the stale docstring at `run_pipeline` (line 174): `"""Drive one run from received → awaiting_approval (or needs_clarification)."""` → `"""Drive one run from received → awaiting_approval (or awaiting_reply on a clarification)."""` (NEEDS_CLARIFICATION was removed in Plan 08-01; `_clarify()` has always routed to `awaiting_reply`, never the dead status).

    **main.py — 1 call site:**
    In `approve()`'s except block (line 502-506): change `repo.record_run_error(run_id, type(exc).__name__)` to `repo.record_run_error(run_id, type(exc).__name__, detail_exc=exc, stage="delivery")`. No `roster=` — `approve()` never loads one; `roster=None` is correct by design (D-8-01b), not a gap.

    **run_detail.html — error banner second line (D-8-06):**
    Inside the `{% if run.status == 'error' %}` block (lines 66-69), keep the existing `<strong>Error</strong> — {{ run.error_reason }}. Use the Re-trigger button below...` line completely unchanged, and add immediately after it: `{% if run.error_detail %}<div class="banner-detail">{{ run.error_detail }}</div>{% endif %}`. Use Jinja2's default autoescaping (do NOT add `| safe` or any autoescape-disabling filter — `error_detail` is scrubbed but still untrusted-origin text; autoescape must stay on). If no `banner-detail`-equivalent CSS class exists in the project stylesheet, reuse the existing `banner-divider`/`column-label` inline-style convention from the same file (lines 73-74) instead of inventing new global CSS.

    **runs_list.html — Summary cell alias switch (D-8-08):**
    Replace the Summary cell body (lines 63-71) — currently `{% if run.decision and run.decision.gate_reasons %}{{ run.decision.gate_reasons[0] }}{% elif run.extracted_data and run.extracted_data.employees %}{{ run.extracted_data.employees | length }} employee...{% else %}—{% endif %}` — with: `{% if run.summary_gate_reason %}{{ run.summary_gate_reason }}{% elif run.employee_count %}{{ run.employee_count }} employee{{ 's' if run.employee_count != 1 else '' }}{% else %}—{% endif %}`. This is the ONLY template consumer of these two aliases; `run_detail.html` continues to load the full run via `load_run`/`RUN_COLS`, unaffected.

    **app/db/supabase.py — WR-02 pool-singleton thread-safety guard:**
    Add `import threading` to the import block. Add a module-level `_pool_lock = threading.Lock()` alongside the existing `_pool: ConnectionPool | None = None`. In `get_pool()`, wrap the existing `if _pool is None: ... ConnectionPool(...)` body in a double-checked-locking pattern: outer `if _pool is None:` unchanged, then `with _pool_lock:` then a RE-CHECK `if _pool is None:` before constructing the `ConnectionPool`. This closes the theoretical race where FastAPI's threadpool executor (sync routes / BackgroundTasks) could interleave two first-callers even under Render's single-process deploy (per 08-RESEARCH.md Open Question 1 — treat as real, implement the lock, do not choose documentation-only).

    **Test doubles — accept new kwargs without erroring (Pitfall 4):**
    In `tests/conftest.py`'s `InMemoryRepo.record_run_error` (line ~331): change the signature from `(self, run_id, reason, conn=None)` to `(self, run_id, reason, *, detail_exc=None, stage=None, roster=None, conn=None)`. The body stays functionally the same (no-op on the new params is acceptable here — the REAL scrub logic is proven by the dedicated unit tests in Plan 08-02 against the real `repo.record_run_error`); this fake only needs to not raise `TypeError` when orchestrator.py/main.py call it with the new keyword args during integration-style tests.
    In `tests/test_threading.py`'s own fake `record_run_error` (line ~700): apply the identical signature change — accept and no-op the new keyword-only params.
  </action>
  <verify>
    <automated>uv run pytest tests/ -k "record_run_error or threading" -q 2>&1 | tail -5 && uv run pytest tests/test_dashboard.py tests/test_delivery.py tests/test_gateway.py tests/test_clarify.py -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "detail_exc=exc, stage=\"pipeline\"" app/pipeline/orchestrator.py` matches
    - `grep -n "detail_exc=exc, stage=\"resume\", roster=roster" app/pipeline/orchestrator.py` matches
    - `grep -n "roster = None" app/pipeline/orchestrator.py` shows the new top-of-try initialization inside `resume_pipeline`
    - `grep -n "detail_exc=exc, stage=\"delivery\"" app/main.py` matches
    - `grep -n "run.error_detail" app/templates/run_detail.html` matches, and the existing `{{ run.error_reason }}` line text is unchanged (byte-diff the surrounding line against the pre-edit version)
    - `grep -n "summary_gate_reason\|employee_count" app/templates/runs_list.html` matches; `run.decision.gate_reasons` and `run.extracted_data.employees` no longer appear in the Summary cell block
    - `grep -n "_pool_lock\|threading.Lock" app/db/supabase.py` matches
    - `uv run pytest tests/ -k "record_run_error or threading" -q` passes (confirms `InMemoryRepo` and `test_threading.py`'s fake accept the new call shape)
    - `uv run pytest tests/test_dashboard.py tests/test_delivery.py tests/test_gateway.py tests/test_clarify.py -q` passes (integration-level exercise of all 3 call sites + both templates)
  </acceptance_criteria>
  <done>All 3 record_run_error call sites pass detail_exc/stage (resume also passes roster, guarded against UnboundLocalError); both templates render the new fields per D-8-06/D-8-08; the pool singleton is thread-safe; both test doubles accept the new kwargs; the full suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live-DB schema apply — indexes, error_detail column, status CHECK swap</name>
  <action>Human-verify checkpoint — apply the Plan 08-01 schema.sql changes to the live Supabase database via bootstrap and confirm via pg_indexes/information_schema/pg_constraint. Claude cannot apply a live-DB migration to Supabase on the human's behalf (project convention: live-DB migrations run at a blocking human checkpoint, never silently in CI).</action>
  <what-built>
    schema.sql (Plan 08-01) now declares the error_detail column, 3 new CREATE INDEX IF NOT EXISTS statements (email_messages composite + 2 payroll_runs), and a DO-block DROP+ADD swap removing 'needs_clarification' from the payroll_runs.status CHECK. This task applies those changes to the LIVE Supabase database via the project's idempotent bootstrap and verifies the result — per project convention (Phase 02.1 P03, Phase 7.5 N4 precedent), live-DB migrations never run silently in CI or as an "auto" task.
  </what-built>
  <how-to-verify>
    1. Pre-migration guard (MUST run and confirm 0 before proceeding — ADD CONSTRAINT validates ALL existing rows against the new CHECK): connect to the live DB (`psql $DATABASE_URL` or the Supabase SQL editor) and run:
       `SELECT count(*) FROM payroll_runs WHERE status = 'needs_clarification';`
       Confirm the result is exactly `0`. If it is NOT 0, STOP — do not proceed to step 2; report back so the plan can be revised (a legacy row in this status would fail the CHECK swap).
    2. Apply the schema: run `uv run python -m app.db.bootstrap` (default non-destructive path — re-applies schema.sql idempotently, does NOT reset data) against the environment pointed to by your `DATABASE_URL`.
    3. Verify the 3 new indexes exist: run
       `SELECT indexname, indexdef FROM pg_indexes WHERE tablename IN ('email_messages','payroll_runs') AND indexname LIKE 'idx_%';`
       Confirm all 3 appear: `idx_email_messages_run_direction_state` on `(run_id, direction, send_state)`, `idx_payroll_runs_created_at` on `(created_at)`, `idx_payroll_runs_status` on `(status)`.
    4. Verify the contact_email hot path is still constraint-covered (D-8-09 — not a new index, the existing UNIQUE constraint's implicit index):
       `SELECT indexname, indexdef FROM pg_indexes WHERE tablename = 'businesses';`
       Confirm a UNIQUE index on `contact_email` is present (created automatically by the `NOT NULL UNIQUE` column constraint — no new index statement was added for it, by design).
    5. Verify the error_detail column exists:
       `SELECT column_name, data_type, is_nullable FROM information_schema.columns WHERE table_name = 'payroll_runs' AND column_name = 'error_detail';`
       Confirm it returns one row: `error_detail | text | YES`.
    6. Verify the status CHECK no longer allows needs_clarification:
       `SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'payroll_runs'::regclass AND contype = 'c' AND conname LIKE '%status%';`
       Confirm the constraint definition's IN-list does NOT contain `'needs_clarification'` and DOES contain all 10 remaining values.
    7. Confirm the hermetic suite still passes against this now-migrated environment: `uv run pytest -q` (should remain 492+ passed with the new Plan 08-01/08-02/08-03 tests added, 0 failed).
  </how-to-verify>
  <verify><human-check>Pre-migration count is 0; all 3 new indexes present via pg_indexes; businesses.contact_email UNIQUE index confirmed present; error_detail column confirmed present and nullable; payroll_runs_status_check confirmed to exclude needs_clarification and contain the 10 remaining values; uv run pytest -q green against the migrated environment</human-check></verify>
  <done>The live Supabase schema has the error_detail column, all 3 new indexes, the contact_email UNIQUE-constraint coverage confirmed, and the 10-value status CHECK applied — confirmed by the 6 SQL checks above and a green test suite run. Per D-8-11: once step 4 confirms contact_email's UNIQUE-constraint coverage, update REQUIREMENTS.md's OPS2-02 wording at phase close from "an index on businesses.contact_email" to "hot path verified index-covered by the existing UNIQUE constraint" — do not ship a duplicate index just to match the requirement's literal wording.</done>
  <resume-signal>Type "approved" once all 6 SQL checks in steps 1, 3, 4, 5, 6 return the expected result and step 7's test suite is green — or describe which check failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `error_detail` (already scrubbed by 08-02) -> Jinja2 template render | Even though scrubbed, `error_detail` still contains exception-derived text of ultimately client/LLM origin — the template layer must not re-introduce an XSS/injection surface by disabling autoescape |
| Live Supabase schema apply | The CHECK-swap ADD CONSTRAINT validates all existing rows; a live migration on a stale/legacy row set is a data-integrity boundary distinct from anything CI can prove |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-08 | Tampering / Injection | `run_detail.html`'s new `{{ run.error_detail }}` render | mitigate | Jinja2's default autoescape stays ON (no `| safe` filter added); `error_detail` is treated as untrusted-origin text even after scrubbing, consistent with how `error_reason` is already rendered on the adjacent line |
| T-8-09 | Tampering (data integrity) | live `payroll_runs.status` CHECK swap | mitigate | Blocking human checkpoint runs the pre-migration `SELECT count(*) WHERE status = 'needs_clarification'` guard and requires a confirmed `0` before the ADD CONSTRAINT step; the DROP+ADD is one `DO $$ ... END $$;` transaction so a failed ADD rolls back the DROP too (worst case: no-op, never half-migrated) |
| T-8-10 | Denial of Service | `get_pool()` double-checked locking (WR-02) | mitigate | `threading.Lock()` around the check-then-create closes the race where FastAPI's threadpool executor could construct two `ConnectionPool` instances (one leaked) under concurrent first-callers, even in Render's single-process deploy |
</threat_model>

<verification>
Wave-level: `uv run pytest -q` must be green after Task 1 (hermetic — no DB needed). Task 2 is a manual live-DB checkpoint; its own verification steps are the 6 SQL checks + the re-run of `uv run pytest -q` against the migrated environment listed in `<how-to-verify>`.
</verification>

<success_criteria>
- All 3 `record_run_error` call sites pass `detail_exc=`/`stage=` (resume also `roster=`, guarded against `UnboundLocalError`).
- `run_detail.html` and `runs_list.html` both render the new Phase 8 fields per D-8-06/D-8-08, with autoescape intact.
- `app/db/supabase.py`'s pool singleton is guarded by a `threading.Lock()`.
- `InMemoryRepo.record_run_error` and `test_threading.py`'s fake accept the new call shape without raising.
- Live Supabase schema has the error_detail column, all 3 new indexes, the contact_email UNIQUE-constraint coverage confirmed, and the 10-value status CHECK applied — verified by a human via the 6 `pg_indexes`/`information_schema`/`pg_constraint` checks in Task 2.
- Full hermetic suite green both before and after the live apply.
</success_criteria>

<output>
Create `.planning/phases/08-data-layer-hygiene-diagnostics/08-03-SUMMARY.md` when done
</output>
</content>
