---
phase: 16-queue-substrate-unblocked-webhook
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/concurrency-proof.yml
  - pyproject.toml
  - app/config.py
  - render.yaml
  - .env.example
  - tests/test_queue_config.py
autonomous: true
requirements: [QUEUE-02, QUEUE-03]
must_haves:
  truths:
    - "A NEW `queueproof` pytest marker exists, is registered in pyproject.toml, and is collected+executed by a NEW step in the ONLY CI workflow with a real Postgres — so any durability proof marked `queueproof`, in any file, in any phase 17-21, runs in CI with zero further workflow edits (D-14)."
    - "The EXISTING two-file `-m integration` step and its skip-guard are byte-identical to before this plan — zero blast radius; no previously-dormant live-DB module is woken (D-14)."
    - "The new queueproof step carries its OWN identical skip-guard: any skip reds the build, and 'nothing passed at all' reds the build. Its comment states the TRUE rationale (pytest already exits 5 on a zero-collecting marker; the log guard is what survives a DISARMED pipeline) and its TRUE gap (it cannot see one newly-added test whose marker is typo'd while others pass). No plan text may claim a zero-collecting marker 'exits green' — that is false."
    - "LEASE_SECONDS, MAX_ATTEMPTS and WORKER_COUNT are env-driven pydantic-settings fields with committed defaults, and LEASE_SECONDS' derivation is written down as a constraint comment (D-03)."
  prohibitions:
    - "MUST NOT change the existing pytest step's file list or its `-m integration` selector. Whole-suite `-m integration` collection would wake 10 dormant live-DB modules against a shared Postgres with a destructive module-scope reset — D-04's original instruction is SUPERSEDED by D-14 and is FORBIDDEN."
    - "MUST NOT write a 'double-run is harmless' claim into LEASE_SECONDS' comment. That claim is FALSE for the client-facing send path until D-13's guard lands (plan 16-10). Write the narrowed claim only."
    - "MUST NOT write, in the workflow comment or anywhere else, that a typo'd marker 'collects nothing and exits green'. pytest exits 5 (NO_TESTS_COLLECTED) and `set -o pipefail` already reds the step. Shipping a plan that teaches the executor a false fact about pytest is its own defect — the cross-AI review caught this one."
  artifacts:
    - "pyproject.toml — `queueproof` registered in [tool.pytest.ini_options] markers."
    - ".github/workflows/concurrency-proof.yml — a NEW second pytest step selecting `-m queueproof` over tests/, with its own skip-guard. The pre-existing step is untouched."
    - "app/config.py — Settings.worker_count (2), .lease_seconds (900), .max_attempts (5), .queue_poll_seconds (20)."
    - "render.yaml + .env.example — the four knobs as committed non-secret values."
    - "tests/test_queue_config.py — defaults, env override, a static guard that the D-03 derivation comment exists, and a guard that the pre-existing CI file list was not widened."
  key_links:
    - "D-14's `queueproof` marker MUST land before the first proof file that carries it (plan 16-04, wave 2; also 16-09 and 16-10). This plan is wave 1 precisely to satisfy that ordering."
    - "WORKER_COUNT feeds the D-07 pool-budget guard in 16-07; LEASE_SECONDS feeds claim_job in 16-04; MAX_ATTEMPTS feeds enqueue_job in 16-04."
  falsifying_mutations:
    - "D-14: delete the NEW queueproof step's `grep -qE '[0-9]+ skipped'` block. A workflow run in which every queueproof test skips must then pass green — proving the guard is what makes the new selection trustworthy. Restore it."
    - "The `[0-9]+ passed` guard (CORRECTED — the previous mutation for this guard was based on a FALSE premise about pytest): append `|| true` to the new step's pytest line AND typo the marker to `-m queueprooof`. pytest's exit 5 is now swallowed, so the log-content guard is the ONLY thing left. The step MUST be RED with the guard present and GREEN with it deleted. Restore both. (The old mutation — 'typo the marker, observe green' — is not reproducible: with pipefail and no `|| true`, exit 5 already reds the step.)"
    - "D-14 no-widening guard: change the existing step's pytest line to `uv run pytest tests/ -m integration`. tests/test_queue_config.py's no-widening assertion MUST go red. Restore."
    - "Config: remove the derivation comment from LEASE_SECONDS. tests/test_queue_config.py's static-comment assertion MUST go red — D-03 requires the derivation be written down, not just the number."
---

<objective>
Build the proof surface and the config knobs the rest of Phase 16 stands on.

Two things, both of which must exist BEFORE any queue code is written:

1. **D-14 — a dedicated `queueproof` marker, collected by a NEW step in `concurrency-proof.yml`.**
   It is the only CI workflow with a real Postgres and it selects test files **by name** (`:89`).
   Its own comment says it: *"A new live-DB test that is not added to this list will skip silently
   and forever."* Four of this phase's proofs need a real database. This plan is in **wave 1** so the
   marker exists before the first file that carries it is committed (16-04, wave 2).

   > **⚠ D-04 IS SUPERSEDED. READ THIS BEFORE TOUCHING THE WORKFLOW.**
   > The original D-04 said "generalize collection to the whole suite (`pytest tests/ -m
   > integration`)" and plan 16-02 called that change *behavior-preserving*. **That was false, and
   > the cross-AI review caught it.** **12** files under `tests/` already carry an `integration`
   > marker; only **2** are in the CI file list. Whole-suite collection would wake **10 dormant
   > live-DB modules** at once, against a shared Postgres service, with a destructive module-scope
   > reset at `tests/conftest.py:74-93`. That is a large, unbudgeted, unrelated change smuggled
   > inside a durability phase.
   >
   > **D-14 replaces it: keep the existing gate byte-identical, and add a NARROW second gate on a
   > new `queueproof` marker.** Zero blast radius; Phases 17-21 still add proofs with zero workflow
   > edits (which is all D-04 actually wanted). The 10 dormant modules are a **pre-existing gap**,
   > recorded as a backlog item — this phase neither widens it nor pretends to fix it.

2. **D-08/D-03 — the env-driven queue knobs**, with `LEASE_SECONDS`' derivation written down as a
   constraint comment rather than dropped in as a magic number.

Purpose: the milestone's own named cross-cutting hazard #1 is a durability proof that never runs.
Fix that once, here, narrowly — and Phases 17-21 add proofs with zero workflow edits.
Output: a marker-driven CI proof surface, byte-identical existing gate, and four committed knobs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-CONTEXT.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-RESEARCH.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-VALIDATION.md
</context>

## Artifacts this phase produces

| Artifact | Owning plan |
|---|---|
| `app/routes/webhook.py::_parse_and_ingest_sync` / `_duplicate_redelivery_sync`; `tests/test_webhook_unblocked.py` | 16-01 |
| **`.github/workflows/concurrency-proof.yml` marker collection; `Settings.worker_count`/`lease_seconds`/`max_attempts`/`queue_poll_seconds`; `tests/test_queue_config.py`** | **16-02** |
| `app/models/job.py` (`JobKind`/`JobState`/`Job`); `jobs` table + `uq_jobs_dedup_key` + `ck_jobs_lease_coherent` + `idx_jobs_claimable`; `bootstrap._DROP_ORDER` gains `"jobs"` | 16-03 |
| `app/db/repo/jobs.py` (`enqueue_job`, `claim_job`, `complete_job`, `fail_job`, `release_leases`, `get_job`); `clear_reply_context -> int`; `rewind_for_reclaim`; conftest `WORKER_COUNT=0` pin + fake_repo names | 16-04 |
| `schema_introspect` covers `jobs`; `tests/test_job_kind_drift.py`; D-05 rewrite of `tests/test_status_drift.py` | 16-05 |
| `app/queue/wake.py`, `dispatch.py`, `handlers/pipeline.py`, `drain.py` | 16-06 |
| `app/queue/worker.py` (`start`/`stop`/`lifespan`), `app/db/supabase.py::POOL_MAX_SIZE`, `app/main.py` lifespan | 16-07 |
| `app/routes/runs.py::_claim_stale_in_flight`; retrigger -> `enqueue_job` | 16-08 |
| `tests/test_queue_durability.py` (Proofs 2/3/4) | 16-04, 16-07, 16-09 |
| `app/db/repo/emails.py::get_unconfirmed_outbound`; `app/pipeline/send_guard.py` (`UnconfirmedSendError`, `assert_no_unconfirmed_send`); the guard call in `clarify()` + `deliver()` | **16-10** (D-13, wave 3) |

<tasks>

<task type="auto">
  <name>Task 1: Register the `queueproof` marker and add a NARROW second CI gate for it (D-14)</name>
  <files>pyproject.toml, .github/workflows/concurrency-proof.yml</files>
  <read_first>
    - `.github/workflows/concurrency-proof.yml` (FULL FILE — you must understand the `services:
      postgres` block, the bootstrap step, the by-name selection at `:89`, and the skip-guard at
      `:90-98` before touching anything).
    - `pyproject.toml` `[tool.pytest.ini_options]` (`:39-44`) — the two existing registered markers
      (`integration`, `live_llm`) and their one-line description convention. Copy that convention.
    - `16-CONTEXT.md` **D-14** (the locked instruction) and **D-04** (superseded — read it only so
      you recognise the instruction you are NOT following, and why).
    - `.planning/REQUIREMENTS.md` PROOF-05.
    - **The inventory that proves D-04 was wrong.** Run this and read the output before you edit:
      `git grep -l 'pytest.mark.integration' -- tests/` — it lists **12** files. The CI step at `:89`
      names exactly **2**. Confirm the number for yourself; it is the entire reason for D-14.
  </read_first>
  <action>
**`pyproject.toml`** — add ONE marker to `[tool.pytest.ini_options] markers`, in the existing style:

`"queueproof: a durability proof for the v4 job queue; MUST execute in CI against a real Postgres"`

`queueproof` is ORTHOGONAL to `integration`, not a replacement for it. A queue durability proof
carries BOTH: `integration` keeps it self-skipping without a database exactly like every other
live-DB test in the repo, and `queueproof` is what the new CI gate SELECTS on.

**`.github/workflows/concurrency-proof.yml`** — two edits, and one hard prohibition.

**PROHIBITION (this is the review finding, do not get this wrong):** the existing step
"Run the real-Postgres invariant proofs" — its `uv run pytest tests/test_concurrency_proof.py
tests/test_email_epoch_arbiter_integration.py -m integration -v -rs` line, its `tee`, and BOTH
halves of its skip-guard — stays **byte-identical**. Do NOT change it to `pytest tests/ -m
integration`. Twelve files under `tests/` already carry an `integration` marker and only two are in
that list; whole-suite collection would wake **ten dormant live-DB modules** at once against a
shared Postgres with a destructive module-scope reset (`tests/conftest.py:74-93`). `git diff` for
that step must show only comment-block changes, never a change to the pytest line.

**Edit 1 — amend that step's comment block, honestly.** Its point 1 currently says test files are
selected BY NAME and a new live-DB test must be added to the list. That remains TRUE for this step,
so keep it. ADD a cross-reference: durability proofs added in Phases 16-21 do NOT go in this list;
they carry `@pytest.mark.queueproof` and are collected by the step below. Do not delete or weaken
its point 2 (why a skipped proof proves nothing).

**Edit 2 — add a NEW step, "Run the queue durability proofs (real Postgres)", immediately after it.**
It mirrors the existing step's shape exactly:
- `shell: bash`, `set -o pipefail`
- `uv run pytest tests/ -m queueproof -v -rs 2>&1 | tee pytest-queueproof.log`
- The SAME two guards, on the new log file: `grep -qE '[0-9]+ skipped'` → exit 1 with a message
  saying a skipped durability proof proves nothing; and `! grep -qE '[0-9]+ passed'` → exit 1 with a
  message saying the run produced no passing proof.

  > **⚠ WRITE THE TRUE RATIONALE FOR THE `[0-9]+ passed` GUARD. An earlier draft of this plan wrote a
  > FALSE one and the cross-AI review caught it.** That draft claimed a typo'd marker "collects zero
  > tests and exits green", making this guard the thing that catches it. **That is not how pytest
  > behaves:** a selection that matches nothing exits **5** (`NO_TESTS_COLLECTED`), and with
  > `set -o pipefail` in front of the `tee`, the step is **already red** on a whole-marker typo. Do not
  > ship a plan that teaches the executor a false fact about their own test runner.
  >
  > **The guard's TRUE value** — keep it, it is worth keeping — is that it fails the step whenever the
  > run produced **no passing test for any reason**, *without depending on pytest's exit code surviving
  > the shell pipeline*. It is the guard that still bites after someone "fixes the flaky CI" by
  > appending `|| true` or dropping `pipefail` — the single most common way a gate gets silently
  > disarmed. (All-skipped is already caught by the skip guard; this is defense in depth against the
  > pipeline itself, not against pytest.)
  >
  > **And write down the gap it does NOT close, because it is the honest one:** the guard cannot detect
  > a typo'd marker on **one newly-added test** while the other `queueproof` tests still pass — the log
  > still says "N passed" and the new proof is silently uncollected. Nothing in CI closes that. What
  > closes it is a per-plan check: `uv run pytest tests/ -m queueproof --collect-only -q` must list
  > every test in the file you just wrote (an acceptance criterion in 16-04, 16-07, 16-09 and 16-10).
  > Say so in the comment, and point at it.

Write the step's comment block to carry the contract: **collection here is by MARKER
(`queueproof`), not by filename — any durability proof anywhere under `tests/`, in any phase, runs
here automatically with zero edits to this file. That is what D-04 wanted; D-14 delivers it without
the blast radius of collecting every `integration`-marked test in the repo.** Also record, in that
comment, the known gap this deliberately does NOT fix: **10 `integration`-marked modules are still
dormant in CI (a pre-existing gap, tracked in the roadmap backlog).** A comment that quietly implies
they run would be worse than no comment.

Do NOT touch the `services: postgres` block, the `DATABASE_URL`/`ALLOW_DB_RESET`/
`ALLOW_UNSIGNED_FIXTURES` env, or the `python -m app.db.bootstrap` step (deliberately no `--reset`:
the conftest fixture is the sole owner of reset, behind its two-factor guard). The new step reuses
all of them — it is a step in the SAME job, so the schema is already applied and the DB is already up.
  </action>
  <verify>
    <automated>uv run python -c "
import pathlib, yaml
s = pathlib.Path('.github/workflows/concurrency-proof.yml').read_text()
yaml.safe_load(s)
assert 'tests/test_concurrency_proof.py tests/test_email_epoch_arbiter_integration.py -m integration' in s, 'existing gate was widened — FORBIDDEN by D-14'
assert 'uv run pytest tests/ -m integration' not in s, 'whole-suite integration collection is FORBIDDEN by D-14'
assert '-m queueproof' in s, 'the new narrow gate is missing'
assert s.count('skipped') >= 2 and s.count('passed') >= 2, 'both steps need their own skip-guard'
p = pathlib.Path('pyproject.toml').read_text()
assert 'queueproof:' in p, 'marker not registered'
print('D-14 OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` registers `queueproof`; `uv run pytest tests/ -m queueproof --collect-only -q`
      runs without an unknown-marker warning (it collects 0 tests at this commit — correct: no proof
      file carries the marker yet; the first is 16-04's, wave 2).
    - The existing "Run the real-Postgres invariant proofs" step's **pytest line is byte-identical**
      to `master`. Prove it, don't eyeball it — extract that one line from both revisions and compare:
      `uv run python -c "import subprocess,pathlib; sel=lambda t:[l.strip() for l in t.splitlines() if 'pytest' in l and 'integration' in l and 'tee' in l]; old=subprocess.run(['git','show','master:.github/workflows/concurrency-proof.yml'],capture_output=True,text=True).stdout; new=pathlib.Path('.github/workflows/concurrency-proof.yml').read_text(); assert sel(old) and sel(old)[0] in sel(new), (sel(old), sel(new)); print('existing gate byte-identical')"`
      (Only its comment block may change. The new queueproof step is an ADDITION, not an edit.)
    - The workflow contains a SECOND pytest step selecting `-m queueproof` over `tests/`, with its own
      `[0-9]+ skipped` guard AND its own `[0-9]+ passed` guard.
    - `uv run python -c "import yaml,pathlib; yaml.safe_load(pathlib.Path('.github/workflows/concurrency-proof.yml').read_text()); print('yaml ok')"` succeeds.
    - The new step's comment block explicitly records the 10 dormant `integration`-marked modules as a
      known, deliberately-unfixed pre-existing gap. Do not imply they run.
    - The new step's comment block records the `[0-9]+ passed` guard's TRUE rationale (it survives a
      disarmed shell pipeline) and its TRUE gap (it cannot see a single newly-added test whose marker
      is typo'd, while the others pass). It must NOT claim that a zero-collecting marker "exits green"
      — pytest exits **5**, and that claim is false.
    - FALSIFYING MUTATION EXECUTED (the honest one for this guard): append `|| true` to the new step's
      pytest line — the exact edit someone makes to "unblock CI" — AND rename the marker to
      `-m queueprooof` (typo). pytest's exit 5 is now swallowed by `|| true`, so **only** the
      `[0-9]+ passed` guard stands between that and a green build. Confirm the step is RED with the
      guard present, and GREEN with the guard deleted. Restore both; paste both runs into the SUMMARY.
      (The previously-specified mutation — "typo the marker and observe a green exit" — was **false**:
      with `pipefail` and no `|| true`, pytest's exit 5 already reds the step.)
  </acceptance_criteria>
  <done>The narrow durability gate exists: any `queueproof`-marked test, in any file, in any phase, runs in CI against a real Postgres and reds the build if it skips or if the selection collects nothing. The pre-existing two-file gate is untouched, and no dormant live-DB module was woken.</done>
</task>

<task type="auto">
  <name>Task 2: Add the four env-driven queue knobs with their derivations (D-08, D-03)</name>
  <files>app/config.py</files>
  <read_first>
    - `app/config.py` (FULL FILE — the `Settings` class, the "no default -> fails fast" precedent on
      `database_url`, and the empty-string-default precedent on optional keys. Match the section
      comment style exactly.)
    - `app/routes/runs.py:34-68` — the `STALE_THRESHOLD` derivation comment. This is the SOURCE of
      `LEASE_SECONDS`' number; you are cross-referencing it, NOT re-deriving the arithmetic.
    - `16-RESEARCH.md` § Discretionary Numbers — take the LEASE_SECONDS derivation and the
      MAX_ATTEMPTS scoping caveat from it; transcribe the substance, do not re-invent it.
      > **⚠ IGNORE that section's "double-run-is-harmless" argument. It is WRONG** — it asserts that
      > delivery's already-sent guard blocks a second client email across a double-run, and it does
      > not: that guard counts only `send_state='sent'`, so a crash between provider-acceptance and
      > the sent-commit leaves it blind. The cross-AI review confirmed this against live source.
      > **Write the narrowed statement in (b) below instead**, and cite D-13 / plan 16-10 for the fix.
    - `16-CONTEXT.md` D-03 (note its "NARROWED BY D-13" banner), D-08, D-09, and **D-13**.
  </read_first>
  <action>
Add a `# ── Durable job queue (v4) ──` section to `Settings` with exactly four fields. Per **D-08**
the first three env names are LOCKED, so the field names must be exactly these (pydantic-settings
uppercases the field name to derive the env var):

- `worker_count: int = 2` (env `WORKER_COUNT`). Comment: 2 daemon threads per QUEUE-03; `0` is the
  test/dev off switch and is what `tests/conftest.py` pins so the suite never spawns real workers
  (D-06). The pool-budget guard that makes an over-large value fail at boot lives in
  `app/queue/worker.py`'s lifespan (D-07, plan 16-07) — not here.

- `lease_seconds: int = 900` (env `LEASE_SECONDS`). This is the **load-bearing safety parameter**
  (D-03) and its comment must carry BOTH halves:
  (a) **The derivation, by cross-reference — do not re-derive it.** `app/routes/runs.py:34-68`
      already computes and documents the pipeline's worst-case gap between two consecutive DB
      writes on the longest real path at **210s** (the resume path's back-to-back double extraction,
      45s x 2 app-retries x 2 calls, plus a 30s clarification draft) and picks 15 minutes as ~4x
      that ceiling for `STALE_THRESHOLD`. `LEASE_SECONDS = 900` reuses that already-reviewed
      number. State explicitly that maintaining two independent copies of the "210s x ~4" arithmetic
      is the drift risk this cross-reference exists to avoid.
  (b) **What a double-run actually costs — the NARROWED claim. Write exactly this; do NOT write
      "a double-run is harmless".** That sentence is FALSE and the cross-AI review caught it.
      - It IS harmless for **pipeline state**: `claim_status`' CAS makes every status advance
        at-most-once, and `replace_line_items` is DELETE-by-run-then-INSERT (idempotent by value).
      - It is **NOT** intrinsically harmless for the **client-facing send**. `gateway.send_outbound`
        writes `send_state='reserved'`, calls the provider, and only THEN flips to `'sent'`
        (`gateway.py:271-289 / 339-345 / 355-359`), while both existing duplicate guards count ONLY
        `send_state='sent'` rows (`emails.py:140-171`, `emails.py:174-218`). A worker killed between
        provider-acceptance and the sent-commit leaves **no `'sent'` row while the client already has
        the email** — so a naive re-run would send a SECOND one.
      - That window is closed **in this phase** by **D-13**'s fail-closed unconfirmed-reservation
        guard (plan **16-10**, wave 3 — it lands BEFORE the first live worker in wave 4). Point the
        comment at `app/pipeline/send_guard.py` and stop there. Do NOT restate D-13's mechanism here;
        one authoritative copy, in the guard's own docstring.
  (c) **No lease heartbeat**, and why: it would burn a pooled connection per extension against a
      `max_size=5` budget and introduce a worse failure mode (heartbeat thread dies, work continues)
      than the one it prevents.

- `max_attempts: int = 5` (env `MAX_ATTEMPTS`). Comment must carry the **Phase-16 scoping caveat**:
  `attempts` is incremented AT CLAIM (not at failure), so in this phase the counter only advances
  via a genuine crash-reclaim — there is no retryable/terminal backoff classification yet (that is
  FAIL-01/FAIL-02, Phase 18). So `MAX_ATTEMPTS=5` here means "a single retrigger survives up to 5
  worker-crash cycles before dead-lettering", NOT "5 retries of a classified failure". Say this
  plainly so a Phase-18 reader does not assume the constant already encodes a backoff policy.

- `queue_poll_seconds: int = 20` (env `QUEUE_POLL_SECONDS`). Comment: the **slow durable fallback**
  of D-09, not the latency path. The in-process `threading.Event` wake (`app/queue/wake.py`) is what
  makes Retrigger feel instant for the demo; this poll exists only to cover what an in-process
  signal cannot — expired-lease reclaims (bounded below by `LEASE_SECONDS`), future-dated backoff
  retries (Phase 18), and a cold-started instance where the enqueuing process no longer exists.
  D-09 locks the band at ~15-30s; 20 splits it. Explain that `LISTEN/NOTIFY` and session advisory
  locks are NOT available here — they fail SILENTLY under Supavisor transaction-mode pooling
  (`app/db/supabase.py:1-18`), which is what forces the Event-plus-slow-poll design.
  This field's name carries the `QUEUE_` prefix because, unlike the other three, D-08 does not lock
  its env name.
  </action>
  <verify>
    <automated>uv run pytest tests/test_queue_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from app.config import Settings; import os; os.environ.setdefault('DATABASE_URL','postgresql://x/y'); s=Settings(); assert (s.worker_count, s.lease_seconds, s.max_attempts, s.queue_poll_seconds) == (2, 900, 5, 20), s; print('defaults ok')"` succeeds.
    - Setting `WORKER_COUNT=0` in the environment yields `Settings().worker_count == 0`.
    - `app/config.py` contains the literal token `STALE_THRESHOLD` and the literal token `210`
      inside the `lease_seconds` comment (this is the machine-checkable form of D-03's "write the
      derivation down" requirement; `tests/test_queue_config.py` pins it in Task 3).
    - `uv run mypy app/config.py` and `uv run ruff check app/config.py` are clean.
  </acceptance_criteria>
  <done>Four env-driven knobs exist with committed defaults, and LEASE_SECONDS carries its derivation, its safety argument, and its no-heartbeat rationale as a constraint comment rather than as a bare number.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the knobs to render.yaml / .env.example and pin the config contract with a test</name>
  <files>render.yaml, .env.example, tests/test_queue_config.py</files>
  <read_first>
    - `render.yaml` — the `envVars:` block. Note the discipline: secrets use `sync: false`
      (Render prompts, never committed); non-secret configuration uses a committed `value:`.
      All four queue knobs are non-secret.
    - `.env.example` — the placeholder convention.
    - `tests/test_status_drift.py` — the shape of a static-file guard in this repo (read a file,
      assert on its text, no DB). Your comment-presence assertion imitates this.
  </read_first>
  <action>
`render.yaml`: add four non-secret `value:` entries under the existing non-secret configuration
section — `WORKER_COUNT: "2"`, `LEASE_SECONDS: "900"`, `MAX_ATTEMPTS: "5"`,
`QUEUE_POLL_SECONDS: "20"` — with a one-line comment noting that `WORKER_COUNT + 2` must stay
within the psycopg pool's `max_size` (5), which the app's lifespan hard-asserts at boot (D-07), so
raising this value past 3 will refuse to start rather than silently starve requests of connections.

Committing `WORKER_COUNT: "2"` means the production deploy starts 2 worker threads. That is the
intent of QUEUE-03 and it is safe in this phase: the only producer on the queue is the operator
Retrigger button, so with no jobs the workers simply block on the wake Event / 20s poll.

`.env.example`: add the same four keys with the same defaults and a pointer to `app/config.py` for
the derivations.

Create `tests/test_queue_config.py` (hermetic, no DB, no `integration`/`queueproof` marker) asserting:
- the four defaults are exactly (2, 900, 5, 20);
- `WORKER_COUNT=0` overrides cleanly through the env (use `monkeypatch.setenv` +
  `get_settings.cache_clear()` before and after, mirroring the `mock_llm` fixture's discipline);
- `render.yaml` contains all four keys (a deploy-config drift guard: a knob that exists in
  `Settings` but not in `render.yaml` is a knob production silently runs on the default of);
- **the D-03 derivation is written down**: `app/config.py`'s source text contains `STALE_THRESHOLD`
  and `210` within the queue-config section. This is a POSITIVE assertion — it fails if a future
  edit strips the derivation comment down to a bare number, which is exactly what D-03 forbids.
- **D-14's no-widening guard** (this is the review finding, mechanised): read
  `.github/workflows/concurrency-proof.yml` and assert BOTH — (i) the pre-existing pytest line still
  names `tests/test_concurrency_proof.py tests/test_email_epoch_arbiter_integration.py`, and (ii) the
  string `uv run pytest tests/ -m integration` does NOT appear anywhere in the file. Comment it with
  the reason: whole-suite `-m integration` collection would wake 10 dormant live-DB modules against a
  shared Postgres. A future contributor who "simplifies" the gate back to whole-suite collection must
  hit a red test that tells them why, not discover it in a flaky CI run.
- **`queueproof` is registered**: `pyproject.toml`'s markers list contains `queueproof`. An
  unregistered marker still *works* (pytest only warns), so nothing else would catch a typo'd
  registration — and the CI gate selects on this string.
  </action>
  <verify>
    <automated>uv run pytest tests/test_queue_config.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_queue_config.py -q` passes with at least 6 tests.
    - `uv run python -c "import yaml,pathlib; d=yaml.safe_load(pathlib.Path('render.yaml').read_text()); keys={e['key'] for s in d['services'] for e in s.get('envVars',[])}; missing={'WORKER_COUNT','LEASE_SECONDS','MAX_ATTEMPTS','QUEUE_POLL_SECONDS'}-keys; assert not missing, missing; print('render.yaml ok')"` succeeds.
    - FALSIFYING MUTATION EXECUTED: strip the derivation comment from `lease_seconds` in
      `app/config.py`; `tests/test_queue_config.py` MUST go red. Restore and paste the red run into
      the SUMMARY.
    - FALSIFYING MUTATION EXECUTED (D-14): change the workflow's existing pytest line to
      `uv run pytest tests/ -m integration -v -rs`; the no-widening test MUST go red. Restore and
      paste the red run into the SUMMARY.
  </acceptance_criteria>
  <done>The knobs are deploy-visible and drift-guarded; D-03's "write the derivation down" is machine-enforced; and D-14's narrow CI gate is protected by a test that reds if someone widens it back.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo -> CI runner | Workflow YAML controls what proofs actually execute. |
| Committed config -> production process | `render.yaml` `value:` entries become live env vars on Render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-05 | Repudiation | `concurrency-proof.yml` skip-guard | high | mitigate | Task 1: the existing skip-guard is preserved byte-identically, and the NEW `queueproof` step carries an identical one. A green build in which every proof skipped is the failure mode this guard exists to make impossible. |
| T-16-05b | Repudiation | a CI step that runs no proof and still goes green | high | mitigate | Task 1: `set -o pipefail` + pytest's exit **5** (`NO_TESTS_COLLECTED`) already reds the step when a marker selects nothing — so the `[0-9]+ passed` guard is NOT what catches a whole-marker typo (an earlier draft claimed it was; that was **false** and the cross-AI review caught it). Its real job is to survive a **disarmed pipeline**: an `\|\| true` or a dropped `pipefail` swallows pytest's exit code, and the log-content guard still bites. **Known gap, stated not papered over:** it cannot see a typo'd marker on ONE newly-added test while other queueproof tests pass; the per-plan `-m queueproof --collect-only` check is what covers that. |
| T-16-05c | Denial of Service | CI blast radius from widening the gate | high | mitigate | Task 1/3: D-14 forbids whole-suite `-m integration` collection (it would wake 10 dormant live-DB modules against a shared Postgres with a destructive module-scope reset). Task 3's no-widening test reds if a future contributor re-widens it. |
| T-16-06 | Denial of Service | `WORKER_COUNT` in `render.yaml` | high | mitigate | Task 3: `WORKER_COUNT=2` is committed, and `WORKER_COUNT + 2 <= 5` is hard-asserted at boot in 16-07 (D-07). A misconfigured deploy refuses to start rather than booting into a state where every request hangs waiting for a connection the workers checked out. |
| T-16-07 | Information Disclosure | `render.yaml` / `.env.example` | medium | mitigate | Task 3: all four queue knobs are non-secret integers committed as `value:`. No secret is added, and no existing `sync: false` entry is converted to a committed value. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages; no `uv add` in this plan. `uv.lock` and `pyproject.toml` are untouched. |
</threat_model>

<verification>
- `uv run pytest -q` green; `uv run ruff check .` and `uv run mypy app` clean.
- The workflow file is valid YAML; the pre-existing pytest line is byte-identical to `master`; a new
  `-m queueproof` step exists with its own two-part skip-guard.
- All three falsifying mutations (new-step guard deletion + marker typo; gate re-widening;
  derivation-comment strip) executed and recorded in the SUMMARY.
</verification>

<success_criteria>
Any `@pytest.mark.queueproof` test added anywhere in `tests/` from this commit forward runs in CI
against a real Postgres, automatically, and reds the build if it skips OR if the selection collects
nothing — with **zero** blast radius onto the 10 dormant `integration`-marked modules, which stay
exactly as dormant as they were (a pre-existing gap, recorded in the roadmap backlog, not this
phase's to fix). The three queue knobs D-08 names are env-driven, and `LEASE_SECONDS` carries its
derivation and the NARROWED (true) statement about what a double-run costs.
</success_criteria>

<output>
Create `.planning/phases/16-queue-substrate-unblocked-webhook/16-02-SUMMARY.md` when done.
</output>
