---
phase: 16-queue-substrate-unblocked-webhook
plan: 05
type: execute
wave: 2
depends_on: ["16-03"]
files_modified:
  - app/db/schema_introspect.py
  - tests/test_job_kind_drift.py
  - tests/test_schema_introspect.py
  - tests/test_health_schema.py
  - tests/test_check_schema_cli.py
autonomous: true
requirements: [QUEUE-05]
must_haves:
  truths:
    - "A CI-enforced guard fails the build if a `jobs.kind` value ever collides with a `payroll_runs.status` value (ROADMAP criterion #5)."
    - "A CI-enforced guard fails the build if `JobKind` drifts from the SQL `CHECK (kind IN (...))` in either direction (ROADMAP criterion #5)."
    - "`/health/schema` covers the `jobs` table, so a live deploy on which `jobs` silently failed to apply is DETECTED rather than reported in_sync (D-12)."
  artifacts:
    - "app/db/schema_introspect.py — `jobs` registered in expected_schema().tables."
    - "tests/test_job_kind_drift.py — the collision guard, the inline-CHECK drift guard, and the jobs-DDL inventory guard."
  key_links:
    - "The inline-CHECK parser is NEW code. `schema_introspect._do_block_check_values` only understands the DO-block migration pattern, which jobs.kind deliberately does not use — calling it here raises ValueError, it does not silently pass."
    - "The dispatch-table half of Proof 5 (`set(JobKind) == set(HANDLERS)`) is APPENDED to this same file by plan 16-06, once app/queue/dispatch.py exists."
  falsifying_mutations:
    - "Add a JobKind member with no corresponding value in the SQL CHECK -> test_job_kind_check_matches_python_enum MUST go red."
    - "Add a value to the SQL CHECK with no JobKind member -> the SAME test MUST go red (the assertion is set EQUALITY, in both directions)."
    - "Rename a JobState member's value to the string RunStatus.ERROR already owns -> the collision guard MUST go red."
    - "D-12: remove `jobs` from expected_schema().tables -> the coverage test MUST go red. Without it, a live deploy missing the `jobs` table reports {'status': 'in_sync'}."
---

<objective>
Make ROADMAP criterion #5 machine-enforced, and close the live-drift blind spot on the newest,
most concurrency-critical table in the schema.

Criterion #5: *a CI-enforced guard fails the build if a `jobs.kind` value ever collides with a
`payroll_runs.status` value or drifts from the `JobKind` enum — the job row can never encode "what
payroll status comes next."* That is INVARIANT J-1 turned into a tripwire.

Plus **D-12**: research CONFIRMED by direct read that `/health/schema` does NOT auto-cover a new
table — `expected_schema()` hardcodes exactly `payroll_runs` and `email_messages`. Leaving `jobs`
uncovered means a Render deploy on which the `jobs` table silently failed to apply would still
report `in_sync`.

Purpose: QUEUE-05's guard, and live-DB drift coverage for the table the whole phase depends on.
Output: `jobs` registered with `schema_introspect`, and `tests/test_job_kind_drift.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-CONTEXT.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-RESEARCH.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-PATTERNS.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Owning plan |
|---|---|
| `app/routes/webhook.py` off-loop ingest; `tests/test_webhook_unblocked.py` | 16-01 |
| `queueproof` marker + the NARROW CI gate (D-14; D-04 superseded); the four queue `Settings` knobs | 16-02 |
| `app/models/job.py` (`JobKind`/`JobState`/`Job`); `jobs` table; `bootstrap._DROP_ORDER`; D-05 index guard | 16-03 |
| `app/db/repo/jobs.py`; `clear_reply_context -> int`; `rewind_for_reclaim`; conftest fakes; Proof 3 | 16-04 |
| **`app/db/schema_introspect.py` covers `jobs` (D-12); `tests/test_job_kind_drift.py` (Proof 5)** | **16-05** |
| `app/queue/wake.py`, `dispatch.py`, `handlers/pipeline.py`, `drain.py` | 16-06 |
| `app/queue/worker.py`, `app/db/supabase.py::POOL_MAX_SIZE`, `app/main.py` lifespan | 16-07 |
| `app/routes/runs.py::_claim_stale_in_flight`; retrigger -> `enqueue_job` | 16-08 |
| `tests/test_queue_durability.py` Proof 2 | 16-09 |
| `app/db/repo/emails.py::get_unconfirmed_outbound`; `app/pipeline/send_guard.py` (`UnconfirmedSendError`, `assert_no_unconfirmed_send`); the guard call in `clarify()` + `deliver()` | **16-10** (D-13, wave 3) |

<tasks>

<task type="auto">
  <name>Task 1: Register `jobs` with /health/schema's column-drift probe (D-12)</name>
  <files>app/db/schema_introspect.py</files>
  <read_first>
    - `app/db/schema_introspect.py` (FULL FILE). Specifically:
      `_create_body` (`:41`) — already generic and parenthesis-balanced;
      `_columns_for_table` — already generic, parameterized by table name;
      `expected_schema()` (`:134-151`) — the `tables` dict, HARDCODED to exactly
      `payroll_runs` and `email_messages`;
      `_live_columns` — generic, parameterized by table name;
      `diff_against_live`'s Q3 CHECK-value query (`:221-242`) — HARDCODED to the same two tables via
      a `CASE WHEN c.conrelid = to_regclass('public.payroll_runs') THEN 'status' ELSE 'purpose' END`
      branch and an `IN (...)` filter.
    - `app/routes/health.py:50-73` — the `/health/schema` endpoint that consumes `diff_against_live`.
    - `16-RESEARCH.md` § Pitfall 2 (the confirmed finding, with the exact reason a new table is NOT
      auto-covered).
    - `16-CONTEXT.md` D-12.
    - `tests/test_schema_introspect.py`, `tests/test_health_schema.py`, `tests/test_check_schema_cli.py`
      — the three existing consumers of `expected_schema()`/`diff_against_live`. Verified: they assert
      column MEMBERSHIP (`"status" in exp.tables["payroll_runs"]`), not an exact key count, so adding
      a third table is safe. BUT `diff_against_live`'s `missing_columns` dict will now carry a `jobs`
      key, which flows into the `/health/schema` response body. Check whether any of these three pins
      that response shape exactly; if so, update it to include `jobs` rather than working around the
      registration.
  </read_first>
  <action>
Add `"jobs": frozenset(_columns_for_table(sql, "jobs"))` to `expected_schema()`'s `tables` dict.
That is the ONLY change needed for column coverage — `_live_columns` is already generic, so the
column-diff half of `diff_against_live` generalizes for free.

**Do NOT extend the Q3 CHECK-value drift query to `jobs`, and comment WHY.** Two independent reasons:
(a) that query's `CASE WHEN conrelid = payroll_runs THEN 'status' ELSE 'purpose' END` shape can only
express two tables — a third would need a genuinely different query, not a wider `IN` list; and
(b) `jobs.kind`/`jobs.state` are declared as INLINE CHECKs inside `CREATE TABLE jobs`, not via the
DO-block migration pattern that `_do_block_check_values` parses, so the expected-side parser it feeds
would raise `ValueError` on them anyway.

ROADMAP criterion #5 requires a **CI guard**, not a live health probe, and that guard is Task 2. The
value of D-12 is narrower and worth stating in the comment: `/health/schema` is the live-DB drift
probe, and leaving the newest, most concurrency-critical table entirely uncovered means a `jobs`
table that silently failed to apply on Render would go undetected while the endpoint reports
`in_sync`. Column-presence coverage catches exactly that, and it is a ten-line add.

Write the boundary into the comment so a future reader does not "fix" the omission by force-fitting
`jobs` into the two-table CASE expression: kind/state VALUE drift is covered statically by
`tests/test_job_kind_drift.py`; only COLUMN drift is covered live.
  </action>
  <verify>
    <automated>uv run python -c "from app.db.schema_introspect import expected_schema; t = expected_schema().tables; assert 'jobs' in t, sorted(t); assert {'id','kind','dedup_key','state','attempts','max_attempts','lease_token','leased_until','available_at'} <= set(t['jobs']), sorted(t['jobs']); assert t['payroll_runs'] and t['email_messages']; assert expected_schema().status_values and expected_schema().purpose_values; print('jobs covered:', len(t['jobs']), 'cols')" && uv run pytest tests/test_schema_introspect.py tests/test_health_schema.py tests/test_check_schema_cli.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The command above succeeds (`jobs` is registered, its key columns are parsed, and the two
      pre-existing tables plus both DO-block value sets are still resolved).
    - `expected_schema()` still returns valid `status_values` and `purpose_values` (the DO-block
      parsers are untouched and still find their two constraint names).
    - The Q3 CHECK-value query in `diff_against_live` is UNCHANGED, with a comment explaining why
      `jobs` is deliberately not added to it.
    - With a live DB: `GET /health/schema` still returns `in_sync` after `bootstrap` (i.e. the new
      expected columns genuinely exist live, and registration did not introduce a false-positive
      drift alarm).
    - `uv run mypy app/db/schema_introspect.py` clean.
  </acceptance_criteria>
  <done>A live deploy that silently failed to create the `jobs` table now trips `/health/schema` instead of reporting healthy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proof 5 — the kind/status collision guard and the JobKind drift guard</name>
  <files>tests/test_job_kind_drift.py</files>
  <behavior>
    - `{m.value for m in JobKind} & {m.value for m in RunStatus}` is empty. Same for JobState.
    - The set of values parsed from the INLINE `CHECK (kind IN (...))` in `CREATE TABLE jobs` equals `{m.value for m in JobKind}` — set EQUALITY, both directions.
    - The same holds for `CHECK (state IN (...))` and `JobState`.
    - `jobs`' DDL inventory is pinned: `uq_jobs_dedup_key`, `ck_jobs_lease_coherent`, `idx_jobs_claimable` all present; the CREATE body has no cascading delete and no `event_id`.
    - `"jobs"` precedes `"payroll_runs"` in `bootstrap._DROP_ORDER`, and `"jobs"` is in `expected_schema().tables`.
  </behavior>
  <read_first>
    - `tests/test_status_drift.py` (FULL FILE) — the exact analog. Mirror its shape: a pure static
      file test, no DB import, a `_SCHEMA_SQL.read_text()` at module scope, comment-stripping
      normalization before parsing, and the `test_no_db_connection_needed` self-guard.
    - `app/db/schema_introspect.py::_create_body` (`:41`) — generic and parenthesis-balanced; reuse it
      to extract the `jobs` CREATE body. `tests/` is intentionally NOT scanned by the BOUND-01 guard
      (read its module docstring: *"tests routinely reach into a module's own internals... outside
      this guard's cross-module scope"*), so importing this private helper from a test is legitimate.
      If it turns out not to fit, write a local balanced-paren extractor IN THE TEST FILE — do NOT add
      a public wrapper to `app/` merely to satisfy a test.
    - `app/db/schema_introspect.py::_do_block_check_values` — read it and confirm for yourself that it
      searches for a constraint-name literal FOLLOWED BY a `CHECK (col IN (...))`, i.e. the executable
      DO-block re-add form. `jobs.kind` has no such DO-block. **Do not call this helper here.**
    - `16-RESEARCH.md` § Proof 5 and § "The NEW job-kind drift test needs its own inline-CHECK parser".
    - `app/models/job.py`, `app/models/status.py`.
  </read_first>
  <action>
Create `tests/test_job_kind_drift.py` — hermetic, static-file parsing, no DB, no `integration` marker.

**A NEW inline-CHECK parser is required.** `jobs.kind`'s CHECK is the first `CHECK (kind IN (...))`
inside the `CREATE TABLE jobs (...)` body — there is no named DO-block re-add to anchor on. Extract
the table body with `_create_body(sql, "jobs")` and run a plain regex for
`CHECK\s*\(\s*kind\s+IN\s*\((.*?)\)\s*\)` against it, splitting on commas and stripping quotes.
This is a genuinely different, simpler parser than the DO-block one — not the same helper with
different arguments.

Tests:

1. `test_job_kind_never_collides_with_run_status` — the intersection of `JobKind` values and
   `RunStatus` values is empty. Failure message must name the colliding value: a `jobs.kind` that
   equals a `payroll_runs.status` is INVARIANT J-1 violated at the vocabulary level, which is exactly
   what criterion #5 forbids.

2. `test_job_state_never_collides_with_run_status` — same for `JobState`. Criterion #5 names `kind`,
   but extending the guard to `state` is free and closes the specific trap: a future `JobState.ERROR`
   would take the string `RunStatus.ERROR` already owns.

3. `test_job_kind_check_matches_python_enum` — the parsed inline-CHECK value set EQUALS
   `{m.value for m in JobKind}`. Set **equality**, both directions: an enum member with no CHECK
   value, and a CHECK value with no enum member, must EACH fail independently.

4. `test_job_state_check_matches_python_enum` — same for `state`.

5. `test_jobs_ddl_inventory_is_pinned` — assert `uq_jobs_dedup_key`, `ck_jobs_lease_coherent` and
   `idx_jobs_claimable` all appear in `schema.sql`; assert the `jobs` CREATE body contains no
   cascading-delete clause (a region-scoped check against the `jobs` body ONLY — other tables in this
   schema legitimately cascade, so a file-wide check would be a false positive); assert no `event_id`
   column exists (its FK target `inbound_events` does not exist until Phase 19).

6. `test_bootstrap_drop_order_includes_jobs` — `"jobs"` is in `bootstrap._DROP_ORDER` at an index
   strictly less than `"payroll_runs"` (Pitfall 4's regression guard).

7. `test_health_schema_covers_jobs` — `"jobs"` is a key of `expected_schema().tables` (D-12's
   regression guard). Without this, the Task-1 registration is one careless refactor from silently
   disappearing.

8. `test_no_db_connection_needed` — copy `test_status_drift.py`'s self-guard so this file can never
   quietly become a live-DB test.

The dispatch-table half of Proof 5 (`set(JobKind) == set(dispatch.HANDLERS)`) is appended to THIS file
by plan **16-06**, once `app/queue/dispatch.py` exists. Leave a clearly-marked placeholder comment at
the bottom naming that plan, so 16-06's executor appends rather than creating a second file.

Write all four falsifying mutations into the module docstring and EXECUTE each one, pasting the red
runs into the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_job_kind_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_job_kind_drift.py -q` passes with at least 8 tests.
    - The file imports no DB module and opens no connection (its own `test_no_db_connection_needed`
      asserts this).
    - The DO-block parser is never CALLED from this file (gate on the call, not on a prose mention, so
      the docstring may explain why it is the wrong tool):
      `grep -cE '_do_block_check_values\s*\(' tests/test_job_kind_drift.py` == 0.
    - The kind/state CHECK assertions are set EQUALITY (`==`), never `<=` / `>=` / `issubset`.
    - FOUR FALSIFYING MUTATIONS EXECUTED, each red-run pasted into the SUMMARY:
      (a) add a `JobKind` member absent from the SQL CHECK;
      (b) add a value to the SQL CHECK absent from `JobKind`;
      (c) rename a `JobState` member's value to a string `RunStatus` already owns;
      (d) remove `"jobs"` from `expected_schema().tables`.
  </acceptance_criteria>
  <done>ROADMAP criterion #5 is machine-enforced in CI, in both drift directions, and each half of the guard is demonstrably able to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Committed schema -> live Render database | `bootstrap` applies `schema.sql`; `/health/schema` is the only live drift probe. |
| Queue vocabulary -> business vocabulary | INVARIANT J-1: a `jobs.kind` must never be able to name a `payroll_runs.status`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-21 | Elevation of Privilege | `jobs.kind` naming a payroll status | high | mitigate | Task 2: the collision guard fails CI if any `JobKind`/`JobState` value equals a `RunStatus` value. This is criterion #5 turned into a tripwire — a job row that could encode "advance to APPROVED" is the whole thing J-1 forbids. |
| T-16-22 | Tampering | `JobKind` <-> SQL CHECK drift | high | mitigate | Task 2: set-EQUALITY assertion in both directions, parsed with a purpose-built inline-CHECK parser. A phantom kind with no CHECK value (or a CHECK value with no enum member) fails independently. |
| T-16-23 | Repudiation | undetected live schema drift on `jobs` | medium | mitigate | Task 1: `jobs` registered in `expected_schema().tables`, so a deploy on which the table silently failed to apply trips `/health/schema` (503) instead of reporting `in_sync`. `test_health_schema_covers_jobs` guards the registration itself. |
| T-16-24 | Repudiation | guard rot via a weakened assertion | medium | mitigate | Task 2: acceptance explicitly forbids `⊇`/`issubset` in place of `==`. A pre-loosened guard is the failure mode Pitfall 7 names. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages; no `uv add` in this plan. |
</threat_model>

<verification>
- `uv run pytest -q` fully green.
- `uv run mypy app` / `uv run ruff check .` clean.
- With a live DB, `GET /health/schema` returns `in_sync` after bootstrap (no false-positive drift).
- All four falsifying mutations executed with red runs pasted into the SUMMARY.
</verification>

<success_criteria>
ROADMAP criterion #5 is TRUE: a CI-enforced guard fails the build if a `jobs.kind` value collides with
a `payroll_runs.status` value or drifts from the `JobKind` enum. And `/health/schema` now sees the
`jobs` table.
</success_criteria>

<output>
Create `.planning/phases/16-queue-substrate-unblocked-webhook/16-05-SUMMARY.md` when done.
</output>
