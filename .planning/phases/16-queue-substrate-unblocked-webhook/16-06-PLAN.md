---
phase: 16-queue-substrate-unblocked-webhook
plan: 06
type: execute
wave: 3
depends_on: ["16-04", "16-05"]
files_modified:
  - app/queue/__init__.py
  - app/queue/wake.py
  - app/queue/dispatch.py
  - app/queue/handlers/__init__.py
  - app/queue/handlers/pipeline.py
  - app/queue/drain.py
  - tests/test_queue_drain.py
  - tests/test_job_kind_drift.py
autonomous: true
requirements: [QUEUE-02, QUEUE-05]
must_haves:
  truths:
    - "`drain_once()` claims one job, dispatches it by kind, and completes or fails it — FENCED on the lease token, in a claim transaction that commits BEFORE any pipeline work begins (QUEUE-02)."
    - "INVARIANT J-1 holds under its PRECISE restatement (see the objective): the ONLY business-status writers reachable from `app/queue/` are the two named CAS writes — `claim_status` (the sole forward transition) and `rewind_for_reclaim` (the sole, `attempts > 1`-gated, backward recovery transition). A static guard fails CI on any third writer, and on any `repo.set_status` call from the queue tier."
    - "That static guard FAILS CLOSED (D-18): it resolves repo import aliases and REJECTS any construct it cannot statically resolve — module re-binding (`r = repo`), `getattr` on a repo name, passing the repo module as an argument, importing functions out of `app.db.repo.*`, and `importlib`/`__import__`. A name-matching guard is green on every one of those bypasses; this one is not."
    - "A FAILED forward CAS is a DONE job, not a retry (QUEUE-05)."
    - "D-01: on a reclaim (`attempts > 1`) the handler rewinds a mid-pipeline run to RECEIVED first, so a crashed retrigger genuinely re-runs instead of being stranded forever by a CAS it can never win."
    - "D-02: that rewind never bumps `reply_epoch` — the machine does not grant itself the licence to email the client twice."
    - "`set(JobKind) == set(dispatch.HANDLERS)` is CI-enforced (ROADMAP criterion #5, dispatch half)."
    - "The KNOWN pre-FAIL-01 gap (a swallowed catastrophic START failure marks the job `done` while the run never ran) is PINNED by a named test, so Phase 18 has a red-to-green target instead of a paragraph."
  prohibitions:
    - "MUST NOT write that 'a double-run is harmless' or that 'delivery's already-sent guard blocks the second confirmation'. False until D-13's guard lands (16-10). Cite `app/pipeline/send_guard.py` instead."
    - "MUST NOT call `repo.set_status` from anywhere under `app/queue/`. Business-status writes from the transport tier are CAS-only, and the guard test enforces it."
    - "MUST NOT describe this phase's guarantee as 'durable execution' without qualification. Phase 16's honest claim is narrower — see the residual-risk block in Task 3."
  artifacts:
    - "app/queue/wake.py — wake(), wait(timeout), clear()."
    - "app/queue/dispatch.py — HANDLERS, handle(job)."
    - "app/queue/handlers/pipeline.py — handle_run_pipeline(job)."
    - "app/queue/drain.py — drain_once(), held_tokens(), _backoff_seconds()."
    - "tests/test_queue_drain.py — hermetic drain / J-1 / D-01 proofs, the J-1 CAS-only static guard, and the pre-FAIL-01 pin test."
  key_links:
    - "drain_once() is the ONE function the worker threads (16-07) AND the Phase-17 pump both call. If it lived inside worker.py, the pump would have to import thread machinery to run a single job."
    - "orchestrator.py's unconditional set_status(EXTRACTING) MUST NOT be deleted in this phase — it is the ONLY thing advancing a plainly-ingested (non-retriggered) run out of `received`, and that path stays on BackgroundTasks until Phase 19."
    - "The handler's rewind is safe for the CLIENT only because D-13's send guard (16-10, wave 3) lands BEFORE the first live worker (16-07, wave 4). Without it, a rewind-and-re-run after a crash mid-send emails the client twice."
  falsifying_mutations:
    - "J-1: make the handler re-raise instead of returning cleanly when the RECEIVED->EXTRACTING CAS fails. The J-1 test MUST go red — a failed CAS is a DONE job, not a retry."
    - "J-1 CAS-only guard: add a `repo.set_status(job.run_id, RunStatus.EXTRACTING)` call to the handler. test_queue_tier_status_writers_are_cas_only MUST go red and MUST name `set_status`."
    - "D-18 (the guard must FAIL CLOSED — run ALL THREE bypasses, each independently, each MUST go red): (i) `r = repo` then `r.set_status(...)` — red on the module-rebinding rule; (ii) `getattr(repo, 'set_status')(...)` — red on the getattr rule; (iii) `from app.db.repo.runs import set_status` at the top of a queue module then a bare `set_status(...)` call — red on the function-import rule. A name-matching guard passes ALL THREE, which is exactly why the round-1 version was decorative."
    - "D-01: remove the `attempts > 1` rewind preamble. The reclaim test MUST go red: the run stays at EXTRACTING, the CAS can never win, the job goes done, and the run is stranded forever — exactly what ROADMAP criterion #2 forbids."
    - "D-01 over-reach: change the rewind guard from `attempts > 1` to unconditional. The `attempts == 1` no-op test MUST go red — a first-attempt CAS failure means another actor legitimately advanced the run, and rewinding it would clobber live work."
    - "Criterion #5: add a JobKind member with no HANDLERS entry. test_job_kind_equals_dispatch_table MUST go red."
---

<objective>
Build `app/queue/` — the execution layer between the durable rows (16-04) and the worker threads
(16-07): the in-process wake signal (D-09), the kind-to-handler dispatch table, the `run_pipeline`
handler carrying **D-01's reclaim rewind** and **INVARIANT J-1's CAS**, and `drain_once()` — the one
function the worker threads and the Phase-17 pump will both call.

**The hardest thing here is D-01, and it is money-adjacent.** The trap: an operator retriggers, the
CAS lands the run at `RECEIVED`, worker W1 claims the job and drives the run to `EXTRACTING`/
`COMPUTED` — and then W1's process dies. The lease expires, W2 reclaims the job and calls the handler
against a run that is no longer at `RECEIVED`. Under a naive reading of J-1 ("a failed CAS is a DONE
job") the job goes `done` and **the run is stranded forever** — precisely what ROADMAP criterion #2
forbids. The discriminator is `attempts`, which QUEUE-02 already increments AT CLAIM, so the signal is
free: `attempts > 1` means "the previous me died halfway" (rewind, re-run); `attempts == 1` means
"someone else legitimately advanced this" (drop cleanly — strict J-1, preserved where it applies).

Purpose: turn a durable row into durable work, without ever letting the transport tier decide what
payroll status comes next.
Output: the `app/queue/` package and its hermetic proofs.

## INVARIANT J-1 — the PRECISE restatement (resolves a blocking review finding)

> **⚠ The old wording was self-contradictory and the cross-AI review was right to block on it.**
> It said the handler's "first durable action" must be `claim_status(RECEIVED → EXTRACTING)` **and**
> that "only the CAS may advance business state" — while ALSO requiring `rewind_for_reclaim()` to run
> **first** when `attempts > 1`. But `rewind_for_reclaim` writes `payroll_runs.status`. So it is a
> second business-state writer, and it runs *before* the "first" durable action. As written, 16-06's
> own "first durable action" test and its "only the CAS advances status" test **could not both pass.**

**J-1, restated. This is the version that goes in the code, in `handle_run_pipeline`'s docstring, and
in the guard test's docstring:**

> `payroll_runs.status` has exactly **TWO** permitted writers reachable from the queue tier, and both
> are **compare-and-swap** writes — a conditional `UPDATE ... WHERE status IN (...) RETURNING id`
> whose `False`/`None` return means *"someone else owns this; drop cleanly."* There is no third
> writer, and there is no unconditional `set_status` from `app/queue/`.
>
> 1. **`claim_status(expected → next)` — the sole FORWARD transition.** A lost forward CAS is a
>    **DONE job, not a retry.** The transport tier never decides what payroll status comes next; it
>    only asks, and accepts *no* for an answer.
>
> 2. **`rewind_for_reclaim(run_id)` — the sole, explicitly-named RECOVERY transition.** It is
>    **not a forward transition**: it moves *backward* (`extracting|computed|sent → received`), it is
>    itself a CAS (so it is exclusive and cannot clobber a run outside its three-status scope), it is
>    permitted **only when `job.attempts > 1`** (i.e. only on a genuine crash-reclaim), and it never
>    bumps `reply_epoch` (D-02).

**Therefore "first durable action" is a *conditional*, and both tests are satisfiable:**
- `attempts == 1` → first durable action is `claim_status`. (Strict J-1, unchanged.)
- `attempts > 1` → first durable action is `rewind_for_reclaim`, which is itself a CAS; `claim_status`
  follows immediately.

Both branches obey the single invariant that actually matters: **every business-status write from the
transport tier is a CAS, and the tier drops cleanly whenever it loses one.** Task 3 turns that into a
static guard (`test_queue_tier_status_writers_are_cas_only`) whose permitted-writer set is exactly
`{claim_status, rewind_for_reclaim}` — so a future third writer, or any `repo.set_status` call from
`app/queue/`, fails CI **by name**.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-CONTEXT.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-RESEARCH.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-PATTERNS.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-04-SUMMARY.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-05-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Owning plan |
|---|---|
| `app/routes/webhook.py` off-loop ingest; `tests/test_webhook_unblocked.py` | 16-01 |
| `queueproof` marker + the NARROW CI gate (D-14; D-04 superseded); the four queue `Settings` knobs | 16-02 |
| `app/models/job.py` (`JobKind`/`JobState`/`Job`); `jobs` table; `_DROP_ORDER`; D-05 index guard | 16-03 |
| `app/db/repo/jobs.py` (`enqueue_job`/`claim_job`/`complete_job`/`fail_job`/`release_leases`/`get_job`); `clear_reply_context -> int`; `rewind_for_reclaim`; conftest fakes; Proof 3 | 16-04 |
| `schema_introspect` covers `jobs`; `tests/test_job_kind_drift.py` | 16-05 |
| **`app/queue/wake.py` (`wake`/`wait`/`clear`); `app/queue/dispatch.py` (`HANDLERS`/`handle`); `app/queue/handlers/pipeline.py` (`handle_run_pipeline`); `app/queue/drain.py` (`drain_once`/`held_tokens`)** | **16-06** |
| `app/queue/worker.py` (`start`/`stop`/`lifespan`); `app/db/supabase.py::POOL_MAX_SIZE`; `app/main.py` lifespan | 16-07 |
| `app/routes/runs.py::_claim_stale_in_flight`; retrigger -> `enqueue_job` | 16-08 |
| `tests/test_queue_durability.py` Proof 2 | 16-09 |
| `app/db/repo/emails.py::get_unconfirmed_outbound`; `app/pipeline/send_guard.py` (`UnconfirmedSendError`, `assert_no_unconfirmed_send`); the guard call in `clarify()` + `deliver()` | **16-10** (D-13, wave 3) |

<tasks>

<task type="auto">
  <name>Task 1: app/queue/wake.py and app/queue/dispatch.py — the wake signal and the kind-to-handler table</name>
  <files>app/queue/__init__.py, app/queue/wake.py, app/queue/dispatch.py, app/queue/handlers/__init__.py</files>
  <read_first>
    - `app/routes/pipeline_glue.py:1-8` — the BOUND-01 module-object import discipline, verbatim.
      This is what `dispatch.py` must copy: a bare-name import binds the function object at import
      time, and the tests' `monkeypatch.setattr(...)` seams then silently stop taking effect.
    - `tests/test_bound01_private_imports.py` — module docstring and `SCAN_ROOTS`. `SCAN_ROOTS` is
      `["app", "eval", "scripts"]`, so the brand-new `app/queue/` package is auto-scanned from its
      first commit. The ONE declared cross-module-private exemption is `app/db/repo/`; nothing in
      `app/queue/` may reference a private name belonging to another module.
    - `app/db/supabase.py:1-18` — the Supavisor transaction-mode rationale. `LISTEN/NOTIFY` and
      session advisory locks are unavailable here and fail SILENTLY, not loudly. That is what forces
      an in-process Event rather than a DB notification.
    - `16-CONTEXT.md` D-09.
    - `app/models/job.py` — `JobKind`, `Job`.
  </read_first>
  <action>
Create the package: `app/queue/__init__.py` and `app/queue/handlers/__init__.py` (docstring only — do
NOT re-export symbols from `__init__.py`; keep every import surface explicit and greppable).

**`app/queue/wake.py`** — the D-09 in-process wake signal. A module-level `threading.Event` plus
three functions: `wake() -> None` (set it), `wait(timeout: float) -> bool` (block until set or
timeout; returns whether it was signalled), and `clear() -> None`.

Why its own module rather than a member of `worker.py`: the PRODUCER (the retrigger route, plan
16-08) and the CONSUMER (the worker threads, plan 16-07) both need it, and an HTTP route should not
have to import thread-lifecycle machinery just to say "there is work." Keeping the signal separate is
also what lets 16-07 and 16-08 be built in parallel.

The docstring must carry D-09's reasoning: the approved design reaches for DB polling because
`LISTEN/NOTIFY` is unavailable under Supavisor transaction-mode pooling — but never notices that in
this deployment the producer and the consumer live **in the same process**. Setting an `Event` after
the enqueue transaction **commits** wakes an idle worker immediately, keeping Retrigger instant for
the 60-90s demo with zero DB chatter against a 5-connection budget. **The wake must fire strictly
AFTER commit, never inside the transaction** — fire it inside and the woken worker races ahead of the
commit, finds no row, and goes back to sleep. State that trap explicitly; it is the one way to get
this wrong. The DB poll (`QUEUE_POLL_SECONDS`, 16-02) is demoted to a slow durable fallback covering
only what an in-process signal cannot: expired-lease reclaims, future-dated backoff retries (Phase
18), and a cold-started instance where the enqueuing process no longer exists.

**`app/queue/dispatch.py`** — the kind-to-handler table.
Import the handler as a MODULE OBJECT (`from app.queue.handlers import pipeline`), never
`from app.queue.handlers.pipeline import handle_run_pipeline`. Build `HANDLERS` so the handler is
resolved by **attribute lookup at dispatch time**, not bound as a function object at import time: map
each `JobKind` to a `(module, function_name)` pair, and have `handle(job: Job) -> None` perform
`getattr(module, name)(job)`. A dict of bound function objects would make
`monkeypatch.setattr(pipeline, "handle_run_pipeline", stub)` a silent no-op — the exact seam failure
BOUND-01 exists to prevent, and the tests in Task 3 depend on that seam being live.

`HANDLERS` has **exactly one entry** in this phase: `JobKind.RUN_PIPELINE`. Not four (Pitfall 7) —
criterion #5's guard is set EQUALITY against `JobKind`, and it must be satisfiable rather than
permanently red.

`handle()` must RAISE on an unknown kind, never silently no-op. A job that is marked `done` without
ever having run is the worst possible outcome: it looks exactly like success.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bound01_private_imports.py -q && uv run python -c "from app.queue import dispatch, wake; from app.models.job import JobKind; assert set(dispatch.HANDLERS) == set(JobKind), (set(dispatch.HANDLERS), set(JobKind)); wake.clear(); assert wake.wait(0.01) is False; wake.wake(); assert wake.wait(0.01) is True; wake.clear(); print('dispatch + wake ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `set(dispatch.HANDLERS) == set(JobKind)` — exactly one entry, `JobKind.RUN_PIPELINE`.
    - `dispatch.HANDLERS` stores `(module, name)` pairs, NOT bound function objects — verify that
      `monkeypatch.setattr(pipeline, "handle_run_pipeline", stub)` causes `dispatch.handle(job)` to
      call the stub (Task 3 asserts this directly).
    - `dispatch.handle` raises on an unknown kind (assert with a synthetic `Job` carrying a bogus kind).
    - `uv run pytest tests/test_bound01_private_imports.py -q` passes — the new `app/queue/` package is
      auto-scanned and contains no cross-module private reference.
    - `uv run mypy app/queue` clean under strict; `uv run ruff check app/queue` clean.
  </acceptance_criteria>
  <done>The wake signal exists as a standalone primitive both the producer and the consumer can reach, and the dispatch table resolves handlers through a live monkeypatch seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: app/queue/handlers/pipeline.py — the D-01 reclaim rewind and INVARIANT J-1's CAS</name>
  <files>app/queue/handlers/pipeline.py</files>
  <behavior>
    - attempts == 1, run at RECEIVED: the CAS RECEIVED->EXTRACTING wins, and `pipeline_glue.run_pipeline_bg(run_id)` is called exactly once.
    - attempts == 1, run at COMPUTED (another actor advanced it): NO rewind happens, the CAS loses, `run_pipeline_bg` is NOT called, and the handler returns cleanly (job -> done). Strict J-1.
    - attempts == 2, run at EXTRACTING (a crashed worker's reclaim): `rewind_for_reclaim` fires FIRST, the run returns to RECEIVED, the CAS then wins, and `run_pipeline_bg` IS called.
    - attempts == 2, run at RECONCILED (the pipeline actually finished; only the completion write was lost): the rewind is a no-op (RECONCILED is outside its scope), the CAS loses, `run_pipeline_bg` is NOT called. A finished run is never re-run.
    - `reply_epoch` is unchanged across every path above (D-02).
  </behavior>
  <read_first>
    - `app/routes/pipeline_glue.py:209-224` — `run_pipeline_bg`. Read it: it is a thin safety net that
      calls `orchestrator.run_pipeline` and swallows a catastrophic START failure. **This is the
      function the handler calls** — not `orchestrator.run_pipeline` directly — because every existing
      test's `monkeypatch.setattr(pipeline_glue, "run_pipeline_bg", ...)` seam then keeps working, which
      is what makes plan 16-08's test migration a two-line change instead of a rewrite.
    - `app/pipeline/orchestrator.py:195-240` — `run_pipeline` -> `_run`. **Confirm for yourself that
      `_run` never raises (it owns a catch-all that persists ERROR), and note the UNCONDITIONAL
      `repo.set_status(run_id, RunStatus.EXTRACTING)` at ~`:232`.**
    - `app/db/repo/pipeline_state.py::rewind_for_reclaim` (created in 16-04) — read its docstring for
      the exact three-status scope and the D-02 argument.
    - `app/db/repo/runs.py::claim_status` — the CAS whose False return is J-1's "drop cleanly" signal.
    - `16-CONTEXT.md` D-01 and D-02 (in full). `16-RESEARCH.md` § Pitfall 6.
  </read_first>
  <action>
Create `app/queue/handlers/pipeline.py` with one public function, `handle_run_pipeline(job: Job) -> None`.

**Open its docstring with the PRECISE J-1 restatement from this plan's objective** — the two permitted
CAS writers, and why the rewind is a *recovery* transition rather than a violation. An executor who
reads only the old wording will write two mutually-unsatisfiable tests.

Import `pipeline_glue`, `repo`, and `RunStatus` as module objects (BOUND-01). Logic, in exactly this
order:

1. **D-01 rewind preamble — guarded by `if job.attempts > 1:`. This is J-1's permitted RECOVERY
   transition, not an exception to J-1.** Call `repo.rewind_for_reclaim(job.run_id)`.
   This is a reclaim after a crash: the previous holder of this job died mid-pipeline, so the run may
   be sitting at `extracting`/`computed`/`sent` and the forward CAS below could never win.
   `rewind_for_reclaim` is itself a **CAS** (a conditional UPDATE scoped to exactly those three
   statuses, returning the row or nothing) — which is why it satisfies the restated invariant: the
   queue tier still never writes a status unconditionally, and it still drops cleanly when it loses.
   It returns the run to `RECEIVED`, clears derived pipeline state, and does NOT bump `reply_epoch`
   (**D-02**). Log whether it actually claimed a row; a `False` return simply means the run was
   outside the rewind's three-status scope (a legitimate resting point, a terminal state, or ERROR),
   which the forward CAS below then handles correctly.

   **What makes the re-run safe for the CLIENT — state it accurately, do not overclaim.** A re-run is
   idempotent for *pipeline state* (`claim_status`' CAS advances at-most-once; `replace_line_items` is
   DELETE-then-INSERT). It is safe for the *client-facing email* because **D-13's fail-closed send
   guard** (`app/pipeline/send_guard.py`, plan **16-10**, wave 3 — it lands BEFORE the first live
   worker in wave 4) refuses to re-send when an unconfirmed (`reserved`/`failed`) outbound row exists
   in this run's current epoch, and escalates to the operator instead. **D-02's epoch stability is
   what keeps that guard able to SEE the row.** Do NOT write "delivery's already-sent guard suppresses
   the second confirmation" — as shipped, that guard counts only `send_state='sent'`, so a worker
   killed between provider-acceptance and the sent-commit leaves it blind. That was the cross-AI
   review's HIGH finding and it is false.

2. **`if not repo.claim_status(job.run_id, RunStatus.RECEIVED, RunStatus.EXTRACTING): return`** —
   J-1's sole FORWARD transition. A failed forward CAS is a **DONE job, not a retry**: another actor
   legitimately advanced the run (or it is at a resting/terminal state we must not disturb). Log and
   return cleanly; `drain_once` will mark the job `done`. **Do NOT raise, do NOT re-enqueue, do NOT
   write an error to the run.** This is the identical contract to `complete_job` returning False for a
   zombie — the repo already has the "lost the CAS, drop cleanly" idiom in four places; this is the
   fifth and it should read the same.

3. **`pipeline_glue.run_pipeline_bg(job.run_id)`** — attribute lookup at call time, preserving the
   monkeypatch seam.

**THREE HARD PROHIBITIONS. Each has bitten a previous phase of this design; each is a trap the
canonical documents actively invite:**

- **DO NOT delete or modify `app/pipeline/orchestrator.py`'s unconditional
  `repo.set_status(run_id, RunStatus.EXTRACTING)` (~`:232`).** The canonical design's general
  integration table lists that deletion, but its OWN phase-by-phase breakdown assigns it to its
  "Phase 4" — this project's **Phase 18**. In Phase 16 the plain webhook path (`new_run` ->
  `run_pipeline_bg` -> `orchestrator.run_pipeline`) still runs on `BackgroundTasks` with NO external
  CAS anywhere, and that line is the ONLY thing that ever moves a plainly-ingested run out of
  `received`. Delete it now and every ordinary, non-retriggered payroll run sits visibly at `received`
  for its entire life. On the retrigger path it simply becomes a harmless same-value redundant write.
  `git diff` for this phase must show ZERO changes to `orchestrator.py`.

- **DO NOT build an `ok`/`retryable`/`terminal` result contract.** That is FAIL-01, Phase 18. A rich
  failure taxonomy would have nothing real to classify yet.

  **BUT — state the residual risk precisely and PIN it with a test. Do not bury it in a deferral
  note.** The cross-AI review is right that the plans' unqualified "durable" language overstates what
  Phase 16 delivers. Trace it: `run_pipeline_bg` (`app/routes/pipeline_glue.py:210-224`) wraps
  `run_pipeline` in `try: ... except Exception: logger.exception(...)` and then **returns normally**.
  `drain_once` treats a normal handler return as success and marks the job `done`. So:

  | Failure | Today's behavior (Phase 16) | Operator-visible? |
  |---|---|---|
  | A **stage** failure (LLM timeout, bad extract) | `orchestrator._run`'s own catch-all persists `ERROR` on the run; the handler returns; job → `done` | **YES** — the run shows `ERROR` with `error_reason` |
  | A **catastrophic START** failure (import error, DB down at `load_run`) | `run_pipeline_bg` swallows it and returns; job → `done`; **the run never advanced** | **Partially** — the run sits visibly at `RECEIVED`, an in-flight status the dashboard already shows; but nothing retries it |

  **Phase 16's honest claim, and the only one the plans may make:** *no accepted retrigger is lost to a
  **process death** — the durable row survives and is reclaimed.* It is **NOT** *"every failure is
  automatically retried"*. That is FAIL-01/FAIL-02, Phase 18, and the roadmap deliberately forces
  Phase 18 before the webhook cutover for exactly this reason.

  **PIN IT (Task 3 writes the test):** `test_swallowed_start_failure_marks_the_job_done_KNOWN_GAP_FAIL01`
  — stub `pipeline_glue.run_pipeline_bg` to behave exactly as it does today (catch an internal
  exception, log, return normally), drain, and assert the job's final state is `done` while the run
  did NOT advance. Docstring, verbatim intent: *"This test PINS a KNOWN GAP, it does not endorse it.
  FAIL-01 (Phase 18) must FLIP this assertion from `done` to `pending`-with-backoff. When it does, this
  test is INVERTED, never deleted — it is FAIL-01's red-to-green target."* Without this pin, Phase 18
  starts from a paragraph; with it, Phase 18 starts from a failing test.

  Dashboard/ops surfacing of dead and swallowed jobs is **OPS-01 (Phase 21)** and is explicitly out of
  scope here (D-10 forbids UI work in this phase). That is a deliberate rejection of the review's
  suggestion to add operator visibility now, and the reason is scope, not disagreement: the run itself
  is already visible in both rows of the table above.

- **DO NOT hold a DB transaction across `run_pipeline_bg`.** The claim already committed (16-04); the
  CAS is its own statement. `orchestrator.py` already documents the rule — no transaction may span a
  network/LLM call — and with `max_size=5`, pinning a connection across a 45s LLM call burns 20% of the
  entire budget per worker. The queue inherits that rule; it does not invent it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_queue_drain.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat app/pipeline/orchestrator.py` is EMPTY for this phase.
    - The handler contains no `raise` on a lost CAS and no re-enqueue.
    - No Phase-18 failure taxonomy is built. Gate on EXECUTABLE code, not on comments (which may
      legitimately explain the deferral): strip comment lines first —
      `grep -vE '^\s*#' app/queue/handlers/pipeline.py | grep -cE '(retryable|terminal)'` == 0.
    - The five behaviors above are each covered by a test in `tests/test_queue_drain.py` (Task 3).
    - `uv run mypy app/queue` clean; `uv run pytest tests/test_bound01_private_imports.py -q` passes.
  </acceptance_criteria>
  <done>The handler rewinds only what a crash actually stranded, never advances business state except through a CAS, and drops cleanly when it loses that CAS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: app/queue/drain.py + the drain/J-1/D-01 proofs + the dispatch half of Proof 5</name>
  <files>app/queue/drain.py, tests/test_queue_drain.py, tests/test_job_kind_drift.py</files>
  <behavior>
    - `drain_once()` on an empty queue returns False and dispatches nothing.
    - `drain_once()` with one pending job: claims it, calls `dispatch.handle`, calls `complete_job` with the SAME lease_token the claim returned, and returns True.
    - While the handler is executing, `held_tokens()` contains that job's token; after completion it does not.
    - When the handler raises, `fail_job` is called (fenced, with a positive backoff) and `complete_job` is NOT.
    - `_backoff_seconds(attempts)` grows exponentially, is capped, and is jittered — and is deterministic when its rand source is stubbed.
  </behavior>
  <read_first>
    - `app/db/repo/jobs.py` (created in 16-04) — `claim_job`, `complete_job`, `fail_job` signatures and
      their fenced/None-return contracts.
    - `.planning/research/ARCHITECTURE.md` §4 — the backoff formula
      (`min(cap, base * 2 ** (attempts - 1)) * uniform(0.5, 1.5)`, computed **in Python** so it is
      unit-testable without a clock).
    - `tests/conftest.py` — `fake_repo` / `InMemoryRepo` (with the job methods added in 16-04). These
      tests are hermetic; the live-DB versions are Proofs 2/3 in 16-04 and 16-09.
    - `16-CONTEXT.md` D-06 (workers are OFF under test; tests call `drain_once()` explicitly — this is
      the exact function the Phase-17 pump and the worker threads both call, so the test proves the
      real path rather than a test-only inline executor).
  </read_first>
  <action>
**`app/queue/drain.py`** — the single drain step, shared by the worker threads (16-07) and the
Phase-17 pump. It lives in its own module, NOT inside `worker.py`, precisely so the pump can execute
one job without importing thread-lifecycle machinery.

`drain_once() -> bool` — returns whether a job was claimed:
1. `job = repo.claim_job()` (lease_seconds from settings). `None` -> return False.
2. Register `job.lease_token` in a module-level, `threading.Lock`-guarded held-token set.
3. `try: dispatch.handle(job)` then `repo.complete_job(job.id, job.lease_token)`.
   `except Exception:` -> `repo.fail_job(job.id, job.lease_token, error=exc, backoff_seconds=_backoff_seconds(job.attempts))`.
4. `finally:` discard the token from the held set.
5. Return True.

Both `complete_job` and `fail_job` are FENCED. A `False`/`None` return means the lease was stolen and
this worker is a zombie: **log and drop.** Do not retry, do not error the run, do not re-enqueue.

`held_tokens() -> list[uuid.UUID]` — a snapshot of the tokens this process currently holds. `worker.stop()`
(16-07) calls `repo.release_leases(...)` with it, which is what makes a redeploy hand work back in
seconds rather than stalling for the full `LEASE_SECONDS`.

`_backoff_seconds(attempts, *, rand=random.uniform)` — the ARCHITECTURE formula, with the rand source
injectable so it is deterministic under test. Module-private and same-module, so BOUND-01 is satisfied.
Note in a comment that in Phase 16 this path fires only for infrastructural failures (a DB blip, an
import error) — `run_pipeline_bg` swallows stage failures, so a pipeline error does NOT reach here.
That changes in Phase 18 (FAIL-01); do not pre-build for it.

**`tests/test_queue_drain.py`** (NEW, hermetic, `fake_repo`): cover all five `drain_once` behaviors
above AND the five `handle_run_pipeline` behaviors from Task 2. Two things that make these
non-vacuous:
- Assert `complete_job` was called with **the exact `lease_token` the claim returned** — not merely
  that it was called. A drain that completes with the wrong token would be fenced out in production
  and the job would be re-run forever, while a call-count assertion stays green.
- For the D-01 tests, assert on the OBSERVED run status transitions and on whether
  `pipeline_glue.run_pipeline_bg` was invoked — not on whether `rewind_for_reclaim` was called. The
  question is whether the run actually re-runs, not whether a function fired.

**PLUS three tests the cross-AI review requires:**

- **`test_first_durable_action_is_a_cas_on_BOTH_branches`** — the review found the old "first durable
  action" test and the old "only the CAS advances status" test could not both pass. Under the restated
  J-1 they can, and this test is what proves it. Record the ORDERED sequence of `repo.*` calls with a
  spy, then assert:
    - `attempts == 1` → the first business-state call is `claim_status`, and `rewind_for_reclaim` is
      **never** called;
    - `attempts == 2` → the first business-state call is `rewind_for_reclaim`, immediately followed by
      `claim_status`, and **nothing else** writes status in between.
  Assert on the ORDER, not on membership — a set assertion would pass with the calls reversed, which is
  the bug (rewinding *after* the CAS is a no-op and strands the run).

- **`test_queue_tier_status_writers_are_cas_only`** — the static guard that makes the restated J-1
  enforceable rather than aspirational. Walk every `.py` file under `app/queue/` with `ast`.

  > **⚠ D-18 — IT MUST FAIL CLOSED. A name-matching guard is a decorative guard.**
  > The previous draft simply collected `repo.<name>(...)` calls. The cross-AI review's round-2
  > finding: that is **trivially bypassable** — `r = repo; r.set_status(...)`, or
  > `getattr(repo, "set_status")(...)`, or `from app.db import repo as r`, or
  > `from app.db.repo.runs import set_status`. A future queue module could add an unconditional
  > business-status write and the guard would stay green. **A guard that is green on the bug it exists
  > to catch is worse than no guard**, because it buys false confidence.

  Structure it in **three passes per file**:

  **Pass 1 — resolve the repo bindings.** Walk `ast.Import` / `ast.ImportFrom` and collect **every**
  local name bound to the repo module — `from app.db import repo` → `{"repo"}`;
  `from app.db import repo as r` → `{"r"}`; `import app.db.repo as r` → `{"r"}`. Do not assume the
  name is literally `repo`.

  **Pass 2 — FAIL CLOSED on anything that defeats static resolution.** The test FAILS, naming the file
  and line, on any of:
  - an `ast.Assign` whose value is a bare repo-bound `Name` (`r = repo`) — re-binding the module;
  - a `getattr(<repo-bound name>, ...)` call anywhere;
  - a repo-bound `Name` passed as a **call argument** (handing the module to something else to call);
  - an `ast.ImportFrom` whose module is `app.db.repo` **or any submodule of it** that imports
    *functions* rather than the module object — i.e. `from app.db.repo.runs import set_status`. This is
    already forbidden by BOUND-01's module-object import discipline (`app/routes/pipeline_glue.py:1-8`),
    so the guard is enforcing an existing convention, not inventing one;
  - any use of `importlib` or `__import__` under `app/queue/`.

  Each of these is legal Python that this tier has **no reason** to need. Refusing what cannot be
  statically resolved is what converts "bypassable by anyone who reads the guard" into "you must first
  turn a red test green, deliberately, in front of a reviewer."

  **Pass 3 — the actual invariant.** For every `<repo-bound name>.<attr>(...)` call, assert the subset
  that writes `payroll_runs.status` is **exactly** `{"claim_status", "rewind_for_reclaim"}`.
  Concretely: `"set_status"` and `"record_run_error"` appear **zero** times, and the permitted set is
  pinned by NAME in the test (not inferred), so a future third status writer must be added to it
  deliberately and reviewed. The failure message names the offending call and the file.

  `ast`, never `grep`: a grep for `set_status` false-positives on the word inside a docstring, and this
  repo has already shipped a self-invalidating grep guard.

  **STATE THE LIMITATION IN THE TEST'S DOCSTRING — do not let a reader trust it further than it goes.**
  The guard covers **direct repo access from `app/queue/`**. It does **not** — and cannot — cover
  status writes performed by code the queue *calls into*: `pipeline_glue.run_pipeline_bg` →
  `orchestrator.run_pipeline`, which contains an unconditional `set_status(EXTRACTING)` at
  `orchestrator.py:~232`. **That is correct and intentional, not a hole: J-1 constrains the TRANSPORT
  tier, not the pipeline it invokes.** The orchestrator's own status writes are explicitly permitted
  (and this phase forbids touching them at all). Say exactly that, so nobody later reads this guard as
  a whole-app status-write inventory — which it is not, and must not be mistaken for.

- **`test_swallowed_start_failure_marks_the_job_done_KNOWN_GAP_FAIL01`** — the pre-FAIL-01 pin (see
  Task 2's residual-risk block for the exact docstring intent). It PINS a known gap so Phase 18 has a
  red-to-green target. Give it a name that cannot be mistaken for an endorsement, and put
  `# KNOWN GAP — FAIL-01 (Phase 18) must INVERT this assertion, never delete this test.` directly above
  the assertion.

**`tests/test_job_kind_drift.py`** — APPEND (do not create a second file; 16-05 created it and left a
marked placeholder at the bottom): `test_job_kind_equals_dispatch_table`, asserting
`{m.value for m in JobKind} == set(dispatch.HANDLERS.keys())`. Set **EQUALITY**, never `⊇` — a
pre-loosened guard silently permits exactly the phantom-kind-with-no-handler this assertion exists to
catch (Pitfall 7). This is the third and final leg of ROADMAP criterion #5.

Execute all four falsifying mutations from this plan's frontmatter and paste each red run into the
SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_queue_drain.py tests/test_job_kind_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_queue_drain.py tests/test_job_kind_drift.py -q` passes.
    - A test asserts `complete_job` received the SAME `lease_token` object the claim returned.
    - `held_tokens()` is non-empty DURING the handler's execution and empty after (assert from inside
      a stub handler, not by inspection).
    - `test_job_kind_equals_dispatch_table` uses `==`, not `<=` / `issubset` / `>=`.
    - `_backoff_seconds` is deterministic with a stubbed rand source and its output is capped.
    - `test_first_durable_action_is_a_cas_on_BOTH_branches` asserts an ORDERED call sequence (not a
      set) and both branches pass — proving the restated J-1 is internally consistent.
    - `test_queue_tier_status_writers_are_cas_only` uses `ast`, not `grep`, and pins the permitted
      writer set by NAME as exactly `{claim_status, rewind_for_reclaim}`.
    - **The guard FAILS CLOSED (D-18).** It resolves repo bindings via `ast.Import`/`ast.ImportFrom`
      (so an `as r` alias is caught), and it REJECTS module re-binding (`r = repo`), `getattr` on a
      repo name, passing a repo name as a call argument, importing functions out of `app.db.repo.*`,
      and `importlib`/`__import__` anywhere under `app/queue/`. Proven by executing all three bypass
      mutations below — a name-matching guard passes every one of them.
    - The guard's docstring STATES its limitation: it covers direct repo access from `app/queue/` and
      deliberately does NOT cover status writes inside code the queue calls into (`run_pipeline_bg` →
      `orchestrator`, whose unconditional `set_status(EXTRACTING)` is explicitly permitted). J-1
      constrains the transport tier, not the pipeline it invokes. Nobody may read this guard as a
      whole-app status-write inventory.
    - `test_swallowed_start_failure_marks_the_job_done_KNOWN_GAP_FAIL01` exists, passes, and its
      docstring says in terms that it PINS a gap for Phase 18 to invert.
    - EIGHT FALSIFYING MUTATIONS EXECUTED, each red run pasted into the SUMMARY:
      (a) handler re-raises on a lost CAS; (b) remove the `attempts > 1` rewind preamble;
      (c) make the rewind unconditional; (d) add a `JobKind` member with no `HANDLERS` entry;
      (e) add a `repo.set_status(...)` call to `handle_run_pipeline` — the CAS-only guard MUST go red
      and MUST name `set_status`; **(f) `r = repo` then `r.set_status(...)`; (g)
      `getattr(repo, "set_status")(...)`; (h) `from app.db.repo.runs import set_status` + a bare
      `set_status(...)` call.** (f)/(g)/(h) are D-18's fail-closed proofs — the round-1 name-matching
      guard was GREEN on all three, which is what made it decorative.
    - `uv run pytest -q` fully green; `uv run mypy app` and `uv run ruff check .` clean.
  </acceptance_criteria>
  <done>drain_once() is the real, shared execution step; the queue's write paths are fenced; and criterion #5's dispatch leg is CI-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Queue transport -> business state machine | INVARIANT J-1: only `claim_status` may advance a run. |
| Worker thread -> `jobs` row | Every completion/failure write must match the lease token this worker holds. |
| Job handler -> client-facing email | A rewind-and-re-run must not become a licence to email the client twice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-25 | Elevation of Privilege | `handle_run_pipeline` advancing state without a CAS | critical | mitigate | Task 2: the handler's FIRST durable action is `claim_status(RECEIVED -> EXTRACTING)`; a lost CAS returns cleanly (job -> done). The transport tier never decides "what payroll status comes next". Falsifying mutation makes it re-raise and the J-1 test reds. |
| T-16-26 | Tampering | duplicate client email on a reclaim | critical | mitigate | Task 2: TWO things together, and neither alone is sufficient. (1) `rewind_for_reclaim` does NOT bump `reply_epoch` (D-02), so the rewound run stays in the SAME epoch. (2) **D-13's fail-closed send guard** (`app/pipeline/send_guard.py`, plan **16-10**, wave 3 — lands BEFORE the first live worker in wave 4) is epoch-scoped, so it SEES any `reserved`/`failed` outbound row the crashed worker left behind, refuses to re-send, and escalates the run to ERROR (which is outside the rewind scope, so the machine can never auto-retry it). **The old mitigation here — "delivery's already-sent guard blocks it" — was FALSE**: that guard counts only `send_state='sent'`, and a crash between provider-acceptance and the sent-commit leaves no `'sent'` row. That was the review's HIGH finding. |
| T-16-26b | Elevation of Privilege | the machine granting itself a second-email licence | critical | mitigate | Only a HUMAN retrigger bumps `reply_epoch`, and only a new epoch clears D-13's guard. That is REQUIREMENTS.md's already-reviewed "Accepted residual risk" — an operator may take it; the reclaim loop may not. |
| T-16-27 | Denial of Service | a permanently stranded run | high | mitigate | Task 2: D-01's `attempts > 1` rewind is what lets a crashed retrigger actually re-run instead of dying on a CAS it can never win. Falsifying mutation (b) proves the run IS stranded without it. |
| T-16-28 | Tampering | over-broad rewind clobbering live work | high | mitigate | Task 2: the rewind is guarded by `attempts > 1` AND scoped by `rewind_for_reclaim` to exactly `extracting`/`computed`/`sent`. A legitimately paused (`awaiting_reply`, `needs_operator`) or finished (`reconciled`) run is never rewound. Falsifying mutation (c) proves the guard bites. |
| T-16-29 | Tampering | zombie worker completing a stolen job | high | mitigate | Task 3: `drain_once` completes/fails with the token the CLAIM returned, and both repo writes are fenced. A stolen lease means `complete_job` returns False -> log and drop. |
| T-16-30 | Information Disclosure | exception text in `jobs.last_error` | high | mitigate | Task 3: `drain_once` passes the raw exception to `fail_job`, which scrubs internally (16-04). No handler formats an error string itself. |
| T-16-31 | Repudiation | a job marked `done` that never ran | high | mitigate | Task 1: `dispatch.handle` RAISES on an unknown kind rather than silently no-op'ing, so a phantom kind fails loudly. Criterion #5's set-equality guard makes a phantom kind unshippable in the first place. |
| T-16-31b | Elevation of Privilege | a J-1 guard that is green on the bug it exists to catch | high | mitigate | Task 3 (**D-18**, cross-AI review round 2): the round-1 AST guard matched `repo.<name>(...)` by name and was therefore bypassable by `r = repo`, `getattr(repo, "set_status")`, an `as r` import alias, or a direct `from app.db.repo.runs import set_status`. It now resolves repo bindings from the import statements and **fails closed** on every construct it cannot statically resolve. Three bypass mutations are executed to prove it. Limitation stated in its docstring: it covers `app/queue/` only, never the pipeline the queue calls into — which is correct, because J-1 constrains the transport tier. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages; no `uv add`. `threading`, `random`, `uuid` are stdlib. |
</threat_model>

<verification>
- `uv run pytest -q` fully green (the live-DB proofs remain deselected).
- `uv run pytest tests/test_bound01_private_imports.py -q` passes — `app/queue/` is auto-scanned and clean.
- `uv run mypy app` (strict) and `uv run ruff check .` clean.
- `git diff --stat app/pipeline/orchestrator.py` is EMPTY.
- All EIGHT falsifying mutations executed with red runs pasted into the SUMMARY — including D-18's
  three fail-closed bypasses (`r = repo`; `getattr`; `from app.db.repo.runs import set_status`).
</verification>

<success_criteria>
ROADMAP criterion #5 is fully TRUE (collision + enum drift + dispatch drift, all CI-enforced), and the
execution path that criteria #2 and #3 depend on exists: a claim, a fenced dispatch, a CAS the
transport tier can only lose, and a rewind that fires only when a crash actually stranded the run.
</success_criteria>

<output>
Create `.planning/phases/16-queue-substrate-unblocked-webhook/16-06-SUMMARY.md` when done.
It MUST contain the pasted RED output of all EIGHT falsifying mutations listed in this plan's
frontmatter and acceptance criteria.
</output>
