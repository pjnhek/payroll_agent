---
phase: 16-queue-substrate-unblocked-webhook
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/job.py
  - app/db/schema.sql
  - app/db/bootstrap.py
  - tests/test_status_drift.py
autonomous: true
requirements: [QUEUE-02, QUEUE-05]
must_haves:
  truths:
    - "A `jobs` table exists with a UNIQUE dedup_key, lease_token + leased_until fencing columns, attempts/max_attempts, and a partial index matching the claim predicate exactly (QUEUE-02)."
    - "`jobs` carries identifiers ONLY — there is physically nowhere to store a payroll status or business data (INVARIANT J-1, QUEUE-05)."
    - "JobKind has exactly one member (run_pipeline) — the only kind with a real handler in this phase — so the CI guard `set(JobKind) == set(HANDLERS)` is satisfiable rather than permanently red (Pitfall 7)."
    - "No JobKind or JobState value collides with any RunStatus value (ROADMAP criterion #5)."
    - "A hermetic test reset (bootstrap --reset) drops `jobs` before ALL THREE of its FK targets — payroll_runs, email_messages, businesses — so live-DB tests do not accumulate orphaned job rows across runs (Pitfall 4)."
    - "The schema guards are pinned against a harvested NAME inventory, not a magic number, so the next index addition fails with the missing name rather than with a bare '3 != 4' (D-05)."
    - "`Job` is a frozen dataclass with EXACTLY the six fields the canonical claim SQL's `RETURNING` clause yields — no `email_id`. The dataclass/RETURNING bijection is machine-asserted in 16-04."
  prohibitions:
    - "`Job` MUST NOT carry an `email_id` field. The canonical claim SQL returns six columns and 16-06 has no consumer for a seventh. The jobs TABLE keeps its email_id COLUMN (reserved for Phase 19); the dataclass does not."
    - "MUST NOT add an `event_id` column — its FK target `inbound_events` does not exist until Phase 19."
  artifacts:
    - "app/models/job.py — JobKind, JobState, Job (6 fields)."
    - "app/db/schema.sql — CREATE TABLE jobs, uq_jobs_dedup_key, ck_jobs_lease_coherent, idx_jobs_claimable."
    - "app/db/bootstrap.py — _DROP_ORDER contains 'jobs' before email_messages / payroll_runs / businesses."
    - "tests/test_status_drift.py — inventory-pinned index guard replacing the magic-number count."
  key_links:
    - "The `jobs.kind` inline CHECK must list exactly the JobKind members; the drift guard that pins that pair lands in 16-05 and 16-06."
    - "idx_jobs_claimable's WHERE clause must match claim_job's WHERE predicate exactly, or the claim degrades to a seq scan over an ever-growing done/dead history."
  falsifying_mutations:
    - "Remove `ck_jobs_lease_coherent`: a half-written lease (state='leased' with a NULL lease_token) becomes storable, and the fencing predicate `lease_token = %(token)s` silently degrades to 'no fence at all'. The constraint is what makes that unrepresentable."
    - "D-05: add a 5th CREATE INDEX to schema.sql without adding its name to the expected set. test_expected_indexes_present_and_no_others MUST go red, and its message MUST name the missing index — not report '4 != 5'."
---

<objective>
Land the durable transport substrate: the `jobs` table, the `JobKind`/`JobState` vocabulary, the
hermetic-reset plumbing, and the D-05 rewrite of the schema guard the new index would otherwise
detonate.

This is **transcription, not design.** The DDL, the constraints, and the partial index are already
specified verbatim in `.planning/research/ARCHITECTURE.md` §4 and were adversarially validated by
four researchers. Transcribe them — with exactly **three documented deviations** (Task 2), each a
correction to the canonical doc, not an improvisation.

Purpose: QUEUE-02's durable state and QUEUE-05's structural enforcement of INVARIANT J-1 — a job row
that has physically nowhere to put "what payroll status comes next."
Output: `app/models/job.py`, the `jobs` block in `app/db/schema.sql`, `_DROP_ORDER` fixed, and
`tests/test_status_drift.py` pinned to a name inventory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-CONTEXT.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-RESEARCH.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-PATTERNS.md
@.planning/research/ARCHITECTURE.md
</context>

## Artifacts this phase produces

| Artifact | Owning plan |
|---|---|
| `app/routes/webhook.py` off-loop ingest; `tests/test_webhook_unblocked.py` | 16-01 |
| `queueproof` marker + the NARROW CI gate (D-14; D-04 superseded); `Settings.worker_count`/`lease_seconds`/`max_attempts`/`queue_poll_seconds` | 16-02 |
| **`app/models/job.py` (`JobKind`, `JobState`, `Job`); `jobs` table + `uq_jobs_dedup_key` + `ck_jobs_lease_coherent` + `idx_jobs_claimable`; `bootstrap._DROP_ORDER` gains `"jobs"`; D-05 index-inventory guard** | **16-03** |
| `app/db/repo/jobs.py` (`enqueue_job`, `claim_job`, `complete_job`, `fail_job`, `release_leases`, `get_job`); `clear_reply_context -> int`; `rewind_for_reclaim`; conftest `WORKER_COUNT=0` + fake_repo names | 16-04 |
| `schema_introspect` covers `jobs` (D-12); `tests/test_job_kind_drift.py` | 16-05 |
| `app/queue/wake.py`, `dispatch.py`, `handlers/pipeline.py`, `drain.py` | 16-06 |
| `app/queue/worker.py`, `app/db/supabase.py::POOL_MAX_SIZE`, `app/main.py` lifespan | 16-07 |
| `app/routes/runs.py::_claim_stale_in_flight`; retrigger -> `enqueue_job` | 16-08 |
| `tests/test_queue_durability.py` (Proofs 2/3/4) | 16-04, 16-07, 16-09 |
| `app/db/repo/emails.py::get_unconfirmed_outbound`; `app/pipeline/send_guard.py` (`UnconfirmedSendError`, `assert_no_unconfirmed_send`); the guard call in `clarify()` + `deliver()` | **16-10** (D-13, wave 3) |

<tasks>

<task type="auto">
  <name>Task 1: app/models/job.py — the JobKind / JobState vocabulary</name>
  <files>app/models/job.py</files>
  <read_first>
    - `app/models/status.py` (FULL FILE — this is the exact analog. Copy the `enum.StrEnum` shape,
      the "this Python class is CANONICAL; the SQL mirrors it" docstring convention, and the
      TEXT+CHECK-rather-than-native-ENUM rationale. Note its 11 members: received, extracting,
      awaiting_reply, computed, awaiting_approval, approved, sent, reconciled, rejected, error,
      needs_operator.)
    - `16-RESEARCH.md` § Pitfall 7 (why exactly ONE JobKind member).
    - `16-PATTERNS.md` § `app/models/job.py`.
  </read_first>
  <action>
Create `app/models/job.py` with three exports.

`JobKind(enum.StrEnum)` — **exactly one member**: `RUN_PIPELINE = "run_pipeline"`.
Do NOT pre-declare `ingest`, `resume_reply`, or `operator_resume`. ARCHITECTURE.md's full design
names four kinds, but Phase 16 ships a real handler for exactly one, and QUEUE-05's CI guard is
`set(JobKind) == set(dispatch.HANDLERS)` — set EQUALITY. Declaring four kinds with one handler makes
that guard unsatisfiable, and the only way to "fix" it would be to weaken `==` to `⊇`, which
silently permits exactly the phantom-kind-with-no-handler the guard exists to catch. Write this
reasoning into the enum's docstring so a future phase widens it deliberately, alongside its handler.

`JobState(enum.StrEnum)` — `PENDING = "pending"`, `LEASED = "leased"`, `DONE = "done"`,
`DEAD = "dead"`. These are the four states the canonical claim/complete/fail SQL moves between.

**Collision constraint (ROADMAP criterion #5), stated in the module docstring:** no `JobKind` or
`JobState` value may ever equal a `RunStatus` value. The queue's vocabulary is transport state; the
run's vocabulary is business state; a job row that could name a payroll status is INVARIANT J-1
violated at the type level. Concretely: never introduce a `JobState` member whose value is the same
string `RunStatus.ERROR` already owns. The four chosen values are collision-free against all 11
`RunStatus` members today, and `tests/test_job_kind_drift.py` (plan 16-05) pins that.

`Job` — a frozen dataclass mirroring **exactly** what the claim SQL's `RETURNING` clause yields.
**SIX fields, in this order:**
`id: uuid.UUID`, `kind: JobKind`, `run_id: uuid.UUID | None`, `attempts: int`,
`max_attempts: int`, `lease_token: uuid.UUID`. Nothing else.

> **⚠ RESOLVED CONTRADICTION — read this, it is the review's #1 blocking finding.**
> An earlier draft of this plan declared **seven** fields, including `email_id: uuid.UUID | None`,
> while the canonical claim SQL (`16-RESEARCH.md:399`, which plan 16-04 tells you to transcribe
> **verbatim**) ends `RETURNING j.id, j.kind, j.run_id, j.attempts, j.max_attempts, j.lease_token;`
> — **six columns, no `email_id`.** Those two instructions were mutually exclusive: an executor
> following both produces either a row-construction failure or a dead field.
>
> **The resolution is: DROP `email_id` from `Job`. Keep the six-column SQL verbatim.** Reasons:
> (a) nothing in this phase reads it — the handler (`16-06`) touches only `.run_id`, `.attempts`,
> `.id`, `.lease_token`; (b) `Job` is a **transport record**, and the contract stated right here is
> that it mirrors `RETURNING` — a field with no column is a lie about that contract; (c) the
> `jobs.email_id` **column** still exists on the table (Task 2 keeps it, reserved for Phase 19) —
> declaring the column is cheap and declaring the dataclass field is not. When Phase 19 needs it, it
> adds the column to `RETURNING` **and** the field to `Job`, together, in one commit.
>
> **This bijection is machine-enforced**, not left to care: plan 16-04's
> `tests/test_repo_jobs_sql.py` parses `claim_job`'s `RETURNING` clause and asserts every returned
> column maps **exactly once** onto a `Job` field, and that every `Job` field has **exactly one**
> returned column. Set equality, both directions. Write a pointer to that test into `Job`'s docstring.

It is a transport record, not a payload envelope — there is deliberately no payload, no status, and
no next-state field.

Add a module docstring cross-reference to `tests/test_job_kind_drift.py`, mirroring how
`app/models/status.py` points at its own drift test.
  </action>
  <verify>
    <automated>uv run python -c "from app.models.job import JobKind, JobState, Job; from app.models.status import RunStatus; assert {m.value for m in JobKind} == {'run_pipeline'}; assert {m.value for m in JobState} == {'pending','leased','done','dead'}; overlap = ({m.value for m in JobKind} | {m.value for m in JobState}) & {m.value for m in RunStatus}; assert not overlap, overlap; print('job vocabulary ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `JobKind` has exactly 1 member; `JobState` has exactly 4.
    - The intersection of `JobKind` ∪ `JobState` values with `RunStatus` values is empty.
    - `Job` is a frozen dataclass with **exactly SIX** fields — `id`, `kind`, `run_id`, `attempts`,
      `max_attempts`, `lease_token` — and no `email_id`, no payload, no status field. Assert it
      mechanically:
      `uv run python -c "import dataclasses; from app.models.job import Job; n=[f.name for f in dataclasses.fields(Job)]; assert n == ['id','kind','run_id','attempts','max_attempts','lease_token'], n; assert dataclasses.fields(Job) and Job.__dataclass_params__.frozen; print('Job shape ok')"`
    - `uv run mypy app/models/job.py` clean under strict; `uv run ruff check app/models/job.py` clean.
  </acceptance_criteria>
  <done>The transport vocabulary exists, is collision-free against the business vocabulary, and is scoped to what actually has a handler.</done>
</task>

<task type="auto">
  <name>Task 2: The `jobs` table in schema.sql (three documented deviations) + bootstrap _DROP_ORDER</name>
  <files>app/db/schema.sql, app/db/bootstrap.py</files>
  <read_first>
    - `.planning/research/ARCHITECTURE.md` §4 "The `jobs` table (NEW)" — the DDL to transcribe.
      Also §3 for the `enqueue_job` `ON CONFLICT (dedup_key) DO NOTHING` contract the UNIQUE backs.
    - `app/db/schema.sql` — the `businesses` block (`:16-30`: the `CREATE TABLE IF NOT EXISTS` +
      `gen_random_uuid()` PK-default idiom), the `payroll_runs` block (`:69-148`: the
      CHECK-constrained TEXT status column and the `CREATE INDEX IF NOT EXISTS` idiom at `:146,:148`),
      the `pgcrypto` line (`:13`), and the comments at `:125` and `:283` explaining WHY
      `CREATE TABLE IF NOT EXISTS` cannot add a column to a live table.
    - `app/db/bootstrap.py:60-85` — `_DROP_ORDER` and its "explicit reverse order documents the
      dependency direction" convention.
    - `16-RESEARCH.md` § Jobs Index Set (the index set is exactly the canonical one — no additions,
      and in particular NO index on `run_id`) and § Pitfall 4.
  </read_first>
  <action>
**schema.sql** — append a `jobs` block, transcribing ARCHITECTURE.md §4's DDL. Columns: `id`
(UUID PK, `gen_random_uuid()` default — pgcrypto is already enabled at `:13`, zero new extension
surface), `kind`, `dedup_key`, `run_id`, `email_id`, `business_id`, `priority`, `state`, `attempts`,
`max_attempts`, `available_at`, `lease_token`, `leased_until`, `last_error`, `created_at`,
`updated_at`. Plus `CONSTRAINT uq_jobs_dedup_key UNIQUE (dedup_key)` and
`CONSTRAINT ck_jobs_lease_coherent CHECK (...)` exactly as specified.

**THREE DEVIATIONS from the canonical DDL. Each is a correction. Each gets a SQL comment saying so:**

1. **`kind` CHECK is scoped to one value, not four:** `kind TEXT NOT NULL CHECK (kind IN ('run_pipeline'))`.
   Only one kind has a handler this phase (Pitfall 7). Comment that widening this CHECK in Phase
   17/19 must use the idempotent DO-block DROP+RE-ADD idiom already proven twice in this file (for
   `payroll_runs.status` and `email_messages.purpose`) — because by then `jobs` will have live rows,
   which is precisely the situation that idiom exists for. Today it has none, so an inline CHECK is
   correct and deliberately adds NO third `ANY (c.conkey)` DO-block.

2. **OMIT the `event_id UUID REFERENCES inbound_events(id)` column entirely.** The canonical DDL
   declares it, but **`inbound_events` DOES NOT EXIST in this schema** — grep `CREATE TABLE` and
   confirm the tables are businesses, employees, payroll_runs, paystub_line_items, email_messages,
   eval_results, demo_sender_bindings. Phase 19 (durable ingest) creates that table. A FK to a
   non-existent relation fails the bootstrap outright. Comment that Phase 19 adds both the table and
   this column, via the `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` idiom this file already documents
   at `:125`/`:283`.

3. **`run_id UUID REFERENCES payroll_runs(id)` with NO cascading delete.** The canonical DDL
   cascades; CONTEXT.md's Claude's-Discretion section overrides it. Keep the attempt history
   append-only, matching the deliberate `email_messages` precedent. Comment the reason: a cascading
   delete would silently vaporize a run's attempt history — the one thing the queue exists to make
   auditable. Runs are never deleted today, so this is theoretical either way; choose the direction
   that cannot lose the audit trail.

**KEEP** `email_id UUID REFERENCES email_messages(id)` and `business_id UUID REFERENCES businesses(id)`
(both nullable, both unused in Phase 16 — their FK targets exist). Declare them NOW because
`CREATE TABLE IF NOT EXISTS` is a no-op against a live table, so a later add costs an explicit ALTER
block, and Phase 19 will need both. Comment them as reserved-for-Phase-19.

**KEEP** `priority INT NOT NULL DEFAULT 100` with the canonical comment: **written, never read in
v4** — it exists so per-tenant fairness stays a later `ORDER BY` change rather than a migration.
Fairness lanes are explicitly Out of Scope (REQUIREMENTS.md).

`ck_jobs_lease_coherent` is load-bearing and its comment must say why: a half-written lease
(`state='leased'` with a NULL `lease_token`) is indistinguishable from an unclaimed job, and the
fencing predicate would silently degrade to no fence at all. The database refuses to store one.

Index — exactly one, exactly as canonical:
`CREATE INDEX IF NOT EXISTS idx_jobs_claimable ON jobs (priority, available_at) WHERE state IN ('pending','leased');`
Its `WHERE` clause must match the claim query's predicate EXACTLY (a partial index: `done`/`dead`
rows — the overwhelming majority over time — are never indexed, so the claim stays O(1) forever with
no purge job). Add NO index on `run_id`: nothing in this phase queries `jobs` by `run_id`; the ops
view that would is OPS-01 (Phase 21).

**bootstrap.py** — insert `"jobs"` into `_DROP_ORDER`.

**Position matters, and the earlier draft of this plan got it wrong.** It said "immediately BEFORE
`payroll_runs`" — but `_DROP_ORDER` is today
`["name_matches", "paystub_line_items", "eval_results", "email_messages", "payroll_runs",
"employees", "businesses"]`, and `jobs` FKs **three** of those: `payroll_runs`, `email_messages`
(via `email_id`), and `businesses` (via `business_id`). Dropping it "before `payroll_runs`" would
still place it AFTER `email_messages` — backwards, by this file's own convention.

**Insert `"jobs"` immediately after `"eval_results"`, i.e. BEFORE `"email_messages"`** — so it
precedes **all three** of its FK targets. Verify the list against the live file before you edit; do
not trust this plan's transcription of it.

This is not cosmetic (Pitfall 4): the `seeded_db` fixture's `--reset` path drops exactly this list,
so without `"jobs"` every live-DB run would leave job rows referencing dropped-and-recreated
`run_id`s — silently accumulating across resets and poisoning isolation for the very durability
proofs this phase adds. Symptom: a proof that passes against a fresh DB but flakes on the second run
in the same session. Each `DROP TABLE IF EXISTS ... CASCADE` already cascades, so exact ordering is
defensive-not-required for correctness — but the file's own convention is that reverse order
**documents** the dependency direction, and a comment that documents it wrong is worse than none.
Add a short comment naming all three FK targets.

**Assert the position, don't eyeball it.** The regression guard in plan 16-05
(`test_bootstrap_drop_order_includes_jobs`) must assert `"jobs"` precedes **every** one of
`payroll_runs`, `email_messages`, `businesses` — not just `payroll_runs`.
  </action>
  <verify>
    <automated>uv run python -c "
import re, pathlib
from app.db.bootstrap import _DROP_ORDER as d
sql = pathlib.Path('app/db/schema.sql').read_text()
assert 'CREATE TABLE IF NOT EXISTS jobs' in sql
for tok in ('uq_jobs_dedup_key', 'ck_jobs_lease_coherent', 'idx_jobs_claimable'):
    assert tok in sql, tok
assert 'inbound_events' not in sql, 'event_id FK target does not exist until Phase 19'
body = sql.split('CREATE TABLE IF NOT EXISTS jobs', 1)[1].split(');', 1)[0]
assert 'CASCADE' not in body, 'jobs.run_id must not cascade (CONTEXT discretion)'
assert re.search(r&quot;CHECK\s*\(\s*kind\s+IN\s*\(\s*'run_pipeline'\s*\)\s*\)&quot;, body), body
assert sql.count('ANY (c.conkey)') == 2, 'jobs CHECKs are inline; no third DO-block'
assert 'jobs' in d, d
for fk in ('payroll_runs', 'email_messages', 'businesses'):
    assert d.index('jobs') &lt; d.index(fk), ('jobs must be dropped before its FK target', fk, d)
print('jobs DDL + drop order ok')
"</automated>
  </verify>
  <acceptance_criteria>
    - `jobs` is created with `CREATE TABLE IF NOT EXISTS`, a `gen_random_uuid()` PK default, no new
      extension, no `event_id` column, and no cascading delete on `run_id`.
    - The `kind` CHECK lists exactly `'run_pipeline'`.
    - `uq_jobs_dedup_key`, `ck_jobs_lease_coherent`, `idx_jobs_claimable` all present;
      `idx_jobs_claimable`'s WHERE clause is `state IN ('pending','leased')`.
    - `sql.count("ANY (c.conkey)")` is still exactly 2.
    - `"jobs"` precedes **all three** of `"payroll_runs"`, `"email_messages"` and `"businesses"` in
      `_DROP_ORDER` (it FKs all three — "before `payroll_runs`" alone is NOT sufficient).
    - With a live DB: `DATABASE_URL=... uv run python -m app.db.bootstrap` applies cleanly and
      idempotently (run it twice; the second is a no-op), and
      `DATABASE_URL=... ALLOW_DB_RESET=1 uv run python -m app.db.bootstrap --reset` succeeds twice
      with `SELECT count(*) FROM jobs` returning 0 afterward.
  </acceptance_criteria>
  <done>The durable transport table exists, carries identifiers only, refuses to store a half-written lease, its claim index matches the claim predicate exactly, and a hermetic reset genuinely resets it.</done>
</task>

<task type="auto">
  <name>Task 3: D-05 — replace the magic-number schema guards with inventory-pinned assertions</name>
  <files>tests/test_status_drift.py</files>
  <read_first>
    - `tests/test_status_drift.py` — specifically `test_exactly_three_new_indexes` (`:327-330`,
      `sql.count("CREATE INDEX IF NOT EXISTS") == 3`) and
      `test_do_block_constraint_drops_are_column_anchored` (`:207-232`,
      `sql.count("ANY (c.conkey)") == 2`). Also note `test_status_exact_count_is_ten` asserting
      `== 11` — living proof that these magic numbers already rot.
    - `16-RESEARCH.md` § "The D-05 test rewrite — precise, verified shape" (both rewritten bodies
      are pre-specified there; transcribe them).
    - `16-CONTEXT.md` D-05.
  </read_first>
  <action>
Adding `idx_jobs_claimable` makes the fourth `CREATE INDEX IF NOT EXISTS` in `schema.sql`, which
detonates `test_exactly_three_new_indexes`. **Do NOT just bump 3 to 4.** Per D-05, pin against a
harvested NAME inventory so the guard stays meaningful as the schema grows.

Rewrite `test_exactly_three_new_indexes` as `test_expected_indexes_present_and_no_others`: harvest
the actual index names with `re.findall(r"CREATE INDEX IF NOT EXISTS (\w+)", sql)` into a set,
compare it for EQUALITY against the explicitly-named expected set
`{idx_payroll_runs_created_at, idx_payroll_runs_status, idx_email_messages_run_direction_state,
idx_jobs_claimable}`, and put the SYMMETRIC DIFFERENCE in the failure message. The point: a future
index addition now fails with the exact name missing from the assertion, not with a bare "4 != 5" —
and the guard is immune to false negatives from reordering.

Rewrite `test_do_block_constraint_drops_are_column_anchored` per D-05's spirit while KEEPING its
count at 2. It does NOT need bumping — research verified this by direct read, and the reason is
load-bearing: the DO-block DROP+RE-ADD idiom exists specifically to widen a CHECK on a table that may
already hold LIVE ROWS under the old constraint. `jobs` is a brand-new table whose `kind`/`state`
CHECKs are declared INLINE in `CREATE TABLE`, so it correctly adds no third occurrence. Replace the
bare `== 2` with a named set — `EXPECTED_DO_BLOCKS = {"payroll_runs_status_check",
"email_messages_purpose_check"}` — assert `sql.count(...) == len(EXPECTED_DO_BLOCKS)`, and write the
above rationale into a comment above it so that a genuine future third DO-block (widening
`jobs.kind`'s CHECK once it has live rows, Phase 17+) is a deliberate reviewed addition to that named
set rather than a silent number bump. KEEP the existing `re.sub(r"--[^\n]*", "", ...)` comment-strip
normalization and the `conname LIKE` prohibition — both unchanged.
  </action>
  <verify>
    <automated>uv run pytest tests/test_status_drift.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_status_drift.py -q` is fully green (no red left over from Task 2).
    - The index guard is a SET comparison, not a count: `tests/test_status_drift.py` contains
      `re.findall` over `CREATE INDEX IF NOT EXISTS (\w+)` and asserts equality against an expected
      set that literally names all four indexes (`idx_payroll_runs_created_at`,
      `idx_payroll_runs_status`, `idx_email_messages_run_direction_state`, `idx_jobs_claimable`).
      Verify by running the guard, not by grepping for the absence of the old form.
    - The DO-block guard still asserts a count of 2 and now derives it from a named set with a
      comment explaining why `jobs`' inline CHECKs deliberately add no third occurrence.
    - FALSIFYING MUTATION EXECUTED: append a dummy 5th `CREATE INDEX IF NOT EXISTS idx_fake_probe ...`
      to `schema.sql`, run the guard, confirm it goes RED and that its message NAMES `idx_fake_probe`
      (not "4 != 5"). Revert the dummy index and paste the red run into the SUMMARY.
  </acceptance_criteria>
  <done>The schema guards are pinned to a real inventory. `uv run pytest -q` is green at this commit — no test is left knowingly red for a later plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Application -> `jobs` table | The only writer is `app/db/repo/jobs.py` (plan 16-04); all values are server-generated identifiers. |
| Job row -> business state machine | INVARIANT J-1: the queue tier may never encode "what payroll status comes next". |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-08 | Tampering | `jobs` lease fencing | high | mitigate | Task 2: `ck_jobs_lease_coherent` makes a half-written lease (`state='leased'`, NULL token) unrepresentable at the DB level. Without it the `lease_token = %(token)s` fence degrades silently to no fence, and a zombie worker's write lands. |
| T-16-09 | Elevation of Privilege | `jobs` schema shape | high | mitigate | Task 2: the table has no payload column and no status column — INVARIANT J-1 made structural. There is physically nowhere for a job row to say "advance this run to APPROVED". QUEUE-05's vocabulary guard (16-05/16-06) sits on top of that. |
| T-16-10 | Tampering | `jobs.id` / `jobs.lease_token` predictability | medium | mitigate | Task 2: both default to `gen_random_uuid()` (pgcrypto, cryptographically random) — the same PK-default pattern already on 6 tables. A guessable lease token would let an attacker with DB write access forge a fence match. |
| T-16-11 | Repudiation | attempt-history loss | medium | mitigate | Task 2: `run_id`'s FK deliberately does not cascade on delete (CONTEXT discretion). A cascade would erase the audit trail the queue exists to create. |
| T-16-12 | Tampering | stale `jobs` rows across test resets | low | mitigate | Task 2: `"jobs"` added to `_DROP_ORDER` before `payroll_runs`. |
| T-16-13 | Repudiation | schema-guard rot | medium | mitigate | Task 3: index guard pinned to a harvested name inventory; a silently-added index now fails by name. |
| T-16-SC | Tampering | npm/pip/cargo installs | high | mitigate | Zero new packages. `pgcrypto` is already enabled at `schema.sql:13`; `SKIP LOCKED` is core Postgres. No `uv add` in this plan; `pyproject.toml`/`uv.lock` untouched. |
</threat_model>

<verification>
- `uv run pytest -q` fully green (including the rewritten `tests/test_status_drift.py`).
- `uv run mypy app` and `uv run ruff check .` clean.
- Live-DB: `bootstrap` and `bootstrap --reset` both apply cleanly, twice, with `jobs` empty after reset.
- Both falsifying mutations (drop `ck_jobs_lease_coherent`; add a 5th index) reasoned/executed and
  recorded in the SUMMARY.
</verification>

<success_criteria>
The durable job substrate exists in the schema, carries transport identifiers only, cannot store a
lease that would defeat its own fence, and the schema guards that watch it are pinned to a real
inventory rather than a number that rots.
</success_criteria>

<output>
Create `.planning/phases/16-queue-substrate-unblocked-webhook/16-03-SUMMARY.md` when done.
</output>
