---
phase: "04-the-eval-the-proof"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/fixtures/01_exact_match_coastal.json
  - eval/fixtures/02_stored_alias_metro.json
  - eval/fixtures/03_collision_metro.json
  - eval/fixtures/04_unknown_shorthand_metro.json
  - eval/fixtures/05_typo_coastal.json
  - eval/fixtures/06_first_time_alias_metro.json
  - eval/fixtures/07_missing_hours_coastal.json
  - eval/fixtures/08_vague_hours_coastal.json
  - eval/fixtures/09_buried_reply_metro.json
  - eval/fixtures/10_multi_employee_coastal.json
  - eval/fixtures/11_multi_employee_metro.json
  - eval/fixtures/12_exact_process_summit.json
  - eval/fixtures/13_exact_process_metro.json
  - eval/fixtures/14_collision_process_summit.json
  - eval/fixtures/15_stored_alias_process_coastal.json
  - eval/fixtures/01_exact_match_coastal_extraction.json
  - eval/fixtures/02_stored_alias_metro_extraction.json
  - eval/fixtures/03_collision_metro_extraction.json
  - eval/fixtures/04_unknown_shorthand_metro_extraction.json
  - eval/fixtures/05_typo_coastal_extraction.json
  - eval/fixtures/06_first_time_alias_metro_extraction.json
  - eval/fixtures/07_missing_hours_coastal_extraction.json
  - eval/fixtures/08_vague_hours_coastal_extraction.json
  - eval/fixtures/09_buried_reply_metro_extraction.json
  - eval/fixtures/10_multi_employee_coastal_extraction.json
  - eval/fixtures/11_multi_employee_metro_extraction.json
  - eval/fixtures/12_exact_process_summit_extraction.json
  - eval/fixtures/13_exact_process_metro_extraction.json
  - eval/fixtures/14_collision_process_summit_extraction.json
  - eval/fixtures/15_stored_alias_process_coastal_extraction.json
  - eval/fixtures/DIVERGENCES.md
  - eval/draft_candidate_emails.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02

must_haves:
  truths:
    - "15 labeled eval fixtures exist in eval/fixtures/ with full expected blocks (D-01, D-02, D-03)"
    - "Each fixture's from_addr matches a seeded contact_email; InboundEmail.model_validate passes on every input portion"
    - "Each fixture carries both a fixture_category (for decision chart) and per-name name_category entries in expected.reconciliation (for per-NAME reconciliation chart, D-03)"
    - "Fixtures include enough clean PROCESS cases that an always-clarify baseline visibly fails (D-11)"
    - "A stubbed _extraction.json is committed beside every fixture (D-05, enables --check in 04-02 without live LLM)"
    - "draft_candidate_emails.py exists, is gated by allow_live_llm, uses tier=draft (Kimi not DeepSeek, D-19 anti-leakage)"
  artifacts:
    - path: "eval/fixtures/03_collision_metro.json"
      provides: "collision fixture (D. Reyes shared alias) — core to false-process headline"
      contains: "collision"
    - path: "eval/fixtures/10_multi_employee_coastal.json"
      provides: "multi-employee fixture covering exact + typo + missing-hours in one email"
      contains: "multi_employee"
    - path: "eval/fixtures/12_exact_process_summit.json"
      provides: "clean PROCESS fixture so always-clarify baseline fails (D-11)"
      contains: "process"
    - path: "eval/draft_candidate_emails.py"
      provides: "bootstrap drafting helper (EVAL-01), throwaway, gated, Kimi only"
      contains: "draft_candidate_emails"
  key_links:
    - from: "eval/fixtures/*.json (from_addr)"
      to: "app/db/seed.py (contact_email)"
      via: "seeded contact_email match"
      pattern: "payroll@coastalcleaning.example|hr@metrodeli.example|finance@summittech.example"
    - from: "eval/fixtures/*_extraction.json"
      to: "app/models/contracts.py Extracted"
      via: "Extracted.model_validate(json)"
      pattern: "Extracted.model_validate"
---

<objective>
Build the eval fixture corpus: 15 hand-labeled eval fixtures covering the full name-resolution taxonomy plus field-case categories, with a stubbed cached extraction committed beside each, and a throwaway bootstrap drafting helper.

Purpose: Provide the data plane for 04-02 (the scorer). Fixtures are self-contained, roster-bound via from_addr, and carry full per-stage expected blocks (D-01, D-02, D-03). The corpus must include enough clean PROCESS cases that an always-clarify baseline visibly fails on the confusion matrix (D-11).

Output:
- eval/fixtures/ — 15 labeled fixture files + 15 stubbed _extraction.json files
- eval/draft_candidate_emails.py — the throwaway Kimi-tier drafting helper (D-19, EVAL-01)
</objective>

<execution_context>
@/Users/pnhek/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pnhek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-the-eval-the-proof/04-CONTEXT.md
@.planning/phases/04-the-eval-the-proof/04-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fixture corpus — 15 labeled eval fixtures with stubbed cached extractions (D-01..D-05, D-18)</name>

  <read_first>
    .planning/phases/04-the-eval-the-proof/04-PATTERNS.md — full expected-block schema (lines 26-100), fixture_category enum, per-name name_category entries, hours-as-JSON-strings rule, seed roster table (lines 463-483)
    .planning/phases/04-the-eval-the-proof/04-CONTEXT.md — D-01 (one self-contained JSON per fixture), D-02 (per-stage expected blocks, net_pay NOT labeled), D-03 (two independent category fields), D-04 (from_addr binds to seed), D-11 (enough PROCESS fixtures), D-13 (taxonomy honesty), D-18 (15 floor, multi-employee)
    app/db/seed.py — three businesses + employees with their UUIDs; collision pair David Reyes (e0000003) + Daniel Reyes (e0000007) sharing "D. Reyes"; Thomas Bergmann (e0000005) SS-straddle; Maria Chen (e0000001), James Okafor (e0000002), Priya Nair (e0000004), Sandra Kim (e0000006)
    app/models/contracts.py — InboundEmail (lines ~35, extra="forbid"), ExtractedEmployee fields (lines ~103-112)
    fixtures/clean_happy_path.json — canonical InboundEmail JSON shape to replicate
  </read_first>

  <action>
  Create eval/fixtures/ (directory) and write the 15 fixture files listed below PLUS a stubbed _extraction.json beside each. Use the exact JSON schema from PATTERNS.md lines 26-100.

  FIXTURE_CATEGORY ENUM (per D-03): exact | stored-alias | first-time-alias | typo | collision | unknown | missing-hours | vague-hours | buried-reply
  NAME_CATEGORY ENUM (same set): the per-name bucket in expected.reconciliation entries.

  FIXTURE SCHEMA: top-level keys = id, message_id, in_reply_to (null), references_header (null), subject, from_addr, to_addr ("agent@payroll-agent.local"), body_text, created_at, fixture_category (string), expected (object).

  EXPECTED BLOCK schema:
  - expected.extracted.employees: list of {submitted_name, hours_regular, hours_overtime, hours_vacation, hours_sick, hours_holiday, contribution_401k_override} — hours values are JSON strings (e.g. "40") or null, per D-06 and PATTERNS.md lines 97-98
  - expected.extracted.pay_period_start: "YYYY-MM-DD" — REQUIRED, NON-NULL on every fixture (Codex HIGH fix: app/models/contracts.py:110 `Extracted.pay_period_start` is a required non-null date, and 04-02's _expected_to_extracted() validates the expected block through Extracted — a null here would raise ValidationError at scoring time). Use a realistic recent date, e.g. "2026-06-15".
  - expected.extracted.pay_period_end: null or "YYYY-MM-DD"
  - expected.reconciliation: list of per-name entries: {submitted_name, name_category, expected_source ("exact"|"alias"|"none"), expected_resolved (bool), expected_matched_employee_id (UUID string or null)}
  - expected.decision: {final_action ("process"|"request_clarification"), gate_reasons_contains (list of trigger substrings), unresolved_names (list of strings), missing_fields (list of strings)}

  NET_PAY is NOT labeled anywhere in expected (per D-02). Do not add a net_pay field.

  ROSTER REFERENCE (from seed.py — use these exact UUIDs and contact_emails):
  - Business 1: payroll@coastalcleaning.example — weekly; Maria Chen e0000001, James Okafor e0000002
  - Business 2: hr@metrodeli.example — weekly; David Reyes e0000003, Priya Nair e0000004, Daniel Reyes e0000007
  - Business 3: finance@summittech.example — biweekly; Thomas Bergmann e0000005, Sandra Kim e0000006
  Collision pair: David Reyes + Daniel Reyes share known_alias "D. Reyes" (verified seed.py:134-150, 245-261)

  FIXTURES TO WRITE (15 total — balance categories per D-11 to ensure enough PROCESS fixtures):

  01_exact_match_coastal.json — fixture_category: "exact"; single employee Maria Chen submitting under exact full_name; final_action: "process"; name_category: "exact"; expected_source: "exact"; expected_resolved: true; expected_matched_employee_id: "e0000001-0000-0000-0000-000000000001" (Maria Chen — verified against app/db/seed.py:80; Codex HIGH fix: was a malformed all-zeros 11-digit UUID)
  Body: casual weekly hours email from payroll@coastalcleaning.example naming "Maria Chen" with 40 regular hours.

  02_stored_alias_metro.json — fixture_category: "stored-alias"; David Reyes submitted as one of his known aliases (NOT "D. Reyes" — that's the collision alias; use an alias that resolves uniquely to David Reyes only — check seed.py for his aliases); final_action: "process" IF the alias is unique to him (confirm from seed); name_category: "stored-alias"
  If David Reyes has no alias other than "D. Reyes", use Priya Nair with a confirmed unique alias from seed.py. Verify before writing.

  03_collision_metro.json — fixture_category: "collision"; submitted_name "D. Reyes" (the shared alias of David Reyes + Daniel Reyes); final_action: "request_clarification"; name_category: "collision"; expected_source: "none"; expected_resolved: false; expected_matched_employee_id: null; gate_reasons_contains: ["D. Reyes"]

  04_unknown_shorthand_metro.json — fixture_category: "unknown"; submitted_name is a realistic shorthand/nickname NOT in any employee's known_aliases (e.g. "Dave Reyes" or "P. Nair" if neither is a stored alias — verify against seed.py known_aliases); final_action: "request_clarification"; name_category: "unknown"; expected_source: "none"; expected_resolved: false

  05_typo_coastal.json — fixture_category: "typo"; submitted_name is a CHARACTER-LEVEL typo of James Okafor (e.g. "James Okafr" — a dropped letter), NOT a whitespace or punctuation variant (Codex MEDIUM fix: reconcile_names.py:32 casefolds + collapses whitespace, so "James  Okafor" with extra spaces normalizes to an EXACT match, not a typo). James has known_aliases=[] (seed.py:103), so "J. Okafor" would also be unresolved — but prefer the dropped-letter form for an unambiguous typo. final_action: "request_clarification"; name_category: "typo"; expected_source: "none"; expected_resolved: false

  06_first_time_alias_metro.json — fixture_category: "first-time-alias"; submitted_name is a novel nickname not in any employee's known_aliases (e.g. "Priya" as a first-name-only submission for Priya Nair — verify Priya Nair has no "Priya" alias in seed); final_action: "request_clarification"; name_category: "first-time-alias"; expected_source: "none"; expected_resolved: false

  07_missing_hours_coastal.json — fixture_category: "missing-hours"; Maria Chen (e0000001, hourly — seed.py:84; Codex MEDIUM fix: James Okafor is SALARY per seed.py:104, a bad missing-hours candidate, so use the hourly employee) submitted with ALL FIVE hours fields null; final_action: "request_clarification"; name_category: "exact" (the name resolves fine but hours are missing); gate_reasons_contains: ["hours"]. (NOTE: 07's cache stays FAITHFUL — the precision-miss divergence lives on a multi-employee fixture as a PHANTOM employee, and the field-miss divergence lives on 08; see the CACHED _EXTRACTION.JSON section. Fabricating Maria's hours here would be a same-name FIELD miss, not the invention/precision case.)

  08_vague_hours_coastal.json — fixture_category: "vague-hours"; email body describes hours vaguely ("same as usual" or "about 40 hours") — extraction is expected to return null for hours fields (the extractor cannot parse a vague number); name resolves exactly; final_action: "request_clarification"; name_category: "exact"

  09_buried_reply_metro.json — fixture_category: "buried-reply"; email body is a forwarded/reply chain where the payroll hours are buried mid-thread (realistic: ">> From last week: David Reyes 40hrs\nAlso Priya 35 regular"); two employees; both resolve exactly from their seeded roster; final_action: "process"; name_category for both: "exact"

  10_multi_employee_coastal.json — fixture_category: "exact" (primary; the fixture covers multiple name categories across employees); two employees: Maria Chen (exact) + James Okafor submitted as a CHARACTER-LEVEL typo (e.g. "Jame Okafor" — dropped letter; Codex MEDIUM fix: NOT "extra space", which normalizes to exact); so this fixture has: one exact match + one typo → final_action: "request_clarification" because one name is unresolved; reconciliation has two entries with name_category "exact" and "typo" respectively. This is a MULTI-EMPLOYEE fixture covering two name categories in one email (D-18).

  11_multi_employee_metro.json — fixture_category: "collision" (primary); three employees submitted: David Reyes (exact), "D. Reyes" (collision), Priya Nair (exact); the collision on D. Reyes forces request_clarification; reconciliation has three entries: "exact", "collision", "exact"; final_action: "request_clarification"

  12_exact_process_summit.json — fixture_category: "exact"; Thomas Bergmann submitted with exact full name, 0 hours (salaried); final_action: "process"; name_category: "exact"; expected_resolved: true; expected_matched_employee_id: "e0000005-0000-0000-0000-000000000005" (Thomas Bergmann — verified against app/db/seed.py:193; Codex HIGH fix: was a malformed all-zeros UUID); PROCESS fixture required for D-11

  13_exact_process_metro.json — fixture_category: "exact"; David Reyes (e0000003, hourly — seed.py:139) submitted by his EXACT FULL NAME "David Reyes" with 40 regular hours (Codex MEDIUM fix: prior version used James Okafor + hourly hours, but James is SALARY per seed.py:104). Submitting the full name resolves EXACTLY — the collision is only on the SHARED ALIAS "D. Reyes", not on the full name (seed.py:138), so "David Reyes" is unambiguous. final_action: "process"; name_category: "exact"; expected_matched_employee_id: "e0000003-0000-0000-0000-000000000003"; from_addr is Metro/business 2's contact_email. PROCESS fixture required for D-11; distinct from 01 (Maria/Coastal). Keep its cache faithful (clean process baseline — not a divergent cache).

  14_collision_process_summit.json — fixture_category: "stored-alias" OR "exact"; Sandra Kim (e0000006) submitted with her exact name; final_action: "process"; name_category: "exact" (or "stored-alias" per her aliases in seed); PROCESS fixture to complement the clarify-heavy suite (D-11)

  15_stored_alias_process_coastal.json — fixture_category: "stored-alias"; an employee submitted via a confirmed unique stored alias (NOT D. Reyes collision alias); final_action: "process"; name_category: "stored-alias"; verify the alias and matched_employee_id from seed.py before writing

  BEFORE WRITING FIXTURES: read app/db/seed.py fully to confirm:
  - Exact UUIDs for all 7 employees
  - known_aliases for each employee (to avoid using collision alias "D. Reyes" in a non-collision fixture, and to correctly pick unique aliases for stored-alias fixtures)
  - pay_type (hourly/salaried) for each employee (to correctly flag missing-hours for hourly only)

  CACHED _EXTRACTION.JSON (committed beside each fixture per D-05):
  For each fixture NNN_name.json, create NNN_name_extraction.json — the cached extractor output the eval REPLAYS so --check needs no live LLM.

  HONESTY / CREDIBILITY RULE (Codex HIGH fix — this was the single most damaging finding): these committed caches are a HAND-AUTHORED PLACEHOLDER standing in for real recorded extractor output until `--record` (04-02) regenerates them live. They are NOT a separate label — the label is the fixture's `expected.extracted` block. Two consequences the executor MUST honor:

  1. The extraction metric (D-06) only carries information if the cache can DIVERGE from `expected.extracted`. If every cache is a perfect copy of expected, extraction F1 and field_accuracy are vacuously 1.0 everywhere and the metric proves nothing (it would advertise a guard against LLM invention while testing zero invention). Therefore:
     - MOST caches mirror `expected.extracted` (a competent extractor on a clean email).
     - AT LEAST TWO caches DELIBERATELY DIVERGE so the metric is exercised and the committed summary.json shows F1/field_accuracy < 1.0 for those categories:
        • One INVENTION case (PRECISION miss — a PHANTOM EMPLOYEE, NOT a same-name hours change): the cache must add an EXTRA employee whose NAME is NOT in `expected.extracted` (so the validator's multiset `cache_names - exp_names` is non-empty and `false_positives > 0`). Codex Round-3 fix: fabricating hours for an employee who IS in expected is a FIELD miss, not a precision miss — it would NOT satisfy `precision_miss_fixtures >= 1`. Recommended host: a MULTI-EMPLOYEE fixture (10_multi_employee_coastal or 11_multi_employee_metro) where the cache lists the real employees PLUS one phantom name the email never contained, with fabricated hours. This is the exact failure validate.py provably cannot catch (it sees any_hours=True and passes a fabricated line). The fixture's `expected.extracted` reflects the TRUTH (no phantom), so precision drops below 1.0.
        • One FIELD-ERROR case (FIELD_ACCURACY miss — SAME employee set, wrong hours value): the cache reports exactly the expected employee NAMES but a wrong hours value on at least one matched employee (so the validator's field-miss branch fires: the name is in both, hours differ). Recommended host: 08_vague_hours_coastal (expected hours are null; the cache "guesses" hours_regular "40" for the SAME employee) — that is a clean field miss because the employee name matches expected, only the hours diverge. (Keep these two divergences on DIFFERENT fixtures so one satisfies precision and the other satisfies field — the validator asserts BOTH independently.)
     - DOCUMENTATION (Codex MEDIUM fix — no `_note` key in the cache): the cache file MUST be exactly the Extracted JSON and nothing else, because `Extracted` has extra="forbid" and would reject an extra `_note` key at validation time. Record which fixtures carry a deliberate divergence in a single committed sidecar doc `eval/fixtures/DIVERGENCES.md` (a 3-line table: fixture · divergence type · why). Do NOT add any non-schema key to the cache JSON.

  2. After --record runs live (04-02 / workflow_dispatch), these placeholders are overwritten by genuine extractor output and the divergences become whatever the real model actually does — at which point the metric measures the real extractor. The placeholders just make the metric demonstrably non-trivial on day one.

  Schema: {run_id: "00000000-0000-0000-0000-000000000000", employees: [...], pay_period_start: "...", pay_period_end: null}
  hours values are JSON strings (e.g. "40.00") or null — match contracts.py Decimal serialization. Extracted has extra="forbid" and pay_period_start is REQUIRED (non-null) — every cache must carry a valid pay_period_start.
  Validate each _extraction.json would pass Extracted.model_validate() — verify against app/models/contracts.py fields.

  EXPECTED day-one scoring (so the committed summary.json is honest, not a wall of 1.0s):
  - extraction_overall_f1 and extraction_overall_field_accuracy are BOTH < 1.0 (the two divergent caches pull them down).
  - the invention fixture's category shows precision < 1.0 (false_positive recorded).
  - the field-error fixture's category shows field_accuracy < 1.0.

  IMPORTANT: For the D-13 taxonomy honesty caveat, ensure the body_text for vague-hours / buried-reply / first-time-alias fixtures is genuinely ambiguous or novel so the stub extraction accurately reflects what a real extractor would produce (null hours for vague, full chain for buried-reply).
  </action>

  <verify>
    <automated>uv run python -c "
import json, pathlib, sys
from app.models.contracts import InboundEmail
from app.models.contracts import Extracted
from app.db.seed import seed

seeded = seed(dry_run=True)
seeded_emails = {b['contact_email'] for b in seeded.businesses}
# Map business contact_email -> set of that business's employee UUID strings (for per-fixture roster id checks)
biz_by_email = {b['contact_email']: b['id'] for b in seeded.businesses}
emp_ids_by_biz = {}
for e in seeded.employees:
    emp_ids_by_biz.setdefault(str(e.business_id), set()).add(str(e.id))
fixture_dir = pathlib.Path('eval/fixtures')
fixtures = sorted(fixture_dir.glob('*.json'))
# Only check fixture files, not extraction caches
main_fixtures = [f for f in fixtures if '_extraction' not in f.name]
extraction_caches = [f for f in fixtures if '_extraction' in f.name]

print(f'Fixture files: {len(main_fixtures)}, Extraction caches: {len(extraction_caches)}')
assert len(main_fixtures) >= 15, f'Expected >=15 fixtures, got {len(main_fixtures)}'
assert len(extraction_caches) == len(main_fixtures), 'Each fixture must have an _extraction.json'

VALID_CATS = {'exact','stored-alias','first-time-alias','typo','collision','unknown','missing-hours','vague-hours','buried-reply'}
for f in main_fixtures:
    raw = json.loads(f.read_text())
    assert 'fixture_category' in raw, f'{f.name}: missing fixture_category'
    assert raw['fixture_category'] in VALID_CATS, f'{f.name}: invalid category {raw[\"fixture_category\"]}'
    assert 'expected' in raw, f'{f.name}: missing expected block'
    inp = {k: v for k, v in raw.items() if k not in ('expected','fixture_category')}
    email = InboundEmail.model_validate(inp)
    assert email.from_addr in seeded_emails, f'{f.name}: from_addr {email.from_addr} not in seed'
    # Resolve the roster (business) this fixture binds to, for id-existence checks
    biz_id = str(biz_by_email[email.from_addr])
    roster_ids = emp_ids_by_biz.get(biz_id, set())
    exp = raw['expected']
    assert 'extracted' in exp, f'{f.name}: expected.extracted missing'
    # Codex HIGH fix: pay_period_start must be non-null (Extracted requires it; _expected_to_extracted validates through it)
    assert exp['extracted'].get('pay_period_start'), f'{f.name}: expected.extracted.pay_period_start must be a non-null YYYY-MM-DD date'
    assert 'reconciliation' in exp, f'{f.name}: expected.reconciliation missing'
    assert 'decision' in exp, f'{f.name}: expected.decision missing'
    assert 'net_pay' not in exp, f'{f.name}: net_pay must NOT be in expected (D-02)'
    for rec in exp['reconciliation']:
        assert 'name_category' in rec, f'{f.name}: reconciliation entry missing name_category (D-03)'
        assert rec['name_category'] in VALID_CATS, f'{f.name}: invalid name_category {rec[\"name_category\"]}'
        # Codex HIGH fix: every non-null expected_matched_employee_id MUST be a real employee in THIS fixture's roster
        # (catches the malformed/wrong UUIDs that would otherwise silently land); unresolved entries must be null.
        emid = rec.get('expected_matched_employee_id')
        if emid is not None:
            assert emid in roster_ids, f'{f.name}: expected_matched_employee_id {emid} not in roster for {email.from_addr} (valid: {sorted(roster_ids)})'
        else:
            assert rec.get('expected_resolved') in (False, None), f'{f.name}: null matched id but expected_resolved is true'
    assert exp['decision']['final_action'] in ('process','request_clarification'), f'{f.name}: bad final_action'

for ef in extraction_caches:
    data = json.loads(ef.read_text())
    Extracted.model_validate(data)

# D-11: must have at least 4 PROCESS fixtures
process_count = sum(1 for f in main_fixtures
    if json.loads(f.read_text())['expected']['decision']['final_action'] == 'process')
print(f'PROCESS fixtures: {process_count}')
assert process_count >= 4, f'Need >=4 PROCESS fixtures for D-11 always-clarify baseline, got {process_count}'

# Codex HIGH+MEDIUM fix: the extraction metric must be exercised by BOTH error directions, not just '2 divergent'.
# Enforce >=1 PRECISION miss (a cache employee not in expected — invention/duplicate) AND >=1 FIELD miss
# (a matched employee with a wrong hours field). Use MULTISET (Counter) alignment so a duplicate-only
# divergence is NOT collapsed away (the same bug we fixed in the scorer must not reappear in the validator).
from collections import Counter
def _emp_key(e): return ' '.join(str(e['submitted_name']).casefold().split())
HOURS = ('hours_regular','hours_overtime','hours_vacation','hours_sick','hours_holiday')
def _norm_hours(e): return tuple((None if e.get(k) is None else str(e.get(k))) for k in HOURS)
precision_miss_fixtures = 0
field_miss_fixtures = 0
for f in main_fixtures:
    raw = json.loads(f.read_text())
    cache = json.loads((f.parent / (f.stem + '_extraction.json')).read_text())
    exp_names = Counter(_emp_key(e) for e in raw['expected']['extracted']['employees'])
    cache_names = Counter(_emp_key(e) for e in cache['employees'])
    # precision miss = any cache employee/duplicate beyond what expected has (multiset difference, NOT set)
    if sum((cache_names - exp_names).values()) > 0:
        precision_miss_fixtures += 1
    # field miss = a name present in BOTH but with mismatched hours on at least one matched instance
    exp_hours = {}
    for e in raw['expected']['extracted']['employees']:
        exp_hours.setdefault(_emp_key(e), []).append(_norm_hours(e))
    for e in cache['employees']:
        k = _emp_key(e)
        if k in exp_hours and _norm_hours(e) not in exp_hours[k]:
            field_miss_fixtures += 1
            break
print(f'Precision-miss caches: {precision_miss_fixtures}, Field-miss caches: {field_miss_fixtures}')
assert precision_miss_fixtures >= 1, 'Need >=1 cache with an invented/duplicate employee (precision miss) so extraction precision < 1.0 is demonstrable (D-06 invention guard)'
assert field_miss_fixtures >= 1, 'Need >=1 cache with a wrong hours value on a matched employee (field-accuracy miss) so field_accuracy < 1.0 is demonstrable (EVAL-04)'

print('All fixture validations passed.')
"
</automated>
  </verify>

  <acceptance_criteria>
    - `uv run python -c "..."` validation above exits 0 with "All fixture validations passed."
    - `ls eval/fixtures/*.json | wc -l` returns 30 (15 fixture files + 15 extraction stubs)
    - At least 4 fixtures have `final_action: "process"` (D-11 always-clarify baseline guard)
    - At least 2 fixtures are multi-employee (D-18 — cover multiple name_categories in one file)
    - `eval/fixtures/03_collision_metro.json` contains `"fixture_category": "collision"` and `"final_action": "request_clarification"` with `"D. Reyes"` as submitted_name (covers the collision pair)
    - No fixture's `expected` block contains a `net_pay` key (D-02 prohibition)
    - Every non-null `expected_matched_employee_id` is a real employee UUID in that fixture's seeded roster (the validation script asserts this against seed(dry_run=True)); the two PROCESS fixtures 01/12 use `e0000001-…001` (Maria Chen) and `e0000005-…005` (Thomas Bergmann) respectively — verified-real, not all-zeros
    - At least 2 `_extraction.json` caches DIVERGE from their fixture's `expected.extracted` (≥1 invention/precision miss + ≥1 field-accuracy miss) so the committed summary.json shows extraction F1 and field_accuracy < 1.0 (the D-06 metric is exercised, not vacuous)
    - All _extraction.json files validate as `Extracted.model_validate()` without error
    - `eval/draft_candidate_emails.py` exists and its first 5 lines contain "throwaway" or "drafting aid" in a docstring (EVAL-01 honest naming)
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Bootstrap drafting helper — draft_candidate_emails.py (D-19, EVAL-01)</name>

  <read_first>
    .planning/phases/04-the-eval-the-proof/04-PATTERNS.md — draft_candidate_emails.py pattern (lines 392-431): exact call_text pattern with tier="draft", allow_live_llm gate, ~20-line structure
    .planning/phases/04-the-eval-the-proof/04-CONTEXT.md — D-19 (Kimi draft tier, NOT DeepSeek, anti-leakage rule; ~20 lines, throwaway, docstring says throwaway)
    app/llm/client.py — call_text(tier, messages, temperature) signature (lines 155-183)
    app/config.py — allow_live_llm flag (line ~50), draft_api_key field
  </read_first>

  <action>
  Create eval/draft_candidate_emails.py. This is a ~20-line throwaway helper; keep it minimal.

  MODULE DOCSTRING (first thing in the file, per EVAL-01 honest naming):
  Triple-quoted docstring that says: "Throwaway bootstrap drafting aid — NOT a production generator. Prompts the Kimi draft model to produce candidate messy payroll emails for the builder to hand-edit and hand-label into eval/fixtures/. The committed fixtures are the source of truth; this script just provides phrasing variety. Delete or ignore after the fixture corpus is built."

  STRUCTURE:
  1. Import get_settings from app.config
  2. Import call_text from app.llm.client
  3. Define _require_live_llm() — check settings.allow_live_llm (raise SystemExit if False), check settings.draft_api_key (raise SystemExit if empty) — per PATTERNS.md lines 424-430
  4. if __name__ == "__main__": call _require_live_llm(); draft = call_text(tier="draft", messages=[{"role":"user","content":"Draft a realistic but intentionally messy payroll email for a small business. Include at least one name abbreviation or nickname that might not match the employee's full name. Be casual and informal, like a real small-business payroll submission. Include a random mix of regular/OT hours. Sign off with a name."}], temperature=0.9); print(draft or "No content returned — retry or hand-write the fixture.")

  ANTI-LEAKAGE (D-19): tier="draft" is Kimi, NOT tier="extraction" (DeepSeek). The routing is config-driven via call_text — no hardcoded model name in this file.

  DO NOT add complex logic — this is a ~20-line script. No argparse, no file writes, no fixture-schema generation. The builder copies printed output, edits it, and writes the JSON by hand.
  </action>

  <verify>
    <automated>uv run python -c "
import ast, pathlib
src = pathlib.Path('eval/draft_candidate_emails.py').read_text()
assert 'throwaway' in src.lower() or 'drafting aid' in src.lower(), 'Missing honest throwaway docstring'
assert 'tier=\"draft\"' in src or \"tier='draft'\" in src, 'Must use tier=draft (Kimi), not extraction tier'
assert 'allow_live_llm' in src, 'Must gate on allow_live_llm'
assert 'draft_api_key' in src, 'Must check draft_api_key'
# Verify it parses cleanly
ast.parse(src)
print('draft_candidate_emails.py structure OK')
"
</automated>
  </verify>

  <acceptance_criteria>
    - File is ~15-25 lines (no over-engineering)
    - Module-level docstring contains "throwaway" (honesty per EVAL-01)
    - Uses `tier="draft"` in the call_text call — never `tier="extraction"` (D-19 anti-leakage)
    - Gates on `settings.allow_live_llm` — raises SystemExit if False
    - Running without ALLOW_LIVE_LLM=true raises SystemExit with a message about the flag
    - `ast.parse()` succeeds (no syntax errors)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON → InboundEmail.model_validate | Untrusted fixture body enters Pydantic; extra="forbid" rejects unknown fields |
| draft_candidate_emails.py → Kimi API | Live outbound call only when allow_live_llm=true AND draft_api_key present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | eval/fixtures/*.json body_text | mitigate | All fixture emails are synthetic/hand-curated; no real PII or real client data committed — docstring in each fixture uses fictional employees from seed.py only |
| T-04-02 | Information Disclosure | draft_candidate_emails.py output | accept | Script only prints to stdout (never commits); builder reviews before adding to fixtures; no key material in output |
| T-04-03 | Tampering | _extraction.json stubs | accept | Stubs are hand-authored deterministic data; no executable code; loaded read-only by run_eval.py |
| T-04-SC | Tampering | pyproject.toml uv add --dev matplotlib (04-03) | mitigate | matplotlib is a dev-only dep — never imported in app/ runtime; Docker --no-dev flag keeps it out of the image |
</threat_model>

<verification>
Run after both tasks complete:

```bash
# All fixtures validate (Task 1)
uv run python -c "
import json, pathlib
from app.models.contracts import InboundEmail, Extracted
from app.db.seed import seed
seeded = seed(dry_run=True)
seeded_emails = {b['contact_email'] for b in seeded.businesses}
fixture_dir = pathlib.Path('eval/fixtures')
main_fixtures = [f for f in sorted(fixture_dir.glob('*.json')) if '_extraction' not in f.name]
extraction_caches = [f for f in sorted(fixture_dir.glob('*.json')) if '_extraction' in f.name]
print(f'Fixtures: {len(main_fixtures)}, Caches: {len(extraction_caches)}')
for f in main_fixtures:
    raw = json.loads(f.read_text())
    inp = {k: v for k, v in raw.items() if k not in ('expected','fixture_category')}
    InboundEmail.model_validate(inp)
for ef in extraction_caches:
    Extracted.model_validate(json.loads(ef.read_text()))
print('OK')
"

# Bootstrap helper syntax (Task 2)
uv run python -c "import ast,pathlib; ast.parse(pathlib.Path('eval/draft_candidate_emails.py').read_text()); print('syntax OK')"
```
</verification>

<success_criteria>
- 15 hand-labeled eval fixture files in eval/fixtures/, each with a full expected block and a committed _extraction.json stub (D-01, D-02, D-03, D-04, D-05, D-18)
- Fixture corpus spans all 9 taxonomy categories; has at least 4 PROCESS fixtures; has at least 2 multi-employee fixtures (D-11, D-18)
- All fixtures pass InboundEmail.model_validate on their input portion; all extraction stubs pass Extracted.model_validate (D-04)
- eval/draft_candidate_emails.py: throwaway docstring, tier="draft" (Kimi, not DeepSeek), allow_live_llm gate (D-19, EVAL-01)
- EVAL-01 and EVAL-02 fully satisfied by this plan
</success_criteria>

<output>
Create .planning/phases/04-the-eval-the-proof/04-01-SUMMARY.md when done.
</output>
