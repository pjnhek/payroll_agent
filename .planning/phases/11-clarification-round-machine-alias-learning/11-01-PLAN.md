---
phase: 11-clarification-round-machine-alias-learning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/schema.sql
  - app/models/status.py
  - app/db/repo.py
  - tests/conftest.py
autonomous: true
requirements: [CLAR2-01, CLAR2-02, CLAR2-06, CLAR2-07]
must_haves:
  truths:
    - "schema.sql declares clarification_round, email_messages.round (NOT NULL DEFAULT 0), consumed_round, needs_operator in BOTH status CHECK spots, and the widened uq_email_run_purpose_round constraint"
    - "insert_email_message's ON CONFLICT arbiter matches the new 3-column constraint (no InvalidColumnReference possible)"
    - "The full offline suite stays green — nothing behavioral changes in this plan (round defaults to 0 everywhere)"
  artifacts:
    - path: "app/db/schema.sql"
      provides: "clarification_round / round / consumed_round columns, needs_operator status, uq_email_run_purpose_round, backfill + alias-shape one-shot migration DO-blocks"
      contains: "uq_email_run_purpose_round"
    - path: "app/models/status.py"
      provides: "RunStatus.NEEDS_OPERATOR enum member"
      contains: "NEEDS_OPERATOR"
    - path: "app/db/repo.py"
      provides: "get_clarification_round, set_clarification_round, get_outbound_for_round, mark_reply_consumed, load_consumed_replies, get_inbound_by_message_id, clear_reply_context, find_stranded_unconsumed_replies, insert_email_message round param"
      contains: "def get_outbound_for_round"
    - path: "tests/conftest.py"
      provides: "InMemoryRepo mirrors for every new repo function"
      contains: "def get_outbound_for_round"
  key_links:
    - from: "app/db/repo.py insert_email_message"
      to: "app/db/schema.sql uq_email_run_purpose_round"
      via: "ON CONFLICT (run_id, purpose, round) arbiter — must match the unique constraint exactly"
      pattern: "ON CONFLICT \\(run_id, purpose, round\\)"
    - from: "app/models/status.py"
      to: "app/db/schema.sql status CHECK (both spots)"
      via: "tests/test_status_drift.py parity forcing function"
      pattern: "needs_operator"
---

<objective>
Land the durable foundations for the Phase 11 round machine: the round/consumed-round columns and widened uniqueness (D-11-01, D-11-02), the `needs_operator` status value (D-11-06), all new repo access functions (D-11-13, D-11-04, D-11-05 data layer), and faithful InMemoryRepo mirrors — with ZERO behavior change (round defaults to 0; no orchestrator/main code reads the new state yet).

Purpose: every later plan (guard re-key, cap/escape, accumulation, bind, redelivery) is pure code over these persisted facts. Landing schema+repo+fakes first keeps each later plan single-concern and keeps the offline suite green throughout.
Output: updated schema.sql, status.py, repo.py, conftest.py; offline suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clarification-round-machine-alias-learning/11-CONTEXT.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema DDL — columns, needs_operator status, widened uniqueness, backfills (D-11-01, D-11-02, D-11-06)</name>
  <files>app/db/schema.sql, app/models/status.py</files>
  <read_first>
    - app/db/schema.sql (FULL FILE — the inline status CHECK at ~:68-80, the column-anchored CHECK drop/re-ADD DO-block at ~:141-171, the idempotent column-add block at ~:109-115, the named-constraint guard at ~:263-278, the D-7.5-03a atomic-DO comment at ~:126-129, the NULL-distinctness comment at ~:219-220)
    - app/models/status.py (the RunStatus StrEnum and its "Ten-state" docstring)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md (§ app/db/schema.sql — exact copy targets)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Code Examples §1, Runtime State Inventory, Pitfalls #1/#2, Open Question #3)
  </read_first>
  <action>
    Implements D-11-01, D-11-02 (columns), D-11-06 (status), and the Runtime State Inventory backfills.

    In app/db/schema.sql:
    1. Idempotent column adds (copy the ADD COLUMN IF NOT EXISTS idiom at ~:109-115):
       `payroll_runs.clarification_round INT NOT NULL DEFAULT 0`;
       `email_messages.round INT NOT NULL DEFAULT 0` (NOT NULL is load-bearing — Pitfall #2: a nullable round makes UNIQUE treat NULLs as distinct and silently disables confirmation dedup);
       `email_messages.consumed_round INT` (nullable — NULL means unconsumed, per D-11-02).
       Add these BEFORE the constraint DO-block so a fresh bootstrap and a live migration both work.
       Also add `round INT NOT NULL DEFAULT 0` and `consumed_round INT` to the inline CREATE TABLE email_messages definition, and `clarification_round INT NOT NULL DEFAULT 0` to the inline CREATE TABLE payroll_runs definition, so fresh bootstraps match migrated DBs.
    2. Add `'needs_operator'` to the payroll_runs status CHECK in BOTH places: the inline CREATE TABLE CHECK (~:68-80) AND the column-anchored drop/re-ADD DO-block (~:141-171). tests/test_status_drift.py parses both; missing one fails CI by design.
    3. Widen the outbound uniqueness in ONE atomic DO-block (per the D-7.5-03a comment — a failed ADD rolls back the DROP): IF constraint `uq_email_run_purpose` exists on email_messages THEN DROP it; IF constraint `uq_email_run_purpose_round` does not exist THEN ADD `UNIQUE (run_id, purpose, round)` named `uq_email_run_purpose_round`. RESEARCH Code Examples §1 has the exact block.
    4. Backfill DO-block (idempotent, one-shot-safe): set `payroll_runs.clarification_round` = the count of that run's email_messages rows with direction='outbound', purpose IN ('clarification','clarification_field_regression'), send_state='sent' (Open Question #3 formula — deterministic and re-runnable because under the new code counter == sent-round-row count).
    5. One-shot alias_candidates shape migration DO-block (Pitfall #6): for payroll_runs rows where alias_candidates IS NOT NULL, transform each top-level entry whose jsonb value type is NOT 'object': JSON null value becomes `{"suggested": null, "bound": null}`; a string value X becomes `{"suggested": null, "bound": X}`. Entries already objects are left untouched (idempotent). Use jsonb_each + jsonb_object_agg + jsonb_typeof inside the DO-block.

    In app/models/status.py: append `NEEDS_OPERATOR = "needs_operator"` to RunStatus; update the "Ten-state" docstring to "Eleven-state" and document the state's meaning (round cap reached — operator resolve+resume or reject; NOT in sweep/retrigger/auto-resume scopes, D-11-06).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_status_drift.py && grep -c "needs_operator" app/db/schema.sql | awk '$1 >= 2 {exit 0} {exit 1}' && grep -q "uq_email_run_purpose_round" app/db/schema.sql && grep -q "consumed_round" app/db/schema.sql && grep -q "NEEDS_OPERATOR" app/models/status.py</automated>
  </verify>
  <acceptance_criteria>
    - `app/db/schema.sql` contains `clarification_round INT NOT NULL DEFAULT 0`, `round INT NOT NULL DEFAULT 0` (email_messages), and `consumed_round INT`
    - `needs_operator` appears in BOTH the inline CREATE TABLE CHECK and the DO-block re-ADD list (grep count ≥ 2)
    - One DO-block both drops `uq_email_run_purpose` (if exists) and adds `uq_email_run_purpose_round UNIQUE (run_id, purpose, round)` (if absent)
    - A backfill statement computes clarification_round from sent outbound rows with purpose IN ('clarification','clarification_field_regression')
    - The alias_candidates migration only rewrites entries whose jsonb_typeof is not 'object' (idempotent re-run safe)
    - `app/models/status.py` contains `NEEDS_OPERATOR = "needs_operator"`
    - `uv run pytest -q tests/test_status_drift.py` exits 0
  </acceptance_criteria>
  <done>Schema and enum agree on eleven statuses; all new columns and the widened constraint exist in DDL; backfills are idempotent; drift guard green.</done>
</task>

<task type="auto">
  <name>Task 2: repo.py — round/consumed accessors, clear-context, stranded query, ON CONFLICT lockstep (D-11-01, D-11-02, D-11-04, D-11-13)</name>
  <files>app/db/repo.py</files>
  <read_first>
    - app/db/repo.py (get_outbound_message_id :1054-1080 — the purpose-whitelist guard + send_state='sent' filter to preserve; link_email_to_run :199-223 — THE `_conn_ctx(conn) as (c, owns)` + `c.transaction() if owns else _nulltx()` write idiom; load_thread_messages :1257-1278 — dict_row multi-row query idiom; insert_email_message :963-1051 — the ON CONFLICT (run_id, purpose) upsert to change in lockstep; claim_status :422; sweep_stranded_runs :459 + _STRANDED_SCOPE_STATUSES comment :450-456)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md (§ app/db/repo.py — copy targets per function)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Pitfalls #1, #3 — get_outbound_for_round must return the row's round)
  </read_first>
  <action>
    All new functions follow the repo's `_conn_ctx(conn) as (c, owns)` convention with optional `conn=` so they can join `_clarify`'s finalize transaction. New functions:

    1. `get_clarification_round(run_id, conn=None) -> int` — SELECT clarification_round FROM payroll_runs; returns 0 if row missing.
    2. `set_clarification_round(run_id, value: int, conn=None) -> None` — plain UPDATE, caller-joinable transaction (copy link_email_to_run's shape).
    3. `get_outbound_for_round(run_id, purpose: str, round: int, conn=None) -> dict | None` — copy get_outbound_message_id's shape INCLUDING the purpose whitelist ValueError guard (T-05-09b) and the `send_state = 'sent'` proof-of-delivery filter, adding `AND round = %s`; return a dict with at least `{"message_id": ..., "round": ...}` (Pitfall #3: callers derive the idempotent next round from the FOUND ROW's round, never a blind +1).
    4. `mark_reply_consumed(message_id: str, round: int, conn=None) -> None` — UPDATE email_messages SET consumed_round = %s WHERE message_id = %s AND direction = 'inbound' AND consumed_round IS NULL (write-once: never overwrite an existing consumed_round).
    5. `load_consumed_replies(run_id, conn=None) -> list[dict]` — copy load_thread_messages' dict_row shape; filter direction='inbound' AND consumed_round IS NOT NULL; ORDER BY consumed_round ASC; select at least body_text, subject, message_id, from_addr, consumed_round, created_at.
    6. `get_inbound_by_message_id(message_id: str, conn=None) -> dict | None` — dict_row single-row lookup on email_messages WHERE message_id = %s AND direction = 'inbound'; select run_id, message_id, subject, body_text, from_addr, to_addr, consumed_round (WR-04 redelivery reads the PERSISTED row, Pitfall #11a).
    7. `clear_reply_context(run_id, conn=None) -> None` — single UPDATE payroll_runs SET clarified_fields = NULL, pre_clarify_extracted = NULL, clarification_round = 0, alias_candidates = NULL (D-11-04: "context lost means ALL of it" — clarified snapshot, round counter, AND suggestion/candidate state). Caller-joinable transaction.
    8. `find_stranded_unconsumed_replies(threshold_seconds: int, conn=None) -> list[dict]` — dict_row query joining email_messages (direction='inbound', consumed_round IS NULL, run_id IS NOT NULL, created_at older than now() - threshold) to payroll_runs (status = 'awaiting_reply'); return reply-row dicts including run_id and the same fields as get_inbound_by_message_id. Add a pinned-scope comment in the _STRANDED_SCOPE_STATUSES style: this D-11-05 auto-resume scope is exactly `awaiting_reply` + unconsumed + stale, deliberately distinct from sweep and retrigger scopes.
    9. `insert_email_message` gains keyword param `round: int = 0` (default keeps all existing callers behavior-identical), writes it in the INSERT column list, and — in LOCKSTEP (Pitfall #1) — changes the upsert arbiter from `ON CONFLICT (run_id, purpose)` to `ON CONFLICT (run_id, purpose, round)`. Per-round retry (reserved→sent advancement, D-13c NEW-1) still upserts; a new round is a new row (D-11-01: no upsert-replace of history). Update the coupling comment at ~:980-987 to name the new constraint uq_email_run_purpose_round.
    Verify every existing caller of insert_email_message (grep `insert_email_message(` across app/) still compiles with the keyword default; do NOT thread a real round value anywhere yet — that is Plan 11-02.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration and not live_llm" && grep -q "ON CONFLICT (run_id, purpose, round)" app/db/repo.py && ! grep -q "ON CONFLICT (run_id, purpose) DO UPDATE" app/db/repo.py && grep -q "def find_stranded_unconsumed_replies" app/db/repo.py</automated>
  </verify>
  <acceptance_criteria>
    - repo.py contains `def get_clarification_round(`, `def set_clarification_round(`, `def get_outbound_for_round(`, `def mark_reply_consumed(`, `def load_consumed_replies(`, `def get_inbound_by_message_id(`, `def clear_reply_context(`, `def find_stranded_unconsumed_replies(`
    - get_outbound_for_round keeps the purpose whitelist ValueError and `send_state = 'sent'` filter and returns the row's round value
    - mark_reply_consumed includes `consumed_round IS NULL` in its WHERE (write-once)
    - clear_reply_context nulls clarified_fields, pre_clarify_extracted, alias_candidates AND zeroes clarification_round in one statement
    - insert_email_message signature includes `round: int = 0` and its ON CONFLICT lists (run_id, purpose, round); the 2-column arbiter no longer exists anywhere in repo.py
    - Full offline suite exits 0 (zero behavior change — round defaults to 0)
  </acceptance_criteria>
  <done>All Phase 11 data-layer primitives exist with repo-idiomatic signatures; the arbiter/constraint coupling is consistent; offline suite green.</done>
</task>

<task type="auto">
  <name>Task 3: conftest.py InMemoryRepo mirrors (Pitfall #7 — prerequisite for every later hermetic module)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (InMemoryRepo :236+ — especially the link_email_to_run mirror :288-297 with its rationale-docstring convention, the email row storage shape, the duplicated-scope-constant convention :247-250, and the insert_email_message fake)
    - app/db/repo.py (the just-written real semantics to mirror — this plan's Task 2)
  </read_first>
  <action>
    Mirror every Task-2 function on InMemoryRepo with REAL semantics (not stubs), each with a one-line rationale docstring naming the decision it serves (D-11-01/02/04/05/13):
    - Fake email rows gain `round` (default 0) and `consumed_round` (default None) keys wherever rows are constructed (insert_email_message fake, inbound insert fake, any fixture builders that hand-build email rows).
    - The insert_email_message mirror's upsert key becomes the (run_id, purpose, round) triple — a same-triple write updates in place (send_state/message_id advancement), a new round appends a new row.
    - `mark_reply_consumed` sets consumed_round only when currently None; `load_consumed_replies` filters inbound + consumed_round is not None, sorted by consumed_round; `get_inbound_by_message_id` returns the stored row dict or None; `clear_reply_context` nulls the four run fields; `find_stranded_unconsumed_replies` applies the awaiting_reply + unconsumed + age filter using the row's created_at (give fake rows a created_at if they lack one); `get_clarification_round`/`set_clarification_round` read/write a `clarification_round` key on the fake run dict (default 0); `get_outbound_for_round` filters direction/purpose/send_state='sent'/round and returns {"message_id", "round"}.
    - Follow the fake's convention of duplicating scope semantics rather than importing constants, so a production scope change forces a visible test failure.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration and not live_llm"</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py InMemoryRepo defines all nine mirrors: get_clarification_round, set_clarification_round, get_outbound_for_round, mark_reply_consumed, load_consumed_replies, get_inbound_by_message_id, clear_reply_context, find_stranded_unconsumed_replies, and the round-aware insert_email_message
    - Fake email rows carry round/consumed_round keys; fake runs carry clarification_round
    - Full offline suite exits 0 with no AttributeError from any monkeypatched repo surface
  </acceptance_criteria>
  <done>Hermetic test infrastructure fully mirrors the new repo surface; the offline suite is green and later plans can write tests without touching conftest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB constraint layer | uniqueness/dedup guarantees the send/confirmation lifecycle depends on |
| live-DB migration | DDL written here executes later against production Supabase (Plan 11-07 checkpoint) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | uq_email_run_purpose_round + nullable round | mitigate | `round INT NOT NULL DEFAULT 0` on email_messages — Postgres NULL-distinctness cannot disable confirmation dedup (Pitfall #2); pinned by grep + delivery tests |
| T-11-02 | Denial of Service | insert_email_message arbiter mismatch | mitigate | Constraint and ON CONFLICT changed in the same plan; grep gate asserts no 2-column arbiter remains (Pitfall #1) |
| T-11-03 | Information Disclosure | new repo functions logging | mitigate | No new log lines emit body_text/roster names; follow type-only/counts discipline |
| T-11-SC | Tampering | package installs | accept | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: nothing to verify); uv.lock unchanged |
</threat_model>

<verification>
- `uv run pytest -q -m "not integration and not live_llm"` — full offline suite green (zero behavior change)
- `uv run pytest -q tests/test_status_drift.py` — enum/CHECK parity across both schema.sql spots
- `grep -q "ON CONFLICT (run_id, purpose, round)" app/db/repo.py` — arbiter/constraint lockstep
</verification>

<success_criteria>
- All Phase 11 columns, the needs_operator status, and the widened constraint exist in schema.sql with idempotent, re-runnable DDL and backfills
- All nine repo primitives exist with repo-idiomatic conn= signatures; InMemoryRepo mirrors each faithfully
- Offline suite green; no production behavior change (nothing reads the new state yet)
</success_criteria>

<output>
Create `.planning/phases/11-clarification-round-machine-alias-learning/11-01-SUMMARY.md` when done.
</output>
