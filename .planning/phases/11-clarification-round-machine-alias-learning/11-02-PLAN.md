---
phase: 11-clarification-round-machine-alias-learning
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - app/pipeline/orchestrator.py
  - app/main.py
  - tests/test_clarify_rounds.py
  - tests/test_needs_operator.py
  - tests/test_stuck_run_recovery.py
autonomous: true
requirements: [CLAR2-01, CLAR2-02]
must_haves:
  truths:
    - "A round-2 clarification question actually sends (guard finds no sent row for the current round) — no run silently parks at awaiting_reply with no email out (WR-05 closed)"
    - "A re-trigger of the SAME round is still suppressed as a true duplicate (CLAR-04 preserved), and its early-return finalize advances the round idempotently"
    - "The 4th would-be send (clarification_round >= 3) escalates to needs_operator with NO LLM call and NO gateway send (D-11-07, D-11-09)"
    - "needs_operator is excluded from sweep scope, retrigger stale scope, and IN_FLIGHT_STATUSES, each pinned by an explicit test"
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "MAX_CLARIFICATION_ROUNDS constant, cap check at top of _clarify, (purpose, round) guard, idempotent round advance in all three finalize paths"
      contains: "MAX_CLARIFICATION_ROUNDS"
    - path: "app/main.py"
      provides: "needs_operator badge class/label; IN_FLIGHT_STATUSES unchanged (exclusion)"
      contains: "needs_operator"
    - path: "tests/test_clarify_rounds.py"
      provides: "CLAR2-01 proof: new-question-sends, same-round-suppressed, crash-idempotent round advance, D-9-02 AST order guard"
      min_lines: 80
    - path: "tests/test_needs_operator.py"
      provides: "CLAR2-02 proof: cap boundary, escalation write order, scope exclusions, badge rendering"
      min_lines: 60
  key_links:
    - from: "app/pipeline/orchestrator.py _clarify"
      to: "app/db/repo.py get_outbound_for_round"
      via: "(purpose, current_round) guard lookup replacing get_outbound_message_id"
      pattern: "get_outbound_for_round"
    - from: "app/pipeline/orchestrator.py _clarify finalize paths"
      to: "app/db/repo.py set_clarification_round"
      via: "sent-row-round + 1 write inside each finalize transaction, before set_status"
      pattern: "set_clarification_round"
---

<objective>
Fix WR-05 by re-keying `_clarify`'s idempotency guard to (purpose, round) over the explicit `clarification_round` counter (D-11-01/D-11-02), and add the round cap + `needs_operator` escalation (D-11-06/D-11-07/D-11-09) with its dashboard badge and scope exclusions. This closes CLAR2-01 and the escalation half of CLAR2-02.

Purpose: today a genuinely new round-2 question is silently swallowed by the purpose-only guard, permanently parking the run — the highest-priority traced failure of the phase (260705-02).
Output: round-aware _clarify, escalation path, badge, and the two new test modules proving both.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clarification-round-machine-alias-learning/11-CONTEXT.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: _clarify — cap check, (purpose, round) guard, idempotent round advance in all three finalize paths</name>
  <files>app/pipeline/orchestrator.py</files>
  <read_first>
    - app/pipeline/orchestrator.py (_clarify :986-1178 in full — the guard at :1022-1037, the record_only finalize at ~:1154-1158, the live-gateway finalize at ~:1175-1178; _defer_field_regression_clarification :746-834 — its step-5 _clarify call is the second call site the cap must cover; the _run_stages direct call at ~:980)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Patterns 1 & 2, Pitfall #3 — derive-from-found-row idempotency; Open Question #4 boundary reading)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md (§ orchestrator — finalize transaction shapes at all three exit paths)
    - app/main.py (:93-101 — the STALE_THRESHOLD documented-derivation comment style to copy for the cap constant)
  </read_first>
  <action>
    Implements D-11-01, D-11-02, D-11-06, D-11-07, D-11-09.

    1. Module-level constant in orchestrator.py: `MAX_CLARIFICATION_ROUNDS = 3` with a STALE_THRESHOLD-style derivation comment (D-11-07: counts ALL clarification sends per run regardless of purpose; boundary semantics — the counter increments at send, so counter==3 means three sends happened; the 4th would-be send escalates. This resolves RESEARCH Open Question #4: "3 total rounds" = 3 sends allowed).
    2. Cap check at the TOP of `_clarify`, before the guard and before ANY LLM or provider call (D-9-01 trivially satisfied): if `repo.get_clarification_round(run_id) >= MAX_CLARIFICATION_ROUNDS` → open repo.get_connection()/conn.transaction() and call `repo.set_status(run_id, RunStatus.NEEDS_OPERATOR, conn=conn)` as the LAST write (D-9-02), log a PII-safe line (run_id + round count only), and return. NO outbound email, NO new purpose (D-11-09 silent handoff). Placing it inside _clarify covers both call sites (_run_stages ~:980 and _defer_field_regression_clarification step 5 ~:826) with one check.
    3. Re-key the guard: replace `repo.get_outbound_message_id(run_id, purpose=purpose)` at :1022 with `current_round = repo.get_clarification_round(run_id)` then `existing = repo.get_outbound_for_round(run_id, purpose=purpose, round=current_round)`. `existing is not None` = true duplicate (crash-retrigger of the same round) → suppress the send and finalize; `None` = genuinely new question → proceed.
    4. Idempotent round advance (Pitfall #3) in ALL THREE finalize paths — early-return duplicate (:1033-1037), record_only (:1154-1158), live gateway (:1175-1178): inside each existing `with conn.transaction():` block, add `repo.set_clarification_round(run_id, sent_row_round + 1, conn=conn)` BEFORE the `repo.set_status(run_id, RunStatus.AWAITING_REPLY, conn=conn)` line, where sent_row_round is: the found row's round (early-return path, from get_outbound_for_round's returned dict) or current_round (the two send paths — the row was just written with round=current_round). NEVER a blind counter+1: derive from the sent row so a crash between send and finalize self-heals on re-entry.
    5. Stamp the round on the outbound row: the insert_email_message / send path for the clarification passes `round=current_round`. Trace the actual call chain from _clarify to insert_email_message (it may route through the gateway send helper) and thread the round kwarg through whichever function performs the outbound insert — the confirmation purpose in _deliver stays default round=0.
    Do NOT touch `is_round_2 = bool(clarified)` at :346 (Pitfall #12 — classify state, not round state) and do NOT modify the 7.5 clarified_fields machine.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration and not live_llm" && grep -q "MAX_CLARIFICATION_ROUNDS = 3" app/pipeline/orchestrator.py && grep -q "get_outbound_for_round" app/pipeline/orchestrator.py</automated>
  </verify>
  <acceptance_criteria>
    - orchestrator.py defines `MAX_CLARIFICATION_ROUNDS = 3` at module level with a derivation comment
    - _clarify's first statements check the cap BEFORE any llm/gateway usage; the escalation transaction's last write is set_status(NEEDS_OPERATOR)
    - No call to `get_outbound_message_id` remains inside _clarify (the (purpose, round) lookup replaces it); other callers of get_outbound_message_id (e.g. _deliver's confirmation guard) are untouched
    - All three finalize transactions contain a set_clarification_round call textually BEFORE their set_status call
    - The clarification outbound insert passes round=current_round; existing suite green
  </acceptance_criteria>
  <done>_clarify is round-aware end to end: new question sends, same-round duplicate suppressed with idempotent round advance, 4th send escalates silently to needs_operator.</done>
</task>

<task type="auto">
  <name>Task 2: main.py status surfaces — badge maps, IN_FLIGHT exclusion; scope-exclusion pins</name>
  <files>app/main.py, tests/test_stuck_run_recovery.py</files>
  <read_first>
    - app/main.py (_BADGE_CLASS/_BADGE_LABEL :185-212 and the accessor :217; IN_FLIGHT_STATUSES :111-113; the retrigger docstring :766-782 — "Do NOT 'fix' this into parity")
    - tests/test_stuck_run_recovery.py (:68-93 — the scope-pin assertion style to extend)
    - app/templates/run_detail.html and the project's CSS (locate the badge class definitions the _BADGE_CLASS values map to)
  </read_first>
  <action>
    Implements the D-11-06 dashboard surface and the CONTEXT.md requirement that needs_operator "must be added to the exclusion tests".

    1. Add `"needs_operator"` to BOTH `_BADGE_CLASS` and `_BADGE_LABEL` in main.py. Label: "Needs Operator". Class: a distinct attention-drawing class per D-11-06 ("own dashboard badge") — reuse an existing attention class if the CSS has one that is not already semantically taken (e.g. the class used for error/pending states is taken; if no distinct class exists, add one small CSS rule following the existing badge-class idiom in the stylesheet/template). Keep the graceful .get fallbacks intact.
    2. Do NOT add needs_operator to `IN_FLIGHT_STATUSES` (:111-113) — it is a settled gate state like awaiting_approval (Pitfall #4 item 6). Do NOT add it to retrigger `stale_statuses` (:777-782) — honor the docstring.
    3. Extend tests/test_stuck_run_recovery.py's scope pins: assert `"needs_operator"` is NOT in the sweep scope (`_STRANDED_SCOPE_STATUSES` stays exactly ["received","extracting","computed"], D-9-12) — an explicit `assert "needs_operator" not in ...` beside the existing exact-list pin.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_stuck_run_recovery.py tests/test_dashboard.py && grep -A4 '_BADGE_LABEL' app/main.py | grep -q "needs_operator" || grep -q '"needs_operator"' app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `"needs_operator"` appears in both _BADGE_CLASS and _BADGE_LABEL dicts in app/main.py
    - IN_FLIGHT_STATUSES and retrigger stale_statuses do NOT contain needs_operator (grep the two definitions)
    - tests/test_stuck_run_recovery.py contains an assertion that "needs_operator" is absent from the sweep scope
    - Existing dashboard tests green
  </acceptance_criteria>
  <done>needs_operator renders with its own badge and joins none of the deliberately-divergent scope lists, each absence pinned by a test.</done>
</task>

<task type="auto">
  <name>Task 3: New test modules — test_clarify_rounds.py + test_needs_operator.py (unguarded, hermetic)</name>
  <files>tests/test_clarify_rounds.py, tests/test_needs_operator.py</files>
  <read_first>
    - tests/test_atomic_persist.py (:170 crash-injection idiom — monkeypatch a repo fn to raise mid-path; :234-298 AST source-order guard parsing a function body to assert transaction membership and status-last ordering)
    - tests/conftest.py (fake_repo / mock_llm fixture surface, InMemoryRepo mirrors from Plan 11-01)
    - tests/test_multiround_context_edge.py (:1-12 — the unguarded-module warning convention; do NOT add tests to test_resume_pipeline.py, it has a module-level DATABASE_URL skip)
    - app/pipeline/orchestrator.py (the Task-1 result being tested)
  </read_first>
  <action>
    Both modules are hermetic and unguarded (no module-level DATABASE_URL skip — restate the warning docstring convention). Money-path assertions target persisted state/behavior, not log strings (Phase 7.5 lesson).

    tests/test_clarify_rounds.py (CLAR2-01):
    1. new-question-sends: run at clarification_round=1 with a sent round-0 row → _clarify (round 1 question) actually sends (fake gateway records a second outbound row with round=1) and finalizes counter to 2. This is the WR-05 regression test.
    2. same-round-suppressed: sent row exists at the CURRENT round → _clarify suppresses (gateway NOT called), early-return finalize runs, run at awaiting_reply (CLAR-04 preserved).
    3. crash-idempotent advance: simulate send-then-crash (sent row at round R exists, counter still R — exactly the post-crash state) → re-enter _clarify → no second send AND counter advances to R+1 (Pitfall #3 proof).
    4. AST source-order guard (copy test_atomic_persist.py:234-298 idiom): parse _clarify; assert each finalize block has set_clarification_round and set_status inside one `with conn.transaction()` with set_status LAST (D-9-02).
    5. outbound row records its round: after a send, the fake email row for the clarification has round == the counter value at send time.

    tests/test_needs_operator.py (CLAR2-02 escalation half):
    1. cap boundary (Open Q4 pin): counter=2 → third send proceeds (gateway called); counter=3 → NO gateway call, NO llm call (assert mock never invoked), status becomes needs_operator.
    2. escalation is silent (D-11-09): no new outbound email row is written on escalation.
    3. escalation write order: AST or behavioral assertion that set_status(NEEDS_OPERATOR) is the last write of its transaction.
    4. scope exclusions: "needs_operator" not in IN_FLIGHT_STATUSES (import from app.main); not in retrigger stale_statuses (assert against the route's behavior or the pinned list per how existing tests pin it).
    5. badge rendering: TestClient GET of a needs_operator run's detail page (or runs list) shows the "Needs Operator" label, not a raw title-cased fallback.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_clarify_rounds.py tests/test_needs_operator.py && uv run pytest -q -m "not integration and not live_llm"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_clarify_rounds.py exists, unguarded, with the five behaviors above; the WR-05 regression test fails if the guard is reverted to purpose-only (verified by the round-1-sends scenario construction)
    - tests/test_needs_operator.py exists, unguarded, with cap-boundary, silent-escalation, write-order, scope-exclusion, and badge tests
    - Neither module contains `DATABASE_URL` skip guards
    - Full offline suite green
  </acceptance_criteria>
  <done>CLAR2-01 and the escalation half of CLAR2-02 are pinned by hermetic tests including the crash-idempotency and D-9-02 ordering proofs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client reply → clarification loop | untrusted email content drives how many rounds occur |
| dashboard (no-auth demo) | needs_operator state visible/actionable from the operator UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04 | Denial of Service | unbounded clarification loop (client keeps replying ambiguously) | mitigate | MAX_CLARIFICATION_ROUNDS = 3 hard cap → needs_operator; boundary test pins the cutoff |
| T-11-05 | Tampering | escalation racing a concurrent status write | mitigate | Escalation uses sanctioned set_status inside a transaction, status-advance-last (D-9-02); AST order guard pins it |
| T-11-06 | Information Disclosure | new escalation/guard log lines | mitigate | Log run_id + round counts only; never token+roster-name together (established discipline) |
| T-11-SC | Tampering | package installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `uv run pytest -q tests/test_clarify_rounds.py tests/test_needs_operator.py` — new behavior pinned
- `uv run pytest -q -m "not integration and not live_llm"` — no regression (7.5 machine, CLAR-04 duplicates, delivery guards)
</verification>

<success_criteria>
- WR-05 closed: round-2 question sends; same-round duplicate suppressed; crash re-entry self-heals the counter
- Cap/escape live: 4th would-be send lands the run in needs_operator silently, excluded from all recovery scopes
- Full offline suite green
</success_criteria>

<output>
Create `.planning/phases/11-clarification-round-machine-alias-learning/11-02-SUMMARY.md` when done.
</output>
