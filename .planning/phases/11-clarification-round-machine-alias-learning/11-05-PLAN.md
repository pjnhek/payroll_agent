---
phase: 11-clarification-round-machine-alias-learning
plan: 05
type: execute
wave: 5
depends_on: [11-01, 11-03]
files_modified:
  - app/main.py
  - app/db/repo.py
  - tests/test_reply_redelivery.py
  - tests/test_cr_regressions.py
autonomous: true
requirements: [CLAR2-06, CLAR2-07]
must_haves:
  truths:
    - "A redelivered webhook carrying a reply whose persisted row has consumed_round IS NULL AND whose run is still awaiting_reply re-schedules _resume_pipeline; the CAS claim in resume_pipeline makes double-scheduling safe (D-11-03, WR-04)"
    - "A redelivery of an ALREADY-consumed reply (consumed_round set) stays a no-op duplicate — no re-schedule (D-11-03)"
    - "The runs-list load, beside the existing D-9-11 sweep, re-schedules _resume_pipeline for awaiting_reply runs whose linked reply is unconsumed AND older than the stale threshold; needs_operator runs are NEVER re-scheduled (D-11-05, D-11-06 exclusion)"
    - "Both retrigger winning branches clear ALL reply context (clarified_fields + pre_clarify_extracted + round counter + suggestion/candidate state) via clear_reply_context AFTER the winning claim and BEFORE _run_pipeline is scheduled, so is_round_2 = bool(clarified) sees a fresh run and provenance badges cannot outlive their data (D-11-04, WR-06)"
  artifacts:
    - path: "app/main.py"
      provides: "webhook duplicate-branch redelivery re-schedule (WR-04); runs-list stranded-unconsumed auto-resume beside the sweep (D-11-05); clear_reply_context at the retrigger post-claim seam (WR-06); _row_to_inbound helper"
      contains: "clear_reply_context"
    - path: "app/db/repo.py"
      provides: "get_inbound_by_message_id + find_stranded_unconsumed_replies selects widened to the full InboundEmail field set (id, in_reply_to, references_header, created_at) so _row_to_inbound can build an InboundEmail without a second lookup"
      contains: "in_reply_to"
    - path: "tests/test_reply_redelivery.py"
      provides: "CLAR2-06 matrix: unconsumed redelivery reschedules; consumed redelivery no-ops; runs-list stranded auto-resume; needs_operator excluded"
      min_lines: 90
    - path: "tests/test_cr_regressions.py"
      provides: "CLAR2-07: retrigger clears ALL reply context; a stale provenance badge cannot reproduce after retrigger"
      min_lines: 40
  key_links:
    - from: "app/main.py inbound() duplicate branch"
      to: "app/db/repo.py get_inbound_by_message_id"
      via: "load the persisted reply row by message_id; re-schedule resume iff consumed_round IS NULL AND run awaiting_reply (WR-04)"
      pattern: "get_inbound_by_message_id"
    - from: "app/main.py runs_list"
      to: "app/db/repo.py find_stranded_unconsumed_replies"
      via: "beside sweep_stranded_runs; re-schedule _resume_pipeline for each stranded unconsumed awaiting_reply reply, swallow-on-failure"
      pattern: "find_stranded_unconsumed_replies"
    - from: "app/main.py retrigger post-claim seam"
      to: "app/db/repo.py clear_reply_context"
      via: "clear ALL reply context after the winning claim, before _run_pipeline is scheduled (WR-06)"
      pattern: "clear_reply_context"
---

<objective>
Wire the persisted round/consumed state (from 11-01) and the consumed marker (from 11-03) into the three main.py runtime seams that make the reply path unstrandable and provenance-safe: WR-04 redelivery re-schedule (D-11-03), D-11-05 stranded-unconsumed auto-resume on the runs-list load, and WR-06 retrigger-clears-all-context (D-11-04). This closes CLAR2-06 and CLAR2-07.

Purpose: without these seams the durable state landed in 11-01/11-03 is inert at the webhook/dashboard/retrigger boundaries — a redelivered or stranded reply is permanently dropped, and a retrigger leaves stale clarified_fields/provenance behind so `is_round_2 = bool(clarified)` mis-classifies a fresh run and a provenance badge outlives the data that produced it. This plan is wave 5 specifically to avoid a same-wave same-file collision on app/main.py (owned by 11-02 at wave 2 and 11-04 at wave 4); it lands AFTER both.
Output: three wired main.py seams + a _row_to_inbound helper, two widened repo selects, and two test modules asserting REAL behavior/values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clarification-round-machine-alias-learning/11-CONTEXT.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-01-SUMMARY.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: repo select widening + _row_to_inbound helper (the InboundEmail bridge for re-scheduling)</name>
  <files>app/db/repo.py, app/main.py</files>
  <read_first>
    - app/models/contracts.py (:35-52 — InboundEmail has extra="forbid" and REQUIRES id, message_id, in_reply_to, references_header, subject, from_addr, to_addr, body_text, created_at; a _row_to_inbound helper must supply ALL of these)
    - app/db/repo.py (get_inbound_by_message_id + find_stranded_unconsumed_replies from Plan 11-01 — their current SELECT column lists; the dict_row query idiom they copy)
    - app/db/schema.sql (:198-217 — email_messages HAS id, in_reply_to, references_header, subject, from_addr, to_addr, body_text, message_id, created_at columns, so widening the SELECT is a pure column-list change, no new columns)
    - app/main.py (_finish_reply_resume :549 — the `email.model_copy(update={"body_text": cleaned})` → _resume_pipeline pattern; _resume_pipeline :611 signature takes (run_id, inbound: InboundEmail))
  </read_first>
  <action>
    Implements the InboundEmail bridge both later tasks reuse. repo.py is owned by 11-01 (wave 1) only; this wave-5 widening has no same-wave collision.

    1. In app/db/repo.py, widen the SELECT column lists of `get_inbound_by_message_id` and `find_stranded_unconsumed_replies` (both from 11-01) to also return `id`, `in_reply_to`, `references_header`, `created_at` (all already columns on email_messages) — the full field set InboundEmail requires. find_stranded_unconsumed_replies keeps returning run_id too. Do NOT change either function's filter/scope logic (the D-11-05 scope comment stays); this is a column-list widening only.
    2. In app/main.py add a module-level helper `_row_to_inbound(row: dict) -> InboundEmail` that maps a persisted email_messages row dict to an InboundEmail: id=row["id"], message_id, in_reply_to, references_header, subject, from_addr, to_addr, body_text=row["body_text"] (already the cleaned body — do NOT re-clean; Pitfall #11a: the persisted row cleaned at first ingest is authoritative), created_at. This is the single conversion point used by both the WR-04 redelivery re-schedule and the D-11-05 stranded auto-resume.
    3. Keep _row_to_inbound pure (no DB I/O) — it receives an already-loaded row.
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration and not live_llm" && grep -q "def _row_to_inbound" app/main.py && grep -q "in_reply_to" app/db/repo.py</automated>
  </verify>
  <acceptance_criteria>
    - get_inbound_by_message_id and find_stranded_unconsumed_replies SELECT id, in_reply_to, references_header, created_at in addition to their prior fields (find_stranded_unconsumed_replies still returns run_id)
    - `_row_to_inbound(row)` builds a valid InboundEmail (extra="forbid" satisfied) without re-cleaning body_text and without DB I/O
    - Full offline suite exits 0
  </acceptance_criteria>
  <done>Both re-schedule seams have a single, tested row→InboundEmail bridge that reuses the authoritative persisted (already-cleaned) body.</done>
</task>

<task type="auto">
  <name>Task 2: WR-04 redelivery re-schedule (duplicate branch) + D-11-05 stranded auto-resume (runs-list) — CLAR2-06</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (inbound() :269 signature has background_tasks in scope; the duplicate branch :456-465 — `outcome == "duplicate"` with `email.message_id` and `existing_run_id` available, POST-COMMIT (below the :453-454 "Transaction committed... post-commit" boundary comment); the runs_list route :1061-1093 — the sweep try/except at :1073-1076 and the current `def runs_list(request: Request)` signature that MUST gain a BackgroundTasks param; _resume_pipeline :611; load_run for the awaiting_reply status check)
    - app/db/repo.py (get_inbound_by_message_id + find_stranded_unconsumed_replies + load_run signatures; RunStatus.AWAITING_REPLY)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Pitfall #11 — read the PERSISTED row, schedule POST-commit; the runs-list hook sketch ~:423-432 `for ... in repo.find_stranded_unconsumed_replies(STALE_THRESHOLD_SECONDS): background_tasks.add_task(_resume_pipeline, run_id, _row_to_inbound(reply_row))`; note the sketch returns tuples but 11-01 returns dicts INCLUDING run_id — adapt to the dict shape)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-CONTEXT.md (D-11-03 redelivery, D-11-05 stranded auto-resume, D-11-06 needs_operator exclusion)
  </read_first>
  <action>
    Implements D-11-03 (WR-04 redelivery) and D-11-05 (stranded auto-resume). Both re-schedules are CAS-gated downstream (resume_pipeline's claim_status(AWAITING_REPLY→EXTRACTING) collapses N schedules to one), so double-scheduling is safe.

    1. WR-04 redelivery, in the `outcome == "duplicate"` branch (:456-465), POST-COMMIT (this branch is already below the commit boundary): after the existing duplicate log/response prep, load the persisted reply row via `repo.get_inbound_by_message_id(email.message_id)`. If the row is not None AND `row["consumed_round"] is None` AND the row has a run_id AND `repo.load_run(row["run_id"])` shows status == RunStatus.AWAITING_REPLY.value → `background_tasks.add_task(_resume_pipeline, row["run_id"], _row_to_inbound(row))`. A consumed reply (consumed_round set) or a run no longer awaiting_reply → do nothing (stays a no-op duplicate, D-11-03). Still return the existing duplicate JSONResponse unchanged. PII-safe log: run_id + "redelivery reschedule" only, no body/names.
    2. D-11-05 stranded auto-resume, in runs_list (:1061-1093): add `background_tasks: BackgroundTasks` to the route signature (FastAPI injects it; the runs-list route currently has none). Inside the SAME try-block that calls sweep_stranded_runs (:1073-1076), AFTER the sweep call, iterate `for row in repo.find_stranded_unconsumed_replies(STALE_THRESHOLD_SECONDS):` and `background_tasks.add_task(_resume_pipeline, row["run_id"], _row_to_inbound(row))`. The query's scope (11-01) is already awaiting_reply + unconsumed + stale and EXCLUDES needs_operator by design — so a needs_operator run is never re-scheduled (D-11-06). Keep the swallow-on-failure except exactly as the sweep uses it (a recovery-sweep failure must never 500 the dashboard).
    3. Do NOT add needs_operator anywhere to sweep scope or these re-schedule conditions. Do NOT re-schedule a run that is not awaiting_reply.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_reply_redelivery.py && uv run pytest -q -m "not integration and not live_llm" && grep -q "get_inbound_by_message_id" app/main.py && grep -q "find_stranded_unconsumed_replies" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - The duplicate branch loads the persisted row by message_id and re-schedules _resume_pipeline ONLY when consumed_round is None AND the run is awaiting_reply; a consumed-reply redelivery re-schedules nothing
    - runs_list gains a BackgroundTasks param and re-schedules _resume_pipeline for each find_stranded_unconsumed_replies row, inside the sweep's swallow-on-failure try/except
    - needs_operator runs are never re-scheduled (verified: the query scope excludes them and no code path adds them)
    - Re-scheduling is post-commit (duplicate branch) / read-only-then-schedule (runs-list); no scheduling inside any transaction
    - Full offline suite exits 0
  </acceptance_criteria>
  <done>A redelivered or stranded unconsumed reply is re-driven through the CAS-gated resume; a consumed reply's redelivery and a needs_operator run stay untouched — no permanently-dropped replies.</done>
</task>

<task type="auto">
  <name>Task 3: WR-06 retrigger-clears-all-reply-context — CLAR2-07</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (retrigger route :701-809 in FULL — the core CAS claims at :751-755 (ERROR/APPROVED→RECEIVED), the stale in-flight CAS at :796-798, and the CONVERGENCE point `if claimed: background_tasks.add_task(_run_pipeline, run_id)` at :807-808 where BOTH winning branches meet before _run_pipeline is scheduled; the "Do NOT 'fix' this into parity" docstring :766-776 — do NOT touch stale_statuses)
    - app/db/repo.py (clear_reply_context from Plan 11-01 — nulls clarified_fields + pre_clarify_extracted + clarification_round + alias_candidates in one UPDATE, caller-joinable via conn=)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Pitfall #8 — clear STRICTLY AFTER a successful claim, in a transaction that commits BEFORE _run_pipeline is scheduled; both retrigger branches dispatch _run_pipeline and lose reply context)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-CONTEXT.md (D-11-04 "context lost means ALL of it"; D-11-14 suggestion/candidate state included)
  </read_first>
  <action>
    Implements D-11-04 (WR-06 clear-at-retrigger). "Context lost means ALL of it": clear clarified_fields + pre_clarify_extracted + round counter + suggestion/candidate state — clear_reply_context (11-01) already covers all four in one UPDATE.

    1. At the retrigger CONVERGENCE point (:807, guarded by `if claimed:`) — which is reached by BOTH winning branches (the ERROR/APPROVED core CAS :751-755 AND the stale in-flight CAS :796-798) — call `repo.clear_reply_context(run_id)` AFTER the claim has won and BEFORE `background_tasks.add_task(_run_pipeline, run_id)`. Doing it at the single convergence point (rather than duplicating inside each branch) satisfies "after the winning claim in BOTH branches" with no duplication and keeps status-advance semantics intact — the claim already advanced status; clear_reply_context touches only the reply-context columns, not status. Wrap the clear in its own committed unit (clear_reply_context opens its own transaction when conn=None) so it durably commits before the background task is scheduled (Pitfall #8: the clear must not be rolled back by a later crash, and must not span the LLM-heavy _run_pipeline).
    2. Do NOT modify the retrigger claim logic, the stale_statuses scope, or the "Do NOT 'fix' this into parity" divergence. Do NOT add needs_operator to any retrigger scope (D-11-06: needs_operator exits via /resolve or reject, not retrigger).
    3. PII-safe log line: run_id + "reply context cleared on retrigger (WR-06)" — no field names/values.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_cr_regressions.py && uv run pytest -q -m "not integration and not live_llm" && grep -q "clear_reply_context" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `repo.clear_reply_context(run_id)` is called at the retrigger post-claim convergence point, after `if claimed:` and before `add_task(_run_pipeline, ...)`, reached by both winning branches
    - The clear commits before the _run_pipeline background task is scheduled (its own transactional unit, not spanning _run_pipeline)
    - retrigger claim logic, stale_statuses, and the parity-divergence docstring are unchanged; needs_operator is not added to any retrigger scope
    - Full offline suite exits 0
  </acceptance_criteria>
  <done>A retrigger wipes clarified_fields, pre_clarify_extracted, the round counter, and suggestion/candidate state after winning the claim and before re-running, so is_round_2 sees a fresh run and no provenance badge can outlive its data.</done>
</task>

<task type="auto">
  <name>Task 4: tests/test_reply_redelivery.py (CLAR2-06) + extend tests/test_cr_regressions.py (CLAR2-07)</name>
  <files>tests/test_reply_redelivery.py, tests/test_cr_regressions.py</files>
  <read_first>
    - tests/conftest.py (fake_repo InMemoryRepo mirrors from 11-01: get_inbound_by_message_id, find_stranded_unconsumed_replies, mark_reply_consumed, load_consumed_replies, load_run, clear_reply_context; the TestClient app fixture; how fake email/run rows carry round/consumed_round/created_at)
    - tests/test_multiround_context_edge.py (:1-14 — the unguarded-module no-DATABASE_URL-skip warning docstring convention to restate; do NOT land these tests in tests/test_resume_pipeline.py which has a module-level DATABASE_URL skip)
    - tests/test_cr_regressions.py (FULL FILE — the existing retrigger/provenance regression style to extend; how it drives the retrigger route and asserts persisted state)
    - app/main.py (Tasks 2 + 3 results — the duplicate-branch re-schedule, runs-list stranded auto-resume, retrigger clear seam under test)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Validation Architecture :516-517 — CLAR2-06 → tests/test_reply_redelivery.py (new); :532 — the new module name; CLAR2-07 → extend tests/test_cr_regressions.py; "assert REAL behavior/values, not labels")
  </read_first>
  <action>
    Both modules hermetic + unguarded (restate the no-DATABASE_URL-skip warning docstring). Assert REAL behavior (re-schedule happened / no-op / persisted columns cleared), never log strings or labels.

    tests/test_reply_redelivery.py (NEW — CLAR2-06):
    1. unconsumed redelivery reschedules: seed an awaiting_reply run with a persisted inbound reply row consumed_round=None → POST the SAME message_id again (duplicate) → assert `_resume_pipeline` was scheduled (spy on background_tasks / on _resume_pipeline via monkeypatch) with the run_id and a _row_to_inbound-built reply whose body_text equals the PERSISTED body (not re-cleaned).
    2. consumed redelivery no-ops: same seed but the reply row has consumed_round set → duplicate POST → assert NO re-schedule (the spy is never called); the duplicate JSONResponse is still returned.
    3. redelivery to a non-awaiting_reply run no-ops: reply row unconsumed but the run is (e.g.) reconciled → no re-schedule.
    4. runs-list stranded auto-resume: seed an awaiting_reply run with an unconsumed reply older than the stale threshold → GET /runs → assert _resume_pipeline was scheduled for it; a FRESH unconsumed reply (not past threshold) is NOT scheduled.
    5. needs_operator excluded (D-11-06): seed a needs_operator run with an unconsumed stale reply → GET /runs → assert it is NOT re-scheduled (the query scope excludes it).

    tests/test_cr_regressions.py (EXTEND — CLAR2-07):
    6. retrigger clears ALL reply context: seed a claimable run (ERROR or stale in-flight) with non-empty clarified_fields, a pre_clarify_extracted snapshot, clarification_round > 0, and alias_candidates set → POST /runs/{id}/retrigger → after the winning claim assert all four columns are cleared (clarified_fields None/{}, pre_clarify_extracted None, clarification_round 0, alias_candidates None) BEFORE/at _run_pipeline dispatch. Assert the run still got claimed + dispatched.
    7. stale provenance badge cannot reproduce: after retrigger clears clarified_fields, assert `is_round_2 = bool(clarified)` would be False for the re-run (drive far enough to show the re-run does not carry a Round-2/provenance badge sourced from the wiped clarified_fields) — assert on the persisted/derived state, not a rendered label string.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_reply_redelivery.py tests/test_cr_regressions.py && uv run pytest -q -m "not integration and not live_llm"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_reply_redelivery.py exists, unguarded, covering: unconsumed-redelivery-reschedules, consumed-redelivery-no-ops, non-awaiting-reply-no-ops, runs-list-stranded-auto-resume, needs_operator-excluded — each asserting the re-schedule fact (not a log string)
    - tests/test_cr_regressions.py gains a CLAR2-07 test asserting all four reply-context columns are cleared on retrigger and a stale provenance badge cannot reproduce
    - Neither module has a DATABASE_URL module-level skip
    - Full offline suite exits 0
  </acceptance_criteria>
  <done>CLAR2-06's redelivery/stranded matrix and needs_operator exclusion are pinned by real re-schedule assertions; CLAR2-07's clear-all-context and no-stale-provenance are pinned by persisted-column assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| redelivered webhook (untrusted, possibly replayed) → resume re-schedule | a duplicate/forged redelivery could trigger repeated resume scheduling |
| runs-list load (no-auth demo dashboard) → autonomous resume re-schedule | a dashboard GET auto-drives interrupted reply processing |
| retrigger route (no-auth demo dashboard) → reply-context wipe | a retrigger permanently discards clarified_fields/snapshot/round/suggestion state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-17 | Denial of Service / Spoofing | replayed "duplicate" webhook triggering resume storms | mitigate | The route's existing signature verification gates entry; the resume CAS claim (AWAITING_REPLY→EXTRACTING) collapses N re-schedules to one; the write-once consumed marker (11-01/11-03) makes post-consumption redeliveries no-ops. Assert the no-op in tests (RESEARCH Security Domain — "no new mitigation needed") |
| T-11-18 | Elevation of Privilege | stranded auto-resume as an unauthorized autonomous restart (D-9-10 concern) | accept | NOT a D-9-10 violation: the client's ORIGINAL reply already authorized this processing — the webhook resume path is identically autonomous; the runs-list sweep only COMPLETES interrupted work it was already authorized to do. Scope is awaiting_reply + unconsumed + stale, EXCLUDING needs_operator (which requires a human). No new authZ surface (D-11-05 rationale, CONTEXT.md) |
| T-11-19 | Tampering | provenance badge outliving its data after retrigger (WR-06) | mitigate | clear_reply_context wipes clarified_fields + snapshot + round + suggestion state after the winning claim and before re-run, so is_round_2 = bool(clarified) sees a fresh run; Task 4 pins the four cleared columns and the no-stale-badge assertion |
| T-11-20 | Tampering | stranded/redelivery re-schedule reading a re-cleaned (mutated) body instead of the authoritative persisted one | mitigate | _row_to_inbound uses the persisted (first-ingest-cleaned) body_text verbatim, never re-cleans (Pitfall #11a); Task 4 asserts the scheduled reply body equals the persisted body |
| T-11-SC | Tampering | package installs | accept | Zero packages installed this plan (RESEARCH Package Legitimacy Audit: nothing to verify); uv.lock unchanged; python-multipart already a runtime dependency |
</threat_model>

<verification>
- `uv run pytest -q tests/test_reply_redelivery.py tests/test_cr_regressions.py` — the redelivery/stranded matrix, needs_operator exclusion, and retrigger clear-all-context
- `uv run pytest -q -m "not integration and not live_llm"` — no regression to the webhook, runs-list, or retrigger routes
- `grep -q "get_inbound_by_message_id" app/main.py`, `grep -q "find_stranded_unconsumed_replies" app/main.py`, `grep -q "clear_reply_context" app/main.py` — all three seams wired
</verification>

<success_criteria>
- CLAR2-06 closed: an unconsumed redelivery and a stranded unconsumed reply both re-drive the CAS-gated resume; a consumed redelivery and a needs_operator run stay no-ops
- CLAR2-07 closed: retrigger clears clarified_fields + pre_clarify_extracted + round counter + suggestion/candidate state after the winning claim and before re-run
- No re-scheduling inside a transaction; needs_operator is never re-scheduled or retrigger-scoped
- Full offline suite green
</success_criteria>

<output>
Create `.planning/phases/11-clarification-round-machine-alias-learning/11-05-SUMMARY.md` when done.
</output>
