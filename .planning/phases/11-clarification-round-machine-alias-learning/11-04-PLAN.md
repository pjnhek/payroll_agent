---
phase: 11-clarification-round-machine-alias-learning
plan: 04
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - app/pipeline/orchestrator.py
  - app/pipeline/reconcile_names.py
  - app/main.py
  - app/templates/run_detail.html
  - tests/test_alias_write.py
  - tests/test_alias_full_loop.py
autonomous: true
requirements: [CLAR2-04]
must_haves:
  truths:
    - "The clarification-time suggestion is persisted in the nested shape {token: {suggested: employee_id|null, bound: null}} — suggest_employees' full_name output is mapped to an employee id at persist time (D-11-14, Pitfall #5)"
    - "The alias binds {token: suggested_id} iff the SUGGESTED employee newly resolves post-resume AND the token is gone from unresolved submitted names — no LLM call in the bind chain; a bare confirming 'yes' works via the D-11-10 anchor (D-11-15)"
    - "The misname case ('no, I meant James') binds nothing: James is a NON-suggested id, so the never-learn-from-inference intent survives verbatim (D-11-15, misname guard)"
    - "The operator resolve form validates every posted employee_id against the RUN'S OWN business roster server-side and rejects the whole POST on any invalid id — no cross-business or arbitrary-UUID binding (Security V4)"
    - "The full-loop hermetic test drives REAL reconcile_names + real _write_aliases_if_safe: nickname → capture → suggest-persist → confirming reply → bind → approve → known_aliases written → a SECOND submission resolves via stored-alias with ZERO clarification (D-11-17)"
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "nested suggestion persistence at _clarify; bind-on-confirmation rewrite at resume (replaces the pre/post count-diff); _write_aliases_if_safe skip-condition on cand['bound']; nested-shape normalize-on-read"
      contains: "\"bound\""
    - path: "app/pipeline/reconcile_names.py"
      provides: "optional per-run operator overrides param (operator mapping wins before exact/alias, tagged source='operator') — no guessing"
      contains: "overrides"
    - path: "app/main.py"
      provides: "POST /runs/{run_id}/resolve route: server-side roster validation, deterministic mapping applied before reconcile, claim_status(NEEDS_OPERATOR, EXTRACTING), operator-resume dispatch, remember-alias checkbox → bound candidate"
      contains: "resolve"
    - path: "app/templates/run_detail.html"
      provides: "needs_operator resolve form (per-name roster dropdown, suggestion pre-selected, remember-alias checkbox default-checked) + Reject"
      contains: "needs_operator"
    - path: "tests/test_alias_full_loop.py"
      provides: "the D-11-17 stops-asking full loop with REAL resolution"
      min_lines: 90
  key_links:
    - from: "app/pipeline/orchestrator.py resume bind check"
      to: "payroll_runs.alias_candidates {token: {suggested, bound}}"
      via: "bind when suggested_id newly resolved AND token gone from unresolved"
      pattern: "suggested"
    - from: "app/main.py resolve route"
      to: "app/pipeline/reconcile_names.reconcile_names overrides"
      via: "operator mapping validated against run's roster, applied as deterministic override before reconcile"
      pattern: "overrides"
    - from: "app/pipeline/orchestrator.py _write_aliases_if_safe"
      to: "repo.update_known_alias"
      via: "write when cand['bound'] is not None; the D-01b collision re-check preserved verbatim"
      pattern: "update_known_alias"
---

<objective>
Make the alias-learning WRITE side reachable and prove the system stops asking. Persist the clarification suggestion in the nested `{token: {suggested: id, bound: null}}` shape (D-11-14); replace the circular pre/post count-diff bind with a deterministic bind-on-confirmation against the persisted suggestion (D-11-15); add the `needs_operator` operator resolve form + resume route with server-side roster validation and the remember-alias checkbox (D-11-08, D-11-16, Security V4); update `_write_aliases_if_safe` to the nested shape while preserving the D-01b collision re-check; and land the D-11-17 full-loop stops-asking test that exercises REAL resolution. This covers CLAR2-04.

Purpose: the current bind requires the newly-resolved employee's id to equal a token that was itself the pending candidate — a circular condition that can never fire for a nickname the client only RESTATES canonically, so the learning loop is unreachable (260705-01). Faked-state tests kept it green; only a real-resolution loop proves it.
Output: nested suggestion persistence, the bind-on-confirmation rewrite, the operator resolve/resume surface, the updated write side, the rewritten alias_write tests, and the full-loop test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-clarification-round-machine-alias-learning/11-CONTEXT.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-01-SUMMARY.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-02-SUMMARY.md
@.planning/phases/11-clarification-round-machine-alias-learning/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Nested suggestion persistence + bind-on-confirmation rewrite + nested-shape write side (D-11-14, D-11-15)</name>
  <files>app/pipeline/orchestrator.py</files>
  <read_first>
    - app/pipeline/orchestrator.py (the alias capture + suggest + persist block :1039-1123 — the set_alias_candidates call at :1091 and the suggest_employees call at :1113; the bind logic :660-735 in FULL — _pre_candidates/_none_tokens, the pre/post reconciliation load, _newly_resolved_ids, the misname-guard _token_resolved_to_newly check, both logger.info skip branches; _pre_resolved_ids setup :292-301; _write_aliases_if_safe :1181-1260 — the `for token, employee_id_str in alias_candidates.items()` loop, the `if employee_id_str is None: continue` skip, the _safe_to_learn_alias D-01b re-check, the batch roster refresh after each accepted write)
    - app/pipeline/suggest.py (suggest_employees :60-80 — returns dict[submitted_name → suggested FULL_NAME], filtered to valid roster full_names; it NEVER returns an id — Pitfall #5)
    - app/db/repo.py (set_alias_candidates, load_run, update_known_alias, load_roster_for_business signatures)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Architecture Patterns §4 bind sketch; Pitfalls #5 name-vs-id, #6 old-shape rows; the Security "alias poisoning" row)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md (§ orchestrator — "suggestion computation to persist", "the bind logic being replaced", "_write_aliases_if_safe")
    - .planning/phases/11-clarification-round-machine-alias-learning/11-01-SUMMARY.md (the alias_candidates one-shot flat→nested migration already applied to schema)
  </read_first>
  <behavior>
    - Persist shape: after suggest_employees returns {token: full_name}, the persisted alias_candidates value for that token is {"suggested": <employee_id_for_full_name>, "bound": null}; a full_name not found in the roster → {"suggested": null, "bound": null}.
    - Bind on confirmation: given a candidate {token: {"suggested": S, "bound": null}}, when post-resume reconciliation has S newly resolved (in post_resolved_ids, not in pre_resolved_ids) AND token is absent from post-resume unresolved submitted names → the candidate becomes {"suggested": S, "bound": S}.
    - Misname guard: given the same candidate but the reply resolves a DIFFERENT id J (not S) — S never appears newly-resolved → bound stays null (nothing learned). Assert no bind on "no, I meant James".
    - No-suggestion: candidate {token: {"suggested": null, "bound": null}} → never binds (no suggested id to confirm).
    - Write side: _write_aliases_if_safe skips a candidate whose normalized value has bound is None; writes update_known_alias only for bound ids; the D-01b _safe_to_learn_alias collision re-check still runs before every write.
    - Old-shape tolerance: a normalize-on-read helper maps a legacy flat value (None or "id-str") to the nested shape so bind + write never raise AttributeError on pre-migration rows (Pitfall #6).
  </behavior>
  <action>
    Implements D-11-14 (nested persistence) and D-11-15 (bind on confirmation), preserving the misname guard intent (D-11-15) and the D-01b collision re-check.

    1. Add a module-level `_normalize_candidate(value) -> dict` helper: None → {"suggested": None, "bound": None}; a str → {"suggested": None, "bound": value}; a dict → the dict as-is (idempotent). Use it everywhere alias_candidates values are read (bind check AND _write_aliases_if_safe) so legacy flat rows never raise (Pitfall #6).
    2. At the persist site (~:1091/:1113): after `suggest_employees` returns `{token: full_name}`, map each full_name → employee id via the already-loaded roster (full_name is UNIQUE per business — Pitfall #5); build the nested candidates `{token: {"suggested": id_or_None, "bound": None}}` and persist via `repo.set_alias_candidates`. Keep `suggest_employees` itself unchanged (compose still needs names for the email copy). Log counts/types only, never token+roster-name together.
    3. Replace the pre/post count-diff bind block (:660-735) with the D-11-15 check: for each candidate token whose normalized `bound` is None and `suggested` (=S) is not None, load post-resume reconciliation (reuse the existing `load_run` + resolved-id set-diff mechanics at :674-684), compute `token_gone = token not in {submitted_name of unresolved post entries}` and `sugg_newly_resolved = S in (post_resolved_ids - pre_resolved_ids)`; bind `{token: {"suggested": S, "bound": S}}` iff both true, via `set_alias_candidates`. Keep both-branches `logger.info` skip-reason style (every non-bind logs why — the misname-guard narrative). A bare "yes" works because the D-11-10 anchor (Plan 11-03) lets extraction attribute the reply to the suggested canonical name; no LLM call sits in this chain.
    4. Update `_write_aliases_if_safe` (:1181-1260): change the skip from `if employee_id_str is None` to `cand = _normalize_candidate(value); if cand["bound"] is None: continue`, and use `cand["bound"]` as the employee id to learn. Keep the `_safe_to_learn_alias` D-01b collision re-check and the batch roster refresh after each accepted write VERBATIM (structure unchanged).
    5. Do NOT reintroduce any confidence number or LLM "did they say yes?" call — the bind chain is LLM-free by decision (D-11-15 anti-pattern).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_alias_write.py && uv run pytest -q -m "not integration and not live_llm" && grep -q "_normalize_candidate" app/pipeline/orchestrator.py && grep -q "\"suggested\"" app/pipeline/orchestrator.py</automated>
  </verify>
  <acceptance_criteria>
    - Persisted candidates use `{token: {"suggested": id|None, "bound": None}}`; a roster-missing suggestion yields `suggested: None` (Pitfall #5 mapping present)
    - The bind block binds `{token: {"suggested": S, "bound": S}}` only when S is newly-resolved AND token gone from unresolved; a non-suggested resolution (misname) binds nothing (a test asserts the "no, I meant James" no-bind)
    - `_write_aliases_if_safe` skips on `bound is None`, writes on a bound id, and still runs `_safe_to_learn_alias` before each write; `_normalize_candidate` handles flat legacy values
    - No LLM call and no confidence value exists in the bind chain
    - tests/test_alias_write.py green (updated to the nested shape in Task 3); full offline suite exits 0
  </acceptance_criteria>
  <done>The suggestion is captured in the nested shape at clarify time and bound deterministically on confirmed resolution of the suggested employee; the misname guard survives; the write side reads the nested shape with legacy tolerance.</done>
</task>

<task type="auto">
  <name>Task 2: Operator resolve form + resume route with server-side roster validation and per-run override (D-11-08, D-11-16, Security V4)</name>
  <files>app/main.py, app/pipeline/reconcile_names.py, app/templates/run_detail.html</files>
  <read_first>
    - app/main.py (approve :642-698 — the CAS-claim + guarded-work + 303-redirect route shape to mirror; reject route; the retrigger route :701-809 and its "Do NOT 'fix' this into parity" docstring :766-776 — DO NOT add needs_operator to stale_statuses; the simulate-reply route for the Form(...) idiom; load_roster_for_business usage; the _DEMO_FIXTURES allowlist philosophy :115-120)
    - app/pipeline/reconcile_names.py (reconcile_names :104-120 and deterministic_match :46 — where an optional overrides param must win BEFORE exact/alias, tagged source='operator'; NameMatchResult shape with source + resolved)
    - app/pipeline/orchestrator.py (resume_pipeline :227 — the operator-resume entry point decision, RESEARCH Open Question #1: prefer generalizing resume_pipeline to accept a from_status / absent-current-reply over a parallel _operator_resume; the accumulated-context assembly from Plan 11-03 that operator-resume reuses)
    - app/templates/run_detail.html (the awaiting_reply banner+form :71-84 and the approve/reject action forms :293-303 — the resolve form copies these; the retrigger-form status list :306 — do NOT add needs_operator; the provenance badge macro :147-159 for badge styling)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Security Domain V4/V5 — server-side roster membership check, reject whole POST on any invalid id; Open Questions #1 and #2; the operator resolve-form route sketch in Code Examples)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md (§ main.py resolve route; § run_detail.html resolve form; No Analog Found — operator-resume + per-run override)
  </read_first>
  <action>
    Implements D-11-08 (operator resolve + resume, or reject), D-11-16 (remember-alias checkbox default ON), and Security V4 (server-side roster validation). No client-facing email at any point (D-11-09 silent handoff).

    1. `reconcile_names` gains an optional `overrides: dict[str, str] | None = None` param (submitted_name → employee_id). An override wins BEFORE exact/stored-alias resolution and produces a resolved NameMatchResult tagged `source="operator"` (add the source value; it is a human decision, not a guess — the no-guess guarantee holds because a human stated it). Default None keeps every existing caller behavior-identical. Do NOT let an override invent an id not on the roster (validate against the passed roster; an unknown id is ignored/raises per the caller's contract).
    2. Operator-resume entry point: generalize `resume_pipeline` (RESEARCH Open Q1 recommendation (i)) to support a NEEDS_OPERATOR → EXTRACTING claim with an absent current reply, rebuilding context purely from the accumulated consumed replies (Plan 11-03's assembly) — the "current reply" section is simply absent. Pass the operator override map into the reconcile step so the token resolves deterministically before reconcile decides.
    3. `POST /runs/{run_id}/resolve` route in app/main.py (mirror `approve`): accept Form fields — per unresolved name a selected `employee_id` and a `remember_<token>` checkbox. VALIDATE server-side: run is `needs_operator`; every posted employee_id ∈ `load_roster_for_business(run.business_id)`; reject the WHOLE POST (4xx / no state change, no partial apply) on any invalid or cross-business id (Security V4). Apply the mapping as the per-run override (drives resolution); for each token whose remember-checkbox is ON, additionally set the candidate `bound` = the chosen id so the existing approval-gate write path (Task 1) persists the alias (checkbox OFF = override only, nothing learned — D-11-16). THEN `claim_status(NEEDS_OPERATOR, EXTRACTING)`; on success `background_tasks.add_task(...)` the operator-resume; always `RedirectResponse(303)`. Post-commit scheduling only.
    4. run_detail.html: add a `{% elif run.status == 'needs_operator' %}` banner + resolve form (copy the awaiting_reply banner/approve-form idiom): per unresolved name a `<select name="...">` over the run's roster employees with the LLM suggestion pre-selected as advisory copy, a `remember this alias` checkbox default-checked, a Resume submit, and the existing Reject. Do NOT add needs_operator to the retrigger-form status list.
    5. PII-safe logging throughout (run_id + counts; never token+roster-name together).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_dashboard.py tests/test_needs_operator.py && uv run pytest -q -m "not integration and not live_llm" && grep -q "overrides" app/pipeline/reconcile_names.py && grep -q "/resolve" app/main.py</automated>
  </verify>
  <acceptance_criteria>
    - `reconcile_names` accepts `overrides=` (default None, behavior-identical when None) and an override yields a resolved NameMatchResult tagged `source="operator"`
    - `resume_pipeline` (or the operator-resume path) claims NEEDS_OPERATOR → EXTRACTING and rebuilds context from accumulated consumed replies with the current-reply section absent
    - `POST /runs/{run_id}/resolve` rejects the whole POST (no state change) when any posted employee_id is not in the run's business roster; a valid POST applies the override, sets `bound` only for checkbox-ON tokens, claims, and dispatches resume
    - run_detail.html renders the needs_operator resolve form (roster dropdown, suggestion pre-selected, remember checkbox default-checked) + Reject; needs_operator is NOT in the retrigger-form status list
    - Existing dashboard + needs_operator scope tests green; full offline suite exits 0
  </acceptance_criteria>
  <done>An operator can deterministically resolve unresolved names against the run's own roster and resume from accumulated context; a validated remember-checkbox binds the alias through the single human gate; cross-business/arbitrary ids are rejected wholesale.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests/test_alias_write.py to the nested shape + real resolution; land tests/test_alias_full_loop.py (D-11-17)</name>
  <files>tests/test_alias_write.py, tests/test_alias_full_loop.py</files>
  <read_first>
    - tests/test_alias_write.py (FULL FILE — the _safe_to_learn_alias unit tests :129-200 that stay as-is; the faked-state binding tests :248-792 that monkeypatch load_run to return hand-built pre/post reconciliation dicts and assert set_alias_candidates calls — update candidate VALUES to the nested {token:{suggested,bound}} shape; the reconciliation dict shape submitted_name/matched_employee_id/source/resolved to preserve)
    - tests/test_multiround_context_edge.py (the hermetic end-to-end drive style: seed fake_repo rows, drive real resume_pipeline with mock_llm, assert persisted VALUES :180-320 — the style model for the full-loop test)
    - tests/conftest.py (fake_repo InMemoryRepo: load_run, set_alias_candidates, update_known_alias, load_known_aliases/load_roster_for_business, load_line_items, the round/consumed mirrors from Plan 11-01; how a business roster + known_aliases are seeded)
    - app/pipeline/reconcile_names.py (reconcile_names + _safe_to_learn_alias — the REAL resolution the full-loop test must exercise, NOT a faked post-state, per D-11-17)
    - app/pipeline/orchestrator.py (Task 1 + Task 2 results under test)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-RESEARCH.md (Validation Architecture CLAR2-04 row — full-loop uses real reconcile_names + real _write_aliases_if_safe, mock LLM only for extraction/suggestion text; Pitfall #9 — mocking the LLM proves nothing about money-safety, so resolution must be real)
    - .planning/phases/11-clarification-round-machine-alias-learning/11-PATTERNS.md (§ test_alias_write.py update; § new test modules — test_alias_full_loop.py copy-from)
  </read_first>
  <action>
    D-11-17's core finding: faked-state tests kept the loop unreachably green. At LEAST the full-loop test must exercise REAL `reconcile_names` resolution (no monkeypatched post-reconciliation).

    tests/test_alias_write.py (UPDATE):
    1. Keep the `_safe_to_learn_alias` unit tests unchanged. Update every faked-state binding test's candidate values from the flat `{token: None}` / `{token: "id"}` to the nested `{token: {"suggested": ..., "bound": ...}}` shape. Update assertions that inspect set_alias_candidates call args to expect the nested shape.
    2. Add a `_normalize_candidate` legacy-shape test: a flat `{token: None}` and `{token: "id"}` row are read without error and behave as `{suggested:None,bound:None}` / `{suggested:None,bound:"id"}` (Pitfall #6 both-shapes coverage).
    3. Add a misname no-bind test at this level if not already covered by the full loop: candidate suggested=S, reply resolves J≠S → bound stays None.

    tests/test_alias_full_loop.py (NEW, unguarded — restate the no-DATABASE_URL-skip warning docstring; hermetic; REAL reconcile_names + REAL _write_aliases_if_safe; mock_llm ONLY for extraction/suggestion text):
    4. Seed fake_repo with a business, a roster employee (e.g. "Robert Nguyen"), and NO known_aliases for a nickname (e.g. "Bobby"). Drive: (a) first submission uses "Bobby" → real reconcile_names returns source=none/unresolved → capture + suggest persists {"Bobby": {"suggested": robert_id, "bound": null}} → clarification sends; (b) a confirming client reply (a bare "yes, that's Bobby" or restating "Robert Nguyen") resumes → real reconcile_names now resolves Robert → bind check sets bound=robert_id; (c) operator approves at the single gate → real _write_aliases_if_safe writes known_aliases (Bobby → robert_id).
    5. THE STOPS-ASKING ASSERTION: a SECOND, independent submission using "Bobby" → real reconcile_names resolves via the now-stored alias (source == "alias"/"stored-alias", resolved True) and the run does NOT enter clarification (no clarification outbound row, run proceeds past the decide gate). Assert the stored alias exists AND the second run took the no-clarification path.
    6. Assert paystub line-item VALUES where a payroll is computed (Phase 7.5 lesson) — the second submission produces a real paystub for Robert, not a clarification.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_alias_full_loop.py tests/test_alias_write.py && uv run pytest -q -m "not integration and not live_llm"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_alias_write.py uses the nested candidate shape throughout, includes a legacy-flat-shape normalize test, and stays green
    - tests/test_alias_full_loop.py exists, is unguarded, and drives REAL reconcile_names + REAL _write_aliases_if_safe (mock LLM only for extraction/suggestion text) — no monkeypatched post-reconciliation state
    - The full-loop test asserts the SECOND "Bobby" submission resolves via the STORED alias (source alias/stored-alias, resolved True) with ZERO clarification sent, and produces a paystub for Robert
    - The misname "no, I meant James" case is pinned to bind nothing
    - Full offline suite exits 0
  </acceptance_criteria>
  <done>The stops-asking loop is proven end-to-end with real resolution — the exact gap (faked-state green loop) D-11-17 targets is closed; the misname guard is pinned no-bind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator resolve form (no-auth demo dashboard) → name resolution + alias write | operator-posted employee_id maps a token to a person and can persist a permanent alias — a money-routing decision |
| client confirming reply → alias bind | untrusted reply drives whether a {token→id} alias is learned |
| alias_candidates JSONB (suggested/bound) → known_aliases write | a wrong bound id silently misroutes every future pay for that token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Tampering / Elevation | resolve-form parameter tampering: post an employee_id from ANOTHER business or an arbitrary UUID → misrouted pay / poisoned alias | mitigate | Server-side membership check: every posted id ∈ `load_roster_for_business(run.business_id)`; reject the WHOLE POST on any invalid id (no partial apply). Test: cross-business id → 4xx/no-op, no state change (Security V4/V5) |
| T-11-12 | Tampering | alias poisoning via a crafted "confirming" reply for a colliding token | mitigate | Bind requires the SPECIFIC server-computed SUGGESTED id to newly resolve (D-11-15) — an attacker cannot choose the target; plus the preserved D-01b `_safe_to_learn_alias` collision re-check at write time and capture-time collision exclusion (unchanged) |
| T-11-13 | Tampering | misname learned as alias ("no, I meant James" binding {token→James}) — a silent future-misroute | mitigate | Bind only when the SUGGESTED id newly resolves; a non-suggested resolution binds nothing (never-learn-from-inference intent, D-11-15). Pinned by the misname no-bind test |
| T-11-14 | Elevation of Privilege | operator resume auto-writing known_aliases without the human gate | mitigate | The alias write stays at the existing single approval gate (`_write_aliases_if_safe` in `_deliver`); the resolve form only sets `bound` on the candidate; checkbox-OFF learns nothing (D-11-16). No auto state change without the human |
| T-11-15 | Information Disclosure | new bind/resolve/suggest log lines leaking tokens + roster names | mitigate | Type-only/counts logging discipline; never token+roster-name together (established) |
| T-11-SC | Tampering | package installs | accept | Zero packages installed this plan (RESEARCH Package Legitimacy Audit: nothing to verify); uv.lock unchanged. `python-multipart` for the resolve form is already a runtime dependency |
</threat_model>

<verification>
- `uv run pytest -q tests/test_alias_full_loop.py tests/test_alias_write.py` — the stops-asking loop with real resolution + the misname no-bind + nested-shape write side
- `uv run pytest -q tests/test_dashboard.py tests/test_needs_operator.py` — resolve form rendering + roster validation + scope exclusions
- `uv run pytest -q -m "not integration and not live_llm"` — no regression
- `grep -q "overrides" app/pipeline/reconcile_names.py` and `grep -q "/resolve" app/main.py` — override + route wired
</verification>

<success_criteria>
- The suggestion persists in the nested shape and binds deterministically on confirmed resolution of the SUGGESTED employee — no LLM, no confidence, in the bind chain
- The misname guard survives verbatim: a non-suggested resolution binds nothing
- The operator can resolve + resume with server-side roster validation; the remember-checkbox binds through the single human gate; cross-business ids are rejected wholesale
- The full-loop test proves the system STOPS asking, driving REAL reconcile_names + real _write_aliases_if_safe (the faked-state gap is closed)
- Full offline suite green
</success_criteria>

<output>
Create `.planning/phases/11-clarification-round-machine-alias-learning/11-04-SUMMARY.md` when done.
</output>
