---
phase: 17-the-pump
plan: 04
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - app/routes/pump.py
  - app/config.py
  - app/main.py
  - tests/test_pump_route.py
autonomous: true
requirements: [PUMP-01]
nyquist_compliant: true

must_haves:
  truths:
    - "GET /internal/pump is authenticated with a constant-time Bearer compare (hmac.compare_digest, D-01), returns 401 on a bad/missing/unset credential (D-02), and fails closed when PUMP_TOKEN is empty/unset (D-03)."
    - "The route loops the SAME drain_once() the worker threads call (PUMP-01 — never a fork), bounded by a max-jobs AND a wall-clock cap (D-05), and returns real per-invocation counts {claimed, done, retried, dead, fenced, queue_depth} — not a bare 200 (criterion #1)."
    - "A dead-lettered or backed-off job returns 200 + counts (normal queue operation, D-09); a genuine infra outage (DB unreachable on claim/count) OR a propagated drain_once() exception (17-01's double-failure re-raise — fail_job's own write failed) returns 503 and turns the cron RED (D-10). The catch-all does not distinguish an infra outage from an unexpected programming error; both surface as 503 — in normal operation only infra failures reach it."
    - "The response satisfies the invariant claimed == done + retried + dead + fenced by construction (each claimed job increments exactly one bucket) — asserted in tests."
  artifacts:
    - "app/routes/pump.py — GET /internal/pump route, _authorized() helper, the bounded drain loop, DrainOutcome aggregation"
    - "app/config.py — Settings.pump_token: str = \"\""
    - "app/main.py — app.include_router(pump.router)"
    - "tests/test_pump_route.py — hermetic auth (401/200) with settings-cache teardown, bounded-drain-cap (max-jobs AND wall-clock), the D-10 double-failure route test (handler raises AND fail_job raises → 503 + lease retained), infra-failure-vs-business-outcome, and the claimed==sum invariant"
  key_links:
    - "app/routes/pump.py ↔ app/queue/drain.drain_once (the one shared implementation; it can now RAISE on the double-failure → the route's try/except returns 503, satisfying D-10) and repo.count_open_jobs (queue_depth)"
    - "_authorized() ↔ get_settings().pump_token ↔ render.yaml PUMP_TOKEN ↔ pump.yml's Bearer header"
    - "the route's _MAX_WALL_CLOCK_SECONDS (120, checked between jobs) ↔ pump.yml's --max-time 420 (17-03) — same design decision in two files; the ~360s request ceiling (120 + a conservative ~240s single-job bound) is derived honestly, NOT from the 210s inter-write stall gap (review finding #2)"
  prohibitions:
    - "The route MUST NOT contain a route-local copy of the claim/dispatch/complete/fail sequence — it calls drain_once() (PUMP-01: never a fork)."
    - "No dev/unauthenticated bypass flag (no ALLOW_* escape hatch) — the test suite calls drain_once() directly (Phase 16 D-06), so the endpoint needs no unauthenticated local path (D-03)."
    - "No cross-module `_private` imports in app/routes/pump.py (tests/test_bound01_private_imports.py auto-scans app/routes/); call the facade repo.<name> and public drain symbols by plain attribute access."
    - "The 401 choice over 404 is deliberate (D-02) — do NOT hide the route behind a 404; a misconfigured PUMP_TOKEN must be loud."
    - "The 503/infra branch MUST log only type(exc).__name__ and return a fixed short body — never str(exc) (could contain a connection string)."
---

<objective>
Add the authenticated pump endpoint (PUMP-01, criterion #1). `GET /internal/pump` is a thin, sync `def`
route modeled on app/routes/health.py: it authenticates with a constant-time Bearer compare
(hmac.compare_digest, D-01) that fails closed on an unset/empty PUMP_TOKEN (D-03) and returns 401 on any
bad/missing credential (D-02, deliberately not 404). It then loops the SAME `drain_once()` the worker
threads call (PUMP-01 — never a fork), bounded by a max-jobs cap AND a wall-clock cap checked between jobs
(D-05), aggregating each DrainOutcome into real counts, and returns
`{claimed, done, retried, dead, fenced, queue_depth}` (queue_depth via repo.count_open_jobs). Business
outcomes (dead-letter, backoff) return 200 + counts (D-09). Any exception mid-drain surfaces as 503 (D-10):
a DB outage on claim/count, OR — critically for D-10 — a propagated `drain_once()` exception (17-01's
double-failure re-raise: `fail_job`'s own write failed during an outage). The catch-all is honest, not
narrow: an unexpected programming error would also 503; in normal operation only infra failures reach it.

Discretion calls (documented at the call site): method is GET (simplest for a curl cron; the drain is
idempotent — SKIP LOCKED makes repeat/concurrent hits safe); env var is PUMP_TOKEN / setting pump_token;
caps are `_MAX_JOBS_PER_PUMP = 20` and `_MAX_WALL_CLOCK_SECONDS = 120`. HONEST derivation (review finding
#2): the 210s figure in app/routes/runs.py:37-72 is the max GAP between two consecutive DB writes (a stall
threshold), NOT a whole-job runtime — `drain_once()` runs the ENTIRE handler before the wall-clock cap is
re-checked. A conservative single-job bound is ~240s (the longest pipeline path's sequential external-call
ceilings ≈ the 210s LLM critical chain + deterministic compute/deliver + slack). The wall-clock cap is
checked BETWEEN drain_once() calls, so a request can begin one final job just under the cap → request ceiling
≈ 120 + 240 = ~360s, which pump.yml's `--max-time 420` covers with ~60s headroom. queue_depth is a separate
cheap SELECT, not part of the claim transaction.

Purpose: the pump is the PRIMARY execution trigger — it must drain with zero live worker threads.
Output: /internal/pump route, pump_token setting, router wiring, hermetic auth/bounded/infra tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-the-pump/17-RESEARCH.md
@.planning/phases/17-the-pump/17-PATTERNS.md
@.planning/phases/17-the-pump/17-CONTEXT.md
@app/routes/health.py
</context>

<artifacts_this_phase_produces>
Newly-created symbols in THIS plan (exclude from drift/source-grounding verification):
- `app/routes/pump.py` — `router`, `pump()` handler, `_authorized()` helper, module constants
  `_MAX_JOBS_PER_PUMP`, `_MAX_WALL_CLOCK_SECONDS`.
- `app/config.py` — `Settings.pump_token: str = ""` field (env var `PUMP_TOKEN`).
- `app/main.py` — `app.include_router(pump.router)` (import of `pump` from app.routes).
Depends on (created by wave-1 siblings, NOT drift): `DrainOutcome`/`drain_once() -> DrainOutcome` (17-01),
`repo.count_open_jobs` (17-02). Related config: `PUMP_TOKEN` render.yaml entry + GitHub secret (17-03).
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pump_token setting, write the /internal/pump route, wire it into main.py</name>
  <files>app/config.py, app/routes/pump.py, app/main.py</files>
  <read_first>
    - app/routes/health.py (full file — the router shape, sync `def`, JSONResponse, and the disclosure discipline: log type(exc).__name__ only, fixed short 503 body)
    - 17-RESEARCH.md § "Pattern 2: The pump route — thin, sync, bounded" (the exact target route body, encoding D-01/D-02/D-03/D-04/D-05/D-09/D-10) and § "Shared Patterns → Constant-time secret compare"
    - 17-PATTERNS.md § "app/routes/pump.py" and § "app/config.py (Settings.pump_token)"
    - app/config.py lines 53-140 (the empty-default-secret convention at 56-57; the `# Durable job queue` block at 72+ where pump_token slots in)
    - app/main.py (full 16 lines — the include_router registration site)
    - app/queue/drain.py (17-01 output — DrainOutcome members + drain_once signature) and app/db/repo/jobs.py (17-02 output — count_open_jobs)
    - .planning/phases/17-the-pump/17-CONTEXT.md D-01/D-02/D-03/D-05/D-09/D-10 and the Claude's-Discretion block
  </read_first>
  <behavior>
    - Missing Authorization header, or `Bearer wrong`, → 401 (no drain performed).
    - PUMP_TOKEN unset/empty in settings → every call is 401 (fail closed), even with a plausible header.
    - Correct `Bearer <PUMP_TOKEN>` → 200 with JSON keys claimed, done, retried, dead, fenced, queue_depth.
    - The drain loop calls drain_once() repeatedly, stops on DrainOutcome.EMPTY, and increments counts[outcome.value].
    - The loop stops at _MAX_JOBS_PER_PUMP even if more jobs are claimable, and stops when the wall-clock deadline passes (checked between drain_once() calls).
    - A DrainOutcome.DEAD or .RETRIED result still yields 200 + counts (D-09), not 5xx.
    - claim/count raising a DB error → 503 with a fixed body, type(exc).__name__ logged only (D-10).
  </behavior>
  <action>
    In app/config.py add `pump_token: str = ""` in (or adjacent to) the `# Durable job queue` block, with a
    one-line trailing comment naming the PUMP_TOKEN env var — the empty-default-secret convention; the
    fail-closed meaning lives in the route, not as field validation (mirroring allow_unsigned_fixtures).
    Create app/routes/pump.py modeled on health.py: `from __future__ import annotations`; import stdlib
    `hmac`, `logging`, `time`; `from fastapi import APIRouter, HTTPException, Request`;
    `from fastapi.responses import JSONResponse`; `from app.config import get_settings`;
    `from app.db import repo`; `from app.queue.drain import DrainOutcome, drain_once`;
    `logger = logging.getLogger("payroll_agent.queue")`; `router = APIRouter()`. Define module constants
    `_MAX_JOBS_PER_PUMP = 20` and `_MAX_WALL_CLOCK_SECONDS = 120` with an HONEST derivation comment (review
    finding #2): the 210s in app/routes/runs.py:37-72 is the max GAP between consecutive DB writes (a stall
    threshold), NOT a whole-job runtime; a conservative single-job bound is ~240s (the longest pipeline
    path's summed sequential external-call ceilings); the cap is checked BETWEEN drain_once() calls, so the
    request ceiling is ~120 + ~240 = ~360s, which pump.yml's --max-time 420 covers with headroom. Write
    `_authorized(request) -> bool`: read
    `get_settings().pump_token`; if falsy return False (D-03 fail-closed); else
    `expected = f"Bearer {token}".encode()`, `got = request.headers.get("authorization", "").encode()`,
    return `hmac.compare_digest(got, expected)`. Write `@router.get("/internal/pump")` sync `def
    pump(request: Request) -> JSONResponse`: if not authorized raise HTTPException(401, "unauthorized")
    (D-02, not 404); initialize counts for done/retried/dead/fenced to 0 and `claimed = 0`; set
    `deadline = time.monotonic() + _MAX_WALL_CLOCK_SECONDS`; wrap the drain loop + the queue-depth read in a
    single try/except: loop while `claimed < _MAX_JOBS_PER_PUMP and time.monotonic() < deadline`, call
    `outcome = drain_once()`, break on `DrainOutcome.EMPTY`, else `claimed += 1` and
    `counts[outcome.value] += 1`; after the loop `queue_depth = repo.count_open_jobs()`; on any exception
    (D-10 — this includes a genuine infra outage on claim/count AND a propagated drain_once() exception from
    17-01's double-failure re-raise; be honest in the comment that an unexpected programming error would also
    land here — the catch-all does not distinguish them, but in normal operation only infra failures reach it)
    `logger.error("pump: infra failure mid-drain: %s", type(exc).__name__)` then raise HTTPException(503,
    "pump unavailable") from exc — never log/return str(exc). Return JSONResponse({"claimed": claimed,
    **counts, "queue_depth": queue_depth}). Note `claimed == done + retried + dead + fenced` holds by
    construction (each claimed job increments exactly one bucket). Add a
    one-line comment at the route noting GET was chosen (idempotent drain; SKIP LOCKED makes repeat hits
    safe). In app/main.py add `pump` to the `from app.routes import ...` line and
    `app.include_router(pump.router)`. Keep drain_once() and repo.count_open_jobs() as plain attribute-access
    calls; add no `_private` cross-module imports.
  </action>
  <verify>
    <automated>uv run ruff check app/routes/pump.py app/config.py app/main.py && uv run mypy app/routes/pump.py && grep -q 'hmac.compare_digest(' app/routes/pump.py && grep -q '@router.get("/internal/pump")' app/routes/pump.py && grep -q 'pump_token' app/config.py && grep -q 'include_router(pump.router)' app/main.py && uv run pytest tests/test_bound01_private_imports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/routes/pump.py` contains `hmac.compare_digest(` and `@router.get("/internal/pump")` and `_authorized`.
    - `app/config.py` contains `pump_token` (grep succeeds); the field default is the empty string.
    - `app/main.py` contains `include_router(pump.router)` and imports `pump` from app.routes.
    - The route contains `_MAX_JOBS_PER_PUMP` and `_MAX_WALL_CLOCK_SECONDS` with a derivation comment; the response keys are claimed/done/retried/dead/fenced/queue_depth.
    - `uv run pytest tests/test_bound01_private_imports.py -q` exits 0 (no cross-module `_private` refs in the new route).
    - `uv run mypy app/routes/pump.py` exits 0.
  </acceptance_criteria>
  <done>The route authenticates constant-time and fail-closed, loops the shared drain_once() under a dual cap, returns the six-key count JSON on business outcomes and 503 only on infra failure, and is wired into main.py; the BOUND-01 scan passes.</done>
</task>

<task type="auto">
  <name>Task 2: Hermetic tests — auth (401/200), bounded drain cap, D-10 infra-vs-business semantics</name>
  <files>tests/test_pump_route.py</files>
  <read_first>
    - tests/test_queue_drain.py lines 1-80 (the fake_repo / seeding style to match) and its drain_once monkeypatch patterns
    - tests/test_repo_jobs_sql.py lines 20-31 (the CANONICAL settings-cache idiom: an autouse fixture that `get_settings.cache_clear()` BOTH before AND after — post-yield — each test; copy this so a cached PUMP_TOKEN cannot leak across tests, review 17-04 MEDIUM)
    - 17-RESEARCH.md § "Validation Architecture → Phase Requirements → Test Map" (the auth, bounded-drain, and infra-failure rows) and § Wave 0 Gaps
    - 17-REVIEWS.md § "Plan 17-04" (the D-10 double-failure route test, the settings-cache teardown, the catch-all honesty, and the claimed==sum invariant)
    - 17-PATTERNS.md § "tests/test_pump_route.py" (TestClient usage; the get_settings.cache_clear + monkeypatch.setenv("PUMP_TOKEN", ...) idiom)
    - app/routes/pump.py (Task 1 output) and app/queue/drain.py (17-01 — drain_once() now RE-RAISES the double-failure; the route's try/except turns that into 503)
    - tests/conftest.py (confirm WORKER_COUNT=0 is pinned suite-wide, so TestClient runs with zero live worker threads by construction)
  </read_first>
  <action>
    Create tests/test_pump_route.py (hermetic, no live Postgres). Use `TestClient(app_main.app)` so the
    real FastAPI 401/503 handling is exercised, not the route function in isolation. SETTINGS-CACHE
    DISCIPLINE (review 17-04 MEDIUM): add an autouse fixture (or a try/finally per test) that
    `get_settings.cache_clear()` BOTH before the test AND after (post-yield), copying tests/test_repo_jobs_sql.py:20-31's
    idiom — set PUMP_TOKEN via `monkeypatch.setenv("PUMP_TOKEN", ...)`; clearing only before would leak a
    cached token into the next test. Tests: (auth) a request with no Authorization header returns 401;
    `Bearer wrong` returns 401; with PUMP_TOKEN unset/empty a correct-looking header still returns 401
    (fail-closed D-03); the correct `Bearer <token>` returns 200 with JSON keys
    claimed/done/retried/dead/fenced/queue_depth present, AND assert the invariant
    `claimed == done + retried + dead + fenced` on the response body (review suggestion). (bounded)
    monkeypatch drain_once to always return a non-EMPTY DrainOutcome (e.g. DONE) and count_open_jobs to a
    constant, hit the authenticated route, assert the response `claimed == _MAX_JOBS_PER_PUMP` (the loop
    stopped at the max-jobs cap rather than looping forever); a SEPARATE wall-clock test must prove the
    wall-clock branch (not the job-count branch) forced the exit: monkeypatch `time.monotonic` to jump past
    `deadline` after exactly N iterations where N < _MAX_JOBS_PER_PUMP, then assert `claimed == N` (strictly
    less than _MAX_JOBS_PER_PUMP). A bare "loop terminates and returns 200" assertion is NOT sufficient — it
    would still pass if the wall-clock check were a no-op and the max-jobs cap did all the work. (infra)
    monkeypatch repo.count_open_jobs (or claim via drain_once) to raise a DB-style exception and assert the
    route returns 503 (not 200, not a swallowed 500-as-200), and that a DrainOutcome.DEAD/RETRIED result
    still returns 200 + counts (D-09 — a business outcome is not an infra failure). (D-10 double-failure —
    the load-bearing finding-#1 path) ADD a test that monkeypatches drain_once to RAISE a RuntimeError
    (simulating 17-01's re-raised double-failure — fail_job's own write failed) and asserts the route returns
    503, NOT 200-with-a-fenced-count; a test that only makes count_open_jobs raise does NOT cover this path
    (the review's explicit point). Name tests so `-k auth`, `-k bounded`, and `-k infra_failure` each select
    the intended subset (matches the VALIDATION.md commands); name the double-failure test so `-k infra_failure`
    selects it too (it is an infra failure).
  </action>
  <verify>
    <automated>uv run pytest tests/test_pump_route.py -q && uv run pytest tests/test_pump_route.py -k auth -q && uv run pytest tests/test_pump_route.py -k bounded -q && uv run pytest tests/test_pump_route.py -k infra_failure -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_pump_route.py -q` exits 0.
    - `-k auth` selects ≥1 test and passes: 401 on missing/wrong/empty-secret, 200 on correct token; the 200 test also asserts `claimed == done + retried + dead + fenced` on the response body.
    - `-k bounded` selects ≥2 tests: one asserts `claimed == _MAX_JOBS_PER_PUMP` (max-jobs cap); a separate one, with `time.monotonic` jumped past `deadline` after N iterations, asserts `claimed == N` where `N < _MAX_JOBS_PER_PUMP` (proves the wall-clock branch — not the job-count cap — forced the exit).
    - `-k infra_failure` selects ≥2 tests: (a) count_open_jobs raising a DB error → 503 while a DEAD/RETRIED business outcome → 200 + counts; (b) drain_once RAISING a RuntimeError (17-01's re-raised double-failure) → 503, NOT 200 (the D-10 finding-#1 path a count_open_jobs-only test would miss).
    - The suite clears the settings cache AFTER each pump test (post-yield/try-finally), not only before: `grep -c "get_settings.cache_clear" tests/test_pump_route.py` is ≥ 2 (before + after) or a single autouse fixture with cache_clear on both sides of the yield.
    - `git diff tests/test_pump_route.py` shows no `pytest.mark.integration`/`queueproof` (this file is hermetic).
  </acceptance_criteria>
  <done>Hermetic tests prove constant-time fail-closed auth (401/200) with settings-cache teardown, the claimed==sum invariant, the dual-cap bounded drain (max-jobs AND wall-clock), the D-10 double-failure path (drain_once raising → 503), and the D-10/D-09 infra-vs-business status split, all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron/client → /internal/pump | Untrusted network request; the only trusted input is the Bearer secret, compared before any drain. |
| pump route → DB (claim/count) | A DB outage here must surface RED, not be swallowed into a GREEN 200. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01 | Information Disclosure | _authorized() Bearer compare | medium | mitigate | `hmac.compare_digest(bytes, bytes)`, never `==` — timing-attack-resistant by construction (D-01, V2/V6). Pinned by the auth tests. |
| T-17-02 | Elevation of Privilege | unset PUMP_TOKEN | high | mitigate | Fail closed: `_authorized()` returns False immediately when the token is falsy, before the compare (D-03). Pinned by the empty-secret auth test. |
| T-17-03 | Denial of Service | drain loop | medium | mitigate | Dual cap — `_MAX_JOBS_PER_PUMP` (20) and `_MAX_WALL_CLOCK_SECONDS` (120, checked between jobs) bound each invocation so a poison/backlog flood cannot pin the request (D-05). Pinned by the bounded test. |
| T-17-04 | Information Disclosure | 401-vs-404 status | low | accept | 401 (not 404) deliberately reveals the route exists, in exchange for a loud RED on a misconfigured token (D-02). The route is not a data-disclosure surface. |
| T-17-05 | Information Disclosure | 503 body / logs | medium | mitigate | The infra branch logs only `type(exc).__name__` and returns a fixed "pump unavailable" body — never `str(exc)` (which could carry a connection string). Mirrors health.py disclosure discipline. The catch-all is honestly broad: it also catches a propagated drain_once() exception (17-01's double-failure) and any programming error — all → 503 with the same fixed body. |
| T-17-13 | Repudiation (false GREEN over an outage) | drain_once() double-failure → 503 | high | mitigate | A DB-write failure inside drain_once() (fail_job's own write failed during an outage) must NOT be reported as a 200 (D-10). 17-01 re-raises it; this route's try/except turns it into 503, so the cron goes RED over a real outage. Pinned by the -k infra_failure double-failure test (drain_once raises → 503, not 200). |
| T-17-SC | Tampering | package installs | low | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit — not applicable). `hmac` is stdlib. |
</threat_model>

<verification>
- `uv run pytest tests/test_pump_route.py -q` green across auth / bounded / infra_failure.
- `uv run pytest tests/test_bound01_private_imports.py tests/test_queue_worker.py -q` green (route scan clean; worker unaffected).
- `uv run mypy app/routes/pump.py app/config.py app/main.py` exits 0.
- `grep -q 'hmac.compare_digest(' app/routes/pump.py` and `grep -q 'include_router(pump.router)' app/main.py`.
</verification>

<success_criteria>
GET /internal/pump authenticates constant-time and fail-closed (401 on bad/missing/unset), loops the one
shared drain_once() under a max-jobs + wall-clock cap (honestly derived — ~360s ceiling under pump.yml's
--max-time 420), returns real six-key counts on business outcomes (with claimed == done+retried+dead+fenced),
and 503 on a genuine infra outage OR a propagated drain_once() double-failure (D-10). It is wired into
main.py and covered by green hermetic tests (with settings-cache teardown).
</success_criteria>

<output>
Create `.planning/phases/17-the-pump/17-04-SUMMARY.md` when done.
</output>
