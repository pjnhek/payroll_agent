---
phase: 13-module-structure-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/repo.py
  - app/db/repo/__init__.py
  - app/db/repo/_shared.py
  - app/db/repo/runs.py
  - app/db/repo/pipeline_state.py
  - app/db/repo/emails.py
  - app/db/repo/roster.py
  - app/db/repo/demo.py
  - tests/test_persistence.py
  - tests/test_threading.py
  - tests/test_alias_write.py
autonomous: true
requirements: [STRUCT-02, STRUCT-04]
must_haves:
  truths:
    - "`from app.db import repo` and `import app.db.repo as repo_mod` both resolve every function that existed on the flat repo.py, unchanged in behavior"
    - "monkeypatch.setattr(repo, \"fn\", ...) intercepts calls the same way it did before the split, for every one of the 14 pre-existing repo monkeypatch seams"
    - "the full suite passes with zero assertion changes anywhere outside the enumerated import-path retargets in this plan"
  artifacts:
    - path: "app/db/repo/__init__.py"
      provides: "package facade re-exporting the full public API (plus _scrub) so all existing import styles keep working"
      contains: "from app.db.repo.runs import"
    - path: "app/db/repo/runs.py"
      provides: "ingest/lifecycle + status CAS + sweep + error/scrub aggregate"
      contains: "def claim_status"
    - path: "app/db/repo/pipeline_state.py"
      provides: "persist/load extracted/decision/line-items/clarify-round JSONB context"
      contains: "def persist_decision"
    - path: "app/db/repo/emails.py"
      provides: "email_messages + threading/header lookups"
      contains: "def insert_email_message"
    - path: "app/db/repo/roster.py"
      provides: "roster read"
      contains: "def load_roster_for_business"
    - path: "app/db/repo/demo.py"
      provides: "demo binding + dashboard list queries"
      contains: "def bind_demo_business"
  key_links:
    - from: "app/db/repo/runs.py"
      to: "app/db/repo/demo.py"
      via: "direct sibling import (create_run calls get_record_only_flag)"
      pattern: "from app\\.db\\.repo import demo as repo_demo"
    - from: "app/db/repo/__init__.py"
      to: "tests/test_persistence.py"
      via: "facade re-export of the private _scrub helper"
      pattern: "_scrub"
---

<objective>
Split `app/db/repo.py` (1,734 lines, ~55 functions) into the package `app/db/repo/` with five
per-aggregate modules (`runs.py`, `pipeline_state.py`, `emails.py`, `roster.py`, `demo.py`) plus
one internal plumbing module (`_shared.py`), behind an `__init__.py` facade that re-exports the
full public API — including the private `_scrub` helper that `tests/test_persistence.py` reaches
directly — so every existing call site (`from app.db import repo`, `import app.db.repo as
repo_mod`, `monkeypatch.setattr(repo, "fn", ...)`) keeps working completely unchanged.

Purpose: STRUCT-02 — right-size the largest data-layer file into aggregate modules with a stable
import surface, so callers and tests migrate via mechanical import-path updates only (STRUCT-04).
Output: `app/db/repo/` package (6 new files) replacing the flat `app/db/repo.py`; three test files
get mechanical import-path retargets for their `inspect.getsource`/direct-`_scrub`-access coupling
points that cross the new module boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-module-structure-boundaries/13-CONTEXT.md
@.planning/phases/13-module-structure-boundaries/13-RESEARCH.md
@.planning/phases/13-module-structure-boundaries/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the app/db/repo/ package — five aggregate modules + _shared.py + facade</name>
  <files>app/db/repo.py, app/db/repo/__init__.py, app/db/repo/_shared.py, app/db/repo/runs.py, app/db/repo/pipeline_state.py, app/db/repo/emails.py, app/db/repo/roster.py, app/db/repo/demo.py</files>
  <read_first>app/db/repo.py (full file — this is the source of truth for every function body being moved verbatim)</read_first>
  <action>
Delete `app/db/repo.py` and create the package `app/db/repo/` with these six files. Every function
body moves VERBATIM (no rewriting, no signature changes, no docstring edits beyond the D-04
deletion below) — this is a cut-and-paste-with-import-fixups operation per CONTEXT.md's D-11/D-01.

`app/db/repo/_shared.py`: internal plumbing only. Move `_conn_ctx` (repo.py lines 156-164) and
`_nulltx` (repo.py lines 1731-1734) here verbatim, with `from __future__ import annotations`,
`import contextlib`, and `from app.db.supabase import get_connection` as the only imports needed.

`app/db/repo/runs.py`: the "Ingest / run lifecycle" aggregate (D-02) — move verbatim: `EMPLOYEE_COLS`
stays OUT of this file (goes to roster.py, see below); `RUN_COLS` (repo.py lines 132-136) and
`_TERMINAL_STATUSES` (lines 138-153) move here. Functions: `insert_inbound_email`,
`link_email_to_run`, `find_business_by_sender`, `find_run_by_message_id`, `load_business_name`,
`create_run`, `load_run`, `load_source_email`, `load_inbound_email` (repo.py lines 171-421),
`set_status`, `claim_status`, `sweep_stranded_runs` (lines 428-521), `_build_accent_class_map`,
`_ACCENT_CLASS_MAP` module constant if present inline, `_compile_name_pattern`, `_scrub`,
`_build_error_detail`, `record_run_error` (lines 556-728). Imports: `from __future__ import
annotations`, `contextlib` is NOT needed directly (only `_shared` uses it), `import json`,
`import logging`, `import re`, `import unicodedata`, `import uuid`, `from typing import Any`,
`import psycopg.rows`, `from app.db.repo._shared import _conn_ctx, _nulltx`, `from app.db.repo
import demo as repo_demo` (see cross-aggregate call below), `from app.db.supabase import
get_connection`, `from app.models.contracts import ClarifiedFields, Decision, Extracted,
PaystubLineItem` (trim to what runs.py actually uses — `record_run_error`'s type hints), `from
app.models.roster import Employee, NameMatchResult, Roster` (trim similarly), `from app.models.status
import RunStatus`, `logger = logging.getLogger("payroll_agent.repo")`.
CROSS-AGGREGATE CALL (D-03 invariant): `create_run` calls `get_record_only_flag` — this is the ONE
public-function-calls-public-function-across-aggregates edge in the whole file. Wire it as
`from app.db.repo import demo as repo_demo` at the top of `runs.py`, called as
`repo_demo.get_record_only_flag(...)` — a DIRECT sibling import, never `from app.db import repo`
(that would import the package `__init__` mid-init and create a circular import, per RESEARCH
Pattern 1's Anti-Pattern warning).

`app/db/repo/pipeline_state.py`: the JSONB persistence aggregate (D-02) — move verbatim:
`persist_extracted`, `persist_decision`, `persist_reconciliation`, `replace_line_items`,
`set_alias_candidates`, `set_pre_clarify_extracted`, `load_pre_clarify_extracted`,
`set_clarified_fields`, `load_clarified_fields`, `get_clarification_round`,
`set_clarification_round`, `clear_reply_context`, `update_known_alias` (repo.py lines 729-1055).
Same-aggregate calls (`set_pre_clarify_extracted`/`set_clarification_round` → `set_status`,
`record_run_error` → `set_status`) are CROSS-aggregate here (pipeline_state.py calling
runs.py's `set_status`) — wire as `from app.db.repo import runs as repo_runs` at the top,
called as `repo_runs.set_status(...)`. Imports: `from __future__ import annotations`, `import json`,
`import logging`, `import uuid`, `from app.db.repo._shared import _conn_ctx, _nulltx`, `from
app.db.repo import runs as repo_runs`, `from app.models.contracts import ClarifiedFields, Decision,
Extracted, PaystubLineItem`, `from app.models.roster import NameMatchResult`, `from app.models.status
import RunStatus`, `logger = logging.getLogger("payroll_agent.repo")`.

`app/db/repo/emails.py`: the email/threading aggregate (D-02) — move verbatim: `insert_email_message`,
`get_outbound_message_id`, `get_outbound_for_round`, `mark_reply_consumed`, `load_consumed_replies`,
`get_inbound_by_message_id`, `find_stranded_unconsumed_replies`, `update_email_message_sent`,
`update_email_message_state`, `get_outbound_references_chain`, `load_outbound_emails` (repo.py
lines 1062-1449), PLUS `_pad_references`, `find_awaiting_reply_for_header`, `find_any_run_for_header`,
`load_thread_messages` (lines 1546-1651 — verified live at these exact lines; these are threading
helpers physically adjacent to the "Demo routing helpers" banner in the current file but belong in
`emails.py` by function per D-02's "email_messages + threading/header lookups" wording).
`update_email_message_sent` calls `update_email_message_state` — same-aggregate, same-module,
no cross-import needed. Imports: `from __future__ import annotations`, `import logging`, `import
uuid`, `import psycopg.rows`, `from app.db.repo._shared import _conn_ctx, _nulltx`, `from
app.db.supabase import get_connection` (if any function opens its own connection outside
`_conn_ctx`), `logger = logging.getLogger("payroll_agent.repo")`.

`app/db/repo/demo.py`: the demo/dashboard aggregate (D-02) — move verbatim: `list_businesses`,
`bind_demo_business`, `get_demo_binding`, `set_record_only`, `get_record_only_flag` (repo.py lines
1455-1545), PLUS `load_line_items`, `load_all_runs` (lines 1657-1710 — dashboard list queries per
D-02's explicit note that these sit under the "Roster" banner in the pre-split file but belong here
by function). Imports: `from __future__ import annotations`, `import logging`, `import uuid`,
`import psycopg.rows`, `from app.db.repo._shared import _conn_ctx, _nulltx`, `from
app.models.contracts import PaystubLineItem`, `from app.models.status import RunStatus`,
`logger = logging.getLogger("payroll_agent.repo")`.

`app/db/repo/roster.py`: the roster aggregate (D-02) — move verbatim: `EMPLOYEE_COLS` (repo.py
lines 109-115) and `load_roster_for_business` (lines 1712-1723). Imports: `from __future__ import
annotations`, `import uuid`, `import psycopg.rows`, `from app.db.repo._shared import _conn_ctx`,
`from app.models.roster import Employee, Roster`.

`app/db/repo/__init__.py`: the package facade (D-01) — re-export the FULL public API from all five
aggregate modules PLUS `_scrub` from `runs.py` (RESEARCH Pitfall 2: `tests/test_persistence.py`
does `monkeypatch.setattr(repo, "_scrub", ...)` and `repo._scrub(...)` directly against `from
app.db import repo` — omitting it breaks that pre-existing test). Also re-export `RUN_COLS`,
`_TERMINAL_STATUSES`, `EMPLOYEE_COLS` if any external caller/test imports them from `app.db.repo`
(grep to confirm before adding — only add what is actually referenced externally, do not
over-export). Facade docstring: "DB repo package facade — re-exports the full public API so
existing callers (`from app.db import repo`, `import app.db.repo as repo_mod`) and test monkeypatch
seams (`monkeypatch.setattr(repo, "fn", ...)`) keep working unchanged post-split." No `from
__future__ import annotations` needed in a pure re-export file.

Per D-04: delete the 76-line function-index docstring (repo.py lines 1-89) entirely — do not carry
it into any new module or the facade. Each of the five aggregate modules gets ONE-LINE placeholder
docstring only, e.g. `"""DB repo — run lifecycle, status CAS, sweep, and error/scrub helpers."""`
for `runs.py`. Do not write comprehensive module-purpose docstrings now (Phase 15/COMM-02 owns that).

Preserve the Discipline paragraph from the old module docstring (repo.py lines 79-89, "Pooled
get_connection() + conn.transaction()...") — this documents a real constraint, not process
history — by placing it as a short comment block in `_shared.py` above `_conn_ctx`, since that is
where the pattern it describes actually lives post-split. Do not duplicate it across all five
aggregate files.

Carry each aggregate's existing `# ---` section banner text into that module's own file header,
scoped to its own concern (e.g. `runs.py` keeps "# Ingest / run lifecycle" as an internal banner
if it still separates two distinct clusters within the file; use judgment — a banner separating
content that's ALL now the same file's single concern can be dropped).

Use `git mv` where practical is not literal here since this is a 1-file-to-6-files split, not a
rename — use `git rm app/db/repo.py` after creating the six new files with `Write`, then `git add`
the new package, so history is preserved via content-similarity detection rather than a rename op.
  </action>
  <acceptance_criteria>
    - `app/db/repo.py` no longer exists; `app/db/repo/__init__.py`, `_shared.py`, `runs.py`,
      `pipeline_state.py`, `emails.py`, `roster.py`, `demo.py` all exist
    - `uv run python -c "from app.db import repo; print(len([n for n in dir(repo) if not n.startswith('__')]))"` prints a count >= 55 (full public surface plus `_scrub`)
    - `uv run python -c "from app.db import repo; repo._scrub"` succeeds with no AttributeError
    - `uv run python -c "import app.db.repo as repo_mod; repo_mod.claim_status"` succeeds with no AttributeError
    - `uv run python -c "from app.db.repo import runs; from app.db.repo import demo; runs.create_run"` succeeds (confirms the cross-aggregate `repo_demo` import does not create a circular-import failure at package-init time)
    - `grep -rn "SELECT \*" app/db/repo/` returns no matches (explicit-column-read discipline preserved)
    - `grep -c "def " app/db/repo/__init__.py` is 0 (facade contains only imports/re-exports, no new function bodies)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from app.db import repo; import app.db.repo as m; assert repo.claim_status is m.claim_status; assert hasattr(repo, '_scrub')"</automated>
  </verify>
  <done>The app/db/repo/ package exists with six files, the facade re-exports the complete public API plus _scrub, no SELECT * anywhere, and both import styles resolve identical objects.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the test-coupling census for the repo split, then verify the full suite green</name>
  <files>tests/test_persistence.py, tests/test_threading.py, tests/test_alias_write.py</files>
  <read_first>tests/test_persistence.py (the repo._scrub direct-access test, 6 call sites), tests/test_threading.py (the inspect.getsource(repo_mod) structural test), tests/test_alias_write.py (the inspect.getsource(repo.set_alias_candidates) structural test)</read_first>
  <action>
Verify (do not assume) the following test-coupling points still resolve correctly against the new
`app/db/repo/` package, and fix any that break:

1. `tests/test_persistence.py` (6 call sites, ~lines 424/466/514/539/559/574): `repo._scrub(message,
   roster=roster)` and `monkeypatch.setattr(repo, "_scrub", ...)` against `from app.db import
   repo` — these should resolve unchanged because Task 1's facade re-exports `_scrub`. Run this
   file in isolation first to confirm; if any call fails with AttributeError, the facade is missing
   the re-export — go back and fix `app/db/repo/__init__.py`, do not change the test.

2. `tests/test_threading.py::test_references_like_is_parameterized` (~line 190): does
   `import app.db.repo as repo_mod; src = inspect.getsource(repo_mod)` then asserts
   `"%(references)s" in src` and `"LIKE f'" not in src and 'LIKE f"' not in src`. Post-split,
   `inspect.getsource(repo_mod)` returns only `app/db/repo/__init__.py`'s source (the facade), which
   does NOT contain the actual SQL — the `%(references)s` placeholder lives in `emails.py`'s
   `find_awaiting_reply_for_header`/`find_any_run_for_header` function bodies. Retarget this test to
   inspect the emails submodule directly: `import app.db.repo.emails as emails_mod; src =
   inspect.getsource(emails_mod)`, keep the exact same two assertions (string content unchanged —
   this is an import-path update only, not an assertion change per STRUCT-04).

3. `tests/test_alias_write.py::test_repo_set_alias_candidates_sql_uses_jsonb_merge_not_overwrite`
   (~line 1585): does `from app.db import repo; src = inspect.getsource(repo.set_alias_candidates)`.
   `inspect.getsource` on a specific FUNCTION object (not a module) works correctly regardless of
   which submodule the function physically lives in, as long as `repo.set_alias_candidates`
   resolves to the real function object — which it does via the facade re-export from Task 1. Run
   this test in isolation to CONFIRM it still passes unchanged; do not modify it unless it fails.

Grep for any other `inspect.getsource` or direct-module-source-string assertion against `repo`
or `app.db.repo` across `tests/` that this plan's read_first scan may have missed (the census in
13-RESEARCH.md and this plan's own upstream grep found exactly these three files); if a new one
surfaces, apply the same retargeting principle (submodule-object inspection, not `repo` package
inspection, whenever the assertion depends on function BODY source text).

After fixing test-coupling, run the full suite. Any failure NOT in the three files above means an
aggregate module has a wiring bug (missing import, wrong cross-aggregate reference, or a symbol
left out of the facade) from Task 1 — fix the SOURCE, never loosen a test assertion to make it
pass.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_persistence.py -q` passes with 0 failures, 0 errors
    - `uv run pytest tests/test_threading.py -q` passes with 0 failures, 0 errors
    - `uv run pytest tests/test_alias_write.py -q` passes with 0 failures, 0 errors
    - `uv run pytest -q` (full suite) passes with the same test COUNT as the pre-split baseline (663 or whatever `pytest --collect-only -q` reports immediately before Task 1 started) and zero failures
    - `git diff tests/test_threading.py` shows only the two-line retarget (`app.db.repo as repo_mod` → `app.db.repo.emails as emails_mod`, and the `inspect.getsource(repo_mod)` → `inspect.getsource(emails_mod)` call) — no assertion string changes
    - `uv run ruff check app/db/repo tests/test_persistence.py tests/test_threading.py tests/test_alias_write.py` reports zero violations
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>All three coupling-point test files pass unmodified in assertions (only the emails.py retarget in test_threading.py changes), the full suite is green at the same test count as before the split, and ruff is clean over the new package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App code ↔ Postgres | `app/db/repo/*` is the sole write/read boundary to `payroll_runs`/`employees`/`email_messages`; this plan relocates but does not alter any SQL statement |
| Test code ↔ production module internals | `tests/test_persistence.py` reaches `repo._scrub` (a private PII-redaction helper) directly — an intentional, pre-existing carve-out this plan must preserve via the facade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-01 | Tampering | `_scrub` PII-redaction call chain (`record_run_error` → `_build_error_detail` → `_scrub`) | mitigate | All four scrub-related helpers (`_build_accent_class_map`, `_compile_name_pattern`, `_scrub`, `_build_error_detail`) land together in `runs.py` alongside `record_run_error` in the same commit — never split across aggregates, per D-02's explicit instruction; Task 2's acceptance criteria re-runs `tests/test_persistence.py` (the PII-scrub regression suite) to confirm the wiring survived verbatim |
| T-13-02 | Tampering | Package-facade re-export completeness (`app/db/repo/__init__.py`) | mitigate | Task 1 requires the facade re-export EVERY public name plus `_scrub`; Task 1's acceptance criteria asserts the re-exported symbol count is >= the pre-split public surface via a live `dir(repo)` check, not a visual diff |
| T-13-03 | Repudiation | Silent circular-import failure at package-init time (`runs.py` importing `demo.py` via the `__init__` facade instead of a direct sibling import) | mitigate | Task 1's action explicitly specifies `from app.db.repo import demo as repo_demo` (direct sibling import) for the one cross-aggregate call (`create_run` → `get_record_only_flag`); Task 1's acceptance criteria includes a live import smoke-test of exactly this edge |
| T-13-04 | Tampering | SQL injection via f-string interpolation reintroduced during the mechanical move | accept | The move is verbatim (copy-paste, no rewriting) so no new SQL construction is introduced; Task 1's acceptance criteria greps for `SELECT *` as a discipline check, and `tests/test_threading.py`'s retargeted `test_references_like_is_parameterized` continues to assert the named-placeholder discipline in `emails.py` post-split |
</threat_model>

<verification>
`uv run pytest -q` passes at the same collected-test count as immediately before this plan's Task 1
began, with zero assertion changes outside the three named coupling-point retargets in Task 2.
`uv run ruff check .` is clean. Both import styles (`from app.db import repo`,
`import app.db.repo as repo_mod`) resolve every previously-public name plus `_scrub`.
</verification>

<success_criteria>
- `app/db/repo.py` (flat file) no longer exists; `app/db/repo/` package exists with 6 files
- Every one of the 14 pre-existing `monkeypatch.setattr(repo, "fn", ...)` seams across
  `tests/test_hitl.py`, `tests/conftest.py`, `tests/test_cr_regressions.py`, `tests/test_gateway.py`,
  `tests/test_persistence.py`, `tests/test_alias_write.py`, `tests/test_stuck_run_recovery.py`,
  `tests/test_atomic_persist.py`, `tests/test_demo_landing.py`, `tests/test_resume_pipeline.py`
  intercepts calls identically to pre-split behavior (verified by the full suite passing)
- Full suite green at the pre-split test count; only 3 test files touched, only for structural
  source-inspection retargeting (no behavioral assertion changed)
</success_criteria>

<output>
Create `.planning/phases/13-module-structure-boundaries/13-01-SUMMARY.md` when done
</output>
</content>
