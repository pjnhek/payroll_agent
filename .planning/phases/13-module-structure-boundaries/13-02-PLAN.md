---
phase: 13-module-structure-boundaries
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/orchestrator.py
  - app/pipeline/alias_learning.py
  - app/pipeline/clarification.py
  - app/pipeline/delivery.py
  - app/pipeline/reconcile_names.py
  - app/pipeline/validate.py
  - eval/run_eval.py
  - tests/test_delivery.py
  - tests/test_alias_write.py
  - tests/test_clarify.py
  - tests/test_clarify_rounds.py
  - tests/test_retrigger_epoch.py
  - tests/test_combined_context.py
  - tests/test_hitl.py
autonomous: true
requirements: [STRUCT-03, STRUCT-04, BOUND-01]
must_haves:
  truths:
    - "resume_pipeline and _run_stages (staying in orchestrator.py) call the moved alias/clarify/delivery helpers via module-object references (alias_learning.fn, clarification.fn, delivery.fn), never bare names"
    - "no function body anywhere in app/pipeline/ imports a `_private` name from a sibling module after this plan lands"
    - "the full suite passes with zero assertion changes outside the enumerated import-path retargets in this plan"
  artifacts:
    - path: "app/pipeline/alias_learning.py"
      provides: "the single home for the alias-learning rule set: normalize_candidate, bind_evidence_for_token, write_aliases_if_safe, safe_to_learn_alias"
      contains: "def write_aliases_if_safe"
    - path: "app/pipeline/clarification.py"
      provides: "the clarify cluster: clarify, defer_field_regression_clarification, render_asked_summary, combined_context_email"
      contains: "def clarify"
    - path: "app/pipeline/delivery.py"
      provides: "confirmation delivery: deliver"
      contains: "def deliver"
  key_links:
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/alias_learning.py"
      via: "module-object import (resume_pipeline calls alias_learning.normalize_candidate / alias_learning.bind_evidence_for_token)"
      pattern: "from app\\.pipeline import alias_learning"
    - from: "app/pipeline/delivery.py"
      to: "app/pipeline/alias_learning.py"
      via: "module-object import (deliver calls alias_learning.write_aliases_if_safe)"
      pattern: "from app\\.pipeline import alias_learning"
    - from: "app/pipeline/alias_learning.py"
      to: "app/pipeline/reconcile_names.py"
      via: "module-level import of the promoted public normalize_name + deterministic_match"
      pattern: "from app\\.pipeline\\.reconcile_names import"
---

<objective>
Split `app/pipeline/orchestrator.py` (1,843 lines) into the core state machine (staying,
~800-900 lines: `run_pipeline`, `_run`, `resume_pipeline`, `_run_stages`, `_compute_line_items`,
`backfill_extracted`) plus three carved-out modules — `app/pipeline/alias_learning.py`,
`app/pipeline/clarification.py`, `app/pipeline/delivery.py` — using module-object imports at
every boundary so existing `monkeypatch.setattr(<module>, "<fn>", ...)` test seams retarget
mechanically to their new owning module. In the same commit series, promote every BOUND-01
violation this split touches: `_norm`→`normalize_name` and `_safe_to_learn_alias`→
`safe_to_learn_alias` (relocated into `alias_learning.py`) in `reconcile_names.py`;
`_HOURS_FIELDS`→`HOURS_FIELDS` and `_is_paid`→`is_paid` in `validate.py`; `_deliver`→`deliver`,
`_clarify`→`clarify`, `_write_aliases_if_safe`→`write_aliases_if_safe`,
`_normalize_candidate`→`normalize_candidate`, `_bind_evidence_for_token`→`bind_evidence_for_token`,
`_defer_field_regression_clarification`→`defer_field_regression_clarification`,
`_render_asked_summary`→`render_asked_summary`, `_combined_context_email`→`combined_context_email`.

Purpose: STRUCT-03 (alias-learning helpers carved out into their own module) + the majority of
BOUND-01 (cross-module `_private` imports promoted to public names) + STRUCT-04 (behavior-neutral,
suite green throughout).
Output: `app/pipeline/orchestrator.py` trimmed to the core state machine; three new pipeline
modules; `reconcile_names.py`/`validate.py` promotion edits; `eval/run_eval.py` import retarget;
seven test files get mechanical import-path/patch-target retargets for their moved-symbol coupling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-module-structure-boundaries/13-CONTEXT.md
@.planning/phases/13-module-structure-boundaries/13-RESEARCH.md
@.planning/phases/13-module-structure-boundaries/13-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve out alias_learning.py, clarification.py, delivery.py; trim orchestrator.py; promote BOUND-01 names in reconcile_names.py and validate.py</name>
  <files>app/pipeline/orchestrator.py, app/pipeline/alias_learning.py, app/pipeline/clarification.py, app/pipeline/delivery.py, app/pipeline/reconcile_names.py, app/pipeline/validate.py, eval/run_eval.py</files>
  <read_first>app/pipeline/orchestrator.py (full file — source of truth for every function body moved verbatim), app/pipeline/reconcile_names.py (full file, 202 lines — _norm and _safe_to_learn_alias definitions), app/pipeline/validate.py lines 1-50 (_HOURS_FIELDS and _is_paid definitions), eval/run_eval.py lines 30-45 (the _norm import to retarget)</read_first>
  <action>
Perform these edits in one coherent commit series (verbatim moves; renames are rename-in-place
with all references updated in the same commit per D-12).

**A. `reconcile_names.py` promotions:**
Rename `_norm` (line 39) → `normalize_name`. Update its two same-module call sites inside
`deterministic_match` (lines 64, 66, 70 — three total `_norm(...)` calls) to `normalize_name(...)`.
DELETE `_safe_to_learn_alias` (lines 167-202) entirely from this file — it relocates to
`alias_learning.py` per D-10 (not promoted-in-place; moved-and-renamed).

**B. `validate.py` promotions:**
Rename `_HOURS_FIELDS` (line 30) → `HOURS_FIELDS`. Rename `_is_paid` (line 39) → `is_paid`. Update
all 4 same-module call sites (grep confirms: lines ~143, ~147 ×2, ~230, ~259) to the new names.
Do NOT touch `app/pipeline/calculate.py`'s own separate `_HOURS_FIELDS` tuple — it is a distinct,
same-module-only symbol, not a BOUND-01 violation (RESEARCH Pitfall 5); any diff to `calculate.py`
in this task is out of scope.

**C. Create `app/pipeline/alias_learning.py`** — the single home for the learning rule set (D-10):
move `_normalize_candidate` (orchestrator.py lines 80-107) → `normalize_candidate`,
`_bind_evidence_for_token` (lines 108-157) → `bind_evidence_for_token`, `_write_aliases_if_safe`
(lines 1503-1590) → `write_aliases_if_safe`, verbatim except: (1) internal calls to
`_normalize_candidate`/`_safe_to_learn_alias` become same-module calls to `normalize_candidate`/
`safe_to_learn_alias` (no import needed, same file); (2) `_safe_to_learn_alias` (relocated from
reconcile_names.py per step A) → `safe_to_learn_alias`, body unchanged, now importing
`from app.pipeline.reconcile_names import deterministic_match` at module top (a NEW, legitimate
cross-module reference to an already-public name — not a BOUND-01 violation). Module imports:
`from __future__ import annotations`, `import logging`, `import uuid`, `from app.db import repo`,
`from app.pipeline.reconcile_names import deterministic_match, normalize_name`,
`logger = logging.getLogger("payroll_agent.orchestrator")` (keep the SAME logger name as
orchestrator.py — these log lines are part of the same operational stream and existing log-based
assertions, if any, must keep matching).

**D. Create `app/pipeline/clarification.py`** — the clarify cluster (D-09): move
`_defer_field_regression_clarification` (orchestrator.py lines 937-1025) → `defer_field_regression_clarification`,
`_render_asked_summary` (lines 1027-1052) → `render_asked_summary`, `_combined_context_email`
(lines 1055-1088) → `combined_context_email`, `_clarify` (lines 1229-1500) → `clarify`, verbatim
except: (1) `defer_field_regression_clarification`'s internal call to `_clarify` becomes
`clarify(...)` (same-module, no import needed); (2) `clarify`'s function-body import at line 1341
(`from app.pipeline.reconcile_names import _norm; norm_token = _norm(candidate_token)`) becomes a
MODULE-LEVEL import `from app.pipeline.reconcile_names import normalize_name` at the top of
`clarification.py`, with the two in-body call sites (lines 1342, 1344, 1349 — three total) updated
to `normalize_name(...)`. This is the exact function-body violation D-14/D-10 says "resolves by
relocation" — it stops being a function-body import entirely once it's a top-level import in the
new owning module. Module imports: `from __future__ import annotations`, `import logging`, `import
uuid`, `from app.db import repo`, `from app.email import gateway`, `from app.models.contracts
import InboundEmail` (used in type hints), `from app.models.status import RunStatus`, `from
app.pipeline.compose_email import clarification_subject, compose_clarification`, `from
app.pipeline.reconcile_names import normalize_name`, `from app.pipeline.suggest import
suggest_employees`, `MAX_CLARIFICATION_ROUNDS = 3` (the module constant moves here — it is used
ONLY inside `clarify`; verify no other staying-core function references it before moving it,
otherwise keep it in orchestrator.py and import it from there instead), `logger =
logging.getLogger("payroll_agent.orchestrator")` (same logger name, per module C's rationale).

**E. Create `app/pipeline/delivery.py`** — confirmation delivery (D-09): move `_deliver`
(orchestrator.py lines 1592-1798) → `deliver`, verbatim except its internal call to
`_write_aliases_if_safe` (lines 1655, 1778 — two call sites) becomes `alias_learning.write_aliases_if_safe(...)`
via a module-object import `from app.pipeline import alias_learning` at the top. Module imports:
`from __future__ import annotations`, `import contextlib`, `import logging`, `import re`, `import
uuid`, `from app.db import repo`, `from app.email import gateway`, `from app.models.status import
RunStatus`, `from app.pipeline import alias_learning`, `from app.pipeline.compose_email import
compose_confirmation, confirmation_subject`, `from app.pipeline.pdf import generate_paystub_pdf`,
`logger = logging.getLogger("payroll_agent.orchestrator")` (same logger name).

**F. Trim `app/pipeline/orchestrator.py`** to the core state machine: KEEP
`MAX_CLARIFICATION_ROUNDS` only if step D determined it must stay (see D's instruction); KEEP
`backfill_extracted`, `run_pipeline`, `_run`, `resume_pipeline`, `_run_stages`,
`_compute_line_items`, `_RunStagesResult`. DELETE the moved functions from this file. Add module-
object imports: `from app.pipeline import alias_learning, clarification, delivery` at the top.
Update EVERY call site of a moved function inside the staying core:
- `resume_pipeline` calls `_normalize_candidate`/`_bind_evidence_for_token` at ~lines 872/903 →
  `alias_learning.normalize_candidate(...)` / `alias_learning.bind_evidence_for_token(...)`.
- `resume_pipeline` calls `_defer_field_regression_clarification` at ~lines 543/838 →
  `clarification.defer_field_regression_clarification(...)`.
- `_run_stages` calls `_clarify` at ~line 1223 → `clarification.clarify(...)`.
Remove now-unneeded imports from the trimmed file's top-level import block: `from
app.pipeline.reconcile_names import _safe_to_learn_alias, reconcile_names` → `from
app.pipeline.reconcile_names import reconcile_names` (only `reconcile_names` is still used
directly in the staying core — verify via grep before finalizing); `from app.pipeline.validate
import _HOURS_FIELDS, _is_paid, detect_field_regression, validate` → `from app.pipeline.validate
import HOURS_FIELDS, is_paid, detect_field_regression, validate` (verify the staying core actually
uses `HOURS_FIELDS`/`is_paid` directly before keeping the import — if only the moved functions used
them, drop the import entirely from orchestrator.py). Remove `from app.pipeline.pdf import
generate_paystub_pdf`, `compose_confirmation`/`confirmation_subject` from `compose_email` imports,
`suggest_employees` import, and `gateway` import if the staying core no longer references them
directly (verify via grep — `_run`/`run_pipeline`/`resume_pipeline`/`_run_stages` may still need
`gateway` for something unrelated to `_deliver`/`_clarify`; check before removing).

**G. `eval/run_eval.py` retarget** (line 39): `from app.pipeline.reconcile_names import _norm as
_normalize` → `from app.pipeline.reconcile_names import normalize_name as _normalize` (the local
`as _normalize` alias of a now-public name is explicitly NOT a BOUND-01 violation per D-14 — keep
the alias, just retarget the source name).

Use `git mv` where the change is a pure rename (e.g. none of these are pure renames — all three new
pipeline files are net-new; use `Write` to create them and `git add`).
  </action>
  <acceptance_criteria>
    - `grep -rn "_norm\b" app/pipeline/ eval/` (excluding `normalize_name`/`normalize_candidate`/`_normalize` local alias) returns zero matches
    - `grep -rn "_safe_to_learn_alias\|_HOURS_FIELDS\|_is_paid\|_deliver\b\|_clarify\b\|_write_aliases_if_safe\|_normalize_candidate\|_bind_evidence_for_token\|_defer_field_regression_clarification\|_render_asked_summary\|_combined_context_email" app/pipeline/orchestrator.py` returns zero matches (all renamed/relocated out of the trimmed core)
    - `uv run python -c "from app.pipeline import alias_learning, clarification, delivery, orchestrator; alias_learning.write_aliases_if_safe; clarification.clarify; delivery.deliver; orchestrator.resume_pipeline"` succeeds with no AttributeError/ImportError
    - `wc -l app/pipeline/orchestrator.py` reports a line count between 750 and 950
    - `uv run ruff check app/pipeline eval/run_eval.py` reports zero violations
    - `grep -n "def resume_pipeline" -A 600 app/pipeline/orchestrator.py | grep -c "alias_learning\.\|clarification\."` is >= 3 (confirms resume_pipeline calls through module-object references, not bare names)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from app.pipeline import orchestrator, alias_learning, clarification, delivery; print('ok')"</automated>
  </verify>
  <done>orchestrator.py is trimmed to the core state machine (~800-900 lines) importing alias_learning/clarification/delivery as module objects; all three new modules exist with their promoted public names; reconcile_names.py and validate.py have their BOUND-01 renames applied with all same-module call sites updated; eval/run_eval.py imports the promoted normalize_name.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the orchestrator test-coupling census, then verify the full suite green</name>
  <files>tests/test_delivery.py, tests/test_alias_write.py, tests/test_clarify.py, tests/test_clarify_rounds.py, tests/test_retrigger_epoch.py, tests/test_combined_context.py, tests/test_hitl.py</files>
  <read_first>tests/test_delivery.py (the orch._deliver / orch.repo / orch.generate_paystub_pdf / orch.compose_confirmation direct-attribute coupling, ~lines 485-551), tests/test_alias_write.py (the orchestrator_mod.suggest_employees / orch_mod._run_stages monkeypatch seams, ~lines 465-485, 920-945), tests/test_clarify_rounds.py and tests/test_retrigger_epoch.py (the `from app.pipeline.orchestrator import _clarify` named import), tests/test_hitl.py (the `monkeypatch.setattr(orch, "_deliver", ...)` seam, ~lines 172-224)</read_first>
  <action>
Retarget every test-coupling point this split breaks. For each file below, apply ONLY the
import-path/patch-target change described — do not alter any assertion's expected value or logic.

1. **`tests/test_delivery.py`** (~lines 491-529, two tests: `test_deliver_attaches_roster_to_exception_after_roster_load`,
   `test_deliver_failure_before_roster_load_carries_no_roster`): `from app.pipeline import
   orchestrator as orch` → `from app.pipeline import delivery as orch` (keep the local alias `orch`
   so the rest of each test body — `orch.repo`, `orch.compose_confirmation`,
   `orch.generate_paystub_pdf`, `orch._deliver`→`orch.deliver` — needs only the module source
   changed, not every attribute access rewritten). Update `orch._deliver(run_id, run)` →
   `orch.deliver(run_id, run)` (2 call sites) since `_deliver` was renamed to `deliver` in Task 1.
   Scan the REST of `tests/test_delivery.py` (549 lines total) for any other `orch.` or
   `orchestrator.` reference and confirm each one now resolves against `delivery.py`'s own
   `repo`/`compose_confirmation`/`generate_paystub_pdf` module-level names — `delivery.py` has its
   own `from app.db import repo` binding (same underlying module object, patchable identically).

2. **`tests/test_alias_write.py`** (5 `import app.pipeline.orchestrator as orchestrator_mod`/`orch_mod`
   sites at ~lines 473, 928, 1087, 1228, 1376): determine per-site whether the patched attribute
   (`suggest_employees`, `_run_stages`, or others) now lives on `orchestrator` (if it's
   `_run_stages`, stays on `orchestrator`) or moved to `clarification`/`alias_learning` (if it's
   `suggest_employees`, which `clarify` uses — retarget to `import app.pipeline.clarification as
   clarification_mod` and patch there). Also retarget the top-level `from
   app.pipeline.orchestrator import _clarify` (~line 43) → `from app.pipeline.clarification import
   clarify as _clarify` (keep the local alias so the ~30+ call sites of `_clarify(...)` throughout
   this 1590-line file need no further changes) — verify this file's `_clarify(...)` call sites all
   still resolve correctly after the alias retarget. Also retarget every `_safe_to_learn_alias`
   reference (this file imports and directly unit-tests it, ~line 43 onward) → `from
   app.pipeline.alias_learning import safe_to_learn_alias as _safe_to_learn_alias` (same
   keep-the-local-alias technique, since this file has ~15+ call sites of `_safe_to_learn_alias(...)`
   as a bare name throughout its test bodies).

3. **`tests/test_clarify.py`** (~line 419-460, `test_orchestrator_suggest_called_after_decide`):
   this test currently reads `orchestrator.__file__`'s source and asserts `decide_pos < suggest_pos`
   within ONE file. Post-split, `decide(` (in `_run_stages`) stays in `orchestrator.py` but
   `suggest_employees(` (in `clarify`) moves to `clarification.py` — the single-file assumption no
   longer holds. Rewrite this test's MECHANISM (not its invariant) to prove the same ordering
   guarantee across the new module boundary: read `orchestrator.py`'s source and confirm
   `decision = decide(` is present and that the clarify branch calls `clarification.clarify(...)`
   (not `suggest_employees` directly) AFTER that point in `_run_stages`'s source; separately, read
   `clarification.py`'s source and confirm `suggest_employees(` appears there and that the call
   site is NOT inside a function that also calls `decide(` (i.e. `clarification.py` has no `decide(`
   call at all — grep-assert `"decide(" not in clarification_src`). This preserves D-21-05's real
   invariant ("suggest_employees is never passed into decide, and only runs after decide has
   returned on the clarify branch") using two source reads instead of one. Keep the existing
   `decide_call` / `"suggest" not in decide_call` assertion pointed at `orchestrator.py`'s source
   (that check is still single-file-valid since `decide(` never moved).

4. **`tests/test_clarify_rounds.py`** (~line 48, `from app.pipeline.orchestrator import _clarify`;
   ~lines 286/376, `import app.pipeline.orchestrator as orch_mod`): retarget the top import to
   `from app.pipeline.clarification import clarify as _clarify`. For the two `orch_mod` sites,
   inspect what attribute each patches — if it's `_clarify`/`suggest_employees`/`compose_clarification`,
   retarget to `import app.pipeline.clarification as orch_mod`; if it's `_run_stages`/`repo`
   used by the staying core, leave pointed at `app.pipeline.orchestrator`.

5. **`tests/test_retrigger_epoch.py`** (~line 40, `from app.pipeline.orchestrator import _clarify,
   resume_pipeline`): split into two imports — `from app.pipeline.clarification import clarify as
   _clarify` and `from app.pipeline.orchestrator import resume_pipeline` (resume_pipeline stays put).

6. **`tests/test_combined_context.py`** (~line 42, a multi-name `from app.pipeline.orchestrator
   import (...)` block; ~line 444, `import app.pipeline.orchestrator as orch_mod`): read the full
   import block to see which of the moved names (`_clarify`, `_combined_context_email`, etc.) it
   imports and split them into `from app.pipeline.clarification import clarify as _clarify,
   combined_context_email as _combined_context_email` (or whichever subset applies) while leaving
   any still-core names (`resume_pipeline`, `run_pipeline`, `backfill_extracted`) imported from
   `app.pipeline.orchestrator` unchanged. Retarget the `orch_mod` attribute-patch site the same way
   as item 4.

7. **`tests/test_hitl.py`** (~lines 172-224, `from app.pipeline import orchestrator as orch;
   monkeypatch.setattr(orch, "_deliver", _deliver_boom)`): retarget to `from app.pipeline import
   delivery as orch; monkeypatch.setattr(orch, "deliver", _deliver_boom)` (both the import target
   and the renamed attribute). Verify the `repo_mod`-side assertions in the same test (`monkeypatch.
   setattr(repo_mod, "record_run_error", _spy)`) are unaffected (repo.py is untouched by this plan).

For every retarget above: after editing, run that ONE test file in isolation
(`uv run pytest tests/<file> -q`) BEFORE moving to the next file, so a wiring bug in Task 1 is
caught close to its source rather than surfacing as a wall of unrelated-looking failures at the
end. If a test fails for a reason OTHER than the described coupling point, the bug is in Task 1's
module split (a missed call-site update, a wrong import) — fix the SOURCE module, never loosen an
assertion.

After all seven files pass in isolation, run the full suite.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_delivery.py tests/test_alias_write.py tests/test_clarify.py tests/test_clarify_rounds.py tests/test_retrigger_epoch.py tests/test_combined_context.py tests/test_hitl.py -q` passes with 0 failures, 0 errors
    - `uv run pytest -q` (full suite) passes with the same collected-test count as immediately before Task 1 of this plan began, zero failures
    - `grep -c "suggest_pos\|decide_pos" tests/test_clarify.py` still returns a nonzero count referencing `decide_pos` (the orchestrator-side assertion is preserved, not deleted) AND a new assertion exists confirming `"decide(" not in` the clarification module's source
    - `uv run ruff check tests/test_delivery.py tests/test_alias_write.py tests/test_clarify.py tests/test_clarify_rounds.py tests/test_retrigger_epoch.py tests/test_combined_context.py tests/test_hitl.py` reports zero violations
    - `git diff --stat tests/` shows no test file with a net line-count change larger than what a mechanical import/patch-target retarget would produce (a large diff signals an accidental assertion rewrite — inspect before finalizing)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>All seven coupling-point test files pass with only import-path/patch-target retargets (test_clarify.py's structural test is re-mechanized to check the two-file invariant, not weakened), and the full suite is green at the pre-split test count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Orchestrator core ↔ carved-out pipeline modules | `orchestrator.py`'s state machine now calls `alias_learning`/`clarification`/`delivery` as module objects across a new internal boundary; a missed call-site update here silently stops calling the intended function |
| Test monkeypatch seams ↔ moved functions | `monkeypatch.setattr(<module>, "<fn>", ...)` only intercepts calls that resolve through that exact module object at call time — a caller still importing the OLD module reference would silently bypass the patch and hit real (possibly live-LLM/live-gateway) code in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-05 | Tampering | Monkeypatch seam silently stops intercepting (module-object vs named-function import mismatch) | mitigate | D-11's module-object-import discipline is applied at every one of the 4 identified call edges (`resume_pipeline`→`alias_learning`, `resume_pipeline`→`clarification`, `_run_stages`→`clarification`, `delivery`→`alias_learning`); Task 2's per-file isolation-run-before-proceeding practice catches a silently-non-intercepting patch as a behavior mismatch (real DB/LLM call attempted) rather than a clean pass |
| T-13-06 | Tampering | PII-safe error logging dropped during the `_deliver`→`delivery.deliver` move (WR-04 roster-attachment-to-exception pattern) | mitigate | Task 1's action specifies verbatim move of the `except Exception as exc: exc.payroll_roster = roster; raise` block with no rewriting; Task 2's `tests/test_delivery.py` retarget re-runs both WR-04 regression tests (`test_deliver_attaches_roster_to_exception_after_roster_load`, `test_deliver_failure_before_roster_load_carries_no_roster`) unmodified in assertions against the new `delivery` module |
| T-13-07 | Information Disclosure | Alias-learning misname guard (`safe_to_learn_alias`) relocated but its collision logic altered in transit | mitigate | Task 1 specifies the relocated function's body moves byte-for-byte from `reconcile_names.py` into `alias_learning.py`, only gaining a new top-level `from app.pipeline.reconcile_names import deterministic_match` import — no logic change; Task 2 re-runs `tests/test_alias_write.py`'s full `_safe_to_learn_alias` unit-test suite (the misname-guard regression tests from STATE.md's "Alias-learning misname guard" prior finding) against the relocated `alias_learning.safe_to_learn_alias` |
| T-13-08 | Repudiation | Suggestion-vs-decision structural firewall (D-21-05: LLM suggestion never influences `decide()`) becomes unverifiable after the single-file structural test breaks | mitigate | Task 2 explicitly re-mechanizes (not deletes) `test_orchestrator_suggest_called_after_decide` to assert the SAME invariant across the new two-file boundary — `decide(` stays in `orchestrator.py`, and a new assertion confirms `clarification.py`'s source contains no `decide(` call at all, closing the gap a naive test deletion would have opened |
</threat_model>

<verification>
`uv run pytest -q` passes at the same collected-test count as immediately before this plan's Task 1
began, with zero assertion-value changes outside the enumerated retargets (test_clarify.py's
structural test mechanism changes but its asserted invariant does not). `uv run ruff check .` is
clean over `app/pipeline/` and every touched test file.
</verification>

<success_criteria>
- `app/pipeline/orchestrator.py` trimmed to ~800-900 lines (core state machine only)
- `app/pipeline/alias_learning.py`, `clarification.py`, `delivery.py` exist with the promoted
  public names from D-12/D-13, wired via module-object imports at every cross-module call edge
- `reconcile_names.py` and `validate.py` have their BOUND-01 renames applied in place
- Every one of the 9 pre-existing orchestrator monkeypatch seams across `tests/test_hitl.py`,
  `tests/test_needs_operator.py`, `tests/test_cr_regressions.py`, `tests/test_delivery.py`,
  `tests/test_orchestrator_states.py`, `tests/test_atomic_persist.py`, `tests/test_resume_pipeline.py`
  either resolves unchanged (functions that stayed in orchestrator.py) or retargets mechanically
  (functions that moved)
- Full suite green at the pre-split test count
</success_criteria>

<output>
Create `.planning/phases/13-module-structure-boundaries/13-02-SUMMARY.md` when done
</output>
</content>
