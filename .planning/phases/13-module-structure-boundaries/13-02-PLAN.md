---
phase: 13-module-structure-boundaries
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/pipeline/orchestrator.py
  - app/pipeline/alias_learning.py
  - app/pipeline/clarification.py
  - app/pipeline/delivery.py
  - app/pipeline/reconcile_names.py
  - app/pipeline/validate.py
  - app/main.py
  - eval/run_eval.py
  - tests/test_delivery.py
  - tests/test_alias_write.py
  - tests/test_clarify.py
  - tests/test_clarify_rounds.py
  - tests/test_retrigger_epoch.py
  - tests/test_combined_context.py
  - tests/test_hitl.py
  - tests/test_atomic_persist.py
  - tests/test_needs_operator.py
  - tests/test_demo_landing.py
  - tests/test_multi_employee_delivery.py
  - tests/test_alias_full_loop.py
  - tests/test_concurrency_proof.py
  - tests/test_cr_regressions.py
autonomous: true
requirements: [STRUCT-03, STRUCT-04, BOUND-01]
must_haves:
  truths:
    - "resume_pipeline and _run_stages (staying in orchestrator.py) call the moved alias/clarify/delivery helpers via module-object references (alias_learning.fn, clarification.fn, delivery.fn), never bare names"
    - "no function body anywhere in app/pipeline/ imports a `_private` name from a sibling module after this plan lands"
    - "D-13: drop-underscore is the default promotion; the genuinely cryptic `_norm` gets the real public name `normalize_name` -- rename-only, no semantic changes"
    - "clarification.py imports Extracted (used by Extracted.model_validate(...) inside the deferred field-regression handling) -- not omitted from its import list"
    - "app/main.py:772's approve() route is retargeted from `from app.pipeline.orchestrator import _deliver` to `from app.pipeline import delivery` in THIS plan's Task 1, not left broken for 13-03 to discover -- STRUCT-04's per-split green gate applies to every commit in this plan, including main.py's one integration point"
    - "every test file that imports or patches a moved symbol (including AST-structural tests targeting the correct module/function/node-shape, string-form monkeypatch targets, and MAX_CLARIFICATION_ROUNDS) is retargeted in this same plan -- the full census below (14 files) is exhaustive, verified against live source this session, not carried forward from a prior document"
    - "the full suite passes with zero assertion changes outside the enumerated import-path retargets in this plan, and with zero remaining reference anywhere in the repo (app/ or tests/) to app.pipeline.orchestrator._deliver"
  artifacts:
    - path: "app/pipeline/alias_learning.py"
      provides: "the single home for the alias-learning rule set: normalize_candidate, bind_evidence_for_token, write_aliases_if_safe, safe_to_learn_alias"
      contains: "def write_aliases_if_safe"
    - path: "app/pipeline/clarification.py"
      provides: "the clarify cluster: clarify, defer_field_regression_clarification, render_asked_summary, combined_context_email, MAX_CLARIFICATION_ROUNDS"
      contains: "def clarify"
    - path: "app/pipeline/delivery.py"
      provides: "confirmation delivery: deliver"
      contains: "def deliver"
  key_links:
    - from: "app/pipeline/orchestrator.py"
      to: "app/pipeline/alias_learning.py"
      via: "module-object import (resume_pipeline calls alias_learning.normalize_candidate / alias_learning.bind_evidence_for_token)"
      pattern: "from app\\.pipeline import alias_learning"
    - from: "app/pipeline/delivery.py"
      to: "app/pipeline/alias_learning.py"
      via: "module-object import (deliver calls alias_learning.write_aliases_if_safe)"
      pattern: "from app\\.pipeline import alias_learning"
    - from: "app/pipeline/alias_learning.py"
      to: "app/pipeline/reconcile_names.py"
      via: "module-level import of the promoted public normalize_name + deterministic_match"
      pattern: "from app\\.pipeline\\.reconcile_names import"
    - from: "app/pipeline/clarification.py"
      to: "app/models/contracts.py"
      via: "Extracted.model_validate(...) inside deferred field-regression handling requires a top-level Extracted import"
      pattern: "from app\\.models\\.contracts import.*Extracted"
    - from: "app/main.py (approve route)"
      to: "app/pipeline/delivery.py"
      via: "this plan's Task 1 retargets the ONE function-body import at app/main.py:772 to a top-level `from app.pipeline import delivery` -- closing the integration point in THIS plan, not deferring it to 13-03"
      pattern: "from app\\.pipeline import delivery"
---

<objective>
Split `app/pipeline/orchestrator.py` (1,843 lines) into the core state machine (staying,
~800-900 lines: `run_pipeline`, `_run`, `resume_pipeline`, `_run_stages`, `_compute_line_items`,
`backfill_extracted`) plus three carved-out modules — `app/pipeline/alias_learning.py`,
`app/pipeline/clarification.py`, `app/pipeline/delivery.py` — using module-object imports at
every boundary so existing `monkeypatch.setattr(<module>, "<fn>", ...)` test seams retarget
mechanically to their new owning module. In the same commit series, promote every BOUND-01
violation this split touches: `_norm`->`normalize_name` and `_safe_to_learn_alias`->
`safe_to_learn_alias` (relocated into `alias_learning.py`) in `reconcile_names.py`;
`_HOURS_FIELDS`->`HOURS_FIELDS` and `_is_paid`->`is_paid` in `validate.py`; `_deliver`->`deliver`,
`_clarify`->`clarify`, `_write_aliases_if_safe`->`write_aliases_if_safe`,
`_normalize_candidate`->`normalize_candidate`, `_bind_evidence_for_token`->`bind_evidence_for_token`,
`_defer_field_regression_clarification`->`defer_field_regression_clarification`,
`_render_asked_summary`->`render_asked_summary`, `_combined_context_email`->`combined_context_email`.

This is Round 3 of a cross-AI (Codex) review cycle. Round 2's confirming review found the
Round 1 fixes (missing `Extracted` import, most of the test census, `MAX_CLARIFICATION_ROUNDS`
placement, `_RunStagesResult` type boundary) all landed correctly, but flagged one decisive
design flaw and a still-incomplete census within already-named files. **Every claim below was
independently re-verified against live source in THIS planning session and cites file:line:**

1. **`app/main.py`'s broken intermediate state (Codex Round 2 HIGH, CONFIRMED this session,
   DECISIVELY FIXED here — no longer deferred to 13-03):** `app/main.py:772`'s `approve()` route
   does `from app.pipeline.orchestrator import _deliver` (verified this session: the import is a
   function-body statement immediately inside `approve()`, at the line directly following the
   docstring, confirmed by reading `app/main.py:769-796` in full). Once THIS plan's Task 1 deletes
   `_deliver` from `orchestrator.py` (renamed and relocated to `delivery.deliver`), that import
   breaks with an `ImportError` the next time `approve()` is called — including inside
   `tests/test_hitl.py`'s own acceptance-gate tests, which this plan's Task 2 is required to keep
   green. **The prior plan revision's design deliberately left this broken as a "documented,
   temporary, cross-plan gap" for 13-03 to close.** This violates STRUCT-04's per-split green gate
   (which applies within a single plan's own commits, not just across the whole phase) and is
   fixed here decisively: Task 1 retargets this ONE `main.py` import in the SAME commit series
   that removes `_deliver` from `orchestrator.py`, exactly as Round 2 recommended. This makes
   `app/main.py` a files_modified entry for this plan (a minimal, single-import change — NOT the
   full `app/main.py` route split, which stays 13-03's job per D-05/D-06/D-07/D-08).
2. **Census gaps within already-named files, closed (Codex Round 2 HIGH, each independently
   re-verified this session with exact line numbers from a fresh read):**
   - `tests/test_atomic_persist.py` has FIVE `from app.pipeline.orchestrator import _deliver`
     imports (confirmed this session at lines 753, 801, 846, 882, 913 — each inside a separate
     test function), not the "two AST tests" the prior revision's task text focused on. It also
     has TWO `_write_aliases_if_safe` patches (`monkeypatch.setattr(orch_mod,
     "_write_aliases_if_safe", ...)` at lines 766 and 940, confirmed this session) and ONE
     `_clarify` patch (`monkeypatch.setattr(orch_mod, "_clarify", _boom_clarify)` at line 530,
     confirmed this session, inside `test_defer_field_regression_write_survives_later_clarify_failure`).
     ALL of these are now explicitly enumerated in this plan's Task 2, not just the two AST
     structural tests.
   - `tests/test_alias_write.py` directly imports `_normalize_candidate` THREE times (confirmed
     this session at lines 1441, 1451, 1460, inside the "Group 5" legacy-shape-tolerance tests) and
     `_write_aliases_if_safe` once (confirmed at line 1471, inside
     `test_write_aliases_if_safe_handles_legacy_flat_shape_without_raising`) — neither was assigned
     a retarget in the prior revision. Now explicit in this plan's Task 2.
   - `tests/test_cr_regressions.py` has a `from app.pipeline.orchestrator import _deliver` import
     at line 315 (confirmed this session, inside `test_cr03_deliver_enriches_run_dict_with_business_name`)
     plus the pre-existing `_write_aliases_if_safe` patch (confirmed at line 375, inside a
     different test using `import app.pipeline.orchestrator as orch`). Both now explicit.
   - `tests/test_clarify_rounds.py` AST-parses `orch_mod.__file__`'s own source looking for
     `ast.FunctionDef` nodes named `_clarify` (confirmed this session at lines 296 and 386, inside
     `test_clarify_finalize_paths_advance_round_before_status_ast` and
     `test_clarify_cap_check_precedes_any_transaction_block`) — this structural target moves
     entirely to `clarification.py`'s `clarify` function post-split; both AST-parse sites are
     retargeted in this plan's Task 2.
3. **`tests/test_atomic_persist.py`'s OWN AST-structural coupling, re-verified this session (this
   part of the prior revision's fix was CORRECT and is preserved unchanged):** two separate
   structural tests read `orch_mod.__file__`'s source: (a)
   `test_run_stages_process_branch_call_order_and_status_last` (~line 232) AST-parses `_run_stages`
   (which STAYS in `orchestrator.py`) looking for a bare `ast.Name` call node with `.id ==
   "_clarify"` — post-split this becomes a module-qualified `ast.Attribute` call
   (`clarification.clarify(...)`), so the node-matching logic changes from `ast.Name` to
   `ast.Attribute` with `.attr == "clarify"`; (b)
   `test_defer_field_regression_clarification_txn_closes_before_clarify_call` (~line 417) AST-parses
   `orch_mod.__file__` looking for `ast.FunctionDef` named `_defer_field_regression_clarification`
   — this function LEAVES `orchestrator.py` entirely, so this test retargets to parse
   `clarification.py`'s source instead, looking for `FunctionDef` named
   `defer_field_regression_clarification` (renamed), with its internal `_clarify` call-shape check
   ALSO changed from `ast.Name` to a same-module bare `ast.Name` call to `clarify` (since
   `defer_field_regression_clarification` and `clarify` are now co-located in `clarification.py`,
   this ONE stays a bare-name `ast.Name` check, unlike the `_run_stages` case above which crosses a
   module boundary).

Purpose: STRUCT-03 (alias-learning helpers carved out into their own module) + the majority of
BOUND-01 (cross-module `_private` imports promoted to public names) + STRUCT-04 (behavior-neutral,
suite green throughout, including within this plan's own commits — no deferred cross-plan gap).
Output: `app/pipeline/orchestrator.py` trimmed to the core state machine; three new pipeline
modules; `reconcile_names.py`/`validate.py` promotion edits; `app/main.py`'s ONE `_deliver` import
retargeted; `eval/run_eval.py` import retarget; fourteen test files get mechanical
import-path/patch-target retargets for their moved-symbol coupling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-module-structure-boundaries/13-CONTEXT.md
@.planning/phases/13-module-structure-boundaries/13-RESEARCH.md
@.planning/phases/13-module-structure-boundaries/13-PATTERNS.md
@.planning/phases/13-module-structure-boundaries/13-REVIEWS.md
@.planning/phases/13-module-structure-boundaries/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Carve out alias_learning.py, clarification.py, delivery.py; trim orchestrator.py; promote BOUND-01 names in reconcile_names.py and validate.py; retarget app/main.py's ONE _deliver import in the same commit series</name>
  <files>app/pipeline/orchestrator.py, app/pipeline/alias_learning.py, app/pipeline/clarification.py, app/pipeline/delivery.py, app/pipeline/reconcile_names.py, app/pipeline/validate.py, app/main.py, eval/run_eval.py</files>
  <read_first>app/pipeline/orchestrator.py (full file -- source of truth for every function body moved verbatim; grep it yourself for the exact Extracted.model_validate call site and every _normalize_candidate/_bind_evidence_for_token/_defer_field_regression_clarification/_clarify/_write_aliases_if_safe/_safe_to_learn_alias call site before finalizing imports), app/pipeline/reconcile_names.py (full file, 202 lines -- _norm and _safe_to_learn_alias definitions), app/pipeline/validate.py lines 1-50 (_HOURS_FIELDS and _is_paid definitions), app/main.py lines 769-796 (approve() -- the exact function-body `_deliver` import and call site this task retargets), eval/run_eval.py lines 30-45 (the _norm import to retarget)</read_first>
  <action>
Perform these edits in one coherent commit series (verbatim moves; renames are rename-in-place
with all references updated in the same commit per D-12).

**A. `reconcile_names.py` promotions:**
Rename `_norm` (line 39) -> `normalize_name`. Update its same-module call sites inside
`deterministic_match` to `normalize_name(...)` (grep to find every one -- do not assume a fixed
count). DELETE `_safe_to_learn_alias` entirely from this file -- it relocates to
`alias_learning.py` per D-10 (not promoted-in-place; moved-and-renamed).

**B. `validate.py` promotions:**
Rename `_HOURS_FIELDS` (line 30) -> `HOURS_FIELDS`. Rename `_is_paid` (line 39) -> `is_paid`.
Grep this file for every same-module call site and update each to the new name. Do NOT touch
`app/pipeline/calculate.py`'s own separate `_HOURS_FIELDS` tuple -- it is a distinct, same-module-
only symbol, not a BOUND-01 violation (RESEARCH Pitfall 5); any diff to `calculate.py` in this task
is out of scope.

**C. Create `app/pipeline/alias_learning.py`** -- the single home for the learning rule set (D-10):
move `_normalize_candidate` (orchestrator.py ~lines 80-107) -> `normalize_candidate`,
`_bind_evidence_for_token` (~108-157) -> `bind_evidence_for_token`, `_write_aliases_if_safe`
(~1503-1590) -> `write_aliases_if_safe`, verbatim except: (1) internal calls to
`_normalize_candidate`/`_safe_to_learn_alias` become same-module calls to `normalize_candidate`/
`safe_to_learn_alias` (no import needed, same file); (2) `_safe_to_learn_alias` (relocated from
reconcile_names.py per step A) -> `safe_to_learn_alias`, body unchanged, now importing `from
app.pipeline.reconcile_names import deterministic_match` at module top (a NEW, legitimate
cross-module reference to an already-public name -- not a BOUND-01 violation). Module imports:
`from __future__ import annotations`, `import logging`, `import uuid`, `from app.db import repo`,
`from app.pipeline.reconcile_names import deterministic_match, normalize_name`, `logger =
logging.getLogger("payroll_agent.orchestrator")` (keep the SAME logger name as orchestrator.py --
these log lines are part of the same operational stream).

**D. Create `app/pipeline/clarification.py`** -- the clarify cluster (D-09): move
`_defer_field_regression_clarification` (orchestrator.py ~lines 937-1025) ->
`defer_field_regression_clarification`, `_render_asked_summary` (~1027-1052) ->
`render_asked_summary`, `_combined_context_email` (~1055-1088) -> `combined_context_email`,
`_clarify` (~1229-1500) -> `clarify`, verbatim except: (1) `defer_field_regression_clarification`'s
internal call to `_clarify` becomes `clarify(...)` (same-module, no import needed); (2) `clarify`'s
function-body import of `_norm` (grep to find the exact current line -- it was at ~1341/1342 in
RESEARCH.md's snapshot but re-verify) becomes a MODULE-LEVEL import `from
app.pipeline.reconcile_names import normalize_name` at the top of `clarification.py`, with every
in-body call site updated to `normalize_name(...)`. This is the exact function-body violation
D-14/D-10 says "resolves by relocation" -- it stops being a function-body import entirely once it's
a top-level import in the new owning module.
**`Extracted` import (Codex-confirmed, verified again this session):** `_defer_field_regression_clarification`'s
body calls `Extracted.model_validate(run_row["extracted_data"])` -- grep `orchestrator.py` for
`Extracted.model_validate` to find the exact line and confirm this is inside the function being
moved. `clarification.py`'s import list MUST include `Extracted` from `app.models.contracts`
alongside `InboundEmail` -- omitting it raises `NameError` the first time this code path executes.
Module imports: `from __future__ import annotations`, `import logging`, `import uuid`, `from
app.db import repo`, `from app.email import gateway`, `from app.models.contracts import Extracted,
InboundEmail` (BOTH names -- verify via grep that no other `app.models.contracts` symbol is used in
the moved functions and add it if so), `from app.models.status import RunStatus`, `from
app.pipeline.compose_email import clarification_subject, compose_clarification`, `from
app.pipeline.reconcile_names import normalize_name`, `from app.pipeline.suggest import
suggest_employees`, `MAX_CLARIFICATION_ROUNDS = 3` (verify via grep that no staying-core function
in `orchestrator.py` references this constant before moving it -- if the grep proves it's used
ONLY inside `clarify`, move it here; RESEARCH/CONTEXT already assume this holds), `logger =
logging.getLogger("payroll_agent.orchestrator")` (same logger name).
**`_RunStagesResult` type boundary:** `_RunStagesResult` stays defined in `orchestrator.py` (it is
the core state machine's own return type). Any function moving to `clarification.py` that type-hints
a `stage: _RunStagesResult` parameter (e.g. `_defer_field_regression_clarification`) uses a
`TYPE_CHECKING`-guarded import: `from typing import TYPE_CHECKING` then `if TYPE_CHECKING: from
app.pipeline.orchestrator import _RunStagesResult`, quoting the annotation as a string
(`stage: "_RunStagesResult"`) if needed to avoid a runtime NameError. This avoids a runtime circular
import (orchestrator imports clarification at module level; clarification must never import
orchestrator at module level) while keeping the type hint accurate for Phase 14's mypy pass.

**E. Create `app/pipeline/delivery.py`** -- confirmation delivery (D-09): move `_deliver`
(orchestrator.py ~lines 1592-1798) -> `deliver`, verbatim except its internal call(s) to
`_write_aliases_if_safe` (grep to find every call site -- verify count) become
`alias_learning.write_aliases_if_safe(...)` via a module-object import `from app.pipeline import
alias_learning` at the top. Module imports: `from __future__ import annotations`, `import
contextlib`, `import logging`, `import re`, `import uuid`, `from app.db import repo`, `from
app.email import gateway`, `from app.models.status import RunStatus`, `from app.pipeline import
alias_learning`, `from app.pipeline.compose_email import compose_confirmation,
confirmation_subject`, `from app.pipeline.pdf import generate_paystub_pdf`, `logger =
logging.getLogger("payroll_agent.orchestrator")` (same logger name).

**F. Trim `app/pipeline/orchestrator.py`** to the core state machine: KEEP `backfill_extracted`,
`run_pipeline`, `_run`, `resume_pipeline`, `_run_stages`, `_compute_line_items`,
`_RunStagesResult`. DELETE the moved functions and `MAX_CLARIFICATION_ROUNDS` (it moved to
`clarification.py` per step D) from this file. Add module-object imports: `from app.pipeline import
alias_learning, clarification, delivery` at the top. Update EVERY call site of a moved function
inside the staying core (grep `orchestrator.py` yourself for the exact current line numbers of each
-- do not trust a stale snapshot):
- `resume_pipeline` calls `_normalize_candidate`/`_bind_evidence_for_token` ->
  `alias_learning.normalize_candidate(...)` / `alias_learning.bind_evidence_for_token(...)`.
- `resume_pipeline` calls `_defer_field_regression_clarification` ->
  `clarification.defer_field_regression_clarification(...)`.
- `_run_stages` calls `_clarify` -> `clarification.clarify(...)` (this is the call site
  `tests/test_atomic_persist.py::test_run_stages_process_branch_call_order_and_status_last`'s
  AST-shape check retargets against in Task 2 -- confirm the call becomes an `ast.Attribute` node,
  not a bare `ast.Name`, as part of finalizing this edit).
Remove now-unneeded imports from the trimmed file's top-level import block: `from
app.pipeline.reconcile_names import _safe_to_learn_alias, reconcile_names` -> `from
app.pipeline.reconcile_names import reconcile_names` (only `reconcile_names` is still used directly
in the staying core -- verify via grep before finalizing); `from app.pipeline.validate import
_HOURS_FIELDS, _is_paid, detect_field_regression, validate` -> `from app.pipeline.validate import
HOURS_FIELDS, is_paid, detect_field_regression, validate` (verify the staying core actually uses
`HOURS_FIELDS`/`is_paid` directly before keeping the import -- if only the moved functions used
them, drop the import entirely from orchestrator.py). Remove `from app.pipeline.pdf import
generate_paystub_pdf`, `compose_confirmation`/`confirmation_subject` from `compose_email` imports,
`suggest_employees` import, and `gateway` import if the staying core no longer references them
directly (verify via grep -- `_run`/`run_pipeline`/`resume_pipeline`/`_run_stages` may still need
`gateway` for something unrelated to `_deliver`/`_clarify`; check before removing).

**G. `app/main.py`'s ONE integration point (Codex Round 2 HIGH -- fixed in THIS plan, not
deferred):** `app/main.py:772`'s `approve()` route does `from app.pipeline.orchestrator import
_deliver` as a function-body import, immediately followed (inside the `if claimed:` block) by a
call `_deliver(run_id, run)`. In the SAME commit series that deletes `_deliver` from
`orchestrator.py` (step F above), retarget this ONE site: replace the function-body import with a
top-level `from app.pipeline import delivery` added to `app/main.py`'s existing top-of-file import
block, and change the call site to `delivery.deliver(run_id, run)`. This is the ONLY change to
`app/main.py` in this plan -- the full router/main.py split (extracting `approve()` itself into
`app/routes/runs.py`) remains 13-03's job entirely; this plan touches `app/main.py` ONLY to keep
this one already-existing route functional across this plan's own split, satisfying STRUCT-04's
per-split green gate within this plan's own commits (not deferred as a "documented gap" for a
later plan to close).

**H. `eval/run_eval.py` retarget** (line ~39): `from app.pipeline.reconcile_names import _norm as
_normalize` -> `from app.pipeline.reconcile_names import normalize_name as _normalize` (the local
`as _normalize` alias of a now-public name is explicitly NOT a BOUND-01 violation per D-14 -- keep
the alias, just retarget the source name).

Use `Write` to create the three new pipeline files (net-new, not renames) and `git add` them.
  </action>
  <acceptance_criteria>
    - `grep -rn "_norm\b" app/pipeline/ eval/` (excluding `normalize_name`/`normalize_candidate`/`_normalize` local alias) returns zero matches
    - `grep -rn "_safe_to_learn_alias\|_HOURS_FIELDS\|_is_paid\b\|_deliver\b\|_clarify\b\|_write_aliases_if_safe\|_normalize_candidate\|_bind_evidence_for_token\|_defer_field_regression_clarification\|_render_asked_summary\|_combined_context_email\|MAX_CLARIFICATION_ROUNDS" app/pipeline/orchestrator.py` returns zero matches (all renamed/relocated out of the trimmed core)
    - `grep -n "orchestrator.*_deliver\|from app.pipeline.orchestrator import _deliver" app/main.py` returns zero matches (the function-body import is gone)
    - `grep -n "from app.pipeline import delivery" app/main.py` returns at least one match (top-level, module-scope import)
    - `grep -n "delivery.deliver(run_id, run)" app/main.py` returns at least one match (the approve() call site is retargeted)
    - `uv run python -c "from app.pipeline import alias_learning, clarification, delivery, orchestrator; alias_learning.write_aliases_if_safe; clarification.clarify; clarification.MAX_CLARIFICATION_ROUNDS; delivery.deliver; orchestrator.resume_pipeline"` succeeds with no AttributeError/ImportError
    - `uv run python -c "from app.main import app; from fastapi.testclient import TestClient; c = TestClient(app); assert c.get('/health/live').status_code == 200"` succeeds (confirms app/main.py still imports cleanly after the one-line retarget, even though the full router split is not this plan's job)
    - `grep -n "class Extracted\|^Extracted\b\|Extracted\." app/pipeline/clarification.py | grep -c "Extracted"` is >= 1 AND `grep -n "^from app.models.contracts import" app/pipeline/clarification.py | grep -c "Extracted"` is 1 (Extracted is both used and imported)
    - `wc -l app/pipeline/orchestrator.py` reports a line count between 750 and 950
    - `uv run ruff check app/pipeline app/main.py eval/run_eval.py` reports zero violations
    - `grep -n "def resume_pipeline" -A 600 app/pipeline/orchestrator.py | grep -c "alias_learning\.\|clarification\."` is >= 3 (confirms resume_pipeline calls through module-object references, not bare names)
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from app.pipeline import orchestrator, alias_learning, clarification, delivery; from app.main import app; print('ok')"</automated>
  </verify>
  <done>orchestrator.py is trimmed to the core state machine (~800-900 lines) importing alias_learning/clarification/delivery as module objects; all three new modules exist with their promoted public names; clarification.py imports Extracted alongside InboundEmail; reconcile_names.py and validate.py have their BOUND-01 renames applied with all same-module call sites updated; app/main.py's approve() route calls delivery.deliver via a top-level import, not a function-body import of the old orchestrator._deliver symbol; eval/run_eval.py imports the promoted normalize_name; app/main.py still imports and serves /health/live successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the FULL orchestrator test-coupling census (14 files, including every _deliver/_write_aliases_if_safe/_clarify site found this session, AST-structural tests, string-form patches, and MAX_CLARIFICATION_ROUNDS), then verify the full suite green with zero remaining orchestrator._deliver references anywhere</name>
  <files>tests/test_delivery.py, tests/test_alias_write.py, tests/test_clarify.py, tests/test_clarify_rounds.py, tests/test_retrigger_epoch.py, tests/test_combined_context.py, tests/test_hitl.py, tests/test_atomic_persist.py, tests/test_needs_operator.py, tests/test_demo_landing.py, tests/test_multi_employee_delivery.py, tests/test_alias_full_loop.py, tests/test_concurrency_proof.py, tests/test_cr_regressions.py</files>
  <read_first>tests/test_atomic_persist.py (the FIVE `_deliver` imports at lines 753/801/846/882/913, the TWO `_write_aliases_if_safe` patches at lines 766/940, the ONE `_clarify` patch at line 530, AND the two AST-structural tests: test_run_stages_process_branch_call_order_and_status_last ~line 232 and test_defer_field_regression_clarification_txn_closes_before_clarify_call ~line 417), tests/test_needs_operator.py line 49 (from app.pipeline.orchestrator import MAX_CLARIFICATION_ROUNDS, _clarify), tests/test_demo_landing.py (orchestrator._clarify/_deliver direct calls plus a string-form monkeypatch on suggest_employees -- grep for the exact current line numbers), tests/test_multi_employee_delivery.py (two _deliver imports, two string-form gateway.send_outbound patches -- grep for exact lines), tests/test_alias_full_loop.py (two _deliver imports -- resume_pipeline/run_pipeline imports are UNAFFECTED), tests/test_concurrency_proof.py (string-form monkeypatch on app.pipeline.orchestrator._deliver, with an explanatory comment -- read it), tests/test_cr_regressions.py line 315 (from app.pipeline.orchestrator import _deliver, inside test_cr03_deliver_enriches_run_dict_with_business_name) plus line 375's orch/_write_aliases_if_safe patch, tests/test_delivery.py (the orch._deliver / orch.repo / orch.generate_paystub_pdf / orch.compose_confirmation direct-attribute coupling), tests/test_alias_write.py (the orchestrator_mod.suggest_employees patch at ~line 483, orch_mod._run_stages patches at multiple sites, AND the direct `_normalize_candidate` imports ×3 at lines 1441/1451/1460 plus `_write_aliases_if_safe` at line 1471 -- AFTER 13-01's own edits to this file have already landed), tests/test_clarify_rounds.py (the FunctionDef("_clarify") AST-parse sites at lines 296 and 386, plus the top-level `from app.pipeline.orchestrator import _clarify` import), tests/test_hitl.py (the monkeypatch.setattr(orch, "_deliver", ...) seam)</read_first>
  <action>
Retarget every test-coupling point this split breaks. For each file below, apply ONLY the
import-path/patch-target change described -- do not alter any assertion's expected value or logic.

1. **`tests/test_delivery.py`** (two tests: `test_deliver_attaches_roster_to_exception_after_roster_load`,
   `test_deliver_failure_before_roster_load_carries_no_roster`): `from app.pipeline import
   orchestrator as orch` -> `from app.pipeline import delivery as orch` (keep the local alias
   `orch`). Update `orch._deliver(run_id, run)` -> `orch.deliver(run_id, run)`. Scan the REST of
   this file for any other `orch.`/`orchestrator.` reference and confirm each resolves against
   `delivery.py`'s own `repo`/`compose_confirmation`/`generate_paystub_pdf` module-level names.

2. **`tests/test_alias_write.py`** (apply on top of whatever state 13-01 already left this file
   in): the top-level `from app.pipeline.orchestrator import _clarify` becomes `from
   app.pipeline.clarification import clarify as _clarify` (keep the local alias so every existing
   `_clarify(...)` call site in this file needs no further change). Every `_safe_to_learn_alias`
   reference (this file unit-tests it directly) becomes `from app.pipeline.alias_learning import
   safe_to_learn_alias as _safe_to_learn_alias` (same keep-the-local-alias technique). The THREE
   direct `from app.pipeline.orchestrator import _normalize_candidate` imports (confirmed this
   session at lines 1441, 1451, 1460) become `from app.pipeline.alias_learning import
   normalize_candidate as _normalize_candidate` (keep the local alias, three sites, same retarget
   each time). The `from app.pipeline.orchestrator import _write_aliases_if_safe` import
   (confirmed this session at line 1471) becomes `from app.pipeline.alias_learning import
   write_aliases_if_safe as _write_aliases_if_safe`. For each `import app.pipeline.orchestrator as
   orchestrator_mod`/`orch_mod` site (e.g. the `suggest_employees` patch at ~line 483), determine
   per-site whether the patched attribute (`suggest_employees` -> now on `clarification`;
   `_run_stages` -> stays on `orchestrator`) belongs to the module that actually owns it
   post-split, and retarget only the sites that moved.

3. **`tests/test_clarify.py`** (`test_orchestrator_suggest_called_after_decide`): this test reads
   `orchestrator.__file__`'s source and asserts `decide_pos < suggest_pos` within ONE file.
   Post-split, `decide(` (in `_run_stages`) stays in `orchestrator.py` but `suggest_employees(` (in
   `clarify`) moves to `clarification.py` -- the single-file assumption no longer holds. Rewrite
   this test's MECHANISM (not its invariant): read `orchestrator.py`'s source and confirm
   `decision = decide(` is present and that the clarify branch calls `clarification.clarify(...)`
   (not `suggest_employees` directly) after that point in `_run_stages`'s source; separately, read
   `clarification.py`'s source and confirm `suggest_employees(` appears there AND that
   `clarification.py` has NO `decide(` call anywhere (grep-assert `"decide(" not in
   clarification_src`). This preserves D-21-05's real invariant ("suggest_employees never feeds
   into decide, and only runs after decide has returned on the clarify branch") across the new
   module boundary. Keep the existing `decide_call`/`"suggest" not in decide_call` assertion
   pointed at `orchestrator.py`'s source (still single-file-valid since `decide(` never moved).

4. **`tests/test_clarify_rounds.py`** (`from app.pipeline.orchestrator import _clarify`; TWO
   `ast.FunctionDef` name-== "_clarify" parse sites at lines 296 and 386, confirmed this session
   inside `test_clarify_finalize_paths_advance_round_before_status_ast` and
   `test_clarify_cap_check_precedes_any_transaction_block`): retarget the top import to `from
   app.pipeline.clarification import clarify as _clarify`. For BOTH AST-parse sites, change the
   module being parsed from `orchestrator.__file__`/`orch_mod.__file__` to
   `clarification.__file__` (`import app.pipeline.clarification as clarification_mod` or
   equivalent), and change the `node.name == "_clarify"` comparison to `node.name == "clarify"`
   (the FunctionDef name itself changes with the rename) -- keep every subsequent assertion about
   `with conn.transaction():` block counts and call-order UNCHANGED, only the source module and the
   target function name change.

5. **`tests/test_retrigger_epoch.py`** (`from app.pipeline.orchestrator import _clarify,
   resume_pipeline`): split into `from app.pipeline.clarification import clarify as _clarify` and
   `from app.pipeline.orchestrator import resume_pipeline` (resume_pipeline stays put).

6. **`tests/test_combined_context.py`** (a multi-name `from app.pipeline.orchestrator import (...)`
   block; an `import app.pipeline.orchestrator as orch_mod` site): read the full import block,
   split moved names (`_clarify`, `_combined_context_email`, etc.) into `from
   app.pipeline.clarification import clarify as _clarify, combined_context_email as
   _combined_context_email` (or whichever subset applies) while leaving still-core names
   (`resume_pipeline`, `run_pipeline`, `backfill_extracted`) imported from `app.pipeline.orchestrator`
   unchanged. Retarget the `orch_mod` attribute-patch site the same way as item 4.

7. **`tests/test_hitl.py`** (`from app.pipeline import orchestrator as orch;
   monkeypatch.setattr(orch, "_deliver", _deliver_boom)`, two sites): retarget to `from
   app.pipeline import delivery as orch; monkeypatch.setattr(orch, "deliver", _deliver_boom)` (both
   the import target and the renamed attribute). The `repo_mod`-side assertions
   (`monkeypatch.setattr(repo_mod, "record_run_error", _spy)`) are unaffected by THIS plan (repo.py
   was split in 13-01, already landed) -- verify they still pass, do not modify them here. This
   file's `approve()`-exercising tests must now pass CLEANLY given Task 1's `app/main.py` fix (no
   longer a documented cross-plan gap) -- confirm this explicitly rather than assuming it.

8. **`tests/test_atomic_persist.py`** (Codex-flagged, exhaustively re-verified this session):
   - The FIVE `from app.pipeline.orchestrator import _deliver` imports (lines 753, 801, 846, 882,
     913, each inside a separate test function body) each retarget to `from app.pipeline.delivery
     import deliver as _deliver` (keep the local alias so every subsequent `_deliver(...)` call in
     each test body needs no further edit).
   - The TWO `monkeypatch.setattr(orch_mod, "_write_aliases_if_safe", ...)` sites (line 766's
     `_boom_alias` and line 940's `_spy_write`) retarget: change `import app.pipeline.orchestrator
     as orch_mod` (at whichever point precedes each site) to ALSO import `from app.pipeline import
     alias_learning`, and change the patch target to `monkeypatch.setattr(alias_learning,
     "write_aliases_if_safe", ...)`. Line 934's `original_write = orch_mod._write_aliases_if_safe`
     (if present alongside the line-940 site) becomes `original_write =
     alias_learning.write_aliases_if_safe`.
   - The ONE `monkeypatch.setattr(orch_mod, "_clarify", _boom_clarify)` site (line 530, inside
     `test_defer_field_regression_write_survives_later_clarify_failure`) retargets: `import
     app.pipeline.orchestrator as orch_mod` stays (this test also needs `resume_pipeline` from
     orchestrator, verify), but ADD `from app.pipeline import clarification` and change the patch
     to `monkeypatch.setattr(clarification, "clarify", _boom_clarify)`.
   - `test_run_stages_process_branch_call_order_and_status_last` (~line 232): parses
     `orch_mod.__file__` (STAYS `orchestrator.py` -- `_run_stages` never moves) looking for a bare
     `ast.Name` call node with `.id == "_clarify"`. Post-split, `_run_stages` calls
     `clarification.clarify(...)` -- a module-qualified `ast.Attribute` call. Change the AST-walk's
     node-matching logic: instead of `isinstance(f, ast.Name) and f.id == "_clarify"`, match
     `isinstance(f, ast.Attribute) and f.attr == "clarify"` (optionally also confirming `f.value`
     is a `Name` with `.id == "clarification"` for precision). Preserve the exact invariant being
     tested (call-site is a sibling statement, never nested in `with conn.transaction():`) -- only
     the AST node type and matching logic change.
   - `test_defer_field_regression_clarification_txn_closes_before_clarify_call` (~line 417): parses
     `orch_mod.__file__` looking for `ast.FunctionDef` named `_defer_field_regression_clarification`
     -- this function LEAVES `orchestrator.py` entirely. Retarget to parse
     `clarification.__file__` (`import app.pipeline.clarification as clarification_mod`) looking
     for `FunctionDef` named `defer_field_regression_clarification` (renamed). Its internal
     `_clarify` call-shape check (currently `isinstance(f, ast.Name) and f.id == "_clarify"`) STAYS
     an `ast.Name` check but the target name changes to `"clarify"` (renamed) -- this ONE stays a
     bare-name check because `defer_field_regression_clarification` and `clarify` are BOTH now
     co-located in `clarification.py` (same-module call, unlike the `_run_stages` case above which
     crosses the `orchestrator.py` -> `clarification.py` boundary). Preserve every assertion about
     `with` block ordering, `set_clarified_fields` call count, and source-order unchanged.

9. **`tests/test_needs_operator.py`** (Codex-flagged omission -- `from app.pipeline.orchestrator
   import MAX_CLARIFICATION_ROUNDS, _clarify` at line 49): split into `from
   app.pipeline.clarification import MAX_CLARIFICATION_ROUNDS, clarify as _clarify` (both names
   moved to `clarification.py` together in Task 1 -- keep the `_clarify` local alias so the rest of
   this file's `_clarify(...)` call sites need no further edits).

10. **`tests/test_demo_landing.py`** (Codex-flagged omission -- direct `orchestrator._clarify(...)`
    and `orchestrator._deliver(...)` calls, plus a string-form monkeypatch): the `from
    app.pipeline import orchestrator` import (at the sites that call `._clarify`/`._deliver`)
    retargets per call: sites calling `orchestrator._clarify(...)` need `from app.pipeline import
    clarification` and `clarification.clarify(...)`; the site calling `orchestrator._deliver(...)`
    needs `from app.pipeline import delivery` and `delivery.deliver(...)`. The string-form
    `monkeypatch.setattr("app.pipeline.orchestrator.suggest_employees", lambda *a, **kw: {})`
    becomes `monkeypatch.setattr("app.pipeline.clarification.suggest_employees", lambda *a, **kw:
    {})` (suggest_employees is imported into clarification.py per Task 1). Also retarget the
    `monkeypatch.setattr("app.pipeline.orchestrator.compose_clarification", ...)` and
    `"app.pipeline.orchestrator.compose_confirmation"`/`"app.pipeline.orchestrator.generate_paystub_pdf"`/
    `"app.pipeline.orchestrator._write_aliases_if_safe"` string-form sites the same way: each string
    literal's MODULE PREFIX changes to whichever new module now owns that name
    (`compose_clarification`/`suggest_employees` -> `app.pipeline.clarification`;
    `compose_confirmation`/`generate_paystub_pdf` -> `app.pipeline.delivery`;
    `_write_aliases_if_safe` -> `app.pipeline.alias_learning.write_aliases_if_safe`, renamed too).
    Read each site's surrounding ~15 lines to confirm the correct target module before editing --
    do not guess from the name alone. NOTE: this file's `raising=False` patches of
    `app.main._run_pipeline` are OUT OF SCOPE for this plan (that name lives in `app/main.py`,
    untouched here except for the one `_deliver` retarget in Task 1's step G) -- 13-03's own plan
    handles that.

11. **`tests/test_multi_employee_delivery.py`** (Codex-flagged omission -- two `from
    app.pipeline.orchestrator import _deliver` sites, two string-form
    `monkeypatch.setattr("app.pipeline.orchestrator.gateway.send_outbound", ...)` patches): both
    imports retarget to `from app.pipeline.delivery import deliver as _deliver` (keep the local
    alias so the rest of each test body's `_deliver(...)` calls need no change). Both string-form
    patches retarget to `"app.pipeline.delivery.gateway.send_outbound"` (the `gateway` module
    object is now imported into `delivery.py`, per Task 1's step E imports).

12. **`tests/test_alias_full_loop.py`** (Codex-flagged omission -- two `from
    app.pipeline.orchestrator import _deliver` sites): retarget both to `from app.pipeline.delivery
    import deliver as _deliver`. The `from app.pipeline.orchestrator import resume_pipeline,
    run_pipeline` import at line 55 is UNCHANGED -- both names stay in `orchestrator.py`, do not
    touch this line.

13. **`tests/test_concurrency_proof.py`** (Codex-flagged omission -- string-form
    `monkeypatch.setattr("app.pipeline.orchestrator._deliver", ...)`, WITH an explanatory comment
    about why it targets Surface B specifically because `_deliver` was imported inside the approve
    route): READ the comment before editing -- it explains this patches the orchestrator-side
    reference because (pre-this-plan) the approve route in `main.py` did a function-body `from
    app.pipeline.orchestrator import _deliver`. **This plan's Task 1 already retargets that
    `main.py` site to `from app.pipeline import delivery` (step G) -- there is no longer a
    cross-plan gap here.** Retarget the string literal to `"app.pipeline.delivery.deliver"`
    (`_deliver` no longer exists anywhere, renamed to `deliver`, and `main.py`'s `approve()` now
    calls `delivery.deliver` via a top-level module-object import, which this string-form patch
    correctly intercepts). Confirm via the full-suite run in this task that this test now passes
    CLEANLY -- there is no longer any documented, expected gap to work around; if it fails, that is
    a genuine bug in Task 1's `main.py` retarget or this plan's own wiring, not an acceptable
    cross-plan deferral.

14. **`tests/test_cr_regressions.py`** (Codex-flagged omission -- TWO separate coupling points,
    confirmed this session): (a) `from app.pipeline.orchestrator import _deliver` at line 315
    (inside `test_cr03_deliver_enriches_run_dict_with_business_name`) retargets to `from
    app.pipeline.delivery import deliver as _deliver`; (b) `import app.pipeline.orchestrator as
    orch` followed by `monkeypatch.setattr(orch, "_write_aliases_if_safe", ...)` at ~line 375
    retargets to `from app.pipeline import alias_learning` plus `monkeypatch.setattr(alias_learning,
    "write_aliases_if_safe", ...)`.

For every retarget above: after editing, run that ONE test file in isolation
(`uv run pytest tests/<file> -q`) BEFORE moving to the next file, so a wiring bug in Task 1 is
caught close to its source rather than surfacing as a wall of unrelated-looking failures at the
end. If a test fails for a reason OTHER than the described coupling point, the bug is in Task 1's
module split (a missed call-site update, a wrong import) -- fix the SOURCE, never loosen an
assertion.

After all fourteen files pass in isolation, run the full suite. **There is no expected or
documented cross-plan gap in this plan** (unlike the prior revision) -- every test named above,
including `test_concurrency_proof.py` and `test_hitl.py`'s `approve()`-exercising tests, must be
GREEN by the end of this task, because Task 1's step G closes the `app/main.py` integration point
within this same plan. If any test still fails after its described retarget, treat it as a genuine
wiring bug requiring a source fix, not an acceptable gap for a later plan.

Also confirm, as a final sweep: `grep -rn "orchestrator\._deliver\|orchestrator\._clarify\b" app/
tests/ eval/ scripts/` returns ZERO matches anywhere in the repository (both names are fully
renamed/relocated; any remaining reference is a missed retarget).
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_delivery.py tests/test_alias_write.py tests/test_clarify.py tests/test_clarify_rounds.py tests/test_retrigger_epoch.py tests/test_combined_context.py tests/test_hitl.py tests/test_atomic_persist.py tests/test_needs_operator.py tests/test_demo_landing.py tests/test_multi_employee_delivery.py tests/test_alias_full_loop.py tests/test_concurrency_proof.py tests/test_cr_regressions.py -q` passes with 0 failures, 0 errors (ALL fourteen files, including test_concurrency_proof.py -- no exclusions)
    - `uv run pytest -q` (full suite) passes with ZERO failures (no documented cross-plan gap; every test must be green)
    - `grep -rn "orchestrator\._deliver\|orchestrator\._clarify\b" app/ tests/ eval/ scripts/` returns zero matches anywhere in the repository
    - `grep -c "suggest_pos\|decide_pos" tests/test_clarify.py` still returns a nonzero count referencing `decide_pos` (the orchestrator-side assertion is preserved, not deleted) AND a new assertion exists confirming `"decide(" not in` the clarification module's source
    - `grep -n "FunctionDef" tests/test_atomic_persist.py | grep -c "clarification\|defer_field_regression_clarification\|clarify"` is >= 1 (confirms the AST test retargets to the new module/names, not left pointed at orchestrator.py)
    - `grep -n "ast.Attribute" tests/test_atomic_persist.py | grep -c "clarify"` is >= 1 (confirms the _run_stages AST-shape check changed from ast.Name to ast.Attribute)
    - `grep -n "FunctionDef" tests/test_clarify_rounds.py | grep -c "clarify"` is >= 1 AND the file no longer parses `orchestrator.__file__`/`orch_mod.__file__` for a `_clarify` FunctionDef search (both AST sites retarget to clarification.py)
    - `grep -n "MAX_CLARIFICATION_ROUNDS" tests/test_needs_operator.py | head -1` shows an import from `app.pipeline.clarification`, not `app.pipeline.orchestrator`
    - `grep -c "from app.pipeline.orchestrator import _deliver\|from app.pipeline.orchestrator import.*_deliver" tests/test_atomic_persist.py tests/test_cr_regressions.py tests/test_multi_employee_delivery.py tests/test_alias_full_loop.py` is 0 across all four files (all _deliver imports retargeted to app.pipeline.delivery)
    - `grep -n "_normalize_candidate\|_write_aliases_if_safe" tests/test_alias_write.py | grep -c "app.pipeline.alias_learning"` is >= 4 (the three _normalize_candidate imports plus one _write_aliases_if_safe import all retarget to alias_learning)
    - `uv run ruff check tests/test_delivery.py tests/test_alias_write.py tests/test_clarify.py tests/test_clarify_rounds.py tests/test_retrigger_epoch.py tests/test_combined_context.py tests/test_hitl.py tests/test_atomic_persist.py tests/test_needs_operator.py tests/test_demo_landing.py tests/test_multi_employee_delivery.py tests/test_alias_full_loop.py tests/test_concurrency_proof.py tests/test_cr_regressions.py` reports zero violations
    - `git diff --stat tests/` shows no test file with a net line-count change larger than what a mechanical import/patch-target/AST-node-type retarget would produce (a large diff signals an accidental assertion rewrite -- inspect before finalizing)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>All fourteen coupling-point test files are retargeted with only import-path/patch-target/AST-node-shape changes (test_clarify.py's, test_atomic_persist.py's, and test_clarify_rounds.py's structural tests are re-mechanized to check the same invariants across the new module boundary, not weakened); the full suite is GREEN with zero failures and zero remaining orchestrator._deliver/orchestrator._clarify references anywhere in the repository -- no documented cross-plan gap, because Task 1's app/main.py retarget closed the integration point within this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Orchestrator core <-> carved-out pipeline modules | `orchestrator.py`'s state machine now calls `alias_learning`/`clarification`/`delivery` as module objects across a new internal boundary; a missed call-site update here silently stops calling the intended function |
| Test monkeypatch seams <-> moved functions | `monkeypatch.setattr(<module>, "<fn>", ...)` only intercepts calls that resolve through that exact module object at call time -- a caller still importing the OLD module reference would silently bypass the patch and hit real (possibly live-LLM/live-gateway) code in tests |
| This plan <-> app/main.py's ONE integration point | `app/main.py`'s approve() route's `_deliver` import is retargeted WITHIN this plan's own Task 1 (not deferred as a cross-plan gap), closing STRUCT-04's per-split green gate at every commit boundary this plan itself introduces; the full `app/main.py` router split remains 13-03's separate, larger job |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-05 | Tampering | Monkeypatch seam silently stops intercepting (module-object vs named-function import mismatch) | mitigate | D-11's module-object-import discipline is applied at every one of the identified call edges (`resume_pipeline`->`alias_learning`, `resume_pipeline`->`clarification`, `_run_stages`->`clarification`, `delivery`->`alias_learning`); Task 2's per-file isolation-run-before-proceeding practice catches a silently-non-intercepting patch as a behavior mismatch (real DB/LLM call attempted) rather than a clean pass |
| T-13-06 | Tampering | PII-safe error logging dropped during the `_deliver`->`delivery.deliver` move (WR-04 roster-attachment-to-exception pattern) | mitigate | Task 1's action specifies verbatim move of the `except Exception as exc: exc.payroll_roster = roster; raise` block with no rewriting; Task 2's `tests/test_delivery.py` retarget re-runs both WR-04 regression tests unmodified in assertions against the new `delivery` module |
| T-13-07 | Information Disclosure | Alias-learning misname guard (`safe_to_learn_alias`) relocated but its collision logic altered in transit | mitigate | Task 1 specifies the relocated function's body moves byte-for-byte from `reconcile_names.py` into `alias_learning.py`, only gaining a new top-level `from app.pipeline.reconcile_names import deterministic_match` import -- no logic change; Task 2 re-runs `tests/test_alias_write.py`'s full `_safe_to_learn_alias` unit-test suite (the misname-guard regression tests, STATE.md's "Alias-learning misname guard" prior finding) against the relocated `alias_learning.safe_to_learn_alias` |
| T-13-08 | Repudiation | Suggestion-vs-decision structural firewall (D-21-05: LLM suggestion never influences `decide()`) becomes unverifiable after the single-file structural test breaks | mitigate | Task 2 explicitly re-mechanizes (not deletes) `test_orchestrator_suggest_called_after_decide` to assert the SAME invariant across the new two-file boundary -- `decide(` stays in `orchestrator.py`, and a new assertion confirms `clarification.py`'s source contains no `decide(` call at all, closing the gap a naive test deletion would have opened |
| T-13-09 | Repudiation | `NameError` at runtime the first time the deferred field-regression clarification path executes (missing `Extracted` import) -- a money-adjacent path (clarification round machine) silently crashing in production rather than in a hermetic test | mitigate | Task 1 explicitly adds `Extracted` to `clarification.py`'s import list per the Codex-confirmed finding; Task 1's acceptance criteria greps for both the usage AND the import to prove they're paired, not just one or the other |
| T-13-10 | Repudiation | An AST-structural test (`test_atomic_persist.py`/`test_clarify_rounds.py`) silently stops verifying its real invariant (transaction-boundary discipline around `clarify`/`defer_field_regression_clarification`) because it was left pointed at the wrong file/function/AST-node-shape after the split, passing vacuously (zero matches, `assert not violations` on an empty list) rather than failing loudly | mitigate | Task 2 explicitly walks through the AST-node-shape changes required for each affected test to KEEP catching a real regression post-split (bare `ast.Name` -> module-qualified `ast.Attribute` for the `_run_stages` cross-module call; `FunctionDef` target module + name change for the same-module `defer_field_regression_clarification`/`clarify` pair; both `test_clarify_rounds.py` FunctionDef-search sites retargeted to `clarification.py`), and the acceptance criteria greps the retargeted test files to confirm they reference the new module/function names, not just that they still "pass" |
| T-13-11 | Repudiation | `app/main.py`'s approve() route silently breaks (ImportError) the moment this plan's Task 1 deletes `orchestrator._deliver`, because the fix was deferred to a later plan instead of applied within this plan's own commit series | mitigate | Task 1's step G explicitly retargets the ONE `app/main.py` import/call site in THIS plan, in the SAME commit series that removes `_deliver` from `orchestrator.py` -- Task 1's acceptance criteria includes a live FastAPI TestClient smoke test against `/health/live` to confirm `app/main.py` still imports and serves cleanly, and Task 2 confirms `test_hitl.py`'s and `test_concurrency_proof.py`'s `approve()`-exercising tests are GREEN with no documented gap |
</threat_model>

<verification>
`uv run pytest -q` passes at the same collected-test count as immediately before this plan's Task 1
began, with zero assertion-value changes outside the enumerated retargets (test_clarify.py's,
test_atomic_persist.py's, and test_clarify_rounds.py's structural tests change mechanism, not
asserted invariant), and with ZERO failures anywhere -- no documented, deferred cross-plan gap.
`uv run ruff check .` is clean over `app/pipeline/`, `app/main.py`, and every touched test file.
</verification>

<success_criteria>
- `app/pipeline/orchestrator.py` trimmed to ~800-900 lines (core state machine only)
- `app/pipeline/alias_learning.py`, `clarification.py`, `delivery.py` exist with the promoted
  public names from D-12/D-13, wired via module-object imports at every cross-module call edge
- `clarification.py` imports `Extracted` alongside `InboundEmail` -- no missing-import regression
- `reconcile_names.py` and `validate.py` have their BOUND-01 renames applied in place
- `app/main.py`'s approve() route calls `delivery.deliver` via a top-level import -- the ONE
  integration point is closed within THIS plan, not deferred
- Every one of the 14 identified test-coupling files (7 from the original census + 6 Codex-found
  omissions in Round 1 + the additional within-file gaps Round 2 found in already-named files) is
  retargeted with import-path/patch-target/AST-shape changes only
- Full suite green at the pre-split test count with ZERO failures -- no documented cross-plan gap
- Zero remaining `orchestrator._deliver`/`orchestrator._clarify` references anywhere in the repo
</success_criteria>

<output>
Create `.planning/phases/13-module-structure-boundaries/13-02-SUMMARY.md` when done.
</output>
