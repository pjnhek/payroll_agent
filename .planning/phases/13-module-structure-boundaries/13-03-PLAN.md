---
phase: 13-module-structure-boundaries
plan: 03
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - app/main.py
  - app/routes/__init__.py
  - app/routes/templating.py
  - app/routes/pipeline_glue.py
  - app/routes/webhook.py
  - app/routes/runs.py
  - app/routes/dashboard.py
  - app/routes/demo.py
  - app/routes/health.py
  - tests/test_webhook.py
  - tests/test_needs_operator.py
  - tests/test_reply_redelivery.py
  - tests/test_concurrency_proof.py
  - tests/test_webhook_dedup_race.py
  - tests/test_stuck_run_recovery.py
  - tests/test_health_schema.py
  - tests/test_demo_landing.py
  - tests/test_dashboard.py
  - tests/test_gateway.py
  - tests/test_ingest.py
  - tests/test_cr_regressions.py
  - tests/test_threading.py
autonomous: true
requirements: [STRUCT-01, STRUCT-04, BOUND-01]
must_haves:
  truths:
    - "app/main.py contains only app creation, router registration, and startup/shutdown wiring -- no route handlers, no business helpers"
    - "TestClient(app_main.app) still dispatches every one of the 19 pre-existing routes identically, regardless of which app/routes/*.py module now owns the handler"
    - "the full suite passes with zero assertion changes outside the enumerated import-path/patch-target retargets in this plan"
  artifacts:
    - path: "app/main.py"
      provides: "thin app assembly -- create app, mount static, include 5 routers"
      contains: "app.include_router"
      min_lines: 15
    - path: "app/routes/webhook.py"
      provides: "POST /webhook/inbound"
      contains: "def inbound"
    - path: "app/routes/runs.py"
      provides: "everything under /runs* -- list, detail, status, approve, reject, resolve, retrigger, pdf, simulate-reply"
      contains: "def retrigger"
    - path: "app/routes/dashboard.py"
      provides: "GET /, /eval, /eval/chart.svg"
      contains: "def landing"
    - path: "app/routes/demo.py"
      provides: "POST /demo/bind, /demo/compose, /demo/send-test"
      contains: "def demo_bind"
    - path: "app/routes/health.py"
      provides: "GET /health/live, /ready, /schema"
      contains: "def health_ready"
    - path: "app/routes/pipeline_glue.py"
      provides: "HTTP-to-orchestrator bridge helpers with public names"
      contains: "def run_pipeline_bg"
    - path: "app/routes/templating.py"
      provides: "shared Jinja2Templates instance + badge filters"
      contains: "templates = Jinja2Templates"
  key_links:
    - from: "app/routes/webhook.py"
      to: "app/routes/pipeline_glue.py"
      via: "module-object import (background_tasks.add_task(pipeline_glue.run_pipeline_bg, run_id))"
      pattern: "from app\\.routes import pipeline_glue"
    - from: "app/routes/runs.py"
      to: "app/pipeline/delivery.py"
      via: "module-object import (approve calls delivery.deliver)"
      pattern: "from app\\.pipeline import delivery"
    - from: "app/routes/runs.py"
      to: "app/routes/demo.py"
      via: "direct import of _DEMO_FIXTURES for runs_list's template context"
      pattern: "from app\\.routes\\.demo import"
---

<objective>
Split `app/main.py` (1,857 lines) into `app/routes/` -- five APIRouter modules by URL-prefix
concern (`webhook.py`, `runs.py`, `dashboard.py`, `demo.py`, `health.py`) plus two shared modules
(`pipeline_glue.py` for the HTTP-to-orchestrator bridge helpers, `templating.py` for the Jinja2
instance + badge filters) -- leaving `main.py` as thin app assembly (~15-30 lines). Every shared
bridge helper (`_run_pipeline`, `_resume_pipeline`, `_route_reply`, `_reply_sender_ok`,
`_row_to_inbound`, `_finish_reply_resume`, `_operator_resume`) is promoted to a public name in
`pipeline_glue.py` and wired via module-object imports, since these are cross-module references
the moment they leave `main.py`.

Purpose: STRUCT-01 (main.py split by concern, thin assembly) + the remaining BOUND-01 piece
(`app/main.py:784`'s function-body `from app.pipeline.orchestrator import _deliver` becomes a
top-level `from app.pipeline import delivery` in `routes/runs.py`) + STRUCT-04 (behavior-neutral).
This plan depends on 13-02 because `approve()`'s delivery call references the post-split
`app.pipeline.delivery` module.
Output: `app/main.py` trimmed to app assembly; five router modules + `pipeline_glue.py` +
`templating.py`; thirteen test files get mechanical import-path/patch-target retargets for their
`app.main` attribute/string-form monkeypatch and `inspect.getsource` structural coupling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-module-structure-boundaries/13-CONTEXT.md
@.planning/phases/13-module-structure-boundaries/13-RESEARCH.md
@.planning/phases/13-module-structure-boundaries/13-PATTERNS.md
@.planning/phases/13-module-structure-boundaries/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/routes/ -- five routers + pipeline_glue.py + templating.py; trim main.py to assembly</name>
  <files>app/main.py, app/routes/__init__.py, app/routes/templating.py, app/routes/pipeline_glue.py, app/routes/webhook.py, app/routes/runs.py, app/routes/dashboard.py, app/routes/demo.py, app/routes/health.py</files>
  <read_first>app/main.py (full file, 1,857 lines -- source of truth for every route/helper moved verbatim), app/pipeline/delivery.py and app/pipeline/clarification.py (created in 13-02 -- confirm the exact public names deliver/clarify this plan's approve()/simulate_reply() routes must call)</read_first>
  <action>
Create the package `app/routes/` with these seven files, then rewrite `app/main.py` to assembly
only. Every route handler and helper body moves VERBATIM -- no rewriting, no signature changes.

A. `app/routes/templating.py` (D-08): move the Jinja2Templates instance, `_BADGE_CLASS` (lines
193-208), `_BADGE_LABEL` (lines 209-224), `_badge_class_filter`/`_badge_label_filter` (lines
225-236), and the two `templates.env.filters[...]` registration lines, all from main.py lines
180-236, verbatim. Exports: `templates` (the module-level Jinja2Templates instance every router
imports).

B. `app/routes/pipeline_glue.py` (D-07 + BOUND-01 promotion): move `_row_to_inbound` (main.py
578-604) to `row_to_inbound`, `_reply_sender_ok` (607-628) to `reply_sender_ok`,
`_finish_reply_resume` (631-681) to `finish_reply_resume`, `_route_reply` (684-735) to
`route_reply`, `_resume_pipeline` (738-749) to `resume_pipeline_bg`, `_run_pipeline` (752-766) to
`run_pipeline_bg`, `_operator_resume` (944-960) to `operator_resume_bg`. All seven get PUBLIC
names (drop the leading underscore) -- this is itself a BOUND-01-motivated promotion (these become
cross-module references the moment `webhook.py`/`runs.py` call them from outside `main.py`).
Internal call-graph updates (same-module post-move, no import needed): `route_reply` calls
`finish_reply_resume`; `finish_reply_resume`'s `background_tasks.add_task(_resume_pipeline, ...)`
becomes `background_tasks.add_task(resume_pipeline_bg, ...)`. Imports: `from __future__ import
annotations`, `import logging`, `import uuid`, `from fastapi import BackgroundTasks`, `from
fastapi.responses import JSONResponse`, `from app.db import repo`, `from app.models.contracts
import InboundEmail`. Verify via grep whether `STALE_THRESHOLD`/`STALE_THRESHOLD_SECONDS` are
needed directly by anything in this file before importing them -- if not needed here, do not add
the import.

C. `app/routes/webhook.py` (D-05/D-06): move `inbound()` (main.py lines 304-577) verbatim. This
route calls `route_reply` -- wire via `from app.routes import pipeline_glue` (module object) then
`pipeline_glue.route_reply(...)`, and schedule `background_tasks.add_task(pipeline_glue.run_pipeline_bg,
run_id)` -- NEVER `from app.routes.pipeline_glue import run_pipeline_bg` (the module-object form
is what keeps `monkeypatch.setattr(pipeline_glue, "run_pipeline_bg", ...)` able to intercept).
Imports: `from __future__ import annotations`, `import json`, `import logging`, `from fastapi
import APIRouter, BackgroundTasks, HTTPException, Request`, `from fastapi.responses import
JSONResponse`, `from app.config import get_settings`, `from app.db import repo`, `from app.email
import gateway`, `from app.email.clean import clean_body`, `from app.models.contracts import
InboundEmail`, `from app.routes import pipeline_glue`. `router = APIRouter()`, decorator
`@router.post("/webhook/inbound")`.

D. `app/routes/runs.py` (D-06 -- "everything under /runs*", the largest router): move verbatim, in
route-declaration order: `approve` (769-814), `reject` (817-834), `resolve` (837-941), `retrigger`
(963-1082), `_build_alias_rationale_notes` (1090-1137 -- keep as a module-private helper if it has
no cross-module caller; verify via grep before deciding whether BOUND-01 requires promoting it --
if called only from `run_detail` in this same file, leaving it `_private` is fine, same-module
privates are not violations), `runs_list` (1335-1395), `run_status` (1401-1425), `run_detail`
(1426-1522), `paystub_pdf` (1583-1626), `simulate_reply` (1632-1750). ALSO move the module-level
constants `STALE_THRESHOLD`/`STALE_THRESHOLD_SECONDS` (main.py lines 102-103) and
`IN_FLIGHT_STATUSES` (113-121) here verbatim -- `retrigger`/`runs_list`/`run_detail` are their
heaviest consumers.
Call-site rewiring: `approve()`'s function-body `from app.pipeline.orchestrator import _deliver`
then `_deliver(run_id, run)` (main.py lines 784, 795) becomes a top-level `from app.pipeline
import delivery` import plus `delivery.deliver(run_id, run)` -- this is the D-07/BOUND-01
integration point named explicitly in CONTEXT.md. `retrigger`'s
`background_tasks.add_task(_run_pipeline, run_id)` calls become
`background_tasks.add_task(pipeline_glue.run_pipeline_bg, run_id)` via
`from app.routes import pipeline_glue` (grep for every `_run_pipeline`/`_resume_pipeline`/
`_route_reply`/`_operator_resume`/`_reply_sender_ok`/`_row_to_inbound` reference that lands inside
this file's moved functions and retarget each to its `pipeline_glue.<promoted_name>` equivalent).
`resolve()`'s `background_tasks.add_task(_operator_resume, run_id, overrides)` becomes
`pipeline_glue.operator_resume_bg(run_id, overrides)`. `simulate_reply`'s call into `_route_reply`
becomes `pipeline_glue.route_reply(...)`. Locate the stranded-sweep block (calls
`repo.sweep_stranded_runs`, `repo.find_stranded_unconsumed_replies`, the reply-sender-ok predicate,
the inbound-row-builder, and a resume dispatch) by grepping main.py for
`sweep_stranded_runs` -- confirm via that grep which function (`runs_list` or `landing`) actually
contains it before moving; do not assume the line-range estimate below is authoritative. If it is
inside `runs_list`, it lands in this file; retarget its four helper calls to
`pipeline_glue.reply_sender_ok`, `pipeline_glue.row_to_inbound`, `pipeline_glue.resume_pipeline_bg`.
`runs_list`'s template context references `_DEMO_FIXTURES` -- import via `from app.routes.demo
import _DEMO_FIXTURES` (a direct sibling import of a still-private demo constant; same-package,
non-function-body, and the correct way to avoid duplicating the dict). Imports: `from __future__
import annotations`, `import uuid`, `from datetime import UTC, datetime, timedelta`, `from io
import BytesIO`, `from fastapi import APIRouter, BackgroundTasks, Form, HTTPException, Request`,
`from fastapi.responses import FileResponse, JSONResponse, RedirectResponse, StreamingResponse`,
`from app.db import repo`, `from app.models.status import RunStatus`, `from app.pipeline import
delivery`, `from app.routes import pipeline_glue`, `from app.routes.demo import _DEMO_FIXTURES`,
`from app.routes.templating import templates`. `router = APIRouter()`.

E. `app/routes/dashboard.py` (D-06): move `landing` (main.py lines ~1143-1213), `eval_view`
(~1528-1559), `eval_chart` (~1565-1577), verbatim. If the stranded-sweep block from step D's grep
turns out to live inside `landing` instead of `runs_list`, it lands HERE instead -- apply the same
`pipeline_glue` retargeting described in step D, importing `STALE_THRESHOLD_SECONDS`,
`IN_FLIGHT_STATUSES` via `from app.routes.runs import STALE_THRESHOLD_SECONDS, IN_FLIGHT_STATUSES`
and `_DEMO_FIXTURES` via `from app.routes.demo import _DEMO_FIXTURES`. Only one of `runs.py` or
`dashboard.py` should end up owning the sweep block -- do not duplicate it into both. Imports:
`from __future__ import annotations`, `from fastapi import APIRouter, BackgroundTasks, Request`,
`from app.db import repo`, `from app.routes.templating import templates`, plus whichever of
`pipeline_glue`/`runs`/`demo` the sweep block actually needs (verify via the step-D grep before
finalizing this file's imports). `router = APIRouter()`.

F. `app/routes/demo.py` (D-06): move `demo_bind` (~1218-1238), `demo_compose` (~1244-1329),
`demo_send_test` (~1751-1857), verbatim, PLUS the module-level constants `_DEMO_FIXTURES`
(~122-150), `DEMO_OPERATOR_EMAIL` (157), `_SEED_CONTACTS` (~161-169), `_SEED_BUSINESS_IDS`
(~170-177). Imports: `from __future__ import annotations`, `import uuid`, `from fastapi import
APIRouter, Form, HTTPException, Request`, `from fastapi.responses import RedirectResponse`, `from
app.db import repo`, `from app.email import gateway` (if `demo_send_test` calls it directly --
verify via grep), `from app.routes.templating import templates`. `router = APIRouter()`.

G. `app/routes/health.py` (D-05): move `health_live` (~244-254), `health_ready` (~255-277),
`health_schema` (~278-303), verbatim. Imports: `from __future__ import annotations`, `import
logging`, `from fastapi import APIRouter, HTTPException`, `from fastapi.responses import
JSONResponse`, `from app.db.schema_introspect import diff_against_live`, `from app.db.supabase
import get_connection`. `router = APIRouter()`.

H. `app/routes/__init__.py`: one-paragraph package docstring in the style of
`app/pipeline/__init__.py` (e.g. describing this as the routes package split by URL-prefix
concern plus the shared pipeline_glue bridge and templating instance).

I. Rewrite `app/main.py` to assembly only (~15-30 lines): `from __future__ import annotations`,
`from fastapi import FastAPI`, `from fastapi.staticfiles import StaticFiles`, `from app.routes
import dashboard, demo, health, runs, webhook`, `app = FastAPI(title="Payroll Agent")`,
`app.mount("/static", StaticFiles(directory="app/static"), name="static")`,
`app.include_router(health.router)`, `app.include_router(webhook.router)`,
`app.include_router(runs.router)`, `app.include_router(dashboard.router)`,
`app.include_router(demo.router)`. Delete every route/helper/constant that moved to `app/routes/*`
from `main.py` -- nothing but the FastAPI app object, the router includes, and the static mount
remains. One-line module docstring describing app assembly only, routes live in app/routes/*.

Before finalizing, grep `app/main.py`'s ORIGINAL content for every function/constant reference to
confirm nothing was missed (e.g. any startup/shutdown event handler, any additional module-level
state not enumerated above) -- if something exists that this action block did not name, place it
in the most concern-appropriate router module per D-05/D-06's URL-prefix grouping, or in
`app/main.py` itself if it is genuinely app-lifecycle wiring (startup/shutdown hooks belong in
`main.py`, not a router).
  </action>
  <acceptance_criteria>
    - wc -l app/main.py reports fewer than 40 lines
    - a Python import of app.main.app succeeds and the FastAPI app object's route count is not reduced from the pre-split baseline (19 application routes plus FastAPI's built-in docs/openapi routes)
    - grep for top-level def/async def in app/main.py returns zero matches (no route handlers left in main.py)
    - grep across app/routes/ for a function-body import of orchestrator._deliver returns zero matches (the D-07 integration point is a top-level "from app.pipeline import delivery" import, not a function-body import)
    - uv run ruff check app/main.py app/routes reports zero violations
    - a FastAPI TestClient GET to /health/live returns status code 200
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "from app.main import app; from fastapi.testclient import TestClient; c = TestClient(app); assert c.get('/health/live').status_code == 200"</automated>
  </verify>
  <done>app/main.py is thin assembly (under 40 lines, no route handlers); all 19 routes are registered via 5 included routers; pipeline_glue.py exposes the 7 promoted-public bridge helpers; templating.py is the single shared Jinja2Templates + badge-filter source; the health-check smoke test passes end-to-end through the FastAPI TestClient.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate the main.py test-coupling census (attribute + string-form monkeypatches, inspect.getsource structural tests), then verify the full suite green</name>
  <files>tests/test_webhook.py, tests/test_needs_operator.py, tests/test_reply_redelivery.py, tests/test_concurrency_proof.py, tests/test_webhook_dedup_race.py, tests/test_stuck_run_recovery.py, tests/test_health_schema.py, tests/test_demo_landing.py, tests/test_dashboard.py, tests/test_gateway.py, tests/test_ingest.py, tests/test_cr_regressions.py, tests/test_threading.py</files>
  <read_first>tests/test_webhook.py (string-form monkeypatch.setattr string literals around lines 232-235 and 301), tests/test_needs_operator.py (inspect.getsource(main_mod.retrigger) AST structural test around line 310, plus an IN_FLIGHT_STATUSES import at line 45), tests/test_reply_redelivery.py (app_main._resume_pipeline attribute patch plus STALE_THRESHOLD_SECONDS imports), tests/test_health_schema.py (patch calls targeting app.main.get_connection and app.main.diff_against_live)</read_first>
  <action>
Retarget every test-coupling point this split breaks. Apply ONLY the import-path/patch-target
change described per file -- never alter an assertion's expected value or logic. Work file-by-file,
running each in isolation before proceeding to the next.

1. tests/test_webhook.py (string-form patches near lines 232, 235, 301): the string literal
   "app.main._resume_pipeline" becomes "app.routes.pipeline_glue.resume_pipeline_bg"; the string
   literal "app.main._run_pipeline" becomes "app.routes.pipeline_glue.run_pipeline_bg". Grep this
   file for any additional "app.main._" string literal beyond these two names.

2. tests/test_needs_operator.py: the import "from app.main import IN_FLIGHT_STATUSES, app" (line
   45) becomes two imports -- "from app.main import app" and "from app.routes.runs import
   IN_FLIGHT_STATUSES" (IN_FLIGHT_STATUSES moved to routes/runs.py per Task 1 step D). The
   inspect.getsource(main_mod.retrigger) structural test (around line 310): change "import app.main
   as main_mod" to "import app.routes.runs as runs_mod", and change
   "src = inspect.getsource(main_mod.retrigger)" to "src = inspect.getsource(runs_mod.retrigger)"
   -- keep every subsequent AST-parsing assertion (the ast.Set/ast.Attribute walk checking
   retrigger's stale-status set literal) completely unchanged; only the source object changes.

3. tests/test_reply_redelivery.py: "import app.main as app_main" followed by
   "monkeypatch.setattr(app_main, '_resume_pipeline', _spy)" becomes "import app.routes.pipeline_glue
   as pipeline_glue_mod" followed by "monkeypatch.setattr(pipeline_glue_mod, 'resume_pipeline_bg',
   _spy)". Three "from app.main import STALE_THRESHOLD_SECONDS" sites (around lines 259, 315, 393)
   become "from app.routes.runs import STALE_THRESHOLD_SECONDS".

4. tests/test_stuck_run_recovery.py: "import app.main as app_main; client = TestClient(app_main.app)"
   (2 sites) stays UNCHANGED -- app_main.app still resolves correctly post-split (the FastAPI app
   object never moved). The monkeypatch.setattr(app_main, "_run_pipeline", ...) site (around line
   317) retargets to "import app.routes.pipeline_glue as pipeline_glue_mod" plus
   "monkeypatch.setattr(pipeline_glue_mod, 'run_pipeline_bg', ...)". Two "from app.main import
   STALE_THRESHOLD_SECONDS" sites (around lines 288, 346) become "from app.routes.runs import
   STALE_THRESHOLD_SECONDS".

5. tests/test_concurrency_proof.py: "import app.main as app_main; client = TestClient(app_main.app)"
   (around line 289) stays unchanged. The monkeypatch.setattr(app_main, "_run_pipeline", ...) site
   (around line 117) has its own comment about needing to patch on the orchestrator module -- read
   that comment carefully before changing; it may indicate this site already targets
   app.pipeline.orchestrator rather than app.main. If it targets app_main, retarget to
   pipeline_glue.run_pipeline_bg; if it targets orchestrator already, leave it untouched (out of
   this plan's scope).

6. tests/test_webhook_dedup_race.py: "import app.main as app_main" followed by
   monkeypatch.setattr(app_main, "_run_pipeline", ...) (around lines 47-58) becomes "import
   app.routes.pipeline_glue as pipeline_glue_mod" followed by monkeypatch.setattr(pipeline_glue_mod,
   "run_pipeline_bg", ...). "client = TestClient(app_main.app)" stays unchanged.

7. tests/test_health_schema.py: patch("app.main.get_connection", ...) and
   patch("app.main.diff_against_live", ...) (around lines 20-42) become
   patch("app.routes.health.get_connection", ...) and patch("app.routes.health.diff_against_live",
   ...) (both names now live in the health.py router's own import scope per Task 1 step G).
   "from app.main import app" stays unchanged.

8. tests/test_demo_landing.py: 14 "from app.main import app as fastapi_app" sites stay unchanged
   (the FastAPI app object is unaffected). The three "import app.main as main_mod" sites (around
   lines 811, 876, 933) -- inspect what each patches; if it is _route_reply or a demo-cluster
   helper, retarget to the appropriate app.routes.* module (pipeline_glue for _route_reply which
   becomes route_reply, or runs if it is a runs-route helper); read the surrounding ~20 lines of
   each site to determine the correct target before editing -- do not guess.

9. tests/test_dashboard.py: "from app.main import app" sites stay unchanged. The three
   "import app.main as _main" sites (around lines 547, 592, 670) patch _route_reply -- retarget to
   "import app.routes.pipeline_glue as _main" with monkeypatch.setattr(_main, "route_reply", ...)
   (both the module target AND the attribute name change, since _route_reply was promoted to
   route_reply).

10. tests/test_gateway.py: "from app.main import app" sites stay unchanged. The
    "import app.main as _main" sites (around lines 1061, 1542) patch _resume_pipeline / _run_pipeline
    / _route_reply -- retarget each to "import app.routes.pipeline_glue as _main" with the
    corresponding promoted name (resume_pipeline_bg / run_pipeline_bg / route_reply).

11. tests/test_ingest.py: "from app.main import app" sites stay unchanged. The
    "import app.main as _main" site (around line 182) patches _run_pipeline -- retarget to "import
    app.routes.pipeline_glue as _main" with monkeypatch.setattr(_main, "run_pipeline_bg", ...).

12. tests/test_cr_regressions.py: "from app.main import app" (around line 39) stays unchanged. The
    three "import app.main as app_main" sites followed by monkeypatch.setattr(app_main,
    "_run_pipeline", ...) (around lines 461, 498, 534) become "import app.routes.pipeline_glue as
    app_main" followed by monkeypatch.setattr(app_main, "run_pipeline_bg", ...).

13. tests/test_threading.py (around line 531): "import app.main as main_mod; src =
    inspect.getsource(main_mod)" asserting that "find_awaiting_reply_for_header" and
    "find_any_run_for_header" appear in the source -- these two strings are repo function CALLS
    made from the webhook's reply-routing path inside inbound(); verify via grep which module
    actually contains these two call sites post-split (the most likely target is
    app.routes.webhook, but confirm before editing). Retarget to "import app.routes.webhook as
    webhook_mod; src = inspect.getsource(webhook_mod)", keeping both assertions unchanged.

After each file's retarget, run it in isolation before moving to the next. If a failure is not
explained by the described coupling point, the bug is in Task 1's route split (a missed call-site
rewrite, a wrong import) -- fix the SOURCE module, never loosen an assertion. After all thirteen
files pass in isolation, run the full suite.
  </action>
  <acceptance_criteria>
    - each of the 13 listed test files passes in isolation with 0 failures, 0 errors
    - the full suite passes with the same collected-test count as immediately before Task 1 of this plan began, zero failures
    - grep confirms zero remaining bare "app.main._run_pipeline" or "app.main._resume_pipeline" or "app.main._route_reply" references (attribute-form or string-form) anywhere under tests/
    - uv run ruff check reports zero violations across all 13 touched test files
    - a diff review of each touched test file shows only import-path/patch-target/attribute-name changes -- no assertion value or logic changed (test_needs_operator.py's structural test keeps its exact ast-walk assertions; test_threading.py keeps its exact substring assertions)
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q</automated>
  </verify>
  <done>All 13 coupling-point test files pass with only import-path/patch-target/attribute-name retargets, and the full suite is green at the pre-split test count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Inbound webhook (untrusted internet) to app/routes/webhook.py | Signature verification (svix HMAC), sender allowlisting, and unsigned-request rejection all move verbatim from main.py's inbound() route; no new input-validation surface introduced |
| Operator gate (approve/reject/resolve/retrigger) to app/routes/runs.py | CAS status-transition logic moves verbatim; the D-07 delivery integration point (approve() calling delivery.deliver) is the single highest-risk relocation in this plan |
| Demo affordances (allowlisted, no live-provider send) to app/routes/demo.py | _SEED_CONTACTS/_SEED_BUSINESS_IDS allowlist validation moves verbatim -- these gate which business_name values reach repo writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-13-09 | Tampering | approve() route's D-07 delivery integration point (function-body orchestrator._deliver import becomes top-level pipeline.delivery import) | mitigate | Task 1's action specifies the exact target shape (module-object import at file top, delivery.deliver(run_id, run) call site) and the acceptance criteria greps app/routes/ for any remaining function-body import of the old symbol; Task 2 does not touch approve()'s own test coverage assertions, only import-path retargets in unrelated coupling-point files |
| T-13-10 | Tampering | Webhook signature verification (svix HMAC) accidentally reordered or dropped during the verbatim move of inbound() | mitigate | Task 1 requires a byte-for-byte verbatim move of inbound() (lines 304-577); the MEDIUM-5 verify-before-parse ordering documented in the route's own docstring is preserved as-is since no rewriting occurs; the acceptance criteria's TestClient smoke test plus the full Task 2 suite run (including test_webhook.py's signature-rejection assertions) is the regression backstop |
| T-13-11 | Spoofing | FIX-5 sender revalidation (reply_sender_ok / GAP-5/CR-5) silently stops being called if a call site is missed during the pipeline_glue retarget | mitigate | Task 1 step D explicitly enumerates every _reply_sender_ok/_row_to_inbound/_resume_pipeline reference inside the moved runs.py functions and requires each retargeted via grep-confirmed call-site rewiring, not a blind find-replace; Task 2's per-file isolation-run-before-proceeding practice catches a silently-non-intercepting patch as a behavior mismatch |
| T-13-12 | Repudiation | Demo allowlist constants (_SEED_CONTACTS/_SEED_BUSINESS_IDS/_DEMO_FIXTURES) accidentally duplicated across two router modules, causing drift between the copy runs.py imports and the copy demo.py mutates | mitigate | Task 1 step D explicitly directs runs.py to import _DEMO_FIXTURES from routes/demo.py rather than redeclaring it, and step E applies the same non-duplication rule to whichever module ends up owning the stranded-sweep block; the acceptance criteria's ruff/grep checks are a backstop against accidental redeclaration |
</threat_model>

<verification>
The full suite passes at the same collected-test count as immediately before this plan's Task 1
began, with zero assertion-value changes outside the enumerated retargets. ruff is clean over
app/main.py, app/routes/, and every touched test file. A FastAPI TestClient smoke test against
/health/live returns 200 through the fully-split router chain.
</verification>

<success_criteria>
- app/main.py trimmed to ~15-30 lines (app assembly only)
- app/routes/webhook.py, runs.py, dashboard.py, demo.py, health.py, pipeline_glue.py,
  templating.py all exist with the promoted public names from D-07/D-08
- Every one of the 15+ pre-existing app.main attribute/string-form monkeypatch and
  inspect.getsource coupling points across the 13 named test files either resolves unchanged
  (the FastAPI app object itself) or retargets mechanically to the correct app/routes/* module
- Full suite green at the pre-split test count
</success_criteria>

<output>
Create `.planning/phases/13-module-structure-boundaries/13-03-SUMMARY.md` when done
</output>
</content>
