---
phase: 6
slug: real-integration-ship
status: draft
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-23
---

# Phase 6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `06-RESEARCH.md` → Validation Architecture + Security Domain.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (dev dep in `pyproject.toml`) |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run pytest -q -m "not integration and not live_llm"` |
| **Full suite command** | `uv run pytest -q` |
| **Estimated runtime** | ~15–30 seconds (mocked suite); integration adds live-DB time |

**Markers:** `integration` (requires live local DB), `live_llm` (requires real API keys — never run in CI/gates). Resend, Render, and live-DB round-trip behaviors are mocked at the `gateway.py` seam — no `live_resend` marker needed because the real provider is exercised only at the BLOCKING human gate (D-09b), not in the suite.

**No-op-swap invariant:** the existing mocked suite (422 tests as of Phase 5) MUST stay green throughout Phase 6 — it is the guard that wiring Resend behind the gateway did not change pipeline behavior.

**MEDIUM-5 xfail protocol:** Wave-0 gateway tests targeting not-yet-implemented behavior are marked `@pytest.mark.xfail(strict=True, reason="implemented in 06-04")`. This keeps the mocked suite exit code 0 between waves. When 06-04 implements the real gateway, each test becomes XPASS (strict=True → suite exits non-zero); 06-04 removes the xfail markers. The suite must have 0 FAILED and 0 XFAIL after 06-04 completes.

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest -q -m "not integration and not live_llm"` (full mocked suite, no live deps) — must exit 0
- **After every plan wave:** Run `uv run pytest -q -m "not live_llm"` (includes integration tests against live local DB)
- **Before `/gsd-verify-work`:** Full suite green (`uv run pytest -q`)
- **Max feedback latency:** ~30 seconds (mocked suite)

---

## Per-Task Verification Map

> Task IDs are assigned by the planner. This map is keyed by requirement/decision + target test file
> so each plan task can cite the row it satisfies. Threat refs map to `06-RESEARCH.md` → Security Domain.
> The three Resend SDK call sites (`resend.Webhooks.verify`, `resend.EmailsReceiving.get`,
> `resend.Emails.send`) are mocked at module level in `tests/test_gateway.py` — no live network.

| Requirement | Behavior | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|-------------|----------|------------|-----------------|-----------|-------------------|-------------|--------|
| OPS-02 / D-17 | Forged inbound POST (bad/absent `svix-signature`) → `resend.Webhooks.verify` raises `ValueError` → route returns HTTP 400, pipeline NOT entered | Spoofed inbound payroll email (Spoofing) | Signature verify is step zero on `raw_body = await request.body()` for Resend-shaped payloads; reject before any extraction | unit (mock `resend.Webhooks.verify` raising) — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "verify or signature" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / D-17 / HIGH-2 | Canonical fixture POST (InboundEmail JSON, no svix headers) succeeds without signature verify — fixture/dev path intact | Fixture path regression (Integrity) | parse_inbound dual-path: Resend-envelope → verify+two-step; canonical shape → passthrough, no verify | unit (no xfail — GREEN immediately from 06-01) | `uv run pytest tests/test_gateway.py -k "canonical_fixture" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / D-17 / BLOCKER-2 | With ALLOW_UNSIGNED_FIXTURES unset/False (prod default), a Resend-envelope payload WITHOUT svix-* headers → route returns 400 — prod default path verified by automated test | Unsigned Resend payload passes in prod (Spoofing) | allow_unsigned_fixtures=False default in Settings (06-02 Task 2); route returns 400 for unsigned Resend-envelope payloads in prod; ALLOW_UNSIGNED_FIXTURES absent from render.yaml value: entries | unit (no xfail — GREEN from 06-04 Task 2) | `uv run pytest tests/test_gateway.py -k "allow_unsigned_fixtures_prod_default" -x -q` AND `grep -q "ALLOW_UNSIGNED_FIXTURES" render.yaml && exit 1 \|\| true` | ❌ W3 | ⬜ pending |
| OPS-02 / HIGH-1 / D-01b / A5 | send_outbound passes `headers={"Message-ID": synthetic_id}` to resend.Emails.send; email_messages.message_id stores the SAME synthetic id; Resend's returned provider id logged only (not persisted — email_messages has no provider_message_id column, HIGH-1 waive); reply routing matches In-Reply-To against the synthetic id — no id mismatch | Reply routing fails after real send (Integrity/Spoofing) | HIGH-1 design: app owns the RFC Message-ID; synthetic id is both the outbound header and the DB routing key; A5 verified at the human round-trip gate (06-05) | unit (mock resend.Emails.send; assert headers["Message-ID"] == synthetic_id; assert provider id NOT in DB UPDATE SQL) — xfail(strict=True) in 06-01, xfail removed in 06-04; A5 round-trip confirmation is manual (06-05) | `uv run pytest tests/test_gateway.py -k "message_id or reserved" -x -q` (Message-ID header assertion) | ❌ W0/W3 | ⬜ pending |
| OPS-02 / HIGH-1-AUTH | `send_outbound` sets `resend.api_key = get_settings().resend_api_key` as its FIRST line (before mint / reserved INSERT / `resend.Emails.send`) so the outbound-only demo path (`/demo/send-test`) authenticates without first running `parse_inbound`; `parse_inbound` Resend path sets it defensively too | Live outbound send fails unauthenticated (Availability) | Per-function key set (not module-init — `get_settings()` is lru_cache'd, so per-function is monkeypatch/test-safe); T-06-04-12 | unit (mock `resend.Emails.send`; assert `resend.api_key` configured at call time) — `test_send_outbound_configures_resend_api_key`, no xfail (GREEN from 06-04) | `uv run pytest tests/test_gateway.py -k "configures_resend_api_key" -x -q` | ❌ W3 | ⬜ pending |
| OPS-02 / REPLY-TO TOPOLOGY (Pass-6) | send_outbound includes `reply_to=get_settings().resend_reply_to` in the resend.Emails.send dict ONLY when resend_reply_to is non-empty; when non-empty, the mocked resend.Emails.send is called with a dict whose "reply_to" key == the configured address (e.g. "payroll@jiodnel.resend.app"); ensures client replies are directed to the inbound .resend.app address (not the unreachable onboarding@resend.dev From address) | Client replies go to onboarding@resend.dev From address (unreachable) instead of inbound webhook address → clarify→reply→resume loop breaks (Integrity/Spoofing) | resend 2.32.2 SDK send dict accepts `reply_to` as str or list[str]; when RESEND_REPLY_TO is set (non-empty), the key is present; when empty/unset, the key is ABSENT (not empty-string — T-06-04-13); threading chain match is address-agnostic so From asymmetry does not break routing IF physical reply reaches inbound address via Reply-To | unit (mock resend.Emails.send capturing the send dict; monkeypatch get_settings().resend_reply_to) — `test_send_outbound_includes_reply_to_when_configured`, no xfail (GREEN from 06-04 Task 1) | `uv run pytest tests/test_gateway.py -k "includes_reply_to_when_configured" -x -q` | ❌ W3 | ⬜ pending |
| OPS-02 / REPLY-TO TOPOLOGY (Pass-6) | When resend_reply_to is empty/unset, the `reply_to` key is OMITTED entirely from the resend.Emails.send dict (do NOT send `reply_to=""` which would produce a malformed Reply-To header); omission is the correct behavior for the default/unconfigured case | Malformed Reply-To header sent when RESEND_REPLY_TO is unset (Integrity) | Conditional: `if _reply_to: send_params["reply_to"] = _reply_to` — key absent when empty; the absence-not-empty-string distinction is load-bearing; verified by asserting `"reply_to" not in captured_send_dict` | unit (mock resend.Emails.send capturing the send dict; monkeypatch get_settings().resend_reply_to="") — `test_send_outbound_omits_reply_to_when_not_configured`, no xfail (GREEN from 06-04 Task 1) | `uv run pytest tests/test_gateway.py -k "omits_reply_to_when_not_configured" -x -q` | ❌ W3 | ⬜ pending |
| OPS-02 / D-13 | Duplicate delivery (same RFC `message_id`) → second webhook returns 200 but `decide.py`/pipeline runs exactly ONCE; enqueue ONLY on inserted row (explicit ON CONFLICT DO NOTHING RETURNING id) | Duplicate-delivery false-process (Integrity) | Atomic insert-or-skip; background task enqueued ONLY if row returned (HIGH-4 explicit dedup) | unit (mocked: `test_duplicate_delivery_pipeline_runs_once_unit`, no live DB) + integration (`test_duplicate_delivery_pipeline_runs_once`, `@pytest.mark.integration`) | `uv run pytest tests/test_ingest.py -k "dedup or duplicate" -x -q` (unit runs without integration marker) | ✅ extend (existing dedup via UNIQUE) | ⬜ pending |
| OPS-02 / D-13 | Dedup keys on the RFC `message_id` from the full fetch, NOT the Resend internal `email_id` | Wrong-key dedup miss (Integrity) | Dedup uses the normalized `InboundEmail.message_id` (RFC), set from the fetched headers | unit — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "message_id" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / D-01a / D-18 | Two-step parse: metadata-only `email.received` webhook → `resend.EmailsReceiving.get(email_id)` fetch → normalized `InboundEmail` (typed `message_id`/`in_reply_to`/`references_header`/`body_text`) | Provider blob leak past seam (Tampering) | `parse_inbound` owns BOTH steps + lowercases `ReceivedEmail.headers` before extracting `in-reply-to`/`references`; no raw provider dict downstream | unit (mock `resend.EmailsReceiving.get`) — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "two_step or parse_inbound" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / D-14 / D-13c / BLOCKER-3 | Outbound send calls `repo.get_outbound_references_chain(run_id)` to LOAD the prior accumulated References chain from DB rows, appends the new `in_reply_to` token to build the full chain, THEN writes `send_state='reserved'` row BEFORE `resend.Emails.send` call; the flip-to-sent UPDATE uses the SYNTHETIC message_id as WHERE key (not the Resend provider id); UPDATE sets ONLY send_state (no updated_at, no provider_message_id — columns do not exist on email_messages) | Threading corruption on dropped/dup delivery (Integrity); send-without-record (Repudiation); WHERE-key ambiguity (Integrity) | Durable threading from DB state — FakeConnection pre-populated with prior outbound row; test_send_outbound_reserved_before_sent_ordering asserts accumulated chain + asserts UPDATE WHERE param is the SYNTHETIC message_id not the provider id (BLOCKER-3) + asserts provider id NOT in UPDATE params (HIGH-1 waive) | unit (mock `resend.Emails.send` + FakeConnection) — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "threading or references or reserved" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / HIGH-3 | send_outbound on provider exception flips reserved row to send_state='failed' and re-raises; run reaches ERROR and is retriggerable (not stranded in 'reserved') | Stranded runs (Repudiation/Availability) | try/except around resend.Emails.send; on exception call repo.update_email_message_state(message_id, "failed"); re-raise | unit (mock resend.Emails.send raising + FakeConnection) — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "failed_on_exception" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / D-14 / MEDIUM-7 | An inbound reply whose In-Reply-To/References points at a prior outbound chain resumes the CORRECT run via existing reply-routing code — not just chain-construction, but actual run-match | Routing miss silently opens a new run (Integrity) | test_inbound_reply_routes_to_correct_run: FakeConnection returns run_A for header match; POST canonical reply; assert BackgroundTasks.add_task called with run_A's run_id | unit (FakeConnection + monkeypatched repo.find_awaiting_reply_for_header) — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "reply_routes" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 | No-op-swap invariant: full existing suite stays green after the real gateway lands; fixture path unchanged | Regression hidden by integration (Integrity) | Real provider isolated to `gateway.py`; every caller + fixture path untouched | full suite | `uv run pytest -q` | ✅ existing suite | ⬜ pending |
| OPS-01 / D-20 | Liveness route returns 200 with NO DB hit (Render deploy health check must not fail on a Supabase blip) | DoS via health-check flap (Availability) | Liveness is pure; DB dependency isolated to readiness | unit — xfail(strict=True) in 06-01 (reason: "implemented in 06-02"), xfail removed in 06-02 | `uv run pytest tests/test_dashboard.py -k "live or health" -x -q` | ❌ W0 | ⬜ pending |
| OPS-01 / D-16 / D-20 | Readiness/keep-alive route runs a real `SELECT` and returns 200 when the pool is up | Silent DB-down served as healthy (Availability) | Readiness proves the DB path; the keep-alive cron's target | integration — xfail(strict=True) in 06-01 (reason: "implemented in 06-02"), xfail removed in 06-02 | `uv run pytest tests/test_dashboard.py -k "ready or keepalive" -m integration -x` | ❌ W0 | ⬜ pending |
| OPS-03 | Keep-alive workflow targets the readiness route (runs a `SELECT`) and includes `workflow_dispatch:` for manual re-enable | Stale keep-alive → Supabase pause (Availability) | Cron hits the DB-touching route (warms Render + un-pauses Supabase in one ping); manual re-trigger documented | manual (YAML inspection) | inspect `.github/workflows/keepalive.yml` | ❌ W0 | ⬜ pending |
| OPS-04 / CALC-04/05 | README contains the locked disclaimer verbatim (educational/not-tax-compliant; OBBBA exclusion; Additional Medicare 0.9% over $200k YTD unmodeled) | Misrepresentation as tax-compliant (Repudiation) | Disclaimer prominent near top per D-11; copied verbatim from CLAUDE.md §5 / REQUIREMENTS.md CALC-04/05 | unit (grep/assert) | `uv run pytest tests/test_readme.py -x -q` OR `grep` check | ❌ W0 (optional) | ⬜ pending |
| OPS-02 / HIGH-3 / attachments | send_outbound forwards PDF attachment bytes to resend.Emails.send as `[{"filename": name, "content": base64_bytes_str}]`; paystub PDFs are NOT silently dropped | PDF dropped on real send (Integrity) | Attachments list[tuple[str,bytes]] mapped to Resend SDK format in gateway.py; test_send_outbound_forwards_attachments asserts the mock capture contains the filename and correct base64 content | unit (mock resend.Emails.send capturing kwargs) — xfail(strict=True) in 06-01, xfail removed in 06-04 | `uv run pytest tests/test_gateway.py -k "forwards_attachments" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / LOW-9 | parse_inbound normalizes provider `from` field with `email.utils.parseaddr` to strip display names before populating InboundEmail.from_addr; `"HR Dept <hr@acme.test>"` → `"hr@acme.test"`; find_business_by_sender gets a bare address | Display-name from_addr causes exact-match miss (Integrity) | parseaddr called on data["data"]["from"] in Path A (Resend envelope); bare address used for InboundEmail.from_addr | unit (xfail(strict=True) in 06-01 test_parse_inbound_parseaddr_display_name; xfail removed in 06-04 when parseaddr implemented) | `uv run pytest tests/test_gateway.py -k "parseaddr_display_name" -x -q` | ❌ W0 | ⬜ pending |
| OPS-02 / MEDIUM-5 | reply-routing integration: a real outbound row inserted in DB with synthetic message_id; inbound reply POST with matching In-Reply-To; `find_awaiting_reply_for_header` NOT monkeypatched; SQL predicate and header normalization exercised end-to-end | Mocked router hides predicate bugs (Integrity) | test_inbound_reply_routes_to_correct_run_integration: real DB insert + FakeConnection or live-DB; reply POST exercises `_HEADER_MATCH_PREDICATE` and `_pad_references()` without monkeypatching the matcher | integration (xfail(strict=True) in 06-01; removed when integration suite passes) | `uv run pytest tests/test_gateway.py -k "reply_routes_to_correct_run_integration" -m integration -x` | ❌ W0 | ⬜ pending |
| OPS-02 / HIGH-4 / D-17 | Unsigned canonical InboundEmail-shaped POST (no data.email_id, no svix-* headers) → route returns 400 when ALLOW_UNSIGNED_FIXTURES=False (prod default); same POST → 200 when ALLOW_UNSIGNED_FIXTURES=True (dev mode) | Unsigned canonical bypass — prod forgery hole (Spoofing) | HIGH-4 + MEDIUM-4: allow_unsigned_fixtures=False rejects ALL unsigned POSTs regardless of shape — this is the ONLY consistent statement (no "canonical works unsigned" in prod); tests test_allow_unsigned_fixtures_canonical_shape_prod_default_returns_400 and test_allow_unsigned_fixtures_canonical_shape_dev_mode_returns_200 lock both paths | unit (no xfail — GREEN from 06-04 Task 2) | `uv run pytest tests/test_gateway.py -k "canonical_shape" -x -q` | ❌ W3 | ⬜ pending |
| OPS-03 / MEDIUM-7 | keepalive.yml fails the workflow job on missing RENDER_URL secret or non-2xx curl response (curl -f without `\|\| echo` swallow) | Silent-green keepalive hides infra failure (Availability) | RENDER_URL is checked before curl (exit 1 if missing); curl uses -f flag (fail on non-2xx); no `\|\| echo` fallback; OPS-03 goes red when the ping actually fails | unit (grep on keepalive.yml) | `grep "curl -f" .github/workflows/keepalive.yml && grep -c "|| echo" .github/workflows/keepalive.yml \| grep "^0$"` | ❌ W1 | ⬜ pending |
| OPS-02 / MEDIUM-5 (R3) | resend==2.32.2 SDK call surfaces exist with the assumed names: resend.Webhooks.verify, resend.EmailsReceiving.get, resend.Emails.send (accepting headers= and attachments=) — Python SDK naming mismatch caught at Wave 0 not at the live gate | SDK naming mismatch discovered at live human gate (Availability) | test_resend_sdk_call_surfaces_exist: hasattr checks on resend.Webhooks.verify, resend.EmailsReceiving.get, resend.Emails.send; inspect.signature check for headers/attachments param or **kwargs on send; no xfail, GREEN immediately at Wave 0 (added in 06-04 Task 0) | unit (pure import+attribute check, no network) | `uv run pytest tests/test_gateway.py::test_resend_sdk_call_surfaces_exist -v` | ❌ W0 (Task 0) | ⬜ pending |
| OPS-04 / HIGH-1 (R4) / HIGH-2 | Per-fixture business routing: every _DEMO_FIXTURES entry has business_name; demo_send_test resolves from_addr from the STABLE _SEED_CONTACTS constant map (NOT a DB contact_email lookup, NOT a global DEMO_CONTACT_EMAIL override) — seed .example contacts are permanently stable (06-08: never mutated), so coastal_exact always routes to Coastal Cleaning Co. and unknown_shorthand_metro always routes to Metro Deli Group, independent of any demo_sender_bindings state; demo_reset.py re-arms the demo_sender_bindings row after seed() (NOT contact_email) | Demo misroutes Coastal body to Metro business (or vice versa) on camera (Integrity) | test_demo_send_test_coastal_routes_to_coastal: _SEED_CONTACTS → Coastal seed contact; POST coastal_exact; assert create_run/routing resolves to Coastal. test_demo_send_test_metro_unknown_shorthand_routes_to_metro: POST unknown_shorthand_metro; assert routes to Metro. test_demo_reset_rearming_writes_demo_sender_bindings_not_contact_email: FakeConn asserts demo_sender_bindings UPSERT present AND no "UPDATE businesses" SQL | unit (no xfail — GREEN from 06-06 Task 1) | `uv run pytest -k "demo_send_test_coastal_routes or demo_send_test_metro_unknown or demo_reset_rearming" -v` | ❌ W5 | ⬜ pending |
| OPS-02 / HIGH-1 (R3 waive) | email_messages schema has NO provider_message_id column and NO updated_at column; repo helpers set ONLY send_state in UPDATE SQL; no runtime column-mismatch failure on live send path | Live send path crashes on UPDATE with nonexistent column (Availability) | grep schema.sql confirms provider_message_id ABSENT and updated_at ABSENT from email_messages; grep repo.py confirms no provider_message_id in email_messages UPDATE SQL; schema.sql in 06-04 files_modified to make this confirmation executable | unit (grep-based schema assertion in done criterion) | `grep "provider_message_id" app/db/schema.sql | wc -l | grep "^0$" && grep "updated_at" app/db/schema.sql | grep "email_messages" | wc -l | grep "^0$"` | ✅ schema confirmed (schema.sql read in 06-04) | ⬜ pending |

| OPS-04 / OPS-02 / CHANGE-5 / HIGH-2 | POST /demo/bind UPSERTs ONE row into demo_sender_bindings (operator_email PK → business_id); single-owner via ON CONFLICT (operator_email); NEVER mutates businesses.contact_email (seed .example contacts permanently stable). find_business_by_sender resolves the operator email via the additive fallback (primary contact_email match None → demo_sender_bindings lookup). Compose routes by business_id, so binding never affects recruiter Path-1 | Demo identity mutating canonical contacts → Path-1 misroute / spoof-guard break (Integrity) | bind_demo_business writes demo_sender_bindings only; test_bind_demo_business_writes_binding_table_not_businesses asserts the UPSERT present AND fake_conn.executed contains no "UPDATE businesses"/"contact_email" SQL; done-criterion `grep -c "UPDATE businesses" app/db/repo.py \| grep "^0$"` | unit (FakeConn, no xfail — GREEN from 06-08 Task 1) | `uv run pytest tests/test_demo_landing.py -k "bind" -v` | ❌ W3 | ⬜ pending |
| OPS-04 / OPS-02 / CHANGE-7 | POST /demo/compose is STATELESS per submit: takes the selected business_name (validated against _SEED_CONTACTS), sets from_addr = _SEED_CONTACTS[business_name] (the selected business's OWN seed .example contact — NOT pjnhek@gmail.com), mints fresh synthetic Message-ID, fires REAL pipeline via BackgroundTasks.add_task(_run_pipeline), redirects to /runs/{run_id}; unknown business_name → redirect to / with no run; does NOT read or mutate the global /demo/bind binding | Composer stamps the operator email or an arbitrary sender → misroute (Integrity); SSRF via body text (Tampering) | from_addr is allowlist-resolved (_SEED_CONTACTS[validated business_name], not user-supplied); body goes to body_text only (no file I/O); test_compose_stamps_selected_business_seed_sender + test_compose_route_redirects_to_run + test_compose_rejects_unknown_business + test_compose_body_ssrf_path_not_loaded | unit (no xfail — GREEN from 06-08 Task 2) | `uv run pytest tests/test_demo_landing.py -k "compose" -v` | ❌ W3 | ⬜ pending |
| OPS-04 / OPS-02 / T-06-08-06 | Concurrency isolation: two recruiter composes for DIFFERENT businesses route independently via their own seed senders; /demo/compose never calls repo.bind_demo_business; no shared mutable binding is read or written in the compose path → simultaneous visitors (or a visitor mid-operator-recording) cannot stomp each other's routing | Temporal stomp: concurrent composes share global binding state and misroute runs (Integrity) | Compose stamps the in-code seed sender per submit and resolves via find_business_by_sender on that address — bind-free; test_compose_two_businesses_route_independently_no_binding_mutation asserts distinct senders (hr@metrodeli.example vs finance@summittech.example) and repo.bind_demo_business NEVER called | unit (no xfail — GREEN from 06-08 Task 2) | `uv run pytest tests/test_demo_landing.py -k "route_independently" -v` | ❌ W3 | ⬜ pending |
| OPS-04 / CHANGE-8 | /runs/{id} thread view renders all email_messages rows (inbound + outbound) in chronological order; load_thread_messages helper returns both directions; test_run_detail_injects_thread_messages asserts INBOUND direction label and body_text excerpt in response | Thread view missing inbound rows — recruiter sees only half the conversation (Integrity/UX) | load_thread_messages: explicit column list, both directions, ORDER BY created_at ASC; run_detail route injects thread_messages; template renders direction + body excerpt | unit (no xfail — GREEN from 06-08 Task 2) | `uv run pytest tests/test_demo_landing.py -k "thread_messages" -v` | ❌ W3 | ⬜ pending |
| OPS-04 / CHANGE-11 | Alias-rationale notes rendered for source=="alias" resolutions: "Resolved 'Dave Perez' to David Perez (known nickname from a prior confirmed run)"; source=="exact" produces no note; misspelling-always-clarifies rule unchanged; NO pipeline/decision logic touched | Alias rationale accidentally changes decision logic or surfaces for exact matches (Integrity) | _build_alias_rationale_notes reads run['decision']['resolutions'] JSONB, filters source=='alias', constructs string; exception-safe → []; test_run_detail_alias_rationale_rendered + test_run_detail_alias_rationale_absent_for_exact; decide.py not in files_modified | unit (no xfail — GREEN from 06-08 Task 2) | `uv run pytest tests/test_demo_landing.py -k "alias_rationale" -v` | ❌ W3 | ⬜ pending |

| OPS-02 / OPS-04 / HIGH-2 (demo-round-2) / T-06-08-09 | record_only early return in _clarify MUST NOT skip alias-candidate capture: in _clarify, repo.set_alias_candidates (D-04 alias capture) runs BEFORE the record_only branch; the record_only check is placed AT the gateway.send_outbound call site (after alias capture + body composition); a record_only clarification: (a) calls repo.set_alias_candidates AND (b) writes a clarification outbound email_messages row WITHOUT calling resend.Emails.send; then simulate-reply → approve → re-compose same shorthand shows NO second clarification (alias learned) — Beat 3 works on in-app path | record_only early return before alias capture silently breaks Beat 3 for all in-app (record_only=True) runs (Integrity) | test_orchestrator_record_only_clarify_skips_resend_but_captures_alias: mock gateway.send_outbound (assert NOT called); mock repo.get_record_only_flag → True; mock repo.set_alias_candidates to capture calls; set decision.unresolved_names = ["Dave"] with 0 candidate_ids; call _clarify; assert gateway.send_outbound NOT called; assert repo.set_alias_candidates WAS called (capture ran before branch); assert repo.insert_email_message called with purpose="clarification"; assert repo.set_status called with AWAITING_REPLY | unit (no xfail — GREEN from 06-08 Task 2) | `uv run pytest tests/test_demo_landing.py -k "skips_resend_but_captures_alias" -v` | ❌ W3 | ⬜ pending |
| OPS-04 / LOW-6 (demo-round-2) | repo.create_run(record_only=True) passed DIRECTLY from /demo/compose — no separate set_record_only UPDATE call; create_run INSERT includes record_only column in the VALUES tuple; existing callers get record_only=False default (no regression) | Separate set_record_only UPDATE is an extra round-trip and a race window (Integrity/Performance) | test_create_run_record_only_true: FakeConn; call create_run(business_id=uuid4(), source_email_id=None, record_only=True, conn=fake_conn); assert fake_conn.executed contains "record_only" in INSERT SQL and True in params; test_compose_sets_record_only_via_create_run: POST /demo/compose; assert repo.create_run called with record_only=True (not via a separate set_record_only spy call) | unit (no xfail — GREEN from 06-08 Task 1+2) | `uv run pytest tests/test_demo_landing.py -k "record_only_true or record_only_via_create_run" -v` | ❌ W3 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Highest-Risk Units (priority test targets)

1. **Inbound dedup before pipeline (D-13) — THE highest-value fix.** A duplicate at-least-once delivery that re-runs `decide.py` is exactly the false-process the thesis forbids, and it is invisible in the stub world. Test: two webhook deliveries with the same RFC `message_id` → the pipeline/`decide.py` is invoked once; the second is an insert-or-skip no-op. HIGH-4 makes this EXPLICIT in the route: enqueue only if the ON CONFLICT insert returned a row.
2. **Signature reject (D-17).** A forged POST to the public money-themed webhook must be refused at step zero. Test: mock `resend.Webhooks.verify` to raise `ValueError` → assert HTTP 400 and that NO extraction/pipeline code ran. HIGH-2 dual-path: verify only fires for Resend-envelope payloads; canonical fixture POSTs are not verified.
3. **Fixture/dev path survival (HIGH-2).** A canonical InboundEmail JSON POST with no svix headers must succeed in dev mode (ALLOW_UNSIGNED_FIXTURES=True). test_parse_inbound_canonical_fixture_still_works must be GREEN from 06-01 onward (no xfail; GREEN immediately). In prod (ALLOW_UNSIGNED_FIXTURES=False), unsigned canonical → 400 — the ONLY consistent statement (MEDIUM-4).
4. **Send-failure routing (HIGH-3).** A provider exception in send_outbound must flip the reserved row to 'failed' and re-raise. Runs must be retriggerable. test_send_outbound_failed_on_provider_exception asserts this.
5. **D-14 reply-routing end-to-end (MEDIUM-7).** Not just chain-construction — an inbound reply with matching In-Reply-To resumes the CORRECT run. test_inbound_reply_routes_to_correct_run asserts the BackgroundTasks.add_task call with the matched run_id.
6. **Durable threading from persisted state (D-14).** Building outbound headers off "the last webhook I saw" lets a dropped/duplicated delivery corrupt threading. Test: with a persisted References chain row, `send_outbound` rebuilds `In-Reply-To`/`References` from the DB row.
7. **No-op-swap invariant.** The 422-test mocked suite green after the gateway swap is the structural proof that the provider wiring touched only `gateway.py`.
8. **SDK call-surface smoke check (MEDIUM-5 R3).** resend.Webhooks.verify, resend.EmailsReceiving.get, resend.Emails.send exist with assumed names on resend 2.32.2 — catch naming mismatch at Wave 0. test_resend_sdk_call_surfaces_exist must be GREEN (no xfail) before any gateway implementation starts.
9. **Multi-business demo routing (HIGH-1 R4 / HIGH-2).** The recording spans 3 businesses. Seed .example contacts are permanently stable (06-08 never mutates them), so coastal_exact MUST always route to Coastal and unknown_shorthand_metro to Metro — independent of the operator demo_sender_bindings state. test_demo_send_test_coastal_routes_to_coastal + test_demo_send_test_metro_unknown_shorthand_routes_to_metro prove per-fixture isolation via the stable _SEED_CONTACTS map. Both GREEN from 06-06 Task 1 (no xfail).
10. **Reply-To topology (Pass-6).** Because From=onboarding@resend.dev cannot serve as a routing anchor, the Reply-To header on every outbound is the SOLE mechanism ensuring the physical reply reaches the inbound webhook address. test_send_outbound_includes_reply_to_when_configured and test_send_outbound_omits_reply_to_when_not_configured lock both paths. Both GREEN from 06-04 Task 1 (no xfail).

---

## Wave 0 Requirements

- [ ] `uv add resend==2.32.2` — adds the only new runtime package (official resendlabs org; self-contained `Webhooks.verify`, no `svix` dep)
- [ ] `tests/test_gateway.py` NEW (no xfail, Wave 0): `test_resend_sdk_call_surfaces_exist` — hasattr/inspect check that resend.Webhooks.verify, resend.EmailsReceiving.get, resend.Emails.send exist on the installed package; GREEN from Wave 0 before any gateway implementation (MEDIUM-5 R3 SDK smoke check — 06-04 Task 0)
- [ ] `tests/test_gateway.py` — all Phase-6-behavior tests marked `@pytest.mark.xfail(strict=True, reason="implemented in 06-04")` EXCEPT `test_parse_inbound_canonical_fixture_still_works` (which must be GREEN immediately with no xfail). Xfail tests: two-step parse, signature-reject, threading/reserved, failed-on-exception, reply-routing-e2e.
- [ ] `tests/test_gateway.py` NEW (no xfail): `test_parse_inbound_canonical_fixture_still_works` — canonical InboundEmail dict passes parse_inbound in the current stub gateway. GREEN from Wave 0.
- [ ] `tests/test_ingest.py` — **EXTEND** with TWO dedup tests: `test_duplicate_delivery_pipeline_runs_once_unit` (mocked, no integration marker, runs in default `uv run pytest -q -m "not integration and not live_llm"`) + `test_duplicate_delivery_pipeline_runs_once` (`@pytest.mark.integration`, live DB). Neither needs xfail.
- [ ] `tests/test_dashboard.py` — **EXTEND** with liveness (xfail reason: "implemented in 06-02") and readiness (xfail, integration) health-route tests
- [ ] `tests/test_readme.py` *(optional)* — assert the locked disclaimer strings are present verbatim in README.md
- [ ] `tests/conftest.py` — extend shared fixtures (mock Resend SDK seams; a received-email fixture with realistic mixed-case headers)

---

## Manual-Only Verifications

> These are the behaviors fixtures structurally cannot test — the BLOCKING human checkpoints (D-09, D-09b)
> and the recording. They retire the ⚠ CONFIRM assumptions (A1, A4, A5, A6) from `06-RESEARCH.md`.

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Thin Render deploy serves dashboard + liveness route, binds `0.0.0.0:$PORT`, reaches Supabase via the pooler, survives a cold-start wake | OPS-01 / D-08 / D-09 | Live Render account creation + secret injection need a human; cold-start is real-infra behavior | Human executes the credentialed deploy from prepared artifacts; confirm the service serves, reaches Supabase, and wakes from spin-down |
| Local pooler pre-check: connect from laptop over 6543 (prepared statements off) and apply `schema.sql` + seed over the 5432 session pooler | OPS-01 / D-08a / D-15 | Isolates the IPv4/pooler/prepared-statement failure class from the container/`$PORT`/cold-start class; needs live Supabase creds | Run the prepared connection + migrate/seed scripts against the fresh project; confirm which port bootstrap needs (⚠ A4) |
| Real email round-trip (HIGH-2 identity + HIGH-1 A5 + LOW-8 + MEDIUM-2/MEDIUM-3 A5 gate + Pass-6 address topology): (a) production demo identity set — one business contact_email updated to real sender before round-trip; (b) Resend inbound webhook created AFTER Render service exists (06-03); (c) RESEND_REPLY_TO confirmed in Render env = payroll@jiodnel.resend.app; (d) send FROM personal email (Resend account-owner) TO payroll@jiodnel.resend.app → outbound email arrives FROM onboarding@resend.dev WITH Reply-To: payroll@jiodnel.resend.app; (e) press Reply in email client → reply goes TO payroll@jiodnel.resend.app (via Reply-To) → Resend fires /webhook/inbound again; (f) A5 GATE: reply In-Reply-To == stored synthetic message_id (PASSED) OR D-03a subject-token fallback complete (FAILED + Branch B); A5 is MORE load-bearing here (From address unreachable as fallback anchor); A5 evidence observed from INBOUND reply's In-Reply-To header PRIMARY source = DB SELECT in_reply_to WHERE direction='inbound' | OPS-02 / D-09b / HIGH-1 / HIGH-2 / LOW-8 / MEDIUM-2 / MEDIUM-3 / Pass-6 | Real sender-client header survival; find_business_by_sender exact-match requires real email in DB (HIGH-2); A5 gate is a blocking conditional — 06-06 only unblocked after PASSED or D-03a subject-token remediation; address topology (Reply-To is the SOLE mechanism routing physical replies to inbound webhook) cannot be tested in fixtures | Human: (0) update businesses.contact_email to real sender email + confirm DEMO_CONTACT_EMAIL and DEMO_BUSINESS_NAME in Render env; (1) create Resend webhook AFTER 06-03 Render deploy (URL required); (2) confirm RESEND_REPLY_TO = payroll@jiodnel.resend.app in Render env; (3) enable LOG_WEBHOOK_DEBUG_IDS=true; (4) send personal email TO payroll@jiodnel.resend.app; (5) confirm Reply-To in outbound email = payroll@jiodnel.resend.app; (6) reply to outbound (verify TO is payroll@jiodnel.resend.app not onboarding@resend.dev); (7) check WEBHOOK_DEBUG INBOUND log (A1); (8) MANDATORY A5 gate: compare INBOUND reply In-Reply-To (DB query) to DB message_id; (9) confirm email_id vs rfc_message_id (A6); (10) confirm all three threading headers present (LOW-8); (11) if A5 FAILED: implement D-03a subject-token (embed [#token] in clarification subject + subject-token router path) and re-verify |
| HIGH-1 multi-business routing smoke test before recording: BOTH a Coastal compose routes to Coastal Cleaning Co. AND a Metro "Dave Reyes" compose routes to Metro Deli Group (R4 — /demo/compose business_id routing via _SEED_BUSINESS_IDS, stable _SEED_CONTACTS senders) | OPS-04 / HIGH-1 (R4) | Confirms /demo/compose routing works for all recording beats, not just the hero | On /demo/compose: select Coastal, compose, confirm a run is created for Coastal Cleaning Co. Select Metro, compose "Dave Reyes 40h", confirm a run for Metro Deli Group that clarifies. If either misroutes → STOP, fix the _SEED_BUSINESS_IDS/_SEED_CONTACTS mapping, then re-test before recording |
| Demo-reset re-arms beat 2: clearing learned aliases + run rows makes a second take clarify again | OPS-04 / D-07 | Cross-run/cross-take state; the learned alias persists to prod and poisons a repeat take | Run `uv run python scripts/demo_reset.py --confirm` (explicit flag required); re-run the unknown_shorthand_metro fixture → confirm it clarifies again |
| Architecture diagram renders in GitHub README (Mermaid source + committed SVG **and** PNG — D-11a mandatory; Mermaid-only NOT sufficient) | OPS-04 / D-11a | GitHub Mermaid/SVG render fidelity is visual; SVG embedding is inconsistent | View the README on GitHub; confirm the Mermaid block renders AND the committed PNG (docs/architecture.png) displays as a fallback; diagram shows pipeline stages + two pause states + the `decide.py` code gate |
| 60–90s demo recording exists and shows all three beats + the eval closing shot; recording is via /demo/compose (recruiter self-serve, record_only=True — no real SMTP during the take); Beat 2 uses Metro Deli Group compose with 'Dave Reyes' unknown shorthand (in-page reply via simulate-reply seam) | OPS-04 / D-04 / D-06 / MEDIUM-3 (demo-round-2) | The hero artifact; recorded via /demo/compose (record_only=True); Path-2 real-email clip is a separate supporting asset (from 06-05 gate); pre-recording smoke test checks /demo/compose routing for both Coastal and Metro via _SEED_BUSINESS_IDS before recording starts | Confirm the recording (all beats via /demo/compose): (1) clean Coastal compose→approve→deliver; (2) Metro 'Dave Reyes' compose→clarify→in-page-reply; (3) Metro re-compose same shorthand resolves without clarification (alias learned); + ~5–10s eval view (`false_process_count=0`) |
| Eval chart served as a committed STATIC asset (not a runtime regen on the ephemeral FS) | OPS-04 / D-21 | Ephemeral FS; a regen would 500 on the dyno | On the deployed service, hit `/eval/chart.svg`; confirm it serves the committed `eval/chart.svg` baked into the image |

---
