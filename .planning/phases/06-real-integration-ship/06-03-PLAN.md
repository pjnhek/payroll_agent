---
phase: "06"
plan: "03"
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
autonomous: false
files_modified: []
requirements:
  - OPS-01
  - OPS-02

user_setup:
  - service: supabase
    why: "Production Postgres for the deployed app (D-09a)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Dashboard → Project Settings → Database → Connection Pooler → Transaction mode (port 6543) — format: postgresql://postgres.{ref}:{password}@aws-{region}.pooler.supabase.com:6543/postgres?sslmode=require"
    dashboard_config:
      - task: "Create a new Supabase project"
        location: "app.supabase.com → New Project"
      - task: "Note the project ref, region, and database password"
        location: "Supabase Dashboard → Project Settings → General"
  - service: render
    why: "Free web service hosting the FastAPI app (D-09)"
    env_vars:
      - name: DATABASE_URL
        source: "Supabase pooler URL (see above) — set as Render env var (NOT in render.yaml)"
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys (will be filled at 06-05 after account creation)"
      - name: WEBHOOK_SIGNING_SECRET
        source: "Resend Dashboard → Webhooks → Signing Secret (filled at 06-05)"
      - name: EXTRACTION_API_KEY
        source: "DeepSeek console API key"
      - name: DRAFT_API_KEY
        source: "Moonshot/Kimi console API key"
    dashboard_config:
      - task: "Create a new Render Web Service from the GitHub repo, runtime=Docker"
        location: "dashboard.render.com → New → Web Service → Connect GitHub repo"
      - task: "Set all 5 sync:false env vars from render.yaml as Render environment variables"
        location: "Render service dashboard → Environment tab"
      - task: "Set RENDER_URL as a GitHub Actions secret (the deployed Render URL)"
        location: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret"

must_haves:
  truths:
    - "Local pooler pre-check: psycopg3 connects to Supabase over 6543 from the laptop (prepare_threshold=None already set)"
    - "Schema and seed apply cleanly via bootstrap using session pooler (port 5432)"
    - "Thin Render container serves GET / (dashboard) and GET /health/live → 200 from the public URL"
    - "Render service reaches Supabase via the 6543 pooler (GET /health/ready → 200)"
    - "Service survives a cold-start wake (spin-down + wait + curl → 200)"
    - "Three threading headers confirmed on manual raw payload logging (⚠ A1, A4, A5, A6 from RESEARCH.md resolved or documented)"
    - "No secrets visible in render.yaml committed to git (all sync:false)"
  artifacts:
    - path: ".env"
      provides: "Local .env with production Supabase DATABASE_URL (not committed)"
      contains: "pooler.supabase.com:6543"
  key_links:
    - from: "Render service"
      to: "Supabase pooler port 6543"
      via: "psycopg pool with prepare_threshold=None"
      pattern: "pooler\\.supabase\\.com:6543"
---

<objective>
Wave 2 — BLOCKING human checkpoint (autonomous:false). The agent has prepared all artifacts; the human executes the credentialed deploy and confirms the thin stack works before Resend is wired.

This checkpoint implements D-08a (local pooler pre-check from laptop) and D-09/D-09a (thin Render deploy + Supabase standup). Two distinct failure domains are isolated: the IPv4/pooler/prepared-statement class (D-08a, local) and the container/$PORT/cold-start class (D-09, Render).

Purpose: Every prior phase with a real external dependency had a BLOCKING human gate (live-DB in Phase 2, live-LLM in Phase 2). Phase 6 has two — this one for the deploy, and 06-05 for the email round-trip. The agent cannot create Supabase projects, inject Render secrets, or confirm a live cold-start wake. A half-finished agent-driven deploy is worse than a clean handoff (D-09).

Output: A confirmed, running Render free service that serves the dashboard and liveness route, reaches Supabase via the pooler, and survives a cold-start wake. The ⚠ CONFIRM flags A4 (bootstrap 6543 vs 5432) are resolved by observation during this gate. No Resend wiring yet.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/db/bootstrap.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/seed.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/supabase.py
@/Users/pnhek/usf msds/github/payroll_agent/render.yaml
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-08a — Local Supabase pooler pre-check from laptop</name>
  <what-built>All deploy artifacts (Dockerfile, render.yaml, keepalive.yml, health routes, config) are committed from 06-01 and 06-02. This step proves the Supabase pooler works from the laptop BEFORE the Render deploy attempt — isolating failure classes (per D-08a).</what-built>
  <how-to-verify>
    STEP 1: Create a fresh Supabase project at app.supabase.com. Note the project ref, region, and database password.

    STEP 2: Obtain TWO connection URLs:
    - RUNTIME URL (6543 transaction mode): `postgresql://postgres.{ref}:{password}@aws-{region}.pooler.supabase.com:6543/postgres?sslmode=require`
    - SESSION URL (5432 session mode, for schema/seed only): same host, port 5432 instead of 6543

    STEP 3: Set DATABASE_URL in your local .env to the RUNTIME URL (6543). Then test the pool:
    ```bash
    uv run python -c "
    from app.db.supabase import get_connection
    with get_connection() as conn:
        row = conn.execute('SELECT 1 AS ok').fetchone()
        print('Pool OK:', row)
    "
    ```
    Expected: `Pool OK: (1,)` — confirms prepare_threshold=None + pooler works.

    STEP 4: Apply schema and seed using the SESSION URL (5432). Temporarily edit .env DATABASE_URL to the 5432 URL OR pass it inline:
    ```bash
    DATABASE_URL="postgresql://postgres.{ref}:{password}@aws-{region}.pooler.supabase.com:5432/postgres?sslmode=require" \
      uv run python -m app.db.bootstrap --reset
    DATABASE_URL="..." uv run python -m app.db.seed
    ```
    If bootstrap works on 6543 too (⚠ A4 confirm), note it. The D-15 guidance says prefer 5432 for DDL.

    STEP 5: Restore .env DATABASE_URL to the 6543 runtime URL. Run integration tests:
    ```bash
    uv run pytest -m integration -x -q
    ```
    These will skip without live DB creds but should now have them. Note any failures.

    CONFIRM per RESEARCH.md assumptions:
    - A4: Did bootstrap work on 6543 or did you need 5432? Document in your reply.

    NOTE (A6 — webhook data.message_id shape): A6 (whether `data.message_id` in the Resend webhook payload is the RFC Message-ID) is confirmed at 06-05 (the email round-trip gate) by logging the raw webhook payload. The 06-04 dedup keys on the RFC message_id from the full fetch (Step 2 of parse_inbound), which is correct regardless of whether `data.message_id` is also the RFC ID or the Resend internal email_id — the full fetch is the authoritative source. No pre-fetch dedup is attempted; the ordering is safe for either A6 outcome.
  </how-to-verify>
  <resume-signal>Type "pooler-ok" with your A4 observation (e.g., "pooler-ok: bootstrap works on 6543" or "pooler-ok: bootstrap needed 5432")</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: D-09/D-09a — Thin Render deploy + cold-start confirmation</name>
  <what-built>After Task 1 confirms local pooler connectivity, deploy the thin container to Render. RESEND_API_KEY and WEBHOOK_SIGNING_SECRET can be placeholder values for now — the thin deploy only needs DATABASE_URL + LLM keys to serve the dashboard.</what-built>
  <how-to-verify>
    STEP 1: Push the current branch to GitHub (06-01 and 06-02 commits must be on the pushed branch or master).

    STEP 2: Create a new Render Web Service at dashboard.render.com:
    - Connect the GitHub repo
    - Runtime: Docker (Render will use the Dockerfile)
    - Instance type: Free

    STEP 3: Set environment variables in Render dashboard (all 5 sync:false vars from render.yaml):
    - DATABASE_URL = Supabase 6543 pooler URL (runtime URL from Task 1)
    - RESEND_API_KEY = placeholder (e.g., "PLACEHOLDER_SET_AT_06-05")
    - WEBHOOK_SIGNING_SECRET = placeholder (e.g., "PLACEHOLDER_SET_AT_06-05")
    - EXTRACTION_API_KEY = your DeepSeek API key
    - DRAFT_API_KEY = your Moonshot/Kimi API key

    STEP 4: Wait for the first deploy to complete. Check Render logs for:
    - `Application startup complete` from uvicorn
    - No `$PORT` literal (shell form CMD must expand it)
    - No `database not ready` errors

    STEP 5: Test the deployed service:
    ```bash
    # Replace with your actual Render URL
    export RENDER_URL="https://payroll-agent-XXXX.onrender.com"
    curl -s "$RENDER_URL/health/live"           # expected: {"status":"ok"}
    curl -s "$RENDER_URL/health/ready"          # expected: {"status":"ready"}
    curl -s "$RENDER_URL/"                      # expected: runs list HTML
    ```

    STEP 6: Test cold-start wake:
    ```bash
    # Wait 16+ minutes (past the 15-min spin-down threshold) or use Render dashboard to manually stop/start
    curl -s --max-time 90 "$RENDER_URL/health/live"  # must return 200 within 60s
    ```

    STEP 7: Set RENDER_URL as a GitHub Actions secret:
    - GitHub repo → Settings → Secrets → Actions → New secret: RENDER_URL = {your Render URL}

    CONFIRM (document in reply):
    - The Render URL where the service is running
    - Whether /health/ready returned 200 (Supabase pool works from Render)
    - Whether cold-start wake succeeded within 60 seconds
  </how-to-verify>
  <resume-signal>Type "deploy-ok: {render-url}" — e.g., "deploy-ok: https://payroll-agent-abc.onrender.com". If cold-start failed, describe the error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Laptop → Supabase pooler 6543 | psycopg3 TLS connection; prepare_threshold=None already set |
| Render → Supabase pooler 6543 | Same pool config; IPv4-reachable via pooler (direct host is IPv6-only) |
| Render env vars | Secrets injected at runtime; never committed to git; sync:false in render.yaml |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Information Disclosure | DATABASE_URL in Render env | mitigate | sync:false in render.yaml (never committed); Render encrypts env vars at rest |
| T-06-03-02 | Spoofing | Supabase direct host (db.{ref}.supabase.co) | mitigate | Use pooler host only (pooler.supabase.com); direct host is IPv6-only → connection fails from Render IPv4, so misconfig is self-revealing |
| T-06-03-03 | Availability | Render spin-down during live demo | accept | Documented in README per D-11 ("may take ~30s to wake"); pre-warm 30-60s before recording (D-05); keep-alive reduces frequency |
| T-06-03-04 | Tampering | Schema/seed over 6543 vs 5432 (⚠ A4) | mitigate | D-15 says prefer session pooler (5432) for DDL; human confirms A4 during this gate and documents finding |
| T-06-03-SC | Tampering | GitHub Actions RENDER_URL secret | accept | URL is public (not sensitive); secret avoids committing and allows update without code change |
</threat_model>

<verification>
Human confirms in resume-signal:
1. "pooler-ok" — local psycopg3 pool connects to Supabase 6543 with prepare_threshold=None
2. "deploy-ok: {url}" — Render service URL where the thin deploy runs
3. /health/live → 200 (liveness probe works, no DB needed)
4. /health/ready → 200 (readiness probe works, Supabase reachable from Render via 6543)
5. Cold-start wake: curl returns 200 within 60s after spin-down
6. A4 observation documented (bootstrap works on 6543 or needed 5432)
</verification>

<success_criteria>
- Production Supabase project created, schema + seed applied, pool connectivity confirmed from laptop (D-08a)
- Thin Render free service deployed, serving dashboard + health routes, reaching Supabase via pooler (D-09)
- Cold-start wake confirmed (Render stays alive after sleep)
- RENDER_URL set as GitHub Actions secret (keepalive.yml ready to use)
- No Resend wiring yet — that is 06-04. This gate only proves the infrastructure stack.
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-03-SUMMARY.md` when done, recording the Render URL and A4/cold-start findings.
</output>
