---
phase: "06"
plan: "05"
type: execute
wave: 4
depends_on:
  - "06-04"
autonomous: false
files_modified: []
requirements:
  - OPS-02

user_setup:
  - service: resend
    why: "Real inbound email provider for send→reply→approve round-trip (D-09b)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys → Create API Key (or use existing)"
      - name: WEBHOOK_SIGNING_SECRET
        source: "Resend Dashboard → Webhooks → Add Endpoint → Signing Secret (shown after creation)"
    dashboard_config:
      - task: "Create or verify a Resend account and create an API key"
        location: "resend.com → Sign in → API Keys"
      - task: "Configure inbound email: add or verify a domain (or use the auto-created .resend.app address for testing)"
        location: "Resend Dashboard → Domains (or use the free shared inbound test address)"
      - task: "Add the webhook endpoint pointing to your Render URL"
        location: "Resend Dashboard → Webhooks → Add Endpoint → URL: https://{render-url}/webhook/inbound → select event: email.received → copy the Signing Secret"
      - task: "Update the Render service env vars: set RESEND_API_KEY and WEBHOOK_SIGNING_SECRET to the real values (replacing the placeholders set in 06-03)"
        location: "Render service dashboard → Environment tab → edit RESEND_API_KEY and WEBHOOK_SIGNING_SECRET"

must_haves:
  truths:
    - "Production demo identity is set up: one seeded business's contact_email is updated to the real sender address used for the round-trip (and the live demo), so find_business_by_sender returns a match for the real inbound email — HIGH-2; this step is a named pre-requisite before the round-trip test"
    - "A real email sent to the Resend inbound address triggers a POST to /webhook/inbound on the deployed service"
    - "Signature verification passes for a real Resend webhook delivery (signing secret confirmed real, not placeholder)"
    - "The three threading headers (Message-ID, In-Reply-To, References) survive the send→reply round-trip"
    - "raw email_obj HEADER KEYS (not body, not PII) are logged by the LOG_WEBHOOK_DEBUG_IDS flag inside gateway.py, capturing real provider header casing for ⚠ A1"
    - "data.message_id vs data.email_id in the raw webhook payload is confirmed (⚠ A6)"
    - "A5 gate (MEDIUM-3 — explicit conditional, not just documented): the operator checks whether the reply's In-Reply-To header equals the synthetic message_id stored in email_messages.message_id; this is a BLOCKING conditional branch — if A5 PASSES (ids match), 06-06 is unblocked; if A5 FAILS (Resend overwrote the header), an explicit A5-remediation mini-step runs BEFORE 06-06 proceeds"
    - "A5-remediation path (only if A5 fails): store Resend's returned outbound RFC Message-ID in email_messages.message_id instead of the synthetic id, re-point reply routing to the real RFC id, re-run the round-trip to verify the new anchor works; only after re-verification does 06-06 proceed"
    - "The full pipeline runs end-to-end on the deployed service for one real inbound email"
    - "LOG_WEBHOOK_DEBUG_IDS env flag is already committed in gateway.py (06-04 Task 1) and must be enabled temporarily in the Render env to capture real header casing (removed in 06-06)"
    - "Three threading headers (Message-ID, In-Reply-To, References) are confirmed present on the real round-trip: the WEBHOOK_DEBUG log shows all three key names in header_keys; the reply In-Reply-To header matches the stored synthetic message_id — LOW-8 evidence collected here (not at the thin-deploy gate in 06-03)"
  artifacts: []
  key_links:
    - from: "Real email client (user's personal email)"
      to: "Resend inbound webhook → /webhook/inbound on Render"
      via: "Resend email.received event → POST to webhook endpoint"
      pattern: "email\\.received"
---

<objective>
Wave 4 — BLOCKING human checkpoint (autonomous:false). The email round-trip verify gate (D-09b). The agent has wired the real Resend provider; the human now sends a real email and confirms the three threading headers survive the send→reply→approve loop on the deployed service.

Purpose: This is the ONE assumption fixtures cannot test — real sender-client header survival. The EMAIL-01 stub always preserved headers because we minted them. Real mail clients (especially Outlook/Exchange) can mangle the References header. The only proof is a real send→reply→approve round-trip. This gate also resolves three ⚠ CONFIRM flags from RESEARCH.md: A1 (header casing), A5 (outbound Message-ID settable), and A6 (webhook data.message_id vs data.email_id). It also confirms the Resend signing secret is real (not a placeholder).

MEDIUM-3 fix (Round-3 — A5 as explicit scheduled gate, not prose): The A5 check is now a CONDITIONAL BLOCKING BRANCH in the how-to-verify, not documentation. The round-trip does NOT simply "document the fallback if Resend overwrites our Message-ID." Instead:
- If A5 PASSES (reply In-Reply-To == stored synthetic message_id): routing works, threading design confirmed, 06-06 is unblocked.
- If A5 FAILS (reply In-Reply-To != stored synthetic message_id): 06-05 BLOCKS 06-06 and triggers the A5-remediation mini-step: update email_messages.message_id to Resend's actual outbound RFC Message-ID, re-point routing, re-run the round-trip to verify the new anchor. Only after that re-verification passes does 06-06 proceed. The resume-signal has explicit branches for both outcomes.

MEDIUM-6 fix (BLOCKER-1 resolved in 06-04): The LOG_WEBHOOK_DEBUG_IDS debug guard was moved to 06-04 Task 1 where gateway.py is already being rewritten. It is committed there — not added here. This plan (06-05) only instructs the human OPERATOR to set LOG_WEBHOOK_DEBUG_IDS=true in the Render environment temporarily, do the round-trip, and read the Render logs. The code-side commit is already done in 06-04; 06-06 removes it.

Output: Confirmed real email delivery, confirmed header survival, A5 verified or remediated (MEDIUM-3 gate), ⚠ CONFIRM flags A1/A5/A6 resolved with actual observations. The D-09b gate closes, unblocking demo recording (06-06) only after A5 passes.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/email/gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-09b — Resend account setup + real email round-trip verify + A5 explicit gate (MEDIUM-3)</name>
  <what-built>The agent has implemented the real gateway (06-04): verify(), dual-path parse_inbound (Resend + fixture), D-13c send_outbound with HIGH-3 failed-state handling, HIGH-1 synthetic Message-ID header, HIGH-3 attachments mapping, and the LOG_WEBHOOK_DEBUG_IDS debug guard (BLOCKER-1 — committed in 06-04 Task 1) are all in place. The Render service has placeholder RESEND_API_KEY and WEBHOOK_SIGNING_SECRET from 06-03. This task: (1) sets up the production demo identity (HIGH-2); (2) replaces those env var placeholders; (3) enables LOG_WEBHOOK_DEBUG_IDS in the Render env; (4) does the first real end-to-end email round-trip; (5) executes the A5 conditional gate (MEDIUM-3) — either confirms the synthetic Message-ID survived or runs the remediation path before proceeding.

The LOG_WEBHOOK_DEBUG_IDS guard is already committed in gateway.py — no further agent edits to gateway.py are needed in this plan. The guard logs ONLY header key names, the Resend email_id, and the RFC message_id (no body content, no PII). The flag is removed from the Render env and from gateway.py in 06-06.</what-built>
  <how-to-verify>
    STEP 0 — Production demo identity setup (HIGH-2, REQUIRED before round-trip):
    The seeded businesses use `.example` contact emails; `find_business_by_sender` is exact-match → a real inbound from your personal email will hit `unknown_sender` and stop unless you update one business's contact_email. Do this BEFORE running the round-trip:
    - Decide which seeded business will be the demo business (e.g., "Metro Deli Group" — the hero fixture uses Metro Deli employees David Reyes/Daniel Reyes for Beat 2; "Coastal Cleaning Co." works for Beat 1).
    - Via the Supabase Table Editor (or a direct SQL query using psql/Supabase SQL editor):
      `UPDATE businesses SET contact_email = 'your.real@email.com' WHERE name = 'Metro Deli Group';`
      Replace 'your.real@email.com' with the EXACT address you will send test emails from.
    - Confirm: `SELECT name, contact_email FROM businesses WHERE contact_email = 'your.real@email.com';` returns the row.
    - IMPORTANT: Note down which business name you chose and which email address — 06-06 demo-reset must preserve this identity (not clobber it with the .example address). Record both in the 06-05-SUMMARY.md.

    STEP 1 — Resend account + inbound setup:
    - Log into resend.com. Create an API key with full access (or inbound+send scope).
    - Go to Resend Webhooks → Add Endpoint:
      - URL: `https://{your-render-url}/webhook/inbound`
      - Events: `email.received`
    - Copy the Signing Secret shown after creation.
    - Go to Resend Domains. For testing, use the shared Resend inbound test address (it auto-routes to your webhook). The shared address format is typically `{something}@inbound.resend.app`.

    STEP 2 — Update Render env vars (replace placeholders from 06-03):
    - Render service → Environment tab → set:
      - `RESEND_API_KEY` = real API key from Step 1
      - `WEBHOOK_SIGNING_SECRET` = Signing Secret from Step 1
      - `LOG_WEBHOOK_DEBUG_IDS` = `true` (TEMPORARY — remove in 06-06)
    - Wait for Render to redeploy. Confirm `Application startup complete` in Render logs.

    STEP 3 — Do the round-trip:
    - Send a real payroll email FROM your personal email TO the Resend inbound address.
    - Watch the Render logs for:
      - `Application startup complete` (if it was sleeping)
      - `WEBHOOK_DEBUG header_keys=[...]` — this is the LOG_WEBHOOK_DEBUG_IDS output inside gateway.py
      - Pipeline progress (extract, decide, etc.)
    - Wait for the run to reach `awaiting_approval` (or `awaiting_reply` if clarification triggered).
    - Optionally approve from the dashboard.

    STEP 4 — Confirm the three ⚠ CONFIRM flags from RESEARCH.md:
    - A1: In the Render logs, find the `WEBHOOK_DEBUG header_keys=[...]` line from inside gateway.py. Record the EXACT key names in the list (e.g., `["In-Reply-To", "References", "Message-ID"]` vs `["in-reply-to", "references", "message-id"]`). This is the definitive answer — it comes from the ReceivedEmail.headers dict BEFORE normalization. Document in your reply.
    - A6: In the `WEBHOOK_DEBUG` log line, compare `email_id=` (Resend internal) vs `rfc_message_id=` (email_obj.message_id). Are they the same or different? This confirms whether the inbound email_id in the webhook payload matches the RFC Message-ID in the fetched headers. Document.

    STEP 5 — A5 CONDITIONAL GATE (MEDIUM-3 — explicit blocking check, not just documentation):
    This step is the critical branch. Execute it before proceeding to 06-06.

    A5 check procedure:
    (a) Find the outbound email your service sent (clarification or confirmation). Check the WEBHOOK_DEBUG log for `rfc_message_id=` — this is the synthetic id your app minted and passed as the Message-ID header.
    (b) Query the DB: `SELECT message_id FROM email_messages WHERE direction='outbound' ORDER BY created_at DESC LIMIT 1;`
        Record the DB message_id.
    (c) Reply to the outbound email from your personal email client (this simulates what the client would do). After your reply arrives and the WEBHOOK_DEBUG log fires for the inbound reply, check: what is the `rfc_message_id=` of the REPLY's In-Reply-To header? You can also look at the raw webhook data or check `SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1;`
    (d) Compare: does the reply's In-Reply-To == the DB message_id from step (b)?

    BRANCH A — A5 PASSES (reply In-Reply-To == synthetic DB message_id):
    The synthetic Message-ID survived. Resend preserved our app-minted header. The HIGH-1 design is confirmed. Record: "A5=confirmed; synthetic id in reply In-Reply-To matches DB message_id." 06-06 is now UNBLOCKED. Continue to STEP 6.

    BRANCH B — A5 FAILS (reply In-Reply-To != synthetic DB message_id):
    Resend OVERWROTE our synthetic Message-ID with its own RFC Message-ID. This BLOCKS 06-06. Execute A5-remediation before proceeding:
    (b-1) Find Resend's actual outbound RFC Message-ID: this is in the `rfc_message_id=` field of the OUTBOUND email's WEBHOOK_DEBUG log (when the outbound email's delivery triggered a Resend webhook), OR look at the reply's In-Reply-To value (that IS Resend's actual id).
    (b-2) Update email_messages to use Resend's RFC id as the routing anchor: `UPDATE email_messages SET message_id = '<resend-actual-rfc-id>' WHERE message_id = '<our-synthetic-id>';`
    (b-3) Re-run a fresh round-trip: send another payroll email from your personal address. This time the outbound email will carry Resend's RFC id. Reply to it. Check that find_awaiting_reply_for_header now matches correctly via the Resend-assigned id.
    (b-4) Confirm the re-run round-trip reaches awaiting_reply→awaiting_approval via the reply routing.
    (b-5) Record the fallback approach and the Resend-assigned id pattern in 06-05-SUMMARY.md for 06-06 to reference.
    Only after b-4 succeeds is 06-06 unblocked.

    STEP 6 — Confirm threading headers survived (only after A5 branch resolves):
    - Confirm that the full pipeline ran end-to-end on the deployed service for the real inbound email.
    - Record in the resume-signal which A5 branch was taken and confirm reply routing worked.

    STEP 7 — If threading headers did NOT survive (D-03 trigger):
    - Note the failure in your reply and document exactly which header was missing.
    - The fallback (D-03a subject-token) is out of scope until confirmed needed. Document the failure and decide whether to implement D-03a before recording.
  </how-to-verify>
  <resume-signal>
    REQUIRED: State which A5 branch was taken (A5 PASSED or A5 FAILED + remediation complete) — 06-06 does NOT proceed until one of these is confirmed.

    If A5 PASSED:
    "roundtrip-ok: demo-identity={business}@{real-email}; A1={exact header key casing from WEBHOOK_DEBUG}; A5=PASSED (synthetic id in reply In-Reply-To matches DB message_id); A6={email_id same/different from rfc_message_id}; threading-headers=all-three-present; threading survived reply→resume; 06-06 UNBLOCKED"

    If A5 FAILED and remediation completed:
    "roundtrip-ok-with-a5-remediation: demo-identity={business}@{real-email}; A1={casing}; A5=FAILED (Resend overwrote to {actual-rfc-id}); A5-remediation=COMPLETE (DB updated, re-run verified, routing works on Resend's RFC id); A6={...}; threading-headers={...}; 06-06 UNBLOCKED"

    If round-trip failed entirely:
    "roundtrip-FAIL: [describe what failed — missing header, wrong key, pipeline error, A5 remediation not yet complete]"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Personal email client → Resend inbound | Real SMTP; headers subject to mail client/server rewriting |
| Resend infrastructure → /webhook/inbound | Signed POST; verify confirms authenticity |
| Render logs + LOG_WEBHOOK_DEBUG_IDS | Temporary debug logging logs ONLY header keys and IDs — no body content, no addresses, no PII; MUST be removed in 06-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Temporary LOG_WEBHOOK_DEBUG_IDS log in gateway.py / Render logs | mitigate | Logs ONLY header key names + message IDs (no body, no addresses, no PII); explicitly scoped TEMPORARY — removed in 06-06 Task 1; Render logs are not public |
| T-06-05-02 | Spoofing | Real Resend webhook signature test confirms D-17 in production | accept | This is the positive confirmation; the 06-01/06-04 tests cover the reject path |
| T-06-05-03 | Information Disclosure | API key and signing secret set in Render env (not committed) | mitigate | Set via Render dashboard UI; never in render.yaml (sync:false); never on camera (D-09) |
| T-06-05-04 | Information Disclosure | LOG_WEBHOOK_DEBUG_IDS flag inadvertently left enabled | mitigate | Flag must be removed from Render env AND the log line removed from gateway.py in 06-06; 06-06's done criteria explicitly checks for its removal |
| T-06-05-05 | Integrity | A5 fails silently — Resend overwrites Message-ID and routing breaks without detection | mitigate | MEDIUM-3: A5 is an explicit conditional BLOCKING gate; if In-Reply-To != DB message_id, the remediation path runs before 06-06; resume-signal requires explicit A5 branch declaration |
| T-06-05-SC | Tampering | Resend inbound domain/webhook configuration | accept | User-controlled account; signed webhooks prevent external tampering |
</threat_model>

<verification>
Human confirms in resume-signal:
0. HIGH-2: Demo identity set up — named business + real sender email updated in DB; 06-06 demo-reset will preserve this identity
1. A5 GATE (MEDIUM-3): EITHER "A5=PASSED (synthetic id matched)" OR "A5=FAILED + remediation complete (Resend RFC id substituted + re-run verified)" — 06-06 only proceeds after one of these is explicitly confirmed
2. "roundtrip-ok" — real email from the demo identity sender arrived at Resend inbound, triggered the webhook, pipeline ran, run reached awaiting_approval (find_business_by_sender matched)
3. A1 resolved: EXACT header key casing from `WEBHOOK_DEBUG header_keys=[...]` log inside gateway.py (affects case-insensitive normalization — if title-case, the existing lowercasing in parse_inbound handles it correctly)
4. A6 resolved: webhook email_id vs rfc_message_id comparison documented (confirms the dedup key is correct — 06-04 uses email_obj.message_id from the full fetch)
5. LOW-8 resolved: three threading headers (Message-ID, In-Reply-To, References) confirmed present in the round-trip WEBHOOK_DEBUG logs; exact key casing recorded
6. Threading survived (reply→resume path worked) OR failure documented with D-03a plan
</verification>

<success_criteria>
- Production demo identity set up: real sender email updated in the businesses table; find_business_by_sender matches the round-trip sender — HIGH-2
- Real email send→reply→approve round-trip completed on the deployed Render service
- A5 GATE (MEDIUM-3): explicitly resolved — either PASSED (synthetic id preserved) or FAILED + remediation complete (Resend's RFC id substituted, re-run verified); 06-06 is only unblocked after this gate is explicitly confirmed in the resume-signal
- All three ⚠ CONFIRM flags (A1, A5, A6) resolved with actual observed values from the LOG_WEBHOOK_DEBUG_IDS output inside gateway.py
- A5/HIGH-1: reply In-Reply-To vs DB message_id comparison documented; if A5 confirmed, synthetic Message-ID design is validated; if A5 failed, remediation complete and documented for 06-06
- LOW-8: all three threading headers (Message-ID, In-Reply-To, References) confirmed present in the real round-trip — this evidence belongs here, not at the thin-deploy gate (06-03)
- Signing secret confirmed working (not placeholder)
- Threading headers confirmed surviving a real round-trip (or failure documented with mitigation plan)
- LOG_WEBHOOK_DEBUG_IDS flag is in place (temporary) and will be removed in 06-06
- Demo recording (06-06) is unblocked ONLY after A5 gate is satisfied
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-05-SUMMARY.md` when done, recording: (1) which business and contact_email were updated for the production demo identity (HIGH-2 — 06-06 needs this to preserve it during reset); (2) ALL ⚠ CONFIRM flag resolutions for A1, A5, A6; (3) the A5/HIGH-1 observation (synthetic id matched or mismatched the reply In-Reply-To) AND which branch was taken (PASSED or FAILED+remediation); (4) the threading headers survival result and exact casing observed (LOW-8).
</output>
