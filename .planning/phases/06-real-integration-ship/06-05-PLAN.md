---
phase: "06"
plan: "05"
type: execute
wave: 4
depends_on:
  - "06-04"
  - "06-08"
autonomous: false
files_modified: []
requirements:
  - OPS-02

user_setup:
  - service: resend
    why: "Real inbound email provider for send→reply→approve round-trip (D-09b)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys → Create API Key (or use existing)"
      - name: WEBHOOK_SIGNING_SECRET
        source: "Resend Dashboard → Webhooks → Add Endpoint → Signing Secret (shown AFTER endpoint creation)"
    dashboard_config:
      - task: "Create or verify a Resend account and create an API key"
        location: "resend.com → Sign in → API Keys"
      - task: "Identify your inbound .resend.app address — the address the operator receives at (e.g. payroll@jiodnel.resend.app). This is used as the Reply-To on every outbound send (RESEND_REPLY_TO env var, set in 06-02). No custom domain needed for inbound on free tier."
        location: "Resend Dashboard → Domains → the auto-created .resend.app domain; confirm the exact address"
      - task: "Create the Resend inbound webhook endpoint — IMPORTANT: this step requires the Render service to already exist from 06-03. You cannot create the webhook until you have the Render service URL. After the Render service exists: Resend Dashboard → Webhooks → Add Endpoint → URL: https://{render-url}/webhook/inbound → select event: email.received → copy the Signing Secret shown after creation."
        location: "Resend Dashboard → Webhooks → Add Endpoint (perform AFTER 06-03 Render deploy — Render URL must be known first)"
      - task: "Update the Render service env vars: set RESEND_API_KEY and WEBHOOK_SIGNING_SECRET to the real values (replacing the placeholders set in 06-03); also set RESEND_REPLY_TO to the inbound .resend.app address (e.g. payroll@jiodnel.resend.app) if not already set in 06-03"
        location: "Render service dashboard → Environment tab → edit RESEND_API_KEY and WEBHOOK_SIGNING_SECRET; confirm RESEND_REPLY_TO is set correctly"

must_haves:
  truths:
    - "Production demo identity is set up: a demo_sender_bindings row maps the real sender address (pjnhek@gmail.com) to the target business_id, so find_business_by_sender resolves the real inbound via the additive fallback — businesses.contact_email is NEVER mutated (seed .example contacts stay stable) — HIGH-2; this step is a named pre-requisite before the round-trip test"
    - "Post-06-08 DDL applied to live Supabase DB (HIGH-1 schema gap): payroll_runs.record_only column and demo_sender_bindings table exist on the production DB before /demo/bind or /demo/compose are exercised; applied via bootstrap WITHOUT --reset (safe: non-destructive additive path confirmed in bootstrap.py); verified via information_schema query + to_regclass check"
    - "A real email sent to the Resend inbound address (payroll@jiodnel.resend.app or the operator's confirmed .resend.app address) triggers a POST to /webhook/inbound on the deployed service"
    - "Signature verification passes for a real Resend webhook delivery (signing secret confirmed real, not placeholder)"
    - "The three threading headers (Message-ID, In-Reply-To, References) survive the send→reply round-trip"
    - "raw email_obj HEADER KEYS (not body, not PII) are logged by the LOG_WEBHOOK_DEBUG_IDS flag inside gateway.py, capturing real provider header casing for ⚠ A1"
    - "data.message_id vs data.email_id in the raw webhook payload is confirmed (⚠ A6)"
    - "ADDRESS TOPOLOGY (Pass-6): Inbound address = payroll@jiodnel.resend.app (wired to the webhook); Outbound From = onboarding@resend.dev (free-tier, can ONLY send to the Resend account-owner email); Reply-To = payroll@jiodnel.resend.app (set by gateway.send_outbound via RESEND_REPLY_TO). The client sends TO the inbound address; the service sends FROM onboarding@resend.dev WITH Reply-To pointing back to the inbound address. The Resend inbound webhook endpoint MUST be created AFTER the Render service exists (06-03) — the webhook URL requires the real Render service URL."
    - "Outbound sender constraint confirmed: onboarding@resend.dev (free tier, no verified domain) can only send TO the Resend account-owner email. The round-trip operator IS the account owner, so the round-trip works. For a real client (non-account-owner), domain verification would be required. This is documented in the README 'For Engineers' section (06-06 Task 2)."
    - "A5 evidence source is the REPLY's In-Reply-To header — observed from the INBOUND webhook that fires when the client replies; the PRIMARY canonical source is the DB query SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1 (reads the stored In-Reply-To value from the reply row); LOG_WEBHOOK_DEBUG_IDS logs header KEY NAMES only (not values) — it is NOT a source for the in_reply_to value itself; A5 is NOT observed from an outbound webhook (Resend does not emit email.received events for outbound sends)"
    - "A5 gate (MEDIUM-2/MEDIUM-3 — explicit conditional, not just documented): the operator checks whether the reply's In-Reply-To header equals the synthetic message_id stored in email_messages.message_id; the PRIMARY evidence is the DB query SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1; this is a BLOCKING conditional branch — if A5 PASSES (ids match), 06-06 is unblocked; if A5 FAILS (Resend overwrote the header), an explicit A5-remediation path runs BEFORE 06-06 proceeds"
    - "A5 is MORE load-bearing due to address asymmetry (Pass-6): because From=onboarding@resend.dev cannot serve as a routing anchor (clients cannot reply-to it and reach the inbound webhook), the header chain (In-Reply-To / References matching the synthetic message_id) and the D-03a subject-token (Branch B fallback) are the SOLE resume anchors. The threading match in find_awaiting_reply_for_header is address-agnostic — it matches on RFC headers, not From/To — so the From asymmetry does NOT break chain routing IF the physical reply arrives at the inbound address (ensured by Reply-To: payroll@jiodnel.resend.app) AND the app-minted Message-ID survives as the reply's In-Reply-To (A5). There is no fallback involving the From/To address to rescue a failed A5."
    - "A5-remediation path — Branch B (only if A5 fails): fall back to the D-03a subject-token anchor (already locked in CONTEXT.md D-03a for exactly this 'headers don't survive' case): embed an opaque token [#XXXXXXXX] in the clarification email subject and add the token-extraction path to the reply router; re-run the round-trip to verify the subject-token anchor routes the reply correctly; only after this re-verification passes is 06-06 unblocked"
    - "The full pipeline runs end-to-end on the deployed service for one real inbound email"
    - "LOG_WEBHOOK_DEBUG_IDS env flag is already committed in gateway.py (06-04 Task 1) and must be enabled temporarily in the Render env to capture real header casing (removed in 06-06)"
    - "Three threading headers (Message-ID, In-Reply-To, References) are confirmed present on the real round-trip: the WEBHOOK_DEBUG log for the INBOUND reply shows all three key names in header_keys; the reply In-Reply-To value is confirmed via DB query SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1 — LOW-8 evidence collected here (not at the thin-deploy gate in 06-03)"
  artifacts: []
  key_links:
    - from: "Real email client (user's personal email / Resend account-owner email)"
      to: "Resend inbound webhook → /webhook/inbound on Render"
      via: "Email sent TO payroll@jiodnel.resend.app → Resend email.received event → POST to webhook endpoint"
      pattern: "email\\.received"
    - from: "gateway.send_outbound"
      to: "Client's email client"
      via: "Sends FROM onboarding@resend.dev WITH Reply-To: payroll@jiodnel.resend.app; client presses Reply → their client sends the reply TO payroll@jiodnel.resend.app (via Reply-To); Resend inbound receives it and fires /webhook/inbound again"
      pattern: "Reply-To|resend_reply_to"
---

<objective>
Wave 4 — BLOCKING human checkpoint (autonomous:false). The email round-trip verify gate (D-09b). The agent has wired the real Resend provider; the human now sends a real email and confirms the three threading headers survive the send→reply→approve loop on the deployed service.

CHANGE-10 (self-contained runbook): This plan contains a COMPLETE, ordered, self-contained operator runbook so it survives a /clear between planning and execution. The Resend inbound webhook creation has been moved to the 06-03 between-gate steps (Step 7 in 06-03 Task 2) — it must be created AFTER the Render URL is known. By the time this gate runs (Wave 4), the webhook is already in place. The 06-08 landing page (GET /) and the POST /demo/bind atomic binding route are deployed (Wave 3, parallel to 06-04). Step 0 below uses POST /demo/bind as the primary binding method instead of direct SQL.

HIGH-1 SCHEMA GAP (post-06-08 DDL apply, Step 0.5): 06-03 (Wave 2) ran bootstrap --reset BEFORE 06-08 (Wave 3) added payroll_runs.record_only + demo_sender_bindings to schema.sql. The new DDL is in schema.sql after 06-08 ships, but the live Supabase DB does not have these objects until a re-apply. CONFIRMED SAFE: bootstrap.py's default (no --reset) path applies schema.sql's idempotent DDL (CREATE TABLE IF NOT EXISTS + ALTER TABLE ADD COLUMN IF NOT EXISTS) without dropping any tables or seeded rows. The only unconditional DROP in bootstrap.py is the dead name_matches table (D-21-06 migration), which is already gone. Step 0.5 below runs this non-destructive apply over the 5432 session-pooler URL and verifies both objects exist before /demo/bind or /demo/compose are exercised.

Purpose: This is the ONE assumption fixtures cannot test — real sender-client header survival. The EMAIL-01 stub always preserved headers because we minted them. Real mail clients (especially Outlook/Exchange) can mangle the References header. The only proof is a real send→reply→approve round-trip. This gate also resolves three ⚠ CONFIRM flags from RESEARCH.md: A1 (header casing), A5 (outbound Message-ID settable), and A6 (webhook data.message_id vs data.email_id). It also confirms the Resend signing secret is real (not a placeholder).

ADDRESS TOPOLOGY (Pass-6): The operator's Resend free-tier account has a concrete inbound/outbound address asymmetry:
- INBOUND (client → agent): payroll@jiodnel.resend.app — the auto-created .resend.app address wired to the Resend inbound webhook; receives all client emails; live inbound works WITHOUT a custom domain.
- OUTBOUND (agent → client): onboarding@resend.dev — the free-tier sender; can only send TO the Resend account-owner email (no verified domain). The operator IS the account owner, so this round-trip works.
- REPLY-TO FIX: gateway.send_outbound sets Reply-To: payroll@jiodnel.resend.app on every outbound send (via RESEND_REPLY_TO env var, defined in 06-02, read in 06-04). This directs client replies to the address the webhook IS connected to, not to onboarding@resend.dev which the app cannot receive at.
- CONSEQUENCE FOR A5: Because From=onboarding@resend.dev can never serve as a routing anchor (unreachable for inbound), the header chain (In-Reply-To / References matching the synthetic message_id) and the D-03a subject-token (Branch B fallback) are the SOLE resume anchors. A5 is more critical in this topology than in a symmetric setup.
- WEBHOOK CREATION ORDERING: The Resend inbound webhook must be created AFTER the Render service exists. You cannot know the webhook URL (https://{render-url}/webhook/inbound) until the Render service has been deployed in 06-03. Create the webhook endpoint in Resend AFTER 06-03 completes.

MEDIUM-2 fix (Round-4 — A5 Branch B concrete remediation + evidence-source wording):
The A5 question is "did our outbound Message-ID survive into the client's reply?" The PRIMARY canonical evidence source is the DB query: `SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1` — this reads the actual stored In-Reply-To value from the inbound reply row that was parsed by gateway.parse_inbound. LOG_WEBHOOK_DEBUG_IDS logs header KEY NAMES only (not their values), so the debug log shows that an In-Reply-To key is present but does not reveal the value — it is NOT the A5 evidence source. A5 is NOT observed from an "outbound WEBHOOK_DEBUG" — Resend does not emit email.received events for outbound sends; there is no outbound webhook to read.

Branch B (A5 FAILS — Resend overwrote our Message-ID) is a concrete mini-plan with a single chosen remediation: fall back to the D-03a subject-token anchor (already locked in CONTEXT.md D-03a for exactly this "headers don't survive" case). Steps: embed an opaque 8-hex token [#XXXXXXXX] in the clarification email subject, add the token-extraction path to the reply router so a header no-match falls through to subject-token matching, then re-run the round-trip to verify the subject-token routes the reply. This avoids a schema change and uses the pre-specified, already-designed fallback. Only after re-verification passes is 06-06 unblocked.

MEDIUM-3 fix (Round-3 — A5 as explicit scheduled gate, not prose): The A5 check is a CONDITIONAL BLOCKING BRANCH in the how-to-verify, not documentation. See Branch A / Branch B below.

MEDIUM-6 fix (BLOCKER-1 resolved in 06-04): The LOG_WEBHOOK_DEBUG_IDS debug guard was moved to 06-04 Task 1 where gateway.py is already being rewritten. It is committed there — not added here. This plan (06-05) only instructs the human OPERATOR to set LOG_WEBHOOK_DEBUG_IDS=true in the Render environment temporarily, do the round-trip, and read the Render logs. The code-side commit is already done in 06-04; 06-06 removes it.

Output: Confirmed real email delivery, confirmed header survival, A5 verified or remediated (MEDIUM-2/MEDIUM-3 gate), ⚠ CONFIRM flags A1/A5/A6 resolved with actual observations. The D-09b gate closes, unblocking demo recording (06-06) only after A5 passes or Branch-B remediation is complete.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/email/gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-09b — post-06-08 DDL apply + Resend account setup + real email round-trip verify + A5 explicit gate (MEDIUM-2/MEDIUM-3)</name>
  <what-built>The agent has implemented the real gateway (06-04): verify(), dual-path parse_inbound (Resend + fixture), D-13c send_outbound with HIGH-3 failed-state handling, HIGH-1 synthetic Message-ID header, HIGH-1-AUTH outbound api_key set as first line of send_outbound, HIGH-3 attachments mapping, Reply-To header (RESEND_REPLY_TO → payroll@jiodnel.resend.app, owned by 06-02, read by 06-04), and the LOG_WEBHOOK_DEBUG_IDS debug guard (BLOCKER-1 — committed in 06-04 Task 1) are all in place. 06-08 (Wave 3) shipped the demo landing/compose/bind routes and updated schema.sql with payroll_runs.record_only + demo_sender_bindings. The Render service has placeholder RESEND_API_KEY and WEBHOOK_SIGNING_SECRET from 06-03. This task: (0.5) applies the post-06-08 DDL to the live DB; (1) sets up the production demo identity (HIGH-2); (2) replaces Render env var placeholders and confirms RESEND_REPLY_TO is set; (3) enables LOG_WEBHOOK_DEBUG_IDS; (4) does the first real end-to-end email round-trip; (5) executes the A5 conditional gate.</what-built>
  <how-to-verify>
    ADDRESS TOPOLOGY REFERENCE (confirm before starting):
    - INBOUND address (client → agent): payroll@jiodnel.resend.app (or your confirmed .resend.app address)
      This is the address clients send TO; Resend routes it to /webhook/inbound on Render.
    - OUTBOUND From address (agent → client): onboarding@resend.dev
      This is what clients see as the FROM in their inbox. NOTE: free-tier constraint — onboarding@resend.dev can ONLY send to the Resend account-owner email. The round-trip operator IS the account owner, so this test works. For sending to arbitrary client addresses, a verified custom domain is required (noted in README For Engineers).
    - REPLY-TO address (set on every outbound by gateway.send_outbound): payroll@jiodnel.resend.app (RESEND_REPLY_TO env var)
      When the client presses Reply in their email client, their reply goes TO this address (not to onboarding@resend.dev). This is the critical fix: the physical reply reaches the inbound webhook address.
    - CONSEQUENCE: Because From=onboarding@resend.dev cannot serve as a routing anchor (clients cannot receive-and-reply-via onboarding@resend.dev), the RFC header chain (In-Reply-To / References matching the synthetic message_id) and the D-03a subject-token (Branch B fallback) are the SOLE resume anchors. A5 is more critical in this topology. Verify RESEND_REPLY_TO is correctly set in Render env before doing the round-trip.

    STEP 0.5 — Post-06-08 DDL apply (HIGH-1 schema gap — REQUIRED before /demo/bind or /demo/compose):
    06-03 ran bootstrap --reset (Wave 2) BEFORE 06-08 added payroll_runs.record_only + demo_sender_bindings to schema.sql (Wave 3). The live Supabase DB does not have these objects yet. Apply them now using bootstrap WITHOUT --reset (safe: non-destructive path applies idempotent DDL without dropping tables or seeded rows).

    CONFIRMED SAFE: bootstrap.py's default path (no --reset flag) reads schema.sql and executes it directly. schema.sql uses CREATE TABLE IF NOT EXISTS for new tables and ALTER TABLE ADD COLUMN IF NOT EXISTS for new columns, so a re-apply over the existing seeded DB is a no-op for objects that already exist and adds only the new objects. The only unconditional DROP in bootstrap.py is for the dead name_matches table (D-21-06), which is already gone on the live DB. Seeded businesses, employees, and payroll data are NEVER touched by a non-reset apply.

    You need the 5432 SESSION POOLER URL (not the 6543 transaction pooler) for bootstrap.
    Supabase Dashboard → Project Settings → Database → Connection Pooler → Session mode → copy the connection string (port 5432). Store it temporarily as SESSION_DB_URL in your shell.

    Run (copy-paste exactly — note single-quotes around the entire -c body, double-quotes for SQL strings inside):

      SESSION_DB_URL="postgresql://postgres.{ref}:{password}@aws-{region}.pooler.supabase.com:5432/postgres?sslmode=require"
      DATABASE_URL="$SESSION_DB_URL" uv run python -m app.db.bootstrap

    You should see output like:
      Bootstrap target: postgresql://postgres.{ref}:***@...
      DROP TABLE IF EXISTS name_matches CASCADE  (D-21-06 dead-table migration)
      ALTER TABLE IF EXISTS paystub_line_items DROP COLUMN IF EXISTS match_confidence  (D-21-06 dead-column migration)
      Bootstrap complete. Tables applied.

    Verify the two new objects exist (run from terminal with DATABASE_URL set to the session pooler URL):

    Verify record_only column:
      uv run python -c 'import os, psycopg; conn=psycopg.connect(os.environ["DATABASE_URL"]); row=conn.execute("SELECT column_name FROM information_schema.columns WHERE table_name='"'"'payroll_runs'"'"' AND column_name='"'"'record_only'"'"'").fetchone(); print("record_only column:", row); conn.close()'
    Expected: record_only column: ('record_only',)

    Verify demo_sender_bindings table:
      uv run python -c 'import os, psycopg; conn=psycopg.connect(os.environ["DATABASE_URL"]); row=conn.execute("SELECT to_regclass('"'"'public.demo_sender_bindings'"'"')").fetchone(); print("demo_sender_bindings table:", row); conn.close()'
    Expected: demo_sender_bindings table: ('public.demo_sender_bindings',)

    If either returns None or an error: re-check that DATABASE_URL is the session-pooler URL (port 5432) and that 06-08's schema.sql changes are committed and deployed. Do NOT proceed to /demo/bind or /demo/compose until both verifications pass.

    STEP 0 — Production demo identity setup (HIGH-2, REQUIRED before round-trip):
    The seeded businesses use `.example` contact emails; `find_business_by_sender` is exact-match — a real inbound from pjnhek@gmail.com would hit `unknown_sender` and stop unless that address maps to a business. Per the 06-08 HIGH-2 fix, this mapping is ADDITIVE and lives in the `demo_sender_bindings` table — it NEVER mutates any business's `contact_email` (seed `.example` contacts stay permanently stable, so recruiter Path-1 routing and the FIX-5 spoof guard are never broken). `find_business_by_sender` resolves pjnhek@gmail.com to the armed business via this additive fallback (primary contact_email match returns None → check demo_sender_bindings). Do this AFTER Step 0.5 (the table must exist first).

    PRIMARY METHOD — use the UNLINKED operator /demo/bind route (from 06-08, deployed in Wave 3):
    - The bind control is intentionally NOT on the public landing page (06-08 MEDIUM fix). Reach it directly: POST to https://{your-render-url}/demo/bind with form field business_name (e.g. "Metro Deli Group" for Beat 2, or "Coastal Cleaning Co." for Beat 1). Example: `curl -X POST https://{your-render-url}/demo/bind -d "business_name=Metro Deli Group"`.
    - This calls POST /demo/bind → repo.bind_demo_business, which UPSERTs one row into demo_sender_bindings (operator_email=pjnhek@gmail.com → the selected business_id) and restores single-owner by replacing any prior binding row. It does NOT touch businesses.contact_email.
    - Confirm: visit the landing page (GET /). It shows the currently-armed business read-only (via the demo_sender_bindings lookup).

    FALLBACK METHOD — direct SQL into demo_sender_bindings (if the route is unavailable). DO NOT update businesses.contact_email — that would break the HIGH-2 invariant. Use the binding table with the fixed seed business_id:
    - Via Supabase SQL Editor or psql (Metro Deli Group example; UUIDs are the fixed seed IDs — Coastal=b0000001-0000-0000-0000-000000000001, Metro=b0000002-0000-0000-0000-000000000002, Summit=b0000003-0000-0000-0000-000000000003):
      INSERT INTO demo_sender_bindings (operator_email, business_id, bound_at) VALUES ('pjnhek@gmail.com', 'b0000002-0000-0000-0000-000000000002', now()) ON CONFLICT (operator_email) DO UPDATE SET business_id = EXCLUDED.business_id, bound_at = EXCLUDED.bound_at;
    - Confirm exactly one binding row: SELECT operator_email, business_id FROM demo_sender_bindings WHERE operator_email = 'pjnhek@gmail.com';
    - Confirm seed contacts are UNCHANGED: SELECT name, contact_email FROM businesses; — all three must still show their `.example` addresses.

    - IMPORTANT: Note which business you armed. Set DEMO_CONTACT_EMAIL = pjnhek@gmail.com and DEMO_BUSINESS_NAME = {the business name} in Render env. These are used by demo_reset.py (06-06) to RE-ARM the demo_sender_bindings row after seed() — NOT as a global from_addr override at send time (see 06-06 HIGH-1 fix), and NOT a contact_email mutation. Record both in 06-05-SUMMARY.md.

    STEP 1 — Confirm Resend credentials are in place (webhook was created in 06-03 between-gate steps):
    Per CHANGE-10: the Resend inbound webhook was created in the 06-03 between-gate steps (Step 7 of 06-03 Task 2), AFTER the Render URL became known. By the time this gate runs, these should already be in place — confirm before proceeding:
    - RESEND_API_KEY is set in Render env (real key, not placeholder).
    - WEBHOOK_SIGNING_SECRET is set in Render env (Signing Secret from 06-03 Step 7).
    - RESEND_REPLY_TO is set in Render env = payroll@jiodnel.resend.app (or your confirmed inbound address).
    - The Resend webhook at https://{render-url}/webhook/inbound is Active in Resend Dashboard -> Webhooks.
    If any are missing: complete the 06-03 between-gate steps first (create webhook, update Render env vars, wait for redeploy).
    - OUTBOUND SENDER NOTE: Free tier sends FROM onboarding@resend.dev TO the Resend account-owner email only. pjnhek@gmail.com IS the account owner, so the round-trip works. For arbitrary recipients, verify a custom domain in Resend Dashboard -> Domains -> Add Domain.


    STEP 2 — Update Render env vars (replace placeholders from 06-03):
    - Render service → Environment tab → set:
      - `RESEND_API_KEY` = real API key from Step 1
      - `WEBHOOK_SIGNING_SECRET` = Signing Secret from Step 1
      - `RESEND_REPLY_TO` = your inbound .resend.app address (e.g. payroll@jiodnel.resend.app) — confirm this is already set from 06-03; if not, add it now as a plain value: (not a secret)
      - `LOG_WEBHOOK_DEBUG_IDS` = `true` (TEMPORARY — remove in 06-06)
      - `DEMO_CONTACT_EMAIL` = the real sender email from Step 0
      - `DEMO_BUSINESS_NAME` = the business name from Step 0
    - Wait for Render to redeploy. Confirm `Application startup complete` in Render logs.

    STEP 3 — Do the round-trip:
    - Send a real payroll email FROM your personal email (the Resend account-owner email) TO the inbound .resend.app address (e.g. payroll@jiodnel.resend.app).
    - Watch the Render logs for:
      - `Application startup complete` (if it was sleeping)
      - `WEBHOOK_DEBUG header_keys=[...]` — this is the LOG_WEBHOOK_DEBUG_IDS output inside gateway.py (fired on the INBOUND parse); it logs header KEY NAMES only, not their values
      - Pipeline progress (extract, decide, etc.)
    - Wait for the run to reach `awaiting_approval` (or `awaiting_reply` if clarification triggered).
    - The service sends its reply FROM onboarding@resend.dev WITH Reply-To: payroll@jiodnel.resend.app. When you press Reply in your email client, the reply should be addressed TO payroll@jiodnel.resend.app (via the Reply-To header), which is what the inbound webhook receives.
    - Optionally approve from the dashboard.

    STEP 4 — Confirm the three ⚠ CONFIRM flags from RESEARCH.md:
    - A1: In the Render logs, find the `WEBHOOK_DEBUG header_keys=[...]` line from inside gateway.py (fires on INBOUND). Record the EXACT key names in the list (e.g., `["In-Reply-To", "References", "Message-ID"]` vs `["in-reply-to", "references", "message-id"]`). This is the definitive answer — it comes from the ReceivedEmail.headers dict BEFORE normalization. Document in your reply.
    - A6: In the `WEBHOOK_DEBUG` log line, compare `email_id=` (Resend internal) vs `rfc_message_id=` (email_obj.message_id). Are they the same or different? This confirms whether the inbound email_id in the webhook payload matches the RFC Message-ID in the fetched headers. Document.

    STEP 5 — A5 CONDITIONAL GATE (MEDIUM-2/MEDIUM-3 — explicit blocking check, not just documentation):
    This step is the critical branch. Execute it before proceeding to 06-06.

    WHY A5 IS MORE LOAD-BEARING HERE (address asymmetry): Because From=onboarding@resend.dev cannot serve as a routing anchor (the app cannot receive replies at onboarding@resend.dev), the ONLY mechanisms for routing a client reply back to the correct run are:
    (1) RFC header chain: client's In-Reply-To matches the synthetic message_id stored in email_messages (Branch A)
    (2) D-03a subject-token: [#token] in the clarification subject, looked up by the reply router on header no-match (Branch B fallback)
    There is NO fallback involving From/To addresses. A5 failing means the ONLY remaining path is Branch B. This is why A5 is an explicit blocking gate, not just a documentation item.

    HOW TO OBSERVE A5 — evidence from the INBOUND reply, PRIMARY source is the DB:
    A5 asks: "did our app-minted Message-ID survive into the client's reply as In-Reply-To?"
    The evidence is the reply's In-Reply-To header — which arrives as a NEW inbound webhook AFTER you reply to the service's outbound email. The PRIMARY/canonical evidence source is the DB query:
      SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1;
    This reads the stored In-Reply-To value from the inbound reply row (populated by gateway.parse_inbound when it processed the reply). Use this DB query as your authoritative answer.
    Note: LOG_WEBHOOK_DEBUG_IDS logs header KEY NAMES only — it will confirm that an "In-Reply-To" key is present in the reply headers, but it does NOT log the value. Do NOT attempt to read the in_reply_to value from the debug log; use the DB query above instead.
    Note: there is NO "outbound WEBHOOK_DEBUG" source for A5 — Resend does not emit email.received events for outbound sends. The A5 observation is purely from the INBOUND reply path.

    A5 check procedure — SELF-CONTAINED (post-/clear copy-paste; requires DATABASE_URL in shell):
    These commands work from the terminal after /clear without any running server. Each uses
    single-quotes around the -c body so the shell does not interfere with double-quotes in the
    Python code. Set DATABASE_URL in your shell to the session-pooler URL (5432) before running.

    (a) Read the latest outbound Message-ID (what your app minted and stored):
        Copy-paste exactly — outer single-quotes, inner double-quotes for SQL:
          uv run python -c 'import os, psycopg; conn=psycopg.connect(os.environ["DATABASE_URL"]); row=conn.execute("SELECT message_id FROM email_messages WHERE direction='"'"'outbound'"'"' ORDER BY created_at DESC LIMIT 1").fetchone(); print("outbound_msg_id:", row[0] if row else "NONE"); conn.close()'
        Record the printed message_id value (e.g. "<xxxxxxxx-xxxx@demo.payroll-agent.local>").

    (b) Reply to the outbound email from your personal email client (this simulates what the client would do).
        The outbound email arrived FROM onboarding@resend.dev WITH Reply-To: payroll@jiodnel.resend.app.
        When you press Reply, your email client should pre-fill the TO as payroll@jiodnel.resend.app (via Reply-To). Confirm this before sending — if your client pre-filled onboarding@resend.dev instead, manually correct it to payroll@jiodnel.resend.app. The reply MUST reach the inbound .resend.app address for the webhook to fire.

    (c) After your reply arrives, read the latest inbound In-Reply-To (the PRIMARY A5 evidence):
        Copy-paste exactly — outer single-quotes, inner double-quotes for SQL:
          uv run python -c 'import os, psycopg; conn=psycopg.connect(os.environ["DATABASE_URL"]); row=conn.execute("SELECT in_reply_to, message_id FROM email_messages WHERE direction='"'"'inbound'"'"' ORDER BY created_at DESC LIMIT 1").fetchone(); print("reply_in_reply_to:", row[0] if row else "NONE", "| reply_message_id:", row[1] if row else "NONE"); conn.close()'
        Record the printed in_reply_to value. This is the PRIMARY evidence — the value stored from
        the reply row when gateway.parse_inbound processed the incoming reply. LOG_WEBHOOK_DEBUG_IDS
        shows only key NAMES, not values — the DB query above is the only source for the actual value.

    (d) Compare: does the reply's in_reply_to (from step c) == the outbound message_id (from step a)?
        If yes → Branch A (A5 PASSES). If no or NULL → Branch B (A5 FAILS).

    Optional: read both together in one command to see the full context:
        uv run python -c 'import os, psycopg; conn=psycopg.connect(os.environ["DATABASE_URL"]); rows=conn.execute("SELECT direction, message_id, in_reply_to, created_at FROM email_messages ORDER BY created_at DESC LIMIT 5").fetchall(); [print(r[0], "|", r[1], "|", r[2], "|", r[3]) for r in rows]; conn.close()'
    This prints the 5 most recent email_messages rows (most recent first) so you can see the
    outbound row and the reply row side-by-side.

    BRANCH A — A5 PASSES (DB in_reply_to == synthetic DB message_id from outbound row):
    The synthetic Message-ID survived. Resend preserved our app-minted header. The HIGH-1 design is confirmed. Record: "A5=confirmed; synthetic id in reply In-Reply-To matches DB message_id — observed from DB SELECT in_reply_to WHERE direction='inbound'." 06-06 is now UNBLOCKED. Continue to STEP 6.

    BRANCH B — A5 FAILS (DB in_reply_to != synthetic DB message_id, or in_reply_to is NULL):
    Resend OVERWROTE our synthetic Message-ID with its own RFC Message-ID. This BLOCKS 06-06. Because From=onboarding@resend.dev cannot serve as an alternative routing anchor (address asymmetry), the ONLY remaining path is the D-03a subject-token anchor. Execute it before proceeding.

    Branch B remediation — D-03a subject-token anchor (concrete steps):
    This is a small code change to the reply router. Do NOT attempt to recover the Resend-assigned RFC Message-ID post-hoc or update the DB rows manually — the D-03a subject-token is the pre-specified fallback and avoids schema changes.

    (b-1) Determine the Resend-assigned outbound RFC Message-ID:
      Look at the reply's in_reply_to value from the DB query (that IS Resend's actual outbound id). Record it (e.g., `<resend-abc123@resend.dev>`).

    (b-2) Implement the D-03a subject-token anchor (code change — requires a new agent task):
      The spec (CONTEXT.md D-03a): embed an opaque 8-hex token `[#XXXXXXXX]` in the clarification email subject when it is drafted. In the reply router, on a header no-match (In-Reply-To not found in email_messages.message_id), scan the subject line for `[#token]` and look it up against a stored token→run_id mapping. "Never in parallel" — the subject-token fires ONLY when the header chain produces no match.
      Request an agent task to: (1) modify the clarification email draft to embed `[#{run_id_short}]` in the subject; (2) add the subject-token extraction path to the reply router as a secondary lookup after the header-chain miss; (3) add a unit test that proves the subject-token routes a headerless reply to the correct run; (4) re-run the round-trip to verify routing.

    (b-3) Re-run the round-trip after the code change is deployed: send a fresh payroll email → clarification email arrives with the subject token → reply to it (your reply client may or may not preserve In-Reply-To, but now the token is in the subject). After reply arrives, confirm via DB:
        uv run python -c 'import os, psycopg; conn=psycopg.connect(os.environ["DATABASE_URL"]); row=conn.execute("SELECT run_id FROM email_messages WHERE direction='"'"'inbound'"'"' ORDER BY created_at DESC LIMIT 1").fetchone(); print("run_id:", row[0] if row else "NONE"); conn.close()'
    run_id should be set (not NULL), indicating the reply was routed to the correct run via subject-token.

    (b-4) Record the Branch B outcome in 06-05-SUMMARY.md: "A5=FAILED; Resend-assigned id={actual-id from DB SELECT in_reply_to WHERE direction='inbound'}; D-03a subject-token implemented and verified; routing confirmed via subject-token path; address asymmetry (From=onboarding@resend.dev unreachable) means subject-token is now the primary anchor." Note that subsequent phases should use the subject-token path as the primary anchor.

    Only after b-3 succeeds is 06-06 unblocked.

    STEP 6 — Confirm threading headers survived (only after A5 branch resolves):
    - Confirm that the full pipeline ran end-to-end on the deployed service for the real inbound email.
    - Record in the resume-signal which A5 branch was taken and confirm reply routing worked.

    STEP 7 — If threading headers entirely absent (D-03 trigger beyond A5):
    - If Message-ID itself was stripped by the receiving mail server (not just the ID value being overwritten), note the failure and document exactly which header was missing.
    - The D-03a subject-token (Branch B) handles the ID-overwrite case. If ALL threading headers are absent, escalate and document before proceeding.
  </how-to-verify>
  <resume-signal>
    REQUIRED: State which A5 branch was taken (A5 PASSED or A5 FAILED + Branch-B remediation complete) — 06-06 does NOT proceed until one of these is confirmed. Also confirm: (a) Step 0.5 DDL apply completed and both verifications passed (record_only column present; demo_sender_bindings table present); (b) address topology (Reply-To observed in the outbound email, inbound .resend.app address confirmed, RESEND_REPLY_TO set in Render env).

    If A5 PASSED:
    "roundtrip-ok: ddl-applied=confirmed (record_only column present; demo_sender_bindings table present); demo-identity={business}@{real-email}; address-topology=confirmed (inbound=payroll@jiodnel.resend.app, outbound-from=onboarding@resend.dev, reply-to=payroll@jiodnel.resend.app set via RESEND_REPLY_TO); A1={exact header key casing from WEBHOOK_DEBUG inbound log}; A5=PASSED (DB SELECT in_reply_to WHERE direction='inbound' returns synthetic id matching outbound DB message_id — DB query is the PRIMARY evidence source; LOG_WEBHOOK_DEBUG_IDS confirmed 'In-Reply-To' key present in header_keys); A6={email_id same/different from rfc_message_id}; threading-headers=all-three-present; threading survived reply→resume; 06-06 UNBLOCKED"

    If A5 FAILED and Branch-B (D-03a subject-token) remediation completed:
    "roundtrip-ok-with-a5-remediation: ddl-applied=confirmed; demo-identity={business}@{real-email}; address-topology=confirmed (inbound=payroll@jiodnel.resend.app, outbound-from=onboarding@resend.dev, reply-to=payroll@jiodnel.resend.app); A1={casing}; A5=FAILED (DB SELECT in_reply_to WHERE direction='inbound' returns {actual-rfc-id} — does NOT match outbound DB message_id; Resend overwrote the synthetic header; From=onboarding@resend.dev cannot serve as alternative anchor — subject-token is now primary); A5-remediation=COMPLETE (D-03a subject-token implemented: [#token] embedded in clarification subject, subject-token router path added, re-run verified reply routing via subject-token — run_id set in inbound reply DB row); A6={...}; threading-headers={...}; 06-06 UNBLOCKED"

    If round-trip failed entirely:
    "roundtrip-FAIL: [describe what failed — DDL apply error, missing table/column, missing header, wrong key, pipeline error, A5 remediation not yet complete, RESEND_REPLY_TO not set or set to wrong address, webhook created before Render existed]"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Personal email client → Resend inbound (payroll@jiodnel.resend.app) | Real SMTP; headers subject to mail client/server rewriting; Reply-To directs replies to this address (not onboarding@resend.dev From) |
| Resend infrastructure → /webhook/inbound | Signed POST; verify confirms authenticity; webhook endpoint created AFTER Render service exists |
| Render logs + LOG_WEBHOOK_DEBUG_IDS | Temporary debug logging logs ONLY header keys and IDs — no body content, no addresses, no PII; MUST be removed in 06-06 |
| onboarding@resend.dev → operator's email | Free-tier constraint: can only send TO Resend account-owner; Reply-To ensures replies route to inbound address; documented in README For Engineers |
| bootstrap non-reset apply → live Supabase DB | Applies idempotent DDL (CREATE TABLE IF NOT EXISTS / ADD COLUMN IF NOT EXISTS) without dropping tables or seeded rows; the only unconditional DROP is name_matches (D-21-06, already gone); confirmed safe by reading bootstrap.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-00 | Integrity | Post-06-08 DDL apply with --reset wipes seeded prod data | mitigate | Step 0.5 explicitly uses bootstrap WITHOUT --reset; the runbook shows the exact command; the done-criterion for Step 0.5 requires both verifications (record_only + demo_sender_bindings) pass before proceeding; if either is missing, re-check that schema.sql was updated in 06-08 and rerun bootstrap (still no --reset) |
| T-06-05-01 | Information Disclosure | Temporary LOG_WEBHOOK_DEBUG_IDS log in gateway.py / Render logs | mitigate | Logs ONLY header key names + message IDs (no body, no addresses, no PII); explicitly scoped TEMPORARY — removed in 06-06 Task 1; Render logs are not public |
| T-06-05-02 | Spoofing | Real Resend webhook signature test confirms D-17 in production | accept | This is the positive confirmation; the 06-01/06-04 tests cover the reject path |
| T-06-05-03 | Information Disclosure | API key and signing secret set in Render env (not committed) | mitigate | Set via Render dashboard UI; never in render.yaml (sync:false); never on camera (D-09) |
| T-06-05-04 | Information Disclosure | LOG_WEBHOOK_DEBUG_IDS flag inadvertently left enabled | mitigate | Flag must be removed from Render env AND the log line removed from gateway.py in 06-06; 06-06's done criteria explicitly checks for its removal |
| T-06-05-05 | Integrity | A5 fails silently — Resend overwrites Message-ID and routing breaks without detection | mitigate | MEDIUM-2/MEDIUM-3: A5 is an explicit conditional BLOCKING gate; PRIMARY evidence is DB SELECT in_reply_to WHERE direction='inbound' (reads stored value from inbound reply row); LOG_WEBHOOK_DEBUG_IDS confirms In-Reply-To KEY present but NOT its value; if DB in_reply_to != DB message_id from outbound row, Branch-B D-03a subject-token path runs before 06-06; resume-signal requires explicit A5 branch declaration |
| T-06-05-06 | Integrity | Branch B (D-03a subject-token) introduces parallel-match bug (token fires on same reply as header-chain) | mitigate | D-03a spec (CONTEXT.md): subject-token fires ONLY on header no-match, NEVER in parallel — the lookup order is strict: (1) RFC header chain; (2) subject-token ONLY if (1) returns no match |
| T-06-05-07 | Spoofing | Reply-To not set → client replies to onboarding@resend.dev → reply never reaches inbound webhook → clarify loop breaks | mitigate | RESEND_REPLY_TO must be confirmed in Render env before round-trip (STEP 2 verification); address topology confirmed in resume-signal; if reply goes to onboarding@resend.dev instead of the .resend.app inbound address, re-check RESEND_REPLY_TO setting and gateway.py conditional logic |
| T-06-05-08 | Integrity | Webhook created before Render service exists → webhook URL is wrong → no inbound delivery | mitigate | Webhook creation ordering: create webhook AFTER 06-03 Render deploy (Render URL known); user_setup task specifies this explicitly; if the webhook points to a wrong URL, delete it and re-create with the correct Render URL |
| T-06-05-SC | Tampering | Resend inbound domain/webhook configuration | accept | User-controlled account; signed webhooks prevent external tampering |
</threat_model>

<verification>
Human confirms in resume-signal:
0.5. STEP 0.5 DDL APPLY: DATABASE_URL=<session-5432> uv run python -m app.db.bootstrap (no --reset) completed successfully; SELECT column_name FROM information_schema.columns WHERE table_name='payroll_runs' AND column_name='record_only' returned one row; SELECT to_regclass('public.demo_sender_bindings') returned non-null; seed data (businesses + employees) confirmed intact
0. HIGH-2: Demo identity set up — demo_sender_bindings row maps pjnhek@gmail.com → target business_id (businesses.contact_email NOT mutated); DEMO_CONTACT_EMAIL and DEMO_BUSINESS_NAME set in Render env; 06-06 demo-reset will re-arm this binding row after seed()
1. ADDRESS TOPOLOGY (Pass-6): Inbound address confirmed (payroll@jiodnel.resend.app); RESEND_REPLY_TO confirmed in Render env; outbound sender constraint noted (onboarding@resend.dev → account-owner only); webhook created AFTER Render service existed (06-03 dependency honored)
2. A5 GATE (MEDIUM-2/MEDIUM-3): EITHER "A5=PASSED (DB SELECT in_reply_to WHERE direction='inbound' matched synthetic outbound message_id — DB query is PRIMARY evidence, not debug log)" OR "A5=FAILED + Branch-B D-03a subject-token implemented and re-run verified (run_id set in inbound reply DB row); address asymmetry (From unreachable) means subject-token is now primary anchor" — 06-06 only proceeds after one of these is explicitly confirmed
3. "roundtrip-ok" — real email from the demo identity sender arrived at Resend inbound, triggered the webhook, pipeline ran, run reached awaiting_approval (find_business_by_sender matched via additive demo_sender_bindings fallback)
4. A1 resolved: EXACT header key casing from `WEBHOOK_DEBUG header_keys=[...]` log in gateway.py (INBOUND parse — before normalization; affects case-insensitive normalization — if title-case, the existing lowercasing in parse_inbound handles it correctly)
5. A6 resolved: webhook email_id vs rfc_message_id comparison documented (confirms the dedup key is correct — 06-04 uses email_obj.message_id from the full fetch)
6. LOW-8 resolved: three threading headers (Message-ID, In-Reply-To, References) confirmed present as key names in the real round-trip WEBHOOK_DEBUG log — this evidence belongs here, not at the thin-deploy gate (06-03)
7. Threading survived (reply→resume path worked) OR Branch-B D-03a remediation complete and re-verified
</verification>

<success_criteria>
- Step 0.5 DDL apply: DATABASE_URL=<session-5432> uv run python -m app.db.bootstrap (no --reset) succeeded; payroll_runs.record_only column confirmed present via information_schema; demo_sender_bindings table confirmed present via to_regclass; seeded data intact; this apply completed BEFORE /demo/bind or /demo/compose were exercised on the deployed service
- Production demo identity set up: demo_sender_bindings row maps the round-trip sender (pjnhek@gmail.com) → target business_id; find_business_by_sender resolves it via the additive fallback; businesses.contact_email unchanged — HIGH-2
- DEMO_CONTACT_EMAIL and DEMO_BUSINESS_NAME set in Render env (used by demo_reset.py in 06-06 for identity re-application — NOT used as a global from_addr override at send time)
- ADDRESS TOPOLOGY (Pass-6): confirmed — inbound=payroll@jiodnel.resend.app (wired to webhook); outbound-from=onboarding@resend.dev (free tier, account-owner only; noted in README For Engineers); RESEND_REPLY_TO set in Render env pointing to inbound address; Resend webhook endpoint created AFTER Render service existed from 06-03 (required for the webhook URL to be known); Reply-To observed in outbound email and reply routed to inbound address
- Real email send→reply→approve round-trip completed on the deployed Render service
- A5 GATE (MEDIUM-2/MEDIUM-3): explicitly resolved — either PASSED (synthetic id preserved, confirmed by DB SELECT in_reply_to WHERE direction='inbound' returning matching value) or FAILED + Branch-B complete (D-03a subject-token implemented: [#token] in clarification subject + subject-token router path + re-run verified via run_id set in inbound reply row); 06-06 is only unblocked after this gate is explicitly confirmed in the resume-signal
- A5 evidence correctly sourced: PRIMARY source is DB query SELECT in_reply_to FROM email_messages WHERE direction='inbound' ORDER BY created_at DESC LIMIT 1 — NOT the debug log (which logs key names only, not values) and NOT an outbound webhook (no such event exists in Resend)
- All three ⚠ CONFIRM flags (A1, A5, A6) resolved with actual observed values: A1 from LOG_WEBHOOK_DEBUG_IDS header_keys (INBOUND path key names); A5 from DB SELECT in_reply_to; A6 from WEBHOOK_DEBUG email_id vs rfc_message_id comparison
- LOW-8: all three threading headers (Message-ID, In-Reply-To, References) confirmed present as key names in the real round-trip WEBHOOK_DEBUG log — this evidence belongs here, not at the thin-deploy gate (06-03)
- Signing secret confirmed working (not placeholder)
- Threading headers confirmed surviving a real round-trip (Branch A) OR D-03a subject-token fallback implemented and verified (Branch B)
- LOG_WEBHOOK_DEBUG_IDS flag is in place (temporary) and will be removed in 06-06
- Demo recording (06-06) is unblocked ONLY after A5 gate is satisfied
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-05-SUMMARY.md` when done, recording: (1) Step 0.5 DDL apply outcome (bootstrap no-reset completed; both SQL verifications passed; seeded data intact); (2) which business the demo_sender_bindings row armed for the production demo identity (operator_email → business_id; businesses.contact_email was NOT mutated) (HIGH-2 — 06-06 needs this to re-arm the binding during reset); (3) ALL ⚠ CONFIRM flag resolutions for A1, A5, A6; (4) the A5 evidence source (DB SELECT in_reply_to WHERE direction='inbound' — PRIMARY; LOG_WEBHOOK_DEBUG_IDS header_keys confirms key presence only, not value; NOT from outbound webhook); (5) which A5 branch was taken: PASSED or FAILED+Branch-B-D-03a-complete; (6) the threading headers survival result and exact casing observed (LOW-8); (7) ADDRESS TOPOLOGY confirmed: inbound address, outbound From, RESEND_REPLY_TO value set in Render env, webhook creation ordering (after 06-03).
</output>
