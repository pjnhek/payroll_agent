---
phase: "06"
plan: "05"
type: execute
wave: 4
depends_on:
  - "06-04"
autonomous: false
files_modified: []
requirements:
  - OPS-02

user_setup:
  - service: resend
    why: "Real inbound email provider for send→reply→approve round-trip (D-09b)"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard → API Keys → Create API Key (or use existing)"
      - name: WEBHOOK_SIGNING_SECRET
        source: "Resend Dashboard → Webhooks → Add Endpoint → Signing Secret (shown after creation)"
    dashboard_config:
      - task: "Create or verify a Resend account and create an API key"
        location: "resend.com → Sign in → API Keys"
      - task: "Configure inbound email: add or verify a domain (or use the auto-created .resend.app address for testing)"
        location: "Resend Dashboard → Domains (or use the free shared inbound test address)"
      - task: "Add the webhook endpoint pointing to your Render URL"
        location: "Resend Dashboard → Webhooks → Add Endpoint → URL: https://{render-url}/webhook/inbound → select event: email.received → copy the Signing Secret"
      - task: "Update the Render service env vars: set RESEND_API_KEY and WEBHOOK_SIGNING_SECRET to the real values (replacing the placeholders set in 06-03)"
        location: "Render service dashboard → Environment tab → edit RESEND_API_KEY and WEBHOOK_SIGNING_SECRET"

must_haves:
  truths:
    - "A real email sent to the Resend inbound address triggers a POST to /webhook/inbound on the deployed service"
    - "Signature verification passes for a real Resend webhook delivery (signing secret confirmed real, not placeholder)"
    - "The three threading headers (Message-ID, In-Reply-To, References) survive the send→reply round-trip"
    - "raw email_obj.headers dict is logged during the round-trip to resolve ⚠ A1 (header key casing)"
    - "data.message_id vs data.email_id in the raw webhook payload is confirmed (⚠ A6)"
    - "Resend outbound message_id is accessible and storable (⚠ A5 resolved)"
    - "The full pipeline runs end-to-end on the deployed service for one real inbound email"
  artifacts: []
  key_links:
    - from: "Real email client (user's personal email)"
      to: "Resend inbound webhook → /webhook/inbound on Render"
      via: "Resend email.received event → POST to webhook endpoint"
      pattern: "email\\.received"
---

<objective>
Wave 4 — BLOCKING human checkpoint (autonomous:false). The email round-trip verify gate (D-09b). The agent has wired the real Resend provider; the human now sends a real email and confirms the three threading headers survive the send→reply→approve loop on the deployed service.

Purpose: This is the ONE assumption fixtures cannot test — real sender-client header survival. The EMAIL-01 stub always preserved headers because we minted them. Real mail clients (especially Outlook/Exchange) can mangle the References header. The only proof is a real send→reply→approve round-trip. This gate also resolves three ⚠ CONFIRM flags from RESEARCH.md: A1 (header casing), A5 (outbound Message-ID settable), and A6 (webhook data.message_id vs data.email_id). It also confirms the Resend signing secret is real (not a placeholder).

Output: Confirmed real email delivery, confirmed header survival, confirmed signature verification. The ⚠ CONFIRM flags A1/A5/A6 are resolved with actual observations. The D-09b gate closes, unblocking demo recording (06-06).
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/email/gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-09b — Resend account setup + real email round-trip verify</name>
  <what-built>The agent has implemented the real gateway (06-04): verify(), two-step parse_inbound, and D-13c send_outbound are all in place. The Render service has placeholder RESEND_API_KEY and WEBHOOK_SIGNING_SECRET from 06-03. This task replaces those placeholders and does the first real end-to-end email round-trip.</what-built>
  <how-to-verify>
    STEP 1 — Resend account + inbound setup:
    - Log into resend.com. Create an API key with full access (or inbound+send scope).
    - Go to Resend Webhooks → Add Endpoint:
      - URL: `https://{your-render-url}/webhook/inbound`
      - Events: `email.received`
    - Copy the Signing Secret shown after creation.
    - Go to Resend Domains. For testing, you can use the shared Resend inbound test address (it auto-routes to your webhook) OR configure your own domain. The shared address format is typically `{something}@inbound.resend.app`.

    STEP 2 — Update Render env vars (replace placeholders from 06-03):
    - Render service → Environment tab → set:
      - `RESEND_API_KEY` = real API key from Step 1
      - `WEBHOOK_SIGNING_SECRET` = Signing Secret from Step 1
    - Wait for Render to redeploy (it redeploys on env var change). Confirm `Application startup complete` in Render logs.

    STEP 3 — Enable raw payload logging (TEMPORARY — remove after this gate):
    - Add a temporary log line in app/main.py's /webhook/inbound handler AFTER `email = gateway.parse_inbound(raw_body)`:
      ```python
      import json as _json
      logger.info("DEBUG_ROUND_TRIP payload=%s headers=%s", raw_body.decode()[:500], dict(request.headers))
      logger.info("DEBUG_ROUND_TRIP email_obj headers=%s", email.in_reply_to)
      ```
    - Deploy this temporary logging change. Remove it in 06-06.

    STEP 4 — Do the round-trip:
    - Send a real payroll email FROM your personal email TO the Resend inbound address.
    - Watch the Render logs for: "Application startup" (if it was sleeping), "DEBUG_ROUND_TRIP", and any pipeline progress.
    - Wait for the run to reach `awaiting_approval` (pipeline runs successfully).
    - Reply to the confirmation/clarification email (if clarify) OR approve from the dashboard.

    STEP 5 — Confirm the three ⚠ CONFIRM flags from RESEARCH.md:
    - A1: In the Render logs, look for `email_obj.headers` dict. Record the EXACT keys for In-Reply-To and References (are they `"In-Reply-To"` or `"in-reply-to"`?). Document in your reply.
    - A5: In the Render logs, look for the outbound `send_outbound` return value. Is it a real RFC Message-ID (e.g., `<uuid@resend.dev>`) or a Resend internal ID? Document.
    - A6: In the `DEBUG_ROUND_TRIP payload=` log line, look at the raw webhook payload `data` object. Does it include `message_id` (RFC) as well as `email_id` (Resend internal)? Document.

    STEP 6 — Confirm threading headers survived:
    - Send a reply to the Resend-sent outbound email from your personal client.
    - Watch Render logs. The reply-routing path in /webhook/inbound should pick up the `In-Reply-To` header and route to the awaiting_reply run. If it does, threading survived.

    STEP 7 — If threading headers did NOT survive (D-03 trigger):
    - Note the failure in your reply and document exactly which header was missing.
    - The fallback (D-03a subject-token) is out of scope until confirmed needed. For now, document the failure and decide whether to implement D-03a before recording.
  </how-to-verify>
  <resume-signal>
    Type "roundtrip-ok" with observations for A1, A5, A6. Example:
    "roundtrip-ok: A1=In-Reply-To key was lowercase 'in-reply-to' in headers dict; A5=Resend send returns Resend internal ID, stored as message_id; A6=webhook data.message_id IS the RFC Message-ID (not email_id); threading survived reply→resume."
    OR: "roundtrip-FAIL: [describe what failed — missing header, wrong key, pipeline error]"
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Personal email client → Resend inbound | Real SMTP; headers subject to mail client/server rewriting |
| Resend infrastructure → /webhook/inbound | Signed POST; verify confirms authenticity |
| Render logs | Temporary debug logging exposes email payload — MUST be removed in 06-06 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Information Disclosure | Temporary DEBUG_ROUND_TRIP log in Render logs | mitigate | Explicitly scoped TEMPORARY — removed in 06-06 Task 1; Render logs are not public but should not store payload data indefinitely |
| T-06-05-02 | Spoofing | Real Resend webhook signature test confirms D-17 in production | accept | This is the positive confirmation; the 06-01/06-04 tests cover the reject path |
| T-06-05-03 | Information Disclosure | API key and signing secret set in Render env (not committed) | mitigate | Set via Render dashboard UI; never in render.yaml (sync:false); never on camera (D-09) |
| T-06-05-SC | Tampering | Resend inbound domain/webhook configuration | accept | User-controlled account; signed webhooks prevent external tampering |
</threat_model>

<verification>
Human confirms in resume-signal:
1. "roundtrip-ok" — real email arrived at Resend inbound, triggered the webhook, pipeline ran, run reached awaiting_approval
2. A1 resolved: exact header key casing documented (affects case-insensitive normalization in gateway.py — if mixed case, confirm the normalization handles it)
3. A5 resolved: outbound Message-ID confirmed accessible (from Resend send response)
4. A6 resolved: webhook data.message_id field confirmed to be the RFC Message-ID (or email_id is used for dedup)
5. Threading survived (reply→resume path worked) OR failure documented with D-03a plan
</verification>

<success_criteria>
- Real email send→reply→approve round-trip completed on the deployed Render service
- All three ⚠ CONFIRM flags (A1, A5, A6) resolved with actual observed values
- Signing secret confirmed working (not placeholder)
- Threading headers confirmed surviving a real round-trip (or failure documented with mitigation plan)
- Demo recording (06-06) is unblocked
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-05-SUMMARY.md` when done, recording ALL ⚠ CONFIRM flag resolutions for A1, A5, A6, and the threading survival result.
</output>
