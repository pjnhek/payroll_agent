---
phase: "06"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - tests/test_gateway.py
  - tests/test_ingest.py
  - tests/test_dashboard.py
  - tests/conftest.py
autonomous: true
requirements:
  - OPS-01
  - OPS-02

must_haves:
  truths:
    - "uv run pytest tests/test_gateway.py -k 'verify or signature' -x -q exits 0 with xfail (XFAIL, not FAILED — suite stays green between waves)"
    - "uv run pytest tests/test_gateway.py -k 'two_step or parse_inbound' -x -q exits 0 with xfail"
    - "uv run pytest tests/test_gateway.py -k 'threading or references or reserved' -x -q exits 0 with xfail (threading test asserts chain built from prior DB outbound row, not from passed-in values only)"
    - "uv run pytest tests/test_dashboard.py -k 'live or health' -x -q exits 0 with xfail"
    - "Existing 422 mocked tests remain green throughout (no-op-swap invariant guard)"
    - "uv run pytest -q -m 'not integration and not live_llm' exits 0 (no FAILED — xfail stubs do not poison the suite)"
    - "`resend==2.32.2` appears in pyproject.toml [project.dependencies]"
  artifacts:
    - path: "pyproject.toml"
      provides: "resend==2.32.2 added to runtime deps"
      contains: "resend==2.32.2"
    - path: "tests/test_gateway.py"
      provides: "xfail stubs for verify, two-step parse, threading, reserved-before-send (all marked xfail strict=True)"
      min_lines: 450
    - path: "tests/test_ingest.py"
      provides: "Extended dedup tests for duplicate delivery gate"
      min_lines: 120
    - path: "tests/test_dashboard.py"
      provides: "Health liveness and readiness test stubs (xfail until 06-02 adds routes)"
      min_lines: 670
    - path: "tests/conftest.py"
      provides: "Mock Resend SDK fixtures and ReceivedEmail stub"
  key_links:
    - from: "tests/test_gateway.py"
      to: "app/email/gateway.py"
      via: "monkeypatch.setattr on resend.Webhooks.verify, resend.EmailsReceiving.get, resend.Emails.send"
      pattern: "monkeypatch\\.setattr.*resend"
    - from: "tests/test_dashboard.py"
      to: "app/main.py /health/live and /health/ready"
      via: "TestClient GET requests"
      pattern: "client\\.get.*health"
---

<objective>
Wave 0: Add the resend runtime package and write the xfail test stubs that gate every Phase 6 behavior. These tests are xfail(strict=True) now; they flip to XPASS (a real failure) exactly when implementation plans (06-04) land and make them green.

Purpose: The nyquist rule — every task has an automated verify before the code exists. These stubs guard the no-op-swap invariant (422 existing mocked tests must stay green throughout), establish the mock seams for the Resend SDK so later plans have clean test infrastructure, and encode the security/correctness requirements (D-13 dedup, D-17 signature verify, D-14 threading, D-01a two-step parse, D-20 health routes) as assertions before any implementation.

MEDIUM-5 fix (Codex review): All gateway test stubs that target not-yet-implemented behavior are marked `@pytest.mark.xfail(strict=True, reason="implemented in 06-04")`. This keeps the no-op-swap gate (`pytest -m "not integration and not live_llm"` green) CLEAN between waves 1 and 3. The xfail markers flip to a real XPASS failure (test suite exits non-zero) exactly when 06-04 implements the real gateway — that XPASS is the trigger to REMOVE the markers in 06-04. Plans 06-04 tasks explicitly remove the xfail markers as part of going green.

Output: resend==2.32.2 in pyproject.toml; xfail gateway tests covering verify/two-step/threading/reserved; extended ingest dedup tests; extended dashboard health tests (xfail); conftest mock fixtures for Resend SDK seams.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-VALIDATION.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-PATTERNS.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/tests/test_gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/tests/test_ingest.py
@/Users/pnhek/usf msds/github/payroll_agent/tests/test_dashboard.py
@/Users/pnhek/usf msds/github/payroll_agent/tests/conftest.py
@/Users/pnhek/usf msds/github/payroll_agent/app/email/gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
@/Users/pnhek/usf msds/github/payroll_agent/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resend==2.32.2 runtime dep and conftest mock fixtures</name>
  <files>pyproject.toml, uv.lock, tests/conftest.py</files>
  <read_first>
    - pyproject.toml (existing [project.dependencies] block to see pinning pattern)
    - tests/conftest.py (existing FakeConnection, fake_conn fixture, mock_llm pattern at lines 37-123 and 488-498 from PATTERNS.md)
    - 06-RESEARCH.md §"Package Legitimacy Audit" and §1 (resend.ReceivedEmail schema, headers dict shape)
  </read_first>
  <action>
    Run `uv add resend==2.32.2` from the repo root — this updates pyproject.toml and uv.lock atomically (per D-02 in CONTEXT.md: never hand-edit uv.lock; never a tracked requirements.txt).

    Then extend tests/conftest.py with shared Resend mock fixtures. Read the existing conftest pattern carefully — use the same monkeypatch style as mock_llm (lines 488-498 in PATTERNS.md). Add:

    (a) A `fake_received_email` fixture that returns a minimal object mimicking `resend.ReceivedEmail` — attributes: `message_id` (str), `text` (str), `html` (None), `headers` (dict with mixed-case keys like `"In-Reply-To"` and `"in-reply-to"` as alternatives). This fixture exercises the A1 assumption (header key casing from real providers).

    (b) A `mock_resend_verify` fixture that uses monkeypatch to replace `resend.Webhooks.verify` with a no-op (passes). A separate variant raises `ValueError("bad sig")` for reject tests.

    (c) A `mock_resend_send` fixture that monkeypatches `resend.Emails.send` to return `{"id": "<out-test@resend.com>"}` without hitting the network.

    Fixtures use the existing conftest pattern: `@pytest.fixture` with `monkeypatch` parameter. Add `import resend` at the top of conftest under the existing imports block (after `from app...` imports). Mark the file with a comment `# Phase 6 Resend SDK mocks — added 06-01`.

    Do NOT import resend at module-level in conftest without a guard — the package must be importable before the tests run (it will be installed via `uv add`).
  </action>
  <verify>
    <automated>uv run pytest -q -m "not integration and not live_llm" 2>&amp;1 | tail -5</automated>
  </verify>
  <done>resend==2.32.2 appears in `grep 'resend' pyproject.toml`; `uv run python -c "import resend; print(resend.__version__)"` prints 2.32.2; existing 422 mocked tests still green after conftest additions.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Write xfail gateway, ingest, and dashboard tests (Wave 0 stubs)</name>
  <files>tests/test_gateway.py, tests/test_ingest.py, tests/test_dashboard.py</files>
  <read_first>
    - tests/test_gateway.py (full file — existing patterns, imports, FakeConnection usage, uuid fixtures)
    - tests/test_ingest.py (full file — existing dedup test structure and TestClient usage)
    - tests/test_dashboard.py (full file — existing TestClient pattern, health test locations)
    - tests/conftest.py (after Task 1 additions — the new Resend mock fixtures to import)
    - 06-PATTERNS.md §"tests/test_gateway.py" (exact test shapes to implement: test_verify_raises_on_bad_signature, test_parse_inbound_two_step_fetch, test_parse_inbound_normalizes_headers_case_insensitively, test_send_outbound_reserved_before_sent_ordering)
    - 06-VALIDATION.md §"Per-Task Verification Map" (the exact test names keyed by requirement ID)
  </read_first>
  <behavior>
    tests/test_gateway.py NEW tests — ALL gateway tests targeting not-yet-implemented behavior MUST be decorated with `@pytest.mark.xfail(strict=True, reason="implemented in 06-04")`. This is the MEDIUM-5 fix: strict=True means the test is EXPECTED to fail; when 06-04 implements the real gateway and the test PASSES, pytest reports XPASS and exits non-zero — that is the signal to remove the xfail marker in 06-04. The no-op-swap gate (`uv run pytest -q -m "not integration and not live_llm"`) stays GREEN (exits 0) throughout waves 1–2.

    - test_verify_raises_on_bad_signature (xfail strict=True): monkeypatch `resend.Webhooks.verify` to raise `ValueError("bad sig")`; call `gateway.verify(b"payload", {"svix-id": "x", "svix-timestamp": "y", "svix-signature": "z"}, "secret")`; assert raises `ValueError`. Tests OPS-02/D-17.

    - test_verify_passes_on_valid_signature (xfail strict=True): monkeypatch `resend.Webhooks.verify` to return None; call `gateway.verify(...)`; no exception raised. Tests OPS-02/D-17 (happy path).

    - test_parse_inbound_two_step_fetch (xfail strict=True): monkeypatch `resend.EmailsReceiving.get` to return a `_FakeReceivedEmail` with `message_id="<abc@resend.com>"`, `text="Maria 40 hours"`, `html=None`, `headers={"In-Reply-To": "<prev@x.test>", "References": "<prev@x.test>"}`. Input: raw webhook dict `{"data": {"email_id": "re_123", "from": "hr@acme.test", "to": ["agent@x.test"], "subject": "hours", "message_id": "<abc@resend.com>"}}`. Assert result is `InboundEmail`, `result.message_id == "<abc@resend.com>"`, `result.in_reply_to == "<prev@x.test>"`, `result.body_text == "Maria 40 hours"`. Tests OPS-02/D-01a/D-18.

    - test_parse_inbound_normalizes_headers_case_insensitively (xfail strict=True): same structure but `headers={"in-reply-to": "<prev@x.test>"}` (lowercase). Assert `result.in_reply_to == "<prev@x.test>"`. Tests OPS-02/D-18/Pitfall 4.

    - test_parse_inbound_dedup_keys_on_rfc_message_id (xfail strict=True): confirm the `InboundEmail.message_id` returned by the two-step fetch is the RFC message_id from `email_obj.message_id`, NOT the Resend internal `email_id`. Tests OPS-02/D-13 (dedup key correctness).

    - test_send_outbound_reserved_before_sent_ordering (xfail strict=True): monkeypatch `resend.Emails.send` to return `{"id": "<out@resend.com>"}`. Call `gateway.send_outbound(run_id=uuid.uuid4(), to_addr="x@test.com", subject="s", body="b", conn=fake_conn)`. Assert `fake_conn.executed[0]` (first SQL row) contains `"reserved"` in the params string — send_state='reserved' must be written to DB BEFORE the provider call. Tests OPS-02/D-13c/D-14.

    - test_send_outbound_failed_on_provider_exception (xfail strict=True): monkeypatch `resend.Emails.send` to raise `RuntimeError("network error")`. Call `gateway.send_outbound(run_id=uuid.uuid4(), to_addr="x@test.com", subject="s", body="b", conn=fake_conn)`. Assert: (1) the row was written with send_state='reserved' before the call, and (2) after the exception, the row is updated to send_state='failed', and (3) the RuntimeError propagates (is re-raised). Tests HIGH-3 fix — reserved→failed path.

    - test_threading_references_rebuilt_from_db_state (xfail strict=True): Verify that `send_outbound` LOADS the prior outbound references_header from DB state (via the repo helper) and BUILDS the full chain before persisting and sending. Setup: FakeConnection is pre-populated to return a prior outbound row with `references_header="<prior@x.test>"` when queried. Call `gateway.send_outbound(run_id=..., to_addr=..., subject=..., body=..., in_reply_to="<inbound@x.test>", conn=fake_conn)`. Assert that the DB INSERT params include a `references_header` containing BOTH `"<prior@x.test>"` AND `"<inbound@x.test>"` (the accumulated chain from DB state PLUS the new message_id), NOT just the caller-passed values verbatim. Tests OPS-02/D-14 durable-threading-from-DB-state.

    - test_inbound_reply_routes_to_correct_run (xfail strict=True): MEDIUM-7 fix — D-14 end-to-end reply-routing test. Setup: use FakeConnection pre-populated with (a) an outbound message row for run_A with message_id="<out@x.test>", and (b) run_A in status='awaiting_reply'. POST /webhook/inbound with a canonical InboundEmail fixture whose `in_reply_to="<out@x.test>"`. Assert that the reply-routing path in the route handler matches run_A (not None), and BackgroundTasks.add_task is called to resume run_A's pipeline — not a new run. This test is xfail until 06-04 fixes the route's dual-path handling and the reply-routing integration with the new raw-Request flow. Tests OPS-02/D-14.

    tests/test_gateway.py ALSO add a test that does NOT need xfail (canonical fixture path):
    - test_parse_inbound_canonical_fixture_still_works: IMPORTANT — no xfail on this test; it MUST stay GREEN throughout. Call `gateway.parse_inbound({"id": str(uuid4()), "message_id": "<test@fixture.test>", "from_addr": "hr@acme.test", "to_addr": "agent@test.com", "subject": "hours", "body_text": "Maria 40h"})` (the canonical InboundEmail dict shape). Assert the result is an InboundEmail with the expected message_id. This confirms the fixture/dev path through the gateway seam still works with the current stub gateway (it is NOT xfail because the current stub already handles this path). This is the HIGH-2 fixture-path guard pre-implementation.

    tests/test_ingest.py EXTENDED (extend existing file):
    - test_duplicate_delivery_pipeline_runs_once_unit: A MOCKED unit test (no live DB). Patch `BackgroundTasks.add_task` to a spy; call `POST /webhook/inbound` twice with the same `message_id` via `TestClient`. Assert `BackgroundTasks.add_task` is invoked at most once (the second delivery short-circuits before the pipeline task is enqueued). Must run in `uv run pytest -q -m "not integration and not live_llm"` — no `@pytest.mark.integration`. Use `monkeypatch` to stub `repo.insert_inbound_email` so the first call returns `(uuid4(), True)` (inserted) and the second returns `(None, False)` (duplicate skip). NO xfail on this test — the existing route + repo already handle this correctly. WARNING-1 (06-04 compatibility): 06-04 Task 2 changes the route signature to raw `Request` + `json.loads`. After 06-04 lands, if this test posts a Pydantic InboundEmail model object, update the test to POST a canonical InboundEmail dict serialized as raw JSON bytes (Path B shape) so it still passes without xfail.
    - test_duplicate_delivery_pipeline_runs_once: The INTEGRATION variant of the same behavior. Simulate two webhook deliveries with the same `message_id` against a live DB. The second delivery must return `{"status": "duplicate"}` and NOT create a second run. Assert that only one `payroll_runs` row exists for that message_id. Mark `@pytest.mark.integration`. Explicitly assert that `decide.py` is NOT called on the duplicate.

    tests/test_dashboard.py EXTENDED (extend existing file):
    - test_health_live_returns_200_no_db (xfail strict=True, reason="implemented in 06-02"): `client.get("/health/live")` → `assert response.status_code == 200` and `response.json()["status"] == "ok"`. xfail until 06-02 adds the route.
    - test_health_ready_returns_200_with_db (xfail strict=True, reason="implemented in 06-02"; also mark `@pytest.mark.integration`): `client.get("/health/ready")` → `assert response.status_code == 200`. xfail until 06-02 adds the route.
  </behavior>
  <action>
    Extend tests/test_gateway.py by appending the new test functions after the existing tests. Follow the existing file's import block and fixture usage exactly — the file already uses `FakeConnection`, `fake_conn` fixture from conftest, and `monkeypatch`. Add `import resend` in the test file (it is now installed). Use `_FakeReceivedEmail` as a local inner class in the test functions (not a fixture) so each test is self-contained.

    CRITICAL: Every new gateway test targeting Phase-6 behavior (verify, two_step parse, threading, reserved-before-send, send_failed_on_exception, reply_routing) MUST be decorated with `@pytest.mark.xfail(strict=True, reason="implemented in 06-04")`. Do NOT mark the existing stub-compatible test (test_parse_inbound_canonical_fixture_still_works) as xfail — it must stay GREEN immediately.

    For `test_send_outbound_failed_on_provider_exception`: structure the xfail test to assert that after `resend.Emails.send` raises, the FakeConnection recorded a second SQL update with send_state='failed'. Assert the exception propagates.

    For `test_inbound_reply_routes_to_correct_run`: use a monkeypatched `repo.find_awaiting_reply_for_header` that returns a fake run_id (simulating a match), and spy on `background_tasks.add_task`. The test is xfail because the route currently uses a Pydantic InboundEmail body param (not raw Request), so the routing flow will be restructured in 06-04.

    Extend tests/test_ingest.py by appending after the existing tests. Add BOTH the mocked unit variant (test_duplicate_delivery_pipeline_runs_once_unit — no @pytest.mark.integration; uses monkeypatch to stub repo.insert_inbound_email return value + spies on BackgroundTasks.add_task) and the integration variant (test_duplicate_delivery_pipeline_runs_once — @pytest.mark.integration; hits the live DB). The unit variant must be runnable in `uv run pytest -q -m "not integration and not live_llm"`. Neither of these dedup tests needs xfail — the existing route already handles this.

    Extend tests/test_dashboard.py by appending at the end. The health route tests use the existing `client = TestClient(app)` module-level client already in the file. Both health tests get xfail(strict=True, reason="implemented in 06-02").

    After writing all tests, run the full mocked suite to confirm the suite exits 0 (no FAILED — only XFAIL) and all existing 422 pass (GREEN). XFAIL is not a failure; the suite exit code must be 0.
  </action>
  <verify>
    <automated>uv run pytest tests/test_gateway.py tests/test_ingest.py tests/test_dashboard.py -v --tb=short -q 2>&amp;1 | grep -E "(PASSED|FAILED|XFAIL|XPASS|ERROR)" | tail -30</automated>
  </verify>
  <done>
    New gateway tests exist and are XFAIL (not FAILED — exit code 0). `uv run pytest -q -m "not integration and not live_llm"` shows 422 passed + N xfailed + 0 failed. test_parse_inbound_canonical_fixture_still_works PASSES immediately (GREEN — fixture path works with current stub). test_duplicate_delivery_pipeline_runs_once_unit PASSES immediately (the existing route + repo already handle dedup). test_health_live_returns_200_no_db is XFAIL (route not yet created). test_parse_inbound_two_step_fetch is XFAIL (current gateway doesn't call EmailsReceiving.get).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite → gateway seam | Tests mock at resend module boundary; no real network in mocked suite |
| pyproject.toml → uv.lock | uv add atomically updates both; lock is the source of truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-SC | Tampering | `uv add resend==2.32.2` install | mitigate | Package legitimacy audit in 06-RESEARCH.md (resendlabs org, 2.6M/wk downloads, 58 versions — approved); pin exact version `==2.32.2`; `uv.lock` hash-pins the wheel |
| T-06-01-02 | Spoofing | test mock boundary — `monkeypatch.setattr(resend.Webhooks, "verify", ...)` | accept | Test-only scope; mocks exercise the CALL path but cannot substitute for real HMAC verification; D-17 real verify tested at D-09b human gate |
| T-06-01-03 | Integrity | conftest.py Resend mock fixtures leak to non-gateway tests | mitigate | Fixtures are function-scoped (pytest default); monkeypatch auto-reverts after each test; no module-level state change |
</threat_model>

<verification>
After both tasks:
1. `uv run python -c "import resend; print(resend.__version__)"` → `2.32.2`
2. `uv run pytest -q -m "not integration and not live_llm"` → all 422 existing tests pass + N xfailed (0 failed — suite exits 0)
3. `grep "resend==2.32.2" pyproject.toml` → exits 0
4. `uv run pytest tests/test_gateway.py -k "canonical_fixture" -v` → PASSED (fixture path GREEN immediately)
5. `uv run pytest tests/test_gateway.py -k "verify" -v` → XFAIL (not FAILED)
6. `uv run pytest tests/test_ingest.py -k "duplicate_unit" -v -m "not integration"` → PASSED (existing dedup already works)
</verification>

<success_criteria>
- resend==2.32.2 installed and importable (per D-02, no svix dep needed)
- Existing 422 mocked tests remain green (no-op-swap invariant — the guard for all of Phase 6)
- New tests are intentionally XFAIL(strict=True): they encode the security and correctness requirements before any implementation, and the suite STILL exits 0
- test_parse_inbound_canonical_fixture_still_works is GREEN immediately — the fixture/dev path works now and must not regress
- Mock fixtures in conftest provide clean Resend SDK seams for 06-03 and 06-04
- xfail markers are the contract with 06-04: when 06-04 lands and tests XPASS, remove the markers
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-01-SUMMARY.md` when done
</output>
