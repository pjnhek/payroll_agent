---
phase: "06"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - render.yaml
  - app/config.py
  - app/main.py
  - .env.example
  - .github/workflows/keepalive.yml
autonomous: true
requirements:
  - OPS-01
  - OPS-03

must_haves:
  truths:
    - "Dockerfile builds cleanly with `docker build -t payroll-agent .` from repo root"
    - "Built image binds to 0.0.0.0 on CMD startup (shell form with ${PORT:-10000})"
    - "WORKDIR=/app so eval/chart.svg, app/templates, app/static resolve correctly"
    - "`uv sync --frozen --no-dev` is the install command in the BUILDER stage (no tracked requirements.txt)"
    - "Runtime stage CMD uses `.venv/bin/uvicorn` directly — NO `uv run` at runtime (no uv binary in runtime stage)"
    - "/health/live returns 200 with no DB hit (liveness probe, Render deploy health check)"
    - "/health/ready route exists (readiness probe for the keep-alive cron target)"
    - "render.yaml exists with healthCheckPath: /health/live and sync:false for all secrets"
    - ".github/workflows/keepalive.yml exists with cron + workflow_dispatch trigger"
    - "RESEND_API_KEY, WEBHOOK_SIGNING_SECRET, and RESEND_FROM_ADDR added to Settings + .env.example + render.yaml"
  artifacts:
    - path: "Dockerfile"
      provides: "Multi-stage uv-in-image container (builder + runtime stages); runtime CMD uses .venv/bin/uvicorn directly"
      contains: ".venv/bin/uvicorn"
    - path: ".dockerignore"
      provides: "Exclude .venv, .git, tests, .env, .planning from image"
      contains: ".venv/"
    - path: "render.yaml"
      provides: "Render Blueprint with healthCheckPath and sync:false secrets"
      contains: "healthCheckPath: /health/live"
    - path: "app/config.py"
      provides: "resend_api_key and webhook_signing_secret fields in Settings"
      contains: "resend_from_addr"
    - path: "app/main.py"
      provides: "GET /health/live (no DB) and GET /health/ready (SELECT) routes"
      contains: "/health/live"
    - path: ".github/workflows/keepalive.yml"
      provides: "Cron + workflow_dispatch keep-alive that pings /health/ready"
      contains: "workflow_dispatch"
  key_links:
    - from: "render.yaml"
      to: "app/main.py /health/live"
      via: "healthCheckPath setting in render.yaml"
      pattern: "healthCheckPath.*health/live"
    - from: ".github/workflows/keepalive.yml"
      to: "app/main.py /health/ready"
      via: "curl ${RENDER_URL}/health/ready in workflow step"
      pattern: "health/ready"
    - from: "Dockerfile CMD"
      to: "app/main.py"
      via: ".venv/bin/uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-10000} (shell form)"
      pattern: "\\.venv/bin/uvicorn"
---

<objective>
Wave 1 (parallel with 06-01): Write all deploy artifacts — Dockerfile, .dockerignore, render.yaml, keepalive.yml workflow, health routes in main.py, and Resend env var additions to config.py and .env.example. These are ALL autonomous (no live accounts needed); the human executes the credentialed deploy at the Wave 2 checkpoint (06-03-DEPLOY).

Purpose: Separating artifact creation from human execution is the project's established BLOCKING-checkpoint pattern (prior phases did this for live-DB and live-LLM). The agent prepares everything; the human executes the credentialed steps at the right gate. This plan produces all the artifacts the D-08a/D-09 human checkpoint needs.

HIGH-1 fix (Codex review): The Dockerfile runtime stage uses `.venv/bin/uvicorn` directly — NOT `uv run uvicorn`. The BUILDER stage installs deps with `uv sync --frozen --no-dev` (D-19 honored) but the `uv` binary is NOT copied into the runtime stage. If CMD used `uv run`, the container would fail to start on Render (502) because no uv binary is present. Shell form is REQUIRED to expand `${PORT:-10000}` (exec form does not expand env vars — Pitfall 3).

Output: A complete set of deploy artifacts ready for the D-08a/D-09 human checkpoint. The health routes also turn the 06-01 Wave 0 xfail test stubs for /health/live GREEN (since no live DB is needed for liveness), flipping them from XFAIL → XPASS → remove-xfail-marker.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-PATTERNS.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/config.py
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
@/Users/pnhek/usf msds/github/payroll_agent/.env.example
@/Users/pnhek/usf msds/github/payroll_agent/.github/workflows/eval.yml
@/Users/pnhek/usf msds/github/payroll_agent/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dockerfile, .dockerignore, render.yaml</name>
  <files>Dockerfile, .dockerignore, render.yaml</files>
  <read_first>
    - 06-RESEARCH.md §5 "Docker (D-19, uv-in-image, multi-stage)" — exact verified pattern from astral-sh/uv-docker-example including UV_COMPILE_BYTECODE=1, UV_LINK_MODE=copy, UV_PYTHON_DOWNLOADS=0, uv version 0.11.23, WORKDIR constraint
    - 06-RESEARCH.md §6 "Render Free Service Specifics" — render.yaml verified schema, sync:false semantics, healthCheckPath
    - 06-PATTERNS.md §Dockerfile — the full critical-constraints block (WORKDIR, shell-form CMD, --frozen, no apt-get)
    - 06-PATTERNS.md §render.yaml — the full verified YAML with all env var entries
    - 06-RESEARCH.md Pitfall 2 (relative paths fail if not WORKDIR=/app) and Pitfall 3 ($PORT not expanded in exec-form CMD)
    - CLAUDE.md §8 Render Docker specifics (0.0.0.0:$PORT, ephemeral FS)
  </read_first>
  <action>
    Create Dockerfile at the repo root using the Astral official multi-stage uv-in-image pattern (per D-19). Two stages: `builder` and `runtime`.

    Builder stage: base `python:3.12-slim AS builder`. Copy uv from `ghcr.io/astral-sh/uv:0.11.23`. Set env vars `UV_COMPILE_BYTECODE=1`, `UV_LINK_MODE=copy`, `UV_PYTHON_DOWNLOADS=0`. WORKDIR=/app. Layer 1: COPY pyproject.toml uv.lock, run `uv sync --frozen --no-dev --no-install-project` (deps cached until lockfile changes). Layer 2: COPY . ., run `uv sync --frozen --no-dev`.

    Runtime stage: base `python:3.12-slim AS runtime`. WORKDIR=/app. COPY --from=builder /app /app. Set `ENV PATH="/app/.venv/bin:$PATH"`.

    HIGH-1 FIX: CMD uses SHELL FORM pointing directly to the venv uvicorn binary — NOT `uv run`. The shell form: `CMD ["/bin/sh", "-c", ".venv/bin/uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-10000}"]`. This is the correct form because: (1) no uv binary is present in the runtime stage (uv is a build tool only — smaller image, no risk of `uv run` env-var behavior at runtime), and (2) shell form is REQUIRED to expand `${PORT:-10000}` (Docker exec form does NOT expand shell variables — Pitfall 3). Do NOT use exec-form (`CMD ["uvicorn", ...]`) because `$PORT` would not be expanded.

    Alternative IF the team prefers keeping uv in the runtime stage: add `COPY --from=ghcr.io/astral-sh/uv:0.11.23 /uv /uvx /bin/` into the runtime stage BEFORE CMD, then CMD can use `uv run uvicorn ...`. BUT the preferred approach is `.venv/bin/uvicorn` directly — simpler, no uv binary at runtime.

    WORKDIR=/app is REQUIRED in both stages — the app uses relative paths for `app/templates`, `app/static`, and `eval/chart.svg` (D-21, Pitfall 2). A wrong WORKDIR makes the eval chart route 404 on Render.

    Create .dockerignore to exclude from the build context what the runtime image must not contain: `.venv/`, `.git/`, `tests/`, `.env`, `__pycache__/`, `*.pyc`, `.planning/`, `payroll_agent.egg-info/`, `scripts/`. Do NOT exclude: `app/`, `eval/chart.svg`, `eval/summary.json`, `eval/fixtures/`, `fixtures/`, `app/templates/`, `app/static/`, `pyproject.toml`, `uv.lock`. The eval chart and demo fixtures must be baked into the image (D-21).

    Create render.yaml at the repo root per the verified RESEARCH.md §6 schema. Set `type: web`, `runtime: docker`, `healthCheckPath: /health/live` (D-20 liveness, no DB — a Supabase blip must not fail the Render deploy health check). Use `sync: false` for all five secrets (DATABASE_URL, RESEND_API_KEY, WEBHOOK_SIGNING_SECRET, EXTRACTION_API_KEY, DRAFT_API_KEY). Use `value:` for non-secret env vars (EXTRACTION_MODEL=deepseek-v4-flash, DRAFT_MODEL=moonshot-v1-8k, EXTRACTION_BASE_URL, DRAFT_BASE_URL, TAX_YEAR="2026", ALLOW_LIVE_LLM="true", RESEND_FROM_ADDR="payroll@resend.dev"). Note: override RESEND_FROM_ADDR in the Render dashboard once a verified sending domain is configured.
  </action>
  <verify>
    <automated>grep "\.venv/bin/uvicorn" Dockerfile &amp;&amp; grep "0\.0\.0\.0" Dockerfile &amp;&amp; grep "uv sync --frozen --no-dev" Dockerfile &amp;&amp; echo "DOCKERFILE_OK"</automated>
  </verify>
  <done>
    `grep "\.venv/bin/uvicorn" Dockerfile` exits 0 (runtime CMD uses venv directly — HIGH-1 fix confirmed). `grep "0\.0\.0\.0" Dockerfile` exits 0. `grep "uv sync --frozen --no-dev" Dockerfile` exits 0 (BUILDER stage still uses uv sync). `grep "sync: false" render.yaml | wc -l` returns 5. `grep "healthCheckPath: /health/live" render.yaml` exits 0. If Docker daemon is available: `docker build -t payroll-agent-test .` exits 0 and `docker inspect payroll-agent-test --format '{{json .Config.WorkingDir}}'` returns `"/app"`.
  </done>
</task>

<task type="auto">
  <name>Task 2: config.py additions + /health routes + .env.example + keepalive.yml</name>
  <files>app/config.py, app/main.py, .env.example, .github/workflows/keepalive.yml</files>
  <read_first>
    - app/config.py (full file — existing Settings class to extend; follow comment-header pattern; lines 16-52 from PATTERNS.md)
    - app/main.py (lines 158-187 for existing webhook route pattern; lines 598-604 for FileResponse eval chart pattern D-21; find existing imports block)
    - .env.example (existing to extend with Resend env var stubs)
    - .github/workflows/eval.yml (existing structure to model keepalive.yml on — checkout, env patterns)
    - 06-PATTERNS.md §app/config.py ("Phase 6 additions" code excerpt)
    - 06-PATTERNS.md §app/main.py ("New health routes to add" code excerpt)
    - 06-PATTERNS.md §keepalive.yml ("New keepalive.yml pattern" code excerpt)
    - 06-RESEARCH.md §8 "GitHub Actions Keep-Alive" (cron pattern, 60-day auto-disable, workflow_dispatch requirement)
  </read_first>
  <action>
    Extend app/config.py: Add four new fields to the Settings class under a `# ── Email provider (Resend, Phase 6)` comment header. Fields: `resend_api_key: str = ""`, `webhook_signing_secret: str = ""`, `resend_from_addr: str = "payroll@resend.dev"`, and `allow_unsigned_fixtures: bool = False`. Follow the existing comment-header pattern in the file (two dashes, category label, more dashes). Empty string defaults for the key/secret: missing keys log a warning but do not fail startup (the stub fixture path must still work locally without Resend creds). `resend_from_addr` has a concrete default (`"payroll@resend.dev"`, the Resend shared test sender usable before domain verification) so gateway.py can read `get_settings().resend_from_addr` unconditionally — no conditional fallback needed. `allow_unsigned_fixtures: bool = False` is the BLOCKER-2 flag: False by default (production safe); setting the env var ALLOW_UNSIGNED_FIXTURES=true enables the dev-mode bypass for unsigned Resend-envelope payloads — this must NEVER be set in render.yaml. Place these four fields after the existing `draft_*` fields. **06-04 reads these fields directly; do not add them again there.**

    Extend app/main.py: Add GET /health/live and GET /health/ready routes. Read the existing route structure to find the correct insertion point (after the existing imports and lifespan setup). Liveness route (`/health/live`): returns `JSONResponse({"status": "ok"})` with NO database access. This is the Render deploy healthCheckPath — a Supabase blip during deploy must not fail the health check (D-20). Readiness route (`/health/ready`): runs a real `SELECT 1 FROM businesses LIMIT 1` via the psycopg pool to prove the DB path works and to count as Supabase DB activity for pause prevention (D-16, D-20). On DB failure, raise `HTTPException(status_code=503, detail="database not ready")`. Import `get_connection` from `app.db.supabase` inside the function body (not top-level) to avoid circular import issues. Use the existing `get_connection()` context manager pattern already in the file. Wrap the DB call in try/except Exception — a 503 is correct for a failed readiness probe.

    Confirm /eval/chart.svg route (D-21): Read the existing `/eval/chart.svg` FileResponse route (lines ~598-604 per PATTERNS.md). Confirm it serves `Path("eval/chart.svg")` as a relative path — this is correct and requires WORKDIR=/app (already set in Dockerfile). No change needed; just document it in a comment: `# D-21: serves committed eval/chart.svg baked into image; relative path requires WORKDIR=/app (Dockerfile)`.

    Extend .env.example: Add three Resend-related env var entries following the existing spacing and comment style. Add: `RESEND_API_KEY=your_resend_api_key_here`, `WEBHOOK_SIGNING_SECRET=your_webhook_signing_secret_here`, and `RESEND_FROM_ADDR=payroll@resend.dev`. The RESEND_FROM_ADDR line is important for dev parity — a developer cloning the repo needs to know this env var exists (the field lives in config.py and render.yaml; this is the .env.example parity per MEDIUM/LOW-9c fix).

    Create .github/workflows/keepalive.yml: Model on eval.yml's YAML structure but adapted for a cron + HTTP ping job (no checkout or uv setup needed — just a curl). Trigger: `schedule: [{cron: "17 10 * * 1,4"}]` (10:17 UTC Monday + Thursday, avoids :00 high-load window per RESEARCH.md §8) PLUS `workflow_dispatch:` (REQUIRED — allows manual re-enable after the 60-day auto-disable on portfolio repos with no pushes; this is Pitfall 6). Job: one step, `curl -sf --max-time 90 "$RENDER_URL/health/ready" || echo "Ping failed..."`. Env: `RENDER_URL: ${{ secrets.RENDER_URL }}`. The `/health/ready` route runs a real SELECT, so one ping keeps both Render warm (inbound HTTP) and Supabase un-paused (DB query).
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py -k "health_live" -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    `uv run pytest tests/test_dashboard.py -k "health_live" -x -q` passes (liveness route now exists and returns 200 — this turns the 06-01 xfail stub from XFAIL → XPASS; remove the xfail decorator from test_health_live_returns_200_no_db in tests/test_dashboard.py). `grep "resend_from_addr" app/config.py` exits 0. `grep "webhook_signing_secret" app/config.py` exits 0. `grep "allow_unsigned_fixtures" app/config.py` exits 0 (BLOCKER-2: field owned here, read by 06-04). `grep "RESEND_FROM_ADDR" render.yaml` exits 0. `grep "RESEND_API_KEY" .env.example` exits 0. `grep "RESEND_FROM_ADDR" .env.example` exits 0 (MEDIUM/LOW-9c fix). `grep "workflow_dispatch" .github/workflows/keepalive.yml` exits 0. `grep "health/live" render.yaml` exits 0. `grep -q "ALLOW_UNSIGNED_FIXTURES" render.yaml && exit 1 || true` exits 0 (BLOCKER-2: flag must be absent from render.yaml value: entries).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public internet → /health/live | No auth; must NOT expose sensitive info; returns only {"status":"ok"} |
| Public internet → /health/ready | No auth; DB-touching but returns only {"status":"ready"} or 503 |
| GitHub Actions → Render URL | RENDER_URL stored as Actions secret; not committed to repo |
| render.yaml secrets (sync:false) | Secret values never committed; Render prompts at Blueprint creation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | /health/live response body | mitigate | Returns `{"status":"ok"}` only — no version, no stack, no DB state exposed |
| T-06-02-02 | Information Disclosure | /health/ready error response | mitigate | On DB failure raises 503 with `"database not ready"` — no connection string or stack trace in response body |
| T-06-02-03 | Tampering | Dockerfile base image tag (`python:3.12-slim`) | accept | Floating `slim` tag; acceptable for a demo portfolio project; production would pin to digest |
| T-06-02-04 | Information Disclosure | .env.example committed with placeholder values | accept | Placeholders only (no real keys); communicates required env vars without exposing secrets |
| T-06-02-05 | Denial of Service | /health/ready hit-storm (cron misconfiguration) | accept | 2x/week cron; manual trigger only; psycopg pool absorbs single pings without saturation |
| T-06-02-SC | Tampering | ghcr.io/astral-sh/uv:0.11.23 pinned in Dockerfile | mitigate | Exact version tag pinned (not `latest`); reproducible builds; official Astral org image |
</threat_model>

<verification>
After both tasks:
1. `grep "\.venv/bin/uvicorn" Dockerfile` exits 0 (HIGH-1 fix: no uv run at runtime)
2. `grep "uv sync --frozen --no-dev" Dockerfile` exits 0 (D-19 builder stage intact)
3. `uv run pytest tests/test_dashboard.py -k "health_live" -x -q` → passes (GREEN — xfail removed)
4. `uv run pytest -q -m "not integration and not live_llm"` → ≥422 passed (no regressions)
5. `grep "sync: false" render.yaml | wc -l` → 5
6. `grep "workflow_dispatch" .github/workflows/keepalive.yml` → exits 0
7. `grep "webhook_signing_secret" app/config.py` → exits 0
8. `grep "resend_from_addr" app/config.py` → exits 0
9. `grep "allow_unsigned_fixtures" app/config.py` → exits 0 (BLOCKER-2: field defined here, default False, owned by this plan)
10. `grep "RESEND_FROM_ADDR" render.yaml` → exits 0
11. `grep "RESEND_FROM_ADDR" .env.example` → exits 0 (MEDIUM/LOW-9c)
12. `grep -q "ALLOW_UNSIGNED_FIXTURES" render.yaml && exit 1 || true` → exits 0 (BLOCKER-2: flag MUST be absent from render.yaml value: entries)
</verification>

<success_criteria>
- All deploy artifacts ready for the D-09 human checkpoint (Dockerfile, render.yaml, keepalive.yml, health routes)
- Dockerfile runtime stage uses .venv/bin/uvicorn directly — no uv binary needed at runtime (HIGH-1 fix)
- Health liveness route GREEN (turns the 06-01 xfail stub from XFAIL → XPASS → marker removed)
- No new secrets committed; all sensitive env vars are `sync: false` in render.yaml and placeholders in .env.example
- RESEND_FROM_ADDR documented in .env.example for dev parity
- Existing mocked suite stays ≥422 green
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-02-SUMMARY.md` when done
</output>
