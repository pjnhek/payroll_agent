---
phase: "06"
plan: "08"
type: execute
wave: 3
depends_on:
  - "06-02"
files_modified:
  - app/main.py
  - app/templates/index.html
  - app/db/repo.py
  - tests/test_demo_landing.py
autonomous: true
requirements:
  - OPS-02
  - OPS-04

must_haves:
  truths:
    - "GET / returns 200 and renders index.html with the three seeded businesses in a picker (per D-06 / CHANGE-6)"
    - "Selecting a business renders that business's employee list below the picker (load_roster_for_business :808 — per CHANGE-6)"
    - "POST /demo/bind atomically sets the selected business's contact_email = pjnhek@gmail.com AND restores all OTHER businesses to their seed .example values — in ONE transaction (per CHANGE-5)"
    - "After binding business X, find_business_by_sender('pjnhek@gmail.com') returns X and exactly one businesses row has that address (single-owner invariant)"
    - "POST /demo/compose builds an InboundEmail from the typed subject+body, sets from_addr = pjnhek@gmail.com (the bound business's contact), mints a fresh synthetic Message-ID, fires the REAL pipeline via BackgroundTasks.add_task(_run_pipeline), and redirects to /runs — same real pipeline as /demo/send-test (per CHANGE-7)"
    - "The composer requires a bound business: if no business has pjnhek@gmail.com as contact_email, POST /demo/compose redirects to / with an error flash (no orphan runs)"
    - "The run detail page at /runs/{id} renders all email_messages rows for the run in conversation order (inbound + outbound, chronological) as a thread view — per CHANGE-8; existing run_detail.html extended, not replaced"
    - "When run.status == 'awaiting_reply', the thread view shows an in-page reply box that POSTs to /runs/{run_id}/simulate-reply — wired to the existing resume seam (:661) per CHANGE-8"
    - "Paystub links (GET /runs/{run_id}/pdf/{employee_id}) appear in the thread view for reconciled/awaiting_approval runs per CHANGE-8"
    - "For each NameMatchResult in Decision.resolutions where source=='alias', the run detail and thread view render a plain-language note: 'Resolved <submitted_name> to <matched employee full_name> (known nickname from a prior confirmed run).' — per CHANGE-11; source=='exact' shows nothing; source=='none' triggers clarification as before, no change"
    - "The alias-rationale note is PRESENTATION ONLY — no pipeline/decision logic touched; reads from run['decision']['resolutions'] via the existing persisted JSONB; decide.py untouched; the misspelling-always-clarifies rule is preserved"
    - "uv run pytest tests/test_demo_landing.py -q exits 0 with 0 FAILED"
    - "uv run pytest -q -m 'not integration and not live_llm' exits 0 — no regressions to the 422+ existing tests"

  artifacts:
    - path: "app/templates/index.html"
      provides: "Landing page: business picker, employee list, composer textarea, binding feedback"
      min_lines: 80
    - path: "app/main.py"
      provides: "GET / route + POST /demo/bind route + POST /demo/compose route; thread_messages injected to run_detail; alias-rationale context injected to run_detail"
      contains: "demo/bind"
    - path: "app/db/repo.py"
      provides: "bind_demo_business helper (atomic single-owner transaction) + load_thread_messages helper"
      contains: "bind_demo_business"
    - path: "tests/test_demo_landing.py"
      provides: "Tests for landing route, binding atomicity, composer routing, thread view, alias-rationale, in-page reply seam"
      min_lines: 160

  key_links:
    - from: "app/templates/index.html business picker"
      to: "POST /demo/bind"
      via: "form action='/demo/bind' method='post'"
      pattern: "demo/bind"
    - from: "POST /demo/compose"
      to: "_run_pipeline (background task)"
      via: "BackgroundTasks.add_task — same as /demo/send-test"
      pattern: "_run_pipeline"
    - from: "app/templates/run_detail.html in-page reply box"
      to: "POST /runs/{run_id}/simulate-reply"
      via: "existing form action='/runs/{run_id}/simulate-reply' — reused seam (:661)"
      pattern: "simulate-reply"
    - from: "run_detail alias-rationale note"
      to: "run['decision']['resolutions']"
      via: "Jinja2 template reads decision['resolutions'] list; filters source=='alias'"
      pattern: "resolutions"

---

<objective>
Wave 3 — Add the interactive self-serve demo as the hero surface (D-06 / CHANGE-5 through CHANGE-11).

This plan adds: (1) a landing page GET / with a business picker + employee list; (2) POST /demo/bind for atomic single-owner business binding; (3) POST /demo/compose for free-text payroll composition that runs the REAL pipeline; (4) an extended /runs/{id} thread view showing all email_messages for the run in conversation order; (5) an in-page reply box on the thread view for awaiting_reply runs (wired to the existing /runs/{id}/simulate-reply seam); (6) alias-resolution rationale surfacing on the thread view (presentation only — no logic changes). This is the hero recruiter surface (OPS-04) and exercises the real pipeline (OPS-02).

Purpose: A visitor can complete the full payroll demo (Beat 1 happy path, Beat 2 clarify+reply, Beat 3 learn-once) entirely in-app, without needing Gmail or real email credentials. The landing page is the project face for hiring managers.

Output: app/templates/index.html (new), POST /demo/bind (atomic binding), POST /demo/compose (real pipeline entry), extended run_detail with thread view + alias rationale + in-page reply, repo.bind_demo_business + repo.load_thread_messages helpers, tests/test_demo_landing.py.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-VALIDATION.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-PATTERNS.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
@/Users/pnhek/usf msds/github/payroll_agent/app/templates/base.html
@/Users/pnhek/usf msds/github/payroll_agent/app/templates/run_detail.html
@/Users/pnhek/usf msds/github/payroll_agent/app/templates/runs_list.html
@/Users/pnhek/usf msds/github/payroll_agent/app/db/repo.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/seed.py
@/Users/pnhek/usf msds/github/payroll_agent/app/models/roster.py
@/Users/pnhek/usf msds/github/payroll_agent/app/models/contracts.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: repo helpers — bind_demo_business (atomic single-owner) + load_thread_messages</name>
  <files>app/db/repo.py, tests/test_demo_landing.py</files>
  <read_first>
    - app/db/repo.py (lines 169–181 find_business_by_sender; lines 198–223 create_run; lines 808–820 load_roster_for_business; the _conn_ctx pattern at lines ~68–78 and the _nulltx helper — these establish the exact idiom for transactions; lines 542–615 insert_email_message for the direction/purpose/message_id column pattern; look for any existing DEMO_OPERATOR_EMAIL or binding helper — there is none)
    - app/db/seed.py (lines 50–69 _BUSINESSES: the exact seed contact_email values — "payroll@coastalcleaning.example", "hr@metrodeli.example", "finance@summittech.example" and business UUIDs b0000001..b0000003 — the restore-to-seed values for the atomic binding transaction)
    - tests/conftest.py (the FakeConnection / fake_conn fixture pattern — Task 1 test uses it; also look at how the existing dedup test stubs repo calls)
  </read_first>
  <behavior>
    NEW REPO HELPER 1 — bind_demo_business(business_name: str, operator_email: str, seed_contacts: dict[str, str], conn=None) -> bool

    Atomically binds one business to the operator email and restores all others to their seed values IN ONE TRANSACTION. This is the single-owner guarantee (CHANGE-5): find_business_by_sender does exact-match, so two businesses sharing the operator email would cause ambiguous routing — the binding MUST be atomic.

    Algorithm:
    (a) In one transaction: for each (name, seed_email) in seed_contacts.items(), execute UPDATE businesses SET contact_email = %s WHERE name = %s — this resets ALL businesses including the target to their seeds.
    (b) Then in the same transaction: UPDATE businesses SET contact_email = %s WHERE name = %s with (operator_email, business_name) — this sets ONLY the target to the operator email.
    (c) Return True if the target UPDATE affected rowcount >= 1, else False (business name not found).
    (d) Use _conn_ctx(conn) as (c, owns) and a c.transaction() if owns block. Parameterized SQL only — no f-strings in execute().

    The seed_contacts dict is passed in (not hard-coded) so the function stays testable without reading the DB for seed data. The caller (the route handler) builds the dict from a module-level constant sourced from seed.py's _BUSINESSES list.

    NEW REPO HELPER 2 — load_thread_messages(run_id: uuid.UUID, conn=None) -> list[dict]

    Returns ALL email_messages rows for a run (both inbound and outbound), ordered chronologically. Columns: direction, purpose, subject, body_text, message_id, from_addr, to_addr, created_at. Uses explicit column list (no SELECT *). dict_row row factory. Ordered by created_at ASC so the thread renders oldest-first (inbound payroll request first, then clarification, then reply, then confirmation).

    Tests (tdd="true") — write BEFORE implementing:

    test_bind_demo_business_atomic_single_owner:
    Use FakeConn. Call bind_demo_business("Metro Deli Group", "pjnhek@gmail.com", {"Coastal Cleaning Co.": "payroll@coastalcleaning.example", "Metro Deli Group": "hr@metrodeli.example", "Summit Tech Solutions": "finance@summittech.example"}, conn=fake_conn). Assert: (1) fake_conn.executed contains exactly three seed-restore UPDATEs (one per business — all to their .example values including Metro initially); (2) the final UPDATE sets contact_email = 'pjnhek@gmail.com' WHERE name = 'Metro Deli Group'; (3) the seed restores occur BEFORE the target-bind UPDATE. Do NOT assert absolute ordering by index — use find-by-content. Test is GREEN immediately after helper is implemented.

    test_bind_demo_business_target_gets_operator_email:
    After calling bind_demo_business with "Coastal Cleaning Co.", assert that among fake_conn.executed the last binding UPDATE params are ("pjnhek@gmail.com", "Coastal Cleaning Co.").

    test_bind_demo_business_others_get_seed_email:
    Assert that among fake_conn.executed there is a seed-restore UPDATE with ("hr@metrodeli.example", "Metro Deli Group") and another with ("finance@summittech.example", "Summit Tech Solutions").

    test_load_thread_messages_returns_both_directions:
    Use FakeConn pre-populated to return two rows: one with direction='inbound' and one with direction='outbound'. Call load_thread_messages(run_id, conn=fake_conn). Assert the result list has length 2 and is ordered chronologically (the inbound row's created_at precedes the outbound). Assert the SQL executed contains "email_messages" and "ORDER BY created_at".

    All tests must be GREEN before moving to Task 2.
  </behavior>
  <action>
    In app/db/repo.py, after the load_outbound_emails function (line ~677), add two new helpers:

    bind_demo_business: See behavior block for the exact algorithm. The function signature is bind_demo_business(business_name: str, operator_email: str, seed_contacts: dict[str, str], conn=None) -> bool. Inside, use the _conn_ctx(conn) as (c, owns) pattern with a c.transaction() if owns guard exactly as create_run does (lines 207–209). All SQL is parameterized — no f-strings. The seed-restore loop runs first (CHANGE-5 atomicity: others back to seeds before the bind), then the target bind.

    load_thread_messages: Explicit column list — "direction, purpose, subject, body_text, message_id, from_addr, to_addr, created_at" — SELECT from email_messages WHERE run_id = %s ORDER BY created_at ASC. Use dict_row row_factory pattern matching load_outbound_emails. Return list of dicts or [].

    SECURITY NOTE: both helpers use parameterized SQL only. The seed_contacts dict values are module-level seed constants (trusted), not user input. The operator_email is validated upstream (route handler only accepts the hardcoded DEMO_OPERATOR_EMAIL constant — no user-supplied email).

    In tests/test_demo_landing.py (new file): import pytest, uuid, and the new repo helpers. Use FakeConn from conftest. Write the four tests listed in behavior. Run `uv run pytest tests/test_demo_landing.py -q` to verify all GREEN before Task 2.
  </action>
  <verify>
    <automated>uv run pytest tests/test_demo_landing.py -q -m "not integration" 2>&1 | tail -5</automated>
  </verify>
  <done>
    grep "bind_demo_business" app/db/repo.py exits 0. grep "load_thread_messages" app/db/repo.py exits 0. uv run pytest tests/test_demo_landing.py -k "bind or thread_messages" -v shows all four tests PASSED. uv run pytest -q -m "not integration and not live_llm" 2>&1 | tail -3 shows 0 FAILED.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Landing page + composer + binding routes + thread view + alias rationale</name>
  <files>app/main.py, app/templates/index.html, app/templates/run_detail.html, tests/test_demo_landing.py</files>
  <read_first>
    - app/main.py (lines 1–100 — imports, _DEMO_FIXTURES, app setup; lines 158–219 inbound webhook to understand the pipeline kickoff pattern; lines 661–777 simulate_reply route — the in-page reply seam this plan wires; lines 780–876 demo_send_test — the composer copies this construction pattern; lines 480–486 the runs list route context to understand demo_fixtures injection; lines 519–553 run_detail route — this is extended to inject thread_messages and alias_rationale context)
    - app/templates/base.html (the nav structure — index.html extends base and adds a link back to Runs and Eval; the page-wrapper class)
    - app/templates/run_detail.html (lines 1–80 — the status badge, in-flight JS poller, banner structure, and the existing simulate-reply form at lines 73–80; this template is EXTENDED not replaced; the alias-rationale rendering is added as a new section within the existing 3-column layout)
    - app/db/seed.py (lines 50–69 _BUSINESSES — to build the DEMO_OPERATOR_EMAIL constant and the _SEED_CONTACTS mapping used by bind_demo_business; the business UUIDs are fixed so the landing page can resolve employee lists without a name search)
    - app/db/repo.py (after Task 1 additions — the bind_demo_business and load_thread_messages helpers just written; also find_business_by_sender :169 for the composer bound-business check; load_roster_for_business :808 for the employee list)
  </read_first>
  <behavior>
    MODULE-LEVEL CONSTANTS in app/main.py (add near _DEMO_FIXTURES):

    DEMO_OPERATOR_EMAIL = "pjnhek@gmail.com"  — the single operator email for the binding. This is a code constant, not an env var — it identifies the operator for demo routing. Never user-supplied. Comment: "D-06 / CHANGE-5: the single demo operator email; bind_demo_business ensures exactly one business holds this address at a time."

    _SEED_CONTACTS: dict[str, str] — maps business name to seed .example contact_email, sourced from app/db/seed._BUSINESSES. Import _BUSINESSES from app.db.seed (or inline the three entries as a constant if the import is unclean). Values: "Coastal Cleaning Co." → "payroll@coastalcleaning.example", "Metro Deli Group" → "hr@metrodeli.example", "Summit Tech Solutions" → "finance@summittech.example". These are the restore values passed to bind_demo_business.

    NEW ROUTE — GET / (landing page):
    Queries repo.find_business_by_sender(DEMO_OPERATOR_EMAIL) to find the currently-bound business (may be None — no business bound yet). If bound, calls repo.load_roster_for_business(bound_business_id) to get the employee list. Calls repo.list_businesses() (see below — add a thin helper that returns all business rows ordered by name) to populate the picker. Renders index.html with context: businesses=list[dict], bound_business_id (str|None), employees=list[Employee]|[], demo_operator_email=DEMO_OPERATOR_EMAIL.

    NEW REPO HELPER (add to repo.py as part of this task if it doesn't exist) — list_businesses(conn=None) -> list[dict]: SELECT id, name, contact_email FROM businesses ORDER BY name. dict_row. Returns [].

    NEW ROUTE — POST /demo/bind:
    Accepts a form field: business_name: str = Form(...). Validates business_name is one of the known seed business names (membership check against _SEED_CONTACTS.keys()) — if invalid, redirect to / (no error state needed; the picker only shows seeded names). Calls repo.bind_demo_business(business_name, DEMO_OPERATOR_EMAIL, _SEED_CONTACTS). On success, redirect to / with query param ?bound=1 so the landing page can show a success flash. On failure (business not found), redirect to /. SECURITY: business_name is validated against the trusted _SEED_CONTACTS allowlist — no arbitrary name accepted. The binding always uses the hardcoded DEMO_OPERATOR_EMAIL — no user-supplied email. T-06-08-01 mitigated here.

    NEW ROUTE — POST /demo/compose:
    Accepts form fields: subject: str = Form(default="Payroll submission"), body: str = Form(default=""). Composer sends the REAL pipeline (not a fixture) — this is PATH 1 (CHANGE-7).

    Step 1: Find the currently-bound business via repo.find_business_by_sender(DEMO_OPERATOR_EMAIL). If None → redirect to / (no bound business; cannot compose). This guard prevents orphan runs.

    Step 2: Build the InboundEmail payload exactly as demo_send_test does (lines 818–835 of main.py), reusing that construction pattern. Set from_addr = DEMO_OPERATOR_EMAIL. Mint a fresh synthetic Message-ID: fresh_message_id = f"<{uuid.uuid4()}@demo.payroll-agent.local>". Set subject and body_text from form inputs. Set in_reply_to=None, references_header=None, to_addr="agent@payroll-agent.local".

    Step 3: Call gateway.parse_inbound(inbound_payload), clean_body, repo.insert_inbound_email, repo.create_run, background_tasks.add_task(_run_pipeline, run_id). Redirect to /runs/{run_id} so the user watches the run progress in real time.

    SECURITY: body and subject are untrusted user input. Jinja2's autoescape=True (FastAPI Jinja2Templates default) handles XSS in the rendered output. The body is used as body_text of an InboundEmail — it passes through clean_body then the LLM extraction stage, never rendered raw in a privileged context. The from_addr is the hardcoded DEMO_OPERATOR_EMAIL constant, not derived from user input. T-06-08-02 (SSRF) mitigated: no URL is loaded from user input; the path does not accept a file path from the user. T-06-08-03 (XSS) mitigated by Jinja2 autoescape on all rendered template variables.

    TEMPLATE — app/templates/index.html:
    Extends base.html. Block content. Title: "Payroll Agent — Demo". Structure:
    - A heading: "Try it live"
    - A one-paragraph intro: "Select a business below to see its employee roster, compose a free-text payroll email, and watch the pipeline process it in real time — no real email required."
    - If bound flash (query param ?bound=1): a green flash div: "Business bound. The pipeline will route your composed email to this business."
    - Business picker: a form (method POST, action /demo/bind). A select element (name=business_name) with one option per business (value=business.name, text=business.name). The currently-bound business is pre-selected (selected attribute). A submit button: "Select & Bind". Below the picker, if employees is non-empty, a simple table with columns: Name, Pay Type, Filing Status — showing each employee's full_name, pay_type, filing_status. Note: if no business is bound yet, display: "Select a business above to see its roster."
    - Composer: a form (method POST, action /demo/compose). Two inputs: a text input for subject (label "Subject", placeholder "e.g. Coastal weekly hours"), and a textarea for body (label "Payroll details", placeholder "e.g. Maria Chen 40 regular hours. James Okafor 32 regular, 8 vacation."). A submit button: "Run pipeline". Below the button in small text: "This fires the REAL extraction + decisioning pipeline. If names are ambiguous, you'll be asked to clarify below."
    - A link: "View all runs →" pointing to /runs. A link: "View eval →" pointing to /eval.
    Vanilla JS (no SPA): after clicking bind, the page naturally POSTs and reloads. After clicking "Run pipeline", the POST redirects to the run detail page. No JS needed for the binding or composition flows.
    Jinja2 autoescape is on by default — all employee.full_name and business.name are escaped automatically. Do NOT use |safe on any user-originated content.

    EXTEND run_detail route in app/main.py to inject two new template context vars:
    (a) thread_messages: call repo.load_thread_messages(run_id) — this is a list of ALL email_messages rows (inbound + outbound) ordered by created_at. Already handles except-on-DB-unavail with empty list fallback (same pattern as outbound_emails loading at line ~539).
    (b) alias_rationale_notes: a list of plain-language strings derived from the persisted decision JSONB. Build it from run.get("decision") if non-None: parse the decision dict, iterate resolutions, for each NameMatchResult where source == "alias" build the string "Resolved '{submitted_name}' to {full_name} (known nickname from a prior confirmed run)." Look up full_name by joining matched_employee_id to the roster (call repo.load_roster_for_business(run["business_id"]) and find the employee by id). Build a list of these strings. If no alias resolutions exist, alias_rationale_notes = []. Exception-safe: any error → []. IMPORTANT: this reads presentation data only — no modification to decision, decide.py, or any pipeline logic. T-06-08-04 (presentation-only, no logic contamination) mitigated.

    EXTEND app/templates/run_detail.html:
    (a) Thread view: add a "Conversation thread" section BELOW the existing 3-column grid. Render thread_messages as a simple card list. Each message card: direction label (INBOUND/OUTBOUND), purpose (if set), subject, body_text excerpt (first 300 chars, truncated with "…"), created_at timestamp. If thread_messages is empty, show nothing (no empty state needed — the section just disappears). This renders the same email_messages rows that the real webhook path and simulate-reply both write, so Path 1 (in-app) and Path 2 (real email round-trip) both produce an identical thread view.
    (b) Alias-rationale notes: if alias_rationale_notes is non-empty, add a small "Name resolutions" section above the paystub columns (or in the decision banner area). For each note, render a plain p element with the note text. Example: "Resolved 'Dave Perez' → David Perez (known nickname from a prior confirmed run)." This section is hidden if the list is empty. Jinja2 autoescape handles XSS automatically — note strings are server-computed from the DB, not raw user input, but autoescape is the default so no |safe needed and none should be added.
    (c) In-page reply box: the existing simulate-reply form at lines 73–80 already works. Verify it's inside the banner for awaiting_reply status. If it's in the banner, no change needed. If it needs to also appear at the bottom of the thread view (for visibility after a long thread), add a duplicate with the same form action. Keep the existing banner form as the primary — the thread-bottom one is a secondary affordance. Do NOT remove or alter the existing form behavior — just ensure it's reachable. CHANGE-8: "confirm simulate-reply does what's needed" — the existing code at :661 already resumes the run with the reply text and routes through the REAL _route_reply path. No wiring changes needed.

    Tests in tests/test_demo_landing.py (extend from Task 1 — append new tests):

    test_landing_get_returns_200:
    Monkeypatch repo.list_businesses to return [{id: uuid4(), name: "Coastal Cleaning Co.", contact_email: "payroll@coastalcleaning.example"}]. Monkeypatch repo.find_business_by_sender to return None (no bound business). POST GET / with TestClient. Assert response.status_code == 200. Assert b"Try it live" in response.content (or equivalent heading text). Assert b"demo/bind" in response.content.

    test_bind_route_sets_single_owner:
    Monkeypatch repo.bind_demo_business to return True. POST /demo/bind with form data {business_name: "Metro Deli Group"}. Assert response.status_code in (303, 200) — redirect to /. Assert repo.bind_demo_business was called once with ("Metro Deli Group", "pjnhek@gmail.com", _SEED_CONTACTS).

    test_bind_route_rejects_unknown_business:
    POST /demo/bind with form data {business_name: "Unknown Corp"}. Assert repo.bind_demo_business was NOT called (the validation guard rejected it). Assert redirect to /.

    test_compose_route_redirects_to_run_when_bound:
    Monkeypatch repo.find_business_by_sender to return uuid4() (bound). Monkeypatch repo.insert_inbound_email to return (uuid4(), True). Monkeypatch repo.create_run to return uuid4(). POST /demo/compose with form data {subject: "Test payroll", body: "Maria 40 hours"}. Assert response.status_code == 303. Assert "runs" in response.headers["location"]. Assert BackgroundTasks.add_task was called with _run_pipeline.

    test_compose_route_redirects_to_landing_when_unbound:
    Monkeypatch repo.find_business_by_sender to return None (unbound). POST /demo/compose. Assert redirect to / (no run created).

    test_compose_body_ssrf_path_not_loaded:
    POST /demo/compose with body="../../../../etc/passwd" and subject="Test". Monkeypatch repo.find_business_by_sender to return uuid4(). Assert that the handler does NOT open any file from the body (body goes to body_text only — no file I/O). This is a negative test: no subprocess, no file open, just the pipeline kickoff. Assert redirect to /runs/{id}.

    test_run_detail_injects_thread_messages:
    Monkeypatch repo.load_run to return a minimal run dict with status="reconciled". Monkeypatch repo.load_thread_messages to return [{"direction": "inbound", "purpose": None, "subject": "hours", "body_text": "Maria 40h", "message_id": "<a@x.test>", "from_addr": "hr@test.example", "to_addr": "agent@x.test", "created_at": datetime.now(tz=timezone.utc)}]. GET /runs/{run_id}. Assert b"INBOUND" in response.content (or equivalent direction label). Assert b"Maria 40h" in response.content (body excerpt rendered).

    test_run_detail_alias_rationale_rendered:
    Set up a run dict with status="reconciled" and decision = a JSON string containing resolutions=[{submitted_name: "Dave Perez", matched_employee_id: str(emp_uuid), source: "alias", resolved: True, reason: "stored alias"}]. Monkeypatch repo.load_roster_for_business to return a Roster with one Employee(full_name="David Perez", id=emp_uuid, ...). GET /runs/{run_id}. Assert b"Resolved" in response.content. Assert b"known nickname" in response.content.

    test_run_detail_alias_rationale_absent_for_exact:
    Same setup but source="exact". Assert b"known nickname" NOT in response.content (exact matches produce no note).

    All new tests must be GREEN. No xfail.
  </behavior>
  <action>
    In app/main.py:

    (1) Add DEMO_OPERATOR_EMAIL = "pjnhek@gmail.com" and _SEED_CONTACTS dict near _DEMO_FIXTURES. Import _BUSINESSES from app.db.seed for the seed email values (or inline as a dict literal if the import is awkward).

    (2) Add the GET / route as @app.get("/") rendering index.html. See behavior for the exact context vars: businesses, bound_business_id, employees, demo_operator_email. Use the same templates.TemplateResponse(request, "index.html", {...}) pattern as run_detail. Add the route BEFORE the @app.get("/runs") route so it doesn't conflict. The route signature must accept `request: Request` and `bound: str = Query(default="")`.

    (3) Add the POST /demo/bind route as @app.post("/demo/bind"). Accepts business_name: str = Form(...), background_tasks: BackgroundTasks is NOT needed. Validate business_name in _SEED_CONTACTS. Call repo.bind_demo_business(business_name, DEMO_OPERATOR_EMAIL, _SEED_CONTACTS). Redirect to "/?bound=1" on success. Redirect to "/" on failure/invalid.

    (4) Add the POST /demo/compose route as @app.post("/demo/compose"). Accepts subject: str = Form(default="Payroll submission"), body: str = Form(default=""), background_tasks: BackgroundTasks. See behavior for the full implementation. The construction mirrors demo_send_test (lines 818–835) — read that code carefully and replicate the InboundEmail payload building, gateway.parse_inbound call, clean_body, insert, create_run, add_task pattern. Redirect to f"/runs/{run_id}" (NOT /runs — go directly to the run page so the user watches it in real time). Wrap in a try/except like demo_send_test; on failure redirect to /.

    (5) In the existing run_detail route (line ~519), add two new context vars after the outbound_emails loading block:
    - thread_messages: try/except repo.load_thread_messages(run_id) or [].
    - alias_rationale_notes: build from run.get("decision") — see behavior for exact algorithm. The helper function can be a module-private _build_alias_rationale_notes(run, roster_fn) that accepts the run dict and a callable for roster lookup (makes it testable). Catch all exceptions and return [].

    (6) Pass thread_messages and alias_rationale_notes into the run_detail TemplateResponse context dict.

    In app/db/repo.py (if list_businesses is not already present): add list_businesses(conn=None) -> list[dict] as described in behavior, immediately before bind_demo_business.

    Create app/templates/index.html: extends base.html. Full implementation per behavior. Use Jinja2 autoescape (default — no |safe). Business picker select uses value="{{ business.name | e }}" and text "{{ business.name | e }}". Employee table uses "{{ emp.full_name | e }}" and "{{ emp.pay_type | e }}". Flash div conditional on "{{ 1 if bound else 0 }}" from the query param. No inline JavaScript required for the basic flows.

    Extend app/templates/run_detail.html: add the three new sections (thread view, alias-rationale notes, secondary in-page reply affordance) BELOW the existing 3-column grid section. Use Jinja2 autoescape throughout. Thread message body_text is truncated in the template: "{{ msg.body_text[:300] }}{% if msg.body_text | length > 300 %}…{% endif %}". The alias-rationale section is wrapped in {% if alias_rationale_notes %} ... {% endif %} so it renders only when notes exist.

    Add import from fastapi import Query at the top of main.py imports if not already present.

    After all changes: run uv run pytest tests/test_demo_landing.py -q and uv run pytest -q -m "not integration and not live_llm" — both must exit 0.
  </action>
  <verify>
    <automated>uv run pytest tests/test_demo_landing.py -q 2>&1 | tail -8 && grep "demo/bind" app/main.py | head -2 && grep "demo/compose" app/main.py | head -2 && ls app/templates/index.html && grep "known nickname\|alias_rationale" app/templates/run_detail.html | head -2</automated>
  </verify>
  <done>
    ls app/templates/index.html exits 0. grep "demo/bind" app/main.py exits 0. grep "demo/compose" app/main.py exits 0. grep "bind_demo_business" app/main.py exits 0. grep "load_thread_messages" app/main.py exits 0. grep "alias_rationale_notes" app/main.py exits 0. grep "known nickname" app/templates/run_detail.html exits 0 (CHANGE-11 alias note in template). grep "thread_messages" app/templates/run_detail.html exits 0 (thread view rendered). uv run pytest tests/test_demo_landing.py -v shows all tests PASSED (no FAILED, no xfail). uv run pytest -q -m "not integration and not live_llm" 2>&1 | tail -3 shows 0 FAILED. grep "DEMO_OPERATOR_EMAIL" app/main.py exits 0. grep -c "autoescape\|autoescape" app/templates/index.html is irrelevant — check instead that grep "|safe" app/templates/index.html exits non-zero (no unsafe filter on user content).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → POST /demo/bind | business_name is form input; validated against trusted _SEED_CONTACTS allowlist before reaching repo |
| visitor → POST /demo/compose | subject + body are free-text user input; body goes to body_text only (no file I/O, no URL fetch); from_addr is the hardcoded DEMO_OPERATOR_EMAIL constant, not derived from user input |
| Jinja2 template rendering | All visitor-originated content (body_text, subject) rendered through Jinja2 autoescape (FastAPI default); alias_rationale_notes are server-computed from DB-stored data, also autoescape-safe |
| repo.bind_demo_business → businesses table | Atomic transaction restores all other businesses to seed values before binding the selected one; prevents multiple businesses sharing the same contact_email (ambiguous routing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Tampering | POST /demo/bind mutates businesses.contact_email for any business name | mitigate | business_name validated against _SEED_CONTACTS.keys() allowlist (three known names only); unknown names redirect to / without calling repo; the atomic bind-and-restore transaction prevents a valid name from leaving two businesses with the same email |
| T-06-08-02 | SSRF / Tampering | POST /demo/compose body/subject could be crafted to probe internal paths | mitigate | body goes to body_text only — no file open, no subprocess, no URL fetch from body; from_addr is the hardcoded DEMO_OPERATOR_EMAIL constant (not user-supplied); the exact same SSRF guard as /demo/send-test (server-side allowlist for paths); test_compose_body_ssrf_path_not_loaded asserts this |
| T-06-08-03 | XSS | Employee names, business names, email body_text rendered in index.html and run_detail.html thread view | mitigate | Jinja2 autoescape=True (FastAPI Jinja2Templates default); no |safe filter applied to any user-originated or DB-stored string; all template variables escaped automatically; grep "|safe" app/templates/index.html exits non-zero is a done criterion |
| T-06-08-04 | Tampering | alias-rationale note rendering reads run['decision']['resolutions'] — risk that reading fails and corrupts the run view | mitigate | _build_alias_rationale_notes is exception-safe (any parse error → []); decision JSONB is read-only in this path; no write to DB from the route; source == "alias" filter is a simple string comparison — no logic fork that could affect pipeline state |
| T-06-08-05 | Elevation of Privilege | POST /demo/bind could be called by an unauthenticated external party to swap all businesses to arbitrary emails | accept | This is a public demo app with no auth (locked constraint from CLAUDE.md). The bind route only accepts the three seeded business names and always sets the HARDCODED DEMO_OPERATOR_EMAIL (not a caller-supplied email) — so a malicious bind can only swap which of the three demo businesses is "active", which is the intended behavior. The worst case is a visitor resets the binding mid-demo, which is a UX nuisance, not a data breach. A CSRF token would address this but adds auth complexity inconsistent with the no-auth constraint. Accept. |
| T-06-08-SC | Tampering | No new packages in this plan | accept | No new npm/pip/cargo installs; only edits to existing files and one new template; no slopcheck needed |
</threat_model>

<verification>
After both tasks:
1. ls app/templates/index.html exits 0 (landing page exists)
2. grep "demo/bind" app/main.py exits 0 (bind route exists)
3. grep "demo/compose" app/main.py exits 0 (composer route exists)
4. grep "bind_demo_business" app/db/repo.py exits 0 (atomic binding helper in repo)
5. grep "load_thread_messages" app/db/repo.py exits 0 (thread messages helper)
6. grep "thread_messages" app/templates/run_detail.html exits 0 (thread view in template)
7. grep "known nickname\|alias_rationale" app/templates/run_detail.html exits 0 (CHANGE-11 alias note rendered)
8. grep -c "|safe" app/templates/index.html | grep "^0$" exits 0 (no unsafe filter — XSS guard)
9. uv run pytest tests/test_demo_landing.py -v — all tests PASSED (0 FAILED, 0 xfail)
10. uv run pytest -q -m "not integration and not live_llm" — 0 FAILED (existing 422+ tests untouched)
11. grep "DEMO_OPERATOR_EMAIL" app/main.py exits 0 (hardcoded operator constant)
12. grep "simulate-reply" app/templates/run_detail.html exits 0 (existing in-page reply seam present — CHANGE-8 wired)
</verification>

<success_criteria>
- Landing page (GET /) renders with business picker and employee list — CHANGE-6 delivered
- Business binding (POST /demo/bind) is atomic and single-owner: after binding X, exactly one business has pjnhek@gmail.com; all others hold their seed .example values — CHANGE-5 enforced
- Composer (POST /demo/compose) builds an InboundEmail from free text, mints a fresh synthetic Message-ID, fires the REAL pipeline via BackgroundTasks, redirects to the run detail page — CHANGE-7 uses the real pipeline (not a fixture); no email send needed
- Thread view on /runs/{id} renders all email_messages rows (inbound + outbound) in chronological order — CHANGE-8 delivered; Path 1 and Path 2 runs produce the same view
- In-page reply box on awaiting_reply runs wired to existing /runs/{id}/simulate-reply seam — CHANGE-8 reply flow complete without Gmail
- Alias-rationale notes rendered for source=="alias" resolutions in the run detail/thread view — CHANGE-11 is PRESENTATION ONLY; decision logic, decide.py, and the misspelling-always-clarifies rule are untouched
- All tests in tests/test_demo_landing.py pass (0 FAILED, 0 xfail)
- No regressions: existing 422+ mocked tests remain green (no-op-swap invariant)
- XSS guard: Jinja2 autoescape handles all rendered content; no |safe on any user-originated string
- SSRF guard: compose body never opens a file or URL; from_addr is the hardcoded DEMO_OPERATOR_EMAIL
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-08-SUMMARY.md` when done.
Update 06-VALIDATION.md: add rows for test_bind_demo_business_atomic_single_owner, test_compose_route_redirects_to_run_when_bound, test_compose_body_ssrf_path_not_loaded, test_run_detail_alias_rationale_rendered, test_run_detail_alias_rationale_absent_for_exact, test_run_detail_injects_thread_messages — all OPS-04/OPS-02, status green.
</output>
