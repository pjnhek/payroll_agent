---
phase: "06"
plan: "06"
type: execute
wave: 5
depends_on:
  - "06-05"
files_modified:
  - scripts/demo_reset.py
  - README.md
  - app/main.py
  - app/email/gateway.py
  - docs/architecture.mmd
  - docs/architecture.png
autonomous: true
requirements:
  - OPS-04

must_haves:
  truths:
    - "scripts/demo_reset.py requires an explicit --confirm flag to run destructive mode (no-arg default does NOT delete anything)"
    - "demo_reset.py --confirm does NOT overwrite the production demo contact_email back to .example — the real sender email set in 06-05 is preserved after reset (HIGH-2); seed() re-seeds employees and known_aliases but must NOT clobber the businesses.contact_email row that was updated for the demo identity"
    - "demo_reset.py reads the demo business name from a script constant or env var and re-applies the real contact_email AFTER calling seed() — or skips the businesses upsert row in seed() that clobbers it — so the real sender address survives the reset"
    - "scripts/demo_reset.py --confirm deletes in FK-safe order (line_items/paystubs/alias_audit → email_messages → payroll_runs) then re-runs seed() to restore known_aliases"
    - "Running demo_reset.py --confirm then replaying the gate_block_hero fixture triggers clarification (Beat 2 re-arms)"
    - "_DEMO_FIXTURES in app/main.py includes the gate_block_hero fixture entry (the Beat-2 hero — 'David Reyez' unknown shorthand → Metro Deli → clarification); the fixture key 'gate_block_hero' maps to the correct eval fixture file (HIGH-2 demo-path fix)"
    - "demo_send_test handler in app/main.py overrides the fixture's from_addr at send time using the selected/seeded business's CURRENT contact_email (loaded from DB via repo.find_business_by_sender inverse lookup or read from DEMO_CONTACT_EMAIL env var), so the demo resolves correctly after the 06-05 identity swap regardless of what from_addr is baked into the fixture file (HIGH-2 demo-path fix)"
    - "test_demo_send_test_gate_block_hero_routes_to_seeded_business: with ALLOW_UNSIGNED_FIXTURES=True and demo contact set to the seeded business, POST /demo/send-test with fixture_key=gate_block_hero → the pipeline is enqueued with the seeded business_id (not unknown_sender 303 redirect)"
    - "test_demo_reset_preserves_demo_contact_then_send_test_still_accepts: after demo_reset.py re-applies contact_email, the seeded business's contact_email in the DB equals DEMO_CONTACT_EMAIL; a subsequent /demo/send-test with gate_block_hero still routes to that business"
    - "README.md contains the locked disclaimer verbatim: educational-only, OBBBA exclusion, Additional Medicare 0.9% over $200k YTD unmodeled"
    - "README has a punchy recruiter top section + Mermaid architecture diagram + For Engineers section"
    - "Mermaid diagram shows the pipeline stages + two pause states (awaiting_reply, awaiting_approval) + the code gate (decide.py)"
    - "docs/architecture.mmd committed (Mermaid source file)"
    - "docs/architecture.png committed (exported PNG — required by D-11a; Mermaid-only is NOT sufficient)"
    - "Temporary LOG_WEBHOOK_DEBUG_IDS log lines removed from app/email/gateway.py (committed in 06-04 for the debug switch)"
    - "Temporary DEBUG_ROUND_TRIP log lines (if any) removed from app/main.py"
    - "Eval chart route confirmed serving committed eval/chart.svg as static asset (D-21)"
    - "LOG_WEBHOOK_DEBUG_IDS cleanup: grep 'LOG_WEBHOOK_DEBUG_IDS' app/email/gateway.py exits non-zero after Task 1 (removed)"
  artifacts:
    - path: "scripts/demo_reset.py"
      provides: "Demo-reset helper: explicit --confirm required; FK-safe ordered deletes + seed() to restore known_aliases + re-apply demo contact_email"
      contains: "known_aliases"
    - path: "app/main.py"
      provides: "Updated _DEMO_FIXTURES with gate_block_hero entry + demo_send_test handler overrides fixture from_addr with current business contact_email (HIGH-2 demo-path fix)"
      contains: "gate_block_hero"
    - path: "README.md"
      provides: "Full recruiter-facing README with disclaimer, diagram, Mermaid source, For Engineers section"
      contains: "educational"
    - path: "docs/architecture.mmd"
      provides: "Mermaid source file (diffable, regeneratable)"
      contains: "awaiting_reply"
    - path: "docs/architecture.png"
      provides: "Exported PNG of architecture diagram — REQUIRED by D-11a for guaranteed GitHub rendering (not optional)"
  key_links:
    - from: "scripts/demo_reset.py"
      to: "app/db/seed.py seed() function"
      via: "from app.db.seed import seed; seed() — ON CONFLICT DO UPDATE resets known_aliases"
      pattern: "from app\\.db\\.seed import seed"
    - from: "app/main.py demo_send_test handler"
      to: "businesses.contact_email (DB or DEMO_CONTACT_EMAIL env)"
      via: "fixture from_addr overridden at send time with the seeded business's current contact_email so find_business_by_sender resolves after 06-05 identity swap"
      pattern: "DEMO_CONTACT_EMAIL|contact_email"
    - from: "README.md Mermaid diagram"
      to: "app/pipeline/orchestrator.py status states"
      via: "awaiting_reply and awaiting_approval pause states shown as nodes"
      pattern: "awaiting_reply|awaiting_approval"
---

<objective>
Wave 5: (1) Add gate_block_hero to _DEMO_FIXTURES and fix the demo_send_test from_addr override (HIGH-2 demo-path fix — the dashboard recording path must resolve post-identity-swap); (2) Build the demo-reset script (D-07 — MUST exist before recording); (3) Write the full README (D-11/D-11a/OPS-04); (4) Remove the temporary debug logging from gateway.py and main.py; (5) Commit the architecture diagram (both Mermaid source + PNG). These are all autonomous — no live accounts needed.

HIGH-2 fix (Round-3 Codex HIGH — demo identity still broken for /demo/send-test): Two problems. First, the demo's Beat-2 hero fixture (gate_block_hero — submits "David Reyez" for the unknown-shorthand → clarify → suggestion beat) is NOT in app/main.py _DEMO_FIXTURES. Without it, the recording's Beat 2 must use a different fixture, or clicking "Send test email" can't select it. Second, the demo_send_test handler uses the fixture's baked from_addr (a .example seed value). After 06-05 updates one business's contact_email to the real sender, find_business_by_sender (:201, exact-match) no longer matches the .example fixture from_addr → unknown_sender → the demo breaks on camera. Fix: (a) Add gate_block_hero to _DEMO_FIXTURES with the correct fixture path for the "David Reyez" scenario (Metro Deli). (b) Override the fixture's from_addr at send time: read the DEMO_CONTACT_EMAIL env var (set by the operator after 06-05) or query the seeded business's current contact_email from the DB, and use that real address instead of the fixture's baked .example address. This ensures the demo resolves after the identity swap regardless of what from_addr is in the fixture file. 06-07's recording checklist depends on this working.

Purpose of D-07 (demo-reset): D-07 treats the reset script as a named deliverable not an afterthought — Beat 3 persists a learned alias to prod, and a second recording take will NOT clarify unless the alias is cleared first. The README is the recruiter's entry point; it must lead with the thesis + disclaimer + diagram before any setup instructions. Removing the temporary debug logs from 06-05 closes the information disclosure window (LOG_WEBHOOK_DEBUG_IDS inside gateway.py).

HIGH-2 round-2 fix (demo identity preservation in reset): The seed() function uses `ON CONFLICT (id) DO UPDATE SET contact_email = EXCLUDED.contact_email` for businesses, which clobbers the real demo email back to the `.example` placeholder. demo_reset.py must re-apply the real contact_email AFTER calling seed(), using DEMO_CONTACT_EMAIL from env or a script constant. Both --confirm and --reset-aliases modes apply this re-application.

Output: A runnable demo-reset script (FK-safe, explicit --confirm), _DEMO_FIXTURES with gate_block_hero, demo_send_test handler with from_addr override, a complete recruiter-facing README with the committed architecture diagram PNG, and a clean codebase with all temporary debug logging removed. 06-07's recording path works end-to-end.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-PATTERNS.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
@/Users/pnhek/usf msds/github/payroll_agent/scripts/reset_stuck_runs.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/seed.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/schema.sql
@/Users/pnhek/usf msds/github/payroll_agent/README.md
@/Users/pnhek/usf msds/github/payroll_agent/app/email/gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HIGH-2 demo-path fix — gate_block_hero in _DEMO_FIXTURES + from_addr override + demo_reset.py + remove temp debug logs</name>
  <files>app/main.py, scripts/demo_reset.py, app/email/gateway.py</files>
  <read_first>
    - app/main.py (lines ~78-100 — _DEMO_FIXTURES dict and _DEMO_FIXTURE_DEFAULT_KEY; lines ~780-876 — the full demo_send_test handler; specifically: how fixture_data["from_addr"] is used at :831; how repo.find_business_by_sender is called at :857; understand the current fixture → from_addr → business routing flow)
    - eval/fixtures/ directory listing — confirm the exact filename for the gate_block_hero "David Reyez" scenario (Metro Deli); look for a fixture file whose body contains "David Reyez" or "Metro Deli"; the fixture path must exist on disk so the demo button can load it
    - 06-RESEARCH.md §10 "Demo-Reset Script" (what Beat 3 writes: David Reyes e0000003, Daniel Reyes e0000007; why re-running seed() is the cleanest reset)
    - scripts/reset_stuck_runs.py (the CLI pattern, DESTRUCTIVE confirm prompt, _conn_ctx usage to replicate)
    - app/db/schema.sql (read the table definitions to identify FK relationships — find which tables have FK to payroll_runs and email_messages)
    - app/db/seed.py (locate the `seed()` function and the businesses upsert block — confirm it uses `ON CONFLICT (id) DO UPDATE SET contact_email = EXCLUDED.contact_email` which clobbers the demo identity)
    - 06-PATTERNS.md §scripts/demo_reset.py (the full recommended shape: modes, seed() call, parameterized SQL discipline)
    - app/email/gateway.py (find the LOG_WEBHOOK_DEBUG_IDS guard block committed in 06-04 — the `if os.getenv("LOG_WEBHOOK_DEBUG_IDS"):` block; remove it)
    - app/main.py (search for any DEBUG_ROUND_TRIP log lines and remove them)
  </read_first>
  <behavior>
    - HIGH-2 fix Part A — gate_block_hero in _DEMO_FIXTURES:
      Add an entry to the `_DEMO_FIXTURES` dict in app/main.py:
      `"gate_block_hero": {"label": "Metro Deli — unknown shorthand 'David Reyez' (clarify + suggest)", "path": "eval/fixtures/{the-correct-gate-block-hero-file}.json"}`
      The correct fixture file path must be confirmed by reading the eval/fixtures/ directory. The fixture contains "David Reyez" submitted hours for Metro Deli, no known alias match → decides → needs_clarification → Beat 2. If no such fixture file exists, note the discrepancy and use the closest Metro Deli clarification fixture available.

    - HIGH-2 fix Part B — from_addr override in demo_send_test handler:
      The demo_send_test handler currently uses `fixture_data.get("from_addr", "payroll@coastalcleaning.example")` as the baked from_addr, which breaks after 06-05 updates a business's contact_email to a real address.
      Fix: after loading the fixture file, OVERRIDE the `from_addr` with the real demo contact. Preferred approach: read `DEMO_CONTACT_EMAIL` from the environment (`os.environ.get("DEMO_CONTACT_EMAIL", "")`). If the env var is non-empty, set `fixture_data["from_addr"] = demo_contact_email`. If the env var is empty (not yet set), fall back to the existing fixture from_addr (preserves behavior before 06-05 identity swap, maintains backward compat in test env). Add a comment: "HIGH-2: override fixture from_addr with the real demo contact so find_business_by_sender resolves after the 06-05 identity swap. Set DEMO_CONTACT_EMAIL env var to the address updated in 06-05."
      Alternative approach (if DEMO_CONTACT_EMAIL env pattern feels too fragile): query the DB for the Metro Deli business's current contact_email and use that directly. Implement whichever is simpler given the existing DB connection pattern in the handler.

    - Tests (tdd="true"):
      NEW TEST: test_demo_send_test_gate_block_hero_routes_to_seeded_business (HIGH-2):
        Monkeypatch DEMO_CONTACT_EMAIL env var to the seeded Metro Deli contact email (e.g. "hr@metrodeli.example" or a test real address). Monkeypatch repo.find_business_by_sender to return a UUID for Metro Deli when called with that address. Monkeypatch repo.insert_inbound_email to return (uuid, True). POST /demo/send-test with `fixture_key=gate_block_hero`. Assert response.status_code == 303 (redirect — not unknown_sender redirect pattern which also 303s but with no run created). Assert repo.create_run was called with the Metro Deli business_id. Assert background_tasks has a task queued. This proves gate_block_hero routes to the seeded business, not unknown_sender.
      NEW TEST: test_demo_reset_preserves_demo_contact_after_seed:
        This is a unit-level test of the demo_reset logic (no live DB). Create a FakeConn. Call the relevant part of demo_reset that applies the contact_email update after seed(). Assert the FakeConn executed an UPDATE businesses SET contact_email = %s WHERE name = %s with the DEMO_CONTACT_EMAIL and DEMO_BUSINESS_NAME values. This proves the identity re-application SQL is present and correct.

    - demo_reset.py (non-destructive default + FK-safe deletion order):
      The script REQUIRES an explicit `--confirm` flag to execute the destructive reset. Running with no args prints usage and exits 0.
      Read schema.sql to find FK-ordering constraints. The safe deletion sequence:
      Step 1 — break circular FK: `UPDATE payroll_runs SET source_email_id = NULL`
      Step 2 — delete child rows in FK order (paystub_line_items → email_messages → payroll_runs)
      Step 3 — call seed() to reset known_aliases via ON CONFLICT DO UPDATE
      Step 4 — re-apply demo contact_email: `UPDATE businesses SET contact_email = %s WHERE name = %s` with (DEMO_CONTACT_EMAIL, DEMO_BUSINESS_NAME) from env vars — read both from os.environ. If DEMO_CONTACT_EMAIL is empty, print warning: "WARNING: DEMO_CONTACT_EMAIL not set — demo identity not restored".
      Add `--reset-aliases` mode (alias reset only, no run/email purge): call seed() only, then re-apply demo contact_email. No --confirm required for this mode.
      Add module docstring explaining D-07 context and the --confirm requirement.

  </behavior>
  <action>
    In app/main.py:
    (1) Add `"gate_block_hero"` to _DEMO_FIXTURES. Read the eval/fixtures/ directory listing to find the correct fixture file for "David Reyez" / Metro Deli / unknown-shorthand scenario. The key must be "gate_block_hero" and the label must indicate the Beat-2 demo scenario. If no gate_block_hero fixture file exists on disk (not yet generated), add a note in the fixture entry and the demo button label will show the path is missing — the executor for 06-07 will generate it if needed.
    (2) In the demo_send_test handler, after `if fixture_path.exists(): fixture_data = json.loads(fixture_path.read_text())`, add the HIGH-2 from_addr override: `demo_contact = os.environ.get("DEMO_CONTACT_EMAIL", "")` and `if demo_contact: fixture_data["from_addr"] = demo_contact`. Ensure `import os` is in app/main.py imports (it likely already is). Add the inline comment as described in behavior.
    (3) In app/email/gateway.py: Find and remove the LOG_WEBHOOK_DEBUG_IDS debug block committed in 06-04. This includes the `if os.getenv("LOG_WEBHOOK_DEBUG_IDS"):` conditional block with the logging calls inside it. After removal, `grep "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py` must exit non-zero. If `import os` was added solely for this block and is no longer needed elsewhere, remove it too.
    (4) In app/main.py: Search for any `DEBUG_ROUND_TRIP` log lines. Remove them if present. `grep "DEBUG_ROUND_TRIP" app/main.py` must exit non-zero.

    Create scripts/demo_reset.py following the exact CLI pattern from reset_stuck_runs.py. See behavior block for the full deletion sequence, seed() call, and demo contact re-application. Parameterized SQL only (never f-string SQL). The script imports `from app.db.seed import seed` inline.

    In tests/test_gateway.py or tests/test_dashboard.py (whichever is more appropriate): add the two new tests described in the behavior block. Both tests are NOT xfail — they must be GREEN immediately.

    After all changes: run `uv run pytest -q -m "not integration and not live_llm" 2>&1 | tail -10` — must pass.
  </action>
  <verify>
    <automated>grep "gate_block_hero" app/main.py &amp;&amp; grep "DEMO_CONTACT_EMAIL" app/main.py &amp;&amp; ls scripts/demo_reset.py &amp;&amp; grep "seed()" scripts/demo_reset.py &amp;&amp; grep "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py | wc -l | grep -q "^0$" &amp;&amp; echo "HIGH2_AND_RESET_OK"</automated>
  </verify>
  <done>
    `grep "gate_block_hero" app/main.py` exits 0 (Beat-2 hero fixture added to _DEMO_FIXTURES — HIGH-2 Part A). `grep "DEMO_CONTACT_EMAIL" app/main.py` exits 0 (from_addr override present — HIGH-2 Part B). `ls scripts/demo_reset.py` exits 0. `grep "seed()" scripts/demo_reset.py` exits 0. `grep "\-\-confirm" scripts/demo_reset.py` exits 0 (explicit confirm flag required). `grep "known_aliases" scripts/demo_reset.py` exits 0. `grep -c "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py | grep "^0$"` exits 0 (debug flag removed — MEDIUM-6 cleanup). `grep "DEBUG_ROUND_TRIP" app/main.py` exits non-zero (removed). HIGH-2: `grep "DEMO_CONTACT_EMAIL" scripts/demo_reset.py` exits 0 (identity re-applied after seed()). `grep "UPDATE businesses" scripts/demo_reset.py` exits 0 (contact_email preserved, not clobbered by seed()). `uv run pytest -q -m "not integration and not live_llm" 2>&1 | tail -5` shows 0 FAILED. `uv run pytest -k "demo_send_test_gate_block_hero or demo_reset_preserves" -v` → both tests PASSED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full README + architecture diagram (Mermaid source + mandatory PNG)</name>
  <files>README.md, docs/architecture.mmd, docs/architecture.png</files>
  <read_first>
    - README.md (full existing file — understand the existing stub structure, the disclaimer text already there, the "full README added in hosting/demo phase" note)
    - 06-CONTEXT.md §D-11 (two-tier README: recruiter top + For Engineers section; disclaimers prominent near top; live link = "bonus, may take ~30s to wake")
    - 06-CONTEXT.md §D-11a (Mermaid source fenced in README + exported SVG + PNG; diagram must show pipeline stages + two pause states + decide.py code gate; NO Mermaid-only escape hatch)
    - 06-CONTEXT.md §D-04 (the hero artifact is the RECORDING not the live link; live link is "bonus, may take 30s to wake")
    - 06-RESEARCH.md §9 "README + Architecture Diagram" (Mermaid GitHub rendering, SVG embedding inconsistency, mmdc generation)
    - .planning/REQUIREMENTS.md §Out-of-Scope table (OBBBA provisions excluded; state withholding out of scope)
    - .planning/REQUIREMENTS.md CALC-04/05 disclaimer text (Additional Medicare 0.9% over $200k YTD unmodeled; OBBBA provisions disclaimed)
  </read_first>
  <action>
    Rewrite README.md with the two-tier D-11 structure. The recruiter top must come first; the For Engineers section is clearly delineated.

    RECRUITER TOP (before any technical section):

    Opening: Two-sentence thesis — "A messy payroll email goes in; a correct, human-approved payroll comes out. Every money-moving judgment call (name match, process-vs-clarify) is gated by deterministic code — not a model score."

    DISCLAIMER (prominent, near top, verbatim locked text per D-11):
    > **Educational only — not tax-compliant software.**
    > This system is a portfolio demonstration of agentic pipeline design, not a real payroll service.
    > OBBBA provisions (qualified-tips/overtime above-the-line deductions, expanded 15-line W-4) are
    > explicitly excluded; the engine uses the standard Pub 15-T percentage method only.
    > Additional Medicare Tax (0.9% over $200k YTD) is NOT modeled — this never triggers in demo
    > wage ranges and is disclaimed here.
    > **Do not use this for real payroll.**

    Architecture diagram (Mermaid fenced block in README):
    The diagram uses `graph TD` with these nodes: A[Client Email], B[/webhook/inbound], C{gateway.verify}, X[HTTP 400], D[gateway.parse_inbound\ntwo-step Resend fetch], E[Dedup on RFC message_id\nON CONFLICT DO NOTHING], Y[200 no pipeline], F[extract → reconcile_names → validate], G{decide.py\npure code gate}, H[_clarify → send_outbound\nstatus: awaiting_reply], B2[client replies], I[_compute_line_items\nstatus: awaiting_approval], J{Operator Dashboard\nApprove / Reject}, K[_deliver → send confirmation\nstatus: reconciled].
    Edges: A→B, B→C, C--invalid sig-->X, C--valid-->D, D→E, E--duplicate-->Y, E--new-->F, F→G, G--unresolved/collision/missing-->H, H--client replies-->B, G--process-->I, I→J, J--approve-->K.

    Then: embed the demo recording (placeholder `[![Demo](docs/demo-thumbnail.png)](link-to-recording)` — the human adds the real link after 06-07).

    Eval chart: `![Eval chart](eval/chart.svg)` — references the committed chart.svg.

    Key design choices (3 bullets): deterministic decisioning, single operator gate, free-stack demo.

    FOR ENGINEERS section (below a `---` separator):
    - Stack (FastAPI, Supabase/psycopg3, Resend, reportlab, uv)
    - Local setup: `uv sync` + set DATABASE_URL + run `uv run uvicorn app.main:app`
    - Run tests: `uv run pytest -q -m "not integration and not live_llm"`
    - Deploy: render.yaml blueprint + Supabase pooler 6543 + set 5 env vars
    - Keep-alive: GitHub Actions keepalive.yml (2x/week cron; auto-disables after 60 days of no repo activity — re-enable with workflow_dispatch)
    - Demo reset between takes: `uv run python scripts/demo_reset.py --confirm` (clears runs + resets learned aliases; requires explicit --confirm flag — NEVER a no-arg default)
    - Live link note: "The Render free service may take ~30-60s to wake from sleep. The demo recording is the primary artifact."

    Architecture image fallback: Add `![Architecture diagram](docs/architecture.png)` img tag in the README below the Mermaid block with caption "(See Mermaid diagram above for interactive version — PNG fallback for non-rendering environments)".

    MEDIUM/LOW-9 fix — MANDATORY committed PNG (no escape hatch):
    D-11a requires BOTH the Mermaid source AND a committed PNG. This is NOT optional. Do NOT accept Mermaid-only. Steps:
    1. Create docs/architecture.mmd with the exact Mermaid source text extracted from the README diagram block. This is the standalone Mermaid file.
    2. Generate docs/architecture.png using `npx -y @mermaid-js/mermaid-cli mmdc -i docs/architecture.mmd -o docs/architecture.png`. If npx is not available in the current environment, export the PNG manually from mermaid.live by pasting the Mermaid source text. The PNG MUST be committed. A stub placeholder PNG is NOT acceptable — it must render the actual diagram.
    3. If SVG generation is feasible: also run `npx -y @mermaid-js/mermaid-cli mmdc -i docs/architecture.mmd -o docs/architecture.svg`. Commit docs/architecture.svg alongside the PNG (D-11a says both SVG and PNG; SVG is preferred for quality but PNG is the guaranteed-render format).
    4. Commit docs/architecture.mmd, docs/architecture.png, and docs/architecture.svg (if generated).

    Create the docs/ directory if it does not already exist.
  </action>
  <verify>
    <automated>grep -c "educational" README.md &amp;&amp; grep -c "OBBBA" README.md &amp;&amp; grep -c "awaiting_reply" README.md &amp;&amp; ls docs/architecture.mmd &amp;&amp; ls docs/architecture.png &amp;&amp; echo "README_AND_DIAGRAM_OK"</automated>
  </verify>
  <done>
    `grep -i "educational" README.md` exits 0 (disclaimer present). `grep "OBBBA" README.md` exits 0. `grep "mermaid" README.md` exits 0 (diagram block present). `grep "awaiting_reply" README.md` exits 0 (diagram shows the pause state). `grep "For Engineers" README.md` exits 0. `grep "demo_reset" README.md` exits 0 (reset command documented with --confirm). `grep "\-\-confirm" README.md` exits 0 (explicit flag in README). `ls docs/architecture.mmd` exits 0 (Mermaid source committed). `ls docs/architecture.png` exits 0 (PNG committed — D-11a mandatory). No Mermaid-only escape hatch accepted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo_reset.py → Supabase prod DB | DESTRUCTIVE script; guarded by explicit --confirm flag (not a confirmation prompt — a CLI flag) |
| README.md → GitHub rendering | Public-facing; disclaimer must be visible and accurate |
| gateway.py cleanup | LOG_WEBHOOK_DEBUG_IDS removed; no residual debug logging in production code |
| app/main.py _DEMO_FIXTURES | Server-side allowlist — gate_block_hero added; fixture paths served from the allowlist, never client-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | demo_reset.py accidental run in prod | mitigate | Explicit `--confirm` flag required (no-arg default does nothing); `--reset-aliases` is non-destructive and needs no flag |
| T-06-06-02 | Repudiation | Disclaimer not visible in README | mitigate | Disclaimer in a blockquote near the top of the recruiter section (D-11 prominent placement); contains verbatim locked text from REQUIREMENTS.md CALC-04/05 |
| T-06-06-03 | Information Disclosure | Temporary debug logging removal | mitigate | grep check for LOG_WEBHOOK_DEBUG_IDS in app/email/gateway.py as an explicit acceptance criterion; also remind human to remove Render env var |
| T-06-06-04 | Tampering | demo_reset.py re-runs seed() which touches prod employees | accept | seed() uses ON CONFLICT DO UPDATE which only resets known_aliases (and other employee fields to seed values) — does NOT delete employees; parameterized SQL only |
| T-06-06-05 | Tampering | FK violations during demo_reset.py delete sequence | mitigate | FK-safe ordered deletion (child rows first; circular FK broken with NULL update first); schema.sql read to confirm FK relationships before writing the delete sequence |
| T-06-06-06 | Tampering | seed() clobbers production demo contact_email → find_business_by_sender stops matching | mitigate | HIGH-2: demo_reset.py re-applies real contact_email via `UPDATE businesses SET contact_email = %s WHERE name = %s` immediately after seed(); DEMO_CONTACT_EMAIL env var or script constant; warning printed if var not set |
| T-06-06-07 | Spoofing | demo_send_test uses baked .example from_addr after 06-05 identity swap → unknown_sender → demo breaks on camera | mitigate | HIGH-2 demo-path fix: demo_send_test handler overrides fixture from_addr with DEMO_CONTACT_EMAIL env var (set after 06-05); gate_block_hero added to _DEMO_FIXTURES allowlist; test_demo_send_test_gate_block_hero_routes_to_seeded_business locks this behavior |
</threat_model>

<verification>
After both tasks:
0. HIGH-2 demo-path: `grep "gate_block_hero" app/main.py` → exits 0 (Beat-2 hero fixture in _DEMO_FIXTURES). `grep "DEMO_CONTACT_EMAIL" app/main.py` → exits 0 (from_addr override present). `uv run pytest -k "demo_send_test_gate_block_hero" -v` → PASSED. `uv run pytest -k "demo_reset_preserves" -v` → PASSED
1. `grep -c "educational" README.md && grep -c "OBBBA" README.md` → both >0
2. `grep "mermaid" README.md` → exits 0 (diagram block present)
3. `grep -c "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py | grep "^0$"` → exits 0 (removed — MEDIUM-6 cleanup complete)
4. `grep "DEBUG_ROUND_TRIP" app/main.py` → exits non-zero (removed)
5. `ls scripts/demo_reset.py` → exits 0
6. `grep "seed()" scripts/demo_reset.py` → exits 0
7. `grep "\-\-confirm" scripts/demo_reset.py` → exits 0 (explicit flag required — MEDIUM-8 fix)
8. `grep "DEMO_CONTACT_EMAIL" scripts/demo_reset.py` → exits 0 (identity re-application present)
9. `grep "UPDATE businesses" scripts/demo_reset.py` → exits 0 (re-applies real email after seed())
10. `ls docs/architecture.mmd` → exits 0 (Mermaid source file committed)
11. `ls docs/architecture.png` → exits 0 (PNG committed — D-11a mandatory, no Mermaid-only escape)
12. `uv run pytest -q -m "not integration and not live_llm"` → ≥422 passed (no regressions from README or script changes)
</verification>

<success_criteria>
- HIGH-2 demo-path fix: gate_block_hero in _DEMO_FIXTURES; demo_send_test handler overrides from_addr with DEMO_CONTACT_EMAIL; test_demo_send_test_gate_block_hero_routes_to_seeded_business GREEN; the dashboard /demo/send-test path works post-identity-swap; 06-07's recording path is confirmed
- HIGH-2 (identity preservation): demo_reset.py --confirm and --reset-aliases both re-apply the production demo contact_email AFTER calling seed() — the real sender address is never clobbered back to .example; the demo identity survives every reset
- Demo-reset script can be run before every recording take (D-07 deliverable — named dependency, not afterthought)
- demo_reset.py requires explicit --confirm (MEDIUM-8 fix — no accidental destructive no-arg default)
- FK-safe deletion order (MEDIUM-8 fix — no FK violations during reset)
- README leads with thesis + disclaimer + diagram (recruiter-first, D-11)
- docs/architecture.mmd committed (Mermaid source for diffability and regeneration)
- docs/architecture.png committed (MANDATORY per D-11a — Mermaid-only is insufficient)
- Temporary LOG_WEBHOOK_DEBUG_IDS flag removed from gateway.py (MEDIUM-6 cleanup)
- Temporary DEBUG_ROUND_TRIP removed from main.py
- files_modified frontmatter includes app/main.py (HIGH-2 fix), docs/architecture.mmd and docs/architecture.png (MEDIUM/LOW-9 fix)
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-06-SUMMARY.md` when done
</output>
