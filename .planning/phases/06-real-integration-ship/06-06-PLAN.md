---
phase: "06"
plan: "06"
type: execute
wave: 5
depends_on:
  - "06-05"
files_modified:
  - scripts/demo_reset.py
  - README.md
  - app/main.py
  - app/email/gateway.py
  - docs/architecture.mmd
  - docs/architecture.png
autonomous: true
requirements:
  - OPS-04

must_haves:
  truths:
    - "scripts/demo_reset.py requires an explicit --confirm flag to run destructive mode (no-arg default does NOT delete anything)"
    - "demo_reset.py --confirm does NOT overwrite the production demo contact_email back to .example — the real sender email set in 06-05 is preserved after reset (HIGH-1); seed() re-seeds employees and known_aliases but must NOT clobber the businesses.contact_email row that was updated for the demo identity"
    - "demo_reset.py reads the demo business name from a script constant or env var and re-applies the real contact_email AFTER calling seed() — or skips the businesses upsert row in seed() that clobbers it — so the real sender address survives the reset"
    - "scripts/demo_reset.py --confirm deletes in FK-safe order (line_items → paystubs → email_messages → payroll_runs — NO alias_audit table exists in schema.sql, so alias_audit is NOT in the deletion list) then re-runs seed() to restore known_aliases"
    - "Running demo_reset.py --confirm then replaying the unknown_shorthand_metro fixture triggers clarification (Beat 2 re-arms)"
    - "_DEMO_FIXTURES in app/main.py carries a 'business_name' key on EVERY entry: coastal_exact/coastal_multi→'Coastal Cleaning Co.', metro_alias→'Metro Deli Group', unknown_shorthand_metro→'Metro Deli Group', summit_exact→'Summit Tech Solutions' (HIGH-1 per-fixture identity)"
    - "_DEMO_FIXTURES has an 'unknown_shorthand_metro' entry whose path is 'eval/fixtures/04_unknown_shorthand_metro.json' — the REAL committed Metro Deli unknown-shorthand Beat-2 fixture ('Dave Reyes', hr@metrodeli.example); no gate_block_hero phantom key"
    - "demo_send_test handler in app/main.py resolves each fixture's from_addr at send time by looking up THAT fixture's business_name in the DB: SELECT contact_email FROM businesses WHERE name = %s; uses that live contact_email as from_addr — NOT a single global DEMO_CONTACT_EMAIL override; so after 06-05 swaps ONE business's contact to the real round-trip sender, only that business's fixtures use the real sender, and ALL other fixtures keep resolving to their own seeded contacts (HIGH-1 demo-routing fix)"
    - "test_demo_send_test_coastal_routes_to_coastal: after Metro Deli's contact_email is swapped to 'real@demo.example', POST /demo/send-test with fixture_key=coastal_exact still routes to Coastal Cleaning Co., not Metro Deli"
    - "test_demo_send_test_metro_unknown_shorthand_routes_to_metro: POST /demo/send-test with fixture_key=unknown_shorthand_metro routes to Metro Deli Group (not unknown_sender 303 with no run) both before and after the identity swap"
    - "test_demo_reset_preserves_demo_contact_then_send_test_still_accepts: after demo_reset.py re-applies contact_email, the seeded business's contact_email in the DB equals the real sender address; a subsequent /demo/send-test with unknown_shorthand_metro still routes to Metro Deli"
    - "README.md contains the locked disclaimer verbatim: educational-only, OBBBA exclusion, Additional Medicare 0.9% over $200k YTD unmodeled"
    - "README has a punchy recruiter top section + Mermaid architecture diagram + For Engineers section"
    - "Mermaid diagram shows the pipeline stages + two pause states (awaiting_reply, awaiting_approval) + the code gate (decide.py)"
    - "docs/architecture.mmd committed (Mermaid source file)"
    - "docs/architecture.png committed (exported PNG — required by D-11a; Mermaid-only is NOT sufficient)"
    - "Temporary LOG_WEBHOOK_DEBUG_IDS log lines removed from app/email/gateway.py (committed in 06-04 for the debug switch)"
    - "Temporary DEBUG_ROUND_TRIP log lines (if any) removed from app/main.py"
    - "Eval chart route confirmed serving committed eval/chart.svg as static asset (D-21)"
    - "LOG_WEBHOOK_DEBUG_IDS cleanup: grep 'LOG_WEBHOOK_DEBUG_IDS' app/email/gateway.py exits non-zero after Task 1 (removed)"
  artifacts:
    - path: "scripts/demo_reset.py"
      provides: "Demo-reset helper: explicit --confirm required; FK-safe ordered deletes (no alias_audit) + seed() to restore known_aliases + re-apply demo contact_email"
      contains: "known_aliases"
    - path: "app/main.py"
      provides: "Updated _DEMO_FIXTURES with business_name on every entry + unknown_shorthand_metro Beat-2 entry; demo_send_test handler resolves from_addr per fixture's business_name via DB lookup (HIGH-1 per-fixture routing fix)"
      contains: "unknown_shorthand_metro"
    - path: "README.md"
      provides: "Full recruiter-facing README with disclaimer, diagram, Mermaid source, For Engineers section"
      contains: "educational"
    - path: "docs/architecture.mmd"
      provides: "Mermaid source file (diffable, regeneratable)"
      contains: "awaiting_reply"
    - path: "docs/architecture.png"
      provides: "Exported PNG of architecture diagram — REQUIRED by D-11a for guaranteed GitHub rendering (not optional)"
  key_links:
    - from: "scripts/demo_reset.py"
      to: "app/db/seed.py seed() function"
      via: "from app.db.seed import seed; seed() — ON CONFLICT DO UPDATE resets known_aliases"
      pattern: "from app\\.db\\.seed import seed"
    - from: "app/main.py demo_send_test handler"
      to: "businesses.contact_email (DB lookup per fixture business_name)"
      via: "SELECT contact_email FROM businesses WHERE name = %s — resolves from_addr per fixture, not one global env var; after 06-05 only ONE business's contact is real, all others keep resolving to their seeded contacts"
      pattern: "business_name|contact_email"
    - from: "README.md Mermaid diagram"
      to: "app/pipeline/orchestrator.py status states"
      via: "awaiting_reply and awaiting_approval pause states shown as nodes"
      pattern: "awaiting_reply|awaiting_approval"
---

<objective>
Wave 5: (1) HIGH-1 per-fixture routing fix — tag every _DEMO_FIXTURES entry with its business_name and resolve from_addr from THAT business's current contact_email at send time, replacing the broken single-global-DEMO_CONTACT_EMAIL override; (2) Add unknown_shorthand_metro to _DEMO_FIXTURES using the REAL committed fixture eval/fixtures/04_unknown_shorthand_metro.json ('Dave Reyes', Metro Deli, Beat 2); (3) Build the demo-reset script (D-07 — FK-safe, no alias_audit table, --confirm required); (4) Write the full README (D-11/D-11a/OPS-04); (5) Remove temporary debug logging from gateway.py and main.py; (6) Commit the architecture diagram.

HIGH-1 fix (Round-4 Codex HIGH — global DEMO_CONTACT_EMAIL misroutes multi-business demo): The round-3 fix overrode EVERY fixture's from_addr from a SINGLE DEMO_CONTACT_EMAIL env var. The recording spans 3 businesses: coastal_exact/coastal_multi → Coastal Cleaning Co. (payroll@coastalcleaning.example), unknown_shorthand_metro/metro_alias → Metro Deli Group (hr@metrodeli.example), summit_exact → Summit Tech Solutions (finance@summittech.example). A single global override misroutes them all — a Coastal-body run routes to Metro, etc. Fix: (a) Add business_name to every _DEMO_FIXTURES entry matching seed.py. (b) At demo-send time, query the DB for THAT fixture's business's current contact_email via SELECT contact_email FROM businesses WHERE name = %s and use that as from_addr. This means: after 06-05 swaps ONE business's contact to the real round-trip sender, that business's fixtures use the real sender; ALL OTHER fixtures keep resolving to their own seeded contacts; every beat routes correctly. DEMO_CONTACT_EMAIL (if still used) becomes only the address to assign to the ONE demo business during 06-05's identity swap — it is NOT used as a from_addr override at send time.

Beat-2 fixture: The REAL committed fixture for the unknown-shorthand Metro Deli beat is eval/fixtures/04_unknown_shorthand_metro.json (submits "Dave Reyes" — a shorthand without a stored alias — for Metro Deli, from_addr hr@metrodeli.example). There is NO gate_block_hero file on disk. The _DEMO_FIXTURES key is 'unknown_shorthand_metro'; the label is "Metro Deli — unknown shorthand 'Dave Reyes' (clarify + suggest)". Do NOT reference a nonexistent gate_block_hero key anywhere in the plan or tests.

LOW-4 fix: schema.sql has NO alias_audit table (tables: businesses, employees, payroll_runs, paystub_line_items, email_messages, eval_results). demo_reset.py must NOT reference alias_audit in the deletion ordering.

Purpose of D-07 (demo-reset): Beat 3 persists a learned alias to prod, and a second recording take will NOT clarify unless the alias is cleared first. The README is the recruiter's entry point. Removing the temporary debug logs closes the information disclosure window.

Output: A runnable demo-reset script (FK-safe, explicit --confirm, no alias_audit), per-fixture business-routing in _DEMO_FIXTURES, a complete recruiter-facing README with the committed architecture diagram PNG, and a clean codebase with all temporary debug logging removed. 06-07's recording path works end-to-end for all three beats.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-PATTERNS.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
@/Users/pnhek/usf msds/github/payroll_agent/scripts/reset_stuck_runs.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/seed.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/schema.sql
@/Users/pnhek/usf msds/github/payroll_agent/README.md
@/Users/pnhek/usf msds/github/payroll_agent/app/email/gateway.py
@/Users/pnhek/usf msds/github/payroll_agent/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HIGH-1 per-fixture routing fix — business_name in _DEMO_FIXTURES + per-fixture DB from_addr lookup + demo_reset.py (no alias_audit) + remove temp debug logs</name>
  <files>app/main.py, scripts/demo_reset.py, app/email/gateway.py</files>
  <read_first>
    - app/main.py (lines ~78-100 — the _DEMO_FIXTURES dict; lines ~780-876 — the full demo_send_test handler; specifically line ~831 where fixture_data["from_addr"] is used; line ~857 where repo.find_business_by_sender is called; understand the current from_addr flow)
    - eval/fixtures/ directory listing — confirm 04_unknown_shorthand_metro.json exists (Metro Deli, "Dave Reyes", from_addr hr@metrodeli.example); confirm there is NO gate_block_hero file
    - app/db/seed.py (lines 50-69 — the _BUSINESSES list; confirm EXACT business names: "Coastal Cleaning Co.", "Metro Deli Group", "Summit Tech Solutions" and their contact_emails: payroll@coastalcleaning.example, hr@metrodeli.example, finance@summittech.example)
    - app/db/schema.sql (confirm NO alias_audit table — only businesses, employees, payroll_runs, paystub_line_items, email_messages, eval_results; note the FK order for deletion)
    - scripts/reset_stuck_runs.py (the CLI pattern, --mode approach, repo._conn_ctx usage, parameterized SQL discipline)
    - 06-PATTERNS.md §scripts/demo_reset.py (the full recommended shape)
    - app/email/gateway.py (find the LOG_WEBHOOK_DEBUG_IDS guard block committed in 06-04; remove it)
  </read_first>
  <behavior>
    HIGH-1 fix Part A — business_name tag on every _DEMO_FIXTURES entry:
    Update the _DEMO_FIXTURES dict in app/main.py to add a "business_name" key to every entry, matching seed.py _BUSINESSES EXACTLY (case-sensitive):
      "coastal_exact": {"label": "Coastal Cleaning Co. — exact match", "path": "eval/fixtures/01_exact_match_coastal.json", "business_name": "Coastal Cleaning Co."}
      "metro_alias": {"label": "Metro Deli — stored alias", "path": "eval/fixtures/02_stored_alias_metro.json", "business_name": "Metro Deli Group"}
      "summit_exact": {"label": "Summit Tech — exact match", "path": "eval/fixtures/12_exact_process_summit.json", "business_name": "Summit Tech Solutions"}
      "coastal_multi": {"label": "Coastal Cleaning Co. — multi-employee", "path": "eval/fixtures/10_multi_employee_coastal.json", "business_name": "Coastal Cleaning Co."}
      "unknown_shorthand_metro": {"label": "Metro Deli — unknown shorthand 'Dave Reyes' (clarify + suggest)", "path": "eval/fixtures/04_unknown_shorthand_metro.json", "business_name": "Metro Deli Group"}
    The "unknown_shorthand_metro" key is the Beat-2 hero. Do NOT add a "gate_block_hero" key — that file does not exist.

    HIGH-1 fix Part B — per-fixture from_addr resolution in demo_send_test handler:
    After loading the fixture file and building inbound_payload, replace any flat fixture from_addr logic with a per-fixture DB lookup. In the handler, after resolving the fixture_key to a fixture dict:
      business_name = fixture.get("business_name")
      If business_name is non-empty, query the DB: SELECT contact_email FROM businesses WHERE name = %s, with (business_name,) as params. Use the returned contact_email as from_addr. Use the same DB connection context that already exists in the handler (repo._conn_ctx or the existing connection pattern).
      If business_name is absent or the DB query returns no row (fallback for tests or misconfigured fixture), fall back to fixture_data.get("from_addr", "payroll@coastalcleaning.example").
    Add a comment: "HIGH-1 (R4): resolve from_addr from THIS fixture's business's current contact_email in the DB, not a global env var. Each fixture routes to its own business; after 06-05 identity swap only ONE business has a real sender address — the others keep their seeded .example contacts."
    Remove any reference to DEMO_CONTACT_EMAIL as a from_addr override at send time. DEMO_CONTACT_EMAIL is only used in the 06-05 identity-swap SQL and demo_reset.py re-application — it is NOT used inside the demo_send_test handler.

    Tests (tdd="true"):
    NEW TEST 1 — test_demo_send_test_coastal_routes_to_coastal (HIGH-1 multi-business proof):
      Set up: monkeypatch repo._conn_ctx (or the DB lookup) so that:
        - "Coastal Cleaning Co." → returns "payroll@coastalcleaning.example"
        - "Metro Deli Group" → returns "real@demo.example"  (simulating after the 06-05 identity swap)
      Monkeypatch repo.find_business_by_sender so that "payroll@coastalcleaning.example" → returns coastal_uuid, "real@demo.example" → returns metro_uuid.
      Monkeypatch repo.insert_inbound_email to return (uuid.uuid4(), True).
      POST /demo/send-test with fixture_key=coastal_exact.
      Assert repo.create_run was called with coastal_uuid (not metro_uuid).
      This proves coastal_exact still routes to Coastal even when Metro's contact was swapped to a real address.

    NEW TEST 2 — test_demo_send_test_metro_unknown_shorthand_routes_to_metro (HIGH-1 Beat-2 fixture):
      Set up: monkeypatch the DB lookup so "Metro Deli Group" → "real@demo.example".
      Monkeypatch repo.find_business_by_sender so "real@demo.example" → returns metro_uuid.
      Monkeypatch repo.insert_inbound_email to return (uuid.uuid4(), True).
      POST /demo/send-test with fixture_key=unknown_shorthand_metro.
      Assert response.status_code == 303.
      Assert repo.create_run was called with metro_uuid (not None — not the unknown_sender path).
      This proves the Beat-2 fixture routes to Metro Deli.

    NEW TEST 3 — test_demo_reset_preserves_demo_contact_after_seed:
      Unit-level test (no live DB). Create a FakeConn.
      Call the contact re-application logic from demo_reset.py directly (extract the function or call the module's reset path with a fake connection).
      Assert FakeConn executed UPDATE businesses SET contact_email = %s WHERE name = %s with the DEMO_CONTACT_EMAIL and DEMO_BUSINESS_NAME values (or equivalent env-driven values).
      This proves seed() cannot clobber the real sender address — the re-application SQL is present and correct.

    All three tests must be GREEN immediately (no xfail).

    demo_reset.py (non-destructive default + FK-safe deletion order — no alias_audit):
    The script REQUIRES an explicit --confirm flag for destructive reset. Running with no args prints usage and exits 0.
    FK-safe deletion sequence (derived from schema.sql — NO alias_audit table):
      Step 1 — break circular FK: UPDATE payroll_runs SET source_email_id = NULL
      Step 2 — delete paystub_line_items (FKs to payroll_runs)
      Step 3 — delete email_messages (FKs to payroll_runs)
      Step 4 — delete payroll_runs
      Step 5 — call seed() to reset known_aliases via ON CONFLICT DO UPDATE
      Step 6 — re-apply demo contact_email: UPDATE businesses SET contact_email = %s WHERE name = %s with (DEMO_CONTACT_EMAIL, DEMO_BUSINESS_NAME) from os.environ. If DEMO_CONTACT_EMAIL is empty, print "WARNING: DEMO_CONTACT_EMAIL not set — demo identity not restored".
    Add --reset-aliases mode (alias reset only, no run/email purge): call seed() only, then re-apply demo contact_email. No --confirm required for this mode.
    Do NOT reference alias_audit in any SQL — that table does not exist in schema.sql.
    Add module docstring explaining D-07 context and the --confirm requirement.

  </behavior>
  <action>
    In app/main.py:
    (1) Update _DEMO_FIXTURES to add "business_name" to each entry and add the "unknown_shorthand_metro" entry (path: "eval/fixtures/04_unknown_shorthand_metro.json", business_name: "Metro Deli Group"). Do NOT add "gate_block_hero" — that file does not exist.
    (2) In the demo_send_test handler, after resolving the fixture dict, add the HIGH-1 per-fixture from_addr lookup: extract fixture.get("business_name"), query SELECT contact_email FROM businesses WHERE name = %s using the existing connection context, use the result as from_addr in inbound_payload. If the lookup fails or business_name is absent, fall back to fixture_data.get("from_addr", "payroll@coastalcleaning.example"). Add the HIGH-1 comment as described in behavior. Remove any DEMO_CONTACT_EMAIL env var override from the send-time handler.
    (3) In app/email/gateway.py: Find and remove the LOG_WEBHOOK_DEBUG_IDS debug block committed in 06-04 (the if os.getenv("LOG_WEBHOOK_DEBUG_IDS"): conditional block). After removal, grep "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py must exit non-zero.
    (4) In app/main.py: Search for any DEBUG_ROUND_TRIP log lines. Remove them if present.

    Create scripts/demo_reset.py following the exact CLI pattern from reset_stuck_runs.py. See behavior block for the full deletion sequence. Parameterized SQL only (never f-string SQL). The script imports from app.db.seed import seed inline. Do NOT reference alias_audit anywhere.

    In tests/test_dashboard.py or tests/test_gateway.py (whichever is most appropriate for demo-path tests): add the three new tests described in the behavior block. All three must be GREEN immediately (no xfail).

    After all changes: run uv run pytest -q -m "not integration and not live_llm" 2>&1 | tail -10 — must pass.
  </action>
  <verify>
    <automated>grep "unknown_shorthand_metro" app/main.py &amp;&amp; grep "business_name" app/main.py &amp;&amp; ls scripts/demo_reset.py &amp;&amp; grep "seed()" scripts/demo_reset.py &amp;&amp; grep -c "alias_audit" scripts/demo_reset.py | grep "^0$" &amp;&amp; grep -c "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py | grep "^0$" &amp;&amp; echo "HIGH1_AND_RESET_OK"</automated>
  </verify>
  <done>
    grep "unknown_shorthand_metro" app/main.py exits 0 (Beat-2 fixture added to _DEMO_FIXTURES). grep "business_name" app/main.py exits 0 (per-fixture identity tags present — HIGH-1 Part A). grep -c "gate_block_hero" app/main.py exits non-zero (phantom key not added). ls scripts/demo_reset.py exits 0. grep "seed()" scripts/demo_reset.py exits 0. grep "--confirm" scripts/demo_reset.py exits 0 (explicit confirm flag required). grep "known_aliases" scripts/demo_reset.py exits 0. grep -c "alias_audit" scripts/demo_reset.py | grep "^0$" exits 0 (alias_audit absent — LOW-4 fix). grep -c "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py | grep "^0$" exits 0 (debug flag removed). grep "DEBUG_ROUND_TRIP" app/main.py exits non-zero (removed). grep "DEMO_CONTACT_EMAIL" scripts/demo_reset.py exits 0 (identity re-applied after seed()). grep "UPDATE businesses" scripts/demo_reset.py exits 0 (contact_email preserved, not clobbered by seed()). grep -c "DEMO_CONTACT_EMAIL" app/main.py | grep "^0$" exits 0 (global override NOT present in the handler — per-fixture DB lookup used instead). uv run pytest -q -m "not integration and not live_llm" 2>&1 | tail -5 shows 0 FAILED. uv run pytest -k "demo_send_test_coastal_routes or demo_send_test_metro_unknown or demo_reset_preserves" -v shows all three tests PASSED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Full README + architecture diagram (Mermaid source + mandatory PNG)</name>
  <files>README.md, docs/architecture.mmd, docs/architecture.png</files>
  <read_first>
    - README.md (full existing file — understand the existing stub structure, the disclaimer text already there, the "full README added in hosting/demo phase" note)
    - 06-CONTEXT.md §D-11 (two-tier README: recruiter top + For Engineers section; disclaimers prominent near top; live link = "bonus, may take ~30s to wake")
    - 06-CONTEXT.md §D-11a (Mermaid source fenced in README + exported SVG + PNG; diagram must show pipeline stages + two pause states + decide.py code gate; NO Mermaid-only escape hatch)
    - 06-CONTEXT.md §D-04 (the hero artifact is the RECORDING not the live link; live link is "bonus, may take 30s to wake")
    - 06-RESEARCH.md §9 "README + Architecture Diagram" (Mermaid GitHub rendering, SVG embedding inconsistency, mmdc generation)
    - .planning/REQUIREMENTS.md §Out-of-Scope table (OBBBA provisions excluded; state withholding out of scope)
    - .planning/REQUIREMENTS.md CALC-04/05 disclaimer text (Additional Medicare 0.9% over $200k YTD unmodeled; OBBBA provisions disclaimed)
  </read_first>
  <action>
    Rewrite README.md with the two-tier D-11 structure. The recruiter top must come first; the For Engineers section is clearly delineated.

    RECRUITER TOP (before any technical section):

    Opening: Two-sentence thesis — "A messy payroll email goes in; a correct, human-approved payroll comes out. Every money-moving judgment call (name match, process-vs-clarify) is gated by deterministic code — not a model score."

    DISCLAIMER (prominent, near top, verbatim locked text per D-11):
    > **Educational only — not tax-compliant software.**
    > This system is a portfolio demonstration of agentic pipeline design, not a real payroll service.
    > OBBBA provisions (qualified-tips/overtime above-the-line deductions, expanded 15-line W-4) are
    > explicitly excluded; the engine uses the standard Pub 15-T percentage method only.
    > Additional Medicare Tax (0.9% over $200k YTD) is NOT modeled — this never triggers in demo
    > wage ranges and is disclaimed here.
    > **Do not use this for real payroll.**

    Architecture diagram (Mermaid fenced block in README):
    The diagram uses graph TD with these nodes: A[Client Email], B[/webhook/inbound], C{gateway.verify}, X[HTTP 400], D[gateway.parse_inbound\ntwo-step Resend fetch], E[Dedup on RFC message_id\nON CONFLICT DO NOTHING], Y[200 no pipeline], F[extract → reconcile_names → validate], G{decide.py\npure code gate}, H[_clarify → send_outbound\nstatus: awaiting_reply], B2[client replies], I[_compute_line_items\nstatus: awaiting_approval], J{Operator Dashboard\nApprove / Reject}, K[_deliver → send confirmation\nstatus: reconciled].
    Edges: A→B, B→C, C--invalid sig-->X, C--valid-->D, D→E, E--duplicate-->Y, E--new-->F, F→G, G--unresolved/collision/missing-->H, H--client replies-->B, G--process-->I, I→J, J--approve-->K.

    Then: embed the demo recording (placeholder "[![Demo](docs/demo-thumbnail.png)](link-to-recording)" — the human adds the real link after 06-07).

    Eval chart: "![Eval chart](eval/chart.svg)" — references the committed chart.svg.

    Key design choices (3 bullets): deterministic decisioning, single operator gate, free-stack demo.

    FOR ENGINEERS section (below a --- separator):
    - Stack (FastAPI, Supabase/psycopg3, Resend, reportlab, uv)
    - Local setup: uv sync + set DATABASE_URL + run uv run uvicorn app.main:app
    - Run tests: uv run pytest -q -m "not integration and not live_llm"
    - Deploy: render.yaml blueprint + Supabase pooler 6543 + set 5 env vars
    - Keep-alive: GitHub Actions keepalive.yml (2x/week cron; auto-disables after 60 days of no repo activity — re-enable with workflow_dispatch)
    - Demo reset between takes: uv run python scripts/demo_reset.py --confirm (clears runs + resets learned aliases; requires explicit --confirm flag — NEVER a no-arg default)
    - Live link note: "The Render free service may take ~30-60s to wake from sleep. The demo recording is the primary artifact."

    Architecture image fallback: Add "![Architecture diagram](docs/architecture.png)" img tag in the README below the Mermaid block with caption "(See Mermaid diagram above for interactive version — PNG fallback for non-rendering environments)".

    MANDATORY committed PNG (no escape hatch — D-11a):
    1. Create docs/architecture.mmd with the exact Mermaid source text extracted from the README diagram block. This is the standalone Mermaid file.
    2. Generate docs/architecture.png using "npx -y @mermaid-js/mermaid-cli mmdc -i docs/architecture.mmd -o docs/architecture.png". If npx is not available, export the PNG manually from mermaid.live by pasting the Mermaid source text. The PNG MUST be committed. A stub placeholder PNG is NOT acceptable — it must render the actual diagram.
    3. If SVG generation is feasible: also run "npx -y @mermaid-js/mermaid-cli mmdc -i docs/architecture.mmd -o docs/architecture.svg". Commit docs/architecture.svg alongside the PNG.
    4. Commit docs/architecture.mmd, docs/architecture.png, and docs/architecture.svg (if generated).

    Create the docs/ directory if it does not already exist.
  </action>
  <verify>
    <automated>grep -c "educational" README.md &amp;&amp; grep -c "OBBBA" README.md &amp;&amp; grep -c "awaiting_reply" README.md &amp;&amp; ls docs/architecture.mmd &amp;&amp; ls docs/architecture.png &amp;&amp; echo "README_AND_DIAGRAM_OK"</automated>
  </verify>
  <done>
    grep -i "educational" README.md exits 0 (disclaimer present). grep "OBBBA" README.md exits 0. grep "mermaid" README.md exits 0 (diagram block present). grep "awaiting_reply" README.md exits 0 (diagram shows the pause state). grep "For Engineers" README.md exits 0. grep "demo_reset" README.md exits 0 (reset command documented with --confirm). grep "\-\-confirm" README.md exits 0 (explicit flag in README). ls docs/architecture.mmd exits 0 (Mermaid source committed). ls docs/architecture.png exits 0 (PNG committed — D-11a mandatory). No Mermaid-only escape hatch accepted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo_reset.py → Supabase prod DB | DESTRUCTIVE script; guarded by explicit --confirm flag (not a confirmation prompt — a CLI flag) |
| README.md → GitHub rendering | Public-facing; disclaimer must be visible and accurate |
| gateway.py cleanup | LOG_WEBHOOK_DEBUG_IDS removed; no residual debug logging in production code |
| app/main.py _DEMO_FIXTURES | Server-side allowlist — unknown_shorthand_metro added with business_name tag; fixture paths served from the allowlist, never client-supplied; per-fixture business routing not a global override |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | demo_reset.py accidental run in prod | mitigate | Explicit --confirm flag required (no-arg default does nothing); --reset-aliases is non-destructive and needs no flag |
| T-06-06-02 | Repudiation | Disclaimer not visible in README | mitigate | Disclaimer in a blockquote near the top of the recruiter section (D-11 prominent placement); contains verbatim locked text from REQUIREMENTS.md CALC-04/05 |
| T-06-06-03 | Information Disclosure | Temporary debug logging removal | mitigate | grep check for LOG_WEBHOOK_DEBUG_IDS in app/email/gateway.py as an explicit acceptance criterion; also remind human to remove Render env var |
| T-06-06-04 | Tampering | demo_reset.py re-runs seed() which touches prod employees | accept | seed() uses ON CONFLICT DO UPDATE which only resets known_aliases (and other employee fields to seed values) — does NOT delete employees; parameterized SQL only |
| T-06-06-05 | Tampering | FK violations during demo_reset.py delete sequence | mitigate | FK-safe ordered deletion (paystub_line_items → email_messages → payroll_runs, after breaking circular FK with NULL update); alias_audit NOT referenced (LOW-4 fix — table does not exist in schema.sql) |
| T-06-06-06 | Tampering | seed() clobbers production demo contact_email → find_business_by_sender stops matching | mitigate | HIGH-1: demo_reset.py re-applies real contact_email via UPDATE businesses SET contact_email = %s WHERE name = %s immediately after seed(); DEMO_CONTACT_EMAIL + DEMO_BUSINESS_NAME env vars; warning printed if var not set |
| T-06-06-07 | Spoofing | demo_send_test uses a single global DEMO_CONTACT_EMAIL override → misroutes Coastal fixture to Metro after identity swap | mitigate | HIGH-1 demo-routing fix (R4): per-fixture business_name in _DEMO_FIXTURES; handler looks up THAT business's current contact_email in the DB via SELECT contact_email FROM businesses WHERE name = %s; each fixture routes to its own business regardless of which one had its contact swapped; test_demo_send_test_coastal_routes_to_coastal + test_demo_send_test_metro_unknown_shorthand_routes_to_metro lock multi-business correctness |
| T-06-06-08 | Tampering | demo_reset.py references alias_audit table that does not exist → reset fails mid-run | mitigate | LOW-4 fix: alias_audit removed from deletion sequence; schema.sql confirmed: only businesses, employees, payroll_runs, paystub_line_items, email_messages, eval_results; grep -c "alias_audit" scripts/demo_reset.py = 0 is a done criterion |
| T-06-06-SC | Tampering | slopcheck | accept | No new packages in this plan; only edits to existing files + two new scripts/docs |
</threat_model>

<verification>
After both tasks:
0. HIGH-1 per-fixture routing: grep "unknown_shorthand_metro" app/main.py exits 0. grep "business_name" app/main.py exits 0. grep -c "gate_block_hero" app/main.py | grep "^0$" exits 0 (phantom key absent). grep -c "DEMO_CONTACT_EMAIL" app/main.py | grep "^0$" exits 0 (no global from_addr override in handler). uv run pytest -k "demo_send_test_coastal_routes or demo_send_test_metro_unknown or demo_reset_preserves" -v — all three PASSED
1. grep -c "educational" README.md && grep -c "OBBBA" README.md — both > 0
2. grep "mermaid" README.md exits 0 (diagram block present)
3. grep -c "LOG_WEBHOOK_DEBUG_IDS" app/email/gateway.py | grep "^0$" exits 0 (removed)
4. grep "DEBUG_ROUND_TRIP" app/main.py exits non-zero (removed)
5. ls scripts/demo_reset.py exits 0
6. grep "seed()" scripts/demo_reset.py exits 0
7. grep "\-\-confirm" scripts/demo_reset.py exits 0 (explicit flag required)
8. grep "DEMO_CONTACT_EMAIL" scripts/demo_reset.py exits 0 (identity re-application present)
9. grep "UPDATE businesses" scripts/demo_reset.py exits 0 (re-applies real email after seed())
10. grep -c "alias_audit" scripts/demo_reset.py | grep "^0$" exits 0 (LOW-4: alias_audit absent)
11. ls docs/architecture.mmd exits 0 (Mermaid source file committed)
12. ls docs/architecture.png exits 0 (PNG committed — D-11a mandatory, no Mermaid-only escape)
13. uv run pytest -q -m "not integration and not live_llm" — no regressions
</verification>

<success_criteria>
- HIGH-1 demo-routing fix (R4): every _DEMO_FIXTURES entry has business_name matching seed.py; demo_send_test resolves from_addr from THAT business's current DB contact_email — not a global env var; coastal_exact still routes to Coastal after Metro's contact is swapped; unknown_shorthand_metro routes to Metro Deli; test_demo_send_test_coastal_routes_to_coastal + test_demo_send_test_metro_unknown_shorthand_routes_to_metro + test_demo_reset_preserves_demo_contact_after_seed all GREEN
- Beat-2 fixture: unknown_shorthand_metro key uses REAL committed file eval/fixtures/04_unknown_shorthand_metro.json; no gate_block_hero phantom anywhere in the plan or code
- LOW-4 fix: alias_audit absent from demo_reset.py deletion sequence (table does not exist in schema.sql)
- Demo-reset script can be run before every recording take (D-07 deliverable — named dependency, not afterthought)
- demo_reset.py requires explicit --confirm (no accidental destructive no-arg default)
- FK-safe deletion order (no FK violations, no alias_audit reference)
- README leads with thesis + disclaimer + diagram (recruiter-first, D-11)
- docs/architecture.mmd committed (Mermaid source for diffability and regeneration)
- docs/architecture.png committed (MANDATORY per D-11a — Mermaid-only is insufficient)
- Temporary LOG_WEBHOOK_DEBUG_IDS flag removed from gateway.py
- Temporary DEBUG_ROUND_TRIP removed from main.py
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-06-SUMMARY.md` when done
</output>
