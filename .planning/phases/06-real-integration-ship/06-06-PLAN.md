---
phase: "06"
plan: "06"
type: execute
wave: 5
depends_on:
  - "06-05"
files_modified:
  - scripts/demo_reset.py
  - README.md
  - app/main.py
autonomous: true
requirements:
  - OPS-04

must_haves:
  truths:
    - "scripts/demo_reset.py --reset-demo runs without error: purges all runs + email_messages + resets employees.known_aliases to seed values"
    - "Running demo_reset.py then replaying the gate_block_hero fixture triggers clarification (Beat 2 re-arms)"
    - "README.md contains the locked disclaimer verbatim: educational-only, OBBBA exclusion, Additional Medicare 0.9% over $200k YTD unmodeled"
    - "README has a punchy recruiter top section + Mermaid architecture diagram + For Engineers section"
    - "Mermaid diagram shows the pipeline stages + two pause states (awaiting_reply, awaiting_approval) + the code gate (decide.py)"
    - "README committed PNG of the diagram exists for guaranteed GitHub rendering"
    - "Temporary DEBUG_ROUND_TRIP log lines removed from app/main.py"
    - "Eval chart route confirmed serving committed eval/chart.svg as static asset (D-21)"
  artifacts:
    - path: "scripts/demo_reset.py"
      provides: "Demo-reset helper: purge all runs/emails + reset known_aliases to seed values"
      contains: "known_aliases"
    - path: "README.md"
      provides: "Full recruiter-facing README with disclaimer, diagram, Mermaid source, For Engineers section"
      contains: "educational"
    - path: "docs/architecture.png"
      provides: "Exported PNG of architecture diagram for guaranteed GitHub rendering"
  key_links:
    - from: "scripts/demo_reset.py"
      to: "app/db/seed.py seed() function"
      via: "from app.db.seed import seed; seed() — ON CONFLICT DO UPDATE resets known_aliases"
      pattern: "from app\\.db\\.seed import seed"
    - from: "README.md Mermaid diagram"
      to: "app/pipeline/orchestrator.py status states"
      via: "awaiting_reply and awaiting_approval pause states shown as nodes"
      pattern: "awaiting_reply|awaiting_approval"
---

<objective>
Wave 5: Build the demo-reset script (D-07 — MUST exist before recording), write the full README (D-11/D-11a/OPS-04), and remove the temporary debug logging from app/main.py. These are all autonomous — no live accounts needed.

Purpose: D-07 treats the reset script as a named deliverable not an afterthought — Beat 3 persists a learned alias to prod, and a second recording take will NOT clarify unless the alias is cleared first. The README is the recruiter's entry point; it must lead with the thesis + disclaimer + diagram before any setup instructions. Removing the temporary debug logs from 06-05 closes the information disclosure window.

Output: A runnable demo-reset script that fully re-arms the demo between takes, a complete recruiter-facing README with the architecture diagram, and a clean codebase with no temporary debug logging.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-PATTERNS.md
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-RESEARCH.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/scripts/reset_stuck_runs.py
@/Users/pnhek/usf msds/github/payroll_agent/app/db/seed.py
@/Users/pnhek/usf msds/github/payroll_agent/README.md
@/Users/pnhek/usf msds/github/payroll_agent/app/main.py
@/Users/pnhek/usf msds/github/payroll_agent/.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: demo_reset.py script + remove temp debug logs</name>
  <files>scripts/demo_reset.py, app/main.py</files>
  <read_first>
    - scripts/reset_stuck_runs.py (full file — the CLI pattern, circular FK break, DESTRUCTIVE confirm prompt, _conn_ctx usage to replicate)
    - app/db/seed.py (locate the `seed()` function or the employee upsert block that uses ON CONFLICT DO UPDATE known_aliases = EXCLUDED.known_aliases — this is the alias reset mechanism per RESEARCH.md §10)
    - 06-PATTERNS.md §scripts/demo_reset.py (the full recommended shape: two modes --reset-demo and --reset-aliases; the seed() call pattern; parameterized SQL discipline)
    - 06-RESEARCH.md §10 "Demo-Reset Script" (what Beat 3 writes: David Reyes e0000003, Daniel Reyes e0000007; the SQL for alias reset; why re-running seed() is the cleanest reset)
    - app/main.py (find the DEBUG_ROUND_TRIP log lines added in 06-05 and remove them)
  </read_first>
  <action>
    Create scripts/demo_reset.py. Follow the exact CLI pattern from reset_stuck_runs.py: argparse-free sys.argv, explicit mode string, DESTRUCTIVE confirmation prompt ("Type 'PURGE' to confirm:"), `with repo._conn_ctx(None) as (c, _owns)` connection pattern, parameterized SQL only (never f-string SQL — the project's locked discipline).

    Two modes:
    - `--reset-demo` (default if no arg): Step 1 — break circular FK: `UPDATE payroll_runs SET source_email_id = NULL` (parameterized). Step 2 — delete all email_messages: `DELETE FROM email_messages`. Step 3 — delete all payroll_runs: `DELETE FROM payroll_runs`. Step 4 — reset known_aliases to seed values by calling `seed()` from `app.db.seed`. Import inline: `from app.db.seed import seed; seed()`. The seed() function uses `ON CONFLICT DO UPDATE known_aliases = EXCLUDED.known_aliases` — this idempotently resets every employee's aliases to the seed values, wiping any learned aliases from Beat 3.
    - `--reset-aliases` (alias reset only, no run/email purge): call `seed()` only.

    Add a DESTRUCTIVE confirmation prompt (same as reset_stuck_runs.py): print the warning, read input, abort if not "PURGE". Add `--yes` flag to bypass for scripted use.

    Add module docstring explaining D-07 context: Beat 3 persists "David Reyez" → David Reyes alias to prod; this script clears it so a second recording take sees Beat 2 clarify again. Document the run command: `uv run python scripts/demo_reset.py --reset-demo`.

    In app/main.py: Search for any `DEBUG_ROUND_TRIP` log lines added in 06-05 and remove them. Confirm no temporary debug logging remains.

    Verify the script runs (against local DB — it will delete runs, which is expected on dev):
    The automated verify uses `uv run python -m pytest` not direct script execution, because the script needs DATABASE_URL set; the verify checks the file exists and the seed import works.
  </action>
  <verify>
    <automated>uv run python -c "import scripts.demo_reset as d; print('import OK')" 2>&amp;1 || uv run python -c "import sys; sys.path.insert(0,'scripts'); exec(open('scripts/demo_reset.py').read().split('def main')[0]); print('parse OK')"</automated>
  </verify>
  <done>
    `ls scripts/demo_reset.py` exits 0. `grep "seed()" scripts/demo_reset.py` exits 0. `grep "known_aliases" scripts/demo_reset.py` exits 0. `grep "DEBUG_ROUND_TRIP" app/main.py` exits non-zero (removed). `grep "PURGE" scripts/demo_reset.py` exits 0 (confirmation prompt present).
  </done>
</task>

<task type="auto">
  <name>Task 2: Full README with disclaimer, Mermaid diagram, and recruiter top section</name>
  <files>README.md</files>
  <read_first>
    - README.md (full existing file — understand the existing stub structure, the disclaimer text already there at lines 27-35, the "full README added in hosting/demo phase" note)
    - 06-CONTEXT.md §D-11 (two-tier README: recruiter top + For Engineers section; disclaimers prominent near top; live link = "bonus, may take ~30s to wake")
    - 06-CONTEXT.md §D-11a (Mermaid source fenced in README + exported SVG + PNG; diagram must show pipeline stages + two pause states + decide.py code gate)
    - 06-CONTEXT.md §D-04 (the hero artifact is the RECORDING not the live link; live link is "bonus, may take 30s to wake")
    - 06-RESEARCH.md §9 "README + Architecture Diagram" (Mermaid GitHub rendering, SVG embedding inconsistency, existing README.md Additional Medicare disclaimer already present)
    - .planning/REQUIREMENTS.md §Out-of-Scope table (OBBBA provisions excluded; state withholding out of scope)
    - .planning/REQUIREMENTS.md CALC-04/05 disclaimer text (Additional Medicare 0.9% over $200k YTD unmodeled; OBBBA provisions disclaimed)
  </read_first>
  <action>
    Rewrite README.md with the two-tier D-11 structure. The recruiter top must come first; the For Engineers section is clearly delineated.

    RECRUITER TOP (before any technical section):

    Opening: Two-sentence thesis — "A messy payroll email goes in; a correct, human-approved payroll comes out. Every money-moving judgment call (name match, process-vs-clarify) is gated by deterministic code — not a model score."

    DISCLAIMER (prominent, near top, verbatim locked text per D-11):
    ```
    > **Educational only — not tax-compliant software.**
    > This system is a portfolio demonstration of agentic pipeline design, not a real payroll service.
    > OBBBA provisions (qualified-tips/overtime above-the-line deductions, expanded 15-line W-4) are
    > explicitly excluded; the engine uses the standard Pub 15-T percentage method only.
    > Additional Medicare Tax (0.9% over $200k YTD) is NOT modeled — this never triggers in demo
    > wage ranges and is disclaimed here.
    > **Do not use this for real payroll.**
    ```

    Architecture diagram (Mermaid fenced block):
    ```mermaid
    graph TD
        A[Client Email] --> B[/webhook/inbound]
        B --> C{gateway.verify}
        C -- invalid sig --> X[HTTP 400]
        C -- valid --> D[gateway.parse_inbound\ntwo-step Resend fetch]
        D --> E[Dedup on RFC message_id\nON CONFLICT DO NOTHING]
        E -- duplicate --> Y[200 no pipeline]
        E -- new --> F[extract → reconcile_names → validate]
        F --> G{decide.py\npure code gate}
        G -- unresolved/collision/missing --> H[_clarify → send_outbound\nstatus: awaiting_reply]
        H -- client replies --> B
        G -- process --> I[_compute_line_items\nstatus: awaiting_approval]
        I --> J{Operator Dashboard\nApprove / Reject}
        J -- approve --> K[_deliver → send confirmation\nstatus: reconciled]
    ```

    Then: embed the demo recording (placeholder `[![Demo](docs/demo-thumbnail.png)](link-to-recording)` — the human adds the real link after 06-07).

    Eval chart: `![Eval chart](eval/chart.svg)` — references the committed chart.svg.

    Key design choices (3 bullets): deterministic decisioning, single operator gate, free-stack demo.

    FOR ENGINEERS section (below a `---` separator):
    - Stack (FastAPI, Supabase/psycopg3, Resend, reportlab, uv)
    - Local setup: `uv sync` + set DATABASE_URL + run `uv run uvicorn app.main:app`
    - Run tests: `uv run pytest -q -m "not integration and not live_llm"`
    - Deploy: render.yaml blueprint + Supabase pooler 6543 + set 5 env vars
    - Keep-alive: GitHub Actions keepalive.yml (2x/week cron; auto-disables after 60 days of no repo activity — re-enable with workflow_dispatch)
    - Demo reset between takes: `uv run python scripts/demo_reset.py --reset-demo` (clears runs + resets learned aliases)
    - Live link note: "The Render free service may take ~30-60s to wake from sleep. The demo recording is the primary artifact."

    For the PNG: Note in the README that `docs/architecture.png` is the fallback rendering. Since mmdc is not locally installed (per RESEARCH.md §9), create a `docs/` directory and add a placeholder PNG note: either export from the Mermaid live editor at mermaid.live, or accept Mermaid-only. Add `docs/architecture.png` to the README's img tag as a fallback: `![Architecture diagram](docs/architecture.png)` with a note `(See Mermaid diagram above for an interactive version)`. If mmdc can be run via npx: `npx -y @mermaid-js/mermaid-cli -i docs/architecture.mmd -o docs/architecture.png`. Create `docs/architecture.mmd` with the Mermaid source extracted from the README block (the source is in the README; the .mmd file makes it easy to regenerate the PNG later).

    The README is the recruiter's first impression. Lead with the thesis, lead with the disclaimer, lead with the diagram. The engineering setup section is secondary.
  </action>
  <verify>
    <automated>grep -c "educational" README.md &amp;&amp; grep -c "OBBBA" README.md &amp;&amp; grep -c "awaiting_reply" README.md &amp;&amp; echo "README_OK"</automated>
  </verify>
  <done>
    `grep -i "educational" README.md` exits 0 (disclaimer present). `grep "OBBBA" README.md` exits 0. `grep "mermaid" README.md` exits 0 (diagram block present). `grep "awaiting_reply" README.md` exits 0 (diagram shows the pause state). `grep "For Engineers" README.md` exits 0. `grep "demo_reset" README.md` exits 0 (reset command documented). `ls docs/architecture.mmd` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo_reset.py → Supabase prod DB | DESTRUCTIVE script; guarded by confirmation prompt |
| README.md → GitHub rendering | Public-facing; disclaimer must be visible and accurate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-01 | Tampering | demo_reset.py accidental run in prod | mitigate | Explicit "PURGE" confirmation prompt (same pattern as reset_stuck_runs.py --purge-all); --yes flag only for scripted use with explicit intent |
| T-06-06-02 | Repudiation | Disclaimer not visible in README | mitigate | Disclaimer in a blockquote near the top of the recruiter section (D-11 prominent placement); contains verbatim locked text from REQUIREMENTS.md CALC-04/05 |
| T-06-06-03 | Information Disclosure | Temporary debug logging removal | mitigate | grep check for DEBUG_ROUND_TRIP in app/main.py as an explicit acceptance criterion |
| T-06-06-04 | Tampering | demo_reset.py re-runs seed() which touches prod employees | accept | seed() uses ON CONFLICT DO UPDATE which only resets known_aliases (and other employee fields to their seed values) — it does NOT delete employees; parameterized SQL only |
</threat_model>

<verification>
After both tasks:
1. `grep -c "educational" README.md && grep -c "OBBBA" README.md` → both >0
2. `grep "mermaid" README.md` → exits 0 (diagram block present)
3. `grep "DEBUG_ROUND_TRIP" app/main.py` → exits non-zero (removed)
4. `ls scripts/demo_reset.py` → exits 0
5. `grep "seed()" scripts/demo_reset.py` → exits 0
6. `uv run pytest -q -m "not integration and not live_llm"` → ≥422 passed (no regressions from README or script changes)
</verification>

<success_criteria>
- Demo-reset script can be run before every recording take (D-07 deliverable — named dependency, not afterthought)
- README leads with thesis + disclaimer + diagram (recruiter-first, D-11)
- Temporary debug logging removed from app/main.py (closes the 06-05 information disclosure window)
- The architecture diagram source is committed (docs/architecture.mmd) for future PNG regeneration
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-06-SUMMARY.md` when done
</output>
