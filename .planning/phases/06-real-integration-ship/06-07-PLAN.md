---
phase: "06"
plan: "07"
type: execute
wave: 6
depends_on:
  - "06-06"
autonomous: false
files_modified:
  - README.md
requirements:
  - OPS-04

must_haves:
  truths:
    - "Demo recording exists with all three beats visible, captured via the /demo/compose self-serve landing page (record_only=True — no real Resend sends during the take)"
    - "Beat 1: clean payroll compose → operator approves → confirmation email sent (record_only, no real Resend); all via /demo/compose for Coastal Cleaning Co. — business selected in picker, employee roster shown, payroll body typed, 'Run pipeline' clicked, thread view shows request → awaiting_approval, Approve clicked, run reaches reconciled"
    - "Beat 2: compose for Metro Deli Group with 'Dave Reyes' unknown shorthand → code gate clarifies → suggestion names 'David Reyes' → in-page reply box shown in thread view → operator types reply and submits; clarification is record_only (synthetic outbound row, no real Resend); in-page reply via simulate-reply seam"
    - "Beat 3: operator approves the resolved Metro run → alias learned → re-compose the same 'Dave Reyes' shorthand for Metro → run resolves directly without clarification ('It learned. Won't ask again.')"
    - "Eval closing shot shows false_process_count=0 in the per-category chart"
    - "Recording is 60-90 seconds after editing (dead-air cuts allowed per D-05)"
    - "README.md links to the recording embed or URL"
    - "Eval chart served as static asset at /eval/chart.svg on the deployed Render service (D-21 confirmed)"
    - "Pre-recording smoke test PASSED for EACH beat: /demo/compose for Coastal Cleaning Co. routes the run to Coastal AND /demo/compose for Metro Deli Group routes the run to Metro (verified by checking business name on the run detail page) — business_id routing via _SEED_BUSINESS_IDS; confirmed before recording starts"
    - "Path-2 real Gmail round-trip is a SEPARATE supporting recorded-proof clip (from 06-05 gate output) — referenced in README 'For Engineers' / proof of real integration section, NOT the primary 60-90s beats"
  artifacts:
    - path: "README.md"
      provides: "Updated with real demo recording link/embed + confirmed Render URL"
      contains: "demo"
  key_links:
    - from: "Demo recording (primary 60-90s clip)"
      to: "/demo/compose landing page (recruiter hero path)"
      via: "Beats 1-3 all recorded via /demo/compose (record_only=True — no real Resend during the take); business selected in picker; _SEED_BUSINESS_IDS routes by business_id directly (no find_business_by_sender, no contact_email lookup); in-page reply for Beat 2 clarify beat via simulate-reply seam"
      pattern: "demo/compose|_SEED_BUSINESS_IDS"
    - from: "Path-2 supporting clip"
      to: "06-05 gate output (real Gmail round-trip)"
      via: "Separate supporting proof clip referenced in README 'For Engineers' section; not part of the main 60-90s recording; payroll@jiodnel.resend.app is the real inbound address"
      pattern: "resend.app|real-email"
---

<objective>
Wave 6 — Final BLOCKING human checkpoint (autonomous:false). The demo recording (D-04..D-06) and the README update linking to it. This is the last deliverable for Phase 6.

Purpose: The demo recording is the HERO ARTIFACT for the recruiter audience (D-04). It is recorded via the /demo/compose self-serve landing page (record_only=True — no real Resend sends during the take, no SMTP, no email client needed) so the operator can control each beat cleanly in-app. The compose flow: pick business → employees shown → type payroll body → "Run pipeline" → thread view shows the full conversation (request → clarify/approve → confirmation). Beat 2's clarify beat uses the in-page reply box (simulate-reply seam) within the thread view. The D-09b gate (06-05) proved real transport works once; the recording proves the three thesis beats (deterministic gate, suggestion call, learned alias) in a clean 60-90s clip driven from /demo/compose.

RECORDING PATH (confirmed by USER DECISION in REVIEWS.md MEDIUM-3):
- Primary 60-90s recording: /demo/compose (self-serve, record_only=True, no real email). All three beats on this path.
- Path-2 supporting clip (real Gmail round-trip): SEPARATE clip from the 06-05 gate session. Referenced in README under "For Engineers" / real-integration proof — not the main demo recording.

ROUTING MODEL (no contact_email lookups in this recording path):
- /demo/compose routes by _SEED_BUSINESS_IDS (stable seed UUIDs) — no find_business_by_sender call, no contact_email lookup, no businesses.contact_email mutation.
- Business picker selects from the three seeded businesses; compose sets from_addr = _SEED_CONTACTS[business_name] (seed .example contact, stable and never mutated).
- Pre-recording smoke test checks that composing for Coastal routes the run to Coastal AND composing for Metro routes the run to Metro — by inspecting the business name on the resulting run detail page.

D-07 is the prerequisite — run `uv run python scripts/demo_reset.py --confirm` (full destructive reset) before starting to ensure Beat 2 clarifies on the first take and Beat 3's learned alias does not survive from a prior run. Use `--reset-aliases` instead if only the learned aliases need re-arming (non-destructive, preserves run history). These are the real flags that 06-06 builds.

WATCH THE 60-90s BUDGET: The three-beat + eval-shot arc must fit in under 90 seconds after dead-air cuts. Keep each beat tight: compose → pipeline → result. The in-page reply for Beat 2 is the most time-consuming beat — have the reply text ready to paste.

Output: A completed, committed recording (or public video link) and an updated README with the embed. Phase 6 is done.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/README.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-04..D-07 — Pre-recording smoke test + Record the /demo/compose demo + update README</name>
  <what-built>scripts/demo_reset.py is ready (06-06), the deployed Render service with Resend is confirmed (06-05), /demo/compose is live (06-08 Wave 3 — recruiter self-serve landing page, record_only=True, routes by _SEED_BUSINESS_IDS with no contact_email mutation), the in-page reply seam (simulate-reply) is in the thread view for Beat 2, and the alias-rationale notes ("known nickname from a prior confirmed run") are rendered in run_detail for Beat 3. The A5 gate was resolved in 06-05 (either PASSED or D-03a subject-token fallback complete). The README is the recruiter-facing entry point awaiting the recording link.</what-built>
  <how-to-verify>
    PRE-RECORDING CHECKLIST (do these before hitting record):

    1. Pre-warm the Render service (D-05): curl https://{render-url}/ 30-60s before recording to wake it from sleep.

    2. Reset the demo state (D-07): Run `uv run python scripts/demo_reset.py --confirm` (full destructive reset — clears all runs and emails, then re-seeds known_aliases and re-arms the demo_sender_bindings row via DEMO_BUSINESS_NAME). Confirm by visiting the Render dashboard — runs list should be empty.

    3. Pre-recording smoke test — /demo/compose routing verification (BOTH beats, REQUIRED before recording):

       These tests use /demo/compose, which routes by _SEED_BUSINESS_IDS directly — no contact_email lookups, no businesses.contact_email mutation. The test verifies that the business picker correctly resolves each business_name to its run.

       BEAT 1 ROUTING CHECK (Coastal Cleaning Co.):
       Visit https://{render-url}/ (the /demo/compose landing page).
       Select "Coastal Cleaning Co." in the business picker (or navigate with ?business=Coastal+Cleaning+Co.).
       Confirm the employee roster for Coastal is shown.
       Type a short payroll body (e.g. "Maria 40h regular") and click "Run pipeline".
       Confirm:
       (a) You are redirected to /runs/{run_id}.
       (b) The run detail shows "Coastal Cleaning Co." as the business — NOT Metro Deli or an error.
       If (b) fails: the _SEED_BUSINESS_IDS constant may have the wrong UUID for Coastal. STOP. Check that seed.py's Coastal UUID (b0000001-0000-0000-0000-000000000001) matches the businesses table entry. Run demo_reset and retry before recording.

       BEAT 2 ROUTING CHECK (Metro Deli Group, "Dave Reyes" shorthand):
       Return to / (landing page).
       Select "Metro Deli Group" in the business picker.
       Confirm the Metro Deli employee roster is shown (should include David Reyes).
       Type a payroll body using the unknown shorthand (e.g. "Dave Reyes 40h").
       Click "Run pipeline". Confirm:
       (a) You are redirected to /runs/{run_id}.
       (b) The run shows "Metro Deli Group" as the business.
       (c) The run reaches awaiting_reply (clarification triggered — "Dave Reyes" is unknown and should clarify).
       (d) The thread view shows the clarification card (outbound, purpose=clarification) with the suggestion naming David Reyes.
       (e) The in-page reply box is visible in the thread view (awaiting_reply state).
       If any of (a)-(e) fail: STOP. Check that the Metro Deli employees include David Reyes and that the decision logic fires clarification for "Dave Reyes". Run demo_reset --reset-aliases and retry.

       Both beats must pass the routing check before proceeding. This confirms _SEED_BUSINESS_IDS routing is correct for all recording beats.

    4. Do a fresh demo_reset before actual recording: `uv run python scripts/demo_reset.py --confirm` (clears the routing-check runs, re-arms aliases and binding).

    RECORDING BEATS (D-06 — all three beats + eval closing shot, all via /demo/compose):

    BEAT 1 (clean compose → approve → deliver):
    - Visit the Render landing page (https://{render-url}/ or the /demo/compose surface).
    - Select "Coastal Cleaning Co." in the picker. Show the employee roster.
    - Type a clean payroll body (e.g. "Maria Rodriguez 40h, James Chen 38h regular") in the composer. Click "Run pipeline".
    - Show the run reaching awaiting_approval in the thread view (request card visible at top).
    - Click Approve.
    - Show the run reaching reconciled; note the confirmation card in the thread view (sent record_only — no real email).

    BEAT 2 (unknown shorthand → clarify + suggestion → in-page reply):
    - Return to / and select "Metro Deli Group". Show the Metro employee roster.
    - Type a payroll body with the unknown shorthand: "Dave Reyes 40h" (or similar). Click "Run pipeline".
    - Show the thread view: request card → clarification card ("Did you mean David Reyes?").
    - Show the in-page reply box below the clarification card.
    - Narrate: "The code gate caught this — the model never decided, it just read the email. The suggestion call named the specific employee."
    - Type a reply (e.g. "Yes, David Reyes 40h") in the reply box and submit (simulate-reply).
    - Show the run resume and reach awaiting_approval with resolved decision.

    BEAT 3 (approve resolved → alias learned → re-compose resolves without clarification):
    - Click Approve on the resolved Metro run.
    - Show the run reaching reconciled. Note the alias-rationale card: "Resolved 'Dave Reyes' to David Reyes (known nickname from a prior confirmed run)."
    - Return to / and select Metro Deli Group again.
    - Type the SAME shorthand: "Dave Reyes 40h". Click "Run pipeline".
    - Show the run going directly to awaiting_approval (no clarification) — alias was learned.
    - Narrate: "It learned. Won't ask again."

    CLOSING SHOT (~5-10s):
    - Navigate to the eval view (/eval).
    - Show the per-category chart: extraction accuracy, reconciliation accuracy, decision accuracy.
    - Highlight false_process_count=0.

    POST-RECORDING:
    - Edit out dead-air (spinner wait times, typing pauses) per D-05. Keep cuts AWAY from network boundaries where the pipeline is actively running (D-05 caution). Watch the 60-90s budget.
    - Upload to YouTube unlisted, Google Drive, Loom, or a hosted GIF.

    PATH-2 SUPPORTING CLIP (reference, not a recording beat):
    - The real Gmail round-trip proof clip from 06-05 is a SEPARATE asset — reference it in the README under "For Engineers" or "Real integration proof". It shows payroll@jiodnel.resend.app receiving a real email from pjnhek@gmail.com and the full Resend round-trip working.
    - Do NOT record it as part of the primary 60-90s beats.

    README UPDATE:
    - Update README.md to replace the demo recording placeholder with the real embed or link.
    - Reference the Path-2 clip separately (e.g. under "For Engineers: Real email round-trip proof").
    - Update the live Render URL if it was a placeholder.
    - Confirm https://{render-url}/eval/chart.svg returns the committed chart (D-21 final check):
      `curl -s -o /dev/null -w "%{http_code}" https://{render-url}/eval/chart.svg`
      Expected: 200.

    FINAL VALIDATION:
    - `uv run pytest -q` (full suite including integration) → all green
    - `curl -s https://{render-url}/health/live` → `{"status":"ok"}`
    - `curl -s https://{render-url}/health/ready` → `{"status":"ready"}`
    - `curl -s -o /dev/null -w "%{http_code}" https://{render-url}/eval/chart.svg` → 200
    - GitHub Actions → keepalive.yml: trigger manually (workflow_dispatch) and confirm it runs without error
  </how-to-verify>
  <resume-signal>Type "demo-recorded: {recording-url}" — e.g., "demo-recorded: https://youtu.be/xyz123". Include confirmation that BOTH routing smoke tests passed (/demo/compose for Coastal routed to Coastal AND /demo/compose for Metro routed to Metro via _SEED_BUSINESS_IDS). Also confirm the three beats are visible in the recording and the eval closing shot shows false_process_count=0. If anything failed, describe what and whether it blocks Phase 6 sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo recording screen | Must not show API keys, DATABASE_URL, or signing secret on camera (D-09 reminder) |
| Public recording URL | Not sensitive; listed in README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information Disclosure | Recording screen capture exposes secrets | mitigate | D-09 explicit instruction: env vars live in Render dashboard only; recording uses the app UI (no env tabs on camera); if Render dashboard appears, close the Environment tab first |
| T-06-07-02 | Repudiation | Demo doesn't show false_process_count=0 | mitigate | Closing shot explicitly on the eval view showing the metric (D-06); eval chart is a baked-in static asset (D-21) |
| T-06-07-03 | Integrity | Beat 1 or Beat 2 compose misroutes — Coastal routes to Metro or Dave Reyes shorthand doesn't clarify | mitigate | Pre-recording /demo/compose smoke test (Step 3 in how-to-verify) explicitly checks BOTH beats BEFORE recording begins; STOP condition defined for each; routing is by _SEED_BUSINESS_IDS (stable seed UUIDs, no contact_email lookup) — businesses.contact_email is never mutated by any compose or bind flow |
| T-06-07-04 | Integrity | Beat 2 in-page reply box not visible (simulate-reply seam broken) | mitigate | Beat 2 routing check step (c)-(e) explicitly confirms the in-page reply box is present in awaiting_reply state; if absent, check that run_detail.html has the simulate-reply form and the run status is awaiting_reply |
| T-06-07-SC | Tampering | slopcheck + blocking human | accept | No new packages in this plan; only README update and recording |
</threat_model>

<verification>
Human confirms:
1. Pre-recording /demo/compose smoke tests BOTH PASSED before recording: composing for Coastal Cleaning Co. routed to Coastal AND composing for Metro Deli Group routed to Metro — routing confirmed via _SEED_BUSINESS_IDS (no contact_email lookup, no businesses mutation)
2. "demo-recorded: {url}" — the recording exists and is publicly accessible
3. All three beats visible in the recording (Beat 1 via /demo/compose Coastal clean path; Beat 2 via /demo/compose Metro "Dave Reyes" clarify + in-page reply; Beat 3 via /demo/compose Metro same shorthand resolves without clarification — alias learned)
4. Eval closing shot shows false_process_count=0
5. README updated with the real recording link + Path-2 real-email clip referenced separately
6. /eval/chart.svg returns 200 on the deployed service (D-21 confirmed)
7. Full pytest suite green: `uv run pytest -q`
8. keepalive.yml triggers successfully via workflow_dispatch
</verification>

<success_criteria>
- Pre-recording /demo/compose smoke test: BOTH beats route correctly via _SEED_BUSINESS_IDS before recording begins — no contact_email lookups, no businesses.contact_email mutation
- 60-90s demo recording exists and is linked from the README; recording is on /demo/compose (record_only=True, no real SMTP during the take)
- All three beats visible: (1) clean Coastal compose→approve→deliver; (2) Metro "Dave Reyes" compose→clarify→in-page-reply; (3) Metro re-compose resolves without clarification (alias learned)
- Path-2 real-email round-trip clip referenced separately in README (not part of the 60-90s primary recording)
- Phase 6 OPS-04 requirement satisfied: README + disclaimer + diagram + demo recording all exist
- The free-stack deployment (Render + Supabase + GitHub Actions) is confirmed end-to-end
- Phase 6 is COMPLETE
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-07-SUMMARY.md` when done.
Then update ROADMAP.md Phase 6 status to "Complete" and record the Render URL, recording URL, and completion date.
</output>
