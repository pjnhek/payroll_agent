---
phase: "06"
plan: "07"
type: execute
wave: 6
depends_on:
  - "06-06"
autonomous: false
files_modified:
  - README.md
requirements:
  - OPS-04

must_haves:
  truths:
    - "Demo recording exists with all three beats visible"
    - "Beat 1: clean payroll email → operator approves → confirmation email + PDFs sent (coastal_exact fixture, routes to Coastal Cleaning Co.)"
    - "Beat 2: unknown shorthand 'Dave Reyes' → code gate clarifies → suggestion call names 'David Reyes' — recorded via the unknown_shorthand_metro fixture from _DEMO_FIXTURES (routes to Metro Deli Group); the per-fixture DB from_addr lookup (06-06 HIGH-1 fix) is confirmed working before recording"
    - "Beat 3: operator approves resolved run → alias learned → second take resolves without clarification"
    - "Eval closing shot shows false_process_count=0 in the per-category chart"
    - "Recording is 60-90 seconds after editing (dead-air cuts allowed per D-05)"
    - "README.md links to the recording embed or URL"
    - "Eval chart served as static asset at /eval/chart.svg on the deployed Render service (D-21 confirmed)"
    - "Pre-recording routing smoke test PASSED for EACH beat: coastal_exact routes to Coastal Cleaning Co. AND unknown_shorthand_metro routes to Metro Deli Group — both confirmed before starting the recording session"
    - "The demo recording was captured via the /demo/send-test button path using unknown_shorthand_metro for Beat 2 — the per-fixture DB from_addr lookup resolved to Metro Deli Group's current contact_email so find_business_by_sender resolved correctly (HIGH-1 demo-routing confirmed working)"
  artifacts:
    - path: "README.md"
      provides: "Updated with real demo recording link/embed + confirmed Render URL"
      contains: "demo"
  key_links:
    - from: "Demo recording"
      to: "/demo/send-test endpoint (DASH-05 button)"
      via: "All three beats recorded via the controllable /demo/send-test button path (D-04); unknown_shorthand_metro fixture used for Beat 2 (HIGH-1 per-fixture routing confirmed in 06-06)"
      pattern: "unknown_shorthand_metro|send-test"
---

<objective>
Wave 6 — Final BLOCKING human checkpoint (autonomous:false). The demo recording (D-04..D-06) and the README update linking to it. This is the last deliverable for Phase 6.

Purpose: The demo recording is the HERO ARTIFACT for the recruiter audience (D-04). It is recorded via the DASH-05 /demo/send-test button (the controllable path — not live inbound webhook) so latency and provider hiccups don't appear mid-take. The D-09b verify gate proved transport works once; the recording proves the thesis beats (deterministic gate, suggestion call, learned alias) in a clean 60-90s clip.

HIGH-1 confirmation (this plan depends on): 06-06 Task 1 added business_name to every _DEMO_FIXTURES entry and replaced the global DEMO_CONTACT_EMAIL override with per-fixture DB lookups (SELECT contact_email FROM businesses WHERE name = %s). BEFORE recording, the pre-recording smoke test verifies EACH beat's fixture routes to its intended business — not just the hero. Beat 1 (coastal_exact → Coastal Cleaning Co.) and Beat 2 (unknown_shorthand_metro → Metro Deli Group) must both be verified independently. If either misroutes, stop, fix, and re-test before recording.

Beat-2 fixture name: The fixture key is unknown_shorthand_metro (path: eval/fixtures/04_unknown_shorthand_metro.json). The label is "Metro Deli — unknown shorthand 'Dave Reyes' (clarify + suggest)". There is no gate_block_hero key.

D-07 is the prerequisite — run `uv run python scripts/demo_reset.py --confirm` (full destructive reset) before starting to ensure Beat 2 clarifies on the first take and Beat 3's learned alias does not survive from a prior run. Use `--reset-aliases` instead if only the learned aliases need re-arming (non-destructive, preserves run history). These are the real flags that 06-06 builds.

Output: A completed, committed recording (or public video link) and an updated README with the embed. Phase 6 is done.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/README.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-04..D-07 — Verify HIGH-1 multi-business routing + Record the demo + update README</name>
  <what-built>scripts/demo_reset.py is ready (06-06), the deployed Render service with Resend is confirmed (06-05), every _DEMO_FIXTURES entry has a business_name and the demo_send_test handler resolves from_addr from THAT business's current DB contact_email — not a single global env var (06-06 Task 1 — HIGH-1 per-fixture routing fix). The A5 gate was resolved in 06-05 (either PASSED or D-03a subject-token fallback complete). The DASH-05 /demo/send-test button drives all three thesis beats. The README is the recruiter-facing entry point awaiting the recording link.</what-built>
  <how-to-verify>
    PRE-RECORDING CHECKLIST (do these before hitting record):

    1. Confirm DEMO_CONTACT_EMAIL and DEMO_BUSINESS_NAME are set in the Render environment (set in 06-05 for demo_reset.py use — these are NOT used as global from_addr overrides at send time; the handler reads each fixture's contact_email from the DB).

    2. HIGH-1 multi-business routing smoke test — verify EACH beat before recording:

       BEAT 1 ROUTING CHECK (coastal_exact → Coastal Cleaning Co.):
       Visit https://{render-url}/demo/send-test (or click the button in the dashboard).
       Select "coastal_exact" fixture (label: "Coastal Cleaning Co. — exact match").
       Click send. Confirm:
       (a) The runs list shows a NEW run was created.
       (b) The new run shows "Coastal Cleaning Co." as the business — NOT Metro Deli or an error.
       If (b) fails: the per-fixture DB lookup for coastal_exact is returning the wrong address. STOP. Check that the businesses table has Coastal Cleaning Co. with its seeded contact_email (payroll@coastalcleaning.example). Run demo_reset to restore seeds. Fix and retry before recording.

       BEAT 2 ROUTING CHECK (unknown_shorthand_metro → Metro Deli Group):
       Select "unknown_shorthand_metro" fixture (label: "Metro Deli — unknown shorthand 'Dave Reyes' (clarify + suggest)").
       Click send. Confirm:
       (a) The runs list shows a NEW run was created (not an empty redirect with no new run — the unknown_sender path).
       (b) The new run shows "Metro Deli Group" as the business.
       If (a) or (b) fails: STOP. Check that Metro Deli Group's contact_email in the DB matches the real sender address set in 06-05 (DEMO_CONTACT_EMAIL). Fix and retry before recording.

       Both beats must pass before proceeding. This proves the HIGH-1 per-fixture routing fix works for the full multi-business recording set, not just one fixture.

    3. Pre-warm the Render service (D-05): curl https://{render-url}/ 30-60s before recording to wake it from sleep.

    4. Reset the demo state (D-07): Run `uv run python scripts/demo_reset.py --confirm` (full destructive reset — clears all runs and emails, then re-seeds known_aliases). Confirm by visiting the Render dashboard — runs list should be empty. If only the learned aliases need re-arming without clearing run history, use `uv run python scripts/demo_reset.py --reset-aliases` instead.

    5. Confirm the controllable path works: do a quick test click on coastal_exact and confirm the run appears. Then do a fresh demo_reset before recording.

    RECORDING BEATS (D-06 — all three + eval closing shot):

    BEAT 1 (clean → approve → deliver):
    - Visit the Render runs list (the dashboard)
    - Click "Send test email" → select the coastal_exact fixture (clean happy path)
    - Show the run reaching awaiting_approval
    - Click Approve
    - Show the run reaching reconciled; mention confirmation email + PDFs sent

    BEAT 2 (unknown shorthand → clarify + suggestion):
    - Run demo_reset --reset-aliases to re-arm Beat 2 if needed
    - Click "Send test email" → select the unknown_shorthand_metro fixture ("Metro Deli — unknown shorthand 'Dave Reyes' (clarify + suggest)")
    - Show the run gate-blocked → awaiting_reply (or needs_clarification)
    - Show the clarification email subject/body — "Did you mean David Reyes?"
    - Narrate: "The code gate caught this — the model never decided, it just read the email. The suggestion call named the specific employee."

    BEAT 3 (approve resolved → alias learned → second take no clarification):
    - Reply to the clarification email (or use the simulate-reply button) with David Reyes' hours
    - Show the run resume, reach awaiting_approval with resolved decision
    - Approve → show reconciled
    - Trigger the unknown_shorthand_metro fixture AGAIN without resetting
    - Show it resolves directly (no clarification) — "It learned. Won't ask again."

    CLOSING SHOT (~5-10s):
    - Navigate to the eval view
    - Show the per-category chart: extraction accuracy, reconciliation accuracy, decision accuracy
    - Highlight false_process_count=0

    POST-RECORDING:
    - Edit out dead-air (spinning indicators, network waits) per D-05. Keep cuts away from network boundaries (D-05 caution).
    - Target: 60-90 seconds final cut.
    - Upload to YouTube unlisted, Google Drive, Loom, or a hosted GIF (whichever the recruiter audience can view without an account).

    README UPDATE:
    - Update README.md to replace the demo recording placeholder with the real embed or link.
    - Update the live Render URL if it was a placeholder.
    - Confirm https://{render-url}/eval/chart.svg returns the committed chart (D-21 final check):
      `curl -s -o /dev/null -w "%{http_code}" https://{render-url}/eval/chart.svg`
      Expected: 200.

    FINAL VALIDATION:
    - `uv run pytest -q` (full suite including integration) → all green
    - `curl -s https://{render-url}/health/live` → `{"status":"ok"}`
    - `curl -s https://{render-url}/health/ready` → `{"status":"ready"}`
    - `curl -s -o /dev/null -w "%{http_code}" https://{render-url}/eval/chart.svg` → 200
    - GitHub Actions → keepalive.yml: trigger manually (workflow_dispatch) and confirm it runs without error
  </how-to-verify>
  <resume-signal>Type "demo-recorded: {recording-url}" — e.g., "demo-recorded: https://youtu.be/xyz123". Include confirmation that BOTH routing smoke tests passed (coastal_exact routed to Coastal AND unknown_shorthand_metro routed to Metro Deli). If anything failed, describe what and whether it blocks Phase 6 sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo recording screen | Must not show API keys, DATABASE_URL, or signing secret on camera (D-09 reminder) |
| Public recording URL | Not sensitive; listed in README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information Disclosure | Recording screen capture exposes secrets | mitigate | D-09 explicit instruction: env vars live in Render dashboard only; recording uses the app UI (no env tabs on camera); if Render dashboard appears, close the Environment tab first |
| T-06-07-02 | Repudiation | Demo doesn't show false_process_count=0 | mitigate | Closing shot explicitly on the eval view showing the metric (D-06); eval chart is a baked-in static asset (D-21) |
| T-06-07-03 | Integrity | Beat 1 or Beat 2 misroutes — coastal_exact routes to Metro or unknown_shorthand_metro routes to unknown_sender (HIGH-1 regression) | mitigate | Pre-recording HIGH-1 multi-business smoke test (Step 2 in how-to-verify) explicitly checks BOTH beats BEFORE recording begins; STOP condition defined for each; per-fixture DB from_addr lookup (06-06 HIGH-1 fix) isolates each fixture's routing to its own business |
| T-06-07-SC | Tampering | slopcheck + blocking human | accept | No new packages in this plan; only README update and recording |
</threat_model>

<verification>
Human confirms:
1. HIGH-1 multi-business smoke tests BOTH PASSED before recording: coastal_exact routed to Coastal Cleaning Co. AND unknown_shorthand_metro routed to Metro Deli Group — confirmed before recording started
2. "demo-recorded: {url}" — the recording exists and is publicly accessible
3. All three beats visible in the recording (Beat 2 uses unknown_shorthand_metro fixture — "Dave Reyes" unknown shorthand)
4. Eval closing shot shows false_process_count=0
5. README updated with the real recording link
6. /eval/chart.svg returns 200 on the deployed service (D-21 confirmed)
7. Full pytest suite green: `uv run pytest -q`
8. keepalive.yml triggers successfully via workflow_dispatch
</verification>

<success_criteria>
- HIGH-1 multi-business smoke test: BOTH coastal_exact (Coastal) AND unknown_shorthand_metro (Metro Deli) route to their correct businesses via /demo/send-test BEFORE recording — 06-06 Task 1 per-fixture routing fix confirmed in production
- 60-90s demo recording exists and is linked from the README
- All three beats (clean→approve, clarify+suggestion, learned→no-clarify) are visible; Beat 2 uses unknown_shorthand_metro (eval/fixtures/04_unknown_shorthand_metro.json — "Dave Reyes")
- Phase 6 OPS-04 requirement satisfied: README + disclaimer + diagram + demo recording all exist
- The free-stack deployment (Render + Supabase + GitHub Actions) is confirmed end-to-end
- Phase 6 is COMPLETE
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-07-SUMMARY.md` when done.
Then update ROADMAP.md Phase 6 status to "Complete" and record the Render URL, recording URL, and completion date.
</output>
