---
phase: "06"
plan: "07"
type: execute
wave: 6
depends_on:
  - "06-06"
autonomous: false
files_modified:
  - README.md
requirements:
  - OPS-04

must_haves:
  truths:
    - "Demo recording exists with all three beats visible"
    - "Beat 1: clean payroll email → operator approves → confirmation email + PDFs sent"
    - "Beat 2: unknown shorthand 'David Reyez' → code gate clarifies → suggestion call names 'David Reyes' — recorded via the gate_block_hero fixture from _DEMO_FIXTURES (HIGH-2 confirmed: 06-06 Task 1 added gate_block_hero and the from_addr override)"
    - "Beat 3: operator approves resolved run → alias learned → second take resolves without clarification"
    - "Eval closing shot shows false_process_count=0 in the per-category chart"
    - "Recording is 60-90 seconds after editing (dead-air cuts allowed per D-05)"
    - "README.md links to the recording embed or URL"
    - "Eval chart served as static asset at /eval/chart.svg on the deployed Render service (D-21 confirmed)"
    - "The demo recording was captured via the /demo/send-test button path using gate_block_hero for Beat 2 — the from_addr was overridden by DEMO_CONTACT_EMAIL so find_business_by_sender resolved correctly (HIGH-2 demo-path confirmed working before recording)"
  artifacts:
    - path: "README.md"
      provides: "Updated with real demo recording link/embed + confirmed Render URL"
      contains: "demo"
  key_links:
    - from: "Demo recording"
      to: "/demo/send-test endpoint (DASH-05 button)"
      via: "All three beats recorded via the controllable /demo/send-test button path (D-04); gate_block_hero fixture used for Beat 2 (HIGH-2 confirmed in 06-06)"
      pattern: "gate_block_hero|send-test"
---

<objective>
Wave 6 — Final BLOCKING human checkpoint (autonomous:false). The demo recording (D-04..D-06) and the README update linking to it. This is the last deliverable for Phase 6.

Purpose: The demo recording is the HERO ARTIFACT for the recruiter audience (D-04). It is recorded via the DASH-05 /demo/send-test button (the controllable path — not live inbound webhook) so latency and provider hiccups don't appear mid-take. The D-09b verify gate proved transport works once; the recording proves the thesis beats (deterministic gate, suggestion call, learned alias) in a clean 60-90s clip.

HIGH-2 confirmation (this plan depends on): 06-06 Task 1 added gate_block_hero to _DEMO_FIXTURES and added the DEMO_CONTACT_EMAIL from_addr override to the demo_send_test handler. BEFORE recording, verify that the /demo/send-test button with gate_block_hero fixture resolves to the seeded business (not unknown_sender) by doing a test click BEFORE starting the recording session. If it redirects to the runs list with a new run created → the HIGH-2 fix is confirmed working. If it redirects to the runs list but creates no run (unknown_sender path) → STOP, fix the DEMO_CONTACT_EMAIL env var, and re-test before recording.

D-07 is the prerequisite — run `uv run python scripts/demo_reset.py --confirm` (full destructive reset) before starting to ensure Beat 2 clarifies on the first take and Beat 3's learned alias does not survive from a prior run. Use `--reset-aliases` instead if only the learned aliases need re-arming (non-destructive, preserves run history). These are the real flags that 06-06 builds.

Output: A completed, committed recording (or public video link) and an updated README with the embed. Phase 6 is done.
</objective>

<execution_context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/phases/06-real-integration-ship/06-CONTEXT.md
</execution_context>

<context>
@/Users/pnhek/usf msds/github/payroll_agent/.planning/ROADMAP.md
@/Users/pnhek/usf msds/github/payroll_agent/README.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: D-04..D-07 — Verify HIGH-2 demo path + Record the demo + update README</name>
  <what-built>scripts/demo_reset.py is ready (06-06), the deployed Render service with Resend is confirmed (06-05), gate_block_hero is in _DEMO_FIXTURES and the demo_send_test handler overrides from_addr with DEMO_CONTACT_EMAIL (06-06 Task 1 — HIGH-2 demo-path fix). The A5 gate was resolved in 06-05 (either PASSED or fallback complete). The DASH-05 /demo/send-test button drives all three thesis beats. The README is the recruiter-facing entry point awaiting the recording link.</what-built>
  <how-to-verify>
    PRE-RECORDING CHECKLIST (do these before hitting record):

    1. Set DEMO_CONTACT_EMAIL env var locally (and confirm it is set in Render environment): this must match the real sender address set in 06-05 and the business contact_email in the DB. Without it, the gate_block_hero from_addr override falls back to the .example address and Beat 2 hits unknown_sender.

    2. HIGH-2 demo-path smoke test (do this before recording):
       Visit https://{render-url}/demo/send-test (or click the button in the dashboard).
       Select "gate_block_hero" fixture from the picker (label: "Metro Deli — unknown shorthand 'David Reyez'").
       Click send. Confirm:
       (a) The runs list shows a NEW run was created (not an empty redirect with no new run — the unknown_sender path).
       (b) The new run shows Metro Deli as the business, not an error.
       If (a) fails: STOP. Check that DEMO_CONTACT_EMAIL is set to the Metro Deli contact_email in the Render env. Fix and retry before recording.
       If (a) passes: the HIGH-2 fix is confirmed. Proceed to demo_reset + recording.

    3. Pre-warm the Render service (D-05): curl https://{render-url}/ 30-60s before recording to wake it from sleep.

    4. Reset the demo state (D-07): Run `uv run python scripts/demo_reset.py --confirm` (full destructive reset — clears all runs and emails, then re-seeds known_aliases). Confirm by visiting the Render dashboard — runs list should be empty. If only the learned aliases need re-arming without clearing run history, use `uv run python scripts/demo_reset.py --reset-aliases` instead.

    5. Confirm the controllable path works: visit https://{render-url}/demo/send-test, click "Send test email" for the coast_exact fixture (Beat 1) and confirm the run appears. Then do a fresh demo_reset before recording.

    RECORDING BEATS (D-06 — all three + eval closing shot):

    BEAT 1 (clean → approve → deliver):
    - Visit the Render runs list (the dashboard)
    - Click "Send test email" → select the coastal_exact fixture (clean happy path)
    - Show the run reaching awaiting_approval
    - Click Approve
    - Show the run reaching reconciled; mention confirmation email + PDFs sent

    BEAT 2 (unknown shorthand → clarify + suggestion):
    - Run demo_reset --reset-aliases to re-arm Beat 2 if needed
    - Click "Send test email" → select the gate_block_hero fixture ("Metro Deli — unknown shorthand 'David Reyez'")
    - Show the run gate-blocked → awaiting_reply (or needs_clarification)
    - Show the clarification email subject/body — "Did you mean David Reyes?"
    - Narrate: "The code gate caught this — the model never decided, it just read the email. The suggestion call named the specific employee."

    BEAT 3 (approve resolved → alias learned → second take no clarification):
    - Reply to the clarification email (or use the simulate-reply button) with David Reyes' hours
    - Show the run resume, reach awaiting_approval with resolved decision
    - Approve → show reconciled
    - Trigger the gate_block_hero fixture AGAIN without resetting
    - Show it resolves directly (no clarification) — "It learned. Won't ask again."

    CLOSING SHOT (~5-10s):
    - Navigate to the eval view
    - Show the per-category chart: extraction accuracy, reconciliation accuracy, decision accuracy
    - Highlight false_process_count=0

    POST-RECORDING:
    - Edit out dead-air (spinning indicators, network waits) per D-05. Keep cuts away from network boundaries (D-05 caution).
    - Target: 60-90 seconds final cut.
    - Upload to YouTube unlisted, Google Drive, Loom, or a hosted GIF (whichever the recruiter audience can view without an account).

    README UPDATE:
    - Update README.md to replace the demo recording placeholder with the real embed or link.
    - Update the live Render URL if it was a placeholder.
    - Confirm https://{render-url}/eval/chart.svg returns the committed chart (D-21 final check):
      `curl -s -o /dev/null -w "%{http_code}" https://{render-url}/eval/chart.svg`
      Expected: 200.

    FINAL VALIDATION:
    - `uv run pytest -q` (full suite including integration) → all green
    - `curl -s https://{render-url}/health/live` → `{"status":"ok"}`
    - `curl -s https://{render-url}/health/ready` → `{"status":"ready"}`
    - `curl -s -o /dev/null -w "%{http_code}" https://{render-url}/eval/chart.svg` → 200
    - GitHub Actions → keepalive.yml: trigger manually (workflow_dispatch) and confirm it runs without error
  </how-to-verify>
  <resume-signal>Type "demo-recorded: {recording-url}" — e.g., "demo-recorded: https://youtu.be/xyz123". Include confirmation that the HIGH-2 smoke test passed (gate_block_hero routed to seeded business, not unknown_sender). If anything failed, describe what and whether it blocks Phase 6 sign-off.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo recording screen | Must not show API keys, DATABASE_URL, or signing secret on camera (D-09 reminder) |
| Public recording URL | Not sensitive; listed in README |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Information Disclosure | Recording screen capture exposes secrets | mitigate | D-09 explicit instruction: env vars live in Render dashboard only; recording uses the app UI (no env tabs on camera); if Render dashboard appears, close the Environment tab first |
| T-06-07-02 | Repudiation | Demo doesn't show false_process_count=0 | mitigate | Closing shot explicitly on the eval view showing the metric (D-06); eval chart is a baked-in static asset (D-21) |
| T-06-07-03 | Integrity | Beat 2 fails because gate_block_hero routes to unknown_sender (HIGH-2 regression) | mitigate | Pre-recording HIGH-2 smoke test (Step 2 in how-to-verify) explicitly checks this BEFORE recording begins; STOP condition defined; DEMO_CONTACT_EMAIL env var must be set |
| T-06-07-SC | Tampering | slopcheck + blocking human | accept | No new packages in this plan; only README update and recording |
</threat_model>

<verification>
Human confirms:
1. HIGH-2 smoke test PASSED: gate_block_hero via /demo/send-test routed to the seeded business (Metro Deli), not unknown_sender — confirmed before recording started
2. "demo-recorded: {url}" — the recording exists and is publicly accessible
3. All three beats visible in the recording (Beat 2 uses gate_block_hero fixture)
4. Eval closing shot shows false_process_count=0
5. README updated with the real recording link
6. /eval/chart.svg returns 200 on the deployed service (D-21 confirmed)
7. Full pytest suite green: `uv run pytest -q`
8. keepalive.yml triggers successfully via workflow_dispatch
</verification>

<success_criteria>
- HIGH-2 smoke test: /demo/send-test with gate_block_hero routes to seeded business BEFORE recording (not unknown_sender) — 06-06 Task 1 fix confirmed in production
- 60-90s demo recording exists and is linked from the README
- All three beats (clean→approve, clarify+suggestion, learned→no-clarify) are visible; Beat 2 uses gate_block_hero
- Phase 6 OPS-04 requirement satisfied: README + disclaimer + diagram + demo recording all exist
- The free-stack deployment (Render + Supabase + GitHub Actions) is confirmed end-to-end
- Phase 6 is COMPLETE
</success_criteria>

<output>
Create `.planning/phases/06-real-integration-ship/06-07-SUMMARY.md` when done.
Then update ROADMAP.md Phase 6 status to "Complete" and record the Render URL, recording URL, and completion date.
</output>
