---
phase: 10-concurrency-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_concurrency_proof.py
  - .github/workflows/concurrency-proof.yml
autonomous: true
requirements: [OPS2-03]

must_haves:
  truths:
    - "A single capstone test module fires N=8 concurrent operations across three risk surfaces (dedup, HTTP approval race, concurrent distinct runs) against a real Postgres under real OS-thread parallelism (D-10-01, D-10-05, D-10-07)."
    - "Surface A asserts no duplicate run per message_id: N duplicate POSTs → exactly one run_id and exactly one _run_pipeline call (OPS2-03)."
    - "Surface B asserts no double-approval through the REAL HTTP /runs/{run_id}/approve route: N concurrent approvals → exactly one _deliver fired and status reaches 'approved' exactly once (D-10-06)."
    - "Surface C asserts no lost update AND no half-write: N distinct message_ids → exactly N runs, each with a non-null source_email_id and a matching email_messages row (OPS2-03)."
    - "The proof runs (not skipped) in GitHub Actions against an ephemeral postgres:16 service container on every push to master — green = standing evidence, red = caught regression (D-10-03)."
  artifacts:
    - path: "tests/test_concurrency_proof.py"
      provides: "The capstone proof: 3 integration tests, 4 invariants, proof-narrative docstrings (D-10-01, D-10-02)"
      contains: "def test_dedup_exactly_one_run_per_message_id"
      min_lines: 120
    - path: ".github/workflows/concurrency-proof.yml"
      provides: "CI job with services: postgres:16 + pg_isready health check + bootstrap --reset + pytest -m integration (D-10-03)"
      contains: "services:"
  key_links:
    - from: "tests/test_concurrency_proof.py"
      to: "app.pipeline.orchestrator._deliver"
      via: "monkeypatch on the orchestrator module (imported inside the approve route)"
      pattern: "app\\.pipeline\\.orchestrator\\._deliver"
    - from: "tests/test_concurrency_proof.py"
      to: "app.main._run_pipeline"
      via: "monkeypatch no-op capturing the winner's scheduled pipeline"
      pattern: "_run_pipeline"
    - from: ".github/workflows/concurrency-proof.yml"
      to: "tests/test_concurrency_proof.py"
      via: "uv run pytest tests/test_concurrency_proof.py -m integration"
      pattern: "test_concurrency_proof\\.py -m integration"
---

<objective>
Deliver the OPS2-03 concurrency proof capstone: ONE legible, hiring-manager-pointable test module (`tests/test_concurrency_proof.py`) that fires N=8 simultaneous operations across the three risk surfaces under real OS-thread parallelism against a real Postgres and asserts all four Phase-9 invariants hold — no duplicate run per `message_id`, no double-approval, no lost update, no half-written state — plus the CI job (`.github/workflows/concurrency-proof.yml`) that runs it against an ephemeral `postgres:16` on every push so the proof is standing evidence, not a skip-guarded local-only test.

Purpose: Phase 9 *established* the atomicity/dedup/recovery invariants; this phase *proves* them under genuine parallelism — the evidence behind the "production-grade" claim (OPS2-03, ROADMAP Phase 10 Success Criteria 1–2). Per D-10-01/D-10-02 the module IS the proof: each invariant is a clearly-named test with a proof-narrative docstring; no separate `PROOF.md`.

Output: two net-new files. Zero production-code changes (test-only phase — a genuine invariant violation this proof exposes would be a Phase-9 bug fix, contingency only, not planned scope).

This plan implements D-10-01 (unified harness), D-10-02 (test-as-proof), D-10-03 (CI ephemeral Postgres), D-10-04 (integration-marked, hermetic suite unaffected), D-10-05 (OS threads / real PG), D-10-06 (approval race via real HTTP route), D-10-07 (N=8, deterministic, pool-safe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-concurrency-proof/10-CONTEXT.md
@.planning/phases/10-concurrency-proof/10-RESEARCH.md
@.planning/phases/10-concurrency-proof/10-PATTERNS.md
@.planning/phases/10-concurrency-proof/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the capstone proof module tests/test_concurrency_proof.py (3 tests, 4 invariants)</name>
  <files>tests/test_concurrency_proof.py</files>
  <read_first>
    - tests/test_concurrency_proof.py (the file being CREATED)
    - tests/test_webhook_dedup_race.py (VERBATIM TEMPLATE — the whole thread+TestClient+real-PG+env pattern: module docstring style :1-18, inline DATABASE_URL skip :33-34, `get_settings.cache_clear()` + `monkeypatch.setenv("ALLOW_UNSIGNED_FIXTURES","true")` :36-43, `_run_pipeline` no-op stub :56-59, raw-thread harness :76-89, dedup assertions :91-108, cache-clear reset :110)
    - tests/test_atomic_persist.py (the `_seed_live_run` helper :120-133 to adapt for Surface B; shared seed IDs `COASTAL_BIZ_ID`/`COASTAL_EMAIL` :49-50; two-factor `_SKIP_LIVE_DB` shape :38-44)
    - tests/conftest.py (`_SKIP_LIVE_DB` two-factor guard :48-55; `seeded_db` module-scoped fixture :58-74 that resets+seeds Coastal roster; resend no-op pattern :~1005)
    - app/main.py (webhook enqueue `background_tasks.add_task(_run_pipeline, run_id)` :539; approve route :738 — `_deliver` imported INSIDE the route :753, `claim_status` CAS :755, synchronous `_deliver` :764, always-303 :783)
    - app/db/repo.py (`insert_inbound_email` ON CONFLICT (message_id) DO NOTHING :171/:193-201; `create_run` :324; `set_status` :433; `claim_status` CAS UPDATE...WHERE id AND status RETURNING :450/:471-473; `load_run` returns `dict|None` :363)
    - app/db/supabase.py (pool min=1/max=5, `prepare_threshold=None`, per-transaction connection lifecycle — the D-10-07 pool-budget constraint)
    - .planning/phases/10-concurrency-proof/10-PATTERNS.md (§ Pattern Assignments — the exact excerpts for the shared stub helper, seed helper, and per-surface assertions)
    - .planning/phases/10-concurrency-proof/10-VALIDATION.md (§ The Four Invariants → Concrete Asserted Conditions — the exact assertion expressions)
  </read_first>
  <action>
    Create `tests/test_concurrency_proof.py` — ONE `@pytest.mark.integration` module (D-10-01) consolidating the three per-surface race patterns into one capstone. Write the module docstring and each per-test docstring in the proof-narrative tone of `test_webhook_dedup_race.py:1-18` (D-10-02): state what is fired, what must hold, and the crash/race window each surface closes. Document the local invocation in the module docstring per D-10-04 (`DATABASE_URL=... ALLOW_DB_RESET=1 ALLOW_UNSIGNED_FIXTURES=true uv run pytest tests/test_concurrency_proof.py -m integration`).

    Module-level scaffolding (copy the proven shapes): the two-factor guard constants `_HAS_DB`/`_HAS_RESET` + `_SKIP_LIVE_DB` skipif (conftest.py:48-55 / test_atomic_persist.py:38-44); shared seed IDs `COASTAL_BIZ_ID = uuid.UUID("b0000001-0000-0000-0000-000000000001")` and `COASTAL_EMAIL = "payroll@coastalcleaning.example"` (test_atomic_persist.py:49-50). Every test carries `@pytest.mark.integration`, `@_SKIP_LIVE_DB`, and depends on the `seeded_db` fixture so the Coastal business/roster exists.

    Two shared helpers:
    (1) `_stub_pipeline_and_send(monkeypatch)` returning the capture lists — installs THREE no-ops: `monkeypatch.setattr(app_main, "_run_pipeline", lambda run_id: pipeline_calls.append(run_id))` (Surfaces A+C, main.py:539 seam); `monkeypatch.setattr("app.pipeline.orchestrator._deliver", lambda rid, run: deliver_calls.append(rid))` (Surface B — MUST patch on the `orchestrator` module because `_deliver` is imported INSIDE the route at main.py:753, NOT on app_main); and a `resend.Emails.send` staticmethod no-op returning `{"id": "fake-resend-id"}` as belt-and-suspenders (conftest.py:~1005 pattern). This isolation is load-bearing: `.env` carries LIVE LLM/Resend keys and TestClient runs BackgroundTasks + the sync `_deliver` inline — any unstubbed path flakes and burns credits.
    (2) `_seed_live_run(*, body, from_addr=COASTAL_EMAIL)` adapted from test_atomic_persist.py:120-133 — `repo.insert_inbound_email(...)` then `repo.create_run(business_id=COASTAL_BIZ_ID, source_email_id=eid)`, returning the run_id (used only by Surface B).

    Per-test env setup (copy test_webhook_dedup_race.py:36-43,110): `get_settings.cache_clear()` → `monkeypatch.setenv("ALLOW_UNSIGNED_FIXTURES", "true")` → build a fresh `TestClient(app_main.app)` → call `_stub_pipeline_and_send(monkeypatch)` BEFORE firing threads → `get_settings.cache_clear()` at the end. This module does NOT inherit any other module's env (RESEARCH Pitfall 3).

    Thread harness: N=8 (D-10-07 — genuinely interleaves, stays inside the min=1/max=5 pool budget because pooled connections are held only for the sub-millisecond ingest/CAS transaction, not the thread lifetime; do NOT open per-thread raw `psycopg.connect` — drive everything through the app pool via TestClient; do NOT exceed ~10). Use `concurrent.futures.ThreadPoolExecutor(max_workers=8)` + `list(ex.map(...))`, OR the raw-`threading.Thread` + `threading.Lock`-guarded results list from the template (test_webhook_dedup_race.py:76-89) — either is fine per D-10-05.

    Surface A — `test_dedup_exactly_one_run_per_message_id` (invariant: no duplicate run): fire N=8 duplicate POSTs to `/webhook/inbound` with ONE shared `message_id` (payload shape at test_webhook_dedup_race.py:63-74). Assert `len({r.get("run_id") for r in results if r.get("run_id")}) == 1` AND `{r.get("status") for r in results} <= {"accepted","duplicate"}` AND `len(pipeline_calls) == 1`. Optionally add the direct DB check `count(payroll_runs WHERE source_email_id=(SELECT id FROM email_messages WHERE message_id=%s)) == 1`.

    Surface B — `test_concurrent_approvals_exactly_one_wins` (invariant: no double-approval, D-10-06): seed ONE run via `_seed_live_run(...)`, then `repo.set_status(run_id, RunStatus.AWAITING_APPROVAL)` to reach the CAS `expected` status. Fire N=8 concurrent `client.post(f"/runs/{run_id}/approve")`. Do NOT assert on HTTP status — the route ALWAYS 303s regardless of claim outcome (main.py:783, RESEARCH Pitfall 5). Assert the winning side effect: `len(deliver_calls) == 1` (exactly one `_deliver` fired above the CAS) AND `repo.load_run(run_id)["status"] == "approved"`. This is the D-10-06 upgrade over test_claim_status.py's CAS-primitive stub — it catches route-level regressions above the CAS.

    Surface C — `test_concurrent_distinct_runs_no_lost_update` (invariants: no lost update + no half-write): fire N=8 webhook POSTs each with a UNIQUE `message_id` AND unique `id` (distinct parallel ingests — throughput/atomicity-under-load, resolving the D-10 discretion point per RESEARCH Mechanic 4). Collect each response's `run_id`. Assert no lost update — exactly N=8 distinct runs created (`count(payroll_runs WHERE source_email_id IN (<the N email ids>)) == 8`, every distinct ingest produced exactly one run, none dropped) AND `len(pipeline_calls) == 8`. Assert no half-write — every run row has a non-null `source_email_id` with a matching `email_messages` row (ingest-txn atomicity, D-9-09); no orphaned/partial rows. Do NOT duplicate the fault-injection half-write machinery — that proof stays in test_atomic_persist.py:170 per D-10-01; the capstone asserts the lighter "no orphaned/partial rows under concurrent distinct ingests" (with `_run_pipeline` stubbed, runs stay at `received`).

    Do NOT delete or modify test_webhook_dedup_race.py / test_claim_status.py / test_atomic_persist.py — they STAY as focused units (D-10-01). Backfilling the test_claim_status.py CAS-primitive stub is OPTIONAL (D-10 discretion) — skip it unless trivial; the capstone HTTP route is the required home for the approval invariant.
  </action>
  <acceptance_criteria>
    - `tests/test_concurrency_proof.py` exists with exactly three `@pytest.mark.integration` tests named `test_dedup_exactly_one_run_per_message_id`, `test_concurrent_approvals_exactly_one_wins`, `test_concurrent_distinct_runs_no_lost_update`.
    - Each test is guarded by `@_SKIP_LIVE_DB` (two-factor: DATABASE_URL + ALLOW_DB_RESET=1) and depends on `seeded_db`; N=8 per surface.
    - The shared stub helper patches `app.main._run_pipeline`, `app.pipeline.orchestrator._deliver`, and `resend.Emails.send` (the `_deliver` patch is on the `orchestrator` module, NOT app_main).
    - Surface A asserts `len({run_ids}) == 1` AND `len(pipeline_calls) == 1`; Surface B asserts `len(deliver_calls) == 1` AND `load_run(run_id)["status"] == "approved"` (NOT on HTTP status); Surface C asserts `count(runs) == 8` AND each run has a non-null `source_email_id` + matching email_messages row.
    - `uv run pytest -m 'not integration'` stays green and DB-free (the capstone is EXCLUDED from the default hermetic suite — D-10-04). Command: `uv run pytest -m 'not integration' -q`.
    - Grep gate: `grep -v '^#' tests/test_concurrency_proof.py | grep -c 'app\.pipeline\.orchestrator\._deliver'` >= 1 (the load-bearing orchestrator-module patch is present).
    - No production-code file under `app/` is modified (test-only phase). `git diff --name-only app/` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -m 'not integration' -q && test -f tests/test_concurrency_proof.py && grep -q "def test_dedup_exactly_one_run_per_message_id" tests/test_concurrency_proof.py && grep -q "def test_concurrent_approvals_exactly_one_wins" tests/test_concurrency_proof.py && grep -q "def test_concurrent_distinct_runs_no_lost_update" tests/test_concurrency_proof.py && grep -q "app.pipeline.orchestrator._deliver" tests/test_concurrency_proof.py && [ -z "$(git diff --name-only app/)" ]</automated>
  </verify>
  <done>The capstone module exists with three integration tests covering all four invariants using the proven thread+TestClient+real-PG harness and wholesale stubbing; the default hermetic suite (`-m 'not integration'`) stays green and DB-free; no app/ production code changed.</done>
</task>

<task type="auto">
  <name>Task 2: Stand up .github/workflows/concurrency-proof.yml (ephemeral postgres:16, runs the proof on every push)</name>
  <files>.github/workflows/concurrency-proof.yml</files>
  <read_first>
    - .github/workflows/concurrency-proof.yml (the file being CREATED)
    - .github/workflows/eval.yml (HOUSE CI SHAPE to copy verbatim — `on: push: branches:["master"]` + `workflow_dispatch` :3-6; the step sequence checkout@v4 → setup-uv@v5 with python-version:"3.12" → `uv sync` :19-26; the `env:` block convention :30-31)
    - .planning/phases/10-concurrency-proof/10-RESEARCH.md (§ Mechanic 1 — the ready-to-adapt full CI sketch :88-136; the required-env table :78-84; the pooler flag note :69-74)
    - .planning/phases/10-concurrency-proof/10-PATTERNS.md (§ concurrency-proof.yml — the NET-NEW services block :189-205, job env block :207-213, bootstrap+run steps :215-221, security note :224)
    - app/db/bootstrap.py (schema apply via plain `psycopg.connect(db_url, prepare_threshold=None)` :96 — NOT the app pool, vanilla-Postgres-clean; entrypoint `python -m app.db.bootstrap [--reset]` :138)
    - app/db/schema.sql (verified vanilla-Postgres-clean: standard DDL, no Supabase extensions/roles, no gen_random_uuid — runs fine on postgres:16)
  </read_first>
  <action>
    Create `.github/workflows/concurrency-proof.yml` — one job named "Concurrency invariants proof (real Postgres)" that mirrors `eval.yml`'s `check` job (checkout@v4 → astral-sh/setup-uv@v5 with `python-version: "3.12"` → `uv sync`) and ADDS a net-new `services: postgres` container so the proof actually runs (D-10-03). Copy the ready-to-adapt sketch from 10-RESEARCH.md:88-136.

    `on:` trigger (copy eval.yml:3-6): `push: branches: ["master"]` + `workflow_dispatch:`.

    `services:` block (10-PATTERNS.md:189-205): `postgres` with `image: postgres:16`, `env:` POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB all `postgres`, `ports: - 5432:5432`, and the canonical health check `options: >- --health-cmd "pg_isready -U postgres" --health-interval 10s --health-timeout 5s --health-retries 5` so no step runs until Postgres accepts connections.

    Job-level `env:` block (10-PATTERNS.md:207-213, RESEARCH Pitfall 4) — a PLAIN LOCAL Postgres URL, NEVER a Supavisor pooler URL and NEVER the real Supabase DATABASE_URL: `DATABASE_URL: "postgresql://postgres:postgres@localhost:5432/postgres"`, `ALLOW_DB_RESET: "1"`, `ALLOW_UNSIGNED_FIXTURES: "true"`. (`prepare_threshold=None` in app/db/supabase.py is a harmless no-op against a vanilla server.)

    Steps, in order: (1) `actions/checkout@v4`; (2) `astral-sh/setup-uv@v5` with `python-version: "3.12"`; (3) `run: uv sync` (all groups — pytest is in the dev group); (4) "Apply schema to ephemeral Postgres" — `run: uv run python -m app.db.bootstrap --reset` (bootstrap.py:96/:138, destructive `--reset` is safe because it only ever touches the ephemeral container); (5) "Run the concurrency proof" — `run: uv run pytest tests/test_concurrency_proof.py -m integration -v` (the `-m integration` selector ensures the proof is NOT skipped — green = standing evidence, red = caught regression).

    Security (RESEARCH § Security, security_enforcement gate): `POSTGRES_PASSWORD: postgres` is an ephemeral network-isolated CI container — NOT a secret, so NO `secrets.*` reference (unlike eval.yml's `EXTRACTION_API_KEY`). NEVER point this job at the real Supabase `DATABASE_URL` — `bootstrap --reset` is destructive and must only touch the container. `ALLOW_UNSIGNED_FIXTURES=true` / `ALLOW_DB_RESET=1` are test/CI-only two-factor guards; their presence here is correct and does not leak into any deployed environment (Render sets neither).
  </action>
  <acceptance_criteria>
    - `.github/workflows/concurrency-proof.yml` exists and is valid YAML (parses).
    - Contains a `services:` block with `image: postgres:16` and a `pg_isready` health-cmd.
    - Job env sets `DATABASE_URL` to a plain `postgresql://postgres:postgres@localhost:5432/postgres` URL (NOT a `pooler.supabase.com` / `:6543` URL), plus `ALLOW_DB_RESET: "1"` and `ALLOW_UNSIGNED_FIXTURES: "true"`.
    - Includes a `uv run python -m app.db.bootstrap --reset` step and a `uv run pytest tests/test_concurrency_proof.py -m integration` step.
    - Copies the eval.yml step shape: `actions/checkout@v4`, `astral-sh/setup-uv@v5` with `python-version: "3.12"`, `uv sync`.
    - No `secrets.*` reference and no `pooler.supabase.com` string anywhere in the file.
    - Grep gates (comment-filtered): `grep -v '^\s*#' .github/workflows/concurrency-proof.yml | grep -c 'postgres:16'` >= 1; `grep -c 'pooler.supabase.com' .github/workflows/concurrency-proof.yml` == 0; `grep -c 'secrets\.' .github/workflows/concurrency-proof.yml` == 0.
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/concurrency-proof.yml'))" && grep -q "image: postgres:16" .github/workflows/concurrency-proof.yml && grep -q "pg_isready" .github/workflows/concurrency-proof.yml && grep -q "app.db.bootstrap --reset" .github/workflows/concurrency-proof.yml && grep -q "tests/test_concurrency_proof.py -m integration" .github/workflows/concurrency-proof.yml && grep -q "localhost:5432" .github/workflows/concurrency-proof.yml && [ "$(grep -c 'pooler.supabase.com' .github/workflows/concurrency-proof.yml)" -eq 0 ] && [ "$(grep -c 'secrets\.' .github/workflows/concurrency-proof.yml)" -eq 0 ]</automated>
  </verify>
  <done>The CI workflow exists, is valid YAML, spins up an ephemeral postgres:16 with a pg_isready health check, applies the schema via `bootstrap --reset` against a plain local (non-pooler, non-Supabase) DATABASE_URL, and runs the capstone with `-m integration` on every push to master — making the proof standing evidence (D-10-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is a **test-only phase** (D-10-01, RESEARCH § Security Domain). It introduces NO new production endpoint, input, auth, or crypto surface — the proof exercises EXISTING Phase-9 seams (`insert_inbound_email` ON CONFLICT, `claim_status` CAS, the ingest transaction). The only new trust boundary is the CI runner ↔ ephemeral Postgres container, which is network-isolated within the GitHub Actions job.

| Boundary | Description |
|----------|-------------|
| CI runner → ephemeral postgres:16 container | Ephemeral, network-isolated, torn down after the job. `POSTGRES_PASSWORD: postgres` is not a secret. |
| (No new production boundary) | The approve/webhook routes' existing access controls (signature verification gated by `ALLOW_UNSIGNED_FIXTURES`, FIX-5 sender revalidation) are unchanged; the proof sets `ALLOW_UNSIGNED_FIXTURES=true` in the TEST/CI context ONLY. |

## STRIDE Threat Register

Per RESEARCH § Security Domain, every applicable ASVS category (V2 Auth, V3 Session, V4 Access Control, V5 Input Validation, V6 Crypto) is "no" for this test-only phase. Block-on: high — there are NO high threats here. The two threats below are CI-hygiene controls, both mitigated by the workflow's construction.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering / Destructive-op | `bootstrap --reset` in CI | mitigate | CI `DATABASE_URL` MUST be the plain local container URL (`postgresql://postgres:postgres@localhost:5432/postgres`), NEVER a `pooler.supabase.com:6543` / real-Supabase URL — `--reset` is destructive and must only touch the ephemeral container. Enforced by the Task 2 grep gate `count(pooler.supabase.com) == 0` and the explicit local URL. |
| T-10-02 | Elevation / Guard-leak | `ALLOW_UNSIGNED_FIXTURES=true` + `ALLOW_DB_RESET=1` | accept (test/CI-only) | These are test/CI-only two-factor guards; their presence in this workflow is correct and does NOT leak into any deployed environment (Render sets neither). No production code sets them. Accepted: they are scoped to the CI job env and the test process only. |
| T-10-03 | Information disclosure | CI env / secrets | mitigate | No `secrets.*` reference is needed or present (unlike eval.yml's `EXTRACTION_API_KEY`); the container password is ephemeral and non-secret. Enforced by the Task 2 grep gate `count(secrets.) == 0`. |
| T-10-SC | Tampering | npm/pip/cargo installs | n/a | No new package-manager installs — the phase adds zero dependencies (pytest, uv, actions/checkout@v4, astral-sh/setup-uv@v5 all already present in eval.yml/pyproject.toml). No package legitimacy checkpoint required. |
</threat_model>

<verification>
Phase-level checks:
- `uv run pytest -m 'not integration' -q` — the default hermetic suite stays green and DB-free (the capstone is excluded, D-10-04). No regression from adding the module.
- CI: the `concurrency-proof.yml` job runs `uv run pytest tests/test_concurrency_proof.py -m integration -v` against the ephemeral `postgres:16` — green in GitHub Actions on push to master = the four invariants hold under real parallelism (standing evidence, D-10-03). This is the phase's authoritative verification and cannot be exercised locally without a Postgres; local reproduction is documented in the module docstring per D-10-04.
- Both files present; no `app/` production-code changes (`git diff --name-only app/` empty).
</verification>

<success_criteria>
- `tests/test_concurrency_proof.py` fires N=8 simultaneous operations across the three risk surfaces (concurrent distinct runs, duplicate webhooks for one `message_id`, simultaneous approvals on one run) under real OS-thread parallelism against a real Postgres (ROADMAP SC-1).
- All four invariants are asserted and fail loudly if violated — no duplicate run per `message_id` (Surface A), no double-approval via the real HTTP `/approve` route (Surface B, D-10-06), no lost update and no half-written state (Surface C) — a genuine regression guard, not a smoke test (ROADMAP SC-2, OPS2-03).
- The proof runs (not skipped) in `.github/workflows/concurrency-proof.yml` against an ephemeral `postgres:16` on every push to master (D-10-03); the default `uv run pytest -m 'not integration'` suite is unaffected (D-10-04).
- Zero production-code changes.
</success_criteria>

<output>
Create `.planning/phases/10-concurrency-proof/10-01-SUMMARY.md` when done.
</output>
