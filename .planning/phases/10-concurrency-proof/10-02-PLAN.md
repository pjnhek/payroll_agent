---
phase: 10-concurrency-proof
plan: 02
type: execute
wave: 1
gap_closure: true
depends_on: [10-01]
files_modified:
  - tests/test_concurrency_proof.py
  - .github/workflows/concurrency-proof.yml
autonomous: true
requirements: [OPS2-03]

must_haves:
  truths:
    - "Surfaces A and C drive the DB-level race through the SYNC repo seam (repo.insert_inbound_email -> repo.create_run) from N OS threads released simultaneously by a threading.Barrier(N) — NOT through the async /webhook/inbound route via a shared TestClient, which serializes on the event loop and never triggers the ON CONFLICT / lost-update race (CR-01, confirmed vs app/main.py:276-459 having no await between the dedup insert and create_run)."
    - "Surface A still asserts exactly one run for one shared message_id via the DB count(*)==1 backstop AND asserts the duplicate-loser shape explicitly (every result is accepted-or-duplicate, exactly one 'accepted') rather than filtering None out of a run_ids set (WR-02)."
    - "The genuinely-concurrent pool-bound surfaces (A, C) use a thread count that fits the app pool (max_size=5) so real overlap does not starve on the 5s pool timeout and flake — N_INGEST = 5 (<= max_size), with the N<->pool relationship stated correctly in a comment, not the incorrect 'stays inside the pool budget' claim at the old line 61 (WR-01, IN-02)."
    - "Surface B (the sync /approve route race) is UNCHANGED in mechanism — it already runs genuinely parallel via Starlette's anyio threadpool and its CAS proof is sound (D-10-06)."
    - "The CI workflow's schema step is non-destructive: it does NOT run 'bootstrap --reset' (the seeded_db fixture owns reset behind its ALLOW_DB_RESET guard); the ephemeral postgres:16 service + pg_isready health check + 'pytest -m integration' run are retained (WR-04, D-10-03)."
    - "Module and per-test docstrings accurately describe what each surface proves — 'genuine parallelism' language is only used where it is now true (A/C via barrier+threads, B via sync route); no docstring claims parallelism for a serialized path (IN-01)."
  artifacts:
    - path: "tests/test_concurrency_proof.py"
      provides: "The corrected capstone: Surfaces A/C genuinely race the DB seam under a threading.Barrier; Surface B unchanged; docstrings truthful."
      contains: "threading.Barrier"
    - path: ".github/workflows/concurrency-proof.yml"
      provides: "CI job with ephemeral postgres:16 that runs the proof; non-destructive schema step (no --reset)."
      contains: "services:"
  key_links:
    - from: "tests/test_concurrency_proof.py"
      to: "app.db.repo.insert_inbound_email"
      via: "direct call from N barrier-released threads (bypasses the async route's event-loop serialization)"
      pattern: "repo\\.insert_inbound_email"
    - from: "tests/test_concurrency_proof.py"
      to: "app.pipeline.orchestrator._deliver"
      via: "monkeypatch on the orchestrator module (Surface B, unchanged)"
      pattern: "app\\.pipeline\\.orchestrator\\._deliver"
---

<objective>
Close code-review finding CR-01 (BLOCKER, CONFIRMED) plus WR-01/WR-02/WR-04/IN-01/IN-02 from `.planning/phases/10-concurrency-proof/10-REVIEW.md`.

CR-01: Surfaces A (dedup) and C (lost-update) fired N=8 threads at `/webhook/inbound`, which is `async def` (app/main.py:276) with the ONLY `await` at `await request.body()` (line 300) — the entire dedup-critical DB body (`insert_inbound_email` ON CONFLICT -> `create_run`, ~lines 377-459) is synchronous blocking psycopg with NO await. Starlette runs async endpoints on the single event loop, and one shared `TestClient` funnels all 8 threads through one portal, so the ingest transactions SERIALIZE — the ON CONFLICT / lost-update race is never triggered. Surface A would pass even with the ON CONFLICT clause deleted. The proof is vacuous for 2 of 3 surfaces. Surface B (sync `def approve`, app/main.py:748) IS genuinely parallel via the anyio threadpool and is sound — leave it alone.

Fix: rewrite Surfaces A and C to drive the race through the SYNC repo seam directly from N OS threads released simultaneously by a `threading.Barrier(N)`, so real DB-level overlap occurs and the ON CONFLICT / lost-update invariants are genuinely exercised. Fold in the corollary warnings (pool-fit thread count, explicit loser-shape assertion, non-destructive CI schema step, truthful docstrings). Test-only — zero production-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-concurrency-proof/10-REVIEW.md
@.planning/phases/10-concurrency-proof/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Surfaces A & C to a barrier-driven direct-seam race; fix WR-01/WR-02/IN-01/IN-02</name>
  <files>tests/test_concurrency_proof.py</files>
  <read_first>
    - tests/test_concurrency_proof.py (the file being MODIFIED — current state has the 3 surfaces; A at :151-215, B at :223-273, C at :281-353; helpers at :64-143)
    - .planning/phases/10-concurrency-proof/10-REVIEW.md (CR-01 fix Option 1 barrier sketch :93-113; WR-01 :132-153; WR-02 :155-175; IN-01 :221-230; IN-02 :232-241)
    - app/db/repo.py (insert_inbound_email :171 — keyword-only, `conn=None` self-manages a pooled connection+transaction via `_conn_ctx`; returns (email_id|None, inserted:bool); create_run :324; load_run/get_connection for the DB backstop)
    - app/db/supabase.py (ConnectionPool min_size=1, max_size=5, timeout=5s — the pool budget that makes N=8 genuine holders starve; drives the WR-01 N_INGEST<=5 choice)
    - app/main.py (:276 `async def inbound` with only `await request.body()` at :300; the sync DB body :377-459 — the confirmed serialization mechanism; :748 `def approve` sync route — Surface B's genuine-parallelism source, DO NOT change Surface B)
    - tests/test_atomic_persist.py (the `_seed_live_run` / repo.* direct-call pattern already mirrored in this module's helpers)
  </read_first>
  <action>
    Rewrite ONLY Surfaces A and C. Leave Surface B (`test_concurrent_approvals_exactly_one_wins`) and its ThreadPoolExecutor-over-the-real-/approve-route mechanism completely unchanged — it is genuinely parallel and sound (D-10-06).

    Thread-count constants (WR-01, IN-02): replace the single `N = 8` + its incorrect "stays inside the pool budget" comment. Introduce `N_APPROVE = 8` (Surface B keeps N=8 — the /approve route uses the anyio threadpool, and each request holds a pooled connection only for the sub-ms CAS, so 8 is fine) and `N_INGEST = 5` for Surfaces A and C (each barrier-released thread holds a pooled connection for the FULL ingest transaction; with pool max_size=5 and timeout=5s, N_INGEST must be <= 5 or genuine overlap starves 3 threads on the 5s timeout and flakes). Add a comment that states the real relationship: `N_INGEST` must be `<= app pool max_size (5)` because these threads are simultaneous connection HOLDERS, unlike Surface B's brief CAS. Update Surface B to use `N_APPROVE`.

    Surface A — `test_dedup_exactly_one_run_per_message_id` (barrier-driven direct seam, CR-01 Option 1):
      - Keep the two-factor `@_SKIP_LIVE_DB` + `@pytest.mark.integration` + `seeded_db` guards and the `_stub_pipeline_and_send(monkeypatch)` call (still needed: create_run does NOT schedule the pipeline, but keep the stub so nothing downstream can fire a live call; and to keep the module's isolation invariant uniform).
      - Remove the TestClient + HTTP fan-out. Instead: one shared `same_message_id`, a `threading.Barrier(N_INGEST)`, a `threading.Lock`-guarded `results` list. Each thread: `barrier.wait()` to release all N simultaneously, then call `repo.insert_inbound_email(message_id=same_message_id, in_reply_to=None, references_header=None, subject="s", from_addr=COASTAL_EMAIL, to_addr="agent@payroll-agent.local", body_text="Maria Chen 40 regular hours.")` -> `(eid, inserted)`; if `inserted`, `rid = repo.create_run(business_id=COASTAL_BIZ_ID, source_email_id=eid)` else `rid=None`; append `(eid, inserted, rid)` under the lock. Start N threads, join all.
      - Assertions (this is the REAL proof now): exactly one thread saw `inserted=True` (`sum(1 for _e,ins,_r in results if ins) == 1`) — the ON CONFLICT DO NOTHING let exactly one winner through under genuine MVCC contention (WR-02: assert the winner/loser split explicitly, do NOT filter None out of a set). The winner's `rid` is non-null; every loser's `rid` is None. Keep the DB backstop: `SELECT count(*) FROM payroll_runs WHERE source_email_id = (SELECT id FROM email_messages WHERE message_id=%s) == 1`. Also assert exactly one distinct non-null `eid` across all results (all losers got the same committed row id back, or None — ON CONFLICT DO NOTHING returns no id to losers; assert every non-null eid is identical).
      - NOTE the winner-id semantics against the real repo: read `insert_inbound_email` to confirm what a duplicate loser receives (email_id=None + inserted=False per the docstring at repo.py:171). Assert against the ACTUAL behavior, not an assumption — if losers get `(None, False)`, assert `[r for r in results if r[1]] ` has length 1 and all `inserted=False` rows have `eid is None`.

    Surface C — `test_concurrent_distinct_runs_no_lost_update` (barrier-driven direct seam):
      - Same guards + stub call. Remove TestClient/HTTP. `message_ids = [f"<distinct-{uuid.uuid4()}@acme.test>" for _ in range(N_INGEST)]`. `threading.Barrier(N_INGEST)`, lock-guarded results. Each thread: `barrier.wait()`, `insert_inbound_email(message_id=mid, ...)` (unique mid) -> always `inserted=True`, `create_run(...)`, append `(mid, eid, rid)`.
      - Assertions: exactly `N_INGEST` distinct non-null `rid`s (no lost update — every distinct ingest produced exactly one run under concurrent load). Then the DB backstop (adapt the existing LEFT JOIN query at current lines 330-351): every run row for those run_ids has a non-null `source_email_id` with a matching `email_messages` row (no half-write, D-9-09). Keep the `pipeline_calls` assertion ONLY if the direct seam actually triggers it — create_run does NOT call _run_pipeline, so DROP the `len(pipeline_calls) == N` assertion for the direct-seam version (it was an artifact of the webhook path). If you want to keep a pipeline-count check, it belongs to the webhook path which we are deliberately leaving; do not assert a count the direct seam cannot produce.

    Docstrings (IN-01): update the module docstring and the Surface A / Surface C per-test docstrings so "genuine OS-thread parallelism" / "genuinely interleaves" / "under genuine parallelism" is accurate — describe the barrier-released direct-seam mechanism ("N threads released simultaneously by a threading.Barrier all enter insert_inbound_email at the same wall-clock instant, contending on Postgres MVCC / the ON CONFLICT unique index"). Keep the module-docstring local-invocation block. Do NOT overclaim for any path.

    Imports: `threading` is already imported; drop `ThreadPoolExecutor`/`TestClient` imports if they become unused in A/C (Surface B still uses ThreadPoolExecutor + TestClient — keep them). `get_settings.cache_clear()` env dance is only needed for the routes that read settings via TestClient — Surface B keeps it; for A/C direct-seam it is no longer required (no route, no ALLOW_UNSIGNED_FIXTURES gate), so it may be removed from A/C, but keeping a harmless `get_settings.cache_clear()` is acceptable. Do not leave a dangling `monkeypatch.setenv("ALLOW_UNSIGNED_FIXTURES", ...)` claim that implies an HTTP path for A/C.
  </action>
  <acceptance_criteria>
    - `tests/test_concurrency_proof.py` uses `threading.Barrier` in Surfaces A and C (grep: `grep -c 'threading.Barrier' tests/test_concurrency_proof.py` >= 2, or >=1 if a shared helper is used).
    - Surfaces A and C call `repo.insert_inbound_email(` and `repo.create_run(` directly from the threaded body (no `client.post("/webhook/inbound"` in Surfaces A or C). Grep: `grep -c 'client.post("/webhook/inbound"' tests/test_concurrency_proof.py` == 0.
    - Surface B is unchanged: still `client.post(f"/runs/{run_id}/approve")` via ThreadPoolExecutor, still asserts `len(deliver_calls) == 1` and `load_run(run_id)["status"] == "approved"`; the `app.pipeline.orchestrator._deliver` patch is intact.
    - Thread counts fit the pool: the ingest surfaces use a constant `<= 5` (grep for `N_INGEST` and a comment referencing the pool max_size). No surface fires more simultaneous connection-holders than the pool can hold.
    - Surface A asserts the explicit winner/loser split (exactly one `inserted=True`) AND keeps the DB `count(*)==1` backstop; it does NOT rely on `{r.get("run_id") for r in results if r.get("run_id")}`.
    - `uv run pytest -m 'not integration' -q` stays green and DB-free (the module is still `@pytest.mark.integration`, excluded from the hermetic suite).
    - No production-code file under `app/` is modified: `git diff --name-only app/` returns empty.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -m 'not integration' -q && grep -q "threading.Barrier" tests/test_concurrency_proof.py && [ "$(grep -c 'client.post(\"/webhook/inbound\"' tests/test_concurrency_proof.py)" -eq 0 ] && grep -q "repo.insert_inbound_email(" tests/test_concurrency_proof.py && grep -q "app.pipeline.orchestrator._deliver" tests/test_concurrency_proof.py && grep -q 'client.post(f"/runs/{run_id}/approve")' tests/test_concurrency_proof.py && [ -z "$(git diff --name-only app/)" ]</automated>
  </verify>
  <done>Surfaces A and C genuinely race the DB seam under a threading.Barrier (the ON CONFLICT / lost-update invariants are now actually exercised, not serialized away); Surface B is untouched and sound; thread counts fit the pool; the loser shape is asserted explicitly; docstrings are truthful; the hermetic suite stays green and DB-free; no app/ change.</done>
</task>

<task type="auto">
  <name>Task 2: Make the CI schema step non-destructive (WR-04)</name>
  <files>.github/workflows/concurrency-proof.yml</files>
  <read_first>
    - .github/workflows/concurrency-proof.yml (current — the "Apply schema" step runs `uv run python -m app.db.bootstrap --reset`)
    - .planning/phases/10-concurrency-proof/10-REVIEW.md (WR-04 :197-217)
    - tests/conftest.py (the `seeded_db` module-scoped fixture calls bootstrap(reset=True)+seed() behind the ALLOW_DB_RESET guard — it already owns reset)
    - app/db/bootstrap.py (entrypoint `python -m app.db.bootstrap [--reset]`; without --reset it applies schema idempotently)
  </read_first>
  <action>
    The `seeded_db` fixture already runs `bootstrap(reset=True) + seed()` at module scope behind the ALLOW_DB_RESET two-factor guard, so the workflow's own `--reset` step is redundant AND runs an unguarded destructive drop (bootstrap honors the `--reset` argv flag but NOT the ALLOW_DB_RESET env guard the suite relies on). Safe against the ephemeral container today, but fragile if the workflow is ever copied/repointed.

    Change the "Apply schema to ephemeral Postgres" step to a NON-destructive pre-flight: `uv run python -m app.db.bootstrap` (drop the `--reset` flag). The `seeded_db` fixture owns the reset behind its guard. Keep everything else identical: the `services: postgres` block with `image: postgres:16` + `pg_isready` health check, the job-level plain-local `DATABASE_URL` (postgresql://postgres:postgres@localhost:5432/postgres — NEVER a pooler/Supabase URL), `ALLOW_DB_RESET: "1"` and `ALLOW_UNSIGNED_FIXTURES: "true"` (these stay — the fixture's reset needs ALLOW_DB_RESET), the checkout@v4 -> setup-uv@v5 (python 3.12) -> `uv sync` sequence, and the `uv run pytest tests/test_concurrency_proof.py -m integration -v` run step. No `secrets.*`, no `pooler.supabase.com`.

    (If dropping --reset causes a first-run "relation already/does-not exist" edge on a truly empty container, the fixture's reset=True still creates the schema before any test runs, so the pre-flight bootstrap-without-reset on an empty DB simply creates the tables idempotently — confirm against bootstrap.py that a no-reset apply on an empty DB is a clean create, and if bootstrap without --reset cannot create from empty, KEEP a schema-creating step but route reset ownership to the fixture. Prefer the simplest change that keeps the destructive drop out of the workflow env.)
  </action>
  <acceptance_criteria>
    - The workflow no longer runs `bootstrap --reset`: `grep -c 'app.db.bootstrap --reset' .github/workflows/concurrency-proof.yml` == 0.
    - A `uv run python -m app.db.bootstrap` step (no `--reset`) OR the fixture-owned-reset equivalent is present so the schema exists before the proof runs.
    - The proof still runs: `grep -q 'tests/test_concurrency_proof.py -m integration' .github/workflows/concurrency-proof.yml`.
    - `services:` with `image: postgres:16` and `pg_isready` retained; job env keeps the plain-local `localhost:5432` DATABASE_URL; no `pooler.supabase.com` and no `secrets.` string.
    - Valid YAML (parses).
  </acceptance_criteria>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/concurrency-proof.yml'))" && [ "$(grep -c 'app.db.bootstrap --reset' .github/workflows/concurrency-proof.yml)" -eq 0 ] && grep -q "app.db.bootstrap" .github/workflows/concurrency-proof.yml && grep -q "image: postgres:16" .github/workflows/concurrency-proof.yml && grep -q "pg_isready" .github/workflows/concurrency-proof.yml && grep -q "tests/test_concurrency_proof.py -m integration" .github/workflows/concurrency-proof.yml && grep -q "localhost:5432" .github/workflows/concurrency-proof.yml && [ "$(grep -c 'pooler.supabase.com' .github/workflows/concurrency-proof.yml)" -eq 0 ] && [ "$(grep -c 'secrets\.' .github/workflows/concurrency-proof.yml)" -eq 0 ]</automated>
  </verify>
  <done>The CI workflow keeps the destructive `--reset` drop out of its job env (reset ownership moves to the seeded_db fixture behind its ALLOW_DB_RESET guard) while still standing up an ephemeral postgres:16 and running the proof with -m integration.</done>
</task>

</tasks>

<verification>
- `uv run pytest -m 'not integration' -q` — hermetic suite stays green and DB-free (the capstone is still integration-marked and excluded).
- The authoritative proof is the push-triggered GitHub Actions run of `concurrency-proof.yml` against the ephemeral postgres:16 — now that Surfaces A/C genuinely race the DB seam, a green run is real standing evidence and a regression (e.g. dropping the ON CONFLICT clause) would turn Surface A red.
- No `app/` production-code changes (`git diff --name-only app/` empty).
- CR-01 dead: `client.post("/webhook/inbound"` appears zero times; the dedup/lost-update races run through the barrier-released repo seam.
</verification>

<success_criteria>
- CR-01 closed: Surfaces A and C exercise real DB-level concurrency (N threads simultaneously inside insert_inbound_email/create_run via threading.Barrier), so the ON CONFLICT / lost-update invariants are genuinely proven, not serialized away.
- WR-01 closed: ingest surfaces use a thread count <= pool max_size (5) so genuine overlap does not flake on the pool timeout.
- WR-02 closed: Surface A asserts the winner/loser split explicitly + DB count backstop, no None-filtered set.
- WR-04 closed: CI schema step is non-destructive; reset ownership is the fixture's guarded path.
- IN-01/IN-02 closed: docstrings truthful; thread-count/pool relationship stated correctly.
- Surface B unchanged and sound. Zero production-code changes. Hermetic suite green.
</success_criteria>

<output>
Create `.planning/phases/10-concurrency-proof/10-02-SUMMARY.md` when done.
</output>
