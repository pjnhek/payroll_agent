---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_cr_regressions.py
  - tests/test_alias_and_run_column_regressions.py
  - tests/test_cr01_classify_union.py
  - tests/test_reply_classify_union.py
  - tests/test_resume_pipeline.py
  - tests/test_atomic_persist.py
  - tests/test_compose_email_field_regression.py
  - tests/test_reply_redelivery.py
  - tests/test_retrigger_epoch.py
  - tests/test_multiround_context_edge.py
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "No test function name embeds a ticket-ID prefix; renames are pure (no assertion changes) and the collected-test count is unchanged"
    - "The two ticket-named test files are renamed to descriptive names via git mv"
    - "The eight rename-affected test files carry zero ticket/provenance references"
  artifacts:
    - path: "tests/test_alias_and_run_column_regressions.py"
      provides: "renamed regression file (was the ticket-prefixed regressions file)"
    - path: "tests/test_reply_classify_union.py"
      provides: "renamed reply-classification file"
  key_links:
    - from: "tests/ rename commits"
      to: "pytest collection"
      via: "identical collect-only count before/after each rename commit"
      pattern: "tests collected"
---

<objective>
Execute the D-06 test-identity cleanup: strip ticket-ID prefixes from test function names
(pure renames, zero assertion changes), rename the two ticket-named test files, and sweep
the same eight files' comments/docstrings/failure-message strings under D-01/D-03 (COMM-01
per D-05 scope; COMM-03 for module docstrings).

Purpose: test names face the hiring-manager audience too — a suite full of review-ticket
prefixes reads as process residue. The identical collected-test count before/after is the
neutrality proof (15-RESEARCH Pitfall 3: careless renames can silently shadow duplicates
and DROP the count).
Output: eight clean test files, two renamed via git mv, collection count unchanged.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** constraint/invariant comments rewritten as plain
  English, ticket reference removed; pure provenance deleted.
- **D-03:** phase numbers, review rounds, planning-doc citations, secondary IDs all go.
  Requirement IDs (CLAR-*, CALC-*, …) are not guard-blocked; rewrite provenance prose around
  them.
- **D-06 renames:** strip the ticket prefix (e.g. a name starting test_cr01_... keeps its
  descriptive tail); where the ticket ID is the only meaningful part, invent a descriptive
  scenario name. NO assertion, fixture, or body changes — names and comment text only.
  Domain words are NOT ticket refs: round0/round2 (clarification rounds), epoch, resume —
  keep them.
- **Failure-message strings are in scope:** assert messages embedding ticket IDs are rewritten
  to plain-English invariant explanations (they are runtime strings the guard will scan).
- NEVER delete or relocate `# noqa:` / `# type: ignore[code]` markers — rewrite reason text
  only.
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: D-06 renames — functions and the two ticket-named files, with collection-count proof</name>
  <files>tests/test_cr_regressions.py, tests/test_cr01_classify_union.py, tests/test_resume_pipeline.py, tests/test_atomic_persist.py, tests/test_compose_email_field_regression.py, tests/test_reply_redelivery.py, tests/test_retrigger_epoch.py, tests/test_multiround_context_edge.py</files>
  <read_first>
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Ticket-shaped
      test-name inventory: 32 functions; concentrations and singletons enumerated; CI
      workflows reference test FILES not function names — deploy-migrate.yml and
      concurrency-proof.yml reference only test_schema_introspect.py and
      test_concurrency_proof.py, neither renamed here)
    - The eight test files themselves (enumerate ticket-named functions at execution time
      with: grep -rnE 'def test_[a-z0-9_]*(cr[0-9]|wr[0-9]|cx[0-9]|fix[0-9]|gap[0-9]|d7509|clar207)' tests/
      — then apply judgment: domain words like round2/round0 are clarification-round
      vocabulary, not ticket refs — they stay)
  </read_first>
  <action>
    Record the baseline collected-test count to a scratch file BEFORE touching any file:
    `uv run pytest -q --collect-only | tail -1 | awk '{print $1}' > /tmp/collect_baseline_15_06`
    (the count alone, not the timing-bearing full line — the verify compares against this
    file and must FAIL on any mismatch, per threat T-15-05). Rename every
    ticket-prefixed test function in the eight files per D-06 — examples of the required
    shape: a cr01-prefixed explicit-zero overpay-guard test drops the cr01 prefix; the
    wr04-labeled deliver-finalize crash test drops wr04 from its name; the two fix5
    sender-revalidation redelivery tests become descriptive sender-revalidation names; the
    d7509-wording compose tests become descriptive wording-pinned names. Ensure every new
    name is unique within its module (duplicate names silently shadow and DROP the collected
    count). Then git mv tests/test_cr_regressions.py to
    tests/test_alias_and_run_column_regressions.py and tests/test_cr01_classify_union.py to
    tests/test_reply_classify_union.py (their names ARE ticket provenance; no CI workflow or
    test references either file — re-verify with grep over .github/workflows/ and tests/
    before moving). Do NOT rename tests/test_bound01_private_imports.py — BOUND-01 is a
    requirements-traceability ID, not a review ticket. Re-run collect-only: the count must
    equal the baseline exactly. Commit renames as their own commit (rename-only, no comment
    sweeping mixed in).
  </action>
  <verify>
    <automated>test "$(uv run pytest -q --collect-only | tail -1 | awk '{print $1}')" = "$(cat /tmp/collect_baseline_15_06)" && ! grep -rnE '(def test_|class Test)[A-Za-z0-9_]*(cr0[0-9]|wr0[0-9]|cx0[0-9]|fix5|d7509|clar207)' tests/ && test -f tests/test_alias_and_run_column_regressions.py && test -f tests/test_reply_classify_union.py && ! test -f tests/test_cr_regressions.py && ! test -f tests/test_cr01_classify_union.py && uv run pytest -q</automated>
  </verify>
  <done>
    All ticket-prefixed test functions renamed; both files moved with git mv; collected-test
    count identical to the pre-rename baseline; full suite green with zero assertion changes.
  </done>
  <acceptance_criteria>
    - The baseline file /tmp/collect_baseline_15_06 was written before the first rename, and
      `test "$(uv run pytest -q --collect-only | tail -1 | awk '{print $1}')" = "$(cat /tmp/collect_baseline_15_06)"`
      exits 0 after the rename commit — a shadowed/dropped test makes this comparison FAIL
      (record both numbers in the summary).
    - The rename-detection grep above returns zero hits under tests/.
    - `git log --diff-filter=R` shows the two file renames (rename detection, not
      delete+add of unrelated content).
    - The rename commit contains no changes to assert statements (source assertion on the
      diff).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep comments in the two renamed files + test_resume_pipeline.py + test_atomic_persist.py</name>
  <files>tests/test_alias_and_run_column_regressions.py, tests/test_reply_classify_union.py, tests/test_resume_pipeline.py, tests/test_atomic_persist.py</files>
  <read_first>
    - The four files (test_resume_pipeline.py is the heaviest test file in the repo at ~115
      hits — its resume/re-extraction invariant comments are money-relevant, keep D-02 depth)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to comments, docstrings, and failure-message strings in the four
    files. Test docstrings that explain WHAT invariant the test locks (e.g. resume re-extracts
    over original + reply bodies so a partial reply is lossless) keep the invariant, lose the
    ticket citations. Comment-only commit, separate from Task 1's rename commit.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' tests/test_alias_and_run_column_regressions.py tests/test_reply_classify_union.py tests/test_resume_pipeline.py tests/test_atomic_persist.py && uv run pytest -q tests/test_alias_and_run_column_regressions.py tests/test_reply_classify_union.py tests/test_resume_pipeline.py tests/test_atomic_persist.py</automated>
  </verify>
  <done>Zero gate-regex hits in the four files; their tests pass unchanged.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the four files.
    - No assert-statement changes in the commit diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep the remaining four rename-affected files + full gates</name>
  <files>tests/test_compose_email_field_regression.py, tests/test_reply_redelivery.py, tests/test_retrigger_epoch.py, tests/test_multiround_context_edge.py</files>
  <read_first>
    - The four files (test_retrigger_epoch.py is the analog plan 15-01 copies — its
      explanatory failure-message style stays, its ticket prefixes go)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to comments, docstrings, and failure-message strings in the four
    files. Multi-line assert messages keep their teach-the-invariant style in plain English.
    Then run the full plan gates: complete suite, ruff, strict mypy.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' tests/test_compose_email_field_regression.py tests/test_reply_redelivery.py tests/test_retrigger_epoch.py tests/test_multiround_context_edge.py && test "$(uv run pytest -q --collect-only | tail -1 | awk '{print $1}')" = "$(cat /tmp/collect_baseline_15_06)" && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>
    Zero gate-regex hits in the four files; full suite + ruff + strict mypy green; collected
    count still equals the Task 1 baseline.
  </done>
  <acceptance_criteria>
    - The gate grep returns no matches for the four files.
    - `uv run pytest -q` exits 0; the collect-only count comparison against
      /tmp/collect_baseline_15_06 (in the verify) exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test renames -> suite coverage | duplicate names after prefix-stripping would silently shadow tests and shrink coverage |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05 | Tampering (self-inflicted) | pytest collection shrinkage via rename shadowing | medium | mitigate | collect-only count machine-compared against the pre-rename baseline file (/tmp/collect_baseline_15_06) in Task 1 and Task 3 verifies — a shadowed test fails the gate; unique-name rule enforced in Task 1 |
| T-15-04 | Tampering (self-inflicted) | comment sweep altering test behavior | low | mitigate | no assert changes; full suite + ruff + mypy green per commit |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Collected-test count identical to baseline at plan end.
- Gate grep clean over all eight (post-rename) files.
- `uv run pytest -q && uv run ruff check && uv run mypy` green.
</verification>

<success_criteria>
- D-06 fully executed: no ticket-prefixed test function names anywhere in tests/, two files
  renamed, zero assertion changes, count-neutral.
</success_criteria>

## Artifacts this phase produces (this plan)

- Renamed file: `tests/test_alias_and_run_column_regressions.py` (git mv from the
  ticket-prefixed regressions file).
- Renamed file: `tests/test_reply_classify_union.py` (git mv from the ticket-prefixed
  classify-union file).
- ~30 renamed test functions across the eight files (descriptive, ticket-free names; exact
  final names at executor discretion under D-06, enumerated in the SUMMARY).
- No other new symbols — comment/docstring text edits only.

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: (def test_|class Test)[A-Za-z0-9_]*(cr0[0-9]|wr0[0-9]|cx0[0-9]|fix5|d7509|clar207) -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-06-SUMMARY.md` when done.
Enumerate every function rename (old -> new) — plan 15-10's guard work and the phase
verification read this list.
</output>
