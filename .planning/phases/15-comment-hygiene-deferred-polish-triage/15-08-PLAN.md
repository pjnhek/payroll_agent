---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_gateway.py
  - tests/test_threading.py
  - tests/test_validate.py
  - tests/test_bound01_private_imports.py
  - tests/test_clarify.py
  - tests/test_delivery.py
  - tests/test_live_llm.py
  - tests/test_hitl.py
  - tests/test_eval_wiring.py
  - tests/test_concurrency_proof.py
  - tests/test_reconcile.py
  - tests/test_ingest.py
  - tests/test_status_drift.py
  - tests/test_gate.py
  - tests/test_demo_landing.py
  - tests/test_multi_employee_delivery.py
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "These sixteen test files carry zero ticket/provenance references (including UI-SPEC/UAT/Codex/HIGH-N/finding-# vocabulary); failure messages teach invariants in plain English"
    - "The private-imports guard test keeps its file name and its requirement-ID references (requirement IDs are traceability, not review tickets)"
    - "type: ignore markers keep their positions with rewritten plain-English reasons"
  artifacts:
    - path: "tests/test_bound01_private_imports.py"
      provides: "provenance-free guard test, file name unchanged"
    - path: "tests/test_threading.py"
      provides: "ticket-free threading header-chain tests"
  key_links:
    - from: "tests/test_eval_wiring.py"
      to: "mypy strict gate"
      via: "type: ignore[attr-defined] marker preserved in place with rewritten reason"
      pattern: "type: ignore"
---

<objective>
Sweep the gateway/threading/validation/clarify/delivery/integration test cluster (sixteen
files, ~310+ matching lines) clean of ticket-ID and process references under D-01/D-03
(COMM-01 per D-05 scope; COMM-03 for module docstrings). Comments, docstrings, and
failure-message strings only — zero assertion changes.

Purpose: completes the tests/ sweep surface alongside plans 15-06 and 15-07 (the remaining
small test files ride with plan 15-09). The BOUND-01 guard file is explicitly handled here:
its file name and requirement-ID vocabulary stay; its review/phase provenance goes.
Post-review addition: tests/test_demo_landing.py and tests/test_multi_employee_delivery.py
were absent from EVERY plan's file list (found by re-running the sweep manifest after the
cross-AI review flagged an inventory omission) — they join Task 3 here rather than growing
plan 15-09 further. test_multi_employee_delivery.py's hits are ALL outside the D-08 guard
subset (UAT/Codex/HIGH-N shapes), so no later gate would have caught the omission.
Output: sixteen comment-clean test files, suite passing unchanged.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** constraint/invariant comments rewritten as plain
  English, ticket reference removed; pure provenance deleted.
- **D-02 money-relevant depth:** delivery/threading test comments explaining strand/mispay
  scenarios keep constraint + failure mode.
- **D-03:** phase numbers, review rounds, planning-doc citations (incl. UI-SPEC / "UAT #N"),
  secondary IDs (Codex, "finding #N", HIGH-N/R2-HIGH severity IDs) all go.
  Requirement IDs (BOUND-01, CLAR-*, EMAIL-*, …) are traceability, NOT review tickets — they
  are not guard-blocked; rewrite only provenance-flavored prose around them.
- **Fresh manifest first (review LOW finding):** hit counts in this plan and 15-RESEARCH.md
  are discussion-time sizing estimates ONLY — before editing each task's file group,
  regenerate a manifest with the task's full gate regex (including the extended vocabulary)
  over those files and work from the manifest, not the counts.
- **Failure-message strings are in scope** (runtime strings the phase guard scans) — rewrite
  ticket-labeled assert messages to plain-English invariant statements.
- Residual ticket-prefixed test NAMES discovered in these files are renamed per D-06
  (unique names, collect-count neutral).
- NEVER delete or relocate `# noqa:` / `# type: ignore[code]` markers — rewrite reason text
  only. test_eval_wiring.py carries a known ignore[attr-defined] whose reason must survive in
  plain English.
- Do NOT rename tests/test_bound01_private_imports.py (BOUND-01 is a requirement ID) and do
  not weaken its scanner logic — text-only edits.
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Sweep test_gateway.py, test_threading.py, test_validate.py, test_bound01_private_imports.py</name>
  <files>tests/test_gateway.py, tests/test_threading.py, tests/test_validate.py, tests/test_bound01_private_imports.py</files>
  <read_first>
    - tests/test_gateway.py (~45 hits), tests/test_threading.py (~38 hits; header-chain
      invariant docstrings keep depth — plan 15-01 added a sibling threading test that is
      already clean)
    - tests/test_validate.py (~37 hits; pay-type rule commentary is money-relevant)
    - tests/test_bound01_private_imports.py (~28 hits; keep the module-docstring spec style,
      the SCAN_ROOTS/scanner structure, and every BOUND-01 reference; strip phase/decision
      citations — this file is also the structural template plan 15-10 copies for the new
      comment guard, so its docstring quality matters twice)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to comments, docstrings, and failure-message strings in the four
    files. Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' tests/test_gateway.py tests/test_threading.py tests/test_validate.py tests/test_bound01_private_imports.py && uv run pytest -q tests/test_gateway.py tests/test_threading.py tests/test_validate.py tests/test_bound01_private_imports.py</automated>
  </verify>
  <done>Zero gate-regex hits in the four files; their tests (including the live-tree guard) pass unchanged.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the four files.
    - tests/test_bound01_private_imports.py still exists under that exact name and still
      contains BOUND-01 references (source assertion).
    - No assert-logic or scanner-logic changes in the commit diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep test_clarify.py, test_delivery.py, test_live_llm.py, test_hitl.py, test_eval_wiring.py</name>
  <files>tests/test_clarify.py, tests/test_delivery.py, tests/test_live_llm.py, tests/test_hitl.py, tests/test_eval_wiring.py</files>
  <read_first>
    - tests/test_clarify.py (~24 hits; a known ticket-labeled failure-message string —
      rewrite the message, keep the assertion), tests/test_delivery.py (~21 hits;
      money-relevant depth)
    - tests/test_live_llm.py (~19 hits; live-marker posture comments stay accurate),
      tests/test_hitl.py (~16 hits)
    - tests/test_eval_wiring.py (~16 hits; the ignore[attr-defined] marker with its
      private re-export rationale — rewrite reason in plain English, never delete)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to the five files. Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' tests/test_clarify.py tests/test_delivery.py tests/test_live_llm.py tests/test_hitl.py tests/test_eval_wiring.py && uv run pytest -q tests/test_clarify.py tests/test_delivery.py tests/test_hitl.py tests/test_eval_wiring.py</automated>
  </verify>
  <done>Zero gate-regex hits in the five files; hermetic tests pass unchanged (live-marked tests stay deselected).</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the five files.
    - The ignore[attr-defined] marker in test_eval_wiring.py is still present on its original
      line with a plain-English reason (source assertion).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep test_concurrency_proof.py, test_reconcile.py, test_ingest.py, test_status_drift.py, test_gate.py, test_demo_landing.py, test_multi_employee_delivery.py + full gates</name>
  <files>tests/test_concurrency_proof.py, tests/test_reconcile.py, tests/test_ingest.py, tests/test_status_drift.py, tests/test_gate.py, tests/test_demo_landing.py, tests/test_multi_employee_delivery.py</files>
  <read_first>
    - tests/test_concurrency_proof.py (~16 hits; the barrier-thread design rationale —
      why ingest threads bypass the async route, why N matches the pool size — is a real
      constraint narrative: keep it fully, drop the ticket labels; this file is referenced by
      the concurrency-proof workflow by FILE name — do not rename)
    - tests/test_status_drift.py (~12 hits; known ticket-labeled failure messages around the
      absent-status enforcement — rewrite messages, keep assertions)
    - tests/test_reconcile.py, tests/test_ingest.py, tests/test_gate.py
    - tests/test_demo_landing.py (post-review addition — 3 gate-shaped + 5 extended-vocab
      hits at manifest time) and tests/test_multi_employee_delivery.py (post-review addition
      — 6 extended-vocab hits, ZERO gate-shaped: only the extended regex forces this file)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to the seven files, then run the full plan gates: complete suite,
    ruff, strict mypy. Addresses review concern: incomplete sweep inventory (HIGH) — the two
    added files close the tests/ coverage gap found by the fresh manifest.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' tests/test_concurrency_proof.py tests/test_reconcile.py tests/test_ingest.py tests/test_status_drift.py tests/test_gate.py tests/test_demo_landing.py tests/test_multi_employee_delivery.py && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>Zero extended-gate-regex hits in the seven files; full suite + ruff + strict mypy green.</done>
  <acceptance_criteria>
    - The extended gate grep returns no matches for the seven files.
    - tests/test_concurrency_proof.py keeps its exact file name (CI workflow references it).
    - `uv run pytest -q` exits 0 with no assertion changes anywhere in the plan's diffs.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text edits -> guard/proof tests | sweeping the BOUND-01 guard and concurrency proof must not weaken what they enforce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | sweep weakening guard/proof tests | medium | mitigate | text-only rule; scanner/assert logic untouched (source-assertion criteria); full suite + ruff + mypy green per commit |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Extended gate grep clean over all sixteen files.
- `uv run pytest -q && uv run ruff check && uv run mypy` green at plan end.
</verification>

<success_criteria>
- The gateway/threading/validation/integration test cluster reads ticket-free with guard and
  proof semantics untouched.
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — comment/docstring/message-string text edits only in the sixteen listed
  files.

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH -->
<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->
<!-- planner-discipline-allow: UI-SPEC -->
<!-- planner-discipline-allow: UAT # -->
<!-- planner-discipline-allow: Codex -->
<!-- planner-discipline-allow: HIGH-[0-9] -->
<!-- planner-discipline-allow: finding # -->
<!-- planner-discipline-allow: R2-HIGH -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-08-SUMMARY.md` when done.
</output>
