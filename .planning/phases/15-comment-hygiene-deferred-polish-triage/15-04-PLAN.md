---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/calculate.py
  - app/pipeline/tax_tables_2026.py
  - app/pipeline/federal_withholding.py
  - app/pipeline/decide.py
  - app/pipeline/validate.py
  - app/pipeline/alias_learning.py
  - app/pipeline/reconcile_names.py
  - app/pipeline/compose_email.py
  - app/pipeline/extract.py
  - app/pipeline/suggest.py
  - app/pipeline/pdf.py
  - app/models/contracts.py
  - app/models/roster.py
  - app/models/status.py
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "The money-core (calculate, tax tables, withholding, decide, validate) carries zero ticket references while keeping full constraint-plus-failure-mode comment depth"
    - "IRS/SSA source URLs survive the sweep (external authoritative citations stay)"
    - "Models and remaining pipeline stages carry zero ticket references; docstrings state purpose and invariants"
  artifacts:
    - path: "app/pipeline/decide.py"
      provides: "pure-code decision module whose docstring states the no-LLM/no-confidence invariant"
    - path: "app/pipeline/tax_tables_2026.py"
      provides: "year-keyed tables still citing the IRS Pub 15-T PDF source URL"
      contains: "irs.gov"
    - path: "app/models/contracts.py"
      provides: "provenance-free contract models"
  key_links:
    - from: "app/pipeline/calculate.py"
      to: "app/pipeline/tax_tables_2026.py"
      via: "unchanged imports — sweep is text-only"
      pattern: "tax_tables_2026"
---

<objective>
Sweep the money-core pipeline files, the remaining pipeline stages, and app/models/ clean of
ticket-ID and project-process references under D-01/D-02/D-03/D-04 (COMM-01 per D-05 scope;
COMM-03 for module docstrings). The money-core files are D-02's named full-depth set: their
failure-mode narration is the guard rail against future mispay-reintroducing simplifications.

Purpose: calculate.py and decide.py are the two files a hiring manager is most likely to open;
their comments must teach the system's invariants with zero process vocabulary.
Output: fourteen comment-clean files, text-only diffs, suite/ruff/mypy green per commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** a ticket comment documenting a real
  constraint/invariant/edge case is rewritten as plain maintainer-facing English, ticket
  reference removed. Pure provenance is deleted entirely.
- **D-02 money-path depth (calculate.py, tax_tables_2026.py, decide.py are NAMED files;
  federal_withholding.py and validate.py's pay-type rule qualify as money-relevant):**
  surviving comments keep BOTH constraint AND failure mode — e.g. "using the MFJ table for
  MFS would halve withholding". Never trim the consequence narration.
- **D-03 all process references go:** phase numbers, review rounds, planning-doc citations
  (incl. UI-SPEC / "UAT #N" — pdf.py cites UI-SPEC for its palette roles and compose_email.py
  cites the UI-SPEC copywriting contract: keep the design info — color roles, copy rules —
  drop the UI-SPEC label per D-01), split history, secondary IDs (OPS-, T-N-N, R2 rounds,
  Codex findings, "finding #N", HIGH-N/R2-HIGH severity IDs). External authoritative
  citations STAY — the IRS Pub 15-T PDF URL and SSA wage-base URL in the tax modules are
  sources, not process history; keep them verbatim.
- **Fresh manifest first (review LOW finding):** hit counts in this plan and 15-RESEARCH.md
  are discussion-time sizing estimates ONLY — before editing, regenerate a manifest with the
  full gate regex (including the extended vocabulary) over this plan's file set and work from
  the manifest, not the counts.
- **D-04 docstrings (COMM-03):** 1–2 sentence purpose; short invariants paragraph only where
  genuine — decide.py's docstring must state its real invariant (pure code over resolution
  facts: no LLM call, no confidence number, collisions always clarify). No history, no TOC.
- Requirement IDs (CALC-*, CLAR-*, LLM-*, …) are NOT guard-blocked; rewrite surrounding prose
  where it reads as provenance, keep the behavioral explanation.
- NEVER delete or relocate `# noqa: CODE` / `# type: ignore[code]` markers — rewrite only the
  reason text (warn_unused_ignores is on).
- Comment/docstring/string-literal text only: no code moves, no renames, no structural edits.
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Sweep the money-core — calculate.py, tax_tables_2026.py, federal_withholding.py, decide.py, validate.py</name>
  <files>app/pipeline/calculate.py, app/pipeline/tax_tables_2026.py, app/pipeline/federal_withholding.py, app/pipeline/decide.py, app/pipeline/validate.py</files>
  <read_first>
    - app/pipeline/calculate.py (~31 hits; FICA straddle, rounding, explicit-zero-overtime
      comments are all D-02 full-depth material)
    - app/pipeline/tax_tables_2026.py, app/pipeline/federal_withholding.py (3 hits each; the
      IRS PDF citation URLs MUST survive)
    - app/pipeline/decide.py (~12 hits), app/pipeline/validate.py (~21 hits; pay-type-aware
      missing-hours rule commentary is money-relevant)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (D-02 file
      list; Pitfall 6)
  </read_first>
  <action>
    Apply the sweep_rubric at FULL D-02 depth to all five files. Every surviving money rule
    comment states what the rule prevents (which mispay, which silent underpay/overpay).
    Keep the IRS Pub 15-T and SSA URLs and any worked-example references to the IRS PDF —
    those are external sources. Rewrite the five module docstrings per D-04; decide.py's gets
    the explicit invariants paragraph (pure code, no LLM, no confidence, collisions always
    clarify — resolving a collision in code would guess with money). Commit as one
    comment-only commit (or one per file — executor's discretion, comment-only either way).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/pipeline/calculate.py app/pipeline/tax_tables_2026.py app/pipeline/federal_withholding.py app/pipeline/decide.py app/pipeline/validate.py && grep -q 'irs.gov' app/pipeline/tax_tables_2026.py && uv run ruff check app/pipeline/</automated>
  </verify>
  <done>
    Zero gate-regex hits across the five money-core files; IRS source URL still present in
    tax_tables_2026.py; ruff green; text-only diffs.
  </done>
  <acceptance_criteria>
    - The gate grep returns no matches for the five files.
    - tax_tables_2026.py still contains an irs.gov URL (external citation preserved).
    - decide.py module docstring contains the no-LLM/no-confidence invariant statement.
    - No executable-line changes in the commit diff(s).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep remaining pipeline stages — alias_learning, reconcile_names, compose_email, extract, suggest, pdf</name>
  <files>app/pipeline/alias_learning.py, app/pipeline/reconcile_names.py, app/pipeline/compose_email.py, app/pipeline/extract.py, app/pipeline/suggest.py, app/pipeline/pdf.py</files>
  <read_first>
    - app/pipeline/alias_learning.py (~21 hits; the learn-only-a-token-that-matches-the-
      resolved-name guard is a money-adjacent invariant — keep its failure mode: learning a
      corrected misname silently misroutes future payroll)
    - app/pipeline/reconcile_names.py (~14), app/pipeline/compose_email.py (~15),
      app/pipeline/extract.py (~7), app/pipeline/suggest.py (~10), app/pipeline/pdf.py (1)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric. reconcile_names.py's exact/stored-alias/none resolution comments
    and alias_learning.py's misname guard keep full depth (money-relevant per D-02).
    suggest.py's isolation invariant (the suggestion is for the clarification email only and
    never feeds the decision) survives as a plain invariant statement. Rewrite module
    docstrings per D-04. Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/pipeline/alias_learning.py app/pipeline/reconcile_names.py app/pipeline/compose_email.py app/pipeline/extract.py app/pipeline/suggest.py app/pipeline/pdf.py && uv run ruff check app/pipeline/</automated>
  </verify>
  <done>Zero gate-regex hits across the six files; ruff green; text-only diffs.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the six files.
    - No executable-line changes in the commit diff(s).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep app/models/ + full gates</name>
  <files>app/models/contracts.py, app/models/roster.py, app/models/status.py</files>
  <read_first>
    - app/models/contracts.py (~32 hits; extra="forbid" rationale and field-shape invariants
      are real constraints — keep them, drop the decision-ID citations)
    - app/models/roster.py (~21 hits; known_aliases READ-side semantics)
    - app/models/status.py (~4 hits; status-machine writer rules)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to the three model files. Contract-shape invariants (why
    extra="forbid", why Decision carries no confidence field, alias semantics) survive as
    plain invariants; ticket citations go. Rewrite module docstrings per D-04. Then run the
    full plan gates: complete suite, ruff, strict mypy (models are imported everywhere —
    mypy is the structural-neutrality proof).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/models/contracts.py app/models/roster.py app/models/status.py && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>
    Zero gate-regex hits across app/models/; full suite + ruff + strict mypy green; text-only
    diffs.
  </done>
  <acceptance_criteria>
    - The gate grep returns no matches for the three model files.
    - `uv run pytest -q` exits 0 with no assertion changes.
    - `uv run mypy` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text edits -> money computation | comment-only sweep of the tax/decision core must not alter behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | money-core sweep altering behavior | high | mitigate | text-only diffs over the most-tested modules in the repo (table-driven IRS tests unchanged); full suite + ruff + strict mypy green; D-02 forbids trimming failure-mode comments that guard future edits |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Gate grep clean over all fourteen files.
- `uv run pytest -q && uv run ruff check && uv run mypy` green at plan end.
- IRS/SSA citation URLs still present in the tax modules.
</verification>

<success_criteria>
- Money-core and model files read as constraint-documented code with zero process history and
  undiminished failure-mode depth.
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — comment/docstring text edits only in the fourteen listed files.

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH -->
<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->
<!-- planner-discipline-allow: UI-SPEC -->
<!-- planner-discipline-allow: UAT # -->
<!-- planner-discipline-allow: Codex -->
<!-- planner-discipline-allow: HIGH-[0-9] -->
<!-- planner-discipline-allow: finding # -->
<!-- planner-discipline-allow: R2-HIGH -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-04-SUMMARY.md` when done.
</output>
