---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 09
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - app/routes/runs.py
  - app/routes/pipeline_glue.py
  - app/routes/webhook.py
  - app/routes/demo.py
  - app/routes/health.py
  - app/routes/templating.py
  - app/routes/dashboard.py
  - app/routes/__init__.py
  - app/llm/client.py
  - app/llm/prompts/extract.py
  - app/llm/prompts/clarify.py
  - app/llm/prompts/suggest.py
  - app/llm/prompts/__init__.py
  - app/email/gateway.py
  - app/email/clean.py
  - app/config.py
  - app/templates/run_detail.html
  - app/templates/eval.html
  - app/templates/runs_list.html
  - app/static/style.css
  - tests/test_dashboard.py
  - tests/test_llm_client.py
  - tests/test_stuck_run_recovery.py
  - tests/test_combined_context.py
  - tests/test_webhook.py
  - tests/test_orchestrator_states.py
  - tests/test_demo_fixtures.py
  - tests/test_suggest.py
  - tests/test_extract.py
  - tests/test_claim_status.py
  - tests/test_webhook_dedup_race.py
  - tests/test_decide_field_regression.py
  - tests/test_bootstrap_safe_url.py
  - tests/test_schema_introspect.py
  - tests/test_pdf.py
  - tests/test_compose_confirmation.py
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "Routes, LLM client/prompts, email layer, config, ALL provenance-bearing templates (run_detail.html, eval.html, runs_list.html), and CSS carry zero ticket/provenance references — including UI-SPEC/UAT/HIGH-N/Codex/finding-# vocabulary outside the D-08 guard subset"
    - "The demo button's user-visible label no longer references a project phase"
    - "The remaining small test files carry zero ticket/provenance references"
  artifacts:
    - path: "app/templates/run_detail.html"
      provides: "audience-facing copy with no phase references"
    - path: "app/routes/runs.py"
      provides: "provenance-free HITL/retrigger routes with sanitizer failure-mode comments intact"
  key_links:
    - from: "app/routes/dashboard.py"
      to: "plan 15-01's containment fix"
      via: "sweep runs after the fix lands (wave dependency) so comments are rewritten once in final code"
      pattern: "is_relative_to"
---

<objective>
Sweep the web layer (routes, LLM client + prompts, email gateway/clean, config, ALL three
provenance-bearing templates and the CSS hits) and the sixteen remaining small test files
clean of ticket-ID and process references under D-01/D-02/D-03/D-04 (COMM-01 per D-05 scope;
COMM-03 for module docstrings). Runs in wave 2 because app/routes/dashboard.py,
app/llm/client.py, tests/test_dashboard.py, and tests/test_llm_client.py receive plan
15-01's sanctioned fixes first — comments are rewritten once, in final code.

Purpose: completes the sweep surface so plan 15-10's guard can go green tree-wide. Includes
the one USER-VISIBLE provenance string: the demo-button label in run_detail.html.
Output: thirty-six comment-clean files, text-only diffs (plus the sanctioned label-copy
rewrite), suite/ruff/mypy green per commit.

Scope note (deliberate, checker-reviewed, re-affirmed after cross-AI review): 36 files
exceeds the usual per-plan file threshold. The Codex review re-raised this (MEDIUM);
disposition: ACCEPTED again rather than split, because the work is mechanical line-level
rewriting under a fixed rubric — no design decisions, no behavior edits — with a per-task
executable gate grep bounding each of the three file groups, and plan 15-10's tree-wide
straggler pass is the designed backstop for anything this sweep misses. To avoid growing
this plan further, the two test files the fresh manifest found unplanned
(tests/test_demo_landing.py, tests/test_multi_employee_delivery.py) were routed to plan
15-08 instead. The review's HIGH inventory finding (app/templates/eval.html omitted) is
fixed here: eval.html AND runs_list.html (a second omission caught by re-running the
manifest) are now in Task 2's file set.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-01-SUMMARY.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** constraint/invariant comments rewritten as plain
  English, ticket reference removed; pure provenance deleted.
- **D-02 money-relevant depth:** runs.py's approval-gate, CAS/claim, retrigger, and
  Content-Disposition sanitizer comments keep constraint + failure mode (e.g. the re.ASCII
  rationale explaining what header injection a permissive pattern would allow — keep it
  fully).
- **D-03:** phase numbers, review rounds, planning-doc citations (incl. UI-SPEC and "UAT #N"
  citations — UI-SPEC.md is a planning artifact), secondary IDs (OPS-, T-N-N, R2, R2-HIGH,
  Codex, "finding #N", severity IDs like HIGH-1/HIGH-1-AUTH/(R5)) all go. Requirement IDs
  (DASH-*, EMAIL-*, LLM-*, INGEST-*) are traceability — rewrite provenance prose around them,
  keep behavioral explanations. Where a UI-SPEC comment carries real design info (e.g. a
  color-role note), keep the info, drop the UI-SPEC label per D-01.
- **Fresh manifest first (review LOW finding):** hit counts quoted in this plan and
  15-RESEARCH.md are discussion-time sizing estimates ONLY — before editing each task's file
  group, regenerate a manifest with the task's full gate regex (including the extended
  vocabulary) over those files and work from the manifest, not the counts.
- **D-04 docstrings (COMM-03):** purpose statements; genuine invariants only (e.g.
  webhook.py's reply-routing-before-first-ingest rule; gateway.py's Message-ID threading
  anchor contract). No history.
- **Runtime and rendered strings are in scope:** the run_detail.html demo-button caption
  currently says the demo path is distinct from a numbered project phase — rewrite to
  "(demo only — production uses the real inbound webhook)". Pre-check whether any test pins
  the old caption text (grep tests/ for the old wording); if one does, update that expected
  string literal in the same commit — a copy-parity edit, not a behavior change.
- Do NOT alter LLM prompt payload semantics: prompt files are swept for comments/docstrings;
  a payload string is only touched if it embeds a ticket pattern, and the DeepSeek json-mode
  requirement (the word "json" + example shape in the prompt) must remain intact.
- templating.py: sweep comments only. Do NOT add a needs_clarification badge entry (the
  status no longer exists and a drift test enforces its absence) and do NOT remove the dead
  "computing" entry — behavior edits beyond plan 15-01's three fixes are out of scope; the
  disposition record in plan 15-10 documents both calls.
- NEVER delete or relocate `# noqa:` / `# type: ignore[code]` markers — rewrite reason text
  only.
- Text-only otherwise; rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Sweep app/routes/ (eight files)</name>
  <files>app/routes/runs.py, app/routes/pipeline_glue.py, app/routes/webhook.py, app/routes/demo.py, app/routes/health.py, app/routes/templating.py, app/routes/dashboard.py, app/routes/__init__.py</files>
  <read_first>
    - app/routes/runs.py (~42 hits; retrigger docstring is dense with decision/review IDs —
      keep the stale-claim guard and reply-context-loss caveats as plain invariants; the
      paystub filename sanitizer block keeps its full injection failure-mode narration)
    - app/routes/pipeline_glue.py (~21), app/routes/webhook.py (~16; reply-routing invariants
      keep depth), app/routes/dashboard.py (3 hits — sweep AFTER 15-01's containment fix,
      which this wave guarantees), app/routes/templating.py (3), app/routes/demo.py (6),
      app/routes/health.py (5), app/routes/__init__.py (1)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to the eight route modules. Rewrite module docstrings per D-04.
    Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -rnE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/routes/ && uv run ruff check app/routes/ && uv run pytest -q tests/test_hitl.py tests/test_webhook.py tests/test_dashboard.py</automated>
  </verify>
  <done>Zero gate-regex hits under app/routes/; route tests pass unchanged; text-only diffs.</done>
  <acceptance_criteria>
    - The gate grep returns no matches under app/routes/.
    - The sanitizer block in runs.py still explains the injection failure mode (source
      assertion: a comment near the filename sanitizer describes what unsanitized input
      could do to the header).
    - No executable-line changes in the commit diff(s).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep llm/, email/, config.py, all three provenance-bearing templates + CSS (incl. the user-visible label)</name>
  <files>app/llm/client.py, app/llm/prompts/extract.py, app/llm/prompts/clarify.py, app/llm/prompts/suggest.py, app/llm/prompts/__init__.py, app/email/gateway.py, app/email/clean.py, app/config.py, app/templates/run_detail.html, app/templates/eval.html, app/templates/runs_list.html, app/static/style.css</files>
  <read_first>
    - app/llm/client.py (~8 hits; sweep AFTER 15-01's scrub fix — keep the one-retry and
      DeepSeek empty-content rationale comments at full depth)
    - app/llm/prompts/*.py (comments only; never weaken the json-mode prompt requirements)
    - app/email/gateway.py (~18 hits; the durable References-chain contract keeps its
      failure-mode narration — losing the anchor breaks client reply threading; the
      send_outbound docstring and body also carry HIGH-1-AUTH/(R5)/HIGH-3/Pass-6 severity IDs
      the D-08 gate regex does NOT match — the extended gate below now forces them; the
      always-authenticated rationale for setting the API key in send_outbound is a real
      constraint: keep it, drop the IDs)
    - app/email/clean.py (~4), app/config.py (~3)
    - app/templates/run_detail.html (line ~108 user-visible demo caption + line ~176 Jinja
      comment on the provenance badge macro — the badge itself is DOMAIN vocabulary for
      live-vs-mock LLM provenance and stays; only the decision-ID citation goes; also UAT #
      comment citations at lines ~7 and ~246)
    - app/templates/eval.html (REVIEW HIGH FINDING — was omitted from this plan: UI-SPEC
      section comments at lines ~7 and ~29, and a "UAT #6"/UI-SPEC citation at line ~85;
      Jinja {# #} comments, rewrite to describe the section's purpose or delete)
    - app/templates/runs_list.html (second omission caught by the fresh manifest: "UAT #3/#4"
      poll-rationale comment at line ~3 — keep the meta-refresh-replacement rationale, drop
      the UAT citation — and a "UAT #6" comment at line ~84)
    - app/static/style.css (a decision-ID comment next to the needs_operator rule at ~289 and
      a "UAT #6" comment at ~736)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Open
      Question 4 resolution: the caption is rendered UI copy and must be rewritten)
  </read_first>
  <action>
    Apply the sweep_rubric to the twelve files (fresh manifest first — the extended gate
    regex below is the work list generator). Rewrite the run_detail.html demo caption to
    "(demo only — production uses the real inbound webhook)" after grepping tests/ for the
    old caption text (update any pinned expected literal in the same commit). Comment-only
    commit(s) plus the sanctioned caption-copy commit. Addresses review concerns: sweep
    inventory omitted eval.html (HIGH); per-plan gates narrower than D-03 vocabulary (MEDIUM).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/llm/client.py app/llm/prompts/extract.py app/llm/prompts/clarify.py app/llm/prompts/suggest.py app/llm/prompts/__init__.py app/email/gateway.py app/email/clean.py app/config.py app/templates/run_detail.html app/templates/eval.html app/templates/runs_list.html app/static/style.css && uv run pytest -q tests/test_llm_client.py tests/test_gateway.py tests/test_extract.py && uv run ruff check</automated>
  </verify>
  <done>
    Zero extended-gate-regex hits across the twelve files; the rendered demo caption is
    phase-free; client/gateway/extract tests pass unchanged.
  </done>
  <acceptance_criteria>
    - The extended gate grep (D-08 subset + UI-SPEC/UAT #/Codex/HIGH-N/finding #/R2-HIGH)
      returns no matches for the twelve files.
    - app/templates/run_detail.html contains "production uses the real inbound webhook"
      and no numbered-phase reference (source assertion).
    - Prompt payload strings still satisfy the json-mode requirement (the word "json"/"JSON"
      present where it was before — source assertion).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep the sixteen remaining test files + full gates</name>
  <files>tests/test_dashboard.py, tests/test_llm_client.py, tests/test_stuck_run_recovery.py, tests/test_combined_context.py, tests/test_webhook.py, tests/test_orchestrator_states.py, tests/test_demo_fixtures.py, tests/test_suggest.py, tests/test_extract.py, tests/test_claim_status.py, tests/test_webhook_dedup_race.py, tests/test_decide_field_regression.py, tests/test_bootstrap_safe_url.py, tests/test_schema_introspect.py, tests/test_pdf.py, tests/test_compose_confirmation.py</files>
  <read_first>
    - tests/test_dashboard.py (~16 hits; includes 15-01's new traversal test, already clean)
      and tests/test_llm_client.py (~3 hits; includes 15-01's new scrub test)
    - The fourteen other small files (~105 hits combined)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to comments, docstrings, and failure-message strings in the
    sixteen files (residual ticket-prefixed names, if discovered, are renamed per D-06 with
    collect-count neutrality). Then run the full plan gates: complete suite, ruff, strict
    mypy.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' tests/test_dashboard.py tests/test_llm_client.py tests/test_stuck_run_recovery.py tests/test_combined_context.py tests/test_webhook.py tests/test_orchestrator_states.py tests/test_demo_fixtures.py tests/test_suggest.py tests/test_extract.py tests/test_claim_status.py tests/test_webhook_dedup_race.py tests/test_decide_field_regression.py tests/test_bootstrap_safe_url.py tests/test_schema_introspect.py tests/test_pdf.py tests/test_compose_confirmation.py && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>Zero gate-regex hits in the sixteen files; full suite + ruff + strict mypy green.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the sixteen files.
    - `uv run pytest -q` exits 0 with no assertion-logic changes in the plan's diffs.
    - `uv run mypy` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text edits -> web/LLM/email behavior | sweep must not alter route behavior, prompt payload semantics, or threading contracts |
| rendered template copy -> demo audience | the caption rewrite is the one sanctioned rendered-copy change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | web-layer sweep altering behavior | medium | mitigate | text-only rule; prompt json-mode requirement pinned by source assertion; full suite + ruff + mypy green per commit |
| T-15-06 | Tampering | Content-Disposition header injection (verified-fixed) | high | mitigate | keep the sanitizer's failure-mode comment at full depth so the mitigation is not "simplified" away later; regression test already pins the property |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Extended gate grep clean over all thirty-six files.
- `uv run pytest -q && uv run ruff check && uv run mypy` green at plan end.
- Rendered demo caption phase-free.
</verification>

<success_criteria>
- The entire web layer and remaining tests read ticket-free; the only user-visible provenance
  string in the app is gone.
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — comment/docstring/string text edits only in the thirty-six listed files.
- Changed rendered copy: run_detail.html demo caption becomes
  "(demo only — production uses the real inbound webhook)".

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH -->
<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->
<!-- planner-discipline-allow: UI-SPEC -->
<!-- planner-discipline-allow: UAT # -->
<!-- planner-discipline-allow: Codex -->
<!-- planner-discipline-allow: HIGH-[0-9] -->
<!-- planner-discipline-allow: finding # -->
<!-- planner-discipline-allow: R2-HIGH -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-09-SUMMARY.md` when done.
</output>
