---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/pipeline/orchestrator.py
  - app/pipeline/clarification.py
  - app/pipeline/delivery.py
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "orchestrator.py, clarification.py, and delivery.py carry zero ticket-ID/provenance references"
    - "Every surviving comment documents a constraint, invariant, or failure mode in plain maintainer-facing English"
    - "Money-relevant comments keep full constraint-plus-failure-mode depth"
  artifacts:
    - path: "app/pipeline/orchestrator.py"
      provides: "provenance-free pipeline orchestration with constraint comments intact"
    - path: "app/pipeline/clarification.py"
      provides: "provenance-free clarification stage"
    - path: "app/pipeline/delivery.py"
      provides: "provenance-free delivery stage at D-02 money-path depth"
  key_links:
    - from: "app/pipeline/delivery.py"
      to: "mypy strict gate"
      via: "type: ignore[code] markers preserved in place with rewritten plain-English reasons"
      pattern: "type: ignore"
---

<objective>
Sweep the three heaviest pipeline files — orchestrator.py (110 hits), clarification.py (46),
delivery.py (31) — clean of ticket-ID and project-process references under D-01/D-02/D-03/D-04,
preserving the codebase's rich constraint documentation as plain maintainer-facing English
(COMM-01 per D-05 scope; COMM-03 for module docstrings).

Purpose: these files hold the pipeline's money-adjacent gate logic — their comments must
teach invariants ("collisions always clarify — resolving here would guess with money"), not
recount review rounds. Behavior-neutrality is proven by the untouched 666-test suite plus
ruff and strict mypy staying green at every commit.
Output: three comment-clean files, text-only diffs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** a ticket comment that documents a real
  constraint/invariant/edge case is rewritten as plain maintainer-facing English with the
  ticket reference removed. Pure provenance (who/when/which review/where code migrated from)
  is deleted entirely.
- **D-02 money-path depth (delivery.py is a NAMED money-path file; orchestrator/clarification
  gate comments are money-relevant):** surviving comments keep BOTH the constraint AND the
  failure mode — e.g. a comment explaining that clearing reply context in only one branch
  would strand a round's clarified fields keeps that consequence narration verbatim in
  spirit. Never trim it.
- **D-03 all process references go:** phase numbers, review rounds, planning-doc citations
  (incl. UI-SPEC / "UAT #N"), split history ("carved out of", "migrated from"), secondary IDs
  (OPS-, T-N-N, R2 rounds, Codex findings, "finding #N", HIGH-N/R2-HIGH severity IDs —
  clarification.py and delivery.py carry many of these outside the base gate regex). External
  authoritative citations STAY (IRS/SSA URLs, provider API docs).
- **Fresh manifest first (review LOW finding):** hit counts in this plan and 15-RESEARCH.md
  are discussion-time sizing estimates ONLY — before editing, regenerate a manifest with the
  full gate regex (including the extended vocabulary) over this plan's file set and work from
  the manifest, not the counts.
- **D-04 docstrings (COMM-03):** module docstring = 1–2 sentence purpose statement; add a
  short invariants paragraph only where genuine (e.g. the orchestrator's "final_action is the
  sole branch source; this module never branches on model output"). No history, no TOC.
- Requirement IDs (CLAR-*, INGEST-*, DEMO-*, …) are NOT guard-blocked; rewrite the prose
  around them where it reads as provenance, keep the behavioral explanation.
- NEVER delete or relocate a `# noqa: CODE` or `# type: ignore[code]` marker — rewrite only
  its reason text in plain English (mypy warn_unused_ignores is on; delivery.py carries a
  known ignore whose rationale must survive as plain English).
- Comment/docstring/string-literal text only: no code moves, no renames, no structural edits,
  no monkeypatch-seam changes (tests patch module attributes — structure is load-bearing).
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Sweep app/pipeline/orchestrator.py</name>
  <files>app/pipeline/orchestrator.py</files>
  <read_first>
    - app/pipeline/orchestrator.py (whole file — enumerate hits at execution time with the
      gate regex; ~110 lines match)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Ticket-Comment
      Inventory; Pitfall 6 rubric-drift warning)
  </read_first>
  <action>
    Apply the sweep_rubric line-by-line. This file's comments encode the pipeline's hardest-won
    invariants (CAS claim discipline, status-writer rules, reply-context clearing, resume
    re-extraction over original + reply bodies, live-vs-mock logging) — each surviving comment
    must state the invariant and its failure mode with no ticket vocabulary. Pure provenance
    lines (which review found it, which fix round changed it) are deleted. Rewrite the module
    docstring per D-04: purpose + the genuine invariants (deterministic gating; the pipeline
    never branches on model output). Commit as a single comment-only commit for this file.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/pipeline/orchestrator.py && uv run ruff check app/pipeline/orchestrator.py</automated>
  </verify>
  <done>Zero gate-regex hits in orchestrator.py; ruff green; diff is comment/docstring text only.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for app/pipeline/orchestrator.py.
    - `git diff` for the commit shows no changes outside comment/docstring lines (no
      executable-line edits).
    - Module docstring states purpose + invariants with no phase/review references.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep app/pipeline/clarification.py</name>
  <files>app/pipeline/clarification.py</files>
  <read_first>
    - app/pipeline/clarification.py (whole file; ~46 hits)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric. Clarification-round machinery comments (round/epoch semantics,
    suggestion-call isolation — the suggested employee id must never leak into the persisted
    Decision, template-floor fallback for draft failures) keep full constraint + failure-mode
    depth; ticket and review-round citations go. Rewrite the module docstring per D-04.
    Single comment-only commit.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/pipeline/clarification.py && uv run ruff check app/pipeline/clarification.py</automated>
  </verify>
  <done>Zero gate-regex hits in clarification.py; ruff green; text-only diff.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for app/pipeline/clarification.py.
    - No executable-line changes in the commit diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep app/pipeline/delivery.py (money-path depth) + full gates</name>
  <files>app/pipeline/delivery.py</files>
  <read_first>
    - app/pipeline/delivery.py (whole file; ~31 hits; contains a `# noqa: BLE001` with a
      ticket-labeled reason and a `# type: ignore` carrying a sanitizer rationale — both
      markers stay in place, reasons rewritten in plain English)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (non-comment
      hits list; D-02 money-path file list)
  </read_first>
  <action>
    Apply the sweep_rubric at FULL D-02 depth — delivery.py is a named money-path file. Every
    surviving comment about finalize/persist ordering, threading anchors, paystub line-item
    sourcing, or exception isolation keeps its consequence narration (what mispay or strand
    the rule prevents). Rewrite the noqa and type-ignore reason text in plain English without
    moving either marker off its line. Rewrite the module docstring per D-04. After this
    commit, run the full plan gates: complete suite, ruff, strict mypy — mypy is the proof no
    ignore marker was orphaned or relocated.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH' app/pipeline/delivery.py && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>
    Zero gate-regex hits in delivery.py; noqa/type-ignore markers intact with plain-English
    reasons; full suite + ruff + strict mypy green.
  </done>
  <acceptance_criteria>
    - The gate grep returns no matches for app/pipeline/delivery.py.
    - `grep -c 'noqa' app/pipeline/delivery.py` count is unchanged from before the sweep, and
      the same for 'type: ignore' (markers preserved, only reason text rewritten).
    - `uv run pytest -q` exits 0 with no assertion changes anywhere.
    - `uv run mypy` exits 0 (warn_unused_ignores proves no ignore was stranded).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text edits -> pipeline behavior | comment-only sweep of money-adjacent gate code must not alter behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | pipeline sweep altering behavior | medium | mitigate | text-only diffs; unchanged 666-test suite + ruff + strict mypy green at every commit; noqa/ignore markers pinned in place |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Gate grep clean over all three files.
- `uv run pytest -q && uv run ruff check && uv run mypy` green at plan end.
- Commit diffs contain no executable-line changes.
</verification>

<success_criteria>
- The three heaviest pipeline files read as constraint-documented production code with zero
  process history.
- Money-relevant comments retain constraint + failure-mode depth (D-02).
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — comment/docstring text edits only in the three listed files.

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|UI-SPEC|UAT #|Codex|HIGH-[0-9]|finding #|R2-HIGH -->
<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->
<!-- planner-discipline-allow: UI-SPEC -->
<!-- planner-discipline-allow: UAT # -->
<!-- planner-discipline-allow: Codex -->
<!-- planner-discipline-allow: HIGH-[0-9] -->
<!-- planner-discipline-allow: finding # -->
<!-- planner-discipline-allow: R2-HIGH -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-03-SUMMARY.md` when done.
</output>
