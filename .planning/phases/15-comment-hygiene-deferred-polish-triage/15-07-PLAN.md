---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_alias_write.py
  - tests/test_persistence.py
  - tests/test_federal_withholding.py
  - tests/test_models_contracts.py
  - tests/test_needs_operator.py
  - tests/test_calculate.py
  - tests/test_clarify_rounds.py
  - tests/test_seed_roundtrip.py
  - tests/test_detect_field_regression.py
  - tests/test_alias_full_loop.py
  - tests/test_tax_tables_2026.py
autonomous: true
requirements: [COMM-01, COMM-03]

must_haves:
  truths:
    - "conftest.py and these eleven test files carry zero ticket/provenance references"
    - "Assertion-message runtime strings embedding ticket IDs are rewritten to plain-English invariant explanations"
    - "No assertion logic changed anywhere — same tests, same results"
  artifacts:
    - path: "tests/conftest.py"
      provides: "provenance-free shared fixtures with seam documentation intact"
    - path: "tests/test_calculate.py"
      provides: "ticket-free assertion messages (including the source-scan guard message)"
  key_links:
    - from: "tests/conftest.py"
      to: "monkeypatch seam docs"
      via: "fixture docstrings keep the module-attribute seam explanations tests depend on"
      pattern: "monkeypatch"
---

<objective>
Sweep tests/conftest.py and eleven test files (the calculation/persistence/alias cluster,
~394 matching lines) clean of ticket-ID and process references under D-01/D-03 (COMM-01 per
D-05 scope; COMM-03 for module docstrings). Comments, docstrings, and failure-message
strings only — zero assertion changes.

Purpose: these tests guard the money engine and the alias-learning loop; their docstrings and
failure messages must teach the invariant they lock, not cite the review that demanded them.
Output: twelve comment-clean test files, suite passing unchanged.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** constraint/invariant comments rewritten as plain
  English, ticket reference removed; pure provenance deleted.
- **D-02 money-relevant depth:** withholding/calculate/tax-table test comments explaining
  which IRS worked example or which mispay a case pins keep full constraint + failure-mode
  depth; IRS/SSA citation URLs STAY (external sources).
- **D-03:** phase numbers, review rounds, planning-doc citations, secondary IDs (OPS-, T-N-N,
  R2, Codex, "finding #N") all go. Requirement IDs (CALC-*, CLAR-*, …) are not guard-blocked;
  rewrite provenance prose around them, keep behavioral explanations.
- **Failure-message strings are in scope:** assert messages embedding ticket IDs are runtime
  strings the phase guard will scan — rewrite to plain-English invariant statements.
- Test function names: any residual ticket-prefixed name found in THESE files is renamed per
  D-06 (unique names, count-neutral) — the known concentrations live in plan 15-06's files,
  but sweep-time discoveries here are handled the same way.
- NEVER delete or relocate `# noqa:` / `# type: ignore[code]` markers — rewrite reason text
  only (warn_unused_ignores is on).
- No fixture/seam changes: conftest.py's fake_repo/mock_llm structure is load-bearing for
  the whole suite — text-only edits.
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Sweep conftest.py, test_alias_write.py, test_persistence.py</name>
  <files>tests/conftest.py, tests/test_alias_write.py, tests/test_persistence.py</files>
  <read_first>
    - tests/conftest.py (~60 hits; fixture docstrings documenting the fake_repo contract and
      LLM stubbing keep their seam explanations — they are real constraints)
    - tests/test_alias_write.py (~66 hits; alias write-side invariants are money-relevant)
    - tests/test_persistence.py (~49 hits)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to comments, docstrings, and failure-message strings in the three
    files. conftest.py's explanations of WHY seams are module-attribute monkeypatches and why
    LLM calls must stay stubbed (live keys in .env) survive as plain constraints. Comment-only
    commit(s).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' tests/conftest.py tests/test_alias_write.py tests/test_persistence.py && uv run pytest -q tests/test_alias_write.py tests/test_persistence.py</automated>
  </verify>
  <done>Zero gate-regex hits in the three files; their tests pass unchanged.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the three files.
    - No assert-statement or fixture-structure changes in the commit diff.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep test_federal_withholding.py, test_models_contracts.py, test_needs_operator.py, test_calculate.py</name>
  <files>tests/test_federal_withholding.py, tests/test_models_contracts.py, tests/test_needs_operator.py, tests/test_calculate.py</files>
  <read_first>
    - tests/test_federal_withholding.py (~42 hits; IRS worked-example citations STAY)
    - tests/test_models_contracts.py (~41 hits), tests/test_needs_operator.py (~32 hits)
    - tests/test_calculate.py (~24 hits; contains an assertion MESSAGE string labeling a
      source-scan guard with a ticket prefix — the message is a runtime string: rewrite it to
      state the invariant plainly, e.g. that a bare assert must not appear in the calculation
      module source; keep the assertion logic identical)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (non-comment
      hits list names the test_calculate.py message strings explicitly)
  </read_first>
  <action>
    Apply the sweep_rubric to the four files. Table-driven withholding tests keep their IRS
    PDF references and per-status worked-example notes (external sources, D-02 depth);
    ticket labels go. Rewrite the test_calculate.py guard-assert messages as plain-English
    invariant statements without changing what they scan or assert. Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' tests/test_federal_withholding.py tests/test_models_contracts.py tests/test_needs_operator.py tests/test_calculate.py && uv run pytest -q tests/test_federal_withholding.py tests/test_models_contracts.py tests/test_needs_operator.py tests/test_calculate.py</automated>
  </verify>
  <done>Zero gate-regex hits in the four files; their tests pass unchanged.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the four files.
    - tests/test_federal_withholding.py still references the IRS source (external citation
      preserved — source assertion).
    - No assert-logic changes in the commit diff (message strings only).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep the remaining five files + full gates</name>
  <files>tests/test_clarify_rounds.py, tests/test_seed_roundtrip.py, tests/test_detect_field_regression.py, tests/test_alias_full_loop.py, tests/test_tax_tables_2026.py</files>
  <read_first>
    - The five files (~80 hits combined; tax-table test comments keep IRS citations)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric to the five files, then run the full plan gates: complete suite,
    ruff, strict mypy.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' tests/test_clarify_rounds.py tests/test_seed_roundtrip.py tests/test_detect_field_regression.py tests/test_alias_full_loop.py tests/test_tax_tables_2026.py && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>Zero gate-regex hits in the five files; full suite + ruff + strict mypy green.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the five files.
    - `uv run pytest -q` exits 0 with no assertion changes anywhere in the plan's diffs.
    - `uv run mypy` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| text edits -> suite semantics | failure-message rewrites must not alter assertion logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | test sweep altering suite behavior | low | mitigate | message-strings-only rule; unchanged pass count; full suite + ruff + mypy green per commit |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Gate grep clean over all twelve files.
- `uv run pytest -q && uv run ruff check && uv run mypy` green at plan end.
</verification>

<success_criteria>
- The calculation/persistence/alias test cluster and shared fixtures read ticket-free with
  invariant-teaching failure messages.
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — comment/docstring/message-string text edits only in the twelve listed
  files.

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-07-SUMMARY.md` when done.
</output>
