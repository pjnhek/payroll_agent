---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/repo/__init__.py
  - app/db/repo/_shared.py
  - app/db/repo/runs.py
  - app/db/repo/emails.py
  - app/db/repo/roster.py
  - app/db/repo/pipeline_state.py
  - app/db/repo/demo.py
  - app/db/seed.py
  - app/db/bootstrap.py
  - app/db/supabase.py
  - app/db/schema.sql
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03]

must_haves:
  truths:
    - "Every split DB module carries a real module-purpose docstring (plus invariants where genuine) with no TOC style and no split/provenance history"
    - "app/db/ and schema.sql carry zero ticket-ID/process references; surviving comments document constraints"
    - "schema.sql edits are comment-only and provably parity-safe (no schema push needed)"
  artifacts:
    - path: "app/db/repo/_shared.py"
      provides: "module docstring keeping the parameterized-SQL-only invariant and the connection monkeypatch-seam explanation"
    - path: "app/db/repo/emails.py"
      provides: "insert_email_message docstring keeping the (run_id, purpose, round, epoch) arbiter invariant at full depth, ticket-free"
    - path: "app/db/schema.sql"
      provides: "comment-clean schema (comment-only diff)"
  key_links:
    - from: "app/db/schema.sql"
      to: "app/db/schema_introspect.py"
      via: "introspection strips SQL line comments before parsing — comment edits cannot cause drift"
      pattern: "_strip_line_comments"
---

<objective>
Sweep the DB layer clean of ticket/provenance references and complete COMM-02: the split
repo/ package modules get their real module-purpose docstrings now (per D-04 and Phase 13's
placeholder handoff), replacing draft docstrings that still cite planning docs and split
history. Also sweep seed/bootstrap/supabase and the 50 ticket-comment lines in schema.sql
(COMM-01 per D-05; COMM-03 for docstrings).

Purpose: repo/ is the transactional heart of the app — its docstrings must state purpose and
the SQL-discipline/seam invariants that tests depend on being documented accurately.
Output: eleven comment-clean files. schema.sql edits are `--` comment lines only, which the
schema-parity introspection strips before parsing (verified in 15-RESEARCH.md against
app/db/schema_introspect.py) — NO database schema push is required by this plan.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** constraint/invariant/edge-case comments are rewritten
  as plain maintainer-facing English, ticket reference removed; pure provenance deleted.
- **D-02 money-path depth:** emails.py's upsert-arbiter block is money-path adjacent — keep
  the arbiter/constraint-drift invariant and the epoch rationale at full constraint-plus-
  failure-mode depth (what strands or overwrites if the arbiter and the unique constraint
  drift apart).
- **D-03 all process references go:** phase numbers, review rounds, planning-doc citations
  (PATTERNS.md / RESEARCH / research-doc open questions), split history ("post-split",
  "migrated from"), secondary IDs (OPS-, T-N-N, R2, Codex, "finding #N"). External
  authoritative citations STAY (Supabase/psycopg docs URLs if present).
- **D-04 docstrings (COMM-02/COMM-03):** 1–2 sentence purpose statement per module; genuine
  invariants get a short paragraph — _shared.py's "parameterized SQL only (%s / named
  placeholders), NEVER f-string SQL"; repo/__init__.py's facade seam caveat (a facade-level
  monkeypatch does not intercept same-module internal calls — tests depend on this being
  documented accurately). No history, no TOC-style function indexes anywhere.
- Requirement IDs are NOT guard-blocked; rewrite surrounding provenance prose, keep behavior
  explanations.
- NEVER delete or relocate `# noqa:` / `# type: ignore[code]` markers — rewrite reason text
  only (warn_unused_ignores is on).
- Text-only edits: no code moves, no renames, no structural changes.
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: COMM-02 — rewrite the split repo/ package docstrings and sweep its comments</name>
  <files>app/db/repo/__init__.py, app/db/repo/_shared.py, app/db/repo/runs.py, app/db/repo/emails.py, app/db/repo/roster.py, app/db/repo/pipeline_state.py, app/db/repo/demo.py</files>
  <read_first>
    - app/db/repo/_shared.py (strip the planning-doc citation from the docstring; KEEP the
      parameterized-SQL invariant and _conn_ctx's package-attribute monkeypatch-seam
      explanation — it is a real constraint tests rely on)
    - app/db/repo/runs.py (~39 hits; the RUN_COLS block comment becomes "a column missing
      from this constant is invisible to every load_run caller" — constraint kept, IDs dropped)
    - app/db/repo/emails.py (~34 hits; insert_email_message docstring keeps the
      (run_id, purpose, round, epoch) arbiter + epoch rationale at D-02 depth, drops IDs)
    - app/db/repo/pipeline_state.py (~25 hits; persist_extracted docstring drops its review
      parenthetical), app/db/repo/demo.py (load_all_runs keeps the explicit-column and
      jsonb_typeof-guard rationale — why a bare COALESCE errors — drops IDs),
      app/db/repo/roster.py, app/db/repo/__init__.py
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Module
      Docstring Survey table — the per-module strip/keep calls are enumerated there)
  </read_first>
  <action>
    Apply the sweep_rubric to all seven repo/ modules, following the per-module strip/keep
    guidance in 15-RESEARCH.md's Module Docstring Survey. This completes COMM-02: each module
    ends with a real 1–2 sentence purpose docstring (no TOC-style function index anywhere in
    the package) plus invariants paragraphs only in _shared.py (SQL discipline + seam) and
    __init__.py (facade seam caveat). emails.py's arbiter documentation keeps full failure-
    mode depth. Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -rnE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|post-split' app/db/repo/ && uv run ruff check app/db/repo/ && uv run pytest -q tests/test_persistence.py</automated>
  </verify>
  <done>
    Zero gate-regex hits under app/db/repo/ (including "post-split"); every module has a
    purpose docstring; the two genuine-invariant paragraphs survive; persistence tests green.
  </done>
  <acceptance_criteria>
    - The gate grep returns no matches under app/db/repo/.
    - _shared.py docstring still contains the words "f-string" (SQL-discipline invariant kept)
      and no planning-doc citation.
    - __init__.py docstring still documents the facade monkeypatch-seam caveat.
    - No executable-line changes in the commit diff(s).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Sweep seed.py, bootstrap.py, supabase.py</name>
  <files>app/db/seed.py, app/db/bootstrap.py, app/db/supabase.py</files>
  <read_first>
    - app/db/seed.py (~24 hits; straddle-seeding rationale is money-relevant — keep depth)
    - app/db/bootstrap.py (~9 hits)
    - app/db/supabase.py (~4 hits; the double-checked-locking rationale and the
      prepare_threshold/Supavisor pooler explanation are genuinely load-bearing — keep the
      substance, drop the ticket labels and research-doc citations)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
  </read_first>
  <action>
    Apply the sweep_rubric. supabase.py keeps the full why of double-checked locking (two
    threads racing module import would each build a pool; the lock plus re-check prevents the
    second) and the pooler/prepared-statement gotcha — with zero ticket vocabulary. seed.py's
    fixed-UUID and wage-straddle explanations keep their failure modes. Rewrite module
    docstrings per D-04. Comment-only commit(s).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|RESEARCH' app/db/seed.py app/db/bootstrap.py app/db/supabase.py && uv run ruff check app/db/</automated>
  </verify>
  <done>Zero gate-regex hits across the three files; ruff green; text-only diffs.</done>
  <acceptance_criteria>
    - The gate grep returns no matches for the three files.
    - supabase.py still documents the lock + re-check rationale (source assertion: a comment
      or docstring explains why the second check under the lock exists).
    - No executable-line changes in the commit diff(s).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Sweep app/db/schema.sql comments (parity-safe, no schema push) + full gates</name>
  <files>app/db/schema.sql</files>
  <read_first>
    - app/db/schema.sql (50 hits, all in `--` line comments citing decision/review IDs;
      constraint comments — e.g. why the outbound unique index arbitrates on
      (run_id, purpose, round, epoch), why a status CHECK exists — keep their failure modes)
    - app/db/schema_introspect.py (the _strip_line_comments helper: SQL `--` comments are
      stripped before parsing, so comment-only schema.sql edits cannot affect the
      schema-parity check or deploy-migrate CI — this is why NO schema push task exists in
      this plan)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Runtime
      State Inventory: live-service config unaffected; deploy-migrate re-applies schema
      idempotently, comment-only DDL diffs are harmless)
  </read_first>
  <action>
    Apply the sweep_rubric to every `--` comment in schema.sql. Touch ONLY comment lines —
    zero DDL changes (the diff must show no non-comment line modified). Constraint-adjacent
    comments (unique-index arbitration semantics, status CHECK rationale, JSONB column
    purpose) keep constraint + failure-mode depth per D-02 where money-relevant. Then run the
    full plan gates including the schema-introspection test, which proves the parsed schema
    is byte-identical to before the sweep.
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' app/db/schema.sql && uv run pytest -q tests/test_schema_introspect.py && uv run pytest -q && uv run ruff check && uv run mypy</automated>
  </verify>
  <done>
    Zero gate-regex hits in schema.sql; the diff contains only `--` comment-line changes;
    schema-introspection test green (parity-safety proof); full suite + ruff + mypy green.
  </done>
  <acceptance_criteria>
    - The gate grep returns no matches for app/db/schema.sql.
    - `git diff` for the schema.sql commit shows changes only on lines that are `--` comments
      (source assertion — no DDL token modified).
    - `uv run pytest -q tests/test_schema_introspect.py` exits 0.
    - `uv run pytest -q` exits 0 repo-wide.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema.sql text -> live DB parity | comment edits must not change the parsed schema surface |
| text edits -> transactional state machine | repo/ sweep must not alter SQL or transaction behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | DB-layer sweep altering behavior | high | mitigate | text-only diffs; schema.sql restricted to `--` comment lines with introspection-test proof; full suite + ruff + strict mypy green per commit |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
No schema push required: comment-only schema.sql edits are stripped by the parity
introspection before parsing (verified against app/db/schema_introspect.py in 15-RESEARCH.md).
</threat_model>

<verification>
- Gate grep clean over all eleven files.
- schema.sql diff is comment-lines-only; tests/test_schema_introspect.py green.
- `uv run pytest -q && uv run ruff check && uv run mypy` green at plan end.
</verification>

<success_criteria>
- COMM-02 complete: split DB modules carry real purpose docstrings, zero TOC style, zero
  split history.
- DB layer and schema comment-clean with constraint documentation intact.
</success_criteria>

## Artifacts this phase produces (this plan)

- No new symbols — comment/docstring text edits only in the eleven listed files.

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|post-split -->
<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md|RESEARCH -->
<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->
<!-- planner-discipline-allow: post-split -->
<!-- planner-discipline-allow: RESEARCH -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-05-SUMMARY.md` when done.
</output>
