---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - eval/run_eval.py
  - eval/judge.py
  - eval/fixtures/DIVERGENCES.md
  - scripts/show_confirmation_subject.py
  - scripts/demo_reset.py
  - scripts/reset_stuck_runs.py
  - eval/fixtures/10_multi_employee_coastal.json
  - eval/summary.json
  - eval/chart.svg
  - .planning/todos/pending/260623-05-fixture-category-label-mismatch.md
  - .planning/todos/completed/260623-05-fixture-category-label-mismatch.md
autonomous: true
requirements: [COMM-01, COMM-03, POLISH-02]

must_haves:
  truths:
    - "eval/ and scripts/ sources carry no ticket-ID or project-process references; surviving comments document real constraints in plain English"
    - "Fixture 10 is labeled fixture_category 'typo' and the regenerated eval artifacts pass the regression gate hermetically"
    - "Todo 260623-05 is closed with a resolution record"
  artifacts:
    - path: "eval/fixtures/10_multi_employee_coastal.json"
      provides: "corrected fixture_category label"
      contains: "\"typo\""
    - path: "eval/summary.json"
      provides: "regenerated aggregates with fixture 10 grouped under typo"
    - path: "eval/chart.svg"
      provides: "regenerated per-category chart"
  key_links:
    - from: "eval/fixtures/10_multi_employee_coastal.json"
      to: "eval/summary.json"
      via: "uv run python eval/run_eval.py regeneration from committed extraction caches"
      pattern: "fixture_category"
    - from: "eval/summary.json"
      to: "eval/chart.svg"
      via: "uv run python eval/run_eval.py --chart"
---

<objective>
Sweep eval/ and scripts/ clean of ticket/provenance references under the D-01/D-02/D-03/D-04
rubric (COMM-01 widened to the whole codebase per D-05; module docstrings per COMM-03), then
close POLISH-02: relabel fixture 10's fixture_category to "typo" per D-13, regenerate the
committed eval artifacts hermetically, and close todo 260623-05.

Purpose: the eval harness is a headline artifact for the hiring-manager audience — its code
must read as constraint-documenting, and its category chart must group fixtures honestly.
Output: clean eval/scripts sources, relabeled fixture + regenerated summary.json/chart.svg
committed atomically, todo 260623-05 moved to completed/.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
@.planning/todos/pending/260623-05-fixture-category-label-mismatch.md
</context>

<sweep_rubric>
Restated from CONTEXT.md — apply per comment, no drift (15-RESEARCH Pitfall 6):

- **D-01 keep constraint, drop label:** a ticket comment that documents a real
  constraint/invariant/edge case is rewritten as plain maintainer-facing English with the
  ticket reference removed. Pure provenance (who/when/which review/where code migrated from)
  is deleted entirely.
- **D-02 money-path depth:** money-relevant comments keep BOTH the constraint AND the failure
  mode (consequence narration is what prevents a future "simplification" from reintroducing a
  mispay). Never trim it.
- **D-03 all process references go:** phase numbers, review rounds, planning-doc citations
  (PATTERNS.md / RESEARCH docs), split history, secondary IDs (OPS-, T-N-N task IDs, R2
  rounds, Codex findings, "finding #N"). A comment must stand alone for a maintainer who has
  never seen .planning/. External authoritative citations STAY (IRS Pub 15-T PDF URL, SSA
  wage-base URL, provider API docs).
- **D-04 docstrings:** 1–2 sentence purpose statement; a short invariants paragraph only where
  the module has genuine invariants. No history, no TOC-style function indexes.
- Requirement IDs (CALC-03, EVAL-*, DASH-01, BOUND-01, …) are NOT guard-blocked, but rewrite
  surrounding prose where it reads as provenance; keep the behavioral explanation.
- NEVER delete or relocate a `# noqa: CODE` or `# type: ignore[code]` marker — rewrite only
  its reason text in plain English (mypy warn_unused_ignores is on).
- Comment/docstring/string-literal text only: no code moves, no renames, no structural edits.
  Runtime strings containing ticket patterns (argparse descriptions, assertion messages) ARE
  in scope — rewrite the text, keep semantics.
- Rewritten lines respect ruff line-length 100 (E501).
</sweep_rubric>

<tasks>

<task type="auto">
  <name>Task 1: Sweep eval/ and scripts/ comments, docstrings, and runtime strings</name>
  <files>eval/run_eval.py, eval/judge.py, eval/fixtures/DIVERGENCES.md, scripts/show_confirmation_subject.py, scripts/demo_reset.py, scripts/reset_stuck_runs.py</files>
  <read_first>
    - eval/run_eval.py (~40 hits: header is a clean purpose statement to keep; body has
      chart-annotation and label comments citing decision IDs; line ~1083 argparse description
      is a RUNTIME string ending in a phase reference — rewrite to "Payroll Agent eval scorer")
    - eval/judge.py (11 hits)
    - eval/fixtures/DIVERGENCES.md (3 decision-ID metric citations — maintainer-facing
      markdown in the code tree, in scope per D-05)
    - scripts/show_confirmation_subject.py, scripts/demo_reset.py, scripts/reset_stuck_runs.py
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Ticket-Comment
      Inventory + Module Docstring Survey rows for these files)
  </read_first>
  <action>
    Apply the sweep_rubric to every matching line in the six files (re-grep at execution time
    with the gate regex below to enumerate — counts shift as other plans land). Per D-04 /
    COMM-03, ensure each module's docstring states purpose (and genuine invariants only).
    The argparse description in eval/run_eval.py is a runtime string: drop the phase suffix,
    keep the tool description. Chart/label comments that explain WHY a label is worded a
    certain way keep the reason, lose the decision-ID citation. DIVERGENCES.md keeps its
    divergence explanations (they are real eval-honesty documentation) minus the decision-ID
    parentheticals. Do NOT change any string that is scored or written into summary.json;
    after the sweep run the eval regression gate to prove output-neutrality. Commit as a
    comment-only commit (separate from Task 2's data commit).
  </action>
  <verify>
    <automated>! grep -nE 'D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md' eval/run_eval.py eval/judge.py eval/fixtures/DIVERGENCES.md scripts/show_confirmation_subject.py scripts/demo_reset.py scripts/reset_stuck_runs.py && uv run python eval/run_eval.py --check && uv run ruff check eval/ scripts/ && uv run mypy && uv run pytest -q tests/test_eval_wiring.py</automated>
  </verify>
  <done>
    Zero gate-regex hits across the six files; `uv run python eval/run_eval.py --check` exits
    0 against the still-committed summary.json (proof the sweep changed no scored output);
    ruff + mypy green.
  </done>
  <acceptance_criteria>
    - The gate grep above returns no matches (exit 1, negated to success).
    - `uv run python eval/run_eval.py --check` exits 0 BEFORE Task 2's relabel (behavior
      neutrality of the sweep itself).
    - eval/run_eval.py argparse description no longer contains a phase reference (source
      assertion) but still describes the scorer.
    - The commit for this task touches only the six listed files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: POLISH-02 — relabel fixture 10 to "typo", regenerate artifacts atomically, close todo 260623-05 (D-13)</name>
  <files>eval/fixtures/10_multi_employee_coastal.json, eval/summary.json, eval/chart.svg, .planning/todos/pending/260623-05-fixture-category-label-mismatch.md</files>
  <read_first>
    - eval/fixtures/10_multi_employee_coastal.json (current "fixture_category": "exact";
      submits Maria Chen exact + Jame Okafor, a typo of roster James Okafor; expected
      final_action is clarification)
    - eval/run_eval.py (grouping points: fixture_category copied into the per-fixture record
      and used as the group key in per_category_extraction and per_category_decision
      aggregation; per_category_reconciliation groups by per-NAME category and is unaffected)
    - .planning/todos/pending/260623-05-fixture-category-label-mismatch.md (the todo being closed)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (POLISH-02
      section: hermetic regeneration flow and the --check gate interaction)
  </read_first>
  <action>
    Edit the single key in eval/fixtures/10_multi_employee_coastal.json: fixture_category
    "exact" -> "typo" (D-13 — matches the existing category used by fixture 05; "Jame Okafor"
    is a typo of roster "James Okafor"). Pre-verify no test couples to the label:
    grep for the fixture name across tests/ (already confirmed empty in research — re-run).
    Regenerate hermetically (committed extraction caches; do NOT enable live LLM; no DB):
    run `uv run python eval/run_eval.py` (rewrites eval/summary.json), then
    `uv run python eval/run_eval.py --chart` (rewrites eval/chart.svg), then
    `uv run python eval/run_eval.py --check` (must exit 0). Inspect eval/summary.json:
    per_category_extraction and per_category_decision now group fixture 10 under "typo"
    (n=2 with fixture 05, absorbing fixture 10's deliberate phantom-employee extraction
    scores per DIVERGENCES.md — that is the honest reading), the "exact" bucket shrinks
    accordingly, per_category_reconciliation is unchanged. Commit fixture + summary.json +
    chart.svg TOGETHER in one commit (15-RESEARCH Pitfall 4: piecemeal commits turn the
    push-time eval gate red). Then close the todo: append a "## Resolution (Phase 15)"
    section to 260623-05 describing the relabel + regeneration proof, and git mv it from
    .planning/todos/pending/ to .planning/todos/completed/ (ticket IDs are fine inside
    .planning/ — it is the provenance record).
  </action>
  <verify>
    <automated>grep -c '"typo"' eval/fixtures/10_multi_employee_coastal.json | grep -qv '^0$' && uv run python eval/run_eval.py --check && ! test -f .planning/todos/pending/260623-05-fixture-category-label-mismatch.md && test -f .planning/todos/completed/260623-05-fixture-category-label-mismatch.md && uv run pytest -q</automated>
  </verify>
  <done>
    Fixture 10 carries fixture_category "typo"; summary.json and chart.svg are regenerated
    and committed in the same commit as the fixture; --check exits 0; todo 260623-05 lives in
    completed/ with a resolution section; full suite green.
  </done>
  <acceptance_criteria>
    - eval/fixtures/10_multi_employee_coastal.json contains "fixture_category": "typo".
    - `uv run python eval/run_eval.py --check` exits 0 after regeneration.
    - eval/summary.json shows fixture 10 grouped under "typo" in per_category_extraction and
      per_category_decision (source assertion on the regenerated JSON).
    - One commit contains all three of: the fixture JSON, eval/summary.json, eval/chart.svg.
    - `grep -rn "10_multi\|multi_employee_coastal" tests/` returns nothing (no test coupling).
    - Todo file moved to .planning/todos/completed/ with a Resolution section appended.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree text edits -> runtime behavior | comment sweep must not alter any scored eval output or script behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04 | Tampering (self-inflicted) | eval/ + scripts/ sweep altering scored output | medium | mitigate | eval regression gate (`run_eval.py --check`) run after the sweep and after the relabel regeneration; full suite + ruff + mypy per commit |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- Gate grep clean over the six swept files.
- `uv run python eval/run_eval.py --check` exits 0 at plan end.
- `uv run pytest -q && uv run ruff check && uv run mypy` green.
- Todo 260623-05 in completed/ with resolution record.
</verification>

<success_criteria>
- eval/ and scripts/ read as constraint-documented, process-history-free code.
- POLISH-02 closed: label corrected, chart grouping verified honest, artifacts regenerated
  atomically, todo closed.
</success_criteria>

## Artifacts this phase produces (this plan)

- No new code symbols — comment/docstring/string text edits only, plus:
- Modified data: `eval/fixtures/10_multi_employee_coastal.json` (`fixture_category` value
  becomes `"typo"`).
- Regenerated committed artifacts: `eval/summary.json`, `eval/chart.svg`.
- Moved file: `.planning/todos/completed/260623-05-fixture-category-label-mismatch.md`
  (from pending/, with appended `## Resolution (Phase 15)` section).

<!-- planner-discipline-allow: D-[0-9]|WR-[0-9]|CR-[0-9]|CX-[0-9]|GAP-[0-9]|R2-[0-9]|OPS2?-[0-9]|T-[0-9]+-[0-9]+|FIX[ -][0-9A-Z]|Pitfall|review fix|Phase [0-9]|PATTERNS\.md -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->
<!-- planner-discipline-allow: PATTERNS\.md -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-02-SUMMARY.md` when done.
</output>
