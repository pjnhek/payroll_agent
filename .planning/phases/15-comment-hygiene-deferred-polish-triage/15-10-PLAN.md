---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 10
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-03", "15-04", "15-05", "15-06", "15-07", "15-08", "15-09", "15-11"]
files_modified:
  - tests/test_comment_provenance_guard.py
  - .planning/todos/pending/260623-01-phase05-review-warnings.md
  - .planning/todos/completed/260623-01-phase05-review-warnings.md
  - .planning/STATE.md
autonomous: true
requirements: [COMM-01, POLISH-01]

must_haves:
  truths:
    - "A pytest guard scans the swept file set for ticket-shaped patterns and passes on the live tree — COMM-01's success criterion is executable, not asserted"
    - "The guard's pattern table was tuned against the FULL corpus-observed process vocabulary (UI-SPEC, UAT #N, HIGH-N, R2-HIGH, Codex, finding #N, planning-doc filenames): every shape with zero false positives on the final tree is guard-enforced, and every deliberately-excluded shape is listed in a named EDITORIAL_ONLY_PATTERNS module constant with a one-line reason"
    - "The guard's synthetic self-test proves every blocked pattern fires, that every EDITORIAL_ONLY_PATTERNS entry is NOT guard-enforced (a file containing it scans clean), and that legitimate prose passes"
    - "The guard emits a machine-readable record — the exact enforced pattern list, the scanned file count, and the excluded-pattern list — so the zero-false-positive claim is evidenced by output, not by narrative"
    - "After the LAST straggler-fix commit, both guard entry points re-ran green AND a second full-tree grep with the complete extended vocabulary over the D-05 file set was run and recorded in the summary"
    - "Todo 260623-01 is closed with an explicit per-item disposition record (WR-01..05, INFO-01/02)"
    - "STATE.md's deferred-items table shows both v2 polish todos resolved — nothing silently open at milestone end"
  artifacts:
    - path: "tests/test_comment_provenance_guard.py"
      provides: "permanent CI-riding COMM-01 regression guard (D-07..D-09) with a named editorial-only exclusion table and a machine-readable scan record"
      contains: "EDITORIAL_ONLY_PATTERNS"
    - path: ".planning/todos/completed/260623-01-phase05-review-warnings.md"
      provides: "per-item disposition record closing POLISH-01"
  key_links:
    - from: "tests/test_comment_provenance_guard.py"
      to: "existing CI test job"
      via: "collected by uv run pytest -q — no ci.yml change (D-07)"
      pattern: "test_comment_provenance_guard"
---

<objective>
Make COMM-01 executable and close the milestone's loose ends: write the comment-provenance
guard test (D-07, patterns per D-08, scope per D-09) modeled on the private-imports guard,
fix any straggler hits it finds tree-wide, then write the per-item POLISH-01 disposition
record, close todo 260623-01, and update STATE.md's deferred-items table (D-11, D-12).

Purpose: this is v3's final plan — the guard turns "no ticket refs anywhere" into a
permanent CI-enforced invariant, and the disposition record leaves zero silently-open
deferred items.
Output: one new guard test riding the existing CI test job, straggler-free tree, closed
todo, updated STATE.md, full phase gates green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-01-SUMMARY.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-11-SUMMARY.md
@.planning/todos/pending/260623-01-phase05-review-warnings.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the comment-provenance guard test and drive the tree green (D-07, D-08, D-09)</name>
  <files>tests/test_comment_provenance_guard.py</files>
  <read_first>
    - tests/test_bound01_private_imports.py (the mandated structural precedent: spec-style
      module docstring, module-level constants, a reusable pure scan function returning
      "path:lineno: line" violation strings, one live-tree test plus one synthetic tmp_path
      self-test, and a NAMED exemption constant with an explanatory comment rather than an
      inline magic check)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (Guard Test
      Design section: scan-glob set, false-positive corpus, Open Question 1 resolution;
      Code Examples section has a starter skeleton — treat as a sketch, tune against the tree)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md (guard
      pattern-assignment section, incl. the scope difference from the precedent)
  </read_first>
  <behavior>
    - Synthetic self-test (detection): for EACH blocked pattern, a tmp_path file containing it is
      flagged; legitimate-prose lines pass — at minimum: "fix the rounding", "this phase of
      parsing", lowercase "phases", a noqa comment with its code, a type-ignore comment with
      its bracketed code, and requirement IDs (CALC-03, BOUND-01, COMM-01).
    - Synthetic self-test (exclusions): for EACH entry in the EDITORIAL_ONLY_PATTERNS constant, a
      tmp_path file containing that shape scans CLEAN — proving the documented exclusions behave
      as documented (recognized as editorial-only, deliberately NOT guard-enforced) rather than
      being an unverified narrative claim.
    - Live-tree test: scanning the swept file set on the real repo returns zero violations, with a
      failure message that lists every hit as path:lineno: line, and emits the machine-readable
      scan record (enforced patterns, scanned file count, excluded patterns).
  </behavior>
  <action>
    Create tests/test_comment_provenance_guard.py mirroring the precedent's structure, fully
    annotated for strict mypy. Scan scope (D-05/D-09) as glob patterns rooted at the repo:
    app/**/*.py, app/db/schema.sql, app/templates/*.html, app/static/*.css, eval/**/*.py,
    eval/fixtures/*.md, scripts/*.py, tests/**/*.py — note this INCLUDES tests/ (a deliberate
    scope difference from the precedent, which excludes it) and excludes .planning/ entirely.
    Self-exemption (D-09): skip the guard's own file via a named module constant with a
    comment explaining why. Pattern table (D-08 — ticket-shaped ONLY, zero false positives on
    the final tree): a D- prefix followed by a digit then word/dot/dash characters; WR/CR/CX/
    GAP/R2 followed by a dash and digits; FIX followed by space-or-dash and a digit or single
    capital; Pitfall followed by a hash-number; the parenthesized review-fix phrase; capital-P
    Phase followed by a digit.

    Extended-vocabulary evaluation (REQUIRED — addresses review concern: guard narrower than
    the D-03 policy, MEDIUM): for EACH corpus-observed process shape beyond the base table —
    UI-SPEC; UAT followed by space-hash-number; HIGH- followed by a digit (covers
    HIGH-1-AUTH-style compounds); R2-HIGH; the reviewer name Codex; "finding" followed by a
    hash-number; planning-doc filename citations (PATTERNS/RESEARCH/CONTEXT/REQUIREMENTS/
    ROADMAP/UI-SPEC + .md); plus the OPS/T-N-N/D-A secondary styles — run a full-tree scan
    over the D-05 file set and check for false positives. Every shape with ZERO false
    positives on the final tree goes INTO the pattern table. This stays inside D-08's locked
    frame: zero-FP priority over breadth, and requirement IDs (CALC-03, BOUND-01, COMM-01, …)
    must NEVER match (Open Question 1 resolution). Note: plans 15-02..15-09's per-plan gate
    greps already enforce this extended vocabulary at sweep time, so the guard's job is
    preventing RETURN, not first-pass cleanup.

    Machine-readable narrowing record (REQUIRED — closes the second-round review's LOW finding,
    which observed that the previous wording could be satisfied by a narrative claim):
    a prose sentence in a docstring is NOT sufficient evidence that a shape was evaluated and
    deliberately excluded. Implement the narrowing as CODE:
    (a) A named module constant `EDITORIAL_ONLY_PATTERNS` holding every shape that was evaluated
        and deliberately NOT guard-enforced (because a legitimate use exists on the final tree),
        each entry carrying its one-line reason — e.g. as a tuple/dict of
        (pattern, reason) pairs, not a bare list of regexes. If a shape is excluded and it is
        not in this constant, the narrowing is silent and the plan is not satisfied.
    (b) A synthetic exclusion test: iterate `EDITORIAL_ONLY_PATTERNS`, write a tmp_path file
        containing each shape, and assert the scanner returns ZERO violations for it — the
        exclusions are proven to behave as documented, not merely asserted in prose. (Pair this
        with the existing per-blocked-pattern firing test so both directions of the pattern
        table are executable.)
    (c) A machine-readable scan record emitted by the live-tree test: the exact enforced pattern
        list, the COUNT of files scanned, and the `EDITORIAL_ONLY_PATTERNS` entries — printed or
        written such that a reader of the test output can see them without reading the source
        (e.g. `print(...)` visible under `pytest -rA`/`-s`, or an assertion message that carries
        them). Paste that record verbatim into the plan summary. A future executor must not be
        able to satisfy this criterion by writing a sentence.

    Both tests use plain-English failure messages with no ticket vocabulary outside the
    pattern-table constants. Ordering (review suggestion): the guard file lands FIRST, then
    run the live-tree test — if stragglers appear anywhere (files missed by waves 1-2, or new
    hits from merge fallout), fix them in place under the same D-01/D-02/D-03 rubric the
    sweep plans used — comment-only edits, committed separately from the guard file itself.
    Straggler cleanup is complete ONLY when both guard entry points are green; after the LAST
    straggler-fix commit, re-run both guard tests AND one final full-tree grep with the
    complete extended vocabulary over the D-05 file set (second-scan proof), recording the
    command and its empty result in the summary.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_comment_provenance_guard.py && uv run ruff check tests/test_comment_provenance_guard.py && uv run mypy</automated>
  </verify>
  <done>
    Guard file exists with both entry points green: the synthetic self-test proves detection
    and non-detection shapes, and the live-tree test passes over the whole D-05 scan set.
  </done>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_comment_provenance_guard.py` exits 0 (both tests collected
      and passing).
    - The scan-glob list includes tests/**/*.py, app/db/schema.sql, app/templates/*.html,
      app/static/*.css, and eval/fixtures/*.md (source assertion).
    - The synthetic self-test contains at least one firing case per pattern-table entry and
      the legitimate-prose pass cases named in behavior (source assertion).
    - The guard file is self-exempted via a named constant (source assertion).
    - Requirement-ID strings (e.g. BOUND-01 in the sibling guard) do not trip the live scan
      (implied by the live test passing while tests/test_bound01_private_imports.py retains
      its BOUND-01 references).
    - The pattern table contains entries for the extended shapes that passed the zero-FP
      check (at minimum, evaluation of UI-SPEC / UAT-hash / HIGH-digit / R2-HIGH / Codex /
      finding-hash / planning-doc-filename shapes is evidenced).
    - `grep -c 'EDITORIAL_ONLY_PATTERNS' tests/test_comment_provenance_guard.py` returns a
      non-zero count: the guard module defines the named exclusion constant, and every entry
      carries a one-line reason (source assertion — no silent narrowing). If the zero-FP check
      excluded nothing, the constant is present and empty with a comment saying so.
    - A synthetic test iterates `EDITORIAL_ONLY_PATTERNS` and asserts a tmp_path file containing
      each excluded shape produces ZERO violations (source assertion — the exclusions are
      executable, not narrative). Deleting an entry from the constant must change what that test
      exercises.
    - The live-tree test emits a machine-readable record containing the enforced pattern list,
      the scanned FILE COUNT, and the excluded-pattern list; that record is pasted verbatim into
      the plan summary (evidence, not a claim).
    - The summary records the post-straggler second scan: both guard tests green after the
      last straggler commit plus one full-tree extended-vocabulary grep with empty output.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: POLISH-01 disposition record, todo closure, STATE.md deferred-items update, phase gates (D-11, D-12)</name>
  <files>.planning/todos/pending/260623-01-phase05-review-warnings.md, .planning/STATE.md</files>
  <read_first>
    - .planning/todos/pending/260623-01-phase05-review-warnings.md (the seven items being
      dispositioned)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-01-SUMMARY.md (outcomes for
      the WR-01 hermetic threading test, the real-Postgres epoch-arbiter test, and the CI wiring
      — including whether the mandatory failure injection worked or the narrow fallback was taken)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-11-SUMMARY.md (outcomes for
      the WR-05 containment fix and the INFO-02 retry-prompt scrub)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md (POLISH-01
      Current-State Verification table — file:line evidence for the already-fixed items)
    - .planning/STATE.md (Deferred Items table rows for the two todos)
  </read_first>
  <action>
    Spot-verify each already-closed item against the live tree before recording it (D-12's
    verify-first instruction; the research evidence to confirm: the double-checked-locking
    pool guard in app/db/supabase.py; the explicit-column load_all_runs projection in
    app/db/repo/demo.py; the ASCII filename sanitizer in app/routes/runs.py with its
    regression test in tests/test_dashboard.py; the absence of the needs_clarification status
    from app/models/status.py with the drift test enforcing it). Then append a
    "## Resolution (Phase 15)" section to todo 260623-01 with one line per item —
    WR-01: closed by TWO proofs, not one (cite the 15-01 outcome: green-first-run or
    test-plus-fix) — a hermetic route-driven crash-retrigger-send threading regression test
    (tests/test_retrigger_threading.py, scoped to route/pipeline/gateway) PLUS a real-Postgres
    test of the four-column outbound upsert arbiter (tests/test_email_epoch_arbiter_integration.py),
    the latter wired into the concurrency-proof workflow so it actually executes in CI rather than
    self-skipping; WR-02: fixed in Phase 8 (pool
    double-checked locking), verified in current source; WR-03: fixed in Phase 8 (explicit
    column projection), verified — recorded as fixed rather than the anticipated "accepted"
    per D-12's verify-first clause; WR-04: fixed during Phase 5 execution (ASCII sanitizer +
    regression test), verified; WR-05: fixed in Phase 15 (path containment in eval_view, plus the
    hoisted module-level eval path constants that made it testable without a chdir — plan 15-11);
    INFO-01: obsolete — the status was removed and a drift test enforces its absence;
    the unrelated dead "computing" badge-map entry was noted and deliberately left (harmless
    under the maps' .get() defaults, and behavior edits beyond the three sanctioned fixes are
    out of phase scope); INFO-02: fixed in Phase 15 (scrubbed retry prompt, plan 15-11).
    Ticket IDs are fine here — .planning/ is the provenance record. git mv the todo to
    .planning/todos/completed/. Update .planning/STATE.md's Deferred Items table: the
    "Phase 05 code-review: deferred Warnings + Info" row and the "Fixture 10 category-label"
    row change status from deferred to resolved (Phase 15). Finish with the full phase gates:
    complete suite, ruff, strict mypy, and the hermetic eval regression check.
  </action>
  <verify>
    <automated>! test -f .planning/todos/pending/260623-01-phase05-review-warnings.md && test -f .planning/todos/completed/260623-01-phase05-review-warnings.md && grep -q "Resolution (Phase 15)" .planning/todos/completed/260623-01-phase05-review-warnings.md && uv run pytest -q && uv run ruff check && uv run mypy && uv run python eval/run_eval.py --check</automated>
  </verify>
  <done>
    Todo 260623-01 closed in completed/ with the seven-item disposition record; STATE.md
    deferred-items table shows both polish todos resolved; full suite + ruff + strict mypy +
    eval check green — the milestone ends with zero silently-open loose ends.
  </done>
  <acceptance_criteria>
    - The todo file exists only under .planning/todos/completed/ and contains a Resolution
      section with an explicit disposition for each of WR-01, WR-02, WR-03, WR-04, WR-05,
      INFO-01, INFO-02.
    - .planning/STATE.md Deferred Items rows for the two polish todos read resolved (Phase 15).
    - `uv run pytest -q` exits 0 (guard included); `uv run ruff check` and `uv run mypy`
      exit 0; `uv run python eval/run_eval.py --check` exits 0.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future commits -> swept sources | without an enforced gate, provenance comments regress silently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-07 | Tampering (regression) | ticket-provenance reintroduction in swept sources | low | mitigate | permanent pytest guard riding the existing CI test job; synthetic self-test proves the scanner works before it is trusted |
| T-15-06 | Tampering | Content-Disposition header injection (WR-04) | high | mitigate | verified-fixed disposition with live-source evidence + existing regression test; recorded in the todo closure |
| T-15-01 | Information Disclosure | eval fixture path traversal (WR-05) | medium | mitigate | fixed in plan 15-11; disposition recorded here |
| T-15-02 | Information Disclosure | retry-prompt model-output echo (INFO-02) | low | mitigate | fixed in plan 15-11; disposition recorded here |
| T-15-08 | Repudiation | email_messages audit log vs the four-column ON CONFLICT arbiter | high | mitigate | proven in plan 15-01 by a real-Postgres test wired into the concurrency-proof workflow; disposition recorded here |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- `uv run pytest -q` green including both guard entry points.
- `uv run ruff check`, `uv run mypy`, `uv run python eval/run_eval.py --check` all exit 0.
- Todo 260623-01 in completed/ with per-item dispositions; STATE.md deferred table updated.
</verification>

<success_criteria>
- COMM-01 is enforced by a permanent, self-proving guard test — the phase success criterion
  is executable.
- POLISH-01 fully closed with explicit per-item dispositions; no silently-open deferred items
  remain at the end of v3.
</success_criteria>

<review_dispositions>
## Cross-AI Review Dispositions (phase-wide record — see 15-REVIEWS.md for findings)

### Round 2 (current — verdict was NOT READY; all four findings closed below)

- HIGH, "the WR-01 real-persistence proof still tests the fake repo" -> SPLIT, per operator
  decision. The `fake_repo` fixture patches `insert_email_message` onto the `app.db.repo` package
  (tests/conftest.py), so no test taking that fixture can ever reach `app/db/repo/emails.py`. The
  old plan text demanded the impossible ("do NOT stub insert_email_message") while simultaneously
  asserting on the fake's store — a self-contradiction, now DELETED. Plan 15-01 Task 1 is a
  hermetic route/pipeline/gateway test whose claims are narrowed to what it executes, and Task 2
  is a NEW real-Postgres test (tests/test_email_epoch_arbiter_integration.py) that drives the
  production upsert and proves the (run_id, purpose, round, epoch) arbiter appends rather than
  clobbers across a real reply_epoch bump. Codex's alternative — "lean on existing SQL tests that
  separately prove the arbiter" — was REJECTED on a verified false premise: no such test existed.
  test_retrigger_epoch.py is hermetic by its own docstring; test_delivery.py only substring-checks
  "ON CONFLICT" against recorded SQL text (it cannot see the column list); test_gateway.py's
  integration test never bumps the epoch.
- HIGH (added by the orchestrator's independent verification, NOT in the review): a new
  integration test would NOT run in CI. ci.yml's test job sets no DATABASE_URL, so every
  `@pytest.mark.integration` test self-skips there, and concurrency-proof.yml — the only job with
  a Postgres — hard-codes a single test file. 34 of 37 existing integration tests therefore never
  execute in CI. -> Plan 15-01 Task 3 edits concurrency-proof.yml to name the new module, with an
  acceptance criterion that greps the workflow for the path. The residual limitation (that
  workflow runs on push-to-master and manual dispatch, not on pull requests) is stated in the
  workflow and the summary rather than silently overclaimed.
- MEDIUM, "the WR-05 test setup will break template loading" -> CONFIRMED and fixed. The shared
  `Jinja2Templates(directory="app/templates")` searchpath is relative, so `monkeypatch.chdir` makes
  eval.html unresolvable. The chdir is DELETED from plan 15-11's Task 1. Because eval_view built
  its two paths as function-local variables, there was nothing to monkeypatch — so the task now
  hoists them to module-level constants first (the same shape eval/run_eval.py already uses and
  tests/test_eval_wiring.py already monkeypatches) and the test redirects the constants. The
  containment fix itself is unchanged. Note `monkeypatch.chdir` appears ZERO times in tests/ —
  it was never this repo's idiom.
- MEDIUM, "the crash -> retrigger proof still allows no crash to occur" -> the post-send failure
  injection is now MANDATORY in plan 15-01 Task 1 (a one-shot failure at the first post-send
  persistence step inside the real clarify stage; the real orchestrator error-wrap persists
  ERROR). Direct ERROR seeding survives only as a narrow, documented fallback — and if taken, the
  test must be renamed away from "crash" so it never claims more than it proves. The route-driven
  assertions are on persisted state only: `run_pipeline_bg` swallows every exception, so
  `pytest.raises` around the route would be a lie.
- LOW, "guard zero-FP evaluation needs a machine-readable record" -> this plan, Task 1: an
  `EDITORIAL_ONLY_PATTERNS` named constant (pattern + one-line reason per entry), a synthetic test
  that iterates it and proves each excluded shape scans clean, and a machine-readable scan record
  (enforced patterns, scanned file count, exclusions) emitted by the live-tree test and pasted into
  the summary. A narrative claim can no longer satisfy the criterion.
- ALSO (orchestrator finding, beyond the review): tests/test_delivery.py's comments and failure
  messages still describe the arbiter as "ON CONFLICT (run_id, purpose)" — the pre-epoch key. They
  are not merely stale provenance, they are factually WRONG about live behavior. Routed to plan
  15-08 Task 2 as an explicit correction (not just a label strip) with its own acceptance criterion.

### Round 1 (historical — incorporated at commit 1bd0dbb, re-verified in Round 2)

Incorporated (executable content changed):
- HIGH, WR-01 test under-specified -> plan 15-01 Task 1 (real retrigger route + pipeline
  mandatory, three assertion surfaces incl. persisted row + stale-row-unchanged).
- HIGH, sweep inventory omitted app/templates/eval.html -> plan 15-09 Task 2; the re-run
  manifest also surfaced app/templates/runs_list.html (15-09), tests/test_demo_landing.py
  and tests/test_multi_employee_delivery.py (routed to 15-08 Task 3).
- MEDIUM, guard narrower than D-03 policy -> this plan, Task 1 extended-vocabulary
  evaluation with mandatory zero-FP check and documented exclusions; ADDITIONALLY every
  sweep plan's per-plan gate grep (15-02..15-09) was extended with the same vocabulary, so
  the editorial D-03 scope is executable at sweep time, before this guard exists.
- MEDIUM, 15-01 route-wiring fallback -> withdrawn in 15-01 Task 1; fallback restricted to
  setup helpers only.
- MEDIUM, conftest resend no-op masks provider boundary -> 15-01 Task 1 asserts persisted
  outbound row values, not only caller kwargs.
- LOW, stale inventory counts -> every sweep plan (15-02..15-09) now carries a
  "fresh manifest first" rubric rule: counts are sizing-only, regenerate before editing.
- LOW, file renames broader than D-06 -> 15-06 Task 1 now requires a REPO-WIDE old-name
  reference search (CI, tooling configs, docs — not only workflows/tests) before git mv.
- Suggestion, guard-before-complete + second full-tree scan -> this plan, Task 1 ordering
  and the post-straggler second-scan acceptance criterion.
- Suggestion, per-batch collect-count verification -> 15-06 Task 1 (compare against the
  baseline file after every rename commit).

Re-affirmed (no plan change beyond documentation):
- MEDIUM, 15-09 scope (34 -> now 36 files) — ACCEPTED again with rationale recorded in
  15-09's objective: mechanical rubric-driven edits, per-task executable gates, this plan's
  tree-wide straggler pass as backstop; the two newly-found test files were routed to 15-08
  precisely to avoid growing 15-09 further.

Rejected (with rationale):
- Suggestion, mechanical "executable content unchanged" check (AST-normalized compare) for
  comment-only plans — REJECTED. (a) The phase's sanctioned runtime-string rewrites
  (assert messages in 15-06/15-07/15-08, the argparse description in 15-02, the demo caption
  in 15-09) ARE executable-content changes by design, so a naive AST/token parity check
  false-positives on exactly the sanctioned edits, and docstrings are AST nodes too — a
  docstring-normalizing comparator is new tooling whose own correctness would need proving.
  (b) Behavior neutrality is already machine-checked from four independent directions: the
  unchanged 666-test suite, strict mypy (warn_unused_ignores catches stranded ignores), the
  eval `--check` output-parity gate (15-02), the schema-introspection parity test (15-05),
  and collect-count baselines (15-06), plus per-commit diff-inspection acceptance criteria.
  Marginal risk reduction does not justify the new tooling inside this phase.
</review_dispositions>

## Artifacts this phase produces (this plan)

- New file: `tests/test_comment_provenance_guard.py` with:
  - module constants: scan-glob list; the ticket-pattern table (base D-08 shapes + every
    extended shape that passed the zero-FP check); a named self-exemption constant; and
    `EDITORIAL_ONLY_PATTERNS` — the named exclusion table, each entry carrying its pattern and a
    one-line reason (present and explicitly empty if nothing was excluded)
  - a reusable pure scan function (e.g. `scan_tree_for_ticket_provenance`) returning
    violation strings
  - test functions: `test_no_ticket_provenance_in_swept_sources` (live tree; also emits the
    machine-readable scan record: enforced patterns, scanned file count, exclusions),
    `test_scanner_detects_synthetic_violations_and_passes_legit_prose` (tmp_path self-proof), and
    `test_editorial_only_patterns_are_not_guard_enforced` (iterates `EDITORIAL_ONLY_PATTERNS` and
    proves each excluded shape scans clean) — final names at executor discretion, descriptive and
    ticket-free
- Moved file: `.planning/todos/completed/260623-01-phase05-review-warnings.md` (from
  pending/, with appended `## Resolution (Phase 15)` section).
- Updated `.planning/STATE.md` Deferred Items rows (two todos -> resolved).

<!-- planner-discipline-allow: UI-SPEC -->
<!-- planner-discipline-allow: UAT # -->
<!-- planner-discipline-allow: Codex -->
<!-- planner-discipline-allow: HIGH-[0-9] -->
<!-- planner-discipline-allow: finding # -->
<!-- planner-discipline-allow: R2-HIGH -->
<!-- planner-discipline-allow: Pitfall -->
<!-- planner-discipline-allow: review fix -->
<!-- planner-discipline-allow: Phase [0-9] -->

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-10-SUMMARY.md` when done.
</output>
