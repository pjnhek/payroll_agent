---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_retrigger_threading.py
  - app/routes/dashboard.py
  - tests/test_dashboard.py
  - app/llm/client.py
  - tests/test_llm_client.py
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "A hermetic regression test drives the REAL POST /runs/{run_id}/retrigger route and the real background pipeline and proves the reply-threading anchor (client inbound Message-ID) survives crash -> retrigger -> outbound send in BOTH the captured gateway kwargs AND the persisted new outbound row, with the pre-crash sent row unchanged"
    - "The eval dashboard refuses a traversal fixture_path from summary.json and renders the missing-file fallback instead of file contents"
    - "The LLM retry prompt no longer echoes raw model-output values from a ValidationError back to the provider"
  artifacts:
    - path: "tests/test_retrigger_threading.py"
      provides: "WR-01 threading regression proof (D-10)"
      contains: "def test_"
    - path: "app/routes/dashboard.py"
      provides: "path-containment check in eval_view before fixture file read"
      contains: "is_relative_to"
    - path: "app/llm/client.py"
      provides: "scrubbed retry prompt (no input values echoed)"
      contains: "include_input=False"
  key_links:
    - from: "tests/test_retrigger_threading.py"
      to: "app/routes/runs.py"
      via: "TestClient POST /runs/{run_id}/retrigger driving the real recovery route + synchronous run_pipeline_bg background task"
      pattern: "retrigger"
    - from: "tests/test_retrigger_threading.py"
      to: "app/email/gateway.py"
      via: "spy-wrap of gateway send_outbound capturing in_reply_to/references kwargs AND persisted-row assertions on fake_repo.outbound"
      pattern: "send_outbound"
    - from: "app/routes/dashboard.py"
      to: "eval/fixtures"
      via: "resolve() + is_relative_to(fixtures_dir.resolve()) gate before read_text"
      pattern: "is_relative_to"
    - from: "tests/test_llm_client.py"
      to: "app/llm/client.py"
      via: "FakeOpenAI create_calls[1] retry-prompt inspection"
      pattern: "create_calls"
---

<objective>
Close the three POLISH-01 items that still need code or proof, test-first: WR-01 (threading
after crash+retrigger) gets a hermetic regression test per D-10; WR-05 (path containment on
eval fixture reads) and INFO-02 (ValidationError content echoed to the provider) get small
test-first fixes per D-12. These are the ONLY sanctioned behavior changes in Phase 15 — the
rest of the phase is comment/docstring/rename-only.

Purpose: POLISH-01 requires todo 260623-01's items resolved with proof, not by reading
(Phase 10 vacuous-proof lesson, restated in D-10). WR-02/WR-03/WR-04/INFO-01 need no code
change (verified in 15-RESEARCH.md) — their dispositions are written in plan 15-10.
Output: one new test file, two small production fixes with their regression tests, all
committed test-first with the suite, ruff, and mypy strict green at every commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md
@.planning/todos/pending/260623-01-phase05-review-warnings.md
</context>

<important_constraints>
- Test-first commit discipline (D-10, D-12, Phase 14 D-08 protocol): for each behavior fix,
  commit the failing test first, then the minimal fix in a separate commit. Never bury a fix
  in a comment commit.
- Live-key hazard: this repo's `.env` contains LIVE LLM keys. Every new test must keep LLM
  seams stubbed (stub `suggest_employees`; FakeOpenAI harness for client tests). Never rely
  on env emptiness.
- All new test code must be born clean: no ticket IDs or phase references in test names,
  docstrings, comments, or failure messages (the plan-15-10 guard scans tests/). Failure
  messages explain the invariant in plain English.
- mypy strict covers tests/ — annotate all new test functions and helpers (`-> None`, typed
  locals where inference fails).
- Every commit: `uv run pytest -q && uv run ruff check && uv run mypy` green (uv only, never
  pip).
</important_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-01 hermetic threading regression test — crash, retrigger, send (D-10)</name>
  <files>tests/test_retrigger_threading.py</files>
  <read_first>
    - tests/test_retrigger_epoch.py (the exact analog: fake_repo + mock_llm fixtures, module-local _bare_roster/_bare_inbound/_bare_decision/_bare_extracted builders, seed-state via fake_repo.runs/fake_repo.outbound dicts, spy-wrap of gateway send_outbound that calls through)
    - tests/conftest.py (fake_repo, mock_llm fixture contracts; NOTE lines ~1035-1045: the
      mocked context globally replaces resend.Emails.send with a no-op — the PROVIDER boundary
      is already stubbed there. The test may layer a capturing provider fake on top, but must
      never stub any app-layer function; the real send_outbound body, including its
      insert_email_message reserved-row write and references-chain rebuild, must execute)
    - app/routes/runs.py (the POST /runs/{run_id}/retrigger route: claims from ERROR/APPROVED/stale in-flight, clears reply context, schedules background resume)
    - app/db/repo/emails.py (insert_email_message upsert arbiter on (run_id, purpose, round, epoch) — the seam WR-01 originally accused; do NOT stub it)
    - app/email/gateway.py (get_outbound_references_chain — durable References chain rebuilt from DB rows; send_outbound signature)
    - app/pipeline/delivery.py (where the outbound send sets in_reply_to/references from the inbound Message-ID)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md (WR-01 section: anti-vacuous-proof pattern)
  </read_first>
  <behavior>
    - Test: after a run crashes post-send and the operator hits the REAL
      POST /runs/{run_id}/retrigger route (TestClient runs the scheduled run_pipeline_bg
      synchronously), the NEXT outbound email's in_reply_to and references anchor to the
      client's original inbound Message-ID (use a distinctive sentinel like
      "&lt;client-inbound-wr-thread@example.test&gt;" seeded on the inbound row) — asserted on
      BOTH the captured send_outbound kwargs AND the persisted new outbound row in
      fake_repo.outbound (in_reply_to + references_header column values).
    - Test (MANDATORY, same file): history is preserved, not overwritten — after recovery the
      pre-crash sent outbound row is unchanged (same message_id, round, epoch, send_state)
      and a NEW row exists with an epoch greater than the pre-crash row's (the retrigger
      bumps the epoch so the fresh send INSERTs rather than upserting over the stale row).
  </behavior>
  <action>
    Create tests/test_retrigger_threading.py as a hermetic regression test driven through the
    real recovery seams (D-10: "verified by reading" is not acceptable). Follow
    tests/test_retrigger_epoch.py structurally: take monkeypatch, fake_repo, mock_llm; build
    minimal contract objects with module-local bare-object helpers.

    SETUP (simulation allowed here ONLY): seed a run whose inbound email row carries the
    sentinel message_id and whose outbound history contains a sent round-0 epoch-0 row, then
    put the run in the post-crash state. Preferred: drive the first pipeline pass with a
    one-shot injected failure at a post-send persistence step so the sent row + ERROR status
    are produced by real code (restore the real attribute before the recovery pass).
    Acceptable setup helper: seed the exact post-crash durable state directly in fake_repo
    (run status ERROR + the sent outbound row). Either way this is setup — the crash may be
    simulated; the recovery may not.

    RECOVERY (no simulation, review-mandated): drive the REAL route — TestClient POST to
    /runs/{run_id}/retrigger; FastAPI's TestClient executes the scheduled run_pipeline_bg
    background task synchronously before returning — and let the real background pipeline,
    delivery stage, and gateway send_outbound body run. The previously-allowed fallback of
    calling the route's lower-level seams directly (clear reply context + the resume entry
    point) is WITHDRAWN per cross-AI review: it no longer proves crash -> operator retrigger
    -> outbound send. Lower-level helpers are permitted for SETUP only; the retrigger route
    and pipeline stay under test.

    Spy-wrap gateway send_outbound (append kwargs to a list, then call the saved real
    function — never replace it) and stub app.pipeline.suggest.suggest_employees to a fixed
    dict (live-key hazard). The provider boundary is already stubbed: tests/conftest.py
    globally no-ops resend.Emails.send in the mocked context — leave it, or layer a capturing
    provider fake; stub ONLY the provider method, never an app-layer function. Do NOT stub
    insert_email_message, the epoch stamp, get_outbound_references_chain, or any
    delivery/orchestrator entry point — those are the seams under test (anti-vacuous rule,
    15-RESEARCH Pitfall 5).

    ASSERT THREE SURFACES (review: captured caller kwargs alone can pass vacuously, because
    callers supply in_reply_to to send_outbound — the durable proof is the persisted row):
    (1) captured send kwargs — in_reply_to / references anchor the sentinel inbound
    Message-ID; (2) the PERSISTED new outbound row in fake_repo.outbound — a new row exists
    with epoch strictly greater than the pre-crash row's, send_state "sent", in_reply_to
    equal to the sentinel, and references_header containing the sentinel; (3) the pre-crash
    sent row is unchanged after recovery (same message_id, round, epoch, send_state) — the
    fresh send INSERTed a new epoch row rather than overwriting the audit history.

    If any assertion is RED against current source: commit the failing test first, then apply
    the smallest fix at the seam that breaks the chain (money-path protocol per D-10); if
    GREEN on first run, that is the proof that the epoch arbiter resolved WR-01's premise —
    keep the test as the permanent regression gate either way. No ticket IDs anywhere in the
    new file. Addresses review concerns: WR-01 test under-specification (HIGH), route-fallback
    weakening of D-10 (MEDIUM), provider-boundary masking / persisted-row proof (MEDIUM).
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_retrigger_threading.py && uv run ruff check tests/test_retrigger_threading.py && uv run mypy</automated>
  </verify>
  <done>
    tests/test_retrigger_threading.py passes hermetically (no DB, no live LLM), drives the
    real POST /runs/{run_id}/retrigger route and background pipeline, and asserts the
    threading anchor on both the captured spied-but-real send_outbound kwargs AND the
    persisted new outbound row (with the pre-crash row unchanged) after a crash -> retrigger
    -> send sequence; the file contains no ticket-shaped identifiers.
  </done>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_retrigger_threading.py` exits 0.
    - The recovery pass is a real TestClient POST to /runs/{run_id}/retrigger (source
      assertion: the route path string appears in the test; no route-internal or
      pipeline_glue function is invoked directly for the recovery pass).
    - The test file monkeypatches gateway send_outbound with a wrapper that CALLS the original
      (source assertion: the wrapper body references the saved real function).
    - The test asserts an in_reply_to or references value equal to / containing the seeded
      inbound sentinel Message-ID (source assertion: sentinel string appears in both the
      seed block and an assert).
    - The test asserts the persisted new outbound row's in_reply_to and references_header
      values read from fake_repo's outbound store — not only the captured call kwargs
      (source assertion).
    - The test asserts the pre-crash sent row's message_id/round/epoch/send_state are
      unchanged after recovery AND that the new row's epoch is strictly greater (source
      assertion).
    - insert_email_message, get_outbound_references_chain, and the delivery/orchestrator
      entry points are not monkeypatched anywhere in the file; the only stubs are the
      provider method (resend.Emails.send) and app.pipeline.suggest.suggest_employees, plus
      any one-shot post-send failure injection used solely to produce the crashed state
      (source assertion).
    - `uv run mypy` exits 0 (new file annotated for strict mode).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-05 path-containment check in eval_view, test-first (D-12)</name>
  <files>app/routes/dashboard.py, tests/test_dashboard.py</files>
  <read_first>
    - app/routes/dashboard.py (eval_view: summary_path = Path("eval/summary.json"),
      fixtures_dir = Path("eval/fixtures"), the existing fixture_file.exists() /
      "‹fixture file missing›" fallback branch — the fix reuses this exact fallback)
    - tests/test_dashboard.py (module-level TestClient `client`; the Content-Disposition
      sanitizer regression test — the file's security-test idiom to copy: monkeypatched data
      seams + property assertion with explanatory failure message)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md (WR-05 sections)
  </read_first>
  <behavior>
    - Test: GET /eval with a summary.json whose per_fixture entry has fixture_path
      "../../secret.txt" (a file that exists OUTSIDE eval/fixtures with sentinel content)
      returns 200, the sentinel content does NOT appear in the response body, and the
      missing-file placeholder is rendered for that fixture instead.
  </behavior>
  <action>
    Test first: extend tests/test_dashboard.py with
    test_eval_view_refuses_fixture_path_traversal. Build the scenario under tmp_path via
    monkeypatch.chdir(tmp_path): write eval/summary.json containing a minimal summary dict
    with a per_fixture list whose single entry sets fixture_path to "../../secret.txt", create
    tmp_path/secret.txt with sentinel content (e.g. "TRAVERSAL_SENTINEL_CONTENT"), and create
    the eval/fixtures directory so the route's relative paths resolve inside tmp_path. Assert
    response.status_code == 200, sentinel not in response.text, and the existing fallback
    marker rendered. Commit the failing test. Fix: in eval_view, before the read, resolve the
    joined path and refuse any fixture_file whose resolved path is not relative to
    fixtures_dir.resolve() — route refusals into the SAME "‹fixture file missing›" fallback
    string already used by the not-exists branch (per D-12: cheap + real, stdlib
    Path.resolve() + Path.is_relative_to(), no new error path, no string prefix checks).
    Behavior for all committed, legitimate fixture paths is unchanged. Do not touch the
    docstring's provenance strings in this commit — plan 15-07 sweeps this file's comments.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_dashboard.py && uv run ruff check app/routes/dashboard.py tests/test_dashboard.py && uv run mypy</automated>
  </verify>
  <done>
    A traversal fixture_path can no longer cause file content outside eval/fixtures to reach
    the eval page; the new regression test locks the property; all existing dashboard tests
    still pass unchanged.
  </done>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_dashboard.py` exits 0 with the new test collected.
    - app/routes/dashboard.py eval_view contains a containment check using is_relative_to
      against fixtures_dir resolved (source assertion), and the refusal branch assigns the
      identical fallback string used by the not-exists branch (source assertion).
    - Two commits exist for this task: failing-test commit, then fix commit.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: INFO-02 scrub ValidationError content from the LLM retry prompt, test-first (D-12)</name>
  <files>app/llm/client.py, tests/test_llm_client.py</files>
  <read_first>
    - app/llm/client.py (the call_structured retry loop: `for attempt in (1, 2)`, the
      except (ValidationError, ValueError) block, and the retry message built as
      f-string interpolating exc followed by the literal instruction "Return ONLY valid JSON
      matching the schema." — only the exc interpolation changes)
    - tests/test_llm_client.py (FakeOpenAI harness: _patch_openai autouse fixture,
      _FakeOpenAI.next_script scripting, _set_tier_env helper, create_calls capture, and
      test_invalid_then_valid_retries_exactly_once as the retry-flow analog)
  </read_first>
  <behavior>
    - Test: script FakeOpenAI with a first response that is syntactically valid JSON but
      schema-invalid, carrying a distinctive input value (e.g. score set to
      "SENTINEL_LEAK_XYZ"), then a valid second response. Assert the call succeeds after
      exactly one retry, and create_calls[1]'s last message content does NOT contain
      SENTINEL_LEAK_XYZ but DOES still contain the word "JSON" and the field location
      (so the model still gets actionable, non-echoing feedback).
    - Test: existing retry-contract tests pass unchanged (one retry, second failure
      propagates, empty-content normalization untouched).
  </behavior>
  <action>
    Test first: extend tests/test_llm_client.py with
    test_retry_prompt_scrubs_validation_input_values using the existing FakeOpenAI harness
    and _set_tier_env. Commit it failing. Fix: in app/llm/client.py, replace the raw
    f-string interpolation of exc in the retry message with a scrubbed summary: when exc is a
    ValidationError, build the summary from exc.errors(include_url=False, include_input=False)
    — verified available in the pinned pydantic (ValidationError.errors accepts include_url,
    include_context, include_input keyword args) — formatting only each error's loc, type,
    and msg (msg is pydantic's generic description, e.g. "Input should be a valid number",
    and carries no input echo); when exc is the empty-content ValueError, keep str(exc) (it
    contains no model output). A small module-private helper such as
    _scrubbed_validation_summary(exc) is fine. Keep everything else byte-identical: the
    one-retry contract, the "Return ONLY valid JSON matching the schema." instruction (the
    word JSON is load-bearing for DeepSeek json_object mode), the second-failure propagation
    and ValueError-to-ValidationError normalization.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_llm_client.py && uv run ruff check app/llm/client.py tests/test_llm_client.py && uv run mypy && uv run pytest -q</automated>
  </verify>
  <done>
    The retry prompt sent to the provider carries field locations and error types but no
    model-output input values; the one-retry contract and all existing client tests pass
    unchanged; full suite green.
  </done>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_llm_client.py` exits 0 with the new test collected.
    - The new test asserts the sentinel is absent from create_calls[1] message content AND
      that "JSON" is present in the same content (source assertion).
    - app/llm/client.py builds the retry summary via errors(include_url=False,
      include_input=False) for ValidationError (source assertion: both kwargs present).
    - Full suite `uv run pytest -q` exits 0.
    - Two commits exist for this task: failing-test commit, then fix commit.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| summary.json content -> filesystem read | fixture_path strings from a committed-but-tamperable JSON drive Path joins in eval_view |
| model output -> provider request | validation-failure detail derived from model output is sent back out to the LLM provider |
| client email -> outbound threading headers | inbound Message-ID flows into outbound In-Reply-To/References |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | app/routes/dashboard.py eval_view fixture read | medium | mitigate | Task 2: Path.resolve() + is_relative_to containment, refusals routed to the existing missing-file fallback, regression test in tests/test_dashboard.py |
| T-15-02 | Information Disclosure | app/llm/client.py retry prompt | low | mitigate | Task 3: errors(include_url=False, include_input=False) summary replaces raw exc interpolation; regression test asserts no input echo |
| T-15-03 | Tampering | outbound threading chain after retrigger | medium | mitigate | Task 1: hermetic regression test drives the real (run_id, purpose, round, epoch) upsert + References chain and pins the anchor |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- `uv run pytest -q` full suite green (new tests included, zero existing assertion changes).
- `uv run ruff check` and `uv run mypy` exit 0.
- Commit history shows failing-test commits preceding fix commits for WR-05 and INFO-02.
</verification>

<success_criteria>
- WR-01 has an executable, hermetic, non-vacuous proof (or a failing-test-then-fix pair if
  the proof exposed a real break).
- WR-05 and INFO-02 are fixed with regression tests; no other behavior changed.
- All three new/extended test surfaces contain zero ticket-shaped identifiers.
</success_criteria>

## Artifacts this phase produces (this plan)

- New file: `tests/test_retrigger_threading.py` — test functions incl.
  `test_threading_anchor_survives_crash_then_retrigger` (route-driven, asserts captured
  kwargs + persisted row) and `test_retrigger_send_preserves_prior_outbound_history`
  (stale-row-unchanged + epoch-bump proof; final names at executor discretion, descriptive
  and ticket-free), module-local bare-object builder helpers.
- New test function: `test_eval_view_refuses_fixture_path_traversal` in tests/test_dashboard.py.
- New test function: `test_retry_prompt_scrubs_validation_input_values` in tests/test_llm_client.py.
- New containment branch inside `eval_view` (app/routes/dashboard.py) using `is_relative_to`.
- Optional new module-private helper `_scrubbed_validation_summary` in app/llm/client.py.

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-01-SUMMARY.md` when done.
Record per-item outcome (WR-01 green-first-run vs fixed; WR-05 fixed; INFO-02 fixed) — plan
15-10 reads this to write the todo 260623-01 disposition record.
</output>
