---
phase: 15-comment-hygiene-deferred-polish-triage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_retrigger_threading.py
  - tests/test_email_epoch_arbiter_integration.py
  - .github/workflows/concurrency-proof.yml
autonomous: true
requirements: [POLISH-01]

must_haves:
  truths:
    - "A hermetic regression test drives the REAL POST /runs/{run_id}/retrigger route and the real background pipeline, and proves route wiring + pipeline resumption + gateway threading-header derivation: the next outbound email's in_reply_to/references anchor the client's original inbound Message-ID, asserted on the captured send_outbound kwargs AND on the row persisted through the in-memory repo"
    - "The crashed state the recovery pass starts from is PRODUCED BY REAL CODE — a one-shot injected failure at a post-send persistence step inside the real clarify stage, with the real orchestrator error-wrap persisting ERROR — not seeded by the test"
    - "A real-Postgres integration test proves the production (run_id, purpose, round, epoch) SQL arbiter: after a real reply_epoch bump, a second outbound write at the SAME (run_id, purpose, round) INSERTs a second row and leaves the historical row byte-for-byte unchanged; a same-epoch retry still upserts in place"
    - "The real-Postgres test actually EXECUTES in CI — .github/workflows/concurrency-proof.yml names the new test file in its pytest selection, alongside tests/test_concurrency_proof.py"
  artifacts:
    - path: "tests/test_retrigger_threading.py"
      provides: "hermetic crash -> retrigger -> send threading regression proof, scoped to route/pipeline/gateway (D-10)"
      contains: "def test_"
    - path: "tests/test_email_epoch_arbiter_integration.py"
      provides: "real-Postgres proof of the four-column ON CONFLICT arbiter and its unique constraint (the seam fake_repo cannot reach)"
      contains: "pytest.mark.integration"
    - path: ".github/workflows/concurrency-proof.yml"
      provides: "the only CI job with a real Postgres — now selects the epoch-arbiter proof too"
      contains: "tests/test_email_epoch_arbiter_integration.py"
  key_links:
    - from: "tests/test_retrigger_threading.py"
      to: "app/routes/runs.py"
      via: "TestClient POST /runs/{run_id}/retrigger driving the real recovery route + the run_pipeline_bg background task TestClient executes inline"
      pattern: "retrigger"
    - from: "tests/test_retrigger_threading.py"
      to: "app/email/gateway.py"
      via: "spy-wrap of send_outbound that calls through — captured in_reply_to/references kwargs plus the row persisted via the in-memory repo"
      pattern: "send_outbound"
    - from: "tests/test_email_epoch_arbiter_integration.py"
      to: "app/db/repo/emails.py"
      via: "real repo.insert_email_message executed against live Postgres, read back with an explicit SELECT over repo.get_connection()"
      pattern: "insert_email_message"
    - from: ".github/workflows/concurrency-proof.yml"
      to: "tests/test_email_epoch_arbiter_integration.py"
      via: "the pytest file selection in the 'real Postgres' job — the only job that sets DATABASE_URL"
      pattern: "test_email_epoch_arbiter_integration"
---

<objective>
Close POLISH-01's WR-01 item (reply threading after crash + retrigger) with proof that is
honest about which seam it exercises. Two tests, not one, plus the CI wiring that makes the
second one real:

1. A HERMETIC test that drives the real retrigger route and the real background pipeline, and
   proves route wiring + pipeline resumption + gateway threading-header derivation against the
   in-memory repo.
2. A REAL-POSTGRES test that drives the production `repo.insert_email_message` and proves the
   `(run_id, purpose, round, epoch)` ON CONFLICT arbiter preserves the historical outbound row
   across a `reply_epoch` bump — the one thing the hermetic test structurally cannot prove.
3. The workflow edit that makes (2) actually execute in CI.

Purpose: D-10 says WR-01 must be verified by an executable proof, not by reading, and the
Phase 10 lesson says a proof that cannot fail is worse than no proof. The `fake_repo` fixture
monkeypatches ~40 names onto the `app.db.repo` package (tests/conftest.py, patch list includes
`insert_email_message`), so ANY test taking `fake_repo` runs a hand-written in-memory
reimplementation of the epoch upsert, never `app/db/repo/emails.py`. That module's own
docstring warns that "the constraint and the ON CONFLICT clause must never drift apart" — and
nothing in the suite currently enforces that pairing. This plan writes the test that does, and
wires it into the only CI job that has a Postgres.

Output: two new test files, one workflow edit. WR-05 and INFO-02 (the other two sanctioned
POLISH-01 behavior fixes) live in plan 15-11.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-CONTEXT.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-RESEARCH.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md
@.planning/phases/15-comment-hygiene-deferred-polish-triage/15-REVIEWS.md
@.planning/todos/pending/260623-01-phase05-review-warnings.md
</context>

<important_constraints>
- Test-first commit discipline (D-10, Phase 14 D-08 protocol): if any assertion is RED against
  current source, commit the failing test first, then the minimal fix in a separate commit.
  Never bury a production fix inside another commit.
- Live-key hazard: this repo's `.env` contains LIVE LLM keys. Every new test must keep LLM
  seams stubbed (stub `app.pipeline.suggest.suggest_employees`; use the `mock_llm` fixture).
  Never rely on env emptiness.
- All new test code must be born clean: no ticket IDs, phase numbers, review-round references,
  or planning-document citations in file names, test names, docstrings, comments, or failure
  messages. Plan 15-10's guard scans `tests/**/*.py` on the live tree and will fail otherwise.
  Failure messages explain the invariant in plain English.
- mypy strict covers tests/ — annotate all new test functions and helpers (`-> None`, typed
  locals where inference fails).
- Every commit: `uv run pytest -q && uv run ruff check && uv run mypy` green (uv only, never
  pip).
- Honesty rule for this plan: no test may claim, in its name, docstring, or failure message,
  to prove a seam it does not execute. The hermetic test's language is scoped to route,
  pipeline, and gateway. The word "SQL", "Postgres", or "constraint" belongs only in the
  integration test.
</important_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hermetic crash -> retrigger -> send threading test (route/pipeline/gateway scope, D-10)</name>
  <files>tests/test_retrigger_threading.py</files>
  <read_first>
    - tests/test_retrigger_epoch.py (the structural analog: `monkeypatch, fake_repo, mock_llm`
      fixtures, module-local `_bare_roster`/`_bare_inbound`/`_bare_decision`/`_bare_extracted`
      builders, seed state written into `fake_repo.runs` / `fake_repo.outbound`, and the
      spy-wrap of `gateway.send_outbound` that calls through to the saved real function)
    - tests/conftest.py — READ THE FIXTURE BODIES, not just the names. Three facts govern this
      task: (a) `fake_repo` monkeypatches ~40 names onto the `app.db.repo` PACKAGE, and the
      patch list INCLUDES `insert_email_message`, so under this fixture `app/db/repo/emails.py`
      never executes and the epoch upsert you observe is the fake's hand-written Python loop;
      (b) the fake mirrors the `reply_epoch` bump inside its own `clear_reply_context`;
      (c) the mocked context globally no-ops `resend.Emails.send`, so the PROVIDER boundary is
      already stubbed — leave it stubbed, and stub nothing else at the app layer. Also confirm
      the fake implements `set_pre_clarify_extracted`, `record_run_error`, and
      `get_record_only_flag`; if a seam the real pipeline calls is missing from the fake,
      extend the fake minimally — never stub an app-layer function to route around it.
    - tests/test_hitl.py lines ~140-162 (`test_retrigger_from_error_backgrounds_pipeline`) —
      the exact route-driving idiom: the conftest `client` fixture,
      `client.post(f"/runs/{run_id}/retrigger", follow_redirects=False)`, expect 303. FastAPI's
      TestClient executes the scheduled BackgroundTask inline before `post()` returns.
    - app/routes/runs.py (the POST /runs/{run_id}/retrigger route at ~line 266: CAS-claims from
      ERROR/APPROVED/stale in-flight, calls clear_reply_context at the single post-claim
      convergence point, schedules run_pipeline_bg, returns 303)
    - app/routes/pipeline_glue.py (`run_pipeline_bg` — its `except Exception` is a background
      safety net that SWALLOWS everything. A route-driven failure therefore cannot propagate
      out of `client.post(...)`. This test must assert on PERSISTED STATE and must never use
      `pytest.raises` around the route call.)
    - app/pipeline/orchestrator.py lines ~210-235 (the error-wrap boundary: it catches, calls
      `repo.record_run_error(...)`, and does NOT re-raise — so a direct `run_pipeline(run_id)`
      call with an injected failure returns normally and leaves the run in ERROR)
    - app/pipeline/clarification.py lines ~450-480 (the live path: `gateway.send_outbound(...)`
      returns FIRST, then a transaction runs `set_pre_clarify_extracted` /
      `set_clarification_round` / `set_status(AWAITING_REPLY)` — the first of those three is the
      post-send persistence step to inject the one-shot failure into)
    - app/email/gateway.py (`send_outbound` signature and `get_outbound_references_chain` — the
      References chain is rebuilt from durable rows, not from ephemeral webhook state)
    - .planning/phases/15-comment-hygiene-deferred-polish-triage/15-PATTERNS.md (WR-01 section)
  </read_first>
  <behavior>
    - Test 1 (threading anchor): after a run crashes at a post-send persistence step and the
      operator hits the REAL POST /runs/{run_id}/retrigger route, the NEXT outbound email's
      in_reply_to and references anchor the client's original inbound Message-ID — a distinctive
      sentinel such as "&lt;client-inbound-thread-anchor@example.test&gt;" seeded on the inbound
      row. Asserted on BOTH the captured send_outbound kwargs AND the newly persisted outbound
      row's in_reply_to + references_header values.
    - Test 2 (history preserved through the recovery pass): after recovery, the pre-crash sent
      outbound row is unchanged (same message_id, round, epoch, send_state) and a NEW row exists
      with a strictly greater epoch — the retrigger's epoch bump makes the fresh send a distinct
      conflict target instead of overwriting the prior send. Scope note this test states plainly
      in its own docstring: the persistence it observes is the in-memory test repo, so this pins
      the ROUTE and GATEWAY behavior (a fresh row is written, the old one is not addressed); the
      production SQL arbiter is proven separately against real Postgres in Task 2.
    - Setup assertion (proves the crash was real): before the retrigger, the run's status is
      ERROR and exactly one outbound clarification row exists at round 0 / epoch 0 with
      send_state "sent" — produced by real code, not written by the test.
  </behavior>
  <action>
    Create tests/test_retrigger_threading.py. Follow tests/test_retrigger_epoch.py structurally:
    take `monkeypatch, client, fake_repo, mock_llm`; build minimal contract objects with
    module-local bare-object helpers.

    SETUP — the crash must be PRODUCED BY REAL CODE (mandatory per the cross-AI review; direct
    ERROR seeding is no longer acceptable as the default):
    (a) Seed a business/run/inbound triple through the fake's own repo helpers: an inbound
        email_messages row carrying the sentinel Message-ID, and a run created against it.
    (b) Script `mock_llm`'s extraction so the submitted employee name does NOT resolve against
        the seeded roster (source=none) — `decide()` then returns request_clarification and the
        real `_clarify` stage runs, which is the stage that sends the outbound clarification.
    (c) Arm a ONE-SHOT failure at the first post-send persistence step: wrap
        `app.db.repo.set_pre_clarify_extracted` with a callable that raises on its FIRST
        invocation and delegates to the saved attribute on every later invocation. This lands
        strictly AFTER `gateway.send_outbound` has returned (so the outbound row is genuinely
        written 'sent' by real code) and strictly BEFORE the status advance — exactly the
        post-send crash window WR-01 describes. One-shot, not permanent, so the orchestrator's
        error-wrap can still persist ERROR on its own call path.
    (d) Drive the first pass through REAL code: call
        `app.pipeline.orchestrator.run_pipeline(run_id)` directly. It owns the error-wrap that
        calls `repo.record_run_error(...)` and persists ERROR; it does NOT re-raise, so do NOT
        wrap this in `pytest.raises`. (Equivalent real driver, if scripting extraction through
        run_pipeline proves awkward: POST /webhook/inbound, which schedules run_pipeline_bg and
        TestClient runs it inline. Either is real code. What is NOT acceptable is writing the
        ERROR status and the sent row by hand.)
    (e) Restore the real attribute, then ASSERT the crashed state before recovering: status
        ERROR; exactly one outbound clarification row; capture its message_id, round, epoch and
        send_state into locals for the post-recovery comparison.

    NARROW FALLBACK (use only if (c)+(d) genuinely cannot produce the crashed state, and say so
    explicitly in the summary): seed the durable post-crash state directly. If you take it, the
    test function names, docstrings, and every acceptance statement must be renamed away from
    "crash" to "durable recovery state" — the test must never claim more than it proves.

    RECOVERY — no simulation: `client.post(f"/runs/{run_id}/retrigger", follow_redirects=False)`
    and assert 303. TestClient runs the scheduled `run_pipeline_bg` inline before the call
    returns, so the real background pipeline, clarify stage, and `gateway.send_outbound` body all
    execute. Calling the route's internals (clear_reply_context + a resume entry point) instead
    of the route is FORBIDDEN for the recovery pass.

    Spy-wrap `gateway.send_outbound` (append kwargs to a list, then call the saved real function
    — never replace it) and stub `app.pipeline.suggest.suggest_employees` to a fixed dict
    (live-key hazard). The provider boundary is already stubbed by conftest's global
    `resend.Emails.send` no-op — leave it. Stub NOTHING else: not the delivery/clarification/
    orchestrator entry points, not `get_outbound_references_chain`.

    On `insert_email_message`: this plan makes NO instruction to leave it unstubbed, because
    under `fake_repo` that is impossible — conftest already patches it onto the repo package,
    and the earlier draft of this plan contradicted itself by demanding otherwise. Do not fight
    the fixture. Instead, state the scope honestly in the module docstring: this file proves the
    retrigger route claims and resumes the run, that the pipeline reaches the send, and that the
    gateway derives the threading headers from the original inbound; the production SQL arbiter
    is proven in tests/test_email_epoch_arbiter_integration.py.

    ASSERT (recovery pass): (1) the captured send kwargs — in_reply_to equals the sentinel and
    references contains it; (2) the newly persisted outbound row — epoch strictly greater than
    the pre-crash row's, send_state "sent", in_reply_to equal to the sentinel, references_header
    containing the sentinel; (3) the pre-crash row is unchanged (message_id, round, epoch,
    send_state identical to the values captured in setup step (e)).

    If any assertion is RED against current source: commit the failing test first, then the
    smallest fix at the seam that breaks the chain (money-path protocol, D-10). If GREEN on the
    first run, that is the proof the epoch machinery already resolved WR-01's premise — keep the
    test as the permanent regression gate either way.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_retrigger_threading.py && uv run ruff check tests/test_retrigger_threading.py && uv run mypy</automated>
  </verify>
  <done>
    tests/test_retrigger_threading.py passes hermetically (no DB, no live LLM). The crashed state
    is produced by a one-shot injected post-send failure through real pipeline code; the recovery
    pass is a real TestClient POST to /runs/{run_id}/retrigger; the threading anchor is asserted
    on both the captured send_outbound kwargs and the persisted row; the pre-crash row is proven
    unchanged. The file's own docstring scopes its claim to route/pipeline/gateway and points at
    the integration twin for the SQL arbiter.
  </done>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_retrigger_threading.py` exits 0.
    - The recovery pass is a real TestClient POST to `/runs/{run_id}/retrigger` and asserts a 303
      (source assertion: the route path string and the 303 appear in the test; no route-internal
      or pipeline_glue function is called directly for the recovery pass).
    - The crashed state is produced by a one-shot failure injection at a post-send persistence
      step followed by a real pipeline entry-point call (source assertion: the injected wrapper
      raises on first call and delegates thereafter, and the real orchestrator or webhook route
      is invoked). If the narrow fallback was taken instead, NO test name, docstring, or failure
      message in the file contains the word "crash", and the summary records why.
    - `pytest.raises` does not appear anywhere in the file (source assertion — `run_pipeline_bg`
      swallows exceptions and `run_pipeline` does not re-raise; state is the only honest oracle).
    - The test file monkeypatches `gateway.send_outbound` with a wrapper that CALLS the original
      (source assertion: the wrapper body references the saved real function).
    - The test asserts an in_reply_to/references value equal to or containing the seeded inbound
      sentinel Message-ID (source assertion: the sentinel string appears in both the seed block
      and an assert).
    - The test asserts the persisted new outbound row's in_reply_to and references_header — not
      only the captured call kwargs (source assertion).
    - The test asserts the pre-crash row's message_id/round/epoch/send_state are unchanged after
      recovery AND that the new row's epoch is strictly greater (source assertion).
    - The module docstring states that the persistence observed here is the in-memory test repo
      and names tests/test_email_epoch_arbiter_integration.py as the proof of the production SQL
      arbiter (source assertion — no overclaiming).
    - The only stubs in the file are the provider method, `suggest_employees`, and the one-shot
      post-send failure injection (source assertion).
    - No ticket-shaped identifier, phase number, or planning-document citation appears in the
      file (plan 15-10's tree-wide guard scans tests/ and will fail otherwise).
    - `uv run mypy` exits 0 (new file annotated for strict mode).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Real-Postgres proof of the four-column ON CONFLICT epoch arbiter</name>
  <files>tests/test_email_epoch_arbiter_integration.py</files>
  <read_first>
    - app/db/repo/emails.py `insert_email_message` — the outbound branch stamps
      `epoch = (SELECT reply_epoch FROM payroll_runs WHERE id = %s)` in the INSERT and arbitrates
      on `ON CONFLICT (run_id, purpose, round, epoch)`. Its docstring states the invariant this
      test exists to enforce: the unique constraint and the ON CONFLICT clause must never drift
      apart. Nothing enforces that today.
    - app/db/schema.sql — the `uq_email_run_purpose_round_epoch` unique constraint (~line 265).
      The four columns here and the four in the ON CONFLICT clause are the pair under test.
    - app/db/repo/pipeline_state.py `clear_reply_context` — the production function that bumps
      `reply_epoch = reply_epoch + 1`. The test must call THIS, not simulate the bump.
    - tests/test_gateway.py `test_inbound_reply_routes_to_correct_run_integration` (~lines
      1156-1226) — the idiom to copy verbatim in shape: `@pytest.mark.integration`, live-DB
      guard, `_seed(dry_run=True)` to get a business_id, real `repo.insert_inbound_email` +
      `repo.create_run` + `repo.insert_email_message`, then an assertion driven through the real
      SQL. Its docstring explicitly frames it as the real-SQL twin of a mocked unit test — this
      file is the same relationship to tests/test_retrigger_threading.py.
    - tests/test_atomic_persist.py lines ~35-45 — the module-local `_HAS_DB` / `_HAS_RESET` /
      `_SKIP_LIVE_DB` two-factor guard idiom (each integration module declares its own).
    - tests/conftest.py lines ~40-75 — the module-scoped `seeded_db` fixture
      (`bootstrap(reset=True)` then `seed()`); take it as a fixture argument.
    - tests/test_concurrency_proof.py line ~250 — the direct-SQL read-back idiom:
      `with repo.get_connection() as conn, conn.cursor() as cur:` … `cur.execute(...)`. Needed
      because `repo.load_outbound_emails` does NOT project epoch / round / send_state.
    - pyproject.toml — the registered `integration` marker.
  </read_first>
  <behavior>
    - Test 1 (epoch bump appends, never clobbers): write an outbound row at
      (run_id, purpose="clarification", round=0) while the run is at reply_epoch 0; call the real
      `repo.clear_reply_context(run_id)` to bump reply_epoch to 1; write again at the SAME
      (run_id, purpose, round) with a DIFFERENT message_id. Real Postgres must now hold exactly
      TWO outbound clarification rows for that run: the epoch-0 row unchanged (same message_id,
      round 0, epoch 0, send_state "sent") plus a new epoch-1 row. One row, or a mutated
      historical row, is the failure — that is a silently clobbered append-only audit log.
    - Test 2 (same-epoch retry still upserts in place): write an outbound row at
      (run_id, purpose, round=0, epoch 0) with send_state "reserved", then write again at the same
      tuple with send_state "sent" and NO epoch bump in between. Real Postgres must hold exactly
      ONE row, advanced to "sent". This is the other half of the arbiter contract — without it, a
      "just always INSERT" regression would pass Test 1.
  </behavior>
  <action>
    Create tests/test_email_epoch_arbiter_integration.py: a real-Postgres integration module that
    executes the PRODUCTION `app.db.repo` functions. It takes NO `fake_repo` and NO `mock_llm` —
    those fixtures are exactly what this file exists to escape.

    Module head: `from __future__ import annotations`, module-local `_HAS_DB` / `_HAS_RESET` /
    `_SKIP_LIVE_DB` two-factor guard mirroring tests/test_atomic_persist.py, and a module
    docstring stating the invariant in plain English: the outbound upsert's ON CONFLICT column
    list and the email_messages unique constraint name the same four columns (run_id, purpose,
    round, epoch); if they ever drift apart, a retriggered run's fresh send silently overwrites
    the pre-retrigger send instead of appending, and the audit log loses a delivered email.
    No ticket IDs, no phase numbers, no planning-document citations.

    Both tests carry `@_SKIP_LIVE_DB` and `@pytest.mark.integration` and take the `seeded_db`
    fixture. Setup per test (copy tests/test_gateway.py's integration idiom): get a business_id
    from `seed(dry_run=True)`, insert a real inbound row via `repo.insert_inbound_email` with a
    uuid-unique sentinel Message-ID, create the run via `repo.create_run(business_id=...,
    source_email_id=...)`. Each test uses its own fresh run so the module-scoped seeded DB is not
    order-sensitive.

    Test 1 sequence, all through production code:
      1. `repo.insert_email_message(run_id=..., direction="outbound", message_id=MID_EPOCH0,
         purpose="clarification", send_state="sent", round=0, in_reply_to=<inbound sentinel>,
         references_header=<inbound sentinel>, subject=..., to_addr=..., from_addr=...,
         body_text=...)` — the run is at reply_epoch 0, so the correlated subquery stamps epoch 0.
      2. `repo.clear_reply_context(run_id)` — the REAL production bump. Assert
         `SELECT reply_epoch FROM payroll_runs WHERE id = %s` is now 1.
      3. `repo.insert_email_message(...)` again with the SAME run_id / purpose / round=0 but
         message_id=MID_EPOCH1.
      4. Read back with an explicit SELECT over `repo.get_connection()`:
         `SELECT message_id, round, epoch, send_state FROM email_messages WHERE run_id = %s AND
         direction = 'outbound' AND purpose = 'clarification' ORDER BY epoch`.
      5. Assert exactly two rows; row[0] == (MID_EPOCH0, 0, 0, "sent") — the historical row
         untouched; row[1].message_id == MID_EPOCH1 and row[1].epoch == 1 and row[1].round == 0.

    Test 2 sequence: insert at (round=0, purpose="clarification") with send_state="reserved",
    then insert again at the same tuple with send_state="sent" and a new message_id, WITHOUT
    calling clear_reply_context. Read back the same way and assert exactly ONE row whose
    send_state is "sent" (the DO UPDATE clause sets send_state and message_id from EXCLUDED).

    Failure messages: plain English, naming the consequence — e.g. that a second outbound row at
    the same purpose/round after an epoch bump must be a NEW row, because overwriting the prior
    row would erase a delivered email from the append-only log and break reply threading for the
    original conversation. No ticket vocabulary.

    Annotate for strict mypy (`-> None` on both tests; type the read-back rows). Run locally
    against a Postgres with DATABASE_URL + ALLOW_DB_RESET=1 exported (e.g. a local container) to
    confirm the tests actually pass rather than skip — a skipped test is not a proof. Record in
    the summary BOTH the skipped-by-default run and the executed run.
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_email_epoch_arbiter_integration.py && uv run ruff check tests/test_email_epoch_arbiter_integration.py && uv run mypy</automated>
  </verify>
  <done>
    The file exists, is marked `integration` + live-DB-guarded (so the default hermetic suite
    skips it cleanly), executes the production `insert_email_message` and `clear_reply_context`
    against real Postgres, and proves both directions of the arbiter: an epoch bump appends a new
    row and leaves the historical row unchanged; a same-epoch retry upserts in place.
  </done>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_email_epoch_arbiter_integration.py` exits 0 with the tests
      SKIPPED when DATABASE_URL / ALLOW_DB_RESET are unset (hermetic default preserved).
    - With DATABASE_URL and ALLOW_DB_RESET=1 exported against a real Postgres, the same command
      exits 0 with the tests EXECUTED (not skipped) — the summary records this run's output.
    - Neither `fake_repo` nor `mock_llm` appears anywhere in the file (source assertion — the
      whole point is to reach `app/db/repo/emails.py`).
    - The file calls `repo.clear_reply_context` and `repo.insert_email_message` directly (source
      assertion), and reads rows back with an explicit SELECT naming the `epoch` column (source
      assertion).
    - Test 1 asserts exactly TWO rows after the bump, with the epoch-0 row's message_id, round,
      epoch and send_state all unchanged (source assertion).
    - Test 2 asserts exactly ONE row for a same-epoch retry, advanced to send_state 'sent'
      (source assertion).
    - Both tests carry `@pytest.mark.integration` (source assertion — Task 3's CI selection uses
      `-m integration`).
    - No ticket-shaped identifier, phase number, or planning-document citation appears in the
      file (plan 15-10's tree-wide guard scans tests/ and will fail otherwise).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire the epoch-arbiter proof into the real-Postgres CI job (without this, Task 2 is vacuous)</name>
  <files>.github/workflows/concurrency-proof.yml</files>
  <read_first>
    - .github/workflows/concurrency-proof.yml — the ONLY job with a real Postgres (a postgres:16
      service, DATABASE_URL, ALLOW_DB_RESET=1, schema applied via `uv run python -m
      app.db.bootstrap`). Its run step hard-codes a SINGLE file:
      `uv run pytest tests/test_concurrency_proof.py -m integration -v`. Note also the `on:`
      block — this workflow triggers on push to master and workflow_dispatch, NOT on pull
      requests.
    - .github/workflows/ci.yml (the `test` job) — it runs `uv run pytest -q` and deliberately
      sets NO DATABASE_URL, so every `@pytest.mark.integration` test SELF-SKIPS there. This is
      why an integration test that is not named in the concurrency-proof workflow never executes
      anywhere in CI.
  </read_first>
  <action>
    Edit the "Run the concurrency proof" step so the new epoch-arbiter module actually executes.
    Change the run line from a single-file selection to:
    `uv run pytest tests/test_concurrency_proof.py tests/test_email_epoch_arbiter_integration.py
    -m integration -v`. Rename the step to reflect that it now runs more than one proof (e.g.
    "Run the real-Postgres invariant proofs").

    Rewrite the step's comment to say, in plain English: `-m integration` selects the live-DB
    proofs that the default hermetic suite skips, and this workflow selects test FILES BY NAME —
    so a new live-DB test must be added to this list or it will silently skip forever in every
    CI job. Name what the second module proves: that the outbound upsert's ON CONFLICT column
    list and the email_messages unique constraint stay in agreement, so a retriggered run appends
    a new outbound row instead of overwriting the one it already sent.

    Also add a short comment near the `on:` block recording the residual limitation honestly:
    these proofs run on push to master and on manual dispatch, not on pull requests — so they are
    a post-merge gate on master, not a pre-merge one. Do NOT add a `pull_request:` trigger in
    this plan (that is a CI-policy change beyond POLISH-01's scope); just stop the phase from
    silently overclaiming PR-level coverage. Record the same limitation, in one sentence, in the
    plan summary.

    Do not change the postgres service block, the env block, the bootstrap step, or the trigger
    list. Keep every comment plain-English and free of ticket IDs (this file is outside plan
    15-10's guard scan set, but the phase's editorial rubric still applies to what we write).
  </action>
  <verify>
    <automated>RUNLINE=$(grep -E '^[[:space:]]+run:.*pytest' .github/workflows/concurrency-proof.yml) && echo "$RUNLINE" | grep -q 'tests/test_concurrency_proof.py' && echo "$RUNLINE" | grep -q 'tests/test_email_epoch_arbiter_integration.py' && echo "$RUNLINE" | grep -q -- '-m integration' && echo "workflow pytest selection: $RUNLINE"</automated>
  </verify>
  <done>
    The real-Postgres CI job's pytest selection names BOTH tests/test_concurrency_proof.py and
    tests/test_email_epoch_arbiter_integration.py, the step comment explains that files are
    selected by name (so future live-DB tests must be added here), and the push-only-not-PR
    limitation is recorded in the workflow and in the summary.
  </done>
  <acceptance_criteria>
    - `grep -q 'tests/test_email_epoch_arbiter_integration.py' .github/workflows/concurrency-proof.yml`
      exits 0 — the workflow names the new test path.
    - The job's single `run:` pytest line contains BOTH test file paths AND `-m integration` (the
      verify command above asserts this mechanically, with no extra dependencies).
    - The postgres service block, `DATABASE_URL`, `ALLOW_DB_RESET`, the bootstrap step, and the
      `on:` trigger list are byte-identical to their pre-edit values (diff inspection).
    - The workflow contains a comment stating that this job selects live-DB test files by name.
    - The workflow contains a comment stating the proofs run on push to master / manual dispatch,
      not on pull requests; the plan summary repeats that sentence.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client email -> outbound threading headers | the client's inbound Message-ID flows into outbound In-Reply-To/References; a break silently strands the conversation |
| pipeline crash -> operator retrigger -> re-send | a recovery pass re-writes an outbound row for a run that already sent one |
| ON CONFLICT clause <-> unique constraint | two independently-editable declarations (Python string and schema.sql) must name the same four columns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03 | Tampering | outbound threading chain after retrigger | medium | mitigate | Task 1: hermetic route-driven regression test pins the inbound Message-ID anchor across crash -> retrigger -> send on both the captured gateway kwargs and the persisted row |
| T-15-08 | Repudiation | email_messages append-only audit log vs the (run_id, purpose, round, epoch) arbiter | high | mitigate | Task 2: real-Postgres test proves a post-bump write APPENDS and leaves the historical row unchanged (drift between the ON CONFLICT clause and uq_email_run_purpose_round_epoch would silently erase a delivered email); Task 3 makes that test execute in CI |
| T-15-09 | Tampering (regression) | a live-DB test that never runs in any CI job | high | mitigate | Task 3: the proof is named in the only workflow that provisions Postgres; an acceptance criterion greps the workflow for the file path, so the test cannot be quietly stranded |

No package installs in this plan (Package Legitimacy Audit: N/A per 15-RESEARCH.md).
</threat_model>

<verification>
- `uv run pytest -q` full suite green (both new files collected; the integration module skips
  hermetically).
- `uv run pytest -q tests/test_email_epoch_arbiter_integration.py` with DATABASE_URL +
  ALLOW_DB_RESET=1 exported: tests EXECUTE and pass (output recorded in the summary).
- `uv run ruff check` and `uv run mypy` exit 0.
- The concurrency-proof workflow's pytest selection names the new integration module.
</verification>

<success_criteria>
- WR-01 has an executable, non-vacuous proof whose claims match the seams it actually executes:
  route/pipeline/gateway hermetically, and the production SQL arbiter against real Postgres.
- The crashed state that the recovery pass recovers from is produced by real code.
- The real-Postgres proof runs in CI rather than silently skipping.
- Both new test files contain zero ticket-shaped identifiers.
</success_criteria>

## Artifacts this phase produces (this plan)

- New file: `tests/test_retrigger_threading.py` — hermetic route-driven threading regression
  (crash produced by a one-shot post-send failure injection; asserts captured send kwargs, the
  new persisted row, and the unchanged pre-crash row). Test names at executor discretion,
  descriptive and ticket-free; module-local bare-object builder helpers.
- New file: `tests/test_email_epoch_arbiter_integration.py` — real-Postgres proof of the
  four-column ON CONFLICT arbiter (module-local `_HAS_DB` / `_HAS_RESET` / `_SKIP_LIVE_DB`
  constants; two `@pytest.mark.integration` tests: epoch bump appends + same-epoch retry upserts).
- Edited file: `.github/workflows/concurrency-proof.yml` — the pytest selection in the
  real-Postgres job now names both proof modules; comments record the by-name selection rule and
  the push-only (not PR) trigger limitation.

<output>
Create `.planning/phases/15-comment-hygiene-deferred-polish-triage/15-01-SUMMARY.md` when done.
Record: whether the hermetic test was green on first run or exposed a real break (and the fix if
so); whether the mandatory failure injection worked or the narrow fallback was taken (and why);
the executed (non-skipped) integration run output; and the one-sentence CI limitation note. Plan
15-10 reads this to write the todo 260623-01 disposition record.
</output>
