---
phase: 22-frontend-foundation-runs-list
plan: 07
type: execute
wave: 4
depends_on: ["22-04"]
files_modified:
  - app/schemas/run_status.py
  - app/schemas/run_columns.py
  - app/routes/runs.py
  - tests/test_schema_projection.py
autonomous: true
requirements: [SHELL-07, GUARD-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "If the parsed run-column set were empty or unparseable the classification test fails loudly rather than passing on an empty difference: it asserts the parsed set is non-empty and equals the known column names exactly, so a vacuous pass is impossible."
    - "Column names are parsed by splitting the SQL column string on commas and stripping surrounding whitespace, so a column written with extra whitespace or across a line continuation still matches its declared classification by exact name."
    - "Adding a column to the run-column list without either exposing it in a page's response shape or naming it internal-only fails CI, and the failure message identifies the column by name."
    - "The list page and the detail page carry separate declared response shapes: the list projection carries a created-at column the detail column list does not, so one shared shape is structurally impossible."
    - "The status poll endpoint declares a response model, and its response body is byte-identical to the body it returned before the model was declared."
    - "Every one of the fifteen run columns has exactly one classification: exposed on the list shape, exposed on the detail shape, or named internal-only."
  artifacts:
    - app/schemas/run_status.py
    - app/schemas/run_columns.py
  key_links:
    - "`app/schemas/run_status.py`'s `RunStatusPoll` is the shape the status poll endpoint already returns field-for-field today; declaring it as the route's response model formalizes an existing contract and puts it into the application's own OpenAPI document natively."
    - "`RunListRow` composes the same seven volatile fields, so the poller in plan 22-10 can replace the volatile part wholesale and the type system enforces the merge."
    - "`app/schemas/run_columns.py` cross-references the run-column constant in `app/db/repo/runs.py`; it reads that constant, it never edits it — `app/db/` is behind the milestone's untouchable fence."
  prohibitions: []
---

<objective>
Declare the poll shape and close GUARD-04: make a new database column that reaches the browser layer
impossible to add silently. Today `_safe_run_for_browser` is a denylist that lets `business_id`,
`source_email_id`, `reply_epoch`, `alias_candidates`, `extracted_data`, `reconciliation` and
`decision` through untouched, and auto-exposes any column later appended to the column list.

Purpose: SHELL-07's two-separate-shapes half, GUARD-04.
Output: `RunStatusPoll`, an explicit three-way column classification, a response model on the status
poll endpoint, and a drift test that names the offending column.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md
@.planning/phases/22-frontend-foundation-runs-list/22-PATTERNS.md
@.planning/phases/22-frontend-foundation-runs-list/22-04-SUMMARY.md
</context>

## Artifacts this phase produces

New Python (app): `app/schemas/__init__.py`; `app/schemas/_projection.py` (`RowProjection`,
`UnclassifiedColumnError`); `app/schemas/runs_list.py` (`RunListRow`, `RunsListPage`);
`app/schemas/run_status.py` (`RunStatusPoll`); `app/schemas/run_columns.py`
(`RUN_COL_CLASSIFICATION`, `ColumnExposure`); `app/routes/templating.py` gains `render_react_page`,
`json_script`, `load_manifest`, `ManifestMissingError`, `_JSON_SCRIPT_ESCAPES`; `app/config.py`
gains `Settings.vite_dev_server_url`.
New Python (scripts): `scripts/render_assertion_inventory.py`; `scripts/generate_openapi_doc.py`.
New Python (tests): `tests/assertion_inventory.py`; `tests/safety_mutation_registry.py`;
`tests/test_inventory_completeness.py`; `tests/test_safety_mutation_registry.py`;
`tests/test_route_shadowing.py`; `tests/test_no_html_on_service_routes.py`;
`tests/test_page_shell_pins.py`; `tests/test_react_page_render.py`;
`tests/test_bundle_asset_exists.py`; `tests/test_schema_projection.py`;
`tests/test_no_fetch_outside_poller.py`; `tests/test_react_dev_mode.py`;
`tests/test_generated_types_staleness.py`; `tests/test_ci_gate_config.py`.
New templates/fixtures: `app/templates/react_page.html`; `tests/fixtures/vite_manifest.json`.
New frontend tree: `frontend/package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`,
`tsconfig.node.json`, `eslint.config.js`, `vitest.config.ts`, `MANIFEST-SHAPE.md`;
`frontend/src/entries/runs.tsx`; `frontend/src/boot/pageData.ts`; `frontend/src/pages/RunsPage.tsx`
+ `RunsPage.test.tsx`; `frontend/src/components/StatusBadge.tsx`, `QueueBadge.tsx`,
`FailureSummary.tsx`, `MutationForm.tsx`, `ConfirmForm.tsx`, `DecisionBanner.tsx`,
`MutationForm.test.tsx`; `frontend/src/hooks/usePoller.ts` + `usePoller.test.ts`;
`frontend/src/types/banner.ts`; `frontend/src/generated/dtos.d.ts`; `frontend/src/test/setup.ts`.
New docs/CI/build: `docs/ASSERTION-INVENTORY.md`; `.github/workflows/ci.yml` jobs `frontend` and
`docker-build` plus steps `Assertion inventory completeness`, `Diff-scope fence`,
`Generated DTO staleness`; `Dockerfile` stage `frontend`; `.dockerignore` entries
`app/static/dist` and `frontend/node_modules`.
Modified existing (no new symbols): `app/routes/runs.py`, `app/templates/runs_list.html`,
`tests/test_design_tokens.py`, `tests/test_dashboard.py`, `README.md`, `.gitignore`.

<tasks>

<task type="auto">
  <name>Task 1: RunStatusPoll and a response model on the status poll endpoint</name>
  <reversibility rating="costly">The poll shape becomes a published contract consumed by the
  generated TypeScript types and reused unchanged by Phase 23's detail page; changing it later means
  regenerating types and re-cutting both pages. Rating carried from D-22-13.</reversibility>
  <files>app/schemas/run_status.py, app/routes/runs.py</files>
  <read_first>
    - app/routes/runs.py lines 885-918 (the status poll endpoint — its response body is today's de
      facto poll shape, field for field)
    - app/routes/runs.py lines 160-245 (`_safe_failure_presentation` and `_safe_run_for_browser` —
      the exact failure object shape and the queue label vocabulary the model must accept)
    - app/routes/templating.py (the badge class and label filters the endpoint already calls)
    - app/schemas/runs_list.py (the tracer's `RunListRow`, which must compose these seven fields
      rather than restate them)
    - app/schemas/_projection.py (`RowProjection`'s config — whether `RunStatusPoll` should inherit
      it or be a plain frozen model, since the poll shape is not built from a repository row)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-13)
  </read_first>
  <action>
Create `app/schemas/run_status.py` declaring `RunStatusPoll`, a frozen Pydantic model with extra
fields forbidden, carrying exactly the seven volatile fields the status poll endpoint returns today:
the status string, the badge class suffix, the badge label, the queue label, the queue badge class
suffix, the open-job flag, and the failure object. Model the failure object as its own nested model
with the four fields the safe-failure presentation produces, rather than as an untyped mapping, so
the generated TypeScript types in plan 22-11 carry real field names.

Refactor `app/schemas/runs_list.py`'s `RunListRow` to compose these seven fields from `RunStatusPoll`
rather than restating them, so the poller can replace the volatile half wholesale and the type system
enforces the merge. Prefer a flat field union over a nested sub-object if a nested one would make the
generated TypeScript merge awkward — confirm which reads cleaner against the generator's output
before committing to one, and record the choice and its reason in SUMMARY.md.

Add `response_model=RunStatusPoll` to the status poll endpoint's route decorator. This is a legal
edit: it is a GET, not one of the fourteen fenced operator-facing mutation handlers. Note the trap —
a route that returns a pre-built JSON response object bypasses response-model validation entirely, so
the endpoint must return the model (or a plain mapping) for the declaration to do any work. Change
the return accordingly and change nothing else about the endpoint: not its status codes, not its
not-found behaviour, not the two exception paths.

The response body must stay byte-identical. Capture the current body for a fixture run before the
change and compare after; if key order or a value's serialized form differs at all, adjust the model
(field declaration order, serialization aliases) until it matches rather than accepting the drift.
The vanilla poller is gone but plan 22-10's hook and any external consumer read this body, and this
milestone's claim is that nothing observable changed.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py -k status -q &amp;&amp; uv run python -c "from app.schemas.run_status import RunStatusPoll; f=set(RunStatusPoll.model_fields); assert f=={'status','badge_class','badge_label','queue_label','queue_badge_class','has_open_job','failure'}, sorted(f); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from app.schemas.run_status import RunStatusPoll; print(sorted(RunStatusPoll.model_fields))"`
      prints exactly the seven field names.
    - The status poll route's decorator declares `response_model=RunStatusPoll`.
    - The route does not return a pre-built JSON response object (confirmed by reading the function
      and recorded in SUMMARY.md), so the declared response model is actually enforced.
    - SUMMARY.md contains the response body captured before and after the change for the same fixture
      run, and they are byte-identical.
    - `uv run python -c "import json,app.main as m; from fastapi.openapi.utils import get_openapi; s=get_openapi(title='t',version='1',routes=m.app.routes); assert 'RunStatusPoll' in s['components']['schemas'], sorted(s['components']['schemas']); print('ok')"` exits 0.
    - `RunListRow` composes the seven fields from `RunStatusPoll` rather than restating them,
      confirmed by reading `app/schemas/runs_list.py`.
    - `uv run pytest -q` exits 0; `uv run ruff check .` and `uv run mypy` exit 0.
  </acceptance_criteria>
  <done>The poll shape is a declared, enforced, OpenAPI-visible contract, the list row composes it,
  and the wire body did not change.</done>
</task>

<task type="auto">
  <name>Task 2: Explicit three-way classification of every run column</name>
  <files>app/schemas/run_columns.py</files>
  <read_first>
    - app/db/repo/runs.py lines 35-45 (the run-column constant — read it, never edit it; `app/db/` is
      behind the milestone's untouchable fence)
    - app/db/repo/demo.py lines 228-262 (the list projection, which carries a created-at column the
      run-column constant does not — the forcing fact behind two separate shapes)
    - app/schemas/runs_list.py (`RunListRow`'s declared fields and excluded set)
    - app/routes/runs.py lines 220-245 (the denylist that stays as an inner layer)
    - tests/test_job_kind_drift.py (the drift-detection-by-name idiom this classification is designed
      to be checked with)
  </read_first>
  <action>
Create `app/schemas/run_columns.py` declaring a string enum `ColumnExposure` with members naming the
three exposures — exposed on the list shape, exposed on the detail shape, and internal-only — and a
mapping `RUN_COL_CLASSIFICATION` from every column name in the run-column constant to exactly one
exposure.

Classify all fifteen live columns. The seven that are PII or internal and pass the current denylist
untouched — the business identifier, the source email identifier, the reply epoch, the alias
candidates, the extracted data, the reconciliation and the decision — must be classified deliberately
rather than by default, and the module's docstring must state that they leak through the existing
denylist today and that this mapping is the layer that stops it. Re-derive the fifteen names from
live source at implementation time rather than trusting any list in a planning document.

Columns whose exposure belongs to the detail page carry the detail exposure and a short comment
naming Phase 23 as the phase that turns them into declared fields of the detail shape. That is a
classification, not a deferral: the drift test in Task 3 fails on an unclassified column, and a
column classified as detail-exposed but never declared on the detail shape is Phase 23's own gate to
close.

Add a module-level docstring in this repo's guard style: what the mapping establishes, what it
explicitly does not (it does not prove any exposed column is safe, only that no column is
unclassified), and why the mapping lives in `app/schemas/` rather than beside the SQL constant (the
fence).
  </action>
  <verify>
    <automated>uv run python -c "from app.schemas.run_columns import RUN_COL_CLASSIFICATION as C, ColumnExposure; assert len(C)==15, len(C); assert all(isinstance(v, ColumnExposure) for v in C.values()); print(sorted(C))"</automated>
  </verify>
  <acceptance_criteria>
    - `app/schemas/run_columns.py` defines `ColumnExposure` with exactly three members and
      `RUN_COL_CLASSIFICATION` whose key set equals the live run-column name set.
    - Every value in `RUN_COL_CLASSIFICATION` is a `ColumnExposure` member.
    - The seven PII/internal column names are present as keys with a non-default classification, and
      the module docstring states that they pass the existing denylist untouched today.
    - `git diff --name-only` for this task touches no path under `app/db/`.
    - `uv run ruff check app/schemas/run_columns.py` and `uv run mypy app/schemas/run_columns.py`
      exit 0.
  </acceptance_criteria>
  <done>Every run column has exactly one deliberate exposure classification, declared outside the
  untouchable database package.</done>
</task>

<task type="auto">
  <name>Task 3: GUARD-04 drift test, demonstrated red by a real new column</name>
  <files>tests/test_schema_projection.py</files>
  <read_first>
    - tests/test_schema_projection.py (the allowlist tests the tracer created in plan 22-04)
    - app/schemas/run_columns.py (the classification from Task 2)
    - app/db/repo/runs.py lines 35-45 (the constant the test parses)
    - tests/test_job_kind_drift.py (the exact-tuple drift idiom and its failure-message style)
    - docs/DURABILITY-PROOFS.md (the demonstrated-red plus byte-identical-revert convention)
  </read_first>
  <action>
Extend `tests/test_schema_projection.py` with the GUARD-04 half. The test is hermetic: the run-column
constant is a module constant, so no database is needed.

Parse the column names by splitting the constant on commas and stripping surrounding whitespace, so a
column written with extra whitespace or across a line continuation still matches its classification
by exact name.

Write:
- `test_run_col_set_is_non_empty_and_known` — assert the parsed set is non-empty and equals the
  expected fifteen names. Without this, an unparseable or emptied constant would make every
  difference-based assertion below pass on an empty set, which is precisely the vacuous pass this
  guard exists to prevent.
- `test_every_run_col_is_classified` — assert the parsed set minus the classification's key set is
  empty, with a failure message naming the offending columns and stating that a new column reached
  the constant while being neither exposed in a page's response shape nor named internal-only.
- `test_no_classification_entry_is_stale` — assert the classification's key set minus the parsed set
  is empty, so a removed column leaves no dangling entry.
- `test_list_exposed_columns_are_declared_on_the_list_shape` — every column classified as
  list-exposed is a declared field of the list row model; every column classified internal-only is
  either in the list row's excluded set or absent from the list projection entirely.
- `test_list_and_detail_shapes_are_separate` — assert the list projection carries a created-at column
  that the run-column constant does not, so one shared shape between the two pages is structurally
  impossible rather than merely undesirable.

Demonstrate the guard red the way GUARD-04's requirement describes: append a plausible sensitive
column name to the run-column constant in the working tree, run the test, and capture the RED output
showing the column named in the failure message. Then revert with a checkout and confirm
`git diff --stat app/db/repo/runs.py` is empty. Before running, confirm with `grep -n` that the edit
landed inside the live constant and not inside the surrounding docstring, which also discusses column
selection — this repo has a recorded scar where a mutation hit a docstring copy and a blind spot
looked like a passing proof.
  </action>
  <verify>
    <automated>uv run pytest tests/test_schema_projection.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_schema_projection.py -x -q` exits 0.
    - The module defines all five test functions named above plus the allowlist tests from plan 22-04.
    - SUMMARY.md contains the RED transcript produced by appending a column to the live constant, and
      the failure message in that transcript names the added column.
    - SUMMARY.md contains the `grep -n` line proving the mutation landed inside the constant and not
      inside the surrounding docstring.
    - SUMMARY.md contains the `git diff --stat app/db/repo/runs.py` line proving the byte-identical
      revert.
    - `git diff --name-only` for the committed change touches no path under `app/db/`.
  </acceptance_criteria>
  <done>Adding a column to the run-column list without classifying it fails CI and names the column,
  and that failure has been produced for real and reverted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payroll_runs table → browser | Every column the run-column constant selects is one serialization away from an unauthenticated browser |
| status poll endpoint → any client | The poll endpoint is an unauthenticated GET returning run state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-28 | Information Disclosure | a new column auto-exposed by the existing denylist | high | mitigate | `RUN_COL_CLASSIFICATION` requires a deliberate exposure decision per column and the drift test fails by name; demonstrated red with a real appended column |
| T-22-29 | Information Disclosure | the seven PII/internal columns that pass the current denylist | high | mitigate | Each is classified explicitly and the list row's excluded set names it; plan 22-04's render test asserts none appears in the embedded payload |
| T-22-30 | Tampering | a response model declared but bypassed by returning a pre-built JSON response | high | mitigate | The endpoint returns the model so the declaration is enforced; an acceptance criterion asserts the pre-built-response path is gone and the wire body is unchanged |
| T-22-31 | Information Disclosure | diagnostics leaking through the failure sub-model | high | mitigate | The failure object is populated only by the existing safe-failure presentation, whose PII-safe reduction is out of this phase's edit scope and must not be bypassed |
| T-22-32 | Repudiation | an edit to the database package smuggled in with the classification | medium | mitigate | An acceptance criterion asserts the committed diff touches no path under the database package; plan 22-05's diff-scope fence enforces the same rule in CI |
</threat_model>

## Flagged planner assumptions (probe rows that stay unresolved)

- **SHELL-07 / unclassified** — "a JSON response for a converted page exposes only fields explicitly
  declared for that page" has no data-shape edge to probe. The planner's assumption is that the
  observable surface for this requirement is the embedded island payload plus the status poll body,
  and that no additional JSON endpoint is introduced for the list page — the key-set assertion works
  by parsing the island out of the response text, so a dedicated route would buy nothing for
  testability (D-22-04). Recorded, not resolved.

<verification>
- `uv run pytest tests/test_schema_projection.py tests/test_react_page_render.py tests/test_dashboard.py -q` exits 0.
- `uv run pytest -q` (full suite) exits 0.
- `uv run ruff check .` and `uv run mypy` exit 0.
- The application's OpenAPI document contains the poll shape natively.
- `git diff --name-only` for this plan touches no path under `app/pipeline/`, `app/queue/`,
  `app/db/`, `app/llm/`, or `app/email/`.
- SUMMARY.md carries the before/after wire-body comparison and the GUARD-04 red transcript with its
  revert evidence.
</verification>

<success_criteria>
- Adding a column to the run-column list without classifying it fails CI, identifying the column by
  name.
- The list page and the detail page have separate declared shapes, and the reason they cannot share
  one is asserted rather than asserted-by-comment.
- The status poll endpoint declares an enforced response model and its wire body is unchanged.
</success_criteria>

<output>
Create `.planning/phases/22-frontend-foundation-runs-list/22-07-SUMMARY.md` when done.
</output>
