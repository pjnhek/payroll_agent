---
phase: 22-frontend-foundation-runs-list
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_design_tokens.py
  - tests/test_route_shadowing.py
  - tests/test_no_html_on_service_routes.py
  - tests/test_page_shell_pins.py
autonomous: true
requirements: [SHELL-01, SHELL-09, SHELL-10, GUARD-05]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/runs` and `/runs/{run_id}/status` are exact-prefix neighbours yet resolve to two distinct endpoint functions, and the sole `Mount` (`/static`) never matches `/runs` — asserted on the resolved endpoint object, not on a status code."
    - "A path matching no registered router (`/does-not-exist`) returns 404, never a 200 HTML shell — the structural signature of an absent catch-all."
    - "`app/main.py` registers exactly one `Mount` and seven routers in the order health, webhook, runs, dashboard, demo, pump, ops; no route added by this phase is registered ahead of health or webhook, and match order is asserted by resolved endpoint identity."
    - "`/runs` and `/runs/{uuid}` are adjacent nav states: each of `/`, `/runs`, `/runs/<uuid>`, `/eval`, `/ops` renders exactly one `aria-current=\"page\"` in the nav — never two, never zero."
    - "A template declaring no `{% block title %}` falls back to the base `Pyrl` title; every operator-facing page declares a non-empty title distinct from that fallback."
    - "Nav link order is Pyrl, Runs, Eval, Ops in the rendered markup, unchanged by conversion."
    - "`POST /webhook/inbound`, `/health/live`, `/health/ready`, `/health/queue`, `/health/schema`, and `/internal/pump` all return a non-HTML content-type, and a synthetic catch-all mount makes the route-shadowing guard red."
    - "`tests/test_design_tokens.py` scans `.ts` and `.tsx` sources and `frontend/src` in addition to `app/templates/*.html`, and pins the scanned-file count against a harvested inventory so the scan cannot silently narrow as pages convert."
    - "`tests/test_design_tokens.py` reads `runs_list.html` inside a test body rather than at module import, so a future template deletion cannot take the WCAG contrast gates down at collection time."
  artifacts:
    - tests/test_route_shadowing.py
    - tests/test_no_html_on_service_routes.py
    - tests/test_page_shell_pins.py
  key_links:
    - "`tests/test_route_shadowing.py` reads the live `app.routes` table from `app.main`, never a hand-maintained path list."
    - "`tests/test_page_shell_pins.py` is the first `<title>` coverage in the repo; `tests/test_ops_route.py::test_ops_page_has_no_script_or_polling` remains the `/ops` script-free pin and is not modified."
    - "The design-token widening must land before any markup moves — plan 22-04 (the tracer) removes the table region that the deleted `js-` hook assertions currently target."
  prohibitions: []
---

<objective>
Widen the design-token and accessibility guards so they can see the frontend tree, and pin the
service-route structure, before any markup moves. Everything here is a precondition for the tracer:
`tests/test_design_tokens.py` narrows silently as pages convert (hard-coded suffix allowlist and
`app/templates/*.html` globs), one of its tests reads `runs_list.html` at module import, and its
`js-` hook assertions target markup the tracer removes.

Purpose: SHELL-01's structural half, SHELL-09, SHELL-10, GUARD-05, D-22-15, D-22-16.
Output: three new guard test modules and a widened, import-safe `tests/test_design_tokens.py`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md
@.planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md
</context>

## Artifacts this phase produces

New Python (app): `app/schemas/__init__.py`; `app/schemas/_projection.py` (`RowProjection`,
`UnclassifiedColumnError`); `app/schemas/runs_list.py` (`RunListRow`, `RunsListPage`);
`app/schemas/run_status.py` (`RunStatusPoll`); `app/routes/templating.py` gains
`render_react_page`, `json_script`, `load_manifest`, `ManifestMissingError`, `_JSON_SCRIPT_ESCAPES`;
`app/config.py` gains `Settings.vite_dev_server_url`.
New Python (scripts): `scripts/render_assertion_inventory.py`; `scripts/generate_openapi_doc.py`.
New Python (tests): `tests/assertion_inventory.py` (`AssertionEntry`, `AssertionClass`,
`AssertionLayer`, `ASSERTION_INVENTORY`, `FILE_SCOPE_NOTES`); `tests/safety_mutation_registry.py`
(`SafetyTarget`, `SafetyPredicate`, `SAFETY_MUTATION_TARGETS`);
`tests/test_inventory_completeness.py`; `tests/test_safety_mutation_registry.py`;
`tests/test_route_shadowing.py`; `tests/test_no_html_on_service_routes.py`;
`tests/test_page_shell_pins.py`; `tests/test_react_page_render.py`;
`tests/test_bundle_asset_exists.py`; `tests/test_schema_projection.py`;
`tests/test_no_fetch_outside_poller.py`; `tests/test_react_dev_mode.py`;
`tests/test_generated_types_staleness.py`.
New templates/fixtures: `app/templates/react_page.html`; `tests/fixtures/vite_manifest.json`.
New frontend tree: `frontend/package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`,
`tsconfig.node.json`, `eslint.config.js`, `vitest.config.ts`, `MANIFEST-SHAPE.md`;
`frontend/src/entries/runs.tsx`; `frontend/src/boot/pageData.ts` (`readInitialData`,
`INITIAL_DATA_ELEMENT_ID`); `frontend/src/pages/RunsPage.tsx` (`RunsPage`) + `RunsPage.test.tsx`;
`frontend/src/components/StatusBadge.tsx`, `QueueBadge.tsx`, `FailureSummary.tsx`,
`MutationForm.tsx`, `ConfirmForm.tsx`, `DecisionBanner.tsx`, `MutationForm.test.tsx`;
`frontend/src/hooks/usePoller.ts` + `usePoller.test.ts`; `frontend/src/types/banner.ts`;
`frontend/src/generated/dtos.d.ts`; `frontend/src/test/setup.ts`.
New docs/CI/build: `docs/ASSERTION-INVENTORY.md`; `.github/workflows/ci.yml` jobs `frontend` and
`docker-build` plus steps `Assertion inventory completeness`, `Diff-scope fence`,
`Generated DTO staleness`; `Dockerfile` stage `frontend`; `.dockerignore` entries
`app/static/dist` and `frontend/node_modules`.
Modified existing (no new symbols): `app/routes/runs.py`, `app/templates/runs_list.html`,
`tests/test_design_tokens.py`, `tests/test_dashboard.py`, `README.md`, `.gitignore`.

<tasks>

<task type="auto">
  <name>Task 1: Widen the design-token and a11y guard scope before the first markup moves</name>
  <reversibility rating="costly">Deleting the script-hook presence test removes the only assertion
  standing between a tidy-up commit and a silently broken poller until plan 22-10 lands its Vitest
  replacement; the deletion touches a guard other phases inherit.</reversibility>
  <files>tests/test_design_tokens.py</files>
  <read_first>
    - tests/test_design_tokens.py (full file — especially the suffix allowlist near line 183, the two
      `app/templates/*.html` globs near lines 191 and 337, the module-scope `runs_list.html` read near
      line 352, and the script-hook test that follows it)
    - app/templates/runs_list.html (the markup those hook assertions target today)
    - app/static/style.css (the `:root` token block the contrast gates parse)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-15, D-22-16, and the SC-5
      amendment note)
  </read_first>
  <action>
Widen the guard's scope so it cannot go blind as pages convert, and retire the one assertion that
this phase's own headline change makes structurally false.

1. Extend the file-suffix allowlist used by the accent-literal scan (currently `{".css", ".html",
   ".py"}`) to also include `".ts"` and `".tsx"`.
2. Extend both `app/templates/*.html` globs to additionally walk `frontend/src` recursively for
   `.ts`/`.tsx` files. Today that walk yields nothing (the directory does not exist yet); write it so
   a missing directory is a no-op rather than an error, because this plan runs before the scaffold.
3. Add a new test `test_token_scan_covers_every_present_extension` that harvests, per extension in
   the allowlist, the count of files the guard actually scanned, and asserts the count is non-zero
   for every extension that is present in the repo at that moment. This is the anti-narrowing pin:
   once `frontend/src` exists the `.tsx` count must be non-zero, and a future change that drops an
   extension from the glob reds instead of silently reducing coverage. Derive "present in the repo"
   from a repo walk, not from a hard-coded list.
4. Move the module-scope read of `app/templates/runs_list.html` into the body of the one test that
   consumes it, so a template deletion in Phase 23 or 24 cannot error the whole module at collection
   and take the WCAG contrast gates down with it.
5. Delete the test that asserts the three prefixed script-hook class names are present in
   `runs_list.html` and absent from `style.css`
   (`test_script_hook_classes_carry_js_prefix_and_stay_out_of_css`, and the unprefixed-former-name
   half inside it). Replace it with a module-level comment recording the justification required by
   D-22-15: that convention existed to stop someone deleting a `document.querySelector` target that
   looked like dead markup; React holds the badge in component state and re-renders it, so there is
   no selector and keeping the class names would create genuinely dead markup — the exact thing the
   convention opposed. The comment must name plan 22-10 as the owner of the replacement Vitest
   in-place-update test, and must state that ROADMAP Phase 22 Success Criterion 5's third sentence is
   amended by this change. Put the same justification in the commit message body.

Do not touch the WCAG contrast computations, the `:root` parsing, the button-composition rule, or
any token value. Keep `ruff` line-length 100 and `mypy --strict` clean.
  </action>
  <verify>
    <automated>uv run pytest tests/test_design_tokens.py -x -q &amp;&amp; uv run python -c "import tests.test_design_tokens as m; assert not hasattr(m, 'test_script_hook_classes_carry_js_prefix_and_stay_out_of_css'); assert hasattr(m, 'test_token_scan_covers_every_present_extension'); print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_design_tokens.py -x -q` exits 0.
    - Importing `tests.test_design_tokens` shows no attribute
      `test_script_hook_classes_carry_js_prefix_and_stay_out_of_css` and a new attribute
      `test_token_scan_covers_every_present_extension`.
    - Importing `tests.test_design_tokens` shows no module-level attribute holding the
      `runs_list.html` text (verified with a `hasattr` check on the former module constant name),
      proving the read moved into a test body.
    - Temporarily renaming `app/templates/runs_list.html` leaves `uv run pytest
      tests/test_design_tokens.py --collect-only -q` exiting 0 (collection no longer depends on the
      template); the rename is reverted and `git status --porcelain app/templates/` is empty. Both
      outputs recorded in SUMMARY.md.
    - The suffix allowlist literal in the file includes `.ts` and `.tsx`.
    - `tests/test_design_tokens.py` contains a comment block naming plan 22-10 as the owner of the
      replacement Vitest test and naming the amended ROADMAP Success Criterion 5.
    - `uv run ruff check tests/test_design_tokens.py` and `uv run mypy tests/test_design_tokens.py`
      exit 0.
  </acceptance_criteria>
  <done>The token/a11y guard scans the frontend tree, pins its own scan breadth, survives a template
  deletion at collection time, and no longer asserts a property this phase deliberately removes.</done>
</task>

<task type="auto">
  <name>Task 2: Route-shadowing and no-HTML-on-service-routes guards, both demonstrated red</name>
  <files>tests/test_route_shadowing.py, tests/test_no_html_on_service_routes.py</files>
  <read_first>
    - app/main.py (full file — the sole `Mount` and the seven `include_router` calls in order)
    - tests/test_ops_route.py (the `test_ops_page_has_no_script_or_polling` function and its
      assertion — the structural-absence idiom this copies)
    - tests/conftest.py (the `client` / `fake_repo` fixtures these tests must reuse rather than
      constructing a second `TestClient`)
    - app/routes/health.py (the exact `/health/*` paths and their response types)
    - app/routes/pump.py (the `/internal/pump` method and its authorization behaviour, so the test
      asserts content-type rather than a 200)
    - .planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md (Architecture Pattern 2 and
      the `GUARD-05's no-HTML response assertion` code example)
  </read_first>
  <action>
Create `tests/test_route_shadowing.py`:
- `test_only_mount_is_static` — collect every `starlette.routing.Mount` in `app.routes` and assert
  the list of their `path` values equals `["/static"]` exactly. A second mount, in either order,
  reds.
- `test_no_route_shadows_a_reserved_prefix` — for each of `/webhook/inbound`, `/health/live`,
  `/health/ready`, `/health/queue`, `/health/schema`, `/internal/pump`, `/ops`, `/runs`, `/eval`,
  and `/`, build an ASGI scope and resolve it against `app.router.routes`, then assert the FIRST
  route whose `matches(scope)` is not `Match.NONE` is the expected `APIRoute` and that its `endpoint`
  is the expected function object. Assert on the endpoint object, never on a status code: a
  catch-all serving `html=True` returns 200 for everything, which makes a status-code assertion
  useless.
- `test_no_path_converter_route_exists` — assert no registered route's `path` contains a
  `:path` converter segment.
- `test_unregistered_path_is_not_swallowed` — request a path that matches no router and assert the
  response status is 404 and its content-type is not HTML.
- `test_router_registration_order_is_unchanged` — assert the ordered list of router module names
  reachable from `app.routes` still begins with health and webhook ahead of runs, so a future
  registration cannot be inserted in front of the service routes.

Create `tests/test_no_html_on_service_routes.py`: a `pytest.mark.parametrize` over
`(path, method)` pairs for `POST /webhook/inbound`, `GET /health/live`, `GET /health/ready`,
`GET /health/queue`, `GET /health/schema`, and `GET /internal/pump`, asserting `"text/html"` is not
in the response's `content-type` header. Assert on content-type only — these routes legitimately
return 4xx without credentials or a signature, and the requirement is about the media type, not the
status. Reuse the existing hermetic `client` fixture so no live database or provider is needed.

Demonstrate both guards red. For `tests/test_route_shadowing.py`, add a second static mount at `/`
with directory serving enabled to `app/main.py` in the working tree, capture the RED output, then
`git checkout -- app/main.py` and confirm `git diff --stat app/main.py` is empty. For
`tests/test_no_html_on_service_routes.py`, use the same mutation — the catch-all is precisely what
makes a health route answer with markup. Before each run, confirm with `grep -n` that the injected
line is live source in `app/main.py` and not a docstring or comment copy.
  </action>
  <verify>
    <automated>uv run pytest tests/test_route_shadowing.py tests/test_no_html_on_service_routes.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_route_shadowing.py tests/test_no_html_on_service_routes.py -x -q`
      exits 0.
    - `tests/test_route_shadowing.py` defines all five test functions named above.
    - `tests/test_no_html_on_service_routes.py` parametrizes exactly the six `(path, method)` pairs
      named above and asserts on the `content-type` header.
    - SUMMARY.md contains the verbatim RED output of both guard modules under the injected
      root-mount mutation, the `grep -n` line proving the mutation landed on live source, and the
      `git diff --stat app/main.py` line proving the byte-identical revert.
    - Neither new module constructs its own `TestClient`; both consume the existing conftest fixture
      (verified by the absence of a `TestClient(` construction in each file).
    - `uv run ruff check` and `uv run mypy` exit 0 for both files.
  </acceptance_criteria>
  <done>Both guards pass on clean master, both go red under a real catch-all mount, and the
  mutation was reverted byte-identically.</done>
</task>

<task type="auto">
  <name>Task 3: Per-page title and single-aria-current pins (zero coverage today)</name>
  <files>tests/test_page_shell_pins.py</files>
  <read_first>
    - app/templates/base.html (full file — the `{% block title %}Pyrl{% endblock %}` fallback and the
      four nav links with their `aria-current` conditions)
    - app/templates/runs_list.html (its `{% block title %}` declaration)
    - app/templates/index.html, app/templates/ops.html, app/templates/eval.html (their title blocks)
    - tests/test_dashboard.py (the existing `aria_current` coverage, so the new module pins the
      title half and the single-match half without duplicating what already passes)
    - tests/conftest.py (the hermetic `client` / `fake_repo` fixtures)
  </read_first>
  <action>
Create `tests/test_page_shell_pins.py`, the repo's first `<title>` coverage.

- `test_every_operator_page_declares_a_distinct_title` — for `/`, `/runs`, `/eval`, `/ops`, parse the
  rendered `<title>` text and assert it is non-empty and is not equal to the bare base fallback
  (`Pyrl`). Parametrize by path so a new converted page joins by adding one row.
- `test_missing_title_block_falls_back_to_the_base_title` — render a throwaway template that extends
  the base layout and declares no title block through the shared `Jinja2Templates` instance, and
  assert the rendered title equals the base fallback. This pins the empty case explicitly rather
  than assuming it.
- `test_exactly_one_aria_current_per_page` — for `/`, `/runs`, `/runs/<a fixed uuid>`, `/eval`,
  `/ops`, count occurrences of the `aria-current` attribute in the nav region and assert the count is
  exactly 1. `/runs` and `/runs/{uuid}` are adjacent nav states that both resolve to the Runs link;
  neither may produce two matches nor zero.
- `test_nav_link_order_is_unchanged` — assert the ordered nav link labels are Pyrl, Runs, Eval, Ops.

Scope the `aria-current` count to the `<nav>` element's inner markup, not the whole document, so a
future page that legitimately uses the attribute inside content does not make the pin unsatisfiable.
Use the existing hermetic fixtures; no live database.
  </action>
  <verify>
    <automated>uv run pytest tests/test_page_shell_pins.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_page_shell_pins.py -x -q` exits 0.
    - The module defines all four test functions named above.
    - Deleting the `{% block title %}` line from `app/templates/runs_list.html` in the working tree
      makes `test_every_operator_page_declares_a_distinct_title` red; the deletion is reverted and
      `git diff --stat app/templates/runs_list.html` is empty. Both outputs in SUMMARY.md.
    - Adding a second `aria-current` attribute to a nav link in `app/templates/base.html` in the
      working tree makes `test_exactly_one_aria_current_per_page` red; reverted byte-identically,
      with `grep -n` evidence the mutation hit live markup. Outputs in SUMMARY.md.
    - The `aria-current` count is computed over the nav region only (the file contains a nav-region
      extraction step, not a whole-document count).
    - `uv run ruff check tests/test_page_shell_pins.py` and `uv run mypy tests/test_page_shell_pins.py`
      exit 0.
  </acceptance_criteria>
  <done>Per-page titles and the single-nav-match invariant are pinned by tests that are proven able
  to fail, closing the repo's zero-`<title>`-coverage gap ahead of conversion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated client → service routes | `POST /webhook/inbound`, `/health/*`, `/internal/pump` are reachable without credentials |
| Render health-checker / `pump.yml` cron → `/health/*`, `/internal/pump` | Automated consumers that treat a 200 as liveness |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-05 | Denial of Service | a catch-all route shadowing `/webhook/inbound` | high | mitigate | `tests/test_route_shadowing.py` asserts the sole `Mount` is `/static` and resolves each reserved path to its expected endpoint object; the guard is demonstrated red under a real root mount |
| T-22-06 | Spoofing | `/health/live` answering 200 + HTML | high | mitigate | `tests/test_no_html_on_service_routes.py` asserts a non-HTML content-type on all six service routes, so a broken deploy cannot look healthy to Render while `pump.yml`'s `curl -f` goes green and the durable queue is never drained |
| T-22-07 | Repudiation | guard scope narrowing as pages convert | high | mitigate | `test_token_scan_covers_every_present_extension` pins scanned-file counts against a harvested inventory, so a silently narrowed glob reds instead of passing vacuously |
| T-22-08 | Information Disclosure | rendered `<title>` / nav markup | low | accept | Titles and nav labels carry no run-specific or client data; the pins assert structure only |
</threat_model>

## Flagged planner assumptions (probe rows that stay unresolved)

The spec-less edge probe classified two of this plan's requirements as `unclassified`; per the
fallback protocol these are surfaced rather than auto-resolved:

- **SHELL-09 / unclassified** — "`/` and `/ops` remain Jinja2, and `/ops` remains free of any script
  tag, `setInterval`, or meta-refresh" has no data-shape edge to probe. The planner's assumption is
  that the existing pin `tests/test_ops_route.py::test_ops_page_has_no_script_or_polling` remains the
  sole authority and is neither modified nor re-pointed in this milestone, and that Vite boot tags
  live only in `app/templates/react_page.html`, never hoisted into `base.html`. Unverified until the
  tracer lands; carried as an assumption, not a resolved truth.
- **GUARD-05 / unclassified** — "a change that makes them return HTML fails CI" has no data-shape
  edge. The planner's assumption is that content-type is the correct assertion surface (rather than
  status code or body text) because these routes legitimately return 4xx without credentials.
  Recorded here so a reviewer can challenge the surface choice.

<verification>
- `uv run pytest tests/test_design_tokens.py tests/test_route_shadowing.py tests/test_no_html_on_service_routes.py tests/test_page_shell_pins.py -x -q` exits 0.
- `uv run pytest -q` (full suite) still passes — this plan changes no application code.
- `uv run ruff check .` and `uv run mypy` exit 0.
- `uv run pytest tests/test_ops_route.py::test_ops_page_has_no_script_or_polling -x` still passes,
  unmodified.
- SUMMARY.md carries four demonstrated-red transcripts (root mount, deleted title block, duplicated
  `aria-current`, renamed template at collection) each with its byte-identical revert evidence.
</verification>

<success_criteria>
- The design-token guard sees `.ts`/`.tsx` and `frontend/src`, pins its own scan breadth, and no
  longer dies at collection if a template is deleted.
- The script-hook presence assertion is deleted with a written justification naming its replacement
  owner and the amended ROADMAP criterion.
- No catch-all route exists and the guard proving it is demonstrated able to fail.
- The six service routes are pinned to non-HTML responses.
- Per-page `<title>` and single-`aria-current` coverage exists for the first time in this repo.
</success_criteria>

<output>
Create `.planning/phases/22-frontend-foundation-runs-list/22-02-SUMMARY.md` when done.
</output>
