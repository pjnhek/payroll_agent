---
phase: 22-frontend-foundation-runs-list
plan: 09
type: execute
wave: 4
depends_on: ["22-04"]
files_modified:
  - app/config.py
  - app/routes/templating.py
  - frontend/vite.config.ts
  - tests/test_react_dev_mode.py
  - README.md
autonomous: true
requirements: [SHELL-04]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One command starts a hot-reloading dev server that proxies to uvicorn, and one command typechecks and lints the frontend; both are documented in the README and both were run by the executor."
    - "The dev-server setting defaults to the disabled value, so a deployment that sets nothing renders production asset tags."
    - "A render performed with default settings emits no developer-host asset URL — asserted by a test, not by inspection."
    - "The image build never sets the dev-server setting, so the dev branch is structurally unreachable in a deployed container."
    - "The dev branch does not require a manifest; the production branch still raises when the manifest is absent, so disabling the dev setting cannot silently downgrade the fail-closed behaviour."
    - "A demo send-test form submitted through the dev origin follows its redirect back to the dev origin, not to the uvicorn origin — the specific failure mode the architecture research named for this proxy direction."
  artifacts:
    - tests/test_react_dev_mode.py
  key_links:
    - "`app/routes/templating.py`'s `render_react_page` branches on the new setting: enabled emits the dev client module and the entry source path from the dev origin, disabled resolves hashed asset paths through the manifest loader."
    - "`frontend/vite.config.ts`'s server proxy forwards non-asset requests to uvicorn, which is what makes the dev origin the document origin and keeps redirect targets same-origin."
    - "`app/config.py`'s settings object is the single env-driven config surface; the new field follows the existing default-to-the-safe-state convention used by the pump token and the unsigned-fixture flag."
  prohibitions:
    - statement: "MUST NOT allow the dev-server branch to be reachable in a deployed image — a production render that emits an asset URL pointing at a developer's machine hands page execution to a host outside the deployment."
      status: resolved
      verification: test
---

<objective>
Give a developer one command for a hot-reloading dev server against uvicorn and one command to
typecheck and lint, and pin the risk that comes with the locked proxy direction. D-22-03 is locked as
the Vite dev server proxying to uvicorn; the architecture research argued for the opposite direction
and named a concrete failure mode — a redirect location resolving against the wrong origin. The user
chose to pin that risk with acceptance criteria rather than a checkpoint, so it is pinned here.

Purpose: SHELL-04.
Output: a dev-server setting that defaults off, a fail-closed dev branch in the page renderer, a Vite
proxy config, a test module, and README instructions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md
@.planning/phases/22-frontend-foundation-runs-list/22-04-SUMMARY.md
</context>

## Artifacts this phase produces

New Python (app): `app/schemas/__init__.py`; `app/schemas/_projection.py` (`RowProjection`,
`UnclassifiedColumnError`); `app/schemas/runs_list.py` (`RunListRow`, `RunsListPage`);
`app/schemas/run_status.py` (`RunStatusPoll`); `app/schemas/run_columns.py`
(`RUN_COL_CLASSIFICATION`, `ColumnExposure`); `app/routes/templating.py` gains `render_react_page`,
`json_script`, `load_manifest`, `ManifestMissingError`, `_JSON_SCRIPT_ESCAPES`; `app/config.py`
gains `Settings.vite_dev_server_url`.
New Python (scripts): `scripts/render_assertion_inventory.py`; `scripts/generate_openapi_doc.py`.
New Python (tests): `tests/assertion_inventory.py`; `tests/safety_mutation_registry.py`;
`tests/test_inventory_completeness.py`; `tests/test_safety_mutation_registry.py`;
`tests/test_route_shadowing.py`; `tests/test_no_html_on_service_routes.py`;
`tests/test_page_shell_pins.py`; `tests/test_react_page_render.py`;
`tests/test_bundle_asset_exists.py`; `tests/test_schema_projection.py`;
`tests/test_no_fetch_outside_poller.py`; `tests/test_react_dev_mode.py`;
`tests/test_generated_types_staleness.py`; `tests/test_ci_gate_config.py`.
New templates/fixtures: `app/templates/react_page.html`; `tests/fixtures/vite_manifest.json`.
New frontend tree: `frontend/package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`,
`tsconfig.node.json`, `eslint.config.js`, `vitest.config.ts`, `MANIFEST-SHAPE.md`;
`frontend/src/entries/runs.tsx`; `frontend/src/boot/pageData.ts`; `frontend/src/pages/RunsPage.tsx`
+ `RunsPage.test.tsx`; `frontend/src/components/StatusBadge.tsx`, `QueueBadge.tsx`,
`FailureSummary.tsx`, `MutationForm.tsx`, `ConfirmForm.tsx`, `DecisionBanner.tsx`,
`MutationForm.test.tsx`; `frontend/src/hooks/usePoller.ts` + `usePoller.test.ts`;
`frontend/src/types/banner.ts`; `frontend/src/generated/dtos.d.ts`; `frontend/src/test/setup.ts`.
New docs/CI/build: `docs/ASSERTION-INVENTORY.md`; `.github/workflows/ci.yml` jobs `frontend` and
`docker-build` plus steps `Assertion inventory completeness`, `Diff-scope fence`,
`Generated DTO staleness`; `Dockerfile` stage `frontend`; `.dockerignore` entries
`app/static/dist` and `frontend/node_modules`.
Modified existing (no new symbols): `app/routes/runs.py`, `app/templates/runs_list.html`,
`tests/test_design_tokens.py`, `tests/test_dashboard.py`, `README.md`, `.gitignore`.

<!-- planner-discipline-allow: localhost -->
<!-- planner-discipline-allow: VITE_DEV_SERVER_URL -->

<tasks>

<task type="auto">
  <name>Task 1: A dev-server setting that defaults off and a fail-closed dev branch in the page renderer</name>
  <reversibility rating="costly">Removing the dev branch later means re-answering SHELL-04 from
  scratch, and any leak of the setting into production serves script tags pointing at a developer's
  machine. Rating carried from D-22-03.</reversibility>
  <files>app/config.py, app/routes/templating.py, tests/test_react_dev_mode.py</files>
  <read_first>
    - app/config.py (the settings class and its default-to-the-safe-state fields — the unsigned-fixture
      flag and the pump token are the convention this field follows; also the settings-config block and
      the cached accessor at the foot of the file)
    - app/routes/templating.py (the manifest loader, the page renderer and the escaping helper added
      by plan 22-04)
    - app/templates/react_page.html (the boot-tag block the dev branch must fill differently)
    - frontend/MANIFEST-SHAPE.md (the production chunk shape, so the two branches produce structurally
      comparable output)
    - Dockerfile (confirm no environment variable is set in the runtime stage that could enable this)
    - tests/conftest.py (the settings-cache-clearing idiom used by existing tests that vary settings)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-03 and its three named
      mitigations)
  </read_first>
  <action>
Add one field to the settings class: an optional dev-server origin, read from the environment variable
`VITE_DEV_SERVER_URL`, defaulting to the empty string. Empty means disabled. This follows the repo's
established convention that a flag defaults to the production-safe value and refuses rather than
degrades.

Branch `render_react_page` on that setting. When it is empty — the default and the only state a
deployed container can be in — resolve the entry's hashed asset paths through the manifest loader
exactly as today, still raising when the manifest is absent. When it is set, emit the dev client
module and the entry's source path from the configured origin instead, and skip the manifest entirely:
in dev there is no build output to read, so requiring one would make the dev branch unusable. Keep the
two branches in one function with a single early return so the production path cannot be reached
through the dev path's code by accident.

Write `tests/test_react_dev_mode.py` asserting all four of D-22-03's mitigations plus the fail-closed
property:
- With default settings, a render of the runs page emits no asset URL pointing at a developer host —
  assert the rendered markup contains no `localhost` substring and no reference to the dev-server
  environment variable's value.
- With default settings and the fixture manifest removed, the render still raises the
  manifest-missing error, so adding the dev branch did not weaken the production fail-closed path.
- With the setting explicitly set to a dev origin, the render emits the dev client module and the
  entry source path from that origin and does not read the manifest at all (assert by pointing the
  manifest path at a nonexistent file and confirming the render still succeeds).
- The image build never sets the setting: assert the `Dockerfile` text contains no occurrence of the
  environment variable name.
Use the settings-cache-clearing idiom already used elsewhere in the suite when varying settings, and
restore the default in a fixture teardown so no other test inherits a dev-mode render.

The environment variable must also be added to the committed example environment file. This repo has
a recorded harness guard that denies writes to that path; if the edit is refused, do not work around
it — record the exact line to paste in SUMMARY.md and flag it as a manual step, exactly as prior quick
tasks in this repo did.
  </action>
  <verify>
    <automated>uv run pytest tests/test_react_dev_mode.py tests/test_react_page_render.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_react_dev_mode.py -x -q` exits 0.
    - `uv run python -c "from app.config import Settings; import inspect; f=Settings.model_fields['vite_dev_server_url']; assert f.default=='' , f.default; print('default is the disabled value')"` exits 0.
    - `uv run python -c "assert 'VITE_DEV_SERVER_URL' not in open('Dockerfile').read(); print('image build never sets it')"` exits 0.
    - The test module contains a case asserting a default-settings render of the runs page contains no
      `localhost` substring.
    - The test module contains a case asserting the production branch still raises the manifest-missing
      error with default settings and no manifest present.
    - The test module contains a case asserting the dev branch renders successfully with the manifest
      path pointed at a nonexistent file.
    - `uv run pytest -q` (full suite) exits 0, confirming no test inherited a dev-mode render.
    - SUMMARY.md records either the committed example-environment-file line or, if the harness refused
      the write, the exact line to paste and an explicit manual-step flag.
    - `uv run ruff check .` and `uv run mypy` exit 0.
  </acceptance_criteria>
  <done>The dev branch exists, defaults off, cannot be enabled by the image build, and did not weaken
  the production fail-closed path — each property asserted by a test rather than by inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Vite dev server proxying to uvicorn, with the redirect-origin failure mode checked</name>
  <precondition>uvicorn can be started locally against the configured database (`uv run uvicorn
  app.main:app --port 8000` reaches a serving state), since the redirect-origin check requires both
  servers running.</precondition>
  <files>frontend/vite.config.ts</files>
  <read_first>
    - frontend/vite.config.ts (the build config from plan 22-03)
    - app/routes/demo.py (the demo send-test handler and its redirect to the newly created run — the
      exact response the origin check exercises)
    - app/main.py (the route prefixes the proxy must forward: the static mount and the seven routers)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-03)
    - .planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md (Open Question 2 — the
      architecture research's stated concern about a redirect location resolving against the wrong
      origin, which the user chose to pin rather than gate)
  </read_first>
  <action>
Add a dev-server section to `frontend/vite.config.ts` configuring a proxy that forwards every
non-asset request path to a locally running uvicorn. Enumerate the forwarded prefixes from the live
route table rather than using a catch-all: the seven router prefixes plus the static mount. A
catch-all proxy here would be the dev-side mirror of the catch-all route this phase's guards exist to
forbid, and it would make it impossible to tell which paths Vite is serving.

Do not enable automatic origin rewriting on the proxy. The whole point of this direction is that the
dev server is the document origin, so a redirect the app emits must resolve against the dev origin
naturally; rewriting the location header would paper over the exact failure being checked.

Then run the redirect-origin check by hand and record it. Start uvicorn on its port, start the dev
server with the dev-server setting exported so the server renders dev asset tags, load the runs page
through the dev origin, submit the demo send-test form, and record: the status code of the form
response, the raw location header value it carried, and the origin the browser actually landed on. The
check passes only if the browser ends on the dev origin's run detail path. If it lands on the uvicorn
origin instead, that is the architecture research's named failure mode reproduced — stop, record it,
and surface it rather than adjusting the test to accept it.

Record the same three values for one more redirecting route so the result is not a single-sample
claim.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run check &amp;&amp; node -e "const s=require('fs').readFileSync('vite.config.ts','utf8');if(!/server\s*:/.test(s))throw new Error('no server section');if(!/proxy\s*:/.test(s))throw new Error('no proxy section');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run check` exits 0 and `frontend/vite.config.ts` contains a dev-server
      section with a proxy map.
    - The proxy map enumerates explicit path prefixes and contains no catch-all entry; the enumerated
      list is quoted in SUMMARY.md alongside the route table it was derived from.
    - The proxy does not enable automatic origin rewriting.
    - SUMMARY.md records, for the demo send-test submission made through the dev origin: the response
      status code, the raw location header value, and the origin the browser landed on — and the
      landing origin is the dev origin.
    - SUMMARY.md records the same three values for a second redirecting operator route.
    - If either landing origin is the uvicorn origin rather than the dev origin, the plan is NOT done:
      the finding is recorded verbatim and surfaced as a blocker rather than accepted.
  </acceptance_criteria>
  <done>The dev server proxies to uvicorn over an explicitly enumerated prefix list, and the redirect
  failure mode the architecture research named has been checked for real against two routes and
  recorded with its raw header values.</done>
</task>

<task type="auto">
  <name>Task 3: README developer instructions for both commands</name>
  <files>README.md</files>
  <read_first>
    - README.md (the existing installation and local-development sections, and its tone — this file
      was rewritten for recruiter-first clarity in quick task 260710-iw0 and corrected where it had
      gone false; do not reintroduce a claim the code does not support)
    - frontend/package.json (the six scripts)
    - CLAUDE.md (the tooling rule: this project uses uv, never pip or a hand-maintained requirements
      file, so the Python half of the instructions must use the uv commands)
    - .planning/phases/22-frontend-foundation-runs-list/22-VALIDATION.md (the Manual-Only
      Verifications row for SHELL-04 — the instructions must be the thing that was actually run)
  </read_first>
  <action>
Add a frontend section to the README documenting exactly two developer commands, because that is what
SHELL-04 asks for: one to start the hot-reloading dev server against uvicorn, and one to typecheck and
lint.

Document the two-process shape honestly: uvicorn runs on its port, the dev server runs on its port
with the dev-server environment variable exported so the app renders dev asset tags, and the developer
browses the dev origin. State that the environment variable defaults to empty, that empty means
production asset tags, and that the image build never sets it — a reader must be able to tell from the
README alone that this is a local-only affordance.

Document the lockfile discipline: install with the lockfile-asserting npm command, never the resolving
one, mirroring the Python side's locked sync. State why in one sentence: a stale lockfile should fail
rather than silently re-resolve.

Document that the production bundle is built inside the image and is deliberately not committed, and
that the output directory is gitignored — a reader who sees an empty output directory locally should
not conclude the build is broken.

Every command written must have been run by the executor in this task, and the observed output
summarized in SUMMARY.md. Do not document a command that was not executed.
  </action>
  <verify>
    <automated>uv run python -c "t=open('README.md').read(); assert 'npm ci' in t; assert 'npm run check' in t; assert 'npm run dev' in t; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains a frontend section naming the dev-server command and the typecheck-and-lint
      command.
    - `README.md` states that the dev-server environment variable defaults to empty, that empty means
      production asset tags, and that the image build never sets it.
    - `README.md` names the lockfile-asserting install command and gives the one-sentence reason.
    - `README.md` states that the bundle is built inside the image and is not committed.
    - SUMMARY.md records the executor running each documented command and summarizes its output.
    - No documented command references a package manager other than uv on the Python side.
  </acceptance_criteria>
  <done>A developer can read the README, run two commands, and get a hot-reloading dev server and a
  typecheck-and-lint gate — and every documented command was executed before it was written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-server origin → browser | In dev the browser loads executable modules from a developer host rather than from the deployed static mount |
| environment variable → rendered asset URLs | One setting decides which origin serves the page's scripts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-38 | Spoofing | the dev setting leaking into a deployed container | high | mitigate | The setting defaults to the disabled value, the image build never sets it (asserted by a test reading the Dockerfile), and a default-settings render is asserted to contain no developer-host URL |
| T-22-39 | Tampering | the dev branch weakening the production fail-closed path | high | mitigate | A test asserts the production branch still raises the manifest-missing error with default settings and no manifest present |
| T-22-40 | Tampering | a redirect location resolving against the wrong origin in dev | medium | mitigate | The proxy performs no origin rewriting and the redirect landing origin is measured for real on two routes and recorded with raw header values; the risk is dev-only and never reaches production |
| T-22-41 | Denial of Service | a catch-all dev proxy masking which paths the app serves | medium | mitigate | The proxy enumerates explicit prefixes derived from the live route table; an acceptance criterion asserts no catch-all entry |
| T-22-42 | Information Disclosure | the example environment file drifting from the real setting name | low | accept | Recorded as a manual step in SUMMARY.md when the harness guard refuses the write, matching this repo's existing precedent |
</threat_model>

## Flagged planner assumptions (probe rows that stay unresolved)

- **Dev-proxy direction (RESEARCH.md Open Question 2)** — resolved by the user in favour of the locked
  D-22-03 direction, with the risk pinned by acceptance criteria rather than a checkpoint. The
  planner's residual assumption is that the redirect-origin check on two routes is a sufficient sample
  to declare the failure mode absent; a route with an absolute location header rather than a relative
  one could still behave differently and is not exercised here. Recorded so Phase 23, which converts
  fourteen forms, knows to re-check when it adds redirecting routes to the dev flow.

<verification>
- `uv run pytest tests/test_react_dev_mode.py tests/test_react_page_render.py -x -q` exits 0.
- `uv run pytest -q` (full suite) exits 0.
- `cd frontend && npm run check` exits 0.
- `uv run ruff check .` and `uv run mypy` exit 0.
- SUMMARY.md records the redirect-origin measurements for two routes, with raw location header values.
- SUMMARY.md records every README command as executed, with its output summarized.
</verification>

<success_criteria>
- One documented command starts a hot-reloading dev server against uvicorn; one documented command
  typechecks and lints; both were run.
- The dev setting defaults off, the image build never sets it, and a default render carries no
  developer-host URL — each asserted by a test.
- The production fail-closed manifest behaviour is unchanged.
- The named redirect-origin failure mode was measured, not assumed.
</success_criteria>

<output>
Create `.planning/phases/22-frontend-foundation-runs-list/22-09-SUMMARY.md` when done.
</output>
