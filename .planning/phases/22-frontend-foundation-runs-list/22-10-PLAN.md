---
phase: 22-frontend-foundation-runs-list
plan: 10
type: execute
wave: 5
depends_on: ["22-06", "22-07"]
files_modified:
  - frontend/src/hooks/usePoller.ts
  - frontend/src/hooks/usePoller.test.ts
  - frontend/src/pages/RunsPage.tsx
  - frontend/src/pages/RunsPage.test.tsx
  - tests/test_no_fetch_outside_poller.py
autonomous: true
requirements: [LIST-02, GUARD-06]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A poll tick returning an unchanged status leaves the rendered badge text and class name byte-identical: the update is idempotent, produces no flicker, and creates no duplicate node."
    - "Each in-flight row owns its own poller instance and its own interval; unmounting one row clears only that row's interval while the others keep ticking, and unmounting the page clears every interval — proven by a request counter that stops increasing after unmount."
    - "The lint rule and the hermetic source guard are two independent enforcement paths: disabling, renaming or misconfiguring the frontend CI job leaves the hermetic guard still failing the existing test job, so neither gate depends on the other's run order."
    - "The hook reproduces all five properties of the poller it replaces: the same request path shape, the same two-second interval, the same sixty-attempt cap, the same settle condition, and the same per-tick error swallowing."
    - "Polling stops when the run settles — when the status is no longer in-flight and no open job remains — and stops again at the attempt cap with no recovery side effect."
    - "The badge updates in place on a new status without a full page reload, asserted by a component test that replaces the deleted script-hook presence assertion."
    - "The request-issuing API is called from exactly one module under the frontend source tree, and a call added anywhere else fails both the lint script and the hermetic test."
  artifacts:
    - frontend/src/hooks/usePoller.ts
    - frontend/src/hooks/usePoller.test.ts
    - tests/test_no_fetch_outside_poller.py
  key_links:
    - "The hook consumes the poll shape declared in `app/schemas/run_status.py` and served by the status poll endpoint; the row merges the seven volatile fields over its static fields and the type system enforces the merge."
    - "`frontend/eslint.config.js`'s network-globals override (from plan 22-03) is scoped to exactly this hook's path; this plan gives that override its real target."
    - "This plan owns the Vitest badge-in-place test that plan 22-02's deletion justification named as its replacement."
  prohibitions: []
---

<objective>
Restore in-place status, queue and failure badge updates on the React list, as a typed hook that
reproduces the replaced poller's five observable properties exactly, and close GUARD-06 with a second,
independent enforcement path so the native-form decision is enforced rather than trusted.

Purpose: LIST-02, GUARD-06.
Output: `usePoller`, its teardown-observable test suite, the badge-in-place component test that plan
22-02's deletion justification promised, and the hermetic source guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md
@.planning/phases/22-frontend-foundation-runs-list/22-06-SUMMARY.md
@.planning/phases/22-frontend-foundation-runs-list/22-07-SUMMARY.md
</context>

## Artifacts this phase produces

New Python (app): `app/schemas/__init__.py`; `app/schemas/_projection.py` (`RowProjection`,
`UnclassifiedColumnError`); `app/schemas/runs_list.py` (`RunListRow`, `RunsListPage`);
`app/schemas/run_status.py` (`RunStatusPoll`); `app/schemas/run_columns.py`
(`RUN_COL_CLASSIFICATION`, `ColumnExposure`); `app/routes/templating.py` gains `render_react_page`,
`json_script`, `load_manifest`, `ManifestMissingError`, `_JSON_SCRIPT_ESCAPES`; `app/config.py`
gains `Settings.vite_dev_server_url`.
New Python (scripts): `scripts/render_assertion_inventory.py`; `scripts/generate_openapi_doc.py`.
New Python (tests): `tests/assertion_inventory.py`; `tests/safety_mutation_registry.py`;
`tests/test_inventory_completeness.py`; `tests/test_safety_mutation_registry.py`;
`tests/test_route_shadowing.py`; `tests/test_no_html_on_service_routes.py`;
`tests/test_page_shell_pins.py`; `tests/test_react_page_render.py`;
`tests/test_bundle_asset_exists.py`; `tests/test_schema_projection.py`;
`tests/test_no_fetch_outside_poller.py`; `tests/test_react_dev_mode.py`;
`tests/test_generated_types_staleness.py`; `tests/test_ci_gate_config.py`.
New templates/fixtures: `app/templates/react_page.html`; `tests/fixtures/vite_manifest.json`.
New frontend tree: `frontend/package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`,
`tsconfig.node.json`, `eslint.config.js`, `vitest.config.ts`, `MANIFEST-SHAPE.md`;
`frontend/src/entries/runs.tsx`; `frontend/src/boot/pageData.ts`; `frontend/src/pages/RunsPage.tsx`
+ `RunsPage.test.tsx`; `frontend/src/components/StatusBadge.tsx`, `QueueBadge.tsx`,
`FailureSummary.tsx`, `MutationForm.tsx`, `ConfirmForm.tsx`, `DecisionBanner.tsx`,
`MutationForm.test.tsx`; `frontend/src/hooks/usePoller.ts` + `usePoller.test.ts`;
`frontend/src/types/banner.ts`; `frontend/src/generated/dtos.d.ts`; `frontend/src/test/setup.ts`.
New docs/CI/build: `docs/ASSERTION-INVENTORY.md`; `.github/workflows/ci.yml` jobs `frontend` and
`docker-build` plus steps `Assertion inventory completeness`, `Diff-scope fence`,
`Generated DTO staleness`; `Dockerfile` stage `frontend`; `.dockerignore` entries
`app/static/dist` and `frontend/node_modules`.
Modified existing (no new symbols): `app/routes/runs.py`, `app/templates/runs_list.html`,
`tests/test_design_tokens.py`, `tests/test_dashboard.py`, `README.md`, `.gitignore`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: usePoller — five-property parity with the poller it replaces, teardown observable</name>
  <reversibility rating="costly">The hook's single-URL, one-instance-per-row signature is the shape
  Phase 23 reuses unchanged; a batch endpoint would change the wire shape this milestone claims to
  preserve and was deferred to backlog. Rating carried from D-22-14.</reversibility>
  <files>frontend/src/hooks/usePoller.ts, frontend/src/hooks/usePoller.test.ts</files>
  <read_first>
    - The pre-conversion poller script, recovered with
      `git show 22-04~1:app/templates/runs_list.html` — lines 10-59 are the behaviour being
      reproduced: the request path shape, the interval, the attempt cap, the settle condition, and the
      per-tick error swallowing
    - app/schemas/run_status.py (the poll response shape the hook is typed against)
    - app/routes/runs.py (the status poll endpoint's not-found behaviour, so the hook's non-ok branch
      matches what the server actually returns)
    - frontend/eslint.config.js (the network-globals override scoped to this hook's exact path)
    - frontend/vitest.config.ts (the jsdom environment and setup file)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-14, and the note that the
      dependency-array rule is the reason ESLint was chosen)
    - .planning/STATE.md (the recorded lesson that a cleanup call which IS the assertion under test
      must never be wrapped in a suppressing construct)
  </read_first>
  <behavior>
    - Given a settle predicate that never returns true, the hook issues one request per interval tick
      and stops after exactly the configured attempt cap.
    - Given a response whose payload satisfies the settle predicate, the hook stops issuing requests
      immediately after that tick.
    - Given a rejected request, the hook swallows the failure for that tick and issues the next tick
      on schedule.
    - Given a non-ok response, the hook does not invoke the update callback for that tick.
    - On unmount, the request counter stops increasing when fake timers are advanced past several
      further intervals.
    - Two hook instances mounted on different targets each issue their own requests; unmounting one
      leaves the other's counter still increasing.
    - The update callback receives the parsed payload typed as the poll shape.
  </behavior>
  <action>
Write every case above in `frontend/src/hooks/usePoller.test.ts` first, run them red, then implement.

The hook signature is single-URL and single-instance: it takes the request path, an options object
carrying the interval, the attempt cap and the settle predicate, and an update callback. The runs list
mounts one instance per in-flight row, which preserves the per-row request shape and the per-row stop
behaviour the replaced poller had, and which Phase 23's detail page reuses with a single instance and
no adaptation.

Reproduce all five observable properties of the poller being replaced, taken from the recovered
source rather than from memory: the same request path shape, the same two-second interval, the same
sixty-attempt cap, the same settle condition (the status is no longer in the in-flight set AND no open
job remains), and the same per-tick error swallowing. The error swallowing is deliberate — it is a
network-blip guard, not error hiding — and must carry a comment saying so, because a reviewer who
sees an empty rejection handler and deletes it turns a transient blip into a stalled badge.

This hook is the single sanctioned caller of the browser request API in the whole frontend tree; the
lint override created in plan 22-03 is already scoped to this exact path. Do not add a second call
site anywhere.

The effect's dependency array is the reason this project chose ESLint. If the dependencies are
intentionally captured once per mount rather than re-derived per render — because re-deriving them
would restart every row's poll on every parent re-render — that decision must be written as an
explicit inline suppression with a reason, not silently widened to satisfy the rule. Whichever way
you resolve it, the reason goes in the source.

Write the teardown test so the cleanup is genuinely the assertion: stub the request API with a call
counter, mount, advance fake timers, unmount, advance further, and assert the counter did not
increase. Do NOT wrap that assertion in a try/finally or any other suppressing construct — this repo
has a recorded scar where a cleanup-as-subject test was wrapped in a suppressing construct and stayed
green while the invariant broke.

Prove the tests are not vacuous: delete the interval-clearing call from the cleanup return, run the
suite, capture the RED case, restore it, and confirm `git diff --stat frontend/src/hooks/usePoller.ts`
is empty. Then separately raise the attempt cap check so it never fires, confirm a different case
reds, and revert. Capture both transcripts.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test -- usePoller &amp;&amp; npm run check</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- usePoller` exits 0 with a passing case for every behavior above.
    - `cd frontend && npm run check` exits 0, including the effect-dependency rule.
    - `frontend/src/hooks/usePoller.ts` contains a comment stating that the per-tick rejection
      handler is a network-blip guard and not error hiding.
    - If the effect dependencies are captured once per mount, the source carries an explicit inline
      suppression with a written reason; if not, no suppression is present. Whichever holds is stated
      in SUMMARY.md.
    - The teardown test contains no try/finally or equivalent suppressing construct around its
      assertion; confirmed by reading the test and recorded in SUMMARY.md.
    - SUMMARY.md contains two RED transcripts — the removed interval-clearing call and the disabled
      attempt cap — each with its `git diff --stat` revert proof.
    - `frontend/src/hooks/usePoller.ts` is the only file under `frontend/src` invoking the browser
      request API, verified by `uv run pytest tests/test_no_fetch_outside_poller.py -x -q` after
      Task 3.
  </acceptance_criteria>
  <done>A typed hook reproduces the replaced poller's five observable properties, its teardown is
  proven observable rather than assumed, and it is the tree's single request call site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the poller into the runs list and pin the in-place badge update</name>
  <files>frontend/src/pages/RunsPage.tsx, frontend/src/pages/RunsPage.test.tsx</files>
  <read_first>
    - frontend/src/pages/RunsPage.tsx (the complete list from plan 22-06)
    - frontend/src/hooks/usePoller.ts (the hook from Task 1)
    - The pre-conversion poller script (`git show 22-04~1:app/templates/runs_list.html`, lines 54-58)
      — the exact per-row start condition: a row polls when it is in-flight OR has an open job
    - app/schemas/runs_list.py (the composition of the seven volatile fields over the static ones)
    - tests/test_design_tokens.py (the deletion justification comment from plan 22-02 naming this plan
      as the owner of the replacement test)
  </read_first>
  <behavior>
    - A row that is neither in-flight nor carrying an open job issues no requests at all.
    - A row that is in-flight issues requests; a row with an open job issues requests.
    - When a tick returns a new status, that row's status badge text and class name change in place
      and no full re-render of the table occurs (the row element identity is unchanged).
    - When a tick returns an unchanged status, the badge's text and class name are byte-identical to
      before and no duplicate badge node exists.
    - When a tick returns a settled status with no open job, that row stops issuing requests while
      another still-in-flight row keeps issuing them.
    - A queue label transition updates the queue badge and its hidden state in place.
    - A failure appearing on a tick renders the failure summary and secondary badge in place.
  </behavior>
  <action>
Mount one poller instance per in-flight row, preserving the replaced poller's per-row start condition
exactly: a row polls when it is in-flight or has an open job, and only then. A row that is settled and
has no open job must issue no request at all — starting a poll for it would change the network shape
this milestone claims to preserve.

Hold the seven volatile fields in row-local component state and merge each tick's payload over them,
leaving the static fields untouched. The merge is what makes the update in-place: the row element and
the table structure are unchanged and only the badges re-render.

Write the Vitest cases for every behavior above first. Two of them carry specific weight:
- The unchanged-status case is the idempotency pin — assert the badge's text and class name are equal
  to their pre-tick values and that exactly one badge node exists, so a tick can never duplicate a
  node or flicker a class.
- The in-place update case is the replacement plan 22-02's deletion justification named. Assert the
  badge text and class change while the row element identity is preserved, and reference the deleted
  guard by name in a comment so the trail from the deletion to its replacement is readable in source.

Prove the wiring red: change the per-row start condition so every row polls regardless of state, run
the suite, and confirm the never-polls case reds. Revert and confirm
`git diff --stat frontend/src/pages/RunsPage.tsx` is empty.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run test -- RunsPage &amp;&amp; npm run check &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run test -- RunsPage` exits 0 with a passing case for every behavior above.
    - `cd frontend && npm run check && npm run build` exits 0.
    - `frontend/src/pages/RunsPage.test.tsx` contains a case asserting the badge text and class change
      in place while the row element identity is preserved, and its comment names the guard deleted in
      plan 22-02 as the assertion it replaces.
    - `frontend/src/pages/RunsPage.test.tsx` contains a case asserting an unchanged-status tick leaves
      the badge text and class name equal and exactly one badge node present.
    - `frontend/src/pages/RunsPage.tsx` calls the hook only for rows that are in-flight or carry an
      open job; the settled-row-never-polls case passes.
    - SUMMARY.md contains the RED transcript from broadening the start condition, with its
      `git diff --stat` revert proof.
    - `uv run pytest tests/test_design_tokens.py -x -q` still exits 0.
  </acceptance_criteria>
  <done>In-flight rows update their status, queue and failure badges in place with no full reload,
  settled rows stop polling, and the deleted script-hook guard has the replacement its justification
  promised.</done>
</task>

<task type="auto">
  <name>Task 3: GUARD-06's second, independent enforcement path in the hermetic Python suite</name>
  <files>tests/test_no_fetch_outside_poller.py</files>
  <read_first>
    - .planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md (the "Hermetic pytest AST guard
      mirroring GUARD-06's second enforcement half" code example — it enumerates the exact tokens to
      scan for, and its own caveat that this is a text scan rather than a real parse)
    - frontend/eslint.config.js (the primary enforcement layer, so the two guards agree on the same
      allowlist path)
    - frontend/src/hooks/usePoller.ts (the single allowed call site)
    - tests/test_proof_mutation_targets.py (the scoped-search discipline, cited in the research as the
      cautionary precedent for why a bare substring scan is weaker)
    - .planning/STATE.md (the recorded lesson that a guard proves nothing about what it does not scan,
      and that a repo grep silently ignored a word-boundary escape)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-17: enforced twice, so the
      ban survives someone disabling, renaming or misconfiguring the frontend job)
  </read_first>
  <action>
Create `tests/test_no_fetch_outside_poller.py`, the second and independent half of GUARD-06. It runs
in the existing hermetic test job, needs no Node, and exists precisely so that disabling, renaming or
misconfiguring the frontend CI job does not silently remove the ban.

Walk every TypeScript and TSX file under the frontend source tree, excluding the single allowed
poller-hook path and excluding any dependency directory, and fail if a file references the browser
request API, the legacy request object, or the third-party HTTP client package. Take the exact token
list from the research code example rather than inventing one.

Be honest about the guard's strength in its docstring, in this repo's guard style: state what it
establishes, what it does not (a determined string-concatenation obfuscation defeats it), and why it
is a text scan rather than a parse (the Python standard library parser cannot read TSX). State that
lint is the primary layer and this is the belt to that suspenders.

Close the blind spot this repo has been burned by twice: add a companion test asserting the scan
actually visited a non-zero number of files, and asserting the allowlisted path exists. A guard whose
walk silently matches nothing passes vacuously, and a stale allowlist path means the real call site
is being scanned while a renamed file is not.

Add the no-false-positive half too: assert the guard reports no offender for the committed tree, and
assert it does report one for a synthetic file placed in a temporary directory, so a future
over-broad pattern is caught.

Demonstrate it red for real: add a scratch module under the frontend source tree that calls the
browser request API, run the guard, capture the RED output naming the file, then delete the scratch
module and confirm `git status --porcelain frontend/src` is empty. Separately demonstrate the lint
half red for the same scratch module and capture its rule id, so both enforcement paths have an
independent transcript.

Finally, demonstrate the independence claim: disable the frontend CI job in the working-tree copy of
the workflow, confirm the hermetic guard still fails on the scratch module, then revert the workflow
and confirm `git diff --stat .github/workflows/ci.yml` is empty.
  </action>
  <verify>
    <automated>uv run pytest tests/test_no_fetch_outside_poller.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_no_fetch_outside_poller.py -x -q` exits 0.
    - The module contains a test asserting the walk visited a non-zero number of files and a test
      asserting the allowlisted poller path exists on disk.
    - The module contains a test asserting a synthetic offending file in a temporary directory IS
      reported, so the pattern is not silently over-narrow.
    - The module docstring states what the guard establishes, what it does not, and why it is a text
      scan rather than a parse.
    - SUMMARY.md contains three transcripts: the hermetic guard red on a scratch module, the lint
      script red on the same module with its rule id, and the hermetic guard still red with the
      frontend CI job disabled.
    - SUMMARY.md contains the `git status --porcelain frontend/src` and
      `git diff --stat .github/workflows/ci.yml` lines proving both mutations were reverted.
    - `uv run ruff check tests/test_no_fetch_outside_poller.py` and
      `uv run mypy tests/test_no_fetch_outside_poller.py` exit 0.
  </acceptance_criteria>
  <done>A request-issuing call added outside the poller hook fails both the lint job and the hermetic
  test job, the two paths are proven independent, and the guard is proven not to pass vacuously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → status poll endpoint | The one sanctioned client-initiated request in the whole frontend |
| frontend source → lint and test gates | Two independent enforcement paths guard the native-form decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-43 | Tampering | a request-based mutation losing the confirmation prompt and the redirect-encoded operator feedback | high | mitigate | Lint bans the request API outside the poller hook and the hermetic Python guard asserts the same invariant in the existing test job; both are demonstrated red on the same scratch module and their independence is demonstrated by disabling the frontend job |
| T-22-44 | Repudiation | a guard passing vacuously because its walk matched nothing | high | mitigate | Companion tests assert a non-zero visited-file count, assert the allowlisted path exists, and assert a synthetic offender IS reported |
| T-22-45 | Denial of Service | a leaked interval polling forever after unmount | medium | mitigate | The teardown test stubs the request API with a counter, unmounts, advances timers and asserts the counter stopped, with no suppressing construct around the assertion; the falsifying mutation removes the clearing call and is run for real |
| T-22-46 | Denial of Service | every row polling regardless of state | medium | mitigate | The per-row start condition is reproduced from the replaced poller and pinned by a settled-row-never-polls case, demonstrated red by broadening the condition |
| T-22-47 | Information Disclosure | the poll response widening beyond the declared shape | medium | mitigate | The endpoint declares an enforced response model (plan 22-07) and the hook is typed against it, so an undeclared field is a compile error rather than a silent leak |
</threat_model>

## Flagged planner assumptions (probe rows that stay unresolved)

None owned by this plan. LIST-02's two probed edges and GUARD-06's single edge are all authored as
explicit `must_haves.truths` above.

<verification>
- `cd frontend && npm run check && npm run test && npm run build` exits 0.
- `uv run pytest tests/test_no_fetch_outside_poller.py tests/test_design_tokens.py -x -q` exits 0.
- `uv run pytest -q` (full suite) exits 0.
- `uv run ruff check .` and `uv run mypy` exit 0.
- SUMMARY.md carries five demonstrated-red transcripts: the removed interval-clearing call, the
  disabled attempt cap, the broadened per-row start condition, the hermetic request guard, and the
  lint request rule — plus the independence demonstration with the frontend job disabled.
</verification>

<success_criteria>
- In-flight rows update their badges in place with no full page reload and stop polling when settled.
- An unchanged status produces a byte-identical badge with no duplicate node.
- Unmounting clears every interval, proven by an observable request counter with no suppressing
  construct.
- A request issued outside the poller hook fails both enforcement paths, and the two paths are proven
  not to depend on each other.
</success_criteria>

<output>
Create `.planning/phases/22-frontend-foundation-runs-list/22-10-SUMMARY.md` when done.
</output>
