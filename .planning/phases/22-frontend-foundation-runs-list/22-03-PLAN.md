---
phase: 22-frontend-foundation-runs-list
plan: 03
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - frontend/package.json
  - frontend/package-lock.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.node.json
  - frontend/eslint.config.js
  - frontend/vitest.config.ts
  - frontend/src/test/setup.ts
  - frontend/src/entries/runs.tsx
  - frontend/MANIFEST-SHAPE.md
  - .gitignore
  - .dockerignore
autonomous: false
requirements: [SHELL-04, SHELL-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`npm run build` inside `frontend/` produces a real bundle under `app/static/dist/`, and the manifest's actual on-disk path and ManifestChunk key set are recorded in a committed file BEFORE any code reads a manifest."
    - "`npm run check` runs typecheck then lint and exits non-zero if either fails; `npm run test` runs Vitest once in run mode, never as a watcher."
    - "Every runtime and dev dependency installs at the pinned version from the phase research table, never at a floating tag, and `package-lock.json` is committed so CI and Docker can use `npm ci`."
    - "`frontend/eslint.config.js` bans the browser network globals and the axios package across `src/`, bans a JSX form opening element outside the two sanctioned form components, and re-enables each ban only through a path-scoped override."
    - "`frontend/eslint.config.js` bans the raw-HTML JSX attribute as a standing rule ahead of Phase 23's conversation-thread surface."
    - "`node_modules` is gitignored, and `frontend/node_modules` plus `app/static/dist` are dockerignored, so no local build artefact or dependency tree can ride into the image and mask the deploy trap."
    - "No stylesheet scaffolded by the Vite template survives: `app/static/style.css` stays the single token source and React applies existing class names only."
  artifacts:
    - frontend/package.json
    - frontend/package-lock.json
    - frontend/vite.config.ts
    - frontend/eslint.config.js
    - frontend/vitest.config.ts
    - frontend/MANIFEST-SHAPE.md
  key_links:
    - "`vite.config.ts` points `build.outDir` at `../app/static/dist` so the bundle is served through the existing `/static` mount in `app/main.py` — no second mount, no new route."
    - "`frontend/MANIFEST-SHAPE.md` is the input plan 22-04's manifest loader is written against; the loader must not be written from an assumed shape."
    - "`frontend/src/test/setup.ts` is registered through `vitest.config.ts` `test.setupFiles` and installs the DOM matchers."
  prohibitions: []
---

<objective>
Stand up the `frontend/` toolchain and resolve the phase's single largest unverified assumption: the
exact on-disk Vite manifest path and chunk shape. No `frontend/` file may exist before the GUARD-01
registry is committed (D-22-08), and the manifest loader must not be written in the same task that
first produces a manifest.

Purpose: SHELL-04's typecheck-and-lint command, SHELL-06's buildable/lintable/testable surface,
GUARD-06's first enforcement half, and RESEARCH.md Open Question 1.
Output: a pinned, lockfile-committed `frontend/` with Vite, TypeScript, ESLint and Vitest configured,
plus a committed record of the real manifest shape.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md
@.planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md
</context>

## Artifacts this phase produces

New Python (app): `app/schemas/__init__.py`; `app/schemas/_projection.py` (`RowProjection`,
`UnclassifiedColumnError`); `app/schemas/runs_list.py` (`RunListRow`, `RunsListPage`);
`app/schemas/run_status.py` (`RunStatusPoll`); `app/routes/templating.py` gains
`render_react_page`, `json_script`, `load_manifest`, `ManifestMissingError`, `_JSON_SCRIPT_ESCAPES`;
`app/config.py` gains `Settings.vite_dev_server_url`.
New Python (scripts): `scripts/render_assertion_inventory.py`; `scripts/generate_openapi_doc.py`.
New Python (tests): `tests/assertion_inventory.py`; `tests/safety_mutation_registry.py`;
`tests/test_inventory_completeness.py`; `tests/test_safety_mutation_registry.py`;
`tests/test_route_shadowing.py`; `tests/test_no_html_on_service_routes.py`;
`tests/test_page_shell_pins.py`; `tests/test_react_page_render.py`;
`tests/test_bundle_asset_exists.py`; `tests/test_schema_projection.py`;
`tests/test_no_fetch_outside_poller.py`; `tests/test_react_dev_mode.py`;
`tests/test_generated_types_staleness.py`.
New templates/fixtures: `app/templates/react_page.html`; `tests/fixtures/vite_manifest.json`.
New frontend tree: `frontend/package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`,
`tsconfig.node.json`, `eslint.config.js`, `vitest.config.ts`, `MANIFEST-SHAPE.md`;
`frontend/src/entries/runs.tsx`; `frontend/src/boot/pageData.ts` (`readInitialData`,
`INITIAL_DATA_ELEMENT_ID`); `frontend/src/pages/RunsPage.tsx` (`RunsPage`) + `RunsPage.test.tsx`;
`frontend/src/components/StatusBadge.tsx`, `QueueBadge.tsx`, `FailureSummary.tsx`,
`MutationForm.tsx`, `ConfirmForm.tsx`, `DecisionBanner.tsx`, `MutationForm.test.tsx`;
`frontend/src/hooks/usePoller.ts` + `usePoller.test.ts`; `frontend/src/types/banner.ts`;
`frontend/src/generated/dtos.d.ts`; `frontend/src/test/setup.ts`.
New docs/CI/build: `docs/ASSERTION-INVENTORY.md`; `.github/workflows/ci.yml` jobs `frontend` and
`docker-build` plus steps `Assertion inventory completeness`, `Diff-scope fence`,
`Generated DTO staleness`; `Dockerfile` stage `frontend`; `.dockerignore` entries
`app/static/dist` and `frontend/node_modules`.
Modified existing (no new symbols): `app/routes/runs.py`, `app/templates/runs_list.html`,
`tests/test_design_tokens.py`, `tests/test_dashboard.py`, `README.md`, `.gitignore`.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy confirmation before the first install</name>
  <what-built>Nothing yet. This gate runs BEFORE any package is installed. Phase research ran
  `gsd-tools query package-legitimacy check --ecosystem npm` over all 13 frontend packages. Zero
  SLOP verdicts. Eight came back SUS on the single too-new signal (vite, react, react-dom,
  @vitejs/plugin-react, @testing-library/jest-dom, jsdom, eslint, typescript-eslint) and four more
  (@types/react, @types/react-dom, @types/node, @testing-library/dom) are ASSUMED versions carried
  forward without an independent registry query. The research argues every SUS verdict is a false
  positive — 41M to 180M weekly downloads, long-established official orgs, no postinstall script,
  not deprecated — and recommends no checkpoint. GSD's package-legitimacy gate is not
  auto-approvable, so the call is yours rather than the researcher's.</what-built>
  <how-to-verify>
    For each package below, open its registry page and confirm the publishing org and that it is not
    deprecated: npmjs.com/package/vite (expect github.com/vitejs/vite), npmjs.com/package/react,
    npmjs.com/package/react-dom, npmjs.com/package/@vitejs/plugin-react,
    npmjs.com/package/@testing-library/jest-dom, npmjs.com/package/jsdom, npmjs.com/package/eslint,
    npmjs.com/package/typescript-eslint, npmjs.com/package/@types/react,
    npmjs.com/package/@types/react-dom, npmjs.com/package/@types/node,
    npmjs.com/package/@testing-library/dom.

    Then confirm the pinned versions still resolve, in a scratch shell:
      npm view vite@8.2.1 version
      npm view react@19.2.8 version
      npm view react-dom@19.2.8 version
      npm view @vitejs/plugin-react@6.0.5 version
      npm view typescript@6.0.3 version
      npm view vitest@4.1.10 version
      npm view typescript-eslint@8.67.0 version
      npm view eslint-plugin-react-hooks@7.1.1 version
      npm view @testing-library/react@16.3.2 version
      npm view @testing-library/jest-dom@7.0.1 version
      npm view jsdom@30.0.1 version
      npm view openapi-typescript@7.13.0 version
      npm view @types/react@19.2.18 version
      npm view @types/react-dom@19.2.4 version
      npm view @types/node@26.2.0 version

    A version that no longer resolves is a real finding — report it rather than approving, and the
    executor will re-pin from the registry before installing.
  </how-to-verify>
  <resume-signal>Type "approved" to install at the pinned versions, or name the packages and
  versions that must change first.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Scaffold frontend/, pin every version, run one real build, record the manifest shape</name>
  <precondition>Node and npm are on PATH (`node --version` and `npm --version` both succeed) and
  `tests/assertion_inventory.py` exists, since the GUARD-01 CI gate forbids any file under
  `frontend/src` while the registry is absent.</precondition>
  <files>frontend/package.json, frontend/package-lock.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tsconfig.node.json, frontend/src/entries/runs.tsx, frontend/MANIFEST-SHAPE.md, .gitignore, .dockerignore</files>
  <read_first>
    - .planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md (Standard Stack Core and
      Supporting tables with the exact pinned versions; the Installation block; Open Question 1;
      Recommended Project Structure)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-01, D-22-02, D-22-12)
    - app/main.py (line 11, the sole `/static` mount the bundle is served through)
    - .gitignore (full file: `dist/` at line 5 already covers `app/static/dist`, and there is no
      `node_modules` entry)
    - .dockerignore (full file: no `dist` and no `node_modules` entry today)
    - .github/workflows/ci.yml (lines 42-45, the `uv sync --locked` comment explaining why a
      lockfile assertion beats silent re-resolution; the npm side mirrors it)
  </read_first>
  <action>
Scaffold the frontend and resolve the manifest-shape unknown before anything is written against an
assumption.

Run the Vite react-ts template scaffolder into a new `frontend/` directory at the repo root. Then,
inside `frontend/`, install at exactly these pinned versions — never a floating dist-tag, so a
future point release cannot silently substitute an unvetted version. Runtime: react 19.2.8,
react-dom 19.2.8. Dev: typescript 6.0.3, vite 8.2.1, @vitejs/plugin-react 6.0.5, @types/react
19.2.18, @types/react-dom 19.2.4, @types/node 26.2.0, vitest 4.1.10, @testing-library/react 16.3.2,
@testing-library/jest-dom 7.0.1, @testing-library/dom, jsdom 30.0.1, eslint (the newest release
satisfying typescript-eslint's eslint peer range), typescript-eslint 8.67.0,
eslint-plugin-react-hooks 7.1.1, openapi-typescript 7.13.0. Commit `package-lock.json`.
typescript-eslint 8.67.0 declares a typescript peer range whose upper bound is below 6.1, so 6.0.3
clears it, and no other package in the set declares a typescript peer at all. The install must
complete with zero unmet peer dependencies; if it does not, stop and report rather than forcing it
with a legacy-peer flag.

Delete every template artefact this project does not use: the scaffolded static HTML entry (the
shell is Jinja), the template App component, both template stylesheets, the template main entry, the
assets directory, and the public directory. `app/static/style.css` stays the single token source and
React components apply its existing class names, so no stylesheet may exist anywhere under
`frontend/`.

Write `frontend/vite.config.ts` with the React plugin, `base` set to the static bundle URL prefix
`/static/dist/`, `build.outDir` pointing at `../app/static/dist`, `build.emptyOutDir` enabled
(required because the output directory sits outside the Vite root), `build.manifest` enabled, and a
top-level `build.rollupOptions.input` object with exactly one named key `runs` pointing at
`src/entries/runs.tsx`. Use the named-object form rather than a bare array so Phase 23 and Phase 24
each add one key without reshaping the config. Create `frontend/src/entries/runs.tsx` as a minimal
placeholder module with a single side-effect-free export, purely so the build has an input; plan
22-04 replaces it with the real mounting entry.

Set `frontend/tsconfig.json` to strict mode with the automatic JSX runtime, bundler module
resolution, no emit, isolated modules, and `src` in `include`. Keep the template's
`tsconfig.node.json` for the config files.

Run the production build once, for real. Then inspect the produced tree and write
`frontend/MANIFEST-SHAPE.md` recording, from the actual output rather than from documentation: the
manifest file's path relative to the configured output directory; the exact top-level key under
which the `runs` entry appears; the full key set present on that chunk object; and whether a css
array is emitted for an entry that imports no stylesheet. Paste the real manifest JSON verbatim into
the file. This document is the input plan 22-04's loader is written against, and it closes
RESEARCH.md Open Question 1 and Assumption A3.

Add a `node_modules` entry to `.gitignore`. Add `frontend/node_modules` and `app/static/dist` to
`.dockerignore`, each with a comment naming the failure it prevents: a locally built bundle riding
into the build context through the builder stage's whole-tree copy would make a local image build
succeed while Render, building from the Git clone where the output directory is ignored, ships a
blank console with nothing failing anywhere. Do NOT un-ignore the output directory in `.gitignore`
— the bundle is built in the image and never committed (D-22-02).
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm ci &amp;&amp; npm run build &amp;&amp; node -e "const fs=require('fs'),p=require('path');const d=p.join('..','app','static','dist');const m=p.join(d,'.vite','manifest.json');const f=fs.existsSync(m)?m:p.join(d,'manifest.json');const j=JSON.parse(fs.readFileSync(f,'utf8'));if(!Object.keys(j).length)process.exit(1);console.log(f,JSON.stringify(Object.keys(j)))"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm ci` exits 0 and reports zero unmet peer dependencies.
    - `cd frontend && npm run build` exits 0 and creates a manifest file under `app/static/dist/`.
    - `frontend/MANIFEST-SHAPE.md` exists, contains the verbatim manifest JSON from the real build,
      and states the manifest path relative to the configured output directory plus the chunk key
      set.
    - `frontend/package.json` pins react and react-dom at 19.2.8, typescript at 6.0.3, vite at
      8.2.1, vitest at 4.1.10, typescript-eslint at 8.67.0, eslint-plugin-react-hooks at 7.1.1, and
      openapi-typescript at 7.13.0.
    - `node -e "const d={...require('./frontend/package.json').dependencies,...require('./frontend/package.json').devDependencies};for(const [k,v] of Object.entries(d)){if(v==='latest'||v==='*')throw new Error(k)}"` exits 0.
    - `frontend/package-lock.json` is committed and `git status --porcelain frontend/package-lock.json`
      is empty after `npm ci`.
    - `frontend/vite.config.ts` contains an `outDir` resolving to `../app/static/dist`, a `manifest`
      option, and a `rollupOptions.input` object whose only key is `runs`.
    - `find frontend -maxdepth 3 -name '*.css' -not -path '*/node_modules/*' | wc -l` reports 0.
    - `test ! -f frontend/index.html` exits 0.
    - `.gitignore` contains a `node_modules` entry; `.dockerignore` contains both
      `frontend/node_modules` and `app/static/dist`, each preceded by an explanatory comment.
    - `git status --porcelain app/static/dist` is empty after the build.
  </acceptance_criteria>
  <done>A pinned, lockfile-committed frontend toolchain builds a real bundle into the existing static
  mount, and the manifest's true on-disk shape is recorded before any code reads it.</done>
</task>

<task type="auto">
  <name>Task 3: ESLint flat config with the GUARD-06 bans, Vitest config, and the npm script surface</name>
  <files>frontend/eslint.config.js, frontend/vitest.config.ts, frontend/src/test/setup.ts, frontend/package.json</files>
  <read_first>
    - frontend/package.json (the scripts block scaffolded in Task 2)
    - .planning/phases/22-frontend-foundation-runs-list/22-RESEARCH.md (Code Examples: the
      package.json scripts block and the ESLint restricted-syntax block; the Security Domain table's
      raw-HTML row)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-17: GUARD-06 is enforced
      twice, ESLint plus a hermetic pytest guard, with the poller module as the one exception)
    - pyproject.toml (the ruff, mypy and pytest sections — the repo's one-config-file-per-tool
      convention the frontend mirrors)
  </read_first>
  <action>
Write `frontend/eslint.config.js` as a flat config composing typescript-eslint's recommended
type-aware rules with the React Hooks plugin's recommended set, and set the exhaustive-dependencies
rule to error. That rule is the entire reason ESLint was chosen over Biome and it is the guard on
the poller's effect dependencies.

Add these project rules over `src/**`:
- A restricted-globals rule banning the two browser network globals, with a message naming GUARD-06
  and pointing at the poller hook as the single sanctioned call site.
- A restricted-imports rule banning the third-party HTTP client package by name, same message.
- A restricted-syntax rule whose selector matches a JSX opening element named `form`, with a message
  naming D-22-12 and the two sanctioned form components.
- A rule banning the JSX attribute that injects raw markup, as a standing rule so Phase 23's
  conversation-thread surface inherits it.

Then add exactly two narrow overrides: one re-enabling the network globals for the poller hook
module path only, and one re-enabling the JSX form element for the two form component paths only.
Those three files do not exist yet; the overrides are path-scoped and inert until plans 22-08 and
22-10 create them.

Write `frontend/vitest.config.ts` with the jsdom test environment, globals disabled so the test API
is imported explicitly, `test.setupFiles` pointing at `src/test/setup.ts`, and `test.include`
covering the `.test.ts` and `.test.tsx` suffixes under `src`. Create `frontend/src/test/setup.ts`
importing the DOM matcher extensions.

Set the scripts block in `frontend/package.json` to: `dev` starting the Vite dev server, `build`
running the production build, `typecheck` running the compiler with no emit, `lint` running ESLint
over the project, `check` chaining typecheck then lint so either failure exits non-zero, and `test`
running Vitest a single time in run mode. The test script must not start a watcher — a watch-mode
script hangs both the CI job and the per-task verify command.

Prove the check command can fail. Introduce a deliberate type error in the placeholder entry module,
run the check command, capture the failing output verbatim, then revert and confirm
`git diff --stat frontend/src/entries/runs.tsx` is empty. Repeat for lint by adding a scratch module
under `src/` that calls a banned network global, capturing the ESLint error together with its rule
id, then deleting the scratch module and confirming `git status --porcelain frontend/src` is empty.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm run check &amp;&amp; npm run test &amp;&amp; node -e "const s=require('./package.json').scripts;for(const k of ['dev','build','typecheck','lint','check','test'])if(!s[k])throw new Error(k);if(/--watch|\bvitest\s*$/.test(s.test))throw new Error('test script must run once');console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npm run check` exits 0 on the committed tree.
    - `cd frontend && npm run test` exits 0 and terminates without waiting for input.
    - `frontend/package.json` scripts contains all six keys `dev`, `build`, `typecheck`, `lint`,
      `check`, `test`, and the `test` value invokes Vitest in run mode.
    - `frontend/eslint.config.js` contains a restricted-globals entry, a restricted-imports entry, a
      restricted-syntax selector matching a JSX `form` opening element, and a rule banning the
      raw-markup JSX attribute.
    - `frontend/eslint.config.js` contains exactly two override blocks, one scoped to
      `src/hooks/usePoller.ts` and one scoped to `src/components/MutationForm.tsx` and
      `src/components/ConfirmForm.tsx`.
    - `frontend/vitest.config.ts` sets the jsdom environment and registers `src/test/setup.ts`.
    - SUMMARY.md carries the verbatim failing output of the check command under the injected type
      error and under the injected lint violation (including the rule id), plus the
      `git diff --stat` / `git status --porcelain` lines proving both mutations were reverted with no
      residue.
  </acceptance_criteria>
  <done>One command typechecks and lints the frontend and is proven able to fail on either half; the
  network-call and form-element bans are configured with narrow, path-scoped exceptions; Vitest runs
  once and exits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build machine and CI | Every installed package executes with developer/CI privileges during install and build |
| build output → runtime image | `app/static/dist` is served to unauthenticated browsers through the existing `/static` mount |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-SC | Tampering | npm installs (13 packages) | high | mitigate | Package-legitimacy gate in Task 1 as a blocking human checkpoint over the eight SUS and four ASSUMED packages, plus exact version pins and a committed lockfile consumed with `npm ci` everywhere so a point release cannot silently substitute |
| T-22-09 | Tampering | `fetch`/`XMLHttpRequest`/axios reaching frontend source | high | mitigate | ESLint restricted-globals and restricted-imports over `src/**` with a single path-scoped override for the poller hook; plan 22-10 adds the independent pytest half so neither gate depends on the other |
| T-22-10 | Tampering (XSS) | raw-markup JSX attribute | high | mitigate | Lint-banned as a standing rule now, before Phase 23 introduces the conversation-thread surface that would tempt it |
| T-22-11 | Information Disclosure | `frontend/node_modules` or a local bundle entering the image | medium | mitigate | `.dockerignore` entries for `frontend/node_modules` and `app/static/dist`, each commented with the failure it prevents |
| T-22-12 | Denial of Service | a watch-mode test script hanging CI | low | mitigate | The `test` script runs Vitest once; the acceptance criterion asserts the script value is not a watcher |
</threat_model>

## Flagged planner assumptions (probe rows that stay unresolved)

- **SHELL-04 / unclassified** — "one command to start a local dev server with hot reload that proxies
  to uvicorn, and one command to typecheck and lint the frontend" has no data-shape edge to probe.
  This plan delivers the typecheck-and-lint half; the dev-server half is plan 22-09. The planner's
  assumption is that a test asserting a script key exists would assert the wrong thing, so SHELL-04's
  real proof is manual execution, matching `22-VALIDATION.md`'s Manual-Only Verifications table.
  Recorded, not resolved.

<verification>
- `cd frontend && npm ci && npm run check && npm run test && npm run build` exits 0 end to end.
- The committed `frontend/MANIFEST-SHAPE.md` matches the manifest produced by that build.
- `uv run pytest -q` (full Python suite) still passes — this plan adds no Python code.
- `uv run ruff check .` and `uv run mypy` exit 0 (neither tool has `frontend/` in scope; this
  confirms the scaffold did not disturb the Python side).
- `git status --porcelain` is clean apart from the intended new files; `app/static/dist` and
  `node_modules` are untracked and ignored.
</verification>

<success_criteria>
- The real manifest path and chunk key set are recorded in the repo before any loader is written.
- Every dependency is pinned and locked; the human confirmed the SUS/ASSUMED package set before
  install.
- One command typechecks and lints, and is demonstrated able to fail on either half.
- The network-call, form-element and raw-markup bans are configured with exactly two narrow
  exceptions.
- No stylesheet and no static HTML entry survives under `frontend/`.
</success_criteria>

<output>
Create `.planning/phases/22-frontend-foundation-runs-list/22-03-SUMMARY.md` when done.
</output>
