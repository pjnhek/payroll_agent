---
phase: 22-frontend-foundation-runs-list
plan: 04
type: execute
wave: 3
depends_on: ["22-01", "22-02", "22-03"]
files_modified:
  - app/schemas/__init__.py
  - app/schemas/_projection.py
  - app/schemas/runs_list.py
  - app/routes/templating.py
  - app/templates/react_page.html
  - app/templates/runs_list.html
  - app/routes/runs.py
  - frontend/src/entries/runs.tsx
  - frontend/src/boot/pageData.ts
  - frontend/src/pages/RunsPage.tsx
  - tests/fixtures/vite_manifest.json
  - tests/conftest.py
  - tests/test_react_page_render.py
  - tests/test_schema_projection.py
  - tests/test_dashboard.py
  - tests/assertion_inventory.py
  - tests/test_bundle_asset_exists.py
  - Dockerfile
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-05, SHELL-07]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An operator loading `/runs` receives the page's rows already present in the HTML response, inside a JSON data island, with no post-load network request required before payroll data is visible."
    - "When a value carried in the data island contains the literal characters that would close a script element, the emitted island still terminates only at its real closing tag and the parsed payload round-trips equal to the source model."
    - "With zero runs the data island still renders as a valid JSON object carrying an empty rows array — never an empty script body and never an omitted island."
    - "The data island preserves the repository's reverse-chronological row order; the serialized array order equals the row order returned by the list projection."
    - "Two sequential renders against different repository snapshots produce different island payloads, and the cached manifest is exposed as a read-only mapping, so no render can mutate another render's data."
    - "The server-rendered shell and the demo send-test form are siblings of the React mount point: removing the mount element leaves the form present and submittable, which is exactly what a JavaScript-disabled browser sees."
    - "With zero runs the JavaScript-disabled shell still renders the page heading, the operator-notice region, and the demo send-test form."
    - "The Jinja-owned chrome renders in the existing order: operator-notice include, page heading, React mount point, demo send-test form."
    - "GET /runs is side-effect free: two consecutive requests against an unchanged snapshot produce byte-identical shells."
    - { statement: "A demo send-test form POST submitted while a GET /runs render is in flight still redirects to the newly created run.", verification: backstop }
    - "A missing manifest raises rather than rendering a bundle-less shell, so no code path can emit markup that would make the phase's own assertions vacuous."
    - "The runs list response shape is an allowlist: any repository row key that is neither a declared field nor a named excluded field raises, and `/runs` and `/runs/{id}` carry separate declared shapes."
    - "The deployed image serves the same built assets a local image build serves: the build context cannot contain a locally built bundle, and a runtime stage missing the bundle fails the image build rather than shipping a blank console."
  artifacts:
    - app/schemas/_projection.py
    - app/schemas/runs_list.py
    - app/templates/react_page.html
    - frontend/src/pages/RunsPage.tsx
    - tests/fixtures/vite_manifest.json
    - tests/test_react_page_render.py
    - tests/test_bundle_asset_exists.py
  key_links:
    - "`app/routes/runs.py::runs_list` calls `render_react_page` with a `RunsListPage` model; the model is serialized by `json_script` into the data island read by `frontend/src/boot/pageData.ts`."
    - "Vite boot tags live only in `app/templates/react_page.html`'s content block, never hoisted into `base.html` — that placement is the reason `/ops` stays script-free."
    - "`RunListRow` is built from the list projection in `app/db/repo/demo.py::load_all_runs`, which carries `created_at`; `RUN_COLS` does not, which is why the two pages cannot share one shape."
    - "The runtime stage's copy of the bundle must land AFTER the existing whole-tree copy from the builder stage, which would otherwise clobber it."
  prohibitions:
    - statement: "MUST NOT embed in the page data island any field the rendered page does not display — specifically business_id, source_email_id, reply_epoch, alias_candidates, extracted_data, reconciliation, or decision — even though the existing denylist lets all seven through today."
      status: resolved
      verification: test
---

<objective>
The tracer. One production-quality path wired through every layer this phase touches: Vite build to
manifest to server-side page renderer to Jinja shell to embedded JSON island to a mounted React
component, and out through a Docker stage that proves the deployed bundle equals the locally built
one. Verified end to end before any expansion task widens it.

Purpose: SHELL-01, SHELL-02, SHELL-03, SHELL-05, SHELL-07. This is the tracer for the phase — the
two wave-1 plans ahead of it are gates the roadmap and CONTEXT.md sequence before any markup may
move (GUARD-01's inventory, and the design-token guard widening), not product-path work.
Output: `app/schemas/`, `render_react_page`, `react_page.html`, a React-rendered `/runs`, a fourth
Docker stage, and a real `docker build` proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md
@.planning/phases/22-frontend-foundation-runs-list/22-PATTERNS.md
@.planning/phases/22-frontend-foundation-runs-list/22-01-SUMMARY.md
@.planning/phases/22-frontend-foundation-runs-list/22-03-SUMMARY.md
</context>

## Artifacts this phase produces

New Python (app): `app/schemas/__init__.py`; `app/schemas/_projection.py` (`RowProjection`,
`UnclassifiedColumnError`); `app/schemas/runs_list.py` (`RunListRow`, `RunsListPage`);
`app/schemas/run_status.py` (`RunStatusPoll`); `app/routes/templating.py` gains
`render_react_page`, `json_script`, `load_manifest`, `ManifestMissingError`, `_JSON_SCRIPT_ESCAPES`;
`app/config.py` gains `Settings.vite_dev_server_url`.
New Python (scripts): `scripts/render_assertion_inventory.py`; `scripts/generate_openapi_doc.py`.
New Python (tests): `tests/assertion_inventory.py`; `tests/safety_mutation_registry.py`;
`tests/test_inventory_completeness.py`; `tests/test_safety_mutation_registry.py`;
`tests/test_route_shadowing.py`; `tests/test_no_html_on_service_routes.py`;
`tests/test_page_shell_pins.py`; `tests/test_react_page_render.py`;
`tests/test_bundle_asset_exists.py`; `tests/test_schema_projection.py`;
`tests/test_no_fetch_outside_poller.py`; `tests/test_react_dev_mode.py`;
`tests/test_generated_types_staleness.py`.
New templates/fixtures: `app/templates/react_page.html`; `tests/fixtures/vite_manifest.json`.
New frontend tree: `frontend/package.json`, `package-lock.json`, `vite.config.ts`, `tsconfig.json`,
`tsconfig.node.json`, `eslint.config.js`, `vitest.config.ts`, `MANIFEST-SHAPE.md`;
`frontend/src/entries/runs.tsx`; `frontend/src/boot/pageData.ts` (`readInitialData`,
`INITIAL_DATA_ELEMENT_ID`); `frontend/src/pages/RunsPage.tsx` (`RunsPage`) + `RunsPage.test.tsx`;
`frontend/src/components/StatusBadge.tsx`, `QueueBadge.tsx`, `FailureSummary.tsx`,
`MutationForm.tsx`, `ConfirmForm.tsx`, `DecisionBanner.tsx`, `MutationForm.test.tsx`;
`frontend/src/hooks/usePoller.ts` + `usePoller.test.ts`; `frontend/src/types/banner.ts`;
`frontend/src/generated/dtos.d.ts`; `frontend/src/test/setup.ts`.
New docs/CI/build: `docs/ASSERTION-INVENTORY.md`; `.github/workflows/ci.yml` jobs `frontend` and
`docker-build` plus steps `Assertion inventory completeness`, `Diff-scope fence`,
`Generated DTO staleness`; `Dockerfile` stage `frontend`; `.dockerignore` entries
`app/static/dist` and `frontend/node_modules`.
Modified existing (no new symbols): `app/routes/runs.py`, `app/templates/runs_list.html`,
`tests/test_design_tokens.py`, `tests/test_dashboard.py`, `README.md`, `.gitignore`.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end React-rendered /runs — one path through every layer</name>
  <precondition>`frontend/MANIFEST-SHAPE.md` exists and records the real manifest path and chunk key
  set from plan 22-03's build; the loader in this task must be written against that recorded shape,
  not an assumed one.</precondition>
  <reversibility rating="costly">The React-owns-data-regions / Jinja-owns-chrome-and-mutations split
  established here is the convention Phase 23 applies across fourteen forms and five confirm sites;
  changing it afterward means re-cutting both pages. Rating carried from D-22-12.</reversibility>
  <files>app/schemas/__init__.py, app/schemas/_projection.py, app/schemas/runs_list.py, app/routes/templating.py, app/templates/react_page.html, app/templates/runs_list.html, app/routes/runs.py, frontend/src/entries/runs.tsx, frontend/src/boot/pageData.ts, frontend/src/pages/RunsPage.tsx, tests/fixtures/vite_manifest.json, tests/conftest.py, tests/test_react_page_render.py, tests/test_schema_projection.py</files>
  <read_first>
    - frontend/MANIFEST-SHAPE.md (the real manifest path and chunk keys recorded by plan 22-03)
    - app/routes/templating.py (full file, 59 lines — the single `Jinja2Templates` instance at line
      10 and the two filter registrations at lines 58-59 this file is extended around)
    - app/templates/runs_list.html (full file — the poller script at 4-60, the notice include at 61,
      the heading at 62, the React-owned region at 64-115, the demo form at 117-128)
    - app/templates/base.html (full file, 21 lines, zero script elements — the placement rule)
    - app/routes/runs.py lines 200-260 (`_safe_run_for_browser`, the denylist that stays as an inner
      layer) and lines 850-918 (the `/runs` route and the status poll route)
    - app/db/repo/demo.py lines 210-262 (`load_all_runs` — the exact list projection column set,
      including `created_at`, `job_attempts`, `job_max_attempts`, `queue_label`)
    - app/db/repo/runs.py lines 35-45 (`RUN_COLS` — the 15 columns that do NOT include `created_at`)
    - tests/conftest.py (the `client` and `fake_repo` fixtures; note this repo's recorded hazard that
      fake-repo method names are registered in hard-coded monkeypatch tuples — no repo method is
      added here, but read the registration mechanism before touching the file)
    - tests/test_dashboard.py (the `/runs` presence assertions this change breaks)
    - docs/ASSERTION-INVENTORY.md (the committed inventory: the authoritative list of which
      assertions are attributed to `/runs`)
    - .planning/phases/22-frontend-foundation-runs-list/22-PATTERNS.md (sections for
      `app/schemas/_projection.py`, `app/schemas/runs_list.py`, `app/routes/templating.py`,
      `app/templates/react_page.html`)
  </read_first>
  <action>
Wire ONE path from the built bundle to the operator's browser, production-quality, with real error
handling on that path. Functionality gaps are acceptable here (badges and polling arrive in plans
22-06 and 22-10); architectural gaps are not.

Create the `app/schemas/` package as a sibling of `app/models/`, not under `app/db/` — placing it
under `app/db/` would breach the milestone's untouchable fence.

`app/schemas/_projection.py` declares `UnclassifiedColumnError` (a `RuntimeError` subclass) and
`RowProjection`, a Pydantic `BaseModel` configured with extra fields forbidden and frozen instances,
carrying a `ClassVar` frozenset named `EXCLUDED` defaulting to empty, and a `from_row` classmethod
that subtracts the declared field names and `EXCLUDED` from the incoming row's key set and raises
`UnclassifiedColumnError` naming the sorted unknown keys before validating the remainder. This is an
allowlist sitting as an outer layer above the existing denylist in `_safe_run_for_browser`; do not
delete or weaken that denylist.

`app/schemas/runs_list.py` declares `RunListRow(RowProjection)` with the fields the list page
actually renders — `id`, `created_at`, `created_at_display`, `business_name`, `status`,
`badge_class`, `badge_label`, `queue_label`, `queue_badge_class`, `has_open_job`, `failure`,
`summary_gate_reason`, `employee_count` — and an `EXCLUDED` frozenset naming every remaining key the
row carries, which
from the live list projection is `business_id`, `error_reason`, `error_detail`, `updated_at`,
`job_attempts`, and `job_max_attempts`. Re-derive that excluded set from the live projection at
implementation time rather than trusting this list. `badge_class` and `badge_label` are populated by
calling the existing `badge_class_filter` and `badge_label_filter`; never re-derive that vocabulary
in TypeScript, and leave the Jinja filter registrations in place for `/` and `/ops`.
`created_at_display` is the preformatted timestamp string the current template produces (the same
year-month-day hour-minute layout, with the em-dash placeholder when the value is absent), computed
server-side for the same reason the badge vocabulary is: a second formatter in TypeScript would
drift, and formatting a timestamp in the browser would silently shift it into the viewer's timezone.
Also declare `RunsListPage`, the page-level model carrying the rows sequence and the in-flight
status set.

Extend `app/routes/templating.py` in place — this repo has exactly one shared `templates` object and
every router imports it. Add: `ManifestMissingError`; a module-level manifest path constant pointing
at the real path recorded in `frontend/MANIFEST-SHAPE.md`; `load_manifest()` returning a read-only
mapping and raising `ManifestMissingError` when the file is absent, so a bundle-less production
deploy is a 500 rather than a silent blank console (the fail-closed convention shared with the pump
token and the bootstrap fence); `json_script(model)` returning Jinja `Markup` built from the model's
JSON dump with the three characters that can terminate a script element replaced by their unicode
escapes; and `render_react_page(request, *, entry, page_title, data, extra_context)` resolving the
entry's chunk from the manifest and returning a `TemplateResponse` over the new shell.
`load_manifest` should be cached, and the cache must be clearable so tests can swap the fixture
manifest — mirror the `get_settings` / `cache_clear` idiom already used in this repo.

Create `app/templates/react_page.html` extending `base.html`, with a title block bound to the page
title and a content block containing, in order: a Jinja-owned chrome block that the calling template
supplies, the mount element, the JSON data island script element carrying the escaped payload, the
stylesheet links from the chunk, and the module script tag pointing at the hashed entry file. Boot
tags go ONLY here — hoisting them into `base.html` is the DRY-looking move that immediately reds
`tests/test_ops_route.py`'s script-free pin and is the whole reason `/ops` stays safe.

Rewrite `app/templates/runs_list.html` per D-22-12: React owns only the former table region.
Delete the vanilla poller script block entirely (it selects DOM nodes React now owns, so leaving it
would be genuinely dead markup). Keep the operator-notice include, the heading, and the demo
send-test form exactly as they are, in that order, with the mount point between the heading and the
form. The `?notice=` channel stays entirely server-side and never round-trips through JSON.

Change `app/routes/runs.py::runs_list` to build a `RunsListPage` from the same
`_safe_run_for_browser`-reduced rows it loads today and render it through `render_react_page`. Keep
the existing try/except that renders an empty list when the database is unavailable — a cold Render
instance must still serve the shell. Do not touch any of the fourteen operator-facing mutation
handlers.

On the frontend: `frontend/src/boot/pageData.ts` exports `INITIAL_DATA_ELEMENT_ID` and
`readInitialData<T>()`, which locates the island element by id, parses its text content, and throws a
descriptive error if the element is missing or the payload does not parse — never returns a silent
empty object. `frontend/src/pages/RunsPage.tsx` exports `RunsPage`, rendering the scroll region with
its existing role, tabindex and aria-label, the table with the five headers in the existing order
(Created, Business, Status, Summary, Action), one row per run carrying the existing per-row data
attributes, and the existing empty-state markup when the rows array is empty. Reuse the class names
already in `app/static/style.css`; introduce no new class name and no stylesheet.
`frontend/src/entries/runs.tsx` replaces the placeholder: read the island, create a React root on the
mount element, render `RunsPage`.

Commit `tests/fixtures/vite_manifest.json`, a small fixture manifest matching the real recorded
shape, and add a fixture in `tests/conftest.py` that points the manifest path constant at it and
clears the loader cache, so the hermetic pytest suite renders the boot-tag path deterministically
with no Node in the `test` CI job (D-22-01).

Create `tests/test_react_page_render.py` asserting, against the real response text since `TestClient`
never executes JavaScript: the island element is present and its parsed payload equals the expected
model dump; the payload's row order equals the repository row order; a zero-run render still emits a
valid island with an empty rows array; a row whose `business_name` contains the characters that would
close a script element does not terminate the island early and round-trips equal; the rendered
markup contains the mount element, the heading, the notice region and the demo form in that order;
two consecutive renders of an unchanged snapshot are byte-identical; two renders over different
snapshots differ; the payload contains none of `business_id`, `source_email_id`, `reply_epoch`,
`alias_candidates`, `extracted_data`, `reconciliation` or `decision`; and removing the fixture
manifest makes the route raise `ManifestMissingError` rather than rendering a bundle-less shell.

Create `tests/test_schema_projection.py` asserting `RowProjection.from_row` raises
`UnclassifiedColumnError` naming the offending key when handed a row with an undeclared,
un-excluded column, and that a fully classified row validates. Plan 22-07 extends this module with
the `RUN_COLS` drift test.
  </action>
  <verify>
    <automated>uv run pytest tests/test_react_page_render.py tests/test_schema_projection.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_react_page_render.py tests/test_schema_projection.py -x -q` exits 0.
    - `GET /runs` returns 200 with a body containing an element whose id is the island id, whose
      parsed JSON has a `runs` array, and whose array length equals the number of rows the fake repo
      returned.
    - `GET /runs` with zero rows returns 200, the island parses to an object with an empty `runs`
      array, and the body still contains the demo form's `action="/demo/send-test"` attribute.
    - Rendering `/runs` with the manifest fixture removed raises `ManifestMissingError` (asserted with
      `pytest.raises`), and no code path returns 200 with an absent bundle.
    - `app/templates/base.html` still contains zero script elements, verified by
      `uv run pytest tests/test_ops_route.py::test_ops_page_has_no_script_or_polling -x` exiting 0.
    - `uv run python -c "import app.schemas.runs_list as m; assert 'business_id' in m.RunListRow.EXCLUDED; assert 'business_id' not in m.RunListRow.model_fields; print('ok')"` exits 0.
    - `cd frontend && npm run check && npm run build` exits 0 with the real entry in place.
    - `uv run ruff check .` and `uv run mypy` exit 0.
  </acceptance_criteria>
  <done>An operator loading `/runs` gets a React-rendered list whose data arrived inside the HTML
  response, the shell and the demo form still work with JavaScript off, a missing bundle fails
  closed, and the response shape is an allowlist.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the /runs-attributed assertions the conversion moved, and record replaced_by</name>
  <files>tests/test_dashboard.py, tests/assertion_inventory.py</files>
  <read_first>
    - docs/ASSERTION-INVENTORY.md (the committed inventory — the authoritative attribution of which
      assertions exercise `/runs` and which class each is)
    - tests/assertion_inventory.py (the registry, including the `replaced_by` field)
    - tests/test_dashboard.py (the assertions the inventory attributes to `/runs`)
    - app/templates/runs_list.html (the post-conversion shell, so a rewritten assertion targets what
      the server actually still renders)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-09: convert by default to
      positive exact-shape assertions; D-22-10: rewrite only the page this phase converts)
  </read_first>
  <action>
Work from the committed inventory, not from a search. For every registry entry whose `route` is
`/runs` and whose `layer` is the JSON island or the React DOM, rewrite the assertion so it cannot go
vacuous.

An assertion whose guarded content now travels inside the data island becomes a positive exact-shape
assertion against the parsed island payload — parse the island out of the response text once in a
helper, then assert on the parsed structure. A positive assertion cannot pass by rendering nothing,
which is the structural fix D-22-09 relies on.

An assertion whose guarded content now renders only in the browser DOM has no server-side surface at
all. Relocate its intent to the Vitest component suite (plan 22-06 owns `RunsPage.test.tsx`); in this
plan, delete the server-side assertion and set its registry entry's `replaced_by` to the planned
Vitest test id, so the trail Phase 24 reads records where the coverage went rather than showing a
silent drop.

Leave every assertion whose `layer` is the Jinja shell untouched — the notice region, the heading,
and the demo form are still server-rendered and those assertions stay meaningful for free. Leave
every `/runs/{run_id}` and `/eval` assertion untouched: Phase 23 and Phase 24 own those rewrites and
this phase must not pin against markup they will replace.

Update each affected `AssertionEntry` in `tests/assertion_inventory.py`: refresh `line`,
`col_offset` and `source_text` for rewritten assertions so the completeness guard's live-source
equality check passes, and fill `replaced_by` for deleted ones. Do not change any entry's `route` or
`assertion_class` — those were classified while the Jinja page still rendered and cannot honestly be
re-derived now.

Record in SUMMARY.md the count of entries rewritten, deleted-with-`replaced_by`, and left untouched,
plus the resulting per-file numbers from the regenerated view.
  </action>
  <verify>
    <automated>uv run pytest tests/test_dashboard.py tests/test_inventory_completeness.py -q &amp;&amp; uv run python scripts/render_assertion_inventory.py --check</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_dashboard.py -q` exits 0.
    - `uv run pytest tests/test_inventory_completeness.py -x -q` exits 0 (every rewritten assertion
      still resolves against live source and every deleted one is accounted for).
    - `uv run python scripts/render_assertion_inventory.py --check` exits 0 after regenerating the
      committed view.
    - Every registry entry whose `route` is `/runs` and whose `layer` is the React DOM has a
      non-null `replaced_by`, asserted by
      `uv run python -c "from tests.assertion_inventory import ASSERTION_INVENTORY as I; bad=[k for k,e in I.items() if e.route=='/runs' and e.layer.value=='react_dom' and e.replaced_by is None]; assert not bad, bad; print('ok')"`.
    - No registry entry's `route` or `assertion_class` value differs from the wave-1 commit, verified
      by `git diff` on `tests/assertion_inventory.py` showing changes only to `line`, `col_offset`,
      `source_text` and `replaced_by` fields; the diff is quoted in SUMMARY.md.
    - `uv run pytest -q` (full suite) exits 0.
  </acceptance_criteria>
  <done>Every `/runs` assertion the conversion moved is either a positive exact-shape assertion
  against the parsed island or is recorded as relocated, and the inventory guard still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Fourth Docker stage, build-time bundle assertion, and a real docker build proof</name>
  <precondition>The Docker daemon is running (`docker info` exits 0). Research recorded the daemon
  down on the authoring machine; without it this task's end-to-end proof cannot run and the executor
  must halt rather than rely on CI alone.</precondition>
  <files>Dockerfile, tests/test_bundle_asset_exists.py</files>
  <read_first>
    - Dockerfile (full file, 61 lines — the two-stage builder/runtime split, the whole-tree copy at
      line 38, the runtime stage's whole-tree copy from the builder at line 53, and the commented
      WORKDIR rationale block at 44-47 whose comment style the new assertion mirrors)
    - .dockerignore (the entries added by plan 22-03)
    - .gitignore (line 5 — the ignored output directory that makes this trap latent)
    - frontend/package.json and frontend/vite.config.ts (the build command and output directory)
    - .planning/phases/22-frontend-foundation-runs-list/22-CONTEXT.md (D-22-02, the three parts of
      the SHELL-05 proof)
  </read_first>
  <action>
Close the deploy trap that is latent today: the bundle output directory is gitignored, the build
context did not exclude it until plan 22-03, and the builder stage copies the whole working tree —
so a local image build succeeds from a tree that has the bundle on disk while Render, building from
the Git clone, ships a blank console with nothing failing anywhere.

Add a new stage to the `Dockerfile` named `frontend`, based on a pinned Node 24 slim image. Copy
only `frontend/package.json` and `frontend/package-lock.json` first and install with the
lockfile-asserting install command (never the resolving one — mirror the builder stage's
`uv sync --frozen` discipline and the CI comment explaining why a stale lockfile should fail the job
rather than merge green). Then copy the rest of `frontend/` and run the production build.

In the runtime stage, add a copy of the built bundle from the `frontend` stage into
`app/static/dist`. This copy MUST come after the existing whole-tree copy from the builder stage,
which would otherwise overwrite the directory wholesale. Immediately after it, add a build-time
existence assertion for the manifest file, with a comment naming the failure it prevents in the same
style as the existing WORKDIR rationale block: without it, a stage-ordering mistake ships a runtime
image whose static mount serves a 404 for the entry script and whose console renders an empty mount
element, and nothing anywhere fails.

Create `tests/test_bundle_asset_exists.py` as the hermetic half: assert that the manifest path
constant used by the template layer resolves under the directory served by the existing `/static`
mount in `app/main.py`, that the loader raises rather than degrading when the file is absent, and
that the committed fixture manifest's chunk objects carry the same key set the real build produced
(read from `frontend/MANIFEST-SHAPE.md`), so fixture drift is caught rather than silently masking a
loader change.

Prove it end to end. Run a real image build from a pristine copy of the tree that contains no local
bundle — export the tree with `git archive HEAD` into a temporary directory and build from there, so
the build sees exactly what Render's clone sees. Capture the successful build output. Then run the
falsifying build: temporarily remove the runtime stage's copy of the bundle, rebuild from the same
pristine export, and capture the RED output from the manifest assertion. Restore the Dockerfile and
confirm `git diff --stat Dockerfile` is empty. Before the mutation, confirm with `grep -n` that the
line removed is the live instruction and not the comment describing it.
  </action>
  <verify>
    <automated>uv run pytest tests/test_bundle_asset_exists.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_bundle_asset_exists.py -x -q` exits 0.
    - `Dockerfile` contains a stage named `frontend` based on a pinned `node:24-slim` tag, a
      lockfile-asserting install step, and a production build step.
    - The runtime stage's bundle copy appears at a later line number than the existing whole-tree
      copy from the builder stage, verified by `grep -n` output quoted in SUMMARY.md.
    - A real image build from a `git archive HEAD` export succeeds; the transcript is in SUMMARY.md.
    - The same build with the runtime stage's bundle copy removed FAILS at the manifest assertion;
      the RED transcript and the `grep -n` proof that the mutation hit the live instruction are in
      SUMMARY.md, and `git diff --stat Dockerfile` is empty afterwards.
    - `.dockerignore` still lists `app/static/dist`, so a locally built bundle cannot enter the build
      context and mask the failure.
  </acceptance_criteria>
  <done>The image builds the bundle itself, a runtime image without a bundle fails the build instead
  of shipping a blank console, and both the green and red paths were run against a pristine tree
  export rather than the working tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository row → browser | Every column the list projection selects is a candidate for the embedded island unless the allowlist stops it |
| client-supplied text → embedded JSON island | `business_name` originates in a business row and is rendered inside a script element |
| Git clone → Render image build | The deployed image is built from the clone, not from a developer's working tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-13 | Information Disclosure | `RunListRow` / the embedded island | high | mitigate | `RowProjection.from_row` allowlists by declared field and raises `UnclassifiedColumnError` on anything neither declared nor named in `EXCLUDED`; a dedicated test asserts the seven internal/PII keys that survive the existing denylist are absent from the payload |
| T-22-14 | Tampering (XSS) | `json_script` / the data island | high | mitigate | The three characters that can terminate a script element are replaced with unicode escapes before the payload reaches `Markup`; a test constructs a hostile `business_name` and asserts the island does not terminate early and round-trips equal |
| T-22-15 | Spoofing | a bundle-less production deploy | high | mitigate | `load_manifest` raises `ManifestMissingError` rather than rendering an empty shell, and the runtime stage asserts the manifest exists at image-build time, so a missing bundle is a build failure or a 500, never a silently blank console |
| T-22-16 | Tampering | Vite boot tags hoisted into the shared layout | high | mitigate | Boot tags live only in `react_page.html`'s content block; `tests/test_ops_route.py`'s script-free pin reds immediately if they move, and the acceptance criteria re-run it |
| T-22-17 | Denial of Service | a catch-all or second mount introduced while wiring the bundle | high | mitigate | The bundle is served through the existing `/static` mount; `tests/test_route_shadowing.py` from plan 22-02 asserts the sole `Mount` is `/static` and is demonstrated red under a root mount |
| T-22-18 | Information Disclosure | `frontend/node_modules` or a local bundle in the image | medium | accept | Mitigated in plan 22-03 by `.dockerignore` entries; re-verified here by building from a `git archive` export rather than the working tree |
</threat_model>

## Flagged planner assumptions (probe rows that stay unresolved)

- **SHELL-05 / unclassified** — "a deployed Render build serves the same built assets a local image
  build serves" has no data-shape edge to probe. The planner's assumption is that building from a
  `git archive HEAD` export is a faithful stand-in for Render's clone-based build, and that the
  image-build manifest assertion plus the CI image-build job (plan 22-05) together close the gap
  without an actual deploy. The genuine end-to-end confirmation is the phase-exit live check
  (`git rev-list --count origin/master..master` equal to 0, then load the deployed `/runs`), which
  this plan cannot perform. Recorded, not resolved.

<verification>
- `uv run pytest -q` (full suite) exits 0.
- `uv run pytest tests/test_react_page_render.py tests/test_schema_projection.py tests/test_bundle_asset_exists.py tests/test_inventory_completeness.py tests/test_ops_route.py tests/test_design_tokens.py -q` exits 0.
- `cd frontend && npm run check && npm run test && npm run build` exits 0.
- `uv run ruff check .` and `uv run mypy` exit 0.
- A real image build from a `git archive HEAD` export succeeds, and the same build with the runtime
  bundle copy removed fails at the manifest assertion.
- `git diff --name-only` for this plan touches no path under `app/pipeline/`, `app/queue/`,
  `app/db/`, `app/llm/`, or `app/email/`.
</verification>

<success_criteria>
- `/runs` is React-rendered from a bundle the image built, with its data already in the HTML response.
- A JavaScript-disabled operator still reads the shell and submits the demo send-test form.
- The response shape is an allowlist and the seven leaking internal/PII keys are provably absent.
- A missing bundle fails the image build or the render — never a blank console.
- Every `/runs` assertion the conversion moved is rewritten positively or recorded as relocated.
</success_criteria>

<output>
Create `.planning/phases/22-frontend-foundation-runs-list/22-04-SUMMARY.md` when done.
</output>
