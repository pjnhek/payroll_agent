---
phase: 19-webhook-cutover-durable-ingest
plan: 09
type: execute
wave: 5
depends_on: [19-07, 19-08]
files_modified:
  - app/db/repo/jobs.py
  - app/db/repo/demo.py
  - app/routes/runs.py
  - app/templates/runs_list.html
  - app/templates/run_detail.html
  - app/static/style.css
  - tests/test_dashboard.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "Runs list and detail show exactly one secondary open-job label without changing payroll status."
    - "Run detail shows the exact durability copy only while pending or leased work exists."
    - "Polling occurs every two seconds for at most 120 seconds and timeout never enqueues, retriggers, or mutates state."
    - "Superseded operator and demo enqueue notices are fixed, bounded, PII-safe, and subordinate to payroll state."
  artifacts:
    - path: "app/db/repo/jobs.py"
      provides: "bounded per-run open-job projection"
    - path: "app/templates/runs_list.html"
      provides: "secondary queue badge and 120-second in-place polling"
    - path: "app/templates/run_detail.html"
      provides: "secondary badge, durability note, bounded notice, and meaningful-change reload"
  key_links:
    - from: "app/routes/runs.py"
      to: "app/db/repo/jobs.py"
      via: "safe queue-label projection merged into browser-safe run copies and status JSON"
      pattern: "queue_label"
    - from: "app/templates/run_detail.html"
      to: "/runs/{run_id}/status"
      via: "2-second bounded polling while business or queue work remains open"
      pattern: "MAX_ATTEMPTS.*60"
---

<objective>
Expose the approved bounded queue-state feedback without creating a payroll status or pulling Phase 21 operations forward.

Purpose: Let an operator or recruiter see that work is durably owed while keeping business state authoritative and browser data PII-safe.
Output: Safe queue projections, exact badges/copy/notices, and bounded no-recovery polling.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-UI-SPEC.md
@app/db/repo/demo.py
@app/routes/runs.py
@app/templates/runs_list.html
@app/templates/run_detail.html
@app/static/style.css
</context>

## Artifacts this phase produces

- `repo.get_run_queue_label(run_id)` returning only `Running`, `Queued`, `Retry queued`, or `None` with fixed precedence.
- `load_all_runs()` bounded `queue_label` projection without job identifiers, attempts, timestamps, or diagnostics.
- `/runs/{run_id}/status` fields `queue_label`, `queue_badge_class`, and `has_open_job` only.
- Secondary queue badge and exact durability note with `aria-live="polite"` on list/detail.
- Fixed, allowlisted demo-failure and `resolution_superseded` notices.
- Shared 2-second/60-attempt polling behavior with no recovery side effects.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin bounded queue projection and hostile browser-data safety</name>
  <files>tests/test_dashboard.py</files>
  <read_first>
    - app/db/repo/jobs.py
    - app/db/repo/demo.py
    - app/routes/runs.py
    - app/templates/runs_list.html
    - app/templates/run_detail.html
    - app/static/style.css
    - tests/test_dashboard.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-UI-SPEC.md
  </read_first>
  <behavior>
    - Leased wins as Running; otherwise due pending is Queued; otherwise delayed pending is Retry queued; no open work is None.
    - Browser/status projections contain no job ID, attempts, raw timestamps, payload, or diagnostics.
    - Badge/copy appear only while open work exists and polling stops after 60 attempts without recovery calls.
    - Superseded and enqueue-failure flags select exact fixed copy and never echo query text.
  </behavior>
  <action>Create RED route/repository/template tests for D-08, D-09, D-14, D-15, D-16, D-17, and D-18 plus every UI-SPEC interaction/copy constraint. Include hostile job rows and query strings to prove allowlisting and autoescape. Statistically inspect the vanilla JS for 2000 ms cadence and 60 attempts, and use route spies to prove polling/status GETs never enqueue, retrigger, consume replies, or change payroll state. Assert primary payroll badge remains first and queue state is a distinct secondary field.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_dashboard.py -k "queued or running or retry or polling or durability or superseded or queue_error"</automated>
  </verify>
  <acceptance_criteria>
    - RED fails on absent queue projection, current 60-second cap, and missing exact copy.
    - All three labels and precedence have explicit cases.
    - Hostile data cannot reach HTML or JSON.
    - Timeout has fail-if-called recovery spies.
  </acceptance_criteria>
  <done>T19-09 and browser-safety behavior are specified before UI changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render secondary queue state and 120-second no-recovery polling</name>
  <files>app/db/repo/jobs.py, app/db/repo/demo.py, app/routes/runs.py, app/templates/runs_list.html, app/templates/run_detail.html, app/static/style.css, tests/test_dashboard.py</files>
  <read_first>
    - tests/test_dashboard.py
    - app/db/repo/jobs.py
    - app/db/repo/demo.py
    - app/routes/runs.py
    - app/routes/templating.py
    - app/templates/runs_list.html
    - app/templates/run_detail.html
    - app/static/style.css
  </read_first>
  <behavior>
    - One bounded projection drives list, detail, and status JSON consistently.
    - Queue badge/copy hide immediately after open work disappears; detail reloads once on meaningful business or queue-state transition.
    - List polling updates badges in place and preserves controls/scroll.
  </behavior>
  <action>Implement D-08, D-09, D-14, D-15, D-16, D-17, and D-18 exactly as approved. Add a parameterized repository projection that returns only the fixed queue label with precedence Running -> Queued -> Retry queued. Extend `load_all_runs`, detail, and status JSON through `_safe_run_for_browser`; strip all raw job fields. Render the queue badge beside the primary status and the exact sentence `This action is durably saved; you can safely leave this page.` only while open. Render the exact superseded and demo failure notices from fixed allowlisted flags. Update both pollers to 2000 ms and 60 attempts; continue while business state is in-flight or a queue label exists, update list in place, reload detail once on meaningful status/queue transition, and stop silently on timeout with no recovery CTA or call. Use existing CSS tokens, neutral styling for Queued/Retry queued, soft indigo only for Running, 12px semibold secondary labels, 8px badge gap, and `aria-live="polite"`. Add no new page, column, modal, metric, SPA, icon, dependency, or operations control.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_dashboard.py</automated>
  </verify>
  <acceptance_criteria>
    - T19-09 passes for exact labels/copy, 120-second cap, and no recovery.
    - Primary payroll status is unchanged and remains visually dominant.
    - JSON/HTML expose no job identifiers, attempts, dates, diagnostics, senders, or payload.
    - All approved accessibility and copy contracts pass.
  </acceptance_criteria>
  <done>Queue feedback is useful, bounded, accessible, and strictly secondary to payroll state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| jobs table to browser | Internal transport state is reduced to a fixed safe presentation. |
| query flags to Jinja | Browser-controlled query strings must not become rendered content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-27 | Information Disclosure | queue/status projection | high | mitigate | Task 1/2 use fixed labels only and strip IDs, attempts, timestamps, diagnostics, and payload. |
| T19-28 | Tampering | notice query flags | medium | mitigate | Task 1/2 map presence to fixed server copy and retain Jinja autoescape; query values are never rendered. |
| T19-29 | Denial of Service | polling | medium | mitigate | Task 2 caps at 60 two-second attempts and stops without auto-recovery. |
| T19-30 | Elevation of Privilege | queue state as payroll outcome | high | mitigate | Task 1/2 preserve separate fields and never add or infer a RunStatus from jobs. |
</threat_model>

<verification>
- `tests/test_dashboard.py` passes completely.
- `uv run ruff check app/db/repo/jobs.py app/db/repo/demo.py app/routes/runs.py tests/test_dashboard.py` passes.
- `uv run mypy app/db/repo/jobs.py app/db/repo/demo.py app/routes/runs.py` passes.
- Visual contract is testable from server output; no manual checkpoint is required for the approved CSS-token reuse.
</verification>

<success_criteria>
- D-08, D-09, D-14, D-15, D-16, D-17, and D-18 are exact.
- UI-SPEC copy, hierarchy, accessibility, and safety contracts pass.
- Phase 21 operations scope remains absent.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-09-SUMMARY.md` when done.
</output>
