---
phase: 19-webhook-cutover-durable-ingest
plan: 07
type: execute
wave: 4
depends_on: [19-04]
files_modified:
  - app/routes/demo.py
  - app/routes/dashboard.py
  - app/templates/dashboard.html
  - app/templates/runs_list.html
  - tests/test_demo_landing.py
  - tests/test_demo_fixtures.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "Both recruiter demo triggers commit email, run, and run_pipeline job atomically."
    - "Successful demo actions redirect directly to the new run detail page and wake only after commit."
    - "Demo enqueue failure rolls everything back and shows the exact bounded retry message on the owning surface."
  artifacts:
    - path: "app/routes/demo.py"
      provides: "two transactional durable demo producers"
    - path: "app/templates/dashboard.html"
      provides: "bounded composer enqueue failure presentation"
    - path: "app/templates/runs_list.html"
      provides: "bounded curated-fixture enqueue failure presentation"
  key_links:
    - from: "app/routes/demo.py"
      to: "app/db/repo/jobs.py"
      via: "email/run/job share one caller-owned transaction"
      pattern: "enqueue_job.*RUN_PIPELINE"
    - from: "app/routes/demo.py"
      to: "app/queue/wake.py"
      via: "wake only after successful transaction exit"
      pattern: "wake.wake"
---

<objective>
Replace both process-memory demo handoffs with atomic durable queue producers while preserving one-click recruiter navigation.

Purpose: Make the demo path exhibit the same durability guarantee as live webhook input.
Output: Two durable demo producers, direct detail redirects, and bounded failure feedback.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-UI-SPEC.md
@app/routes/demo.py
@app/routes/runs.py
@app/db/repo/jobs.py
</context>

## Artifacts this phase produces

- Transactional `demo_compose` and `demo_send_test` producers with `demo_run:{run_id}` dedup keys.
- Post-commit queue wake for both routes.
- Direct `/runs/{run_id}` redirect for both success paths.
- Bounded query/projection flag for exact copy `We couldn't queue this demo run. Please try again.` on the action-owning surface.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove demo atomicity, redirect, and bounded failure behavior</name>
  <files>tests/test_demo_landing.py, tests/test_demo_fixtures.py</files>
  <read_first>
    - app/routes/demo.py
    - app/routes/dashboard.py
    - app/templates/dashboard.html
    - app/templates/runs_list.html
    - tests/test_demo_landing.py
    - tests/test_demo_fixtures.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-UI-SPEC.md
  </read_first>
  <behavior>
    - Composer and curated fixture each commit one inbound email, one run, and one RUN_PIPELINE job, then redirect to that run detail.
    - Failure at any of the three writes leaves no email/run/job and wakes nothing.
    - Failure redirects to the originating surface and renders the exact bounded retry copy without exception text or identifiers.
  </behavior>
  <action>Write RED coverage for D-07, D-10, and D-21 across both demo routes. Use a transaction-recording fake that can fail each insert/enqueue boundary and assert rollback plus no wake. Assert both 303 Location values name the exact new run. Assert the two owning surfaces render the approved error copy and do not render exception strings, job IDs, message IDs, or submitted body text. Pin absence of `BackgroundTasks` route parameters and `.add_task()` in demo.py.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_demo_landing.py tests/test_demo_fixtures.py -k "demo or redirect or enqueue or rollback or queue"</automated>
  </verify>
  <acceptance_criteria>
    - RED fails on current split transactions, list redirect, and BackgroundTasks scheduling.
    - Every write-failure location is tested independently.
    - Error presentation is exact, bounded, and autoescaped.
  </acceptance_criteria>
  <done>Both demo paths have non-vacuous durability and UX contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit each demo run with its durable job and bounded notice</name>
  <files>app/routes/demo.py, app/routes/dashboard.py, app/templates/dashboard.html, app/templates/runs_list.html, tests/test_demo_landing.py, tests/test_demo_fixtures.py</files>
  <read_first>
    - tests/test_demo_landing.py
    - tests/test_demo_fixtures.py
    - app/routes/demo.py
    - app/routes/runs.py
    - app/db/repo/jobs.py
    - app/queue/wake.py
    - app/templates/dashboard.html
    - app/templates/runs_list.html
  </read_first>
  <behavior>
    - Parsing/cleaning may precede the transaction, but all database writes and enqueue are one atomic unit.
    - Wake occurs only after the transaction commits.
    - Success and failure redirects follow the UI contract exactly.
  </behavior>
  <action>Implement D-07, D-10, D-21, and the demo portion of D-23. Remove `BackgroundTasks` imports/parameters and both `.add_task()` calls. For each route, open one caller-owned connection/transaction around inbound insert, business/run creation, and `enqueue_job(JobKind.RUN_PIPELINE, dedup_key=f"demo_run:{run_id}", run_id=run_id, business_id=business_id, conn=conn)`. Treat an unexpected duplicate insert or enqueue as transaction failure. Wake only after commit, then redirect to `/runs/{run_id}`. On failure, log only the exception type at a PII-safe boundary, redirect with a fixed allowlisted flag, and render exactly the approved retry copy on the composer or runs surface. Preserve fixture allowlists, input caps, record-only behavior, and all deterministic money logic.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_demo_landing.py tests/test_demo_fixtures.py tests/test_dashboard.py -k "demo or queue or redirect"</automated>
  </verify>
  <acceptance_criteria>
    - Each successful click owes exactly one durable run job before redirect.
    - Rollback leaves no orphan email or run and does not wake.
    - Both success paths land on real run detail.
    - Failure messages contain no internal details or PII.
  </acceptance_criteria>
  <done>The recruiter demo is durable, one-click, and honest about bounded enqueue failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public demo form to payroll run | Allowlisted but user-controlled text reaches durable pipeline work. |
| transaction failure to browser notice | Internal failure information must not escape into recruiter-facing UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-18 | Tampering | demo transaction | high | mitigate | Task 1/2 commit email, run, and identifier-only job as one unit and retain existing allowlists/caps. |
| T19-19 | Denial of Service | duplicate or failed demo enqueue | medium | mitigate | Task 2 rolls back and provides one retry path without automatic re-enqueue. |
| T19-20 | Information Disclosure | demo failure presentation | medium | mitigate | Task 1/2 allow only fixed copy and fixed query flag; no exceptions, identifiers, or body text. |
| T19-21 | Repudiation | demo job ownership | low | accept | Stable run-scoped dedup and existing audit rows are adequate for a recruiter-only demo surface. |
</threat_model>

<verification>
- Demo-focused tests and dashboard copy tests pass.
- `uv run ruff check app/routes/demo.py app/routes/dashboard.py tests/test_demo_landing.py tests/test_demo_fixtures.py` passes.
- `uv run mypy app/routes/demo.py app/routes/dashboard.py` passes.
</verification>

<success_criteria>
- ROADMAP criterion 1 includes both demo producers.
- D-07 and D-10 are visible behavior, not only route internals.
- No process-memory demo producer remains.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-07-SUMMARY.md` when done.
</output>
