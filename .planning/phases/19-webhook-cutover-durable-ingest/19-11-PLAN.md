---
phase: 19-webhook-cutover-durable-ingest
plan: 11
type: execute
wave: 6
depends_on: [19-05, 19-06, 19-07, 19-08]
files_modified:
  - tests/test_retrigger_threading.py
  - tests/test_ingest.py
  - tests/test_concurrency_proof.py
  - tests/test_gateway.py
  - tests/test_stuck_run_recovery.py
  - tests/test_hitl.py
  - tests/test_webhook_dedup_race.py
  - tests/test_queue_drain.py
  - tests/test_send_idempotency.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "Every test that formerly called or monkeypatched a fire-and-forget pipeline wrapper proves the equivalent behavior at a durable queue handler, persisted-job, or explicit PipelineResult value seam."
    - "Request and read-only route tests fail if pipeline execution happens inline, while asserting the exact identifier-only job that is durably owed."
  artifacts:
    - path: "tests/test_queue_drain.py"
      provides: "durable handler/value/settlement coverage with no background-wrapper consumer inventory"
    - path: "tests/test_gateway.py"
      provides: "delayed-ingest reply classification and exact durable RESUME_REPLY versus RUN_PIPELINE assertions"
  key_links:
    - from: "tests/test_gateway.py"
      to: "app/ingest.py"
      via: "persisted inbound event processing creates the authorized identifier-only resume job"
      pattern: "process_inbound_event|RESUME_REPLY"
    - from: "tests/test_queue_drain.py"
      to: "app/queue/drain.py"
      via: "PipelineResult is consumed only through fenced durable settlement"
      pattern: "drain_once|settle_pipeline_job|settle_infrastructure_failure"
---

<objective>
Migrate every stale background-wrapper test consumer to the durable queue/value contract before the compatibility surface is deleted.

Purpose: Make D-23 executable without breaking the full suite or silently discarding the negative guarantees those tests previously carried.
Output: Nine explicitly migrated test modules ready for Plan 19-12's compatibility deletion and permanent guard.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-PATTERNS.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-05-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-06-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-07-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-08-SUMMARY.md
@app/routes/pipeline_glue.py
@app/queue/drain.py
</context>

## Artifacts this phase produces

- Execution-oriented legacy tests call `run_pipeline_now`, queue handlers, or `drain_once` according to who owns the result; none rely on a swallowing procedure.
- Request-path tests assert committed identifier-only jobs and use fail-if-called value/handler spies to prove no inline payroll execution.
- `tests/test_queue_drain.py` recognizes only the durable drain as a PipelineResult consumer and retains propagation, fencing, retry, and terminal proofs.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move execution-policy tests from swallowing wrappers to value and durable-settlement seams</name>
  <files>tests/test_retrigger_threading.py, tests/test_queue_drain.py, tests/test_send_idempotency.py</files>
  <read_first>
    - app/routes/pipeline_glue.py
    - app/queue/handlers/pipeline.py
    - app/queue/handlers/resume_reply.py
    - app/queue/drain.py
    - app/db/repo/job_settlement.py
    - tests/test_retrigger_threading.py
    - tests/test_queue_drain.py
    - tests/test_send_idempotency.py
  </read_first>
  <behavior>
    - Retrigger threading and send-idempotency tests invoke `run_pipeline_now` and assert the bounded PipelineResult plus the same delivered/error/awaiting-reply state they already protect.
    - Retry, terminal, and infrastructure behavior is tested through a claimed durable job, handler return, and fenced drain settlement rather than through first-attempt background compatibility policy.
    - The PipelineResult call-graph inventory has no retired route consumer; `drain_once` remains the sole result consumer and the explicit `*_now` functions remain value producers that propagate catastrophic start failures.
  </behavior>
  <action>Migrate the three direct execution consumers before production deletion. In `test_retrigger_threading.py`, replace the crash-pass call with `pipeline_glue.run_pipeline_now(run_id)`, assert its terminal bounded result, and preserve the post-send ERROR/thread-anchor assertions. In `test_send_idempotency.py`, use `run_pipeline_now` for all four clarification guard/non-vacuity cases so provider-call, ERROR, AWAITING_REPLY, and exact diagnostic assertions remain unchanged without adding queue settlement to a send-policy unit. In `test_queue_drain.py`, remove tests and comments whose subject is first-attempt wrapper retry/swallow policy; rewrite their meaningful retry, terminal, exact-email rehydration, propagation, and no-recursive-retry guarantees against `pipeline.handle_run_pipeline`, `resume_reply.handle_resume_reply`, `drain_once`, and the fenced settlement outcomes shipped in Plans 19-05/19-08. Update the exact producer/consumer AST sets so `run_pipeline_now`/`resume_pipeline_now` are producers and `drain_once` is the only result consumer. Preserve the negative guarantee by making queued-handler tests fail if a route compatibility seam or direct in-memory retry is invoked; do not weaken or delete the catastrophic-propagation, retry/backoff, terminal, stale-token, or no-PII assertions.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_retrigger_threading.py tests/test_queue_drain.py tests/test_send_idempotency.py</automated>
  </verify>
  <acceptance_criteria>
    - The three files contain no call or monkeypatch of a retired background-only symbol.
    - Send-idempotency non-vacuity still proves the provider fires exactly once when no guard row exists.
    - Queue tests still prove value propagation, durable retry/terminal settlement, reply rehydration, fencing, and no recursive in-memory retry.
    - The exact call-graph guard fails if a new PipelineResult sink is introduced.
  </acceptance_criteria>
  <done>Execution-policy coverage is bound to durable consumers and explicit values before wrapper deletion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move route and concurrency tests to committed-job and fail-if-inline seams</name>
  <files>tests/test_ingest.py, tests/test_concurrency_proof.py, tests/test_gateway.py, tests/test_stuck_run_recovery.py, tests/test_hitl.py</files>
  <read_first>
    - app/ingest.py
    - app/routes/webhook.py
    - app/routes/runs.py
    - app/db/repo/jobs.py
    - app/queue/handlers/ingest.py
    - tests/test_ingest.py
    - tests/test_concurrency_proof.py
    - tests/test_gateway.py
    - tests/test_stuck_run_recovery.py
    - tests/test_hitl.py
  </read_first>
  <behavior>
    - Duplicate/new inbound tests assert one durable transport event/job and, after delayed processing, one exact downstream job/run; they never infer execution from an in-memory callback count.
    - Authorized reply classification asserts one `RESUME_REPLY` job for the persisted email/run and zero `RUN_PIPELINE` jobs; sender/cross-run fail-if-called guards remain before conversion or orchestration.
    - Read-only runs list and concurrency proofs fail if enqueue, wake, drain, a queue handler, or an explicit pipeline value seam executes unexpectedly.
    - HITL retrigger tests prove epoch-specific durable jobs and drain behavior without patching a symbol the route no longer calls.
  </behavior>
  <action>Migrate all five route/concurrency consumers explicitly. Rewrite `test_ingest.py` and the affected `test_gateway.py` cases around the Phase 19 receipt plus delayed `process_inbound_event` contract: assert stable event identity, exact identifier-only INGEST/RUN_PIPELINE/RESUME_REPLY rows, one run, and separate Svix/RFC dedup; install fail-if-called `run_pipeline_now`, `resume_pipeline_now`, and queue-handler spies on the request boundary so a regression to inline execution is still caught. The authorized reply case must prove same-run and `reply_sender_ok` precede conversion and that only RESUME_REPLY is owed. In `test_concurrency_proof.py`, remove the obsolete wrapper stub and synchronous-BackgroundTasks narrative; isolate live providers by patching the explicit value/handler seam that could actually execute, while retaining the genuine OS-thread database race and exact row assertions. In `test_stuck_run_recovery.py`, replace the missing wrapper monkeypatch with fail-if-called durable mutation/execution seams (`enqueue_job`, wake/drain, resume handler/value entrypoint) so GET `/runs` remains side-effect-free. In `test_hitl.py`, remove the no-op wrapper patch and keep both job-generation assertions plus real `drain_once` execution. Do not delete coverage merely because the old symbol is gone.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_ingest.py tests/test_concurrency_proof.py tests/test_gateway.py tests/test_stuck_run_recovery.py tests/test_hitl.py</automated>
  </verify>
  <acceptance_criteria>
    - All five modules are free of retired-symbol references and stale BackgroundTasks assumptions.
    - Negative route guarantees now fail on inline value/handler execution, not on a deleted compatibility wrapper.
    - Reply authorization and two-layer dedup assertions remain explicit and non-vacuous.
    - Concurrency tests retain real-thread barriers and never call live LLM/Resend providers.
    - Retrigger generation/epoch coverage still drains the exact durable jobs it asserts.
  </acceptance_criteria>
  <done>Every route/concurrency consumer asserts the durable contract it actually owns.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate the existing RFC dedup race consumer before deletion</name>
  <files>tests/test_webhook_dedup_race.py</files>
  <read_first>
    - tests/test_webhook_dedup_race.py
    - app/routes/webhook.py
    - app/ingest.py
    - app/db/repo/jobs.py
    - app/queue/handlers/ingest.py
  </read_first>
  <behavior>
    - The existing guarded real-Postgres RFC Message-ID race reaches delayed ingest through committed event/job seams and contains no process-memory wrapper monkeypatch.
    - Two distinct transport events carrying one RFC Message-ID still produce exactly one email, one run, and one downstream RUN_PIPELINE job.
    - Request-boundary fail-if-called value/handler spies prove that neither concurrent webhook request executes payroll inline.
    - Plan 19-10 may later add the separate same-Svix event/job race only after this consumer migration and Plan 19-12 deletion are complete.
  </behavior>
  <action>Migrate the ninth active consumer before production deletion. Remove the obsolete background-wrapper monkeypatch and synchronous request-task narrative. Preserve the guarded real-Postgres OS-thread barrier, but post distinct transport events carrying the same RFC identity, assert their committed INGEST work, process that work through the durable service/handler seam, and prove exactly one persisted email/run plus one identifier-only downstream RUN_PIPELINE job. Add fail-if-called request-boundary value/handler spies so the negative no-inline-execution guarantee survives. Keep this task limited to migrating the existing RFC race; dependent Plan 19-10 exclusively owns the later same-Svix event/job concurrency extension and must not undo this wrapper-free shape.</action>
  <verify>
    <automated>! rg -n 'run_pipeline_bg|resume_pipeline_bg|operator_resume_bg|_consume_background_result|finish_reply_resume|BackgroundTasks' tests/test_webhook_dedup_race.py &amp;&amp; UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_webhook_dedup_race.py</automated>
  </verify>
  <acceptance_criteria>
    - The file contains no retired wrapper reference or stale synchronous-BackgroundTasks assumption before Plan 19-12 begins.
    - The existing RFC race remains a genuine OS-thread/Postgres proof and reports missing DATABASE_URL as unavailable evidence.
    - The test asserts committed identifier-only jobs and exact one-message/one-run behavior without live LLM or Resend calls.
    - Plan 19-10 changes this file only after Plan 19-12 and only for the separate same-Svix race extension.
  </acceptance_criteria>
  <done>The ninth stale consumer is durably migrated before compatibility deletion and safely sequenced for later extension.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy tests to durable execution contract | A stale monkeypatch can hide inline execution or make the suite fail only after symbol deletion. |
| request producer to queue consumer | A route must commit identifiers, never invoke a swallowing or value-producing pipeline seam inline. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-36 | Tampering | stale test consumers | high | mitigate | Tasks 1-2 give every named consumer explicit durable seam ownership and retain non-vacuous negative guarantees. |
| T19-39 | Elevation of Privilege | inline request execution | high | mitigate | Task 2 uses fail-if-called value/handler spies and asserts only committed identifier-only jobs at request boundaries. |
| T19-40 | Information Disclosure | migrated route tests | medium | mitigate | Tasks 1-2 retain bounded results and exclude provider bodies, sender data, mappings, diagnostics, and job IDs from browser responses. |
</threat_model>

<verification>
- Every plan frontmatter and task structure check passes.
- All nine migrated consumer modules pass before wrapper deletion.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync ruff check tests/test_retrigger_threading.py tests/test_ingest.py tests/test_concurrency_proof.py tests/test_gateway.py tests/test_stuck_run_recovery.py tests/test_hitl.py tests/test_webhook_dedup_race.py tests/test_queue_drain.py tests/test_send_idempotency.py` passes.
- `git diff --check` passes.
</verification>

<success_criteria>
- D-23 and QUEUE-04 have no stale test consumer blocking Plan 19-12's compatibility deletion; Plan 19-10 only extends an already-migrated race module after deletion.
- Every prior negative guarantee is rewritten against a live durable handler/value seam, not discarded.
- No new dependency, business payload, payroll status, Phase 20 send behavior, or Phase 21 operations surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-11-SUMMARY.md` when done.
</output>
