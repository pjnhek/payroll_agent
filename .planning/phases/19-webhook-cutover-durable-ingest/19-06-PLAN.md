---
phase: 19-webhook-cutover-durable-ingest
plan: 06
type: execute
wave: 5
depends_on: [19-04, 19-05]
files_modified:
  - app/routes/webhook.py
  - tests/test_durable_ingest.py
  - tests/test_webhook_unblocked.py
  - tests/test_webhook.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "The async webhook returns 200 only after one authenticated bounded event and one ingest job commit atomically."
    - "Synchronous psycopg receipt persistence runs through run_in_threadpool, so a slow database cannot block unrelated event-loop work."
    - "A repeated Svix event returns duplicate with the same event ID and creates no second job."
    - "The request path never fetches a body, routes a sender, creates a run, or returns run/job IDs."
  artifacts:
    - path: "app/routes/webhook.py"
      provides: "bounded verify-before-parse receipt route plus synchronous transaction helper"
    - path: "tests/test_webhook_unblocked.py"
      provides: "slow-database off-loop responsiveness proof preserving QUEUE-01"
    - path: "tests/test_durable_ingest.py"
      provides: "receipt atomicity, rollback, wake, and stable redelivery contract"
  key_links:
    - from: "app/routes/webhook.py::inbound"
      to: "app/routes/webhook.py::_persist_verified_receipt_sync"
      via: "await run_in_threadpool with the exact bounded authenticated bytes and transport identity"
      pattern: "await run_in_threadpool"
    - from: "app/routes/webhook.py::_persist_verified_receipt_sync"
      to: "app/db/repo/inbound_events.py"
      via: "event insert and INGEST enqueue in one caller-owned psycopg transaction"
      pattern: "insert_or_get_inbound_event|enqueue_job"
---

<objective>
Cut the webhook to a bounded authenticated event receipt while preserving the completed QUEUE-01 off-loop guarantee.

Purpose: Close the post-200 loss window without moving synchronous psycopg work back onto the async event loop.
Output: Off-loop receipt helper, atomic event/job acceptance, stable redelivery response, and a slow-database responsiveness proof.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-VALIDATION.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-UI-SPEC.md
@.planning/phases/16-queue-substrate-unblocked-webhook/16-01-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-04-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-05-SUMMARY.md
@app/routes/webhook.py
@app/db/repo/inbound_events.py
@app/db/repo/jobs.py
</context>

## Artifacts this phase produces

- `app/routes/webhook.py::_persist_verified_receipt_sync(...)`, a bounded synchronous helper owning the event-plus-job transaction.
- `app/routes/webhook.py::inbound`, retaining async stream/auth work and awaiting the helper through `run_in_threadpool`.
- Response `{status: accepted|duplicate, event_id: <stable internal UUID>}` only.
- 256 KiB streaming request cap and fixture-only `sha256:<hex>` external-key fallback.
- Slow/blocked repository seam proving unrelated event-loop work progresses while receipt persistence waits.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin atomic receipt behavior and slow-database event-loop responsiveness</name>
  <files>tests/test_durable_ingest.py, tests/test_webhook_unblocked.py, tests/test_webhook.py</files>
  <read_first>
    - app/routes/webhook.py
    - app/email/gateway.py
    - app/db/repo/inbound_events.py
    - app/db/repo/jobs.py
    - tests/test_webhook_unblocked.py
    - tests/test_webhook.py
    - .planning/phases/16-queue-substrate-unblocked-webhook/16-01-SUMMARY.md
  </read_first>
  <behavior>
    - Exact bytes are streamed with a 256 KiB cap and authenticated before JSON/minimal-envelope processing.
    - New event and INGEST job commit together before wake/200; transaction failure returns bounded 503 and commits neither.
    - Same Svix event returns duplicate plus the same event UUID and creates no second job.
    - While a synchronous repository transaction is deliberately blocked, an unrelated coroutine reaches its sentinel before the database seam is released.
    - Slow/raising provider body-fetch spies are never called by the request route.
  </behavior>
  <action>Create RED tests for D-01, D-02, D-03, D-04, D-05, D-06, D-21, D-23, QUEUE-04, and preserved QUEUE-01. In `tests/test_webhook_unblocked.py`, install a deterministic slow/blocked synchronous receipt-repository seam controlled by threading events, start the async webhook request, wait until persistence is blocked, and prove unrelated event-loop work advances before releasing the database seam; the test must fail if persistence executes directly in the async route. Pin the exact helper boundary by requiring the route to await `run_in_threadpool(...)`. In `tests/test_durable_ingest.py`, assert event/job co-commit, rollback/no-wake on enqueue or commit failure, commit-before-wake-before-200 ordering, stable duplicate receipt, and no run/job fields. Preserve signature-before-parse, 256 KiB cap, unsigned-fixture gating, and no request-path provider fetch/business processing.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py tests/test_webhook_unblocked.py tests/test_webhook.py -k "acceptance or rollback or wake or signature or cap or duplicate or off_loop or slow_database or unblocked"</automated>
  </verify>
  <acceptance_criteria>
    - RED fails because receipt persistence is not yet an awaited threadpool helper.
    - The slow-database test proves an unrelated coroutine progresses before persistence completes; it does not merely assert a helper was called.
    - Rollback produces neither event nor job and returns diagnostic-free 503.
    - Slow/raising body-fetch spies remain uncalled.
  </acceptance_criteria>
  <done>The request contract and QUEUE-01 preservation are executable before route cutover.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut the webhook to an off-loop authenticated durable receipt</name>
  <files>app/routes/webhook.py, tests/test_durable_ingest.py, tests/test_webhook_unblocked.py, tests/test_webhook.py</files>
  <read_first>
    - tests/test_durable_ingest.py
    - tests/test_webhook_unblocked.py
    - app/routes/webhook.py
    - app/email/gateway.py
    - app/db/repo/inbound_events.py
    - app/db/repo/jobs.py
    - app/queue/wake.py
  </read_first>
  <behavior>
    - Async route work is limited to bounded streaming, exact-byte authentication, and awaiting the off-loop receipt boundary.
    - The synchronous helper accepts the exact bounded authenticated bytes plus trusted transport identity, validates only the minimal fetch envelope, and owns the full event-plus-job transaction.
    - The helper returns only bounded accepted/duplicate receipt data after commit; wake happens only after successful commit.
  </behavior>
  <action>Implement D-01, D-02, D-03, D-04, D-05, and D-06 plus the webhook portion of D-21 and D-23 while explicitly preserving completed QUEUE-01. Keep `inbound` as `async def`; replace unbounded body access with the approved `request.stream()` accumulator and verify the exact bytes before parsing. After authentication, hand those exact bounded bytes and the trusted Svix identity to a synchronous `_persist_verified_receipt_sync(...)` boundary through `await run_in_threadpool(...)`. The helper must parse/validate only the minimal Resend fetch identifier, derive the signed external key (or fixture-only SHA-256 fallback), and own one caller-owned psycopg transaction that insert/gets the event and enqueues `INGEST` with `ingest:{event_id}` only when new. It returns bounded accepted/duplicate receipt data only after transaction exit; the async route wakes only after the committed inserted result and then returns the fixed receipt. Map persistence failure to diagnostic-free 503 and malformed authenticated envelope to the existing bounded client-error policy. Delete body fetch, DATA-02 execution, run response fields, `BackgroundTasks` parameter/import, and `.add_task()` producers. Never run psycopg repository work directly on the event loop.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py tests/test_webhook_unblocked.py tests/test_webhook.py</automated>
  </verify>
  <acceptance_criteria>
    - Success proves commit-before-wake-before-200 ordering.
    - Slow database persistence does not prevent unrelated event-loop progress.
    - Redelivery returns the same event ID and no run ID or job ID.
    - 256 KiB, signature-before-parse, fixture gating, and no-fetch guards remain effective.
    - Route source contains an awaited `run_in_threadpool` receipt call and no direct sync transaction.
  </acceptance_criteria>
  <done>An accepted webhook is durably owed work without regressing event-loop responsiveness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public webhook to authenticated bytes | Untrusted streamed bytes cross size and Svix verification controls. |
| authenticated route to synchronous Postgres | Verified metadata crosses into a blocking transaction that must remain off-loop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-W01 | Spoofing | `/webhook/inbound` | high | mitigate | Tasks 1/2 verify exact bounded bytes before parse or persistence; unsigned fixtures remain explicitly gated. |
| T19-W02 | Denial of Service | request body | high | mitigate | Tasks 1/2 enforce the 256 KiB streaming cap before allocation/persistence. |
| T19-W03 | Denial of Service | synchronous receipt database work | high | mitigate | Tasks 1/2 require awaited threadpool execution plus a behavior-level slow-database responsiveness proof. |
| T19-W04 | Tampering | event/job atomicity | high | mitigate | Tasks 1/2 commit event plus identifier-only job in one transaction before wake and response. |
| T19-W05 | Information Disclosure | response/error boundary | medium | mitigate | Tasks 1/2 return fixed status/event UUID only and bounded diagnostics-free errors. |
</threat_model>

<verification>
- Webhook receipt, auth, cap, dedup, rollback, and off-loop suites pass.
- `uv run ruff check app/routes/webhook.py tests/test_durable_ingest.py tests/test_webhook_unblocked.py tests/test_webhook.py` passes.
- `uv run mypy app/routes/webhook.py` passes.
- `git diff --check` passes.
</verification>

<success_criteria>
- ROADMAP criteria 2 and 3 are implemented at the request boundary.
- QUEUE-01 remains behaviorally proven after the transaction changes from business ingest to durable receipt.
- D-01 through D-06 and D-21 through D-23 are honored at the webhook seam.
- No request-path fetch, business processing, direct sync DB call, or process-memory producer remains.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-06-SUMMARY.md` when done.
</output>
