---
phase: 19-webhook-cutover-durable-ingest
plan: 10
type: execute
wave: 8
depends_on: [19-06, 19-07, 19-08, 19-09, 19-12]
files_modified:
  - app/db/repo/inbound_events.py
  - app/routes/pump.py
  - tests/test_durable_ingest.py
  - tests/test_webhook_dedup_race.py
  - tests/test_queue_durability.py
  - tests/test_pump_route.py
autonomous: false
requirements: [QUEUE-04]

must_haves:
  truths:
    - "The pump enforces 30-day terminal-only inbound-event retention without deleting open work or job history."
    - "A process can die immediately after webhook 200 and a later drain still completes the accepted event."
    - "Same-Svix redelivery yields one event, one ingest job, and one run across a real Postgres boundary when available."
    - "A persistent database fence blocks the Phase 18 /resolve writer before preflight and remains closed through additive schema, data migration, Phase 19 activation, and final postflight."
    - "Live deployment is blocked until legacy authority inventory is unambiguous, sole generations are migrated to one winner with remember=false, the exact Phase 19 revision is active, and authority/schema/fence assertions pass."
  artifacts:
    - path: "app/db/repo/inbound_events.py"
      provides: "bounded terminal-only retention executor"
    - path: "tests/test_durable_ingest.py"
      provides: "post-200 zero-worker then later-drain proof"
    - path: "tests/test_webhook_dedup_race.py"
      provides: "Plan 19-11's wrapper-free RFC race plus this plan's later real-Postgres same-Svix event/job extension"
    - path: "scripts/migrate_operator_resolution_authority.py"
      provides: "persistent writer-fence close/check/reopen protocol spanning the full live migration interval"
  key_links:
    - from: "app/routes/pump.py"
      to: "app/db/repo/inbound_events.py"
      via: "bounded maintenance call on existing authenticated 30-minute pump"
      pattern: "purge_terminal_inbound_events"
    - from: "tests/test_durable_ingest.py"
      to: "app/queue/drain.py"
      via: "webhook accepted with zero workers, then later drain executes owed ingest"
      pattern: "drain_once"
---

<objective>
Close Phase 19 with bounded retention, non-vacuous durability/cutover evidence, and a fail-closed live schema gate.

Purpose: Prove the headline post-200 guarantee at the strongest available boundary while refusing to overclaim unavailable live evidence.
Output: Pump-driven retention, guarded real-Postgres proofs, full cutover gates, and an explicit deployment checkpoint.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-VALIDATION.md
@.planning/phases/18-failure-policy-sweep-deletion/18-VERIFICATION.md
@app/routes/pump.py
@app/db/bootstrap.py
@app/db/check_schema.py
</context>

## Artifacts this phase produces

- `purge_terminal_inbound_events(older_than_days=30, batch_size=100)` with open-job exclusion and history-preserving FK behavior.
- Pump maintenance wiring using the existing authenticated 30-minute trigger and no new response/ops surface.
- Guarded post-200 process-death and same-Svix concurrency proofs.
- Complete producer inventory/full-suite closeout evidence.
- Recorded writer-fence close/check, exact aggregate preflight, non-destructive schema apply, authority migration, exact Phase 19 deployment activation, repeated postflight, and gated reopen evidence when credentials/authorization are available.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce terminal-only 30-day event retention through the pump</name>
  <files>app/db/repo/inbound_events.py, app/routes/pump.py, tests/test_durable_ingest.py, tests/test_pump_route.py</files>
  <read_first>
    - app/db/repo/inbound_events.py
    - app/routes/pump.py
    - app/db/repo/jobs.py
    - app/db/schema.sql
    - tests/test_durable_ingest.py
    - tests/test_pump_route.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
  </read_first>
  <behavior>
    - Only events older than 30 days whose ingest job is terminal are deleted, in bounded batches.
    - Pending/leased ingest events and all younger events remain; terminal jobs retain audit rows with event_id nulled by the FK.
    - Pump calls maintenance without adding queue metrics, job details, or manual controls.
  </behavior>
  <action>Create RED retention cases, then implement D-06's executable retention policy with parameterized SQL and a hard bounded batch. Exclude any event referenced by pending/leased ingest work, rely on the named `ON DELETE SET NULL` relationship to preserve terminal job audit, and return only a count to internal callers. Invoke it from the existing authenticated pump after bounded draining so the established 30-minute external trigger executes retention without a new scheduler. Keep pump response shape and Phase 21 operations surface unchanged; a maintenance DB failure follows the pump's existing bounded 503/error-type-only policy.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py tests/test_pump_route.py -k "retention or purge or terminal or open_job or pump"</automated>
  </verify>
  <acceptance_criteria>
    - Open work cannot lose its event envelope.
    - Old terminal event payloads are removed without deleting job audit.
    - Batch and age bounds are test-pinned.
    - No Phase 21 UI/API field is introduced.
  </acceptance_criteria>
  <done>The durable inbox is bounded by an actually invoked retention executor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove post-200 recovery, two-layer dedup, and complete cutover</name>
  <files>tests/test_durable_ingest.py, tests/test_webhook_dedup_race.py, tests/test_queue_durability.py</files>
  <read_first>
    - tests/test_durable_ingest.py
    - tests/test_webhook_dedup_race.py
    - tests/test_queue_durability.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-11-SUMMARY.md
    - .planning/phases/19-webhook-cutover-durable-ingest/19-12-SUMMARY.md
    - app/routes/webhook.py
    - app/queue/drain.py
    - app/db/repo/inbound_events.py
    - .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Webhook accepts with worker threads disabled; no pipeline work starts before response; later drain completes the owed event/run.
    - Concurrent same-Svix delivery leaves one inbound event and one ingest job; delayed RFC duplicate handling leaves one message/run.
    - Producer guard enumerates the historical eight seams and production contains no process-memory pipeline producer.
  </behavior>
  <action>Complete D-01, D-05, D-20, and D-23 plus ROADMAP criteria 1 through 4 and T19-01/T19-03/T19-08 evidence. Treat Plan 19-11's edit to `tests/test_webhook_dedup_race.py` as the completed prerequisite consumer migration: do not restore any retired wrapper reference or synchronous-BackgroundTasks assumption. Add a zero-worker acceptance test that asserts event/job existence immediately after 200, simulates process-boundary loss by discarding in-memory wake state, then drives the shared drain/pump and asserts the run outcome. Extend the already-migrated race module only with the distinct guarded real-Postgres same-Svix event/job race using a threading barrier and exact row counts; preserve its separate RFC Message-ID assertion after fetch. Consume Plan 19-12's complete producer/retired-symbol guard as an unchanged closeout gate rather than reopening its inventory ownership. Do not package final mutation/red-run CI evidence here; Phase 21 owns that proof artifact and workflow registration.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py tests/test_webhook_dedup_race.py tests/test_queue_durability.py tests/test_background_task_cutover.py -k "accepted or drain or svix or duplicate or producer or cutover"</automated>
  </verify>
  <acceptance_criteria>
    - Immediate post-200 evidence is the committed event/job, not an in-memory call record.
    - The later drain is the only action that creates/advances the run.
    - Svix and RFC dedup assertions are separate and both exact.
    - Guarded DB skips are reported unavailable, not as a pass.
    - This wave-8 extension contains no retired wrapper reference and does not duplicate or undo Plan 19-11's earlier consumer migration.
  </acceptance_criteria>
  <done>The phase goal and all four ROADMAP criteria have focused automated owners.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Fence the Phase 18 writer through verified Phase 19 activation</name>
  <files>app/db/schema.sql, app/db/schema_introspect.py, scripts/check_operator_resolution_inventory.py, scripts/migrate_operator_resolution_authority.py</files>
  <read_first>
    - scripts/check_operator_resolution_inventory.py
    - scripts/migrate_operator_resolution_authority.py
    - app/db/schema.sql
    - app/db/bootstrap.py
    - app/db/check_schema.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-VALIDATION.md
  </read_first>
  <action>Execute the D-11 through D-13 live cutover as one fail-closed ordered protocol only after every automated plan check is green and the exact Phase 19 git revision/deploy artifact is identified. Step 1, before accepting any inventory result, run `uv run python scripts/migrate_operator_resolution_authority.py --fence-writes`. Its ACCESS EXCLUSIVE lock must wait out every in-flight Phase 18 parent-generation insert, install/enable the persistent trigger, set the singleton closed, and commit. Immediately run `--check-fence` and require `writer_fence=closed`; from this committed point until reopen, every old or new `operator_resume_resolutions` INSERT must fail closed while migration UPDATEs remain permitted. Step 2, run the separate read-only `uv run python scripts/check_operator_resolution_inventory.py` and present exactly `unresolved_run_count`, `single_generation_run_count`, and `ambiguous_run_count`. If it exits nonzero or ambiguity is nonzero, stop with the fence closed; never infer authority from timestamp, UUID, sequence, or worker history. Step 3, after explicit mutation approval, run `uv run python -m app.db.bootstrap` without `--reset`, rerun `--check-fence`, then run `uv run python scripts/migrate_operator_resolution_authority.py`. The migration rechecks ambiguity in-transaction before its first authority write, marks each sole legacy generation authoritative, and preserves every override as `remember=false`. Step 4, while still fenced, require migration `--check` to report zero ambiguity, winnerless state, and supersession conflict with exactly one winner per affected unresolved run; require `uv run python -m app.db.check_schema` to report `schema check: in_sync`; and require `--check-fence` still closed. Step 5, deploy the identified Phase 19 revision to Render without opening the fence. Record the Render deployment ID plus git revision, require the deployment to be Live, require the previous Phase 18 instance/deploy to be stopped/replaced, and verify `/health/ready` plus `/health/schema` against that live revision. Step 6, before reopening, rerun the exact three-field read-only inventory, authority `--check`, schema check, and `--check-fence`; require zero unclassified/winnerless/ambiguous authority state and `writer_fence=closed`. Step 7 only, run `uv run python scripts/migrate_operator_resolution_authority.py --reopen-writes --deployed-revision <exact-revision>`; the command must repeat the authority/schema/fence checks in one invocation before setting the singleton open. Verify `--check-fence` reports `writer_fence=open` and the same revision. If any step fails, credentials disappear, the deploy rolls back, the revision cannot be proven active, or a prior instance remains, keep the fence closed and do not reopen or claim completion; rollback means resolution submissions remain intentionally unavailable while the app/deploy is repaired, never reopening the Phase 18 writer. Record only PII-safe aggregates, fence state, revision/deployment identifiers, health outcomes, commands, and evidence level.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_operator_resolution_inventory.py tests/test_operator_resolution_migration.py tests/test_schema_introspect.py tests/test_check_schema_cli.py</automated>
    <human-check>Confirm the fence closed before preflight, the exact three-field inventory is unambiguous, approve additive schema/migration, verify the exact Phase 19 revision is Live with the prior instance replaced, confirm repeated authority/schema/fence postflight, and approve reopen only after every condition passes.</human-check>
  </verify>
  <acceptance_criteria>
    - ACCESS EXCLUSIVE fence close waits out every in-flight old writer; the accepted preflight runs only after `writer_fence=closed` commits.
    - A Phase 18-shaped generation INSERT cannot commit at any point from accepted preflight through Phase 19 activation and repeated postflight.
    - Ambiguous multi-generation history exits nonzero before any authority mutation and leaves the fence closed.
    - The apply command never uses `--reset` and uses uv only.
    - Every sole legacy generation on an affected unresolved run is authoritative with `remember=false` after migration.
    - Authority postflight proves exactly one winner per affected unresolved run and zero ambiguity/winnerless/supersession conflict.
    - Schema and fence postflight prove exact live objects and a closed enabled trigger before deployment and again after the exact Phase 19 revision replaces Phase 18.
    - Reopen is impossible without the verified revision and repeated zero-unclassified authority/schema/fence checks.
    - Any failure or rollback keeps writes fenced; missing credentials/approval are pending evidence, not success.
  </acceptance_criteria>
  <what-built>Persistent old-writer fence, read-only aggregate authority preflight, additive Phase 19 schema, transactional sole-generation migration, exact Phase 19 activation proof, repeated authority/schema postflight, and gated reopen.</what-built>
  <how-to-verify>
    1. Confirm `--fence-writes` completed under the table lock and `--check-fence` reports `writer_fence=closed` before the inventory is accepted.
    2. Review the exact three aggregate inventory counts; they contain no UUIDs, names, mappings, exceptions, or arbitrary diagnostics, and `ambiguous_run_count` is zero.
    3. Approve additive bootstrap without `--reset` and the sole-generation migration; confirm remember=false and the in-transaction ambiguity recheck.
    4. Confirm authority `--check`, `schema check: in_sync`, and `writer_fence=closed` before deployment.
    5. Confirm Render reports the exact Phase 19 revision Live, the prior Phase 18 instance replaced, and both readiness/schema health succeed.
    6. Confirm the repeated inventory, authority check, schema check, and fence check remain clean after activation.
    7. Approve `--reopen-writes` only now; confirm the recorded revision matches and `--check-fence` reports open.
  </how-to-verify>
  <resume-signal>Type `approved` only after fence close, preflight, additive apply, migration, exact-revision activation, repeated postflight, and gated reopen are verified; otherwise describe the step that left the fence closed.</resume-signal>
  <done>Live deployment evidence is explicitly verified end to end, or operator-resolution writes remain explicitly fenced without unsafe inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated pump to retained PII | Scheduled maintenance deletes bounded event payloads without losing open work. |
| local plans to live Postgres | Deployment must not invent historical authority or claim unobserved schema state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-31 | Information Disclosure | retained inbound envelopes | high | mitigate | Task 1 executes 30-day terminal-only bounded purge from the authenticated pump. |
| T19-32 | Denial of Service | premature retention | high | mitigate | Task 1 excludes pending/leased references and preserves terminal job audit through SET NULL. |
| T19-33 | Repudiation | durability proof | medium | mitigate | Task 2 asserts committed rows immediately after 200 and later drain behavior; unavailable DB evidence is labeled honestly. |
| T19-34 | Tampering | live operator authority/schema | high | mitigate | Task 3 blocks before mutation on aggregate ambiguity, migrates sole generations transactionally with remember=false, and requires exact authority plus schema postflight. |
| T19-35 | Tampering | Phase 18 writer during deployment | high | mitigate | Task 3 closes a persistent trigger fence under ACCESS EXCLUSIVE lock before preflight, verifies it throughout deployment, and reopens only after exact Phase 19 activation plus repeated postflight. |
</threat_model>

<verification>
- Focused Phase 19 route/queue/schema/dashboard tests pass.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q` passes for executable tests; skips are listed.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync ruff check .` passes.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync mypy` passes.
- `git diff --check` passes.
</verification>

<success_criteria>
- The inbox is durably bounded and the post-200 recovery path is demonstrated.
- QUEUE-04 and every Phase 19 validation row have an executable owner.
- Live schema and writer-cutover evidence is fail-closed, spans the full preflight-to-activation interval, and is explicitly labeled.
- Phase 20 send idempotency and Phase 21 operations/proof packaging remain untouched.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-10-SUMMARY.md` when done.
</output>
