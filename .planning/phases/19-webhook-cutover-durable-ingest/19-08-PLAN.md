---
phase: 19-webhook-cutover-durable-ingest
plan: 08
type: execute
wave: 4
depends_on: [19-02, 19-04]
files_modified:
  - app/routes/runs.py
  - app/routes/pipeline_glue.py
  - app/queue/handlers/resume_reply.py
  - app/queue/handlers/operator_resume.py
  - tests/test_resume_pipeline.py
  - tests/test_reply_redelivery.py
  - tests/test_needs_operator.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "Reply classification and simulated replies commit the persisted reply context and RESUME_REPLY job atomically."
    - "Every durable reply attempt proves same-run ownership and sender authorization before conversion or orchestration."
    - "Every valid operator generation gets a generation-specific durable job, but only the first-commit winner can advance payroll or alias learning."
  artifacts:
    - path: "app/routes/runs.py"
      provides: "durable reply and operator producers plus bounded superseded notice flag"
    - path: "app/queue/handlers/operator_resume.py"
      provides: "winner-only authority consumer"
  key_links:
    - from: "app/routes/runs.py"
      to: "app/db/repo/operator_resume_resolutions.py"
      via: "generation commit/classification and enqueue share one transaction"
      pattern: "commit_operator_resume_resolution"
    - from: "app/queue/handlers/resume_reply.py"
      to: "app/routes/pipeline_glue.py"
      via: "same-run ownership and reply_sender_ok before row_to_inbound"
      pattern: "reply_sender_ok"
---

<objective>
Finish every runs/reply/operator producer cutover while preserving sender authorization and commit-selected operator authority.

Purpose: Complete QUEUE-04 without allowing sender revalidation or deterministic operator authority to regress during migration.
Output: Transactional reply/operator enqueues and winner-only durable handlers ready for Plan 19-11's compatibility-consumer migration and Plan 19-12's deletion gate.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-14-SUMMARY.md
@app/routes/runs.py
@app/routes/pipeline_glue.py
@app/queue/handlers/resume_reply.py
@app/queue/handlers/operator_resume.py
</context>

## Artifacts this phase produces

- Transactional reply-classification/simulated-reply enqueue using `resume_reply:{run_id}:{email_id}`.
- Transactional operator-generation enqueue using `operator_resume:{run_id}:{resolution_id}` for winner and superseded audit jobs.
- Winner-only handler preparation and alias-candidate projection.
- Bounded `resolution_superseded` redirect flag consumed by Plan 19-09.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make reply and simulated-reply continuations transactionally durable</name>
  <files>app/routes/runs.py, app/routes/pipeline_glue.py, app/queue/handlers/resume_reply.py, tests/test_resume_pipeline.py, tests/test_reply_redelivery.py</files>
  <read_first>
    - app/routes/runs.py
    - app/routes/pipeline_glue.py
    - app/queue/handlers/resume_reply.py
    - app/ingest.py
    - app/db/repo/emails.py
    - app/db/repo/jobs.py
    - tests/test_resume_pipeline.py
    - tests/test_reply_redelivery.py
  </read_first>
  <behavior>
    - Simulated and first-delivery valid replies persist/link email plus RESUME_REPLY job in one transaction, then wake after commit.
    - Duplicate valid replies ensure the same deduplicated job; unauthorized, cross-run, late, or advanced-state replies are OK/no-op.
    - Every attempt checks row.run_id equality and reply_sender_ok before row_to_inbound or orchestration.
  </behavior>
  <action>Implement D-20 through D-22 for remaining reply seams. Refactor pure reply classification/response shaping so routes can pass a caller-owned connection and atomically link/insert the persisted email plus enqueue `RESUME_REPLY`; wake after commit only. Update `simulate_reply` to use that same durable classification rather than `BackgroundTasks`. Keep handler authorization ordered as persisted row -> canonical same-run equality -> run load -> `reply_sender_ok` -> `row_to_inbound` -> orchestration. Branch on authoritative stored status/CAS, never job attempts alone. Unauthorized/cross-run/late/advanced input returns bounded OK/no-op with no sender or identifier content in logs.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_resume_pipeline.py tests/test_reply_redelivery.py tests/test_webhook.py -k "sender or unauthorized or binding or ownership or simulated or redelivery or enqueue"</automated>
  </verify>
  <acceptance_criteria>
    - T19-05 sender/ownership tests pass with fail-if-called downstream spies.
    - Reply state and owed job rollback together on enqueue failure.
    - Duplicate redelivery never creates a second resume job.
    - No reply route passes body text or mapping through jobs.
  </acceptance_criteria>
  <done>All reply continuations are durable and sender-authorized on every attempt.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enqueue every operator generation and apply only the winner</name>
  <files>app/routes/runs.py, app/queue/handlers/operator_resume.py, tests/test_needs_operator.py</files>
  <read_first>
    - app/db/repo/operator_resume_resolutions.py
    - app/routes/runs.py
    - app/queue/handlers/operator_resume.py
    - app/routes/pipeline_glue.py
    - tests/test_needs_operator.py
    - app/models/contracts.py
  </read_first>
  <behavior>
    - A complete valid submission commits its immutable generation, winner/superseded classification, and one generation-specific job atomically.
    - Superseded submitter redirects with a bounded flag; its handler returns OK before payroll or alias mutation.
    - Winner handler validates complete unresolved names and roster membership, projects only winner remember intent, then resumes from NEEDS_OPERATOR.
  </behavior>
  <action>Wire Plan 19-02's authority API into `/runs/{run_id}/resolve` per D-11 through D-14 and D-21/D-22. Keep form/roster validation before the transaction, then revalidate run/business/unresolved identity under the repository's run lock. Persist every valid generation and enqueue its generation-specific `OPERATOR_RESUME` job in that transaction; preserve the loser row/job and redirect it with only a fixed `resolution_superseded` flag. Do not project alias candidates in the route. Update `handle_operator_resume` to call winner preparation; superseded generations return explicit OK/no-op, while only the authoritative generation may apply `remember=true` candidates and invoke resume. Preserve deterministic Decision and one human approval gate.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_needs_operator.py tests/test_queue_durability.py -k "generation or authority or superseded or remember or operator_resume"</automated>
  </verify>
  <acceptance_criteria>
    - First commit remains authoritative even when loser handler runs first.
    - Losing generation and job remain auditable and no-op.
    - Only the winning remember intent reaches alias candidates.
    - Notice selection contains no submitted names, mappings, or employee IDs.
  </acceptance_criteria>
  <done>Queue scheduling cannot decide operator money-moving authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted reply to resume orchestration | Headers and sender fields are attacker-controlled until same-run and business checks pass. |
| operator form to queued authority | Competing submissions must not mutate payroll by worker order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-22 | Spoofing | RESUME_REPLY handler | high | mitigate | Task 1 requires same-run ownership and sender/business revalidation before conversion on every attempt. |
| T19-23 | Tampering | operator resume | high | mitigate | Task 2 consumes commit-selected authority and validates complete run-roster ownership; losers no-op. |
| T19-24 | Information Disclosure | redirect/log boundaries | high | mitigate | Tasks 1/2 use fixed flags and bounded codes, excluding senders, mappings, and identifiers. |
| T19-25 | Repudiation | superseded submissions | medium | mitigate | Task 2 retains immutable generation plus job history with explicit supersession. |
</threat_model>

<verification>
- Reply/operator/cutover suites pass.
- `uv run ruff check` and `uv run mypy` pass for modified production modules.
- Guarded authority DB proof is reported honestly when unavailable.
- `git diff --check` passes.
</verification>

<success_criteria>
- ROADMAP criteria 1 and 4 are met for every reply/operator seam, with compatibility/test-consumer migration owned by Plan 19-11 and final wrapper deletion owned by Plan 19-12.
- D-11 through D-14 and D-20 through D-23 are fully wired.
- The producer surfaces are ready for Plan 19-11's compatibility/test-consumer migration; Plan 19-12 owns production wrapper deletion and the permanent non-vacuous negative/positive architecture guard.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-08-SUMMARY.md` when done.
</output>
