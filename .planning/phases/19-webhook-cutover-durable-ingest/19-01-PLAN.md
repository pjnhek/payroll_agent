---
phase: 19-webhook-cutover-durable-ingest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/db/schema.sql
  - app/db/bootstrap.py
  - app/db/schema_introspect.py
  - scripts/check_operator_resolution_inventory.py
  - scripts/migrate_operator_resolution_authority.py
  - tests/test_schema_introspect.py
  - tests/test_operator_resolution_inventory.py
  - tests/test_operator_resolution_migration.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "The live-safe schema can store one bounded verified inbound envelope before any payroll run exists."
    - "Operator generations can record one authoritative winner, immutable superseded generations, and per-override remember intent without inferring historical commit order."
    - "A pre-deploy read-only inventory fails closed when existing unresolved runs contain ambiguous multiple generations."
    - "After an unambiguous preflight, every unresolved run with one legacy generation is migrated to exactly one authoritative winner with remember=false before code cutover."
    - "A persistent database writer fence can block every new operator-resolution generation before preflight and remain closed until the new Phase 19 writer is active."
  artifacts:
    - path: "app/db/schema.sql"
      provides: "inbound_events, jobs.event_id, operator authority/supersession/remember schema, indexes, and foreign keys"
    - path: "app/db/schema_introspect.py"
      provides: "exact catalog expectations for every Phase 19 schema object"
    - path: "scripts/check_operator_resolution_inventory.py"
      provides: "read-only PII-safe legacy-generation aggregate inventory and fail-closed exit contract"
    - path: "scripts/migrate_operator_resolution_authority.py"
      provides: "persistent old-writer fence controls, transactional single-generation authority migration, and exact postflight assertions"
  key_links:
    - from: "scripts/check_operator_resolution_inventory.py"
      to: "operator_resume_resolutions"
      via: "read-only grouped count restricted to unresolved operator-owned runs"
      pattern: "SELECT.*operator_resume_resolutions"
    - from: "app/db/schema_introspect.py"
      to: "app/db/schema.sql"
      via: "exact table, column, index, constraint, and foreign-key catalog tuples"
      pattern: "inbound_events|event_id|authoritative|remember"
    - from: "scripts/migrate_operator_resolution_authority.py"
      to: "operator_resume_resolutions"
      via: "transactional ambiguity recheck, sole-generation winner update, and postflight winner-count assertion"
      pattern: "authoritative|remember|FOR UPDATE"
---

<objective>
Create the additive database and deployment-safety foundation for durable webhook receipts and deterministic operator-generation authority.

Purpose: Make the irreversible schema boundary explicit and verifiable before any producer starts writing Phase 19 rows.
Output: Live-safe DDL, exact schema-health expectations, a read-only fail-closed inventory, and an explicit single-generation authority migration/postflight command.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-PATTERNS.md
@.planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-12-SUMMARY.md
</context>

## Artifacts this phase produces

- `inbound_events` with internal UUID `id`, unique external transport key, verified-envelope JSONB, and `received_at`.
- `jobs.event_id` with a named history-preserving `ON DELETE SET NULL` foreign key.
- Operator-generation authority/supersession fields, one-winner partial unique index, and per-override `remember` boolean.
- Exact schema-introspection tuples for the new table, columns, index, checks, and foreign keys.
- `scripts/check_operator_resolution_inventory.py::main`, a read-only inventory that prints exactly `unresolved_run_count`, `single_generation_run_count`, and `ambiguous_run_count`, then exits nonzero when ambiguity is nonzero.
- `scripts/migrate_operator_resolution_authority.py`, an explicitly mutating post-schema command that rechecks ambiguity inside its transaction, marks each sole legacy generation authoritative, preserves every override as `remember=false`, and supports an assertion-only postflight.
- A persistent singleton writer-fence row plus enabled parent-generation INSERT trigger, installable/closable before the authority preflight and reopenable only after verified new-code activation.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin Phase 19 catalog and inventory contracts before DDL</name>
  <files>tests/test_schema_introspect.py, tests/test_operator_resolution_inventory.py</files>
  <read_first>
    - app/db/schema.sql
    - app/db/schema_introspect.py
    - app/db/bootstrap.py
    - tests/test_schema_introspect.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
  </read_first>
  <behavior>
    - Missing or malformed `inbound_events`, `jobs.event_id`, retention index, authority index, authority/supersession fields, remember field, writer-fence table/trigger, or named foreign key is reported as schema drift.
    - An inventory with zero or one legacy generation per unresolved run exits successfully and emits exactly `unresolved_run_count`, `single_generation_run_count`, and `ambiguous_run_count` as integer aggregates.
    - An inventory with multiple legacy generations for one unresolved run exits nonzero without ordering by timestamps or mutating rows.
  </behavior>
  <action>Create RED tests for the exact fresh-schema and live-catalog shapes required by D-06 and D-11 through D-13, including the singleton writer-fence relation and enabled parent-generation INSERT trigger. Add a scripted fake-connection test for the read-only operator inventory: it must group by unresolved run, use no timestamp/UUID ordering as an authority proxy, issue no INSERT/UPDATE/DELETE, emit only the three exact aggregate fields `unresolved_run_count`, `single_generation_run_count`, and `ambiguous_run_count`, and fail closed with a nonzero exit whenever `ambiguous_run_count` is nonzero. Output must contain no run UUIDs, names, mappings, exception text, or arbitrary diagnostics. Keep these as Wave 0 contracts; do not change production code in this task.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_schema_introspect.py tests/test_operator_resolution_inventory.py</automated>
  </verify>
  <acceptance_criteria>
    - The tests fail specifically because Phase 19 catalog entries and inventory script do not exist yet.
    - The inventory test asserts statement read-onlyness, exact aggregate field names, and PII-free output, not merely exit status.
    - Every expected object is compared by exact owning table, columns, type, and reference target.
  </acceptance_criteria>
  <done>Wave 0 has executable RED coverage for schema drift and ambiguous legacy authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install additive receipt and authority schema with fail-closed inventory</name>
  <files>app/db/schema.sql, app/db/bootstrap.py, app/db/schema_introspect.py, scripts/check_operator_resolution_inventory.py</files>
  <read_first>
    - tests/test_schema_introspect.py
    - tests/test_operator_resolution_inventory.py
    - app/db/schema.sql
    - app/db/bootstrap.py
    - app/db/schema_introspect.py
    - app/db/repo/operator_resume_resolutions.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
  </read_first>
  <behavior>
    - Reapplying schema is idempotent and preserves existing jobs and operator generations.
    - Reapplying full additive schema cannot change a closed writer fence to open; singleton initialization is insert-if-absent only.
    - Event retention can later null only terminal ingest audit references; open work remains referentially intact.
    - Exactly one authoritative generation per run is representable; later complete generations remain immutable and can point to the winner.
  </behavior>
  <action>Implement D-06 and the schema half of D-11 through D-13. Add `inbound_events`, its unique external-key and received-at index, nullable `jobs.event_id`, and a named `ON DELETE SET NULL` foreign key in both fresh CREATE and additive live DDL. Extend operator generation/override tables with explicit authority, supersession, and `remember` storage plus an at-most-one-winner partial unique index. Define an idempotent singleton writer-fence table, fail-closed trigger function, and enabled BEFORE INSERT trigger on `operator_resume_resolutions`; when closed, the trigger rejects every new parent generation before child overrides can be written. Initialize the singleton only with INSERT-if-absent semantics: full bootstrap/schema reapply must never update or reopen an already-closed fence. Preserve the established reverse-dependency reset convention by placing `inbound_events` immediately after `jobs` in `_DROP_ORDER`, so the referencing `jobs` table is dropped before its referenced target; add ordering coverage. Extend schema introspection with exact catalog tuples for the fence as well as authority objects. Add the inventory CLI using the existing Settings/psycopg boundary; it must be read-only, print exactly the three PII-safe aggregate counts pinned in Task 1, return nonzero when `ambiguous_run_count` is nonzero, and never claim a live count during planning. Do not introduce Alembic, Supabase migrations, or any dependency.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_schema_introspect.py tests/test_operator_resolution_inventory.py tests/test_bootstrap_safe_url.py tests/test_repo_jobs_sql.py</automated>
  </verify>
  <acceptance_criteria>
    - Fresh and live-safe schema paths define the same Phase 19 objects.
    - The inventory script cannot write, cannot infer commit order from `created_at`, and fails closed on ambiguous history.
    - Reset-order coverage proves `jobs` precedes `inbound_events` in `_DROP_ORDER`.
    - Schema health rejects same-named malformed indexes or constraints.
    - Reapplying `app.db.bootstrap` while fenced leaves `writer_fence=closed` throughout.
    - No new package, migration framework, payroll status, or business payload column exists.
  </acceptance_criteria>
  <done>The schema is ready for code activation, and deployment has a non-destructive authority preflight.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the persistent old-writer fence, sole-generation migration, and postflight controls</name>
  <files>scripts/migrate_operator_resolution_authority.py, tests/test_operator_resolution_migration.py</files>
  <read_first>
    - scripts/check_operator_resolution_inventory.py
    - app/db/schema.sql
    - app/db/repo/operator_resume_resolutions.py
    - tests/test_operator_resolution_inventory.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
  </read_first>
  <behavior>
    - Fence close acquires an ACCESS EXCLUSIVE lock on the parent generation table, waits out every in-flight Phase 18 writer, installs/enables the trigger, and commits the closed singleton state before preflight can be accepted.
    - While closed, every new parent-generation INSERT fails before an unclassified generation can commit; migration updates remain possible.
    - The mutating command rechecks unresolved-generation cardinality inside one transaction and performs zero authority writes if any run has multiple generations.
    - For every unresolved run with exactly one legacy generation, that generation becomes the sole authoritative winner and every existing override remains `remember=false`.
    - Assertion-only postflight reports zero ambiguous runs, zero winnerless affected runs, and exactly one winner for every affected unresolved run without exposing identifiers or PII.
    - Fence reopen fails closed unless exact fence/schema/authority postflight inputs are supplied after verified Phase 19 deployment activation.
  </behavior>
  <action>Create RED tests, then implement the procedural resolution and full deployment fence for the unknown live state. Keep the default inventory command read-only and its exact three-field output unchanged. Add explicit `--fence-writes`, `--check-fence`, and `--reopen-writes` modes to the mutating script. Fence close must use one transaction with `LOCK TABLE operator_resume_resolutions IN ACCESS EXCLUSIVE MODE` so all earlier Phase 18 inserts either commit before the fence and become visible to the later preflight or roll back; then install/enable the trigger if needed, set the singleton closed, and commit before printing only `writer_fence=closed`. The trigger rejects every new parent INSERT while closed, independent of application version. Add a separately named authority migration mode that runs only after the additive schema exists and after the checkpoint accepts the post-fence read-only preflight. Inside its caller-owned transaction, lock/recount unresolved generations and abort before its first authority UPDATE if any run has more than one generation. When unambiguous, mark the sole legacy generation for each affected unresolved run `authoritative=true` with no supersession target and explicitly set all persisted overrides to `remember=false`; never order by timestamp, UUID, sequence, or worker history. Provide an assertion-only `--check` postflight for one winner per affected unresolved run and a catalog/state `--check-fence` that proves the trigger remains enabled and closed. Reopen must be a distinct explicit mode that refuses unless the caller supplies the verified deployed revision and both schema/authority postflight checks pass in the same invocation; it then locks the parent table, rechecks zero unclassified/winnerless/ambiguous rows, opens the singleton, and emits only `writer_fence=open` plus the approved revision. Tests must prove close waits out an in-flight old writer, no INSERT can commit while closed, and failure at any later deployment step leaves the fence closed.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_operator_resolution_inventory.py tests/test_operator_resolution_migration.py</automated>
  </verify>
  <acceptance_criteria>
    - Fence close serializes behind an in-flight old insert and the subsequent exact preflight sees that committed generation.
    - A Phase 18-shaped parent INSERT fails while closed and leaves no parent/override rows.
    - Ambiguous multi-generation state exits nonzero before any authority UPDATE is attempted.
    - Sole legacy generations become authoritative and retain `remember=false` deterministically.
    - Postflight detects both zero-winner and multiple-winner states and emits no row identifiers, names, mappings, or exception text.
    - Fresh databases and unresolved runs with zero generations remain unchanged and pass with zero affected rows.
    - Reopen without verified new-revision/schema/authority inputs exits nonzero and leaves `writer_fence=closed`.
  </acceptance_criteria>
  <done>The live-data procedure can fence every old writer, preserve one legacy authority per unresolved run, and reopen only for verified Phase 19 code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deploy operator to live Postgres | A schema application can irreversibly classify legacy money-moving authority. |
| application schema to catalog health | Same-named but malformed objects could falsely report a safe deployment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-01 | Tampering | operator authority migration | high | mitigate | Tasks 1-3 require read-only aggregate preflight, transactional ambiguity recheck, sole-generation migration, and exact postflight; no timestamp or UUID ordering is accepted. |
| T19-01A | Tampering | Phase 18 writer during cutover | high | mitigate | Task 3 installs and closes a database trigger under ACCESS EXCLUSIVE lock before preflight and keeps it closed through new-code activation. |
| T19-02 | Tampering | schema drift health | high | mitigate | Task 1/2 compare exact catalog tuples and cover fresh plus additive DDL. |
| T19-03 | Information Disclosure | inventory/migration output | medium | mitigate | Tasks 1-3 allow only named aggregate counts and forbid UUIDs, names, mappings, provider payloads, and DB errors. |
| T19-04 | Denial of Service | raw-event retention relationship | medium | mitigate | Task 2 installs a received-at index and SET NULL-compatible history design; bounded execution is wired in Plan 19-10. |
</threat_model>

<verification>
- `uv run ruff check` passes for the script, introspection module, and tests.
- `uv run mypy app/db/schema_introspect.py scripts/check_operator_resolution_inventory.py scripts/migrate_operator_resolution_authority.py` passes.
- `git diff --check` passes.
- No live database pass is claimed unless the executor actually has `DATABASE_URL`; the guarded deployment checkpoint is Plan 19-10.
</verification>

<success_criteria>
- All irreversible Phase 19 schema choices are additive, introspected, and test-pinned.
- Legacy multi-generation authority is never guessed, and a sole generation is explicitly migrated to one winner with `remember=false`.
- The old Phase 18 resolution writer has a persistent, test-pinned fail-closed fence spanning preflight through verified Phase 19 activation.
- D-06 and the storage prerequisites for D-11 through D-13 are executable without new dependencies.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-01-SUMMARY.md` when done.
</output>
