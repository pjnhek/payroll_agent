---
phase: 19-webhook-cutover-durable-ingest
plan: 12
type: execute
wave: 7
depends_on: [19-09, 19-11]
files_modified:
  - app/routes/pipeline_glue.py
  - app/routes/runs.py
  - app/queue/handlers/pipeline.py
  - tests/test_background_task_cutover.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "The compatibility wrappers are deleted only after all nine former test consumers pass against durable replacements."
    - "Production route/pipeline modules contain no process-memory payroll producer or background-only pipeline signature."
    - "A non-vacuous architecture guard rejects any reintroduced producer or retired wrapper definition/reference."
  artifacts:
    - path: "app/routes/pipeline_glue.py"
      provides: "explicit PipelineResult-producing and pure/durable reply seams only"
    - path: "tests/test_background_task_cutover.py"
      provides: "complete producer and retired-symbol AST inventory with synthetic falsification"
  key_links:
    - from: "tests/test_background_task_cutover.py"
      to: "app/routes/pipeline_glue.py"
      via: "explicit AST inventory rejects scheduling and retired compatibility definitions"
      pattern: "ast|pipeline_glue"
    - from: "app/queue/handlers/pipeline.py"
      to: "app/routes/pipeline_glue.py"
      via: "durable handler calls the explicit value seam and lets failures reach drain settlement"
      pattern: "run_pipeline_now"
---

<objective>
Delete the fully migrated background compatibility surface and make its absence permanent.

Purpose: Complete D-23 after Plan 19-11 proves that no test or behavior still depends on the retired symbols.
Output: Wrapper-free route/pipeline code and a complete mutation-proven cutover guard.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-PATTERNS.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-09-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-11-SUMMARY.md
@app/routes/pipeline_glue.py
@app/routes/runs.py
@app/queue/handlers/pipeline.py
</context>

## Artifacts this phase produces

- Deletion of `finish_reply_resume`, `_consume_background_result`, `resume_pipeline_bg`, `run_pipeline_bg`, and `operator_resume_bg` after every consumer migration is green.
- Removal of remaining pipeline-related `BackgroundTasks` imports/parameters, `.add_task()` scheduling, and obsolete background-only comments.
- `tests/test_background_task_cutover.py` scanning every historical producer/signature surface plus all nine former test consumers, with synthetic producer and retired-definition mutations.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the migrated compatibility procedures and stale scheduling signatures</name>
  <files>app/routes/pipeline_glue.py, app/routes/runs.py, app/queue/handlers/pipeline.py</files>
  <read_first>
    - .planning/phases/19-webhook-cutover-durable-ingest/19-09-SUMMARY.md
    - .planning/phases/19-webhook-cutover-durable-ingest/19-11-SUMMARY.md
    - app/routes/webhook.py
    - app/routes/demo.py
    - app/routes/runs.py
    - app/routes/pipeline_glue.py
    - app/queue/handlers/pipeline.py
    - app/queue/handlers/resume_reply.py
    - app/queue/handlers/operator_resume.py
    - tests/test_webhook_dedup_race.py
  </read_first>
  <behavior>
    - Only explicit PipelineResult value seams and pure/durable reply helpers remain in pipeline glue.
    - Queued handlers propagate escaping infrastructure failures to `drain_once`; no swallowing procedure can turn them into success.
    - Approval, durable retrigger, simulated reply, reply/operator handlers, and the read-only runs list preserve their post-cutover behavior.
  </behavior>
  <action>Complete the production half of D-23 after verifying Plan 19-11's nine migrated modules are green, including the now-wrapper-free existing RFC Message-ID race in `tests/test_webhook_dedup_race.py`. Delete `finish_reply_resume`, `_consume_background_result`, `resume_pipeline_bg`, `run_pipeline_bg`, and `operator_resume_bg` from `app/routes/pipeline_glue.py`; preserve `run_pipeline_now`, `resume_pipeline_now`, and any pure/durable reply classification helper still used by Plans 19-03/19-08. Remove the unused approval BackgroundTasks parameter/import and every residual pipeline-related background signature from `app/routes/runs.py`. Rewrite queue-handler and route comments so they describe explicit value propagation, identifier-only jobs, and fenced drain settlement without naming the retired execution path. Do not alter D-20 sender ordering, operator winner authority, durable retrigger, payroll state transitions, or delivery behavior.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_retrigger_threading.py tests/test_ingest.py tests/test_concurrency_proof.py tests/test_gateway.py tests/test_stuck_run_recovery.py tests/test_hitl.py tests/test_webhook_dedup_race.py tests/test_queue_drain.py tests/test_send_idempotency.py tests/test_resume_pipeline.py tests/test_needs_operator.py</automated>
  </verify>
  <acceptance_criteria>
    - All migrated consumers and reply/operator suites import and pass after production deletion.
    - No queued handler can consume a swallowed result or execute a recursive in-memory retry.
    - Sender revalidation and first-commit operator authority remain ordered before orchestration.
    - No new job payload, payroll status, dependency, or provider behavior is introduced.
  </acceptance_criteria>
  <done>The production compatibility surface is absent without breaking its durable replacements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the complete producer and retired-symbol architecture guard</name>
  <files>tests/test_background_task_cutover.py</files>
  <read_first>
    - app/routes/webhook.py
    - app/routes/demo.py
    - app/routes/runs.py
    - app/routes/pipeline_glue.py
    - tests/test_fake_repo_pairing.py
    - tests/test_retrigger_threading.py
    - tests/test_ingest.py
    - tests/test_concurrency_proof.py
    - tests/test_gateway.py
    - tests/test_stuck_run_recovery.py
    - tests/test_hitl.py
    - tests/test_webhook_dedup_race.py
    - tests/test_queue_drain.py
    - tests/test_send_idempotency.py
  </read_first>
  <behavior>
    - The guard inventories a nonempty explicit production and former-consumer surface.
    - Pipeline producer scheduling imports, annotations, calls, retired definitions, and stale references are rejected.
    - Synthetic producer and retired-definition mutations each make the guard fail.
  </behavior>
  <action>Create `tests/test_background_task_cutover.py` as the permanent D-23/T19-08 gate. Use AST plus an explicit nonempty inventory covering every historical route/signature producer, pipeline glue, and all nine Plan 19-11 consumer files, including `tests/test_webhook_dedup_race.py`. Reject FastAPI background scheduling imports/annotations, pipeline `.add_task()` calls, definitions of the five retired symbols, and references to those symbols. Add separate synthetic positive mutations proving the detector bites on a producer call and on a retired wrapper definition. Keep the scope limited to payroll pipeline producer surfaces so unrelated framework background facilities remain legal. Run the complete pytest suite after the focused guard because source deletion must not leave any unlisted runtime or collection-time consumer.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_background_task_cutover.py tests/test_webhook_dedup_race.py</automated>
  </verify>
  <acceptance_criteria>
    - Real inventory is nonempty, exhaustive for the historical eight producers and nine former consumers, and green.
    - Both synthetic regressions are detected non-vacuously.
    - The full suite is green; guarded Postgres skips are reported as unavailable evidence.
    - The guard does not ban unrelated framework features outside the named pipeline surfaces.
  </acceptance_criteria>
  <done>Process memory cannot return as a payroll execution guarantee without a deterministic test failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrated tests to production deletion | Removing symbols before every consumer migrates can break collection or erase behavior coverage. |
| request producer to queue consumer | A route must commit identifiers, never invoke pipeline execution in request-owned memory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-37 | Denial of Service | deletion sequencing | high | mitigate | Task 1 depends on Plan 19-11 and reruns all nine migrated consumers before/after deletion. |
| T19-38 | Repudiation | cutover architecture guard | high | mitigate | Task 2 uses a nonempty explicit inventory plus synthetic producer and retired-definition falsification. |
| T19-41 | Elevation of Privilege | request-owned payroll execution | high | mitigate | Tasks 1-2 remove and prohibit every background scheduling seam while preserving identifier-only queue producers. |
| T19-42 | Tampering | queued exception semantics | high | mitigate | Task 1 retains explicit value seams and tests escaping failures through fenced drain settlement. |
</threat_model>

<verification>
- `tests/test_background_task_cutover.py` and all nine migrated consumer modules pass after deletion.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q` passes; guarded Postgres skips are listed as unavailable evidence.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync ruff check app/routes/pipeline_glue.py app/routes/runs.py app/queue/handlers/pipeline.py tests/test_background_task_cutover.py` passes.
- `UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync mypy app/routes/pipeline_glue.py app/routes/runs.py app/queue/handlers/pipeline.py` passes.
- `git diff --check` passes.
</verification>

<success_criteria>
- D-23 and QUEUE-04 close without a stale test reference or compatibility wrapper.
- Wrapper deletion is ordered after every consumer migration and before Plan 19-10 closeout.
- The complete producer and retired-symbol inventory is non-vacuous and mutation-proven.
- No new dependency, business payload, payroll status, Phase 20 send behavior, or Phase 21 operations surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-12-SUMMARY.md` when done.
</output>
