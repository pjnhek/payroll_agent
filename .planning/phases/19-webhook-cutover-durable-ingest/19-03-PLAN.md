---
phase: 19-webhook-cutover-durable-ingest
plan: 03
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - app/ingest.py
  - app/db/repo/inbound_events.py
  - app/db/repo/__init__.py
  - tests/test_durable_ingest.py
  - tests/test_webhook.py
  - tests/test_reply_redelivery.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "Delayed ingest preserves the exact DATA-02 outcome ordering and atomically creates every downstream job it owes."
    - "Svix event identity and RFC Message-ID identity remain independent deduplication layers."
    - "First-delivery and redelivery clarification replies revalidate sender and same-run ownership before resume work is owed."
  artifacts:
    - path: "app/ingest.py"
      provides: "transport-event to five-outcome business-ingest service"
    - path: "app/db/repo/inbound_events.py"
      provides: "idempotent bounded event insert/load primitives"
    - path: "tests/test_durable_ingest.py"
      provides: "Wave 0 receipt/service/two-layer-dedup behavior contracts"
  key_links:
    - from: "app/ingest.py"
      to: "app/db/repo/jobs.py"
      via: "identifier-only downstream enqueue inside the DATA-02 caller-owned transaction"
      pattern: "enqueue_job.*conn"
    - from: "app/ingest.py"
      to: "email_messages.message_id"
      via: "existing insert_inbound_email duplicate outcome after delayed provider fetch"
      pattern: "insert_inbound_email"
---

<objective>
Extract the delayed, transactional business-ingest service and durable event repository before exposing the new webhook producer.

Purpose: Preserve DATA-02 and sender authorization as a testable correctness unit separate from HTTP receipt and queue settlement.
Output: `app/ingest.py`, inbound-event repository primitives, and focused RED/GREEN behavior coverage.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-PATTERNS.md
@app/routes/webhook.py
@app/routes/pipeline_glue.py
@app/db/repo/jobs.py
</context>

## Artifacts this phase produces

- `app/db/repo/inbound_events.py::insert_or_get_inbound_event` returning stable internal event ID plus inserted/duplicate state.
- `app/db/repo/inbound_events.py::load_inbound_event` returning only the verified bounded envelope needed by the delayed fetch.
- `app/ingest.py::process_inbound_event(event_id)` and bounded `IngestOutcome` values for duplicate, reply candidate, late reply, unknown sender, and new run.
- Stable downstream dedup keys for `run_pipeline` and `resume_reply` jobs based only on persisted identifiers.
- `tests/test_durable_ingest.py` covering delayed fetch, DATA-02 atomicity, both dedup layers, and sender guards.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze delayed-ingest and two-layer-dedup behavior</name>
  <files>tests/test_durable_ingest.py, tests/test_webhook.py, tests/test_reply_redelivery.py</files>
  <read_first>
    - app/routes/webhook.py
    - app/routes/pipeline_glue.py
    - app/db/repo/runs.py
    - app/db/repo/emails.py
    - tests/test_webhook.py
    - tests/test_reply_redelivery.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
  </read_first>
  <behavior>
    - Fetch/parse happens only when the service processes a persisted event, never during event insertion.
    - Each of duplicate, reply-candidate, late-reply, unknown-sender, and new-run commits atomically with its exact current ordering.
    - A repeated RFC Message-ID creates no second message/run and transactionally ensures the same owed reply job when a valid unconsumed reply remains.
    - Unauthorized or cross-run replies enqueue nothing and leave the run awaiting reply.
  </behavior>
  <action>Create the missing Wave 0 `tests/test_durable_ingest.py` and extend existing outcome/redelivery suites before production changes. Pin D-05, D-19, D-20, D-21, and D-22: event insertion is transport-only; delayed processing performs the provider fetch; the existing five outcomes retain their order; every owed `run_pipeline`/`resume_reply` job is identifier-only and transaction-co-resident; same-run ownership and `reply_sender_ok` precede orchestration. Include rollback cases proving no domain row commits without its downstream job. Keep Phase 21 CI/red-run packaging out of this task.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py tests/test_webhook.py tests/test_reply_redelivery.py -k "ingest or duplicate or redelivery or sender or rollback"</automated>
  </verify>
  <acceptance_criteria>
    - New tests fail on the missing service/repository boundary, not import or syntax errors.
    - A single-layer implementation cannot satisfy both Svix and RFC duplicate cases.
    - Sender-mismatch tests use fail-if-called spies for conversion, enqueue, and orchestration.
  </acceptance_criteria>
  <done>The moved transaction and security ordering are executable contracts before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the bounded inbound-event repository</name>
  <files>app/db/repo/inbound_events.py, app/db/repo/__init__.py, tests/test_durable_ingest.py</files>
  <read_first>
    - tests/test_durable_ingest.py
    - app/db/repo/runs.py
    - app/db/repo/jobs.py
    - app/db/repo/_shared.py
    - app/db/repo/__init__.py
    - app/db/schema.sql
  </read_first>
  <behavior>
    - Inserting a new external event key returns a new internal UUID and inserted=true.
    - Repeating the external key returns the same internal UUID and inserted=false.
    - Stored envelope size is already bounded by the route; repository reads never expose raw DB errors.
  </behavior>
  <action>Implement the repository half of D-04, D-05, D-06, and D-22 following caller-owned `_conn_ctx`/`_nulltx`, parameterized SQL, explicit columns, and exact duplicate-loser lookup. The transport key is the signed Svix ID; unsigned fixtures use only the stable SHA-256 key supplied by the route in Plan 19-06. Persist the verified minimal envelope JSON, not a fetched body, business payload, next status, or log projection. Export every new method through the facade. Do not enqueue `ingest` yet; Plan 19-04 activates the enum/model/SQL/dispatch/handler contract and Plan 19-05 makes every null-run settlement path safe before exposure.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py -k "event_repository or stable_event or conflict"</automated>
  </verify>
  <acceptance_criteria>
    - Duplicate transport delivery returns the same internal event UUID.
    - All SQL is parameterized and supports a caller-owned transaction.
    - The facade exports resolve without exposing provider bodies or diagnostics.
  </acceptance_criteria>
  <done>The route can atomically pair this repository insert with the ingest enqueue in Plan 19-06 after Plans 19-04 and 19-05 complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move DATA-02 intact into the delayed ingest service</name>
  <files>app/ingest.py, tests/test_durable_ingest.py, tests/test_webhook.py, tests/test_reply_redelivery.py</files>
  <read_first>
    - tests/test_durable_ingest.py
    - app/routes/webhook.py
    - app/routes/pipeline_glue.py
    - app/db/repo/inbound_events.py
    - app/db/repo/runs.py
    - app/db/repo/jobs.py
    - app/pipeline/result.py
  </read_first>
  <behavior>
    - The service loads one event, performs the delayed gateway parse/fetch, cleans once, and enters one DATA-02 transaction.
    - The five outcomes and their ordering match the pre-cutover webhook behavior.
    - Valid new-run/reply work and its downstream job commit together; duplicate/late/unknown outcomes never create a second run.
  </behavior>
  <action>Move, do not redesign, `_parse_and_ingest_sync` into `app/ingest.py` per D-19. Load the exact event envelope by internal UUID, call the gateway only there, clean fetched content once, and preserve duplicate -> reply candidate -> late reply -> unknown sender -> new run ordering. Within the same transaction, enqueue `RUN_PIPELINE` for a new run or `RESUME_REPLY` for an authorized linked reply using stable identifier-only dedup keys. For RFC duplicates, rehydrate the persisted email/run and transactionally ensure only the same owed resume job when unconsumed, same-run, awaiting-reply, and sender-authorized. Return bounded `PipelineResult`/outcome metadata and log no payload, sender address, or DB/provider exception text. The HTTP route remains uncut until Plan 19-04 installs the real `INGEST` kind and Plan 19-05 completes null-run settlement.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_durable_ingest.py tests/test_webhook.py tests/test_reply_redelivery.py</automated>
  </verify>
  <acceptance_criteria>
    - DATA-02 outcome tests remain behaviorally identical.
    - Downstream enqueue failure rolls back email/run/reply linkage.
    - Both first delivery and duplicate redelivery apply same-run and sender authorization before enqueue.
    - Jobs carry event/run/email identifiers only.
  </acceptance_criteria>
  <done>The worker-facing ingest service is complete but not yet exposed to production enqueue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted transport envelope to provider fetch | Authenticated metadata triggers external retrieval of sensitive email content. |
| fetched email to payroll state | Untrusted sender/body/header values cross into money-moving workflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-09 | Spoofing | clarification reply sender | high | mitigate | Task 1/3 require exact same-run binding and sender/business revalidation before any resume enqueue. |
| T19-10 | Tampering | DATA-02 transaction ordering | high | mitigate | Task 1/3 preserve the five-outcome unit and co-tenant downstream enqueue. |
| T19-11 | Information Disclosure | fetched message and diagnostics | high | mitigate | Task 2/3 keep bodies out of jobs/logs/results and persist only existing bounded domain records. |
| T19-12 | Repudiation | two-layer replay | medium | mitigate | Task 1/3 preserve unique Svix receipt and RFC Message-ID audit independently. |
</threat_model>

<verification>
- Focused event/service/outcome tests pass.
- `uv run ruff check app/ingest.py app/db/repo/inbound_events.py tests/test_durable_ingest.py` passes.
- `uv run mypy app/ingest.py app/db/repo/inbound_events.py` passes.
- No route producer or JobKind is widened in this plan.
</verification>

<success_criteria>
- D-05 and D-19 through D-22 are implemented at the moved transaction boundary.
- The service is safe to register only after null-run settlement lands.
- No new dependency or deferred Phase 20/21 surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-03-SUMMARY.md` when done.
</output>
