---
phase: 19-webhook-cutover-durable-ingest
plan: 04
type: execute
wave: 3
depends_on: [19-01, 19-03]
files_modified:
  - app/db/schema.sql
  - app/models/job.py
  - app/db/repo/jobs.py
  - app/queue/dispatch.py
  - app/queue/handlers/ingest.py
  - tests/test_job_kind_drift.py
  - tests/test_repo_jobs_sql.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "INGEST is activated as one exact enum, SQL, model, enqueue, claim, dispatch, and handler contract."
    - "Open ingest jobs carry event_id only, while existing run-associated kinds retain their exact contexts."
    - "No HTTP request can enqueue INGEST until the separate null-run settlement plan is complete."
  artifacts:
    - path: "app/queue/handlers/ingest.py"
      provides: "registered identifier-only ingest consumer over the delayed service"
    - path: "app/models/job.py"
      provides: "four-kind transport vocabulary and exact event identifier model"
    - path: "tests/test_job_kind_drift.py"
      provides: "exact enum, SQL, and dispatch parity"
  key_links:
    - from: "app/queue/dispatch.py"
      to: "app/queue/handlers/ingest.py"
      via: "late-bound module/function entry set-equal to JobKind"
      pattern: "JobKind.INGEST"
    - from: "app/db/repo/jobs.py"
      to: "app/models/job.py"
      via: "event_id enqueue validation and claim RETURNING bijection"
      pattern: "event_id"
---

<objective>
Activate the exact identifier-only INGEST queue vocabulary, persistence, claim, dispatch, and handler contract without exposing an HTTP producer.

Purpose: Keep the queue contract internally coherent while leaving null-run settlement and fake parity to a bounded dependent plan.
Output: Exact four-kind queue vocabulary, event identifier model/SQL, delayed ingest handler, and drift/claim proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-VALIDATION.md
@.planning/phases/18-failure-policy-sweep-deletion/18-09-SUMMARY.md
@app/models/job.py
@app/db/repo/jobs.py
@app/queue/dispatch.py
</context>

## Artifacts this phase produces

- `JobKind.INGEST` and `Job.event_id` in exact claim-column order.
- Fresh/live SQL `ingest` kind and exact identifier-context check.
- `app/queue/handlers/ingest.py::handle_ingest` and late-bound dispatch registration.
- Exact enum/SQL/dispatch equality plus enqueue-context and claim-bijection tests.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin exact ingest vocabulary, context, claim, and dispatch contracts</name>
  <files>tests/test_job_kind_drift.py, tests/test_repo_jobs_sql.py</files>
  <read_first>
    - app/models/job.py
    - app/db/repo/jobs.py
    - app/db/schema.sql
    - app/queue/dispatch.py
    - tests/test_job_kind_drift.py
    - tests/test_repo_jobs_sql.py
  </read_first>
  <behavior>
    - Python enum, SQL kind check, and handler map are exactly equal at four kinds.
    - Ingest requires only event_id and forbids run/email/operator identifiers while open.
    - Claim RETURNING maps every persisted identifier exactly once into Job.
    - Dispatch resolves the ingest handler through the established late-bound module/name seam.
  </behavior>
  <action>Create RED Wave 0 contract tests for D-22 before widening production. Require exact set equality, the new `event_id` claim/dataclass bijection, parameterized enqueue, exact ingest context checks, and module-object dispatch. Add malformed-context and synthetic drift cases so subset-only, handler-only, or partially widened implementations cannot pass. Preserve J-1: no payload, next status, or payroll vocabulary enters Job. Do not modify settlement, reaper, fakes, or HTTP producers in this plan.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_job_kind_drift.py tests/test_repo_jobs_sql.py -k "ingest or kind or context or dispatch or claim"</automated>
  </verify>
  <acceptance_criteria>
    - RED fails on absent INGEST/event_id contracts.
    - Tests use exact set and column-bijection assertions, never subset checks.
    - Context tests reject every extra business identifier and any payload/next-status field.
  </acceptance_criteria>
  <done>The full vocabulary/model/SQL/dispatch surface has executable RED contracts before activation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activate the identifier-only ingest kind and delayed handler</name>
  <files>app/db/schema.sql, app/models/job.py, app/db/repo/jobs.py, app/queue/dispatch.py, app/queue/handlers/ingest.py, tests/test_job_kind_drift.py, tests/test_repo_jobs_sql.py</files>
  <read_first>
    - tests/test_job_kind_drift.py
    - tests/test_repo_jobs_sql.py
    - app/ingest.py
    - app/models/job.py
    - app/db/repo/jobs.py
    - app/db/schema.sql
    - app/queue/dispatch.py
  </read_first>
  <behavior>
    - INGEST dispatches by event_id to `process_inbound_event` and forwards its bounded PipelineResult.
    - Open ingest jobs carry event_id only; terminal audit rows may later have event_id nulled by retention.
    - Existing three run-associated kinds retain their exact identifier contexts.
  </behavior>
  <action>Widen the queue vocabulary atomically per D-22: add `INGEST`, `event_id`, exact fresh/live SQL kind and context checks, enqueue validation, claim mapping, and late-bound handler registration. Implement `handle_ingest(job)` as a fail-closed identifier validator over Plan 19-03's `process_inbound_event`; it forwards only the bounded PipelineResult and never fetches by run ID or writes payroll status directly. Preserve exact handler/enum/SQL equality, parameterized SQL, identifier-only jobs, stable `ingest:{event_id}` dedup, and existing three-kind behavior. Do not touch settlement/reaper/fakes or any public route; Plan 19-05 makes transport outcomes safe, and Plan 19-06 is the only webhook producer cutover.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_job_kind_drift.py tests/test_repo_jobs_sql.py</automated>
  </verify>
  <acceptance_criteria>
    - Four-kind equality and claim bijection pass.
    - Ingest cannot carry run/email/operator identifiers, payload, or next status while open.
    - Handler resolution stays late-bound for monkeypatch seams.
    - No route can enqueue INGEST as part of this plan.
  </acceptance_criteria>
  <done>The new kind is structurally real and dispatchable, but remains unreachable from HTTP pending Plan 19-05 and Plan 19-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| queue lease to delayed ingest service | A transport-only identifier triggers authenticated provider retrieval and business ingest. |
| SQL queue row to Python dispatch | Vocabulary or column drift could invoke the wrong handler or rehydrate wrong context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-13 | Tampering | queue vocabulary/context | high | mitigate | Tasks 1/2 enforce exact enum, SQL, model, claim, dispatch, and identifier-context equality. |
| T19-14 | Elevation of Privilege | ingest job context | high | mitigate | Tasks 1/2 permit only event_id for open INGEST work and reject business payload/identifiers. |
| T19-15 | Information Disclosure | job boundary | medium | mitigate | Tasks 1/2 keep envelope/body/sender/mappings out of jobs and handler results. |
</threat_model>

<verification>
- Task-focused vocabulary, SQL, claim, and dispatch tests pass.
- `uv run ruff check` passes for all modified production/tests.
- `uv run mypy app/models/job.py app/db/repo/jobs.py app/queue/handlers/ingest.py` passes.
- `git diff --check` passes.
</verification>

<success_criteria>
- INGEST is an exact extension of the Phase 18 queue vocabulary and identifier model.
- D-22 and T19-10 have direct automated owners without mixing in settlement/fake work.
- No HTTP producer, payroll mutation, or Phase 20/21 surface is introduced.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-04-SUMMARY.md` when done.
</output>
