---
phase: 19-webhook-cutover-durable-ingest
plan: 05
type: execute
wave: 4
depends_on: [19-04]
files_modified:
  - app/db/repo/job_settlement.py
  - tests/conftest.py
  - tests/test_queue_drain.py
  - tests/test_queue_durability.py
  - tests/test_fake_repo_pairing.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "Null-run ingest success, retry, dead-letter, terminal, fencing, and final-lease expiry settle without writing payroll status."
    - "An expired final-attempt ingest lease cannot remain fenced forever."
    - "Every new settlement/facade seam resolves through the active fake and retains the established lease fence."
  artifacts:
    - path: "app/db/repo/job_settlement.py"
      provides: "kind-aware transport-only ingest settlement and final-attempt reaping"
    - path: "tests/conftest.py"
      provides: "fake queue and repository parity for the new transport paths"
    - path: "tests/test_queue_durability.py"
      provides: "hermetic and guarded null-run settlement/reaper matrix"
  key_links:
    - from: "app/db/repo/job_settlement.py"
      to: "app/models/job.py"
      via: "kind-aware INGEST branch before any run requirement"
      pattern: "JobKind.INGEST"
    - from: "tests/conftest.py"
      to: "app/db/repo"
      via: "explicit fake patch inventory paired with every public facade seam"
      pattern: "monkeypatch"
---

<objective>
Make every null-run INGEST result settle, retry, dead-letter, and reap safely before any webhook can enqueue the new kind.

Purpose: Complete the transport failure-policy and fake-parity half of INGEST without reopening the queue vocabulary plan's scope.
Output: Kind-aware null-run settlement/reaping plus exact fake repository parity and non-vacuous proofs.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-VALIDATION.md
@.planning/phases/18-failure-policy-sweep-deletion/18-04-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-13-SUMMARY.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-04-SUMMARY.md
@app/db/repo/job_settlement.py
</context>

## Artifacts this phase produces

- Kind-aware transport settlement for null-run ingest OK/retry/dead/terminal/final-lease paths.
- Exact lease-token, attempt-cap, backoff, and stale-worker fencing preservation.
- Fake queue/facade implementations and patch inventories for all new public seams.
- Guarded Postgres and hermetic proofs that no ingest result writes `payroll_runs.status`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin null-run settlement, reaper, and fake-pairing behavior</name>
  <files>tests/test_queue_drain.py, tests/test_queue_durability.py, tests/test_fake_repo_pairing.py</files>
  <read_first>
    - app/db/repo/job_settlement.py
    - app/models/job.py
    - tests/conftest.py
    - tests/test_queue_drain.py
    - tests/test_queue_durability.py
    - tests/test_fake_repo_pairing.py
  </read_first>
  <behavior>
    - Null-run ingest OK, retry, exhaustion, terminal, stale-fence, and expired-final-attempt paths never invoke a payroll status writer.
    - Expired final-attempt ingest work becomes dead and clears its lease under the existing fence.
    - Existing run-associated settlement matrices remain byte-for-byte behaviorally unchanged.
    - Fake facade pairing fails if any new public repository seam reaches the real database.
  </behavior>
  <action>Create RED Wave 0 contracts for D-21/D-22 and T19-04 before production settlement changes. Add hermetic and guarded-Postgres cases for OK, retry, exhaustion, terminal, stale fence, and final expired lease with `run_id=None`; use fail-if-called payroll writers. Pin lease-token matching, attempt cap, backoff, bounded diagnostic class/code, and lease clearing. Add nonempty fake inventory and a synthetic failure proving every new queue/repository export resolves to the active fake. Skipped guarded tests are unavailable evidence, never success.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_queue_drain.py tests/test_queue_durability.py tests/test_fake_repo_pairing.py -k "ingest or null_run or retry or terminal or final_attempt or fake"</automated>
  </verify>
  <acceptance_criteria>
    - RED fails on absent null-run settlement/reaper behavior.
    - Every transport outcome asserts zero payroll status writes.
    - Final-attempt expiry and stale-token fencing have distinct assertions.
    - Fake pairing includes a non-vacuous synthetic failure.
  </acceptance_criteria>
  <done>The transport settlement and fake surface has executable RED coverage before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Settle, reap, and fake-pair every null-run ingest outcome</name>
  <files>app/db/repo/job_settlement.py, tests/conftest.py, tests/test_queue_drain.py, tests/test_queue_durability.py, tests/test_fake_repo_pairing.py</files>
  <read_first>
    - tests/test_queue_drain.py
    - tests/test_queue_durability.py
    - tests/test_fake_repo_pairing.py
    - app/db/repo/job_settlement.py
    - app/models/job.py
    - tests/conftest.py
  </read_first>
  <behavior>
    - OK marks only the ingest job done; retryable reschedules or dead-letters only the ingest job; terminal settles transport without payroll mutation.
    - An expired final-attempt ingest lease with null run_id becomes dead and clears its lease instead of remaining fenced forever.
    - Existing run-associated jobs preserve the complete Phase 18 policy.
  </behavior>
  <action>Extend `settle_pipeline_job`, `settle_infrastructure_failure`, and `reap_expired_final_attempt` with the kind-aware transport-only branch required by D-21/D-22 and research. Branch on INGEST before any run requirement, reuse the existing locked-row lease fence/backoff/attempt cap, and never call a payroll status writer for ingest. Preserve every existing run-associated settlement matrix unchanged. Complete fake queue/facade support and patch inventories so every new queue/repository method resolves to the active fake. Make guarded Postgres tests reset-authority aware and report absence honestly.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_queue_drain.py tests/test_queue_durability.py tests/test_fake_repo_pairing.py</automated>
  </verify>
  <acceptance_criteria>
    - Null-run ingest paths settle without payroll writes and retain fencing.
    - Final-attempt expiry cannot strand a null-run ingest lease.
    - Existing run-associated jobs keep Phase 18 behavior.
    - Every new facade method is fake-paired and proven non-vacuously.
  </acceptance_criteria>
  <done>INGEST is safe to expose because settlement, reaping, and test doubles agree exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest result to queue settlement | A null-run transport outcome must settle without becoming a payroll outcome. |
| expired lease to reaper | A crashed final-attempt worker must be fenced and made terminal without a run row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-16 | Elevation of Privilege | transport settlement | high | mitigate | Tasks 1/2 branch on INGEST before run requirements and use fail-if-called payroll writers. |
| T19-17 | Denial of Service | final-attempt null-run lease | high | mitigate | Tasks 1/2 require fenced dead-letter and lease clearing instead of perpetual fencing. |
| T19-18 | Tampering | stale worker settlement | high | mitigate | Tasks 1/2 preserve lease-token and attempt fences for every ingest outcome. |
| T19-19 | Information Disclosure | fake/diagnostic boundaries | medium | mitigate | Tasks 1/2 retain bounded diagnostic codes and keep envelope/body/sender data out of settlement. |
</threat_model>

<verification>
- Task-focused settlement, reaper, and fake suites pass after each commit.
- `uv run ruff check app/db/repo/job_settlement.py tests/conftest.py tests/test_queue_drain.py tests/test_queue_durability.py tests/test_fake_repo_pairing.py` passes.
- `uv run mypy app/db/repo/job_settlement.py` passes.
- Guarded Postgres settlement cases are reported honestly.
- `git diff --check` passes.
</verification>

<success_criteria>
- T19-04 has a direct automated owner before the webhook producer cutover.
- No null-run transport outcome can mutate payroll or remain fenced forever.
- Fake parity is exact and non-vacuous.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-05-SUMMARY.md` when done.
</output>
