---
phase: 19-webhook-cutover-durable-ingest
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - app/db/repo/operator_resume_resolutions.py
  - tests/test_needs_operator.py
autonomous: true
requirements: [QUEUE-04]

must_haves:
  truths:
    - "The first valid complete operator generation committed under the run lock becomes the sole payroll authority."
    - "Every later valid generation is retained as superseded audit history and can be enqueued without affecting payroll."
    - "Only the winner's remember intent can become alias-candidate state."
  artifacts:
    - path: "app/db/repo/operator_resume_resolutions.py"
      provides: "commit-serialized generation submission, authority reads, and winner preparation"
    - path: "tests/test_needs_operator.py"
      provides: "sequential and real-thread counterexamples for worker-race authority"
  key_links:
    - from: "app/db/repo/operator_resume_resolutions.py"
      to: "payroll_runs"
      via: "SELECT FOR UPDATE before generation insertion and winner selection"
      pattern: "FOR UPDATE"
    - from: "app/db/repo/operator_resume_resolutions.py"
      to: "operator_resume_overrides.remember"
      via: "winner-only preparation of alias candidates"
      pattern: "remember"
---

<objective>
Make operator-resolution authority deterministic at commit time, before queue scheduling can influence payroll.

Purpose: Preserve the code-gated money-moving contract under competing valid operator submissions.
Output: Repository primitives and tests for first-commit authority, immutable supersession, and winner-only alias learning.
</objective>

<execution_context>
@/Users/pnhek/.codex/gsd-core/workflows/execute-plan.md
@/Users/pnhek/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
@.planning/phases/19-webhook-cutover-durable-ingest/19-RESEARCH.md
@.planning/phases/18-failure-policy-sweep-deletion/18-02-SUMMARY.md
@.planning/phases/18-failure-policy-sweep-deletion/18-03-SUMMARY.md
@app/db/repo/operator_resume_resolutions.py
</context>

## Artifacts this phase produces

- `OperatorResolutionSubmission` bounded result containing `resolution_id`, `authoritative`, and `winner_id` only.
- `commit_operator_resume_resolution(...)` for run-locked complete generation persistence and winner/superseded classification.
- `prepare_authoritative_operator_resume(...)` for winner validation and winner-only remember projection; superseded generations return an intentional no-op result.
- Generation-specific dedup convention `operator_resume:{run_id}:{resolution_id}` consumed by Plan 19-08.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove commit order rather than worker order selects authority</name>
  <files>tests/test_needs_operator.py</files>
  <read_first>
    - app/db/repo/operator_resume_resolutions.py
    - app/routes/runs.py
    - app/queue/handlers/operator_resume.py
    - tests/test_needs_operator.py
    - .planning/phases/19-webhook-cutover-durable-ingest/19-CONTEXT.md
  </read_first>
  <behavior>
    - Two complete valid generations for one NEEDS_OPERATOR run yield exactly one first-commit authority even when the losing worker executes first.
    - The loser remains loadable, is marked superseded by the winner, and its preparation is a bounded successful no-op.
    - Mixed remember choices persist per override, and only the winner can project remembered aliases.
  </behavior>
  <action>Create Wave 0 RED cases for D-11 through D-13. Include a guarded real-Postgres test using two genuine OS threads and a barrier, with the target run row lock determining commit order; do not substitute sequential HTTP calls for the concurrency proof. Also add hermetic repository-shape tests for complete mapping validation, generation-specific dedup, immutable loser history, winner-only remember projection, and no PII in bounded results. Skipped live-DB cases must report unavailable evidence, never pass-by-skip.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_needs_operator.py -k "generation or authority or superseded or remember"</automated>
  </verify>
  <acceptance_criteria>
    - RED fails because authority/supersession/remember repository behavior is absent.
    - The worker-first counterexample would fail an implementation that lets handler scheduling choose authority.
    - The test asserts one winner and preserved loser, not last-writer-wins.
  </acceptance_criteria>
  <done>Authority semantics are specified independently of the route and queue worker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement commit-serialized authority and winner preparation</name>
  <files>app/db/repo/operator_resume_resolutions.py, tests/test_needs_operator.py</files>
  <read_first>
    - tests/test_needs_operator.py
    - app/db/repo/operator_resume_resolutions.py
    - app/db/repo/_shared.py
    - app/db/repo/runs.py
    - app/db/schema.sql
    - app/models/contracts.py
  </read_first>
  <behavior>
    - Submission locks the run, revalidates NEEDS_OPERATOR/business/unresolved-name identity, inserts the complete immutable generation, and classifies authority before commit.
    - Exact replay of one resolution UUID is idempotent; conflicting reuse fails closed.
    - Preparation validates generation ownership, run roster membership, complete unresolved keys, and authority before any alias-candidate write.
  </behavior>
  <action>Implement D-11, D-12, and D-13 in the repository layer. Keep `SELECT ... FOR UPDATE` and all generation, override, authority, supersession, remember, and later enqueue-facing results in one caller-owned transaction. Return a bounded structured result with no submitted names/mappings. Preserve exact duplicate generation idempotency, reject conflicting or corrupt generations, and expose winner preparation that projects only `remember=true` aliases after proving authority and current run ownership. A superseded generation must return an intentional no-op without payroll status or alias mutation. Do not use `created_at`, UUID order, sequence allocation, or worker start order as commit-order proxies.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/gsd-phase19-uv-cache uv run --offline --no-sync pytest -q tests/test_needs_operator.py -k "generation or authority or superseded or remember or mapping"</automated>
  </verify>
  <acceptance_criteria>
    - One and only one authoritative generation is returned under contention.
    - Every valid loser stays immutable and explicitly references the winner.
    - Only authoritative preparation can write alias candidates.
    - All SQL is parameterized and every public repository result is PII-bounded.
  </acceptance_criteria>
  <done>Operator authority is determined durably before the queue can schedule either generation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator form to payroll authority | User-controlled mappings cross into money-moving state. |
| concurrent transactions to queue workers | Scheduling order must not become business authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T19-05 | Tampering | competing operator mappings | high | mitigate | Task 1/2 serialize on the run row, enforce one authority, validate complete run-roster ownership, and preserve losers as no-ops. |
| T19-06 | Elevation of Privilege | cross-business employee mapping | high | mitigate | Task 2 validates every employee against the run business roster before authority or alias projection. |
| T19-07 | Repudiation | losing generation audit | medium | mitigate | Task 2 retains immutable superseded generations and generation-specific queue history. |
| T19-08 | Information Disclosure | bounded submission result | medium | mitigate | Task 1/2 return only authority booleans/UUIDs and exclude names, mappings, and diagnostics. |
</threat_model>

<verification>
- Focused operator tests pass hermetically.
- Guarded real-thread Postgres cases either pass with reset authority or are reported skipped as unavailable.
- `uv run ruff check app/db/repo/operator_resume_resolutions.py tests/test_needs_operator.py` passes.
- `uv run mypy app/db/repo/operator_resume_resolutions.py` passes.
</verification>

<success_criteria>
- D-11 through D-13 are decided before worker execution.
- The repository exposes the exact route/handler interfaces Plan 19-08 needs.
- No operator mapping can cross a run/business boundary or learn an alias from a loser.
</success_criteria>

<output>
Create `.planning/phases/19-webhook-cutover-durable-ingest/19-02-SUMMARY.md` when done.
</output>
