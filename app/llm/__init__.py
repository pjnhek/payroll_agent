"""LLM client package — the one OpenAI-compatible wrapper (LLM-01/LLM-02).

Exposes the per-tier structured-call surface every judgment stage imports:
    from app.llm.client import call_structured, call_text, Tier
"""
