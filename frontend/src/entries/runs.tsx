// Placeholder entry so the Vite build has a real input to compile and the
// manifest shape can be recorded from an actual build (plan 22-03, Task 2).
// Plan 22-04 replaces this with the real mounting entry that reads
// __INITIAL_DATA__ and renders <RunsPage>.
export const RUNS_ENTRY_PLACEHOLDER = "runs";
