name: eval

on:
  push:
    branches: ["master"]
  workflow_dispatch:
    inputs:
      live_record:
        description: "Re-record extraction cache (requires EXTRACTION_API_KEY secret)"
        type: boolean
        default: false
        required: false

jobs:
  check:
    name: "Eval regression gate (hermetic)"
    runs-on: ubuntu-latest
    # Push and all workflow_dispatch runs get the hermetic check job
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up uv + Python 3.12
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - name: Install deps (all groups)
        run: uv sync
      - name: Run eval regression gate
        run: uv run python eval/run_eval.py --check
        env:
          DATABASE_URL: "placeholder"
          # NOTE: ALLOW_LIVE_LLM intentionally NOT set -- eval gate stays hermetic on push.
          # Green CI = deterministic scoring didn't regress against committed summary.json.
          # Green CI does NOT mean extraction is good (no live LLM was called).

  record:
    name: "Live re-record (workflow_dispatch only)"
    if: github.event_name == 'workflow_dispatch' && inputs.live_record
    runs-on: ubuntu-latest
    # Least-privilege: the only write this job performs is committing the
    # re-recorded extraction caches back to the branch (CR-01).
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up uv + Python 3.12
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.12"
      - name: Install deps (all groups)
        run: uv sync
      - name: Re-record extraction cache
        run: uv run python eval/run_eval.py --record
        env:
          DATABASE_URL: "placeholder"
          ALLOW_LIVE_LLM: "true"
          EXTRACTION_API_KEY: ${{ secrets.EXTRACTION_API_KEY }}
      - name: Commit & push re-recorded caches
        # Without this step the live re-record runs on the ephemeral runner and
        # is discarded -- burning API credits with no durable change (CR-01).
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore(eval): re-record extraction caches via workflow_dispatch"
          file_pattern: "eval/fixtures/*_extraction.json eval/summary.json"
