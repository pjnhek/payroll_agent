name: ci

# Least-privilege token: lint/test only ever need to read the repo contents.
permissions:
  contents: read

# Pre-merge gate. `pull_request` is what makes it one: without it a fork PR would run the
# real-Postgres proofs (concurrency-proof.yml triggers on pull_request) while lint, tests,
# and mypy never ran at all — the worst of both worlds.
#
# ACCEPTED TRADEOFF: push is scoped to master rather than left unrestricted, so a same-repo
# PR runs the suite once (for the PR) instead of twice (branch push + PR). The cost is that
# a feature-branch push with no PR open gets no CI. That is deliberate — every path that can
# reach master is still gated (direct master push, or PR), and this repo's actual workflow is
# committing to master. If branch-before-PR feedback is ever wanted, drop the `branches:`
# filter and accept the duplicate runs.
on:
  pull_request:
  push:
    branches: ["master"]
  workflow_dispatch:

# lint/test runs are safely cancellable (unlike deploy-migrate's mid-flight DDL,
# which must finish once started) -- cancel superseded runs on the same branch.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    name: "Lint (ruff check)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1 (v4)

      - name: Set up uv + Python 3.12
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86  # v5.4.2 (v5)
        with:
          python-version: "3.12"

      - name: Install deps (all groups)
        # --locked asserts uv.lock matches pyproject.toml instead of silently
        # re-resolving -- a stale lockfile fails the job rather than merging green.
        run: uv sync --locked

      - name: Run ruff check
        run: uv run ruff check .

      # GUARD-01: the generated markdown view is a derived artifact of
      # tests/assertion_inventory.py; --check regenerates it in memory and
      # diffs against the committed file so a reviewer's markdown can never
      # silently drift from what the completeness guard enforces.
      - name: Assertion inventory view is current
        run: uv run python scripts/render_assertion_inventory.py --check

      # GUARD-01: the assertion inventory is a *gate*, not a task -- the
      # route-attribution and presence/absence classification pass can only
      # be performed honestly while the Jinja pages still render. Once a
      # frontend/src file lands, a converted page makes a vacuous absence
      # assertion indistinguishable from a valid one, so no conversion source
      # may exist before the registry that classified it does.
      - name: Assertion inventory precedes conversion (GUARD-01 gate)
        run: |
          if [ -d frontend/src ] && [ -n "$(find frontend/src -type f 2>/dev/null)" ] \
             && [ ! -f tests/assertion_inventory.py ]; then
            echo "GUARD-01 violation: frontend/src contains files but" \
                 "tests/assertion_inventory.py does not exist." >&2
            echo "The assertion inventory must be committed BEFORE any page" \
                 "conversion -- after conversion, a vacuous absence assertion" \
                 "is indistinguishable from a valid one, and no later phase" \
                 "can honestly redo the route-attribution + classification pass." >&2
            exit 1
          fi
          echo "GUARD-01 gate OK: no frontend/src file precedes the assertion inventory."

  test:
    name: "Test suite (hermetic)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1 (v4)

      - name: Set up uv + Python 3.12
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86  # v5.4.2 (v5)
        with:
          python-version: "3.12"

      - name: Install deps (all groups)
        # --locked asserts uv.lock matches pyproject.toml instead of silently
        # re-resolving -- a stale lockfile fails the job rather than merging green.
        run: uv sync --locked

      # tests/test_pdf.py shells out to `pdftotext` (poppler-utils) to assert real
      # extracted text from a generated paystub. Present on macOS dev machines but not
      # on ubuntu-latest -- a runner-provided OS package, not an application dependency,
      # so it is installed here rather than via uv/pyproject.toml.
      - name: Install poppler-utils (pdftotext, for tests/test_pdf.py)
        run: sudo apt-get update -qq && sudo apt-get install -y -qq poppler-utils

      # No DATABASE_URL / ALLOW_DB_RESET / ALLOW_LIVE_LLM set -- tests/conftest.py's
      # two-factor guards skip DB/live-LLM tests hermetically without either var.
      - name: Run test suite
        run: uv run pytest -q

  typecheck:
    name: "Type check (mypy --strict)"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1 (v4)

      - name: Set up uv + Python 3.12
        uses: astral-sh/setup-uv@d4b2f3b6ecc6e67c4457f6d3e41ec42d3d0fcb86  # v5.4.2 (v5)
        with:
          python-version: "3.12"

      - name: Install deps (all groups)
        run: uv sync --locked

      - name: Run mypy
        run: uv run mypy

  frontend:
    name: "Frontend (typecheck, lint, test, build)"
    runs-on: ubuntu-latest
    # No job-level concurrency group: this job inherits the workflow-level
    # `ci-${{ github.ref }}` group above, so two pull requests on different refs
    # each get their own run and never cancel each other's. Declaring a
    # job-level group here would scope cancellation to this job alone, which
    # is not what a pre-merge gate wants.
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1 (v4)

      - name: Set up Node 24
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4.4.0 (v4)
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      # npm ci (never npm install) fails on any lockfile/package.json mismatch
      # rather than silently re-resolving -- a stale lockfile must fail the
      # job, not merge green, matching the Python builder's `uv sync --locked`.
      - name: Install deps (npm ci, lockfile-asserting)
        run: cd frontend && npm ci

      - name: Typecheck
        run: cd frontend && npm run typecheck

      - name: Lint
        run: cd frontend && npm run lint

      - name: Test
        run: cd frontend && npm run test

      - name: Build
        run: cd frontend && npm run build

  docker-build:
    name: "Docker image build (proves the deployed-asset parity claim)"
    runs-on: ubuntu-latest
    # No job-level concurrency group, and no `needs:` on the frontend job above --
    # this job and `frontend` are independent and must fail independently.
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4.3.1 (v4)

      # This job exists to catch one specific gap: app/static/dist is gitignored
      # and dockerignored on purpose, so a developer's local `docker build` can
      # succeed only because their own working tree happens to have a
      # previously built bundle sitting on disk, while a build from a clean
      # clone -- which is exactly what this checkout is -- has no bundle at
      # all and must build it in-image. Building from the checked-out clone,
      # not a developer's working tree, is what makes this proof real; the
      # runtime stage's own build-time manifest assertion is what turns a
      # missing bundle into a build failure instead of a blank console.
      # No stage target is passed, and no registry credentials are configured --
      # this job only proves the image builds, it never pushes anywhere, so the
      # workflow's `permissions: contents: read` stays sufficient.
      - name: Build image from the checked-out clone
        run: docker build -t payroll-agent-ci .
